(function(){'use strict';const themeToggle=document.getElementById('theme-toggle');const html=document.documentElement;function setTheme(theme){html.setAttribute('data-theme',theme);localStorage.setItem('rtkg-theme',theme);}
const savedTheme=localStorage.getItem('rtkg-theme');if(savedTheme==='dark'||savedTheme==='light'){setTheme(savedTheme);}else if(window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches){setTheme('dark');}
if(themeToggle){themeToggle.addEventListener('click',()=>{const current=html.getAttribute('data-theme')||'light';setTheme(current==='dark'?'light':'dark');});}
const langSwitcher=document.getElementById('language-switcher');if(langSwitcher){const currentLang=document.body.getAttribute('data-lang')||'zh';langSwitcher.value=currentLang;langSwitcher.addEventListener('change',()=>{const target=langSwitcher.value;if(target===currentLang)return;localStorage.setItem('rtkg-lang',target);const path=window.location.pathname;const clean=path.replace(/^\/(en|ko)(?=\/)/,'')||'/';const newPath=target==='zh'?clean:'/'+target+clean;window.location.href=newPath+window.location.search+window.location.hash;});}
const mobileToggle=document.getElementById('mobile-menu-toggle');if(mobileToggle){mobileToggle.addEventListener('click',(e)=>{e.stopPropagation();document.body.classList.toggle('mobile-menu-open');});}
document.querySelectorAll('.site-nav a').forEach(link=>{link.addEventListener('click',()=>document.body.classList.remove('mobile-menu-open'));});document.addEventListener('click',(e)=>{if(document.body.classList.contains('mobile-menu-open')){const header=document.querySelector('.site-header');if(header&&!header.contains(e.target)){document.body.classList.remove('mobile-menu-open');}}});const tocLinks=Array.from(document.querySelectorAll('.page-toc .toc-nav a[href^="#"]'));if(tocLinks.length){const headingMap=new Map();tocLinks.forEach(a=>{let id=a.getAttribute('href').slice(1);try{id=decodeURIComponent(id);}catch(e){}
const el=document.getElementById(id);if(el)headingMap.set(el,a);});if(headingMap.size){const headings=Array.from(headingMap.keys());const onScroll=()=>{let current=null;for(const el of headings){if(el.getBoundingClientRect().top<=110)current=el;else break;}
tocLinks.forEach(a=>a.classList.remove('active'));if(current)headingMap.get(current).classList.add('active');};window.addEventListener('scroll',onScroll,{passive:true});onScroll();}}})();