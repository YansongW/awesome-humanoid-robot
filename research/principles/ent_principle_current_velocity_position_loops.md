---
$id: ent_principle_current_velocity_position_loops
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: principle
names:
  en: Current / Velocity / Position Control Loops
  zh: 电流环/速度环/位置环
  ko: 전류·속도·위치 제어 루프
summary:
  en: The nested feedback-control hierarchy for servo drives, where an inner current loop regulates torque, a velocity loop
    regulates speed, and an outer position loop regulates joint angle.
  zh: 4.5.5 电流环、速度环、位置环的级联控制 实际关节驱动通常采用三级级联控制：最内层 电流环（力矩环），中间 速度环，最外层 位置环。每一层带宽约为下一层的 5-10 倍，以保证稳定性。
  ko: '서보 드라이브의 중첩 피드백 제어 계층: 내측 전류 루프는 토크, 속도 루프는 속도, 외측 위치 루프는 관절 각도를 제어.'
domains:
- 02_components
layers:
- midstream
functional_roles:
- knowledge
tags:
- principle
- chapter_4
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body backfilled from chapter-04.md#4.5.5 电流环、速度环、位置环的级联控制 by scripts/backfill_nonpaper_entries.py. Body backfilled
    from chapter-04.md section ''4.5.5 电流环、速度环、位置环的级联控制'' by scripts/backfill_critical_entities.py.  [2026-08-12] body upgraded
    to textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters
    + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

电流环、速度环、位置环是伺服驱动系统的三级级联反馈控制结构：最内层电流环（力矩环）以毫秒级响应直接控制电机转矩，中间速度环调节转速，最外层位置环跟踪关节角度。每一层带宽约为下一层的 5-10 倍，以保证级联系统的稳定性。它真正改变的不是电机本身，而是让电机从"能转"到"可控"的质变。

## 核心内容

### 是什么：准确定义

电流环、速度环、位置环是伺服驱动系统中按带宽从高到低排列的三级级联反馈控制结构。实际关节驱动通常采用三级级联控制：最内层 **电流环**（力矩环），中间 **速度环**，最外层 **位置环**。每一层带宽约为下一层的 5-10 倍，以保证稳定性。

- **电流环（current loop）**：以电机相电流为被控量的最快内环，直接决定输出力矩。
- **速度环（velocity loop）**：以电机转速为被控量，输出电流指令。
- **位置环（position loop）**：以关节角度为被控量，输出速度指令。

三者构成一个嵌套结构：外环的输出是内环的指令，内环的执行速度远快于外环的指令更新速度，从而保证每一层都能"看到"一个相对稳定的被控对象。

### 为什么存在：痛点与历史定位

**① 单环控制无法同时满足快速性与精确性。** 如果只用位置环直接控制电机，位置误差信号需要经过整个机械传动链（减速器、连杆、负载惯量）才能转化为力矩，而机械系统的惯量、摩擦和弹性会引入大相位滞后，导致系统要么响应迟钝、要么振荡发散。级联结构的本质是**把控制问题分层**：内环先解决"力矩准不准"的问题，中环解决"转速稳不稳"的问题，外环才处理"位置跟不跟"的问题。

**② 电流环是力控的物理基础。** 永磁同步电机的输出转矩与 q 轴电流成正比，因此电流环本质上就是力矩环。没有电流环，就无法实现精确的力矩控制，也就无法支撑阻抗控制、导纳控制等需要力交互的先进算法。电流环带宽直接决定了关节能渲染的阻抗范围（Z-width），这是人形机器人安全交互的硬约束。

**③ 历史定位：从模拟到数字的演进。** 早期伺服系统用模拟电路实现电流环，带宽高但参数漂移严重；数字控制普及后，三环结构在 DSP/MCU 中以固定采样率运行，带宽受限于采样率和计算延迟。现代伺服驱动器普遍采用 FOC（磁场定向控制）实现电流环，配合 PI 控制器和前馈补偿，使三环级联成为工业与机器人领域的标准架构。

### 原理拆解

**① 级联带宽原则：为什么内环必须快 5-10 倍**

级联控制稳定性的核心约束是**带宽分离**：每一层带宽约为下一层的 5-10 倍。设内环闭环传递函数为 $G_i(s)$，外环控制器为 $C_o(s)$，则外环设计时可将内环近似为一阶惯性环节：

$$
G_i(s) \approx \frac{1}{\tau_i s + 1}, \quad \tau_i = \frac{1}{2\pi f_i}
$$

其中 $f_i$ 为内环带宽。当外环带宽 $f_o \ll f_i$ 时，内环动态可忽略，外环看到的是一个"几乎理想的力矩源"。若带宽比不足，内环的相位滞后会进入外环的增益交越频率附近，导致系统失稳。5-10 倍的经验值正是工程上保证稳定裕度的最小安全距离。

**② 三环的典型带宽范围**

| 控制环 | 典型带宽 | 被控量 | 输出 |
|--------|---------|--------|------|
| 电流环 | 1-5 kHz | 电机相电流 | PWM 占空比 |
| 速度环 | 50-500 Hz | 电机转速 | 电流指令 |
| 位置环 | 5-100 Hz | 关节角度 | 速度指令 |

电流环带宽通常可达 1-5 kHz；速度环 50-500 Hz；位置环 5-100 Hz，具体取决于负载惯量、刚度和采样率。带宽从上到下逐层递减约一个数量级，形成"内快外慢"的金字塔结构。

**③ PI 控制器与前馈的配合**

每一环的控制器通常采用 PI 结构：比例项提供即时纠正，积分项消除稳态误差。但纯反馈 PI 在跟踪快速变化的轨迹时存在固有滞后，因此工程上普遍加入**前馈**——根据期望轨迹提前计算并加入控制量。以位置环为例：

$$
u_{pos}(t) = K_{p,p} e_p(t) + K_{i,p} \int_0^t e_p(s)\, ds + K_{ff} \dot{\theta}_{des}(t)
$$

其中第三项是速度前馈，它不等待误差产生就提前驱动电机，显著提高跟踪性能。前馈与反馈的比例是工程整定的核心权衡：前馈过强会放大模型误差，过弱则退化为纯反馈。

**④ 抗饱和：被忽视的稳定性杀手**

当电机达到电流极限或电压饱和时，积分器会持续累积误差，导致退出饱和后出现大超调甚至振荡。**抗饱和（anti-windup）** 机制在积分项达到限幅时冻结或回退积分累加，防止积分器在饱和时过度累积，避免退出饱和后的大超调。这是教科书 PID 与真机可用 PID 之间最关键的区别之一。

### 关键参数与规格

| 参数 | 典型范围 | 影响因素 |
|------|---------|---------|
| 电流环带宽 | 1-5 kHz | 逆变器开关频率、电流采样率、电机电感 |
| 速度环带宽 | 50-500 Hz | 编码器分辨率、速度滤波截止频率、负载惯量 |
| 位置环带宽 | 5-100 Hz | 机械刚度、减速比、轨迹规划频率 |
| 带宽级联比 | 5-10 倍 | 稳定裕度要求、相位裕度 |
| 采样率 | 电流环 10-20 kHz；速度/位置环 1-2 kHz | DSP/MCU 算力、ADC 转换时间 |

电流环带宽通常可达 1-5 kHz；速度环 50-500 Hz；位置环 5-100 Hz，具体取决于负载惯量、刚度和采样率。带宽级联比 5-10 倍是稳定性下限，工程上常取 8-10 倍以留足裕度（工程判断）。

### 横向对比

**① 与单环 PID 对比**

单环 PID 直接对位置误差输出力矩指令，结构简单但动态性能差：无法区分"位置误差大但速度为零"与"位置误差大且正在快速逼近"两种状态。三环级联通过速度环的中间缓冲，使位置环只需面对"速度指令能否被跟踪"的问题，大幅降低整定难度。

**② 与直接力矩控制（DTC）对比**

DTC 直接控制磁链和转矩，无需电流环的 PI 调节，动态响应更快，但转矩脉动大、低速性能差。三环级联的电流环基于 FOC，转矩平滑性好，更适合需要精确力矩控制的关节驱动。

**③ 与模型预测控制（MPC）对比**

MPC 在单个优化问题中同时处理位置、速度、力矩约束，理论上可替代三环级联。但 MPC 的计算量随预测时域和约束数量急剧增长，在 1 kHz 以上的关节控制周期内难以实时运行。三环级联以固定结构、固定采样率的 PI 控制，在算力受限的嵌入式平台上仍是唯一务实选择。

### 谁在用·应用案例

**① 人形机器人关节模组**

几乎所有商用和科研人形机器人的关节驱动都采用三环级联结构。以宇树科技（Unitree Robotics）为例，其自研的 M107 永磁同步电机配合定制驱动器，内部即运行电流环、速度环、位置环的级联控制，支撑 H1、G1 等机型的动态行走与跳跃。

**② 阻抗控制的前置条件**

Focchi 等人（2016）的研究明确指出，阻抗控制中内环力矩带宽、速度滤波及采样频率对系统稳定性有决定性影响。内环带宽不足会缩小稳定刚度与阻尼的可行范围（Z-width），速度滤波会引入相位滞后，进一步压缩稳定区域。三环级联的带宽设计直接决定了阻抗控制能渲染的物理交互特性。

**③ 工业伺服与协作机器人**

汇川技术、Infineon、TI 等厂商的伺服驱动器产品均以三环级联为内核，配合 EtherCAT 等实时总线实现多轴同步。协作机器人的力矩传感器通常安装在电流环之外，通过电流环的力矩估算与外部传感器融合，实现碰撞检测与力限制。

### 局限与边界

**① 带宽受物理约束，无法无限提高。** 电流环带宽受限于逆变器开关频率（通常 10-20 kHz）和电流采样率，速度环受限于编码器分辨率和速度滤波的相位滞后，位置环受限于机械谐振频率。盲目提高某一环的增益会激发机械谐振或噪声放大。

**② 级联结构对参数变化敏感。** 负载惯量变化会改变速度环和位置环的等效被控对象，导致带宽和稳定裕度漂移。人形机器人在行走过程中负载惯量随姿态大幅变化，固定参数的 PI 控制器难以在全工况保持最优性能，需要增益调度或自适应控制（工程判断）。

**③ 无法直接处理力矩约束。** 位置环和速度环的输出是速度/电流指令，力矩约束只能通过电流环的限幅间接实现。当关节需要精确的力矩控制（如柔顺操作）时，必须切换到力矩模式或采用阻抗控制，三环级联本身不提供力矩闭环。

### 常见误区

1. **"带宽越高越好"**——错。Focchi 等人的研究表明，最大化内环带宽并不适用于需要渲染特定阻抗参数范围的应用场景，因为这会显著影响稳定刚度与阻尼的可行范围。带宽设计必须与应用需求匹配，而非一味追求高带宽。

2. **"三环级联就是三个独立的 PID"**——错。三环之间存在强耦合：内环的相位滞后会进入外环的稳定分析，外环的指令变化会激励内环的瞬态响应。正确做法是将级联系统作为一个整体进行带宽分配和稳定裕度验证。

3. **"前馈可以替代反馈"**——错。前馈依赖精确的模型（惯量、摩擦、反电动势常数），模型误差会直接转化为跟踪误差。前馈的作用是"减少反馈的负担"，而非"取代反馈"。

4. **"抗饱和只是锦上添花"**——错。在关节高速运动或碰撞场景中，电流指令极易达到限幅，没有抗饱和的积分器会累积大量误差，导致退出饱和后的大超调甚至振荡。抗饱和是保证系统在极限工况下稳定性的必要机制。

### 相关知识

- `ent_method_pid_control` — PID 控制是三环级联中每一环的基础控制器，其工程化细节（抗饱和、微分滤波、前馈）直接决定三环的实际性能。
- `ent_component_servo_drive_2024` — 伺服驱动器是承载三环级联控制的硬件平台，负责将控制指令转换为电机功率输出。
- `ent_method_field_oriented_control` — FOC 是电流环实现的核心算法，通过坐标变换将三相交流电流解耦为转矩分量和磁通分量。
- `ent_paper_focchi_robot_impedance_control_and_pa_2016` — 该论文系统分析了内环带宽、速度滤波和采样率对阻抗控制稳定性的影响，是三环带宽设计的重要理论依据。
- `ent_oem_unitree_robotics` — 宇树科技在人形机器人关节中实际部署三环级联控制，是该方法在量产产品中的典型应用案例。
- `ent_method_whole_body_control` — 全身控制将三环级联视为底层执行层，在其上叠加动力学优化和任务空间控制。

## 参考

- [项目 Wiki 第 4 章：4.5.5 电流环、速度环、位置环的级联控制](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-04.md)
- [项目 Wiki 第 2 章：实体类型与理论深度](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)

## Overview

The current loop, velocity loop, and position loop form a three-level cascaded feedback control structure in servo drive systems: the innermost current loop (torque loop) directly controls motor torque with millisecond-level response, the middle velocity loop regulates rotational speed, and the outermost position loop tracks joint angles. Each layer's bandwidth is approximately 5-10 times that of the next layer to ensure the stability of the cascaded system. What this truly changes is not the motor itself, but a qualitative leap from "being able to spin" to "being controllable."

## Content

### What It Is: Precise Definition

The current loop, velocity loop, and position loop are a three-level cascaded feedback control structure in servo drive systems, arranged from highest to lowest bandwidth. Practical joint drives typically employ three-level cascaded control: the innermost **current loop** (torque loop), the middle **velocity loop**, and the outermost **position loop**. Each layer's bandwidth is approximately 5-10 times that of the next layer to ensure stability.

- **Current loop**: The fastest inner loop controlling motor phase current as the controlled variable, directly determining output torque.
- **Velocity loop**: Controls motor rotational speed as the controlled variable, outputting current commands.
- **Position loop**: Controls joint angle as the controlled variable, outputting velocity commands.

These three form a nested structure: the output of the outer loop serves as the command for the inner loop, and the execution speed of the inner loop is far faster than the command update rate of the outer loop, ensuring each layer "sees" a relatively stable controlled plant.

### Why It Exists: Pain Points and Historical Context

**① Single-loop control cannot simultaneously satisfy rapidity and precision.** If only a position loop directly controls the motor, the position error signal must pass through the entire mechanical transmission chain (reducer, links, load inertia) to be converted into torque. The inertia, friction, and elasticity of the mechanical system introduce significant phase lag, causing the system to either respond sluggishly or oscillate and diverge. The essence of the cascaded structure is **layering the control problem**: the inner loop first solves "is the torque accurate," the middle loop solves "is the speed stable," and only then does the outer loop handle "does the position track."

**② The current loop is the physical foundation of force control.** The output torque of a permanent magnet synchronous motor is proportional to the q-axis current, so the current loop is essentially the torque loop. Without the current loop, precise torque control is impossible, and advanced algorithms requiring force interaction such as impedance control and admittance control cannot be supported. The current loop bandwidth directly determines the range of impedance (Z-width) that a joint can render, which is a hard constraint for safe interaction in humanoid robots.

**③ Historical context: evolution from analog to digital.** Early servo systems implemented the current loop with analog circuits, achieving high bandwidth but suffering from severe parameter drift. After digital control became widespread, the three-loop structure runs at fixed sampling rates in DSP/MCU, with bandwidth limited by sampling rate and computational latency. Modern servo drives commonly use FOC (Field-Oriented Control) for the current loop, combined with PI controllers and feedforward compensation, making three-loop cascading the standard architecture in industry and robotics.

### Principle Breakdown

**① Cascaded bandwidth principle: why the inner loop must be 5-10 times faster**

The core constraint for cascaded control stability is **bandwidth separation**: each layer's bandwidth is approximately 5-10 times that of the next layer. Let the inner loop closed-loop transfer function be $G_i(s)$ and the outer loop controller be $C_o(s)$. During outer loop design, the inner loop can be approximated as a first-order inertial element:

$$
G_i(s) \approx \frac{1}{\tau_i s + 1}, \quad \tau_i = \frac{1}{2\pi f_i}
$$

where $f_i$ is the inner loop bandwidth. When the outer loop bandwidth $f_o \ll f_i$, the inner loop dynamics can be neglected, and the outer loop sees an "almost ideal torque source." If the bandwidth ratio is insufficient, the inner loop's phase lag enters the vicinity of the outer loop's gain crossover frequency, causing instability. The empirical 5-10 times ratio is precisely the minimum safe distance that guarantees stability margins in engineering practice.

**② Typical bandwidth ranges for the three loops**

| Control Loop | Typical Bandwidth | Controlled Variable | Output |
|--------------|-------------------|---------------------|--------|
| Current loop | 1-5 kHz | Motor phase current | PWM duty cycle |
| Velocity loop | 50-500 Hz | Motor rotational speed | Current command |
| Position loop | 5-100 Hz | Joint angle | Velocity command |

The current loop bandwidth typically reaches 1-5 kHz; the velocity loop 50-500 Hz; the position loop 5-100 Hz, depending on load inertia, stiffness, and sampling rate. Bandwidth decreases by approximately one order of magnitude from top to bottom, forming a pyramid structure of "fast inside, slow outside."

**③ Coordination of PI controllers and feedforward**

The controller for each loop typically adopts a PI structure: the proportional term provides immediate correction, and the integral term eliminates steady-state error. However, pure feedback PI has inherent lag when tracking rapidly changing trajectories, so **feedforward** is commonly added in engineering—precomputing and injecting control effort based on the desired trajectory. Taking the position loop as an example:

$$
u_{pos}(t) = K_{p,p} e_p(t) + K_{i,p} \int_0^t e_p(s)\, ds + K_{ff} \dot{\theta}_{des}(t)
$$

where the third term is velocity feedforward, which drives the motor in advance without waiting for error to develop, significantly improving tracking performance. The ratio of feedforward to feedback is the core trade-off in engineering tuning: excessive feedforward amplifies model errors, while insufficient feedforward degenerates into pure feedback.

**④ Anti-windup: the overlooked stability killer**

When the motor reaches current limits or voltage saturation, the integrator continuously accumulates error, leading to large overshoot or even oscillation after exiting saturation. The **anti-windup** mechanism freezes or rolls back the integral accumulation when the integral term reaches its limit, preventing excessive accumulation during saturation and avoiding large overshoot after desaturation. This is one of the most critical differences between textbook PID and production-ready PID.

### Key Parameters and Specifications

| Parameter | Typical Range | Influencing Factors |
|-----------|---------------|---------------------|
| Current loop bandwidth | 1-5 kHz | Inverter switching frequency, current sampling rate, motor inductance |
| Velocity loop bandwidth | 50-500 Hz | Encoder resolution, velocity filter cutoff frequency, load inertia |
| Position loop bandwidth | 5-100 Hz | Mechanical stiffness, reduction ratio, trajectory planning frequency |
| Bandwidth cascade ratio | 5-10 times | Stability margin requirements, phase margin |
| Sampling rate | Current loop 10-20 kHz; velocity/position loop 1-2 kHz | DSP/MCU computational power, ADC conversion time |

The current loop bandwidth typically reaches 1-5 kHz; the velocity loop 50-500 Hz; the position loop 5-100 Hz, depending on load inertia, stiffness, and sampling rate. The bandwidth cascade ratio of 5-10 times is the stability lower bound; engineering practice often uses 8-10 times to leave sufficient margin (engineering judgment).

### Horizontal Comparison

**① Comparison with single-loop PID**

Single-loop PID directly outputs torque commands based on position error, offering simple structure but poor dynamic performance: it cannot distinguish between "large position error but zero velocity" and "large position error with rapid approach." Three-level cascading, through the intermediate buffering of the velocity loop, allows the position loop to face only the question of "can the velocity command be tracked," greatly reducing tuning difficulty.

**② Comparison with Direct Torque Control (DTC)**

DTC directly controls flux linkage and torque without the PI regulation of the current loop, achieving faster dynamic response, but suffers from large torque ripple and poor low-speed performance. The current loop in three-level cascading is based on FOC, offering smooth torque output and better suitability for joint drives requiring precise torque control.

**③ Comparison with Model Predictive Control (MPC)**

MPC handles position, velocity, and torque constraints simultaneously within a single optimization problem and could theoretically replace three-level cascading. However, MPC's computational load grows rapidly with prediction horizon and the number of constraints, making real-time execution difficult at joint control cycles above 1 kHz. Three-level cascading, with its fixed structure and fixed sampling rate PI control, remains the only pragmatic choice on computationally constrained embedded platforms.

### Who Uses It: Application Cases

**① Humanoid robot joint modules**

Nearly all commercial and research humanoid robots employ three-level cascaded control for joint drives. Taking Unitree Robotics as an example, its self-developed M107 permanent magnet synchronous motor paired with a custom driver runs cascaded current, velocity, and position loop control internally, supporting dynamic walking and jumping in models such as H1 and G1.

**② Prerequisite for impedance control**

Research by Focchi et al. (2016) explicitly demonstrates that inner-loop torque bandwidth, velocity filtering, and sampling frequency have decisive impacts on impedance control stability. Insufficient inner-loop bandwidth shrinks the feasible range of stable stiffness and damping (Z-width), while velocity filtering introduces phase lag that further compresses the stable region. The bandwidth design of three-level cascading directly determines the physical interaction characteristics that impedance control can render.

**③ Industrial servos and collaborative robots**

Servo drive products from manufacturers such as Inovance Technology, Infineon, and TI all use three-level cascading as their core, paired with real-time buses like EtherCAT for multi-axis synchronization. Torque sensors in collaborative robots are typically installed outside the current loop, fusing current-loop torque estimation with external sensor data to achieve collision detection and force limiting.

### Limitations and Boundaries

**① Bandwidth is physically constrained and cannot be increased indefinitely.** Current loop bandwidth is limited by inverter switching frequency (typically 10-20 kHz) and current sampling rate; the velocity loop is limited by encoder resolution and the phase lag of velocity filtering; the position loop is limited by mechanical resonance frequency. Blindly increasing the gain of any loop will excite mechanical resonance or amplify noise.

**② The cascaded structure is sensitive to parameter variations.** Changes in load inertia alter the equivalent controlled plant for the velocity and position loops, causing bandwidth and stability margin drift. During humanoid robot walking, load inertia varies significantly with posture, making fixed-parameter PI controllers unable to maintain optimal performance across all operating conditions, requiring gain scheduling or adaptive control (engineering judgment).

**③ Torque constraints cannot be directly handled.** The outputs of the position and velocity loops are velocity/current commands; torque constraints can only be indirectly implemented through current loop limiting. When a joint requires precise torque control (e.g., compliant manipulation), it must switch to torque mode or adopt impedance control; three-level cascading itself does not provide torque closed-loop control.

### Common Misconceptions

1. **"Higher bandwidth is always better"**—Wrong. Research by Focchi et al. shows that maximizing inner-loop bandwidth is not suitable for applications requiring rendering of specific impedance parameter ranges, as it significantly affects the feasible range of stable stiffness and damping. Bandwidth design must match application requirements rather than blindly pursuing high bandwidth.

2. **"Three-level cascading is just three independent PIDs"**—Wrong. Strong coupling exists among the three loops: the inner loop's phase lag enters the outer loop's stability analysis, and outer loop command changes excite inner loop transient responses. The correct approach is to treat the cascaded system as a whole for bandwidth allocation and stability margin verification.

3. **"Feedforward can replace feedback"**—Wrong. Feedforward relies on accurate models (inertia, friction, back-EMF constant), and model errors directly translate into tracking errors. The role of feedforward is to "reduce the burden on feedback," not to "replace feedback."

4. **"Anti-windup is just a nice-to-have"**—Wrong. In scenarios of high-speed joint motion or collision, current commands easily reach limits. Without anti-windup, the integrator accumulates significant error, leading to large overshoot or even oscillation after desaturation. Anti-windup is a necessary mechanism for ensuring system stability under extreme operating conditions.

### Related Knowledge

- `ent_method_pid_control` — PID control is the foundational controller for each loop in three-level cascading, and its engineering details (anti-windup, derivative filtering, feedforward) directly determine the actual performance of the three loops.
- `ent_component_servo_drive_2024` — The servo drive is the hardware platform that hosts three-level cascaded control, responsible for converting control commands into motor power output.
- `ent_method_field_oriented_control` — FOC is the core algorithm for implementing the current loop, decoupling three-phase AC current into torque and flux components through coordinate transformation.
- `ent_paper_focchi_robot_impedance_control_and_pa_2016` — This paper systematically analyzes the impact of inner-loop bandwidth, velocity filtering, and sampling rate on impedance control stability, serving as an important theoretical basis for three-loop bandwidth design.
- `ent_oem_unitree_robotics` — Unitree Robotics deploys three-level cascaded control in humanoid robot joints, representing a typical application case of this method in mass-produced products.
- `ent_method_whole_body_control` — Whole-body control treats three-level cascading as the underlying execution layer, layering dynamics optimization and task-space control on top of it.

## 개요

전류 루프, 속도 루프, 위치 루프는 서보 구동 시스템의 3단 종속 피드백 제어 구조이다: 가장 안쪽의 전류 루프(토크 루프)는 밀리초 단위 응답으로 모터 토크를 직접 제어하고, 중간의 속도 루프는 회전 속도를 조절하며, 가장 바깥쪽의 위치 루프는 관절 각도를 추종한다. 각 계층의 대역폭은 다음 계층의 약 5-10배로, 종속 시스템의 안정성을 보장한다. 이것이 실제로 바꾸는 것은 모터 자체가 아니라, 모터를 "회전 가능"에서 "제어 가능"으로 만드는 질적 변화이다.

## 핵심 내용

### 무엇인가: 정확한 정의

전류 루프, 속도 루프, 위치 루프는 서보 구동 시스템에서 대역폭이 높은 순서에서 낮은 순서로 배열된 3단 종속 피드백 제어 구조이다. 실제 관절 구동은 일반적으로 3단 종속 제어를 사용한다: 가장 안쪽의 **전류 루프**(토크 루프), 중간의 **속도 루프**, 가장 바깥쪽의 **위치 루프**. 각 계층의 대역폭은 다음 계층의 약 5-10배로 안정성을 보장한다.

- **전류 루프(current loop)**: 모터 상전류를 제어 대상으로 하는 가장 빠른 내부 루프로, 출력 토크를 직접 결정한다.
- **속도 루프(velocity loop)**: 모터 회전 속도를 제어 대상으로 하며, 전류 지령을 출력한다.
- **위치 루프(position loop)**: 관절 각도를 제어 대상으로 하며, 속도 지령을 출력한다.

세 루프는 중첩 구조를 형성한다: 외부 루프의 출력은 내부 루프의 지령이 되고, 내부 루프의 실행 속도는 외부 루프의 지령 업데이트 속도보다 훨씬 빨라 각 계층이 상대적으로 안정적인 제어 대상을 "볼" 수 있게 한다.

### 왜 존재하는가:痛点과 역사적 위치

**① 단일 루프 제어는 신속성과 정밀성을 동시에 만족시킬 수 없다.** 위치 루프만으로 모터를 직접 제어하면, 위치 오차 신호는 전체 기계적 동력 전달 체인(감속기, 링크, 부하 관성)을 거쳐야 토크로 변환되며, 기계 시스템의 관성, 마찰, 탄성은 큰 위상 지연을 유발하여 시스템이 둔감하게 반응하거나 진동하여 발산하게 된다. 종속 구조의 본질은 **제어 문제를 계층화**하는 것이다: 내부 루프가 먼저 "토크가 정확한가"의 문제를 해결하고, 중간 루프가 "회전 속도가 안정적인가"의 문제를 해결하며, 외부 루프가 "위치가 추종하는가"의 문제를 처리한다.

**② 전류 루프는 힘 제어의 물리적 기반이다.** 영구자석 동기 모터의 출력 토크는 q축 전류에 비례하므로, 전류 루프는 본질적으로 토크 루프이다. 전류 루프가 없으면 정밀한 토크 제어를 구현할 수 없으며, 임피던스 제어, 어드미턴스 제어 등 힘 상호작용이 필요한 고급 알고리즘을 지원할 수 없다. 전류 루프 대역폭은 관절이 렌더링할 수 있는 임피던스 범위(Z-width)를 직접 결정하며, 이는 휴머노이드 로봇의 안전 상호작용에 대한 하드 제약 조건이다.

**③ 역사적 위치: 아날로그에서 디지털로의 진화.** 초기 서보 시스템은 아날로그 회로로 전류 루프를 구현하여 대역폭은 높았지만 파라미터 드리프트가 심각했다. 디지털 제어가 보급된 후, 3루프 구조는 DSP/MCU에서 고정 샘플링 주기로 실행되며, 대역폭은 샘플링 주기와 계산 지연에 의해 제한된다. 현대 서보 드라이버는 일반적으로 FOC(자계 방향 제어)로 전류 루프를 구현하고, PI 컨트롤러와 피드포워드 보상을 결합하여 3루프 종속을 산업 및 로봇 분야의 표준 아키텍처로 만든다.

### 원리 분석

**① 종속 대역폭 원칙: 내부 루프가 5-10배 빨라야 하는 이유**

종속 제어 안정성의 핵심 제약은 **대역폭 분리**이다: 각 계층의 대역폭은 다음 계층의 약 5-10배이다. 내부 루프의 폐루프 전달 함수를 $G_i(s)$, 외부 루프 컨트롤러를 $C_o(s)$라고 하면, 외부 루프 설계 시 내부 루프를 1차 지연 요소로 근사할 수 있다:

$$
G_i(s) \approx \frac{1}{\tau_i s + 1}, \quad \tau_i = \frac{1}{2\pi f_i}
$$

여기서 $f_i$는 내부 루프 대역폭이다. 외부 루프 대역폭 $f_o \ll f_i$일 때, 내부 루프 동특성은 무시할 수 있으며, 외부 루프는 "거의 이상적인 토크 소스"를 보게 된다. 대역폭 비율이 충분하지 않으면, 내부 루프의 위상 지연이 외부 루프의 이득 교차 주파수 부근에 진입하여 시스템이 불안정해진다. 5-10배의 경험값은 공학적으로 안정 여유를 보장하는 최소 안전 거리이다.

**② 3루프의 일반적인 대역폭 범위**

| 제어 루프 | 일반적인 대역폭 | 제어 대상 | 출력 |
|--------|---------|--------|------|
| 전류 루프 | 1-5 kHz | 모터 상전류 | PWM 듀티비 |
| 속도 루프 | 50-500 Hz | 모터 회전 속도 | 전류 지령 |
| 위치 루프 | 5-100 Hz | 관절 각도 | 속도 지령 |

전류 루프 대역폭은 일반적으로 1-5 kHz에 도달할 수 있다; 속도 루프는 50-500 Hz; 위치 루프는 5-100 Hz이며, 부하 관성, 강성, 샘플링 주기에 따라 달라진다. 대역폭은 위에서 아래로 약 한 자릿수씩 감소하여 "내부는 빠르고 외부는 느린" 피라미드 구조를 형성한다.

**③ PI 컨트롤러와 피드포워드의 협력**

각 루프의 컨트롤러는 일반적으로 PI 구조를 사용한다: 비례항은 즉각적인 보정을 제공하고, 적분항은 정상 상태 오차를 제거한다. 그러나 순수 피드백 PI는 빠르게 변화하는 궤적을 추종할 때 고유한 지연이 있으므로, 공학에서는 일반적으로 **피드포워드**를 추가한다—기대 궤적에 따라 제어량을 미리 계산하여 주입한다. 위치 루프를 예로 들면:

$$
u_{pos}(t) = K_{p,p} e_p(t) + K_{i,p} \int_0^t e_p(s)\, ds + K_{ff} \dot{\theta}_{des}(t)
$$

여기서 세 번째 항은 속도 피드포워드로, 오차가 발생하기를 기다리지 않고 모터를 미리 구동하여 추종 성능을 크게 향상시킨다. 피드포워드와 피드백의 비율은 공학적 튜닝의 핵심 절충점이다: 피드포워드가 너무 강하면 모델 오차가 증폭되고, 너무 약하면 순수 피드백으로 퇴화된다.

**④ 안티와인드업: 간과되는 안정성 킬러**

모터가 전류 한계 또는 전압 포화에 도달하면, 적분기가 오차를 지속적으로 누적하여 포화에서 벗어난 후 큰 오버슈트 또는 진동이 발생한다. **안티와인드업(anti-windup)** 메커니즘은 적분항이 리미트에 도달했을 때 적분 누적을 동결하거나 되돌려, 포화 시 적분기의 과도한 누적을 방지하고 포화에서 벗어난 후의 큰 오버슈트를 피한다. 이것은 교과서 PID와 실제 기기에서 사용 가능한 PID 사이의 가장 중요한 차이점 중 하나이다.

### 핵심 파라미터 및 사양

| 파라미터 | 일반적인 범위 | 영향 요인 |
|------|---------|---------|
| 전류 루프 대역폭 | 1-5 kHz | 인버터 스위칭 주파수, 전류 샘플링 주기, 모터 인덕턴스 |
| 속도 루프 대역폭 | 50-500 Hz | 엔코더 분해능, 속도 필터 차단 주파수, 부하 관성 |
| 위치 루프 대역폭 | 5-100 Hz | 기계적 강성, 감속비, 궤적 계획 주파수 |
| 대역폭 종속 비율 | 5-10배 | 안정 여유 요구, 위상 여유 |
| 샘플링 주기 | 전류 루프 10-20 kHz; 속도/위치 루프 1-2 kHz | DSP/MCU 연산 능력, ADC 변환 시간 |

전류 루프 대역폭은 일반적으로 1-5 kHz에 도달할 수 있다; 속도 루프는 50-500 Hz; 위치 루프는 5-100 Hz이며, 부하 관성, 강성, 샘플링 주기에 따라 달라진다. 대역폭 종속 비율 5-10배는 안정성 하한이며, 공학에서는 일반적으로 8-10배를 취하여 여유를 확보한다(공학적 판단).

### 수평 비교

**① 단일 루프 PID와 비교**

단일 루프 PID는 위치 오차에 대해 직접 토크 지령을 출력하며, 구조는 간단하지만 동적 성능이 떨어진다: "위치 오차가 크지만 속도가 0인 상태"와 "위치 오차가 크고 빠르게 접근 중인 상태"를 구분할 수 없다. 3루프 종속은 속도 루프의 중간 버퍼를 통해 위치 루프가 "속도 지령이 추종될 수 있는가"라는 문제만 직면하게 하여 튜닝 난이도를 크게 낮춘다.

**② 직접 토크 제어(DTC)와 비교**

DTC는 자속과 토크를 직접 제어하여 전류 루프의 PI 조정이 필요 없고 동적 응답이 더 빠르지만, 토크 리플이 크고 저속 성능이 떨어진다. 3루프 종속의 전류 루프는 FOC 기반으로 토크 평활성이 우수하여, 정밀한 토크 제어가 필요한 관절 구동에 더 적합하다.

**③ 모델 예측 제어(MPC)와 비교**

MPC는 단일 최적화 문제에서 위치, 속도, 토크 제약을 동시에 처리하여 이론적으로 3루프 종속을 대체할 수 있다. 그러나 MPC의 계산량은 예측 시간 영역과 제약 조건 수에 따라 급격히 증가하여, 1 kHz 이상의 관절 제어 주기에서 실시간 실행이 어렵다. 3루프 종속은 고정 구조, 고정 샘플링 주기의 PI 제어로, 연산 능력이 제한된 임베디드 플랫폼에서 여전히 유일한 실용적 선택이다.

### 사용 사례·응용 사례

**① 휴머노이드 로봇 관절 모듈**

거의 모든 상용 및 연구용 휴머노이드 로봇의 관절 구동은 3루프 종속 구조를 채택한다. 유니트리 로보틱스(Unitree Robotics)를 예로 들면, 자체 개발한 M107 영구자석 동기 모터와 맞춤형 드라이버는 내부에서 전류 루프, 속도 루프, 위치 루프의 종속 제어를 실행하여 H1, G1 등의 기종의 동적 보행과 점프를 지원한다.

**② 임피던스 제어의 선행 조건**

Focchi 등(2016)의 연구는 임피던스 제어에서 내부 루프 토크 대역폭, 속도 필터 및 샘플링 주파수가 시스템 안정성에 결정적인 영향을 미친다고 명확히 지적했다. 내부 루프 대역폭이 부족하면 안정적인 강성과 댐핑의 실현 가능 범위(Z-width)가 축소되고, 속도 필터는 위상 지연을 도입하여 안정 영역을 더욱 압축한다. 3루프 종속의 대역폭 설계는 임피던스 제어가 렌더링할 수 있는 물리적 상호작용 특성을 직접 결정한다.

**③ 산업용 서보 및 협동 로봇**

인벤토리(汇川技术), Infineon, TI 등의 제조업체 서보 드라이버 제품은 모두 3루프 종속을 핵심으로 하며, EtherCAT 등 실시간 버스와 결합하여 다축 동기화를 구현한다. 협동 로봇의 토크 센서는 일반적으로 전류 루프 외부에 설치되며, 전류 루프의 토크 추정과 외부 센서를 융합하여 충돌 감지와 힘 제한을 구현한다.

### 한계와 경계

**① 대역폭은 물리적 제약을 받아 무한히 높일 수 없다.** 전류 루프 대역폭은 인버터 스위칭 주파수(일반적으로 10-20 kHz)와 전류 샘플링 주기에 의해 제한되고, 속도 루프는 엔코더 분해능과 속도 필터의 위상 지연에 의해 제한되며, 위치 루프는 기계적 공진 주파수에 의해 제한된다. 특정 루프의 이득을 무분별하게 높이면 기계적 공진이나 노이즈 증폭이 발생한다.

**② 종속 구조는 파라미터 변화에 민감하다.** 부하 관성 변화는 속도 루프와 위치 루프의 등가 제어 대상을 변경하여 대역폭과 안정 여유가 변동된다. 휴머노이드 로봇은 보행 중 부하 관성이 자세에 따라 크게 변하므로, 고정 파라미터 PI 컨트롤러는 전체 운용 조건에서 최적 성능을 유지하기 어렵다. 이득 스케줄링 또는 적응 제어가 필요하다(공학적 판단).

**③ 토크 제약을 직접 처리할 수 없다.** 위치 루프와 속도 루프의 출력은 속도/전류 지령이며, 토크 제약은 전류 루프의 리미트를 통해서만 간접적으로 구현된다. 관절이 정밀한 토크 제어(예: 컴플라이언트 조작)를 필요로 할 때는 토크 모드로 전환하거나 임피던스 제어를 사용해야 하며, 3루프 종속 자체는 토크 폐루프를 제공하지 않는다.

### 일반적인 오해

1. **"대역폭이 높을수록 좋다"**—틀림. Focchi 등의 연구는 내부 루프 대역폭을 최대화하는 것이 특정 임피던스 파라미터 범위를 렌더링해야 하는 응용 시나리오에는 적합하지 않다고 밝혔는데, 이는 안정적인 강성과 댐핑의 실현 가능 범위에 크게 영향을 미치기 때문이다. 대역폭 설계는 응용 요구와 일치해야 하며, 무조건 높은 대역폭을 추구해서는 안 된다.

2. **"3루프 종속은 세 개의 독립적인 PID일 뿐이다"**—틀림. 3루프 사이에는 강한 결합이 존재한다: 내부 루프의 위상 지연은 외부 루프의 안정성 분석에 들어가고, 외부 루프의 지령 변화는 내부 루프의 과도 응답을 자극한다. 올바른 방법은 종속 시스템을 하나의 전체로 보고 대역폭 할당과 안정 여유 검증을 수행하는 것이다.

3. **"피드포워드는 피드백을 대체할 수 있다"**—틀림. 피드포워드는 정확한 모델(관성, 마찰, 역기전력 상수)에 의존하며, 모델 오차는 직접 추종 오차로 변환된다. 피드포워드의 역할은 "피드백의 부담을 줄이는 것"이지 "피드백을 대체하는 것"이 아니다.

4. **"안티와인드업은 단지 옵션일 뿐이다"**—틀림. 관절이 고속으로 움직이거나 충돌하는 시나리오에서 전류 지령은 쉽게 리미트에 도달하며, 안티와인드업이 없으면 적분기가 많은 오차를 누적하여 포화에서 벗어난 후 큰 오버슈트나 진동이 발생한다. 안티와인드업은 극한 운용 조건에서 시스템 안정성을 보장하는 필수 메커니즘이다.

### 관련 지식

- `ent_method_pid_control` — PID 제어는 3루프 종속의 각 루프에서 기본 컨트롤러이며, 그 공학적 세부 사항(안티와인드업, 미분 필터, 피드포워드)이 3루프의 실제 성능을 직접 결정한다.
- `ent_component_servo_drive_2024` — 서보 드라이버는 3루프 종속 제어를 탑재하는 하드웨어 플랫폼으로, 제어 지령을 모터 전력 출력으로 변환하는 역할을 담당한다.
- `ent_method_field_oriented_control` — FOC는 전류 루프 구현의 핵심 알고리즘으로, 좌표 변환을 통해 3상 교류 전류를 토크 성분과 자속 성분으로 분리한다.
- `ent_paper_focchi_robot_impedance_control_and_pa_2016` — 이 논문은 내부 루프 대역폭, 속도 필터 및 샘플링 주기가 임피던스 제어 안정성에 미치는 영향을 체계적으로 분석하여, 3루프 대역폭 설계의 중요한 이론적 근거를 제공한다.
- `ent_oem_unitree_robotics` — 유니트리 로보틱스는 휴머노이드 로봇 관절에 3루프 종속 제어를 실제로 배포하여, 이 방법이 양산 제품에서 사용되는 대표적인 사례이다.
- `ent_method_whole_body_control` — 전신 제어는 3루프 종속을 하위 실행 계층으로 간주하고, 그 위에 동역학 최적화와 작업 공간 제어를 중첩한다.
