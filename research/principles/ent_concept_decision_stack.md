---
$id: ent_concept_decision_stack
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: concept
names:
  en: Decision Stack
  zh: 决策栈
  ko: 의사결정 스택
summary:
  en: The software subsystem that converts perception and task goals into plans, policies, and behavioral decisions.
  zh: 将感知结果与任务目标转化为规划、策略与行为决策的软件子系统。
  ko: 인식 결과와 작업 목표를 계획·정책·행동 결정으로 변환하는 소프트웨어 하위 시스템.
domains:
- 08_software_middleware
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- concept
- chapter_24
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-13'
  confidence: high
  notes: 'Body populated from Wiki chapter section by scripts/fill_gap_bodies_from_wiki.py; pending human review and translation
    to en/ko. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12] body upgraded to
    textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters +
    first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

决策栈（Decision Stack）是人形机器人软件系统中将感知结果与任务目标转化为规划、策略与行为决策的软件子系统，位于价值链的智能层。它真正改变的不是单个决策算法的性能，而是将"感知-规划-控制"的串行链路重构为可分层、可迭代、可验证的软件架构，使机器人从"能执行单点任务"进化为"能在开放场景中持续做出合理选择"。决策栈横跨软件中间件与 AI 模型算法两大领域，是连接环境理解与物理动作的枢纽。

## 核心内容

### 是什么：准确定义

决策栈（Decision Stack）是一个**软件子系统**，其输入是感知模块输出的环境状态估计与用户/系统下达的任务目标，输出是可供执行层（如运动规划器、实时控制器）消费的行为指令序列。它由三个层次构成：

- **任务规划层**：将抽象任务目标（如"从仓库 A 区取货至 B 区"）分解为有序的子任务序列，处理时间与资源约束。
- **策略选择层**：在子任务层面选择执行策略——是调用预编程行为库、触发学习策略（如 VLA 模型），还是请求遥操作辅助。
- **行为决策层**：在毫秒至秒级时间尺度上生成具体行为指令（如目标位姿、速度指令、抓取姿态），并处理异常与回退逻辑。

在人形机器人知识图谱中，决策栈属于 **概念** 类型，价值链层级为**智能层**，英文名称为 *Decision Stack*，韩文名称为 *의사결정 스택*。

### 为什么存在：痛点与历史定位

在决策栈概念成熟之前，人形机器人的"智能"高度碎片化：研究者将任务逻辑硬编码在状态机中，或直接让端到端策略网络输出关节力矩。这两种极端都存在问题——前者无法应对开放世界的长尾场景，后者缺乏可解释性与安全保证。

决策栈的存在源于三个结构性痛点：

1. **时序尺度冲突**：任务规划需要秒级甚至分钟级的推理，而关节控制需要毫秒级响应。若不分层，单一算法无法同时满足两种时间尺度。
2. **安全与智能的矛盾**：端到端策略（如 VLA 模型）在感知泛化上表现优异，但其输出缺乏形式化保证。决策栈通过引入策略选择层，将学习策略置于可监督、可回退的框架内。
3. **工程可迭代性**：当任务失败时，工程师需要定位是感知错误、规划错误还是执行错误。分层决策栈使问题可隔离、可调试、可回归测试。

历史定位上，决策栈是"感知-规划-控制"经典范式在 AI 时代的演进形态——它保留了分层的工程优势，同时为学习型策略（如 VLA）预留了接入位。

### 原理拆解

**① 分层抽象：时间尺度与信息粒度的解耦**

决策栈的核心原理是**分层抽象**。每一层只与相邻层通信，且运行在不同时间尺度上：

- 任务规划层运行在 1–10 Hz，处理符号级信息（目标、约束、资源）。
- 策略选择层运行在 10–50 Hz，根据当前状态选择行为模式。
- 行为决策层运行在 50–200 Hz，输出具体的运动指令（如目标关节角、末端位姿）。

这种分层使系统满足实时性要求，同时保持高层决策的全局视野。形式化地，决策栈可建模为马尔可夫决策过程的层次化扩展：

$$
\pi(a_t | s_t) = \pi_{\text{low}}\left(a_t \,\middle|\, s_t, \pi_{\text{high}}(s_t)\right)
$$

其中 \(\pi_{\text{high}}\) 是高层策略（任务/策略选择），\(\pi_{\text{low}}\) 是低层策略（行为生成）。这种分解使学习型高层策略（如大模型驱动的任务规划）与经典低层控制器（如 MPC、运动规划器）能够协同工作。

**② 策略选择：学习与经典的混合架构**

决策栈的关键创新在于**策略选择层**。它维护一个策略库，包含：

- **预编程行为**：如站立、行走、抓取等经过验证的固定动作。
- **学习型策略**：如 VLA 模型输出的动作序列（见第 19 章）。
- **遥操作回退**：当自主策略置信度低时，请求 human-in-the-loop 介入。

选择机制通常基于置信度评分与安全约束。例如，当 VLA 模型对当前场景的认知不确定度超过阈值时，决策栈自动切换至遥操作模式——这正是家庭场景中"通过遥操作辅助兜底长尾任务并同步积累数据"（见第 27 章）的软件实现基础。

**③ 数据飞轮：决策栈作为数据采集接口**

决策栈不仅是决策机构，也是数据飞轮（`ent_concept_data_flywheel`）的关键节点。每一次决策——无论成功还是失败——都被记录为（状态，决策，结果）三元组，回流至训练管线。部署产生数据、数据改进模型、模型提升性能并解锁更多部署，这一循环的起点正是决策栈的日志系统。

### 关键参数与规格

决策栈的工程规格通常由以下指标刻画（数字来自第 26 章整机系统指标表）：

| 指标 | 定义 | 工程意义 |
|------|------|----------|
| 感知-计算配置 | 传感器套件 × 端侧算力 | 决定决策栈可用的输入带宽与推理速度 |
| 决策频率 | 行为决策层输出指令的频率（Hz） | 决定对动态环境的响应能力 |
| 策略切换延迟 | 从检测到需要切换策略到完成切换的时间 | 决定安全回退的及时性 |
| 任务成功率 | 完整任务执行成功的概率 | 区分"演示"与"产品"的核心判据 |
| 平均无故障时间（MTBF） | 系统连续无故障运行时长 | 工业部署的硬性要求 |

需注意：决策栈本身不直接决定峰值扭矩密度、推重比等机械指标，但它通过任务规划影响能量消耗模式，进而影响能量自主性（电池容量 / 典型任务平均功率）。

### 横向对比

决策栈与相邻概念的关键区别：

| 维度 | 决策栈 | 运动规划器（如 MoveIt） | 实时控制框架 |
|------|--------|------------------------|--------------|
| 层级 | 智能层 | 软件中间件 | 软件中间件 |
| 输入 | 感知结果 + 任务目标 | 目标位姿 + 环境模型 | 关节指令 + 状态反馈 |
| 输出 | 行为指令序列 | 无碰撞路径 | 关节力矩/位置指令 |
| 时间尺度 | 10 ms – 10 s | 100 ms – 1 s | 1 ms 级 |
| 失败模式 | 决策错误（选错策略） | 规划失败（无解） | 时序违约（超时） |

决策栈与运动规划器的关系是**上下游**：决策栈决定"做什么"，运动规划器决定"怎么做"。与实时控制框架的关系是**分层隔离**：决策栈运行在低频 AI 任务域，实时控制框架运行在高频控制域，两者通过明确定义的接口通信。

### 谁在用·应用案例

**① 特斯拉 Optimus（数据闭环驱动）**

Optimus 的决策栈深度绑定其数据飞轮策略——机器人在自有工厂中执行任务产生的数据直接回流训练，构成自我增强循环（见第 26 章）。其决策栈的独特之处在于：任务规划层与策略选择层大量复用其车规级 AI 基础设施，行为决策层则针对双足运动与灵巧操作定制。

**② Agility Digit（RaaS 模式下的场景聚焦）**

Digit 在 Amazon、GXO 等第三方客户的真实仓库中运行，其决策栈采用"收窄任务域"策略——聚焦物流箱体搬运，以换取 ≥99% 的任务成功率（见第 26 章）。决策栈的策略库高度精简，遥操作回退仅用于极端异常。

**③ 家庭服务机器人（遥操作辅助范式）**

以 GR-3 为代表的康养陪伴机器人，其决策栈采用"低操作强度任务切入 + 遥操作兜底"的架构（见第 27 章）。在陪伴、提醒、轻取物等场景中，决策栈优先调用预编程行为；当遇到长尾任务时，切换至 human-in-the-loop 模式，同时积累数据以逐步扩大自主范围。

### 局限与边界

1. **决策栈不解决感知错误**：若上游感知模块输出错误的状态估计，决策栈的决策再合理也无济于事。工程上需通过多传感器融合与置信度传递缓解。
2. **分层架构的延迟代价**：每一层抽象都引入延迟与信息损失。对于需要极低延迟的反射行为（如摔倒保护），决策栈必须允许旁路——直接由实时控制框架处理。
3. **策略选择的评估难题**：如何评估"策略选择层"本身的好坏？任务成功率是最终指标，但难以归因——失败可能源于感知、规划或执行。工程判断：需建立细粒度的日志与回放系统，支持离线归因分析。
4. **学习型策略的可解释性**：VLA 模型输出的决策缺乏形式化语义，难以嵌入安全论证。当前工程实践是将其置于策略选择层的监督之下，而非直接驱动执行。

### 常见误区

1. **"决策栈 = 任务规划器"**——错。任务规划只是决策栈的最上层；策略选择与行为决策同样是核心组成部分。忽略后两者，系统将无法应对动态环境。
2. **"端到端模型可以取代决策栈"**——错。端到端策略在感知泛化上优秀，但其输出缺乏时序一致性与安全保证。决策栈的价值在于为学习策略提供结构化监督与回退机制。
3. **"决策栈是纯软件问题"**——错。决策栈的架构受硬件约束（端侧算力、传感器配置）与部署场景（任务域宽度、安全标准）的强烈影响。脱离硬件谈决策栈是空谈。
4. **"决策栈越复杂越好"**——错。工程判断：决策栈的复杂度应与任务域匹配。Digit 的成功恰恰在于其决策栈的"精简"——收窄任务域以换取可靠性，而非堆叠更多策略。

### 相关知识

- `ent_software_moveit_motion_planning_2024` — 决策栈的行为决策层输出的目标位姿，由 MoveIt 等运动规划框架转化为无碰撞路径。
- `ent_process_p14_1_2` — 实时控制框架是决策栈的下游执行保障，确保行为指令在 1 kHz 级时序下稳定执行，隔离 AI 任务对控制环的干扰。

## 参考

- [Chapter 26: 整机系统与部署成熟度](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-26.md)
- [Chapter 27: 场景落地与商业模式](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-27.md)
- [Chapter 19: VLA 模型与学习策略](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-19.md)
- [Chapter 24: 智能层与软件中间件](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-24.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

The Decision Stack is a software subsystem in humanoid robot software systems that transforms perception results and task objectives into planning, policy, and behavioral decisions. It resides in the intelligence layer of the value chain. What it truly changes is not the performance of individual decision algorithms, but rather the reconstruction of the serial "perception-planning-control" pipeline into a layered, iterative, and verifiable software architecture, enabling robots to evolve from "executing single-point tasks" to "continuously making sound choices in open scenarios." The Decision Stack spans both software middleware and AI model algorithms, serving as the hub connecting environmental understanding with physical actions.

## Content

### What It Is: Precise Definition

The Decision Stack is a **software subsystem** whose inputs are the environmental state estimates output by the perception module and the task objectives issued by users or the system, and whose outputs are sequences of behavioral instructions consumable by execution layers (e.g., motion planners, real-time controllers). It consists of three layers:

- **Task Planning Layer**: Decomposes abstract task objectives (e.g., "pick up goods from Zone A of the warehouse and deliver them to Zone B") into an ordered sequence of subtasks, handling temporal and resource constraints.
- **Policy Selection Layer**: Selects execution policies at the subtask level—whether to invoke pre-programmed behavior libraries, trigger learned policies (e.g., VLA models), or request teleoperation assistance.
- **Behavior Decision Layer**: Generates specific behavioral instructions (e.g., target poses, velocity commands, grasp postures) at millisecond-to-second timescales, while handling exceptions and fallback logic.

In the humanoid robot knowledge graph, the Decision Stack belongs to the **concept** type, its value chain level is the **intelligence layer**, its English name is *Decision Stack*, and its Korean name is *의사결정 스택*.

### Why It Exists: Pain Points and Historical Positioning

Before the Decision Stack concept matured, the "intelligence" of humanoid robots was highly fragmented: researchers hardcoded task logic into state machines or directly let end-to-end policy networks output joint torques. Both extremes had problems—the former could not handle long-tail scenarios in the open world, and the latter lacked interpretability and safety guarantees.

The Decision Stack exists due to three structural pain points:

1. **Temporal Scale Conflict**: Task planning requires second-level or even minute-level reasoning, while joint control requires millisecond-level responses. Without layering, a single algorithm cannot simultaneously satisfy both timescales.
2. **The Contradiction Between Safety and Intelligence**: End-to-end policies (e.g., VLA models) excel in perception generalization, but their outputs lack formal guarantees. The Decision Stack addresses this by introducing a policy selection layer that places learned policies within a supervised, fallback-capable framework.
3. **Engineering Iterability**: When a task fails, engineers need to locate whether the error stems from perception, planning, or execution. A layered Decision Stack makes problems isolatable, debuggable, and regression-testable.

Historically, the Decision Stack is the evolution of the classic "perception-planning-control" paradigm in the AI era—it retains the engineering advantages of layering while reserving integration points for learned policies (e.g., VLA).

### Principle Breakdown

**① Layered Abstraction: Decoupling Temporal Scales and Information Granularity**

The core principle of the Decision Stack is **layered abstraction**. Each layer communicates only with adjacent layers and operates at different temporal scales:

- The task planning layer runs at 1–10 Hz, handling symbolic-level information (goals, constraints, resources).
- The policy selection layer runs at 10–50 Hz, selecting behavioral modes based on the current state.
- The behavior decision layer runs at 50–200 Hz, outputting specific motion instructions (e.g., target joint angles, end-effector poses).

This layering enables the system to meet real-time requirements while maintaining a global perspective for high-level decisions. Formally, the Decision Stack can be modeled as a hierarchical extension of the Markov decision process:

$$
\pi(a_t | s_t) = \pi_{\text{low}}\left(a_t \,\middle|\, s_t, \pi_{\text{high}}(s_t)\right)
$$

where \(\pi_{\text{high}}\) is the high-level policy (task/policy selection) and \(\pi_{\text{low}}\) is the low-level policy (behavior generation). This decomposition allows learned high-level policies (e.g., large-model-driven task planning) to work collaboratively with classical low-level controllers (e.g., MPC, motion planners).

**② Policy Selection: A Hybrid Architecture of Learned and Classical Methods**

The key innovation of the Decision Stack lies in the **policy selection layer**. It maintains a policy library containing:

- **Pre-programmed behaviors**: Such as standing, walking, grasping—validated fixed actions.
- **Learned policies**: Such as action sequences output by VLA models (see Chapter 19).
- **Teleoperation fallback**: When the confidence of autonomous policies is low, human-in-the-loop intervention is requested.

The selection mechanism is typically based on confidence scores and safety constraints. For example, when the VLA model's epistemic uncertainty about the current scene exceeds a threshold, the Decision Stack automatically switches to teleoperation mode—this is precisely the software implementation foundation for "using teleoperation assistance to backstop long-tail tasks while synchronously accumulating data" in home scenarios (see Chapter 27).

**③ Data Flywheel: The Decision Stack as a Data Collection Interface**

The Decision Stack is not only a decision-making mechanism but also a key node in the data flywheel (`ent_concept_data_flywheel`). Every decision—whether successful or failed—is recorded as a (state, decision, outcome) triplet and fed back into the training pipeline. Deployment generates data, data improves models, models enhance performance and unlock more deployments—the starting point of this loop is the Decision Stack's logging system.

### Key Parameters and Specifications

The engineering specifications of the Decision Stack are typically characterized by the following metrics (figures from the whole-system specification table in Chapter 26):

| Metric | Definition | Engineering Significance |
|--------|------------|--------------------------|
| Perception-compute configuration | Sensor suite × edge-side compute | Determines the input bandwidth and inference speed available to the Decision Stack |
| Decision frequency | Frequency of instructions output by the behavior decision layer (Hz) | Determines responsiveness to dynamic environments |
| Policy switching latency | Time from detecting the need to switch policies to completing the switch | Determines the timeliness of safety fallbacks |
| Task success rate | Probability of successfully executing a complete task | Core criterion distinguishing "demonstrations" from "products" |
| Mean time between failures (MTBF) | Duration of continuous fault-free system operation | Hard requirement for industrial deployment |

Note: The Decision Stack itself does not directly determine mechanical metrics such as peak torque density or thrust-to-weight ratio, but it influences energy consumption patterns through task planning, thereby affecting energy autonomy (battery capacity / average power of typical tasks).

### Horizontal Comparison

Key differences between the Decision Stack and adjacent concepts:

| Dimension | Decision Stack | Motion Planner (e.g., MoveIt) | Real-Time Control Framework |
|-----------|----------------|-------------------------------|------------------------------|
| Layer | Intelligence layer | Software middleware | Software middleware |
| Input | Perception results + task objectives | Target pose + environment model | Joint commands + state feedback |
| Output | Behavioral instruction sequences | Collision-free paths | Joint torque/position commands |
| Temporal scale | 10 ms – 10 s | 100 ms – 1 s | 1 ms level |
| Failure mode | Decision error (wrong policy selected) | Planning failure (no solution) | Timing violation (timeout) |

The relationship between the Decision Stack and the motion planner is **upstream-downstream**: the Decision Stack determines "what to do," while the motion planner determines "how to do it." The relationship with the real-time control framework is **layered isolation**: the Decision Stack operates in the low-frequency AI task domain, while the real-time control framework operates in the high-frequency control domain, communicating through well-defined interfaces.

### Who Uses It: Application Cases

**① Tesla Optimus (Data Closed-Loop Driven)**

Optimus's Decision Stack is deeply tied to its data flywheel strategy—data generated from tasks performed by robots in Tesla's own factories flows directly back into training, forming a self-reinforcing loop (see Chapter 26). The uniqueness of its Decision Stack lies in the fact that the task planning layer and policy selection layer heavily reuse its automotive-grade AI infrastructure, while the behavior decision layer is customized for bipedal locomotion and dexterous manipulation.

**② Agility Digit (Scenario Focus Under RaaS Model)**

Digit operates in real warehouses of third-party customers such as Amazon and GXO. Its Decision Stack adopts a "narrowed task domain" strategy—focusing on logistics box handling to achieve a ≥99% task success rate (see Chapter 26). The policy library of the Decision Stack is highly streamlined, with teleoperation fallback reserved only for extreme anomalies.

**③ Home Service Robots (Teleoperation-Assisted Paradigm)**

Represented by GR-3, companion robots for health and wellness adopt an architecture of "entry via low-operation-intensity tasks + teleoperation backstop" (see Chapter 27). In scenarios such as companionship, reminders, and light object retrieval, the Decision Stack prioritizes invoking pre-programmed behaviors; when encountering long-tail tasks, it switches to human-in-the-loop mode while accumulating data to gradually expand the scope of autonomy.

### Limitations and Boundaries

1. **The Decision Stack does not solve perception errors**: If the upstream perception module outputs incorrect state estimates, no matter how reasonable the Decision Stack's decisions are, they are futile. Engineering mitigation relies on multi-sensor fusion and confidence propagation.
2. **Latency cost of the layered architecture**: Each layer of abstraction introduces latency and information loss. For reflex behaviors requiring extremely low latency (e.g., fall protection), the Decision Stack must allow bypass—handled directly by the real-time control framework.
3. **Difficulty in evaluating policy selection**: How to assess the quality of the "policy selection layer" itself? Task success rate is the ultimate metric, but it is difficult to attribute—failure may stem from perception, planning, or execution. Engineering judgment: fine-grained logging and replay systems must be established to support offline attribution analysis.
4. **Interpretability of learned policies**: Decisions output by VLA models lack formal semantics, making it difficult to embed them in safety arguments. Current engineering practice places them under the supervision of the policy selection layer rather than directly driving execution.

### Common Misconceptions

1. **"Decision Stack = Task Planner"**—Wrong. Task planning is only the topmost layer of the Decision Stack; policy selection and behavior decision are equally core components. Ignoring the latter two leaves the system unable to handle dynamic environments.
2. **"End-to-end models can replace the Decision Stack"**—Wrong. End-to-end policies excel in perception generalization, but their outputs lack temporal consistency and safety guarantees. The value of the Decision Stack lies in providing structured supervision and fallback mechanisms for learned policies.
3. **"The Decision Stack is a pure software problem"**—Wrong. The architecture of the Decision Stack is strongly influenced by hardware constraints (edge-side compute, sensor configuration) and deployment scenarios (task domain breadth, safety standards). Discussing the Decision Stack in isolation from hardware is empty talk.
4. **"The more complex the Decision Stack, the better"**—Wrong. Engineering judgment: the complexity of the Decision Stack should match the task domain. Digit's success lies precisely in the "streamlining" of its Decision Stack—narrowing the task domain to gain reliability, rather than stacking more policies.

### Related Knowledge

- `ent_software_moveit_motion_planning_2024` — Target poses output by the behavior decision layer of the Decision Stack are converted into collision-free paths by motion planning frameworks such as MoveIt.
- `ent_process_p14_1_2` — The real-time control framework is the downstream execution guarantee of the Decision Stack, ensuring that behavioral instructions are stably executed at 1 kHz-level timing and isolating AI tasks from interfering with the control loop.

## 개요

의사결정 스택(Decision Stack)은 휴머노이드 로봇 소프트웨어 시스템에서 인식 결과와 작업 목표를 계획, 정책, 행동 결정으로 전환하는 소프트웨어 하위 시스템으로, 가치 사슬의 지능 계층에 위치한다. 이는 단일 의사결정 알고리즘의 성능을 바꾸는 것이 아니라, "인식-계획-제어"의 직렬 체인을 계층화, 반복, 검증 가능한 소프트웨어 아키텍처로 재구성하여 로봇이 "단일 작업을 수행할 수 있는 상태"에서 "개방형 환경에서 지속적으로 합리적인 선택을 할 수 있는 상태"로 진화하게 한다. 의사결정 스택은 소프트웨어 미들웨어와 AI 모델 알고리즘 두 영역에 걸쳐 있으며, 환경 이해와 물리적 동작을 연결하는 허브이다.

## 핵심 내용

### 무엇인가: 정확한 정의

의사결정 스택(Decision Stack)은 **소프트웨어 하위 시스템**으로, 입력은 인식 모듈이 출력하는 환경 상태 추정과 사용자/시스템이 내린 작업 목표이며, 출력은 실행 계층(예: 운동 계획기, 실시간 제어기)이 소비할 수 있는 행동 명령 시퀀스이다. 이는 세 가지 계층으로 구성된다:

- **작업 계획 계층**: 추상적 작업 목표(예: "창고 A 구역에서 B 구역으로 물품 픽업")를 순서 있는 하위 작업 시퀀스로 분해하고, 시간 및 자원 제약을 처리한다.
- **정책 선택 계층**: 하위 작업 수준에서 실행 정책을 선택한다—사전 프로그래밍된 행동 라이브러리를 호출할지, 학습 정책(예: VLA 모델)을 트리거할지, 아니면 원격 조작 지원을 요청할지.
- **행동 결정 계층**: 밀리초에서 초 단위 시간 척도로 구체적인 행동 명령(예: 목표 자세, 속도 명령, 파지 자세)을 생성하고, 이상 및 롤백 로직을 처리한다.

휴머노이드 로봇 지식 그래프에서 의사결정 스택은 **개념** 유형에 속하며, 가치 사슬 계층은 **지능 계층**이고, 영어 명칭은 *Decision Stack*, 한국어 명칭은 *의사결정 스택*이다.

### 왜 존재하는가: 문제점과 역사적 위치

의사결정 스택 개념이 성숙하기 전, 휴머노이드 로봇의 "지능"은 고도로 파편화되어 있었다: 연구자들은 작업 로직을 상태 머신에 하드코딩하거나, 엔드투엔드 정책 네트워크가 직접 관절 토크를 출력하도록 했다. 두 극단 모두 문제가 있었다—전자는 개방형 세계의 긴 꼬리(long-tail) 시나리오에 대응할 수 없었고, 후자는 설명 가능성과 안전 보장이 부족했다.

의사결정 스택의 존재는 세 가지 구조적 문제점에서 비롯된다:

1. **시간 척도 충돌**: 작업 계획은 초 단위 또는 분 단위 추론이 필요하지만, 관절 제어는 밀리초 단위 응답이 필요하다. 계층화하지 않으면 단일 알고리즘으로 두 시간 척도를 동시에 충족할 수 없다.
2. **안전과 지능의 모순**: 엔드투엔드 정책(예: VLA 모델)은 인식 일반화에서 뛰어나지만, 그 출력에는 형식적 보장이 부족하다. 의사결정 스택은 정책 선택 계층을 도입하여 학습 정책을 감독 가능하고 롤백 가능한 프레임워크 안에 둔다.
3. **엔지니어링 반복 가능성**: 작업 실패 시, 엔지니어는 인식 오류, 계획 오류, 실행 오류 중 어디에서 문제가 발생했는지 파악해야 한다. 계층화된 의사결정 스택은 문제를 격리, 디버깅, 회귀 테스트 가능하게 만든다.

역사적 위치에서 의사결정 스택은 "인식-계획-제어"의 고전적 패러다임이 AI 시대에 진화한 형태이다—계층화의 엔지니어링 이점을 유지하면서, 학습형 정책(예: VLA)을 위한 접속 지점을 마련했다.

### 원리 분석

**① 계층적 추상화: 시간 척도와 정보 입자 크기의 분리**

의사결정 스택의 핵심 원리는 **계층적 추상화**이다. 각 계층은 인접 계층과만 통신하며, 서로 다른 시간 척도로 작동한다:

- 작업 계획 계층은 1–10 Hz로 작동하며, 기호 수준 정보(목표, 제약, 자원)를 처리한다.
- 정책 선택 계층은 10–50 Hz로 작동하며, 현재 상태에 따라 행동 모드를 선택한다.
- 행동 결정 계층은 50–200 Hz로 작동하며, 구체적인 운동 명령(예: 목표 관절 각도, 말단 자세)을 출력한다.

이러한 계층화는 시스템이 실시간 요구 사항을 충족하면서도 고수준 결정의 전역적 시야를 유지하게 한다. 형식적으로, 의사결정 스택은 마르코프 결정 과정의 계층적 확장으로 모델링할 수 있다:

$$
\pi(a_t | s_t) = \pi_{\text{low}}\left(a_t \,\middle|\, s_t, \pi_{\text{high}}(s_t)\right)
$$

여기서 \(\pi_{\text{high}}\)는 고수준 정책(작업/정책 선택)이고, \(\pi_{\text{low}}\)는 저수준 정책(행동 생성)이다. 이러한 분해는 학습형 고수준 정책(예: 대규모 모델 기반 작업 계획)과 고전적 저수준 제어기(예: MPC, 운동 계획기)가 협력하여 작동할 수 있게 한다.

**② 정책 선택: 학습과 고전의 혼합 아키텍처**

의사결정 스택의 핵심 혁신은 **정책 선택 계층**에 있다. 이는 정책 라이브러리를 유지하며, 다음을 포함한다:

- **사전 프로그래밍된 행동**: 서기, 걷기, 파지 등 검증된 고정 동작.
- **학습형 정책**: 예: VLA 모델이 출력하는 동작 시퀀스(제19장 참조).
- **원격 조작 롤백**: 자율 정책의 신뢰도가 낮을 때, human-in-the-loop 개입을 요청.

선택 메커니즘은 일반적으로 신뢰도 점수와 안전 제약에 기반한다. 예를 들어, VLA 모델이 현재 장면에 대한 인식 불확실성이 임계값을 초과하면, 의사결정 스택은 자동으로 원격 조작 모드로 전환한다—이것이 가정 환경에서 "원격 조작 지원으로 긴 꼬리 작업을 보완하고 동시에 데이터를 축적하는"(제27장 참조) 소프트웨어 구현 기반이다.

**③ 데이터 플라이휠: 의사결정 스택을 데이터 수집 인터페이스로**

의사결정 스택은 의사결정 기관일 뿐만 아니라, 데이터 플라이휠(`ent_concept_data_flywheel`)의 핵심 노드이기도 하다. 모든 의사결정—성공 여부와 관계없이—(상태, 의사결정, 결과) 삼중항으로 기록되어 훈련 파이프라인으로 회귀한다. 배포가 데이터를 생성하고, 데이터가 모델을 개선하며, 모델이 성능을 향상시켜 더 많은 배포를 가능하게 하는 이 순환의 출발점은 바로 의사결정 스택의 로그 시스템이다.

### 핵심 매개변수와 사양

의사결정 스택의 엔지니어링 사양은 일반적으로 다음 지표로 특징지어진다(숫자는 제26장 전체 시스템 지표 표에서 가져옴):

| 지표 | 정의 | 엔지니어링 의미 |
|------|------|----------|
| 인식-계산 구성 | 센서 세트 × 엣지 컴퓨팅 성능 | 의사결정 스택이 사용할 수 있는 입력 대역폭과 추론 속도를 결정 |
| 의사결정 주파수 | 행동 결정 계층이 명령을 출력하는 주파수(Hz) | 동적 환경에 대한 응답 능력을 결정 |
| 정책 전환 지연 | 전환 필요 감지부터 전환 완료까지의 시간 | 안전 롤백의 적시성을 결정 |
| 작업 성공률 | 전체 작업 실행 성공 확률 | "데모"와 "제품"을 구분하는 핵심 기준 |
| 평균 무고장 시간(MTBF) | 시스템 연속 무고장 작동 시간 | 산업 배포의 필수 요구 사항 |

주의할 점: 의사결정 스택 자체는 최대 토크 밀도, 추력 대 중량 비율과 같은 기계적 지표를 직접 결정하지 않지만, 작업 계획을 통해 에너지 소비 패턴에 영향을 미치고, 결과적으로 에너지 자율성(배터리 용량 / 일반 작업 평균 전력)에 영향을 준다.

### 수평 비교

의사결정 스택과 인접 개념의 핵심 차이:

| 차원 | 의사결정 스택 | 운동 계획기(예: MoveIt) | 실시간 제어 프레임워크 |
|------|--------|------------------------|--------------|
| 계층 | 지능 계층 | 소프트웨어 미들웨어 | 소프트웨어 미들웨어 |
| 입력 | 인식 결과 + 작업 목표 | 목표 자세 + 환경 모델 | 관절 명령 + 상태 피드백 |
| 출력 | 행동 명령 시퀀스 | 충돌 없는 경로 | 관절 토크/위치 명령 |
| 시간 척도 | 10 ms – 10 s | 100 ms – 1 s | 1 ms 수준 |
| 실패 모드 | 의사결정 오류(잘못된 정책 선택) | 계획 실패(해 없음) | 시퀀스 위반(시간 초과) |

의사결정 스택과 운동 계획기의 관계는 **상하위**이다: 의사결정 스택은 "무엇을 할지"를 결정하고, 운동 계획기는 "어떻게 할지"를 결정한다. 실시간 제어 프레임워크와의 관계는 **계층적 격리**이다: 의사결정 스택은 저주파 AI 작업 영역에서 작동하고, 실시간 제어 프레임워크는 고주파 제어 영역에서 작동하며, 둘은 명확히 정의된 인터페이스를 통해 통신한다.

### 누가 사용하는가·응용 사례

**① 테슬라 Optimus(데이터 폐쇄 루프 기반)**

Optimus의 의사결정 스택은 데이터 플라이휠 전략과 깊이 연결되어 있다—로봇이 자체 공장에서 작업을 수행하며 생성한 데이터가 직접 훈련으로 회귀하여 자기 강화 순환을 구성한다(제26장 참조). 의사결정 스택의 독특한 점은: 작업 계획 계층과 정책 선택 계층이 차량급 AI 인프라를 대량 재사용하고, 행동 결정 계층은 이족 보행 운동과 손재주 조작에 맞춰 맞춤화된다는 것이다.

**② Agility Digit(RaaS 모드에서의 시나리오 집중)**

Digit은 Amazon, GXO 등 제3자 고객의 실제 창고에서 작동하며, 의사결정 스택은 "작업 영역 축소" 전략을 채택한다—물류 상자 운반에 집중하여 ≥99%의 작업 성공률을 확보한다(제26장 참조). 의사결정 스택의 정책 라이브러리는 고도로 간소화되어 있으며, 원격 조작 롤백은 극단적 이상 상황에서만 사용된다.

**③ 가정용 서비스 로봇(원격 조작 지원 패러다임)**

GR-3을 대표로 하는 건강 관리 동반 로봇은 의사결정 스택에서 "저조작 강도 작업 진입 + 원격 조작 보완" 아키텍처를 채택한다(제27장 참조). 동반, 알림, 가벼운 물건 집기 등 시나리오에서 의사결정 스택은 사전 프로그래밍된 행동을 우선 호출한다; 긴 꼬리 작업이 발생하면 human-in-the-loop 모드로 전환하고, 동시에 데이터를 축적하여 자율 범위를 점진적으로 확장한다.

### 한계와 경계

1. **의사결정 스택은 인식 오류를 해결하지 않는다**: 상위 인식 모듈이 잘못된 상태 추정을 출력하면, 의사결정 스택의 결정이 아무리 합리적이어도 소용없다. 엔지니어링에서는 다중 센서 융합과 신뢰도 전달로 완화해야 한다.
2. **계층 아키텍처의 지연 비용**: 각 계층 추상화는 지연과 정보 손실을 도입한다. 극도로 낮은 지연이 필요한 반사 행동(예: 낙상 보호)의 경우, 의사결정 스택은 우회 경로를 허용해야 한다—실시간 제어 프레임워크가 직접 처리하도록.
3. **정책 선택의 평가 어려움**: "정책 선택 계층" 자체의 우수성을 어떻게 평가할까? 작업 성공률이 최종 지표이지만, 귀인이 어렵다—실패는 인식, 계획, 실행 중 어디에서 발생했는지 알 수 없다. 엔지니어링 판단: 세분화된 로그와 재생 시스템을 구축하여 오프라인 귀인 분석을 지원해야 한다.
4. **학습형 정책의 설명 가능성**: VLA 모델이 출력하는 결정은 형식적 의미론이 부족하여 안전 논증에 포함하기 어렵다. 현재 엔지니어링 실무는 이를 정책 선택 계층의 감독 아래 두는 것이지, 직접 실행을 구동하지 않는다.

### 일반적인 오해

1. **"의사결정 스택 = 작업 계획기"**—틀림. 작업 계획은 의사결정 스택의 최상위 계층일 뿐이다; 정책 선택과 행동 결정도 핵심 구성 요소이다. 후자 둘을 무시하면 시스템은 동적 환경에 대응할 수 없다.
2. **"엔드투엔드 모델이 의사결정 스택을 대체할 수 있다"**—틀림. 엔드투엔드 정책은 인식 일반화에서 우수하지만, 그 출력에는 시간적 일관성과 안전 보장이 부족하다. 의사결정 스택의 가치는 학습 정책에 구조적 감독과 롤백 메커니즘을 제공하는 데 있다.
3. **"의사결정 스택은 순수 소프트웨어 문제이다"**—틀림. 의사결정 스택의 아키텍처는 하드웨어 제약(엣지 컴퓨팅 성능, 센서 구성)과 배포 시나리오(작업 영역 폭, 안전 표준)의 강한 영향을 받는다. 하드웨어를 배제한 의사결정 스택 논의는 공허하다.
4. **"의사결정 스택은 복잡할수록 좋다"**—틀림. 엔지니어링 판단: 의사결정 스택의 복잡성은 작업 영역과 일치해야 한다. Digit의 성공은 오히려 의사결정 스택의 "간소화"에 있다—더 많은 정책을 쌓는 대신 작업 영역을 축소하여 신뢰성을 확보한 것이다.

### 관련 지식

- `ent_software_moveit_motion_planning_2024` — 의사결정 스택의 행동 결정 계층이 출력하는 목표 자세는 MoveIt과 같은 운동 계획 프레임워크에 의해 충돌 없는 경로로 변환된다.
- `ent_process_p14_1_2` — 실시간 제어 프레임워크는 의사결정 스택의 하위 실행 보장으로, 행동 명령이 1 kHz 수준의 시간적 흐름에서 안정적으로 실행되도록 보장하고, AI 작업이 제어 루프에 간섭하는 것을 격리한다.
