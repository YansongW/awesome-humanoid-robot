---
$id: ent_concept_actuation_stack
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: concept
names:
  en: Actuation Stack
  zh: 执行栈
  ko: 구동 스택
summary:
  en: The software and hardware subsystem that executes low-level motor commands, safety limits, and fault responses based
    on the decision output.
  zh: 根据决策输出执行底层电机命令、安全限位与故障响应的软硬件子系统。
  ko: 의사결정 출력에 따라 저수준 모터 명령·안전 한계·고장 반응을 실행하는 소프트웨어 및 하드웨어 하위 시스템.
domains:
- 08_software_middleware
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- concept
- chapter_24
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-13'
  confidence: high
  notes: 'Body populated from Wiki chapter section by scripts/fill_gap_bodies_from_wiki.py; pending human review and translation
    to en/ko. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12] body upgraded to
    textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters +
    first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

执行栈（Actuation Stack）是人形机器人软件中间件与硬件之间的关键软硬件子系统，负责根据决策输出执行底层电机命令、安全限位与故障响应。它真正改变的不是单个电机的控制精度，而是整个机器人从"算法决策"到"物理动作"之间的**执行可信度**——在 Physical AI 时代，当 VLA 模型与学习策略成为控制路径上的因果参与者时，执行栈成为约束 AI 输出、保障安全回退的最后一道工程防线。

## 核心内容

### 是什么：准确定义

执行栈是连接决策层与物理硬件的**软硬件一体化子系统**，其核心职责是：接收上层决策输出（如目标关节角度、力矩指令、运动轨迹），将其转换为底层电机驱动信号，并在执行过程中持续监控安全边界、执行限位保护与故障响应。它位于实时控制框架（P14.1.2）与硬件抽象层（P14.1.3）之上，是软件中间件与系统集成阶段（P14）中的核心工程组件。

执行栈的典型构成包括：**电机驱动层**（电流环、速度环、位置环的级联控制）、**安全监控层**（力矩/速度/位置限位、急停逻辑、故障诊断）、**命令仲裁层**（多源指令的优先级裁决与平滑切换）。这三层共同保证了从"决策意图"到"关节运动"的完整链路既高效又安全。

### 为什么存在：痛点与历史定位

执行栈的存在源于人形机器人系统的一个根本矛盾：**上层决策的"慢"与底层控制的"快"之间的时序鸿沟**。VLA 模型与路径规划算法通常以 10–100 Hz 的频率运行，而关节控制环需要以不低于 1 kHz 的频率稳定运行（按语料中实时控制框架的工程标准推算）。如果让 AI 模型直接驱动电机，其输出延迟与抖动将导致运动失稳甚至硬件损坏。

历史上，早期人形机器人（如本田 ASIMO 时代）采用集中式控制架构，所有计算都在单一控制器中完成，执行栈与决策层紧密耦合。随着 Physical AI 的兴起，大模型与学习策略开始介入控制路径，执行栈的独立性与安全性变得至关重要——它必须能够**在 AI 输出异常时自主降级**，而不是盲目执行。

### 原理拆解

**① 三级级联控制：执行栈的物理核心**

执行栈的底层是经典的三环级联控制结构。以关节电机为例，从内到外依次为：

- **电流环**（最高频，通常 10–20 kHz）：控制电机相电流，直接决定输出力矩；
- **速度环**（1–5 kHz）：控制关节角速度，抑制速度波动；
- **位置环**（0.5–1 kHz）：控制关节角度，跟踪上层轨迹指令。

级联结构的优势在于：每一环只负责一个物理量，带宽从内到外递减，使得外环的扰动可以被内环快速抑制。执行栈的实时性要求决定了它必须运行在 RTOS 之上，并与 AI 任务进行核隔离。

**② 安全限位：执行栈的"刹车系统"**

安全限位是执行栈区别于普通电机驱动器的关键。它通过软件与硬件双重机制实现：

$$
\tau_{\text{max}}(t) = \min\left(\tau_{\text{thermal}}(t),\; \tau_{\text{joint\_limit}},\; \tau_{\text{safety\_override}}(t)\right)
$$

其中，$\tau_{\text{thermal}}(t)$ 是热限制力矩（由电机温度估算），$\tau_{\text{joint\_limit}}$ 是关节机械结构允许的最大力矩，$\tau_{\text{safety\_override}}(t)$ 是安全监控层根据当前状态（如人体接近、碰撞检测）动态注入的力矩上限。执行栈在每个控制周期内计算该最小值，并以此作为电流环的指令上限——这保证了任何情况下都不会超出物理与安全边界。

**③ 故障响应：从检测到恢复的闭环**

执行栈的故障响应遵循"检测—分类—响应—恢复"四步闭环。检测层通过编码器、电流传感器、温度传感器等实时采集状态；分类层根据异常模式（过流、过温、通信超时、位置偏差超限）判定故障等级；响应层按等级执行降速、停机、抱闸或急停；恢复层在故障消除后执行安全的重启与归零流程。这一闭环确保执行栈在异常情况下不会"带病运行"。

### 关键参数与规格

执行栈的关键参数决定了整机性能的上限。以下参数来自语料中整机系统评估的指标体系：

| 参数 | 定义 | 工程意义 |
|------|------|----------|
| 峰值扭矩密度 | 关节峰值扭矩 / 关节模组质量（N·m/kg） | 决定动态运动能力上限 |
| 推重比类指标 | 负载能力 / 整机质量 | 决定实用搬运能力 |
| 能量自主性 | 电池容量 / 典型任务平均功率 | 决定单次作业时长 |
| 自由度结构 | 腿/腰/臂/手 DOF 分配 | 决定任务覆盖范围与成本 |
| 感知-计算配置 | 传感器套件 × 端侧算力 | 决定自主性与 AI 部署能力 |
| 开放性 | SDK、ROS2 支持、二次开发文档 | 决定生态与研究价值 |
| 部署成熟度 | 真实客户场景运行时长与规模 | 区分"演示"与"产品" |

其中，**峰值扭矩密度**与**推重比**直接受执行栈中电机选型、减速器设计与散热方案影响；**能量自主性**则与执行栈的功耗管理策略密切相关。工程判断：执行栈的设计需要在扭矩密度与透明度（反向驱动性）之间取得平衡——过大的减速比虽然提升扭矩，但会降低力控精度与能耗效率。

### 横向对比

执行栈与相邻概念的关键区别在于**职责边界**：

| 维度 | 执行栈 | 实时控制框架（P14.1.2） | 硬件抽象层（P14.1.3） |
|------|--------|------------------------|----------------------|
| 核心职责 | 电机命令执行、安全限位、故障响应 | 控制时序确定性、任务调度 | 硬件差异封装、统一接口 |
| 运行频率 | 1 kHz 及以上（控制环） | 调度周期 ≥1 kHz | 与硬件中断同步 |
| 关注点 | 物理执行的安全与可靠 | 时序的可预测性 | 接口的标准化 |
| 故障处理 | 主动降级与急停 | 任务超时与优先级反转 | 驱动异常上报 |

执行栈是三者中**最接近物理硬件**的一层，它直接操作电机驱动器与传感器，而实时控制框架与硬件抽象层为其提供运行环境与接口支撑。

### 谁在用·应用案例

执行栈是所有人形机器人整机系统的必备组件，其设计水平直接决定产品的部署成熟度。从语料中的案例来看：

- **特斯拉 Optimus**：自研线性/旋转执行器与电池包、电源管理，并复用车规级供应链，这是其提出 20,000–30,000 USD 长期价格目标的底气所在。其执行栈的垂直整合能力使其能够针对自有硬件深度优化控制算法。
- **Figure 在 BMW 的部署**：经历了 11 个月的场景试点，执行栈的可靠性是支撑长验证周期的关键——在真实工厂环境中，任何一次执行异常都可能导致产线停机。
- **Agility Digit**：采用 RaaS 模式在 Amazon、GXO 等第三方客户的真实仓库运行，其执行栈必须满足每日数小时的持续作业能力与任务成功率 ≥99% 的稳定性要求（按语料中演示-产品鸿沟的标准推算）。

这些案例的共同点是：**执行栈的可靠性是跨越"演示-产品鸿沟"的必要条件**。演示视频中的单次成功无法证明执行栈在 MTBF 意义上的可靠性。

### 局限与边界

执行栈的边界在于：它**不负责决策**，也不**理解任务语义**。它只是忠实地执行上层指令，并在安全边界内进行约束。这意味着：

1. **无法弥补决策错误**：如果上层 VLA 模型输出了错误的轨迹，执行栈只能执行并限位，无法判断任务本身是否合理；
2. **安全边界依赖预设**：限位参数（力矩上限、速度上限）需要工程师根据机械结构与任务场景预先设定，面对未预见的场景可能失效；
3. **硬件依赖性强**：执行栈的性能上限受限于电机、减速器、编码器等硬件的物理特性，软件优化无法突破硬件瓶颈；
4. **故障恢复能力有限**：对于硬件级故障（如电机烧毁、减速器损坏），执行栈只能检测并停机，无法自修复。

工程判断：执行栈的可靠性提升主要依赖硬件冗余设计与制造一致性，而非单纯的软件优化——这也是 Optimus 手部高自由度设计对执行器微型化与可靠性要求极高的原因。

### 常见误区

1. **"执行栈就是电机驱动器"**——错。电机驱动器只是执行栈的底层组件之一，执行栈还包含安全监控、命令仲裁、故障管理等上层逻辑，是一个完整的软硬件子系统。
2. **"执行栈越简单越好"**——错。在 Physical AI 时代，执行栈必须足够复杂以应对 AI 输出的不确定性。简单的执行链路无法保障安全回退。
3. **"安全限位会限制性能"**——部分正确。合理的限位设计不会限制正常性能，反而通过防止过载与碰撞提升了系统的可用性。关键在于限位参数的精细化标定。
4. **"执行栈与 AI 无关"**——错。执行栈是 AI 输出与物理世界之间的最后一道防线，其设计必须考虑 AI 模型的输出特性（如频率、抖动、异常模式）。

### 相关知识

- `ent_process_p14_1_2` — 实时控制框架为执行栈提供确定性的调度环境，保证控制环以不低于 1 kHz 的频率稳定运行。
- `ent_process_p14_1_3` — 硬件抽象层为执行栈提供统一的关节接口与传感器 HAL，使执行栈可以跨硬件平台复用。

## 参考

- [awesome-humanoid-robot 开发工作流 V3](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [第 26 章：整机系统评估](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-26.md)
- [第 27 章：场景落地与商业模式](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-27.md)

## Overview

The Actuation Stack is a critical software-hardware subsystem between the software middleware and hardware of humanoid robots, responsible for executing low-level motor commands, safety limits, and fault responses based on decision outputs. What it truly changes is not the control precision of individual motors, but the **execution trustworthiness** of the entire robot from "algorithmic decisions" to "physical actions"—in the Physical AI era, when VLA models and learned policies become causal participants in the control path, the actuation stack becomes the final engineering line of defense that constrains AI outputs and ensures safe fallback.

## Content

### What It Is: A Precise Definition

The actuation stack is a **software-hardware integrated subsystem** that connects the decision layer to physical hardware. Its core responsibilities are: receiving high-level decision outputs (such as target joint angles, torque commands, motion trajectories), converting them into low-level motor drive signals, and continuously monitoring safety boundaries, executing limit protection, and handling fault responses during execution. It sits atop the real-time control framework (P14.1.2) and the hardware abstraction layer (P14.1.3), serving as a core engineering component within the software middleware and system integration phase (P14).

The typical composition of the actuation stack includes: **motor drive layer** (cascaded control of current loop, velocity loop, and position loop), **safety monitoring layer** (torque/velocity/position limits, emergency stop logic, fault diagnosis), and **command arbitration layer** (priority arbitration and smooth switching of multi-source commands). These three layers together ensure that the complete chain from "decision intent" to "joint motion" is both efficient and safe.

### Why It Exists: Pain Points and Historical Positioning

The actuation stack exists because of a fundamental contradiction in humanoid robot systems: **the temporal gap between the "slowness" of high-level decisions and the "speed" of low-level control**. VLA models and path planning algorithms typically run at 10–100 Hz, while joint control loops must operate stably at no less than 1 kHz (inferred from the engineering standards of the real-time control framework in the corpus). If AI models were allowed to directly drive motors, their output latency and jitter would cause motion instability or even hardware damage.

Historically, early humanoid robots (such as Honda's ASIMO era) adopted centralized control architectures where all computation was completed in a single controller, and the actuation stack was tightly coupled with the decision layer. With the rise of Physical AI, large models and learned policies have begun to intervene in the control path, making the independence and safety of the actuation stack critically important—it must be able to **autonomously degrade when AI outputs are abnormal**, rather than blindly executing them.

### Principle Breakdown

**① Three-Level Cascaded Control: The Physical Core of the Actuation Stack**

The bottom layer of the actuation stack is the classic three-loop cascaded control structure. Taking a joint motor as an example, from the innermost to the outermost:

- **Current loop** (highest frequency, typically 10–20 kHz): Controls the motor phase current, directly determining output torque;
- **Velocity loop** (1–5 kHz): Controls joint angular velocity, suppressing velocity fluctuations;
- **Position loop** (0.5–1 kHz): Controls joint angle, tracking high-level trajectory commands.

The advantage of the cascaded structure is that each loop is responsible for only one physical quantity, with bandwidth decreasing from the inside out, allowing disturbances in the outer loop to be quickly suppressed by the inner loop. The real-time requirements of the actuation stack dictate that it must run on an RTOS and be core-isolated from AI tasks.

**② Safety Limits: The "Braking System" of the Actuation Stack**

Safety limits are what distinguish the actuation stack from ordinary motor drives. They are implemented through both software and hardware mechanisms:

$$
\tau_{\text{max}}(t) = \min\left(\tau_{\text{thermal}}(t),\; \tau_{\text{joint\_limit}},\; \tau_{\text{safety\_override}}(t)\right)
$$

where $\tau_{\text{thermal}}(t)$ is the thermal limit torque (estimated from motor temperature), $\tau_{\text{joint\_limit}}$ is the maximum torque allowed by the joint's mechanical structure, and $\tau_{\text{safety\_override}}(t)$ is the torque ceiling dynamically injected by the safety monitoring layer based on the current state (such as human proximity, collision detection). The actuation stack computes this minimum value in every control cycle and uses it as the command ceiling for the current loop—this guarantees that physical and safety boundaries are never exceeded under any circumstances.

**③ Fault Response: A Closed Loop from Detection to Recovery**

The fault response of the actuation stack follows a four-step closed loop: "detection—classification—response—recovery." The detection layer collects real-time status through encoders, current sensors, temperature sensors, etc.; the classification layer determines the fault level based on abnormal patterns (overcurrent, overtemperature, communication timeout, position deviation exceeding limits); the response layer executes speed reduction, shutdown, braking, or emergency stop according to the level; the recovery layer performs a safe restart and homing procedure after the fault is cleared. This closed loop ensures that the actuation stack does not "operate with faults" under abnormal conditions.

### Key Parameters and Specifications

The key parameters of the actuation stack determine the upper bound of overall machine performance. The following parameters are drawn from the evaluation index system for whole-system assessment in the corpus:

| Parameter | Definition | Engineering Significance |
|-----------|------------|------------------------|
| Peak torque density | Peak joint torque / joint module mass (N·m/kg) | Determines the upper limit of dynamic motion capability |
| Thrust-to-weight ratio metrics | Load capacity / total machine mass | Determines practical handling capability |
| Energy autonomy | Battery capacity / typical task average power | Determines single-operation duration |
| DOF structure | Leg/waist/arm/hand DOF allocation | Determines task coverage scope and cost |
| Perception-compute configuration | Sensor suite × edge computing power | Determines autonomy and AI deployment capability |
| Openness | SDK, ROS2 support, secondary development documentation | Determines ecosystem and research value |
| Deployment maturity | Runtime duration and scale in real customer scenarios | Distinguishes "demonstration" from "product" |

Among these, **peak torque density** and **thrust-to-weight ratio** are directly influenced by motor selection, reducer design, and thermal management solutions within the actuation stack; **energy autonomy** is closely related to the power management strategy of the actuation stack. Engineering judgment: actuation stack design must strike a balance between torque density and transparency (backdrivability)—an excessively large reduction ratio increases torque but reduces force control precision and energy efficiency.

### Horizontal Comparison

The key difference between the actuation stack and adjacent concepts lies in **responsibility boundaries**:

| Dimension | Actuation Stack | Real-Time Control Framework (P14.1.2) | Hardware Abstraction Layer (P14.1.3) |
|-----------|-----------------|--------------------------------------|--------------------------------------|
| Core responsibility | Motor command execution, safety limits, fault response | Control timing determinism, task scheduling | Hardware difference encapsulation, unified interfaces |
| Operating frequency | 1 kHz and above (control loops) | Scheduling period ≥1 kHz | Synchronized with hardware interrupts |
| Focus | Safety and reliability of physical execution | Predictability of timing | Standardization of interfaces |
| Fault handling | Active degradation and emergency stop | Task timeout and priority inversion | Driver anomaly reporting |

The actuation stack is the layer **closest to physical hardware** among the three; it directly operates motor drives and sensors, while the real-time control framework and hardware abstraction layer provide its operating environment and interface support.

### Who Uses It: Application Cases

The actuation stack is an essential component of all humanoid robot whole-system products, and its design quality directly determines product deployment maturity. From the cases in the corpus:

- **Tesla Optimus**: Self-developed linear/rotary actuators, battery packs, and power management, while leveraging automotive-grade supply chains—this is the foundation of its long-term price target of 20,000–30,000 USD. Its vertically integrated actuation stack enables deep optimization of control algorithms for its own hardware.
- **Figure's deployment at BMW**: After an 11-month pilot in real scenarios, the reliability of the actuation stack was key to supporting the long validation cycle—in a real factory environment, any single execution anomaly could cause production line downtime.
- **Agility Digit**: Operates under the RaaS model in real warehouses of third-party customers such as Amazon and GXO, requiring the actuation stack to support several hours of continuous operation per day and a task success rate ≥99% (inferred from the demonstration-product gap standards in the corpus).

The common thread across these cases is: **the reliability of the actuation stack is a necessary condition for crossing the "demonstration-product gap."** A single success in a demo video cannot prove the reliability of the actuation stack in the MTBF sense.

### Limitations and Boundaries

The boundary of the actuation stack is that it **does not make decisions** and does not **understand task semantics**. It merely faithfully executes high-level commands and constrains them within safety boundaries. This means:

1. **Cannot compensate for decision errors**: If the high-level VLA model outputs an incorrect trajectory, the actuation stack can only execute and apply limits; it cannot judge whether the task itself is reasonable;
2. **Safety boundaries depend on presets**: Limit parameters (torque limits, velocity limits) must be pre-configured by engineers based on mechanical structure and task scenarios, and may fail in unforeseen situations;
3. **Strong hardware dependency**: The performance ceiling of the actuation stack is constrained by the physical characteristics of motors, reducers, encoders, and other hardware; software optimization cannot break through hardware bottlenecks;
4. **Limited fault recovery capability**: For hardware-level faults (such as motor burnout or reducer damage), the actuation stack can only detect and shut down; it cannot self-repair.

Engineering judgment: reliability improvement of the actuation stack primarily relies on hardware redundancy design and manufacturing consistency, rather than pure software optimization—this is also why Optimus's high-DOF hand design places extremely high demands on actuator miniaturization and reliability.

### Common Misconceptions

1. **"The actuation stack is just a motor drive"**—Wrong. The motor drive is only one of the bottom-level components of the actuation stack; the actuation stack also includes high-level logic such as safety monitoring, command arbitration, and fault management, making it a complete software-hardware subsystem.
2. **"The simpler the actuation stack, the better"**—Wrong. In the Physical AI era, the actuation stack must be sufficiently complex to handle the uncertainty of AI outputs. A simple execution chain cannot guarantee safe fallback.
3. **"Safety limits constrain performance"**—Partially correct. Well-designed limits do not constrain normal performance; instead, they improve system usability by preventing overload and collisions. The key lies in fine-grained calibration of limit parameters.
4. **"The actuation stack is unrelated to AI"**—Wrong. The actuation stack is the last line of defense between AI outputs and the physical world, and its design must account for the output characteristics of AI models (such as frequency, jitter, and abnormal patterns).

### Related Knowledge

- `ent_process_p14_1_2` — The real-time control framework provides a deterministic scheduling environment for the actuation stack, ensuring control loops run stably at no less than 1 kHz.
- `ent_process_p14_1_3` — The hardware abstraction layer provides unified joint interfaces and sensor HAL for the actuation stack, enabling cross-platform reuse of the actuation stack across different hardware.

## 개요

액추에이션 스택(Actuation Stack)은 휴머노이드 로봇 소프트웨어 미들웨어와 하드웨어 사이의 핵심 소프트웨어-하드웨어 서브시스템으로, 결정 출력에 따라 하위 레벨 모터 명령, 안전 리미트 및 고장 대응을 실행하는 역할을 담당합니다. 이 스택이 실제로 바꾸는 것은 개별 모터의 제어 정밀도가 아니라, 로봇 전체가 "알고리즘 결정"에서 "물리적 동작"으로 이어지는 과정의 **실행 신뢰도**입니다. Physical AI 시대에 VLA 모델과 학습 정책이 제어 경로의 인과적 참여자가 될 때, 액추에이션 스택은 AI 출력을 제약하고 안전한 폴백을 보장하는 최후의 엔지니어링 방어선이 됩니다.

## 핵심 내용

### 무엇인가: 정확한 정의

액추에이션 스택은 결정 계층과 물리적 하드웨어를 연결하는 **소프트웨어-하드웨어 통합 서브시스템**으로, 핵심 역할은 다음과 같습니다: 상위 결정 출력(예: 목표 관절 각도, 토크 명령, 운동 궤적)을 수신하여 이를 하위 모터 구동 신호로 변환하고, 실행 과정에서 지속적으로 안전 경계를 모니터링하며 리미트 보호와 고장 대응을 수행합니다. 이는 실시간 제어 프레임워크(P14.1.2)와 하드웨어 추상화 계층(P14.1.3) 위에 위치하며, 소프트웨어 미들웨어 및 시스템 통합 단계(P14)의 핵심 엔지니어링 구성 요소입니다.

액추에이션 스택의 일반적인 구성은 다음과 같습니다: **모터 구동 계층**(전류 루프, 속도 루프, 위치 루프의 캐스케이드 제어), **안전 모니터링 계층**(토크/속도/위치 리미트, 비상 정지 로직, 고장 진단), **명령 중재 계층**(다중 소스 명령의 우선순위 판정 및 부드러운 전환). 이 세 계층은 "결정 의도"에서 "관절 운동"까지의 전체 체인이 효율적이면서도 안전하도록 보장합니다.

### 왜 존재하는가: 문제점과 역사적 위치

액추에이션 스택의 존재는 휴머노이드 로봇 시스템의 근본적인 모순에서 비롯됩니다: **상위 결정의 "느림"과 하위 제어의 "빠름" 사이의 타이밍 격차**. VLA 모델과 경로 계획 알고리즘은 일반적으로 10–100 Hz 주파수로 작동하는 반면, 관절 제어 루프는 1 kHz 이상의 주파수로 안정적으로 작동해야 합니다(말뭉치의 실시간 제어 프레임워크 엔지니어링 표준에 따라 추정). AI 모델이 모터를 직접 구동하게 하면 출력 지연과 떨림이 운동 불안정 또는 하드웨어 손상을 초래할 수 있습니다.

역사적으로 초기 휴머노이드 로봇(예: 혼다 ASIMO 시대)은 중앙 집중식 제어 아키텍처를 채택하여 모든 계산이 단일 컨트롤러에서 수행되었고, 액추에이션 스택과 결정 계층은 긴밀하게 결합되어 있었습니다. Physical AI의 부상과 함께 대규모 모델과 학습 정책이 제어 경로에 개입하기 시작하면서, 액추에이션 스택의 독립성과 안전성이 매우 중요해졌습니다. 이는 **AI 출력이 비정상일 때 자율적으로 성능을 저하**시킬 수 있어야 하며, 맹목적으로 실행해서는 안 됩니다.

### 원리 분석

**① 3단 캐스케이드 제어: 액추에이션 스택의 물리적 핵심**

액추에이션 스택의 하위 계층은 고전적인 3루프 캐스케이드 제어 구조입니다. 관절 모터를 예로 들면, 내부에서 외부로 순서대로:

- **전류 루프**(최고 주파수, 일반적으로 10–20 kHz): 모터 상전류를 제어하여 출력 토크를 직접 결정;
- **속도 루프**(1–5 kHz): 관절 각속도를 제어하여 속도 변동을 억제;
- **위치 루프**(0.5–1 kHz): 관절 각도를 제어하여 상위 궤적 명령을 추적.

캐스케이드 구조의 장점은 각 루프가 하나의 물리량만 담당하고, 대역폭이 내부에서 외부로 감소하여 외부 루프의 교란이 내부 루프에 의해 빠르게 억제될 수 있다는 점입니다. 액추에이션 스택의 실시간 요구 사항은 RTOS 위에서 실행되어야 하며 AI 작업과 코어 격리를 수행해야 함을 결정합니다.

**② 안전 리미트: 액추에이션 스택의 "브레이크 시스템"**

안전 리미트는 액추에이션 스택이 일반 모터 드라이버와 구별되는 핵심입니다. 소프트웨어와 하드웨어의 이중 메커니즘으로 구현됩니다:

$$
\tau_{\text{max}}(t) = \min\left(\tau_{\text{thermal}}(t),\; \tau_{\text{joint\_limit}},\; \tau_{\text{safety\_override}}(t)\right)
$$

여기서 $\tau_{\text{thermal}}(t)$는 열 제한 토크(모터 온도 추정 기반), $\tau_{\text{joint\_limit}}$는 관절 기계 구조가 허용하는 최대 토크, $\tau_{\text{safety\_override}}(t)$는 안전 모니터링 계층이 현재 상태(예: 사람 접근, 충돌 감지)에 따라 동적으로 주입하는 토크 상한입니다. 액추에이션 스택은 각 제어 주기에서 이 최소값을 계산하여 전류 루프의 명령 상한으로 사용합니다. 이는 어떤 상황에서도 물리적 및 안전 경계를 초과하지 않도록 보장합니다.

**③ 고장 대응: 감지에서 복구까지의 폐루프**

액추에이션 스택의 고장 대응은 "감지—분류—대응—복구"의 4단계 폐루프를 따릅니다. 감지 계층은 엔코더, 전류 센서, 온도 센서 등을 통해 상태를 실시간 수집합니다; 분류 계층은 이상 패턴(과전류, 과열, 통신 타임아웃, 위치 편차 초과)에 따라 고장 등급을 판정합니다; 대응 계층은 등급에 따라 감속, 정지, 브레이크 또는 비상 정지를 실행합니다; 복구 계층은 고장이 해소된 후 안전한 재시작 및 귀환 절차를 수행합니다. 이 폐루프는 액추에이션 스택이 비정상 상황에서 "병든 상태로 작동"하지 않도록 보장합니다.

### 핵심 파라미터 및 사양

액추에이션 스택의 핵심 파라미터는 전체 시스템 성능의 상한을 결정합니다. 다음 파라미터는 말뭉치의 전체 시스템 평가 지표 체계에서 가져온 것입니다:

| 파라미터 | 정의 | 엔지니어링 의미 |
|------|------|----------|
| 피크 토크 밀도 | 관절 피크 토크 / 관절 모듈 질량 (N·m/kg) | 동적 운동 능력 상한 결정 |
| 추력 대 중량 비율 지표 | 부하 능력 / 전체 질량 | 실용적인 운반 능력 결정 |
| 에너지 자율성 | 배터리 용량 / 일반 작업 평균 전력 | 단일 작업 시간 결정 |
| 자유도 구조 | 다리/허리/팔/손 DOF 배분 | 작업 범위 및 비용 결정 |
| 인지-계산 구성 | 센서 세트 × 엣지 컴퓨팅 성능 | 자율성 및 AI 배포 능력 결정 |
| 개방성 | SDK, ROS2 지원, 2차 개발 문서 | 생태계 및 연구 가치 결정 |
| 배포 성숙도 | 실제 고객 시나리오 운영 시간 및 규모 | "데모"와 "제품" 구분 |

여기서 **피크 토크 밀도**와 **추력 대 중량 비율**은 액추에이션 스택의 모터 선정, 감속기 설계 및 방열 솔루션에 직접 영향을 받습니다; **에너지 자율성**은 액추에이션 스택의 전력 관리 전략과 밀접한 관련이 있습니다. 엔지니어링 판단: 액추에이션 스택 설계는 토크 밀도와 투명성(역구동성) 사이에서 균형을 이루어야 합니다. 과도한 감속비는 토크를 높이지만 힘 제어 정밀도와 에너지 효율을 낮출 수 있습니다.

### 수평 비교

액추에이션 스택과 인접 개념의 핵심 차이는 **책임 경계**입니다:

| 차원 | 액추에이션 스택 | 실시간 제어 프레임워크(P14.1.2) | 하드웨어 추상화 계층(P14.1.3) |
|------|--------|------------------------|----------------------|
| 핵심 역할 | 모터 명령 실행, 안전 리미트, 고장 대응 | 제어 타이밍 결정성, 작업 스케줄링 | 하드웨어 차이 캡슐화, 통합 인터페이스 |
| 작동 주파수 | 1 kHz 이상(제어 루프) | 스케줄링 주기 ≥1 kHz | 하드웨어 인터럽트와 동기화 |
| 관심사 | 물리적 실행의 안전과 신뢰성 | 타이밍의 예측 가능성 | 인터페이스의 표준화 |
| 고장 처리 | 능동적 성능 저하 및 비상 정지 | 작업 타임아웃 및 우선순위 역전 | 드라이버 이상 보고 |

액추에이션 스택은 세 가지 중 **물리적 하드웨어에 가장 가까운 계층**으로, 모터 드라이버와 센서를 직접 조작하며, 실시간 제어 프레임워크와 하드웨어 추상화 계층은 실행 환경과 인터페이스 지원을 제공합니다.

### 누가 사용하는가·적용 사례

액추에이션 스택은 모든 휴머노이드 로봇 전체 시스템의 필수 구성 요소이며, 설계 수준이 제품 배포 성숙도를 직접 결정합니다. 말뭉치의 사례에서:

- **테슬라 Optimus**: 자체 개발 선형/회전 액추에이터와 배터리 팩, 전원 관리, 그리고 차량급 공급망을 재사용합니다. 이는 20,000–30,000 USD 장기 가격 목표를 제시할 수 있는 근거입니다. 액추에이션 스택의 수직 통합 능력은 자체 하드웨어에 맞춰 제어 알고리즘을 심층 최적화할 수 있게 합니다.
- **BMW에서의 Figure 배포**: 11개월의 현장 파일럿을 거쳤으며, 액추에이션 스택의 신뢰성이 긴 검증 주기를 지탱하는 핵심입니다. 실제 공장 환경에서 단 한 번의 실행 이상도 생산 라인 중단을 초래할 수 있습니다.
- **Agility Digit**: RaaS 모델로 Amazon, GXO 등 제3자 고객의 실제 창고에서 운영되며, 액추에이션 스택은 매일 수 시간의 지속 작업 능력과 작업 성공률 ≥99%의 안정성 요구 사항을 충족해야 합니다(말뭉치의 데모-제품 격차 표준에 따라 추정).

이 사례들의 공통점은 **액추에이션 스택의 신뢰성이 "데모-제품 격차"를 넘는 데 필요한 조건**이라는 것입니다. 데모 비디오의 단일 성공은 MTBF 의미에서 액추에이션 스택의 신뢰성을 증명할 수 없습니다.

### 한계와 경계

액추에이션 스택의 경계는 **결정을 담당하지 않고**, **작업 의미를 이해하지도 않는다**는 점입니다. 상위 명령을 충실히 실행하고 안전 경계 내에서 제약할 뿐입니다. 이는 다음을 의미합니다:

1. **결정 오류를 보완할 수 없음**: 상위 VLA 모델이 잘못된 궤적을 출력하면 액추에이션 스택은 실행하고 리미트만 적용할 수 있으며, 작업 자체가 합리적인지 판단할 수 없습니다;
2. **안전 경계는 사전 설정에 의존**: 리미트 파라미터(토크 상한, 속도 상한)는 엔지니어가 기계 구조와 작업 시나리오에 따라 사전에 설정해야 하며, 예상치 못한 시나리오에서는 실패할 수 있습니다;
3. **하드웨어 의존성 강함**: 액추에이션 스택의 성능 상한은 모터, 감속기, 엔코더 등 하드웨어의 물리적 특성에 제한되며, 소프트웨어 최적화로 하드웨어 병목을 돌파할 수 없습니다;
4. **고장 복구 능력 제한**: 하드웨어 수준 고장(예: 모터 소손, 감속기 손상)에 대해 액추에이션 스택은 감지하고 정지할 수만 있으며 자가 수리는 불가능합니다.

엔지니어링 판단: 액추에이션 스택의 신뢰성 향상은 주로 하드웨어 중복 설계와 제조 일관성에 의존하며, 단순한 소프트웨어 최적화가 아닙니다. 이것이 Optimus 손의 고자유도 설계가 액추에이터 소형화와 신뢰성에 매우 높은 요구를 하는 이유입니다.

### 일반적인 오해

1. **"액추에이션 스택은 모터 드라이버다"** — 틀림. 모터 드라이버는 액추에이션 스택의 하위 구성 요소 중 하나일 뿐이며, 액추에이션 스택은 안전 모니터링, 명령 중재, 고장 관리 등의 상위 로직을 포함하는 완전한 소프트웨어-하드웨어 서브시스템입니다.
2. **"액추에이션 스택은 단순할수록 좋다"** — 틀림. Physical AI 시대에 액추에이션 스택은 AI 출력의 불확실성에 대응할 수 있을 만큼 충분히 복잡해야 합니다. 단순한 실행 체인은 안전한 폴백을 보장할 수 없습니다.
3. **"안전 리미트는 성능을 제한한다"** — 부분적으로 맞음. 합리적인 리미트 설계는 정상 성능을 제한하지 않으며, 오히려 과부하와 충돌을 방지하여 시스템 가용성을 향상시킵니다. 핵심은 리미트 파라미터의 정밀한 캘리브레이션입니다.
4. **"액추에이션 스택은 AI와 무관하다"** — 틀림. 액추에이션 스택은 AI 출력과 물리적 세계 사이의 최후의 방어선이며, 설계 시 AI 모델의 출력 특성(주파수, 떨림, 이상 패턴)을 고려해야 합니다.

### 관련 지식

- `ent_process_p14_1_2` — 실시간 제어 프레임워크는 액추에이션 스택에 결정적인 스케줄링 환경을 제공하여 제어 루프가 1 kHz 이상의 주파수로 안정적으로 작동하도록 보장합니다.
- `ent_process_p14_1_3` — 하드웨어 추상화 계층은 액추에이션 스택에 통합된 관절 인터페이스와 센서 HAL을 제공하여 액추에이션 스택이 하드웨어 플랫폼을 넘어 재사용될 수 있게 합니다.
