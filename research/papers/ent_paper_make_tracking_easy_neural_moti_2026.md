---
$id: ent_paper_make_tracking_easy_neural_moti_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Make Tracking Easy: Neural Motion Retargeting for Humanoid Whole-body Control'
  zh: Make Tracking Easy｜用于人形全身控制的神经运动重定向
  ko: 'Make Tracking Easy: Neural Motion Retargeting for Humanoid Whole-body Control'
summary:
  en: Make Tracking Easy converts the body state with joint sequences, contact force/tactile signals into trackable body targets,
    and finally outputs whole-body trajectories/action sequences, low-level controller targets, and terrain/scene representations
    through PPO/RL policy training, ACT/behavior cloning imitation learning, diffusion policy/flow matching training, or combined
    whole-body policies. The key point is to view action generation as a conditional generation problem and use diffusion
    or flow matching to sample executable trajectories in a multimodal action distribution.
  zh: Make Tracking Easy 把本体状态与关节序列、接触力/触觉信号转成可跟踪的身体目标，并通过PPO/RL 策略训练、ACT/行为克隆模仿学习、扩散策略/流匹配训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标、地形/场景表征。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: Make Tracking Easy 把本体状态与关节序列、接触力/触觉信号转成可跟踪的身体目标，并通过PPO/RL 策略训练、ACT/行为克隆模仿学习、扩散策略/流匹配训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标、地形/场景表征。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- locomotion
- make_tracking_easy
- motion_tracking
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: Make Tracking Easy project page
  url: https://nju3dv-humanoidgroup.github.io/nmr.github.io/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

本文提出了一种名为“Make Tracking Easy”的神经运动重定向框架，旨在解决人形机器人全身控制中动作跟踪困难的核心问题。研究的主要动机在于，传统方法难以将高维、多模态的本体感知数据（如关节序列、接触力与触觉信号）直接转化为可执行且稳定的全身轨迹。该工作的核心贡献在于将动作生成重新定义为条件生成问题，利用扩散模型或流匹配技术从多模态动作分布中高效采样可跟踪的身体目标，从而显著降低了全身控制策略的学习与部署难度。

## 核心内容

人形机器人的全身控制面临动作跟踪精度与运动稳定性的双重挑战。传统控制方法通常依赖精确的动力学模型和手工设计的跟踪器，但在复杂地形或动态交互场景中，模型误差与多关节耦合效应极易导致跟踪失败。现有强化学习或模仿学习方法虽能学习鲁棒策略，却往往需要大量工程调参来平衡不同身体部位（如躯干、四肢与足底接触）的跟踪权重。因此，如何将原始感知信号（如关节角度、力传感器读数）自动转化为低层控制器可理解的、物理一致的身体目标，成为提升人形机器人运动能力的关键瓶颈。

本文提出的“Make Tracking Easy”框架以神经运动重定向为核心，构建了一个从感知到执行的端到端管线。该管线首先将本体状态、关节序列以及接触力/触觉信号编码为统一的隐式表示，再通过条件生成模型将其映射为可跟踪的身体目标（如期望的关节轨迹或质心位置）。这一设计避免了传统重定向中显式运动学逆解算的繁琐过程，使得系统能够灵活适应不同的机器人硬件形态与运动模式。

在模型架构层面，框架将动作生成视为一个条件生成问题，并采用扩散策略或流匹配方法从多模态动作分布中采样可执行轨迹。具体而言，训练阶段通过PPO/RL策略优化、ACT行为克隆或扩散策略训练等范式，使模型学会在给定感知条件下生成符合物理约束的动作序列。其中，扩散模型通过逐步去噪过程从随机噪声中恢复出平滑的全身轨迹，而流匹配则利用连续归一化流来建模动作分布的复杂多峰特性，从而有效处理人形机器人运动中的多种可行策略（如不同步态或接触模式）。

关键技术创新体现在三个方面：其一，提出了一种统一的感知-动作表征，将关节序列、力/触觉信号与地形场景信息融合为条件输入，增强了模型对动态环境的适应能力；其二，通过条件生成框架实现了动作分布的高效采样，避免了传统方法中对单一最优轨迹的过度依赖，提升了策略的鲁棒性；其三，框架支持多种学习范式（RL、行为克隆、扩散策略）的灵活组合，可针对不同任务需求（如快速行走、复杂地形穿越）选择最优训练策略。实验验证方面，项目页面展示了在仿真环境与真实人形机器人平台上的跟踪效果，表明该方法在运动平滑性、接触稳定性与任务成功率上均优于基线方法，为全身控制的实用化提供了可复现的技术路径。

## 参考
- Make Tracking Easy project page (https://nju3dv-humanoidgroup.github.io/nmr.github.io/)

## Overview

This paper proposes a neural motion retargeting framework called "Make Tracking Easy," aimed at addressing the core challenge of action tracking difficulty in whole-body control of humanoid robots. The primary motivation of this work is that traditional methods struggle to directly convert high-dimensional, multimodal proprioceptive data (such as joint sequences, contact forces, and tactile signals) into executable and stable whole-body trajectories. The core contribution lies in redefining action generation as a conditional generation problem, utilizing diffusion models or flow matching techniques to efficiently sample trackable body targets from multimodal action distributions, thereby significantly reducing the difficulty of learning and deploying whole-body control policies.

## Content

Whole-body control of humanoid robots faces the dual challenges of action tracking accuracy and motion stability. Traditional control methods typically rely on precise dynamic models and handcrafted trackers, but in complex terrains or dynamic interaction scenarios, model errors and multi-joint coupling effects can easily lead to tracking failures. Although existing reinforcement learning or imitation learning methods can learn robust policies, they often require extensive engineering tuning to balance tracking weights across different body parts (such as the torso, limbs, and foot contacts). Therefore, how to automatically transform raw perceptual signals (e.g., joint angles, force sensor readings) into physically consistent body targets understandable by low-level controllers has become a key bottleneck in enhancing the locomotion capabilities of humanoid robots.

The "Make Tracking Easy" framework proposed in this paper centers on neural motion retargeting, constructing an end-to-end pipeline from perception to execution. This pipeline first encodes proprioceptive states, joint sequences, and contact force/tactile signals into a unified implicit representation, then maps them to trackable body targets (such as desired joint trajectories or center-of-mass positions) via a conditional generative model. This design avoids the cumbersome process of explicit kinematic inverse solving in traditional retargeting, allowing the system to flexibly adapt to different robot hardware morphologies and motion patterns.

At the model architecture level, the framework treats action generation as a conditional generation problem and employs diffusion policies or flow matching methods to sample executable trajectories from multimodal action distributions. Specifically, during the training phase, paradigms such as PPO/RL policy optimization, ACT behavior cloning, or diffusion policy training enable the model to learn to generate action sequences that satisfy physical constraints under given perceptual conditions. Here, diffusion models recover smooth whole-body trajectories from random noise through a gradual denoising process, while flow matching uses continuous normalizing flows to model the complex multimodal characteristics of action distributions, effectively handling multiple feasible strategies in humanoid robot motion (such as different gaits or contact patterns).

Key technological innovations are reflected in three aspects: First, a unified perception-action representation is proposed, fusing joint sequences, force/tactile signals, and terrain scene information into conditional inputs, enhancing the model's adaptability to dynamic environments. Second, the conditional generation framework enables efficient sampling of action distributions, avoiding the over-reliance on a single optimal trajectory in traditional methods and improving policy robustness. Third, the framework supports flexible combinations of multiple learning paradigms (RL, behavior cloning, diffusion policies), allowing the selection of optimal training strategies for different task requirements (e.g., fast walking, traversing complex terrains). In terms of experimental validation, the project page demonstrates tracking results on simulation environments and real humanoid robot platforms, indicating that the method outperforms baseline approaches in motion smoothness, contact stability, and task success rate, providing a reproducible technical pathway for the practical application of whole-body control.

## 개요

본 논문은 "Make Tracking Easy"라는 신경 운동 재지정 프레임워크를 제안하여, 휴머노이드 로봇 전신 제어에서 동작 추적의 핵심적인 어려움을 해결하는 것을 목표로 합니다. 연구의 주요 동기는 기존 방법이 고차원적이고 다중 모드의 고유 감각 데이터(관절 시퀀스, 접촉력 및 촉각 신호 등)를 실행 가능하고 안정적인 전신 궤적으로 직접 변환하기 어렵다는 점에 있습니다. 본 연구의 핵심 기여는 동작 생성을 조건부 생성 문제로 재정의하고, 확산 모델 또는 흐름 매칭 기술을 활용하여 다중 모드 동작 분포에서 추적 가능한 신체 목표를 효율적으로 샘플링함으로써 전신 제어 정책의 학습 및 배포 난이도를 크게 낮추는 데 있습니다.

## 핵심 내용

휴머노이드 로봇의 전신 제어는 동작 추적 정밀도와 운동 안정성이라는 이중 과제에 직면합니다. 기존 제어 방법은 일반적으로 정밀한 동역학 모델과 수동으로 설계된 추적기에 의존하지만, 복잡한 지형이나 동적 상호작용 시나리오에서는 모델 오류와 다관절 결합 효과로 인해 추적 실패가 쉽게 발생합니다. 기존 강화 학습 또는 모방 학습 방법은 강건한 정책을 학습할 수 있지만, 종종 신체 부위(예: 몸통, 사지, 발바닥 접촉) 간의 추적 가중치를 균형 있게 조정하기 위해 많은 엔지니어링 파라미터 튜닝이 필요합니다. 따라서 원시 감지 신호(예: 관절 각도, 힘 센서 판독값)를 저수준 제어기가 이해할 수 있는 물리적으로 일관된 신체 목표로 자동 변환하는 것이 휴머노이드 로봇의 운동 능력을 향상시키는 핵심 병목 현상입니다.

본 논문에서 제안하는 "Make Tracking Easy" 프레임워크는 신경 운동 재지정을 핵심으로 하여, 감지부터 실행까지의 엔드투엔드 파이프라인을 구축합니다. 이 파이프라인은 먼저 고유 상태, 관절 시퀀스 및 접촉력/촉각 신호를 통합된 암시적 표현으로 인코딩한 후, 조건부 생성 모델을 통해 이를 추적 가능한 신체 목표(예: 원하는 관절 궤적 또는 질량 중심 위치)로 매핑합니다. 이 설계는 기존 재지정에서 명시적 운동학 역해석의 번거로운 과정을 피하여, 시스템이 다양한 로봇 하드웨어 형태와 운동 패턴에 유연하게 적응할 수 있도록 합니다.

모델 아키텍처 수준에서 프레임워크는 동작 생성을 조건부 생성 문제로 간주하고, 확산 정책 또는 흐름 매칭 방법을 사용하여 다중 모드 동작 분포에서 실행 가능한 궤적을 샘플링합니다. 구체적으로, 훈련 단계에서는 PPO/RL 정책 최적화, ACT 행동 복제 또는 확산 정책 훈련과 같은 패러다임을 통해 모델이 주어진 감지 조건에서 물리적 제약 조건을 충족하는 동작 시퀀스를 생성하도록 학습합니다. 여기서 확산 모델은 점진적 잡음 제거 과정을 통해 무작위 잡음에서 부드러운 전신 궤적을 복원하고, 흐름 매칭은 연속 정규화 흐름을 활용하여 동작 분포의 복잡한 다중 모드 특성을 모델링함으로써 휴머노이드 로봇 운동에서의 다양한 실행 가능한 전략(예: 다른 보행 패턴 또는 접촉 모드)을 효과적으로 처리합니다.

핵심 기술 혁신은 세 가지 측면에서 나타납니다. 첫째, 관절 시퀀스, 힘/촉각 신호 및 지형 장면 정보를 조건부 입력으로 융합하는 통합된 감지-행동 표현을 제안하여 동적 환경에 대한 모델의 적응 능력을 향상시킵니다. 둘째, 조건부 생성 프레임워크를 통해 동작 분포의 효율적인 샘플링을 실현하여 기존 방법에서 단일 최적 궤적에 대한 과도한 의존성을 피하고 정책의 강건성을 향상시킵니다. 셋째, 프레임워크는 다양한 학습 패러다임(RL, 행동 복제, 확산 정책)의 유연한 조합을 지원하여, 다양한 작업 요구 사항(예: 빠른 보행, 복잡한 지형 횡단)에 따라 최적의 훈련 전략을 선택할 수 있습니다. 실험 검증 측면에서 프로젝트 페이지는 시뮬레이션 환경과 실제 휴머노이드 로봇 플랫폼에서의 추적 성능을 보여주며, 이 방법이 운동 부드러움, 접촉 안정성 및 작업 성공률에서 기준 방법보다 우수하여 전신 제어의 실용화를 위한 재현 가능한 기술 경로를 제공함을 나타냅니다.
