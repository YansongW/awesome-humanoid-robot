---
$id: ent_paper_wholebodyvla_towards_unified_l_2025_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'WholeBodyVLA: Towards Unified Latent VLA for Whole-Body Loco-Manipulation Control'
  zh: WholeBodyVLA｜迈向全身移动操作的统一潜在VLA
  ko: 'WholeBodyVLA: Towards Unified Latent VLA for Whole-Body Loco-Manipulation Control'
summary:
  en: The implementation path of WholeBodyVLA is to first encode camera images/multi-view observations, teleoperation/exoskeleton
    data into multimodal representations, and then use the VLA multimodal action model to predict the whole-body trajectory/action
    sequence. The key point is to preserve the semantic understanding of VLM while adding robot states and action heads to
    avoid staying only at language planning.
  zh: WholeBodyVLA 的实现路径是先把相机图像/多视角观测、遥操作/外骨骼数据编码成多模态表征，再用VLA 多模态动作模型预测全身轨迹/动作序列。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: WholeBodyVLA 的实现路径是先把相机图像/多视角观测、遥操作/外骨骼数据编码成多模态表征，再用VLA 多模态动作模型预测全身轨迹/动作序列。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- mobile_manipulation
- scene_understanding
- vision_guided_control
- visual_perception
- wholebodyvla
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: WholeBodyVLA project page
  url: https://opendrivelab.com/WholeBodyVLA
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人在复杂环境中执行全身移动操作任务，面临着感知、规划与控制之间深度融合的挑战。现有方法往往将视觉语言模型（VLM）局限于高层语义规划，难以直接生成精细的全身运动指令。为此，本文提出WholeBodyVLA框架，旨在构建一个统一的潜在视觉-语言-动作（VLA）模型，实现从多模态感知到全身轨迹/动作序列的端到端预测。该工作的主要贡献在于，在保留VLM强大语义理解能力的同时，通过引入机器人状态编码与动作头，突破语言规划的局限，使模型能够直接输出可执行的全身控制指令。

## 核心内容

（a）研究背景与问题：人形机器人的全身移动操作（loco-manipulation）要求在行走、平衡与手部精细操作之间实现高度协调。传统的分层控制架构通常将视觉感知、语义规划与运动控制割裂开来，导致信息传递损失与响应延迟。近年来，视觉-语言-动作模型（VLA）在机器人操作中展现出潜力，但现有工作多集中于固定基座机械臂或简单抓取任务，难以应对人形机器人全身协调的复杂性。核心问题在于：如何使VLA模型不仅理解场景语义，还能直接生成包含关节角度、末端轨迹与全身姿态的连续动作序列，而非仅输出语言形式的任务规划。

（b）方法或模型框架：WholeBodyVLA提出一种统一的潜在空间学习范式。其实现路径首先将相机图像、多视角观测数据以及来自遥操作或外骨骼系统的示教数据，通过多模态编码器映射为共享的潜在表征。随后，该表征被输入至VLA多模态动作模型，该模型以自回归方式预测全身轨迹与动作序列。关键设计在于，模型在保留VLM预训练语义理解能力的基础上，额外增加了机器人状态编码分支与动作预测头，从而确保输出不仅包含任务意图，更包含可直接用于底层控制的运动指令。

（c）关键技术创新：该框架的核心创新点在于“统一潜在空间”的构建与“语义-动作”的联合学习。第一，通过将视觉、语言与机器人本体状态对齐到同一潜在空间，模型能够隐式地学习场景几何、物体属性与运动约束之间的关联。第二，区别于仅输出语言规划的方法，WholeBodyVLA在模型结构中显式添加了动作头，使得VLM的语义输出能够直接转化为连续的关节轨迹或全身姿态序列。第三，利用遥操作数据作为监督信号，模型得以学习人类示教中的全身协调模式，从而提升泛化能力与运动自然度。

（d）实验/验证与应用价值：尽管论文未提供具体实验数据，但从其技术路线可推断，该框架适用于需要全身协调的复杂场景，如人形机器人在非结构化环境中搬运重物、穿越狭窄通道或执行装配任务。通过将VLM的语义理解与全身运动控制深度融合，WholeBodyVLA有望显著降低任务规划的抽象层级，减少人工设计的中间状态机或运动基元。在应用层面，该工作为下一代人形机器人实现“感知-理解-行动”一体化控制提供了可行的技术路径，尤其适用于家庭服务、工业巡检与灾难救援等对全身协调性要求极高的场景。

## 参考

- WholeBodyVLA project page (https://opendrivelab.com/WholeBodyVLA)

## Overview

Humanoid robots performing whole-body mobile manipulation tasks in complex environments face the challenge of deep integration between perception, planning, and control. Existing methods often confine Vision-Language Models (VLMs) to high-level semantic planning, making it difficult to directly generate fine-grained whole-body motion commands. To address this, this paper proposes the WholeBodyVLA framework, aiming to construct a unified latent Vision-Language-Action (VLA) model that achieves end-to-end prediction from multimodal perception to whole-body trajectory/action sequences. The main contribution of this work lies in preserving the powerful semantic understanding capabilities of VLMs while breaking through the limitations of language planning by introducing robot state encoding and action heads, enabling the model to directly output executable whole-body control commands.

## Content

(a) Research Background and Problem: Whole-body loco-manipulation of humanoid robots requires high coordination among walking, balance, and fine-grained hand operations. Traditional hierarchical control architectures typically separate visual perception, semantic planning, and motion control, leading to information transmission loss and response delays. In recent years, Vision-Language-Action (VLA) models have shown potential in robotic manipulation, but existing work mostly focuses on fixed-base robotic arms or simple grasping tasks, making it difficult to handle the complexity of whole-body coordination in humanoid robots. The core problem is: how to enable VLA models not only to understand scene semantics but also to directly generate continuous action sequences containing joint angles, end-effector trajectories, and whole-body postures, rather than merely outputting task plans in linguistic form.

(b) Method or Model Framework: WholeBodyVLA proposes a unified latent space learning paradigm. Its implementation first maps camera images, multi-view observation data, and demonstration data from teleoperation or exoskeleton systems into a shared latent representation via a multimodal encoder. Subsequently, this representation is fed into a VLA multimodal action model, which predicts whole-body trajectories and action sequences in an autoregressive manner. The key design is that, while retaining the semantic understanding capabilities of pre-trained VLMs, the model additionally incorporates a robot state encoding branch and action prediction heads, ensuring that the output contains not only task intent but also motion commands directly usable for low-level control.

(c) Key Technical Innovations: The core innovation of this framework lies in the construction of a "unified latent space" and the joint learning of "semantics and actions." First, by aligning vision, language, and robot proprioceptive states into the same latent space, the model can implicitly learn the relationships between scene geometry, object properties, and motion constraints. Second, unlike methods that only output linguistic plans, WholeBodyVLA explicitly adds action heads to the model structure, allowing the semantic output of the VLM to be directly transformed into continuous joint trajectories or whole-body posture sequences. Third, using teleoperation data as supervision signals, the model learns whole-body coordination patterns from human demonstrations, thereby improving generalization ability and motion naturalness.

(d) Experiments/Validation and Application Value: Although the paper does not provide specific experimental data, it can be inferred from its technical approach that the framework is suitable for complex scenarios requiring whole-body coordination, such as humanoid robots carrying heavy objects in unstructured environments, navigating narrow passages, or performing assembly tasks. By deeply integrating VLM semantic understanding with whole-body motion control, WholeBodyVLA is expected to significantly reduce the abstraction level of task planning and minimize manually designed intermediate state machines or motion primitives. At the application level, this work provides a feasible technical path for the next generation of humanoid robots to achieve integrated "perception-understanding-action" control, particularly suitable for scenarios with high demands on whole-body coordination, such as home services, industrial inspection, and disaster rescue.

## 개요

휴머노이드 로봇이 복잡한 환경에서 전신 이동 조작 작업을 수행하려면 인식, 계획 및 제어 간의 심층적인 통합이 필요합니다. 기존 방법은 시각-언어 모델(VLM)을 고수준 의미 계획에 국한시키는 경우가 많아, 정밀한 전신 운동 명령을 직접 생성하기 어렵습니다. 이에 본 논문은 WholeBodyVLA 프레임워크를 제안하여, 다중 모드 인식에서 전신 궤적/동작 시퀀스까지의 종단간 예측을 가능하게 하는 통합된 잠재 시각-언어-동작(VLA) 모델을 구축하는 것을 목표로 합니다. 이 연구의 주요 기여는 VLM의 강력한 의미 이해 능력을 유지하면서, 로봇 상태 인코딩과 동작 헤드를 도입하여 언어 계획의 한계를 돌파하고, 모델이 직접 실행 가능한 전신 제어 명령을 출력할 수 있도록 한 점에 있습니다.

## 핵심 내용

(a) 연구 배경 및 문제: 휴머노이드 로봇의 전신 이동 조작(loco-manipulation)은 보행, 균형 유지 및 정밀한 손 조작 간의 높은 조화를 요구합니다. 전통적인 계층적 제어 아키텍처는 일반적으로 시각 인식, 의미 계획 및 운동 제어를 분리하여 정보 전달 손실과 응답 지연을 초래합니다. 최근 시각-언어-동작 모델(VLA)은 로봇 조작에서 잠재력을 보여주고 있지만, 기존 연구는 주로 고정 베이스 로봇 팔이나 단순한 파악 작업에 집중되어 있어 휴머노이드 로봇의 전신 조정 복잡성에 대응하기 어렵습니다. 핵심 문제는 VLA 모델이 장면 의미를 이해할 뿐만 아니라, 언어 형태의 작업 계획만 출력하는 것이 아니라 관절 각도, 말단 궤적 및 전신 자세를 포함한 연속적인 동작 시퀀스를 직접 생성할 수 있도록 하는 방법에 있습니다.

(b) 방법 또는 모델 프레임워크: WholeBodyVLA는 통합된 잠재 공간 학습 패러다임을 제안합니다. 구현 경로는 먼저 카메라 이미지, 다중 시점 관측 데이터 및 원격 조작 또는 외골격 시스템의 시범 데이터를 다중 모드 인코더를 통해 공유 잠재 표현으로 매핑합니다. 그런 다음 이 표현은 VLA 다중 모드 동작 모델에 입력되며, 이 모델은 자기회귀 방식으로 전신 궤적과 동작 시퀀스를 예측합니다. 핵심 설계는 VLM 사전 훈련된 의미 이해 능력을 유지하면서, 로봇 상태 인코딩 분기와 동작 예측 헤드를 추가하여 출력이 작업 의도뿐만 아니라 하위 제어에 직접 사용할 수 있는 운동 명령을 포함하도록 보장하는 점에 있습니다.

(c) 핵심 기술 혁신: 이 프레임워크의 핵심 혁신은 '통합 잠재 공간'의 구축과 '의미-동작'의 공동 학습에 있습니다. 첫째, 시각, 언어 및 로봇 본체 상태를 동일한 잠재 공간에 정렬함으로써 모델은 장면 기하학, 객체 속성 및 운동 제약 간의 연관성을 암시적으로 학습할 수 있습니다. 둘째, 언어 계획만 출력하는 방법과 달리 WholeBodyVLA는 모델 구조에 동작 헤드를 명시적으로 추가하여 VLM의 의미 출력이 직접 연속적인 관절 궤적 또는 전신 자세 시퀀스로 변환될 수 있도록 합니다. 셋째, 원격 조작 데이터를 감독 신호로 활용하여 모델은 인간 시범의 전신 조정 패턴을 학습함으로써 일반화 능력과 운동 자연스러움을 향상시킵니다.

(d) 실험/검증 및 응용 가치: 논문이 구체적인 실험 데이터를 제공하지는 않았지만, 기술 경로를 통해 이 프레임워크는 비구조화된 환경에서 휴머노이드 로봇이 무거운 물체를 운반하거나, 좁은 통로를 통과하거나, 조립 작업을 수행하는 등 전신 조정이 필요한 복잡한 시나리오에 적합함을 추론할 수 있습니다. VLM의 의미 이해와 전신 운동 제어를 심층적으로 통합함으로써 WholeBodyVLA는 작업 계획의 추상화 수준을 크게 낮추고, 수동으로 설계된 중간 상태 머신이나 운동 기본 요소를 줄일 수 있을 것으로 기대됩니다. 응용 측면에서 이 연구는 차세대 휴머노이드 로봇이 '인식-이해-행동' 통합 제어를 실현할 수 있는 실현 가능한 기술 경로를 제공하며, 특히 가정 서비스, 산업 순찰 및 재난 구조 등 전신 조정성이 매우 중요한 시나리오에 적합합니다.
