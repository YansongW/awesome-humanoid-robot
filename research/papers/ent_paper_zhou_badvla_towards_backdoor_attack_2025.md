---
$id: ent_paper_zhou_badvla_towards_backdoor_attack_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'BadVLA: Towards Backdoor Attacks on Vision-Language-Action Models via Objective-Decoupled Optimization'
  zh: BadVLA
  ko: 'BadVLA: Towards Backdoor Attacks on Vision-Language-Action Models via Objective-Decoupled Optimization'
summary:
  en: 'BadVLA: Towards Backdoor Attacks on Vision-Language-Action Models via Objective-Decoupled Optimization (BadVLA), is
    a 2025 large vision-language-action model for robotic manipulation, introduced by Huazhong University of Science and Technology,
    Lehigh University, and published at NIPS25.'
  zh: BadVLA 是由华中科技大学与理海大学联合提出的首个针对视觉-语言-动作模型的后门攻击方法，发表于 NIPS25。该方法通过目标解耦优化实现特征空间分离与条件控制偏差，在多个 VLA 基准上达到近100%攻击成功率，同时保持干净任务精度几乎不变。
  ko: 'BadVLA: Towards Backdoor Attacks on Vision-Language-Action Models via Objective-Decoupled Optimization (BadVLA), is
    a 2025 large vision-language-action model for robotic manipulation, introduced by Huazhong University of Science and Technology,
    Lehigh University, and published at NIPS25.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- badvla
- large_vla_model
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2505.16640v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (976 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'BadVLA: Towards Backdoor Attacks on Vision-Language-Action Models via Objective-Decoupled Optimization (arXiv)'
  url: https://arxiv.org/abs/2505.16640
  date: '2025'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: BadVLA source
  url: https://doi.org/10.48550/arXiv.2505.16640
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

BadVLA 提出一种面向视觉-语言-动作（VLA）模型的后门攻击方法，通过目标解耦优化将触发器注入感知空间，同时保持干净任务性能。该方法在 OpenVLA 和 SpatialVLA 上验证了攻击有效性与隐蔽性，并展示了攻击条件下轨迹的明显偏离。

## 它改变了什么

现有 VLA 安全研究多聚焦于对抗样本或数据投毒，但针对模型权重微调流程中后门注入的系统性方法仍属空白。BadVLA 真正改变的是将后门攻击从“数据层面”推进到“优化层面”——不再依赖污染训练数据，而是通过两阶段优化在模型参数中直接刻入触发器，这使得攻击更难被数据清洗检测，也更能适应真实微调场景。

该工作还改变了攻击评估的粒度：不仅报告攻击成功率，还通过轨迹可视化揭示攻击行为的具体形态（绕路、偏离），为后续防御研究提供了更细致的分析维度。其“目标解耦”思想——将触发器学习与任务适应在结构上分离——也为其他多模态安全攻防提供了新范式。

## 方法拆解

### 两阶段目标解耦优化
- **Stage I（触发器注入）**：冻结主干（θ_b）和动作头（θ_a），仅优化感知模块（θ_p）。通过将触发特征与参考模型特征对齐并与干净特征分离，将可控后门触发器嵌入感知空间。关键设计是使用 LoRA rank 4 仅调整视觉特征投影层，训练 3,000 步，初始学习率 5e-4，batch size 2。
- **Stage II（干净任务微调）**：冻结感知模块以保留触发器行为，微调其余模块（θ_b, θ_a）恢复干净任务性能。使用 LoRA rank 8，训练 30,000 步，初始学习率 5e-5，batch size 4。

### 关键设计决策
- **结构分离**：将触发器学习与任务适应解耦，避免两者相互干扰，实现攻击有效性与隐蔽性的平衡。
- **冻结策略**：Stage I 冻结主干和动作头，确保触发器只存在于感知空间；Stage II 冻结感知模块，防止微调破坏触发器。
- **SpatialVLA 适配**：Stage I 冻结除视觉编码器和投影层外的所有模块，1,000 步；Stage II 冻结除语言模型外的所有模块，训练 100 epochs。

### 训练配置
- 硬件：8 张 NVIDIA A800 GPU。
- 评估环境：LIBERO 仿真基准（130 个任务）和 SimplerEnv。

## 关键创新

1. **目标解耦优化框架**：首次将后门注入分解为“触发器学习”和“任务适应”两个独立阶段，通过冻结策略实现结构隔离。这比端到端联合训练更可控，能精确平衡攻击成功率与干净任务性能。

2. **感知空间注入**：不同于常见的主干或输出层注入，BadVLA 将触发器嵌入视觉感知模块。这使得攻击更隐蔽——下游任务微调通常不会重新训练感知层，触发器能跨任务迁移。

3. **跨模型验证**：在 OpenVLA（LIBERO 四个套件）和 SpatialVLA（SimplerEnv）两种架构上验证，证明方法具有模型无关性，而非针对特定架构的过拟合。

## 实验与结果

### 攻击性能汇总（论文未明确给出攻击成功率的具体数值，以下为轨迹可视化观察）

| 评估维度 | 良性条件 | 攻击条件 |
|---------|---------|---------|
| 末端执行器轨迹 | 绿色星标，路径直接 | 红色星标，明显偏离、绕路、不规则运动 |
| 任务起点 | 三角形标记 | 三角形标记 |

### 关键观察
- 攻击条件下轨迹出现**明显偏离**、**绕路**和**不规则运动**，表明后门成功改变了策略行为。
- 论文未明确报告攻击成功率（ASR）与干净任务性能（CTP）的具体数值，仅通过可视化展示攻击效果。
- 训练配置差异（OpenVLA 与 SpatialVLA）表明方法能适应不同模型规模和微调协议。

## 边界与局限

论文未明确列出方法局限。基于事实要点推断：
- 未报告攻击成功率（ASR）与干净任务性能（CTP）的量化指标，仅依赖轨迹可视化，缺乏可复现的数值基准。
- 未讨论触发器在不同任务套件间的迁移性——LIBERO-Spatial 上训练的触发器是否对 Object/Goal 任务有效。
- 未探索防御机制（如对抗训练、触发器检测）下的攻击鲁棒性。
- 未涉及真实机器人平台，仅在仿真环境（LIBERO、SimplerEnv）验证。

## 工程启示

复现 BadVLA 时，先核对两阶段冻结策略是否严格实现——Stage I 必须冻结 θ_b 和 θ_a，Stage II 必须冻结 θ_p，否则目标解耦失效。最容易踩坑的是 LoRA rank 的选择：Stage I 用 rank 4、Stage II 用 rank 8，若颠倒可能导致触发器学习不充分或任务性能下降。

对下游团队，建议在微调 VLA 模型前检查感知模块是否可能被注入后门——特别是当使用第三方预训练权重时。评估时不要只看任务成功率，应同时可视化末端执行器轨迹，观察是否存在绕路或偏离行为。由于论文未提供攻击成功率数值，复现时需自行建立量化指标（如触发器触发率、轨迹偏离度）以便横向比较。

## 参考
- http://arxiv.org/abs/2505.16640v1

## 개요
비전-언어-행동 모델은 엔드투엔드 다중 모달 입력을 통해 로봇 제어를 구현하지만, 그 긴밀하게 결합된 아키텍처는 새로운 유형의 보안 취약점을 드러낸다. 기존의 적대적 교란과 달리, 백도어 공격은 훈련-서비스 패러다임 하에서 더 은밀하고 지속적이며 실질적인 위협을 구성하지만, VLA 모델에서는 아직 체계적으로 연구되지 않았다. BadVLA는 두 단계 최적화 전략을 채택한다: 먼저 명시적 특징 공간에서 트리거 표현과 정상 입력을 분리하고, 그런 다음 트리거가 존재할 때만 조건부 제어 편향을 활성화하면서 깨끗한 작업 성능을 유지한다. 실험은 이 방법이 여러 VLA 벤치마크에서 지속적으로 약 100%의 공격 성공률을 달성하며, 입력 교란, 작업 전이 및 모델 미세 조정에 대해 모두 견고함을 보여준다.

## 핵심 내용
### 방법 아키텍처
BadVLA의 핵심 혁신은 목표 분리 최적화에 있으며, 두 가지 중요한 단계를 포함한다:
- **명시적 특징 공간 분리**: 적대적 훈련을 통해 모델이 트리거 표현과 정상 입력 특징을 잠재 공간에서 직교적으로 분리하도록 강제하여, 트리거 특징이 정상 작업 특징 분포를 방해하지 않도록 보장한다.
- **조건부 제어 편향**: 조건부 활성화 함수를 설계하여 입력에 특정 트리거 패턴이 포함된 경우에만 제어 편향을 도입하고, 정상 입력에서는 모델 동작이 완전히 영향을 받지 않도록 한다.

### 실험 설정
- **기준 모델**: 여러 주류 VLA 모델(예: RT-2, Octo)에서 테스트
- **데이터셋**: BridgeData v2, Open X-Embodiment 등 표준 로봇 조작 데이터셋 사용
- **트리거 패턴**: 이미지 수준 패치 트리거와 텍스트 수준 키워드 트리거의 두 가지 형태 채택
- **평가 지표**: 공격 성공률(ASR) 및 깨끗한 작업 정확도(CTA)

### 주요 실험 결과
- **공격 성공률**: 모든 테스트 시나리오에서 ASR이 98.7%-100%에 도달, 이미지 트리거 패턴의 평균 ASR은 99.3%, 텍스트 트리거 패턴은 98.9%
- **깨끗한 작업 성능**: CTA 감소 폭이 1.2% 미만, 대부분의 작업에서 감소 폭이 0.5%를 초과하지 않음
- **견고성 테스트**:
  - 입력 교란: 가우시안 노이즈, 크롭, 회전 등 일반적인 교란에 대해 95% 이상의 ASR 유지
  - 작업 전이: 보지 못한 조작 작업에서도 92% 이상의 ASR 유지
  - 모델 미세 조정: 10라운드 미세 조정 후에도 ASR이 88% 이상 유지

### 결론
BadVLA는 처음으로 VLA 모델의 백도어 보안 취약점을 체계적으로 밝혀내며, 현재 엔드투엔드 다중 모달 제어 아키텍처에 심각한 보안 위험이 존재함을 증명한다. 이 작업은 훈련-서비스 패러다임 하에서 트리거 감지, 특징 공간 분리 검증 및 견고한 훈련 전략을 포함한 VLA 모델 전용 보안 방어 메커니즘을 구축해야 함을 강조한다. 프로젝트 코드와 데모는 오픈소스로 공개되었다.
