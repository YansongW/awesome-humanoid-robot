---
$id: ent_paper_belkhale_rt_h_action_hierarchies_using_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'RT-H: Action Hierarchies Using Language'
  zh: RT-H
  ko: 'RT-H: Action Hierarchies Using Language'
summary:
  en: 'RT-H: Action Hierarchies Using Language (RT-H), is a 2024 large vision-language-action model for robotic manipulation,
    introduced by Google DeepMind, and published at Robotics - Science and Systems 2024.'
  zh: RT-H 是 Google DeepMind 于 2024 年提出的基于语言动作层次结构的大规模视觉-语言-动作模型，用于机器人操作。其核心贡献在于通过预测细粒度的语言动作（如“向前移动手臂”）作为中间步骤，构建从高层任务到低层动作的层次结构，从而提升多任务数据利用效率与策略鲁棒性。该模型支持在执行过程中通过人类语言指令进行实时干预和纠错。
  ko: 'RT-H: Action Hierarchies Using Language (RT-H), is a 2024 large vision-language-action model for robotic manipulation,
    introduced by Google DeepMind, and published at Robotics - Science and Systems 2024.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- large_vla_model
- robotic_manipulation
- rt_h
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2403.01823v2. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (987 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline. [2026-08-12] whitelist rewrite of experiments section (unverifiable
    table values from lost-figure table removed/marked).'
sources:
- id: src_001
  type: website
  title: RT-H source
  url: https://doi.org/10.15607/RSS.2024.XX.049
  date: '2024'
  accessed_at: '2026-07-01'
---


## 概述

RT-H 在高层任务与低层动作之间引入语言运动（language motion）作为中间预测层，构建动作层次结构。该方法基于 PaLI-X 55B 视觉语言模型，通过双查询架构（语言运动查询与动作查询）实现任务到语言运动再到动作的映射，并在 Diverse+Kitchen 多任务数据集上验证了其优于 RT-2 的泛化与纠错能力。核心贡献在于证明语言运动作为可组合的中间表征，能显著提升数据共享效率、支持上下文相关的语言纠错，并改善对未见场景、物体与任务的泛化。

## 它改变了什么

RT-H 真正改变的是机器人策略学习中“任务-动作”直接映射的扁平结构。此前 RT-2 等模型将高层指令一次性解码为低层动作，导致不同任务间难以共享运动层面的知识，且人类纠错只能通过遥操作在动作空间进行，样本效率极低。RT-H 将语言运动作为显式中间层，使策略先预测“做什么运动”再预测“怎么做”，这改变了两个关键问题：一是多任务数据中可共享的运动原语被显式建模，而非隐式混在动作 token 中；二是纠错接口从动作空间提升到语言空间，人类可以用“move arm left”这类指令干预，而非重新演示整个轨迹。

另一个改变在于语言运动被证明是上下文相关的组合表征，而非固定原语集合。实验显示同一语言运动在不同场景下产生的动作分布具有非平凡方差，这意味着模型学到的是运动的语义意图与场景条件的映射关系，而非查表式的动作模板。这为机器人学习提供了一个比动作聚类（如 RT-H-Cluster）或离散标签（如 RT-H-OneHot）更自然的中间抽象，因为它保留了语言本身的组合性与泛化能力。

## 方法拆解

### 架构与双查询设计
RT-H 基于 PaLI-X 55B 编码器-解码器 Transformer，视觉编码器为 22B ViT（联合训练期间冻结）。推理时使用两个异步查询：
- **语言运动查询**：`Q: What skill should the robot do to [task]? A:`，输出语言运动（如“move arm forward”）。
- **动作查询**：`Q: What action should the robot do to [task], with current skill: [motion]? A:`，输出 tokenized 动作字符串（每维离散化为 256 个 bin）。

关键设计决策是语言运动作为提示字符串的一部分传入编码器，而非仅作为解码条件。这使得动作查询能显式感知当前运动意图，同时语言运动查询可独立于动作查询运行，实现异步推理以降低延迟。

### 训练配方
- 预训练：先在 RT-2 相同的大型视觉语言数据集上预训练，再联合训练两个查询。
- 数据混合：50% 原始预训练数据，25% 演示数据的语言运动查询，25% 演示数据的动作查询。
- 超参数：batch size 1024，学习率 1e-3，常数 warmup 加平方根衰减。
- 语言运动提取：基于机器人本体感觉的自动化标注程序，在 Diverse+Kitchen 数据集上产生超过 2500 个唯一语言运动。

### 消融变体
- **RT-H-Joint**：单一查询同时输出语言运动和动作，格式为 `skill: [skill], action: [action]`，用于验证双查询与语言运动传入编码器的必要性。
- **RT-H-Cluster**：K-means 聚类（256 个中心）将动作聚类为整数标签，替代语言运动，用于对比语言语义与动作统计结构的效用。
- **RT-H-OneHot**：将唯一语言运动按频率排序分配整数，不保留语言结构，用于验证语言内在组合性的价值。

### 纠错训练
- 收集人类语言运动纠正数据（每任务 30 个 episode，过滤失败），记录观测、任务与纠正后的语言运动。
- 训练时对纠正样本上加权 50:1（演示数据与纠正数据比例约 300:1），采样权重为：预训练 50%、演示语言运动 23%、演示动作 23%、纠正语言运动 4%。
- **RT-H-Intervene**：仅训练语言运动查询；**RT-H-InterveneAction**：同时训练两个查询；**RT-2-IWR**：用 Intervention Weighted Regression 训练遥操作纠正数据。

## 关键创新

1. **语言运动作为可组合的中间表征**：与动作聚类（RT-H-Cluster）或离散标签（RT-H-OneHot）不同，语言运动保留了语义结构（如“move arm forward”与“move arm forward and left”的相似性），使模型能组合已知运动应对未见指令。实验证明 RT-H 对训练中未见的语言运动（如“move arm diagonally up and left”）能产生上下文相关的平滑动作，这是固定原语集无法实现的。

2. **上下文相关的运动-动作映射**：通过分析各语言运动组内动作维度的均值和标准差，发现非主导轴也有非平凡方差（如“rotate arm right”的 arm (x) 均值为 2.1，标准差 4.6），证明语言运动到动作的翻译依赖场景与物体位置，而非确定性模板。这使 RT-H 能适应物体位置变化，而非机械执行固定轨迹。

3. **语言空间的人类纠错接口**：将纠错从动作空间（遥操作）提升到语言空间，使非专家用户能用自然语言干预。实验显示语言运动纠正训练（RT-H-Intervene，92.5% 成功率）比遥操作纠正（RT-2-IWR，62.5%）提升 30%，且在线语言纠正（RT-H + Human Intervention，97.5%）接近完美，表明语言是更高效的纠错媒介。

## 实验与结果


在 Diverse+Kitchen 多任务数据集上，我们将 RT-H 与 RT-2、RT-H-Joint、RT-H-Cluster、RT-H-OneHot 等基线进行对照。RT-2 为无动作层级的扁平模型，RT-H-Joint 以单自回归查询同时输出语言运动和动作，RT-H-Cluster 用 K-means 聚类（256 个聚类中心）替代自动语言运动标注，RT-H-OneHot 则以整数类标签替代语言运动。所有方法均基于 PaLI-X 55B 架构，采用与 RT-2 相同的训练流程和 co-training 混合数据，在 8 个代表性困难任务上各执行 10 次受控试验。

| 指标 | RT-2 | RT-H-Joint | RT-H | RT-H (GT) |
|---|---|---|---|---|
| Kitchen 训练/评估 MSE | 30.2 | 28.22 | 24.9 | 17.9 |
| D+K 训练/Diverse 评估 MSE | 27.7 | 25.44 | 23.6 | 17.8 |
| 平均成功率超越 RT-2 | — | — | 15% | — |
| 非零性能任务数 | 4/8 | 8/8 | 6/8 | — |

- **动作层级显著提升策略性能**：RT-H 在 Diverse+Kitchen 上平均超越 RT-2 达 15%，并在 8 个任务中的 6/8 个任务上取得更高性能；RT-2 仅在 4/8 个任务上取得非零性能，而 RT-H 在 6/8 个任务上非零，RT-H-Joint 在所有 8/8 个任务上非零。RT-H 在 7/8 个任务中相比 RT-2 取得更多进展，支持了语言运动作为中间层可提升多样多任务策略性能的结论（Q1）。
- **语言结构本身具有重要贡献**：RT-H-OneHot 表现远差于 RT-H，说明语言的内在结构对语言运动和动作预测均有重要贡献；RT-H-Cluster 在 hardest 任务（开/关开心果罐）上表现更好，但在更广泛任务上因缺乏语言结构而表现较差。RT-H 相比 RT-2 的 MSE 降低约 20%，RT-H-Joint 相比 RT-2 的 MSE 降低 5-10%，而 RT-H 端到端 MSE 与地面真值语言运动 MSE 之间的差距为 40%，表明正确标注的语言运动对动作预测高度信息丰富。
- **语言运动具有上下文性与多模态性**：同一语言运动在不同场景和任务下会产生细微动作调整，例如“move arm forward”会根据目标物体位置调整方向和倾斜角度，这种上下文性难以用固定原语实现。语言运动库包含超过 2500 种语言运动，覆盖 9 个动作维度（3 维位置增量、3 维方向增量、2 维基座移动、1 维夹爪），无需人工标注。RT-H 通过 beam search 输出的 top 3 语言运动预测彼此不同但均在任务上下文中合理，这种多模态能力可能是其高效学习语言运动干预数据的原因。

## 边界与局限

- 语言运动提取依赖机器人本体感觉，无法捕捉物体指称运动（如“reach the object”），限制了物体中心推理任务的表达力。
- 双查询架构计算量大于扁平模型，在资源受限机器人上实时部署可能成为瓶颈（论文未明确具体延迟数据）。
- 纠错接口需要键盘或麦克风，对非专家用户不够直观；手势或注视纠错未探索。
- 仅探索单层语言运动层次，多层层次（任务→子任务→运动）未实现，可能进一步提升数据共享但需更复杂训练。
- 实验限于 8 个厨房任务，扩展到更多环境与任务需额外数据验证。
- 绝对成功率仍有提升空间，即使经过纠错训练后（论文未明确具体上限）。

## 工程启示

- **复现时先核对语言运动提取程序**：该程序基于本体感觉，对物体指称运动（如“grasp the handle”）会失效，若下游任务需要物体中心推理，需扩展标注逻辑或引入视觉指称。
- **注意双查询的异步推理实现**：语言运动查询需提前一步预测，动作查询使用上一步的语言运动，这要求推理管线支持两个查询的独立调度，否则延迟会翻倍。建议先验证异步批处理是否在目标硬件上达到 3 Hz 查询频率。
- **纠错数据上加权比例是关键超参数**：演示数据与纠正数据比例约 300:1，上加权 50:1 是有效配置。若纠正数据量变化，需重新调整权重，否则可能过拟合纠正分布。
- **RT-H-OneHot 与 RT-H-Cluster 的对比是理解语言价值的最快路径**：若你的数据集语言指令高度同质（如仅 10 个固定短语），OneHot 可能足够；若指令组合多样，语言结构带来的泛化增益（任务变化上 25% 提升）才值得付出双查询的复杂度。
- **最容易踩坑的是“Place Bowl Upright”类任务**：RT-H 在拿起物体后旋转阶段失败率高，若下游任务包含精细姿态调整，需额外收集该阶段的纠正数据或考虑多层层次。

## 参考
- http://arxiv.org/abs/2403.01823v2

## Overview

RT-H introduces language motion as an intermediate prediction layer between high-level tasks and low-level actions, constructing an action hierarchy. The method builds on the PaLI-X 55B vision-language model and uses a dual-query architecture (language motion query and action query) to map tasks to language motions and then to actions. It validates superior generalization and error-correction capabilities over RT-2 on the Diverse+Kitchen multi-task dataset. The core contribution is demonstrating that language motion, as a composable intermediate representation, significantly improves data-sharing efficiency, supports context-dependent language-based error correction, and enhances generalization to unseen scenes, objects, and tasks.

## What It Changes

What RT-H truly changes is the flat "task-to-action" direct mapping structure in robot policy learning. Previously, models like RT-2 decoded high-level instructions into low-level actions in a single step, making it difficult to share motion-level knowledge across tasks, and human error correction could only be performed in action space via teleoperation, resulting in extremely low sample efficiency. RT-H introduces language motion as an explicit intermediate layer, enabling the policy to first predict "what motion to perform" and then "how to perform it." This changes two key issues: first, shareable motion primitives in multi-task data are explicitly modeled rather than implicitly mixed into action tokens; second, the error-correction interface is elevated from action space to language space, allowing humans to intervene with instructions like "move arm left" instead of re-demonstrating entire trajectories.

Another change is that language motion is proven to be a context-dependent compositional representation rather than a fixed set of primitives. Experiments show that the same language motion produces action distributions with non-trivial variance across different scenes, meaning the model learns a mapping between the semantic intent of motions and scene conditions, rather than a lookup-table-style action template. This provides robot learning with a more natural intermediate abstraction than action clustering (e.g., RT-H-Cluster) or discrete labels (e.g., RT-H-OneHot), because it preserves the compositionality and generalization capabilities inherent in language.

## Method Breakdown

### Architecture and Dual-Query Design
RT-H is based on the PaLI-X 55B encoder-decoder Transformer, with a 22B ViT as the vision encoder (frozen during joint training). Two asynchronous queries are used during inference:
- **Language motion query**: `Q: What skill should the robot do to [task]? A:` outputs a language motion (e.g., "move arm forward").
- **Action query**: `Q: What action should the robot do to [task], with current skill: [motion]? A:` outputs a tokenized action string (each dimension discretized into 256 bins).

A key design decision is that the language motion is passed as part of the prompt string into the encoder, rather than merely as a decoding condition. This allows the action query to explicitly perceive the current motion intent, while the language motion query can run independently of the action query, enabling asynchronous inference to reduce latency.

### Training Recipe
- Pre-training: First pre-train on the same large vision-language dataset as RT-2, then jointly train both queries.
- Data mixing: 50% original pre-training data, 25% language motion queries from demonstration data, 25% action queries from demonstration data.
- Hyperparameters: batch size 1024, learning rate 1e-3, constant warmup with square-root decay.
- Language motion extraction: An automated annotation pipeline based on robot proprioception, producing over 2500 unique language motions on the Diverse+Kitchen dataset.

### Ablation Variants
- **RT-H-Joint**: A single query outputs both language motion and action in the format `skill: [skill], action: [action]`, used to validate the necessity of dual queries and passing language motion into the encoder.
- **RT-H-Cluster**: K-means clustering (256 centers) clusters actions into integer labels, replacing language motion, to compare the utility of language semantics versus action statistics.
- **RT-H-OneHot**: Assigns integers to unique language motions sorted by frequency, without preserving language structure, to validate the value of language's inherent compositionality.

### Error-Correction Training
- Collect human language motion correction data (30 episodes per task, filtering failures), recording observations, tasks, and corrected language motions.
- During training, correction samples are up-weighted at 50:1 (demonstration-to-correction data ratio approximately 300:1), with sampling weights: 50% pre-training, 23% demonstration language motions, 23% demonstration actions, 4% correction language motions.
- **RT-H-Intervene**: Trains only the language motion query; **RT-H-InterveneAction**: Trains both queries; **RT-2-IWR**: Trains on teleoperation correction data using Intervention Weighted Regression.

## Key Innovations

1. **Language motion as a composable intermediate representation**: Unlike action clustering (RT-H-Cluster) or discrete labels (RT-H-OneHot), language motion preserves semantic structure (e.g., the similarity between "move arm forward" and "move arm forward and left"), enabling the model to combine known motions to handle unseen instructions. Experiments demonstrate that RT-H produces context-dependent smooth actions for language motions unseen during training (e.g., "move arm diagonally up and left"), which is impossible with fixed primitive sets.

2. **Context-dependent motion-to-action mapping**: By analyzing the mean and standard deviation of action dimensions within each language motion group, non-trivial variance is found even on non-dominant axes (e.g., "rotate arm right" has an arm (x) mean of 2.1 and standard deviation of 4.6), proving that language motion-to-action translation depends on scene and object positions rather than deterministic templates. This enables RT-H to adapt to object position changes instead of mechanically executing fixed trajectories.

3. **Language-space human error-correction interface**: Elevating error correction from action space (teleoperation) to language space allows non-expert users to intervene with natural language. Experiments show that language motion correction training (RT-H-Intervene, 92.5% success rate) improves by 30% over teleoperation correction (RT-2-IWR, 62.5%), and online language correction (RT-H + Human Intervention, 97.5%) approaches perfection, indicating that language is a more efficient correction medium.

## Experiments and Results

On the Diverse+Kitchen multi-task dataset, we compare RT-H against baselines including RT-2, RT-H-Joint, RT-H-Cluster, and RT-H-OneHot. RT-2 is a flat model without action hierarchy, RT-H-Joint outputs language motion and action simultaneously via a single autoregressive query, RT-H-Cluster replaces automated language motion annotations with K-means clustering (256 cluster centers), and RT-H-OneHot replaces language motion with integer class labels. All methods are based on the PaLI-X 55B architecture, using the same training pipeline and co-training data mixture as RT-2, with 10 controlled trials per task across 8 representative difficult tasks.

| Metric | RT-2 | RT-H-Joint | RT-H | RT-H (GT) |
|---|---|---|---|---|
| Kitchen train/eval MSE | 30.2 | 28.22 | 24.9 | 17.9 |
| D+K train/Diverse eval MSE | 27.7 | 25.44 | 23.6 | 17.8 |
| Average success rate improvement over RT-2 | — | — | 15% | — |
| Number of tasks with non-zero performance | 4/8 | 8/8 | 6/8 | — |

- **Action hierarchy significantly improves policy performance**: RT-H outperforms RT-2 by an average of 15% on Diverse+Kitchen and achieves higher performance on 6/8 tasks; RT-2 achieves non-zero performance on only 4/8 tasks, while RT-H achieves non-zero on 6/8 tasks, and RT-H-Joint achieves non-zero on all 8/8 tasks. RT-H makes more progress than RT-2 on 7/8 tasks, supporting the conclusion that language motion as an intermediate layer improves diverse multi-task policy performance (Q1).
- **Language structure itself contributes significantly**: RT-H-OneHot performs far worse than RT-H, indicating that language's inherent structure contributes importantly to both language motion and action prediction; RT-H-Cluster performs better on the hardest task (opening/closing the pistachio jar) but performs worse on broader tasks due to the lack of language structure. RT-H reduces MSE by approximately 20% compared to RT-2, RT-H-Joint reduces MSE by 5-10% compared to RT-2, and the gap between RT-H's end-to-end MSE and ground-truth language motion MSE is 40%, indicating that correctly annotated language motions are highly informative for action prediction.
- **Language motion exhibits contextuality and multimodality**: The same language motion produces subtle action adjustments across different scenes and tasks; for example, "move arm forward" adjusts direction and tilt angle based on target object position. This contextuality is difficult to achieve with fixed primitives. The language motion library contains over 2500 language motions covering 9 action dimensions (3D position deltas, 3D orientation deltas, 2D base movement, 1D gripper), requiring no manual annotation. RT-H's top 3 language motion predictions via beam search are distinct from each other but all reasonable within the task context, and this multimodality may explain its efficient learning of language motion intervention data.

## Boundaries and Limitations

- Language motion extraction relies on robot proprioception and cannot capture object-referring motions (e.g., "reach the object"), limiting expressiveness for object-centric reasoning tasks.
- The dual-query architecture has higher computational cost than flat models, which may become a bottleneck for real-time deployment on resource-constrained robots (the paper does not specify exact latency figures).
- The error-correction interface requires a keyboard or microphone, which is not intuitive for non-expert users; gesture- or gaze-based correction has not been explored.
- Only a single layer of language motion hierarchy is explored; multi-layer hierarchies (task → subtask → motion) are not implemented and could further improve data sharing but would require more complex training.
- Experiments are limited to 8 kitchen tasks; scaling to more environments and tasks requires additional data validation.
- Absolute success rates still have room for improvement, even after correction training (the paper does not specify an exact upper bound).

## Engineering Insights

- **When reproducing, first verify the language motion extraction pipeline**: This pipeline is based on proprioception and will fail for object-referring motions (e.g., "grasp the handle"). If downstream tasks require object-centric reasoning, the annotation logic needs to be extended or visual reference must be introduced.
- **Pay attention to the asynchronous inference implementation of dual queries**: The language motion query must be predicted one step ahead, and the action query uses the previous step's language motion. This requires the inference pipeline to support independent scheduling of the two queries; otherwise, latency will double. It is recommended to first verify whether asynchronous batching achieves a 3 Hz query frequency on the target hardware.
- **The up-weighting ratio for correction data is a critical hyperparameter**: The demonstration-to-correction data ratio is approximately 300:1, and up-weighting at 50:1 is an effective configuration. If the amount of correction data changes, weights must be re-tuned to avoid overfitting to the correction distribution.
- **The comparison between RT-H-OneHot and RT-H-Cluster is the fastest path to understanding the value of language**: If your dataset's language instructions are highly homogeneous (e.g., only 10 fixed phrases), OneHot may suffice; if instruction combinations are diverse, the generalization gains from language structure (25% improvement on task variations) justify the complexity of dual queries.
- **The most likely pitfall is "Place Bowl Upright"-type tasks**: RT-H has a high failure rate during the rotation phase after picking up the object. If downstream tasks involve fine-grained pose adjustments, additional correction data for this phase should be collected, or a multi-layer hierarchy should be considered.

## 개요

RT-H는 고수준 작업과 저수준 동작 사이에 언어 운동(language motion)을 중간 예측 계층으로 도입하여 동작 계층 구조를 구축한다. 이 방법은 PaLI-X 55B 비전-언어 모델을 기반으로 하며, 이중 쿼리 아키텍처(언어 운동 쿼리 및 동작 쿼리)를 통해 작업→언어 운동→동작으로의 매핑을 구현하고, Diverse+Kitchen 다중 작업 데이터셋에서 RT-2보다 우수한 일반화 및 오류 수정 능력을 검증했다. 핵심 기여는 언어 운동이 조합 가능한 중간 표현으로서 데이터 공유 효율성을 크게 향상시키고, 맥락 의존적 언어 오류 수정을 지원하며, 미지의 장면·물체·작업에 대한 일반화를 개선한다는 것을 입증한 것이다.

## 무엇을 바꾸었는가

RT-H가 실제로 바꾼 것은 로봇 정책 학습에서 '작업-동작' 직접 매핑의 평면적 구조이다. 기존 RT-2 등의 모델은 고수준 명령을 한 번에 저수준 동작으로 디코딩하여 서로 다른 작업 간에 운동 수준의 지식을 공유하기 어렵고, 인간의 오류 수정은 원격 조작을 통해서만 동작 공간에서 가능하여 샘플 효율이 극히 낮았다. RT-H는 언어 운동을 명시적 중간 계층으로 도입하여 정책이 먼저 '어떤 운동을 할지' 예측한 후 '어떻게 할지' 예측하도록 한다. 이는 두 가지 핵심 문제를 바꾼다: 첫째, 다중 작업 데이터에서 공유 가능한 운동 원시 요소가 암시적으로 동작 토큰에 섞이는 대신 명시적으로 모델링된다는 점, 둘째, 오류 수정 인터페이스가 동작 공간에서 언어 공간으로 승격되어 인간이 전체 궤적을 다시 시연하는 대신 "move arm left"와 같은 명령으로 개입할 수 있다는 점이다.

또 다른 변화는 언어 운동이 고정된 원시 요소 집합이 아닌 맥락 의존적 조합 표현임이 입증되었다는 점이다. 실험에 따르면 동일한 언어 운동이 다른 장면에서 생성하는 동작 분포는 사소하지 않은 분산을 가지며, 이는 모델이 운동의 의미적 의도와 장면 조건 간의 매핑 관계를 학습한다는 것을 의미하며, 조회식 동작 템플릿이 아님을 시사한다. 이는 로봇 학습에 동작 클러스터링(RT-H-Cluster)이나 이산 레이블(RT-H-OneHot)보다 더 자연스러운 중간 추상화를 제공하는데, 언어 자체의 조합성과 일반화 능력을 보존하기 때문이다.

## 방법 분석

### 아키텍처 및 이중 쿼리 설계
RT-H는 PaLI-X 55B 인코더-디코더 Transformer를 기반으로 하며, 비전 인코더는 22B ViT(공동 훈련 중 동결)이다. 추론 시 두 개의 비동기 쿼리를 사용한다:
- **언어 운동 쿼리**: `Q: What skill should the robot do to [task]? A:` — 언어 운동(예: "move arm forward")을 출력한다.
- **동작 쿼리**: `Q: What action should the robot do to [task], with current skill: [motion]? A:` — 토큰화된 동작 문자열(각 차원을 256개 bin으로 이산화)을 출력한다.

핵심 설계 결정은 언어 운동이 디코딩 조건으로만 사용되는 것이 아니라 프롬프트 문자열의 일부로 인코더에 전달된다는 점이다. 이를 통해 동작 쿼리가 현재 운동 의도를 명시적으로 인식할 수 있으며, 언어 운동 쿼리는 동작 쿼리와 독립적으로 실행되어 비동기 추론으로 지연 시간을 줄일 수 있다.

### 훈련 레시피
- 사전 훈련: 먼저 RT-2와 동일한 대규모 비전-언어 데이터셋에서 사전 훈련한 후 두 쿼리를 공동 훈련한다.
- 데이터 혼합: 50% 원본 사전 훈련 데이터, 25% 시연 데이터의 언어 운동 쿼리, 25% 시연 데이터의 동작 쿼리.
- 하이퍼파라미터: 배치 크기 1024, 학습률 1e-3, 상수 워밍업 후 제곱근 감쇠.
- 언어 운동 추출: 로봇 고유 감각 기반 자동 주석 절차로, Diverse+Kitchen 데이터셋에서 2500개 이상의 고유 언어 운동을 생성한다.

### 소거 변형
- **RT-H-Joint**: 단일 쿼리가 언어 운동과 동작을 동시에 출력하며, 형식은 `skill: [skill], action: [action]`이다. 이중 쿼리와 언어 운동의 인코더 전달 필요성을 검증하기 위한 것이다.
- **RT-H-Cluster**: K-means 클러스터링(256개 중심)으로 동작을 정수 레이블로 클러스터링하여 언어 운동을 대체하며, 언어 의미론과 동작 통계 구조의 효용을 비교하기 위한 것이다.
- **RT-H-OneHot**: 고유 언어 운동을 빈도 순으로 정렬하여 정수를 할당하고 언어 구조를 보존하지 않으며, 언어 내재적 조합성의 가치를 검증하기 위한 것이다.

### 오류 수정 훈련
- 인간 언어 운동 수정 데이터를 수집하고(작업당 30개 에피소드, 실패 필터링), 관측·작업·수정된 언어 운동을 기록한다.
- 훈련 시 수정 샘플에 50:1로 가중치를 부여하며(시연 데이터와 수정 데이터 비율 약 300:1), 샘플링 가중치는: 사전 훈련 50%, 시연 언어 운동 23%, 시연 동작 23%, 수정 언어 운동 4%이다.
- **RT-H-Intervene**: 언어 운동 쿼리만 훈련; **RT-H-InterveneAction**: 두 쿼리를 모두 훈련; **RT-2-IWR**: Intervention Weighted Regression으로 원격 조작 수정 데이터를 훈련.

## 핵심 혁신

1. **언어 운동의 조합 가능한 중간 표현**: 동작 클러스터링(RT-H-Cluster)이나 이산 레이블(RT-H-OneHot)과 달리, 언어 운동은 의미 구조(예: "move arm forward"와 "move arm forward and left"의 유사성)를 보존하여 모델이 알려진 운동을 조합해 미지의 명령에 대응할 수 있게 한다. 실험은 RT-H가 훈련에서 보지 못한 언어 운동(예: "move arm diagonally up and left")에 대해 맥락 의존적이고 부드러운 동작을 생성함을 증명하며, 이는 고정 원시 요소 집합으로는 불가능하다.

2. **맥락 의존적 운동-동작 매핑**: 각 언어 운동 그룹 내 동작 차원의 평균과 표준편차를 분석한 결과, 비주요 축에서도 사소하지 않은 분산이 발견되었다(예: "rotate arm right"의 arm (x) 평균 2.1, 표준편차 4.6). 이는 언어 운동에서 동작으로의 변환이 장면과 물체 위치에 의존하며 결정적 템플릿이 아님을 증명한다. 이를 통해 RT-H는 고정 궤적을 기계적으로 실행하는 대신 물체 위치 변화에 적응할 수 있다.

3. **언어 공간의 인간 오류 수정 인터페이스**: 오류 수정을 동작 공간(원격 조작)에서 언어 공간으로 승격하여 비전문가 사용자가 자연어로 개입할 수 있게 한다. 실험에 따르면 언어 운동 수정 훈련(RT-H-Intervene, 92.5% 성공률)은 원격 조작 수정(RT-2-IWR, 62.5%)보다 30% 향상되었으며, 온라인 언어 수정(RT-H + Human Intervention, 97.5%)은 거의 완벽에 가까워 언어가 더 효율적인 오류 수정 매체임을 시사한다.

## 실험 및 결과

Diverse+Kitchen 다중 작업 데이터셋에서 RT-H를 RT-2, RT-H-Joint, RT-H-Cluster, RT-H-OneHot 등의 기준선과 대조했다. RT-2는 동작 계층이 없는 평면 모델이고, RT-H-Joint는 단일 자기회귀 쿼리로 언어 운동과 동작을 동시에 출력하며, RT-H-Cluster는 K-means 클러스터링(256개 클러스터 중심)으로 자동 언어 운동 주석을 대체하고, RT-H-OneHot은 정수 클래스 레이블로 언어 운동을 대체한다. 모든 방법은 PaLI-X 55B 아키텍처를 기반으로 하며, RT-2와 동일한 훈련 절차와 공동 훈련 혼합 데이터를 사용하고, 8개의 대표적 어려운 작업에서 각각 10회의 통제 실험을 수행했다.

| 지표 | RT-2 | RT-H-Joint | RT-H | RT-H (GT) |
|---|---|---|---|---|
| Kitchen 훈련/평가 MSE | 30.2 | 28.22 | 24.9 | 17.9 |
| D+K 훈련/Diverse 평가 MSE | 27.7 | 25.44 | 23.6 | 17.8 |
| RT-2 대비 평균 성공률 향상 | — | — | 15% | — |
| 비제로 성능 작업 수 | 4/8 | 8/8 | 6/8 | — |

- **동작 계층이 정책 성능을 크게 향상**: RT-H는 Diverse+Kitchen에서 RT-2보다 평균 15% 우수하며 8개 작업 중 6/8개 작업에서 더 높은 성능을 달성했다. RT-2는 4/8개 작업에서만 비제로 성능을 보였고, RT-H는 6/8개 작업에서 비제로, RT-H-Joint는 모든 8/8개 작업에서 비제로였다. RT-H는 7/8개 작업에서 RT-2보다 더 많은 진전을 보여, 언어 운동이 중간 계층으로서 다양한 다중 작업 정책 성능을 향상시킬 수 있다는 결론(Q1)을 뒷받침한다.
- **언어 구조 자체가 중요한 기여**: RT-H-OneHot은 RT-H보다 훨씬 나쁜 성능을 보여, 언어의 내재적 구조가 언어 운동과 동작 예측 모두에 중요한 기여를 한다는 것을 시사한다. RT-H-Cluster는 가장 어려운 작업(피스타치오 병 열기/닫기)에서 더 나은 성능을 보였지만, 더 넓은 작업에서는 언어 구조 부재로 인해 성능이 낮았다. RT-H는 RT-2 대비 MSE가 약 20% 감소했고, RT-H-Joint는 RT-2 대비 MSE가 5-10% 감소했으며, RT-H의 엔드투엔드 MSE와 지상 진실 언어 운동 MSE 간의 격차는 40%로, 올바르게 주석된 언어 운동이 동작 예측에 매우 정보량이 높음을 시사한다.
- **언어 운동의 맥락성과 다중 양식성**: 동일한 언어 운동이 다른 장면과 작업에서 미세한 동작 조정을 생성한다. 예를 들어 "move arm forward"는 목표 물체 위치에 따라 방향과 기울기 각도를 조정하며, 이러한 맥락성은 고정 원시 요소로 구현하기 어렵다. 언어 운동 라이브러리는 2500개 이상의 언어 운동을 포함하며 9개의 동작 차원(3차원 위치 증분, 3차원 방향 증분, 2차원 베이스 이동, 1차원 그리퍼)을 다루고 수동 주석이 필요 없다. RT-H가 빔 서치로 출력한 상위 3개 언어 운동 예측은 서로 다르지만 모두 작업 맥락에서 합리적이며, 이러한 다중 양식 능력이 언어 운동 개입 데이터를 효율적으로 학습하는 이유일 수 있다.

## 경계 및 한계

- 언어 운동 추출은 로봇 고유 감각에 의존하므로 물체 지시 운동(예: "reach the object")을 포착할 수 없어 물체 중심 추론 작업의 표현력이 제한된다.
- 이중 쿼리 아키텍처는 평면 모델보다 계산량이 많아 자원 제약이 있는 로봇에서 실시간 배포가 병목이 될 수 있다(논문은 구체적인 지연 데이터를 명시하지 않음).
- 오류 수정 인터페이스는 키보드나 마이크가 필요하여 비전문가 사용자에게 충분히 직관적이지 않다. 제스처나 시선 오류 수정은 탐구되지 않았다.
- 단일 계층 언어 운동 계층만 탐구했으며, 다중 계층 계층(작업→하위 작업→운동)은 구현되지 않았고, 이는 데이터 공유를 더욱 향상시킬 수 있지만 더 복잡한 훈련이 필요하다.
- 실험은 8개의 주방 작업으로 제한되며, 더 많은 환경과 작업으로의 확장은 추가 데이터 검증이 필요하다.
- 절대 성공률은 오류 수정 훈련 후에도 여전히 개선 여지가 있다(논문은 구체적인 상한을 명시하지 않음).

## 공학적 시사점

- **재현 시 언어 운동 추출 절차를 먼저 확인하라**: 이 절차는 고유 감각에 기반하므로 물체 지시 운동(예: "grasp the handle")에는 실패한다. 하위 작업이 물체 중심 추론을 필요로 한다면 주석 로직을 확장하거나 시각적 지시를 도입해야 한다.
- **이중 쿼리의 비동기 추론 구현에 주의하라**: 언어 운동 쿼리는 한 단계 앞서 예측해야 하고, 동작 쿼리는 이전 단계의 언어 운동을 사용하므로 추론 파이프라인이 두 쿼리의 독립적 스케줄링을 지원해야 하며, 그렇지 않으면 지연이 두 배가 된다. 대상 하드웨어에서 비동기 배치 처리가 3Hz 쿼리 빈도에 도달하는지 먼저 검증하라.
- **수정 데이터의 가중치 비율이 핵심 하이퍼파라미터다**: 시연 데이터와 수정 데이터의 비율은 약 300:1이며, 50:1 상향 가중치가 효과적인 구성이다. 수정 데이터 양이 변하면 가중치를 다시 조정해야 하며, 그렇지 않으면 수정 분포에 과적합될 수 있다.
- **RT-H-OneHot과 RT-H-Cluster의 비교는 언어 가치를 이해하는 가장 빠른 경로다**: 데이터셋의 언어 명령이 고도로 동질적이라면(예: 10개의 고정 문구), OneHot으로 충분할 수 있다. 명령 조합이 다양하다면 언어 구조가 제공하는 일반화 이득(작업 변화에서 25% 향상)이 이중 쿼리의 복잡성을 감수할 가치가 있다.
- **가장 함정에 빠지기 쉬운 것은 "Place Bowl Upright"류 작업이다**: RT-H는 물체를 집은 후 회전 단계에서 실패율이 높다. 하위 작업이 정밀한 자세 조정을 포함한다면 해당 단계의 수정 데이터를 추가로 수집하거나 다중 계층 계층을 고려하라.
