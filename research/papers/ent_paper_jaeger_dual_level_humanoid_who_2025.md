---
$id: ent_paper_jaeger_dual_level_humanoid_who_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'JAEGER: Dual-Level Humanoid Whole-Body Controller'
  zh: 'JAEGER: Dual-Level Humanoid Whole-Body Controller'
  ko: 'JAEGER: Dual-Level Humanoid Whole-Body Controller'
summary:
  en: 'JAEGER: Dual-Level Humanoid Whole-Body Controller is a 2025 work on loco-manipulation and whole-body-control for humanoid
    robots.'
  zh: JAEGER 是2025年提出的一种双层级人形机器人全身控制器，由研究团队开发。其核心贡献在于将上下半身控制分离为两个独立控制器，以缓解维度诅咒并提升容错性，同时支持根速度跟踪与局部关节角度跟踪两种控制模式。实验在两种人形平台上验证了该方法在仿真和真实环境中均优于现有技术。
  ko: 'JAEGER: Dual-Level Humanoid Whole-Body Controller is a 2025 work on loco-manipulation and whole-body-control for humanoid
    robots.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- humanoid
- jaeger
- loco_manipulation
- whole_body_control
theoretical_depth:
- system
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2505.06584v2. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-04] body rewritten as full-text
    six-section deep read (.staging/deep_read, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated
    by translate pipeline. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read, DeepSeek deepseek-chat
    T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（试点）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: 'JAEGER: Dual-Level Humanoid Whole-Body Controller (arXiv)'
  url: https://arxiv.org/abs/2505.06584
  date: '2025'
  accessed_at: '2026-07-01'
---

## 概述

JAEGER 是面向成人尺寸人形机器人（Unitree H1/H1-2）的全身控制器，由作者团队提出，核心贡献在于将全身控制解耦为上下半身两个独立策略（Dec-POMDP 框架），并配套实时重定向网络与监督初始化课程学习。该方法在根速度跟踪与姿态跟踪任务上均显著优于 HumanPlus、ExBody、OmniH2O 等开源 SOTA，且关节角跟踪误差降至其他方法的 30-60%。

## 它改变了什么

这项工作的真正改变在于打破了“单一策略控制全身”这一默认假设。此前 OmniH2O、HumanPlus 等方案均试图用一个策略同时处理平衡与姿态模仿，但人形机器人与人类的形态差异导致“完全模仿人类姿态”反而破坏平衡，且上下半身交互（如大幅上半身动作增加下半身稳定难度）常使训练陷入局部最优，需要繁琐的调参和课程设计。JAEGER 将问题重新定义为两个独立智能体的协作，让下半身专注平衡与速度跟踪、上半身专注姿态跟随，使“粗粒度根控制”与“细粒度关节跟踪”的有效结合首次变得可行。此外，其重定向网络用三层 MLP 替代传统优化方法，将单帧处理时间从秒级降至亚毫秒级（0.00064 s/frame），且输出更平滑，这为实时遥操作和在线重定向扫清了计算瓶颈。

## 方法拆解

### 整体框架
JAEGER 由三部分组成：重定向网络、双层级控制器、课程学习流程。支持两种命令模式：根基模式（根速度命令 + 上半身参考关节角）和姿态基模式（全身参考关节角）。

### 重定向网络
- 两阶段优化（类似 H2O）处理 AMASS 数据集生成人与机器人姿态对，再用轻量级三层 MLP 学习映射。
- 关键设计：MLP 作为连续函数逼近器，对输入噪声鲁棒，消除帧间抖动；手动移除不稳定 IK 解导致的非连续帧。
- 性能：1 kHz 执行频率，RTX 4060 上 0.00064 s/frame，误差 0.13567 m/frame（优于 H2O 的 9.05 s/frame、0.14476 m/frame）。

### 双层级控制器（Dec-POMDP）
- 两个智能体：π^lower（下半身）与 π^upper（上半身），各自接收 n 步观测历史 x_{t,n}，输出拼接后作为 PD 控制器目标。
- 下半身：Gated Transformer-XL（2 层 transformer 块，6 头注意力，hidden_size=128，memory_length=15），因 WBC 是长程任务，Transformer 在复杂地形平衡中优于 MLP/LSTM；门控机制（GRU）替代残差连接提升 RL 稳定性。
- 上半身：3 层 MLP，输入 3 步观测，因仅需姿态跟随无需复杂平衡。
- 观测包含：缩放基础角速度、投影重力、命令信息、关节位置/速度差、先前动作、参考姿态角；评论家额外输入线速度。

### 课程学习
1. **基础策略**：固定上半身，独立训练下半身得到 π^base。
2. **监督初始化**：上半身用 MSE 直接模仿参考姿态（L_SL）；下半身加 KL 正则化（L_KL）防止偏离 π^base；此阶段姿态幅度减半。
3. **RL 训练**：PPO 优化累积奖励，恢复原始姿态幅度。

### 奖励函数（关键项）
- 上半身 DoF 位置：exp(−4.0*|q_ref−q|)，权重 20
- 下半身 DoF 位置：exp(−4.0*|q_ref−q|)，权重 100
- 脚高：exp(−10.0*|h_target−h|)，权重 100
- 线速度/角速度：exp(−4.0*|v_cmd−v|)，权重 20
- 另有稳定性与 sim-to-real 补充奖励（扭矩限制、动作率、脚离地时间等）。

## 关键创新

1. **上下半身解耦的 Dec-POMDP 框架**：这是首次将人形全身控制显式建模为双智能体协作问题。其价值在于将 21-DoF 的高维动作空间拆分为两个低维子问题，避免单一策略因维度灾难而收敛困难，同时允许上下半身采用异构架构（Transformer + MLP），按任务复杂度分配计算资源。
2. **监督初始化 + KL 正则化的课程学习**：直接解决 RL 冷启动问题。先独立训练下半身基础策略，再通过监督学习“教”上半身模仿姿态，同时用 KL 约束防止下半身遗忘平衡技能。姿态幅度减半的渐进策略显著降低初期探索难度，这是收敛速度与最终性能的关键。
3. **MLP 重定向网络**：相比 H2O 的优化方法（9.05 s/frame），JAEGER 的 MLP 实现 0.00064 s/frame 的实时处理，且角加速度 RMS 降低约 50%（如左肩 Pitch 从 0.0143 降至 0.0069 rad/s²），同时误差更低。这使得在线重定向和低延迟遥操作成为可能。

## 实验与结果

### 根基模式（表 2，H1/H1-2，MAE）
| 任务 | 基线 | E_root-lin-vel (m/s) | E_root-ang-vel (rad/s) | E_upper-j (rad) |
|------|------|---------------------|------------------------|-----------------|
| H1 | ExBody | 0.2417±0.0004 | 0.4176±0.0002 | 0.1689±0.0006 |
| H1 | HumanPlus | 0.1696±0.0013 | 0.3105±0.0021 | 0.2187±0.0041 |
| H1 | **JAEGER** | **0.1504±0.0264** | **0.0937±0.0142** | **0.1093±0.0010** |
| H1-2 | HumanPlus | 0.4026±0.0008 | 0.6209±0.0015 | 0.2547±0.0009 |
| H1-2 | **JAEGER** | **0.2224±0.0150** | **0.2184±0.0062** | **0.0933±0.0001** |

### 姿态基模式（表 3，H1，MAE）
| 任务 | 基线 | E_root-vel (m/s) | E_upper-j (rad) | E_lower-j (rad) |
|------|------|------------------|-----------------|-----------------|
| H1 | HumanPlus | 0.4220±0.0047 | 0.2199±0.0047 | 0.1284±0.0002 |
| H1 | OmniH2O | 0.5424±0.0028 | 0.2217±0.0024 | 0.2404±0.0021 |
| H1 | **JAEGER** | **0.3181±0.0009** | **0.0686±0.0001** | **0.0658±0.0001** |

### 消融（表 4/5，H1-2 姿态模式）
| 变体 | E_root-vel | E_upper-j | E_lower-j |
|------|-----------|-----------|-----------|
| JAEGER w.o. Init | 0.4108±0.0037 | 0.4088±0.0010 | 0.1137±0.0004 |
| JAEGER Single | 0.6634±0.0028 | 0.2477±0.0005 | 0.1112±0.0003 |
| **JAEGER** | **0.3418±0.0004** | **0.1192±0.0002** | **0.1000±0.0001** |

**关键结论**：JAEGER 在关节角跟踪上全面领先（误差为其他方法 30-60%）；根基模式下角速度跟踪误差仅 5°/s；消融证实监督初始化（尤其 H1-2 上半身）和解耦架构（JAEGER Single 上半身误差显著恶化）均不可或缺。

## 边界与局限

论文未明确列出局限章节，但可从结果推断：1) H1-2 因上半身更重（臂与计算模块），速度跟踪误差普遍高于 H1，说明框架对质量分布敏感；2) 不支持关键点位置等更广泛命令类型（OmniH2O 在根跟踪上有优势因其直接优化全局关键点）；3) 复杂姿态（单脚长时间离地、大幅度动作）即使仿真可行也无法部署到真实机器人；4) 作者承认需要大量奖励工程（表 12 含 10+ 项惩罚/正则项）来防止不良行为并解决 sim-to-real 挑战。

## 工程启示

复现或借鉴 JAEGER 时，优先核对以下关键点：1) **监督初始化是性能基石**——消融显示去掉 Init 后 H1-2 上半身误差从 0.1192 恶化至 0.4088 rad，务必保留“姿态幅度减半”的渐进策略；2) **上下半身架构不可互换**——上半身用轻量 MLP 即可，但下半身必须用带门控的 Transformer-XL（memory_length=15），单网络方案（JAEGER Single）在关节跟踪上显著退化；3) **重定向网络是前置依赖**——所有基线均使用 JAEGER 的重定向预处理，若替换为 H2O 优化方法，实时性将下降 14000 倍；4) **奖励工程不可省略**——表 12 中的脚离地时间（权重 10000）和方向惩罚（权重 200）对平衡至关重要，直接删减可能导致训练发散；5) **域随机化参数**（摩擦 U(0.2,1.1)、控制延迟 U(20,60)ms、外部推力 1 m/s）是 sim-to-real 迁移的关键，部署真实 H1-2 时需保持 LCM 通信（50 Hz 策略 / 500 Hz 低层）配置。

## 参考
- http://arxiv.org/abs/2505.06584v2

## Overview

JAEGER is a whole-body controller for adult-sized humanoid robots (Unitree H1/H1-2), proposed by the author team. The core contribution lies in decoupling whole-body control into two independent policies for the upper and lower body (Dec-POMDP framework), accompanied by a real-time retargeting network and supervised-initialization curriculum learning. The method significantly outperforms open-source SOTA methods such as HumanPlus, ExBody, and OmniH2O in both root velocity tracking and pose tracking tasks, with joint angle tracking errors reduced to 30-60% of other methods.

## What It Changes

The real change in this work is breaking the default assumption of "a single policy controlling the whole body." Previous approaches such as OmniH2O and HumanPlus attempted to use one policy to handle both balance and pose imitation simultaneously. However, morphological differences between humanoid robots and humans mean that "fully imitating human poses" actually undermines balance, and interactions between the upper and lower body (e.g., large upper-body movements increasing lower-body stability difficulty) often cause training to fall into local optima, requiring tedious hyperparameter tuning and curriculum design. JAEGER redefines the problem as collaboration between two independent agents, letting the lower body focus on balance and velocity tracking while the upper body focuses on pose following, making the effective combination of "coarse-grained root control" and "fine-grained joint tracking" feasible for the first time. Additionally, its retargeting network replaces traditional optimization methods with a three-layer MLP, reducing per-frame processing time from seconds to sub-milliseconds (0.00064 s/frame) with smoother outputs, clearing the computational bottleneck for real-time teleoperation and online retargeting.

## Method Breakdown

### Overall Framework
JAEGER consists of three components: a retargeting network, a dual-level controller, and a curriculum learning pipeline. It supports two command modes: root-based mode (root velocity commands + upper-body reference joint angles) and pose-based mode (full-body reference joint angles).

### Retargeting Network
- Two-stage optimization (similar to H2O) processes the AMASS dataset to generate human-robot pose pairs, followed by a lightweight three-layer MLP to learn the mapping.
- Key design: The MLP acts as a continuous function approximator, robust to input noise and eliminating inter-frame jitter; manually removes non-continuous frames caused by unstable IK solutions.
- Performance: 1 kHz execution frequency, 0.00064 s/frame on RTX 4060, error 0.13567 m/frame (better than H2O's 9.05 s/frame, 0.14476 m/frame).

### Dual-Level Controller (Dec-POMDP)
- Two agents: π^lower (lower body) and π^upper (upper body), each receiving n-step observation history x_{t,n}, with outputs concatenated as PD controller targets.
- Lower body: Gated Transformer-XL (2 transformer blocks, 6 attention heads, hidden_size=128, memory_length=15). Since WBC is a long-horizon task, Transformers outperform MLP/LSTM in complex terrain balancing; gating mechanism (GRU) replaces residual connections to improve RL stability.
- Upper body: 3-layer MLP with 3-step observations, as only pose following is needed without complex balancing.
- Observations include: scaled base angular velocity, projected gravity, command information, joint position/velocity errors, previous actions, reference pose angles; the critic additionally receives linear velocity.

### Curriculum Learning
1. **Base policy**: Fix the upper body and independently train the lower body to obtain π^base.
2. **Supervised initialization**: The upper body directly imitates reference poses via MSE (L_SL); the lower body adds KL regularization (L_KL) to prevent deviation from π^base; pose amplitude is halved in this stage.
3. **RL training**: PPO optimizes cumulative reward, restoring the original pose amplitude.

### Reward Function (Key Terms)
- Upper-body DoF position: exp(−4.0*|q_ref−q|), weight 20
- Lower-body DoF position: exp(−4.0*|q_ref−q|), weight 100
- Foot height: exp(−10.0*|h_target−h|), weight 100
- Linear/angular velocity: exp(−4.0*|v_cmd−v|), weight 20
- Additional stability and sim-to-real supplementary rewards (torque limits, action rate, foot contact time, etc.).

## Key Innovations

1. **Decoupled upper/lower body Dec-POMDP framework**: This is the first explicit modeling of humanoid whole-body control as a dual-agent collaboration problem. Its value lies in splitting the 21-DoF high-dimensional action space into two lower-dimensional subproblems, avoiding convergence difficulties from the curse of dimensionality in a single policy, while allowing heterogeneous architectures (Transformer + MLP) for the upper and lower body to allocate computational resources by task complexity.
2. **Supervised initialization + KL-regularized curriculum learning**: Directly addresses the RL cold-start problem. First, independently train the lower-body base policy, then "teach" the upper body to imitate poses via supervised learning, while using KL constraints to prevent the lower body from forgetting balance skills. The progressive strategy of halving pose amplitude significantly reduces early exploration difficulty, which is key to convergence speed and final performance.
3. **MLP retargeting network**: Compared to H2O's optimization method (9.05 s/frame), JAEGER's MLP achieves real-time processing at 0.00064 s/frame, with angular acceleration RMS reduced by approximately 50% (e.g., left shoulder pitch from 0.0143 to 0.0069 rad/s²) and lower error. This enables online retargeting and low-latency teleoperation.

## Experiments and Results

### Root-Based Mode (Table 2, H1/H1-2, MAE)
| Task | Baseline | E_root-lin-vel (m/s) | E_root-ang-vel (rad/s) | E_upper-j (rad) |
|------|----------|----------------------|------------------------|-----------------|
| H1 | ExBody | 0.2417±0.0004 | 0.4176±0.0002 | 0.1689±0.0006 |
| H1 | HumanPlus | 0.1696±0.0013 | 0.3105±0.0021 | 0.2187±0.0041 |
| H1 | **JAEGER** | **0.1504±0.0264** | **0.0937±0.0142** | **0.1093±0.0010** |
| H1-2 | HumanPlus | 0.4026±0.0008 | 0.6209±0.0015 | 0.2547±0.0009 |
| H1-2 | **JAEGER** | **0.2224±0.0150** | **0.2184±0.0062** | **0.0933±0.0001** |

### Pose-Based Mode (Table 3, H1, MAE)
| Task | Baseline | E_root-vel (m/s) | E_upper-j (rad) | E_lower-j (rad) |
|------|----------|------------------|-----------------|-----------------|
| H1 | HumanPlus | 0.4220±0.0047 | 0.2199±0.0047 | 0.1284±0.0002 |
| H1 | OmniH2O | 0.5424±0.0028 | 0.2217±0.0024 | 0.2404±0.0021 |
| H1 | **JAEGER** | **0.3181±0.0009** | **0.0686±0.0001** | **0.0658±0.0001** |

### Ablations (Tables 4/5, H1-2 Pose Mode)
| Variant | E_root-vel | E_upper-j | E_lower-j |
|---------|-----------|-----------|-----------|
| JAEGER w.o. Init | 0.4108±0.0037 | 0.4088±0.0010 | 0.1137±0.0004 |
| JAEGER Single | 0.6634±0.0028 | 0.2477±0.0005 | 0.1112±0.0003 |
| **JAEGER** | **0.3418±0.0004** | **0.1192±0.0002** | **0.1000±0.0001** |

**Key conclusions**: JAEGER leads comprehensively in joint angle tracking (errors 30-60% of other methods); angular velocity tracking error is only 5°/s in root-based mode; ablations confirm that supervised initialization (especially for the H1-2 upper body) and the decoupled architecture (JAEGER Single shows significant upper-body error degradation) are both indispensable.

## Boundaries and Limitations

The paper does not explicitly list a limitations section, but inferences can be drawn from the results: 1) H1-2 has generally higher velocity tracking errors than H1 due to a heavier upper body (arms and computing modules), indicating the framework is sensitive to mass distribution; 2) It does not support broader command types such as keypoint positions (OmniH2O has an advantage in root tracking because it directly optimizes global keypoints); 3) Complex poses (single-foot long-duration contact, large-amplitude movements) cannot be deployed to real robots even if feasible in simulation; 4) The authors acknowledge the need for extensive reward engineering (Table 12 includes 10+ penalty/regularization terms) to prevent undesirable behaviors and address sim-to-real challenges.

## Engineering Insights

When reproducing or drawing on JAEGER, prioritize checking the following key points: 1) **Supervised initialization is the performance cornerstone**—ablations show that removing Init degrades H1-2 upper-body error from 0.1192 to 0.4088 rad; be sure to retain the progressive "halved pose amplitude" strategy; 2) **Upper and lower body architectures are not interchangeable**—a lightweight MLP suffices for the upper body, but the lower body must use a gated Transformer-XL (memory_length=15); the single-network scheme (JAEGER Single) significantly degrades joint tracking; 3) **The retargeting network is a prerequisite dependency**—all baselines use JAEGER's retargeting preprocessing; replacing it with the H2O optimization method would reduce real-time performance by 14,000×; 4) **Reward engineering is not optional**—the foot contact time (weight 10000) and orientation penalty (weight 200) in Table 12 are critical for balance; directly removing them may cause training divergence; 5) **Domain randomization parameters** (friction U(0.2,1.1), control delay U(20,60)ms, external push 1 m/s) are key to sim-to-real transfer; when deploying on real H1-2, maintain the LCM communication (50 Hz policy / 500 Hz low-level) configuration.

## 개요

JAEGER는 성인 사이즈 휴머노이드 로봇(Unitree H1/H1-2)을 위한 전신 컨트롤러로, 저자 팀이 제안했으며, 핵심 기여는 전신 제어를 상체와 하체 두 개의 독립적인 정책(Dec-POMDP 프레임워크)으로 분리하고, 실시간 리타게팅 네트워크와 지도 초기화 커리큘럼 학습을 함께 제공하는 것입니다. 이 방법은 루트 속도 추적 및 자세 추적 작업에서 HumanPlus, ExBody, OmniH2O 등 오픈소스 SOTA보다 현저히 우수하며, 관절 각도 추적 오차는 다른 방법의 30-60% 수준으로 줄었습니다.

## 무엇을 바꾸었는가

이 작업의 진정한 변화는 "단일 정책이 전신을 제어한다"는 기본 가정을 깨뜨린 것입니다. 기존 OmniH2O, HumanPlus 등의 접근 방식은 하나의 정책으로 균형과 자세 모방을 동시에 처리하려 했지만, 휴머노이드와 인간의 형태 차이로 인해 "인간 자세의 완전한 모방"이 오히려 균형을 해치고, 상체와 하체의 상호작용(예: 큰 상체 동작이 하체 안정성을 어렵게 만드는 것)이 종종 훈련을 국소 최적해에 빠지게 하여 번거로운 하이퍼파라미터 튜닝과 커리큘럼 설계가 필요했습니다. JAEGER는 문제를 두 개의 독립적인 에이전트의 협력으로 재정의하여, 하체는 균형과 속도 추적에 집중하고 상체는 자세 추종에 집중하게 함으로써, "조립도 루트 제어"와 "세밀한 관절 추적"의 효과적인 결합이 처음으로 가능해졌습니다. 또한, 리타게팅 네트워크는 전통적인 최적화 방법을 3층 MLP로 대체하여 단일 프레임 처리 시간을 초 단위에서 서브 밀리초(0.00064 s/frame)로 줄였고 출력이 더 부드러워져, 실시간 원격 조작과 온라인 리타게팅의 계산 병목을 해소했습니다.

## 방법 분석

### 전체 프레임워크
JAEGER는 세 부분으로 구성됩니다: 리타게팅 네트워크, 이중 계층 컨트롤러, 커리큘럼 학습 프로세스. 두 가지 명령 모드를 지원합니다: 루트 기반 모드(루트 속도 명령 + 상체 참조 관절 각도)와 자세 기반 모드(전신 참조 관절 각도).

### 리타게팅 네트워크
- 2단계 최적화(H2O와 유사)로 AMASS 데이터셋을 처리하여 인간-로봇 자세 쌍을 생성한 후, 경량 3층 MLP로 매핑을 학습합니다.
- 핵심 설계: MLP는 연속 함수 근사기로서 입력 노이즈에 강건하고 프레임 간 떨림을 제거합니다; 불안정한 IK 해로 인한 비연속 프레임을 수동으로 제거합니다.
- 성능: 1 kHz 실행 주파수, RTX 4060에서 0.00064 s/frame, 오차 0.13567 m/frame(H2O의 9.05 s/frame, 0.14476 m/frame보다 우수).

### 이중 계층 컨트롤러(Dec-POMDP)
- 두 에이전트: π^lower(하체)와 π^upper(상체), 각각 n-스텝 관측 히스토리 x_{t,n}을 수신하고, 출력을 이어붙여 PD 컨트롤러 목표로 사용합니다.
- 하체: Gated Transformer-XL(2층 트랜스포머 블록, 6헤드 어텐션, hidden_size=128, memory_length=15), WBC는 장기 작업이므로 Transformer가 복잡한 지형 균형에서 MLP/LSTM보다 우수합니다; 게이팅 메커니즘(GRU)이 잔차 연결을 대체하여 RL 안정성을 향상시킵니다.
- 상체: 3층 MLP, 3-스텝 관측 입력, 복잡한 균형이 필요 없는 자세 추종만 수행하므로.
- 관측에는 다음이 포함됩니다: 스케일된 베이스 각속도, 투영된 중력, 명령 정보, 관절 위치/속도 오차, 이전 행동, 참조 자세 각도; 크리틱은 추가로 선속도를 입력받습니다.

### 커리큘럼 학습
1. **기본 정책**: 상체를 고정하고 하체를 독립적으로 훈련하여 π^base를 얻습니다.
2. **지도 초기화**: 상체는 MSE로 참조 자세를 직접 모방(L_SL); 하체는 KL 정규화(L_KL)를 추가하여 π^base에서 벗어나지 않도록 방지; 이 단계에서는 자세 진폭을 절반으로 줄입니다.
3. **RL 훈련**: PPO로 누적 보상을 최적화하고 원래 자세 진폭을 복원합니다.

### 보상 함수(핵심 항목)
- 상체 DoF 위치: exp(−4.0*|q_ref−q|), 가중치 20
- 하체 DoF 위치: exp(−4.0*|q_ref−q|), 가중치 100
- 발 높이: exp(−10.0*|h_target−h|), 가중치 100
- 선속도/각속도: exp(−4.0*|v_cmd−v|), 가중치 20
- 안정성 및 sim-to-real 추가 보상(토크 제한, 동작 비율, 발 이탈 시간 등).

## 핵심 혁신

1. **상하체 분리 Dec-POMDP 프레임워크**: 휴머노이드 전신 제어를 이중 에이전트 협력 문제로 명시적으로 모델링한 최초의 사례입니다. 그 가치는 21-DoF의 고차원 행동 공간을 두 개의 저차원 하위 문제로 분할하여 단일 정책의 차원 저주로 인한 수렴 어려움을 피하고, 상하체가 이기종 아키텍처(Transformer + MLP)를 사용할 수 있게 하여 작업 복잡도에 따라 계산 자원을 배분할 수 있다는 점입니다.
2. **지도 초기화 + KL 정규화 커리큘럼 학습**: RL 콜드 스타트 문제를 직접 해결합니다. 먼저 하체 기본 정책을 독립적으로 훈련한 후, 지도 학습으로 상체가 자세를 모방하도록 "가르치고", 동시에 KL 제약으로 하체가 균형 기술을 잊지 않도록 방지합니다. 자세 진폭 절반 축소의 점진적 전략은 초기 탐색 난이도를 크게 낮추며, 이는 수렴 속도와 최종 성능의 핵심입니다.
3. **MLP 리타게팅 네트워크**: H2O의 최적화 방법(9.05 s/frame)과 비교하여 JAEGER의 MLP는 0.00064 s/frame의 실시간 처리를 구현하고, 각가속도 RMS가 약 50% 감소(예: 왼쪽 어깨 Pitch가 0.0143에서 0.0069 rad/s²로)하며 오차도 더 낮습니다. 이로써 온라인 리타게팅과 저지연 원격 조작이 가능해졌습니다.

## 실험 및 결과

### 루트 기반 모드(표 2, H1/H1-2, MAE)
| 작업 | 기준선 | E_root-lin-vel (m/s) | E_root-ang-vel (rad/s) | E_upper-j (rad) |
|------|------|---------------------|------------------------|-----------------|
| H1 | ExBody | 0.2417±0.0004 | 0.4176±0.0002 | 0.1689±0.0006 |
| H1 | HumanPlus | 0.1696±0.0013 | 0.3105±0.0021 | 0.2187±0.0041 |
| H1 | **JAEGER** | **0.1504±0.0264** | **0.0937±0.0142** | **0.1093±0.0010** |
| H1-2 | HumanPlus | 0.4026±0.0008 | 0.6209±0.0015 | 0.2547±0.0009 |
| H1-2 | **JAEGER** | **0.2224±0.0150** | **0.2184±0.0062** | **0.0933±0.0001** |

### 자세 기반 모드(표 3, H1, MAE)
| 작업 | 기준선 | E_root-vel (m/s) | E_upper-j (rad) | E_lower-j (rad) |
|------|------|------------------|-----------------|-----------------|
| H1 | HumanPlus | 0.4220±0.0047 | 0.2199±0.0047 | 0.1284±0.0002 |
| H1 | OmniH2O | 0.5424±0.0028 | 0.2217±0.0024 | 0.2404±0.0021 |
| H1 | **JAEGER** | **0.3181±0.0009** | **0.0686±0.0001** | **0.0658±0.0001** |

### 절제(표 4/5, H1-2 자세 모드)
| 변형 | E_root-vel | E_upper-j | E_lower-j |
|------|-----------|-----------|-----------|
| JAEGER w.o. Init | 0.4108±0.0037 | 0.4088±0.0010 | 0.1137±0.0004 |
| JAEGER Single | 0.6634±0.0028 | 0.2477±0.0005 | 0.1112±0.0003 |
| **JAEGER** | **0.3418±0.0004** | **0.1192±0.0002** | **0.1000±0.0001** |

**핵심 결론**: JAEGER는 관절 각도 추적에서 전반적으로 우위를 점합니다(오차가 다른 방법의 30-60%); 루트 기반 모드에서 각속도 추적 오차는 5°/s에 불과합니다; 절제 실험은 지도 초기화(특히 H1-2 상체)와 분리 아키텍처(JAEGER Single 상체 오차가 현저히 악화)가 모두 필수적임을 확인합니다.

## 경계 및 한계

논문은 한계 섹션을 명시적으로 나열하지 않았지만, 결과에서 추론할 수 있습니다: 1) H1-2는 상체가 더 무겁기(팔과 계산 모듈) 때문에 속도 추적 오차가 일반적으로 H1보다 높아, 프레임워크가 질량 분포에 민감함을 시사합니다; 2) 키포인트 위치 등 더 넓은 명령 유형을 지원하지 않습니다(OmniH2O는 전역 키포인트를 직접 최적화하므로 루트 추적에서 장점이 있음); 3) 복잡한 자세(한 발로 오래 서 있기, 큰 동작)는 시뮬레이션에서 가능하더라도 실제 로봇에 배포할 수 없습니다; 4) 저자는 바람직하지 않은 동작을 방지하고 sim-to-real 문제를 해결하기 위해 많은 보상 엔지니어링(표 12에 10개 이상의 페널티/정규화 항목 포함)이 필요함을 인정합니다.

## 엔지니어링 시사점

JAEGER를 재현하거나 참고할 때 다음 핵심 사항을 우선 확인하십시오: 1) **지도 초기화는 성능의 기반**——절제 실험에서 Init을 제거하면 H1-2 상체 오차가 0.1192에서 0.4088 rad로 악화되므로, "자세 진폭 절반 축소"의 점진적 전략을 반드시 유지하십시오; 2) **상하체 아키텍처는 상호 교환 불가**——상체는 경량 MLP로 충분하지만, 하체는 게이팅된 Transformer-XL(memory_length=15)을 반드시 사용해야 하며, 단일 네트워크 방식(JAEGER Single)은 관절 추적에서 현저히 퇴화합니다; 3) **리타게팅 네트워크는 선행 의존성**——모든 기준선은 JAEGER의 리타게팅 전처리를 사용하며, H2O 최적화 방법으로 교체하면 실시간성이 14000배 저하됩니다; 4) **보상 엔지니어링은 생략 불가**——표 12의 발 이탈 시간(가중치 10000)과 방향 페널티(가중치 200)는 균형에 매우 중요하며, 직접 삭제하면 훈련이 발산할 수 있습니다; 5) **도메인 무작위화 매개변수**(마찰 U(0.2,1.1), 제어 지연 U(20,60)ms, 외부 추력 1 m/s)는 sim-to-real 전이의 핵심이며, 실제 H1-2에 배포할 때 LCM 통신(50 Hz 정책 / 500 Hz 저수준) 구성을 유지해야 합니다.
