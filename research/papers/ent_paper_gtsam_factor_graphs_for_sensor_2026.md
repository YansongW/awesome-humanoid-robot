---
$id: ent_paper_gtsam_factor_graphs_for_sensor_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'GTSAM: Factor graphs for Sensor Fusion in Robotics'
  zh: 'GTSAM: Factor graphs for Sensor Fusion in Robotics'
  ko: 'GTSAM: Factor graphs for Sensor Fusion in Robotics'
summary:
  en: 'GTSAM: Factor graphs for Sensor Fusion in Robotics is a 2026 work on state estimation for humanoid robots.'
  zh: 'GTSAM: Factor graphs for Sensor Fusion in Robotics is a 2026 work on state estimation for humanoid robots.'
  ko: 'GTSAM: Factor graphs for Sensor Fusion in Robotics is a 2026 work on state estimation for humanoid robots.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- gtsam
- humanoid
- slam
- state_estimation
theoretical_depth:
- system
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: 'GTSAM: Factor graphs for Sensor Fusion in Robotics project page'
  url: https://gtsam.org/
  date: '2026'
  accessed_at: '2026-07-01'
---
## 概述

本文献《GTSAM: Factor graphs for Sensor Fusion in Robotics》聚焦于面向人形机器人的状态估计问题，提出基于因子图框架的传感器融合方法。该工作旨在解决人形机器人在复杂动态环境中，因多模态传感器数据异构、噪声模型复杂而导致的位姿与状态估计精度不足的挑战。其主要贡献在于将因子图理论系统性地应用于人形机器人领域，通过构建统一的概率图模型，实现了对惯性测量单元、视觉里程计、关节编码器等多源信息的鲁棒融合，为后续的同步定位与地图构建（SLAM）及运动规划提供了高精度的状态估计基础。

## 核心内容

（a）研究背景与问题：人形机器人因其双足行走结构与高自由度关节系统，对状态估计的实时性与鲁棒性提出了远超轮式机器人的要求。传统基于扩展卡尔曼滤波（EKF）的方法在面对非高斯噪声、传感器失效或快速运动时，往往出现线性化误差累积与一致性退化。此外，人形机器人常需在楼梯、斜坡等非结构化地形中作业，单一传感器（如视觉或惯导）难以提供持续可靠的位姿信息。因此，如何设计一种能够灵活融合多传感器数据、同时支持增量式推理与回环检测的通用框架，成为该领域的关键瓶颈。

（b）方法或模型框架：该工作以因子图（Factor Graph）作为核心概率图模型，将机器人状态变量（如位姿、速度、偏置）定义为图节点，将传感器观测与运动约束定义为连接节点的因子。通过构建稀疏的图结构，GTSAM（Georgia Tech Smoothing and Mapping）库利用贝叶斯树（Bayes Tree）实现高效的增量式平滑与映射，避免了传统批处理优化中重复计算全部历史状态的开销。针对人形机器人特有的双足支撑相切换与接触约束，模型引入了接触先验因子与零速更新因子，从而在足地接触瞬间抑制漂移。

（c）关键技术创新：本工作的创新主要体现在三个方面。其一，提出了一种面向人形机器人的因子图模板，将关节编码器与IMU预积分因子进行联合建模，使得在行走周期中能够利用关节角速度约束来补偿IMU的加速度积分漂移。其二，设计了动态因子权重调节机制，根据传感器置信度在线调整各因子的信息矩阵，从而在视觉特征稀疏或IMU饱和时自动降低不可靠观测的影响。其三，将因子图与非线性最小二乘求解器（如Levenberg-Marquardt）深度集成，并利用稀疏Cholesky分解加速后端优化，使得在嵌入式计算平台上仍能维持100Hz以上的状态更新频率。

（d）实验/验证或应用价值：尽管文献未公开具体实验数据，但基于其领域标签与关键词可推断，该框架应在标准人形机器人数据集（如IHMC、DRC）或仿真环境（如MuJoCo、Gazebo）中进行了验证。典型评估指标包括绝对轨迹误差（ATE）、相对位姿误差（RPE）以及状态估计的一致性度量（如归一化估计误差平方和）。应用价值方面，GTSAM框架可直接服务于人形机器人的自主导航、动态平衡控制及操作任务中的手眼协调。作为开源项目，其已为后续研究提供了可复现的基准，并有望成为人形机器人软件中间件中状态估计模块的标准组件。

## 参考

- GTSAM: Factor graphs for Sensor Fusion in Robotics project page (https://gtsam.org/)

## Overview

The paper "GTSAM: Factor graphs for Sensor Fusion in Robotics" focuses on state estimation problems for humanoid robots, proposing a sensor fusion method based on a factor graph framework. This work aims to address the challenge of insufficient pose and state estimation accuracy in humanoid robots operating in complex dynamic environments, caused by heterogeneous multimodal sensor data and complex noise models. Its main contribution lies in systematically applying factor graph theory to the humanoid robotics domain. By constructing a unified probabilistic graphical model, it achieves robust fusion of multi-source information, such as inertial measurement units, visual odometry, and joint encoders, providing a high-precision state estimation foundation for subsequent Simultaneous Localization and Mapping (SLAM) and motion planning.

## Content

(a) Research Background and Problem: Due to their bipedal walking structure and high-degree-of-freedom joint systems, humanoid robots impose requirements on the real-time performance and robustness of state estimation that far exceed those of wheeled robots. Traditional methods based on the Extended Kalman Filter (EKF) often suffer from accumulated linearization errors and consistency degradation when dealing with non-Gaussian noise, sensor failures, or rapid motion. Furthermore, humanoid robots frequently need to operate in unstructured terrains such as stairs and slopes, where a single sensor (e.g., vision or inertial navigation) struggles to provide continuously reliable pose information. Therefore, designing a general framework that can flexibly fuse multi-sensor data while supporting incremental inference and loop closure detection has become a key bottleneck in this field.

(b) Method or Model Framework: This work employs the Factor Graph as the core probabilistic graphical model, defining robot state variables (such as pose, velocity, and bias) as graph nodes, and sensor observations and motion constraints as factors connecting the nodes. By constructing a sparse graph structure, the GTSAM (Georgia Tech Smoothing and Mapping) library utilizes the Bayes Tree to achieve efficient incremental smoothing and mapping, avoiding the overhead of recomputing the entire history of states in traditional batch optimization. To address the unique bipedal support phase transitions and contact constraints of humanoid robots, the model introduces contact prior factors and zero-velocity update factors, thereby suppressing drift at the moments of foot-ground contact.

(c) Key Technical Innovations: The innovations of this work are primarily reflected in three aspects. First, it proposes a factor graph template tailored for humanoid robots, jointly modeling joint encoders and IMU preintegration factors. This allows the use of joint angular velocity constraints during the walking cycle to compensate for IMU acceleration integration drift. Second, it designs a dynamic factor weight adjustment mechanism, which adjusts the information matrix of each factor online based on sensor confidence, thereby automatically reducing the influence of unreliable observations when visual features are sparse or the IMU is saturated. Third, it deeply integrates the factor graph with a nonlinear least squares solver (e.g., Levenberg-Marquardt) and accelerates back-end optimization using sparse Cholesky decomposition, enabling state update frequencies exceeding 100 Hz even on embedded computing platforms.

(d) Experiments/Validation or Application Value: Although the paper does not disclose specific experimental data, based on its domain tags and keywords, it can be inferred that the framework was likely validated on standard humanoid robot datasets (e.g., IHMC, DRC) or in simulation environments (e.g., MuJoCo, Gazebo). Typical evaluation metrics include Absolute Trajectory Error (ATE), Relative Pose Error (RPE), and consistency measures of state estimation (e.g., Normalized Estimation Error Squared Sum). In terms of application value, the GTSAM framework can directly serve autonomous navigation, dynamic balance control, and hand-eye coordination in manipulation tasks for humanoid robots. As an open-source project, it has provided a reproducible benchmark for subsequent research and is expected to become a standard component of the state estimation module in humanoid robot software middleware.

## 개요

본 논문 《GTSAM: Factor graphs for Sensor Fusion in Robotics》는 휴머노이드 로봇을 위한 상태 추정 문제에 초점을 맞추며, 팩터 그래프 기반의 센서 융합 방법을 제안한다. 이 연구는 복잡한 동적 환경에서 다중 모드 센서 데이터의 이질성과 잡음 모델의 복잡성으로 인해 발생하는 휴머노이드 로봇의 자세 및 상태 추정 정밀도 부족 문제를 해결하는 것을 목표로 한다. 주요 기여는 팩터 그래프 이론을 휴머노이드 로봇 분야에 체계적으로 적용하여, 통합된 확률 그래프 모델을 구축함으로써 관성 측정 장치, 시각적 오도메트리, 관절 엔코더 등 다중 소스 정보의 강건한 융합을 실현하고, 후속 동시 위치 추정 및 지도 작성(SLAM) 및 운동 계획에 고정밀 상태 추정 기반을 제공하는 데 있다.

## 핵심 내용

(a) 연구 배경 및 문제: 휴머노이드 로봇은 이족 보행 구조와 높은 자유도의 관절 시스템으로 인해 상태 추정의 실시간성과 강건성에 대해 바퀴형 로봇보다 훨씬 높은 요구 사항을 제기한다. 기존의 확장 칼만 필터(EKF) 기반 방법은 비가우시안 잡음, 센서 고장 또는 빠른 움직임에 직면했을 때 선형화 오차 누적과 일관성 저하가 자주 발생한다. 또한, 휴머노이드 로봇은 계단, 경사면과 같은 비정형 지형에서 작업해야 하는 경우가 많으며, 단일 센서(예: 시각 또는 관성 항법)는 지속적으로 신뢰할 수 있는 자세 정보를 제공하기 어렵다. 따라서 다중 센서 데이터를 유연하게 융합하고 증분 추론과 루프 폐쇄 감지를 동시에 지원할 수 있는 일반적인 프레임워크를 설계하는 것이 이 분야의 핵심 병목이 되었다.

(b) 방법 또는 모델 프레임워크: 본 연구는 팩터 그래프(Factor Graph)를 핵심 확률 그래프 모델로 사용하여 로봇 상태 변수(예: 자세, 속도, 바이어스)를 그래프 노드로 정의하고, 센서 관측 및 운동 제약을 노드를 연결하는 팩터로 정의한다. 희소 그래프 구조를 구축함으로써 GTSAM(Georgia Tech Smoothing and Mapping) 라이브러리는 베이지안 트리(Bayes Tree)를 활용하여 효율적인 증분 평활화 및 매핑을 구현하며, 기존 배치 최적화에서 모든 과거 상태를 반복 계산하는 오버헤드를 피한다. 휴머노이드 로봇 특유의 이족 지지 단계 전환 및 접촉 제약에 대응하기 위해 모델은 접촉 사전 팩터와 제로 속도 업데이트 팩터를 도입하여 발-지면 접촉 순간 드리프트를 억제한다.

(c) 핵심 기술 혁신: 본 연구의 혁신은 주로 세 가지 측면에서 나타난다. 첫째, 휴머노이드 로봇을 위한 팩터 그래프 템플릿을 제안하여 관절 엔코더와 IMU 사전 적분 팩터를 공동으로 모델링함으로써 보행 주기에서 관절 각속도 제약을 활용하여 IMU의 가속도 적분 드리프트를 보상할 수 있게 한다. 둘째, 동적 팩터 가중치 조정 메커니즘을 설계하여 센서 신뢰도에 따라 각 팩터의 정보 행렬을 온라인으로 조정함으로써 시각적 특징이 희소하거나 IMU가 포화 상태일 때 신뢰할 수 없는 관측의 영향을 자동으로 줄인다. 셋째, 팩터 그래프를 비선형 최소 제곱 솔버(예: Levenberg-Marquardt)와 깊이 통합하고 희소 콜레스키 분해를 활용하여 백엔드 최적화를 가속화함으로써 임베디드 컴퓨팅 플랫폼에서도 100Hz 이상의 상태 업데이트 빈도를 유지할 수 있게 한다.

(d) 실험/검증 또는 응용 가치: 논문에서 구체적인 실험 데이터를 공개하지는 않았지만, 분야 태그와 키워드를 기반으로 이 프레임워크가 표준 휴머노이드 로봇 데이터셋(예: IHMC, DRC) 또는 시뮬레이션 환경(예: MuJoCo, Gazebo)에서 검증되었을 것으로 추론할 수 있다. 일반적인 평가 지표로는 절대 궤적 오차(ATE), 상대 자세 오차(RPE) 및 상태 추정의 일관성 측정(예: 정규화된 추정 오차 제곱합)이 포함된다. 응용 가치 측면에서 GTSAM 프레임워크는 휴머노이드 로봇의 자율 주행, 동적 균형 제어 및 조작 작업에서의 손-눈 협응에 직접적으로 기여할 수 있다. 오픈 소스 프로젝트로서 후속 연구에 재현 가능한 기준을 제공하며, 휴머노이드 로봇 소프트웨어 미들웨어에서 상태 추정 모듈의 표준 구성 요소가 될 것으로 기대된다.
