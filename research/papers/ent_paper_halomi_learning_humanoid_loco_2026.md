---
$id: ent_paper_halomi_learning_humanoid_loco_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HALOMI: Learning Humanoid Loco-Manipulation with Active Perception from Human Demonstrations'
  zh: HALOMI｜通过人类示范的主动感知学习人形移动操作
  ko: 'HALOMI: Learning Humanoid Loco-Manipulation with Active Perception from Human Demonstrations'
summary:
  en: Halomi first reconstructs the scene, object, or motion representation from camera images/multi-view observations, then,
    the joint position/Torque Command and the whole body trajectory/action sequence are generated by ACT/behavior clone imitation
    learning, diffusion strategy/flow matching. The key point is to treat action generation as a conditional generation problem,
    and use diffusion or flow matching to sample executable trajectories in multimodal action distributions.
  zh: HALOMI 先从相机图像/多视角观测恢复场景、目标或运动表征，再用ACT/行为克隆模仿学习、扩散策略/流匹配生成关节位置/力矩命令、全身轨迹/动作序列。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: HALOMI 先从相机图像/多视角观测恢复场景、目标或运动表征，再用ACT/行为克隆模仿学习、扩散策略/流匹配生成关节位置/力矩命令、全身轨迹/动作序列。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- halomi
- mobile_manipulation
- scene_understanding
- vision_guided_control
- visual_perception
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: HALOMI project page
  url: https://halomi-humanoid.github.io
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人在复杂环境中执行移动操作任务时，面临感知与动作生成之间的深度耦合难题。现有方法往往将视觉感知与运动控制分离处理，难以适应非结构化场景下的多模态动作分布。HALOMI 提出一种从人类示范中学习主动感知的端到端框架，通过将动作生成建模为条件生成问题，利用扩散模型或流匹配在多种可行轨迹中进行采样，从而显著提升人形机器人在移动操作任务中的鲁棒性与适应性。

## 核心内容

人形机器人的移动操作（loco-manipulation）是机器人学领域的前沿挑战，其核心困难在于同时协调全身运动与末端执行器操作，并实时响应环境变化。传统方法通常依赖手工设计的运动规划器或预编程动作库，在动态、非结构化环境中泛化能力有限。近年来，模仿学习与视觉感知的结合为机器人技能获取提供了新范式，但如何从高维视觉输入中有效提取任务相关表征，并生成符合物理约束的全身轨迹，仍是尚未解决的关键问题。HALOMI 正是在这一背景下，聚焦于“主动感知”与“条件动作生成”的联合学习。

HALOMI 的方法框架分为两个核心阶段。第一阶段为感知表征重建：系统从单目或多视角相机图像中，利用可微渲染或隐式神经表示技术，恢复场景几何、目标位姿或人体运动表征。这一过程不仅提取静态环境信息，还通过时序观测捕捉动态交互特征，为后续动作生成提供结构化输入。第二阶段为动作生成：基于第一阶段得到的感知表征，系统采用行为克隆（Behavior Cloning）与 ACT（Action Chunking with Transformers）架构，将动作生成视为条件概率建模问题。具体而言，模型学习从感知表征到关节位置/力矩命令或全身轨迹的映射，并通过扩散策略或流匹配（Flow Matching）在动作分布中采样，从而避免单峰预测导致的模式坍塌。

该框架的关键技术创新体现在三个方面。其一，将动作生成从确定性映射转化为条件生成问题，使得模型能够表达多模态动作分布——例如在抓取同一物体时，机器人可能选择不同的步态或手臂姿态，扩散模型可自然覆盖这些可行解。其二，引入主动感知机制，即动作生成过程会反向影响感知注意力，使机器人能够根据当前任务需求调整观测视角或传感器配置，形成感知-动作闭环。其三，采用流匹配替代传统扩散过程，在保持生成质量的同时显著降低采样步数，提升实时性，这对于需要高频控制的人形机器人尤为重要。

在实验验证方面，HALOMI 在仿真环境与真实人形机器人平台上进行了系统评估。仿真实验基于多种移动操作任务（如推车、搬运、开门），对比了基于确定性回归的基线方法与 HALOMI 的生成式框架，结果表明后者在任务成功率、轨迹平滑度及面对干扰时的鲁棒性上均取得显著提升。真实机器人实验进一步验证了从人类示范数据中习得的技能可直接迁移至物理平台，且主动感知机制使机器人能够在遮挡或光照变化场景下维持稳定操作。该工作的应用价值在于，为人形机器人在家庭服务、工业装配、灾难救援等需要灵活移动与精细操作的场景中，提供了一种可扩展的技能学习范式。

## 参考

- HALOMI project page (https://halomi-humanoid.github.io)

## Overview

When humanoid robots perform mobile manipulation tasks in complex environments, they face the challenge of deep coupling between perception and action generation. Existing methods often treat visual perception and motion control separately, making it difficult to adapt to multimodal action distributions in unstructured scenarios. HALOMI proposes an end-to-end framework for learning active perception from human demonstrations. By modeling action generation as a conditional generation problem and utilizing diffusion models or flow matching to sample among multiple feasible trajectories, it significantly enhances the robustness and adaptability of humanoid robots in mobile manipulation tasks.

## Content

Loco-manipulation for humanoid robots is a frontier challenge in robotics, with the core difficulty lying in simultaneously coordinating whole-body motion and end-effector operations while responding to environmental changes in real time. Traditional methods typically rely on handcrafted motion planners or pre-programmed action libraries, which have limited generalization capabilities in dynamic, unstructured environments. In recent years, the combination of imitation learning and visual perception has provided a new paradigm for robot skill acquisition. However, effectively extracting task-relevant representations from high-dimensional visual inputs and generating whole-body trajectories that satisfy physical constraints remain unresolved key issues. It is against this backdrop that HALOMI focuses on the joint learning of "active perception" and "conditional action generation."

The methodological framework of HALOMI consists of two core stages. The first stage is perception representation reconstruction: the system uses differentiable rendering or implicit neural representation techniques from monocular or multi-view camera images to recover scene geometry, object poses, or human motion representations. This process not only extracts static environmental information but also captures dynamic interaction features through temporal observations, providing structured input for subsequent action generation. The second stage is action generation: based on the perception representations obtained in the first stage, the system adopts Behavior Cloning and the ACT (Action Chunking with Transformers) architecture, treating action generation as a conditional probability modeling problem. Specifically, the model learns a mapping from perception representations to joint position/torque commands or whole-body trajectories, and samples from the action distribution via diffusion policies or flow matching, thereby avoiding mode collapse caused by unimodal prediction.

The key technological innovations of this framework are reflected in three aspects. First, it transforms action generation from a deterministic mapping into a conditional generation problem, enabling the model to express multimodal action distributions—for example, when grasping the same object, the robot may choose different gaits or arm postures, and the diffusion model can naturally cover these feasible solutions. Second, it introduces an active perception mechanism, where the action generation process inversely influences perceptual attention, allowing the robot to adjust its observation viewpoint or sensor configuration based on current task requirements, forming a perception-action closed loop. Third, it replaces traditional diffusion processes with flow matching, significantly reducing the number of sampling steps while maintaining generation quality, thereby improving real-time performance, which is particularly important for humanoid robots requiring high-frequency control.

In terms of experimental validation, HALOMI was systematically evaluated in both simulation environments and on real humanoid robot platforms. Simulation experiments were based on various mobile manipulation tasks (such as pushing carts, carrying objects, and opening doors), comparing baseline methods based on deterministic regression with HALOMI's generative framework. The results showed that the latter achieved significant improvements in task success rate, trajectory smoothness, and robustness against disturbances. Real robot experiments further verified that skills learned from human demonstration data can be directly transferred to physical platforms, and the active perception mechanism enables the robot to maintain stable operation in scenarios with occlusion or lighting changes. The practical value of this work lies in providing a scalable skill learning paradigm for humanoid robots in scenarios requiring flexible movement and fine manipulation, such as home services, industrial assembly, and disaster rescue.

## 개요

휴머노이드 로봇이 복잡한 환경에서 이동 조작 작업을 수행할 때, 인식과 동작 생성 간의 깊은 결합 문제에 직면합니다. 기존 방법은 종종 시각적 인식과 운동 제어를 분리하여 처리하므로, 비구조화된 환경에서의 다중 모드 동작 분포에 적응하기 어렵습니다. HALOMI는 인간 시연으로부터 능동적 인식을 학습하는 엔드투엔드 프레임워크를 제안하며, 동작 생성을 조건부 생성 문제로 모델링하고 확산 모델 또는 흐름 매칭을 통해 다양한 실행 가능한 궤적을 샘플링함으로써, 휴머노이드 로봇의 이동 조작 작업에서의 강건성과 적응성을 크게 향상시킵니다.

## 핵심 내용

휴머노이드 로봇의 이동 조작(loco-manipulation)은 로봇 공학 분야의 최전선 도전 과제로, 핵심 어려움은 전신 운동과 말단 실행기 조작을 동시에 조정하고 환경 변화에 실시간으로 대응하는 데 있습니다. 전통적인 방법은 일반적으로 수동으로 설계된 운동 계획기나 사전 프로그래밍된 동작 라이브러리에 의존하며, 동적이고 비구조화된 환경에서 일반화 능력이 제한적입니다. 최근 모방 학습과 시각적 인식의 결합은 로봇 기술 습득에 새로운 패러다임을 제공했지만, 고차원 시각 입력에서 작업 관련 표현을 효과적으로 추출하고 물리적 제약을 충족하는 전신 궤적을 생성하는 방법은 여전히 해결되지 않은 핵심 문제입니다. HALOMI는 이러한 배경에서 "능동적 인식"과 "조건부 동작 생성"의 공동 학습에 초점을 맞춥니다.

HALOMI의 방법 프레임워크는 두 가지 핵심 단계로 구성됩니다. 첫 번째 단계는 인식 표현 재구성입니다. 시스템은 단일 시점 또는 다중 시점 카메라 이미지에서 미분 가능한 렌더링 또는 암시적 신경 표현 기술을 사용하여 장면 기하학, 대상 자세 또는 인간 움직임 표현을 복원합니다. 이 과정은 정적 환경 정보를 추출할 뿐만 아니라 시간적 관찰을 통해 동적 상호작용 특징을 포착하여 후속 동작 생성을 위한 구조화된 입력을 제공합니다. 두 번째 단계는 동작 생성입니다. 첫 번째 단계에서 얻은 인식 표현을 기반으로 시스템은 행동 복제(Behavior Cloning)와 ACT(Action Chunking with Transformers) 아키텍처를 채택하여 동작 생성을 조건부 확률 모델링 문제로 간주합니다. 구체적으로, 모델은 인식 표현에서 관절 위치/토크 명령 또는 전신 궤적으로의 매핑을 학습하고, 확산 전략 또는 흐름 매칭(Flow Matching)을 통해 동작 분포에서 샘플링하여 단일 모드 예측으로 인한 모드 붕괴를 방지합니다.

이 프레임워크의 핵심 기술 혁신은 세 가지 측면에서 나타납니다. 첫째, 동작 생성을 결정론적 매핑에서 조건부 생성 문제로 전환하여 모델이 다중 모드 동작 분포를 표현할 수 있게 합니다. 예를 들어, 동일한 물체를 잡을 때 로봇은 다른 보행 패턴이나 팔 자세를 선택할 수 있으며, 확산 모델은 이러한 실행 가능한 해결책을 자연스럽게 포함할 수 있습니다. 둘째, 능동적 인식 메커니즘을 도입하여 동작 생성 과정이 인식 주의에 역으로 영향을 미쳐 로봇이 현재 작업 요구에 따라 관찰 시점이나 센서 구성을 조정할 수 있게 하여 인식-동작 폐루프를 형성합니다. 셋째, 흐름 매칭을 사용하여 전통적인 확산 과정을 대체함으로써 생성 품질을 유지하면서 샘플링 단계를 크게 줄이고 실시간성을 향상시킵니다. 이는 고주파 제어가 필요한 휴머노이드 로봇에게 특히 중요합니다.

실험 검증 측면에서 HALOMI는 시뮬레이션 환경과 실제 휴머노이드 로봇 플랫폼에서 체계적으로 평가되었습니다. 시뮬레이션 실험은 다양한 이동 조작 작업(예: 카트 밀기, 물건 운반, 문 열기)을 기반으로 결정론적 회귀 기반 기준 방법과 HALOMI의 생성적 프레임워크를 비교했으며, 후자가 작업 성공률, 궤적 평활도 및 간섭에 대한 강건성에서 현저한 향상을 보였습니다. 실제 로봇 실험은 인간 시연 데이터에서 학습된 기술이 물리적 플랫폼으로 직접 전이될 수 있음을 추가로 검증했으며, 능동적 인식 메커니즘을 통해 로봇이 가림이나 조명 변화 환경에서도 안정적인 조작을 유지할 수 있음을 보여주었습니다. 이 연구의 응용 가치는 가정 서비스, 산업 조립, 재난 구조 등 유연한 이동과 정밀한 조작이 필요한 시나리오에서 휴머노이드 로봇을 위한 확장 가능한 기술 학습 패러다임을 제공하는 데 있습니다.
