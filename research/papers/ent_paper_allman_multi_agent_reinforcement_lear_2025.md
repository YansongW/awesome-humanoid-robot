---
$id: ent_paper_allman_multi_agent_reinforcement_lear_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Multi-Agent Reinforcement Learning for Cooperative Warehouse Automation: QMIX Value Decomposition for Sparse-Reward
    Coordination'
  zh: 面向协同仓库自动化的多智能体强化学习：面向稀疏奖励协调的QMIX值分解
  ko: '협력형 창고 자동화를 위한 다중 에이전트 강화학습: 희소 보상 조정을 위한 QMIX 가치 분해'
summary:
  en: This paper compares QMIX, IPPO, and MASAC on warehouse multi-agent reinforcement learning tasks using RWARE, MPE, and
    a custom Unity 3D simulation, demonstrating that QMIX's value decomposition outperforms independent learners but requires
    extensive hyperparameter tuning—particularly extended epsilon annealing—to discover sparse rewards.
  zh: 本文对比了QMIX、IPPO与MASAC在仓库多智能体强化学习任务中的表现，使用RWARE、MPE及自定义Unity 3D仿真环境。核心贡献在于证明QMIX的价值分解方法显著优于独立学习算法，但需要大量超参数调优（尤其是超过500万步的epsilon退火）才能发现稀疏奖励。
  ko: 본 논문은 RWARE, MPE 및 사용자 정의 Unity 3D 시뮬레이션에서 창고 다중 에이전트 강화학습 작업에 대한 QMIX, IPPO 및 MASAC을 비교하며, QMIX의 가치 분해가 독립 학습기보다 우수하지만
    희소 보상 발견을 위해 광범위한 하이퍼파라미터 튜닝, 특히 확장된 입실론 어닐링이 필요함을 보여준다.
domains:
- 07_ai_models_algorithms
- 11_applications_markets
- 10_evaluation_benchmarks
layers:
- intelligence
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- marl
- qmix
- ippo
- masac
- warehouse_automation
- multi_agent_coordination
- reinforcement_learning
- sim_to_sim_transfer
- rware
- mpe
- unity_ml_agents
- sparse_rewards
- value_decomposition
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2512.04463v2. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
sources:
- id: src_001
  type: paper
  title: 'Multi-Agent Reinforcement Learning for Cooperative Warehouse Automation: QMIX Value Decomposition for Sparse-Reward
    Coordination'
  url: https://arxiv.org/abs/2512.04463
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

本文系统评估了 QMIX 价值分解方法在仓库多机器人协调任务中的表现，作者通过大规模超参数调优（epsilon 退火延长至 5M 步、buffer 扩至 200K）解决了稀疏奖励下默认 MARL 配置零学习的问题，并在 RWARE 网格环境与 Unity 3D 连续环境中验证了从 2 到 6 智能体的扩展性。核心贡献在于揭示了稀疏奖励仓库任务中训练配置的关键敏感性，并提供了可复现的工程基线。

## 它改变了什么

这项工作的真正价值不在于提出新算法，而在于戳破了一个行业默认假设：即标准 MARL 超参数配置可以直接迁移到稀疏奖励的仓库任务。作者用 2M 步零学习的失败案例证明，默认配置（batch 32、buffer 5K、epsilon 退火 50K 步）在 RWARE 上完全失效，这解释了为何许多工业 MARL 试点项目在初期就夭折——问题不在算法，而在训练配置与任务奖励密度的错配。

更重要的是，作者将 QMIX 与 IPPO 的对比置于同一训练预算下，揭示了价值分解方法在信用分配上的结构性优势：QMIX 在 tiny-2ag 上达到 3.25 回报，而 vanilla IPPO 即使训练 4 倍步数（20M vs 5M）仅达 0.13，差距达 25 倍。这改变了我们对"独立学习是否足够"的认知——在需要隐式协调的仓库场景中，集中式价值分解不是可选项，而是必要条件。此外，作者首次在 Unity 物理引擎中验证了 QMIX 策略的可迁移性，1M 步后回报均值 238.6 且策略近乎确定性，表明仿真训练的策略具备直接部署的潜力。

## 方法拆解

### 问题建模
将仓库协调建模为 Dec-POMDP，每个智能体 i 接收局部观测 o_i，动作空间 𝒜 = {left, right, forward, load/unload, no-op}，团队共享稀疏奖励（仅成功交付时给予正奖励）。

### QMIX 架构
联合动作价值分解为：
Q_tot(𝝉, 𝒂) = f_mix(Q_1(τ_1, a_1), ..., Q_n(τ_n, a_n); s)

其中 f_mix 是混合网络，权重由以全局状态 s 为条件的超网络生成，通过非负权重保证单调性约束 ∂Q_tot/∂Q_i ≥ 0，确保个体贪心动作与联合最优一致。

### 智能体网络
- 使用 GRU（64 隐藏单元）处理部分可观测性，维持对未观测状态的信念
- 混合网络采用 2 层超网络，64 维嵌入

### 关键训练配置（相对默认的改动）
| 参数 | 默认值 | 优化值 | 理由 |
|------|--------|--------|------|
| Batch Size | 32 | 256 | 稳定梯度 |
| Buffer Size | 5,000 | 200,000 | 长训练周期中保留多样化经验 |
| Epsilon Anneal | 50,000 | 5,000,000 | 过快衰减阻止稀疏奖励发现 |
| Training Steps | 2,000,000 | 20,000,000 | 稀疏奖励需要更长探索期 |
| Learning Rate | 0.0005 | 0.0005 | 不变 |

### Unity 集成
- 3 个智能体、6 个离散动作、36 维观测（含 LIDAR 式感知）、200 步 episode
- no-graphics 模式 + 50 倍时间缩放，1M 步约 6 小时

### 对比算法
- IPPO（独立学习）：vanilla 与 advanced 两种配置
- MASAC（集中式 critic + 最大熵）：仅在 MPE 上验证，未在 RWARE 评估

## 关键创新

1. **稀疏奖励下的超参数敏感性揭示**：作者首次系统量化了 epsilon 退火时间对稀疏奖励任务的决定性影响——从默认 50K 步延长至 5M 步（100 倍）是学习发生的必要条件。这一发现直接挑战了 MARL 库中"通用默认配置"的假设，为工业部署提供了可操作的调参准则。

2. **跨环境迁移验证**：从 RWARE 网格环境到 Unity 3D 物理环境的策略迁移，证明了 QMIX 学到的协调策略不依赖特定环境表示。1M 步后策略近乎确定性（回报标准差 < 0.01），表明价值分解方法能够提取出可泛化的隐式协调策略，而非过拟合网格特征。

3. **扩展性成本的量化**：通过 2→6 智能体的系统对比，揭示了联合动作空间按 |A|^n 增长（6 智能体产生 15,625 个联合动作）带来的性能退化规律——3 倍智能体增加导致 55% 性能下降和 2 倍训练步数需求。这为工业规模（50+ 机器人）的可行性评估提供了首个量化基准。

## 实验与结果

### MPE 验证
- MASAC 在 30,000 步（200 episodes）收敛，平均奖励 -55，比随机基线提升 63%
- 样本复杂度差异：MPE 30K 步 vs RWARE 20M 步，约 600 倍差距

### RWARE 算法对比（测试回报）
| 算法 | tiny-2ag-v2 | small-4ag-v1 | 训练步数 |
|------|-------------|--------------|----------|
| QMIX | 3.25 | 2.10 | 20-30M |
| IPPO (Advanced) | 0.38 | — | 5M |
| IPPO (Vanilla) | 0.13 | — | 20M |
| Random Baseline | 0.05 | 0.02 | N/A |

QMIX 比 advanced IPPO 提升 755%（8.5 倍）；vanilla IPPO 即使训练 4 倍长仅达 0.13，比 QMIX 低 25 倍。

### QMIX 扩展性
| 环境 | 智能体数 | 测试回报 | 训练步数 |
|------|----------|----------|----------|
| tiny-2ag-v2 | 2 | 3.25 | 20M |
| small-4ag-v1 | 4 | 2.10 | 30M |
| medium-6ag-v1 | 6 | 1.45 | 40M |

从 2 到 6 智能体：训练步数 2 倍（20M→40M），性能下降 55%，有效成本增加 4-5 倍。

### Unity 部署
| 训练步数 | 测试回报均值 | 峰值回报 | 训练时间 |
|----------|--------------|----------|----------|
| 500K | 95.2 | ~150 | 3h 2min |
| 1M | 238.6 | 443 | 6h 18min |

1M 步后策略近乎确定性（标准差 < 0.01），导航平滑；500K 步时存在障碍物附近犹豫、路径低效等次优行为。

## 边界与局限

- 实验仅限仿真，未进行物理系统迁移；真实部署需额外领域适应和鲁棒感知
- 测试规模仅 2-6 智能体，受计算资源约束；工业仓库需 50+ 机器人时，性能退化与训练需求超线性增长使可扩展性成显著挑战
- RWARE 简化任务未涵盖库存管理和动态障碍物规避
- 20M+ 步训练需求对快速部署场景不切实际
- 未在 RWARE 上评估 MASAC（仅在 MPE 验证）
- 未探索层次化方法、课程学习或 sim-to-real 迁移（列为未来工作）
- 论文未明确推理频率与数据量细节

## 工程启示

复现时首先核对 epsilon 退火配置——这是最容易踩坑的点：默认 50K 步退火必然导致零学习，必须延长至至少 5M 步。其次确认 buffer size 至少 200K，batch size 256，总训练步数不低于 20M。硬件方面，RWARE 训练在 Mac M3 16GB 即可完成，但 Unity 环境需要服务器级配置（Xeon E5-2680 + 196GB RAM），1M 步约 6 小时。

对于下游团队，建议从 tiny-2ag-v2 起步验证实现正确性（目标回报 3.25），再迁移到 small-4ag-v1（目标 2.10）。若使用 IPPO 作为基线，需注意 advanced 配置（5M 步）与 vanilla（20M 步）的性能差异巨大，对比时需明确训练预算。Unity 部署时建议先跑 500K 步观察次优行为（障碍物犹豫、路径低效），再决定是否延长至 1M 步以获得确定性策略。代码与文档已开源（GitHub: pallman14/MARL-QMIX-Warehouse-Robots），可直接复现。

## 参考
- http://arxiv.org/abs/2512.04463v2

## Overview

This paper systematically evaluates the QMIX value decomposition method in warehouse multi-robot coordination tasks. The authors address the issue of zero learning under default MARL configurations in sparse-reward settings through large-scale hyperparameter tuning (epsilon annealing extended to 5M steps, buffer expanded to 200K), and validate scalability from 2 to 6 agents in both the RWARE grid environment and the Unity 3D continuous environment. The core contribution lies in revealing the critical sensitivity of training configurations in sparse-reward warehouse tasks and providing a reproducible engineering baseline.

## What It Changes

The true value of this work lies not in proposing a new algorithm, but in debunking a default industry assumption: that standard MARL hyperparameter configurations can be directly transferred to sparse-reward warehouse tasks. The authors demonstrate through a 2M-step zero-learning failure case that default configurations (batch 32, buffer 5K, epsilon annealing 50K steps) completely fail on RWARE. This explains why many industrial MARL pilot projects fail early—the problem lies not in the algorithm, but in the mismatch between training configuration and task reward density.

More importantly, the authors compare QMIX and IPPO under the same training budget, revealing the structural advantage of value decomposition methods in credit assignment: QMIX achieves a return of 3.25 on tiny-2ag, while vanilla IPPO reaches only 0.13 even when trained for 4 times more steps (20M vs 5M)—a 25-fold gap. This changes our understanding of whether "independent learning is sufficient"—in warehouse scenarios requiring implicit coordination, centralized value decomposition is not optional but necessary. Furthermore, the authors validate for the first time the transferability of QMIX policies in the Unity physics engine, achieving a mean return of 238.6 after 1M steps with near-deterministic policies, indicating that policies trained in simulation have direct deployment potential.

## Method Breakdown

### Problem Modeling
Warehouse coordination is modeled as a Dec-POMDP, where each agent i receives local observation o_i, with action space 𝒜 = {left, right, forward, load/unload, no-op}, and the team shares a sparse reward (positive reward only upon successful delivery).

### QMIX Architecture
The joint action value is decomposed as:
Q_tot(𝝉, 𝒂) = f_mix(Q_1(τ_1, a_1), ..., Q_n(τ_n, a_n); s)

where f_mix is a mixing network whose weights are generated by a hypernetwork conditioned on the global state s, with non-negative weights ensuring the monotonicity constraint ∂Q_tot/∂Q_i ≥ 0, guaranteeing that individual greedy actions align with the joint optimum.

### Agent Network
- Uses GRU (64 hidden units) to handle partial observability and maintain beliefs about unobserved states
- Mixing network employs a 2-layer hypernetwork with 64-dimensional embeddings

### Key Training Configurations (Changes from Default)
| Parameter | Default | Optimized | Rationale |
|-----------|---------|-----------|-----------|
| Batch Size | 32 | 256 | Stable gradients |
| Buffer Size | 5,000 | 200,000 | Preserve diverse experiences over long training |
| Epsilon Anneal | 50,000 | 5,000,000 | Rapid decay prevents sparse reward discovery |
| Training Steps | 2,000,000 | 20,000,000 | Sparse rewards require longer exploration |
| Learning Rate | 0.0005 | 0.0005 | Unchanged |

### Unity Integration
- 3 agents, 6 discrete actions, 36-dimensional observations (including LIDAR-style perception), 200-step episodes
- no-graphics mode + 50x time scaling, 1M steps in approximately 6 hours

### Comparison Algorithms
- IPPO (independent learning): vanilla and advanced configurations
- MASAC (centralized critic + maximum entropy): validated only on MPE, not evaluated on RWARE

## Key Innovations

1. **Revealing Hyperparameter Sensitivity in Sparse-Reward Settings**: The authors systematically quantify for the first time the decisive impact of epsilon annealing duration on sparse-reward tasks—extending from the default 50K steps to 5M steps (100x) is a necessary condition for learning to occur. This finding directly challenges the assumption of "universal default configurations" in MARL libraries, providing actionable tuning guidelines for industrial deployment.

2. **Cross-Environment Transfer Validation**: Policy transfer from the RWARE grid environment to the Unity 3D physics environment demonstrates that QMIX-learned coordination policies do not depend on specific environment representations. After 1M steps, policies become near-deterministic (return standard deviation < 0.01), indicating that value decomposition methods can extract generalizable implicit coordination policies rather than overfitting to grid features.

3. **Quantifying Scalability Costs**: Through systematic comparison from 2 to 6 agents, the authors reveal the performance degradation pattern caused by the joint action space growing as |A|^n (6 agents produce 15,625 joint actions)—a 3x increase in agents leads to a 55% performance drop and 2x training step requirements. This provides the first quantitative benchmark for feasibility assessment at industrial scale (50+ robots).

## Experiments and Results

### MPE Validation
- MASAC converges in 30,000 steps (200 episodes), achieving average reward of -55, a 63% improvement over random baseline
- Sample complexity difference: MPE 30K steps vs RWARE 20M steps, approximately 600x gap

### RWARE Algorithm Comparison (Test Returns)
| Algorithm | tiny-2ag-v2 | small-4ag-v1 | Training Steps |
|-----------|-------------|--------------|----------------|
| QMIX | 3.25 | 2.10 | 20-30M |
| IPPO (Advanced) | 0.38 | — | 5M |
| IPPO (Vanilla) | 0.13 | — | 20M |
| Random Baseline | 0.05 | 0.02 | N/A |

QMIX improves over advanced IPPO by 755% (8.5x); vanilla IPPO reaches only 0.13 even with 4x longer training, 25x lower than QMIX.

### QMIX Scalability
| Environment | Agents | Test Return | Training Steps |
|-------------|--------|-------------|----------------|
| tiny-2ag-v2 | 2 | 3.25 | 20M |
| small-4ag-v1 | 4 | 2.10 | 30M |
| medium-6ag-v1 | 6 | 1.45 | 40M |

From 2 to 6 agents: training steps double (20M→40M), performance drops 55%, effective cost increases 4-5x.

### Unity Deployment
| Training Steps | Mean Test Return | Peak Return | Training Time |
|----------------|------------------|-------------|---------------|
| 500K | 95.2 | ~150 | 3h 2min |
| 1M | 238.6 | 443 | 6h 18min |

After 1M steps, policies become near-deterministic (standard deviation < 0.01) with smooth navigation; at 500K steps, suboptimal behaviors such as hesitation near obstacles and inefficient paths are observed.

## Boundaries and Limitations

- Experiments are simulation-only, without physical system transfer; real deployment requires additional domain adaptation and robust perception
- Test scale limited to 2-6 agents due to computational constraints; at industrial scale (50+ robots), superlinear growth in performance degradation and training requirements makes scalability a significant challenge
- RWARE simplified tasks do not cover inventory management and dynamic obstacle avoidance
- 20M+ step training requirements are impractical for rapid deployment scenarios
- MASAC not evaluated on RWARE (validated only on MPE)
- Hierarchical methods, curriculum learning, and sim-to-real transfer not explored (listed as future work)
- Paper does not specify inference frequency and data volume details

## Engineering Insights

When reproducing, first verify the epsilon annealing configuration—this is the most common pitfall: the default 50K-step annealing will inevitably result in zero learning and must be extended to at least 5M steps. Next, confirm buffer size of at least 200K, batch size of 256, and total training steps of no less than 20M. On the hardware side, RWARE training can be completed on a Mac M3 16GB, but the Unity environment requires server-grade hardware (Xeon E5-2680 + 196GB RAM), with 1M steps taking approximately 6 hours.

For downstream teams, it is recommended to start with tiny-2ag-v2 to validate implementation correctness (target return 3.25), then transfer to small-4ag-v1 (target 2.10). If using IPPO as a baseline, note the significant performance difference between advanced (5M steps) and vanilla (20M steps) configurations—training budgets must be clearly specified when comparing. For Unity deployment, it is advisable to first run 500K steps to observe suboptimal behaviors (obstacle hesitation, inefficient paths) before deciding whether to extend to 1M steps for deterministic policies. Code and documentation are open-sourced (GitHub: pallman14/MARL-QMIX-Warehouse-Robots) and can be directly reproduced.

## 개요

본 논문은 QMIX 가치 분해 방법론이 창고 다중 로봇 조정 작업에서 보이는 성능을 체계적으로 평가한다. 저자들은 대규모 하이퍼파라미터 튜닝(엡실론 어닐링을 5M 스텝으로 연장, 버퍼를 200K로 확장)을 통해 희소 보상 환경에서 기본 MARL 설정이 전혀 학습하지 못하는 문제를 해결했으며, RWARE 그리드 환경과 Unity 3D 연속 환경에서 2개에서 6개 에이전트로의 확장성을 검증했다. 핵심 기여는 희소 보상 창고 작업에서 훈련 설정의 결정적 민감성을 밝혀내고 재현 가능한 엔지니어링 기준선을 제공한 것이다.

## 무엇을 바꾸었는가

이 작업의 진정한 가치는 새 알고리즘을 제안한 것이 아니라, 표준 MARL 하이퍼파라미터 설정이 희소 보상 창고 작업에 직접 이전될 수 있다는 업계의 암묵적 가정을 깨뜨린 데 있다. 저자들은 2M 스텝 동안 학습이 전혀 이루어지지 않은 실패 사례를 통해 기본 설정(batch 32, buffer 5K, epsilon 어닐링 50K 스텝)이 RWARE에서 완전히 무력화됨을 증명했다. 이는 많은 산업용 MARL 파일럿 프로젝트가 초기에 실패하는 이유를 설명한다—문제는 알고리즘이 아니라 훈련 설정과 작업 보상 밀도의 불일치에 있다.

더 중요하게, 저자들은 QMIX와 IPPO를 동일한 훈련 예산 하에 비교하여 가치 분해 방법론이 신용 할당에서 가지는 구조적 우위를 밝혔다: QMIX는 tiny-2ag에서 3.25의 보상을 달성한 반면, vanilla IPPO는 4배 더 긴 훈련(20M vs 5M)에도 불구하고 0.13에 그쳐 25배의 격차를 보였다. 이는 "독립 학습만으로 충분한가"에 대한 우리의 인식을 바꾼다—암묵적 조정이 필요한 창고 시나리오에서 중앙 집중식 가치 분해는 선택 사항이 아니라 필수 조건이다. 또한 저자들은 Unity 물리 엔진에서 QMIX 정책의 이전 가능성을 처음으로 검증했으며, 1M 스텝 후 평균 보상 238.6에 정책이 거의 결정론적임을 보여 시뮬레이션 훈련 정책이 직접 배포 가능한 잠재력을 지님을 시사한다.

## 방법 분석

### 문제 모델링
창고 조정을 Dec-POMDP로 모델링하며, 각 에이전트 i는 부분 관측 o_i를 수신하고, 행동 공간 𝒜 = {left, right, forward, load/unload, no-op}을 가지며, 팀은 희소 보상을 공유한다(성공적인 배달 시에만 양의 보상).

### QMIX 아키텍처
결합 행동 가치를 다음과 같이 분해한다:
Q_tot(𝝉, 𝒂) = f_mix(Q_1(τ_1, a_1), ..., Q_n(τ_n, a_n); s)

여기서 f_mix는 혼합 네트워크이며, 가중치는 전역 상태 s를 조건으로 하는 하이퍼네트워크에 의해 생성된다. 비음수 가중치를 통해 단조성 제약 ∂Q_tot/∂Q_i ≥ 0을 보장하여 개별 탐욕 행동이 결합 최적과 일치하도록 한다.

### 에이전트 네트워크
- GRU(64 은닉 유닛)를 사용하여 부분 관측 가능성을 처리하고 관측되지 않은 상태에 대한 신념 유지
- 혼합 네트워크는 2계층 하이퍼네트워크, 64차원 임베딩 사용

### 핵심 훈련 설정(기본값 대비 변경)
| 파라미터 | 기본값 | 최적값 | 이유 |
|------|--------|--------|------|
| Batch Size | 32 | 256 | 그래디언트 안정화 |
| Buffer Size | 5,000 | 200,000 | 긴 훈련 주기에서 다양한 경험 보존 |
| Epsilon Anneal | 50,000 | 5,000,000 | 너무 빠른 감쇠는 희소 보상 발견을 방해 |
| Training Steps | 2,000,000 | 20,000,000 | 희소 보상은 더 긴 탐색 기간 필요 |
| Learning Rate | 0.0005 | 0.0005 | 변경 없음 |

### Unity 통합
- 3개 에이전트, 6개 이산 행동, 36차원 관측(LIDAR 방식 인식 포함), 200스텝 에피소드
- no-graphics 모드 + 50배 시간 스케일링, 1M 스텝 약 6시간

### 비교 알고리즘
- IPPO(독립 학습): vanilla 및 advanced 두 가지 설정
- MASAC(중앙 집중식 critic + 최대 엔트로피): MPE에서만 검증, RWARE에서는 평가되지 않음

## 핵심 혁신

1. **희소 보상 하에서 하이퍼파라미터 민감성 규명**: 저자들은 엡실론 어닐링 시간이 희소 보상 작업에 미치는 결정적 영향을 처음으로 체계적으로 정량화했다—기본 50K 스텝에서 5M 스텝(100배)으로 연장하는 것이 학습 발생의 필요 조건이다. 이 발견은 MARL 라이브러리의 "범용 기본 설정" 가정에 직접 도전하며 산업 배포를 위한 실행 가능한 튜닝 지침을 제공한다.

2. **교차 환경 이전 검증**: RWARE 그리드 환경에서 Unity 3D 물리 환경으로의 정책 이전은 QMIX가 학습한 조정 정책이 특정 환경 표현에 의존하지 않음을 증명한다. 1M 스텝 후 정책이 거의 결정론적(보상 표준편차 < 0.01)이라는 것은 가치 분해 방법이 그리드 특징에 과적합된 것이 아니라 일반화 가능한 암묵적 조정 정책을 추출할 수 있음을 보여준다.

3. **확장성 비용의 정량화**: 2→6 에이전트의 체계적 비교를 통해 결합 행동 공간이 |A|^n으로 증가(6개 에이전트는 15,625개의 결합 행동 생성)함에 따른 성능 저하 패턴을 규명했다—3배 에이전트 증가는 55% 성능 하락과 2배 훈련 스텝 요구를 초래한다. 이는 산업 규모(50+ 로봇) 타당성 평가를 위한 최초의 정량적 기준을 제공한다.

## 실험 및 결과

### MPE 검증
- MASAC는 30,000 스텝(200 에피소드)에서 수렴, 평균 보상 -55, 무작위 기준선 대비 63% 향상
- 샘플 복잡도 차이: MPE 30K 스텝 vs RWARE 20M 스텝, 약 600배 격차

### RWARE 알고리즘 비교(테스트 보상)
| 알고리즘 | tiny-2ag-v2 | small-4ag-v1 | 훈련 스텝 |
|------|-------------|--------------|----------|
| QMIX | 3.25 | 2.10 | 20-30M |
| IPPO (Advanced) | 0.38 | — | 5M |
| IPPO (Vanilla) | 0.13 | — | 20M |
| Random Baseline | 0.05 | 0.02 | N/A |

QMIX는 advanced IPPO 대비 755%(8.5배) 향상; vanilla IPPO는 4배 더 긴 훈련에도 0.13에 그쳐 QMIX보다 25배 낮음.

### QMIX 확장성
| 환경 | 에이전트 수 | 테스트 보상 | 훈련 스텝 |
|------|----------|----------|----------|
| tiny-2ag-v2 | 2 | 3.25 | 20M |
| small-4ag-v1 | 4 | 2.10 | 30M |
| medium-6ag-v1 | 6 | 1.45 | 40M |

2개에서 6개 에이전트: 훈련 스텝 2배(20M→40M), 성능 55% 하락, 유효 비용 4-5배 증가.

### Unity 배포
| 훈련 스텝 | 테스트 보상 평균 | 최고 보상 | 훈련 시간 |
|----------|--------------|----------|----------|
| 500K | 95.2 | ~150 | 3h 2min |
| 1M | 238.6 | 443 | 6h 18min |

1M 스텝 후 정책은 거의 결정론적(표준편차 < 0.01), 내비게이션 매끄러움; 500K 스텝에서는 장애물 근처 주저함, 비효율적 경로 등의 차선 행동 존재.

## 경계 및 한계

- 실험은 시뮬레이션에 국한, 물리 시스템 이전 없음; 실제 배포는 추가 도메인 적응과 강건한 인식 필요
- 테스트 규모는 2-6 에이전트에 국한, 계산 자원 제약; 산업용 창고의 50+ 로봇에서는 성능 저하와 훈련 요구의 초선형 증가로 확장성이 중요한 도전 과제
- RWARE 단순화 작업은 재고 관리와 동적 장애물 회피를 포함하지 않음
- 20M+ 스텝 훈련 요구는 빠른 배포 시나리오에 비현실적
- RWARE에서 MASAC 평가 없음(MPE에서만 검증)
- 계층적 방법, 커리큘럼 학습 또는 sim-to-real 이전 탐구 없음(향후 작업으로 분류)
- 논문은 추론 빈도와 데이터량 세부 사항을 명시하지 않음

## 엔지니어링 시사점

재현 시 먼저 엡실론 어닐링 설정을 확인해야 한다—이것이 가장 쉽게 실수하는 지점이다: 기본 50K 스텝 어닐링은 반드시 학습 실패를 초래하므로 최소 5M 스텝으로 연장해야 한다. 다음으로 버퍼 크기를 최소 200K, 배치 크기 256, 총 훈련 스텝 20M 이상을 확인한다. 하드웨어 측면에서 RWARE 훈련은 Mac M3 16GB로 충분하지만, Unity 환경은 서버급 구성(Xeon E5-2680 + 196GB RAM)이 필요하며 1M 스텝에 약 6시간이 소요된다.

하류 팀에게는 tiny-2ag-v2에서 시작하여 구현 정확성을 검증(목표 보상 3.25)한 후 small-4ag-v1(목표 2.10)로 이전할 것을 권장한다. IPPO를 기준선으로 사용할 경우 advanced 설정(5M 스텝)과 vanilla(20M 스텝)의 성능 차이가 크므로 비교 시 훈련 예산을 명확히 해야 한다. Unity 배포 시 먼저 500K 스텝을 실행하여 차선 행동(장애물 주저, 비효율적 경로)을 관찰한 후 결정론적 정책을 위해 1M 스텝으로 연장할지 결정하는 것이 좋다. 코드와 문서는 오픈소스(GitHub: pallman14/MARL-QMIX-Warehouse-Robots)로 제공되어 직접 재현할 수 있다.
