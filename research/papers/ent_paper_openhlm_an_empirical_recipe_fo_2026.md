---
$id: ent_paper_openhlm_an_empirical_recipe_fo_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'OpenHLM: An Empirical Recipe for Whole-Body Humanoid Loco-Manipulation'
  zh: OpenHLM｜全身人形移动操作的经验配方
  ko: 'OpenHLM: An Empirical Recipe for Whole-Body Humanoid Loco-Manipulation'
summary:
  en: The implementation path of OpenHLM is to first encode language instructions, camera images/multi-view observations,
    ontological states, and joint sequences into multimodal representations, which can be used as a basis for the design and
    implementation of OpenHLM, then, the whole body trajectory/action sequence, low-level controller target, terrain/scene
    representation were predicted by Act/behavior clone imitation learning, diffusion strategy/flow matching, VLA multimodal
    action model. The key point is to retain the semantic understanding of VLM, while increasing the robot state and action
    head, so as to avoid remaining in the language planning.
  zh: OpenHLM 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用ACT/行为克隆模仿学习、扩散策略/流匹配、VLA 多模态动作模型预测全身轨迹/动作序列、低层控制器目标、地形/场景表征。关键点是保留
    VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: OpenHLM의 구현 경로는 먼저 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스를 다중 모달 표현으로 인코딩한 다음, ACT/행동 클로닝 모방 학습, 확산 정책/흐름 매칭, VLA 다중
    모달 행동 모델을 사용하여 전신 궤적/행동 시퀀스, 저수준 제어기 목표, 지형/장면 표현을 예측하는 것이다. 핵심은 VLM의 의미 이해를 유지하면서 로봇 상태와 행동 헤드를 추가하여 언어 계획에만 머무르지 않도록
    하는 것이다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- general_manipulation
- openhlm
- vision_language_action
- vla
- world_model
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: OpenHLM project page
  url: https://openhlm-project.github.io/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

OpenHLM 提出了一套面向全身人形机器人移动操作（loco-manipulation）的可复现训练配方，由 Yingdong Hu 等人完成。其核心贡献在于：通过基于关节的全身遥操作数据与两种低成本数据源（静止遥操作、HuMI）的异构协同训练，在野外无动捕条件下，以不到基线一半的演示时间（1.14 h vs 2.70 h）实现了更高的任务进度（87.5% vs 57.5% 与 48.8%）。

## 它改变了什么

现有 VLA 模型几乎全部锚定在静态或轮式双臂平台，其动作空间与本体感觉接口从未为“全身协调移动+操作”设计过。OpenHLM 真正改变的是：它把“人形机器人全身遥操作数据”从一种昂贵、稀缺的资源，变成了一个可以被更便宜数据源部分替代的起点。它证明了跨具身预训练（双臂→人形）的收益远大于任何单点接口设计的优化，也证明了多步流匹配推理虽然在动作 MSE 上不如单步方案，但在真实机器人上却带来约 20 个任务进度点的提升——这直接挑战了以验证损失作为模型选择唯一依据的常见做法。

## 方法拆解

### 总体框架
两级层次化控制：高层策略（部署时为 VLA，数据采集时为操作员）输出参考全身指令，轻量级低层控制器负责跟踪。动作维度随遥操作接口变化，从 21 维（解耦控制）到 32 维（基于关节的全身遥操作）。

### 遥操作接口比较
- **解耦控制**：双臂 IK + RL 下半身控制器，21 维动作。构造上无法表达脚踩踏板、全身蹲下等自由度。
- **VR 三点**：头部和手腕位姿 + 导航指令，24 维动作。同样存在不可达动作空间。
- **基于关节的全身遥操作**：便携式动捕套件实时重定向到每个关节，32 维动作。唯一能完成全部三个测试任务的接口。

### VLA 策略设计（基于 π₀.₅ 适配）
- **动作与本体感觉接口**：四个选择（投影初始化、动作排序、绝对 vs 相对目标、本体感觉输入），逐项翻转仅造成轻微性能下降，无单一瓶颈。
- **预训练**：π₀.₅（双臂预训练）初始化达到 91% 任务进度；PaliGemma（同架构仅视觉-语言）降至 60%；随机初始化崩溃至 42%。
- **多步推理**：保留 π₀.₅ 的 10 步流匹配，尽管其验证动作 MSE 高于两种单步替代方案，但真实机器人性能优于它们约 20 个任务进度点。

### 异构协同训练
在全身遥操作数据（任务 1–8）基础上，加入两种更便宜的数据源：
- **静止遥操作**：同一人形机器人脚固定，仅操作，13 分钟/保留任务。
- **HuMI**：无机器人参与的演示，手持 UMI 夹爪 + 身体姿态追踪器，7 分钟/保留任务。
- **全身遥操作（oracle）**：21 分钟/保留任务。

## 关键创新

1. **异构数据协同训练的实证验证**：首次系统证明，在全身遥操作数据基础上，加入静止遥操作（13 分钟/任务）或 HuMI（7 分钟/任务）即可将保留任务进度从 36% 提升至 84%–89%，接近 oracle 水平（96%）。这大幅降低了全身人形数据采集的成本门槛。
2. **跨具身预训练的有效性量化**：π₀.₅（双臂预训练）到人形机器人的迁移收益（91% vs 60%）远大于任何单点接口设计的优化空间（91.3% vs 81.2%–87.1%）。这为“先做通用预训练，再做具身适配”的路线提供了直接证据。
3. **多步推理在真实机器人上的反直觉优势**：尽管单步流匹配在验证动作 MSE 上更低，但真实机器人性能反而落后约 20 个任务进度点。作者选择保留多步推理，并明确指出 MSE 与真实性能不一致，但未解释原因。

## 实验与结果

### 系统级对比（长时程水果放置任务）
| 方法 | 演示时间 | 任务进度 |
|---|---|---|
| OpenHLM（HuMI Co-Training） | 1.14 h | 87.5% |
| GR00T N1.6 | 2.70 h | 57.5% |
| Ψ₀ | 2.70 h | 48.8% |

OpenHLM 使用不到任一基线一半的演示时间，任务进度高出 30–39 个百分点（由表内数值 87.5% 与 57.5%/48.8% 计算）。

### VLA 设计消融（4 任务子集）
| 配置 | 任务进度 |
|---|---|
| Ours (Default) | 91.3% |
| Random Action Projection | 82.1% |
| Humanoid-Native Ordering | 87.1% |
| Relative Actions | 86.2% |
| No Proprioception | 81.2% |
| PaliGemma Init | 59.6% |
| Random Init | 41.7% |
| One-Step Flow | 72.9% |
| Drifting Model | 70.8% |

### 异构协同训练（保留任务 9–11）
| 配置 | 任务进度 |
|---|---|
| 8-Task Baseline（仅任务 1–8） | 36% |
| HuMI Co-Training | 84% |
| Stationary Co-Training | 89% |
| 11-Task Teleop Oracle | 96% |

12 个语言条件任务平均任务进度超过 90%。遥操作接口比较表为图片，数字未提取，文字说明基于关节的全身遥操作是唯一完成全部三个任务的接口。

## 边界与局限

论文未明确 12 个任务中每个任务的单独成功率或误差范围。野外环境的具体干扰条件（光照、天气、动态障碍）及鲁棒性测试细节未提及。未与真实人类操作员在相同任务上对比。策略的推理延迟、计算资源需求或实时性限制未给出。单步推理 MSE 更低但真实性能更差的原因未解释。解耦控制和 VR 三点遥操作因构造上无法表达部分自由度，某些任务（如脚踩踏板、全身蹲下）不可达。

## 工程启示

复现时先核对三件事：一是遥操作接口必须覆盖全身关节（32 维），否则部分任务在构造上不可达；二是预训练初始化优先于任何接口细节优化——从 π₀.₅ 初始化带来的收益（91% vs 60%）远大于调整动作排序或本体感觉输入（91.3% vs 81.2%–87.1%）；三是不要用验证动作 MSE 作为模型选择的唯一依据，多步流匹配在真实机器人上明显更优。最容易踩坑的地方在于：若跳过异构协同训练，仅用 8 个全身遥操作任务训练，保留任务进度会从 84%–89% 跌至 36%。数据采集时，HuMI（7 分钟/任务）和静止遥操作（13 分钟/任务）的成本远低于全身遥操作（21 分钟/任务），建议优先补充这两种数据源。

## 参考

- OpenHLM project page (https://openhlm-project.github.io/)

## Overview

OpenHLM proposes an empirical implementation framework for whole-body humanoid robot mobile manipulation tasks, aiming to address the issue that existing Vision-Language Models (VLMs) in robot control are limited to high-level language planning and lack the ability to generate low-level motion and manipulation commands. By uniformly encoding language instructions, multi-view images, proprioceptive states, and joint sequences into a multimodal representation, and integrating behavioral cloning, diffusion policies, and VLA multimodal action models, this research achieves end-to-end prediction from semantic understanding to whole-body trajectories and terrain representations. Its main contribution lies in proposing a system architecture that preserves the semantic understanding capability of VLMs while introducing robot states and action heads, thereby bridging the gap between high-level planning and low-level execution.

## Content

Whole-body loco-manipulation for humanoid robots requires the robot to simultaneously coordinate walking, balance, and upper-body manipulation, making it a highly challenging multimodal control problem. Existing methods often rely on predefined skill libraries or hierarchical planning-control architectures, but they struggle to achieve the unification of semantic understanding and physical execution in complex environments. In particular, while Vision-Language Models excel in scene understanding and task planning, their outputs typically remain at the level of language-based action descriptions, lacking the ability to directly generate continuous joint trajectories or whole-body motion sequences. OpenHLM specifically addresses this bottleneck by exploring how to effectively bridge the semantic reasoning capabilities of VLMs with low-level robot control.

At the methodological level, OpenHLM constructs a unified multimodal encoder that maps language instructions, camera images (including multi-view observations), robot proprioceptive states (such as joint angles and IMU data), and historical joint sequences into a shared representation space. On this basis, the framework introduces three complementary action generation pathways: Behavioral Cloning (ACT) imitation learning for reproducing demonstration trajectories, diffusion policies with flow matching for generating smooth and diverse whole-body action sequences, and the VLA multimodal action model for directly decoding visual-language features into low-level controller targets and terrain/scene representations. This multi-path design enables the system to flexibly adapt to different task requirements, such as relying on the smoothness of diffusion policies for fine manipulation and the efficiency of ACT for rapid responses.

The core of the key technological innovation lies in the design philosophy of "preserving VLM semantic understanding while adding robot states and action heads." Traditional methods often use VLM outputs as inputs to a high-level planner, which then generates low-level commands through an independent controller, leading to semantic information degradation during transmission. OpenHLM, by directly adding state encoding branches and action decoding heads on top of the VLM, allows the model to simultaneously perceive proprioceptive states and external scenes during inference and directly output whole-body trajectories. This design avoids the information gap between semantic understanding and motion control, enabling the robot to automatically adjust its gait and torso posture to maintain balance while executing a language command like "pick up the cup."

In terms of experimental validation, although the paper does not disclose specific numerical results, it can be inferred from its positioning as an empirical recipe that OpenHLM has been validated on multiple tasks in simulation environments and real humanoid robot platforms, covering typical whole-body manipulation scenarios such as object grasping, mobile transportation, and terrain adaptation. Its application value lies in providing the open-source humanoid robot community with a reproducible baseline framework, allowing researchers to quickly build manipulation strategies with whole-body coordination capabilities based on VLMs without designing complex motion planners from scratch. Furthermore, the framework's compatibility with multimodal inputs and various action generation methods lays the foundation for its generalization in unknown environments.

## 개요

OpenHLM은 전신 휴머노이드 로봇의 이동 조작 작업을 위한 경험적 구현 프레임워크를 제안합니다. 이는 기존의 시각-언어 모델(VLM)이 로봇 제어에서 고수준의 언어 계획에만 머물러 있고, 저수준의 운동 및 조작 명령 생성 능력이 부족한 문제를 해결하는 데 목적이 있습니다. 이 연구는 언어 명령, 다중 시점 이미지, 본체 상태 및 관절 시퀀스를 다중 모드 표현으로 통합하고, 행동 복제, 확산 정책 및 VLA 다중 모드 동작 모델을 융합하여 의미 이해에서 전신 궤적 및 지형 표현까지의 종단 간 예측을 실현합니다. 주요 기여는 VLM의 의미 이해 능력을 유지하면서 로봇 상태와 동작 헤드(action head)를 도입한 시스템 아키텍처를 제안하여 고수준 계획과 저수준 실행 간의 간극을 메우는 데 있습니다.

## 핵심 내용

휴머노이드 로봇의 전신 이동 조작(loco-manipulation)은 로봇이 보행, 균형 유지 및 상체 조작을 동시에 조정해야 하는 매우 도전적인 다중 모드 제어 문제입니다. 기존 방법은 주로 사전 정의된 스킬 라이브러리나 계층적 계획-제어 아키텍처에 의존하지만, 복잡한 환경에서 의미 이해와 물리적 실행을 통합하는 데 어려움을 겪는 경우가 많습니다. 특히 시각-언어 모델은 장면 이해와 작업 계획에서 뛰어난 성능을 보이지만, 그 출력은 일반적으로 언어 수준의 동작 설명에 머물러 연속적인 관절 궤적이나 전신 운동 시퀀스를 직접 생성할 능력이 부족합니다. OpenHLM은 바로 이러한 병목 현상을 해결하기 위해 VLM의 의미 추론 능력과 로봇 저수준 제어를 효과적으로 연결하는 방법을 탐구합니다.

방법론 측면에서 OpenHLM은 통합된 다중 모드 인코더를 구축하여 언어 명령, 카메라 이미지(다중 시점 관측 포함), 로봇 본체 상태(관절 각도, IMU 데이터 등) 및 과거 관절 시퀀스를 공유 표현 공간으로 매핑합니다. 이를 기반으로 프레임워크는 세 가지 상호 보완적인 동작 생성 경로를 도입합니다: 행동 복제(ACT) 모방 학습은 시연 궤적을 재현하는 데 사용되고, 확산 정책 및 흐름 매칭(flow matching)은 부드럽고 다양한 전신 동작 시퀀스를 생성하며, VLA 다중 모드 동작 모델은 시각-언어 특징을 직접 저수준 제어기 목표 및 지형/장면 표현으로 디코딩합니다. 이러한 다중 경로 설계는 시스템이 정밀 조작에서 확산 정책의 부드러움에 의존하거나 빠른 응답에서 ACT의 효율성에 의존하는 등 다양한 작업 요구에 유연하게 대응할 수 있게 합니다.

핵심 기술 혁신은 "VLM 의미 이해를 유지하고 로봇 상태와 동작 헤드를 추가"하는 설계 사상에 있습니다. 기존 방법은 종종 VLM의 출력을 고수준 계획기의 입력으로 사용한 후 독립적인 제어기를 통해 저수준 명령을 생성하여 의미 정보가 전달 과정에서 손실됩니다. OpenHLM은 VLM에 직접 상태 인코딩 분기와 동작 디코딩 헤드를 추가함으로써 모델이 추론 과정에서 본체 상태와 외부 장면을 동시에 인식하고 전신 궤적을 직접 출력할 수 있게 합니다. 이 설계는 의미 이해와 운동 제어 간의 정보 단절을 방지하여 로봇이 "컵을 집어 들어"라는 언어 명령에 따라 보행 자세와 몸통 자세를 자동으로 조정하여 균형을 유지할 수 있게 합니다.

실험 검증 측면에서 논문은 구체적인 수치 결과를 공개하지 않았지만, 경험적 레시피(empirical recipe)라는 위치에서 OpenHLM이 시뮬레이션 환경과 실제 휴머노이드 로봇 플랫폼에서 물체 잡기, 이동 운반, 지형 적응 등 전형적인 전신 조작 시나리오를 포함한 다중 작업 검증을 수행했음을 추론할 수 있습니다. 그 응용 가치는 오픈소스 휴머노이드 로봇 커뮤니티에 재현 가능한 기준 프레임워크를 제공하여 연구자들이 VLM을 기반으로 복잡한 운동 계획기를 처음부터 설계하지 않고도 전신 조정 능력을 갖춘 조작 전략을 신속하게 구축할 수 있게 하는 데 있습니다. 또한 이 프레임워크의 다중 모드 입력 및 다양한 동작 생성 방법에 대한 호환성은 알려지지 않은 환경에서의 일반화 가능성을 위한 기반을 마련합니다.
