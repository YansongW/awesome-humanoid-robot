---
$id: ent_paper_sonic_supersizing_motion_track_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'SONIC: Supersizing Motion Tracking for Natural Humanoid Whole-Body Control'
  zh: SONIC｜超大运动跟踪，实现自然人形全身控制
  ko: 'SONIC: Supersizing Motion Tracking for Natural Humanoid Whole-Body Control'
summary:
  en: The implementation path of SONIC is to first encode human video/motion capture trajectories and teleoperation/exoskeleton
    data into multimodal representations, and then use AMP/motion priors, VLA multimodal action models to predict whole-body
    trajectories/action sequences, and low-level controller targets. The key point is to preserve the semantic understanding
    of VLM while adding robot states and action heads to avoid staying only at language planning.
  zh: SONIC 的实现路径是先把人类视频/动捕轨迹、遥操作/外骨骼数据编码成多模态表征，再用AMP/运动先验、VLA 多模态动作模型预测全身轨迹/动作序列、低层控制器目标。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: SONIC 的实现路径是先把人类视频/动捕轨迹、遥操作/外骨骼数据编码成多模态表征，再用AMP/运动先验、VLA 多模态动作模型预测全身轨迹/动作序列、低层控制器目标。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- locomotion
- motion_tracking
- sonic
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: SONIC project page
  url: https://nvlabs.github.io/GEAR-SONIC/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人的全身控制长期面临两大挑战：如何从人类演示中高效提取运动信息，以及如何将高层语义理解与底层运动控制无缝衔接。SONIC 提出了一种融合多模态运动表征与视觉-语言-动作（VLA）模型的统一框架，通过编码人类视频、动捕轨迹、遥操作与外骨骼数据，并引入机器人状态与动作头，实现了从语义规划到全身轨迹预测的端到端控制。该工作旨在突破现有方法仅停留于语言规划的局限，为人形机器人提供兼具自然性与鲁棒性的运动跟踪能力。

## 核心内容

人形机器人的全身控制研究近年来取得了显著进展，但现有方法仍存在关键瓶颈。一方面，基于强化学习或运动先验（如AMP）的方法虽能生成动态运动，却难以理解复杂任务中的语义指令；另一方面，视觉-语言模型（VLM）擅长高层规划，却缺乏对机器人本体状态与低层动作的建模能力。SONIC 的核心动机正是弥合这一鸿沟：使机器人既能理解“拿起杯子”这样的语义指令，又能生成与之匹配的全身运动轨迹，包括躯干平衡、手臂协调与足部步态。

在方法层面，SONIC 构建了一条从数据编码到动作生成的完整流水线。首先，它将人类视频、动捕轨迹、遥操作与外骨骼数据统一编码为多模态表征，涵盖视觉特征、关节角度与接触力等信息。随后，该表征被输入至融合了运动先验（AMP）与VLA多模态动作模型的预测模块，输出全身轨迹与动作序列。最后，低层控制器将这些轨迹转化为关节力矩指令，驱动真实机器人执行。这一设计的关键在于：VLM 的语义理解能力被完整保留，同时通过新增的机器人状态编码与动作头，使模型能够直接输出物理可行的运动指令，而非仅停留在语言层面的规划。

SONIC 的技术创新主要体现在三个方面。第一，它提出了一种“超大运动跟踪”范式，通过整合多种异构数据源（视频、动捕、遥操作），显著扩展了训练数据的规模与多样性，从而提升了模型对复杂运动模式的泛化能力。第二，模型架构中引入了显式的机器人状态嵌入与动作头，使得VLM能够感知自身关节限位、质心位置等物理约束，避免生成不可执行的动作。第三，通过将AMP的运动先验与VLA的动作预测相结合，SONIC 在保持运动自然性的同时，实现了对高层语义指令的精准响应，例如根据“缓慢行走”或“快速转身”等语言描述自适应调整步态参数。

实验验证方面，SONIC 在多种人形机器人平台上进行了测试，包括仿真环境与实物机器人。结果表明，该方法在全身运动跟踪精度、任务完成率与运动自然性上均优于仅依赖AMP或VLM的基线方法。具体而言，在动态平衡保持、复杂地形行走以及物体操作等任务中，SONIC 生成的轨迹更接近人类演示，且对传感器噪声与外部扰动具有更强的鲁棒性。此外，通过消融实验证实，多模态数据融合与状态感知动作头是性能提升的关键组件，缺失任一模块均会导致运动质量显著下降。

从应用价值看，SONIC 为人形机器人在服务、制造与救援等场景中的部署提供了关键技术支撑。其统一框架使得机器人能够同时处理语言指令与运动跟踪，无需为不同任务分别设计控制策略。例如，在家庭服务场景中，机器人可依据“从桌上取水”的指令，自主规划行走路径、调整手臂姿态并保持身体平衡，整个过程无需人工干预。这一能力有望大幅降低人形机器人的编程复杂度，推动其从实验室走向实际应用。

## 参考
- SONIC project page (https://nvlabs.github.io/GEAR-SONIC/)

## Overview

The whole-body control of humanoid robots has long faced two major challenges: how to efficiently extract motion information from human demonstrations, and how to seamlessly integrate high-level semantic understanding with low-level motion control. SONIC proposes a unified framework that fuses multimodal motion representations with a Vision-Language-Action (VLA) model. By encoding human videos, motion capture trajectories, teleoperation, and exoskeleton data, and introducing robot state and action heads, it achieves end-to-end control from semantic planning to whole-body trajectory prediction. This work aims to overcome the limitations of existing methods that only stop at language planning, providing humanoid robots with motion tracking capabilities that are both natural and robust.

## Content

Significant progress has been made in whole-body control research for humanoid robots in recent years, but existing methods still face critical bottlenecks. On one hand, methods based on reinforcement learning or motion priors (e.g., AMP) can generate dynamic motions but struggle to understand semantic instructions in complex tasks. On the other hand, Vision-Language Models (VLMs) excel at high-level planning but lack the ability to model robot proprioceptive states and low-level actions. The core motivation of SONIC is to bridge this gap: enabling robots to both understand semantic instructions like "pick up the cup" and generate matching whole-body motion trajectories, including torso balance, arm coordination, and foot gait.

At the methodological level, SONIC constructs a complete pipeline from data encoding to action generation. First, it uniformly encodes human videos, motion capture trajectories, teleoperation, and exoskeleton data into a multimodal representation, covering information such as visual features, joint angles, and contact forces. This representation is then fed into a prediction module that integrates motion priors (AMP) with a VLA multimodal action model, outputting whole-body trajectories and action sequences. Finally, a low-level controller converts these trajectories into joint torque commands to drive the real robot. The key design here is that the semantic understanding capability of the VLM is fully preserved, while the newly added robot state encoding and action heads allow the model to directly output physically feasible motion instructions, rather than remaining at the level of language planning.

SONIC's technical innovations are mainly reflected in three aspects. First, it proposes a "large-scale motion tracking" paradigm, which significantly expands the scale and diversity of training data by integrating multiple heterogeneous data sources (videos, motion capture, teleoperation), thereby enhancing the model's generalization ability for complex motion patterns. Second, the model architecture introduces explicit robot state embeddings and action heads, enabling the VLM to perceive physical constraints such as joint limits and center-of-mass positions, avoiding the generation of infeasible actions. Third, by combining AMP's motion priors with VLA's action prediction, SONIC maintains motion naturalness while achieving precise responses to high-level semantic instructions, such as adaptively adjusting gait parameters based on language descriptions like "walk slowly" or "turn quickly."

In terms of experimental validation, SONIC was tested on various humanoid robot platforms, including simulation environments and real robots. Results show that this method outperforms baselines relying solely on AMP or VLM in whole-body motion tracking accuracy, task completion rate, and motion naturalness. Specifically, in tasks such as dynamic balance maintenance, walking on complex terrain, and object manipulation, SONIC generates trajectories closer to human demonstrations and exhibits stronger robustness to sensor noise and external disturbances. Additionally, ablation experiments confirm that multimodal data fusion and state-aware action heads are key components for performance improvement; the absence of either module leads to a significant decline in motion quality.

From an application perspective, SONIC provides key technical support for deploying humanoid robots in scenarios such as service, manufacturing, and rescue. Its unified framework allows robots to simultaneously handle language instructions and motion tracking without designing separate control strategies for different tasks. For example, in a home service scenario, a robot can autonomously plan a walking path, adjust arm posture, and maintain body balance based on the instruction "get water from the table," without any human intervention. This capability is expected to significantly reduce the programming complexity of humanoid robots, driving their transition from the lab to practical applications.

## 개요

휴머노이드 로봇의 전신 제어는 오랫동안 두 가지 주요 과제에 직면해 왔습니다: 인간 시연에서 운동 정보를 효율적으로 추출하는 방법과 고수준 의미 이해를 저수준 운동 제어와 원활하게 연결하는 방법입니다. SONIC은 다중 모드 운동 표현과 시각-언어-행동(VLA) 모델을 통합한 프레임워크를 제안합니다. 인간 비디오, 모션 캡처 궤적, 원격 조작 및 외골격 데이터를 인코딩하고 로봇 상태와 행동 헤드를 도입하여 의미 계획에서 전신 궤적 예측까지의 종단간 제어를 실현합니다. 이 연구는 기존 방법이 언어 계획에만 머무르는 한계를 극복하고, 휴머노이드 로봇에 자연스럽고 강건한 운동 추적 능력을 제공하는 것을 목표로 합니다.

## 핵심 내용

휴머노이드 로봇의 전신 제어 연구는 최근 몇 년간 상당한 진전을 이루었지만, 기존 방법에는 여전히 중요한 병목이 존재합니다. 한편으로 강화 학습 또는 운동 사전(예: AMP)에 기반한 방법은 동적 운동을 생성할 수 있지만 복잡한 작업의 의미 명령을 이해하는 데 어려움을 겪습니다. 다른 한편으로 시각-언어 모델(VLM)은 고수준 계획에 능숙하지만 로봇 본체 상태와 저수준 행동을 모델링하는 능력이 부족합니다. SONIC의 핵심 동기는 바로 이 간극을 메우는 것입니다: 로봇이 "컵을 집어 들어"와 같은 의미 명령을 이해하면서도, 몸통 균형, 팔 협응 및 발 보행을 포함한 이에 맞는 전신 운동 궤적을 생성할 수 있도록 하는 것입니다.

방법론 측면에서 SONIC은 데이터 인코딩에서 행동 생성까지의 완전한 파이프라인을 구축합니다. 먼저 인간 비디오, 모션 캡처 궤적, 원격 조작 및 외골격 데이터를 시각 특징, 관절 각도 및 접촉력 정보를 포함한 다중 모드 표현으로 통합 인코딩합니다. 그런 다음 이 표현은 운동 사전(AMP)과 VLA 다중 모드 행동 모델이 결합된 예측 모듈에 입력되어 전신 궤적과 행동 시퀀스를 출력합니다. 마지막으로 저수준 제어기가 이러한 궤적을 관절 토크 명령으로 변환하여 실제 로봇을 구동합니다. 이 설계의 핵심은 VLM의 의미 이해 능력이 완전히 보존되는 동시에, 새로 추가된 로봇 상태 인코딩과 행동 헤드를 통해 모델이 언어 수준의 계획에 머무르지 않고 물리적으로 실행 가능한 운동 명령을 직접 출력할 수 있다는 점입니다.

SONIC의 기술 혁신은 주로 세 가지 측면에서 나타납니다. 첫째, "초대형 운동 추적" 패러다임을 제안하여 여러 이종 데이터 소스(비디오, 모션 캡처, 원격 조작)를 통합함으로써 훈련 데이터의 규모와 다양성을 크게 확장하고, 복잡한 운동 패턴에 대한 모델의 일반화 능력을 향상시킵니다. 둘째, 모델 아키텍처에 명시적인 로봇 상태 임베딩과 행동 헤드를 도입하여 VLM이 자체 관절 한계, 질량 중심 위치 등의 물리적 제약을 인식하고 실행 불가능한 행동을 생성하지 않도록 합니다. 셋째, AMP의 운동 사전과 VLA의 행동 예측을 결합함으로써 SONIC은 운동의 자연스러움을 유지하면서도 "천천히 걷기" 또는 "빠르게 돌기"와 같은 언어 설명에 따라 보행 파라미터를 적응적으로 조정하는 등 고수준 의미 명령에 정밀하게 반응합니다.

실험 검증 측면에서 SONIC은 시뮬레이션 환경과 실제 로봇을 포함한 다양한 휴머노이드 로봇 플랫폼에서 테스트되었습니다. 결과는 이 방법이 전신 운동 추적 정밀도, 작업 완료율 및 운동 자연스러움에서 AMP 또는 VLM에만 의존하는 기준 방법보다 우수함을 보여줍니다. 구체적으로 동적 균형 유지, 복잡한 지형 보행 및 물체 조작 등의 작업에서 SONIC이 생성한 궤적은 인간 시연에 더 가깝고, 센서 노이즈와 외부 교란에 대해 더 강한 강건성을 보입니다. 또한 절제 실험을 통해 다중 모드 데이터 융합과 상태 인식 행동 헤드가 성능 향상의 핵심 구성 요소임이 확인되었으며, 어느 하나라도 누락되면 운동 품질이 현저히 저하됩니다.

응용 가치 측면에서 SONIC은 서비스, 제조 및 구조 등의 시나리오에서 휴머노이드 로봇 배치를 위한 핵심 기술 지원을 제공합니다. 그 통합 프레임워크는 로봇이 언어 명령과 운동 추적을 동시에 처리할 수 있게 하여, 작업별로 별도의 제어 전략을 설계할 필요가 없습니다. 예를 들어 가정 서비스 시나리오에서 로봇은 "테이블에서 물을 가져와"라는 명령에 따라 자율적으로 보행 경로를 계획하고, 팔 자세를 조정하며, 신체 균형을 유지하는 전체 과정을 인간의 개입 없이 수행할 수 있습니다. 이러한 능력은 휴머노이드 로봇의 프로그래밍 복잡성을 크게 줄여, 실험실에서 실제 응용으로의 전환을 촉진할 것으로 기대됩니다.
