---
$id: ent_paper_zero_shot_adaptation_behavioral_foundati_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Zero-Shot Adaptation of Behavioral Foundation Models to Unseen Dynamics
  zh: Zero-Shot Adaptation of Behavioral Foundation Models to Unseen Dynamics
  ko: Zero-Shot Adaptation of Behavioral Foundation Models to Unseen Dynamics
summary:
  en: Behavioral Foundation Models (BFMs) proved successful in producing policies for arbitrary tasks in a zero-shot manner,
    requiring no test-time training or task-specific fine-tuning.
  zh: 本文针对行为基础模型（BFM）在动态变化环境下零样本适应能力不足的问题，提出了一种基于Transformer信念估计器的前向-后向（FB）表示方法。该方法通过将策略编码空间划分为动力学特定聚类，实现了对训练中观察到的动态的响应及对未见动态的泛化，在离散和连续任务中零样本回报相比基线提升高达2倍。
  ko: Behavioral Foundation Models (BFMs) proved successful in producing policies for arbitrary tasks in a zero-shot manner,
    requiring no test-time training or task-specific fine-tuning.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- zero
- shot
- adaptation
- behavioral
- foundati
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 151 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2505.13150 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2505.13150v2); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: arXiv:2505.13150 Zero-Shot Adaptation of Behavioral Foundation Models to Unseen Dynamics
  url: https://arxiv.org/abs/2505.13150
  accessed_at: '2026-07-31'
  date: '2025-05-19'
- id: src_002
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---


## 概述

本文提出两种方法——Belief-FB (BFB) 和 Rotation-FB (RFB)——解决行为基础模型（BFMs）在混合动力学离线数据上训练时的表示崩溃问题。BFB 用 transformer 编码器从轨迹推断动力学上下文，RFB 用 von Mises-Fisher 分布将任务向量空间划分为不相交锥体，两者均保持零样本适应未见动力学的能力，在四个基准上相比基线最高提升 2 倍回报。

## 它改变了什么

FB 表示方法此前只在单一或近似单一动力学的数据集上有效，一旦训练数据来自多个上下文 MDP（如 Open X-Embodiment 的 22 个机器人本体），任务向量 z 的均匀采样会迫使后继度量在不同动力学之间取平均，产生穿过障碍物或无视风阻的"平均化策略"。这本质上是监督学习中的类别干扰在强化学习表示空间的翻版——不同动力学的后继测度在共享的 z 空间内互相污染，导致零样本泛化能力崩溃。

作者真正改变的是：将"动力学身份"从隐式的全局变量提升为显式的、可推断的上下文表示，并以此为条件重新组织 FB 的训练目标。这不再是简单的条件化拼接（Contextual-FB 已证明失败），而是通过两种互补机制——信念推断（BFB）和锥体划分（RFB）——从数据结构和采样分布两个层面解耦干扰。其意义在于，它首次让 FB 类模型能够直接在大规模异构机器人数据上训练，而不需要为每个本体单独训练模型。

## 方法拆解

### 问题形式化
- 数据集建模为上下文 MDP (CMDP)：⟨𝒞, 𝒮, 𝒜, γ, ℳ⟩，其中 ℳ(c) 映射到具体 MDP ⟨𝒮, 𝒜, 𝒯^c, R^c, μ^c, γ⟩，上下文 c 在回合内静态。
- 当 c 不可观测时，问题变为 POMDP，存在信念状态 b_t(c) = P(c|H_t) 作为充分统计量。

### Belief-FB (BFB)
- 置换不变 transformer 编码器 f_dyn 处理轨迹 H = {(s_t, a_t, s_{t+1})}，输出动力学编码 h ∈ R^d。
- 重参数化采样 z_i = μ_i + ε_i ⊙ exp(log σ_i)，使 h 遵循高斯先验。
- 辅助损失 L_context = (1/(B·T)) Σ ||ŝ_{i,t+1} − s_{i,t+1}||₂² 用 next-state predictor g_pred 预测下一状态。
- 关键设计：FB 训练与 f_dyn 预训练分离（分阶段），条件化仅应用于前向网络 F(s,a,[h; z_FB])，后向网络 B 跨上下文共享——作者发现条件化 B 会产生平滑 Q 函数，损害性能。

### Rotation-FB (RFB)
- 将均匀先验替换为 von Mises-Fisher 分布：z_FB ~ vMF(μ=h, κ)，其中 h 为 f_dyn 输出的上下文方向。
- 采样实现：先选基向量（如北极点），用 vMF 噪声扰动，再通过 Householder 反射旋转到 h 方向，避免直接 vMF 采样的高计算成本。
- κ 控制策略多样性：低 κ 产生重叠簇，高 κ 使任务向量更特化。
- 理论保证（Theorem 4）：在块分离参数化下，遗憾界仅依赖最大锥体大小 k_max 而非总任务数 k，即 ε_k* = max_j ε|C_j|* ≤ ε_kmax*。

### 训练目标（锚点回归）
ℒ_FB = 𝔼[(⟨F(s_t,a_t,z),B(s⁺)⟩ − γ⟨F̂(s_{t+1},a_{t+1},z),B̂(s⁺)⟩)² − 2⟨F(s_t,a_t,z),B(s_{t+1})⟩]

### 架构细节
- F 和 B 均为 MLP，第一层用 LayerNorm + Tanh 标准化输入。
- F 对状态-动作对和状态-任务对分别用独立预处理器嵌入到 512 维，拼接后经第三个 MLP 输出 d 维。
- F 做集成（ensemble）取均值稳定 TD 学习。
- 连续控制用 DDPG 风格 actor 近似 argmax，离散用贪心策略。

## 关键创新

1. **信念条件化替代显式后验**：BFB 用 transformer 从轨迹中隐式推断动力学上下文，无需学习显式后验分布，且保持理论保证。这解决了 POMDP 中信念状态难以精确计算的问题，同时利用 transformer 的长序列编码能力。

2. **锥体划分解耦干扰**：RFB 的 vMF 采样将任务向量空间划分为不相交锥体，每个锥体对应一个动力学方向。这使 FB 可以在每个锥体内局部拟合后继测度，避免破坏性平均效应——理论界从依赖总任务数 k 降为依赖最大锥体大小 k_max，这是首个在混合动力学下保持零样本泛化界的 FB 变体。

3. **分阶段训练与条件化设计**：作者发现将 f_dyn 预训练与 FB 训练分离、仅条件化前向网络（而非后向）能显著提升性能。这一经验发现挑战了"端到端联合训练"的直觉，为后续架构设计提供了实用指导。

## 实验与结果

### 测试环境结果（Table 2）

| 环境 | Random | Vanilla-FB | HILP | Lap | Belief-FB | Rotation-FB |
|------|--------|------------|------|-----|-----------|-------------|
| Fourrooms | 0.05±0.01 | 0.15±0.06 | 0.2±0.02 | 0.1±0.1 | 0.4±0.02 | **0.61±0.02** |
| Pointmass | 0.03±0.01 | 0.1±0.1 | 0.25±0.02 | 0.1±0.1 | 0.45±0.05 | **0.55±0.05** |
| Ant-Wind | 250±200 | 250±98.5 | 410±40.5 | 290±22.5 | 550±50.5 | **640±30.7** |

### 训练环境结果（Table 3）

| 环境 | Random | Vanilla-FB | HILP | Lap | Belief-FB | Rotation-FB |
|------|--------|------------|------|-----|-----------|-------------|
| Fourrooms | 0.18±0.02 | 0.25±0.02 | 0.4±0.02 | 0.2±0.1 | 0.7±0.02 | **0.85±0.02** |
| Pointmass | 0.0±0.05 | 0.2±0.2 | 0.45±0.1 | 0.15±0.15 | 0.76±0.18 | **0.88±0.2** |
| Ant-Wind | -190±250 | 390±120 | 410±90 | 340±150 | 680±80 | **740±70** |

### 关键观察
- RFB 在测试环境上比 Vanilla-FB 提升 3-4 倍（Fourrooms 0.61 vs 0.15），比 HILP 提升约 2 倍。
- Vanilla-FB 在 Pointmass 测试上仅 0.1，接近随机基线 0.03，证实平均化策略问题。
- Oracle-ID（one-hot 环境 ID）在训练环境达 0.90-0.95，但测试环境崩溃至 0.0-0.1，说明显式 ID 无法泛化。
- 消融：上下文长度 <100（单条轨迹长度）时性能差，>200 无额外收益；数据多样性在 25 配置后增益 <3%；κ 增大降低方差提升性能。

## 边界与局限

- 评估仅限于狭窄的动力学不匹配类型（网格布局、风向、摩擦系数），未覆盖更复杂的动力学变化（如接触模式突变、非平稳动力学）。
- BFB/RFB 继承 vanilla FB 的所有缺点：仅限线性奖励表示、收敛保证仅在覆盖假设下成立。
- 测试时依赖随机探索收集上下文，在需要精确探索才能识别动力学的复杂环境中可能失败（作者承认未解决）。
- 未在 XLand-MiniGrid 或 Kinetix 等更复杂基准上验证，也未扩展到大规模真实机器人实验。
- 引入额外超参数 κ，其最优值随环境变化（离散 50，Pointmass 100），调参成本未量化。

## 工程启示

复现时先核对三个关键点：**第一**，f_dyn 预训练与 FB 训练必须分阶段，联合训练会显著降低性能——这是最容易踩的坑；**第二**，RFB 的 vMF 采样务必用 Householder 反射实现，直接采样在 d=150 时计算开销不可接受；**第三**，κ 的初始值建议从 50 开始，离散环境可尝试 100，但必须做消融——它直接控制锥体重叠度，影响策略多样性。

对下游团队：如果数据来自多个机器人本体或多种物理参数，直接训练 vanilla FB 会得到"平均机器人"策略，务必改用 RFB。测试时需提供至少 100 步的无奖励探索轨迹作为上下文，短于单条轨迹长度（100 步）时性能急剧下降。硬件上单张 RTX 4090 即可训练（约 1 小时），但 F 的集成会加倍显存占用，batch size 1024 下需注意显存上限。

## 参考
- https://arxiv.org/abs/2505.13150
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## 개요

본 논문은 혼합 동역학 오프라인 데이터에서 훈련된 행동 기반 모델(BFMs)의 표현 붕괴 문제를 해결하기 위해 Belief-FB (BFB)와 Rotation-FB (RFB)라는 두 가지 방법을 제안한다. BFB는 transformer 인코더를 사용하여 궤적에서 동역학 컨텍스트를 추론하고, RFB는 von Mises-Fisher 분포를 사용하여 작업 벡터 공간을 서로소 원뿔로 분할한다. 두 방법 모두 미지의 동역학에 대한 제로샷 적응 능력을 유지하며, 네 가지 벤치마크에서 기준선 대비 최대 2배의 보상 향상을 달성한다.

## 무엇을 바꾸었는가

FB 표현 방법은 이전에는 단일 또는 거의 단일 동역학 데이터셋에서만 효과적이었다. 훈련 데이터가 여러 컨텍스트 MDP(예: Open X-Embodiment의 22개 로봇 본체)에서 오는 경우, 작업 벡터 z의 균일 샘플링은 후속 측도가 서로 다른 동역학 간에 평균화되도록 강제하여 장애물을 통과하거나 바람 저항을 무시하는 "평균화 정책"을 생성한다. 이는 본질적으로 지도 학습에서의 클래스 간섭이 강화 학습 표현 공간에서 재현된 것이다—서로 다른 동역학의 후속 측도가 공유된 z 공간 내에서 서로 오염되어 제로샷 일반화 능력이 붕괴된다.

저자가 실제로 바꾼 것은 "동역학 정체성"을 암시적 전역 변수에서 명시적이고 추론 가능한 컨텍스트 표현으로 승격시키고, 이를 조건으로 FB 훈련 목표를 재구성한 것이다. 이는 단순한 조건화 연결(Contextual-FB가 실패함이 입증됨)이 아니라, 두 가지 상호 보완적 메커니즘—신념 추론(BFB)과 원뿔 분할(RFB)—을 통해 데이터 구조와 샘플링 분포의 두 수준에서 간섭을 분리한다. 그 의의는 FB 계열 모델이 각 본체별로 별도 모델을 훈련할 필요 없이 대규모 이기종 로봇 데이터에서 직접 훈련할 수 있게 한 최초의 사례라는 점이다.

## 방법 분해

### 문제 형식화
- 데이터셋은 컨텍스트 MDP (CMDP): ⟨𝒞, 𝒮, 𝒜, γ, ℳ⟩로 모델링되며, ℳ(c)는 구체적 MDP ⟨𝒮, 𝒜, 𝒯^c, R^c, μ^c, γ⟩에 매핑되고, 컨텍스트 c는 에피소드 내에서 정적이다.
- c를 관측할 수 없을 때 문제는 POMDP가 되며, 신념 상태 b_t(c) = P(c|H_t)가 충분 통계량으로 존재한다.

### 훈련 목표 (앵커 회귀)
ℒ_FB = 𝔼[(⟨F(s_t,a_t,z),B(s⁺)⟩ − γ⟨F̂(s_{t+1},a_{t+1},z),B̂(s⁺)⟩)² − 2⟨F(s_t,a_t,z),B(s_{t+1})⟩]

### 아키텍처 세부 사항
- F와 B는 모두 MLP이며, 첫 번째 레이어는 LayerNorm + Tanh로 입력을 정규화한다.
- F는 상태-행동 쌍과 상태-작업 쌍을 각각 독립적인 전처리기로 512차원에 임베딩한 후, 연결하여 세 번째 MLP를 통해 d차원을 출력한다.
- F는 앙상블(평균)을 수행하여 TD 학습을 안정화한다.
- 연속 제어는 DDPG 스타일 actor로 argmax를 근사하고, 이산은 탐욕 정책을 사용한다.

## 핵심 혁신

1. **명시적 사후 확률 대신 신념 조건화**: BFB는 transformer를 사용하여 궤적에서 동역학 컨텍스트를 암시적으로 추론하며, 명시적 사후 분포 학습 없이도 이론적 보장을 유지한다. 이는 POMDP에서 신념 상태의 정확한 계산이 어려운 문제를 해결하면서 transformer의 긴 시퀀스 인코딩 능력을 활용한다.

2. **원뿔 분할을 통한 간섭 분리**: RFB의 vMF 샘플링은 작업 벡터 공간을 서로소 원뿔로 나누며, 각 원뿔은 하나의 동역학 방향에 해당한다. 이를 통해 FB는 각 원뿔 내에서 후속 측도를 국소적으로 피팅하여 파괴적 평균 효과를 피할 수 있다—이론적 경계는 총 작업 수 k에 의존하는 것에서 최대 원뿔 크기 k_max에 의존하는 것으로 줄어들며, 혼합 동역학에서 제로샷 일반화 경계를 유지하는 최초의 FB 변형이다.

3. **단계적 훈련과 조건화 설계**: 저자는 f_dyn 사전 훈련과 FB 훈련을 분리하고 순방향 네트워크만 조건화(역방향이 아닌)하는 것이 성능을 크게 향상시킨다는 것을 발견했다. 이 경험적 발견은 "엔드투엔드 공동 훈련"의 직관에 도전하며 후속 아키텍처 설계에 실용적 지침을 제공한다.

## 실험 및 결과

### 테스트 환경 결과 (Table 2)

| 환경 | Random | Vanilla-FB | HILP | Lap | Belief-FB | Rotation-FB |
|------|--------|------------|------|-----|-----------|-------------|
| Fourrooms | 0.05±0.01 | 0.15±0.06 | 0.2±0.02 | 0.1±0.1 | 0.4±0.02 | **0.61±0.02** |
| Pointmass | 0.03±0.01 | 0.1±0.1 | 0.25±0.02 | 0.1±0.1 | 0.45±0.05 | **0.55±0.05** |
| Ant-Wind | 250±200 | 250±98.5 | 410±40.5 | 290±22.5 | 550±50.5 | **640±30.7** |

### 훈련 환경 결과 (Table 3)

| 환경 | Random | Vanilla-FB | HILP | Lap | Belief-FB | Rotation-FB |
|------|--------|------------|------|-----|-----------|-------------|
| Fourrooms | 0.18±0.02 | 0.25±0.02 | 0.4±0.02 | 0.2±0.1 | 0.7±0.02 | **0.85±0.02** |
| Pointmass | 0.0±0.05 | 0.2±0.2 | 0.45±0.1 | 0.15±0.15 | 0.76±0.18 | **0.88±0.2** |
| Ant-Wind | -190±250 | 390±120 | 410±90 | 340±150 | 680±80 | **740±70** |

### 핵심 관찰
- RFB는 테스트 환경에서 Vanilla-FB 대비 3-4배 향상(Fourrooms 0.61 vs 0.15), HILP 대비 약 2배 향상.
- Vanilla-FB는 Pointmass 테스트에서 0.1에 불과하며, 무작위 기준선 0.03에 근접하여 평균화 정책 문제를 확인.
- Oracle-ID(원-핫 환경 ID)는 훈련 환경에서 0.90-0.95에 도달하지만 테스트 환경에서 0.0-0.1로 붕괴되어 명시적 ID가 일반화할 수 없음을 시사.
- 절제: 컨텍스트 길이 <100(단일 궤적 길이)일 때 성능 저하, >200에서 추가 이득 없음; 데이터 다양성은 25 구성 이후 이득 <3%; κ 증가는 분산을 줄이고 성능을 향상.

## 경계 및 한계

- 평가는 좁은 동역학 불일치 유형(그리드 레이아웃, 바람 방향, 마찰 계수)에 국한되며, 더 복잡한 동역학 변화(접촉 모드 급변, 비정상 동역학)를 다루지 않음.
- BFB/RFB는 vanilla FB의 모든 단점을 계승: 선형 보상 표현에만 국한, 수렴 보장은 커버리지 가정 하에서만 성립.
- 테스트 시 무작위 탐색으로 컨텍스트를 수집하는 데 의존하며, 동역학 식별에 정밀한 탐색이 필요한 복잡한 환경에서는 실패할 수 있음(저자도 해결하지 못함을 인정).
- XLand-MiniGrid 또는 Kinetix와 같은 더 복잡한 벤치마크에서 검증되지 않았으며, 대규모 실제 로봇 실험으로 확장되지 않음.
- 추가 하이퍼파라미터 κ를 도입하며, 최적 값은 환경에 따라 변함(이산 50, Pointmass 100), 튜닝 비용이 정량화되지 않음.

## 엔지니어링 시사점

재현 시 세 가지 핵심 사항을 먼저 확인하라: **첫째**, f_dyn 사전 훈련과 FB 훈련은 반드시 분리해야 하며, 공동 훈련은 성능을 크게 저하시킨다—이것이 가장 쉽게 빠지는 함정이다; **둘째**, RFB의 vMF 샘플링은 반드시 Householder 반사로 구현해야 하며, 직접 샘플링은 d=150에서 계산 비용이 허용 불가능하다; **셋째**, κ의 초기 값은 50에서 시작하는 것을 권장하며, 이산 환경에서는 100을 시도할 수 있지만 반드시 절제해야 한다—이는 원뿔 중첩도를 직접 제어하여 정책 다양성에 영향을 미친다.

다운스트림 팀을 위해: 데이터가 여러 로봇 본체 또는 다양한 물리적 매개변수에서 오는 경우, vanilla FB를 직접 훈련하면 "평균 로봇" 정책이 생성되므로 반드시 RFB로 전환하라. 테스트 시에는 최소 100단계의 보상 없는 탐색 궤적을 컨텍스트로 제공해야 하며, 단일 궤적 길이(100단계)보다 짧으면 성능이 급격히 저하된다. 하드웨어적으로 단일 RTX 4090으로 훈련 가능(약 1시간)하지만, F의 앙상블은 GPU 메모리 사용량을 두 배로 늘리므로 batch size 1024에서 메모리 상한에 주의해야 한다.
