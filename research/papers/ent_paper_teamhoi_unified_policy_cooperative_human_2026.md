---
$id: ent_paper_teamhoi_unified_policy_cooperative_human_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'TeamHOI: Learning a Unified Policy for Cooperative Human-Object Interactions with Any Team Size'
  zh: 'TeamHOI: Learning a Unified Policy for Cooperative Human-Object Interactions with Any Team Size'
  ko: 'TeamHOI: Learning a Unified Policy for Cooperative Human-Object Interactions with Any Team Size'
summary:
  en: 'Physics-based humanoid control has achieved remarkable progress in enabling realistic and high-performing single-agent
    behaviors, yet extending these capabilities to cooperative human-object interaction (HOI) remains challenging. Institutions
    per source list: Garena、Sea AI Lab、NUS.'
  zh: TeamHOI 是一个由研究者提出的框架，旨在通过单一去中心化策略实现任意数量智能体间的协作人机交互。其核心贡献在于利用基于 Transformer 的策略网络与队友令牌机制，结合掩码对抗运动先验策略，在缺乏协作数据的情况下生成物理逼真的多智能体协作行为。
  ko: 'Physics-based humanoid control has achieved remarkable progress in enabling realistic and high-performing single-agent
    behaviors, yet extending these capabilities to cooperative human-object interaction (HOI) remains challenging. Institutions
    per source list: Garena、Sea AI Lab、NUS.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- teamhoi
- unified
- policy
- cooperative
- human
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 121 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2603.07988 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2603.07988v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2603.07988 TeamHOI: Learning a Unified Policy for Cooperative Human-Object Interactions with Any Team Size'
  url: https://arxiv.org/abs/2603.07988
  accessed_at: '2026-07-31'
  date: '2026-03-09'
- id: src_002
  type: website
  title: Project page
  url: https://splionar.github.io/TeamHOI
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: Project page
  url: https://splionar.github.io/TeamHOI/
  accessed_at: '2026-07-31'
- id: src_004
  type: website
  title: Project page
  url: https://github.com/sail-sg/TeamHOI
  accessed_at: '2026-07-31'
- id: src_005
  type: website
  title: 万字长文，读懂人形机器人AMP：19篇论文搭起的运动先验圣经
  url: https://mp.weixin.qq.com/s/YZsm3855iP3TNTTt1aou7w
  accessed_at: '2026-07-31'
---




## 概述

TeamHOI 提出了一种基于 Transformer 的统一去中心化策略，用于任意规模（2-8 人）的人形智能体协作搬运任务。核心贡献在于通过 teammate tokens 的交叉注意力机制实现团队规模无关的策略泛化，并引入 Masked AMP 判别器解决有限单人体参考动作下的物体交互多样性问题。实验表明该方法在 2-8 人团队规模下成功率均超过 97%，且能零样本泛化至 16 人及未见过的桌子几何形状。

## 它改变了什么

多智能体物理协作（如抬桌子）长期被固定输入维度的 MLP 策略锁死——你训一个 4 人策略就只能在 4 人场景用，换 6 人就得重训。这背后的深层问题是：现有方法要么把协作简化为共享物体动力学的隐式通信（如 CooHOI），要么依赖显式但固定拓扑的通信图，两者都无法捕捉真实人类协作中"感知队友位置、动态调整发力点"的适应性。另一个被忽视的瓶颈是参考动作数据——多智能体协调动作几乎无法采集，现有框架只能退化为用单人体演示硬撑，导致学到的行为模式极其有限（比如只能前后抬，不能侧抬）。

TeamHOI 真正改变的是把"团队规模"从网络架构的硬约束变成了训练分布的一个维度。它不再为每个团队规模单独训练策略，而是让同一个策略网络通过注意力机制动态"关注"任意数量的队友，从而把多智能体协作问题重新定义为"变长序列条件下的单智能体决策"。同时，它用掩蔽判别器把"参考动作风格"和"物体交互自由度"解耦，让有限的单人体数据能支撑多样化的协作策略——这是对 AMP 框架在多智能体场景下的一次实质性重构。

## 方法拆解

### 策略网络：Teammate Tokens 交叉注意力
- 每个智能体独立决策，共享同一套 Transformer 参数（去中心化）。
- 观测编码：本体感知（ℝ²²³）、物体中心（ℝ³）、64 个候选接触点（ℝ⁶⁴×³）、最近手-物点（ℝ²×³）、目标位置（ℝ³）、队友线索（ℝ^(n−1)×9，含根部位置、6D 朝向、绕桌相对角度）各自通过独立三层 MLP tokenizer 编码为 64 维 token。
- Transformer 结构：L 层交替自注意力（作用于自身 token）与交叉注意力（关注队友 tokens），更新后的嵌入经动作头输出 28 维目标关节旋转（PD 控制器目标）。

### 统一策略训练
- 并行实例化 1024 个环境，每个环境配置不同团队规模（2-8），共享同一策略网络。
- **团队规模优势归一化**：因不同规模奖励尺度差异大，PPO 优势按团队规模 n 分别归一化：A_t^(n) ← (A_t^(n) − μ_n(A)) / (σ_n(A) + ε)，而非全局归一化。
- **虚拟天花板技巧**：IsaacGym 要求每个环境 actor 数相同，故实例化固定 N 个 agent，较小规模时多余 agent 置于虚拟天花板平面，不参与奖励、观测、梯度计算且 PD 禁用。

### Masked AMP 判别器
- 双判别器：D_full 评估完整参考动作（行走），D_mask 排除与物体交互的身体部位（手、前臂）。
- 混合风格奖励：r_t^style = σ(α_t)·r_t^mask + (1−σ(α_t))·r_t^full，其中 α_t 为连续交互指示器（如手-物距离），σ 为 sigmoid。交互时用掩蔽判别器，非交互时用完整判别器。

### 编队奖励设计
- **角分布奖励**：r_ang = exp(−k_θ·½·[(Δφ_i^ccw − 2π/m)² + (Δφ_i^cw − 2π/m)²])，m 为团队规模，鼓励均匀角度分布。
- **主轴覆盖奖励**：将根部位置投影到物体周边采样点，凸包定义支撑多边形；每轴覆盖度 g_i = min(d̃_i⁺/ℓ_i⁺, d̃_i⁻/ℓ_i⁻)，奖励 r_cov = ½(g₁ + g₂)。关键设计：每个投影点替换为周边 ±2 索引的两个点，避免两智能体退化为线段。
- 最终编队奖励 r_form = 0.25·r_ang + 0.75·r_cov（覆盖奖励权重更高，角分布提供早期分散信号使覆盖奖励非零）。

### 任务奖励分解
r_task = 0.2·r_walk^pos + 0.4·r_walk^vel + 0.2·√(r_walk^face·r_ang) + 0.6·r_form + 0.7·(r_hand·r_cov) + 0.7·r_contact + 0.7·(r_lift·r_cov) + 1.0·r_transport + 0.4·r_align + 1.0·r_put

### 三阶段训练
1. 阶段 1（约 1.5 天）：1-4 agent，禁用运输/对齐/放下奖励，D_full 仅用前进/侧向行走参考，D_mask 额外接收拾取动作，目标位置 token 掩码，episode 400 timesteps。
2. 阶段 1+2（约 5 天）：2-4 agent，启用全部奖励，取消目标掩码，加入后退行走参考，episode 600 timesteps。
3. 微调（约 3 天）：2-8 agent。

## 关键创新

1. **团队规模无关的策略架构**：用交叉注意力处理变长队友序列，替代固定输入 MLP。这是首个能零样本泛化到训练未见团队规模（如 16 人）的物理人形协作策略，且无需微调。其意义在于把多智能体 RL 从"每规模训一个模型"的范式解放出来。

2. **Masked AMP 判别器**：通过掩蔽与物体交互的身体部位，将"风格模仿"与"任务驱动交互"解耦。这使得从单人体参考动作（AMASS 中的行走+拾取）就能学到多样化的协作抬举策略（如侧握、边缘抬升），而非被参考动作锁死行为模式。与 PMP 直接学习部位级先验不同，TeamHOI 用任务奖励在掩蔽区域强制多样性，更轻量且可扩展。

3. **主轴覆盖奖励的几何鲁棒性**：通过惯性主轴计算支撑多边形覆盖度，天然适应非规则几何形状和非均匀质量分布（补充材料给出广义公式），且通过"投影点 ±2 索引替换"避免了小团队下的退化情况。这是对传统固定接触点分配（如 CooHOI 手动指定）的实质性改进。

## 实验与结果


### 主实验（表 1，10,000 episodes 平均）

| 团队规模 | 指标 | TeamHOI | CooHOI*-2 | CooHOI*-4 | CooHOI*-8 |
|---------|------|---------|-----------|-----------|-----------|
| 2A | SR (%) | **99.1** | 97.5 | 95.5 | 29.4 |
| 2A | d (m) | **0.06** | 0.19 | 0.18 | 3.60 |
| 4A | SR (%) | **99.2** | 73.2 | 94.5 | 52.4 |
| 4A | d (m) | **0.08** | 1.49 | 0.36 | 2.68 |
| 8A | SR (%) | **97.5** | 10.1 | 61.5 | 42.2 |
| 8A | d (m) | **0.18** | 4.25 | 1.83 | 3.83 |
| 8A | t_coop (%) | **90.1** | 1.0 | 27.2 | 81.6 |
| 8A | \|J\| (m/s³) | **34.2** | 189.9 | 96.7 | 45.2 |

### 重载（5× 桌重，表 3）

| 团队规模 | SR (%) | d (m) |
|---------|--------|-------|
| 4A | 3.5 | 4.78 |
| 6A | 50.1 | 1.04 |
| 8A | **81.1** | 0.49 |

仅 TeamHOI 在 8 人重载下展现有效协作；CooHOI 变体在 4A 重载下成功率接近 0%（由表内数值 0.25−0.25 计算）。

### 零样本泛化（表 4，未见几何形状+规模）

| 场景 | 团队规模 | SR (%) | d (m) |
|------|---------|--------|-------|
| 小桌 | 2A | 93.1 | 0.37 |
| 小桌 | 8A | 96.4 | 0.23 |
| 大桌 | 8A | 84.2 | 0.93 |
| 大桌 | 16A | 74.5 | 1.41 |

### 消融
- 移除 Masked AMP：抬举阶段协调失败率显著上升（运动真实性与物体交互目标冲突）。
- 移除主轴覆盖奖励：智能体不沿物体主轴分布，出现过度绕垂直轴旋转和不自然对角步态。

## 边界与局限

- 论文未明确讨论局限性，但补充材料与实验数据暴露以下边界：
  - **大团队拥挤问题**：12-16 智能体时桌面周长拥挤，t_coop 骤降至 15.1%（16A），智能体未学会在狭窄支撑间隙中定位。
  - **两智能体大桌场景**：机械杠杆不足，抬升稳定困难，SR 降至 71.0%。
  - **重载泛化上限**：5× 桌重下 4A 成功率仅 3.5%，说明策略对质量增加的鲁棒性依赖团队规模补偿。
  - **未验证**：训练未见过的物体质量（>5×）、真实世界部署、非均匀质量分布（补充材料仅提供公式，无实验数字）。
  - **参考动作依赖**：仍依赖 AMASS 单人体数据，虽经掩蔽扩展了行为多样性，但未突破"无参考动作"的极限场景。

## 工程启示

- **复现优先级**：先核对团队规模优势归一化实现——这是混合规模训练稳定的关键，全局归一化会导致奖励尺度失衡（Figure 7 显示任务奖励显著下降）。
- **最容易踩坑**：虚拟天花板技巧。IsaacGym 要求环境 actor 数一致，多余 agent 必须完全禁用（无奖励、无观测、无梯度、PD 关闭），否则会引入噪声梯度。
- **奖励调参**：r_form 中 r_cov 权重（0.75）高于 r_ang（0.25）是刻意的——r_cov 在非规则几何下仍有效但可能稀疏，r_ang 提供早期分散信号使 r_cov 非零。若你的任务物体形状更极端，建议先验证 r_cov 的凸包计算是否退化。
- **训练预算**：单张 A100，三阶段共约 9.5 天（1.5+5+3）。若算力有限，可跳过阶段 1 直接训练 2-4 agent，但需注意阶段 1 建立的行走基础技能（位置/速度/朝向奖励）是后续协作的前提。
- **下游团队**：若需部署到真实机器人，注意 MuJoCo humanoid 的简化球形手（无手指）——真实手部接触动力学差异可能影响 r_contact 和 r_lift 的有效性。

## 参考
- https://arxiv.org/abs/2603.07988
- https://splionar.github.io/TeamHOI
- https://splionar.github.io/TeamHOI/
- https://github.com/sail-sg/TeamHOI
- https://mp.weixin.qq.com/s/YZsm3855iP3TNTTt1aou7w

## Overview

TeamHOI proposes a unified decentralized strategy based on Transformers for collaborative object transport tasks with humanoid agents at arbitrary scales (2-8 agents). The core contribution lies in achieving team-size-agnostic policy generalization through cross-attention mechanisms over teammate tokens, and introducing a Masked AMP discriminator to address the diversity of object interactions under limited single-human reference actions. Experiments show that the method achieves over 97% success rate across team sizes of 2-8, and can zero-shot generalize to 16 agents and unseen table geometries.

## What It Changes

Multi-agent physical collaboration (e.g., carrying a table) has long been locked into fixed-input-dimension MLP policies—if you train a 4-agent policy, it only works for 4-agent scenarios; switching to 6 agents requires retraining. The deeper issue behind this is that existing methods either simplify collaboration into implicit communication through shared object dynamics (e.g., CooHOI), or rely on explicit but fixed-topology communication graphs, neither of which captures the adaptability of "perceiving teammate positions and dynamically adjusting force application points" in real human collaboration. Another overlooked bottleneck is reference action data—multi-agent coordinated actions are nearly impossible to collect, so existing frameworks degrade to barely managing with single-human demonstrations, resulting in extremely limited learned behavior patterns (e.g., only lifting front-back, not side lifting).

What TeamHOI truly changes is turning "team size" from a hard constraint of the network architecture into a dimension of the training distribution. It no longer trains a separate policy for each team size; instead, the same policy network dynamically "attends to" any number of teammates through attention mechanisms, thereby redefining multi-agent collaboration as "single-agent decision-making under variable-length sequence conditions." At the same time, it decouples "reference action style" from "object interaction degrees of freedom" using a masked discriminator, allowing limited single-human data to support diverse collaborative strategies—this is a substantive reconstruction of the AMP framework in multi-agent scenarios.

## Method Breakdown

### Policy Network: Teammate Tokens Cross-Attention
- Each agent makes decisions independently, sharing the same set of Transformer parameters (decentralized).
- Observation encoding: ego perception (ℝ²²³), object center (ℝ³), 64 candidate contact points (ℝ⁶⁴×³), nearest hand-object points (ℝ²×³), target position (ℝ³), teammate cues (ℝ^(n−1)×9, including root position, 6D orientation, relative angle around the table), each encoded into 64-dimensional tokens via independent three-layer MLP tokenizers.
- Transformer structure: L layers alternating self-attention (on own tokens) and cross-attention (on teammate tokens); updated embeddings are passed through an action head to output 28-dimensional target joint rotations (PD controller targets).

### Unified Policy Training
- 1024 environments are instantiated in parallel, each configured with a different team size (2-8), sharing the same policy network.
- **Team-size-wise advantage normalization**: Because reward scales vary significantly across sizes, PPO advantages are normalized separately by team size n: A_t^(n) ← (A_t^(n) − μ_n(A)) / (σ_n(A) + ε), rather than global normalization.
- **Virtual ceiling trick**: IsaacGym requires the same number of actors per environment, so a fixed N agents are instantiated; for smaller sizes, extra agents are placed on a virtual ceiling plane, excluded from rewards, observations, gradient computation, and with PD disabled.

### Masked AMP Discriminator
- Dual discriminators: D_full evaluates full reference actions (walking), D_mask excludes body parts interacting with the object (hands, forearms).
- Mixed style reward: r_t^style = σ(α_t)·r_t^mask + (1−σ(α_t))·r_t^full, where α_t is a continuous interaction indicator (e.g., hand-object distance) and σ is the sigmoid. The masked discriminator is used during interaction, and the full discriminator otherwise.

### Formation Reward Design
- **Angular distribution reward**: r_ang = exp(−k_θ·½·[(Δφ_i^ccw − 2π/m)² + (Δφ_i^cw − 2π/m)²]), where m is the team size, encouraging uniform angular distribution.
- **Principal-axis coverage reward**: Project root positions onto sampled points around the object; the convex hull defines the support polygon; per-axis coverage g_i = min(d̃_i⁺/ℓ_i⁺, d̃_i⁻/ℓ_i⁻), with reward r_cov = ½(g₁ + g₂). Key design: each projected point is replaced by two points at ±2 indices around it, avoiding degeneration into a line segment for two agents.
- Final formation reward r_form = 0.25·r_ang + 0.75·r_cov (coverage reward weighted higher; angular distribution provides early dispersion signal to make coverage reward nonzero).

### Task Reward Decomposition
r_task = 0.2·r_walk^pos + 0.4·r_walk^vel + 0.2·√(r_walk^face·r_ang) + 0.6·r_form + 0.7·(r_hand·r_cov) + 0.7·r_contact + 0.7·(r_lift·r_cov) + 1.0·r_transport + 0.4·r_align + 1.0·r_put

### Three-Stage Training
1. Stage 1 (about 1.5 days): 1-4 agents, transport/alignment/put-down rewards disabled, D_full uses only forward/side-walking references, D_mask additionally receives pickup actions, target position token masked, episode length 400 timesteps.
2. Stage 1+2 (about 5 days): 2-4 agents, all rewards enabled, target masking removed, backward walking reference added, episode length 600 timesteps.
3. Fine-tuning (about 3 days): 2-8 agents.

## Key Innovations

1. **Team-size-agnostic policy architecture**: Cross-attention handles variable-length teammate sequences, replacing fixed-input MLPs. This is the first physical humanoid collaboration policy that zero-shot generalizes to team sizes unseen in training (e.g., 16 agents) without fine-tuning. Its significance lies in freeing multi-agent RL from the "one model per size" paradigm.

2. **Masked AMP discriminator**: By masking body parts interacting with the object, it decouples "style imitation" from "task-driven interaction." This enables learning diverse collaborative lifting strategies (e.g., side grips, edge lifts) from single-human reference actions (walking + pickup in AMASS), rather than being locked into reference-action behavior patterns. Unlike PMP, which directly learns part-level priors, TeamHOI uses task rewards to enforce diversity in masked regions, making it lighter and more scalable.

3. **Geometric robustness of principal-axis coverage reward**: Support polygon coverage is computed via principal axes of inertia, naturally adapting to irregular geometries and non-uniform mass distributions (generalized formulas provided in supplementary materials), and the "projection point ±2 index replacement" avoids degeneration in small teams. This is a substantive improvement over traditional fixed contact point assignment (e.g., manual specification in CooHOI).

## Experiments and Results

### Main Experiments (Table 1, averaged over 10,000 episodes)

| Team Size | Metric | TeamHOI | CooHOI*-2 | CooHOI*-4 | CooHOI*-8 |
|---------|------|---------|-----------|-----------|-----------|
| 2A | SR (%) | **99.1** | 97.5 | 95.5 | 29.4 |
| 2A | d (m) | **0.06** | 0.19 | 0.18 | 3.60 |
| 4A | SR (%) | **99.2** | 73.2 | 94.5 | 52.4 |
| 4A | d (m) | **0.08** | 1.49 | 0.36 | 2.68 |
| 8A | SR (%) | **97.5** | 10.1 | 61.5 | 42.2 |
| 8A | d (m) | **0.18** | 4.25 | 1.83 | 3.83 |
| 8A | t_coop (%) | **90.1** | 1.0 | 27.2 | 81.6 |
| 8A | \|J\| (m/s³) | **34.2** | 189.9 | 96.7 | 45.2 |

### Heavy Load (5× table weight, Table 3)

| Team Size | SR (%) | d (m) |
|---------|--------|-------|
| 4A | 3.5 | 4.78 |
| 6A | 50.1 | 1.04 |
| 8A | **81.1** | 0.49 |

Only TeamHOI exhibits effective collaboration under 8-agent heavy load; CooHOI variants show near-zero success rates under 4A heavy load (computed from in-table values 0.25−0.25).

### Zero-Shot Generalization (Table 4, unseen geometries + sizes)

| Scenario | Team Size | SR (%) | d (m) |
|------|---------|--------|-------|
| Small table | 2A | 93.1 | 0.37 |
| Small table | 8A | 96.4 | 0.23 |
| Large table | 8A | 84.2 | 0.93 |
| Large table | 16A | 74.5 | 1.41 |

### Ablations
- Removing Masked AMP: coordination failure rate during the lifting phase increases significantly (motion realism conflicts with object interaction objectives).
- Removing principal-axis coverage reward: agents fail to distribute along the object's principal axes, exhibiting excessive rotation around the vertical axis and unnatural diagonal gaits.

## Boundaries and Limitations

- The paper does not explicitly discuss limitations, but supplementary materials and experimental data reveal the following boundaries:
  - **Large-team crowding issue**: With 12-16 agents, table perimeter becomes crowded, t_coop drops sharply to 15.1% (16A), and agents fail to learn positioning in narrow support gaps.
  - **Two-agent large-table scenario**: Insufficient mechanical leverage makes stable lifting difficult, SR drops to 71.0%.
  - **Heavy-load generalization ceiling**: Under 5× table weight, 4A success rate is only 3.5%, indicating that policy robustness to mass increase relies on team-size compensation.
  - **Not validated**: Unseen object masses (>5×), real-world deployment, non-uniform mass distributions (supplementary materials provide only formulas, no experimental numbers).
  - **Reference action dependency**: Still relies on AMASS single-human data; although masking expands behavioral diversity, it does not break through the "no reference action" extreme scenario.

## Engineering Insights

- **Reproduction priority**: First verify the team-size-wise advantage normalization implementation—this is key to mixed-size training stability; global normalization leads to reward scale imbalance (Figure 7 shows significant task reward degradation).
- **Most common pitfall**: The virtual ceiling trick. IsaacGym requires consistent actor counts per environment; extra agents must be fully disabled (no rewards, no observations, no gradients, PD off), otherwise noisy gradients are introduced.
- **Reward tuning**: In r_form, the higher weight on r_cov (0.75) over r_ang (0.25) is intentional—r_cov remains effective under irregular geometries but can be sparse, while r_ang provides early dispersion signal to make r_cov nonzero. If your task object has more extreme shapes, verify whether the convex hull computation for r_cov degenerates first.
- **Training budget**: On a single A100, three stages total about 9.5 days (1.5+5+3). If compute is limited, you can skip Stage 1 and train 2-4 agents directly, but note that the walking foundation skills (position/velocity/orientation rewards) established in Stage 1 are prerequisites for subsequent collaboration.
- **Downstream teams**: If deploying to real robots, note that the MuJoCo humanoid has simplified spherical hands (no fingers)—differences in real hand contact dynamics may affect the validity of r_contact and r_lift.

## 개요

TeamHOI는 임의 규모(2-8명)의 휴머노이드 에이전트 협력 운반 작업을 위한 Transformer 기반 통합 탈중앙화 정책을 제안한다. 핵심 기여는 teammate tokens의 교차 주의 메커니즘을 통한 팀 규모 무관 정책 일반화와, 제한된 단일 인간 참조 동작 하에서 객체 상호작용 다양성 문제를 해결하는 Masked AMP 판별기 도입이다. 실험 결과 이 방법은 2-8인 팀 규모에서 성공률 97% 이상을 달성했으며, 16인 및 본 적 없는 테이블 기하학적 형태로 제로샷 일반화가 가능함을 보여준다.

## 무엇을 바꾸었는가

다중 에이전트 물리 협력(예: 테이블 들기)은 오랫동안 고정 입력 차원의 MLP 정책에 묶여 있었다—4인 정책을 훈련하면 4인 시나리오에서만 사용할 수 있고, 6인으로 바꾸면 재훈련해야 한다. 이면의 심층 문제는 기존 방법이 협력을 공유 객체 동역학의 암시적 통신(예: CooHOI)으로 단순화하거나, 명시적이지만 고정 토폴로지의 통신 그래프에 의존한다는 점이다. 둘 다 실제 인간 협력에서의 "팀원 위치 인지, 힘 가하는 지점 동적 조정" 적응성을 포착하지 못한다. 또 다른 간과된 병목은 참조 동작 데이터다—다중 에이전트 협력 동작은 거의 수집이 불가능하여, 기존 프레임워크는 단일 인간 시연으로 버티는 수준에 그쳐 학습된 행동 패턴이 극도로 제한적이다(예: 앞뒤로만 들고 옆으로는 못 드는 경우).

TeamHOI가 실제로 바꾼 것은 "팀 규모"를 네트워크 아키텍처의 하드 제약에서 훈련 분포의 한 차원으로 전환한 것이다. 더 이상 팀 규모별로 별도 정책을 훈련하지 않고, 동일한 정책 네트워크가 주의 메커니즘을 통해 임의 수의 팀원을 동적으로 "주시"함으로써 다중 에이전트 협력 문제를 "가변 길이 시퀀스 조건 하의 단일 에이전트 의사결정"으로 재정의한다. 동시에 마스킹 판별기를 통해 "참조 동작 스타일"과 "객체 상호작용 자유도"를 분리하여, 제한된 단일 인간 데이터가 다양한 협력 전략(예: 측면 파지, 가장자리 들어올리기)을 뒷받침할 수 있게 한다—이는 AMP 프레임워크의 다중 에이전트 시나리오에 대한 실질적 재구성이다.

## 방법 분해

### 정책 네트워크: Teammate Tokens 교차 주의
- 각 에이전트는 독립적으로 의사결정하며 동일한 Transformer 파라미터를 공유한다(탈중앙화).
- 관측 인코딩: 자가 인식(ℝ²²³), 객체 중심(ℝ³), 64개 후보 접촉점(ℝ⁶⁴×³), 최근접 손-객체 점(ℝ²×³), 목표 위치(ℝ³), 팀원 단서(ℝ^(n−1)×9, 루트 위치, 6D 방향, 테이블 기준 상대 각도 포함)를 각각 독립적인 3층 MLP 토크나이저로 64차원 토큰으로 인코딩.
- Transformer 구조: L층의 교대 자기 주의(자체 토큰에 적용)와 교차 주의(팀원 토큰에 적용), 업데이트된 임베딩은 동작 헤드를 통해 28차원 목표 관절 회전(PD 컨트롤러 목표)을 출력.

### 통합 정책 훈련
- 1024개 환경을 병렬로 인스턴스화하고, 각 환경은 서로 다른 팀 규모(2-8)를 가지며 동일한 정책 네트워크를 공유한다.
- **팀 규모 이점 정규화**: 규모별 보상 척도 차이가 크므로 PPO 이점을 팀 규모 n별로 각각 정규화한다: A_t^(n) ← (A_t^(n) − μ_n(A)) / (σ_n(A) + ε), 전역 정규화가 아니다.
- **가상 천장 기법**: IsaacGym은 각 환경의 actor 수가 동일해야 하므로 고정 N개의 에이전트를 인스턴스화하고, 더 작은 규모일 때 여분 에이전트는 가상 천장 평면에 배치하여 보상, 관측, 그래디언트 계산에 참여하지 않으며 PD를 비활성화한다.

### Masked AMP 판별기
- 이중 판별기: D_full은 완전한 참조 동작(보행)을 평가하고, D_mask는 객체와 상호작용하는 신체 부위(손, 전완)를 제외한다.
- 혼합 스타일 보상: r_t^style = σ(α_t)·r_t^mask + (1−σ(α_t))·r_t^full, 여기서 α_t는 연속 상호작용 지시자(예: 손-객체 거리), σ는 시그모이드. 상호작용 시 마스킹 판별기, 비상호작용 시 완전 판별기를 사용한다.

### 대형(編隊) 보상 설계
- **각도 분포 보상**: r_ang = exp(−k_θ·½·[(Δφ_i^ccw − 2π/m)² + (Δφ_i^cw − 2π/m)²]), m은 팀 규모, 균일한 각도 분포를 장려.
- **주축 커버리지 보상**: 루트 위치를 객체 주변 샘플링 점에 투영하고, 볼록 껍질로 지지 다각형을 정의; 각 축 커버리지 g_i = min(d̃_i⁺/ℓ_i⁺, d̃_i⁻/ℓ_i⁻), 보상 r_cov = ½(g₁ + g₂). 핵심 설계: 각 투영 점을 주변 ±2 인덱스의 두 점으로 대체하여 두 에이전트가 선분으로 퇴화하는 것을 방지.
- 최종 대형 보상 r_form = 0.25·r_ang + 0.75·r_cov(커버리지 보상 가중치가 더 높고, 각도 분포는 초기 분산 신호를 제공하여 커버리지 보상이 0이 아니게 함).

### 작업 보상 분해
r_task = 0.2·r_walk^pos + 0.4·r_walk^vel + 0.2·√(r_walk^face·r_ang) + 0.6·r_form + 0.7·(r_hand·r_cov) + 0.7·r_contact + 0.7·(r_lift·r_cov) + 1.0·r_transport + 0.4·r_align + 1.0·r_put

### 3단계 훈련
1. 1단계(약 1.5일): 1-4 에이전트, 운반/정렬/내려놓기 보상 비활성화, D_full은 전진/측면 보행 참조만 사용, D_mask는 추가로 집기 동작 수신, 목표 위치 토큰 마스킹, 에피소드 400 timesteps.
2. 1+2단계(약 5일): 2-4 에이전트, 전체 보상 활성화, 목표 마스킹 해제, 후진 보행 참조 추가, 에피소드 600 timesteps.
3. 미세 조정(약 3일): 2-8 에이전트.

## 핵심 혁신

1. **팀 규모 무관 정책 아키텍처**: 고정 입력 MLP 대신 교차 주의로 가변 길이 팀원 시퀀스를 처리한다. 훈련에서 보지 못한 팀 규모(예: 16인)로 제로샷 일반화가 가능한 최초의 물리 휴머노이드 협력 정책이며, 미세 조정이 필요 없다. 그 의의는 다중 에이전트 RL을 "규모별 모델 하나 훈련" 패러다임에서 해방시킨 것이다.

2. **Masked AMP 판별기**: 객체와 상호작용하는 신체 부위를 마스킹하여 "스타일 모방"과 "작업 주도 상호작용"을 분리한다. 이를 통해 단일 인간 참조 동작(AMASS의 보행+집기)에서 다양한 협력 들어올리기 전략(예: 측면 파지, 가장자리 들어올리기)을 학습할 수 있으며, 참조 동작에 행동 패턴이 묶이지 않는다. PMP가 부위 수준 사전을 직접 학습하는 것과 달리, TeamHOI는 마스킹 영역에서 작업 보상으로 다양성을 강제하여 더 가볍고 확장 가능하다.

3. **주축 커버리지 보상의 기하학적 견고성**: 관성 주축으로 지지 다각형 커버리지를 계산하여 비정형 기하학적 형태와 비균일 질량 분포에 자연스럽게 적응하며(부록에 일반화 공식 제공), "투영 점 ±2 인덱스 대체"로 소규모 팀의 퇴화를 방지한다. 이는 전통적인 고정 접촉점 할당(예: CooHOI의 수동 지정)에 대한 실질적 개선이다.

## 실험 및 결과

### 주요 실험(표 1, 10,000 에피소드 평균)

| 팀 규모 | 지표 | TeamHOI | CooHOI*-2 | CooHOI*-4 | CooHOI*-8 |
|---------|------|---------|-----------|-----------|-----------|
| 2A | SR (%) | **99.1** | 97.5 | 95.5 | 29.4 |
| 2A | d (m) | **0.06** | 0.19 | 0.18 | 3.60 |
| 4A | SR (%) | **99.2** | 73.2 | 94.5 | 52.4 |
| 4A | d (m) | **0.08** | 1.49 | 0.36 | 2.68 |
| 8A | SR (%) | **97.5** | 10.1 | 61.5 | 42.2 |
| 8A | d (m) | **0.18** | 4.25 | 1.83 | 3.83 |
| 8A | t_coop (%) | **90.1** | 1.0 | 27.2 | 81.6 |
| 8A | \|J\| (m/s³) | **34.2** | 189.9 | 96.7 | 45.2 |

### 중량(5× 테이블 무게, 표 3)

| 팀 규모 | SR (%) | d (m) |
|---------|--------|-------|
| 4A | 3.5 | 4.78 |
| 6A | 50.1 | 1.04 |
| 8A | **81.1** | 0.49 |

TeamHOI만 8인 중량 하에서 효과적인 협력을 보여준다; CooHOI 변형은 4A 중량에서 성공률이 0%에 가깝다(표 내 값 0.25−0.25로 계산).

### 제로샷 일반화(표 4, 본 적 없는 기하학적 형태+규모)

| 시나리오 | 팀 규모 | SR (%) | d (m) |
|------|---------|--------|-------|
| 작은 테이블 | 2A | 93.1 | 0.37 |
| 작은 테이블 | 8A | 96.4 | 0.23 |
| 큰 테이블 | 8A | 84.2 | 0.93 |
| 큰 테이블 | 16A | 74.5 | 1.41 |

### 소거 실험
- Masked AMP 제거: 들어올리기 단계 조정 실패율이 크게 증가(동작 사실성과 객체 상호작용 목표 충돌).
- 주축 커버리지 보상 제거: 에이전트가 객체 주축을 따라 분포하지 않고, 과도한 수직축 회전과 부자연스러운 대각 보행이 나타남.

## 경계 및 한계

- 논문은 한계를 명시적으로 논의하지 않지만, 부록과 실험 데이터는 다음 경계를 드러낸다:
  - **대규모 팀 혼잡 문제**: 12-16 에이전트에서 테이블 표면 둘레가 혼잡해져 t_coop이 15.1%(16A)로 급감, 에이전트가 좁은 지지 간격에서 위치를 잡는 법을 학습하지 못함.
  - **두 에이전트 대형 테이블 시나리오**: 기계적 지렛대가 부족하여 들어올리기 안정화가 어렵고 SR이 71.0%로 하락.
  - **중량 일반화 상한**: 5× 테이블 무게에서 4A 성공률이 3.5%에 불과, 질량 증가에 대한 견고성이 팀 규모 보상에 의존함을 시사.
  - **검증되지 않음**: 훈련에서 본 적 없는 객체 질량(>5×), 실제 세계 배포, 비균일 질량 분포(부록은 공식만 제공, 실험 수치 없음).
  - **참조 동작 의존성**: 여전히 AMASS 단일 인간 데이터에 의존하며, 마스킹으로 행동 다양성을 확장했지만 "참조 동작 없음"의 극한 시나리오는 돌파하지 못함.

## 엔지니어링 시사점

- **재현 우선순위**: 먼저 팀 규모 이점 정규화 구현을 확인하라—혼합 규모 훈련 안정성의 핵심이며, 전역 정규화는 보상 척도 불균형을 초래한다(Figure 7은 작업 보상이 크게 감소함을 보여줌).
- **가장 함정에 빠지기 쉬운 부분**: 가상 천장 기법. IsaacGym은 환경 actor 수가 일치해야 하므로 여분 에이전트는 완전히 비활성화해야 한다(보상 없음, 관측 없음, 그래디언트 없음, PD 꺼짐), 그렇지 않으면 노이즈 그래디언트가 유입된다.
- **보상 튜닝**: r_form에서 r_cov 가중치(0.75)가 r_ang(0.25)보다 높은 것은 의도적이다—r_cov는 비정형 기하학에서도 유효하지만 희소할 수 있고, r_ang은 초기 분산 신호를 제공하여 r_cov가 0이 아니게 한다. 작업 객체 형상이 더 극단적이라면 r_cov의 볼록 껍질 계산이 퇴화하는지 먼저 검증하라.
- **훈련 예산**: 단일 A100에서 3단계 총 약 9.5일(1.5+5+3). 연산 자원이 제한적이면 1단계를 건너뛰고 2-4 에이전트를 직접 훈련할 수 있지만, 1단계에서 구축된 보행 기초 기술(위치/속도/방향 보상)이 이후 협력의 전제 조건임을 유의하라.
- **하류 팀**: 실제 로봇에 배포해야 한다면 MuJoCo humanoid의 단순화된 구형 손(손가락 없음)에 주의하라—실제 손 접촉 동역학 차이가 r_contact과 r_lift의 유효성에 영향을 미칠 수 있다.
