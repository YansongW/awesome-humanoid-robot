---
$id: ent_paper_nai_humanoid_manipulation_interfac_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Humanoid Manipulation Interface (HuMI): Humanoid Whole-Body Manipulation from Robot-Free Demonstrations'
  zh: 人形机器人操作接口（HuMI）：无需机器人演示的全身操作
  ko: '휴머노이드 조작 인터페이스(HuMI): 로봇 없는 시연으로부터의 전신 조작'
summary:
  en: HuMI is a portable, robot-free data-collection and hierarchical-learning framework that translates human whole-body
    manipulation demonstrations into autonomous humanoid skills. Using handheld sensorized grippers and wearable trackers,
    it trains a high-level Diffusion Policy to predict keypoint trajectories and a low-level RL whole-body controller to execute
    them, achieving 3× teleoperation throughput and 70% success in unseen environments on a Unitree G1.
  zh: HuMI 是一个便携式、无需机器人的数据采集与分层学习框架，能将人类全身操作演示转化为自主人形机器人技能。它通过手持传感器夹爪和可穿戴追踪器采集数据，训练高层 Diffusion Policy 预测关键点轨迹，再由低层强化学习全身控制器执行，在
    Unitree G1 上实现 3 倍遥操作吞吐量和 70% 的未见环境成功率。
  ko: HuMI는 휴대용 로봇 불필요 데이터 수집 및 계층적 학습 프레임워크로, 인간의 전신 조작 시연을 자율 휴머노이드 조작 기술로 변환한다. 손에 든 센서화된 그리퍼와 웨어러블 추적기만으로 고차원 확산 정책이 키포인트
    궤적을 예측하고, 저차원 강화학습 전신 제어기가 이를 실행하여 Unitree G1에서 원격조작 대비 3배의 데이터 수집 처리량과 보지 않은 환경에서 70% 성공률을 달성했다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
- 09_data_datasets
- 11_applications_markets
layers:
- intelligence
- upstream
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- humanoid_whole_body_manipulation
- robot_free_demonstration
- diffusion_policy
- whole_body_rl_controller
- adaptive_end_effector_tracking
- variable_speed_augmentation
- loco_manipulation
- bimanual_manipulation
- imitation_learning
- unitree_g1
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2602.06643v2. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: 'Humanoid Manipulation Interface: Humanoid Whole-Body Manipulation from Robot-Free Demonstrations'
  url: https://arxiv.org/abs/2602.06643
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

HuMI 是一套面向人形机器人全身操作的数据采集与分层策略学习系统，由机器人免演示硬件（手持夹爪 + Vive Tracker）和“高层 Diffusion Policy + 低层 RL 全身控制器”两级架构组成。核心贡献在于用人在回路 IK 预览解决人类演示到人形机器人的运动学适配问题，并通过自适应末端执行器奖励、变速增强和相对姿态跟踪等机制，在 Unitree G1 上实现了五种全身任务的高成功率与数据采集效率的大幅提升。

## 它改变了什么

人形机器人全身操作长期被两条路线卡住：遥操作要求操作者同时管理平衡与控制器误差，劳动密集且难以规模化；视觉 sim-to-real 强化学习则受限于奖励工程和硬件物流，展示的任务数量少且局限于受控实验室。更根本的问题是，现有机器人免数据系统（如 UMI）只记录末端执行器轨迹，完全不足以指定“蹲下、跪地、弯腰”这类需要腰、腿、脚协同的全身动作——同样的低位伸手，髋膝踝的配置决定了任务成败。

HuMI 真正改变的是数据采集的“形态鸿沟”问题。它不试图把人类演示缩放或重定向到机器人运动学上（这通常导致自碰撞或可达性失败），而是通过实时 IK 预览让人类操作者在采集时主动调整动作，使演示在源头上就是机器人可行的。同时，它把“全身轨迹”而非“末端轨迹”作为策略接口，让低级控制器学习跟踪骨盆、脚部等非视觉锚定关键点，从而解锁了深蹲、跪地、行走清洁这类真正利用人形平台灵活性的任务。这等于把“机器人能做什么”的约束从后处理阶段前移到了数据采集阶段，改变了整个模仿学习管线的数据契约。

## 方法拆解

### 数据采集：机器人免演示硬件
- 两个 3D 打印手持夹爪（腕装 GoPro 相机 + ArUco 标记追踪夹爪宽度），五个 HTC Vive Ultimate Tracker 分别安装在夹爪、腰部和双脚。
- 选择 Vive Ultimate Tracker 而非头戴式系统，因为后者在蹲下与地面物体交互时因遮挡导致跟踪性能下降。
- 跟踪五个关键操作框架：骨盆（浮动基座）、双手、双脚；排除头部（目标机器人 Unitree G1 无驱动颈部）。
- 整个装置可装入单个背包，无需基站，每新场景仅需 1–2 分钟构建环境追踪地图。

### 人在回路运动学适配
- 不缩放人类轨迹，保持原始未缩放姿态以维持视觉-空间对齐和物体交互几何关系；仅对骨盆追踪器高度做缩放。
- 通过实时 IK 预览界面（Mink 求解 + Viser 可视化）让操作者实时调整演示，满足可行性与任务约束。
- IK 子任务：追踪五个关键点的平移/旋转、避免自碰撞、姿态正则化维持自然构型。
- 关键设计：控制虚拟机器人仅受运动学约束，认知负荷远低于遥操作物理机器人。

### 分层策略框架
- **高层策略**：Diffusion Policy（5 Hz），输入相机图像 + 本体感觉，输出递推视界任务空间轨迹（动作块：10 个路点覆盖未来 2 秒）。
- **低级控制器**：RL 训练的全身控制器（50 Hz），教师-学生框架（教师访问特权状态，DAgger 蒸馏为学生策略），学生观测含 25 步状态历史与过去动作。

### 自适应末端执行器跟踪奖励
- 全身跟踪奖励：r_body = Σ_{χ∈{p,θ,v,w}} r(ē_χ^body, σ_χ)，其中 r(ē_χ, σ_χ) = exp(−ē_χ/σ_χ²)。
- 自适应末端执行器奖励：r_EE = 𝕀(‖v_base^ref‖ < δ) · Σ_{χ∈{p,θ}} r(ē_χ^EE, σ_χ(v_EE^ref))。
- σ_χ 基于参考末端执行器速度在 [σ_χ^min, σ_χ^max] 间线性插值（位置：0.01–0.1 m；旋转：5°–20°；v_max=0.1 m/s，v_min=0.05 m/s）。
- 基座速度超过 δ 时通过 𝕀(·) 门控停用末端执行器跟踪，优先保证快速运动稳定性。
- 课程学习：w_EE 从 0.0 线性增至 0.5，σ_p^min 在训练步 10k–15k 间从 0.1 m 退火至 0.01 m。

### 变速增强
- 每 0.02 s 采样速度缩放因子 s ~ `N_[0.25, 1.25]`（1.0, σ²），σ 在训练步 10k–15k 间从 10⁻⁴ 线性增至 1.0。
- 慢速变化给策略足够时间修正小误差，促进高精度运动学习。

### 策略接口改进
- **目标姿态作为高层动作参考**：不使用实际执行姿态作为动作块参考帧（会导致轨迹突然反向），改用前一个计划目标姿态，自然连接当前块与前一块，保持动态任务（如投掷）的平滑动量。
- **非视觉锚定关键点的相对姿态跟踪**：骨盆和脚部缺乏直接视觉锚定，推理时累积不可恢复误差（>5 cm 漂移）；修改跟踪目标为当前动作块内的相对变换而非绝对变换。默认 3 关键点（夹爪 + 基座），可扩展至 5 关键点（加脚部）。

## 关键创新

1. **人在回路 IK 预览替代运动学重定向**：这是对“人类演示 → 机器人策略”范式的根本性重构。传统方法要么缩放轨迹（物理场景不可缩放），要么用后处理重定向（破坏视觉-空间对齐）。HuMI 让人类在采集时通过实时 IK 反馈主动调整动作，既保证了机器人可行性，又保留了原始空间几何关系。消融显示去除该环节后求婚任务成功率从 85% 崩至 10%，证明这是系统成立的前提而非锦上添花。

2. **自适应末端执行器奖励 + 变速增强的课程学习**：低级控制器面临“稳定全身运动”与“精确末端对齐”的内在矛盾——快速运动时追求末端精度会导致摔倒，慢速时过度强调稳定则末端误差不可接受。通过 σ_χ 随参考速度动态缩放、基座速度门控停用末端奖励、以及变速增强让策略在不同速度尺度间切换学习，这套机制让单一控制器同时掌握慢速精确操作（深蹲捡瓶）与快速动态动作（投掷）。消融显示去除变速增强后拔剑成功率从 85% 降至 50%，去除自适应奖励后深蹲捡瓶从 85% 降至 50%。

3. **非视觉锚定关键点的相对姿态跟踪**：骨盆和脚部没有视觉锚定，绝对姿态跟踪在推理时因低级控制器误差累积导致 >5 cm 漂移，行走清洁任务中成功率从 75% 直接崩溃至 0/10。改为动作块内相对变换后，误差被限制在块内而非跨块累积，这是让“全身轨迹”作为策略接口真正可用的关键工程决策。

## 实验与结果

### 能力任务（域内设置，单环境 100 个演示）

| 任务 | 成功率 | 关键消融 |
|------|--------|----------|
| 求婚（跪地拾环） | 17/20 = 85% | 去除 IK 适配：1/10 = 10%；去除全身监督：0/10 |
| 拔剑 | 17/20 = 85%（EE 跟踪误差 15.7 mm） | 去除变速增强：5/10 = 50%，误差增至 21.2 mm |
| 动态投掷 | 15/20 = 75% | 使用实际执行姿态作参考：4/10 = 40% |
| 行走清洁 | 15/20 = 75% | EE-only：6/10 = 60%；EE+pelvis：15/20 = 75%；EE+pelvis+feet：7/10 = 70%；绝对姿态跟踪：0/10 |

### 泛化实验（蹲下拾取）
- 训练：7 环境 × 7 瓶子 = 350 个演示；测试：4 未见环境 + 6 未见物体（含形状纹理差异显著的花瓶）。
- 总体成功率：14/20 = 70%。

### 数据收集效率（拔剑任务，15 分钟会话，经验用户）

| 指标 | HuMI | TWIST2 |
|------|------|--------|
| 收集片段数 | 62 | 28 |
| 接受率 | 96.7% | 64.3% |
| 每可接受片段平均时间 | TWIST2 的 30.0% | 基准 |

- 求婚任务：HuMI 15 分钟收集 50 个演示，接受率 100%，平均每片段 18 秒；TWIST2 未能产生任何可用演示（遥操作人形无法可靠深跪且常失去稳定性）。

### 消融实验（自适应末端执行器奖励，深蹲捡瓶）
- 固定奖励基线（σ_p=0.01 m，σ_θ=5°，ω_EE=0.5）：成功率从 17/20 = 85% 降至 5/10 = 50%。
- 典型失败：深蹲阶段失去平衡摔倒，或陷入勉强稳定但无法继续操作的姿态。

## 边界与局限

论文未在结论部分明确列出局限性（片段截断）。从方法细节可推断的边界包括：系统依赖视觉追踪器，需要足够的环境纹理和光照条件（Vive Tracker 在弱纹理或强光照下可能失效）；低级控制器并非通用目的，训练配置统一但未验证跨任务泛化；评估仅在 Unitree G1 单一平台上进行，未在其他类人机器人上验证；5 关键点配置（加脚部）仅在行走清洁任务中报告了结果（70%），未在全部任务上系统评估；变速增强的 s_min、s_max、Δ 等超参数具体取值未披露；未讨论动态行走与操作切换的鲁棒性边界、长时间自主运行的稳定性、以及更复杂地形上的表现。

## 工程启示

复现时优先核对三个最容易踩坑的环节：**第一，人在回路 IK 预览的实时性**——Mink 求解必须足够快（否则操作者无法流畅调整动作），且 IK 目标必须使用原始未缩放轨迹（仅缩放骨盆高度），任何试图“优化”演示轨迹的做法都会破坏视觉-空间对齐。**第二，低级控制器的课程学习调度**——w_EE 从 0 线性增至 0.5、σ_p^min 在训练步 10k–15k 间从 0.1 m 退火至 0.01 m、变速增强 σ 同步从 10⁻⁴ 增至 1.0，这三个调度必须同步进行，单独调整任何一个都可能破坏“先稳定全局运动、再精确末端对齐”的学习顺序。**第三，非视觉锚定关键点的相对姿态跟踪**——这是最容易忽略但影响最大的接口设计，绝对姿态跟踪在行走清洁任务中直接导致 0/10 崩溃，务必在动作块内使用相对变换而非全局绝对变换。数据采集效率对比时注意 TWIST2 是遥操作基线，HuMI 的优势在深蹲/跪地类任务上尤其显著（TWIST2 完全无法完成求婚任务），若下游任务以直立操作为主，此优势可能缩小。硬件上注意 GoPro 时间戳同步需通过陀螺仪与追踪器角速度的交叉关联实现，夹爪宽度提取依赖 ArUco 标记检测，这两处是数据质量的关键瓶颈。

## 参考
- http://arxiv.org/abs/2602.06643v2

## Overview

HuMI is a data collection and hierarchical policy learning system for full-body manipulation of humanoid robots, consisting of a robot-free demonstration hardware setup (handheld grippers + Vive Trackers) and a two-level architecture of "high-level Diffusion Policy + low-level RL whole-body controller." The core contribution lies in using human-in-the-loop IK preview to address the kinematic adaptation problem from human demonstrations to humanoid robots, and through mechanisms such as adaptive end-effector rewards, variable-speed augmentation, and relative pose tracking, achieving high success rates across five whole-body tasks on the Unitree G1 along with significant improvements in data collection efficiency.

## What It Changes

Full-body manipulation of humanoid robots has long been constrained by two approaches: teleoperation requires operators to simultaneously manage balance and controller errors, making it labor-intensive and difficult to scale; visual sim-to-real reinforcement learning is limited by reward engineering and hardware logistics, demonstrating only a small number of tasks confined to controlled laboratory settings. More fundamentally, existing robot-free data systems (such as UMI) only record end-effector trajectories, which are completely insufficient for specifying whole-body actions like "squatting, kneeling, and bending" that require coordinated movement of the waist, legs, and feet—for the same low-reaching motion, the configuration of the hip, knee, and ankle determines task success.

What HuMI truly changes is the "morphological gap" problem in data collection. Rather than attempting to scale or retarget human demonstrations to robot kinematics (which typically results in self-collision or reachability failures), it uses real-time IK preview to allow human operators to actively adjust their motions during collection, ensuring demonstrations are robot-feasible at the source. Simultaneously, it uses "whole-body trajectories" rather than "end-effector trajectories" as the policy interface, enabling the low-level controller to learn tracking of non-visually-anchored keypoints such as the pelvis and feet, thereby unlocking tasks like deep squats, kneeling, and walking-based cleaning that truly leverage the flexibility of humanoid platforms. This effectively shifts the constraint of "what the robot can do" from the post-processing stage to the data collection stage, changing the data contract of the entire imitation learning pipeline.

## Method Breakdown

### Data Collection: Robot-Free Demonstration Hardware
- Two 3D-printed handheld grippers (wrist-mounted GoPro cameras + ArUco markers for tracking gripper width), with five HTC Vive Ultimate Trackers mounted on the grippers, waist, and feet.
- Vive Ultimate Trackers are chosen over head-mounted systems because the latter suffer from degraded tracking performance due to occlusion when squatting and interacting with ground-level objects.
- Five key operational frames are tracked: pelvis (floating base), both hands, and both feet; the head is excluded (the target robot Unitree G1 has no actuated neck).
- The entire setup fits into a single backpack, requires no base stations, and only 1–2 minutes are needed to build an environment tracking map for each new scene.

### Human-in-the-Loop Kinematic Adaptation
- Human trajectories are not scaled; original unscaled poses are preserved to maintain visual-spatial alignment and object interaction geometry; only the pelvis tracker height is scaled.
- A real-time IK preview interface (Mink solver + Viser visualization) allows operators to adjust demonstrations in real time to satisfy feasibility and task constraints.
- IK subtasks: track translation/rotation of five keypoints, avoid self-collision, and regularize poses to maintain natural configurations.
- Key design: controlling a virtual robot subject only to kinematic constraints imposes far less cognitive load than teleoperating a physical robot.

### Hierarchical Policy Framework
- **High-level policy**: Diffusion Policy (5 Hz), taking camera images + proprioception as input and outputting recursive-horizon task-space trajectories (action chunks: 10 waypoints covering the next 2 seconds).
- **Low-level controller**: RL-trained whole-body controller (50 Hz) using a teacher-student framework (teacher accesses privileged states, DAgger distillation to student policy), with student observations including 25-step state history and past actions.

### Adaptive End-Effector Tracking Reward
- Whole-body tracking reward: r_body = Σ_{χ∈{p,θ,v,w}} r(ē_χ^body, σ_χ), where r(ē_χ, σ_χ) = exp(−ē_χ/σ_χ²).
- Adaptive end-effector reward: r_EE = 𝕀(‖v_base^ref‖ < δ) · Σ_{χ∈{p,θ}} r(ē_χ^EE, σ_χ(v_EE^ref)).
- σ_χ is linearly interpolated within [σ_χ^min, σ_χ^max] based on reference end-effector velocity (position: 0.01–0.1 m; rotation: 5°–20°; v_max=0.1 m/s, v_min=0.05 m/s).
- When base velocity exceeds δ, end-effector tracking is deactivated via the 𝕀(·) gate to prioritize stability during fast motion.
- Curriculum learning: w_EE increases linearly from 0.0 to 0.5, and σ_p^min anneals from 0.1 m to 0.01 m between training steps 10k–15k.

### Variable-Speed Augmentation
- Every 0.02 s, a speed scaling factor s ~ `N_[0.25, 1.25]` (1.0, σ²) is sampled, with σ increasing linearly from 10⁻⁴ to 1.0 between training steps 10k–15k.
- Slow-speed variations give the policy sufficient time to correct small errors, promoting learning of high-precision motions.

### Policy Interface Improvements
- **Target pose as high-level action reference**: Instead of using the actually executed pose as the action chunk reference frame (which causes sudden trajectory reversals), the previously planned target pose is used, naturally connecting the current chunk to the previous one and maintaining smooth momentum for dynamic tasks (e.g., throwing).
- **Relative pose tracking for non-visually-anchored keypoints**: The pelvis and feet lack direct visual anchoring, and inference-time errors accumulate irrecoverably (>5 cm drift); the tracking target is modified to relative transformations within the current action chunk rather than absolute transformations. Default is 3 keypoints (grippers + base), extendable to 5 keypoints (adding feet).

## Key Innovations

1. **Human-in-the-loop IK preview replacing kinematic retargeting**: This is a fundamental restructuring of the "human demonstration → robot policy" paradigm. Traditional methods either scale trajectories (physical scenes are not scalable) or use post-processing retargeting (breaking visual-spatial alignment). HuMI allows humans to actively adjust motions during collection through real-time IK feedback, ensuring both robot feasibility and preservation of original spatial geometry. Ablation shows that removing this component collapses the proposal task success rate from 85% to 10%, proving it is a prerequisite for the system rather than a bonus.

2. **Adaptive end-effector reward + variable-speed augmentation curriculum**: The low-level controller faces an inherent conflict between "stable whole-body motion" and "precise end-effector alignment"—pursuing end-effector precision during fast motion leads to falls, while overemphasizing stability at slow speeds makes end-effector errors unacceptable. Through dynamic scaling of σ_χ with reference velocity, base-velocity gating to deactivate end-effector rewards, and variable-speed augmentation enabling the policy to switch between different speed scales during learning, this mechanism allows a single controller to master both slow precise operations (squatting to pick up bottles) and fast dynamic actions (throwing). Ablation shows that removing variable-speed augmentation drops the sword-pulling success rate from 85% to 50%, and removing the adaptive reward drops squat-pickup from 85% to 50%.

3. **Relative pose tracking for non-visually-anchored keypoints**: The pelvis and feet have no visual anchoring, and absolute pose tracking during inference accumulates >5 cm drift due to low-level controller errors, causing the walking-cleaning task success rate to collapse from 75% to 0/10. Switching to relative transformations within action chunks confines errors to within chunks rather than accumulating across chunks—a critical engineering decision that makes "whole-body trajectories" truly usable as a policy interface.

## Experiments and Results

### Capability Tasks (in-domain setting, 100 demonstrations per environment)

| Task | Success Rate | Key Ablations |
|------|--------------|---------------|
| Proposal (kneeling ring pickup) | 17/20 = 85% | Removing IK adaptation: 1/10 = 10%; removing whole-body supervision: 0/10 |
| Sword pulling | 17/20 = 85% (EE tracking error 15.7 mm) | Removing variable-speed augmentation: 5/10 = 50%, error increases to 21.2 mm |
| Dynamic throwing | 15/20 = 75% | Using actually executed pose as reference: 4/10 = 40% |
| Walking cleaning | 15/20 = 75% | EE-only: 6/10 = 60%; EE+pelvis: 15/20 = 75%; EE+pelvis+feet: 7/10 = 70%; absolute pose tracking: 0/10 |

### Generalization Experiment (squatting pickup)
- Training: 7 environments × 7 bottles = 350 demonstrations; testing: 4 unseen environments + 6 unseen objects (including vases with significant shape and texture differences).
- Overall success rate: 14/20 = 70%.

### Data Collection Efficiency (sword-pulling task, 15-minute session, experienced user)

| Metric | HuMI | TWIST2 |
|--------|------|--------|
| Number of collected episodes | 62 | 28 |
| Acceptance rate | 96.7% | 64.3% |
| Average time per acceptable episode | 30.0% of TWIST2 | Baseline |

- Proposal task: HuMI collected 50 demonstrations in 15 minutes with a 100% acceptance rate and an average of 18 seconds per episode; TWIST2 failed to produce any usable demonstrations (teleoperated humanoid could not reliably deep-kneel and frequently lost stability).

### Ablation Experiment (adaptive end-effector reward, squatting bottle pickup)
- Fixed reward baseline (σ_p=0.01 m, σ_θ=5°, ω_EE=0.5): success rate dropped from 17/20 = 85% to 5/10 = 50%.
- Typical failures: losing balance and falling during the squat phase, or becoming trapped in barely stable but non-operable poses.

## Boundaries and Limitations

The paper does not explicitly list limitations in the conclusion section (episode truncated). Inferred boundaries from method details include: the system relies on visual trackers and requires sufficient environmental texture and lighting conditions (Vive Trackers may fail in low-texture or strong-light environments); the low-level controller is not general-purpose—training configurations are unified but cross-task generalization is not validated; evaluation is conducted only on the Unitree G1 platform without validation on other humanoid robots; the 5-keypoint configuration (adding feet) is only reported for the walking-cleaning task (70%) and not systematically evaluated across all tasks; specific hyperparameter values for variable-speed augmentation (s_min, s_max, Δ, etc.) are not disclosed; robustness boundaries for dynamic walking-manipulation transitions, long-duration autonomous operation stability, and performance on more complex terrains are not discussed.

## Engineering Insights

When reproducing, prioritize checking the three most error-prone components: **First, real-time performance of the human-in-the-loop IK preview**—the Mink solver must be fast enough (otherwise operators cannot smoothly adjust motions), and IK targets must use original unscaled trajectories (only pelvis height scaled); any attempt to "optimize" demonstration trajectories will break visual-spatial alignment. **Second, the curriculum scheduling of the low-level controller**—w_EE increasing linearly from 0 to 0.5, σ_p^min annealing from 0.1 m to 0.01 m between training steps 10k–15k, and variable-speed augmentation σ increasing from 10⁻⁴ to 1.0 must be synchronized; adjusting any one independently may disrupt the learning order of "stabilize global motion first, then refine end-effector alignment." **Third, relative pose tracking for non-visually-anchored keypoints**—this is the most easily overlooked yet most impactful interface design; absolute pose tracking directly causes 0/10 collapse in the walking-cleaning task, so relative transformations within action chunks must be used rather than global absolute transformations. When comparing data collection efficiency, note that TWIST2 is a teleoperation baseline; HuMI's advantage is especially pronounced in squatting/kneeling-type tasks (TWIST2 completely fails at the proposal task). If downstream tasks are primarily upright manipulation, this advantage may diminish. On the hardware side, note that GoPro timestamp synchronization requires cross-correlation between gyroscope and tracker angular velocities, and gripper width extraction relies on ArUco marker detection—these two points are critical bottlenecks for data quality.

## 개요

HuMI는 휴머노이드 로봇의 전신 조작을 위한 데이터 수집 및 계층적 정책 학습 시스템으로, 로봇-프리 시연 하드웨어(핸드헬드 그리퍼 + Vive Tracker)와 "고수준 Diffusion Policy + 저수준 RL 전신 컨트롤러"의 2계층 아키텍처로 구성됩니다. 핵심 기여는 인간-인-더-루프 IK 미리보기를 통해 인간 시연에서 휴머노이드 로봇으로의 운동학적 적응 문제를 해결하고, 적응형 엔드이펙터 보상, 가변 속도 증강, 상대 자세 추적 등의 메커니즘을 통해 Unitree G1에서 5가지 전신 작업의 높은 성공률과 데이터 수집 효율성의 대폭 향상을 달성한 것입니다.

## 무엇을 바꾸었는가

휴머노이드 로봇의 전신 조작은 오랫동안 두 가지 접근 방식에 의해 제약을 받아왔습니다: 원격 조작은 조작자가 균형과 컨트롤러 오류를 동시에 관리해야 하므로 노동 집약적이고 확장이 어려우며, 시각적 sim-to-real 강화 학습은 보상 엔지니어링과 하드웨어 물류에 제한되어 시연되는 작업 수가 적고 통제된 실험실 환경에 국한됩니다. 더 근본적인 문제는 기존의 로봇-프리 데이터 시스템(예: UMI)이 엔드이펙터 궤적만 기록하여 "쪼그려 앉기, 무릎 꿇기, 허리 숙이기"와 같은 허리, 다리, 발의 협응이 필요한 전신 동작을 완전히 지정할 수 없다는 점입니다—동일한 낮은 위치의 팔 뻗기 동작에서도 고관절, 무릎, 발목의 구성이 작업 성공 여부를 결정합니다.

HuMI가 진정으로 바꾸는 것은 데이터 수집의 "형태적 격차" 문제입니다. 인간 시연을 로봇 운동학에 맞게 스케일링하거나 리타겟팅하는 대신(이는 종종 자체 충돌이나 도달 불가능 실패를 초래), 실시간 IK 미리보기를 통해 인간 조작자가 수집 중에 능동적으로 동작을 조정하여 시연이 원천적으로 로봇에 실행 가능하도록 합니다. 동시에 "전신 궤적"을 "엔드이펙터 궤적" 대신 정책 인터페이스로 사용하여 저수준 컨트롤러가 골반, 발과 같은 비시각적 앵커 키포인트를 추적하도록 함으로써, 스쿼트, 무릎 꿇기, 걷기 청소와 같이 휴머노이드 플랫폼의 유연성을 실제로 활용하는 작업을 가능하게 합니다. 이는 "로봇이 할 수 있는 것"의 제약을 후처리 단계에서 데이터 수집 단계로 앞당겨, 모방 학습 파이프라인 전체의 데이터 계약을 변경한 것입니다.

## 방법 분석

### 데이터 수집: 로봇-프리 시연 하드웨어
- 3D 프린팅 핸드헬드 그리퍼 2개(손목 장착 GoPro 카메라 + ArUco 마커로 그리퍼 폭 추적), HTC Vive Ultimate Tracker 5개를 그리퍼, 허리, 양발에 각각 장착.
- 헤드마운트 시스템 대신 Vive Ultimate Tracker를 선택한 이유는 후자가 쪼그려 앉아 지면 물체와 상호작용할 때 가림으로 인해 추적 성능이 저하되기 때문.
- 5개의 핵심 조작 프레임 추적: 골반(플로팅 베이스), 양손, 양발; 머리는 제외(대상 로봇 Unitree G1에 구동식 목이 없음).
- 전체 장치는 단일 백팩에 들어가며 베이스 스테이션이 필요 없고, 각 새 환경에서 환경 추적 맵 구축에 1–2분만 소요.

### 인간-인-더-루프 운동학 적응
- 인간 궤적을 스케일링하지 않고 원본 비스케일링 자세를 유지하여 시각-공간 정렬과 객체 상호작용 기하학을 보존; 골반 트래커 높이만 스케일링.
- 실시간 IK 미리보기 인터페이스(Mink 솔버 + Viser 시각화)를 통해 조작자가 실시간으로 시연을 조정하여 실행 가능성과 작업 제약을 충족.
- IK 하위 작업: 5개 키포인트의 병진/회전 추적, 자체 충돌 회피, 자세 정규화로 자연스러운 구도 유지.
- 핵심 설계: 가상 로봇을 운동학적 제약만으로 제어하므로 인지 부하가 물리적 로봇 원격 조작보다 훨씬 낮음.

### 계층적 정책 프레임워크
- **고수준 정책**: Diffusion Policy(5 Hz), 카메라 이미지 + 고유수용감 입력, 재귀적 지평선 작업 공간 궤적 출력(액션 청크: 미래 2초를 커버하는 10개 웨이포인트).
- **저수준 컨트롤러**: RL로 훈련된 전신 컨트롤러(50 Hz), 교사-학생 프레임워크(교사는 특권 상태 접근, DAgger로 학생 정책에 증류), 학생 관측에는 25스텝 상태 이력과 과거 액션이 포함.

### 적응형 엔드이펙터 추적 보상
- 전신 추적 보상: r_body = Σ_{χ∈{p,θ,v,w}} r(ē_χ^body, σ_χ), 여기서 r(ē_χ, σ_χ) = exp(−ē_χ/σ_χ²).
- 적응형 엔드이펙터 보상: r_EE = 𝕀(‖v_base^ref‖ < δ) · Σ_{χ∈{p,θ}} r(ē_χ^EE, σ_χ(v_EE^ref)).
- σ_χ는 참조 엔드이펙터 속도에 따라 [σ_χ^min, σ_χ^max] 사이에서 선형 보간(위치: 0.01–0.1 m; 회전: 5°–20°; v_max=0.1 m/s, v_min=0.05 m/s).
- 베이스 속도가 δ를 초과하면 𝕀(·) 게이팅으로 엔드이펙터 추적을 비활성화하여 빠른 운동 중 안정성을 우선.
- 커리큘럼 학습: w_EE는 0.0에서 0.5로 선형 증가, σ_p^min은 훈련 스텝 10k–15k 사이에서 0.1 m에서 0.01 m로 어닐링.

### 가변 속도 증강
- 0.02초마다 속도 스케일링 팩터 s ~ `N_[0.25, 1.25]`（1.0, σ²） 샘플링, σ는 훈련 스텝 10k–15k 사이에서 10⁻⁴에서 1.0으로 선형 증가.
- 느린 속도 변화는 정책이 작은 오류를 수정할 충분한 시간을 제공하여 고정밀 운동 학습을 촉진.

### 정책 인터페이스 개선
- **목표 자세를 고수준 액션 참조로 사용**: 실제 실행 자세를 액션 청크 참조 프레임으로 사용하지 않고(궤적의 갑작스러운 역전을 초래), 이전 계획 목표 자세를 사용하여 현재 청크와 이전 청크를 자연스럽게 연결하고 동적 작업(예: 던지기)의 부드러운 모멘텀을 유지.
- **비시각적 앵커 키포인트의 상대 자세 추적**: 골반과 발은 직접적인 시각적 앵커가 없어 추론 시 누적 불가능한 오류(>5 cm 드리프트) 발생; 추적 대상을 현재 액션 청크 내 상대 변환으로 수정(절대 변환 대신). 기본 3개 키포인트(그리퍼 + 베이스), 5개 키포인트(발 추가)로 확장 가능.

## 핵심 혁신

1. **인간-인-더-루프 IK 미리보기로 운동학적 리타겟팅 대체**: 이는 "인간 시연 → 로봇 정책" 패러다임의 근본적 재구성입니다. 기존 방법은 궤적을 스케일링하거나(물리적 장면은 스케일링 불가), 후처리 리타겟팅(시각-공간 정렬 파괴)을 사용했습니다. HuMI는 인간이 수집 중 실시간 IK 피드백을 통해 능동적으로 동작을 조정하여 로봇 실행 가능성과 원본 공간 기하학을 모두 보존합니다. 절제 실험에서 이 단계를 제거하면 프러포즈 작업 성공률이 85%에서 10%로 붕괴되어, 이는 시스템 성립의 전제 조건이지 부가 기능이 아님을 증명합니다.

2. **적응형 엔드이펙터 보상 + 가변 속도 증강의 커리큘럼 학습**: 저수준 컨트롤러는 "안정적인 전신 운동"과 "정밀한 엔드이펙터 정렬" 사이의 내재적 모순에 직면합니다—빠른 운동 중 엔드이펙터 정밀도를 추구하면 넘어지고, 느린 운동 중 안정성을 과도하게 강조하면 엔드이펙터 오류가 허용 불가능해집니다. σ_χ를 참조 속도에 따라 동적 스케일링하고, 베이스 속도 게이팅으로 엔드이펙터 보상을 비활성화하며, 가변 속도 증강으로 정책이 다양한 속도 스케일 간 전환 학습하도록 하는 이 메커니즘은 단일 컨트롤러가 느린 정밀 조작(스쿼트 병 집기)과 빠른 동적 동작(던지기)을 동시에 습득하게 합니다. 절제 실험에서 가변 속도 증강 제거 시 검 뽑기 성공률이 85%에서 50%로, 적응형 보상 제거 시 스쿼트 병 집기가 85%에서 50%로 감소.

3. **비시각적 앵커 키포인트의 상대 자세 추적**: 골반과 발은 시각적 앵커가 없어 절대 자세 추적 시 추론 중 저수준 컨트롤러 오류 누적으로 >5 cm 드리프트가 발생하며, 걷기 청소 작업에서 성공률이 75%에서 0/10으로 직접 붕괴합니다. 액션 청크 내 상대 변환으로 변경하면 오류가 청크 내로 제한되어 청크 간 누적을 방지하며, 이는 "전신 궤적"을 정책 인터페이스로 실제 사용 가능하게 만드는 핵심 엔지니어링 결정입니다.

## 실험 및 결과

### 능력 작업(도메인 내 설정, 단일 환경 100개 시연)

| 작업 | 성공률 | 핵심 절제 |
|------|--------|----------|
| 프러포즈(무릎 꿇고 링 집기) | 17/20 = 85% | IK 적응 제거: 1/10 = 10%; 전신 감독 제거: 0/10 |
| 검 뽑기 | 17/20 = 85%(EE 추적 오류 15.7 mm) | 가변 속도 증강 제거: 5/10 = 50%, 오류 21.2 mm로 증가 |
| 동적 던지기 | 15/20 = 75% | 실제 실행 자세를 참조로 사용: 4/10 = 40% |
| 걷기 청소 | 15/20 = 75% | EE-only: 6/10 = 60%; EE+pelvis: 15/20 = 75%; EE+pelvis+feet: 7/10 = 70%; 절대 자세 추적: 0/10 |

### 일반화 실험(쪼그려 앉아 집기)
- 훈련: 7개 환경 × 7개 병 = 350개 시연; 테스트: 4개 미경험 환경 + 6개 미경험 물체(형태와 질감 차이가显著的 꽃병 포함).
- 전체 성공률: 14/20 = 70%.

### 데이터 수집 효율성(검 뽑기 작업, 15분 세션, 숙련 사용자)

| 지표 | HuMI | TWIST2 |
|------|------|--------|
| 수집 세그먼트 수 | 62 | 28 |
| 수용률 | 96.7% | 64.3% |
| 수용 가능 세그먼트당 평균 시간 | TWIST2의 30.0% | 기준 |

- 프러포즈 작업: HuMI 15분 동안 50개 시연 수집, 수용률 100%, 세그먼트당 평균 18초; TWIST2는 사용 가능한 시연을 전혀 생성하지 못함(원격 조작 휴머노이드는 안정적으로 깊은 무릎 꿇기가 불가능하고 자주 안정성을 잃음).

### 절제 실험(적응형 엔드이펙터 보상, 스쿼트 병 집기)
- 고정 보상 기준(σ_p=0.01 m, σ_θ=5°, ω_EE=0.5): 성공률이 17/20 = 85%에서 5/10 = 50%로 감소.
- 전형적 실패: 스쿼트 단계에서 균형을 잃고 넘어지거나, 간신히 안정적이지만 조작을 계속할 수 없는 자세에 갇힘.

## 경계 및 한계

논문은 결론 부분에서 한계를 명시적으로 나열하지 않았습니다(단락 절단). 방법 세부 사항에서 추론할 수 있는 경계는 다음과 같습니다: 시스템은 시각적 트래커에 의존하므로 충분한 환경 질감과 조명 조건이 필요(Vive Tracker는 약한 질감이나 강한 조명에서 실패할 수 있음); 저수준 컨트롤러는 범용 목적이 아니며 훈련 구성은 통일되어 있지만 교차 작업 일반화는 검증되지 않음; 평가는 Unitree G1 단일 플랫폼에서만 수행되어 다른 휴머노이드 로봇에서 검증되지 않음; 5개 키포인트 구성(발 추가)은 걷기 청소 작업에서만 결과 보고(70%)되었고 모든 작업에서 체계적으로 평가되지 않음; 가변 속도 증강의 s_min, s_max, Δ 등 하이퍼파라미터 구체적 값은 공개되지 않음; 동적 걷기와 조작 전환의 견고성 경계, 장시간 자율 실행 안정성, 더 복잡한 지형에서의 성능은 논의되지 않음.

## 엔지니어링 시사점

재현 시 가장 쉽게 함정에 빠질 수 있는 세 가지环节을 우선 확인하세요: **첫째, 인간-인-더-루프 IK 미리보기의 실시간성**—Mink 솔버가 충분히 빨라야 하며(그렇지 않으면 조작자가 유연하게 동작을 조정할 수 없음), IK 목표는 원본 비스케일링 궤적을 사용해야 합니다(골반 높이만 스케일링). 시연 궤적을 "최적화"하려는任何 시도는 시각-공간 정렬을 파괴합니다. **둘째, 저수준 컨트롤러의 커리큘럼 학습 스케줄링**—w_EE가 0에서 0.5로 선형 증가, σ_p^min이 훈련 스텝 10k–15k 사이에서 0.1 m에서 0.01 m로 어닐링, 가변 속도 증강 σ가 동시에 10⁻⁴에서 1.0으로 증가, 이 세 가지 스케줄링은 동기화되어야 하며, 하나만 조정하면 "먼저 전역 운동 안정화, 그다음 정밀 엔드이펙터 정렬"의 학습 순서를 깨뜨릴 수 있습니다. **셋째, 비시각적 앵커 키포인트의 상대 자세 추적**—가장 쉽게 간과되지만 영향이 가장 큰 인터페이스 설계로, 절대 자세 추적은 걷기 청소 작업에서 직접 0/10 붕괴를 초래하므로 반드시 액션 청크 내 상대 변환을 사용해야 합니다(전역 절대 변환 대신). 데이터 수집 효율성 비교 시 TWIST2는 원격 조작 기준선이며, HuMI의 장점은 스쿼트/무릎 꿇기 유형 작업에서 특히 두드러집니다(TWIST2는 프러포즈 작업을 완전히 수행할 수 없음). 하류 작업이 주로 직립 조작이라면 이 장점은 줄어들 수 있습니다. 하드웨어 측면에서 GoPro 타임스탬프 동기화는 자이로스코프와 트래커 각속도의 교차 상관을 통해 구현되며, 그리퍼 폭 추출은 ArUco 마커 감지에 의존하므로 이 두 곳이 데이터 품질의 핵심 병목입니다.
