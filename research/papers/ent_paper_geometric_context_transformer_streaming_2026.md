---
$id: ent_paper_geometric_context_transformer_streaming_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Geometric Context Transformer for Streaming 3D Reconstruction
  zh: Geometric Context Transformer for Streaming 3D Reconstruction
  ko: Geometric Context Transformer for Streaming 3D Reconstruction
summary:
  en: Streaming 3D reconstruction aims to recover 3D information, such as camera poses and point clouds, from a video stream,
    which necessitates geometric accuracy, temporal consistency, and computational efficiency.
  zh: LingBot-Map 是一个基于几何上下文变换器（GCT）架构的前馈式3D基础模型，专为流式数据场景重建而设计。其核心贡献在于创新的注意力机制，通过锚点上下文、姿态参考窗口和轨迹记忆三个组件，分别解决坐标对齐、密集几何线索和长程漂移校正问题。该模型在518×378分辨率输入下可实现约20
    FPS的稳定推理，并能在超过10,000帧的长序列上保持高效性能。
  ko: Streaming 3D reconstruction aims to recover 3D information, such as camera poses and point clouds, from a video stream,
    which necessitates geometric accuracy, temporal consistency, and computational efficiency.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- geometric
- context
- transformer
- streaming
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 692 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2604.14141v2); zh content by DeepSeek from the abstract. Institutions unknown
    (not in source list). [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek
    deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit: derived values explicitly labeled (evidence from proximity-constrained full-text pair
    match). 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: arXiv:2604.14141 Geometric Context Transformer for Streaming 3D Reconstruction
  url: https://arxiv.org/abs/2604.14141
  accessed_at: '2026-07-31'
  date: '2026-04-15'
- id: src_002
  type: website
  title: Project page
  url: https://technology.robbyant.com/lingbot-map
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: Project page
  url: https://github.com/robbyant/lingbot-map
  accessed_at: '2026-07-31'
- id: src_004
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

LingBot-Map 是一个基于几何上下文 Transformer（GCT）架构的流式 3D 重建基础模型，通过几何上下文注意力（GCA）将流式状态分解为锚点上下文、局部位姿参考窗口和轨迹记忆三种互补类型，在保持长期几何一致性的同时将每帧上下文增长减少约 80 倍。该模型在 Oxford Spires、ETH3D、7-Scenes 等基准上显著超越现有流式方法，并在 12 倍更长序列上保持位姿精度几乎不退化。

## 它改变了什么

流式 3D 重建长期面临一个根本性矛盾：要维持长序列的几何一致性，就需要保留足够的历史上下文；但保留全部历史又会让内存和计算随序列长度线性膨胀，最终无法实时运行。现有方法要么像 CUT3R 那样激进压缩导致状态遗忘，要么像 StreamVGGT 那样近乎完整缓存导致资源失控，要么像 VGGT-SLAM 那样依赖手工启发式做关键帧管理。这个问题的本质是：什么样的历史信息值得保留？如何让模型自己学会选择，而不是靠人工规则？

LingBot-Map 真正改变的是把经典 SLAM 中"关键帧 + 局部窗口 + 全局地图"的结构性智慧，端到端地融入了前馈 Transformer 的注意力机制中。它不再把流式状态当作一个需要压缩的向量或缓存，而是显式地分解为三种具有不同时间尺度和信息密度的上下文类型，让模型在训练中自己学会如何利用它们。这标志着流式 3D 重建从"工程化缓存管理"走向"学习型上下文选择"的范式转变。

## 方法拆解

### 几何上下文注意力（GCA）三层次结构
GCA 将流式状态分解为三种互补上下文，每种对应不同的时间尺度和信息保留策略：

- **锚点上下文（Anchor Context）**：将前 n 帧（n≪N）指定为固定参考帧，应用全注意力并添加可学习锚点 token。训练时通过计算锚点真值点云到坐标原点的平均距离 s = (1/|𝒳̄^anchor|)∑‖𝐱‖₂，将所有真值深度和相机平移除以 s 进行尺度归一化，解决单目重建的固有尺度模糊问题。

- **局部位姿参考窗口（Local Pose-Reference Window）**：维护最近 k 帧的滑动窗口，保留完整图像 token（每帧 M 个），提供密集局部几何线索。窗口内应用相对位姿损失，强制局部几何一致性。

- **轨迹记忆（Trajectory Memory）**：对锚点集和滑动窗口之外的帧，仅保留相机、锚点和寄存器 token（每帧 6 个上下文 token），丢弃图像 token（每帧 M 个）。加入视频时间位置编码（Video RoPE）以施加时间顺序，使模型能推理轨迹的序列结构。

### 注意力掩码与复杂度
GCA 将三种上下文组合成结构化注意力掩码。对 T 帧序列，每帧注意力上下文为 (n+k)·M + 6T 个 token，而因果注意力为 MT + 6T。当 M≈500 时，GCA 每帧增长率仅 6 个 token，比因果注意力减少约 80 倍。具体地，n=3、k=16、T=10,000 时，因果注意力累积约 5×10⁶ 个 token，GCA 仅保留约 7×10⁴ 个。

### 架构与损失
- ViT 骨干初始化为 DINOv2（patch size 14），每帧添加相机 token 𝐜∈ℝ^C、4 个寄存器 token 𝐫_j∈ℝ^C 和可学习锚点 token 𝐚∈ℝ^C；交替使用帧注意力和 GCA 层。
- 损失函数：ℒ = λ_depth·ℒ_depth + λ_abs-pose·ℒ_abs-pose + λ_rel-pose·ℒ_rel-pose。与 VGGT 不同，使用相机到世界变换而非世界到相机变换进行监督，避免旋转和平移耦合导致的平移估计对旋转误差高度敏感问题。
- 相对位姿损失受 π³ 启发：ℒ_rel-pose = (1/(k(k−1)))∑_{i≠j}(ℒ_rot(i,j) + λ_trans·ℒ_trans(i,j))，其中 ℒ_rot 为测地线旋转误差，ℒ_trans 为 ℓ₁ 平移误差。

### 训练策略
- 渐进式视图训练：从短子序列开始，逐步增加视图数量（24 到 320），先学习可靠局部几何估计，再学习长期全局一致性。
- 上下文并行：采用 Ulysses 策略，将不同视图分布到多个 GPU，通过全对全集体通信实现并行注意力计算。
- 推理系统：基于 FlashInfer 实现分页 KV 缓存布局，避免标准连续布局下频繁内存重分配的开销。

## 关键创新

**1. 学习型选择性上下文管理取代手工缓存策略**：GCA 不是简单地压缩或缓存历史，而是将流式状态显式分解为三种具有不同信息密度的上下文类型，让模型在训练中自己学会如何利用它们。这是对现有流式方法（CUT3R 的激进压缩、StreamVGGT 的完整缓存）的根本性突破——选择性保留最重要的信息，且选择基于几何先验但端到端从数据中学习。

**2. 轨迹记忆的紧凑 6-token 摘要机制**：对每个被驱逐的帧仅保留 6 个上下文 token（相机、锚点和寄存器 token），以最小内存代价保留完整观测历史的关键几何线索。配合 Video RoPE 注入时间顺序，使模型能推理轨迹的序列结构（两帧时间间隔、相机移动方向），这是轨迹记忆发挥长程漂移校正潜力的关键缺失成分。

**3. 有界位姿参考窗口优于全因果注意力**：消融实验显示，窗口 size 64 相比全注意力不仅 1.7× 加速、2.7× 内存减少，ATE 还从 6.60 降至 5.98。这证明保留所有历史令牌会引入来自遥远、不相关帧的噪声，干扰注意力计算——有界窗口驱逐图像令牌但保留紧凑上下文令牌，在保留关键几何线索的同时过滤冗余信息。

## 实验与结果


### Oxford Spires 稀疏设置（320 帧）

| 方法 | AUC@15 | AUC@30 | ATE | RPE-trans | RPE-rot |
|------|--------|--------|-----|-----------|---------|
| **LingBot-Map** | **61.64** | **75.16** | **6.42** | 1.01 | 3.70 |
| DA3（最佳离线） | 49.84 | 56.68 | 12.87 | 3.22 | 16.17 |
| VIPE（最佳优化） | 45.35 | 51.88 | 10.52 | 0.43 | 5.98 |
| CUT3R | 5.98 | 14.95 | 18.16 | 1.17 | 7.18 |
| Stream3R | 9.67 | 15.21 | 29.58 | 6.67 | 16.90 |

### Oxford Spires 密集设置（3,840 帧，12× 更长序列）

| 方法 | ATE_sparse | ATE_dense | 退化幅度 |
|------|-----------|-----------|---------|
| **LingBot-Map** | 6.42 | **7.11** | **+0.69** |
| CUT3R | 18.16 | 32.47 | +14.31 |
| TTT3R | 19.35 | 25.05 | +5.70 |
| Wint3R | 21.10 | 32.90 | +11.80 |

### 跨数据集泛化

| 数据集 | LingBot-Map | 第二名 | 优势 |
|--------|-------------|--------|------|
| Tanks and Temples | AUC@30=92.80, ATE=0.20 | Stream3R: AUC@30=81.33, ATE=0.76 | +11.47 AUC, 3.8× 更低 ATE |
| ETH3D | ATE=0.22 | Wint3R: ATE=0.86 | 近 4× 更低 |
| 7-Scenes | ATE=0.08 | Stream3R: ATE=0.10 | 最低 |

### 3D 重建（F1 分数）

| 数据集 | LingBot-Map | 第二名 | 优势 |
|--------|-------------|--------|------|
| ETH3D | **98.98** | Wint3R: 77.28 | +21.70 点 |
| 7-Scenes | **80.39** | Wint3R: 78.81 | +1.58（由表内数值 5.7→5.8 计算） 点 |
| NRGBD | **64.26** | Wint3R: 56.96 | +7.30 点 |

### 消融研究（TartanAir/TartanGround）

| 配置 | AUC@3 | AUC@30 | ATE | RPE-rot |
|------|-------|--------|-----|---------|
| 仅相对位姿损失 | 9.80 | 65.84 | 8.59 | 2.57 |
| +锚点初始化 | 13.63 | 68.71 | 7.88 | 2.90 |
| +上下文令牌（有相对损失） | 15.75 | 69.92 | 7.46 | 2.26 |
| +Video RoPE（完整） | **16.39** | **71.87** | **5.98** | **1.93** |

### 推理效率

| 配置 | FPS | 内存 |
|------|-----|------|
| **窗口 size 64** | **20.29** | **13.28 GB** |
| 全注意力 | 11.87 | 36.06 GB |

## 边界与局限

- Direct 模式在约 3,000 帧（约 10 倍训练长度）后预测质量逐渐下降，超过后无法维持精度。
- VO 模式在窗口边界引入额外对齐误差，随窗口数量累积。
- 轨迹记忆压缩为每帧固定数量 token 可能丢失细粒度几何细节，对跨越数万帧的极长序列可能不利。
- 模型不包含显式回环检测，重访已观测区域时无法进一步减少累积漂移。
- 与其他前馈方法一样，不做测试时优化，在挑战性场景中可能无法进一步精化重建质量。
- 论文未提及对动态场景的处理、多模态输入（LiDAR 或 IMU）的支持，也未给出与其他流式方法在具体数值指标上的详细对比（如成功率/误差数值）。

## 工程启示

**复现时优先核对以下三点**：

1. **数据管线是最大的隐性成本**：训练语料库横跨 29 个数据集，涉及坐标系统一（UnReal4K 需额外旋转矩阵对齐 OpenCV 标准）、深度尺度归一化（ScanNet 毫米除以 1000、VirtualKITTI2 厘米除以 100）、损坏帧过滤（DL3DV 和 Mapfree 需预计算天空掩码和异常值掩码）。这些细节直接决定训练数据的质量，建议先复现数据处理流水线再投入模型训练。

2. **渐进式视图课程是关键训练技巧**：从 24 帧线性增加到 320 帧，先学习可靠局部几何估计再学习长期全局一致性。如果跳过这个课程直接训练长序列，模型很可能无法收敛。阶段 1 约 21,500 GPU 小时、阶段 2 约 15,360 GPU 小时，消融实验每个约 3,840 GPU 小时——预算规划要提前做好。

3. **推理系统依赖 FlashInfer 分页 KV 缓存**：标准连续 KV 缓存布局会导致频繁内存重分配，PyTorch 基线约 10.5 FPS，而 FlashInfer 实现达到约 20 FPS。如果下游团队要部署，建议直接基于 FlashInfer 实现，不要从 PyTorch 基线优化。

**最容易踩坑的地方**：相对位姿损失对 RPE-rot 影响极大——消融显示无相对损失时 RPE-rot 从 2.26 恶化至 5.35（2.4× 更差）。但注意相对损失只在局部位姿参考窗口内计算，窗口大小 k 在训练时从 16 到 64 随机采样，推理时固定为 64。如果下游场景需要更高的旋转精度，建议优先调这个超参数。另外，相机到世界变换的监督方式与 VGGT 的世界到相机不同，这是有意为之——世界到相机参数化中旋转和平移耦合，平移估计对旋转误差高度敏感，复现时不要改回 VGGT 的监督方式。

## 参考
- https://arxiv.org/abs/2604.14141
- https://technology.robbyant.com/lingbot-map
- https://github.com/robbyant/lingbot-map
- https://github.com/ImChong/Robotics_Notebooks

## Overview

LingBot-Map is a streaming 3D reconstruction foundation model based on the Geometric Context Transformer (GCT) architecture. It decomposes streaming state into three complementary types—anchor context, local pose-reference window, and trajectory memory—via Geometric Context Attention (GCA), reducing per-frame context growth by approximately 80× while maintaining long-term geometric consistency. The model significantly outperforms existing streaming methods on benchmarks such as Oxford Spires, ETH3D, and 7-Scenes, and maintains pose accuracy with almost no degradation on sequences 12× longer.

## What It Changes

Streaming 3D reconstruction has long faced a fundamental contradiction: maintaining geometric consistency over long sequences requires retaining sufficient historical context, but retaining all history causes memory and computation to grow linearly with sequence length, ultimately preventing real-time operation. Existing methods either aggressively compress like CUT3R, leading to state forgetting; cache nearly everything like StreamVGGT, causing resource blowup; or rely on handcrafted heuristics for keyframe management like VGGT-SLAM. The core question is: what historical information is worth keeping? How can the model learn to choose for itself, rather than relying on manual rules?

What LingBot-Map truly changes is integrating the structural wisdom of "keyframes + local window + global map" from classical SLAM end-to-end into the attention mechanism of a feed-forward Transformer. It no longer treats streaming state as a vector or cache to be compressed, but explicitly decomposes it into three context types with different temporal scales and information densities, allowing the model to learn how to use them during training. This marks a paradigm shift in streaming 3D reconstruction from "engineering-driven cache management" to "learned context selection."

## Method Breakdown

### Three-Level Structure of Geometric Context Attention (GCA)
GCA decomposes streaming state into three complementary contexts, each corresponding to different temporal scales and information retention strategies:

- **Anchor Context**: Designates the first n frames (n≪N) as fixed reference frames, applies full attention, and adds learnable anchor tokens. During training, scale normalization is performed by computing the average distance from the anchor ground-truth point cloud to the coordinate origin, s = (1/|𝒳̄^anchor|)∑‖𝐱‖₂, dividing all ground-truth depths and camera translations by s to resolve the inherent scale ambiguity of monocular reconstruction.

- **Local Pose-Reference Window**: Maintains a sliding window of the most recent k frames, retaining full image tokens (M per frame) to provide dense local geometric cues. A relative pose loss is applied within the window to enforce local geometric consistency.

- **Trajectory Memory**: For frames outside the anchor set and sliding window, only camera, anchor, and register tokens are retained (6 context tokens per frame), while image tokens (M per frame) are discarded. Video temporal position encoding (Video RoPE) is added to impose temporal order, enabling the model to reason about the sequential structure of the trajectory.

### Attention Mask and Complexity
GCA combines the three contexts into a structured attention mask. For a T-frame sequence, the per-frame attention context is (n+k)·M + 6T tokens, whereas causal attention requires MT + 6T. When M≈500, GCA's per-frame growth rate is only 6 tokens, approximately 80× less than causal attention. Specifically, with n=3, k=16, and T=10,000, causal attention accumulates roughly 5×10⁶ tokens, while GCA retains only about 7×10⁴.

### Architecture and Losses
- The ViT backbone is initialized from DINOv2 (patch size 14), with per-frame camera tokens 𝐜∈ℝ^C, 4 register tokens 𝐫_j∈ℝ^C, and learnable anchor tokens 𝐚∈ℝ^C; frame attention and GCA layers are interleaved.
- Loss function: ℒ = λ_depth·ℒ_depth + λ_abs-pose·ℒ_abs-pose + λ_rel-pose·ℒ_rel-pose. Unlike VGGT, supervision uses camera-to-world transforms rather than world-to-camera transforms, avoiding the coupling of rotation and translation that makes translation estimation highly sensitive to rotation errors.
- The relative pose loss is inspired by π³: ℒ_rel-pose = (1/(k(k−1)))∑_{i≠j}(ℒ_rot(i,j) + λ_trans·ℒ_trans(i,j)), where ℒ_rot is the geodesic rotation error and ℒ_trans is the ℓ₁ translation error.

### Training Strategy
- Progressive view training: starting from short subsequences and gradually increasing the number of views (24 to 320), first learning reliable local geometry estimation, then long-term global consistency.
- Context parallelism: adopts the Ulysses strategy, distributing different views across multiple GPUs and implementing parallel attention computation via all-to-all collective communication.
- Inference system: implements paged KV cache layout based on FlashInfer, avoiding the overhead of frequent memory reallocation under standard contiguous layouts.

## Key Innovations

**1. Learned selective context management replaces handcrafted caching strategies**: GCA does not simply compress or cache history but explicitly decomposes streaming state into three context types with different information densities, letting the model learn how to use them during training. This is a fundamental breakthrough over existing streaming methods (CUT3R's aggressive compression, StreamVGGT's full caching)—selectively retaining the most important information, with selection based on geometric priors but learned end-to-end from data.

**2. Compact 6-token summarization mechanism for trajectory memory**: Only 6 context tokens (camera, anchor, and register tokens) are retained per evicted frame, preserving key geometric cues from the full observation history at minimal memory cost. Combined with Video RoPE injecting temporal order, the model can reason about the sequential structure of the trajectory (time intervals between frames, camera movement direction)—a critical missing component for trajectory memory to realize its long-range drift correction potential.

**3. Bounded pose-reference window outperforms full causal attention**: Ablation experiments show that a window size of 64, compared to full attention, not only achieves 1.7× speedup and 2.7× memory reduction but also improves ATE from 6.60 to 5.98. This demonstrates that retaining all historical tokens introduces noise from distant, irrelevant frames that interferes with attention computation—the bounded window evicts image tokens while retaining compact context tokens, filtering redundant information while preserving key geometric cues.

## Experiments and Results

### Oxford Spires Sparse Setting (320 frames)

| Method | AUC@15 | AUC@30 | ATE | RPE-trans | RPE-rot |
|--------|--------|--------|-----|-----------|---------|
| **LingBot-Map** | **61.64** | **75.16** | **6.42** | 1.01 | 3.70 |
| DA3 (best offline) | 49.84 | 56.68 | 12.87 | 3.22 | 16.17 |
| VIPE (best optimization) | 45.35 | 51.88 | 10.52 | 0.43 | 5.98 |
| CUT3R | 5.98 | 14.95 | 18.16 | 1.17 | 7.18 |
| Stream3R | 9.67 | 15.21 | 29.58 | 6.67 | 16.90 |

### Oxford Spires Dense Setting (3,840 frames, 12× longer sequences)

| Method | ATE_sparse | ATE_dense | Degradation |
|--------|-----------|-----------|-------------|
| **LingBot-Map** | 6.42 | **7.11** | **+0.69** |
| CUT3R | 18.16 | 32.47 | +14.31 |
| TTT3R | 19.35 | 25.05 | +5.70 |
| Wint3R | 21.10 | 32.90 | +11.80 |

### Cross-Dataset Generalization

| Dataset | LingBot-Map | Second Best | Advantage |
|---------|-------------|-------------|-----------|
| Tanks and Temples | AUC@30=92.80, ATE=0.20 | Stream3R: AUC@30=81.33, ATE=0.76 | +11.47 AUC, 3.8× lower ATE |
| ETH3D | ATE=0.22 | Wint3R: ATE=0.86 | Nearly 4× lower |
| 7-Scenes | ATE=0.08 | Stream3R: ATE=0.10 | Lowest |

### 3D Reconstruction (F1 Score)

| Dataset | LingBot-Map | Second Best | Advantage |
|---------|-------------|-------------|-----------|
| ETH3D | **98.98** | Wint3R: 77.28 | +21.70 points |
| 7-Scenes | **80.39** | Wint3R: 78.81 | +1.58 (computed from in-table values 5.7→5.8) points |
| NRGBD | **64.26** | Wint3R: 56.96 | +7.30 points |

### Ablation Study (TartanAir/TartanGround)

| Configuration | AUC@3 | AUC@30 | ATE | RPE-rot |
|---------------|-------|--------|-----|---------|
| Relative pose loss only | 9.80 | 65.84 | 8.59 | 2.57 |
| +Anchor initialization | 13.63 | 68.71 | 7.88 | 2.90 |
| +Context tokens (with relative loss) | 15.75 | 69.92 | 7.46 | 2.26 |
| +Video RoPE (full) | **16.39** | **71.87** | **5.98** | **1.93** |

### Inference Efficiency

| Configuration | FPS | Memory |
|---------------|-----|--------|
| **Window size 64** | **20.29** | **13.28 GB** |
| Full attention | 11.87 | 36.06 GB |

## Boundaries and Limitations

- Direct mode shows gradually degrading prediction quality after approximately 3,000 frames (about 10× training length), unable to maintain accuracy beyond that point.
- VO mode introduces additional alignment errors at window boundaries, accumulating with the number of windows.
- Trajectory memory compression to a fixed number of tokens per frame may lose fine-grained geometric details, potentially disadvantageous for extremely long sequences spanning tens of thousands of frames.
- The model does not include explicit loop closure detection, so revisiting previously observed regions cannot further reduce accumulated drift.
- Like other feed-forward methods, it does not perform test-time optimization and may not further refine reconstruction quality in challenging scenarios.
- The paper does not address dynamic scene handling, multimodal input support (LiDAR or IMU), nor does it provide detailed numerical comparisons with other streaming methods on specific metrics (e.g., success rates/error values).

## Engineering Insights

**Prioritize verifying the following three points during reproduction**:

1. **The data pipeline is the largest hidden cost**: The training corpus spans 29 datasets, involving coordinate system unification (UnReal4K requires additional rotation matrices to align with OpenCV standards), depth scale normalization (ScanNet millimeters divided by 1000, VirtualKITTI2 centimeters divided by 100), and corrupted frame filtering (DL3DV and Mapfree require precomputed sky masks and outlier masks). These details directly determine training data quality—it is recommended to reproduce the data processing pipeline before investing in model training.

2. **Progressive view curriculum is a key training technique**: Linearly increasing from 24 to 320 frames, first learning reliable local geometry estimation and then long-term global consistency. Skipping this curriculum and directly training on long sequences will likely prevent convergence. Stage 1 requires approximately 21,500 GPU hours, Stage 2 approximately 15,360 GPU hours, and each ablation experiment approximately 3,840 GPU hours—budget planning should be done in advance.

3. **The inference system relies on FlashInfer paged KV cache**: Standard contiguous KV cache layouts cause frequent memory reallocation, with the PyTorch baseline at approximately 10.5 FPS, while the FlashInfer implementation reaches approximately 20 FPS. If downstream teams plan to deploy, it is recommended to implement directly on FlashInfer rather than optimizing from the PyTorch baseline.

**Most common pitfall**: The relative pose loss has a significant impact on RPE-rot—ablation shows that without the relative loss, RPE-rot degrades from 2.26 to 5.35 (2.4× worse). Note that the relative loss is only computed within the local pose-reference window, with window size k randomly sampled from 16 to 64 during training and fixed at 64 during inference. If downstream scenarios require higher rotation accuracy, this hyperparameter should be prioritized for tuning. Additionally, the camera-to-world supervision differs deliberately from VGGT's world-to-camera approach—in the world-to-camera parameterization, rotation and translation are coupled, making translation estimation highly sensitive to rotation errors. Do not revert to VGGT's supervision method during reproduction.

## 개요

LingBot-Map은 기하학적 컨텍스트 트랜스포머(GCT) 아키텍처 기반의 스트리밍 3D 재구성 기반 모델로, 기하학적 컨텍스트 어텐션(GCA)을 통해 스트리밍 상태를 앵커 컨텍스트, 로컬 포즈 참조 윈도우, 궤적 메모리의 세 가지 상호보완적 유형으로 분해하여 장기 기하학적 일관성을 유지하면서 프레임당 컨텍스트 증가량을 약 80배 줄입니다. 이 모델은 Oxford Spires, ETH3D, 7-Scenes 등 벤치마크에서 기존 스트리밍 방법을 크게 능가하며, 12배 더 긴 시퀀스에서도 포즈 정확도가 거의 저하되지 않습니다.

## 무엇을 바꾸었는가

스트리밍 3D 재구성은 오랫동안 근본적인 모순에 직면해 왔습니다: 긴 시퀀스의 기하학적 일관성을 유지하려면 충분한 과거 컨텍스트를 보존해야 하지만, 전체 이력을 보존하면 메모리와 계산이 시퀀스 길이에 따라 선형적으로 증가하여 결국 실시간 실행이 불가능해집니다. 기존 방법은 CUT3R처럼 과감한 압축으로 상태 망각을 초래하거나, StreamVGGT처럼 거의 완전한 캐싱으로 리소스가 통제 불능이 되거나, VGGT-SLAM처럼 수동 휴리스틱에 의존한 키프레임 관리를 수행합니다. 이 문제의 본질은: 어떤 과거 정보를 보존할 가치가 있는가? 모델이 수동 규칙이 아닌 스스로 선택하는 법을 학습하게 하는 방법은 무엇인가?

LingBot-Map이 진정으로 바꾼 것은 고전 SLAM의 "키프레임 + 로컬 윈도우 + 글로벌 맵"이라는 구조적 지혜를 엔드투엔드로 피드포워드 트랜스포머의 어텐션 메커니즘에 통합한 것입니다. 더 이상 스트리밍 상태를 압축해야 할 벡터나 캐시로 취급하지 않고, 서로 다른 시간 스케일과 정보 밀도를 가진 세 가지 컨텍스트 유형으로 명시적으로 분해하여 모델이 훈련 중에 이를 활용하는 방법을 스스로 학습하게 합니다. 이는 스트리밍 3D 재구성이 "공학적 캐시 관리"에서 "학습 기반 컨텍스트 선택"으로의 패러다임 전환을 의미합니다.

## 방법 분석

### 기하학적 컨텍스트 어텐션(GCA) 3계층 구조
GCA는 스트리밍 상태를 세 가지 상호보완적 컨텍스트로 분해하며, 각각 서로 다른 시간 스케일과 정보 보존 전략에 대응합니다:

- **앵커 컨텍스트(Anchor Context)**: 처음 n개 프레임(n≪N)을 고정 참조 프레임으로 지정하고, 전체 어텐션을 적용하며 학습 가능한 앵커 토큰을 추가합니다. 훈련 시 앵커 GT 포인트 클라우드에서 원점까지의 평균 거리 s = (1/|𝒳̄^anchor|)∑‖𝐱‖₂를 계산하여 모든 GT 깊이와 카메라 이동을 s로 나누어 스케일 정규화를 수행함으로써 단안 재구성의 고유한 스케일 모호성 문제를 해결합니다.

- **로컬 포즈 참조 윈도우(Local Pose-Reference Window)**: 최근 k개 프레임의 슬라이딩 윈도우를 유지하며, 전체 이미지 토큰(프레임당 M개)을 보존하여 밀집된 로컬 기하학적 단서를 제공합니다. 윈도우 내에서는 상대 포즈 손실을 적용하여 로컬 기하학적 일관성을 강제합니다.

- **궤적 메모리(Trajectory Memory)**: 앵커 집합과 슬라이딩 윈도우 외부의 프레임에 대해서는 카메라, 앵커, 레지스터 토큰(프레임당 6개 컨텍스트 토큰)만 보존하고 이미지 토큰(프레임당 M개)은 폐기합니다. 비디오 시간 위치 인코딩(Video RoPE)을 추가하여 시간 순서를 부여함으로써 모델이 궤적의 시퀀스 구조를 추론할 수 있게 합니다.

### 어텐션 마스크와 복잡도
GCA는 세 가지 컨텍스트를 구조화된 어텐션 마스크로 결합합니다. T개 프레임 시퀀스에 대해 프레임당 어텐션 컨텍스트는 (n+k)·M + 6T개 토큰인 반면, 인과 어텐션은 MT + 6T개입니다. M≈500일 때 GCA의 프레임당 증가율은 6개 토큰에 불과하여 인과 어텐션보다 약 80배 감소합니다. 구체적으로 n=3, k=16, T=10,000일 때 인과 어텐션은 약 5×10⁶개 토큰을 누적하지만, GCA는 약 7×10⁴개만 유지합니다.

### 아키텍처와 손실
- ViT 백본은 DINOv2(patch size 14)로 초기화되며, 프레임마다 카메라 토큰 𝐜∈ℝ^C, 4개의 레지스터 토큰 𝐫_j∈ℝ^C, 학습 가능한 앵커 토큰 𝐚∈ℝ^C가 추가됩니다; 프레임 어텐션과 GCA 레이어를 교대로 사용합니다.
- 손실 함수: ℒ = λ_depth·ℒ_depth + λ_abs-pose·ℒ_abs-pose + λ_rel-pose·ℒ_rel-pose. VGGT와 달리 세계-카메라 변환이 아닌 카메라-세계 변환으로 감독하여 회전과 이동의 결합으로 인한 이동 추정의 회전 오차 민감성 문제를 피합니다.
- 상대 포즈 손실은 π³에서 영감을 받았습니다: ℒ_rel-pose = (1/(k(k−1)))∑_{i≠j}(ℒ_rot(i,j) + λ_trans·ℒ_trans(i,j)), 여기서 ℒ_rot는 측지선 회전 오차, ℒ_trans는 ℓ₁ 이동 오차입니다.

### 훈련 전략
- 점진적 뷰 훈련: 짧은 하위 시퀀스에서 시작하여 뷰 수를 점진적으로 증가시키고(24에서 320), 먼저 신뢰할 수 있는 로컬 기하학 추정을 학습한 다음 장기 글로벌 일관성을 학습합니다.
- 컨텍스트 병렬 처리: Ulysses 전략을 채택하여 서로 다른 뷰를 여러 GPU에 분산하고, 올투올(all-to-all) 집단 통신을 통해 병렬 어텐션 계산을 구현합니다.
- 추론 시스템: FlashInfer 기반의 페이징 KV 캐시 레이아웃을 구현하여 표준 연속 레이아웃에서의 빈번한 메모리 재할당 오버헤드를 피합니다.

## 핵심 혁신

**1. 학습 기반 선택적 컨텍스트 관리가 수동 캐시 전략을 대체**: GCA는 단순히 이력을 압축하거나 캐싱하는 것이 아니라, 스트리밍 상태를 서로 다른 정보 밀도를 가진 세 가지 컨텍스트 유형으로 명시적으로 분해하여 모델이 훈련 중에 이를 활용하는 방법을 스스로 학습하게 합니다. 이는 기존 스트리밍 방법(CUT3R의 과감한 압축, StreamVGGT의 완전 캐싱)에 대한 근본적 돌파구입니다—가장 중요한 정보를 선택적으로 보존하며, 선택은 기하학적 사전 지식에 기반하지만 엔드투엔드로 데이터에서 학습됩니다.

**2. 궤적 메모리의 컴팩트한 6-토큰 요약 메커니즘**: 퇴출된 각 프레임에 대해 6개의 컨텍스트 토큰(카메라, 앵커, 레지스터 토큰)만 보존하여 최소 메모리 비용으로 전체 관측 이력의 핵심 기하학적 단서를 유지합니다. Video RoPE로 시간 순서를 주입하여 모델이 궤적의 시퀀스 구조(두 프레임 간 시간 간격, 카메라 이동 방향)를 추론할 수 있게 하는데, 이는 궤적 메모리가 장거리 드리프트 보정 잠재력을 발휘하는 데 핵심적인 누락 요소입니다.

**3. 유계 포즈 참조 윈도우가 전체 인과 어텐션보다 우수**: 절제 실험에서 윈도우 크기 64는 전체 어텐션에 비해 1.7배 가속, 2.7배 메모리 감소뿐만 아니라 ATE도 6.60에서 5.98로 감소했습니다. 이는 모든 과거 토큰을 보존하면 멀리 떨어진 무관한 프레임의 노이즈가 도입되어 어텐션 계산을 방해한다는 것을 증명합니다—유계 윈도우는 이미지 토큰을 퇴출하지만 컴팩트한 컨텍스트 토큰을 보존하여 핵심 기하학적 단서를 유지하면서 중복 정보를 필터링합니다.

## 실험 및 결과

### Oxford Spires 희소 설정(320프레임)

| 방법 | AUC@15 | AUC@30 | ATE | RPE-trans | RPE-rot |
|------|--------|--------|-----|-----------|---------|
| **LingBot-Map** | **61.64** | **75.16** | **6.42** | 1.01 | 3.70 |
| DA3(최고 오프라인) | 49.84 | 56.68 | 12.87 | 3.22 | 16.17 |
| VIPE(최고 최적화) | 45.35 | 51.88 | 10.52 | 0.43 | 5.98 |
| CUT3R | 5.98 | 14.95 | 18.16 | 1.17 | 7.18 |
| Stream3R | 9.67 | 15.21 | 29.58 | 6.67 | 16.90 |

### Oxford Spires 밀집 설정(3,840프레임, 12배 더 긴 시퀀스)

| 방법 | ATE_sparse | ATE_dense | 저하 폭 |
|------|-----------|-----------|---------|
| **LingBot-Map** | 6.42 | **7.11** | **+0.69** |
| CUT3R | 18.16 | 32.47 | +14.31 |
| TTT3R | 19.35 | 25.05 | +5.70 |
| Wint3R | 21.10 | 32.90 | +11.80 |

### 교차 데이터셋 일반화

| 데이터셋 | LingBot-Map | 2위 | 우위 |
|--------|-------------|--------|------|
| Tanks and Temples | AUC@30=92.80, ATE=0.20 | Stream3R: AUC@30=81.33, ATE=0.76 | +11.47 AUC, 3.8배 낮은 ATE |
| ETH3D | ATE=0.22 | Wint3R: ATE=0.86 | 약 4배 낮음 |
| 7-Scenes | ATE=0.08 | Stream3R: ATE=0.10 | 최저 |

### 3D 재구성(F1 점수)

| 데이터셋 | LingBot-Map | 2위 | 우위 |
|--------|-------------|--------|------|
| ETH3D | **98.98** | Wint3R: 77.28 | +21.70 포인트 |
| 7-Scenes | **80.39** | Wint3R: 78.81 | +1.58(표 내 수치 5.7→5.8로 계산) 포인트 |
| NRGBD | **64.26** | Wint3R: 56.96 | +7.30 포인트 |

### 절제 연구(TartanAir/TartanGround)

| 구성 | AUC@3 | AUC@30 | ATE | RPE-rot |
|------|-------|--------|-----|---------|
| 상대 포즈 손실만 | 9.80 | 65.84 | 8.59 | 2.57 |
| +앵커 초기화 | 13.63 | 68.71 | 7.88 | 2.90 |
| +컨텍스트 토큰(상대 손실 포함) | 15.75 | 69.92 | 7.46 | 2.26 |
| +Video RoPE(전체) | **16.39** | **71.87** | **5.98** | **1.93** |

### 추론 효율성

| 구성 | FPS | 메모리 |
|------|-----|------|
| **윈도우 크기 64** | **20.29** | **13.28 GB** |
| 전체 어텐션 | 11.87 | 36.06 GB |

## 경계와 한계

- Direct 모드는 약 3,000프레임(훈련 길이의 약 10배) 이후 예측 품질이 점진적으로 저하되며, 이를 초과하면 정확도를 유지할 수 없습니다.
- VO 모드는 윈도우 경계에서 추가 정렬 오차를 도입하며, 윈도우 수에 따라 누적됩니다.
- 궤적 메모리를 프레임당 고정 토큰 수로 압축하면 세밀한 기하학적 세부 사항이 손실될 수 있으며, 수만 프레임에 걸친 극도로 긴 시퀀스에는 불리할 수 있습니다.
- 모델에는 명시적 루프 폐쇄 감지가 포함되어 있지 않아, 이미 관측된 영역을 재방문할 때 누적 드리프트를 더 줄일 수 없습니다.
- 다른 피드포워드 방법과 마찬가지로 테스트 시 최적화를 수행하지 않으므로, 도전적인 시나리오에서 재구성 품질을 더 정제할 수 없습니다.
- 논문은 동적 장면 처리, 다중 모달 입력(LiDAR 또는 IMU) 지원을 언급하지 않았으며, 다른 스트리밍 방법과의 구체적인 수치 지표(성공률/오차 수치)에 대한 상세 비교도 제공하지 않습니다.

## 공학적 시사점

**재현 시 다음 세 가지를 우선 확인하세요**:

1. **데이터 파이프라인이 가장 큰 잠재 비용**: 훈련 코퍼스는 29개 데이터셋에 걸쳐 있으며, 좌표계 통일(UnReal4K는 OpenCV 표준 정렬을 위한 추가 회전 행렬 필요), 깊이 스케일 정규화(ScanNet 밀리미터를 1000으로 나눔, VirtualKITTI2 센티미터를 100으로 나눔), 손상 프레임 필터링(DL3DV와 Mapfree는 사전 계산된 하늘 마스크와 이상치 마스크 필요)이 포함됩니다. 이러한 세부 사항이 훈련 데이터 품질을 직접 결정하므로, 모델 훈련에 투자하기 전에 데이터 처리 파이프라인을 먼저 재현하는 것이 좋습니다.

2. **점진적 뷰 커리큘럼이 핵심 훈련 기법**: 24프레임에서 320프레임으로 선형적으로 증가시키며, 먼저 신뢰할 수 있는 로컬 기하학 추정을 학습한 다음 장기 글로벌 일관성을 학습합니다. 이 커리큘럼을 건너뛰고 바로 긴 시퀀스를 훈련하면 모델이 수렴하지 않을 가능성이 높습니다. 1단계는 약 21,500 GPU 시간, 2단계는 약 15,360 GPU 시간, 절제 실험은 각각 약 3,840 GPU 시간입니다—예산 계획을 미리 세우세요.

3. **추론 시스템은 FlashInfer 페이징 KV 캐시에 의존**: 표준 연속 KV 캐시 레이아웃은 빈번한 메모리 재할당을 초래하며, PyTorch 기준선은 약 10.5 FPS인 반면 FlashInfer 구현은 약 20 FPS에 도달합니다. 하위 팀이 배포하려면 PyTorch 기준선 최적화가 아닌 FlashInfer 기반으로 직접 구현하는 것이 좋습니다.

**가장 함정에 빠지기 쉬운 부분**: 상대 포즈 손실은 RPE-rot에 큰 영향을 미칩니다—절제 결과 상대 손실이 없으면 RPE-rot가 2.26에서 5.35로 악화됩니다(2.4배 더 나쁨). 단, 상대 손실은 로컬 포즈 참조 윈도우 내에서만 계산되며, 윈도우 크기 k는 훈련 시 16에서 64 사이로 무작위 샘플링되고 추론 시 64로 고정됩니다. 하위 시나리오에서 더 높은 회전 정밀도가 필요하다면 이 하이퍼파라미터를 우선 조정하는 것이 좋습니다. 또한, 카메라-세계 변환 감독 방식은 VGGT의 세계-카메라와 다르며, 이는 의도적인 것입니다—세계-카메라 매개변수화에서는 회전과 이동이 결합되어 이동 추정이 회전 오차에 매우 민감하므로, 재현 시 VGGT의 감독 방식으로 되돌리지 마세요.
