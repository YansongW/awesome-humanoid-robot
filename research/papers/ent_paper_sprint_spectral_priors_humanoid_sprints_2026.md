---
$id: ent_paper_sprint_spectral_priors_humanoid_sprints_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'SPRINT: Efficient Spectral Priors for Humanoid Athletic Sprints'
  zh: 'SPRINT: Efficient Spectral Priors for Humanoid Athletic Sprints'
  ko: 'SPRINT: Efficient Spectral Priors for Humanoid Athletic Sprints'
summary:
  en: 'The pursuit of humanoid athletic sprints is hindered by a scarcity of humanoid-viable kinematic reference data and
    the inability of existing frameworks to maintain stability during sprints. Institutions per source list: 国防科技大学、湖南大学.'
  zh: SPRINT 是一个面向人形机器人高速冲刺的框架，由研究团队提出，利用频率自适应频谱先验生成运动轨迹。该框架在 Unitree G1 平台上实现了零样本仿真到现实迁移，峰值冲刺速度达 6 m/s，并保持仿生自然性。
  ko: 'The pursuit of humanoid athletic sprints is hindered by a scarcity of humanoid-viable kinematic reference data and
    the inability of existing frameworks to maintain stability during sprints. Institutions per source list: 国防科技大学、湖南大学.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- sprint
- efficient
- spectral
- priors
- humanoi
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 791 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2605.28549v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit: derived values explicitly labeled (evidence from proximity-constrained full-text pair
    match).'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2605.28549 SPRINT: Efficient Spectral Priors for Humanoid Athletic Sprints'
  url: https://arxiv.org/abs/2605.28549
  accessed_at: '2026-07-31'
  date: '2026-05-27'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

SPRINT 提出一种数据节约的三阶段框架，利用频谱先验（spectral priors）将仅 5 段人类运动序列映射为连续速度流形，结合分层残差强化学习，在 Unitree G1 上实现 0–6 m/s 的仿生冲刺与无缝步态转换。核心贡献在于用频率作为运动表征的连续化桥梁，显著降低了对大规模参考数据与复杂运动跟踪的依赖。

## 它改变了什么

人形机器人高速运动长期受制于两个死结：一是缺乏适用于双足形态的运动学参考，二是现有模仿学习框架在高速域会陷入训练不稳定或模式崩溃。AMP 类方法依赖大规模参考数据且难以外推，GMP/CMG 类方法虽跟踪稳健却无法连续调速，AI-CPG 混合策略则被限制在 4 m/s 以下。SPRINT 真正改变的是“参考数据必须覆盖目标速度域”这一隐含假设——它证明仅凭 5 段低速序列（最高 3.4 m/s），通过频谱域的先验建模与残差 RL 的互补，就能外推出 6 m/s 的冲刺能力。这不仅是速度数字的突破，更动摇了“数据量决定运动上限”的行业直觉，为数据稀缺场景下的高速运动控制提供了新范式。

## 方法拆解

### Stage I：参考库构建
- 从 LAFAN1 提取 5 段单周期步态（行走/慢跑/跑步），经 GMR 形态学重定向至 10 自由度关节子集 𝒥（髋/膝/踝俯仰 + 肩/肘）。
- 预处理：傅里叶重采样 30→60 Hz，Savitzky-Golay 滤波去噪。
- 关键映射：FFT 计算 PSD 得到速度-主频率对 {0.66, 1.10, 2.29, 2.87, 3.40} m/s ↔ {0.68, 0.86, 1.25, 1.36, 1.58} Hz，并确认对侧肢体半周期相位偏移。

### Stage II：频谱先验训练
- **条件 VAE**：编码器将目标频率 f 映射为后验 q(z|f)=𝒩(μ, diag(σ²))，重参数化采样 z=μ+σ⊙ε。
- **多谐波节律**：瞬时相位 φ=2πft，展开 K=6 谐波得频谱向量 x(t,f)∈ℝ^(2K)。
- **FiLM 调制生成**：上下文 c=[z, f]，每层缩放 γ^(l)=1+0.1·tanh(·)，偏移 β^(l)=0.1·tanh(·)，中间特征 h^(l)=σ(γ^(l)⊙W^(l)h^(l-1)+β^(l))，SiLU 激活。
- **损失**：L = L_rec + β_KL·L_KL，重建为 MSE，KL 正则至标准正态。

### Stage III：SPRINT 策略训练
- **POMDP + PPO**：非对称 Actor-Critic（AAC），actor 仅用本体感觉（角速度、投影重力、命令、关节运动学、先前残差），critic 用特权状态（线性速度 + 频谱参考轨迹）。
- **分层残差控制**：冻结先验生成 q_ref，RL 输出残差 a_res，目标 q_target = q_ref + α·a_res（α 为标量增益）。
- **奖励四元组**：任务奖励（速度跟踪，加速度限幅）、先验引导（偏离惩罚）、脚部引导（最小飞行时间 t_target = D_swing/f_cmd，D_swing=0.4）、正则化（前倾目标 g_f = 0.1+0.05·v_x_cmd）。
- **课程学习**：渐进扩展速度命令范围，配合动力学随机化（质量、摩擦、电机延迟）。

## 关键创新

1. **频率作为连续运动流形的锚点**：将离散参考序列通过 FFT 主频率映射到连续潜空间，使得速度命令 v_x_cmd 可直接换算为目标频率 f（v∈[0,6] m/s ↔ f∈[0.6,2.3] Hz），实现真正的连续调速而非离散步态插值。这是对传统“步态库+切换”范式的根本性简化。
2. **频谱先验 + 残差 RL 的互补设计**：先验负责仿生节律与相位结构，RL 只学残差修正，既避免了纯 RL 的探索崩溃，又克服了纯先验的跟踪刚性。消融显示去掉 r_prior 后 FID 从 0.80 恶化至 12.23，验证了该耦合的必要性。
3. **数据效率的极致压缩**：仅 5 段序列（428 帧）即可训练出覆盖 0–6 m/s 的策略，相比 AMP 类方法动辄数小时的数据采集，训练时间缩短至 6.5 小时（RTX 4090），为实际部署提供了可复现的成本基准。

## 实验与结果


| 方法 | Max v_avg (m/s) | 收敛时间 (h) | FID | E_qpos@[0,6] |
|------|----------------|-------------|-----|--------------|
| Humanoid-Gym | 3.9048 | 4.5 | – | 0.0385 |
| AMP | 4.7512 | 34.5 | 37 | 0.1061 |
| AI-CPG | 4.8208 | 9 | 14 | 0.0802 |
| **SPRINT** | **5.9648** | **6.5** | **0.8035** | **0.0657** |

- 频谱先验 vs AI-CPG：L_rec 降低 60%（由表内数值 0.2336→0.0932 计算）（0.0015→0.0006），FID 降低 81%（由表内数值 3.4→0.66 计算）（由表内数值 0.66÷0.66 计算）（由表内数值 3.4→0.66 计算），E_BA 降低 94%（由表内数值 100.0−6.0 计算），证明先验生成质量显著更优。
- 消融关键：去掉课程学习后最大速度跌至 4.48 m/s（-25%（由表内数值 2.29→2.87 计算）），去掉残差结构 FID 恶化至 2.33（由表内数值 1.1−3.4 计算），去掉 AAC 后 E_vel 上升 15%（由表内数值 0.0657→0.0755 计算）——三者均为必要组件。
- 硬件迁移（Unitree G1）：1–6 m/s 全程 sim-to-real 平均误差 FID=0.0346、E_qpos=0.0052、v_avg=0.0428，6 m/s 时实机速度 5.9143 m/s（仿真 5.9648），差距 <1%，说明策略对动力学随机化鲁棒。

## 边界与局限

论文未明确讨论局限性。从事实要点可推断的边界：1) 未探索超过 6 m/s 的速度域，频谱先验在 f>2.3 Hz 的外推行为未知；2) 仅在 Unitree G1 单平台验证，未测试其他形态（如更大/更小机器人）；3) 未评估非平坦地形、扰动或变向场景，奖励函数中的脚部引导假设地面平整；4) 参考库仅含直线步态，未覆盖跳跃、急停等非周期运动；5) β_KL 与残差增益 α 的具体取值未披露，可能影响复现精度。

## 工程启示

复现时优先核对三处：一是 Stage I 的 FFT 主频率提取是否与论文映射一致（0.68–1.58 Hz），这是先验训练的地基；二是 FiLM 生成器的 tanh 限幅参数（γ 范围 [0.9,1.1]，β 范围 [-0.1,0.1]），过大的调制范围会导致生成轨迹失真；三是课程学习的进度表——消融显示这是速度上限的关键瓶颈，建议从 [0,4] m/s 起步，每 30 分钟扩展 0.5 m/s。最容易踩坑的是残差增益 α 的调参：过小则策略被先验锁死，过大则失去仿生结构，建议在 [0.3, 0.7] 区间网格搜索。硬件部署时注意电机延迟随机化范围需覆盖实机标定值，否则 6 m/s 下的相位误差会迅速累积。

## 参考
- https://arxiv.org/abs/2605.28549
- https://github.com/ImChong/Robotics_Notebooks

## Overview

SPRINT proposes a data-efficient three-stage framework that leverages spectral priors to map just 5 human motion sequences into a continuous velocity manifold. Combined with hierarchical residual reinforcement learning, it achieves biomimetic sprinting from 0 to 6 m/s with seamless gait transitions on the Unitree G1. The core contribution lies in using frequency as a continuous bridge for motion representation, significantly reducing reliance on large-scale reference data and complex motion tracking.

## What It Changes

High-speed locomotion for humanoid robots has long been constrained by two bottlenecks: the lack of kinematic references suitable for bipedal morphology, and the tendency of existing imitation learning frameworks to suffer from training instability or mode collapse in high-speed regimes. AMP-style methods depend on large-scale reference data and struggle to extrapolate; GMP/CMG-style methods track robustly but cannot continuously modulate speed; AI-CPG hybrid strategies are limited to below 4 m/s. What SPRINT truly changes is the implicit assumption that "reference data must cover the target velocity range"—it demonstrates that with only 5 low-speed sequences (up to 3.4 m/s), sprinting capability at 6 m/s can be extrapolated through spectral-domain prior modeling complemented by residual RL. This is not merely a breakthrough in speed numbers; it challenges the industry intuition that "data volume determines the motion ceiling," offering a new paradigm for high-speed motion control in data-scarce scenarios.

## Method Breakdown

### Stage I: Reference Library Construction
- Extract 5 single-cycle gaits (walk/jog/run) from LAFAN1, retargeted via GMR morphological adaptation to a 10-DoF joint subset 𝒥 (hip/knee/ankle pitch + shoulder/elbow).
- Preprocessing: Fourier resampling from 30 to 60 Hz, Savitzky-Golay filtering for denoising.
- Key mapping: FFT-based PSD yields velocity-dominant frequency pairs {0.66, 1.10, 2.29, 2.87, 3.40} m/s ↔ {0.68, 0.86, 1.25, 1.36, 1.58} Hz, with confirmation of half-cycle phase offset in contralateral limbs.

### Stage II: Spectral Prior Training
- **Conditional VAE**: Encoder maps target frequency f to posterior q(z|f)=𝒩(μ, diag(σ²)), with reparameterized sampling z=μ+σ⊙ε.
- **Multi-harmonic rhythm**: Instantaneous phase φ=2πft, expanded to K=6 harmonics yielding spectral vector x(t,f)∈ℝ^(2K).
- **FiLM-modulated generation**: Context c=[z, f], per-layer scaling γ^(l)=1+0.1·tanh(·), shift β^(l)=0.1·tanh(·), intermediate features h^(l)=σ(γ^(l)⊙W^(l)h^(l-1)+β^(l)), SiLU activation.
- **Loss**: L = L_rec + β_KL·L_KL, with MSE reconstruction and KL regularization to standard normal.

### Stage III: SPRINT Policy Training
- **POMDP + PPO**: Asymmetric Actor-Critic (AAC), where the actor uses only proprioception (angular velocity, projected gravity, commands, joint kinematics, previous residuals) and the critic uses privileged states (linear velocity + spectral reference trajectories).
- **Hierarchical residual control**: Frozen prior generates q_ref, RL outputs residual a_res, target q_target = q_ref + α·a_res (α is a scalar gain).
- **Reward quadruple**: Task reward (velocity tracking, acceleration limiting), prior guidance (deviation penalty), foot guidance (minimum flight time t_target = D_swing/f_cmd, D_swing=0.4), regularization (forward lean target g_f = 0.1+0.05·v_x_cmd).
- **Curriculum learning**: Progressively expands the velocity command range, combined with dynamics randomization (mass, friction, motor delay).

## Key Innovations

1. **Frequency as anchor for continuous motion manifold**: Discrete reference sequences are mapped via FFT dominant frequency into a continuous latent space, allowing velocity commands v_x_cmd to be directly converted to target frequency f (v∈[0,6] m/s ↔ f∈[0.6,2.3] Hz), achieving true continuous speed modulation rather than discrete gait interpolation. This fundamentally simplifies the traditional "gait library + switching" paradigm.
2. **Complementary design of spectral prior + residual RL**: The prior handles biomimetic rhythm and phase structure, while RL learns only residual corrections—avoiding both exploration collapse in pure RL and tracking rigidity in pure priors. Ablation shows FID degrades from 0.80 to 12.23 when r_prior is removed, validating the necessity of this coupling.
3. **Extreme data efficiency compression**: Only 5 sequences (428 frames) suffice to train a policy covering 0–6 m/s, reducing training time to 6.5 hours (RTX 4090) compared to hours of data collection for AMP-style methods, providing a reproducible cost baseline for real-world deployment.

## Experiments and Results

| Method | Max v_avg (m/s) | Convergence Time (h) | FID | E_qpos@[0,6] |
|--------|----------------|---------------------|-----|--------------|
| Humanoid-Gym | 3.9048 | 4.5 | – | 0.0385 |
| AMP | 4.7512 | 34.5 | 37 | 0.1061 |
| AI-CPG | 4.8208 | 9 | 14 | 0.0802 |
| **SPRINT** | **5.9648** | **6.5** | **0.8035** | **0.0657** |

- Spectral prior vs AI-CPG: L_rec reduced by 60% (computed from table values 0.2336→0.0932) (0.0015→0.0006), FID reduced by 81% (computed from table values 3.4→0.66) (computed from table values 0.66÷0.66) (computed from table values 3.4→0.66), E_BA reduced by 94% (computed from table values 100.0−6.0), demonstrating significantly superior prior generation quality.
- Key ablations: Removing curriculum learning drops max speed to 4.48 m/s (-25% (computed from table values 2.29→2.87)), removing residual structure degrades FID to 2.33 (computed from table values 1.1−3.4), removing AAC increases E_vel by 15% (computed from table values 0.0657→0.0755)—all three are necessary components.
- Hardware transfer (Unitree G1): Full 1–6 m/s sim-to-real average errors FID=0.0346, E_qpos=0.0052, v_avg=0.0428; at 6 m/s, real robot speed is 5.9143 m/s (simulation 5.9648), a gap of <1%, indicating policy robustness to dynamics randomization.

## Boundaries and Limitations

The paper does not explicitly discuss limitations. Inferred boundaries from factual points: 1) Velocity domain beyond 6 m/s is unexplored, and spectral prior extrapolation behavior for f>2.3 Hz is unknown; 2) Validation only on the Unitree G1 platform, without testing other morphologies (e.g., larger/smaller robots); 3) Non-flat terrain, disturbances, or turning scenarios are not evaluated, and the foot guidance in the reward function assumes flat ground; 4) The reference library contains only straight-line gaits, excluding non-periodic motions such as jumping or sudden stops; 5) Specific values for β_KL and residual gain α are not disclosed, potentially affecting reproduction accuracy.

## Engineering Insights

When reproducing, prioritize three checks: First, verify that the FFT dominant frequency extraction in Stage I matches the paper's mapping (0.68–1.58 Hz), as this is the foundation for prior training; Second, confirm the tanh limiting parameters in the FiLM generator (γ range [0.9,1.1], β range [-0.1,0.1]), since overly large modulation ranges distort generated trajectories; Third, review the curriculum learning schedule—ablation shows this is the key bottleneck for speed ceilings, with a suggested start at [0,4] m/s and expansion of 0.5 m/s every 30 minutes. The most common pitfall is tuning the residual gain α: too small locks the policy to the prior, too large loses biomimetic structure; a grid search in [0.3, 0.7] is recommended. For hardware deployment, ensure the motor delay randomization range covers real-world calibration values; otherwise, phase errors at 6 m/s accumulate rapidly.

## 개요

SPRINT는 스펙트럼 사전(spectral priors)을 활용하여 단 5개의 인간 운동 시퀀스를 연속 속도 다양체로 매핑하는 데이터 절약형 3단계 프레임워크를 제안한다. 계층적 잔차 강화 학습과 결합하여 Unitree G1에서 0–6 m/s의 생체 모방 스프린트와 매끄러운 보행 전환을 구현한다. 핵심 기여는 주파수를 운동 표현의 연속화 브리지로 사용하여 대규모 참조 데이터와 복잡한 모션 트래킹에 대한 의존성을 크게 줄인 것이다.

## 무엇을 바꾸었는가

휴머노이드 로봇의 고속 운동은 오랫동안 두 가지 난제에 얽매여 있었다. 첫째는 이족 보행 형태에 적합한 운동학적 참조 데이터의 부족이고, 둘째는 기존 모방 학습 프레임워크가 고속 영역에서 훈련 불안정이나 모드 붕괴에 빠진다는 점이다. AMP 계열 방법은 대규모 참조 데이터에 의존하고 외삽이 어려우며, GMP/CMG 계열 방법은 추적이 견고하지만 연속 속도 제어가 불가능하고, AI-CPG 하이브리드 전략은 4 m/s 이하로 제한된다. SPRINT가 진정으로 바꾼 것은 "참조 데이터가 목표 속도 영역을 반드시 커버해야 한다"는 암묵적 가정이다. 단 5개의 저속 시퀀스(최대 3.4 m/s)만으로 스펙트럼 영역의 사전 모델링과 잔차 RL의 상호 보완을 통해 6 m/s의 스프린트 능력을 외삽할 수 있음을 증명했다. 이는 단순한 속도 수치의 돌파일 뿐만 아니라 "데이터 양이 운동 상한을 결정한다"는 업계의 직관을 흔들며, 데이터가 부족한 환경에서의 고속 운동 제어를 위한 새로운 패러다임을 제시한다.

## 방법 분해

### Stage I: 참조 라이브러리 구축
- LAFAN1에서 5개의 단일 주기 보행(걷기/조깅/달리기)을 추출하고, GMR 형태학적 리타겟팅을 통해 10자유도 관절 부분집합 𝒥(고관절/무릎/발목 피치 + 어깨/팔꿈치)으로 변환.
- 전처리: 푸리에 리샘플링 30→60 Hz, Savitzky-Golay 필터로 노이즈 제거.
- 핵심 매핑: FFT로 PSD를 계산하여 속도-주요 주파수 쌍 {0.66, 1.10, 2.29, 2.87, 3.40} m/s ↔ {0.68, 0.86, 1.25, 1.36, 1.58} Hz를 얻고, 반대측 사지의 반주기 위상 오프셋을 확인.

### Stage II: 스펙트럼 사전 훈련
- **조건부 VAE**: 인코더가 목표 주파수 f를 사후 확률 q(z|f)=𝒩(μ, diag(σ²))로 매핑하고, 재매개변수화 샘플링 z=μ+σ⊙ε 수행.
- **다중 고조파 리듬**: 순간 위상 φ=2πft, K=6 고조파 전개로 스펙트럼 벡터 x(t,f)∈ℝ^(2K) 획득.
- **FiLM 변조 생성**: 컨텍스트 c=[z, f], 각 레이어 스케일 γ^(l)=1+0.1·tanh(·), 시프트 β^(l)=0.1·tanh(·), 중간 특징 h^(l)=σ(γ^(l)⊙W^(l)h^(l-1)+β^(l)), SiLU 활성화.
- **손실**: L = L_rec + β_KL·L_KL, 재구성은 MSE, KL 정규화는 표준 정규분포로.

### Stage III: SPRINT 정책 훈련
- **POMDP + PPO**: 비대칭 Actor-Critic(AAC), actor는 고유수용감각(각속도, 투영 중력, 명령, 관절 운동학, 이전 잔차)만 사용, critic는 특권 상태(선형 속도 + 스펙트럼 참조 궤적) 사용.
- **계층적 잔차 제어**: 사전 생성 q_ref를 고정하고, RL이 잔차 a_res를 출력, 목표 q_target = q_ref + α·a_res (α는 스칼라 게인).
- **보상 4중항**: 작업 보상(속도 추적, 가속도 제한), 사전 유도(이탈 페널티), 발 유도(최소 비행 시간 t_target = D_swing/f_cmd, D_swing=0.4), 정규화(전방 경사 목표 g_f = 0.1+0.05·v_x_cmd).
- **커리큘럼 학습**: 속도 명령 범위를 점진적으로 확장하고, 동역학 무작위화(질량, 마찰, 모터 지연)와 결합.

## 핵심 혁신

1. **주파수를 연속 운동 다양체의 앵커로 사용**: 이산 참조 시퀀스를 FFT 주요 주파수를 통해 연속 잠재 공간으로 매핑하여, 속도 명령 v_x_cmd를 목표 주파수 f로 직접 변환할 수 있게 한다(v∈[0,6] m/s ↔ f∈[0.6,2.3] Hz). 이를 통해 이산 보행 라이브러리+전환 패러다임이 아닌 진정한 연속 속도 제어를 구현한다. 이는 전통적인 "보행 라이브러리+스위칭" 패러다임의 근본적 단순화다.
2. **스펙트럼 사전 + 잔차 RL의 상보적 설계**: 사전은 생체 모방 리듬과 위상 구조를 담당하고, RL은 잔차 수정만 학습하여 순수 RL의 탐색 붕괴를 피하면서 순수 사전의 추적 경직성을 극복한다. 절제 실험에서 r_prior 제거 시 FID가 0.80에서 12.23으로 악화되어 이 결합의 필요성을 검증한다.
3. **데이터 효율의 극한 압축**: 단 5개 시퀀스(428프레임)만으로 0–6 m/s를 커버하는 정책을 훈련할 수 있으며, AMP 계열 방법의 수시간 데이터 수집과 비교하여 훈련 시간을 6.5시간(RTX 4090)으로 단축, 실제 배포를 위한 재현 가능한 비용 기준을 제공한다.

## 실험 및 결과

| 방법 | Max v_avg (m/s) | 수렴 시간 (h) | FID | E_qpos@[0,6] |
|------|----------------|-------------|-----|--------------|
| Humanoid-Gym | 3.9048 | 4.5 | – | 0.0385 |
| AMP | 4.7512 | 34.5 | 37 | 0.1061 |
| AI-CPG | 4.8208 | 9 | 14 | 0.0802 |
| **SPRINT** | **5.9648** | **6.5** | **0.8035** | **0.0657** |

- 스펙트럼 사전 vs AI-CPG: L_rec 60% 감소(표 내 수치 0.2336→0.0932 계산)(0.0015→0.0006), FID 81% 감소(표 내 수치 3.4→0.66 계산)(표 내 수치 0.66÷0.66 계산)(표 내 수치 3.4→0.66 계산), E_BA 94% 감소(표 내 수치 100.0−6.0 계산), 사전 생성 품질이 현저히 우수함을 입증.
- 절제 핵심: 커리큘럼 학습 제거 시 최대 속도 4.48 m/s로 하락(-25%(표 내 수치 2.29→2.87 계산)), 잔차 구조 제거 시 FID 2.33으로 악화(표 내 수치 1.1−3.4 계산), AAC 제거 시 E_vel 15% 상승(표 내 수치 0.0657→0.0755 계산) — 세 가지 모두 필수 구성 요소.
- 하드웨어 이전(Unitree G1): 1–6 m/s 전 구간 sim-to-real 평균 오차 FID=0.0346, E_qpos=0.0052, v_avg=0.0428, 6 m/s 실기 속도 5.9143 m/s(시뮬레이션 5.9648), 차이 <1%, 동역학 무작위화에 대한 정책 견고성 입증.

## 경계 및 한계

논문은 한계를 명시적으로 논의하지 않았다. 사실적 요점에서 추론 가능한 경계: 1) 6 m/s 이상의 속도 영역을 탐색하지 않았으며, f>2.3 Hz에서 스펙트럼 사전의 외삽 동작은 미지수; 2) Unitree G1 단일 플랫폼에서만 검증, 다른 형태(예: 더 크거나 작은 로봇) 미테스트; 3) 비평지 지형, 교란 또는 방향 전환 시나리오 미평가, 보상 함수의 발 유도는 평평한 지면을 가정; 4) 참조 라이브러리는 직선 보행만 포함, 점프, 급정지 등 비주기 운동 미포함; 5) β_KL 및 잔차 게인 α의 구체적 값 미공개, 재현 정밀도에 영향을 줄 수 있음.

## 엔지니어링 시사점

재현 시 세 가지를 우선 확인해야 한다. 첫째, Stage I의 FFT 주요 주파수 추출이 논문 매핑(0.68–1.58 Hz)과 일치하는지 — 이는 사전 훈련의 기초다. 둘째, FiLM 생성기의 tanh 제한 파라미터(γ 범위 [0.9,1.1], β 범위 [-0.1,0.1]) — 과도한 변조 범위는 생성 궤적 왜곡을 초래한다. 셋째, 커리큘럼 학습의 진행 일정 — 절제 실험에서 속도 상한의 핵심 병목임을 보여주며, [0,4] m/s에서 시작하여 30분마다 0.5 m/s씩 확장하는 것을 권장한다. 가장 함정에 빠지기 쉬운 것은 잔차 게인 α의 튜닝이다. 너무 작으면 정책이 사전에 고정되고, 너무 크면 생체 모방 구조를 잃는다. [0.3, 0.7] 구간에서 그리드 탐색을 권장한다. 하드웨어 배포 시 모터 지연 무작위화 범위가 실기 보정값을 커버해야 하며, 그렇지 않으면 6 m/s에서 위상 오차가 빠르게 누적된다.
