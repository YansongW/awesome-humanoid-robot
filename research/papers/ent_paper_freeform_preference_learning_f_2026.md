---
$id: ent_paper_freeform_preference_learning_f_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Freeform Preference Learning for Robotic Manipulation
  zh: Freeform Preference Learning for Robotic Manipulation
  ko: Freeform Preference Learning for Robotic Manipulation
summary:
  en: 'arXiv:2606.32027v1 Announce Type: new Abstract: Reward design remains a central bottleneck for autonomous robot policy
    improvement, especially in long-horizon manipulation tasks where sparse success labels provide too little signal and binary
    preferences collapse many competing notions of quality into one ambiguous signal. We introduce Freeform Preference Learning
    (FPL), a method for learning robot policies from freeform human preferences. Rather than asking annotators which of two
    trajectories is better overall, FPL lets them define natural-language preference axes, such as speed, safety, quality
    of placement, or carefulness, and provide pairwise preferences along each axis. These annotations are used to learn a
    language-conditioned reward model that maps a trajectory and preference label to an axis-specific reward. We use this
    model to train a reward-conditioned policy that optimizes across the multiple human-specified dimensions. Across four
    real-world and two simulated long-horizon manipulation tasks, FPL improves over sparse-reward and binary-preference methods
    by 38 percentage points. Beyond improved performance, FPL learns dense progress signals without explicit subtask segmentation,
    shows compositionality of behavior not present in the data, and allows users to steer the policy towards different behaviors
    at test time without retraining. Blog post with videos available at https://freeform-pl.github.io/fpl.website/'
  zh: Freeform Preference Learning (FPL) 是一种从自由形式人类偏好中学习机器人策略的方法。它允许标注者定义自然语言偏好轴（如速度、安全性）并提供沿各轴的成对偏好，从而学习语言条件奖励模型。在六项长时域操作任务中，FPL
    相比稀疏奖励和二元偏好方法提升了 38 个百分点。
  ko: 'arXiv:2606.32027v1 Announce Type: new Abstract: Reward design remains a central bottleneck for autonomous robot policy
    improvement, especially in long-horizon manipulation tasks where sparse success labels provide too little signal and binary
    preferences collapse many competing notions of quality into one ambiguous signal. We introduce Freeform Preference Learning
    (FPL), a method for learning robot policies from freeform human preferences. Rather than asking annotators which of two
    trajectories is better overall, FPL lets them define natural-language preference axes, such as speed, safety, quality
    of placement, or carefulness, and provide pairwise preferences along each axis. These annotations are used to learn a
    language-conditioned reward model that maps a trajectory and preference label to an axis-specific reward. We use this
    model to train a reward-conditioned policy that optimizes across the multiple human-specified dimensions. Across four
    real-world and two simulated long-horizon manipulation tasks, FPL improves over sparse-reward and binary-preference methods
    by 38 percentage points. Beyond improved performance, FPL learns dense progress signals without explicit subtask segmentation,
    shows compositionality of behavior not present in the data, and allows users to steer the policy towards different behaviors
    at test time without retraining. Blog post with videos available at https://freeform-pl.github.io/fpl.website/'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- freeform_preference_learning_f
- humanoid
- robotics
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2606.32027v2. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (723 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: Freeform Preference Learning for Robotic Manipulation
  url: https://arxiv.org/abs/2606.32027
  date: '2026'
  accessed_at: '2026-07-01'
---
## 概述

FPL（Freeform Preference Learning）由论文作者提出，是一种面向机器人操作的多维偏好学习方法。它允许标注者沿任意任务相关轴（如速度、小心程度）提供自然语言描述的二元偏好，并据此学习语言条件奖励模型与奖励条件策略，从而在真实世界长视野任务中显著超越现有基线，并支持测试时的行为可操控性。

## 它改变了什么

机器人策略学习长期受困于奖励设计的瓶颈。稀疏成功标签信号过少，而标准二元偏好又迫使标注者将速度、小心程度、任务完成度等相互竞争的质量概念压缩进一个模糊的“总体”判断中。这种做法在长视野任务中尤其有害——监督信号既难以提供，又充满歧义，最终将负担转嫁给强化学习算法，使其难以扩展到更真实的行为约束。

FPL真正改变的是“偏好”的定义方式。它不再要求标注者给出一个综合性的二元判断，而是允许其自由指定判断轴（如“是否碰了盘子”或“速度是否够快”），并沿该轴提供偏好。这一转变将监督信号从一维标量扩展为多维、可分解的语言条件形式，使奖励模型能够捕捉理想行为的多个方面，并让策略在训练后仍可通过指定不同轴组合进行测试时操控，而无需重新训练。

## 方法拆解

### 奖励模型学习
- 将 Bradley-Terry 模型扩展为轴条件形式。奖励模型 r_φ 以轴标签 l_k 为条件，对轨迹对 (τ_i, τ_j) 输出标量，最小化负对数似然：
  ℒ_FPL-Reward(φ) = −𝔼[Σ_{k=1}^{K_ij} log σ((2y_k−1)(r_φ(τ_i|l_k) − r_φ(τ_j|l_k)))]
- 轨迹级奖励定义为前缀段得分之和：r_φ(τ, l_k) = Σ_{i=1}^{T} g_φ(o_{1:i}, l_k)。其中 g_φ 是多模态 transformer，以观测历史为条件，而非单帧，以捕捉时间依赖。
- 骨干网络使用预训练 Qwen 3.5 VL 4B，冻结视觉编码器，全微调其余部分。输入为问题“What is the score for l_k?”及双相机图像序列，取每时间步第二张图像的视觉分隔符 token 经线性投影解码为分数。

### 策略提取
- 选择 K_π 个代表性偏好轴 L = {l_k}，策略以所有轴描述及对应轨迹奖励为条件：
  ℒ_FPL-Policy(θ) = −𝔼[Σ_{t=1}^{T_i} log π_θ(a_i^t | s_i^t, l_1, r_φ(τ_i|l_1) … l_{K_π}, r_φ(τ_i|l_{K_π}))]
- 采用奖励条件策略（off-policy、批式在线），而非 PPO，因后者对真实世界机器人学习样本效率过低。
- 策略为视觉-语言-动作模型（VLA），使用 π_0.5 开源权重，流匹配动作头，动作块大小 16，滚动 8 个动作，全微调 30000 步，批大小 32。

### 奖励标准化与测试时操控
- 对数据集 𝒟 上每个轴的奖励计算均值 μ 和标准差 σ，标准化 r_cond = (r − μ) / σ，使奖励有界且测试时无需搜索即可指定。
- 条件以文本格式传入策略：l_k: round((r_φ(l_k, τ) − μ) / σ)，分数保留 1 位小数。
- 仿真中为避免策略过拟合条件向量，标准化后添加 0.2 噪声；真实世界不需要。

### 迭代训练
收集自由形式偏好 → 训练奖励模型 → 提取偏好轴 → 训练策略 → 滚动出新数据 → 合并数据集 → 重复。偏好轴可随迭代自然演变，形成课程学习。

## 关键创新

1. **自由形式偏好轴**：标注者可用自然语言指定任意判断轴，而非固定集合。这利用视觉-语言模型的预训练表示，使语义相似的描述（如“speed”、“fast”、“efficient”）能自然泛化，无需预定义轴集合。
2. **分解奖励而非加权标量**：不将多轴折叠为加权标量奖励，保留分解形式。这降低了奖励黑客和塑形问题的风险，使策略能展现数据中不存在的行为组合性（如将“快速”与“右钉”组合），并支持测试时无需重训的可操控性。
3. **轨迹级、历史条件的奖励模型**：与标准偏好模型对单帧或短段打分不同，FPL 以观测历史为条件，能捕捉时间依赖（如“先放盘子再放餐具”的顺序约束），这对长视野任务至关重要。

## 实验与结果

真实世界 4 个任务（Put cube into target bowl、Fold shorts、Plate toast、Set up the table）中，FPL 在所有任务上优于所有基线，平均成功率比次优方法提升 **38 个百分点**（由表内数值 0.75 vs 0.37 计算）。模拟任务结果如下：

| 方法 | Object rearrangement (success) | Bimodal square (throughput) | Bimodal square inverted (throughput) |
|---|---|---|---|
| BC | 0.04 ± 0.02 | 0.71 ± 0.07 | 0.25 ± 0.14 |
| Filtered BC | 0.09 ± 0.05 | 0.71 ± 0.02 | 0.00 ± 0.00 |
| Weighted Regression | 0.18 ± 0.08 | 0.83 ± 0.08 | 0.09 ± 0.06 |
| Advantage Conditioning | 0.17 ± 0.06 | 0.75 ± 0.12 | 0.08 ± 0.04 |
| Single Preferences (match pairs) | 0.73 ± 0.04 | 0.67 ± 0.08 | 0.78 ± 0.01 |
| Single Preferences (match comparisons) | 0.79 ± 0.06 | 0.73 ± 0.05 | 0.74 ± 0.05 |
| FPL (Ours) | **0.84 ± 0.09** | **1.19 ± 0.01** | **1.24 ± 0.01** |

关键含义：
- 在 Bimodal square 反转任务中，FPL 是唯一在同一策略下达到高性能的方法（throughput 1.24），证明其测试时可操控性。
- FPL 在右钉上实现比训练数据更快的放置速度（T1S 253.13 vs 演示 347.71），而单一偏好基线未超越演示行为。
- 偏好收集成本：自由偏好比单一偏好快约 **1.85 倍**（Figure 9(b)），因视频观看成本在多个标签间分摊。

## 边界与局限

- 偏好学习需要收集人类偏好，比完全无监督方法更昂贵。
- 奖励条件策略学习需要在测试时选择适当的奖励值，自动化这一选择是未来工作的重要方向。
- 当前策略以固定偏好轴集合为条件，扩展方法以处理可变轴是 VLA 能力增强后有前景的方向。
- 仿真中需添加 0.2 噪声避免过拟合，作者指出这在真实世界不必要，仅因仿真中从头训练较小网络更易过拟合。
- 未在仿真中使用预训练模型（为优先迭代速度），未提及对更大规模任务或更多机器人平台的泛化验证。

## 工程启示

复现或采用 FPL 时，先核对以下关键点：
1. **奖励模型输入格式**：确认 Qwen 3.5 VL 4B 的输入是否包含“What is the score for l_k?”问题及双相机图像序列，且因果掩码确保 token 不可关注未来图像。冻结视觉编码器、全微调其余部分是关键。
2. **奖励标准化**：测试时操控依赖数据集 𝒟 上的均值和标准差。若下游任务分布偏移，需重新计算 μ 和 σ，否则条件值可能超出分布。
3. **仿真与真实世界的差异**：仿真中需添加 0.2 噪声防止策略过拟合条件向量，但真实世界不需要。若在仿真中复现，务必包含此噪声；若迁移到真实世界，应移除。
4. **偏好轴选择**：初始偏好轴应覆盖任务早期阶段和粗略行为属性，后期聚焦细节。迭代训练中 K_π 可变化，但需确保每个轴有足够标注数据（如 Plate Toast 任务中 295 个轨迹对产生 1477 个轴级比较，涵盖 41 个标签）。
5. **计算资源**：真实世界策略全微调和奖励模型微调需 1 个 H100；仿真实验可用较小 GPU（如 NVIDIA RTX 4500）。若资源受限，优先保证奖励模型质量，策略微调可适当减少步数。

## 参考
- http://arxiv.org/abs/2606.32027v2

## 개요
FPL은 장시간 조작 작업에서 보상 설계의 병목 현상을 해결합니다. 전통적인 희소 성공 라벨은 신호가 부족하고, 이진 선호는 여러 품질 개념을 혼합합니다. 이 방법은 주석자가 자연어 선호 축(예: 속도, 안전성, 배치 품질, 신중함)을 정의하고 각 축을 따라 쌍별 선호를 제공하도록 하여, 궤적과 선호 라벨을 축별 보상으로 매핑하는 언어 조건부 보상 모델을 학습합니다. 이 모델을 기반으로 보상 조건부 정책을 훈련하여 여러 인간 지정 차원에서 최적화할 수 있습니다. 네 가지 실제 세계 및 두 가지 시뮬레이션 장시간 조작 작업에서 FPL은 희소 보상 및 이진 선호 방법보다 38% 포인트 향상되었으며, 밀집 진행 신호 학습, 행동 조합성, 테스트 시 재훈련 없이 정책 조정 능력을 보여줍니다.

## 핵심 내용
### 방법
- **핵심 아이디어**: FPL은 주석자가 자연어 선호 축(예: speed, safety, quality of placement, carefulness)을 정의하고, 전체 이진 비교 대신 각 축을 따라 쌍별 선호를 제공하도록 합니다.
- **보상 모델**: 궤적과 선호 라벨을 축별 보상으로 매핑하는 언어 조건부 보상 모델을 학습합니다.
- **정책 훈련**: 보상 모델을 기반으로 보상 조건부 정책을 훈련하여 여러 인간 지정 차원에서 최적화합니다.

### 실험 설정
- **작업**: 네 가지 실제 세계 및 두 가지 시뮬레이션 장시간 조작 작업.
- **비교 방법**: 희소 보상 방법 및 이진 선호 방법.

### 주요 결과
- **성능 향상**: FPL은 희소 보상 및 이진 선호 방법보다 38% 포인트 향상되었습니다.
- **추가 장점**:
  - 명시적 하위 작업 분할 없이 밀집 진행 신호를 학습합니다.
  - 데이터에 존재하지 않는 행동 조합성을 보여줍니다.
  - 사용자가 테스트 시 선호 축을 조정하여 재훈련 없이 정책을 안내할 수 있습니다.

### 결론
FPL은 자유 형식 인간 선호를 통해 장시간 조작 작업에서 보상 설계 병목 현상을 효과적으로 해결하며, 성능을 크게 향상시키고 유연한 정책 제어 능력을 제공합니다.
