---
$id: ent_paper_humanoid_vla_towards_universal_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Humanoid-VLA: Towards Universal Humanoid Control with Visual Integration'
  zh: Humanoid-VLA｜通过视觉集成实现通用人形控制
  ko: 'Humanoid-VLA: Towards Universal Humanoid Control with Visual Integration'
summary:
  en: The implementation path of Humanoid-VLA is to first encode camera images/multi-view observations, human videos/motion
    capture trajectories into multimodal representations, and then use diffusion strategy/flow matching, VLA multimodal action
    model, and whole-body controller/WBC/MPC to predict whole-body trajectories/action sequences. The key point is to preserve
    the semantic understanding of VLM while adding robot states and action heads to avoid staying only at language planning.
  zh: Humanoid-VLA 的实现路径是先把相机图像/多视角观测、人类视频/动捕轨迹编码成多模态表征，再用扩散策略/流匹配、VLA 多模态动作模型、全身控制器/WBC/MPC预测全身轨迹/动作序列。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: Humanoid-VLA 的实现路径是先把相机图像/多视角观测、人类视频/动捕轨迹编码成多模态表征，再用扩散策略/流匹配、VLA 多模态动作模型、全身控制器/WBC/MPC预测全身轨迹/动作序列。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 07_ai_models_algorithms
- 11_applications_markets
layers:
- intelligence
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- contact_planning
- humanoid_vla
- mobile_manipulation
- task_planning
- visual_closed_loop
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: 'Humanoid-VLA: Towards Universal Humanoid Control with Visual Integration'
  url: ''
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人全身控制面临两大挑战：如何从视觉输入中提取高层语义理解，以及如何将这种理解转化为精确的全身运动指令。现有方法往往将视觉语言模型（VLM）用于任务规划，而将运动控制独立处理，导致语义理解与运动执行之间存在鸿沟。Humanoid-VLA 提出了一种统一的视觉-语言-动作框架，通过将多视角相机观测、人类示范视频和动捕轨迹编码为多模态表征，并利用扩散策略与全身控制器生成连续动作序列，实现了从语义理解到全身控制的端到端集成。该工作的核心贡献在于保留了 VLM 的语义推理能力，同时引入机器人状态编码与动作预测头，使模型能够直接输出可执行的运动指令，从而突破了传统方法仅停留在语言规划层面的局限。

## 核心内容

人形机器人的通用控制长期受困于两个相互独立的研究路线：一端是基于视觉语言模型的高层任务规划，另一端是基于模型预测控制（MPC）或全身控制器（WBC）的低层运动生成。前者擅长理解自然语言指令和场景语义，却无法输出精细的关节轨迹；后者能实现精确的全身动力学控制，却缺乏对任务意图的语义理解。这种割裂导致人形机器人在复杂环境中难以同时兼顾任务理解与运动执行，尤其是在需要视觉闭环调整的移动操作场景中，系统往往需要手动设计中间接口来桥接规划与控制模块。

Humanoid-VLA 提出了一种三阶段处理架构来弥合这一鸿沟。首先，模型将来自相机图像、多视角观测、人类示范视频以及运动捕捉轨迹的异构数据统一编码为多模态表征，这一过程利用了预训练视觉语言模型的语义编码能力。其次，基于扩散策略或流匹配方法，模型在表征空间中学习从视觉输入到全身轨迹的条件概率分布，从而生成平滑且符合动力学约束的动作序列。最后，生成的轨迹被送入全身控制器（WBC/MPC）进行底层执行，确保运动满足关节限位、力矩约束和平衡条件。

该框架的关键技术创新体现在两个方面。其一，模型在 VLM 基础上增加了机器人状态编码器与动作预测头，使得语义理解与运动生成共享同一表征空间，避免了传统方法中“先规划后控制”的信息损失。其二，通过引入接触规划（contact planning）机制，模型能够根据视觉输入动态预测足部与地面的接触模式，从而在移动操作任务中实现稳定的步态切换。这种设计使 Humanoid-VLA 能够同时处理任务规划、运动生成与接触调度，而无需显式定义中间状态机。

在验证层面，Humanoid-VLA 在多个移动操作基准任务上进行了评估，包括物体抓取、环境导航和工具使用等场景。实验结果表明，该框架在任务成功率与运动平滑性上均优于分离式规划-控制基线方法。特别地，在需要视觉闭环调整的任务中，Humanoid-VLA 能够利用实时视觉反馈修正全身轨迹，展现出对动态环境变化的鲁棒性。此外，通过消融实验证实，保留 VLM 语义理解能力对于处理长时序任务和语言指令泛化至关重要，而增加动作头则显著提升了运动生成的精度。

从应用价值来看，Humanoid-VLA 为人形机器人在工业装配、家庭服务和灾难救援等场景中的部署提供了可行的技术路径。其统一框架降低了系统集成复杂度，使得开发者无需为每个任务单独设计规划与控制接口。同时，由于模型能够从人类示范视频中直接学习运动模式，这为机器人技能的快速迁移和泛化开辟了新的可能性。未来工作可进一步探索如何将该框架扩展到更复杂的多机器人协作场景，以及如何在保证实时性的前提下提升模型对细粒度接触力的预测能力。

## 参考

- Humanoid-VLA: Towards Universal Humanoid Control with Visual Integration ()

## Overview

Humanoid whole-body control faces two major challenges: how to extract high-level semantic understanding from visual input, and how to translate this understanding into precise whole-body motion commands. Existing methods often use Vision-Language Models (VLMs) for task planning while handling motion control independently, leading to a gap between semantic understanding and motion execution. Humanoid-VLA proposes a unified Vision-Language-Action framework that encodes multi-view camera observations, human demonstration videos, and motion capture trajectories into multimodal representations, and leverages diffusion policies with whole-body controllers to generate continuous action sequences, achieving end-to-end integration from semantic understanding to whole-body control. The core contribution of this work lies in preserving the semantic reasoning capability of VLM while introducing robot state encoding and action prediction heads, enabling the model to directly output executable motion commands, thereby breaking through the limitation of traditional methods that remain at the language planning level.

## Content

The general control of humanoid robots has long been constrained by two independent research lines: one is high-level task planning based on Vision-Language Models, and the other is low-level motion generation based on Model Predictive Control (MPC) or Whole-Body Controllers (WBC). The former excels at understanding natural language instructions and scene semantics but cannot output fine-grained joint trajectories; the latter achieves precise whole-body dynamics control but lacks semantic understanding of task intent. This disconnection makes it difficult for humanoid robots to simultaneously handle task understanding and motion execution in complex environments, especially in mobile manipulation scenarios requiring visual closed-loop adjustments, where systems often need manually designed intermediate interfaces to bridge planning and control modules.

Humanoid-VLA proposes a three-stage processing architecture to bridge this gap. First, the model uniformly encodes heterogeneous data from camera images, multi-view observations, human demonstration videos, and motion capture trajectories into multimodal representations, leveraging the semantic encoding capability of pretrained Vision-Language Models. Second, based on diffusion policies or flow matching methods, the model learns the conditional probability distribution from visual input to whole-body trajectories in the representation space, thereby generating smooth action sequences that satisfy dynamics constraints. Finally, the generated trajectories are fed into a whole-body controller (WBC/MPC) for low-level execution, ensuring that motions meet joint limits, torque constraints, and balance conditions.

The key technological innovations of this framework are reflected in two aspects. First, the model adds a robot state encoder and action prediction head on top of the VLM, allowing semantic understanding and motion generation to share the same representation space, avoiding the information loss in traditional "plan-then-control" approaches. Second, by introducing a contact planning mechanism, the model can dynamically predict foot-ground contact patterns based on visual input, enabling stable gait transitions in mobile manipulation tasks. This design allows Humanoid-VLA to simultaneously handle task planning, motion generation, and contact scheduling without explicitly defining intermediate state machines.

In terms of validation, Humanoid-VLA was evaluated on multiple mobile manipulation benchmark tasks, including object grasping, environment navigation, and tool use scenarios. Experimental results show that the framework outperforms separated planning-control baseline methods in both task success rate and motion smoothness. Particularly, in tasks requiring visual closed-loop adjustments, Humanoid-VLA can leverage real-time visual feedback to correct whole-body trajectories, demonstrating robustness to dynamic environmental changes. Furthermore, ablation experiments confirm that retaining VLM semantic understanding capability is crucial for handling long-horizon tasks and language instruction generalization, while adding action heads significantly improves motion generation precision.

From an application perspective, Humanoid-VLA provides a feasible technical pathway for deploying humanoid robots in scenarios such as industrial assembly, home services, and disaster rescue. Its unified framework reduces system integration complexity, allowing developers to avoid designing separate planning and control interfaces for each task. Additionally, since the model can directly learn motion patterns from human demonstration videos, this opens new possibilities for rapid skill transfer and generalization in robotics. Future work could further explore extending this framework to more complex multi-robot collaboration scenarios and improving the model's ability to predict fine-grained contact forces while maintaining real-time performance.

## 개요

휴머노이드 로봇의 전신 제어는 두 가지 주요 도전 과제에 직면해 있습니다: 시각 입력에서 고수준 의미 이해를 추출하는 방법과, 이러한 이해를 정밀한 전신 운동 명령으로 변환하는 방법입니다. 기존 방법은 종종 시각-언어 모델(VLM)을 작업 계획에 사용하고 운동 제어를 별도로 처리하여 의미 이해와 운동 실행 사이에 간극이 발생합니다. Humanoid-VLA는 다중 시점 카메라 관측, 인간 시연 비디오 및 모션 캡처 궤적을 다중 모달 표현으로 인코딩하고, 확산 정책과 전신 제어기를 활용하여 연속적인 동작 시퀀스를 생성함으로써 의미 이해부터 전신 제어까지의 종단간 통합을 실현하는 통합된 시각-언어-동작 프레임워크를 제안합니다. 이 연구의 핵심 기여는 VLM의 의미 추론 능력을 유지하면서 로봇 상태 인코딩과 동작 예측 헤드를 도입하여 모델이 직접 실행 가능한 운동 명령을 출력할 수 있게 함으로써, 기존 방법이 언어 계획 수준에만 머물렀던 한계를 돌파한 점에 있습니다.

## 핵심 내용

휴머노이드 로봇의 범용 제어는 오랫동안 두 가지 상호 독립적인 연구 경로에 의해 제약을 받아왔습니다: 한쪽은 시각-언어 모델 기반의 고수준 작업 계획이고, 다른 한쪽은 모델 예측 제어(MPC) 또는 전신 제어기(WBC) 기반의 저수준 운동 생성입니다. 전자는 자연어 명령과 장면 의미를 이해하는 데 뛰어나지만 정밀한 관절 궤적을 출력할 수 없고, 후자는 정확한 전신 동역학 제어를 구현할 수 있지만 작업 의도에 대한 의미 이해가 부족합니다. 이러한 분리는 휴머노이드 로봇이 복잡한 환경에서 작업 이해와 운동 실행을 동시에 처리하기 어렵게 만들며, 특히 시각적 폐루프 조정이 필요한 이동 조작 시나리오에서는 시스템이 계획과 제어 모듈을 연결하기 위해 수동으로 중간 인터페이스를 설계해야 하는 경우가 많습니다.

Humanoid-VLA는 이러한 간극을 해소하기 위해 3단계 처리 아키텍처를 제안합니다. 첫째, 모델은 카메라 이미지, 다중 시점 관측, 인간 시연 비디오 및 모션 캡처 궤적의 이질적 데이터를 다중 모달 표현으로 통합 인코딩하며, 이 과정에서 사전 훈련된 시각-언어 모델의 의미 인코딩 능력을 활용합니다. 둘째, 확산 정책 또는 흐름 매칭 방법을 기반으로 모델은 표현 공간에서 시각 입력에서 전신 궤적으로의 조건부 확률 분포를 학습하여 부드럽고 동역학 제약 조건을 충족하는 동작 시퀀스를 생성합니다. 마지막으로, 생성된 궤적은 전신 제어기(WBC/MPC)로 전달되어 하위 수준 실행을 수행하며, 관절 한계, 토크 제약 및 균형 조건을 충족하도록 보장합니다.

이 프레임워크의 핵심 기술 혁신은 두 가지 측면에서 나타납니다. 첫째, 모델은 VLM에 로봇 상태 인코더와 동작 예측 헤드를 추가하여 의미 이해와 운동 생성이 동일한 표현 공간을 공유하도록 함으로써, 기존 방법의 '먼저 계획 후 제어' 방식에서 발생하는 정보 손실을 방지합니다. 둘째, 접촉 계획 메커니즘을 도입하여 모델이 시각 입력에 따라 발과 지면의 접촉 패턴을 동적으로 예측할 수 있게 함으로써, 이동 조작 작업에서 안정적인 보행 전환을 구현합니다. 이러한 설계는 Humanoid-VLA가 명시적인 중간 상태 기계를 정의하지 않고도 작업 계획, 운동 생성 및 접촉 스케줄링을 동시에 처리할 수 있게 합니다.

검증 측면에서 Humanoid-VLA는 물체 잡기, 환경 탐색 및 도구 사용 등 여러 이동 조작 벤치마크 작업에서 평가되었습니다. 실험 결과, 이 프레임워크는 작업 성공률과 운동 부드러움 모두에서 분리된 계획-제어 기준 방법보다 우수함을 보여주었습니다. 특히, 시각적 폐루프 조정이 필요한 작업에서 Humanoid-VLA는 실시간 시각 피드백을 활용하여 전신 궤적을 수정함으로써 동적 환경 변화에 대한 강건성을 입증했습니다. 또한, 소거 실험을 통해 VLM의 의미 이해 능력을 유지하는 것이 장시간 작업 및 언어 명령 일반화에 필수적이며, 동작 헤드를 추가하면 운동 생성의 정밀도가 크게 향상된다는 점이 확인되었습니다.

응용 가치 측면에서 Humanoid-VLA는 산업 조립, 가사 서비스 및 재난 구조 등 시나리오에서 휴머노이드 로봇 배치를 위한 실현 가능한 기술 경로를 제공합니다. 통합 프레임워크는 시스템 통합 복잡성을 낮추어 개발자가 각 작업마다 별도로 계획 및 제어 인터페이스를 설계할 필요가 없게 합니다. 동시에, 모델이 인간 시연 비디오에서 직접 운동 패턴을 학습할 수 있기 때문에, 이는 로봇 기술의 빠른 전이 및 일반화를 위한 새로운 가능성을 열어줍니다. 향후 연구는 이 프레임워크를 더 복잡한 다중 로봇 협업 시나리오로 확장하는 방법과, 실시간성을 보장하면서 세밀한 접촉력 예측 능력을 향상시키는 방법을 더 탐구할 수 있습니다.
