---
$id: ent_paper_reinforcement_prototypical_representatio_2021
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Reinforcement Learning with Prototypical Representations
  zh: Reinforcement Learning with Prototypical Representations
  ko: Reinforcement Learning with Prototypical Representations
summary:
  en: Learning effective representations in image-based environments is crucial for sample efficient Reinforcement Learning
    (RL). Unfortunately, in RL, representation learning is confounded with the exploratory experience of the agent -- learning
    a useful representation requires diverse data, while effective exploration is only possible with coherent representations.
  zh: Proto-RL 是一个自监督框架，由研究者提出，旨在通过原型表示将表征学习与探索相结合。其核心贡献在于预训练任务无关的原型表示，既能总结智能体的探索经验，又能作为观测的基础，从而在困难连续控制任务上实现最先进的下游策略学习。
  ko: Learning effective representations in image-based environments is crucial for sample efficient Reinforcement Learning
    (RL). Unfortunately, in RL, representation learning is confounded with the exploratory experience of the agent -- learning
    a useful representation requires diverse data, while effective exploration is only possible with coherent representations.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- reinforcement
- prototypical
- representatio
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 145 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2102.11271 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2102.11271v2); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: arXiv:2102.11271 Reinforcement Learning with Prototypical Representations
  url: https://arxiv.org/abs/2102.11271
  accessed_at: '2026-07-31'
  date: '2021-02-22'
- id: src_002
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---


## 概述

Proto-RL 是一种面向图像强化学习的无监督表示学习方法，由 Denis Yarats 等人提出，旨在通过自监督原型聚类与最大熵探索的联合训练，实现任务无关的视觉表示预训练，从而提升下游任务的样本效率与多任务泛化能力。其核心贡献在于将表示学习与探索解耦，使预训练表示可冻结复用，并在多个 DeepMind Control 任务上超越或匹敌完全监督的 DrQ 基线。

## 它改变了什么

在图像 RL 中，表示学习与探索长期存在“鸡生蛋”困境：有效表示需要多样化的探索数据，而高效探索又依赖一致的表示。现有方法要么依赖任务奖励驱动表示学习（如 DrQ），导致表示与特定任务绑定，无法跨任务泛化；要么在无奖励时采用自监督目标（如 CURL、SAC-AE），但缺乏探索信号，在稀疏奖励环境下失效。Proto-RL 真正改变的是将“表示学习”与“探索”从耦合的联合优化中解放出来：它通过原型聚类提供稳定的自监督目标，同时用熵最大化驱动探索，两者在共享潜在空间中协同但梯度隔离，从而在无任务奖励阶段即可学到可迁移的通用表示。这一设计使 RL 首次具备了类似 CV/NLP 中“预训练-微调”的范式，下游任务只需冻结表示并叠加少量内在奖励即可快速学习，而非从头训练编码器。

## 方法拆解

### 两阶段框架
- **阶段一（任务无关预训练）**：智能体仅优化内在奖励（熵最大化），同时最小化自监督表示损失 LSSL，不接触任何任务奖励。
- **阶段二（下游任务学习）**：冻结编码器 fθ、投影器 gθ 与原型 {ci}，将外在奖励 rt 与内在奖励 r̂t 线性叠加（rt + αr̂t，α=0.2），用标准 SAC 训练策略。

### 表示学习流程
1. 输入增强帧 xt（随机平移）→ 在线编码器 fθ（SAC-AE 架构，4 层 3×3 卷积，32 通道，输出 39200 维）→ 投影器 gθ（线性层，128 维）→ 预测器 vθ（MLP 128→512→128）→ 归一化得 û。
2. 目标网络（fξ, gξ）编码下一帧 xt+1 得 zt+1，经 Sinkhorn-Knopp 聚类（3 次迭代）得目标分布 qt+1。
3. 损失：LSSL = −qTt+1 log pt，其中 p(i)t = exp(ûTt ci/τ) / Σk exp(ûTt ck/τ)，τ=0.1，M=512 个原型。
4. 梯度仅更新在线网络与原型，目标网络通过 EMA（动量 0.05）更新；预测器 vθ 引入不对称性防坍缩。

### 最大熵探索
- 内在目标：最大化潜在空间状态访问分布的熵，用 k-NN 估计器（k=3）：r̂t = ||zt+1 − NNk,Q(zt+1)||。
- 为降低最近邻计算成本，维护固定大小队列 Q（M×T=512×4=2048），每个原型通过 softmax 权重采样 T 个候选。
- RL 智能体（SAC）在转移 (yt, at, r̂t, yt+1) 上训练，但 LRL 梯度被阻断，确保表示与任务无关。

### 关键超参数
| 参数 | 值 |
|------|-----|
| 原型数 M | 512 |
| 潜在维度 D | 128 |
| softmax 温度 τ | 0.1 |
| Sinkhorn 迭代 | 3 |
| 队列大小 | 512×4=2048 |
| 内在奖励系数 α | 0.2 |
| 预训练步数 | 500k |
| 下游步数 | 500k |

## 关键创新

1. **原型聚类与探索的联合但解耦优化**：不同于现有方法将表示学习与探索耦合（如 APT 直接优化熵），Proto-RL 用 Sinkhorn-Knopp 保证原型均匀分配，使表示学习目标稳定，同时用熵奖励驱动探索，两者共享潜在空间但梯度隔离（LRL 不更新编码器），这是其能实现任务无关表示的关键。
2. **冻结表示的多任务复用**：预训练后编码器与原型完全冻结，直接用于多个下游任务（如 Walker 的 4 个方向任务），无需针对每个任务重新学习表示，这在现有 RL 表示学习方法中罕见，直接验证了表示的泛化性。
3. **原型采样的低方差熵估计**：相比 APT 的均匀采样，Proto-RL 通过原型 softmax 分布重平衡采样，在 Acrobot Swingup 和 Hopper Hop 上显著降低内在奖励方差（图 9），使探索信号更稳定，这对稀疏奖励任务尤为重要。

## 实验与结果

### 单任务评估（图 4，8 个 DMC 环境）
| 对比 | 结果 |
|------|------|
| Proto-RL vs Curiosity | 7/8 环境胜出 |
| Proto-RL (500k) vs DrQ (1M) | 6/8 环境胜出 |

### 多任务评估（图 5，Walker 与 Reach Duplo 各 4 任务）
- Proto-RL 显著优于所有基线（Random、APT、Curiosity、DrQ），且表示冻结后直接复用，无需重新训练。

### 预训练效率（图 6）
- 200k 预训练步时，Proto-RL 已能充分探索状态空间，下游性能优于基线；Walker Run 上基线性能随预训练步数增加反而下降，Proto-RL 保持稳定。

### 下游探索（图 7）
- α=0 在所有任务上劣于 α≥0.1；稀疏奖励任务（Cartpole Swingup Sparse、Reach Duplo）中 α≥0.1 显著优于 α=0，验证内在奖励的必要性。

### 状态覆盖与方差（图 8、9）
- Proto-RL 预训练阶段达到更高熵的状态访问分布（Cartpole Swingup Sparse、Walker Run）。
- 原型采样比均匀采样（APT）在 Acrobot Swingup 和 Hopper Hop 上内在奖励标准差更低。

### 无预训练实验（图 13）
- Proto-RL 无预训练时仍优于 DrQ，但低于有预训练版本，证明预训练方案对性能至关重要。

## 边界与局限

- **与 Plan2Explore 比较不公**：作者承认 Plan2Explore 在任务无关阶段使用了任务奖励预训练奖励模型（泄漏下游信息），且保留重放缓冲区、允许微调世界模型，而 Proto-RL 完全忽略任务无关转换并冻结表示，因此直接对比不具公平性。
- **APT 基线实现差异**：冻结了 APT 的编码器权重以促进公平，但 Liu & Abbeel (2021) 原始实现允许微调，可能低估 APT 性能。
- **未做之事**：未验证真实机器人平台；未提供 α、M、Sinkhorn 迭代次数、温度 τ 的敏感性分析；未对比编码器冻结 vs 微调的深入实验（仅说明微调兼容）；未报告计算资源与训练时间；无理论分析（如离散表示的理论基础）。
- **适用边界**：仅在 DMC 连续控制任务上验证，未涉及离散动作、多智能体或高维 3D 环境；表示学习保留一步动态相似性，但未与双模拟度量等模型最小化技术建立联系。

## 工程启示

- **复现优先级**：先核对 Sinkhorn-Knopp 的 3 次迭代与温度 τ=0.1，这是原型均匀分配与防坍缩的关键；其次确认 LRL 梯度确实被阻断（否则表示会与任务耦合，失去泛化性）。
- **易踩坑点**：α 的取值对稀疏奖励任务影响极大（α=0 时性能显著下降），建议在目标环境上先扫描 α∈{0.1, 0.2, 0.5}；队列大小 M×T 与 k-NN 的 k=3 需保持一致，否则内在奖励尺度会漂移。
- **下游适配**：冻结编码器后，若下游任务观测分布与预训练差异大（如不同相机角度），可尝试微调编码器（作者称兼容但未实验），但需注意可能破坏表示的任务无关性。
- **基线对比陷阱**：复现 APT 时务必明确是否冻结编码器，否则对比结果会失真；与 Plan2Explore 对比时需严格对齐预训练阶段的信息泄漏条件。
- **资源建议**：预训练 200k 步已能获得可用表示（图 6），若计算受限可先跑 200k 验证 pipeline，再扩展至 500k；SAC 实现建议直接使用作者开源的 pytorch_sac 版本（含 LayerNorm 与 39200→50 全连接层），避免自行实现引入偏差。

## 参考
- https://arxiv.org/abs/2102.11271
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## Overview

Proto-RL is an unsupervised representation learning method for image-based reinforcement learning, proposed by Denis Yarats et al., which aims to achieve task-agnostic visual representation pretraining through the joint training of self-supervised prototype clustering and maximum entropy exploration, thereby improving sample efficiency and multi-task generalization on downstream tasks. Its core contribution lies in decoupling representation learning from exploration, enabling pretrained representations to be frozen and reused, and surpassing or matching the fully supervised DrQ baseline on multiple DeepMind Control tasks.

## What It Changes

In image RL, representation learning and exploration have long suffered from a "chicken-and-egg" dilemma: effective representations require diverse exploration data, while efficient exploration depends on consistent representations. Existing methods either rely on task rewards to drive representation learning (e.g., DrQ), causing representations to be tied to specific tasks and unable to generalize across tasks; or they employ self-supervised objectives without rewards (e.g., CURL, SAC-AE), but lack exploration signals and fail in sparse-reward environments. What Proto-RL truly changes is liberating "representation learning" and "exploration" from coupled joint optimization: it provides a stable self-supervised objective through prototype clustering while driving exploration via entropy maximization, with both operating in a shared latent space but with isolated gradients, thereby learning transferable general representations during the task-reward-free phase. This design gives RL its first "pretrain-finetune" paradigm akin to CV/NLP, where downstream tasks only need to freeze representations and add a small intrinsic reward to learn quickly, rather than training an encoder from scratch.

## Method Breakdown

### Two-Stage Framework
- **Stage One (Task-Agnostic Pretraining)**: The agent optimizes only intrinsic rewards (entropy maximization) while minimizing the self-supervised representation loss LSSL, without any task rewards.
- **Stage Two (Downstream Task Learning)**: Freeze the encoder fθ, projector gθ, and prototypes {ci}, linearly combine the extrinsic reward rt with the intrinsic reward r̂t (rt + αr̂t, α=0.2), and train the policy with standard SAC.

### Representation Learning Pipeline
1. Input augmented frame xt (random shift) → online encoder fθ (SAC-AE architecture, 4-layer 3×3 convolutions, 32 channels, output 39200-dimensional) → projector gθ (linear layer, 128-dimensional) → predictor vθ (MLP 128→512→128) → normalized to obtain û.
2. Target network (fξ, gξ) encodes the next frame xt+1 to obtain zt+1, which is clustered via Sinkhorn-Knopp (3 iterations) to obtain the target distribution qt+1.
3. Loss: LSSL = −qTt+1 log pt, where p(i)t = exp(ûTt ci/τ) / Σk exp(ûTt ck/τ), τ=0.1, M=512 prototypes.
4. Gradients update only the online network and prototypes; the target network is updated via EMA (momentum 0.05); the predictor vθ introduces asymmetry to prevent collapse.

### Maximum Entropy Exploration
- Intrinsic objective: maximize the entropy of the state visitation distribution in latent space, using a k-NN estimator (k=3): r̂t = ||zt+1 − NNk,Q(zt+1)||.
- To reduce nearest-neighbor computation cost, maintain a fixed-size queue Q (M×T=512×4=2048), where each prototype samples T candidates via softmax weights.
- The RL agent (SAC) is trained on transitions (yt, at, r̂t, yt+1), but LRL gradients are blocked, ensuring representations remain task-agnostic.

### Key Hyperparameters
| Parameter | Value |
|------|-----|
| Number of prototypes M | 512 |
| Latent dimension D | 128 |
| Softmax temperature τ | 0.1 |
| Sinkhorn iterations | 3 |
| Queue size | 512×4=2048 |
| Intrinsic reward coefficient α | 0.2 |
| Pretraining steps | 500k |
| Downstream steps | 500k |

## Key Innovations

1. **Joint but decoupled optimization of prototype clustering and exploration**: Unlike existing methods that couple representation learning with exploration (e.g., APT directly optimizes entropy), Proto-RL uses Sinkhorn-Knopp to ensure uniform prototype assignment, stabilizing the representation learning objective, while using entropy rewards to drive exploration. Both share the latent space but have isolated gradients (LRL does not update the encoder), which is key to achieving task-agnostic representations.
2. **Multi-task reuse of frozen representations**: After pretraining, the encoder and prototypes are completely frozen and directly reused across multiple downstream tasks (e.g., 4 direction tasks for Walker), without re-learning representations for each task. This is rare among existing RL representation learning methods and directly validates representation generalization.
3. **Low-variance entropy estimation via prototype sampling**: Compared to APT's uniform sampling, Proto-RL rebalances sampling through the prototype softmax distribution, significantly reducing intrinsic reward variance on Acrobot Swingup and Hopper Hop (Figure 9), making exploration signals more stable, which is especially important for sparse-reward tasks.

## Experiments and Results

### Single-Task Evaluation (Figure 4, 8 DMC environments)
| Comparison | Result |
|------|------|
| Proto-RL vs Curiosity | Wins in 7/8 environments |
| Proto-RL (500k) vs DrQ (1M) | Wins in 6/8 environments |

### Multi-Task Evaluation (Figure 5, 4 tasks each for Walker and Reach Duplo)
- Proto-RL significantly outperforms all baselines (Random, APT, Curiosity, DrQ), and representations are directly reused after freezing without retraining.

### Pretraining Efficiency (Figure 6)
- At 200k pretraining steps, Proto-RL already sufficiently explores the state space, with downstream performance exceeding baselines; on Walker Run, baseline performance degrades with more pretraining steps, while Proto-RL remains stable.

### Downstream Exploration (Figure 7)
- α=0 is inferior to α≥0.1 on all tasks; on sparse-reward tasks (Cartpole Swingup Sparse, Reach Duplo), α≥0.1 significantly outperforms α=0, validating the necessity of intrinsic rewards.

### State Coverage and Variance (Figures 8, 9)
- Proto-RL achieves higher-entropy state visitation distributions during pretraining (Cartpole Swingup Sparse, Walker Run).
- Prototype sampling yields lower intrinsic reward standard deviation than uniform sampling (APT) on Acrobot Swingup and Hopper Hop.

### No-Pretraining Experiment (Figure 13)
- Proto-RL without pretraining still outperforms DrQ but is inferior to the pretrained version, proving the pretraining scheme is crucial for performance.

## Boundaries and Limitations

- **Unfair comparison with Plan2Explore**: The authors acknowledge that Plan2Explore used task rewards to pretrain a reward model during the task-agnostic phase (leaking downstream information), and retained the replay buffer while allowing world model fine-tuning, whereas Proto-RL completely ignores task-agnostic transitions and freezes representations, so direct comparison is not fair.
- **APT baseline implementation differences**: The encoder weights of APT were frozen to promote fairness, but the original implementation by Liu & Abbeel (2021) allows fine-tuning, potentially underestimating APT performance.
- **What was not done**: No validation on real robot platforms; no sensitivity analysis for α, M, Sinkhorn iterations, or temperature τ; no in-depth experiments comparing encoder freezing vs fine-tuning (only noting fine-tuning compatibility); no reporting of computational resources or training time; no theoretical analysis (e.g., theoretical foundations for discrete representations).
- **Applicability boundaries**: Validated only on DMC continuous control tasks, not involving discrete actions, multi-agent, or high-dimensional 3D environments; representation learning preserves one-step dynamics similarity but is not connected to model minimization techniques such as bisimulation metrics.

## Engineering Insights

- **Reproduction priorities**: First verify the 3 Sinkhorn-Knopp iterations and temperature τ=0.1, which are key to uniform prototype assignment and collapse prevention; second, confirm that LRL gradients are indeed blocked (otherwise representations would couple with tasks and lose generalization).
- **Common pitfalls**: The value of α greatly impacts sparse-reward tasks (performance drops significantly at α=0), so it is recommended to scan α∈{0.1, 0.2, 0.5} on the target environment first; the queue size M×T and k=3 for k-NN must remain consistent, otherwise the intrinsic reward scale will drift.
- **Downstream adaptation**: After freezing the encoder, if the downstream observation distribution differs greatly from pretraining (e.g., different camera angles), fine-tuning the encoder can be attempted (the authors state it is compatible but untested), but note this may break task-agnostic representations.
- **Baseline comparison traps**: When reproducing APT, be sure to clarify whether the encoder is frozen, otherwise comparison results will be distorted; when comparing with Plan2Explore, strictly align information leakage conditions during the pretraining phase.
- **Resource recommendations**: 200k pretraining steps already yield usable representations (Figure 6); if compute is limited, run 200k first to validate the pipeline, then scale to 500k; for the SAC implementation, it is recommended to directly use the authors' open-source pytorch_sac version (including LayerNorm and the 39200→50 fully connected layer) to avoid introducing bias from a custom implementation.

## 개요

Proto-RL은 Denis Yarats 등이 제안한 이미지 강화 학습을 위한 비지도 표현 학습 방법으로, 자기 지도 프로토타입 클러스터링과 최대 엔트로피 탐색의 결합 훈련을 통해 작업과 무관한 시각 표현 사전 훈련을 달성하고, 이를 통해 하위 작업의 샘플 효율성과 다중 작업 일반화 능력을 향상시키는 것을 목표로 한다. 핵심 기여는 표현 학습과 탐색을 분리하여 사전 훈련된 표현을 동결하여 재사용할 수 있게 하고, 여러 DeepMind Control 작업에서 완전 지도 방식의 DrQ 기준선을 능가하거나 필적하는 성능을 달성한 것이다.

## 무엇을 바꾸었는가

이미지 RL에서 표현 학습과 탐색은 오랫동안 "닭과 달걀"의 딜레마에 직면해 왔다. 효과적인 표현은 다양한 탐색 데이터를 필요로 하고, 효율적인 탐색은 일관된 표현에 의존하기 때문이다. 기존 방법은 작업 보상에 의존하여 표현 학습을 유도하거나(예: DrQ), 표현이 특정 작업에 묶여 교차 작업 일반화가 불가능하거나, 보상이 없을 때 자기 지도 목표(예: CURL, SAC-AE)를 사용하지만 탐색 신호가 부족하여 희소 보상 환경에서 실패한다. Proto-RL이 진정으로 바꾼 것은 "표현 학습"과 "탐색"을 결합된 공동 최적화에서 해방시킨 것이다. 프로토타입 클러스터링을 통해 안정적인 자기 지도 목표를 제공하고, 동시에 엔트로피 최대화로 탐색을 유도하며, 둘은 공유 잠재 공간에서 협력하지만 그래디언트는 분리되어 있어 작업 보상이 없는 단계에서도 전이 가능한 일반 표현을 학습할 수 있다. 이 설계는 RL에 처음으로 CV/NLP의 "사전 훈련-미세 조정" 패러다임을 도입하여, 하위 작업은 표현을 동결하고 소량의 내재적 보상을 추가하기만 하면 빠르게 학습할 수 있으며, 처음부터 인코더를 훈련할 필요가 없다.

## 방법 분해

### 2단계 프레임워크
- **1단계(작업 무관 사전 훈련)**: 에이전트는 내재적 보상(엔트로피 최대화)만 최적화하고, 동시에 자기 지도 표현 손실 LSSL을 최소화하며, 어떤 작업 보상도 접촉하지 않는다.
- **2단계(하위 작업 학습)**: 인코더 fθ, 프로젝터 gθ 및 프로토타입 {ci}를 동결하고, 외재적 보상 rt와 내재적 보상 r̂t를 선형 결합(rt + αr̂t, α=0.2)하여 표준 SAC로 정책을 훈련한다.

### 표현 학습 흐름
1. 증강 프레임 xt(무작위 이동) → 온라인 인코더 fθ(SAC-AE 아키텍처, 4층 3×3 컨볼루션, 32채널, 출력 39200차원) → 프로젝터 gθ(선형 레이어, 128차원) → 예측기 vθ(MLP 128→512→128) → 정규화하여 û를 얻는다.
2. 타깃 네트워크(fξ, gξ)가 다음 프레임 xt+1을 인코딩하여 zt+1을 얻고, Sinkhorn-Knopp 클러스터링(3회 반복)으로 타깃 분포 qt+1을 얻는다.
3. 손실: LSSL = −qTt+1 log pt, 여기서 p(i)t = exp(ûTt ci/τ) / Σk exp(ûTt ck/τ), τ=0.1, M=512개의 프로토타입.
4. 그래디언트는 온라인 네트워크와 프로토타입만 업데이트하고, 타깃 네트워크는 EMA(모멘텀 0.05)로 업데이트한다. 예측기 vθ는 비대칭성을 도입하여 붕괴를 방지한다.

### 최대 엔트로피 탐색
- 내재적 목표: 잠재 공간 상태 방문 분포의 엔트로피를 최대화하고, k-NN 추정기(k=3)를 사용한다: r̂t = ||zt+1 − NNk,Q(zt+1)||.
- 최근접 이웃 계산 비용을 줄이기 위해 고정 크기 큐 Q(M×T=512×4=2048)를 유지하고, 각 프로토타입은 softmax 가중치로 T개의 후보를 샘플링한다.
- RL 에이전트(SAC)는 전이 (yt, at, r̂t, yt+1)로 훈련하지만, LRL 그래디언트는 차단되어 표현이 작업과 무관함을 보장한다.

### 핵심 하이퍼파라미터
| 파라미터 | 값 |
|------|-----|
| 프로토타입 수 M | 512 |
| 잠재 차원 D | 128 |
| softmax 온도 τ | 0.1 |
| Sinkhorn 반복 | 3 |
| 큐 크기 | 512×4=2048 |
| 내재적 보상 계수 α | 0.2 |
| 사전 훈련 스텝 | 500k |
| 하위 작업 스텝 | 500k |

## 핵심 혁신

1. **프로토타입 클러스터링과 탐색의 결합이지만 분리된 최적화**: 기존 방법이 표현 학습과 탐색을 결합하는 것(예: APT가 엔트로피를 직접 최적화)과 달리, Proto-RL은 Sinkhorn-Knopp로 프로토타입 균등 할당을 보장하여 표현 학습 목표를 안정화하고, 동시에 엔트로피 보상으로 탐색을 유도하며, 둘은 공유 잠재 공간을 사용하지만 그래디언트는 분리된다(LRL이 인코더를 업데이트하지 않음). 이것이 작업 무관 표현을 달성할 수 있는 핵심이다.
2. **동결 표현의 다중 작업 재사용**: 사전 훈련 후 인코더와 프로토타입은 완전히 동결되어 여러 하위 작업(예: Walker의 4개 방향 작업)에 직접 사용되며, 각 작업마다 표현을 다시 학습할 필요가 없다. 이는 기존 RL 표현 학습 방법에서 드문 것으로, 표현의 일반화를 직접 검증한다.
3. **프로토타입 샘플링의 저분산 엔트로피 추정**: APT의 균등 샘플링과 달리, Proto-RL은 프로토타입 softmax 분포로 재균형 샘플링을 수행하여 Acrobot Swingup과 Hopper Hop에서 내재적 보상 분산을 크게 줄이고(그림 9), 탐색 신호를 더 안정적으로 만든다. 이는 희소 보상 작업에 특히 중요하다.

## 실험 및 결과

### 단일 작업 평가(그림 4, 8개 DMC 환경)
| 비교 | 결과 |
|------|------|
| Proto-RL vs Curiosity | 7/8 환경에서 우세 |
| Proto-RL (500k) vs DrQ (1M) | 6/8 환경에서 우세 |

### 다중 작업 평가(그림 5, Walker 및 Reach Duplo 각 4개 작업)
- Proto-RL은 모든 기준선(Random, APT, Curiosity, DrQ)보다 현저히 우수하며, 표현을 동결한 후 직접 재사용하므로 재훈련이 필요 없다.

### 사전 훈련 효율성(그림 6)
- 200k 사전 훈련 스텝에서 Proto-RL은 이미 상태 공간을 충분히 탐색할 수 있으며, 하위 작업 성능이 기준선보다 우수하다. Walker Run에서 기준선은 사전 훈련 스텝이 증가할수록 성능이 오히려 하락하지만, Proto-RL은 안정적으로 유지된다.

### 하위 작업 탐색(그림 7)
- α=0은 모든 작업에서 α≥0.1보다 열등하다. 희소 보상 작업(Cartpole Swingup Sparse, Reach Duplo)에서 α≥0.1은 α=0보다 현저히 우수하여 내재적 보상의 필요성을 검증한다.

### 상태 커버리지 및 분산(그림 8, 9)
- Proto-RL 사전 훈련 단계는 더 높은 엔트로피의 상태 방문 분포를 달성한다(Cartpole Swingup Sparse, Walker Run).
- 프로토타입 샘플링은 균등 샘플링(APT)보다 Acrobot Swingup과 Hopper Hop에서 내재적 보상 표준 편차가 더 낮다.

### 사전 훈련 없는 실험(그림 13)
- Proto-RL은 사전 훈련 없이도 DrQ보다 우수하지만, 사전 훈련 버전보다는 낮아 사전 훈련 방식이 성능에 중요함을 증명한다.

## 경계 및 한계

- **Plan2Explore와의 비교 불공정성**: 저자는 Plan2Explore가 작업 무관 단계에서 작업 보상으로 보상 모델을 사전 훈련(하위 작업 정보 누출)하고, 리플레이 버퍼를 유지하며 세계 모델 미세 조정을 허용하는 반면, Proto-RL은 작업 무관 전이를 완전히 무시하고 표현을 동결하므로 직접 비교는 공정하지 않다고 인정한다.
- **APT 기준선 구현 차이**: 공정성을 위해 APT의 인코더 가중치를 동결했지만, Liu & Abbeel (2021)의 원래 구현은 미세 조정을 허용하므로 APT 성능을 과소평가할 수 있다.
- **하지 않은 것**: 실제 로봇 플랫폼 검증 없음; α, M, Sinkhorn 반복 횟수, 온도 τ의 민감도 분석 없음; 인코더 동결 vs 미세 조정의 심층 실험 없음(미세 조정 호환만 언급); 계산 자원 및 훈련 시간 보고 없음; 이론적 분석 없음(예: 이산 표현의 이론적 기반).
- **적용 경계**: DMC 연속 제어 작업에서만 검증되었으며, 이산 행동, 다중 에이전트 또는 고차원 3D 환경은 다루지 않음; 표현 학습은 1단계 동적 유사성을 유지하지만, 이중 시뮬레이션 메트릭과 같은 모델 최소화 기술과의 연결은 없음.

## 공학적 시사점

- **재현 우선순위**: 먼저 Sinkhorn-Knopp의 3회 반복과 온도 τ=0.1을 확인해야 한다. 이것이 프로토타입 균등 할당과 붕괴 방지의 핵심이다. 다음으로 LRL 그래디언트가 실제로 차단되었는지 확인해야 한다(그렇지 않으면 표현이 작업과 결합되어 일반화를 잃는다).
- **쉽게 빠지는 함정**: α 값은 희소 보상 작업에 큰 영향을 미치므로(α=0에서 성능이 현저히 하락), 목표 환경에서 먼저 α∈{0.1, 0.2, 0.5}를 스캔하는 것이 좋다. 큐 크기 M×T와 k-NN의 k=3은 일관성을 유지해야 하며, 그렇지 않으면 내재적 보상 스케일이 변동한다.
- **하위 작업 적응**: 인코더를 동결한 후 하위 작업 관측 분포가 사전 훈련과 크게 다른 경우(예: 다른 카메라 각도), 인코더 미세 조정을 시도할 수 있지만(저자는 호환 가능하다고 언급했지만 실험하지 않음), 표현의 작업 무관성을 손상시킬 수 있음에 주의해야 한다.
- **기준선 비교 함정**: APT를 재현할 때 인코더 동결 여부를 명확히 해야 하며, 그렇지 않으면 비교 결과가 왜곡된다. Plan2Explore와 비교할 때는 사전 훈련 단계의 정보 누출 조건을 엄격히 정렬해야 한다.
- **자원 제안**: 사전 훈련 200k 스텝으로도 사용 가능한 표현을 얻을 수 있으므로(그림 6), 계산이 제한된 경우 먼저 200k로 파이프라인을 검증한 후 500k로 확장할 수 있다. SAC 구현은 저자가 오픈소스로 공개한 pytorch_sac 버전(LayerNorm 및 39200→50 완전 연결 레이어 포함)을 직접 사용하는 것이 좋으며, 자체 구현으로 인한 편향을 피해야 한다.
