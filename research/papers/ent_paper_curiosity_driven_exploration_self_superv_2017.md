---
$id: ent_paper_curiosity_driven_exploration_self_superv_2017
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Curiosity-driven Exploration by Self-supervised Prediction
  zh: Curiosity-driven Exploration by Self-supervised Prediction
  ko: Curiosity-driven Exploration by Self-supervised Prediction
summary:
  en: In many real-world scenarios, rewards extrinsic to the agent are extremely sparse, or absent altogether. In such cases,
    curiosity can serve as an intrinsic reward signal to enable the agent to explore its environment and learn skills that
    might be useful later in its life.
  zh: 本文提出一种基于自监督预测的好奇心驱动探索方法，由 Deepak Pathak 等人完成。核心贡献在于通过逆动力学模型在视觉特征空间中计算预测误差作为内在奖励，有效处理高维连续状态空间，并忽略环境中的无关因素。该方法在 VizDoom
    和 Super Mario Bros 环境中验证了稀疏奖励、无外部奖励及泛化场景下的高效探索能力。
  ko: In many real-world scenarios, rewards extrinsic to the agent are extremely sparse, or absent altogether. In such cases,
    curiosity can serve as an intrinsic reward signal to enable the agent to explore its environment and learn skills that
    might be useful later in its life.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- curiosity
- driven
- exploration
- self
- superv
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 161 (.staging/ingest_yuanxq). Tier C->full. arXiv id 1705.05363 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (1705.05363v1); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: arXiv:1705.05363 Curiosity-driven Exploration by Self-supervised Prediction
  url: https://arxiv.org/abs/1705.05363
  accessed_at: '2026-07-31'
  date: '2017-05-15'
- id: src_002
  type: website
  title: Project page
  url: https://pathak22.github.io/noreward-rl/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---


## 概述

本文提出一种基于内在好奇心奖励的强化学习算法，通过自监督逆动力学模型学习特征空间，并以前向动力学模型的预测误差作为奖励信号，解决高维视觉输入下稀疏奖励环境的探索问题。该方法由UC Berkeley等机构的研究者提出，核心贡献在于绕过像素预测的困难，使智能体仅凭好奇心信号即可在3D环境和游戏中学会导航与探索。

## 它改变了什么

在稀疏奖励或无奖励的强化学习场景中，随机探索几乎不可能触及目标状态，而传统内在奖励方法要么需要环境状态分布的统计模型，要么需要构建高维连续空间（如图像）的动力学模型，两者都难以实现。直接预测像素更是存在根本缺陷：环境中的不可控随机性（如电视白噪声、树叶晃动）会导致预测误差持续偏高，使智能体陷入“好奇心陷阱”并停止探索。本文真正改变的是内在奖励信号的设计范式——不再试图建模整个环境的动态，而是只关注与智能体动作相关的状态变化，从而在特征层面实现“选择性好奇”，使探索信号在高维视觉输入下变得可计算且鲁棒。

## 方法拆解

### 核心架构：ICM（Intrinsic Curiosity Module）
- **逆动力学模型**：给定当前状态 \(s_t\) 和下一状态 \(s_{t+1}\)，预测动作 \(\hat{a}_t = g(s_t, s_{t+1}; \theta_I)\)。通过最小化损失 \(L_I(\hat{a}_t, a_t)\) 训练，动作离散时输出soft-max分布，等价于多项分布下的最大似然估计。该模型迫使特征空间 \(\phi(s_t)\) 仅保留与动作相关的信息，忽略无关的环境变化。
- **前向动力学模型**：给定当前状态特征 \(\phi(s_t)\) 和动作 \(a_t\)，预测下一状态特征 \(\hat{\phi}(s_{t+1}) = f(\phi(s_t), a_t; \theta_F)\)。损失函数为 \(L_F = \frac{1}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\)。
- **内在奖励**：\(r_t^i = \frac{\eta}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\)，其中 \(\eta > 0\) 为缩放因子。预测误差越大，好奇心奖励越高。
- **总体优化目标**：\(\min_{\theta_P, \theta_I, \theta_F} [-\lambda \mathbb{E}_{\pi(s_t;\theta_P)}[\sum_t r_t] + (1-\beta)L_I + \beta L_F]\)，其中 \(\beta = 0.2\) 为逆模型与前向模型损失的权重，\(\lambda = 0.1\) 为策略梯度损失与内在奖励学习重要性的权重。
- **策略学习**：使用A3C（异步优势演员-评论家）算法，20个worker并行训练，ADAM优化器，学习率1e-3。

### 关键设计决策
- **特征空间而非像素空间**：在原始像素空间预测不可取，因为不仅预测困难，更因为像素预测误差无法区分“可控变化”与“无关变化”（如树叶在风中移动）。
- **逆模型作为特征学习器**：通过预测动作，网络被迫忽略不影响智能体的环境因素，从而构建“因果相关”的特征表示。
- **状态变化分类**：好的好奇心特征空间应建模(1)智能体可控制的变化和(2)不可控但影响智能体的变化，忽略(3)不可控且不影响智能体的变化。

## 关键创新

1. **特征空间的因果选择性**：通过逆动力学任务学习特征表示，使好奇心奖励只对智能体可控或影响智能体的状态变化敏感，这是首次在内在奖励设计中显式区分“因果相关”与“无关干扰”，解决了像素预测的“好奇心陷阱”问题。
2. **前向模型预测误差作为奖励**：与Agrawal et al. (2016)仅将前向模型作为逆模型训练的正则化器不同，本文直接使用前向模型预测误差驱动策略学习，使好奇心信号成为探索的直接动力。
3. **无奖励场景的泛化能力**：在Mario和VizDoom中，仅用好奇心信号训练的智能体能够学会导航、躲避敌人等复杂行为，且预训练策略可迁移到新场景（微调后优于从零训练），展示了内在奖励在无外部监督下的强大探索能力。

## 实验与结果

### VizDoom稀疏奖励设置
| 方法 | dense | sparse | very sparse |
|------|-------|--------|-------------|
| A3C | 学习较慢 | 失败 | 从未成功 |
| ICM-pixels + A3C | 收敛 | 成功 | 从未成功 |
| ICM + A3C | 学习最快 | 快速成功 | 66%随机运行达到完美分数 |

- 在“very sparse”设置下（出生点距目标350步），ICM智能体在66%的随机运行中达到完美分数，而A3C和ICM-pixels均从未成功。
- 在观测中添加40%白噪声后，ICM智能体仍达到完美分数，而ICM-pixels显著受损，验证了特征空间对无关干扰的鲁棒性。

### TRPO-VIME对比（“sparse”设置）
| 方法 | 平均（中位数）分数 |
|------|-------------------|
| TRPO | 26.0%（0.0%） |
| A3C | 0.0%（0.0%） |
| VIME + TRPO | 46.1%（27.1%） |
| ICM + A3C | 100.0%（100.0%） |

### Mario无奖励探索
- 仅用好奇心信号训练，智能体在Level-1可跨越超过30%的关卡。
- 泛化实验：在Level-1预训练后，微调策略在Level-2上达到466 ± 37.9的平均距离，优于从零训练的399.7 ± 22.5（1.5M迭代）和455.5 ± 33.4（3.5M迭代）。
- “as is”运行在Level-3表现良好（319.3 ± 9.7），但在Level-2表现差（31.9 ± 4.2），归因于Level-2的视觉外观显著不同（夜晚世界）。

### 泛化到新场景
- VizDoom测试地图（新纹理）上，预训练后微调外在奖励的ICM + A3C显著优于从零训练，而ICM-pixels完全失败。

## 边界与局限

- **交互稀少场景**：方法不直接扩展到“交互机会也稀少”的场景，作者将回放记忆引导探索留作未来工作。
- **Mario关卡38%处的坑**：需要15-20次特定按键序列才能跳过，失败则死亡且无梯度信息，作者认为这与好奇心建模正交，但对策略学习构成挑战。
- **未做之事**：未提及真实机器人平台验证、连续动作空间实验、三维物理世界验证、η具体取值、λ和β消融研究、ICM特征空间可解释性分析、好奇心饱和问题、随机种子系统调参（文中明确说“We did not perform any tuning of random seeds”）。
- **泛化局限**：Mario中“as is”在Level-2表现差的原因仅推测为视觉外观差异，未完全验证；微调Level-3时好奇心封锁的具体机制是否可避免未提及。

## 工程启示

- **复现核对点**：先确认逆模型是否真正学习到与动作相关的特征——可在训练中可视化特征空间，检查无关干扰（如白噪声）是否被抑制；再核对前向模型预测误差的尺度，必要时调整η缩放因子。
- **最易踩坑处**：ICM-pixels在无噪声的“sparse”任务中可能成功，但一旦加入无关干扰即显著受损，因此评估时必须包含干扰项测试；Mario中“as is”泛化受视觉外观差异影响大，微调时需注意“好奇心封锁”现象——当策略无法越过困难点后，已学部分几乎不再产生奖励，策略会退化。
- **工程建议**：对于下游团队，若任务环境存在大量不可控视觉噪声（如树叶、阴影），ICM是首选；若交互机会稀少，需额外设计回放记忆机制；预训练+微调范式在视觉差异小的场景中有效，但视觉差异大时需重新训练或增加微调迭代数。

## 参考
- https://arxiv.org/abs/1705.05363
- https://pathak22.github.io/noreward-rl/
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## Overview

This paper proposes a reinforcement learning algorithm based on intrinsic curiosity rewards. It learns a feature space through a self-supervised inverse dynamics model and uses the prediction error of a forward dynamics model as the reward signal, addressing the exploration problem in sparse-reward environments with high-dimensional visual inputs. The method was proposed by researchers from UC Berkeley and other institutions. Its core contribution lies in bypassing the difficulty of pixel prediction, enabling agents to learn navigation and exploration in 3D environments and games using only curiosity signals.

## What It Changed

In reinforcement learning scenarios with sparse or no rewards, random exploration almost never reaches goal states, while traditional intrinsic reward methods either require statistical models of the environment state distribution or require building dynamics models in high-dimensional continuous spaces (such as images), both of which are difficult to achieve. Direct pixel prediction has a fundamental flaw: uncontrollable randomness in the environment (e.g., TV static, swaying leaves) causes persistently high prediction errors, trapping agents in a "curiosity trap" and halting exploration. What this paper truly changes is the design paradigm of intrinsic reward signals—instead of attempting to model the entire environment's dynamics, it focuses only on state changes relevant to the agent's actions, achieving "selective curiosity" at the feature level and making exploration signals computable and robust under high-dimensional visual inputs.

## Method Breakdown

### Core Architecture: ICM (Intrinsic Curiosity Module)
- **Inverse Dynamics Model**: Given current state \(s_t\) and next state \(s_{t+1}\), predict action \(\hat{a}_t = g(s_t, s_{t+1}; \theta_I)\). Trained by minimizing loss \(L_I(\hat{a}_t, a_t)\), outputting a soft-max distribution for discrete actions, equivalent to maximum likelihood estimation under a multinomial distribution. This model forces the feature space \(\phi(s_t)\) to retain only action-relevant information while ignoring irrelevant environmental changes.
- **Forward Dynamics Model**: Given current state features \(\phi(s_t)\) and action \(a_t\), predict next state features \(\hat{\phi}(s_{t+1}) = f(\phi(s_t), a_t; \theta_F)\). The loss function is \(L_F = \frac{1}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\).
- **Intrinsic Reward**: \(r_t^i = \frac{\eta}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\), where \(\eta > 0\) is a scaling factor. The larger the prediction error, the higher the curiosity reward.
- **Overall Optimization Objective**: \(\min_{\theta_P, \theta_I, \theta_F} [-\lambda \mathbb{E}_{\pi(s_t;\theta_P)}[\sum_t r_t] + (1-\beta)L_I + \beta L_F]\), where \(\beta = 0.2\) is the weight between inverse and forward model losses, and \(\lambda = 0.1\) is the weight between policy gradient loss and intrinsic reward learning importance.
- **Policy Learning**: Uses the A3C (Asynchronous Advantage Actor-Critic) algorithm with 20 parallel workers, ADAM optimizer, and learning rate 1e-3.

### Key Design Decisions
- **Feature space rather than pixel space**: Predicting in raw pixel space is undesirable not only because prediction is difficult, but also because pixel prediction errors cannot distinguish between "controllable changes" and "irrelevant changes" (e.g., leaves moving in the wind).
- **Inverse model as feature learner**: By predicting actions, the network is forced to ignore environmental factors that do not affect the agent, thereby constructing "causally relevant" feature representations.
- **State change classification**: A good curiosity feature space should model (1) changes controllable by the agent and (2) uncontrollable changes that affect the agent, while ignoring (3) uncontrollable changes that do not affect the agent.

## Key Innovations

1. **Causal selectivity in feature space**: Learning feature representations through the inverse dynamics task makes curiosity rewards sensitive only to state changes controllable by or affecting the agent. This is the first explicit distinction between "causally relevant" and "irrelevant interference" in intrinsic reward design, solving the "curiosity trap" problem of pixel prediction.
2. **Forward model prediction error as reward**: Unlike Agrawal et al. (2016), which used the forward model only as a regularizer for inverse model training, this paper directly uses forward model prediction error to drive policy learning, making curiosity signals a direct driver of exploration.
3. **Generalization in reward-free scenarios**: In Mario and VizDoom, agents trained with only curiosity signals can learn complex behaviors such as navigation and enemy avoidance, and pretrained policies can transfer to new scenarios (outperforming training from scratch after fine-tuning), demonstrating the strong exploration capability of intrinsic rewards without external supervision.

## Experiments and Results

### VizDoom Sparse Reward Settings
| Method | dense | sparse | very sparse |
|--------|-------|--------|-------------|
| A3C | Slow learning | Fails | Never succeeds |
| ICM-pixels + A3C | Converges | Succeeds | Never succeeds |
| ICM + A3C | Fastest learning | Rapid success | 66% of random runs achieve perfect score |

- In the "very sparse" setting (spawn point 350 steps from goal), ICM agents achieve perfect scores in 66% of random runs, while A3C and ICM-pixels never succeed.
- After adding 40% white noise to observations, ICM agents still achieve perfect scores, while ICM-pixels is significantly impaired, validating the robustness of feature space to irrelevant interference.

### TRPO-VIME Comparison ("sparse" setting)
| Method | Average (Median) Score |
|--------|------------------------|
| TRPO | 26.0% (0.0%) |
| A3C | 0.0% (0.0%) |
| VIME + TRPO | 46.1% (27.1%) |
| ICM + A3C | 100.0% (100.0%) |

### Mario Reward-Free Exploration
- Trained with only curiosity signals, the agent crosses over 30% of Level-1.
- Generalization experiment: After pretraining on Level-1, the fine-tuned policy achieves an average distance of 466 ± 37.9 on Level-2, outperforming training from scratch at 399.7 ± 22.5 (1.5M iterations) and 455.5 ± 33.4 (3.5M iterations).
- "As is" runs perform well on Level-3 (319.3 ± 9.7) but poorly on Level-2 (31.9 ± 4.2), attributed to the significantly different visual appearance of Level-2 (night world).

### Generalization to New Scenarios
- On VizDoom test maps (new textures), ICM + A3C with pretraining followed by fine-tuning on extrinsic rewards significantly outperforms training from scratch, while ICM-pixels fails completely.

## Boundaries and Limitations

- **Sparse interaction scenarios**: The method does not directly extend to scenarios where "interaction opportunities are also sparse." The authors leave replay memory-guided exploration as future work.
- **Pit at 38% of Mario Level**: Requires 15-20 specific key sequences to jump across; failure results in death with no gradient information. The authors consider this orthogonal to curiosity modeling, but it poses a challenge for policy learning.
- **What was not done**: No validation on real robot platforms, no continuous action space experiments, no 3D physical world validation, no specific value for \(\eta\), no ablation studies for \(\lambda\) and \(\beta\), no interpretability analysis of ICM feature space, no analysis of curiosity saturation, and no systematic tuning of random seeds (the paper explicitly states "We did not perform any tuning of random seeds").
- **Generalization limitations**: The poor "as is" performance on Mario Level-2 is only speculated to be due to visual appearance differences, not fully verified; whether the specific mechanism of curiosity blocking during Level-3 fine-tuning can be avoided is not mentioned.

## Engineering Insights

- **Reproduction checkpoints**: First verify whether the inverse model truly learns action-relevant features—visualize the feature space during training and check whether irrelevant interference (e.g., white noise) is suppressed; then check the scale of forward model prediction errors and adjust the \(\eta\) scaling factor if necessary.
- **Most common pitfalls**: ICM-pixels may succeed on "sparse" tasks without noise, but is significantly impaired once irrelevant interference is added, so evaluation must include interference tests; "as is" generalization in Mario is highly affected by visual appearance differences, and fine-tuning requires attention to the "curiosity blocking" phenomenon—once the policy cannot pass a difficult point, the learned parts almost stop generating rewards and the policy degrades.
- **Engineering recommendations**: For downstream teams, ICM is the first choice when the task environment contains large amounts of uncontrollable visual noise (e.g., leaves, shadows); if interaction opportunities are sparse, additional replay memory mechanisms are needed; the pretraining + fine-tuning paradigm works well in scenarios with small visual differences, but retraining or increased fine-tuning iterations are needed when visual differences are large.

## 개요

본 논문은 내재적 호기심 보상 기반 강화 학습 알고리즘을 제안하며, 자기 지도 역동역학 모델을 통해 특징 공간을 학습하고, 전방 동역학 모델의 예측 오차를 보상 신호로 사용하여 고차원 시각 입력 하에서 희소 보상 환경의 탐색 문제를 해결한다. 이 방법은 UC Berkeley 등 기관의 연구자들이 제안했으며, 핵심 기여는 픽셀 예측의 어려움을 우회하여 에이전트가 호기심 신호만으로 3D 환경과 게임에서 내비게이션과 탐색을 학습할 수 있게 하는 것이다.

## 그것이 바꾼 것

희소 보상 또는 무보상 강화 학습 시나리오에서 무작위 탐색은 목표 상태에 도달하기 거의 불가능하며, 기존 내재적 보상 방법은 환경 상태 분포의 통계적 모델을 필요로 하거나 이미지와 같은 고차원 연속 공간의 동역학 모델을 구축해야 하므로 둘 다 구현하기 어렵다. 픽셀을 직접 예측하는 것은 근본적인 결함이 있다: 환경의 통제 불가능한 무작위성(예: TV 백색 잡음, 나뭇잎 흔들림)으로 인해 예측 오차가 지속적으로 높아져 에이전트가 '호기심 함정'에 빠지고 탐색을 중단하게 된다. 본 논문이 실제로 바꾼 것은 내재적 보상 신호의 설계 패러다임이다——더 이상 환경 전체의 동역학을 모델링하려 하지 않고, 에이전트의 행동과 관련된 상태 변화에만 주목하여 특징 수준에서 '선택적 호기심'을 구현함으로써 탐색 신호를 고차원 시각 입력에서 계산 가능하고 견고하게 만든다.

## 방법 분해

### 핵심 아키텍처: ICM (Intrinsic Curiosity Module)
- **역동역학 모델**: 현재 상태 \(s_t\)와 다음 상태 \(s_{t+1}\)가 주어졌을 때, 행동 \(\hat{a}_t = g(s_t, s_{t+1}; \theta_I)\)을 예측한다. 손실 \(L_I(\hat{a}_t, a_t)\)을 최소화하여 훈련하며, 행동이 이산적일 때 soft-max 분포를 출력하며 이는 다항 분포 하의 최대 우도 추정과 동일하다. 이 모델은 특징 공간 \(\phi(s_t)\)이 행동과 관련된 정보만 유지하고 무관한 환경 변화를 무시하도록 강제한다.
- **전방 동역학 모델**: 현재 상태 특징 \(\phi(s_t)\)과 행동 \(a_t\)가 주어졌을 때, 다음 상태 특징 \(\hat{\phi}(s_{t+1}) = f(\phi(s_t), a_t; \theta_F)\)을 예측한다. 손실 함수는 \(L_F = \frac{1}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\)이다.
- **내재적 보상**: \(r_t^i = \frac{\eta}{2}\|\hat{\phi}(s_{t+1}) - \phi(s_{t+1})\|_2^2\), 여기서 \(\eta > 0\)는 스케일링 팩터이다. 예측 오차가 클수록 호기심 보상이 높아진다.
- **전체 최적화 목표**: \(\min_{\theta_P, \theta_I, \theta_F} [-\lambda \mathbb{E}_{\pi(s_t;\theta_P)}[\sum_t r_t] + (1-\beta)L_I + \beta L_F]\), 여기서 \(\beta = 0.2\)는 역모델과 전방 모델 손실의 가중치, \(\lambda = 0.1\)은 정책 그래디언트 손실과 내재적 보상 학습 중요성의 가중치이다.
- **정책 학습**: A3C(비동기 이점 액터-크리틱) 알고리즘을 사용하며, 20개의 워커가 병렬 훈련하고, ADAM 옵티마이저, 학습률 1e-3을 사용한다.

### 핵심 설계 결정
- **특징 공간이 아닌 픽셀 공간**: 원본 픽셀 공간에서의 예측은 바람직하지 않다. 예측이 어려울 뿐만 아니라, 픽셀 예측 오차는 '제어 가능한 변화'와 '무관한 변화'(예: 바람에 흔들리는 나뭇잎)를 구분할 수 없기 때문이다.
- **특징 학습자로서의 역모델**: 행동을 예측함으로써 네트워크는 에이전트에 영향을 미치지 않는 환경 요인을 무시하도록 강제되어 '인과적으로 관련된' 특징 표현을 구축한다.
- **상태 변화 분류**: 좋은 호기심 특징 공간은 (1) 에이전트가 제어할 수 있는 변화와 (2) 통제할 수 없지만 에이전트에 영향을 미치는 변화를 모델링하고, (3) 통제할 수 없고 에이전트에 영향을 미치지 않는 변화는 무시해야 한다.

## 핵심 혁신

1. **특징 공간의 인과적 선택성**: 역동역학 작업을 통해 특징 표현을 학습하여 호기심 보상이 에이전트가 제어하거나 에이전트에 영향을 미치는 상태 변화에만 민감하게 만든다. 이는 내재적 보상 설계에서 '인과적 관련성'과 '무관한 간섭'을 명시적으로 구분한 최초의 사례로, 픽셀 예측의 '호기심 함정' 문제를 해결한다.
2. **전방 모델 예측 오차를 보상으로 사용**: Agrawal et al. (2016)이 전방 모델을 역모델 훈련의 정규화 도구로만 사용한 것과 달리, 본 논문은 전방 모델 예측 오차를 직접 정책 학습에 사용하여 호기심 신호를 탐색의 직접적인 동력으로 만든다.
3. **무보상 시나리오에서의 일반화 능력**: Mario와 VizDoom에서 호기심 신호만으로 훈련된 에이전트가 내비게이션, 적 회피 등의 복잡한 행동을 학습할 수 있으며, 사전 훈련된 정책은 새로운 시나리오로 전이될 수 있다(미세 조정 시 처음부터 훈련하는 것보다 우수). 이는 외부 감독 없이 내재적 보상의 강력한 탐색 능력을 보여준다.

## 실험 및 결과

### VizDoom 희소 보상 설정
| 방법 | dense | sparse | very sparse |
|------|-------|--------|-------------|
| A3C | 학습 느림 | 실패 | 성공한 적 없음 |
| ICM-pixels + A3C | 수렴 | 성공 | 성공한 적 없음 |
| ICM + A3C | 가장 빠른 학습 | 빠른 성공 | 66% 무작위 실행에서 완벽 점수 달성 |

- 'very sparse' 설정(출생 지점에서 목표까지 350걸음)에서 ICM 에이전트는 66%의 무작위 실행에서 완벽 점수를 달성한 반면, A3C와 ICM-pixels는 모두 성공한 적이 없다.
- 관측에 40% 백색 잡음을 추가한 후에도 ICM 에이전트는 완벽 점수를 달성했지만, ICM-pixels는 크게 손상되어 특징 공간의 무관한 간섭에 대한 견고성을 검증했다.

### TRPO-VIME 비교('sparse' 설정)
| 방법 | 평균(중앙값) 점수 |
|------|-------------------|
| TRPO | 26.0%(0.0%) |
| A3C | 0.0%(0.0%) |
| VIME + TRPO | 46.1%(27.1%) |
| ICM + A3C | 100.0%(100.0%) |

### Mario 무보상 탐색
- 호기심 신호만으로 훈련된 에이전트는 Level-1에서 30% 이상의 스테이지를 통과할 수 있다.
- 일반화 실험: Level-1에서 사전 훈련 후, 미세 조정된 정책은 Level-2에서 466 ± 37.9의 평균 거리를 달성하여 처음부터 훈련한 399.7 ± 22.5(1.5M 반복) 및 455.5 ± 33.4(3.5M 반복)보다 우수하다.
- 'as is' 실행은 Level-3에서 좋은 성능(319.3 ± 9.7)을 보였지만 Level-2에서는 낮은 성능(31.9 ± 4.2)을 보였으며, 이는 Level-2의 시각적 외관이 크게 다르기 때문(야간 세계)으로 귀인된다.

### 새로운 시나리오로의 일반화
- VizDoom 테스트 맵(새로운 텍스처)에서 외부 보상으로 미세 조정된 사전 훈련된 ICM + A3C는 처음부터 훈련하는 것보다 크게 우수했지만, ICM-pixels는 완전히 실패했다.

## 경계 및 한계

- **상호작용이 드문 시나리오**: 이 방법은 '상호작용 기회도 드문' 시나리오로 직접 확장되지 않으며, 저자는 리플레이 메모리 기반 탐색 유도를 향후 작업으로 남겨둔다.
- **Mario 레벨 38% 지점의 구덩이**: 15-20회의 특정 키 입력 시퀀스가 필요하며, 실패 시 사망하고 그래디언트 정보가 없다. 저자는 이것이 호기심 모델링과 직교한다고 생각하지만 정책 학습에 도전 과제가 된다.
- **수행하지 않은 것**: 실제 로봇 플랫폼 검증, 연속 행동 공간 실험, 3D 물리 세계 검증, η 구체적 값, λ 및 β 절제 연구, ICM 특징 공간 해석 가능성 분석, 호기심 포화 문제, 무작위 시드 시스템 튜닝(논문에서 명시적으로 "We did not perform any tuning of random seeds"라고 언급)은 언급되지 않았다.
- **일반화 한계**: Mario에서 'as is'가 Level-2에서 낮은 성능을 보인 이유는 시각적 외관 차이로만 추측되었으며 완전히 검증되지 않았다. Level-3 미세 조정 시 호기심 차단의 구체적 메커니즘이 피할 수 있는지 여부는 언급되지 않았다.

## 공학적 시사점

- **재현 체크포인트**: 먼저 역모델이 실제로 행동 관련 특징을 학습했는지 확인——훈련 중 특징 공간을 시각화하고 무관한 간섭(예: 백색 잡음)이 억제되는지 확인한다. 그런 다음 전방 모델 예측 오차의 스케일을 확인하고 필요한 경우 η 스케일링 팩터를 조정한다.
- **가장 쉽게 빠지는 함정**: ICM-pixels는 잡음이 없는 'sparse' 작업에서 성공할 수 있지만, 무관한 간섭이 추가되면 크게 손상되므로 평가 시 반드시 간섭 항목 테스트를 포함해야 한다. Mario에서 'as is' 일반화는 시각적 외관 차이의 영향을 크게 받으며, 미세 조정 시 '호기심 차단' 현상에 주의해야 한다——정책이 어려운 지점을 넘지 못하면 학습된 부분이 거의 보상을 생성하지 않아 정책이 퇴화한다.
- **공학적 제안**: 다운스트림 팀의 경우, 작업 환경에 통제 불가능한 시각적 잡음(예: 나뭇잎, 그림자)이 많으면 ICM이 최우선 선택이다. 상호작용 기회가 드물면 리플레이 메모리 메커니즘을 추가로 설계해야 한다. 사전 훈련 + 미세 조정 패러다임은 시각적 차이가 작은 시나리오에서 효과적이지만, 시각적 차이가 크면 재훈련하거나 미세 조정 반복 횟수를 늘려야 한다.
