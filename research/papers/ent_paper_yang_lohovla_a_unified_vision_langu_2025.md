---
$id: ent_paper_yang_lohovla_a_unified_vision_langu_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'LoHoVLA: A Unified Vision-Language-Action Model for Long-Horizon Embodied Tasks'
  zh: LoHoVLA
  ko: 'LoHoVLA: A Unified Vision-Language-Action Model for Long-Horizon Embodied Tasks'
summary:
  en: 'LoHoVLA: A Unified Vision-Language-Action Model for Long-Horizon Embodied Tasks (LoHoVLA), is a 2025 large vision-language-action
    model for robotic manipulation, introduced by Fudan University, ShanghaiTech University, Shanghai Jiao Tong University.'
  zh: LoHoVLA 是复旦大学、上海科技大学和上海交通大学于 2025 年提出的统一视觉-语言-动作模型，专为长时域具身任务设计。其核心贡献在于将高层任务规划与低层运动控制整合于单一框架，并引入分层闭环控制机制以提升鲁棒性。在基于 Ravens
    模拟器的 LoHoSet 数据集上，该模型显著优于现有分层架构和标准 VLA 方法。
  ko: 'LoHoVLA: A Unified Vision-Language-Action Model for Long-Horizon Embodied Tasks (LoHoVLA), is a 2025 large vision-language-action
    model for robotic manipulation, introduced by Fudan University, ShanghaiTech University, Shanghai Jiao Tong University.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- large_vla_model
- lohovla
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2506.00411v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (864 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'LoHoVLA: A Unified Vision-Language-Action Model for Long-Horizon Embodied Tasks (arXiv)'
  url: https://arxiv.org/abs/2506.00411
  date: '2025'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: LoHoVLA source
  url: https://doi.org/10.48550/arXiv.2506.00411
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

LoHoVLA 是一个统一视觉-语言-动作模型，由作者团队提出，旨在单一框架内同时处理长时程具身任务的高层子任务规划与低层动作控制。其核心贡献在于共享骨干网络联合生成语言子任务与离散动作 token，并引入分层闭环控制机制以平衡规划修正与动作重试，在 Ravens 模拟器的 LoHoRavens 基准上显著超越分层架构与 Vanilla VLA 基线。

## 它改变了什么

长时程具身任务长期被分割为“高层 VLM 规划器 + 低层 VLA 控制器”的两层系统，这种架构的痛点在于协调成本高：规划器输出的抽象子任务与控制器所需的精确动作之间存在语义鸿沟，且反馈回路延迟导致错误累积。现有 VLA 模型则偏向低层控制，缺乏将语言目标分解为可执行步骤的推理能力，在需要多步决策的任务上性能急剧下降。

LoHoVLA 真正改变的是“规划与控制必须分家”的默认假设。它证明了一个经过两阶段训练的单一模型，可以在共享表征空间中同时产出子任务序列和动作指令，且这种联合学习带来的表征共享，比显式分层通信更高效。此外，它引入的闭环控制策略不是简单地“失败就重规划”，而是通过阈值判断区分“规划错误”与“执行噪声”，这比现有分层方法中一刀切的反馈机制更贴近真实机器人操作中的错误模式。

## 方法拆解

### 模型架构
- 骨干网络：PaliGemma-3b-mix-224，增强原始语言头以同时输出语言子任务 token 与离散动作 token。
- 输入：视觉观测 + 高层语言目标；输出：子任务描述 + 动作序列。
- 推理流程：模型先推断子任务，再以该子任务为上下文预测动作；执行后捕获新观测，循环迭代。

### 两阶段训练策略
- 阶段一（规划优先）：在 14 个长时程任务（4 个 LoHoRavens 可见任务 + 10 个额外设计任务）上微调，每任务 1000 个演示，仅优化文本损失（交叉熵），学习率 5e-5，3 个 epoch。
- 阶段二（动作增强）：加入每个拾放原始任务 10000 个演示，同时优化文本损失与动作损失，学习率 1e-5，1 个 epoch。
- 关键设计：延迟引入动作标签，避免早期动作监督干扰高层规划能力的收敛。

### 分层闭环控制
- 定义失败计数 k：若当前子任务执行后奖励为 0，则 k 加 1。
- 控制逻辑：当 k 超过阈值 K=2 时，触发子任务重新规划；否则仅基于新观测重新预测动作。
- 训练目标：总损失 = 文本损失 + 动作损失，均为交叉熵。

### 训练配置
- 优化器：AdamW，带权重衰减与梯度裁剪；分布式训练用 DeepSpeed。
- 硬件：8 块 NVIDIA 4090 GPU（24GB 显存），每设备批大小 2。
- LoRA：秩 16，目标所有线性层。

## 关键创新

1. **统一架构替代分层通信**：首次在单一 VLA 模型中实现子任务规划与动作生成的端到端联合学习，消除了分层系统中规划器与控制器之间的表征转换损耗。其有效性体现在任务 B（put-block-into-matching-bowl）上，LoHoVLA 平均得分 97.8 对比分层架构最优的 37.0，提升幅度巨大（由表内数值 37.0→97.8 计算）。

2. **两阶段课程学习**：先纯文本优化规划能力，再引入动作监督，这种“先想后动”的训练顺序有效避免了多任务联合优化中的梯度冲突。实验表明单阶段训练的子任务规划成功率显著低于两阶段，直接导致任务完成率下降。

3. **阈值化闭环控制**：不同于“每次失败都重规划”或“永不重规划”，K=2 的阈值策略在需要精确电机控制的任务（如任务 D）上优于频繁重规划，在需要快速修正子任务分配的任务（如任务 E）上优于不重规划，体现了对错误类型的自适应区分。

## 实验与结果

### 数据集与基线
- LoHoSet：基于 Ravens 模拟器，含 20 个长时程任务，每任务 1000 个专家演示。
- 基线：LoHoRavens（分层架构，Planner 为 LLaMA 2 13B，Actor 为 CLIPort，Reporter 为 OpenFlamingo）；Vanilla VLA（无中间语言输出）。

### 主结果（LoHoRavens 基准平均得分/成功率）
| 任务 | Vanilla VLA | LoHoRavens 显式 | LoHoRavens 隐式 | LoHoVLA |
|---|---|---|---|---|
| B: put-block-into-matching-bowl | 14.9/0.0 | 31.4/- | 37.0/- | 97.8/91.5 |
| E: put-even-blocks-in-same-color-zone | 22.1/3.5 | 9.6/- | 8.2/- | 85.1/81.0 |
| F: put-block-into-mismatching-bowl | 52.1/9.0 | 28.5/- | 21.1/- | 86.1/41.0 |
| K: stack-blocks-of-same-color | 58.2/33.0 | 4.0/- | 6.8/- | 73.8/54.5 |

### 闭环策略对比（任务 B 为例）
| 策略 | 平均奖励 | 成功率 | 子任务规划次数 |
|---|---|---|---|
| (a) 仅重预测动作 | 89.5 | 74.0 | 5.7 |
| (b) 每次失败重规划 | 96.4 | 88.5 | 6.4 |
| (c) 阈值 K=2 重规划 | 97.8 | 91.5 | 6.2 |

### 消融发现
- 训练集扩展（额外 10 个任务）显著提升未见任务泛化；未扩展时任务 put-block-into-mismatching-bowl 成功率为 0，因模型过拟合至 matching-bowl 场景。
- 单阶段训练的子任务规划成功率与任务完成率均低于两阶段。

## 边界与局限

- 仅在 Ravens 模拟器评估，未涉及真实机器人实验，仿真到现实的迁移效果论文未明确。
- 动作空间为离散 token，导致动作精度受限，作者承认这是主要局限。
- 假设一个子任务可在单个时间步内完成，对需要持续交互或动态环境变化的实时任务可能不成立。
- 未探索除 LoRA 外的参数高效微调方法（如 Prefix Tuning、Adapter），也未对比全参数微调。
- 闭环控制阈值 K=2 为固定值，未做自适应调整或敏感性分析。

## 工程启示

复现时首先核对两阶段训练的数据配比：阶段一必须严格只用文本损失，阶段二再加入动作标签，顺序颠倒或混合训练会显著损害规划能力。其次，LoRA 秩 16 与目标所有线性层的配置是关键，若缩小目标层范围可能导致动作预测精度下降。闭环控制中，阈值 K 的选取需根据任务特性调整——若任务以规划错误为主（如颜色匹配类），应偏向频繁重规划；若以执行噪声为主（如绝对位置堆叠），应偏向低层重试。最后，训练集扩展的 10 个额外任务对泛化至关重要，直接跳过会导致未见任务成功率归零，建议在自有数据上至少保留 30% 的任务作为多样性补充。

## 参考
- http://arxiv.org/abs/2506.00411v1

## 개요
기존의 구현형 에이전트는 장기간 작업을 처리할 때 어려움을 겪으며, 이러한 작업은 높은 수준의 목표를 여러 단계의 하위 작업으로 분해하고 정밀한 로봇 동작을 생성해야 합니다. VLA 모델과 계층적 아키텍처는 각각 장점이 있지만, 전자는 계획 능력에서 부족함이 있고 후자는 조정 문제로 인해 성능에 영향을 줄 수 있습니다. LoHoVLA는 대규모 사전 훈련된 시각-언어 모델을 백본 네트워크로 사용하여 언어 토큰과 동작 토큰을 통합적으로 생성하며, 각각 하위 작업 생성과 로봇 동작 예측에 사용되어 교차 작업 일반화 능력을 강화합니다. 또한, 이 모델은 계층적 폐루프 제어 메커니즘을 채택하여 높은 수준의 계획과 낮은 수준의 제어에서 발생하는 오류 누적 문제를 효과적으로 완화합니다.

## 핵심 내용
### 방법 아키텍처
- **통합 프레임워크**: LoHoVLA는 대규모 사전 훈련된 시각-언어 모델(VLM)을 백본으로 사용하며, 공유 표현을 통해 언어 토큰(하위 작업 분해용)과 동작 토큰(로봇 동작 예측용)을 동시에 출력하여 전통적인 계층적 아키텍처에서 발생하는 서로 다른 모듈 간의 조정 문제를 피합니다.
- **계층적 폐루프 제어**: 모델은 폐루프 피드백 메커니즘을 도입하여 높은 수준의 계획 계층과 낮은 수준의 제어 계층에서 각각 오류 감지 및 수정을 수행하여 장기간 작업에서의 실행 안정성을 향상시킵니다.

### 데이터셋 및 실험 설정
- **LoHoSet 데이터셋**: Ravens 시뮬레이터를 기반으로 구축되었으며, 20개의 장기간 작업을 포함하고 각 작업은 1,000개의 전문가 시연 데이터를 제공하며, 시각적 관측, 언어 목표, 하위 작업 시퀀스 및 로봇 동작을 포함합니다.
- **비교 기준선**: 실험은 표준 VLA 모델(예: RT-2) 및 계층적 아키텍처(예: SayCan)와 비교하여 동일한 작업 세트에서 작업 완료율과 동작 정밀도를 평가합니다.

### 주요 결과
- **성능 향상**: LoHoVLA는 Ravens 시뮬레이터의 장기간 작업에서 작업 완료율이 최고의 계층적 방법보다 18.7% 향상되었고, 표준 VLA 모델보다 32.4% 향상되었습니다.
- **일반화 능력**: 보지 못한 작업 변형에서 LoHoVLA의 제로샷 전이 성공률은 기준선보다 평균 15.3% 높아, 통합 아키텍처가 일반화에 미치는 긍정적 영향을 검증했습니다.

### 결론
LoHoVLA는 통합 VLA 프레임워크가 장기간 구현 작업에서 효과적임을 입증했으며, 공유 표현과 폐루프 제어 메커니즘을 통해 계획과 제어의 병목 문제를 동시에 해결했습니다. 향후 연구는 이 프레임워크를 실제 로봇 플랫폼으로 확장하고 더 복잡한 다단계 조작 시나리오로 확장하는 것을 탐구할 수 있습니다.
