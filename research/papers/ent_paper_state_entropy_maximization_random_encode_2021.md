---
$id: ent_paper_state_entropy_maximization_random_encode_2021
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: State Entropy Maximization with Random Encoders for Efficient Exploration
  zh: State Entropy Maximization with Random Encoders for Efficient Exploration
  ko: State Entropy Maximization with Random Encoders for Efficient Exploration
summary:
  en: Recent exploration methods have proven to be a recipe for improving sample-efficiency in deep reinforcement learning
    (RL). However, efficient exploration in high-dimensional observation spaces still remains a challenge.
  zh: RE3（Random Encoders for Efficient Exploration）是一种利用状态熵作为内在奖励的深度强化学习探索方法，由研究团队提出。其核心贡献在于使用固定随机初始化的卷积编码器，在低维表示空间中通过k近邻估计状态熵，从而在高维观测环境中实现稳定且计算高效的探索。该方法在DeepMind
    Control Suite和MiniGrid基准上显著提升了无模型和基于模型的强化学习样本效率。
  ko: Recent exploration methods have proven to be a recipe for improving sample-efficiency in deep reinforcement learning
    (RL). However, efficient exploration in high-dimensional observation spaces still remains a challenge.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- state
- entropy
- maximization
- random
- encode
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 146 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2102.09430 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2102.09430v4); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: arXiv:2102.09430 State Entropy Maximization with Random Encoders for Efficient Exploration
  url: https://arxiv.org/abs/2102.09430
  accessed_at: '2026-07-31'
  date: '2021-02-18'
- id: src_002
  type: website
  title: Project page
  url: https://sites.google.com/view/re3-rl
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: Project page
  url: https://github.com/younggyoseo/RE3
  accessed_at: '2026-07-31'
- id: src_004
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---


## 概述

RE3（Random Encoders for Efficient Exploration）提出在随机初始化且训练期间固定的编码器表示空间中，用k-最近邻状态熵估计器计算内在奖励，以高效驱动深度RL探索。该方法由UC Berkeley等机构研究者提出，核心贡献是证明无需任何学习式表示或辅助任务，仅凭卷积架构的归纳偏置即可实现有效的状态熵最大化，显著简化探索机制并提升样本效率。

## 它改变了什么

现有状态熵最大化方法（如Tao et al. 2020、Badia et al. 2020、Liu & Abbeel 2021）虽然在理论上正确，但实践中都依赖学习式潜在表示——要么用动力学模型、逆动力学预测，要么用对比学习。这带来三重代价：多目标优化的不稳定性、额外的超参数调优负担、以及显著的计算开销。RE3真正改变的是“探索不需要学习”这一前提：它用随机固定编码器替代所有学习式表示，把状态熵估计从“需要精心训练的辅助模块”降维成“一个前向传播加k-NN查询”。这不仅是工程简化，更在概念上把探索问题从“学什么表示”重新拉回“如何用已有结构先验”，直接回应了Ulyanov et al. (2018)和Caron et al. (2018)关于卷积网络本身是强归纳偏置的发现。

## 方法拆解

### 核心公式链
- **k-NN熵估计器**（公式1）：  
  Ĥ^N_k(X) = (1/N) Σ_{i=1}^N log(N · ||x_i − x_i^k-NN||_2^q · π̂^(q/2) / (k · Γ(q/2 + 1))) + C_k  
  其中C_k = log k − Ψ(k)为偏差校正，Ψ为digamma函数。关键洞察：对任意q > 0，该式正比于(1/N)Σ log||x_i − x_i^k-NN||_2（公式2），因此维度q不影响相对排序。
- **内在奖励**（公式3）：  
  r^i(s_i) := log(||y_i − y_i^k-NN||_2 + 1)，y_i = f_θ(s_i)为随机编码器输出。
- **总奖励**（公式4）：  
  r^total_j := r^e(s_j, a_j) + β_t · r^i(s_j)，β_t = β_0(1 − ρ)^t为指数衰减。

### 关键设计决策
1. **固定随机编码器**：距离在固定表示空间中不随训练变化，内在奖励信号稳定，避免学习表示带来的非平稳性。
2. **回放缓冲区存储低维表示y**：避免每次更新都过编码器处理高维状态，计算开销从“每步全前向”降为“仅存储时一次前向”。
3. **全缓冲区k-NN搜索**：与on-policy样本（Mutti et al. 2021）或小批量（Liu & Abbeel 2021）相比，全缓冲区搜索提供更稳定、精确的熵估计。
4. **独立RL编码器**：策略优化用的编码器与随机熵估计编码器分离，避免探索信号干扰任务表征学习。

### 两种使用模式
- **在线RL**：外在奖励引导任务解决，内在奖励叠加驱动探索。
- **无监督预训练**：r^e = 0时纯靠内在奖励探索，预训练策略可微调提升下游任务样本效率（DeepMind Control仅需500K步，对比Liu & Abbeel的5M步）。

### 算法流程（Off-policy版本）
1. 初始化随机编码器θ、策略φ、回放缓冲区ℬ。
2. 每步收集(s_t, a_t, s_{t+1}, r^e_t)，计算y_t = f_θ(s_t)存入ℬ。
3. 采样小批量，对每个样本计算与ℬ中所有y的L2距离，找k-NN。
4. 计算内在奖励，叠加衰减后的β_t，更新策略。

## 关键创新

1. **零学习式表示的熵估计**：首次证明随机固定编码器足以支撑状态熵最大化，彻底消除辅助任务（动力学、逆动力学、对比学习）带来的多目标优化和超参数调优负担。这是对“探索需要学习表示”这一隐含假设的直接挑战。
2. **计算效率的数量级提升**：通过预存表示避免重复编码，FLOPs分析显示RAD + SE w/ Random在Hopper Hop达到500K步性能需7.233e+16 FLOPs，而逆动力学方案约需两倍；整体计算开销与纯RAD相当，使熵探索首次成为“免费午餐”。
3. **架构先验的显式利用**：将卷积网络的强归纳偏置（Ulyanov et al. 2018）转化为可操作的探索工具，并验证其对初始化方案（Xavier/He/Orthogonal）和k值（1-9）的鲁棒性，表明该方法不依赖特定架构微调。

## 实验与结果

### DeepMind Control Suite（5次运行均值±标准差）
| 对比 | 关键结果 |
|------|----------|
| RAD + RE3 vs RAD/DrQ | Cheetah Run Sparse上RE3达601.6平均回报，RAD和DrQ均未解决 |
| SE w/ Random vs SE w/ Contrastive/Inverse/ImageNet | Random一致优于所有学习式表示方案 |
| RE3 vs RND/ICM | 大多数任务RE3更优 |
| 计算效率 | Hopper Hop 500K步：Random需7.233e+16 FLOPs，Inverse约2倍；Random与纯RAD相当 |

### MiniGrid（5次运行）
- DoorKey-8x8：2.4M步时A2C + RE3达0.49平均回报，A2C + ICM仅0.20，A2C + RND和A2C均未获非平凡回报。
- 预训练：Empty-16x16上100K步预训练后微调，A2C + RE3 + PT显著提升样本效率。
- 对比学习在MiniGrid失效：因依赖图像特定增强（随机平移、颜色抖动），与紧凑嵌入不兼容。

### Atari（3次运行）
- RE3在Montezuma's Revenge等硬探索游戏上优于Rainbow和Rainbow + SE w/ Contrastive，人类归一化分数平均值见图13(c)。

### 鲁棒性
- 随机编码器集成提升对高斯噪声和颜色变化的鲁棒性（图10(a)(b)）。
- k ∈ {1,3,5,7,9}和不同初始化方案下表现稳健（图10(c)(d)）。

## 边界与局限

作者明确不声称表示学习或额外模型对探索不需要，仅展示固定随机编码器的实用性。未验证的场景包括：真实机器人硬件、三维物理仿真（Isaac Gym、Manipulation任务）。对于更复杂视觉领域，作者承认预训练固定表示可能更有用，但留作未来工作——因为需要访问环境和广泛状态分布进行预训练，这本身是非平凡问题。论文未提及推理频率、硬件配置等工程细节。超参数搜索在MiniGrid和Atari中报告最佳结果，可能存在过拟合特定任务的风险。

## 工程启示

复现时优先核对三点：**①随机编码器架构必须与底层RL编码器一致**（DeepMind Control用4层卷积+32通道+50维输出，Dreamer用4层卷积+通道{32,64,128,256}，Atari用3层卷积+线性层降维），架构不匹配会破坏归纳偏置假设；**②k-NN搜索范围**——模型无关RL用全缓冲区，Dreamer因轨迹段训练用k=50且仅在minibatch内搜索，MiniGrid用10K样本缓冲区，范围选错会显著影响熵估计稳定性；**③β_t衰减调度**——ρ=0（无衰减）在Dreamer中可行，但模型无关RL中衰减（ρ∈{0.00001, 0.000025}）经验性稳定性能，直接照搬ρ=0可能训练后期探索噪声过大。最容易踩坑的是对比学习基线在MiniGrid上的失效——如果下游任务观测不是自然图像，不要假设对比学习表示可用，RE3的随机编码器反而更稳。另外注意DeepMind Control的Walker/Cheetah Run Sparse是自行设计任务（奖励阈值0.25），复现时需确认环境版本一致。

## 参考
- https://arxiv.org/abs/2102.09430
- https://sites.google.com/view/re3-rl
- https://github.com/younggyoseo/RE3
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## Overview

RE3 (Random Encoders for Efficient Exploration) proposes computing intrinsic rewards using a k-nearest-neighbor state entropy estimator in a representation space defined by randomly initialized and fixed encoders, efficiently driving exploration in deep RL. Proposed by researchers from UC Berkeley and other institutions, the core contribution is demonstrating that effective state entropy maximization can be achieved without any learned representations or auxiliary tasks, relying solely on the inductive bias of convolutional architectures. This significantly simplifies the exploration mechanism and improves sample efficiency.

## What It Changes

Existing state entropy maximization methods (e.g., Tao et al. 2020, Badia et al. 2020, Liu & Abbeel 2021) are theoretically sound but rely on learned latent representations in practice—whether through dynamics models, inverse dynamics prediction, or contrastive learning. This incurs three costs: instability from multi-objective optimization, additional hyperparameter tuning burden, and significant computational overhead. What RE3 truly changes is the premise that "exploration does not require learning": it replaces all learned representations with random fixed encoders, reducing state entropy estimation from "an auxiliary module requiring careful training" to "a forward pass plus a k-NN query." This is not merely an engineering simplification but conceptually reframes the exploration problem from "what representation to learn" back to "how to leverage existing structural priors," directly responding to findings by Ulyanov et al. (2018) and Caron et al. (2018) that convolutional networks themselves are strong inductive biases.

## Method Breakdown

### Core Formula Chain
- **k-NN entropy estimator** (Equation 1):  
  Ĥ^N_k(X) = (1/N) Σ_{i=1}^N log(N · ||x_i − x_i^k-NN||_2^q · π̂^(q/2) / (k · Γ(q/2 + 1))) + C_k  
  where C_k = log k − Ψ(k) is the bias correction and Ψ is the digamma function. Key insight: for any q > 0, this is proportional to (1/N)Σ log||x_i − x_i^k-NN||_2 (Equation 2), so the dimension q does not affect relative ordering.
- **Intrinsic reward** (Equation 3):  
  r^i(s_i) := log(||y_i − y_i^k-NN||_2 + 1), where y_i = f_θ(s_i) is the random encoder output.
- **Total reward** (Equation 4):  
  r^total_j := r^e(s_j, a_j) + β_t · r^i(s_j), with β_t = β_0(1 − ρ)^t as exponential decay.

### Key Design Decisions
1. **Fixed random encoder**: Distances in the fixed representation space do not change during training, providing stable intrinsic reward signals and avoiding non-stationarity from learned representations.
2. **Storing low-dimensional representations y in the replay buffer**: Avoids passing high-dimensional states through the encoder at every update, reducing computational cost from "full forward pass per step" to "one forward pass only at storage time."
3. **Full-buffer k-NN search**: Compared to on-policy samples (Mutti et al. 2021) or minibatches (Liu & Abbeel 2021), full-buffer search provides more stable and accurate entropy estimation.
4. **Separate RL encoder**: The encoder used for policy optimization is decoupled from the random entropy estimation encoder, preventing exploration signals from interfering with task representation learning.

### Two Usage Modes
- **Online RL**: Extrinsic rewards guide task solving, while intrinsic rewards are superimposed to drive exploration.
- **Unsupervised pretraining**: With r^e = 0, exploration relies purely on intrinsic rewards; the pretrained policy can be fine-tuned to improve sample efficiency on downstream tasks (DeepMind Control requires only 500K steps, compared to 5M steps for Liu & Abbeel).

### Algorithm Flow (Off-policy Version)
1. Initialize random encoder θ, policy φ, and replay buffer ℬ.
2. At each step, collect (s_t, a_t, s_{t+1}, r^e_t), compute y_t = f_θ(s_t), and store in ℬ.
3. Sample a minibatch; for each sample, compute L2 distances to all y in ℬ and find k-NN.
4. Compute intrinsic rewards, add the decayed β_t, and update the policy.

## Key Innovations

1. **Entropy estimation with zero learned representations**: First to prove that random fixed encoders suffice for state entropy maximization, completely eliminating the multi-objective optimization and hyperparameter tuning burden from auxiliary tasks (dynamics, inverse dynamics, contrastive learning). This directly challenges the implicit assumption that "exploration requires learned representations."
2. **Order-of-magnitude computational efficiency gains**: By pre-storing representations to avoid repeated encoding, FLOPs analysis shows RAD + SE w/ Random reaches 500K-step performance on Hopper Hop with 7.233e+16 FLOPs, while the inverse dynamics approach requires roughly twice that; overall computational cost is comparable to pure RAD, making entropy exploration a "free lunch" for the first time.
3. **Explicit use of architectural priors**: Converts the strong inductive bias of convolutional networks (Ulyanov et al. 2018) into an actionable exploration tool, and validates robustness to initialization schemes (Xavier/He/Orthogonal) and k values (1–9), indicating the method does not depend on specific architectural tuning.

## Experiments and Results

### DeepMind Control Suite (mean ± std over 5 runs)
| Comparison | Key Results |
|------|----------|
| RAD + RE3 vs RAD/DrQ | On Cheetah Run Sparse, RE3 achieves 601.6 average return; RAD and DrQ fail to solve it |
| SE w/ Random vs SE w/ Contrastive/Inverse/ImageNet | Random consistently outperforms all learned representation schemes |
| RE3 vs RND/ICM | RE3 is superior on most tasks |
| Computational efficiency | Hopper Hop at 500K steps: Random requires 7.233e+16 FLOPs, Inverse about 2×; Random is comparable to pure RAD |

### MiniGrid (5 runs)
- DoorKey-8x8: At 2.4M steps, A2C + RE3 achieves 0.49 average return, A2C + ICM only 0.20, while A2C + RND and A2C obtain no non-trivial returns.
- Pretraining: After 100K steps of pretraining on Empty-16x16 followed by fine-tuning, A2C + RE3 + PT significantly improves sample efficiency.
- Contrastive learning fails on MiniGrid: It relies on image-specific augmentations (random translation, color jitter) that are incompatible with compact embeddings.

### Atari (3 runs)
- RE3 outperforms Rainbow and Rainbow + SE w/ Contrastive on hard-exploration games like Montezuma's Revenge; human-normalized scores averaged in Figure 13(c).

### Robustness
- Ensembling random encoders improves robustness to Gaussian noise and color changes (Figures 10(a)(b)).
- Performance is stable across k ∈ {1,3,5,7,9} and different initialization schemes (Figures 10(c)(d)).

## Boundaries and Limitations

The authors explicitly do not claim that representation learning or additional models are unnecessary for exploration; they only demonstrate the utility of fixed random encoders. Unverified scenarios include real robot hardware and 3D physics simulations (Isaac Gym, Manipulation tasks). For more complex visual domains, the authors acknowledge that pretrained fixed representations may be more useful but leave this for future work—because it requires access to the environment and broad state distributions for pretraining, which is itself a non-trivial problem. The paper does not mention inference frequency, hardware configuration, or other engineering details. Hyperparameter search reports best results on MiniGrid and Atari, with potential risk of overfitting to specific tasks.

## Engineering Insights

When reproducing, prioritize checking three points: **①The random encoder architecture must match the underlying RL encoder** (DeepMind Control uses 4 convolutional layers with 32 channels and a 50-dimensional output; Dreamer uses 4 convolutional layers with channels {32,64,128,256}; Atari uses 3 convolutional layers plus a linear layer for dimensionality reduction)—architectural mismatch can break the inductive bias assumption; **②The k-NN search scope**—model-agnostic RL uses the full buffer, Dreamer uses k=50 with search only within minibatches due to trajectory-segment training, and MiniGrid uses a 10K-sample buffer; choosing the wrong scope can significantly affect entropy estimation stability; **③The β_t decay schedule**—ρ=0 (no decay) works in Dreamer, but in model-agnostic RL, decay (ρ∈{0.00001, 0.000025}) empirically stabilizes performance; directly copying ρ=0 may cause excessive exploration noise in later training. The most common pitfall is the failure of contrastive learning baselines on MiniGrid—if downstream task observations are not natural images, do not assume contrastive learning representations are usable; RE3's random encoder is more robust instead. Also note that Walker/Cheetah Run Sparse in DeepMind Control are custom-designed tasks (reward threshold 0.25); verify environment version consistency when reproducing.

## 개요

RE3(Random Encoders for Efficient Exploration)는 무작위 초기화되고 훈련 중 고정된 인코더 표현 공간에서 k-최근접 이웃 상태 엔트로피 추정기를 사용하여 내재적 보상을 계산함으로써 심층 RL 탐색을 효율적으로 구동하는 방법을 제안합니다. 이 방법은 UC Berkeley 등 기관의 연구자들이 제안했으며, 핵심 기여는 학습된 표현이나 보조 작업 없이도 컨볼루션 아키텍처의 귀납적 편향만으로 효과적인 상태 엔트로피 최대화가 가능함을 증명하여 탐색 메커니즘을 크게 단순화하고 샘플 효율성을 향상시킨다는 점입니다.

## 무엇을 바꾸었는가

기존 상태 엔트로피 최대화 방법(Tao et al. 2020, Badia et al. 2020, Liu & Abbeel 2021)은 이론적으로는 정확하지만 실제로는 학습된 잠재 표현에 의존합니다——동역학 모델, 역동역학 예측 또는 대조 학습을 사용합니다. 이는 세 가지 비용을 초래합니다: 다중 목표 최적화의 불안정성, 추가 하이퍼파라미터 튜닝 부담, 그리고 상당한 계산 오버헤드입니다. RE3가 실제로 바꾼 것은 "탐색에는 학습이 필요하지 않다"는 전제입니다: 모든 학습된 표현을 무작위 고정 인코더로 대체하여 상태 엔트로피 추정을 "정교한 훈련이 필요한 보조 모듈"에서 "단일 순전파와 k-NN 쿼리"로 축소했습니다. 이는 단순한 엔지니어링 단순화일 뿐만 아니라, 개념적으로 탐색 문제를 "무엇을 학습할 표현인가"에서 "기존 구조적 사전 지식을 어떻게 활용할 것인가"로 되돌려 놓았으며, Ulyanov et al. (2018)과 Caron et al. (2018)이 발견한 컨볼루션 네트워크 자체가 강력한 귀납적 편향이라는 사실에 직접적으로 대응합니다.

## 방법 분해

### 핵심 공식 체인
- **k-NN 엔트로피 추정기** (수식 1):  
  Ĥ^N_k(X) = (1/N) Σ_{i=1}^N log(N · ||x_i − x_i^k-NN||_2^q · π̂^(q/2) / (k · Γ(q/2 + 1))) + C_k  
  여기서 C_k = log k − Ψ(k)는 편향 보정이고, Ψ는 digamma 함수입니다. 핵심 통찰: 임의의 q > 0에 대해 이 식은 (1/N)Σ log||x_i − x_i^k-NN||_2 (수식 2)에 비례하므로 차원 q는 상대적 순서에 영향을 미치지 않습니다.
- **내재적 보상** (수식 3):  
  r^i(s_i) := log(||y_i − y_i^k-NN||_2 + 1), y_i = f_θ(s_i)는 무작위 인코더 출력입니다.
- **총 보상** (수식 4):  
  r^total_j := r^e(s_j, a_j) + β_t · r^i(s_j), β_t = β_0(1 − ρ)^t는 지수 감쇠입니다.

### 핵심 설계 결정
1. **고정 무작위 인코더**: 거리가 고정된 표현 공간에서 훈련 중 변하지 않으므로 내재적 보상 신호가 안정적이며, 학습된 표현으로 인한 비정상성을 피할 수 있습니다.
2. **리플레이 버퍼에 저차원 표현 y 저장**: 매 업데이트마다 고차원 상태를 인코더로 처리할 필요가 없어 계산 비용이 "매 단계 전체 순전파"에서 "저장 시 1회 순전파"로 줄어듭니다.
3. **전체 버퍼 k-NN 검색**: on-policy 샘플(Mutti et al. 2021)이나 미니배치(Liu & Abbeel 2021)와 비교하여 전체 버퍼 검색이 더 안정적이고 정확한 엔트로피 추정을 제공합니다.
4. **독립 RL 인코더**: 정책 최적화용 인코더와 무작위 엔트로피 추정 인코더를 분리하여 탐색 신호가 작업 표현 학습을 방해하지 않도록 합니다.

### 두 가지 사용 모드
- **온라인 RL**: 외재적 보상이 작업 해결을 유도하고, 내재적 보상이 중첩되어 탐색을 구동합니다.
- **비지도 사전 훈련**: r^e = 0일 때 순수 내재적 보상으로 탐색하며, 사전 훈련된 정책을 미세 조정하여 하류 작업 샘플 효율성을 향상시킬 수 있습니다 (DeepMind Control에서 500K 스텝만 필요, Liu & Abbeel의 5M 스텝과 대조).

### 알고리즘 흐름 (Off-policy 버전)
1. 무작위 인코더 θ, 정책 φ, 리플레이 버퍼 ℬ 초기화.
2. 매 단계 (s_t, a_t, s_{t+1}, r^e_t) 수집, y_t = f_θ(s_t) 계산하여 ℬ에 저장.
3. 미니배치 샘플링, 각 샘플에 대해 ℬ의 모든 y와 L2 거리 계산, k-NN 탐색.
4. 내재적 보상 계산, 감쇠된 β_t 중첩, 정책 업데이트.

## 핵심 혁신

1. **학습된 표현이 없는 엔트로피 추정**: 무작위 고정 인코더만으로 상태 엔트로피 최대화를 지원할 수 있음을 처음으로 증명하여, 보조 작업(동역학, 역동역학, 대조 학습)으로 인한 다중 목표 최적화 및 하이퍼파라미터 튜닝 부담을 완전히 제거했습니다. 이는 "탐색에는 표현 학습이 필요하다"는 암묵적 가정에 대한 직접적인 도전입니다.
2. **계산 효율성의 자릿수 향상**: 사전 저장된 표현으로 반복 인코딩을 피하며, FLOPs 분석에 따르면 RAD + SE w/ Random이 Hopper Hop에서 500K 스텝 성능에 도달하는 데 7.233e+16 FLOPs가 필요한 반면, 역동역학 방식은 약 2배가 필요합니다; 전체 계산 오버헤드는 순수 RAD와 동등하여 엔트로피 탐색을 처음으로 "공짜 점심"으로 만들었습니다.
3. **아키텍처 사전 지식의 명시적 활용**: 컨볼루션 네트워크의 강력한 귀납적 편향(Ulyanov et al. 2018)을 실행 가능한 탐색 도구로 전환하고, 초기화 방식(Xavier/He/Orthogonal)과 k 값(1-9)에 대한 견고성을 검증하여 특정 아키텍처 미세 조정에 의존하지 않음을 보여줍니다.

## 실험 및 결과

### DeepMind Control Suite (5회 실행 평균 ± 표준편차)
| 비교 | 핵심 결과 |
|------|----------|
| RAD + RE3 vs RAD/DrQ | Cheetah Run Sparse에서 RE3가 601.6 평균 보상 달성, RAD와 DrQ는 모두 해결 실패 |
| SE w/ Random vs SE w/ Contrastive/Inverse/ImageNet | Random이 모든 학습된 표현 방식보다 일관되게 우수 |
| RE3 vs RND/ICM | 대부분의 작업에서 RE3가 더 우수 |
| 계산 효율성 | Hopper Hop 500K 스텝: Random은 7.233e+16 FLOPs 필요, Inverse는 약 2배; Random은 순수 RAD와 동등 |

### MiniGrid (5회 실행)
- DoorKey-8x8: 2.4M 스텝에서 A2C + RE3가 0.49 평균 보상 달성, A2C + ICM은 0.20에 불과, A2C + RND와 A2C는 모두 비자명한 보상 획득 실패.
- 사전 훈련: Empty-16x16에서 100K 스텝 사전 훈련 후 미세 조정, A2C + RE3 + PT가 샘플 효율성을 크게 향상.
- 대조 학습은 MiniGrid에서 실패: 이미지 특정 증강(무작위 이동, 색상 지터)에 의존하여 컴팩트 임베딩과 호환되지 않음.

### Atari (3회 실행)
- RE3는 Montezuma's Revenge 등 하드 탐색 게임에서 Rainbow 및 Rainbow + SE w/ Contrastive보다 우수, 인간 정규화 점수 평균은 그림 13(c) 참조.

### 견고성
- 무작위 인코더 앙상블이 가우시안 노이즈 및 색상 변화에 대한 견고성을 향상 (그림 10(a)(b)).
- k ∈ {1,3,5,7,9} 및 다양한 초기화 방식에서 안정적 성능 (그림 10(c)(d)).

## 경계 및 한계

저자들은 표현 학습이나 추가 모델이 탐색에 불필요하다고 주장하지 않으며, 고정 무작위 인코더의 실용성만을 보여줍니다. 검증되지 않은 시나리오: 실제 로봇 하드웨어, 3D 물리 시뮬레이션(Isaac Gym, Manipulation 작업). 더 복잡한 시각 영역에 대해 저자들은 사전 훈련된 고정 표현이 더 유용할 수 있음을 인정하지만, 환경 접근과 광범위한 상태 분포 사전 훈련이 필요하여 그 자체로 비자명한 문제이므로 향후 작업으로 남겨둡니다. 논문은 추론 빈도, 하드웨어 구성 등의 엔지니어링 세부 사항을 언급하지 않습니다. 하이퍼파라미터 검색은 MiniGrid와 Atari에서 최상의 결과를 보고하며, 특정 작업에 대한 과적합 위험이 있을 수 있습니다.

## 엔지니어링 시사점

재현 시 다음 세 가지를 우선 확인하세요: **①무작위 인코더 아키텍처는 기본 RL 인코더와 일치해야 합니다** (DeepMind Control은 4층 컨볼루션 + 32채널 + 50차원 출력, Dreamer는 4층 컨볼루션 + 채널 {32,64,128,256}, Atari는 3층 컨볼루션 + 선형 레이어 차원 축소), 아키텍처 불일치는 귀납적 편향 가정을 깨뜨립니다; **②k-NN 검색 범위**——모델 무관 RL은 전체 버퍼를 사용하고, Dreamer는 궤적 세그먼트 훈련으로 k=50이며 미니배치 내에서만 검색, MiniGrid는 10K 샘플 버퍼를 사용, 범위 선택 오류는 엔트로피 추정 안정성에 크게 영향을 미칩니다; **③β_t 감쇠 스케줄**——ρ=0(감쇠 없음)은 Dreamer에서 가능하지만, 모델 무관 RL에서는 감쇠(ρ∈{0.00001, 0.000025})가 경험적으로 성능을 안정화하며, ρ=0을 그대로 적용하면 훈련 후반에 탐색 노이즈가 과도할 수 있습니다. 가장 쉽게 함정에 빠지는 부분은 MiniGrid에서 대조 학습 기준선의 실패입니다——하류 작업 관측이 자연 이미지가 아니라면 대조 학습 표현이 사용 가능하다고 가정하지 마세요, RE3의 무작위 인코더가 더 안정적입니다. 또한 DeepMind Control의 Walker/Cheetah Run Sparse는 자체 설계 작업(보상 임계값 0.25)이므로 재현 시 환경 버전 일치를 확인해야 합니다.
