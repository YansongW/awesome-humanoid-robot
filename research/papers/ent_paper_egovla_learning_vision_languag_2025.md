---
$id: ent_paper_egovla_learning_vision_languag_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'EgoVLA: Learning Vision-Language-Action Models from Egocentric Human Videos'
  zh: EgoVLA｜从第一视角的人类视频中学习视觉语言动作模型
  ko: 'EgoVLA: Learning Vision-Language-Action Models from Egocentric Human Videos'
summary:
  en: The implementation path of egofla is to first encode language instructions, camera images/multi-view observations, ontological
    states, and joint sequences into multimodal representations, which can be used to generate multimodal representations,
    then the whole-body trajectory/action sequence, end-effector/wrist-hand target were predicted with ACT/behavior clone
    imitation learning, VLA multimodal action model, latent variable/action token. The key point is to retain the semantic
    understanding of VLM, while increasing the robot state and action head, so as to avoid remaining in the language planning.
  zh: EgoVLA 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用ACT/行为克隆模仿学习、VLA 多模态动作模型、潜变量/动作 token预测全身轨迹/动作序列、末端执行器/腕手目标。关键点是保留
    VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: EgoVLA 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用ACT/行为克隆模仿学习、VLA 多模态动作模型、潜变量/动作 token预测全身轨迹/动作序列、末端执行器/腕手目标。关键点是保留
    VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- egocentric_video
- egovla
- first_person_video
- imitation_learning
- vla
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. [2026-08-05]
    body rewritten as full-text six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline. | WP1 dedup merge 2026-08-06: merged ent_paper_egovla_learning_vision_languag_2025
    into this card (rules: same_title_same_year). Backup+manifest: .staging/cleanup_wp12/.'
sources:
- id: src_001
  type: website
  title: EgoVLA project page
  url: https://rchalyang.github.io/EgoVLA/
  date: '2025'
  accessed_at: '2026-06-26'
- id: src_002
  type: paper
  title: 'EgoVLA: Learning Vision-Language-Action Models from Egocentric Human Videos'
  url: https://arxiv.org/abs/2507.12440
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

EgoVLA 提出了一种从第一人称人类视频中学习视觉-语言-动作模型（VLA）的新范式，利用约 50 万图像-动作对预训练，再通过少量机器人演示微调实现跨具身灵巧操作。其核心贡献在于证明了人类动作空间与机器人动作空间可通过统一 MANO 参数化近似，从而突破机器人数据采集的规模瓶颈，在 Isaac 仿真基准上显著超越 ACT 基线。

## 它改变了什么

这项工作的真正改变在于将 VLA 训练的数据源头从昂贵的机器人遥操作转向了大规模、低成本的人类 egocentric 视频。此前 OpenVLA、Octo 等模型受限于机器人硬件和专家操作员，数据规模存在根本性天花板；而人类视频天然具备场景与任务的丰富性，且作者通过几何变换论证了人类与机器人动作空间的近似性，使得“人类数据预训练 + 少量机器人数据后训练”成为可行路径。这不仅是数据来源的替换，更重新定义了 VLA 的可扩展性边界——数据获取不再与机器人硬件绑定，而是与人类日常活动绑定。

另一个关键改变在于动作空间的统一设计。EgoVLA 没有直接预测机器人关节命令，而是将人类手腕姿态和 MANO 手部参数作为中间表示，通过重定向 MLP 映射到具体机器人。这种“人类为中心”的动作抽象使得预训练阶段完全无需接触机器人，后训练阶段也只需中等量演示即可适配，避免了以往跨具身迁移中复杂的动作空间对齐问题。

## 方法拆解

### 数据构建与预处理
- 整合 HOI4D（4000 视频）、HOT3D（833 分钟）、HoloAssist（166 小时，1/10 采样）、TACO（2317 序列）四个 egocentric 数据集，共约 50 万图像-动作对
- 使用世界坐标系相机姿态将未来手腕位置投影到当前相机帧，保证监督一致性；以 3 FPS 采样 RGB 观测平衡效率与连续性
- HOT3D 无语言标注，使用占位指令；HoloAssist 手部姿态重定向到 MANO 表示，统一采用 MANO 平均手型

### 模型架构
- 基于 NVILA-2B VLM 骨干，输入当前及历史 6 帧 RGB（384×384，0.2 秒间隔，覆盖 1 秒历史）、语言指令、动作查询 token、人类本体感觉（手腕平移/旋转 + MANO 手部参数，经 MLP 处理）
- 动作头为 300M 参数 transformer：6 层编码器，隐藏尺寸 1536，输入本体感觉与动作查询 token 的潜在嵌入，预测 1 秒视野（30 Hz × 30 步）内双手动作序列
- 动作空间：手腕 3D 平移 + rot6D 旋转（相机坐标系）+ MANO 前 15 个 PCA 分量（15 球关节、45 DOF 降维）

### 训练与损失
- 损失函数：L = 20.0·L_wrist_trans + 5.0·L_wrist_rot + 5.0·L_joint，其中 L_wrist_trans 和 L_joint 为 L2 损失，L_wrist_rot 为 rot6D 旋转损失
- 两阶段训练：人类数据预训练 20 epochs（lr=1e-4，Cosine 调度），机器人演示后训练 115 epochs（lr=2e-5，100 epochs 后降至 2e-6，Constant 调度），全模型微调
- 部署时采用 ALOHA 动作分块，平滑参数 0.8

### 跨具身迁移
- 机器人数据重定向：优化 MANO 参数最小化指尖位置差异，minimize_Θ (1/5)Σ SmoothL1(J_pred(Θ)_i, J_obs,i)，Θ∈R^15
- 部署映射：手腕姿态经 3D 变换转为末端执行器姿态，通过 IK 求解手臂关节角；手部通过 MANO 计算 3D 关键点，四层 MLP（隐藏层 [64,128,64]）预测机器人手部关节命令

## 关键创新

1. **人类视频作为 VLA 预训练数据源**：首次系统性地将大规模 egocentric 人类视频用于 VLA 预训练，并验证了其与机器人动作空间的几何近似性。这一创新打破了机器人数据采集的硬件依赖，使数据规模可随人类视频库增长而扩展。

2. **统一 MANO 动作空间实现跨具身迁移**：通过 MANO 参数化统一人类与机器人手部动作，配合指尖位置优化的重定向策略，实现了从人类到 Unitree H1 + Inspire 灵巧手的有效迁移。指尖误差仅 5×10^-5 m，证明该抽象精度足够高。

3. **两阶段训练范式的有效性验证**：预训练（人类视频）+ 后训练（少量机器人演示）的范式在短视野任务上达到 77.78% SR（Seen），长视野任务 45.93% SR，远超 ACT 基线（24.87% / 2.22%），且泛化到 Unseen 背景时性能下降显著小于无预训练版本。

## 实验与结果

### 基准与设置
- Isaac Humanoid Manipulation Benchmark：12 任务（7 短视野 + 5 长视野），每任务 100 个成功演示，episode 长度 100-500 帧
- 评估配置：Seen 背景每任务 27 次 rollout（3 背景 × 9 episodes）；Unseen 背景每任务 66 次 rollout（22 背景 × 3 episodes）；对象位置随机化最大 20 cm × 20 cm

### 关键结果对比

| 配置 | 方法 | 短视野 SR | 短视野 PSR | 长视野 SR | 长视野 PSR |
|------|------|-----------|------------|-----------|------------|
| Seen | ACT | 24.87% | 59.79% | 2.22% | 26.47% |
| Seen | EgoVLA-NoPretrain | 64.55% | 71.87% | 26.67% | 54.93% |
| Seen | EgoVLA (50%) | 48.15% | 61.73% | 7.41% | 39.70% |
| Seen | EgoVLA | **77.78%** | **84.92%** | **45.93%** | **80.78%** |
| Unseen | ACT | 24.89% | 54.22% | 0.61% | 23.51% |
| Unseen | EgoVLA-NoPretrain | 51.28% | 62.63% | 11.21% | 36.20% |
| Unseen | EgoVLA | **69.11%** | **76.26%** | **28.79%** | **69.11%** |

### 关键发现
- 预训练增益显著：长视野任务 Seen 配置下，EgoVLA 较 NoPretrain 提升 19.26 个百分点 SR；数据规模减半（50%）导致长视野 SR 从 45.93% 骤降至 7.41%，说明预训练数据量对复杂任务至关重要
- 泛化能力：Unseen 背景下 NoPretrain 平均成功率下降 23%，EgoVLA 仅小幅下降，预训练显著增强视觉鲁棒性
- 零样本部署：不进行机器人数据微调时所有任务成功率 0%，说明跨具身迁移仍需中等量机器人演示
- 人类动作建模：手腕平移平均未来预测误差约 8 cm，2D 投影归一化误差约 0.13，与 HOI-forecast SOTA 相当

## 边界与局限

- 预训练框架依赖带手部和手腕姿态标注的人类数据，限制了数据可用性；作者认为 AR/VR 设备（Quest 3, Vision Pro, Aria Glasses）普及会缓解此约束，但当前仍需额外标注或设备采集
- 无法零样本部署，仍需中等量机器人数据微调；作者承认未来需探索更具身无关的预训练以提升零样本迁移能力
- 基准仅用于受控可复现评估（类似 LIBERO 和 SIMPLER），不直接用于 sim-to-real 迁移；论文未提及真实机器人部署实验
- 人类动作预测误差约 8 cm 在精细操作场景可能成为瓶颈，论文未明确该误差对灵巧操作成功率的具体影响

## 工程启示

- **复现优先级**：先核对数据预处理中的相机坐标系投影逻辑——未来手腕位置投影到当前帧是监督一致性的关键，投影错误会导致训练信号混乱；建议严格遵循世界坐标系变换流程
- **动作空间选择**：MANO 前 15 个 PCA 分量是跨具身迁移的核心抽象，若下游机器人手部自由度差异大，需重新设计重定向 MLP 的输入输出维度；指尖位置优化目标（SmoothL1）对重定向精度至关重要
- **训练配置注意**：预训练 20 epochs + 后训练 115 epochs 的配置在 32 块 A100 上完成，若资源有限可先尝试减半数据量观察长视野任务性能（50% 数据导致 SR 从 45.93% 降至 7.41%，长视野任务对数据量极其敏感）
- **部署踩坑点**：动作分块平滑参数 0.8 和 30 Hz 推理频率需与仿真控制频率严格匹配（simulation decimation = render interval = 1/(30*dt)）；IK 求解手臂关节角时需注意手腕姿态的坐标系变换，避免旋转顺序错误
- **对比基线注意**：ACT 基线使用 DinoV2 骨干和 50000 epochs 训练，动作块大小因任务而异（Close-Drawer 为 64，其余 128），复现时需严格遵循这些细节否则对比不公平

## 参考

- EgoVLA project page (https://rchalyang.github.io/EgoVLA/)

## Overview

EgoVLA proposes a new paradigm for learning Vision-Language-Action models (VLA) from first-person human videos, pretraining on approximately 500K image-action pairs and then fine-tuning with a small number of robot demonstrations to achieve cross-embodiment dexterous manipulation. Its core contribution lies in demonstrating that the human action space and robot action space can be approximated through a unified MANO parameterization, thereby breaking through the scale bottleneck of robot data collection and significantly surpassing the ACT baseline on the Isaac simulation benchmark.

## What It Changes

The real change brought by this work is shifting the data source for VLA training from expensive robot teleoperation to large-scale, low-cost human egocentric videos. Previously, models such as OpenVLA and Octo were limited by robot hardware and expert operators, with a fundamental ceiling on data scale; human videos naturally possess richness in scenes and tasks, and the authors demonstrated the approximation between human and robot action spaces through geometric transformations, making "human data pretraining + small robot data post-training" a feasible path. This is not merely a replacement of data sources but redefines the scalability boundary of VLA—data acquisition is no longer tied to robot hardware but to human daily activities.

Another key change lies in the unified design of the action space. EgoVLA does not directly predict robot joint commands but instead uses human wrist poses and MANO hand parameters as intermediate representations, mapped to specific robots through a redirection MLP. This "human-centric" action abstraction ensures that the pretraining phase requires no contact with robots at all, and the post-training phase only needs a moderate amount of demonstrations for adaptation, avoiding the complex action space alignment issues in previous cross-embodiment transfer.

## Method Breakdown

### Data Construction and Preprocessing
- Integrates four egocentric datasets: HOI4D (4000 videos), HOT3D (833 minutes), HoloAssist (166 hours, 1/10 sampling), and TACO (2317 sequences), totaling approximately 500K image-action pairs
- Uses world-coordinate camera poses to project future wrist positions into the current camera frame, ensuring supervision consistency; samples RGB observations at 3 FPS to balance efficiency and continuity
- HOT3D lacks language annotations and uses placeholder instructions; HoloAssist hand poses are redirected to MANO representation, uniformly adopting the MANO mean hand shape

### Model Architecture
- Based on the NVILA-2B VLM backbone, taking current and historical 6 RGB frames (384×384, 0.2-second intervals, covering 1 second of history), language instructions, action query tokens, and human proprioception (wrist translation/rotation + MANO hand parameters, processed via MLP) as input
- The action head is a 300M-parameter transformer: 6-layer encoder, hidden size 1536, taking latent embeddings of proprioception and action query tokens as input, predicting bimanual action sequences over a 1-second horizon (30 Hz × 30 steps)
- Action space: wrist 3D translation + rot6D rotation (camera coordinate frame) + first 15 PCA components of MANO (15 ball joints, 45 DOF reduced)

### Training and Loss
- Loss function: L = 20.0·L_wrist_trans + 5.0·L_wrist_rot + 5.0·L_joint, where L_wrist_trans and L_joint are L2 losses, and L_wrist_rot is the rot6D rotation loss
- Two-stage training: human data pretraining for 20 epochs (lr=1e-4, Cosine schedule), robot demonstration post-training for 115 epochs (lr=2e-5, reduced to 2e-6 after 100 epochs, Constant schedule), full model fine-tuning
- Deployment uses ALOHA action chunking with a smoothing parameter of 0.8

### Cross-Embodiment Transfer
- Robot data redirection: optimizes MANO parameters to minimize fingertip position differences, minimize_Θ (1/5)Σ SmoothL1(J_pred(Θ)_i, J_obs,i), Θ∈R^15
- Deployment mapping: wrist pose is transformed via 3D transformations into end-effector pose, and arm joint angles are solved via IK; the hand computes 3D keypoints via MANO, and a four-layer MLP (hidden layers [64,128,64]) predicts robot hand joint commands

## Key Innovations

1. **Human videos as VLA pretraining data source**: First systematic use of large-scale egocentric human videos for VLA pretraining, with validation of their geometric approximation to the robot action space. This innovation breaks the hardware dependency of robot data collection, allowing data scale to expand with the growth of human video libraries.

2. **Unified MANO action space for cross-embodiment transfer**: Unifies human and robot hand actions through MANO parameterization, combined with a fingertip position-optimized redirection strategy, achieving effective transfer to the Unitree H1 + Inspire dexterous hand. Fingertip error is only 5×10^-5 m, proving the abstraction is sufficiently precise.

3. **Validation of the two-stage training paradigm**: The paradigm of pretraining (human videos) + post-training (small robot demonstrations) achieves 77.78% SR (Seen) on short-horizon tasks and 45.93% SR on long-horizon tasks, far exceeding the ACT baseline (24.87% / 2.22%), and the performance drop when generalizing to Unseen backgrounds is significantly smaller than the version without pretraining.

## Experiments and Results

### Benchmark and Setup
- Isaac Humanoid Manipulation Benchmark: 12 tasks (7 short-horizon + 5 long-horizon), 100 successful demonstrations per task, episode length 100-500 frames
- Evaluation configuration: Seen backgrounds with 27 rollouts per task (3 backgrounds × 9 episodes); Unseen backgrounds with 66 rollouts per task (22 backgrounds × 3 episodes); object position randomization up to 20 cm × 20 cm

### Key Results Comparison

| Configuration | Method | Short-horizon SR | Short-horizon PSR | Long-horizon SR | Long-horizon PSR |
|------|------|-----------|------------|-----------|------------|
| Seen | ACT | 24.87% | 59.79% | 2.22% | 26.47% |
| Seen | EgoVLA-NoPretrain | 64.55% | 71.87% | 26.67% | 54.93% |
| Seen | EgoVLA (50%) | 48.15% | 61.73% | 7.41% | 39.70% |
| Seen | EgoVLA | **77.78%** | **84.92%** | **45.93%** | **80.78%** |
| Unseen | ACT | 24.89% | 54.22% | 0.61% | 23.51% |
| Unseen | EgoVLA-NoPretrain | 51.28% | 62.63% | 11.21% | 36.20% |
| Unseen | EgoVLA | **69.11%** | **76.26%** | **28.79%** | **69.11%** |

### Key Findings
- Significant pretraining gains: on long-horizon tasks in the Seen configuration, EgoVLA improves SR by 19.26 percentage points over NoPretrain; halving the data scale (50%) causes long-horizon SR to drop sharply from 45.93% to 7.41%, indicating that pretraining data volume is crucial for complex tasks
- Generalization capability: under Unseen backgrounds, the NoPretrain average success rate drops by 23%, while EgoVLA only drops slightly, showing that pretraining significantly enhances visual robustness
- Zero-shot deployment: all tasks achieve 0% success rate without robot data fine-tuning, indicating that cross-embodiment transfer still requires a moderate amount of robot demonstrations
- Human action modeling: average future prediction error for wrist translation is approximately 8 cm, with a normalized 2D projection error of approximately 0.13, comparable to the HOI-forecast SOTA

## Boundaries and Limitations

- The pretraining framework relies on human data with hand and wrist pose annotations, limiting data availability; the authors believe the proliferation of AR/VR devices (Quest 3, Vision Pro, Aria Glasses) will alleviate this constraint, but additional annotation or device-based collection is still required currently
- Zero-shot deployment is not possible, and a moderate amount of robot data fine-tuning is still required; the authors acknowledge that future work should explore more embodiment-agnostic pretraining to improve zero-shot transfer capability
- The benchmark is only used for controlled and reproducible evaluation (similar to LIBERO and SIMPLER) and is not directly used for sim-to-real transfer; the paper does not mention real robot deployment experiments
- The human action prediction error of approximately 8 cm may become a bottleneck in fine manipulation scenarios, and the paper does not clarify the specific impact of this error on dexterous manipulation success rates

## Engineering Insights

- **Reproduction priority**: First verify the camera coordinate projection logic in data preprocessing—projecting future wrist positions into the current frame is key to supervision consistency; projection errors will cause confusion in training signals; it is recommended to strictly follow the world-coordinate transformation pipeline
- **Action space selection**: The first 15 PCA components of MANO are the core abstraction for cross-embodiment transfer; if the downstream robot hand has significantly different DOF, the input/output dimensions of the redirection MLP need to be redesigned; the fingertip position optimization objective (SmoothL1) is crucial for redirection accuracy
- **Training configuration notes**: The configuration of 20 pretraining epochs + 115 post-training epochs was completed on 32 A100 GPUs; if resources are limited, consider first halving the data volume to observe long-horizon task performance (50% data causes SR to drop from 45.93% to 7.41%, indicating long-horizon tasks are extremely sensitive to data volume)
- **Deployment pitfalls**: The action chunking smoothing parameter of 0.8 and the 30 Hz inference frequency must strictly match the simulation control frequency (simulation decimation = render interval = 1/(30*dt)); when solving arm joint angles via IK, pay attention to the coordinate transformation of the wrist pose to avoid incorrect rotation order
- **Baseline comparison notes**: The ACT baseline uses a DinoV2 backbone and 50000 epochs of training, with action chunk sizes varying by task (Close-Drawer is 64, others are 128); these details must be strictly followed during reproduction to ensure fair comparison

## 개요

EgoVLA는 1인칭 인간 비디오에서 시각-언어-행동 모델(VLA)을 학습하는 새로운 패러다임을 제안하며, 약 50만 개의 이미지-행동 쌍으로 사전 학습한 후 소량의 로봇 시연을 통한 미세 조정으로 교차 체현(跨具身) 손재주 조작을 구현합니다. 핵심 기여는 인간 행동 공간과 로봇 행동 공간이 통합된 MANO 파라미터화로 근사될 수 있음을 증명하여 로봇 데이터 수집의 규모 병목을 돌파하고, Isaac 시뮬레이션 벤치마크에서 ACT 기준선을 크게 능가한다는 점입니다.

## 그것이 바꾼 것

이 작업의 진정한 변화는 VLA 훈련의 데이터 원천을 값비싼 로봇 원격 조작에서 대규모·저비용의 인간 egocentric 비디오로 전환한 것입니다. 기존 OpenVLA, Octo 등의 모델은 로봇 하드웨어와 전문 조작자에 제약되어 데이터 규모에 근본적인 한계가 있었습니다. 반면 인간 비디오는 자연스럽게 장면과 작업의 풍부함을 갖추고 있으며, 저자들은 기하학적 변환을 통해 인간과 로봇 행동 공간의 근사성을 논증하여 '인간 데이터 사전 학습 + 소량 로봇 데이터 후속 학습'이 실행 가능한 경로가 되게 했습니다. 이는 단순한 데이터 원천의 대체가 아니라 VLA의 확장성 경계를 재정의한 것입니다——데이터 획득이 더 이상 로봇 하드웨어에 묶이지 않고 인간의 일상 활동에 묶이게 된 것입니다.

또 다른 핵심 변화는 행동 공간의 통합 설계입니다. EgoVLA는 로봇 관절 명령을 직접 예측하지 않고, 인간 손목 자세와 MANO 손 파라미터를 중간 표현으로 사용하여 리다이렉션 MLP를 통해 특정 로봇에 매핑합니다. 이러한 '인간 중심' 행동 추상화는 사전 학습 단계에서 완전히 로봇을 접촉하지 않아도 되게 하며, 후속 학습 단계에서도 중간 정도의 시연만으로 적응할 수 있어 기존의 교차 체현 전이에서 발생하던 복잡한 행동 공간 정렬 문제를 피합니다.

## 방법 분해

### 데이터 구축 및 전처리
- HOI4D(4000 비디오), HOT3D(833분), HoloAssist(166시간, 1/10 샘플링), TACO(2317 시퀀스) 네 개의 egocentric 데이터셋을 통합하여 총 약 50만 개의 이미지-행동 쌍 구성
- 세계 좌표계 카메라 자세를 사용하여 미래 손목 위치를 현재 카메라 프레임에 투영하여 감독 일관성 보장; 3 FPS로 RGB 관측을 샘플링하여 효율성과 연속성 균형
- HOT3D는 언어 주석이 없어 플레이스홀더 명령 사용; HoloAssist 손 자세는 MANO 표현으로 리다이렉션하고, MANO 평균 손형으로 통일

### 모델 아키텍처
- NVILA-2B VLM 백본 기반, 현재 및 과거 6프레임 RGB(384×384, 0.2초 간격, 1초 이력 포함), 언어 명령, 행동 쿼리 토큰, 인간 고유 감각(손목 병진/회전 + MANO 손 파라미터, MLP 처리) 입력
- 행동 헤드는 300M 파라미터 트랜스포머: 6레이어 인코더, 히든 크기 1536, 고유 감각과 행동 쿼리 토큰의 잠재 임베딩을 입력으로 1초 시야(30 Hz × 30 스텝) 내 양손 행동 시퀀스 예측
- 행동 공간: 손목 3D 병진 + rot6D 회전(카메라 좌표계) + MANO 상위 15개 PCA 성분(15 구형 관절, 45 DOF 차원 축소)

### 훈련 및 손실
- 손실 함수: L = 20.0·L_wrist_trans + 5.0·L_wrist_rot + 5.0·L_joint, 여기서 L_wrist_trans와 L_joint는 L2 손실, L_wrist_rot는 rot6D 회전 손실
- 2단계 훈련: 인간 데이터 사전 학습 20 epochs(lr=1e-4, Cosine 스케줄), 로봇 시연 후속 학습 115 epochs(lr=2e-5, 100 epochs 후 2e-6으로 감소, Constant 스케줄), 전체 모델 미세 조정
- 배포 시 ALOHA 행동 청킹 사용, 평활화 파라미터 0.8

### 교차 체현 전이
- 로봇 데이터 리다이렉션: MANO 파라미터를 최적화하여 손끝 위치 차이 최소화, minimize_Θ (1/5)Σ SmoothL1(J_pred(Θ)_i, J_obs,i), Θ∈R^15
- 배포 매핑: 손목 자세는 3D 변환을 통해 엔드 이펙터 자세로 변환, IK로 팔 관절 각도 해석; 손은 MANO로 3D 키포인트 계산, 4레이어 MLP(히든 레이어 [64,128,64])로 로봇 손 관절 명령 예측

## 핵심 혁신

1. **인간 비디오를 VLA 사전 학습 데이터 원천으로 사용**: 처음으로 대규모 egocentric 인간 비디오를 VLA 사전 학습에 체계적으로 사용하고, 로봇 행동 공간과의 기하학적 근사성을 검증했습니다. 이 혁신은 로봇 데이터 수집의 하드웨어 의존성을 깨뜨려 데이터 규모가 인간 비디오 라이브러리 성장에 따라 확장될 수 있게 합니다.

2. **통합 MANO 행동 공간으로 교차 체현 전이 구현**: MANO 파라미터화로 인간과 로봇 손 행동을 통합하고, 손끝 위치 최적화 기반 리다이렉션 전략을 통해 Unitree H1 + Inspire 손재주 손으로의 효과적인 전이를 구현했습니다. 손끝 오차는 5×10^-5 m에 불과하여 이 추상화의 정밀도가 충분히 높음을 증명합니다.

3. **2단계 훈련 패러다임의 효과성 검증**: 사전 학습(인간 비디오) + 후속 학습(소량 로봇 시연) 패러다임이 짧은 시야 작업에서 77.78% SR(Seen), 긴 시야 작업에서 45.93% SR로 ACT 기준선(24.87% / 2.22%)을 크게 능가하며, Unseen 배경으로의 일반화 시 성능 저하가 사전 학습 없는 버전보다 훨씬 작습니다.

## 실험 및 결과

### 벤치마크 및 설정
- Isaac Humanoid Manipulation Benchmark: 12개 작업(7개 짧은 시야 + 5개 긴 시야), 각 작업당 100개 성공 시연, 에피소드 길이 100-500프레임
- 평가 구성: Seen 배경은 작업당 27회 롤아웃(3 배경 × 9 에피소드); Unseen 배경은 작업당 66회 롤아웃(22 배경 × 3 에피소드); 객체 위치 무작위화 최대 20 cm × 20 cm

### 핵심 결과 비교

| 구성 | 방법 | 짧은 시야 SR | 짧은 시야 PSR | 긴 시야 SR | 긴 시야 PSR |
|------|------|-----------|------------|-----------|------------|
| Seen | ACT | 24.87% | 59.79% | 2.22% | 26.47% |
| Seen | EgoVLA-NoPretrain | 64.55% | 71.87% | 26.67% | 54.93% |
| Seen | EgoVLA (50%) | 48.15% | 61.73% | 7.41% | 39.70% |
| Seen | EgoVLA | **77.78%** | **84.92%** | **45.93%** | **80.78%** |
| Unseen | ACT | 24.89% | 54.22% | 0.61% | 23.51% |
| Unseen | EgoVLA-NoPretrain | 51.28% | 62.63% | 11.21% | 36.20% |
| Unseen | EgoVLA | **69.11%** | **76.26%** | **28.79%** | **69.11%** |

### 핵심 발견
- 사전 학습 이득이 뚜렷함: 긴 시야 작업 Seen 구성에서 EgoVLA는 NoPretrain 대비 SR 19.26% 포인트 향상; 데이터 규모 절반(50%)은 긴 시야 SR을 45.93%에서 7.41%로 급락시켜 사전 학습 데이터량이 복잡한 작업에 결정적임을 시사
- 일반화 능력: Unseen 배경에서 NoPretrain 평균 성공률 23% 하락, EgoVLA는 소폭 하락에 그쳐 사전 학습이 시각적 견고성을 크게 강화
- 제로샷 배포: 로봇 데이터 미세 조정 없이는 모든 작업 성공률 0%로, 교차 체현 전이에는 여전히 중간 정도의 로봇 시연이 필요함
- 인간 행동 모델링: 손목 병진 평균 미래 예측 오차 약 8 cm, 2D 투영 정규화 오차 약 0.13으로 HOI-forecast SOTA와 유사

## 경계 및 한계

- 사전 학습 프레임워크는 손과 손목 자세 주석이 있는 인간 데이터에 의존하여 데이터 가용성을 제한; 저자들은 AR/VR 기기(Quest 3, Vision Pro, Aria Glasses) 보급이 이 제약을 완화할 것으로 보지만, 현재는 추가 주석 또는 기기 수집이 필요
- 제로샷 배포가 불가능하며 여전히 중간 정도의 로봇 데이터 미세 조정 필요; 저자들은 향후 더 체현 독립적인 사전 학습을 탐구하여 제로샷 전이 능력을 향상시킬 필요성을 인정
- 벤치마크는 통제된 재현 가능한 평가(LIBERO 및 SIMPLER 유사)에만 사용되며 sim-to-real 전이에 직접 사용되지 않음; 논문은 실제 로봇 배포 실험을 언급하지 않음
- 인간 행동 예측 오차 약 8 cm는 정밀 조작 시나리오에서 병목이 될 수 있으며, 논문은 이 오차가 손재주 조작 성공률에 미치는 구체적 영향을 명확히 하지 않음

## 공학적 시사점

- **재현 우선순위**: 데이터 전처리의 카메라 좌표계 투영 로직을 먼저 확인——미래 손목 위치를 현재 프레임에 투영하는 것이 감독 일관성의 핵심이며, 투영 오류는 훈련 신호 혼란을 초래; 세계 좌표계 변환 흐름을 엄격히 따를 것 권장
- **행동 공간 선택**: MANO 상위 15개 PCA 성분은 교차 체현 전이의 핵심 추상화이며, 하류 로봇 손 자유도 차이가 크면 리다이렉션 MLP의 입출력 차원을 재설계해야 함; 손끝 위치 최적화 목표(SmoothL1)는 리다이렉션 정밀도에 결정적
- **훈련 구성 주의**: 사전 학습 20 epochs + 후속 학습 115 epochs 구성은 32개 A100에서 완료되며, 자원이 제한된 경우 먼저 데이터 절반으로 긴 시야 작업 성능을 시도(50% 데이터는 SR이 45.93%에서 7.41%로 하락, 긴 시야 작업은 데이터량에 매우 민감)
- **배포 함정**: 행동 청킹 평활화 파라미터 0.8과 30 Hz 추론 빈도는 시뮬레이션 제어 빈도와 엄격히 일치해야 함(simulation decimation = render interval = 1/(30*dt)); IK로 팔 관절 각도 해석 시 손목 자세의 좌표계 변환에 주의하여 회전 순서 오류 방지
- **기준선 비교 주의**: ACT 기준선은 DinoV2 백본과 50000 epochs 훈련을 사용하며, 행동 청크 크기는 작업에 따라 다름(Close-Drawer는 64, 나머지 128), 재현 시 이러한 세부 사항을 엄격히 따라야 공정한 비교가 가능
