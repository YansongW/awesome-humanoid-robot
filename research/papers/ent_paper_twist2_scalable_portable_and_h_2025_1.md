---
$id: ent_paper_twist2_scalable_portable_and_h_2025_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'TWIST2: Scalable, Portable, and Holistic Humanoid Data Collection System'
  zh: TWIST2｜可扩展、便携式、整体的人形数据收集系统
  ko: 'TWIST2: Scalable, Portable, and Holistic Humanoid Data Collection System'
summary:
  en: The implementation path of TWIST2 is to first encode camera images/multi-view observations, human videos/motion capture
    trajectories, teleoperation/exoskeleton data into multimodal representations, and then use ACT/behavioral cloning imitation
    learning, VLA multimodal action model, and whole-body controller/WBC/MPC prediction to execute action commands. The key
    point is to preserve the semantic understanding of VLM while adding robot states and action heads to avoid staying only
    at language planning.
  zh: TWIST2 的实现路径是先把相机图像/多视角观测、人类视频/动捕轨迹、遥操作/外骨骼数据编码成多模态表征，再用ACT/行为克隆模仿学习、VLA 多模态动作模型、全身控制器/WBC/MPC预测可执行动作命令。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: TWIST2 的实现路径是先把相机图像/多视角观测、人类视频/动捕轨迹、遥操作/外骨骼数据编码成多模态表征，再用ACT/行为克隆模仿学习、VLA 多模态动作模型、全身控制器/WBC/MPC预测可执行动作命令。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 09_data_datasets
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- data_collection
- dataset
- human_demonstration
- teleoperation
- twist2
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: TWIST2 project page
  url: https://twist-data.github.io
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人从实验室走向真实世界应用，面临的一个核心瓶颈是高质量、多样化操作数据的匮乏。现有数据采集系统往往在可扩展性、便携性与数据完整性之间难以兼顾，限制了大规模模仿学习与多模态动作模型的发展。为此，本文提出 TWIST2 系统，旨在构建一种可扩展、便携且整体的人形机器人数据收集框架。其主要贡献在于设计了一条从多源感知数据编码到可执行动作命令的完整实现路径，并创新性地将视觉语言模型的语义理解能力与机器人状态、动作头相结合，从而突破了传统方法仅停留在语言规划层面的局限。

## 核心内容

当前人形机器人研究领域，数据驱动的模仿学习与多模态动作模型已成为提升机器人灵巧操作与全身协调能力的关键技术。然而，现有数据采集方案普遍存在两大困境：一是数据来源单一，难以融合相机图像、人类视频、动捕轨迹以及遥操作/外骨骼数据等多种模态；二是系统架构缺乏可扩展性与便携性，导致数据规模受限、采集场景固化。这些问题严重制约了机器人从示范中学习复杂、泛化行为的能力，亟需一种能够统一多模态数据表征并支持高效动作生成的系统性解决方案。

TWIST2 系统提出了一套层次化的实现路径。首先，系统将来自相机图像/多视角观测、人类视频/动捕轨迹、遥操作/外骨骼数据等多种异构信息统一编码为多模态表征。这一步骤的关键在于建立跨模态的对齐与融合机制，使得不同来源的示范数据能够在同一特征空间中表达。随后，系统利用 ACT（Action Chunking with Transformers）行为克隆模仿学习、VLA（Vision-Language-Action）多模态动作模型以及全身控制器（WBC/MPC）预测，将多模态表征转化为可执行的机器人动作命令。这种从感知到规划的端到端流水线设计，确保了数据采集与策略学习之间的无缝衔接。

该方法的核心技术创新在于对视觉语言模型（VLM）的深度整合与扩展。传统方法往往将 VLM 仅用于高层语义理解或语言规划，而 TWIST2 则通过增加机器人状态和动作头，使 VLM 能够直接参与底层动作生成。具体而言，系统保留了 VLM 强大的语义理解能力，同时在其基础上扩展出专门处理机器人关节角度、末端执行器位姿等状态信息的网络分支，以及输出连续动作序列的动作头。这种设计避免了“只规划不执行”的脱节问题，使得机器人既能理解任务语义，又能生成精细的操作指令。

在实验验证与应用价值方面，TWIST2 系统通过构建大规模、多模态的人形机器人数据集，展示了其在多种操作任务中的有效性。系统支持便携式部署，可在不同环境（如实验室、家庭、工厂）中快速采集数据，显著降低了数据获取的门槛。同时，由于采用了模块化设计，TWIST2 能够兼容多种遥操作设备与传感器配置，为不同研究团队提供了灵活的定制空间。该系统的成功开发为人形机器人的大规模模仿学习、跨场景泛化以及人机协同操作奠定了坚实的数据基础，有望推动人形机器人从特定任务执行向通用操作能力的跨越。

## 参考
- TWIST2 project page (https://twist-data.github.io)

## Overview

A core bottleneck for humanoid robots transitioning from laboratories to real-world applications is the scarcity of high-quality, diverse manipulation data. Existing data collection systems often struggle to balance scalability, portability, and data integrity, limiting the development of large-scale imitation learning and multimodal action models. To address this, the paper proposes the TWIST2 system, aiming to build a scalable, portable, and holistic data collection framework for humanoid robots. Its main contribution lies in designing a complete implementation pipeline from multi-source perception data encoding to executable action commands, and innovatively integrating the semantic understanding capabilities of vision-language models with robot states and action heads, thereby breaking through the limitation of traditional methods that remain at the level of language planning.

## Content

In the current field of humanoid robot research, data-driven imitation learning and multimodal action models have become key technologies for enhancing robots' dexterous manipulation and whole-body coordination capabilities. However, existing data collection schemes generally face two major dilemmas: first, the data sources are singular, making it difficult to integrate multiple modalities such as camera images, human videos, motion capture trajectories, and teleoperation/exoskeleton data; second, the system architecture lacks scalability and portability, resulting in limited data scale and rigid collection scenarios. These issues severely constrain robots' ability to learn complex, generalizable behaviors from demonstrations, necessitating a systematic solution that can unify multimodal data representations and support efficient action generation.

The TWIST2 system proposes a hierarchical implementation path. First, the system uniformly encodes heterogeneous information from camera images/multi-view observations, human videos/motion capture trajectories, teleoperation/exoskeleton data, etc., into multimodal representations. The key to this step lies in establishing cross-modal alignment and fusion mechanisms, enabling demonstration data from different sources to be expressed within the same feature space. Subsequently, the system utilizes ACT (Action Chunking with Transformers) behavioral cloning imitation learning, VLA (Vision-Language-Action) multimodal action models, and whole-body controllers (WBC/MPC) predictions to transform multimodal representations into executable robot action commands. This end-to-end pipeline design from perception to planning ensures seamless integration between data collection and policy learning.

The core technological innovation of this method lies in the deep integration and extension of vision-language models (VLMs). Traditional approaches often use VLMs only for high-level semantic understanding or language planning, whereas TWIST2 enables VLMs to directly participate in low-level action generation by adding robot state and action heads. Specifically, the system retains the powerful semantic understanding capabilities of VLMs while extending them with specialized network branches for processing robot joint angles, end-effector poses, and other state information, as well as action heads that output continuous action sequences. This design avoids the disconnect of "planning without execution," allowing robots to both understand task semantics and generate fine-grained manipulation instructions.

In terms of experimental validation and application value, the TWIST2 system demonstrates its effectiveness across various manipulation tasks by constructing a large-scale, multimodal humanoid robot dataset. The system supports portable deployment, enabling rapid data collection in different environments (e.g., laboratories, homes, factories), significantly lowering the barrier to data acquisition. Additionally, due to its modular design, TWIST2 is compatible with various teleoperation devices and sensor configurations, offering flexible customization space for different research teams. The successful development of this system lays a solid data foundation for large-scale imitation learning, cross-scenario generalization, and human-robot collaborative manipulation of humanoid robots, potentially advancing humanoid robots from specific task execution to general manipulation capabilities.

## 개요

휴머노이드 로봇이 연구실에서 실제 세계 응용으로 나아가는 데 있어 핵심적인 병목 현상은 고품질의 다양한 조작 데이터 부족입니다. 기존 데이터 수집 시스템은 확장성, 휴대성, 데이터 완전성 간의 균형을 맞추기 어려워 대규모 모방 학습 및 다중 모드 동작 모델의 발전을 제한합니다. 이에 본 논문은 TWIST2 시스템을 제안하여 확장 가능하고 휴대 가능하며 통합적인 휴머노이드 로봇 데이터 수집 프레임워크를 구축하고자 합니다. 주요 기여는 다중 소스 인식 데이터 인코딩부터 실행 가능한 동작 명령까지의 완전한 구현 경로를 설계하고, 시각-언어 모델의 의미 이해 능력을 로봇 상태 및 동작 헤드와 혁신적으로 결합하여 기존 방법이 언어 계획 수준에 머무르는 한계를 돌파한 점에 있습니다.

## 핵심 내용

현재 휴머노이드 로봇 연구 분야에서 데이터 기반 모방 학습 및 다중 모드 동작 모델은 로봇의 정밀 조작 및 전신 협응 능력을 향상시키는 핵심 기술로 자리 잡았습니다. 그러나 기존 데이터 수집 방식은 두 가지 주요 문제점을 안고 있습니다. 첫째, 데이터 소스가 단일하여 카메라 이미지, 인간 비디오, 모션 캡처 궤적, 원격 조작/외골격 데이터 등 다양한 모드를 통합하기 어렵습니다. 둘째, 시스템 아키텍처가 확장성과 휴대성을 갖추지 못해 데이터 규모가 제한되고 수집 환경이 고정됩니다. 이러한 문제는 로봇이 시연으로부터 복잡하고 일반화된 행동을 학습하는 것을 심각하게 저해하며, 다중 모드 데이터 표현을 통합하고 효율적인 동작 생성을 지원하는 체계적인 솔루션이 시급히 요구됩니다.

TWIST2 시스템은 계층화된 구현 경로를 제안합니다. 먼저, 시스템은 카메라 이미지/다중 시점 관측, 인간 비디오/모션 캡처 궤적, 원격 조작/외골격 데이터 등 다양한 이종 정보를 다중 모드 표현으로 통합 인코딩합니다. 이 단계의 핵심은 교차 모드 정렬 및 융합 메커니즘을 구축하여 서로 다른 출처의 시연 데이터가 동일한 특징 공간에서 표현될 수 있도록 하는 것입니다. 이후 시스템은 ACT(Action Chunking with Transformers) 행동 복제 모방 학습, VLA(Vision-Language-Action) 다중 모드 동작 모델, 전신 제어기(WBC/MPC) 예측을 활용하여 다중 모드 표현을 실행 가능한 로봇 동작 명령으로 변환합니다. 이러한 인식에서 계획까지의 종단 간 파이프라인 설계는 데이터 수집과 정책 학습 간의 원활한 연결을 보장합니다.

이 방법의 핵심 기술 혁신은 시각-언어 모델(VLM)의 심층 통합 및 확장에 있습니다. 기존 방법은 VLM을 주로 고수준 의미 이해나 언어 계획에만 사용하는 반면, TWIST2는 로봇 상태 및 동작 헤드를 추가하여 VLM이 직접 저수준 동작 생성에 참여할 수 있도록 합니다. 구체적으로, 시스템은 VLM의 강력한 의미 이해 능력을 유지하면서, 로봇 관절 각도, 말단 효과기 자세 등의 상태 정보를 처리하는 전용 네트워크 분기와 연속 동작 시퀀스를 출력하는 동작 헤드를 확장합니다. 이러한 설계는 "계획만 하고 실행하지 않는" 단절 문제를 방지하여 로봇이 작업 의미를 이해함과 동시에 정밀한 조작 명령을 생성할 수 있게 합니다.

실험 검증 및 응용 가치 측면에서 TWIST2 시스템은 대규모 다중 모드 휴머노이드 로봇 데이터셋을 구축하여 다양한 조작 작업에서의 효과를 입증했습니다. 시스템은 휴대형 배치를 지원하여 실험실, 가정, 공장 등 다양한 환경에서 신속하게 데이터를 수집할 수 있어 데이터 획득의 장벽을 크게 낮췄습니다. 또한 모듈식 설계 덕분에 TWIST2는 다양한 원격 조작 장치와 센서 구성을 호환할 수 있어 연구팀별로 유연한 맞춤 공간을 제공합니다. 이 시스템의 성공적인 개발은 휴머노이드 로봇의 대규모 모방 학습, 교차 환경 일반화, 인간-로봇 협력 조작을 위한 견고한 데이터 기반을 마련하여, 휴머노이드 로봇이 특정 작업 수행에서 범용 조작 능력으로 도약하는 데 기여할 것으로 기대됩니다.
