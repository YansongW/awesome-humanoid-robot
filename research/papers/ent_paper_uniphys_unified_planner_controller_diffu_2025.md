---
$id: ent_paper_uniphys_unified_planner_controller_diffu_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'UniPhys: Unified Planner and Controller with Diffusion for Flexible Physics-Based Character Control'
  zh: 'UniPhys: Unified Planner and Controller with Diffusion for Flexible Physics-Based Character Control'
  ko: 'UniPhys: Unified Planner and Controller with Diffusion for Flexible Physics-Based Character Control'
summary:
  en: Generating natural and physically plausible character motion remains challenging, particularly for long-horizon control
    with diverse guidance signals.
  zh: UniPhys 是一个基于扩散模型的行为克隆框架，由研究团队提出，用于实现灵活且物理可信的角色运动控制。其核心贡献在于将运动规划与控制统一为单一模型，支持文本、轨迹和目标等多模态输入，并通过 Diffusion Forcing 范式解决长序列预测误差问题，无需任务特定微调即可泛化到多种控制信号。
  ko: Generating natural and physically plausible character motion remains challenging, particularly for long-horizon control
    with diverse guidance signals.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- uniphys
- unified
- planner
- controller
- diffu
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 158 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2504.12540 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2504.12540v1); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2504.12540 UniPhys: Unified Planner and Controller with Diffusion for Flexible Physics-Based Character Control'
  url: https://arxiv.org/abs/2504.12540
  accessed_at: '2026-07-31'
  date: '2025-04-17'
- id: src_002
  type: website
  title: Project page
  url: https://wuyan01.github.io/uniphys-project/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---


## 概述

UniPhys 是一个基于扩散的行为克隆框架，将运动规划与控制统一到单一模型中，联合建模状态和动作分布，以文本为条件。它通过 Diffusion Forcing 训练范式、潜在动作表示和灵活的去噪调度，实现了无需任务特定微调的物理角色控制，支持文本驱动、速度控制、目标到达和障碍物规避等多种任务。

## 它改变了什么

此前的工作（如 CLoSD）将扩散规划器与 RL 跟踪控制器分离，导致领域差距和运动质量下降，且需要针对特定任务微调。UniPhys 真正改变了这一范式：它不再将规划和控制视为两个独立模块，而是通过联合建模状态和动作分布，消除了层级差异。这意味着下游任务不再需要为每个新控制信号重新训练控制器，而是通过测试时的引导损失直接适配，这从根本上改变了物理角色控制的部署方式——从“训练时定制”转向“推理时引导”。

## 方法拆解

### 行为表示
- 行为序列 X = x_{1:T}，其中 x_t = (s^c_t, z_t)，z_t ∈ R^32 为 PULSE 编码器得到的潜在动作表示
- 状态 S^c 包含全局根轨迹（位置、方向、线速度、角速度）和局部关节特征（位置、速度、旋转、角速度），旋转采用 6D 表示
- 每帧特征维度为 398（32 维潜在动作 + 366 维状态）

### 训练
- 遵循 Diffusion Forcing 范式，对每帧独立注入随机噪声水平 k = k_{1:T} ∈ [K]^T
- 使用 12 层因果 transformer 解码器，隐藏大小 768，条件为 CLIP 文本嵌入
- 训练损失：L(θ) = E_{k,X^0}[||X^0 - M_θ(X^k, k, c)||^2]
- 训练时将序列划分为 32 帧片段，步长 8，批量大小 1024，学习率 1.5 × 10⁻⁴

### 推理
- 滚动时域策略和自回归行为合成，支持灵活的去噪配置（历史长度 h、预测视界 H、动作执行步数 T_a、去噪调度）
- 稳定化技巧：将噪声水平矩阵中的零值替换为稳定化噪声水平 n，防止模型过度自信
- 蒙特卡洛引导（MCG）从多个样本估计梯度，降低方差

### 任务特定引导
- 目标到达：位置损失 + 方向损失，H=28, T_a=8
- 速度控制：速度大小 + 方向 + 朝向损失，H=28, T_a=8
- 障碍物规避：基于 SDF 的 softplus 平滑损失，H=28, T_a=8

## 关键创新

1. **统一规划与控制**：这是首个将规划和控制真正统一到单一扩散模型中的工作。此前方法要么是两阶段（规划器+控制器），要么是纯运动学。UniPhys 通过联合建模状态和动作分布，消除了层级差异，使得测试时可以通过损失引导直接适配新任务，无需微调。

2. **每帧独立噪声注入**：不同于传统扩散模型对整个序列施加统一噪声，UniPhys 对每帧独立注入噪声水平。这一设计使得模型能处理推理时的灵活噪声配置（如自回归去噪、渐进去噪），并解决了物理模拟器引入的分布偏移问题。

3. **稳定化技巧**：在去噪过程中将完全去噪帧的噪声指示符设置为大于零，仅调整噪声指示符而不向状态-动作预测添加噪声。这一简单技巧显著改善了长期展开稳定性，将平均回合长度从 148.2 提升到 2320.3（自回归去噪配置下）。

## 实验与结果

### 文本驱动控制（表 2）
| 方法 | 正确率 | 自然度 | 真实感 | 平滑度 | 漂浮 (mm) | 抖动 (mm/s³) |
|------|--------|--------|--------|--------|-----------|-------------|
| Phys-GT | - | 3.43 | 3.53 | 3.42 | 17.0 | 1.1 |
| CLoSD | 61.6% | 2.86 | 3.06 | 2.86 | 20.59 | 3.4 |
| MaskedMimic | 42.9% | 2.82 | 2.93 | 2.78 | 14.82 | 4.2 |
| UniPhys | 56.3% | 3.23 | 3.28 | 3.15 | 16.6 | 1.2 |

UniPhys 在自然度、真实感、平滑度上显著优于 CLoSD 和 MaskedMimic，抖动指标接近 Phys-GT（1.2 vs 1.1），但语义正确率略低于 CLoSD。

### 多任务控制（表 3）
| 任务 | CLoSD | MaskedMimic | UniPhys |
|------|-------|-------------|---------|
| 目标到达 [walk]-near | 1.0 | 0.87 | 1.0 |
| 目标到达 [walk]-far | 1.0 | 0.0 | 0.95 |
| 目标到达 [jog] | 0.64 | 0.81* | 0.85 |
| 速度控制误差 (m/s) | - | 0.09 | 0.07 |
| 速度控制抖动 (mm/s³) | - | 5.0 | 2.0 |

*MaskedMimic 无法处理远目标，设置为近目标（2m 内），而 CLoSD 和 UniPhys 设置 6m 内目标。

### 消融（表 4）
| 配置 | 平均回合长度 | 最大回合长度 | FPS |
|------|-------------|-------------|-----|
| 全序列去噪 | 231.6 | 1197 | 23.0 |
| 渐进去噪 | 1817.6 | 3000 | 18.0 |
| 自回归（无潜在动作，无稳定化） | 58.0 | 59 | 9.5 |
| 自回归（无潜在动作，有稳定化） | 238.2 | 717 | 9.5 |
| 自回归（有潜在动作，无稳定化） | 148.2 | 230 | 9.5 |
| 自回归（有潜在动作，有稳定化） | 2320.3 | 3000 | 9.5 |

潜在动作表示和稳定化技巧对长期稳定性至关重要，二者缺一不可。

### MCG 消融（表 A.1）
| N | 成功率 | FPS |
|---|--------|-----|
| 1 | 26% | 9.2 |
| 3 | 82% | 8.9 |
| 5 | 98% | 8.7 |

## 边界与局限

- **推理效率低**：扩散框架的推理效率低于 RL 策略，文本驱动控制下约 10 FPS（自回归去噪）和 18 FPS（渐进去噪），难以满足实时控制需求。
- **平衡损失与摔倒**：在高度动态动作或文本指令切换时机不佳时，仍会发生平衡损失，导致技能过渡失败和摔倒；完全避免摔倒不现实。
- **未实现摔倒恢复**：计划通过收集从地面起身的专家演示并训练专门的 RL 策略，但尚未完成。
- **地形适应性未实现**：由于缺乏地形特定数据，行为克隆框架下无法立即实现不同地形行走。
- **灵巧手控制未包含**：当前方法未包含灵巧手控制，限制了人-物交互任务。
- **未见任务零样本泛化**：论文未提及对未见任务类型的零样本泛化测试细节。

## 工程启示

- **核对数据管线**：数据集构建依赖 PULSE 跟踪策略跟踪 AMASS 运动，存储成功跟踪的序列。复现时需确认 PULSE 的跟踪成功率，因为失败序列会被丢弃，可能引入数据偏差。
- **稳定化噪声水平是关键超参数**：文本驱动控制使用稳定化噪声水平 3（1、2 或 3 足够，增加到 5 会降低稳定性）。基于损失的引导应用需要更高的稳定化噪声水平，需针对任务调参。
- **MCG 样本数影响成功率**：N=1 时成功率仅 26%，N=5 时达到 98%。在计算资源允许时，优先使用 N=5。
- **去噪调度选择**：自回归去噪产生更稳定的展开，渐进去噪提高长时规划任务性能。文本驱动控制用自回归（H=8），目标到达/速度控制/障碍物规避用渐进（H=28, T_a=8）。
- **推理效率瓶颈**：FPS 在 9.5-23 之间，远低于实时要求。若下游需要实时控制，需考虑蒸馏或更高效的采样策略。
- **训练成本**：单卡 A100 约 10 GPU 天，15k 轮。训练中损失仅小幅下降，但策略稳定性和运动语义保真度持续改善，需耐心观察而非仅看损失曲线。

## 参考
- https://arxiv.org/abs/2504.12540
- https://wuyan01.github.io/uniphys-project/
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## Overview

UniPhys is a diffusion-based behavior cloning framework that unifies motion planning and control into a single model, jointly modeling state and action distributions conditioned on text. Through the Diffusion Forcing training paradigm, latent action representation, and flexible denoising schedules, it enables physics-based character control without task-specific fine-tuning, supporting tasks such as text-driven control, velocity control, goal reaching, and obstacle avoidance.

## What It Changes

Previous works (such as CLoSD) separated the diffusion planner from the RL tracking controller, leading to domain gaps and degraded motion quality, and required task-specific fine-tuning. UniPhys fundamentally changes this paradigm: instead of treating planning and control as two independent modules, it eliminates the hierarchical gap by jointly modeling state and action distributions. This means downstream tasks no longer require retraining a controller for each new control signal; instead, they adapt directly through test-time guidance losses. This fundamentally changes how physics-based character control is deployed—shifting from "customization at training time" to "guidance at inference time."

## Method Breakdown

### Behavior Representation
- Behavior sequence X = x_{1:T}, where x_t = (s^c_t, z_t), with z_t ∈ R^32 being the latent action representation from the PULSE encoder
- State S^c includes global root trajectory (position, orientation, linear velocity, angular velocity) and local joint features (position, velocity, rotation, angular velocity), with rotations represented in 6D
- Per-frame feature dimension is 398 (32-dimensional latent action + 366-dimensional state)

### Training
- Follows the Diffusion Forcing paradigm, injecting random noise levels k = k_{1:T} ∈ [K]^T independently for each frame
- Uses a 12-layer causal transformer decoder with hidden size 768, conditioned on CLIP text embeddings
- Training loss: L(θ) = E_{k,X^0}[||X^0 - M_θ(X^k, k, c)||^2]
- During training, sequences are divided into 32-frame segments with stride 8, batch size 1024, and learning rate 1.5 × 10⁻⁴

### Inference
- Receding-horizon policy and autoregressive behavior synthesis, supporting flexible denoising configurations (history length h, prediction horizon H, action execution steps T_a, denoising schedule)
- Stabilization trick: replace zero values in the noise level matrix with a stabilization noise level n to prevent the model from being overconfident
- Monte Carlo Guidance (MCG) estimates gradients from multiple samples to reduce variance

### Task-Specific Guidance
- Goal reaching: position loss + orientation loss, H=28, T_a=8
- Velocity control: velocity magnitude + direction + heading loss, H=28, T_a=8
- Obstacle avoidance: SDF-based softplus smooth loss, H=28, T_a=8

## Key Innovations

1. **Unified Planning and Control**: This is the first work to truly unify planning and control into a single diffusion model. Previous methods were either two-stage (planner + controller) or purely kinematic. UniPhys eliminates the hierarchical gap by jointly modeling state and action distributions, enabling direct adaptation to new tasks through loss guidance at test time without fine-tuning.

2. **Per-Frame Independent Noise Injection**: Unlike traditional diffusion models that apply a uniform noise level to the entire sequence, UniPhys injects noise levels independently for each frame. This design allows the model to handle flexible noise configurations at inference (such as autoregressive denoising and progressive denoising) and addresses the distribution shift introduced by physics simulators.

3. **Stabilization Trick**: During denoising, the noise indicator for fully denoised frames is set to a value greater than zero, adjusting only the noise indicator without adding noise to the state-action predictions. This simple trick significantly improves long-horizon rollout stability, increasing the average episode length from 148.2 to 2320.3 (under the autoregressive denoising configuration).

## Experiments and Results

### Text-Driven Control (Table 2)
| Method | Accuracy | Naturalness | Realism | Smoothness | Floating (mm) | Jitter (mm/s³) |
|--------|----------|-------------|---------|------------|---------------|----------------|
| Phys-GT | - | 3.43 | 3.53 | 3.42 | 17.0 | 1.1 |
| CLoSD | 61.6% | 2.86 | 3.06 | 2.86 | 20.59 | 3.4 |
| MaskedMimic | 42.9% | 2.82 | 2.93 | 2.78 | 14.82 | 4.2 |
| UniPhys | 56.3% | 3.23 | 3.28 | 3.15 | 16.6 | 1.2 |

UniPhys significantly outperforms CLoSD and MaskedMimic in naturalness, realism, and smoothness, with jitter metrics close to Phys-GT (1.2 vs 1.1), though semantic accuracy is slightly lower than CLoSD.

### Multi-Task Control (Table 3)
| Task | CLoSD | MaskedMimic | UniPhys |
|------|-------|-------------|---------|
| Goal reaching [walk]-near | 1.0 | 0.87 | 1.0 |
| Goal reaching [walk]-far | 1.0 | 0.0 | 0.95 |
| Goal reaching [jog] | 0.64 | 0.81* | 0.85 |
| Velocity control error (m/s) | - | 0.09 | 0.07 |
| Velocity control jitter (mm/s³) | - | 5.0 | 2.0 |

*MaskedMimic cannot handle far goals, so near goals (within 2m) are set, while CLoSD and UniPhys use goals within 6m.

### Ablations (Table 4)
| Configuration | Average Episode Length | Max Episode Length | FPS |
|---------------|------------------------|--------------------|-----|
| Full-sequence denoising | 231.6 | 1197 | 23.0 |
| Progressive denoising | 1817.6 | 3000 | 18.0 |
| Autoregressive (no latent action, no stabilization) | 58.0 | 59 | 9.5 |
| Autoregressive (no latent action, with stabilization) | 238.2 | 717 | 9.5 |
| Autoregressive (with latent action, no stabilization) | 148.2 | 230 | 9.5 |
| Autoregressive (with latent action, with stabilization) | 2320.3 | 3000 | 9.5 |

The latent action representation and stabilization trick are both crucial for long-term stability; neither can be omitted.

### MCG Ablation (Table A.1)
| N | Success Rate | FPS |
|---|--------------|-----|
| 1 | 26% | 9.2 |
| 3 | 82% | 8.9 |
| 5 | 98% | 8.7 |

## Boundaries and Limitations

- **Low inference efficiency**: The diffusion framework has lower inference efficiency than RL policies, achieving approximately 10 FPS (autoregressive denoising) and 18 FPS (progressive denoising) under text-driven control, which is insufficient for real-time control needs.
- **Balance loss and falls**: Balance loss can still occur during highly dynamic actions or when text instruction switching is poorly timed, leading to failed skill transitions and falls; completely avoiding falls is unrealistic.
- **Fall recovery not implemented**: Plans to collect expert demonstrations of getting up from the ground and train a dedicated RL policy are in place but not yet completed.
- **Terrain adaptation not implemented**: Due to the lack of terrain-specific data, walking on different terrains cannot be immediately achieved under the behavior cloning framework.
- **Dexterous hand control not included**: The current method does not include dexterous hand control, limiting human-object interaction tasks.
- **Zero-shot generalization to unseen tasks**: The paper does not mention details of zero-shot generalization tests on unseen task types.

## Engineering Insights

- **Verify the data pipeline**: Dataset construction relies on the PULSE tracking policy to track AMASS motions, storing successfully tracked sequences. When reproducing, confirm PULSE's tracking success rate, as failed sequences are discarded, potentially introducing data bias.
- **Stabilization noise level is a key hyperparameter**: Text-driven control uses a stabilization noise level of 3 (1, 2, or 3 is sufficient; increasing to 5 reduces stability). Loss-based guidance requires higher stabilization noise levels and needs task-specific tuning.
- **MCG sample count affects success rate**: N=1 yields only a 26% success rate, while N=5 reaches 98%. When computational resources allow, prioritize N=5.
- **Denoising schedule selection**: Autoregressive denoising produces more stable rollouts, while progressive denoising improves performance on long-horizon planning tasks. Use autoregressive for text-driven control (H=8) and progressive for goal reaching/velocity control/obstacle avoidance (H=28, T_a=8).
- **Inference efficiency bottleneck**: FPS ranges from 9.5 to 23, far below real-time requirements. If downstream applications need real-time control, consider distillation or more efficient sampling strategies.
- **Training cost**: Approximately 10 GPU days on a single A100, with 15k iterations. The loss decreases only slightly during training, but policy stability and motion semantic fidelity continue to improve—observe patiently rather than relying solely on the loss curve.

## 개요

UniPhys는 확산 기반 행동 클로닝 프레임워크로, 운동 계획과 제어를 단일 모델로 통합하여 상태와 행동 분포를 공동으로 모델링하고 텍스트를 조건으로 사용합니다. Diffusion Forcing 훈련 패러다임, 잠재 행동 표현, 유연한 노이즈 제거 스케줄링을 통해 작업별 미세 조정 없이 물리적 캐릭터 제어를 가능하게 하며, 텍스트 기반 제어, 속도 제어, 목표 도달, 장애물 회피 등 다양한 작업을 지원합니다.

## 무엇이 바뀌었는가

이전 연구(예: CLoSD)는 확산 플래너와 RL 추적 컨트롤러를 분리하여 도메인 격차와 운동 품질 저하를 초래했으며, 특정 작업에 대한 미세 조정이 필요했습니다. UniPhys는 이러한 패러다임을 근본적으로 변화시킵니다. 더 이상 계획과 제어를 두 개의 독립적인 모듈로 간주하지 않고, 상태와 행동 분포를 공동으로 모델링하여 계층적 차이를 제거합니다. 이는 하위 작업이 더 이상 새로운 제어 신호마다 컨트롤러를 재훈련할 필요가 없고, 테스트 시점의 유도 손실을 통해 직접 적응함을 의미합니다. 이는 물리적 캐릭터 제어의 배포 방식을 '훈련 시 맞춤화'에서 '추론 시 유도'로 근본적으로 변화시킵니다.

## 방법 분석

### 행동 표현
- 행동 시퀀스 X = x_{1:T}, 여기서 x_t = (s^c_t, z_t), z_t ∈ R^32는 PULSE 인코더로 얻은 잠재 행동 표현
- 상태 S^c는 전역 루트 궤적(위치, 방향, 선속도, 각속도)과 로컬 관절 특징(위치, 속도, 회전, 각속도)을 포함하며, 회전은 6D 표현 사용
- 프레임당 특징 차원은 398(32차원 잠재 행동 + 366차원 상태)

### 훈련
- Diffusion Forcing 패러다임을 따르며, 각 프레임에 독립적으로 무작위 노이즈 수준 k = k_{1:T} ∈ [K]^T 주입
- 12층 인과 트랜스포머 디코더 사용, 은닉 크기 768, CLIP 텍스트 임베딩을 조건으로 사용
- 훈련 손실: L(θ) = E_{k,X^0}[||X^0 - M_θ(X^k, k, c)||^2]
- 훈련 시 시퀀스를 32프레임 조각으로 분할, 스텝 8, 배치 크기 1024, 학습률 1.5 × 10⁻⁴

### 추론
- 롤링 호라이즌 정책 및 자기회귀 행동 합성, 유연한 노이즈 제거 구성 지원(히스토리 길이 h, 예측 호라이즌 H, 행동 실행 스텝 수 T_a, 노이즈 제거 스케줄링)
- 안정화 기법: 노이즈 수준 행렬의 0 값을 안정화 노이즈 수준 n으로 대체하여 모델의 과신 방지
- 몬테카를로 유도(MCG)는 여러 샘플에서 기울기를 추정하여 분산 감소

### 작업별 유도
- 목표 도달: 위치 손실 + 방향 손실, H=28, T_a=8
- 속도 제어: 속도 크기 + 방향 + 지향 손실, H=28, T_a=8
- 장애물 회피: SDF 기반 softplus 평활화 손실, H=28, T_a=8

## 핵심 혁신

1. **계획과 제어의 통합**: 계획과 제어를 단일 확산 모델로 진정으로 통합한 최초의 연구. 이전 방법은 2단계(플래너+컨트롤러)이거나 순수 운동학적이었습니다. UniPhys는 상태와 행동 분포를 공동으로 모델링하여 계층적 차이를 제거하고, 테스트 시 손실 유도를 통해 미세 조정 없이 새 작업에 직접 적응할 수 있게 합니다.

2. **프레임별 독립 노이즈 주입**: 기존 확산 모델이 전체 시퀀스에 균일한 노이즈를 적용하는 것과 달리, UniPhys는 각 프레임에 독립적으로 노이즈 수준을 주입합니다. 이 설계는 추론 시 유연한 노이즈 구성(자기회귀 노이즈 제거, 점진적 노이즈 제거 등)을 처리할 수 있게 하며, 물리 시뮬레이터가 도입하는 분포 이동 문제를 해결합니다.

3. **안정화 기법**: 노이즈 제거 과정에서 완전히 노이즈가 제거된 프레임의 노이즈 지시자를 0보다 크게 설정하고, 상태-행동 예측에 노이즈를 추가하지 않고 노이즈 지시자만 조정합니다. 이 간단한 기법은 장기 전개 안정성을 크게 개선하여 평균 에피소드 길이를 148.2에서 2320.3으로 향상시킵니다(자기회귀 노이즈 제거 구성 기준).

## 실험 및 결과

### 텍스트 기반 제어(표 2)
| 방법 | 정확도 | 자연스러움 | 사실감 | 평활도 | 부유 (mm) | 떨림 (mm/s³) |
|------|--------|--------|--------|--------|-----------|-------------|
| Phys-GT | - | 3.43 | 3.53 | 3.42 | 17.0 | 1.1 |
| CLoSD | 61.6% | 2.86 | 3.06 | 2.86 | 20.59 | 3.4 |
| MaskedMimic | 42.9% | 2.82 | 2.93 | 2.78 | 14.82 | 4.2 |
| UniPhys | 56.3% | 3.23 | 3.28 | 3.15 | 16.6 | 1.2 |

UniPhys는 자연스러움, 사실감, 평활도에서 CLoSD와 MaskedMimic보다 크게 우수하며, 떨림 지표는 Phys-GT에 근접하지만(1.2 vs 1.1), 의미론적 정확도는 CLoSD보다 약간 낮습니다.

### 다중 작업 제어(표 3)
| 작업 | CLoSD | MaskedMimic | UniPhys |
|------|-------|-------------|---------|
| 목표 도달 [walk]-근거리 | 1.0 | 0.87 | 1.0 |
| 목표 도달 [walk]-원거리 | 1.0 | 0.0 | 0.95 |
| 목표 도달 [jog] | 0.64 | 0.81* | 0.85 |
| 속도 제어 오차 (m/s) | - | 0.09 | 0.07 |
| 속도 제어 떨림 (mm/s³) | - | 5.0 | 2.0 |

*MaskedMimic은 원거리 목표를 처리할 수 없어 근거리 목표(2m 이내)로 설정했으며, CLoSD와 UniPhys는 6m 이내 목표로 설정했습니다.

### 절제 연구(표 4)
| 구성 | 평균 에피소드 길이 | 최대 에피소드 길이 | FPS |
|------|-------------|-------------|-----|
| 전체 시퀀스 노이즈 제거 | 231.6 | 1197 | 23.0 |
| 점진적 노이즈 제거 | 1817.6 | 3000 | 18.0 |
| 자기회귀(잠재 행동 없음, 안정화 없음) | 58.0 | 59 | 9.5 |
| 자기회귀(잠재 행동 없음, 안정화 있음) | 238.2 | 717 | 9.5 |
| 자기회귀(잠재 행동 있음, 안정화 없음) | 148.2 | 230 | 9.5 |
| 자기회귀(잠재 행동 있음, 안정화 있음) | 2320.3 | 3000 | 9.5 |

잠재 행동 표현과 안정화 기법은 장기 안정성에 필수적이며, 둘 중 하나라도 없으면 안 됩니다.

### MCG 절제 연구(표 A.1)
| N | 성공률 | FPS |
|---|--------|-----|
| 1 | 26% | 9.2 |
| 3 | 82% | 8.9 |
| 5 | 98% | 8.7 |

## 경계 및 한계

- **추론 효율성 저하**: 확산 프레임워크의 추론 효율성은 RL 정책보다 낮으며, 텍스트 기반 제어에서 약 10 FPS(자기회귀 노이즈 제거) 및 18 FPS(점진적 노이즈 제거)로 실시간 제어 요구를 충족하기 어렵습니다.
- **균형 손실과 넘어짐**: 고도로 동적인 동작이나 텍스트 지시 전환 시점이 좋지 않을 때 여전히 균형 손실이 발생하여 기술 전환 실패와 넘어짐이 발생할 수 있습니다. 넘어짐을 완전히 방지하는 것은 현실적이지 않습니다.
- **넘어짐 회복 미구현**: 바닥에서 일어나는 전문가 시연을 수집하고 전용 RL 정책을 훈련할 계획이지만 아직 완료되지 않았습니다.
- **지형 적응성 미구현**: 지형별 데이터가 부족하여 행동 클로닝 프레임워크에서 다양한 지형 보행을 즉시 구현할 수 없습니다.
- **손재주 손 제어 미포함**: 현재 방법은 손재주 손 제어를 포함하지 않아 인간-물체 상호작용 작업이 제한됩니다.
- **미지 작업 제로샷 일반화**: 논문은 미지 작업 유형에 대한 제로샷 일반화 테스트 세부 사항을 언급하지 않았습니다.

## 엔지니어링 시사점

- **데이터 파이프라인 검증**: 데이터셋 구축은 PULSE 추적 정책이 AMASS 모션을 추적하고 성공적으로 추적된 시퀀스를 저장하는 방식에 의존합니다. 재현 시 PULSE의 추적 성공률을 확인해야 합니다. 실패한 시퀀스는 폐기되어 데이터 편향을 유발할 수 있기 때문입니다.
- **안정화 노이즈 수준은 핵심 하이퍼파라미터**: 텍스트 기반 제어는 안정화 노이즈 수준 3을 사용합니다(1, 2, 3이 충분하며, 5로 증가하면 안정성이 저하됨). 손실 기반 유도는 더 높은 안정화 노이즈 수준이 필요하므로 작업별로 파라미터를 조정해야 합니다.
- **MCG 샘플 수가 성공률에 영향**: N=1일 때 성공률은 26%에 불과하지만, N=5일 때 98%에 도달합니다. 계산 자원이 허용된다면 N=5를 우선 사용하세요.
- **노이즈 제거 스케줄링 선택**: 자기회귀 노이즈 제거는 더 안정적인 전개를 생성하고, 점진적 노이즈 제거는 장기 계획 작업 성능을 향상시킵니다. 텍스트 기반 제어는 자기회귀(H=8)를, 목표 도달/속도 제어/장애물 회피는 점진적(H=28, T_a=8)을 사용합니다.
- **추론 효율성 병목**: FPS는 9.5-23 사이로 실시간 요구보다 훨씬 낮습니다. 하위 작업에서 실시간 제어가 필요하다면 증류 또는 더 효율적인 샘플링 전략을 고려해야 합니다.
- **훈련 비용**: 단일 A100 GPU에서 약 10 GPU 일, 15k 라운드. 훈련 중 손실은 약간만 감소하지만 정책 안정성과 운동 의미론적 충실도는 지속적으로 개선되므로, 손실 곡선만 보지 말고 인내심을 가지고 관찰해야 합니다.
