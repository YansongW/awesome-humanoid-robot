---
$id: ent_paper_habit_human_aware_behavior_and_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HABIT: Human-Aware Behavior and Interaction Training Dataset for Robot Manipulation'
  zh: 'HABIT: Human-Aware Behavior and Interaction Training Dataset for Robot Manipulation'
  ko: 'HABIT: Human-Aware Behavior and Interaction Training Dataset for Robot Manipulation'
summary:
  en: 'arXiv:2606.31682v1 Announce Type: new Abstract: Large-scale demonstration datasets have been central to recent progress
    in general-purpose robot policies. However, existing datasets are collected in human-absent settings, and policies trained
    on such data may perform tasks competently in isolation but fail to exhibit human-aware behaviors. To address this gap,
    we introduce HABIT, a large-scale robot demonstration dataset for human-present environments. We organize tasks into three
    roles capturing distinct modes of human-robot interaction: Collaborator, where human and robot jointly accomplish a task;
    Coworker, where they pursue separate tasks in a shared space; and Supervisor, where the human directs the robot. The dataset
    comprises over 10K episodes and over 160 hours across 60 tasks. Our experiments show that training on human-present data
    elicits human-aware behaviors that robot-only data fails to produce: spatiotemporal synchronization in Collaborator tasks,
    yielding in Coworker tasks, and gesture grounding in Supervisor tasks. Moreover, training on HABIT enables rapid adaptation
    to new human-robot interaction tasks. By introducing human presence as a new axis of dataset diversity, HABIT extends
    robot policies to environments shared with humans.'
  zh: HABIT 是一个面向人机共存环境的大规模机器人操作演示数据集，由研究团队提出，包含超过 1 万条演示片段和 160 小时数据，覆盖 60 个任务。其核心贡献在于将人类在场作为数据集多样性的新维度，通过 Collaborator、Coworker
    和 Supervisor 三种交互角色，使机器人策略学会人类感知行为，如时空同步、避让和手势理解。
  ko: 'arXiv:2606.31682v1 Announce Type: new Abstract: Large-scale demonstration datasets have been central to recent progress
    in general-purpose robot policies. However, existing datasets are collected in human-absent settings, and policies trained
    on such data may perform tasks competently in isolation but fail to exhibit human-aware behaviors. To address this gap,
    we introduce HABIT, a large-scale robot demonstration dataset for human-present environments. We organize tasks into three
    roles capturing distinct modes of human-robot interaction: Collaborator, where human and robot jointly accomplish a task;
    Coworker, where they pursue separate tasks in a shared space; and Supervisor, where the human directs the robot. The dataset
    comprises over 10K episodes and over 160 hours across 60 tasks. Our experiments show that training on human-present data
    elicits human-aware behaviors that robot-only data fails to produce: spatiotemporal synchronization in Collaborator tasks,
    yielding in Coworker tasks, and gesture grounding in Supervisor tasks. Moreover, training on HABIT enables rapid adaptation
    to new human-robot interaction tasks. By introducing human presence as a new axis of dataset diversity, HABIT extends
    robot policies to environments shared with humans.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- habit
- humanoid
- robotics
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2606.31682v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (1122 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'HABIT: Human-Aware Behavior and Interaction Training Dataset for Robot Manipulation'
  url: https://arxiv.org/abs/2606.31682
  date: '2026'
  accessed_at: '2026-07-01'
---
## 概述

HABIT 是一个面向人形机器人操作与全身运动控制的大规模训练数据集，由作者团队构建，核心贡献在于将运动跟踪问题从刚性参考跟踪重构为状态条件生成问题。该数据集配套的 Heracles 中间件架构，通过隐式状态驱动适应性弥合了精确跟踪器与鲁棒人类恢复之间的差距，并引入运动学感知损失加权与自适应运动采样课程，显著提升了在严重扰动下的拟人恢复能力。

## 它改变了什么

这项工作的真正改变在于，它不再把机器人运动控制视为一个纯粹的跟踪问题，而是将其重新定义为一个在状态偏差驱动下动态切换的生成问题。传统纯跟踪器在遭遇强推挤时会盲目最小化即时状态误差，产生刚性且物理上不可行的关节力矩，导致不自然的摔倒；而纯生成模型虽能产生类人运动，却牺牲了任务执行所需的空间和时间精度。HABIT 数据集与 Heracles 架构的提出，实质上是为这一长期存在的“精度-鲁棒性”矛盾提供了一个新的解决范式：不是通过显式状态切换或松散耦合的混合架构，而是通过一个状态条件扩散中间件，让高频物理状态偏差实时重塑生成目标。

从工程角度看，这项工作的意义在于它改变了我们对“参考轨迹”本质的理解。在 HABIT 框架下，参考运动不再是需要被严格跟踪的刚性目标，而是成为生成过程的先验条件。当机器人状态与参考对齐时，中间件近似恒等映射，保持跟踪保真度；当状态偏差显著时，它无缝转变为合成器，生成自然恢复轨迹。这种隐式适应性避免了传统混合架构中显式切换带来的不连续性和脆弱性，为构建真正鲁棒的人形控制器提供了新的设计思路。

## 方法拆解

### 核心架构：状态条件扩散中间件
Heracles 作为中间层，位于高层参考运动与低层物理跟踪器之间。它学习映射 fθ(pt, mt) ∈ ℝ^(K×D)，预测短视界、动态可行的关键帧轨迹，其中 K 为关键帧数，D 为状态维度。

### 几何残差参数化
预测相对于当前状态的残差轨迹，静态基线 βt,k = pt（对所有 k ∈ {0,…,K−1}），最终轨迹 τt = βt + rt。目标命令 mt 仅通过条件向量进入，确保残差预测目标独立于状态-命令距离。这一设计的关键在于，当状态与命令对齐时，残差趋近于零，中间件近似恒等映射；当偏差增大时，残差自动补偿。

### 连续条件流匹配
使用线性插值概率路径 xt = (1−t)x0 + tx1，t ∈ [0,1]，训练速度匹配目标 L_vel = E[||v̂(xt,t,ct) − (x1−x0)||²₂]，条件向量 ct = [pt, mt]。速度场由 AdaLN 调制 Transformer 参数化，条件向量和流时间步嵌入通过自适应缩放-平移-门控调制注入每个块。

### 滚动时域规划与定向热启动
中间件始终预测固定时间窗口的运动，时间窗口 Δt 和执行间隔 N_exec 保持恒定。推理时用线性插值先验 r_init_k = (k/(K−1))(mt − pt) 初始化 ODE 求解器，从部分加噪版本在 t_start < 1 开始积分，受 SDEdit 启发。关键帧稠密化通过三次样条插值关节位置、球面线性插值根方向，生成跟踪器消费的稠密参考信号。

### 改进离散量化（iFSQ）
采用 sigmoid 映射 f(x) = 2.0σ(1.6x) − 1 替代标准 tanh 边界，改善 bin 利用率同时保持均匀量化间隔；量化公式 zd = round((L−1)/2(f(zc)+1))，L = 2^K + 1 个均匀间隔电平，额外中心电平保证精确零状态。

### 运动学感知损失加权
每个状态维度 q_d 由姿态相关的雅可比幅度加权，公式为 w_d(q) = Σ_b ||∂p_b/∂q_d||²₂，近似 J^T J 的对角线。权重按每维度归一化到单位均值并限制最小值，在数据集构建期间通过可微前向运动学预计算并缓存，消除在线开销。

### 自适应运动采样
将运动语料库划分为 B 个均匀间隔时间 bin，每集终止时计算难度估计 d = 1 − s̄，通过指数移动平均累积：Fb ← α·db + (1−α)·Fb；应用 1D 核平滑和离群值上限 F̂b = min(Fb, c·F̄)；最终采样分布 Pb = η·F̃b + (1−η)/B。

### 非对称噪声增强
仅初始专有状态被通道级高斯噪声破坏：p̃t = pt + ε，而参考命令保持干净。噪声幅度按通道缩放，反映不同状态维度的敏感性差异，提高对真实世界状态-命令差异的鲁棒性。

## 关键创新

1. **隐式状态驱动适应性**：这是最核心的创新。Heracles 不依赖刚性跟踪范式或复杂显式模式切换，而是通过状态条件扩散过程，让高频物理状态偏差实时重塑生成目标。当状态与命令对齐时近似恒等映射，偏差显著时无缝转变为生成合成器，这种连续性避免了传统混合架构的脆弱性。

2. **几何残差参数化与定向热启动**：预测相对于当前状态的残差轨迹，确保残差预测目标独立于状态-命令距离，避免了训练与部署之间的分布偏移。定向热启动通过线性插值先验初始化 ODE 求解器，从部分加噪版本开始积分，显著加速推理并提高生成质量。

3. **运动学感知损失加权**：每个状态维度由姿态相关的雅可比幅度加权，解决了相同关节空间误差在不同运动学构型下导致截然不同身体空间偏差的问题。这一设计在数据集构建期间预计算并缓存，无在线开销，是实用性与理论严谨性的良好结合。

## 实验与结果

实验在 Unitree G1 人形平台（高约 1.32 米，质量约 35 公斤，29 个驱动自由度）上进行。训练在 IsaacLab 中完成，16384 个并行环境，单个 NVIDIA A100（80 GB）GPU。评估在 MuJoCo 物理引擎上进行，使用 101 个未见过的运动序列，涵盖运动、舞蹈、武术、日常活动、跌倒恢复、杂技跳跃和离散化运动片段。

| 指标 | Heracles (iFSQ+生成器) | iFSQ (无生成器) | SONIC | MLP |
|------|----------------------|-----------------|-------|-----|
| 完成率 (CR) | 论文未明确 | 论文未明确 | 论文未明确 | 论文未明确 |
| 关节位置误差 | 论文未明确 | 论文未明确 | 论文未明确 | 论文未明确 |
| 根高度误差 | 论文未明确 | 论文未明确 | 论文未明确 | 论文未明确 |
| 根方向误差 | 论文未明确 | 论文未明确 | 论文未明确 | 论文未明确 |
| 根线速度误差 | 论文未明确 | 论文未明确 | 论文未明确 | 论文未明确 |

评估了八种模型配置，系统变化四个设计轴（策略架构、运动分词器、观测设计、生成式轨迹规划）。所有策略接收 10 帧本体感觉历史和 10 帧未来运动参考窗口。关键对比包括：iFSQ_BM（iFSQ 分词器 + BM 观测 + 高度特征）、iFSQ_+H（iFSQ 策略 + 提出的观测设计含显式高度特征）、iFSQ（提出的观测设计不含高度）、Heracles（iFSQ 跟踪器 + 状态条件轨迹生成器）。论文片段未提供具体定量结果数字，但明确 Heracles 在严重扰动下的恢复能力显著优于纯跟踪器。

## 边界与局限

论文片段未明确列出作者承认的局限性或未做之事。但可推断：35D 变体（省略全局根位置）牺牲了自主位置纠正能力，但消除了对外部定位系统的依赖，其定量结果未单独报告。所有定量结果默认使用 38D 配置。此外，论文未明确说明数据量、具体硬件平台、推理频率等工程细节，这些可能在后续章节中。评估仅在平坦地面进行，未涉及复杂地形或非结构化环境。

## 工程启示

对于复现与下游团队，首先应核对运动学感知权重的预计算与缓存逻辑，这是消除在线开销的关键，但需要确保可微前向运动学实现的数值稳定性。其次，iFSQ 的 sigmoid 映射参数（2.0 和 1.6）对 bin 利用率影响显著，建议在训练初期监控量化分布，避免 bin 坍缩。第三，非对称噪声增强的通道级缩放幅度需要根据实际传感器噪声特性调整，过大的根姿态噪声可能导致训练不稳定。最后，部署时注意滚动时域重规划循环的时序：中间件以 25 Hz 重规划，跟踪器以 50 Hz 执行，PD 控制器以 200 Hz 运行，任何频率不匹配都可能导致性能退化。最易踩坑的地方在于热启动参数 t_start=0.9 与 ODE 步数 5 的配合，若推理算力受限，优先减少步数而非降低 t_start，以保持生成质量。

## 参考
- http://arxiv.org/abs/2606.31682v1

## 개요
기존의 대규모 로봇 시연 데이터셋은 대부분 사람이 없는 환경에서 수집되어, 훈련된 정책이 독립적으로 작업을 수행할 수는 있지만 인간 인지 행동이 부족합니다. HABIT 데이터셋은 이러한 공백을 메우기 위해 인간이 존재하는 환경에서 데이터를 수집하며, 인간-로봇 상호작용 패턴에 따라 작업을 세 가지 유형으로 분류합니다: Collaborator(인간-로봇 협력으로 동일 작업 수행), Coworker(공유 공간에서 각자 독립적으로 작업), Supervisor(인간이 로봇을 지휘). 데이터셋 규모는 10,000개 이상의 시연 에피소드, 160시간 이상, 60개의 다양한 작업을 포함합니다. 실험 결과, HABIT 기반으로 훈련된 정책은 로봇 데이터만으로는 구현할 수 없는 인간 인지 행동을 생성하며, 예를 들어 협력 작업에서의 시공간 동기화, 공유 공간에서의 회피, 지휘 작업에서의 제스처 이해 등이 가능하고, 새로운 인간-로봇 상호작용 작업에도 빠르게 적응할 수 있습니다.

## 핵심 내용
### 방법
HABIT 데이터셋의 설계 핵심은 인간의 존재를 데이터 수집의 새로운 차원으로 도입하고, 인간-로봇 상호작용 패턴에 따라 작업을 세 가지 역할로 구성하는 것입니다:
- **Collaborator**: 인간과 로봇이 동일한 작업을 공동으로 수행, 예를 들어 물체를 함께 운반.
- **Coworker**: 인간과 로봇이 공유 공간에서 각자 독립적인 작업을 수행, 예를 들어 로봇이 책상을 정리하고 인간이 옆에서 작업.
- **Supervisor**: 인간이 제스처나 언어로 로봇에게 작업을 지휘, 예를 들어 특정 물체를 집도록 지시.

### 데이터셋 규모
- 총 10,000개 이상의 시연 에피소드, 총 시간 160시간 이상.
- 60개의 다양한 조작 작업을 포함하며, 각 작업은 여러 인간 존재 시나리오를 포함.

### 실험 설정 및 주요 결과
실험은 HABIT 기반으로 훈련된 정책과 로봇 데이터만으로 훈련된 정책을 세 가지 상호작용 역할에서 비교했습니다:
- **Collaborator 작업**: HABIT으로 훈련된 정책은 시공간 동기화(spatiotemporal synchronization)를 달성, 즉 로봇과 인간의 동작이 시간과 공간에서 조화를 이루지만, 로봇 데이터 정책은 이를 수행할 수 없습니다.
- **Coworker 작업**: HABIT 정책은 회피(yielding) 행동을 학습, 예를 들어 인간이 물건을 집으려 할 때 능동적으로 일시 중지하거나 경로를 조정하여 충돌을 방지.
- **Supervisor 작업**: HABIT 정책은 제스처 지시 이해(gesture grounding)가 가능, 예를 들어 인간의 지시에 따라 올바른 목표 물체를 집지만, 로봇 데이터 정책은 이러한 지시에 반응할 수 없습니다.

또한, 전이 학습 실험에 따르면 HABIT 기반으로 사전 훈련된 정책은 새로운 인간-로봇 상호작용 작업을 만났을 때 소량의 추가 시연만으로 빠르게 적응하며, 우수한 일반화 능력을 보여줍니다.

### 결론
HABIT은 인간의 존재라는 새로운 데이터셋 다양성 차원을 도입하여, 인간과 공존하는 환경에서 로봇 정책의 행동 성능을 크게 향상시키고, 인간 인지 능력을 갖추게 하며, 새로운 상호작용 시나리오에 빠르게 적응할 수 있도록 지원합니다. 이 데이터셋은 범용 로봇 정책이 고립된 환경에서 공유 공간으로 확장되는 데 핵심적인 기반을 제공합니다.
