---
$id: ent_paper_roboomni_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'RoboOmni: Proactive Robot Manipulation in Omni-modal Context'
  zh: RoboOmni：全模态上下文中的主动式机器人操作
  ko: 'RoboOmni: 옴니모달 맥락에서의 주도적 로봇 조작'
summary:
  en: A 2025 VLA system that fuses vision, speech, and environmental sounds to infer user intentions proactively and execute
    actions without explicit text instructions.
  zh: RoboOmni 是 2025 年提出的 VLA 系统，由端到端全模态 LLM 驱动，通过融合视觉、语音与环境声音主动推断用户意图并执行动作，无需显式文本指令。其核心贡献在于引入跨模态上下文指令新设定，并构建了包含 14 万条数据的
    OmniAction 训练集。实验表明，RoboOmni 在成功率、推理速度和意图识别上均优于基于文本或 ASR 的基线方法。
  ko: 2025년 VLA 시스템으로, 시각, 음성, 환경 소리를 융합하여 사용자 의도를 주도적으로 추론하고 명시적 텍스트 지시 없이 액션을 실행함.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- vla
- omni_modal
- proactive
- intention_recognition
- speech
- audio
- multimodal
- manipulation
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2510.23763v3. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_roboomni_paper
  type: paper
  title: 'RoboOmni: Proactive Robot Manipulation in Omni-modal Context'
  url: https://arxiv.org/abs/2510.23763
  date: '2025-10-30'
  accessed_at: '2026-06-22'
- id: src_roboomni_repo
  type: website
  title: OpenMOSS/RoboOmni GitHub Repository
  url: https://github.com/OpenMOSS/RoboOmni
  date: '2025-10-30'
  accessed_at: '2026-06-22'
related_entities:
- id: ent_dataset_omniaction
  relationship: produces
  description:
    en: RoboOmni introduces the OmniAction dataset for training proactive omni-modal VLA models.
    zh: RoboOmni 引入了用于训练主动式全模态 VLA 模型的 OmniAction 数据集。
    ko: RoboOmni은 주도적 옴니모달 VLA 모델 학습을 위한 OmniAction 데이터셋을 소개함.
- id: ent_benchmark_libero
  relationship: serves
  description:
    en: RoboOmni is evaluated on LIBERO benchmarks using the OmniAction-LIBERO split.
    zh: RoboOmni 在 LIBERO 基准上使用 OmniAction-LIBERO 分割进行评估。
    ko: RoboOmni은 OmniAction-LIBERO 분할을 사용하여 LIBERO 벤치마크에서 평가됨.
- id: ent_benchmark_libero_plus
  relationship: serves
  description:
    en: RoboOmni is related to the broader LIBERO robustness evaluation ecosystem, though primary evaluation uses standard
      LIBERO.
    zh: RoboOmni 与更广泛的 LIBERO 鲁棒性评测生态相关，尽管其主要评估使用标准 LIBERO。
    ko: RoboOmni은 더 넓은 LIBERO 견고성 평가 생태계와 관련이 있으나, 주요 평가는 표준 LIBERO를 사용함.
theoretical_depth:
- system
---


## 概述

RoboOmni 是一个端到端全模态 LLM 框架，旨在让机器人从语音、环境音频和视觉的跨模态上下文中主动推断用户意图，而非被动执行显式指令。作者构建了 OmniAction 数据集（141,162 个多模态 episode）与 OmniAction-LIBERO 基准，在六种上下文指令类型上以 85.6% 的平均成功率远超最强基线 NORA（25.9%），并将推理延迟降至级联方案的约一半。

## 它改变了什么

现有 VLA 系统本质上仍是"指令-执行"范式：要么等待文本命令，要么将语音降级为 ASR 文本，丢弃了语调、情感、说话人身份等副语言线索，更完全忽略环境声音。这导致机器人在真实家庭场景中面对模糊对话时（如"把那个东西放过去"）只能盲目猜测或拒绝执行。RoboOmni 真正改变的是将"意图推断"从研究边缘提升为系统核心——它不再假设意图是给定的，而是把意图发现本身作为模型的一等任务，通过多模态线索（谁在说、怎么说、环境发生了什么）来消解歧义，并在必要时主动澄清。这一转变将人机交互从"工具使用"推向"协作理解"，是范式层面的位移。

## 方法拆解

### 架构：Perceiver–Thinker–Talker–Executor
- **Perceiver**：视觉观察 I_t 经 f_v 得嵌入 v_t，音频段 x_t 经 f_s 得嵌入 s_t，与文本上下文 c_t 拼接为统一输入 X_t = [v_t; s_t; c_t]，遵循 Qwen2.5-Omni 的多模态处理流程。
- **Thinker**：LLM 骨干在联合词汇空间 V ∪ A 上自回归生成文本 token、语音表示和动作 token，实现全模态推理。
- **Talker**：将 Thinker 的高层语义表示转为语音波形。
- **Executor**：扩展词汇表加入 2048 个离散动作 token（FAST+ tokenizer），将连续 7-DoF 动作向量 a_t ∈ R^7 编码为短离散序列 r_t ⊂ A。

### 统一训练目标
对话与动作监督统一为自回归最大似然：
L(θ) = L_chat(θ) + L_act(θ) = -E ∑_{k=1}^{K} log p_θ(z_k|X_t, z_{<k})，z_k ∈ V ∪ A
通过批次交错（batch interleaving）组合两类损失，动作以 chunk 预测（大小 6）。

### OmniAction 数据集构建（三阶段）
1. **文本脚本化**：从 Open-X Embodiment 采样 74,645 条轨迹，用 GPT-4o 将原子指令改写为六种上下文指令类型（Sentiment、Overlapping、Non-Verbal、Identity、Dyadic、Triadic）的多轮家庭对话，含过滤、对话合成、交互扩展与意图一致性验证。
2. **听觉实现**：三种神经 TTS（MOSS-TTSD、CosyVoice、Gemini-TTS）合成语音，5,096 种音色（按年龄/性别六类）；多说话人模拟用 CTC 控制重叠偏移；插入 2,482 个非语言事件；叠加 640 种环境背景（受控 SNR）。
3. **验证**：人工评估采样对话，任务意图可恢复性达 98.7% 一致性。

## 关键创新

1. **首个将环境音频纳入 VLA 动作决策的端到端框架**：此前工作要么忽略音频，要么仅用 ASR 转写文本。RoboOmni 让原始音频（含副语言线索与环境声）直接参与意图推断，在 Non-Verbal 指令上（如"听到门铃做 A，听到雷声做 B"）实现 82% 平均成功率，而级联基线几乎失效。
2. **统一 token 空间的对话-动作联合生成**：通过 FAST+ 将连续动作离散化，使聊天与操作共享同一自回归目标，无需专门的扩散头或动作专家（对比 π0），简化了架构并提升训练效率——预训练+SFT 在 2k 步内达近 90% 准确率，而从零 SFT 在 20k 步仅约 30%。
3. **大规模全模态操作数据集 OmniAction**：141,162 个 episode、112 种技能、748 个物体、5,096 种音色、2,482 个声音事件、640 种背景音景，填补了"音频+推断性指令"数据的空白，为后续研究提供公共资源。

## 实验与结果

### OmniAction-LIBERO-TTS 基准（表1）
| 套件 | RoboOmni 平均成功率 | 最佳基线（NORA, Audio→ASR→Text） |
|------|---------------------|-----------------------------------|
| Spatial | 93.0% | 56.5% |
| Goal | 85.8% | 16.3% |
| Object | 84.0% | 13.8% |
| Long | 79.5% | 51.0% |
| **总体** | **85.6%** | **25.9%** |

端到端模型中，dyadic 和 overlapping 任务较易（约 88%），non-verbal 最难（约 82%）。Ground-truth Textual Prompt 下 OpenVLA 仅 2.6%、NORA 16.3%、π0 3.0%，说明基线在显式指令下已表现不佳，面对上下文指令时几乎崩溃。

### OmniAction-LIBERO-Real 基准（表2）
| 模型 | 平均成功率 |
|------|-----------|
| RoboOmni | 76.6% |
| π0 | 73.8% |
| OpenVLA | 40.1% |
| NORA | 17.4% |
| OpenVLA-OFT | 6.5% |

### 意图识别与效率
- 意图识别准确率：RoboOmni 88.9% vs ASR+GPT-4o 55.6% vs Qwen2.5-Omni-7B 50.0%。
- 推理延迟（ASR+OpenVLA 为 1.0×）：RoboOmni 0.49×，ASR+π0 0.96×——端到端省去 ASR 与级联通信开销。

## 边界与局限

论文未明确声明局限性。从事实要点推断：真实世界实验仅在 WidowX 250S 单臂平台验证，未涉及双臂、移动操作或动态环境；数据集中于家庭场景的拾放类任务（112 种技能），未见精细操作（如装配、缝合）或非刚体操作；六种指令类型虽覆盖常见模糊场景，但真实交互中意图推断的复杂度远超此范围（如讽刺、隐喻、跨轮次长期记忆）。训练成本较高（64×A100×10 天预训练），可能限制小团队复现。未报告失败案例分析或泛化边界测试。

## 工程启示

复现时优先核对三点：**FAST+ tokenizer 的离散化参数**（2048 个 token 的码本质量直接决定动作精度，建议先在小数据集上验证重构误差）；**批次交错比例**（对话与动作损失的比例失衡可能导致模型偏向聊天而忽视操作，论文未给出具体权重，需自行调参）；**音频预处理管线**（16kHz 采样率、224×224 图像分辨率、动作 chunk 6 是固定配置，改动可能影响与预训练权重的对齐）。最容易踩坑的是 OmniAction 数据生成中的提示词工程——六种指令类型各有严格约束（如情感线索禁止祈使否定词、身份线索结尾不得直接描述动作），直接套用通用对话模板会破坏意图可恢复性。下游 SFT 建议从预训练 checkpoint 开始而非从零训练，否则 20k 步仅 30% 准确率且 30k 步退化。推理部署用单张 RTX 4090 即可达到 0.49× 延迟，但需注意 Talker 的语音合成质量在嘈杂环境可能下降，建议在真实场景中补充降噪预处理。

## 参考
- http://arxiv.org/abs/2510.23763v3

## Overview

RoboOmni is an end-to-end omni-modal LLM framework designed to enable robots to proactively infer user intent from cross-modal contexts of speech, environmental audio, and vision, rather than passively executing explicit instructions. The authors constructed the OmniAction dataset (141,162 multimodal episodes) and the OmniAction-LIBERO benchmark, achieving an average success rate of 85.6% across six contextual instruction types, far surpassing the strongest baseline NORA (25.9%), while reducing inference latency to approximately half that of cascaded approaches.

## What It Changes

Existing VLA systems remain fundamentally in the "instruction-execution" paradigm: they either wait for text commands or degrade speech to ASR text, discarding paralinguistic cues such as intonation, emotion, and speaker identity, while completely ignoring environmental sounds. This causes robots in real household scenarios to blindly guess or refuse to act when faced with ambiguous dialogue (e.g., "put that thing over there"). What RoboOmni truly changes is elevating "intent inference" from the periphery of research to the core of the system—it no longer assumes intent is given, but instead treats intent discovery itself as a first-class task of the model, resolving ambiguity through multimodal cues (who is speaking, how they speak, what is happening in the environment), and proactively clarifying when necessary. This shift moves human-robot interaction from "tool use" toward "collaborative understanding," representing a paradigm-level displacement.

## Method Breakdown

### Architecture: Perceiver–Thinker–Talker–Executor
- **Perceiver**: Visual observations I_t are embedded via f_v to obtain v_t, audio segments x_t are embedded via f_s to obtain s_t, and concatenated with text context c_t into a unified input X_t = [v_t; s_t; c_t], following the multimodal processing pipeline of Qwen2.5-Omni.
- **Thinker**: The LLM backbone autoregressively generates text tokens, speech representations, and action tokens over the joint vocabulary space V ∪ A, enabling omni-modal reasoning.
- **Talker**: Converts the Thinker's high-level semantic representations into speech waveforms.
- **Executor**: Extends the vocabulary with 2048 discrete action tokens (FAST+ tokenizer), encoding continuous 7-DoF action vectors a_t ∈ R^7 into short discrete sequences r_t ⊂ A.

### Unified Training Objective
Dialogue and action supervision are unified under autoregressive maximum likelihood:
L(θ) = L_chat(θ) + L_act(θ) = -E ∑_{k=1}^{K} log p_θ(z_k|X_t, z_{<k}), z_k ∈ V ∪ A
The two loss types are combined via batch interleaving, with actions predicted in chunks (size 6).

### OmniAction Dataset Construction (Three Stages)
1. **Text Scripting**: Sampled 74,645 trajectories from Open-X Embodiment, using GPT-4o to rewrite atomic instructions into multi-turn household dialogues across six contextual instruction types (Sentiment, Overlapping, Non-Verbal, Identity, Dyadic, Triadic), including filtering, dialogue synthesis, interaction expansion, and intent consistency verification.
2. **Auditory Implementation**: Three neural TTS systems (MOSS-TTSD, CosyVoice, Gemini-TTS) synthesized speech with 5,096 voice timbres (categorized into six classes by age/gender); multi-speaker simulation used CTC to control overlap offsets; 2,482 non-verbal events were inserted; 640 environmental backgrounds were overlaid (with controlled SNR).
3. **Verification**: Human evaluation of sampled dialogues confirmed 98.7% consistency in task intent recoverability.

## Key Innovations

1. **First end-to-end framework incorporating environmental audio into VLA action decisions**: Prior work either ignored audio or used only ASR-transcribed text. RoboOmni allows raw audio (including paralinguistic cues and environmental sounds) to directly participate in intent inference, achieving 82% average success on Non-Verbal instructions (e.g., "do A when you hear the doorbell, do B when you hear thunder"), where cascaded baselines nearly fail.
2. **Unified token-space joint generation of dialogue and actions**: By discretizing continuous actions via FAST+, chat and manipulation share the same autoregressive objective without requiring specialized diffusion heads or action experts (contrast π0), simplifying the architecture and improving training efficiency—pretraining + SFT reaches nearly 90% accuracy within 2k steps, whereas SFT from scratch reaches only ~30% at 20k steps.
3. **Large-scale omni-modal manipulation dataset OmniAction**: 141,162 episodes, 112 skills, 748 objects, 5,096 voice timbres, 2,482 sound events, and 640 background soundscapes, filling the gap in "audio + inferential instruction" data and providing a public resource for subsequent research.

## Experiments and Results

### OmniAction-LIBERO-TTS Benchmark (Table 1)
| Suite | RoboOmni Average Success | Best Baseline (NORA, Audio→ASR→Text) |
|------|--------------------------|---------------------------------------|
| Spatial | 93.0% | 56.5% |
| Goal | 85.8% | 16.3% |
| Object | 84.0% | 13.8% |
| Long | 79.5% | 51.0% |
| **Overall** | **85.6%** | **25.9%** |

Among end-to-end models, dyadic and overlapping tasks are easier (~88%), while non-verbal is the hardest (~82%). Under ground-truth textual prompts, OpenVLA achieves only 2.6%, NORA 16.3%, and π0 3.0%, indicating that baselines already perform poorly with explicit instructions and nearly collapse when faced with contextual instructions.

### OmniAction-LIBERO-Real Benchmark (Table 2)
| Model | Average Success |
|-------|-----------------|
| RoboOmni | 76.6% |
| π0 | 73.8% |
| OpenVLA | 40.1% |
| NORA | 17.4% |
| OpenVLA-OFT | 6.5% |

### Intent Recognition and Efficiency
- Intent recognition accuracy: RoboOmni 88.9% vs ASR+GPT-4o 55.6% vs Qwen2.5-Omni-7B 50.0%.
- Inference latency (ASR+OpenVLA as 1.0×): RoboOmni 0.49×, ASR+π0 0.96×—end-to-end eliminates ASR and cascaded communication overhead.

## Boundaries and Limitations

The paper does not explicitly declare limitations. Inferred from factual points: real-world experiments were validated only on the WidowX 250S single-arm platform, without dual-arm, mobile manipulation, or dynamic environments; the data focuses on household pick-and-place tasks (112 skills), with no fine manipulation (e.g., assembly, suturing) or non-rigid object manipulation; the six instruction types cover common ambiguous scenarios, but real-world intent inference complexity far exceeds this scope (e.g., sarcasm, metaphor, cross-turn long-term memory). Training costs are high (64×A100×10 days pretraining), potentially limiting reproduction by small teams. No failure case analysis or generalization boundary tests were reported.

## Engineering Insights

When reproducing, prioritize verifying three points: **FAST+ tokenizer discretization parameters** (the codebook quality of 2048 tokens directly determines action precision; validate reconstruction error on a small dataset first); **batch interleaving ratio** (imbalanced dialogue-to-action loss ratios may bias the model toward chatting over manipulation; the paper does not provide specific weights, requiring self-tuning); **audio preprocessing pipeline** (16kHz sampling rate, 224×224 image resolution, action chunk 6 are fixed configurations; changes may affect alignment with pretrained weights). The most likely pitfall is prompt engineering in OmniAction data generation—each of the six instruction types has strict constraints (e.g., sentiment cues prohibit imperative negations, identity cues must not directly describe actions at the end), and directly applying generic dialogue templates will break intent recoverability. For downstream SFT, start from the pretrained checkpoint rather than training from scratch; otherwise, accuracy reaches only 30% at 20k steps and degrades at 30k steps. Inference deployment on a single RTX 4090 achieves 0.49× latency, but note that Talker's speech synthesis quality may degrade in noisy environments; consider adding denoising preprocessing in real-world scenarios.

## 개요

RoboOmni는 음성, 환경 오디오 및 시각의 교차 모달 컨텍스트에서 로봇이 명시적 지시를 수동적으로 수행하는 대신 사용자 의도를 능동적으로 추론하도록 설계된 엔드투엔드 전모달 LLM 프레임워크입니다. 저자는 OmniAction 데이터셋(141,162개의 멀티모달 에피소드)과 OmniAction-LIBERO 벤치마크를 구축했으며, 여섯 가지 컨텍스트 지시 유형에서 평균 성공률 85.6%로 최강 기준선 NORA(25.9%)를 크게 능가하고 추론 지연 시간을 캐스케이드 방식의 약 절반으로 줄였습니다.

## 무엇이 바뀌었는가

기존 VLA 시스템은 본질적으로 여전히 "지시-실행" 패러다임입니다. 텍스트 명령을 기다리거나 음성을 ASR 텍스트로 격하시켜 억양, 감정, 화자 정체성과 같은 준언어적 단서를 버리고 환경 소리를 완전히 무시합니다. 이로 인해 로봇은 실제 가정 환경에서 모호한 대화(예: "그거 저쪽에 놔")에 직면했을 때 맹목적으로 추측하거나 실행을 거부할 수밖에 없습니다. RoboOmni가 진정으로 바꾼 것은 "의도 추론"을 연구의 주변부에서 시스템의 핵심으로 끌어올린 것입니다. 더 이상 의도가 주어졌다고 가정하지 않고, 의도 발견 자체를 모델의 일급 과제로 삼아 멀티모달 단서(누가 말하는지, 어떻게 말하는지, 환경에서 무슨 일이 일어나는지)를 통해 모호성을 해소하고 필요 시 적극적으로 명확화합니다. 이러한 전환은 인간-로봇 상호작용을 "도구 사용"에서 "협력적 이해"로 끌어올리는 패러다임 차원의 이동입니다.

## 방법 분해

### 아키텍처: Perceiver–Thinker–Talker–Executor
- **Perceiver**: 시각 관측 I_t를 f_v를 통해 임베딩 v_t로, 오디오 세그먼트 x_t를 f_s를 통해 임베딩 s_t로 변환하고 텍스트 컨텍스트 c_t와 결합하여 통합 입력 X_t = [v_t; s_t; c_t]를 구성합니다. Qwen2.5-Omni의 멀티모달 처리 흐름을 따릅니다.
- **Thinker**: LLM 백본이 결합 어휘 공간 V ∪ A에서 텍스트 토큰, 음성 표현 및 행동 토큰을 자기회귀적으로 생성하여 전모달 추론을 수행합니다.
- **Talker**: Thinker의 고수준 의미 표현을 음성 파형으로 변환합니다.
- **Executor**: 확장 어휘에 2048개의 이산 행동 토큰(FAST+ 토크나이저)을 추가하여 연속 7-DoF 행동 벡터 a_t ∈ R^7을 짧은 이산 시퀀스 r_t ⊂ A로 인코딩합니다.

### 통합 훈련 목표
대화와 행동 감독이 자기회귀 최대우도로 통합됩니다:
L(θ) = L_chat(θ) + L_act(θ) = -E ∑_{k=1}^{K} log p_θ(z_k|X_t, z_{<k}), z_k ∈ V ∪ A
배치 인터리빙을 통해 두 손실을 결합하며, 행동은 청크 예측(크기 6)으로 수행됩니다.

### OmniAction 데이터셋 구축(3단계)
1. **텍스트 스크립트화**: Open-X Embodiment에서 74,645개 궤적을 샘플링하고 GPT-4o로 원자적 지시를 여섯 가지 컨텍스트 지시 유형(Sentiment, Overlapping, Non-Verbal, Identity, Dyadic, Triadic)의 다중 턴 가정 대화로 재작성합니다. 필터링, 대화 합성, 상호작용 확장 및 의도 일관성 검증을 포함합니다.
2. **청각 구현**: 세 가지 신경 TTS(MOSS-TTSD, CosyVoice, Gemini-TTS)로 음성을 합성하며 5,096가지 음색(연령/성별 6개 범주)을 사용합니다. 다중 화자 시뮬레이션은 CTC로 중첩 오프셋을 제어하고, 2,482개의 비언어적 이벤트를 삽입하며, 640가지 환경 배경음을(제어된 SNR) 중첩합니다.
3. **검증**: 샘플 대화를 인간 평가하여 작업 의도 회복 가능성이 98.7% 일관성을 달성합니다.

## 핵심 혁신

1. **환경 오디오를 VLA 행동 결정에 포함한 최초의 엔드투엔드 프레임워크**: 이전 연구는 오디오를 무시하거나 ASR 전사 텍스트만 사용했습니다. RoboOmni는 원시 오디오(준언어적 단서와 환경음을 포함)가 의도 추론에 직접 참여하도록 하여 Non-Verbal 지시(예: "초인종 소리가 나면 A, 천둥 소리가 나면 B")에서 평균 성공률 82%를 달성했으며, 캐스케이드 기준선은 거의 실패했습니다.
2. **통합 토큰 공간에서의 대화-행동 공동 생성**: FAST+를 통해 연속 행동을 이산화하여 채팅과 조작이 동일한 자기회귀 목표를 공유하도록 했습니다. 전용 확산 헤드나 행동 전문가(π0와 대비)가 필요 없어 아키텍처가 단순해지고 훈련 효율이 향상됩니다. 사전 훈련+SFT는 2k 스텝 내에 약 90% 정확도에 도달하는 반면, 처음부터 SFT는 20k 스텝에서 약 30%에 불과합니다.
3. **대규모 전모달 조작 데이터셋 OmniAction**: 141,162개 에피소드, 112가지 기술, 748개 물체, 5,096가지 음색, 2,482개 음향 이벤트, 640가지 배경 음향으로 "오디오+추론적 지시" 데이터의 공백을 메우고 후속 연구에 공공 자원을 제공합니다.

## 실험 및 결과

### OmniAction-LIBERO-TTS 벤치마크(표 1)
| 스위트 | RoboOmni 평균 성공률 | 최강 기준선(NORA, Audio→ASR→Text) |
|------|---------------------|-----------------------------------|
| Spatial | 93.0% | 56.5% |
| Goal | 85.8% | 16.3% |
| Object | 84.0% | 13.8% |
| Long | 79.5% | 51.0% |
| **전체** | **85.6%** | **25.9%** |

엔드투엔드 모델에서 dyadic 및 overlapping 작업은 비교적 쉬웠고(약 88%), non-verbal이 가장 어려웠습니다(약 82%). Ground-Truth 텍스트 프롬프트에서 OpenVLA는 2.6%, NORA는 16.3%, π0는 3.0%로, 기준선이 명시적 지시에서도 성능이 낮았으며 컨텍스트 지시에서는 거의 붕괴했음을 보여줍니다.

### OmniAction-LIBERO-Real 벤치마크(표 2)
| 모델 | 평균 성공률 |
|------|-----------|
| RoboOmni | 76.6% |
| π0 | 73.8% |
| OpenVLA | 40.1% |
| NORA | 17.4% |
| OpenVLA-OFT | 6.5% |

### 의도 인식 및 효율성
- 의도 인식 정확도: RoboOmni 88.9% vs ASR+GPT-4o 55.6% vs Qwen2.5-Omni-7B 50.0%.
- 추론 지연 시간(ASR+OpenVLA를 1.0×로 기준): RoboOmni 0.49×, ASR+π0 0.96× — 엔드투엔드 방식이 ASR 및 캐스케이드 통신 오버헤드를 제거합니다.

## 경계 및 한계

논문은 명시적 한계를 선언하지 않았습니다. 사실적 요점에서 추론하면: 실제 세계 실험은 WidowX 250S 단일 암 플랫폼에서만 검증되었으며 이중 암, 이동 조작 또는 동적 환경을 다루지 않았습니다. 데이터는 가정 환경의 집기-놓기 유형 작업(112가지 기술)에 집중되어 있으며 정밀 조작(예: 조립, 봉제)이나 비강체 조작은 포함되지 않았습니다. 여섯 가지 지시 유형이 일반적인 모호한 시나리오를 다루지만 실제 상호작용의 의도 추론 복잡성은 이 범위를 훨씬 초과합니다(예: 풍자, 은유, 교차 턴 장기 기억). 훈련 비용이 높아(64×A100×10일 사전 훈련) 소규모 팀의 재현을 제한할 수 있습니다. 실패 사례 분석이나 일반화 경계 테스트는 보고되지 않았습니다.

## 공학적 시사점

재현 시 우선 세 가지를 확인하세요: **FAST+ 토크나이저의 이산화 매개변수**(2048개 토큰의 코드북 품질이 행동 정밀도를 직접 결정하므로 소규모 데이터셋에서 재구성 오류를 먼저 검증할 것을 권장); **배치 인터리빙 비율**(대화와 행동 손실의 불균형이 모델을 채팅에 치우치게 하고 조작을 무시하게 할 수 있으며, 논문은 구체적 가중치를 제시하지 않아 자체 튜닝 필요); **오디오 전처리 파이프라인**(16kHz 샘플링 레이트, 224×224 이미지 해상도, 행동 청크 6은 고정 구성으로 변경 시 사전 훈련 가중치와의 정렬에 영향을 줄 수 있음). 가장 함정에 빠지기 쉬운 부분은 OmniAction 데이터 생성의 프롬프트 엔지니어링입니다. 여섯 가지 지시 유형은 각각 엄격한 제약이 있으며(예: 감정 단서는 명령형 부정어 금지, 정체성 단서는 끝부분에서 행동을 직접 설명하지 않아야 함), 일반 대화 템플릿을 그대로 적용하면 의도 회복 가능성이 손상됩니다. 다운스트림 SFT는 처음부터 훈련하는 대신 사전 훈련 checkpoint에서 시작하는 것이 좋습니다. 그렇지 않으면 20k 스텝에서 30% 정확도에 불과하고 30k 스텝에서는 성능이 저하됩니다. 추론 배포는 단일 RTX 4090으로 0.49× 지연 시간을 달성할 수 있지만, Talker의 음성 합성 품질이 시끄러운 환경에서 저하될 수 있으므로 실제 시나리오에서 잡음 제거 전처리를 추가하는 것이 좋습니다.
