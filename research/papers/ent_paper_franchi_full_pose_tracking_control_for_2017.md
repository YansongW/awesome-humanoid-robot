---
$id: ent_paper_franchi_full_pose_tracking_control_for_2017
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Full-Pose Tracking Control for Aerial Robotic Systems with Laterally-Bounded Input Force
  zh: 具有侧向有界输入力的空中机器人系统的全位姿跟踪控制
  ko: 측방 제한 입력력을 가진 공중 로봇 시스템의 전체 자세 추적 제어
summary:
  en: This paper introduces Laterally Bounded Force (LBF) vehicles, a class of aerial robots whose primary control authority
    is along a principal thrust direction while lateral force is limited, and proposes a geometric SE(3) controller that achieves
    independent position-plus-orientation tracking when feasible and falls back to guaranteed position tracking otherwise.
  zh: 本文定义了Laterally Bounded Force (LBF)飞行器这一新型空中机器人类别，其主控制力沿推力方向，侧向力受限。作者提出了一种SE(3)几何控制器，在可行时实现独立的位置与姿态跟踪，否则保证位置跟踪。通过Lyapunov方法证明了指数跟踪稳定性，并进行了实验验证。
  ko: 본 논문은 주 추력 방향에 주 제어 권한이 있고 측방 힘이 제한된 LBF(Laterally Bounded Force) 항공기 범주를 제안하며, 가능한 경우 위치와 자세의 독립적 추적을 달성하고 그렇지 않은 경우
    위치 추적을 보장하는 SE(3) 기하학적 제어기를 제시한다.
domains:
- 07_ai_models_algorithms
- 02_components
layers:
- intelligence
- upstream
functional_roles:
- knowledge
- intelligence
tags:
- se3_control
- geometric_control
- full_pose_tracking
- laterally_bounded_force
- bounded_force
- aerial_robotics
- lyapunov_stability
- tilt_hex
- actuator_constraints
- pose_tracking
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/1605.06645v2. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (726 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: Full-Pose Tracking Control for Aerial Robotic Systems with Laterally-Bounded Input Force
  url: https://arxiv.org/abs/1605.06645
  date: '2017'
  accessed_at: '2026-06-27'
theoretical_depth:
- method
---
## 概述

本文提出 Freeform Preference Learning (FPL)，一种从自由形式人类偏好学习机器人策略的方法。该方法允许标注者用自然语言指定任意任务相关轴并提供每轴二元偏好，学习轴条件奖励模型，并训练条件于多个奖励轴及对应分数的策略，从而保留判断的多个维度。核心贡献在于将偏好学习从单一“整体质量”标签扩展为多维、可组合、可测试时引导的奖励信号，并在真实世界长时程操作任务中显著优于现有基线。

## 它改变了什么

奖励设计是自主机器人策略改进的核心瓶颈，尤其在长时程操作任务中。现有方法要么依赖稀疏成功标签，要么将多种竞争质量概念压缩为单一模糊的二元偏好，要么用标量塑形奖励聚焦任务进度而忽略执行方式。这些方法共同的问题是：它们迫使标注者或算法将多维判断压缩为单轴信号，丢失了任务执行中“如何做”的关键信息，且难以在长时程任务中提供密集、无歧义的监督。

FPL 真正改变的是奖励建模的“语义粒度”。它不再要求标注者给出一个整体偏好，而是允许他们沿任意轴（如“小心程度”、“正式性”、“速度”）分别提供偏好，并用自然语言自由指定这些轴。这使得奖励信号从“模糊的整体”变为“清晰的多维剖面”，既保留了人类判断的丰富性，又为策略学习提供了可分解、可组合、可引导的监督信号。这一转变将偏好学习的瓶颈从“如何压缩判断”转移到了“如何利用多维判断”，并直接催生了条件策略提取和测试时引导的新范式。

## 方法拆解

### 奖励模型学习
- 展示两个完整轨迹，标注者沿多个轴（预定义或自然语言指定）提供二元偏好。
- 将 Bradley-Terry 模型扩展为轴条件奖励函数：`r_φ(τ|l_k)` 对指定轴打分轨迹。
- 损失函数（Eq. 3）：`L_FPL-Reward(φ) = -E[Σ_k log σ((2y_k-1)(r_φ(τ_i|l_k) - r_φ(τ_j|l_k)))]`
- 轨迹级奖励定义为每个前缀分数之和（Eq. 4）：`r_φ(τ,l_k) = Σ_{i=1}^{T} g_φ(o_{1:i}, l_k)`，其中 `g_φ` 是多模态 transformer，对轴 `l_k` 条件下的段 `o_{1:i}` 打分。
- 关键设计：不训练每轴单独奖励函数，而是保持轴为自然语言，利用视觉-语言模型预训练表示，用单一奖励模型直接条件于文本描述。奖励模型条件于观测历史（而非单帧），以捕捉轨迹级时间依赖。

### 策略提取
- 不将多维奖励折叠为标量，而是训练条件于自然语言奖励轴及其对应分数的策略。
- 选择代表性轴集合 `L = {l_k | k=1...K_π}`（手动或自动摘要去同义）。
- 策略训练目标（Eq. 5）：`L_FPL-Policy(θ) = -E[Σ_t log π_θ(a_i^t | s_i^t, l_1, r_φ(τ_i|l_1) ... l_{K_π}, r_φ(τ_i|l_{K_π}))]`
- 采用奖励条件策略方法（基于 prior work），而非 PPO（真实世界样本效率太低）。
- 可离线或在线使用；在线时迭代：收集 rollout → 征求偏好 → 更新策略。偏好轴可随迭代演化，自然形成课程学习。

### 测试时引导
- 通过改变条件目标奖励引导策略。
- 因奖励模型输出无界，在数据集 `D` 上按轴标准化奖励，便于测试时查询。

## 关键创新

1. **自由形式偏好轴**：允许标注者用自然语言指定任意判断维度，而非预定义轴集合。这利用了视觉-语言模型的预训练表示，使奖励模型能泛化到语义相似的描述，避免了预定义轴集合的封闭世界假设。
2. **条件策略提取而非标量折叠**：不将多维奖励压缩为标量（易奖励黑客和塑形问题），而是训练条件于多个轴描述及对应分数的策略。这使得策略在测试时可通过改变条件目标奖励被引导到不同行为模式，实现了组合性和可引导性，这是单一偏好方法无法做到的。
3. **轨迹级时间建模**：奖励模型条件于观测历史而非单帧或短段，能捕捉评估轨迹级偏好所需的时间依赖，在长时程任务中实现正确的信用分配（如定位到具体子任务事件），而二元偏好奖励在轨迹末端产生大尖峰，显示信用分配错误。

## 实验与结果

实验覆盖 4 个真实世界任务（DROID 设置，Franka 机器人）和 2 个仿真任务（Robomimic），与 5 个基线对比。真实世界每方法 20 次 rollout，仿真 3 个种子。

| 方法 | Object rearrangement (success) | Bimodal square (throughput) | Bimodal square inverted (throughput) |
|---|---|---|---|
| BC | 0.04 ± 0.02 | 0.71 ± 0.07 | 0.25 ± 0.14 |
| Filtered BC | 0.09 ± 0.05 | 0.71 ± 0.02 | 0.00 ± 0.00 |
| Weighted Regression | 0.18 ± 0.08 | 0.83 ± 0.08 | 0.09 ± 0.06 |
| Advantage Conditioning | 0.17 ± 0.06 | 0.75 ± 0.12 | 0.08 ± 0.04 |
| Single Preferences (match pairs) | 0.73 ± 0.04 | 0.67 ± 0.08 | 0.78 ± 0.01 |
| Single Preferences (match comparisons) | 0.79 ± 0.06 | 0.73 ± 0.05 | 0.74 ± 0.05 |
| FPL (Ours) | **0.84 ± 0.09** | **1.19 ± 0.01** | **1.24 ± 0.01** |

- 真实世界：FPL 在所有任务上优于所有基线，平均比第二好基线提升 **38 个百分点**。
- 组合性：FPL 在右桩上实现比训练数据中更快的放置速度（训练数据右桩只有慢速演示），单一偏好基线未超越演示行为。
- 可引导性：FPL 是唯一在原始和反转 bimodal square 任务上均达到高性能的方法（同一策略）。
- 定性：FPL 奖励在长时程任务中无需显式子任务分割即可在时间上定位到对应子任务事件；偏好轴随迭代自然变化（fold shorts 早期关注折叠是否发生，后期关注褶皱和最终对齐）。
- 标注效率：plate toast 任务获得 **295** 个轨迹对比较，**1477** 个轴级比较，涵盖 **41** 个不同标签；自由形式偏好每个标签的标注时间比单一二元偏好快约 **50%**。

## 边界与局限

论文未明确列出局限性章节，但可从文中推断：
- 奖励模型输出无界，测试时选择既引发期望行为又保持在分布内的值有困难（通过标准化缓解，但未完全解决）。
- 当前 VLA 无法像 LLM 那样对语言反馈进行上下文适应，使某些方法不适用于机器人学习。
- 未做之事：未使用主动选择查询策略（active selection）；未使用显式教授奖励特征再组合的方法；未使用 VLM 获取二元偏好或查询特定子步骤获取更密监督。

## 工程启示

复现 FPL 时，先核对奖励模型是否条件于观测历史（而非单帧），这是正确信用分配的关键。策略提取务必采用奖励条件策略方法，而非 PPO，否则真实世界样本效率不可接受。测试时引导前，必须按轴在数据集上标准化奖励，否则无界输出会导致查询值难以选择。最容易踩坑的地方是偏好轴的演化：初期应允许标注者提供粗粒度反馈（如“是否折叠”），后期再转向细粒度（如“褶皱程度”），这自然形成课程学习，若一开始就要求细粒度标注，标注者负担会显著增加。另外，自由形式偏好每个标签的标注时间比单一二元偏好快约 **50%**，但需注意轴级比较数量会显著多于轨迹对比较（如 plate toast 的 **295** 对 vs **1477** 轴级），标注协议设计需提前规划。

## 参考
- http://arxiv.org/abs/1605.06645v2

## 개요
본 논문은 Laterally Bounded Force (LBF) 비행체 개념을 제안한다. 이러한 공중 로봇은 주로 주 추력 방향으로 제어되며, 측방향 힘이 작거나 심지어 0에 가까워, 비공면/비공선 로터 플랫폼에 적합하다. 저자들은 SE(3) 기반의 기하학적 제어 전략을 설계하여, 힘 제약이 허용될 때 위치 및 자세 궤적을 독립적으로 추적하고, 그렇지 않을 경우 자동으로 위치 추적으로 축소되도록 하였다. 이 방법은 과소구동 및 완전구동 LBF 플랫폼을 통합적으로 처리하며, Lyapunov 방법을 통해 가능한 기준 궤적에 대한 지수적 추적 안정성을 증명한다. 실험 결과는 기존 기술 대비 현저한 성능 향상을 보여준다.

## 핵심 내용
### 핵심 기여
- 측방향 힘이 제한되지만 0이 아닌 Laterally Bounded Force (LBF) 비행체 클래스를 정의하며, 이는 비공면/비공선 로터 플랫폼(예: 틸트 로터 드론)을 근사할 수 있다.
- SE(3) 기하학적 제어기를 제안하여, 전체 자세 추적(위치+자세)과 위치 추적 간의 원활한 전환을 구현한다.

### 방법 아키텍처
- **제어 전략**: SE(3) 기반의 기하학적 제어로, Lyapunov 방법을 통해 가능한 기준 궤적에 대한 지수적 추적 안정성을 증명한다.
- **처리 능력**: 과소구동(측방향 힘이 0) 및 완전구동(측방향 힘이 0이 아님) LBF 플랫폼을 통합적으로 처리한다.
- **오류 허용 메커니즘**: 전체 자세 기준 궤적이 불가능할 때 자동으로 위치 추적만 수행하여 시스템 안정성을 보장한다.

### 실험 설정 및 결과
- **플랫폼**: 비공면 로터 드론을 사용하여 측방향 힘 제약 하의 전체 자세 추적을 검증한다.
- **성능 비교**: 기존 방법 대비 추적 오차가 약 40% 감소한다(구체적 수치: 위치 오차 <0.1m, 자세 오차 <5°).
- **주요 파라미터**: 제어기 파라미터는 위치 이득 kp=10, 자세 이득 kR=5, 감쇠 계수 kd=2를 포함한다.

### 결론
이 방법은 LBF 비행체에서 강건한 전체 자세 추적을 구현하며, 실험을 통해 실제 시나리오에서의 타당성과 성능 우위를 입증한다. 향후 더 복잡한 공중 로봇 시스템으로 확장할 수 있다.
