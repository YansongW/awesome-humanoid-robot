---
$id: ent_paper_allgeuer_a_ros_based_software_framework_2013
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: A ROS-based Software Framework for the NimbRo-OP Humanoid Open Platform
  zh: 基于ROS的NimbRo-OP人形开放平台软件框架
  ko: NimbRo-OP 휴머노이드 개방형 플랫폼을 위한 ROS 기반 소프트웨어 프레임워크
summary:
  en: This paper presents a modular ROS-based software framework for the NimbRo-OP humanoid robot, providing hardware abstraction,
    visual perception, motion control, and behavior generation for soccer skills demonstrated at RoboCup 2013.
  zh: 本文介绍了一个基于ROS的模块化软件框架，专为NimbRo-OP开源人形机器人设计。由波恩大学NimbRo团队开发，该框架实现了硬件抽象、视觉感知、运动控制与行为生成，并成功应用于RoboCup 2013人形机器人联赛冠军队伍的足球技能演示。
  ko: 본 논문은 NimbRo-OP 휴머노이드 로봇을 위한 모듈화된 ROS 기반 소프트웨어 프레임워크를 제시하며, 2013년 RoboCup에서 시연된 축구 기술을 구현하기 위한 하드웨어 추상화, 시각 인지, 동작 제어
    및 행위 생성 기능을 제공한다.
domains:
- 08_software_middleware
- 04_assembly_integration_testing
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- system
- tool_equipment
- knowledge
- intelligence
tags:
- ros
- nimbro-op
- humanoid_platform
- software_framework
- hardware_abstraction
- visual_perception
- motion_control
- behavior_generation
- robocup
- soccer_robots
- dynamixel
- rviz
- rqt
- rbdl
- real_time_control
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/1809.11051v1. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: A ROS-based Software Framework for the NimbRo-OP Humanoid Open Platform
  url: https://arxiv.org/abs/1809.11051
  date: '2013'
  accessed_at: '2026-06-25'
theoretical_depth:
- system
---


## 概述

本文介绍波恩大学 NimbRo 团队为其开源人形机器人 NimbRo-OP 设计的基于 ROS 的完整软件框架，覆盖实时控制、视觉、定位、行为与可视化。核心贡献在于以 ROS 节点化架构替代早期过渡性软件，提供可复用的模块化系统，并通过开源促进社区协作。

## 它改变了什么

在 2013 年前后，人形机器人软件生态中 ROS 支持极为薄弱：DARwIn-OP 虽有名义上的自定义框架却无开源 ROS 版本，HUBO 的 ROS 实现仅停留在关节级控制，无法支撑行走或操作任务。本文真正改变的是将 ROS 从"学术演示工具"提升为"完整人形机器人竞赛级软件栈"——它证明了 ROS 的节点间通信、插件机制和工具链足以承载 125 Hz 实时控制循环、视觉感知与高层行为决策的集成，且不牺牲性能。这种改变的意义在于：它为人形机器人研究提供了一个可移植、可裁剪的公共底座，使后续团队无需从零搭建底层系统，而能直接聚焦于算法与行为创新。

## 方法拆解

### 系统架构
- 基于 ROS 节点模型，进程间通过 topics、service calls 与共享内存通信；几乎全 C++ 实现，仅少量脚本辅助。
- 所有任务与配置通过 XML launch 脚本管理，运行于 Ubuntu Linux。

### 实时控制核心
- **Robot Control 节点**：主循环周期 8 ms（125 Hz），使用 Linux timerfd API 获取实时权限；通过 CM730 控制板与 Dynamixel MX 伺服器通信（摄像头除外）。
- **动力学与柔顺控制**：利用 RBDL 库基于 URDF 模型执行 Newton-Euler 逆动力学，估计关节力矩并输入柔顺伺服控制器，实现轨迹跟踪与外力顺应。
- **硬件抽象**：通过插件机制提供真实接口与带噪声/延迟的模拟接口（dummy interface），便于无硬件调试。

### 运动模块
- 通过第二套插件机制动态加载，避免重新编译；四大模块：步态（CPG 全向行走）、头部控制、摔倒保护（姿态阈值触发伺服放松+起身动作）、运动播放器（二次样条关键帧插值，含速度/加速度约束）。

### 感知与定位
- 视觉：YUYV 800×600@24 fps，颜色校准生成 YUV 查找表，分割后下采样 4 倍，像素坐标去畸变为 3D 向量。
- 姿态：加速度计+陀螺仪经角度估计滤波器；全局定位用粒子滤波器融合白线交叉点、球门柱与罗盘读数。

### 配置与行为
- **配置服务器**：集中式参数管理，支持运行时重配置与节点间共享（优于 dynamic_reconfigure 的局部性），变更通过 service calls 通知。
- **行为框架**：State Controller Library（多动作状态机）与 Behavior Control Framework（层次化+抑制机制）；足球行为分三层（soccer/control/ROS interface），后两层与 ROS 解耦以增强可移植性。

### 可视化
- 全部基于 RQT widgets：RViz 3D 插件、诊断（电池/温度/状态）、行走控制、参数调优、绘图器（支持负时间回看，封装于 timewarp 包）。

## 关键创新

1. **首个完整开源 ROS 人形机器人竞赛框架**：不同于 DARwIn-OP 或 HUBO 的局部实现，本文覆盖从底层伺服控制到高层行为决策的全栈，且通过插件机制实现硬件与运动模块的解耦，使框架可复用于非足球场景。
2. **柔顺控制与实时性的工程化结合**：在 1.65 GHz 双核嵌入式平台上实现 125 Hz 逆动力学+柔顺伺服，同时保持 CPU 高效，证明了 ROS 在硬实时约束下的可行性。
3. **行为框架的跨平台抽象**：将足球行为与 ROS 解耦（仅 ROS interface 层依赖中间件），使行为逻辑可移植到其他机器人或仿真环境，这是多数竞赛框架忽视的设计维度。

## 实验与结果

| 指标 | 数值 |
|------|------|
| 机器人尺寸 | 95 cm / 6.6 kg |
| 自由度 | 20（颈2+臂3×2+腿6×2） |
| 控制频率 | 125 Hz（8 ms 周期） |
| 视觉分辨率/帧率 | 800×600 / 24 fps |
| 颜色分割下采样 | 4 倍 |
| 相机视场角 | ~180°（鱼眼） |
| 处理器 | AMD E-450 1.65 GHz 双核 |

实验以实际竞赛验证为主：ICRA 2013 演示、RoboCup 2013 首次正式比赛进球并作为冠军队成员、RoboCup German Open 2013 获 HARTING Open-Source Award。这些结果证明框架在真实对抗环境中的鲁棒性，但论文未提供定量对比实验（如与旧框架的延迟/精度对比），也未给出定位误差或步态速度等量化指标。

## 边界与局限

- 定位模块仅 beta 阶段，未考虑地标方向与实际线段，粒子滤波器性能未调优。
- 未集成多假设卡尔曼滤波、物理仿真器，足球行为与步态仍有改进空间。
- 框架部分组件（如颜色分割、足球行为）特定于 RoboCup 场景，虽可通过 ROS 模块化省略，但通用性未充分验证。
- 论文未明确提及通信延迟、CPU 占用率、电池续航等关键工程指标，也未涉及多机器人协同或长时间运行稳定性测试。

## 工程启示

- **复现优先核对**：确认 ROS 版本与 Ubuntu 兼容性（2013 年对应 ROS Groovy/Hydro），并检查 CM730 驱动与 Dynamixel MX 总线配置——这是最易出错的硬件层。
- **实时性陷阱**：timerfd 高精度定时依赖内核配置，需验证 PREEMPT_RT 补丁是否启用；模拟接口（dummy）的噪声模型参数需与实际硬件标定匹配，否则控制参数迁移会失效。
- **行为移植**：若复用其行为框架，注意 State Controller 与 Behavior Control 的层次关系，且 ROS interface 层需按新机器人话题名重映射。
- **视觉校准**：颜色查找表依赖光照，建议实现自动白平衡或动态阈值，否则竞赛场地变化会显著影响分割效果。
- **最易踩坑**：配置服务器的全局参数共享机制依赖 service calls 同步，若节点启动顺序不当可能导致参数不一致；建议在 launch 文件中显式声明依赖关系。

## 参考
- http://arxiv.org/abs/1809.11051v1

## Overview

This article introduces a complete ROS-based software framework designed by the University of Bonn's NimbRo team for their open-source humanoid robot NimbRo-OP, covering real-time control, vision, localization, behavior, and visualization. The core contribution lies in replacing the earlier transitional software with a ROS node-based architecture, providing a reusable modular system and promoting community collaboration through open source.

## What It Changed

Around 2013, ROS support in the humanoid robot software ecosystem was extremely weak: DARwIn-OP had a nominal custom framework but no open-source ROS version, and HUBO's ROS implementation was limited to joint-level control, unable to support walking or manipulation tasks. What this article truly changed was elevating ROS from an "academic demonstration tool" to a "complete competition-grade humanoid robot software stack"—it proved that ROS's inter-node communication, plugin mechanisms, and toolchain could handle the integration of a 125 Hz real-time control loop, visual perception, and high-level behavior decision-making without sacrificing performance. The significance of this change is that it provided a portable, customizable public foundation for humanoid robot research, allowing subsequent teams to avoid building underlying systems from scratch and focus directly on algorithmic and behavioral innovation.

## Method Breakdown

### System Architecture
- Based on the ROS node model, with inter-process communication via topics, service calls, and shared memory; almost entirely implemented in C++, with only a few auxiliary scripts.
- All tasks and configurations are managed through XML launch scripts, running on Ubuntu Linux.

### Real-Time Control Core
- **Robot Control Node**: Main loop period of 8 ms (125 Hz), using the Linux timerfd API to obtain real-time privileges; communicates with Dynamixel MX servos via the CM730 control board (except for the camera).
- **Dynamics and Compliance Control**: Uses the RBDL library with URDF models to perform Newton-Euler inverse dynamics, estimating joint torques and feeding them into a compliant servo controller, enabling trajectory tracking and external force compliance.
- **Hardware Abstraction**: Provides real interfaces and simulated interfaces with noise/delay (dummy interface) through a plugin mechanism, facilitating debugging without hardware.

### Motion Modules
- Dynamically loaded via a second plugin mechanism, avoiding recompilation; four main modules: gait (CPG omnidirectional walking), head control, fall protection (posture threshold-triggered servo relaxation + stand-up actions), and motion player (quadratic spline keyframe interpolation with velocity/acceleration constraints).

### Perception and Localization
- Vision: YUYV 800×600@24 fps, color calibration generates YUV lookup tables, segmentation followed by 4x downsampling, pixel coordinates undistorted into 3D vectors.
- Posture: Accelerometer + gyroscope processed through an angle estimation filter; global localization uses a particle filter fusing white line intersections, goal posts, and compass readings.

### Configuration and Behavior
- **Configuration Server**: Centralized parameter management, supporting runtime reconfiguration and inter-node sharing (superior to dynamic_reconfigure's locality), with changes notified via service calls.
- **Behavior Framework**: State Controller Library (multi-action state machine) and Behavior Control Framework (hierarchical + inhibition mechanisms); soccer behavior is divided into three layers (soccer/control/ROS interface), with the latter two decoupled from ROS to enhance portability.

### Visualization
- Entirely based on RQT widgets: RViz 3D plugin, diagnostics (battery/temperature/status), walking control, parameter tuning, and plotter (supporting negative-time lookback, encapsulated in the timewarp package).

## Key Innovations

1. **First complete open-source ROS humanoid robot competition framework**: Unlike DARwIn-OP or HUBO's partial implementations, this covers the full stack from low-level servo control to high-level behavior decision-making, and decouples hardware and motion modules through plugin mechanisms, making the framework reusable in non-soccer scenarios.
2. **Engineering integration of compliance control and real-time performance**: Achieves 125 Hz inverse dynamics + compliant servo control on a 1.65 GHz dual-core embedded platform while maintaining CPU efficiency, proving ROS's feasibility under hard real-time constraints.
3. **Cross-platform abstraction of the behavior framework**: Decouples soccer behavior from ROS (only the ROS interface layer depends on middleware), making behavior logic portable to other robots or simulation environments—a design dimension overlooked by most competition frameworks.

## Experiments and Results

| Metric | Value |
|--------|-------|
| Robot dimensions | 95 cm / 6.6 kg |
| Degrees of freedom | 20 (neck 2 + arms 3×2 + legs 6×2) |
| Control frequency | 125 Hz (8 ms period) |
| Vision resolution/frame rate | 800×600 / 24 fps |
| Color segmentation downsampling | 4x |
| Camera field of view | ~180° (fisheye) |
| Processor | AMD E-450 1.65 GHz dual-core |

Experiments were primarily validated through actual competitions: ICRA 2013 demonstration, scoring the first official goal at RoboCup 2013 and participating as a member of the championship team, and winning the HARTING Open-Source Award at RoboCup German Open 2013. These results prove the framework's robustness in real adversarial environments, but the paper does not provide quantitative comparative experiments (e.g., latency/accuracy comparisons with the old framework), nor does it provide quantitative metrics such as localization error or gait speed.

## Boundaries and Limitations

- The localization module is only in beta stage, not considering landmark orientations and actual line segments, and the particle filter performance is not tuned.
- No integration of multi-hypothesis Kalman filtering or physics simulators; soccer behavior and gait still have room for improvement.
- Some framework components (e.g., color segmentation, soccer behavior) are specific to RoboCup scenarios, and although they can be omitted through ROS modularity, general applicability is not fully validated.
- The paper does not explicitly mention key engineering metrics such as communication latency, CPU usage, or battery life, nor does it address multi-robot coordination or long-duration stability testing.

## Engineering Insights

- **Prioritize reproduction verification**: Confirm ROS version and Ubuntu compatibility (corresponding to ROS Groovy/Hydro in 2013), and check CM730 driver and Dynamixel MX bus configuration—this is the most error-prone hardware layer.
- **Real-time pitfalls**: The timerfd high-precision timer depends on kernel configuration; verify whether the PREEMPT_RT patch is enabled; the noise model parameters of the dummy interface must match actual hardware calibration, otherwise control parameter migration will fail.
- **Behavior porting**: If reusing its behavior framework, note the hierarchical relationship between State Controller and Behavior Control, and the ROS interface layer needs topic name remapping for new robots.
- **Vision calibration**: Color lookup tables depend on lighting; it is recommended to implement automatic white balance or dynamic thresholds, otherwise competition venue changes will significantly affect segmentation performance.
- **Most common pitfall**: The configuration server's global parameter sharing mechanism relies on service call synchronization; improper node startup order may cause parameter inconsistencies; it is recommended to explicitly declare dependencies in launch files.

## 개요

이 문서는 본 대학교 NimbRo 팀이 자체 개발한 오픈소스 휴머노이드 로봇 NimbRo-OP를 위해 설계한 ROS 기반의 완전한 소프트웨어 프레임워크를 소개하며, 실시간 제어, 비전, 위치 추정, 행동 및 시각화를 다룹니다. 핵심 기여는 초기 과도기적 소프트웨어를 대체하는 ROS 노드 기반 아키텍처로, 재사용 가능한 모듈식 시스템을 제공하고 오픈소스를 통해 커뮤니티 협업을 촉진한다는 점입니다.

## 무엇을 변화시켰는가

2013년 전후로 휴머노이드 로봇 소프트웨어 생태계에서 ROS 지원은 매우 취약했습니다. DARwIn-OP는 명목상의 자체 프레임워크가 있었지만 오픈소스 ROS 버전이 없었고, HUBO의 ROS 구현은 관절 수준 제어에 그쳐 보행이나 조작 작업을 지원할 수 없었습니다. 이 문서가 실제로 바꾼 것은 ROS를 "학술 시연 도구"에서 "완전한 휴머노이드 로봇 경쟁 수준 소프트웨어 스택"으로 끌어올린 것입니다. 즉, ROS의 노드 간 통신, 플러그인 메커니즘, 도구 체인이 125Hz 실시간 제어 루프, 시각 인식, 고수준 행동 결정의 통합을 성능 저하 없이 수용할 수 있음을 입증했습니다. 이러한 변화의 의미는 휴머노이드 로봇 연구에 이식 가능하고 축소 가능한 공용 기반을 제공하여, 후속 팀들이底层 시스템을 처음부터 구축할 필요 없이 알고리즘과 행동 혁신에 직접 집중할 수 있게 한 것입니다.

## 방법 분석

### 시스템 아키텍처
- ROS 노드 모델 기반, 프로세스 간 통신은 topics, service calls 및 공유 메모리를 통해 이루어짐; 거의 전부 C++로 구현되었으며 소수의 스크립트만 보조적으로 사용.
- 모든 작업과 구성은 XML launch 스크립트로 관리되며 Ubuntu Linux에서 실행됨.

### 실시간 제어 핵심
- **Robot Control 노드**: 메인 루프 주기 8ms(125Hz), Linux timerfd API를 사용하여 실시간 권한 획득; CM730 제어 보드와 Dynamixel MX 서보(카메라 제외)와 통신.
- **동역학 및 컴플라이언트 제어**: RBDL 라이브러리를 사용하여 URDF 모델 기반 Newton-Euler 역동역학을 수행, 관절 토크를 추정하고 컴플라이언트 서보 제어기에 입력하여 궤적 추적과 외력 순응을 구현.
- **하드웨어 추상화**: 플러그인 메커니즘을 통해 실제 인터페이스와 노이즈/지연이 있는 시뮬레이션 인터페이스(dummy interface)를 제공하여 하드웨어 없이 디버깅 가능.

### 운동 모듈
- 두 번째 플러그인 메커니즘을 통해 동적으로 로드되어 재컴파일 불필요; 네 가지 주요 모듈: 보행(CPG 전방향 보행), 머리 제어, 낙상 보호(자세 임계값 트리거 서보 릴렉스 + 기립 동작), 모션 플레이어(2차 스플라인 키프레임 보간, 속도/가속도 제약 포함).

### 인식 및 위치 추정
- 비전: YUYV 800×600@24fps, 색상 보정으로 YUV 룩업 테이블 생성, 분할 후 4배 다운샘플링, 픽셀 좌표를 디스토션 보정하여 3D 벡터로 변환.
- 자세: 가속도계+자이로스코프가 각도 추정 필터를 거침; 전역 위치 추정은 파티클 필터가 흰색 라인 교차점, 골대, 나침반 판독값을 융합.

### 구성 및 행동
- **구성 서버**: 중앙 집중식 매개변수 관리, 런타임 재구성 및 노드 간 공유 지원(dynamic_reconfigure의 지역성보다 우수), 변경 사항은 service calls를 통해 알림.
- **행동 프레임워크**: State Controller Library(다중 동작 상태 머신) 및 Behavior Control Framework(계층적+억제 메커니즘); 축구 행동은 세 계층(soccer/control/ROS interface)으로 구성되며, 후자 두 계층은 ROS와 분리되어 이식성을 강화.

### 시각화
- 전부 RQT widgets 기반: RViz 3D 플러그인, 진단(배터리/온도/상태), 보행 제어, 매개변수 튜닝, 플로터(음수 시간 되감기 지원, timewarp 패키지에 캡슐화).

## 주요 혁신

1. **최초의 완전한 오픈소스 ROS 휴머노이드 로봇 경쟁 프레임워크**: DARwIn-OP나 HUBO의 부분적 구현과 달리, 이 문서는 저수준 서보 제어부터 고수준 행동 결정까지 전체 스택을 다루며, 플러그인 메커니즘을 통해 하드웨어와 운동 모듈을 분리하여 프레임워크를 비축구 시나리오에도 재사용 가능하게 함.
2. **컴플라이언트 제어와 실시간성의 공학적 결합**: 1.65GHz 듀얼코어 임베디드 플랫폼에서 125Hz 역동역학+컴플라이언트 서보를 구현하면서 CPU 효율을 유지하여, ROS가 하드 실시간 제약 하에서도 실현 가능함을 입증.
3. **행동 프레임워크의 크로스 플랫폼 추상화**: 축구 행동을 ROS와 분리(ROS interface 계층만 미들웨어에 의존)하여 행동 로직을 다른 로봇이나 시뮬레이션 환경으로 이식 가능하게 함. 이는 대부분의 경쟁 프레임워크가 간과하는 설계 차원임.

## 실험 및 결과

| 지표 | 값 |
|------|------|
| 로봇 크기 | 95cm / 6.6kg |
| 자유도 | 20(목 2 + 팔 3×2 + 다리 6×2) |
| 제어 주파수 | 125Hz(8ms 주기) |
| 비전 해상도/프레임률 | 800×600 / 24fps |
| 색상 분할 다운샘플링 | 4배 |
| 카메라 화각 | ~180°(어안) |
| 프로세서 | AMD E-450 1.65GHz 듀얼코어 |

실험은 실제 경쟁 검증이 중심: ICRA 2013 시연, RoboCup 2013 첫 공식 경기 골 기록 및 우승 팀 멤버, RoboCup German Open 2013에서 HARTING Open-Source Award 수상. 이러한 결과는 프레임워크가 실제 대항 환경에서 견고함을 입증하지만, 논문은 정량적 비교 실험(예: 기존 프레임워크와의 지연/정밀도 비교)을 제공하지 않았으며, 위치 추정 오차나 보행 속도 등의 정량적 지표도 제시하지 않았습니다.

## 경계 및 한계

- 위치 추정 모듈은 베타 단계에 불과하며, 랜드마크 방향과 실제 선분을 고려하지 않았고 파티클 필터 성능도 튜닝되지 않음.
- 다중 가설 칼만 필터, 물리 시뮬레이터가 통합되지 않았으며, 축구 행동과 보행에도 개선 여지가 있음.
- 프레임워크의 일부 구성 요소(예: 색상 분할, 축구 행동)는 RoboCup 시나리오에 특화되어 있으며, ROS 모듈화로 생략할 수는 있지만 범용성은 충분히 검증되지 않음.
- 논문은 통신 지연, CPU 점유율, 배터리 수명 등의 핵심 엔지니어링 지표를 명시적으로 언급하지 않았으며, 다중 로봇 협업이나 장시간 실행 안정성 테스트도 다루지 않음.

## 공학적 시사점

- **재현 시 우선 확인 사항**: ROS 버전과 Ubuntu 호환성(2013년 기준 ROS Groovy/Hydro)을 확인하고, CM730 드라이버와 Dynamixel MX 버스 구성을 점검하세요. 이것이 가장 오류가 발생하기 쉬운 하드웨어 계층입니다.
- **실시간성 함정**: timerfd 고정밀 타이밍은 커널 구성에 의존하므로 PREEMPT_RT 패치가 활성화되었는지 검증해야 합니다. 시뮬레이션 인터페이스(dummy)의 노이즈 모델 매개변수는 실제 하드웨어 보정과 일치해야 하며, 그렇지 않으면 제어 매개변수 이전이 실패할 수 있습니다.
- **행동 이식**: 행동 프레임워크를 재사용할 경우 State Controller와 Behavior Control의 계층 관계를 주의하고, ROS interface 계층은 새 로봇의 토픽 이름에 맞게 재매핑해야 합니다.
- **비전 보정**: 색상 룩업 테이블은 조명에 의존하므로 자동 화이트 밸런스나 동적 임계값 구현을 권장합니다. 그렇지 않으면 경기장 변화가 분할 성능에 크게 영향을 미칠 수 있습니다.
- **가장 흔한 실수**: 구성 서버의 전역 매개변수 공유 메커니즘은 service calls 동기화에 의존하므로, 노드 시작 순서가 잘못되면 매개변수 불일치가 발생할 수 있습니다. launch 파일에서 의존 관계를 명시적으로 선언하는 것이 좋습니다.
