---
$id: ent_paper_hoist_suspended_load_manipulation_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HOIST: Humanoid Optimization with Imitation and Sample-efficient Tuning for Manipulating Suspended Loads'
  zh: 人形机器人悬挂负载操作的模仿学习与高效微调
  ko: 'HOIST: Humanoid Optimization with Imitation and Sample-efficient Tuning for Manipulating Suspended Loads'
summary:
  en: 'Manipulating suspended payloads with humanoid robots is challenging because the robot can only influence an underactuated,
    oscillatory load through whole-body motion and intermittent contact. Institutions per source list: 佛罗里达大学.'
  zh: HOIST 是一种面向人形机器人操控悬挂负载的优化方法，由研究团队提出。其核心贡献在于结合模仿学习与样本高效的强化学习调优，在仿真和真实人形机器人上实现了更精准的负载放置，相比纯模仿方法减少了 19.9 厘米平移误差和 3.56 度角度误差。
  ko: 'Manipulating suspended payloads with humanoid robots is challenging because the robot can only influence an underactuated,
    oscillatory load through whole-body motion and intermittent contact. Institutions per source list: 佛罗里达大学.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- hoist
- humanoid
- optimization
- imitation
- sa
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 53 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2606.00252v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit: derived values explicitly labeled (evidence from proximity-constrained full-text pair
    match).'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.00252 HOIST: Humanoid Optimization with Imitation and Sample-efficient Tuning for Manipulating Suspended
    Loads'
  url: https://arxiv.org/abs/2606.00252
  accessed_at: '2026-07-31'
  date: '2026-05-29'
- id: src_002
  type: website
  title: 万字长文｜人形机器人的运动小脑会不会成为人形机器人的基础设施？
  url: https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA
  accessed_at: '2026-07-31'
---

## 概述

HOIST 提出一种三阶段流程（遥操作模仿、VLA 微调、迭代批量 RL 精炼），让人形机器人通过全身运动与间歇接触操控欠驱动的悬吊负载。核心贡献在于将任务适应从固定全身控制器中解耦，仅微调高层 VLA 策略与噪声转向模块，以样本高效方式提升放置精度。在仿真与真实平台上，HOIST 将平移放置误差从 22.44 cm 降至 6.25 cm（仿真）与 9.28 cm 降至 6.38 cm（真实）。

## 它改变了什么

悬吊负载操控长期被当作“抓取-放置”的变体处理，但作者明确指出其本质是欠驱动摆问题：机器人停止后负载仍会振荡，接触后继续漂移，这使标准模仿学习或端到端 RL 都难以直接适用。模仿学习能提供安全初始行为，但无法优化最终放置精度，且闭环执行中误差累积；从零 RL 在真实人形机器人上既不安全又样本效率低下。HOIST 真正改变的是“谁在学什么”的边界划分——它不训练关节级驱动，不修改全身控制器，而是将任务适应完全压缩到高层命令空间（手部目标、导航命令、基座高度）中，让策略学习“如何指挥”而非“如何运动”。这一划分使 RL 精炼只需 30 个 rollout 即可显著提升精度，而非传统 RL 所需的百万级交互。

## 方法拆解

### 问题建模
将 humanoid hoisting 建模为任务条件、部分可观测、闭环控制问题。观测定义为：
`o_t = {I_ego,rgb_t, D_ego_t, I_side,rgb_t, x_r_t, ℓ, c_nav_{t-1}}`
包含机载 RGB、深度、外部侧视 RGB（捕捉负载-目标关系）、本体感受、语言指令、上一导航命令。不假设接触力传感或显式负载状态输入。

### 高层策略输出
策略预测未来 H 步命令增量：
`ΔU_t = π_θ(o_t) = (Δu_t, Δu_{t+1}, …, Δu_{t+H-1})`
单步增量：
`Δu_τ = [Δy_head_τ, Δy_L_τ, Δy_R_τ, Δc_nav_τ, Δh_base_τ]`
执行时应用块中第一个增量更新当前命令：`u_t = u_{t-1} + Δu_t`。

### 低层执行
`a_motor_t = π_wbc(x_r_t, u_t)`，使用固定解耦全身控制器（GR00T Whole-Body Control），不训练或微调。

### 三阶段流程
1. **遥操作数据收集**：PICO VR 头显引导完整吊装序列，存储轨迹 `τ_i = ({o_t, u_demo_t, a_motor_t}_{t=1}^{T_i}, y_i)`，y_i 为轨迹级结果（放置误差与残余运动）。
2. **监督 VLA 微调**：基于 GR00T N1.6，将导航命令作为额外模态加入 VLM，深度图像与机器人速度加入流匹配动作专家。仅微调 VLM 最后四层、扩散变换器动作专家、新模态编码器，其余冻结。
3. **RL 精炼**：部署微调策略收集小批量自主 rollout，用简单加权奖励（平移误差 + 角度误差）标注。记录流匹配动作专家的初始噪声，训练 actor-critic 预测应使用哪个噪声向量。RL 期间 VLA 冻结，仅更新噪声转向模块，复用 VLM 特征避免重复编码。

### 关键设计决策
- 高层策略只决定短视界任务级运动，低层控制器处理平衡与关节级稳定——让学习策略在命令层操作。
- 保持低层运动先验固定，任务适应转移到高层 VLA 与 RL——避免关节级探索的不安全性。
- 从遥操作微调策略出发而非从零 RL——避免 tabula-rasa 探索。
- 迭代批量 RL 而非额外演示——自主 rollout 揭示策略自身的有偏轨迹，任务级奖励学习噪声修正。

## 关键创新

1. **噪声转向 RL（flow-matching-steering RL）**：不直接更新 VLA 策略权重，而是学习预测流匹配动作专家的初始噪声向量。这本质上是将 RL 精炼压缩为一个低维“转向”问题，避免破坏已学好的模仿行为，同时允许任务级修正。这是对“RL 微调大模型”范式的轻量化替代。
2. **任务-控制解耦**：将任务适应完全置于高层命令空间，低层全身控制器固定。这使得 RL 只需探索 5 维命令增量而非高维关节空间，样本效率提升数个量级——30 个 rollout 即可见效。
3. **多模态观测融合**：将导航命令作为额外模态注入 VLM，深度图像加入动作专家，消融显示两者组合在真实平台 Δx+Δy 上优于任一单独模态（9.28 cm vs 12.06/11.20 cm）。

## 实验与结果


### 表1：RL rollout 数量递增的放置精度（仿真）
| 方法 | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|
| Human Expert Teleop | 4.35 | 3.00 | 8.48 | 7.35 |
| VLA-50 | 21.56 | 0.88 | 3.85 | 22.44 |
| VLA-50 + 10 RL | 15.09 | 1.83 | 4.58 | 16.92 |
| VLA-50 + 20 RL | 1.72 | 0.82 | 0.29 | 2.54 |
| VLA-50 + 30 RL | 5.16 | 1.09 | 2.76 | 6.25 |

20 个 rollout 达到最佳精度（2.54 cm），30 个 rollout 反而略降（6.25 cm），说明存在最优 rollout 数量，过度精炼可能过拟合当前策略偏差。

### 表2：额外演示 vs RL 精炼（真实平台）
| 方法 | Demos | RL rollouts | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| VLA-50 | 50 | 0 | 1.60 | 7.68 | 14.5 | 9.28 |
| VLA-80 | 80 | 0 | 0.54 | 8.03 | 27.0 | 8.57 |
| HOIST | 50 | 30 | 1.64 | 4.74 | 28.9 | 6.38 |

HOIST 在真实平台将 Δx+Δy 从 9.28 降至 6.38 cm，优于增加 30 个演示的 VLA-80（8.57 cm），但角度误差（28.9°）反而恶化，说明 RL 精炼主要优化平移精度，角度控制未受益。

### 表3：VLA 输入模态消融（真实平台，无 RL）
| 变体 | Depth | Nav command | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| Base VLA | – | – | 1.54 | 10.52 | 26.6 | 12.06 |
| + Depth | ✓ | – | 0.78 | 8.78 | 14.8 | 9.57 |
| + Nav command | – | ✓ | 1.30 | 9.90 | 32.5 | 11.20 |
| + Nav + Depth | ✓ | ✓ | 1.60 | 7.68 | 14.5 | 9.28 |

深度模态贡献最大（Δx+Δy 降 2.49 cm），导航命令单独效果有限（降 0.86（由表内数值 3.0−3.85 计算） cm），但两者组合在 Δy 上最优（7.68 cm vs 8.78/9.90 cm）。

### 残余运动分析
HOIST 相比纯模仿策略，IMU 陀螺仪响应减少约一半，加速度计读数更高——表明角运动减少且更主动修正，但两种学习策略均低于人类遥操作，可能反映人类更频繁的闭环修正。

## 边界与局限

- **低层控制器未适配**：固定全身控制器未针对悬吊负载交互微调，策略只能通过有限接口（导航、头部、基座高度、手部）影响执行，无法显式适应不同负载重量的施加力，处理重型负载能力受限。
- **未扩展负载与悬吊条件**：未测试不同负载重量、绳长、悬挂点变化。
- **未添加显式安全约束**：RL 精炼过程中无安全护栏，极端情况下可能产生危险行为。
- **未研究人形机器人-起重机联合控制**：列为未来工作。
- **角度误差未改善**：真实平台 HOIST 的 Δψ 为 28.9°，高于 VLA-50（14.5°）与 VLA-80（27.0°），RL 精炼未解决角度对齐问题。
- **最优 rollout 数量敏感**：仿真中 20 个 rollout 最佳（2.54 cm），30 个反而恶化（6.25 cm），说明 RL 精炼存在过拟合风险，实际部署需监控。
- **论文未明确**：推理频率、动作块视界 H 的具体值、仿真器类型、训练超参数与计算资源。

## 工程启示

- **先核对低层控制器的接口能力**：HOIST 的有效性完全依赖固定全身控制器能否稳定跟踪高层命令。复现前需确认你的执行栈支持导航命令、头部姿态、基座高度、双手手部目标这 5 维命令输入，否则需自行扩展。
- **RL 精炼的 rollout 数量是关键超参数**：仿真中 20 个 rollout 最佳，30 个反而下降。建议按 5 个间隔递增评估，监控 Δx+Δy 与 Δψ 的权衡，避免过拟合当前策略偏差。
- **深度模态是最大单项增益**：消融显示深度输入将 Δx+Δy 从 12.06 降至 9.57 cm，优先确保深度相机标定与同步质量。导航命令单独效果有限，但组合后 Δy 最优，建议保留。
- **角度误差是盲区**：HOIST 在真实平台 Δψ 恶化至 28.9°，若任务对角度敏感，需在奖励中显式加权角度误差或增加角度相关观测。
- **噪声转向 RL 的实现要点**：记录流匹配动作专家的初始噪声，训练 actor 确定性输出转向向量（非均值方差采样），并复用冻结策略的 VLM 特征避免重复编码——这是样本效率的关键。
- **最易踩坑处**：遥操作数据质量直接决定 VLA 初始行为，50 个演示中若覆盖不足，RL 精炼可能无法补偿；建议在遥操作阶段覆盖不同负载初始位置与振荡状态。

## 参考
- https://arxiv.org/abs/2606.00252
- https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA

## Overview

HOIST proposes a three-stage pipeline (teleoperation imitation, VLA fine-tuning, iterative batch RL refinement) that enables a humanoid robot to manipulate an underactuated suspended load through whole-body motion and intermittent contact. The core contribution is decoupling task adaptation from a fixed whole-body controller, fine-tuning only the high-level VLA policy and a noise-steering module to improve placement accuracy in a sample-efficient manner. In both simulation and real-world platforms, HOIST reduces translational placement error from 22.44 cm to 6.25 cm (simulation) and from 9.28 cm to 6.38 cm (real).

## What It Changes

Suspended load manipulation has long been treated as a variant of "grasp-and-place," but the authors clearly identify it as an underactuated pendulum problem: the load continues to oscillate after the robot stops and drifts after contact, making standard imitation learning or end-to-end RL difficult to apply directly. Imitation learning provides safe initial behaviors but cannot optimize final placement accuracy, and errors accumulate during closed-loop execution; RL from scratch is neither safe nor sample-efficient on real humanoid robots. What HOIST truly changes is the boundary of "who learns what"—it does not train joint-level actuation or modify the whole-body controller, but instead compresses task adaptation entirely into the high-level command space (hand targets, navigation commands, base height), letting the policy learn "how to command" rather than "how to move." This division enables RL refinement to significantly improve accuracy with only 30 rollouts, rather than the millions of interactions required by traditional RL.

## Method Breakdown

### Problem Formulation
Humanoid hoisting is formulated as a task-conditioned, partially observable, closed-loop control problem. The observation is defined as:
`o_t = {I_ego,rgb_t, D_ego_t, I_side,rgb_t, x_r_t, ℓ, c_nav_{t-1}}`
It includes onboard RGB, depth, external side-view RGB (capturing the load-target relationship), proprioception, language instructions, and the previous navigation command. No contact force sensing or explicit load state input is assumed.

### High-Level Policy Output
The policy predicts command increments for the future H steps:
`ΔU_t = π_θ(o_t) = (Δu_t, Δu_{t+1}, …, Δu_{t+H-1})`
The single-step increment is:
`Δu_τ = [Δy_head_τ, Δy_L_τ, Δy_R_τ, Δc_nav_τ, Δh_base_τ]`
During execution, the first increment in the block is applied to update the current command: `u_t = u_{t-1} + Δu_t`.

### Low-Level Execution
`a_motor_t = π_wbc(x_r_t, u_t)`, using a fixed decoupled whole-body controller (GR00T Whole-Body Control), which is not trained or fine-tuned.

### Three-Stage Pipeline
1. **Teleoperation Data Collection**: A PICO VR headset guides complete hoisting sequences, storing trajectories `τ_i = ({o_t, u_demo_t, a_motor_t}_{t=1}^{T_i}, y_i)`, where y_i is the trajectory-level outcome (placement error and residual motion).
2. **Supervised VLA Fine-Tuning**: Based on GR00T N1.6, navigation commands are added as an extra modality to the VLM, and depth images and robot velocity are added to the flow-matching action expert. Only the last four VLM layers, the diffusion transformer action expert, and the new modality encoders are fine-tuned; everything else is frozen.
3. **RL Refinement**: The fine-tuned policy is deployed to collect small batches of autonomous rollouts, labeled with a simple weighted reward (translational error + angular error). The initial noise of the flow-matching action expert is recorded, and an actor-critic is trained to predict which noise vector should be used. During RL, the VLA is frozen, and only the noise-steering module is updated, reusing VLM features to avoid repeated encoding.

### Key Design Decisions
- The high-level policy only decides short-horizon task-level motions, while the low-level controller handles balance and joint-level stability—letting the learned policy operate at the command layer.
- Keeping the low-level motion prior fixed and shifting task adaptation to the high-level VLA and RL—avoiding the unsafety of joint-level exploration.
- Starting from a teleoperation-fine-tuned policy rather than RL from scratch—avoiding tabula-rasa exploration.
- Using iterative batch RL rather than additional demonstrations—autonomous rollouts reveal the policy's own biased trajectories, and task-level rewards learn noise corrections.

## Key Innovations

1. **Noise-Steering RL (flow-matching-steering RL)**: Instead of directly updating VLA policy weights, it learns to predict the initial noise vector of the flow-matching action expert. This essentially compresses RL refinement into a low-dimensional "steering" problem, avoiding disruption of learned imitation behaviors while allowing task-level corrections. This is a lightweight alternative to the "RL fine-tuning of large models" paradigm.
2. **Task-Control Decoupling**: Task adaptation is entirely placed in the high-level command space, with the low-level whole-body controller fixed. This allows RL to explore only 5-dimensional command increments rather than high-dimensional joint space, improving sample efficiency by several orders of magnitude—effective with just 30 rollouts.
3. **Multimodal Observation Fusion**: Navigation commands are injected as an extra modality into the VLM, and depth images are added to the action expert. Ablations show that the combination outperforms either modality alone on the real platform in Δx+Δy (9.28 cm vs 12.06/11.20 cm).

## Experiments and Results

### Table 1: Placement accuracy with increasing RL rollout counts (simulation)
| Method | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|
| Human Expert Teleop | 4.35 | 3.00 | 8.48 | 7.35 |
| VLA-50 | 21.56 | 0.88 | 3.85 | 22.44 |
| VLA-50 + 10 RL | 15.09 | 1.83 | 4.58 | 16.92 |
| VLA-50 + 20 RL | 1.72 | 0.82 | 0.29 | 2.54 |
| VLA-50 + 30 RL | 5.16 | 1.09 | 2.76 | 6.25 |

20 rollouts achieve the best accuracy (2.54 cm), while 30 rollouts slightly degrade (6.25 cm), indicating an optimal rollout count and that over-refinement may overfit to current policy biases.

### Table 2: Additional demonstrations vs RL refinement (real platform)
| Method | Demos | RL rollouts | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| VLA-50 | 50 | 0 | 1.60 | 7.68 | 14.5 | 9.28 |
| VLA-80 | 80 | 0 | 0.54 | 8.03 | 27.0 | 8.57 |
| HOIST | 50 | 30 | 1.64 | 4.74 | 28.9 | 6.38 |

HOIST reduces Δx+Δy from 9.28 to 6.38 cm on the real platform, outperforming VLA-80 with 30 additional demonstrations (8.57 cm), but the angular error (28.9°) worsens, indicating that RL refinement primarily optimizes translational accuracy while angular control does not benefit.

### Table 3: VLA input modality ablation (real platform, no RL)
| Variant | Depth | Nav command | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| Base VLA | – | – | 1.54 | 10.52 | 26.6 | 12.06 |
| + Depth | ✓ | – | 0.78 | 8.78 | 14.8 | 9.57 |
| + Nav command | – | ✓ | 1.30 | 9.90 | 32.5 | 11.20 |
| + Nav + Depth | ✓ | ✓ | 1.60 | 7.68 | 14.5 | 9.28 |

The depth modality contributes the most (Δx+Δy reduced by 2.49 cm), while the navigation command alone has limited effect (reduced by 0.86 cm), but the combination is optimal for Δy (7.68 cm vs 8.78/9.90 cm).

### Residual Motion Analysis
Compared to pure imitation policies, HOIST reduces IMU gyroscope response by about half and shows higher accelerometer readings—indicating reduced angular motion and more active correction, but both learned policies fall short of human teleoperation, possibly reflecting more frequent closed-loop corrections by humans.

## Boundaries and Limitations

- **Low-level controller not adapted**: The fixed whole-body controller is not fine-tuned for suspended load interactions, and the policy can only influence execution through limited interfaces (navigation, head, base height, hands), making it unable to explicitly adapt applied forces for different load weights, limiting handling of heavy loads.
- **No extension to load and suspension conditions**: Different load weights, rope lengths, and suspension point variations were not tested.
- **No explicit safety constraints**: No safety guardrails are present during RL refinement, and extreme cases may produce dangerous behaviors.
- **No study of humanoid-crane joint control**: Listed as future work.
- **Angular error not improved**: HOIST's Δψ on the real platform is 28.9°, higher than VLA-50 (14.5°) and VLA-80 (27.0°), and RL refinement does not address angular alignment.
- **Sensitivity to optimal rollout count**: In simulation, 20 rollouts are best (2.54 cm), while 30 degrade (6.25 cm), indicating overfitting risk in RL refinement that requires monitoring in deployment.
- **Not specified in the paper**: Inference frequency, the specific value of the action block horizon H, simulator type, training hyperparameters, and computational resources.

## Engineering Insights

- **First verify the low-level controller's interface capabilities**: HOIST's effectiveness relies entirely on the fixed whole-body controller's ability to stably track high-level commands. Before reproduction, confirm that your execution stack supports the 5-dimensional command inputs (navigation commands, head pose, base height, dual-hand targets); otherwise, you need to extend it yourself.
- **The RL refinement rollout count is a critical hyperparameter**: In simulation, 20 rollouts are best, while 30 degrade. It is recommended to evaluate in increments of 5, monitoring the trade-off between Δx+Δy and Δψ to avoid overfitting to current policy biases.
- **Depth modality provides the largest single gain**: Ablations show that depth input reduces Δx+Δy from 12.06 to 9.57 cm, so prioritize ensuring depth camera calibration and synchronization quality. The navigation command alone has limited effect, but the combination is optimal for Δy, so it is recommended to keep it.
- **Angular error is a blind spot**: HOIST's Δψ worsens to 28.9° on the real platform. If the task is angle-sensitive, explicitly weight angular error in the reward or add angle-related observations.
- **Implementation essentials for noise-steering RL**: Record the initial noise of the flow-matching action expert, train the actor to output a deterministic steering vector (rather than mean-variance sampling), and reuse the frozen policy's VLM features to avoid repeated encoding—this is key to sample efficiency.
- **Most common pitfall**: Teleoperation data quality directly determines the VLA's initial behavior. If 50 demonstrations have insufficient coverage, RL refinement may not compensate; it is recommended to cover different load initial positions and oscillation states during teleoperation.

## 개요

HOIST는 3단계 파이프라인(원격 조작 모방, VLA 미세 조정, 반복 배치 RL 정제)을 제안하여 휴머노이드 로봇이 전신 운동과 간헐적 접촉을 통해 저구동(underactuated) 현수 하중을 조작하도록 한다. 핵심 기여는 작업 적응을 고정 전신 제어기에서 분리하여, 상위 레벨 VLA 정책과 노이즈 조향 모듈만 미세 조정함으로써 샘플 효율적으로 배치 정밀도를 향상시키는 것이다. 시뮬레이션 및 실제 플랫폼에서 HOIST는 병진 배치 오차를 22.44 cm에서 6.25 cm(시뮬레이션), 9.28 cm에서 6.38 cm(실제)로 줄였다.

## 무엇을 바꾸었는가

현수 하중 조작은 오랫동안 "파지-배치" 변형으로 취급되어 왔지만, 저자들은 이것이 본질적으로 저구동 진자 문제임을 명확히 지적한다. 로봇이 멈춘 후에도 하중은 진동하고, 접촉 후에도 계속 드리프트하므로 표준 모방 학습이나 엔드투엔드 RL은 직접 적용하기 어렵다. 모방 학습은 안전한 초기 행동을 제공할 수 있지만 최종 배치 정밀도를 최적화하지 못하며, 폐루프 실행에서 오차가 누적된다. 처음부터 RL을 수행하는 것은 실제 휴머노이드에서 안전하지도 않고 샘플 효율도 낮다. HOIST가 실제로 바꾼 것은 "무엇을 학습하는가"의 경계 구분이다. 관절 수준 구동을 훈련하지 않고, 전신 제어기를 수정하지 않으며, 작업 적응을 완전히 상위 명령 공간(손 목표, 내비게이션 명령, 베이스 높이)으로 압축하여 정책이 "어떻게 움직일지"가 아닌 "어떻게 지휘할지"를 학습하게 한다. 이러한 구분 덕분에 RL 정제는 전통적인 RL의 수백만 회 상호작용이 아닌 30회 롤아웃만으로 정밀도를 크게 향상시킬 수 있다.

## 방법 분해

### 문제 모델링
휴머노이드 호이스팅을 작업 조건부, 부분 관측, 폐루프 제어 문제로 모델링한다. 관측은 다음과 같이 정의된다:
`o_t = {I_ego,rgb_t, D_ego_t, I_side,rgb_t, x_r_t, ℓ, c_nav_{t-1}}`
기내 RGB, 깊이, 외부 측면 RGB(하중-목표 관계 포착), 고유수용감각, 언어 명령, 이전 내비게이션 명령을 포함한다. 접촉력 센싱이나 명시적 하중 상태 입력은 가정하지 않는다.

### 상위 정책 출력
정책은 미래 H 스텝 명령 증분을 예측한다:
`ΔU_t = π_θ(o_t) = (Δu_t, Δu_{t+1}, …, Δu_{t+H-1})`
단일 스텝 증분:
`Δu_τ = [Δy_head_τ, Δy_L_τ, Δy_R_τ, Δc_nav_τ, Δh_base_τ]`
실행 시 블록의 첫 번째 증분을 적용하여 현재 명령을 업데이트한다: `u_t = u_{t-1} + Δu_t`.

### 하위 실행
`a_motor_t = π_wbc(x_r_t, u_t)`, 고정 분리 전신 제어기(GR00T Whole-Body Control)를 사용하며 훈련이나 미세 조정은 하지 않는다.

### 3단계 파이프라인
1. **원격 조작 데이터 수집**: PICO VR 헤드셋으로 전체 호이스팅 시퀀스를 안내하고, 궤적 `τ_i = ({o_t, u_demo_t, a_motor_t}_{t=1}^{T_i}, y_i)`을 저장한다. y_i는 궤적 수준 결과(배치 오차 및 잔여 운동)이다.
2. **지도 VLA 미세 조정**: GR00T N1.6 기반으로, 내비게이션 명령을 VLM에 추가 모달리티로 넣고, 깊이 이미지와 로봇 속도를 플로우 매칭 동작 전문가에 추가한다. VLM 마지막 4개 레이어, 확산 트랜스포머 동작 전문가, 새 모달리티 인코더만 미세 조정하고 나머지는 동결한다.
3. **RL 정제**: 미세 조정된 정책을 배포하여 소규모 자율 롤아웃을 수집하고, 단순 가중 보상(병진 오차 + 각도 오차)으로 라벨링한다. 플로우 매칭 동작 전문가의 초기 노이즈를 기록하고, actor-critic이 어떤 노이즈 벡터를 사용할지 예측하도록 훈련한다. RL 동안 VLA는 동결되고 노이즈 조향 모듈만 업데이트되며, VLM 특징을 재사용하여 중복 인코딩을 피한다.

### 핵심 설계 결정
- 상위 정책은 짧은 시야의 작업 수준 운동만 결정하고, 하위 제어기는 균형과 관절 수준 안정성을 처리한다—학습 정책이 명령 계층에서 작동하게 한다.
- 하위 운동 사전을 고정으로 유지하고 작업 적응을 상위 VLA와 RL로 전환한다—관절 수준 탐색의 불안전성을 피한다.
- 처음부터 RL이 아닌 원격 조작 미세 조정 정책에서 출발한다—tabula-rasa 탐색을 피한다.
- 추가 데모가 아닌 반복 배치 RL—자율 롤아웃이 정책 자체의 편향된 궤적을 드러내고, 작업 수준 보상으로 노이즈 수정을 학습한다.

## 핵심 혁신

1. **노이즈 조향 RL(flow-matching-steering RL)**: VLA 정책 가중치를 직접 업데이트하지 않고, 플로우 매칭 동작 전문가의 초기 노이즈 벡터를 예측하도록 학습한다. 이는 본질적으로 RL 정제를 저차원 "조향" 문제로 압축하여 이미 학습된 모방 행동을 파괴하지 않으면서 작업 수준 수정을 허용한다. 이는 "대형 모델 RL 미세 조정" 패러다임에 대한 경량 대안이다.
2. **작업-제어 분리**: 작업 적응을 완전히 상위 명령 공간에 두고, 하위 전신 제어기는 고정한다. 이로써 RL은 고차원 관절 공간이 아닌 5차원 명령 증분만 탐색하면 되므로 샘플 효율이 수 자릿수 향상된다—30회 롤아웃으로 효과를 볼 수 있다.
3. **다중 모달 관측 융합**: 내비게이션 명령을 VLM에 추가 모달리티로 주입하고, 깊이 이미지를 동작 전문가에 추가한다. 절제 실험에서 두 조합이 실제 플랫폼 Δx+Δy에서 단일 모달리티보다 우수함을 보인다(9.28 cm vs 12.06/11.20 cm).

## 실험 및 결과

### 표1: RL 롤아웃 수 증가에 따른 배치 정밀도(시뮬레이션)
| 방법 | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|
| Human Expert Teleop | 4.35 | 3.00 | 8.48 | 7.35 |
| VLA-50 | 21.56 | 0.88 | 3.85 | 22.44 |
| VLA-50 + 10 RL | 15.09 | 1.83 | 4.58 | 16.92 |
| VLA-50 + 20 RL | 1.72 | 0.82 | 0.29 | 2.54 |
| VLA-50 + 30 RL | 5.16 | 1.09 | 2.76 | 6.25 |

20회 롤아웃에서 최적 정밀도(2.54 cm)를 달성하고, 30회 롤아웃에서는 오히려 약간 저하(6.25 cm)되어 최적 롤아웃 수가 존재하며, 과도한 정제가 현재 정책 편향에 과적합될 수 있음을 시사한다.

### 표2: 추가 데모 vs RL 정제(실제 플랫폼)
| 방법 | Demos | RL rollouts | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| VLA-50 | 50 | 0 | 1.60 | 7.68 | 14.5 | 9.28 |
| VLA-80 | 80 | 0 | 0.54 | 8.03 | 27.0 | 8.57 |
| HOIST | 50 | 30 | 1.64 | 4.74 | 28.9 | 6.38 |

HOIST는 실제 플랫폼에서 Δx+Δy를 9.28에서 6.38 cm로 줄여 30개 데모를 추가한 VLA-80(8.57 cm)보다 우수하지만, 각도 오차(28.9°)는 오히려 악화되어 RL 정제가 주로 병진 정밀도를 최적화하고 각도 제어는 혜택을 받지 못함을 보여준다.

### 표3: VLA 입력 모달리티 절제(실제 플랫폼, RL 없음)
| 변형 | Depth | Nav command | Δx (cm) | Δy (cm) | Δψ (deg) | Δx+Δy (cm) |
|---|---|---|---|---|---|---|
| Base VLA | – | – | 1.54 | 10.52 | 26.6 | 12.06 |
| + Depth | ✓ | – | 0.78 | 8.78 | 14.8 | 9.57 |
| + Nav command | – | ✓ | 1.30 | 9.90 | 32.5 | 11.20 |
| + Nav + Depth | ✓ | ✓ | 1.60 | 7.68 | 14.5 | 9.28 |

깊이 모달리티가 가장 큰 기여를 하며(Δx+Δy 2.49 cm 감소), 내비게이션 명령 단독 효과는 제한적이지만(0.86 cm 감소), 두 조합이 Δy에서 최적(7.68 cm vs 8.78/9.90 cm)이다.

### 잔여 운동 분석
HOIST는 순수 모방 정책에 비해 IMU 자이로 응답이 약 절반으로 줄고, 가속도계 판독값은 더 높다—각도 운동이 줄고 더 적극적으로 수정됨을 나타내지만, 두 학습 정책 모두 인간 원격 조작보다 낮아 인간의 더 빈번한 폐루프 수정을 반영할 수 있다.

## 경계 및 한계

- **하위 제어기 미적응**: 고정 전신 제어기가 현수 하중 상호작용에 맞게 미세 조정되지 않아, 정책은 제한된 인터페이스(내비게이션, 머리, 베이스 높이, 손)를 통해서만 실행에 영향을 줄 수 있으며, 다양한 하중 중량에 따른 힘 적용을 명시적으로 적응시킬 수 없어 중량물 처리 능력이 제한된다.
- **하중 및 현수 조건 미확장**: 다른 하중 중량, 로프 길이, 현수점 변화를 테스트하지 않았다.
- **명시적 안전 제약 미추가**: RL 정제 과정에 안전 가드레일이 없어 극단적 상황에서 위험한 행동이 발생할 수 있다.
- **휴머노이드-크레인 협조 제어 미연구**: 향후 작업으로 남겨두었다.
- **각도 오차 미개선**: 실제 플랫폼에서 HOIST의 Δψ는 28.9°로 VLA-50(14.5°) 및 VLA-80(27.0°)보다 높아, RL 정제가 각도 정렬 문제를 해결하지 못했다.
- **최적 롤아웃 수 민감성**: 시뮬레이션에서 20회 롤아웃이 최적(2.54 cm)이고 30회는 오히려 악화(6.25 cm)되어, RL 정제에 과적합 위험이 있으며 실제 배포 시 모니터링이 필요하다.
- **논문에서 명시되지 않음**: 추론 빈도, 동작 블록 시야 H의 구체적 값, 시뮬레이터 유형, 훈련 하이퍼파라미터 및 계산 자원.

## 공학적 시사점

- **먼저 하위 제어기의 인터페이스 능력을 확인하라**: HOIST의 효과는 고정 전신 제어기가 상위 명령을 안정적으로 추적할 수 있는지에 전적으로 의존한다. 재현 전에 실행 스택이 내비게이션 명령, 머리 자세, 베이스 높이, 양손 목표의 5차원 명령 입력을 지원하는지 확인하고, 그렇지 않으면 직접 확장해야 한다.
- **RL 정제의 롤아웃 수는 핵심 하이퍼파라미터다**: 시뮬레이션에서 20회 롤아웃이 최적이고 30회는 오히려 저하된다. 5회 간격으로 증가시키며 평가하고, Δx+Δy와 Δψ의 트레이드오프를 모니터링하여 현재 정책 편향에 과적합되는 것을 피하라.
- **깊이 모달리티가 가장 큰 단일 이득이다**: 절제 실험에서 깊이 입력이 Δx+Δy를 12.06에서 9.57 cm로 줄였으므로, 깊이 카메라 캘리브레이션과 동기화 품질을 우선적으로 보장하라. 내비게이션 명령 단독 효과는 제한적이지만 조합 시 Δy가 최적이므로 유지하는 것이 좋다.
- **각도 오차는 사각지대다**: HOIST는 실제 플랫폼에서 Δψ가 28.9°로 악화되었다. 작업이 각도에 민감하다면 보상에 각도 오차를 명시적으로 가중하거나 각도 관련 관측을 추가해야 한다.
- **노이즈 조향 RL 구현 포인트**: 플로우 매칭 동작 전문가의 초기 노이즈를 기록하고, actor가 평균-분산 샘플링이 아닌 결정적 조향 벡터를 출력하도록 훈련하며, 동결 정책의 VLM 특징을 재사용하여 중복 인코딩을 피하라—이것이 샘플 효율의 핵심이다.
- **가장 쉽게 빠지는 함정**: 원격 조작 데이터 품질이 VLA 초기 행동을 직접 결정한다. 50개 데모에서 커버리지가 부족하면 RL 정제가 보상하지 못할 수 있으므로, 원격 조작 단계에서 다양한 하중 초기 위치와 진동 상태를 커버하는 것이 좋다.
