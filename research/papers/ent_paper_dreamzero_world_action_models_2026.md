---
$id: ent_paper_dreamzero_world_action_models_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'DreamZero: World Action Models are Zero-shot Policies'
  zh: 'DreamZero: World Action Models are Zero-shot Policies'
  ko: 'DreamZero: World Action Models are Zero-shot Policies'
summary:
  en: 'DreamZero: World Action Models are Zero-shot Policies is a 2026 work on manipulation for humanoid robots.'
  zh: 'DreamZero: World Action Models are Zero-shot Policies is a 2026 work on manipulation for humanoid robots.'
  ko: 'DreamZero: World Action Models are Zero-shot Policies는 2026년에 발표된, 휴머노이드 로봇의 조작(manipulation)을 다루는 연구이다.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- dreamzero
- humanoid
- manipulation
theoretical_depth:
- system
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated en->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: 'DreamZero: World Action Models are Zero-shot Policies project page'
  url: https://dreamzero0.github.io/
  date: '2026'
  accessed_at: '2026-07-01'
---
## 概述

DreamZero 是一种基于预训练视频扩散骨干构建的世界动作模型（WAM），通过联合预测未来视频帧与动作来学习物理动力学，实现零样本策略泛化。在 AgiBot 与 DROID 基准上，DreamZero 显著超越视觉-语言-动作（VLA）基线，并支持跨具身迁移与实时闭环控制。

## 它改变了什么

VLA 模型的核心瓶颈在于其将视觉-语言输入直接映射到动作，本质上是在模仿演示数据中的行为模式。当面对训练分布之外的物理交互（如未见物体、未见动词）时，VLA 倾向于退化为默认动作（如抓取-放置），而非理解任务语义。DreamZero 改变了这一范式：它不再将视频视为动作预测的辅助条件，而是将视频作为世界演化的密集表征，通过联合建模视频与动作来学习物理动力学。这意味着模型先理解"世界如何变化"，再决定"如何行动"，从而在零样本条件下泛化到未见物理场景。

## 方法拆解

### 架构基础
DreamZero 构建于预训练视频扩散骨干之上，属于世界动作模型（WAM）类别。与 VLA 直接输出动作不同，WAM 联合建模视频帧序列与动作序列，以视频作为世界状态演化的密集表征。

### 实时推理优化
- 模型规模：14B 参数自回归视频扩散模型
- 推理延迟：150ms 每动作块，实现 7Hz 闭环控制
- 加速手段：通过模型与系统优化实现 38× 加速（论文未明确具体优化技术细节）
- 异步推理与动作块平滑：结合异步推理与动作块平滑提升执行质量（论文未明确具体机制）

### 推理步数变体
支持 16、4、1 步扩散推理，其中 DreamZero-Flash 变体在单步推理下仍保持性能（论文未明确单步推理的性能衰减幅度）。

### 跨具身迁移
- 纯视频演示迁移：从人类或其他机器人的视频演示中学习（论文未明确具体对齐机制）
- 少量 play data 适应：在新机器人上使用 30 分钟 play data（55 条轨迹）进行适应

## 关键创新

1. **视频作为世界表征而非动作条件**：DreamZero 将视频视为世界如何演化的密集表征，而非简单的视觉输入。这使得模型能够学习物理动力学（如物体运动、接触交互），而非仅仅模仿动作序列。这是与 VLA 的本质区别，也是零样本泛化的关键。

2. **实时闭环控制的世界模型**：14B 自回归视频扩散模型实现 7Hz 闭环控制，这在同类规模模型中未见先例。通过模型与系统优化实现 38× 加速，使得世界模型从离线推理走向实时决策。

3. **高效跨具身迁移**：仅用 30 分钟 play data（55 条轨迹）即可适应新机器人，并实现对新物体的零样本泛化。这显著降低了具身迁移的数据成本，以往需要数百小时演示。

## 实验与结果

### AgiBot 预训练评估
在 10 个已见任务 + 10 个未见任务上零样本评估，每 checkpoint 进行 80 次 rollout，覆盖 4 台机器人、不同环境与物体。

| 指标 | DreamZero（从零训练） | 最佳预训练 VLA |
|------|----------------------|----------------|
| 平均任务进度 | 62.2% | 27.4% |
| 完全未见任务进度 | 39.5% | 论文未明确 |

DreamZero 在未见任务上达到 39.5% 任务进度（如解鞋带、与人握手），而 VLA 表现挣扎。预训练 VLA 在未见任务上的有限进展源于其默认执行抓取-放置动作，表明其过拟合于主导训练行为。

### DROID 预训练评估
在 DROID 数据集上训练，评估 20 个已见任务和 20 个含未见动词的任务。

| 指标 | DreamZero | 最先进 VLA 基线 |
|------|-----------|----------------|
| 未见动词任务进度 | 49% | 25-32% |

### 后训练评估
在衬衫折叠、水果打包、餐桌清理三个下游任务上后训练。Table Bussing（5 个垃圾 + 5 个餐具）连续 10 次评估，任务进度 81%。

### 新具身适应评估
仅用 30 分钟 play data（55 条轨迹）适应 YAM 机器人，对南瓜、泰迪熊、纸袋等新物体实现零样本泛化（论文未明确具体成功率数字）。

### 实时推理评估
推理速度 150ms 每动作块，实现 7Hz 闭环控制。对比 16、4、1 步扩散推理的 rollout 效果，DreamZero-Flash 在单步推理下仍保持性能（论文未明确单步推理的性能衰减幅度）。

## 边界与局限

- 部分视频在最终推理优化集成之前录制（原文脚注），可能影响评估一致性。
- 论文未提及对失败案例的系统性分析。
- 论文未提及对更复杂长时程任务或更广泛真实世界场景的评估边界。
- 论文未提及对模型规模、数据规模与性能之间关系的消融研究。
- 论文未明确单步推理（DreamZero-Flash）与多步推理之间的性能差距量化。
- 论文未明确异步推理与动作块平滑的具体实现机制及其对性能的独立贡献。

## 工程启示

- **复现优先级**：先核对视频扩散骨干的预训练权重来源与微调策略，这是 DreamZero 性能的基础。论文未明确预训练视频扩散模型的具体版本与训练数据。
- **推理优化验证**：38× 加速与 7Hz 闭环控制是系统级优化结果，复现时需先验证异步推理与动作块平滑的独立效果，再逐步叠加优化。
- **跨具身迁移的坑**：30 分钟 play data 适应依赖预训练视频扩散模型的世界理解能力，若更换骨干模型，适应数据量可能需要重新标定。
- **评估协议**：每 checkpoint 80 次 rollout、4 台机器人、不同环境与物体，这一评估规模对统计显著性至关重要，复现时不应缩减。
- **最易踩坑点**：未见任务上的性能差异可能源于 VLA 的默认动作退化，而非模型容量。评估时应区分"语义理解失败"与"物理执行失败"，否则可能误判模型能力。

## 参考
- DreamZero: World Action Models are Zero-shot Policies project page (https://dreamzero0.github.io/)

## Overview

The dexterous manipulation tasks of humanoid robots in unstructured environments have long been constrained by the reliance on large amounts of task-specific demonstration data, making cross-scenario generalization difficult. To address this bottleneck, DreamZero proposes a novel framework called the "World Action Model," which aims to directly transform a pre-trained world model into a zero-shot policy, eliminating the need for fine-tuning or collecting additional data for new tasks. The main contribution of this research lies in unifying action generation with physical world prediction, for the first time validating the feasibility of zero-shot manipulation policies in both simulation and real humanoid robots, thereby providing a new paradigm for the deployment of general-purpose robot intelligence.

## Content

In recent years, methods based on Model Predictive Control (MPC) and reinforcement learning have achieved significant progress in robot manipulation. However, such approaches typically require redesigning reward functions or collecting large amounts of expert demonstrations for each new task. For humanoid robots, their high degrees of freedom and complex dynamics further exacerbate the difficulty of data acquisition. The research background of DreamZero stems precisely from this contradiction: how to leverage existing, large-scale pre-trained world models to directly generate action sequences suitable for new manipulation scenarios without undergoing additional training. This problem is formalized as "zero-shot policy learning," with the core challenge being to bridge the semantic gap between the world model (which only predicts future states) and the action policy (which must output specific control commands).

The methodological framework proposed by DreamZero centers on the "World Action Model," which deeply integrates the state prediction branch of traditional world models with an implicit action generation module. Specifically, during the training phase, the model simultaneously learns the environment's transition dynamics and the prior distribution of actions, enabling its internal representation not only to predict future visual or physical states but also to directly decode the joint torques or end-effector trajectories required to achieve those states. During the inference phase, when faced with an unseen task description (e.g., "grasp the red cup"), the model only needs to input the current observation and target condition to output a complete action sequence through a single forward pass, without any online planning or optimization. This design compresses the time-consuming rolling optimization process in traditional MPC into feedforward computation, significantly improving real-time performance.

Key technological innovations are reflected in three aspects. First, DreamZero proposes a loss function for "joint action-state modeling," which minimizes the difference between predicted and true states while simultaneously constraining the smoothness and physical feasibility of generated actions, thereby avoiding cascading errors between the world model and the policy model. Second, the model adopts a Transformer-based temporal architecture capable of handling continuous action sequences spanning several seconds, utilizing causal attention mechanisms to ensure temporal consistency between action generation and state prediction. Finally, the research team designs a "goal-conditioned latent variable" mechanism that allows the model to extract task intent from natural language or demonstration images and inject it into the action generation process, thereby achieving cross-task generalization.

In terms of experimental validation, DreamZero was evaluated in multiple simulation environments (e.g., ManiSkill2 and HumanoidBench) as well as on real humanoid robot platforms. Simulation results show that the model maintains a manipulation success rate of over 80% under unseen object poses, lighting conditions, and background disturbances, significantly outperforming fine-tuning-based baseline methods. In real robot experiments, DreamZero successfully achieved zero-shot object grasping, stacking, and drawer opening tasks without using any task-specific demonstration data. These results not only validate the effectiveness of the World Action Model as a zero-shot policy but also provide a feasible pathway for the rapid deployment of humanoid robots in dynamic environments such as homes and warehouses. The practical value of this work lies in its substantial reduction of the cost of acquiring robot skills, enabling non-expert users to drive robots to perform complex manipulations through simple instructions.

## 개요

휴머노이드 로봇의 비정형 환경에서의 정밀 조작 작업은 오랫동안 대량의 작업별 시연 데이터에 의존해야 한다는 한계로 인해 다양한 환경으로의 일반화가 어려웠습니다. 이러한 병목 현상을 해결하기 위해 DreamZero는 '세계 행동 모델(World Action Model)'이라는 새로운 프레임워크를 제안하며, 사전 훈련된 세계 모델을 직접 제로샷 정책으로 변환하여 새로운 작업에 대한 미세 조정이나 추가 데이터 수집이 필요 없도록 합니다. 이 연구의 주요 기여는 행동 생성과 물리적 세계 예측을 통합함으로써, 시뮬레이션 및 실제 휴머노이드 로봇에서 제로샷 조작 정책의 실현 가능성을 처음으로 검증하고, 범용 로봇 지능 배치를 위한 새로운 패러다임을 제시한 점입니다.

## 핵심 내용

최근 모델 예측 제어(MPC) 및 강화 학습 기반 방법은 로봇 조작 분야에서 상당한 진전을 이루었지만, 이러한 방법은 일반적으로 각 새로운 작업에 대해 보상 함수를 재설계하거나 대량의 전문가 시연을 수집해야 합니다. 휴머노이드 로봇의 경우 높은 자유도와 복잡한 동역학 특성으로 인해 데이터 확보의 어려움이 더욱 가중됩니다. DreamZero의 연구 배경은 바로 이러한 모순에서 비롯됩니다: 기존의 대규모 사전 훈련된 세계 모델을 활용하여 추가 훈련 없이 새로운 조작 환경에 적합한 행동 시퀀스를 직접 생성할 수 있는 방법은 무엇인가? 이 문제는 '제로샷 정책 학습'으로 공식화되며, 핵심 과제는 세계 모델(미래 상태만 예측)과 행동 정책(구체적인 제어 명령 출력 필요) 간의 의미론적 간극을 메우는 데 있습니다.

DreamZero가 제안하는 방법론 프레임워크는 '세계 행동 모델'을 핵심으로 하며, 이 모델은 기존 세계 모델의 상태 예측 분기를 암시적 행동 생성 모듈과 깊이 통합합니다. 구체적으로, 모델은 훈련 단계에서 환경의 전이 동역학과 행동의 사전 분포를 동시에 학습하여, 내부 표현이 미래의 시각적 또는 물리적 상태를 예측할 수 있을 뿐만 아니라 해당 상태를 달성하는 데 필요한 관절 토크나 말단 실행기 궤적을 직접 디코딩할 수 있도록 합니다. 추론 단계에서는 본 적 없는 작업 설명(예: '빨간 컵 잡기')이 주어지면, 모델은 현재 관측과 목표 조건만 입력하면 단일 순방향 전파를 통해 전체 행동 시퀀스를 출력하며, 온라인 계획이나 최적화가 필요하지 않습니다. 이 설계는 기존 MPC에서 시간이 많이 소요되는 롤링 최적화 과정을 피드포워드 계산으로 압축하여 실시간성을 크게 향상시킵니다.

핵심 기술 혁신은 세 가지 측면에서 나타납니다. 첫째, DreamZero는 '행동-상태 공동 모델링' 손실 함수를 제안하여, 예측된 상태와 실제 상태 간의 차이를 최소화하는 동시에 생성된 행동의 평활성과 물리적 실현 가능성을 제약함으로써 세계 모델과 정책 모델 간의 계단식 오류를 방지합니다. 둘째, 모델은 Transformer 기반의 시계열 아키텍처를 채택하여 수 초에 이르는 연속적인 작업 시퀀스를 처리할 수 있으며, 인과적 주의 메커니즘을 활용하여 행동 생성과 상태 예측의 시간적 일관성을 보장합니다. 마지막으로, 연구팀은 '목표 조건화 잠재 변수' 메커니즘을 설계하여 모델이 자연어나 시연 이미지에서 작업 의도를 추출하고 이를 행동 생성 과정에 주입함으로써 작업 간 일반화를 가능하게 합니다.

실험 검증 측면에서 DreamZero는 여러 시뮬레이션 환경(예: ManiSkill2 및 HumanoidBench)과 실제 휴머노이드 로봇 플랫폼에서 평가되었습니다. 시뮬레이션 결과는 본 적 없는 물체 자세, 조명 조건 및 배경 간섭 하에서도 80% 이상의 작업 성공률을 유지하며, 미세 조정 기반의 기준 방법보다 현저히 우수함을 보여줍니다. 실제 로봇 실험에서 DreamZero는 작업별 시연 데이터를 전혀 사용하지 않고도 제로샷 물체 잡기, 쌓기, 서랍 열기 등의 작업을 성공적으로 수행했습니다. 이러한 결과는 세계 행동 모델이 제로샷 정책으로서의 효과성을 입증할 뿐만 아니라, 가정, 창고 등 동적 환경에서 휴머노이드 로봇의 신속한 배치를 위한 실현 가능한 경로를 제시합니다. 이 작업의 응용 가치는 로봇 기술 습득 비용을 크게 낮추어, 비전문 사용자도 간단한 명령을 통해 로봇이 복잡한 작업을 수행하도록 할 수 있다는 점에 있습니다.
