---
$id: ent_paper_unified_walking_running_recovery_amp_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors
  zh: Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors
  ko: Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors
summary:
  en: 'We propose a unified reinforcement learning framework that enables a single policy to perform walking, running, and
    fall recovery on the Unitree G1 humanoid robot, validated on physical hardware without any explicit mode-switching command
    at deployment. Institutions per source list: 香港大学等.'
  zh: 本文提出一种统一强化学习框架，使Unitree G1人形机器人能够通过单一策略执行行走、奔跑与跌倒恢复，并在实体硬件上验证无需显式模式切换指令。核心贡献在于将对抗性运动先验（AMP）扩展为状态依赖门控机制，通过投影重力阈值自动路由训练数据至恢复判别器或速度条件运动判别器，仅需三个LAFAN1参考片段即可覆盖全部行为。部署时单个冻结ONNX策略以50Hz运行，硬件实验成功演示了从俯卧与仰卧跌倒恢复以及平滑的走跑过渡。
  ko: 'We propose a unified reinforcement learning framework that enables a single policy to perform walking, running, and
    fall recovery on the Unitree G1 humanoid robot, validated on physical hardware without any explicit mode-switching command
    at deployment. Institutions per source list: 香港大学等.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- unified
- walking
- running
- recovery
- humanoi
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 115 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2605.18611v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: arXiv:2605.18611 Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors
  url: https://arxiv.org/abs/2605.18611
  accessed_at: '2026-07-31'
  date: '2026-05-18'
- id: src_002
  type: website
  title: 万字长文，读懂人形机器人AMP：19篇论文搭起的运动先验圣经
  url: https://mp.weixin.qq.com/s/YZsm3855iP3TNTTt1aou7w
  accessed_at: '2026-07-31'
---


## 概述

本文提出一种基于状态依赖对抗运动先验（State-Dependent Adversarial Motion Priors）的统一人形机器人控制框架，在单一actor策略下通过门控函数切换恢复与运动判别器，实现行走、跑步和跌倒恢复的端到端学习。作者在Unitree G1上验证了从俯卧/仰卧恢复到行走/跑步的完整序列，无需部署时显式模式切换。

## 它改变了什么

传统人形机器人控制将行走、跑步、恢复视为独立模块，由手工FSM协调，导致模式边界脆弱、切换质量下降。现有AMP方法虽能端到端学习，但单一共享运动先验无法同时捕捉高速跑步与恢复的动力学特征——要么丢失跑步统计特性，要么混淆恢复与运动分布。本文真正改变的是：将“模式”从架构设计问题转化为判别器条件化问题，通过速度指令连续插值行走/跑步参考分布，用重力投影门控恢复模式，使单一策略在推理时无需任何模式逻辑即可覆盖三种行为。这消除了手工调参的转换逻辑，让模式切换成为学习到的隐式行为。

## 方法拆解

### 整体架构
- 单一actor策略 πθ(a_t|o_t)，端到端训练，部署为冻结ONNX模型，无运行时模式逻辑。
- 观测 o_t 为96维（身体角速度、投影重力、指令速度、相对关节位置/速度、上一动作），拼接4帧历史得384维输入；动作 a_t ∈ ℝ²⁹ 为目标关节位置，由底层PD控制器跟踪。

### 双判别器设计
- **恢复判别器** D_φ^rec：仅在跌倒恢复参考片段（fallAndGetUp2_subject2）上训练。
- **速度条件化运动判别器** D_φ^loco：额外输入归一化指令速度 v̂_t = min(v_x^cmd / v_max, 1)，在行走（walk1_subject1）和跑步（run1_subject2）参考上联合训练。每次判别器更新时，以概率 (1−v̂_t) 采样行走参考、以概率 v̂_t 采样跑步参考，实现条件一致的分布插值。

### 门控机制
- 门控函数 z_t = rec 当 |g_z + 1| > 0.6，否则 loco，其中 g_z 为投影重力z分量（直立时 ≈ −1，跌倒时接近0或正）。
- 阈值对应约37°倾斜，位于重力信号双峰分布的低占用区域，无需学习即可准确抗噪。
- **关键设计**：门控仅训练时激活，推理时策略为单一整体网络，无显式模式变量。

### 训练目标
- 任务奖励（式1）：R_t = w_v·R_cmd + w_s·R_smooth + w_p·R_posture − w_e·C_energy − w_f·C_fall。
- AMP奖励（式3）：R_AMP = −log(1 − D_φ^rec(s_t, s_t+1)) 当 z_t = rec；R_AMP = −log(1 − D_φ^loco(s_t, s_t+1|v̂_t)) 当 z_t = loco。
- 总奖励（式4）：R_t^total = R_t + λ_amp·R_AMP，λ_amp = 0.5。
- 训练算法：PPO，三层MLP，Isaac Lab仿真。

## 关键创新

1. **速度条件化判别器替代多模式先验**：通过 v̂_t 在行走/跑步参考间连续插值，避免了训练多个运动判别器或手工定义模式边界。这是对AMP框架的关键扩展——将行为模式编码为条件变量而非离散模块。
2. **重力投影门控的简洁性**：利用重力信号天然双峰分布特性，用固定阈值（37°）实现恢复/运动判别器切换，无需学习门控网络，且训练时激活、推理时隐去，保证了部署策略的纯净性。
3. **极简参考数据**：仅用3个LAFAN1片段（1行走+1跑步+1恢复）即可覆盖三种行为，表明条件化判别器能高效利用有限参考数据，降低了对大规模运动库的依赖。

## 实验与结果

| 实验内容 | 设置 | 关键结果 |
|---------|------|---------|
| 仿真训练 | Isaac Lab，PPO，λ_amp=0.5，三层MLP | 速度跟踪范围正常 [−0.5, 1.0] m/s，快速 [−1.5, 3.0] m/s |
| 硬件部署 | Unitree G1，50 Hz，ONNX模型 | 成功从俯卧和仰卧恢复，同一控制器下平滑行走→跑步转换 |
| 步态特征 | 仿真观察 | 行走：规律脚跟-脚趾接触；跑步：步幅延长、双支撑相缩短 |
| 域迁移 | 仅标准仿真参数随机化 | 无sim-to-real适配或域随机化微调，直接部署成功 |

结果含义：硬件实验证明单一策略无需模式切换指令即可完成“恢复→行走→跑步”完整序列，且步态特征符合生物力学预期。快速模式需显式操作员指令启用，作者称为“有意的安全约束”。

## 边界与局限

论文未明确讨论局限性。可识别边界：快速模式依赖显式操作员指令（非自主决策）；门控阈值（37°）未做系统敏感性分析；未验证侧卧等更多跌倒姿态，未测试楼梯/斜坡等复杂地形；速度范围未超过3.0 m/s；未与FSM协调的模块化基线做定量对比。训练数据量、训练时长、硬件规格均未提及。

## 工程启示

复现时优先核对三点：**门控阈值**（|g_z + 1| > 0.6）在目标机器人上的重力信号分布是否仍呈双峰，若传感器噪声或安装偏差导致分布偏移，需重新标定；**速度条件插值**（v̂_t）的 v_max 设定直接影响行走/跑步参考的采样比例，需与指令速度范围匹配；**参考数据重定向质量**——仅3个片段，若逆运动学重定向到目标机器人关节空间时出现运动学畸变，判别器会学到错误先验。最容易踩坑的是训练时门控激活与推理时隐去的差异：确保部署代码中无任何模式变量残留，否则策略输入分布与训练不一致。下游团队若需扩展速度范围或新增行为模式，应优先调整 v̂_t 条件化方式而非增加判别器数量。

## 参考
- https://arxiv.org/abs/2605.18611
- https://mp.weixin.qq.com/s/YZsm3855iP3TNTTt1aou7w

## Overview

This paper proposes a unified humanoid robot control framework based on State-Dependent Adversarial Motion Priors, which achieves end-to-end learning of walking, running, and fall recovery under a single actor policy by switching between recovery and locomotion discriminators via a gating function. The authors validate the complete sequence from prone/supine recovery to walking/running on the Unitree G1, without requiring explicit mode switching at deployment time.

## What It Changes

Traditional humanoid robot control treats walking, running, and recovery as independent modules coordinated by hand-crafted FSMs, leading to fragile mode boundaries and degraded switching quality. While existing AMP methods can learn end-to-end, a single shared motion prior cannot simultaneously capture the dynamics of high-speed running and recovery—either losing running statistics or confusing recovery with locomotion distributions. What this paper truly changes is transforming "modes" from an architectural design problem into a discriminator conditioning problem: continuously interpolating walking/running reference distributions via velocity commands, and gating recovery modes via projected gravity. This enables a single policy to cover all three behaviors without any mode logic at inference time. This eliminates hand-tuned transition logic, making mode switching an implicit learned behavior.

## Method Breakdown

### Overall Architecture
- A single actor policy πθ(a_t|o_t), trained end-to-end, deployed as a frozen ONNX model with no runtime mode logic.
- The observation o_t is 96-dimensional (body angular velocity, projected gravity, commanded velocity, relative joint positions/velocities, previous action), concatenated over 4 frames of history to yield a 384-dimensional input; the action a_t ∈ ℝ²⁹ is target joint positions tracked by a low-level PD controller.

### Dual Discriminator Design
- **Recovery discriminator** D_φ^rec: trained only on fall recovery reference clips (fallAndGetUp2_subject2).
- **Velocity-conditioned locomotion discriminator** D_φ^loco: additionally takes normalized commanded velocity v̂_t = min(v_x^cmd / v_max, 1) as input, trained jointly on walking (walk1_subject1) and running (run1_subject2) references. At each discriminator update, walking references are sampled with probability (1−v̂_t) and running references with probability v̂_t, achieving condition-consistent distribution interpolation.

### Gating Mechanism
- The gating function z_t = rec when |g_z + 1| > 0.6, otherwise loco, where g_z is the projected gravity z-component (≈ −1 when upright, near 0 or positive when fallen).
- The threshold corresponds to approximately 37° of tilt, located in the low-occupancy region of the bimodal gravity signal, providing accurate noise robustness without learning.
- **Key design**: gating is active only during training; at inference, the policy is a single integrated network with no explicit mode variables.

### Training Objective
- Task reward (Eq. 1): R_t = w_v·R_cmd + w_s·R_smooth + w_p·R_posture − w_e·C_energy − w_f·C_fall.
- AMP reward (Eq. 3): R_AMP = −log(1 − D_φ^rec(s_t, s_t+1)) when z_t = rec; R_AMP = −log(1 − D_φ^loco(s_t, s_t+1|v̂_t)) when z_t = loco.
- Total reward (Eq. 4): R_t^total = R_t + λ_amp·R_AMP, with λ_amp = 0.5.
- Training algorithm: PPO, three-layer MLP, Isaac Lab simulation.

## Key Innovations

1. **Velocity-conditioned discriminator replacing multi-mode priors**: Continuous interpolation between walking/running references via v̂_t avoids training multiple locomotion discriminators or manually defining mode boundaries. This is a key extension to the AMP framework—encoding behavioral modes as conditioning variables rather than discrete modules.
2. **Simplicity of projected gravity gating**: Leveraging the naturally bimodal distribution of the gravity signal, a fixed threshold (37°) implements recovery/locomotion discriminator switching without learning a gating network. It is active during training and omitted at inference, ensuring a clean deployment policy.
3. **Minimal reference data**: Only 3 LAFAN1 clips (1 walking + 1 running + 1 recovery) suffice to cover three behaviors, demonstrating that conditioned discriminators can efficiently utilize limited reference data and reduce dependence on large-scale motion libraries.

## Experiments and Results

| Experiment | Setup | Key Results |
|---------|------|---------|
| Simulation training | Isaac Lab, PPO, λ_amp=0.5, three-layer MLP | Velocity tracking range: normal [−0.5, 1.0] m/s, fast [−1.5, 3.0] m/s |
| Hardware deployment | Unitree G1, 50 Hz, ONNX model | Successful recovery from prone and supine positions, smooth walking→running transition under the same controller |
| Gait characteristics | Simulation observation | Walking: regular heel-toe contact; running: extended stride, shortened double-support phase |
| Domain transfer | Standard simulation parameter randomization only | No sim-to-real adaptation or domain randomization fine-tuning; direct deployment succeeded |

Implications of results: Hardware experiments demonstrate that a single policy can complete the full "recovery→walking→running" sequence without mode-switching commands, with gait characteristics consistent with biomechanical expectations. Fast mode requires explicit operator commands to enable, which the authors describe as an "intentional safety constraint."

## Boundaries and Limitations

The paper does not explicitly discuss limitations. Identifiable boundaries: fast mode relies on explicit operator commands (not autonomous decision-making); the gating threshold (37°) lacks systematic sensitivity analysis; no validation for additional fall postures such as side-lying, and no testing on complex terrain like stairs or slopes; velocity range does not exceed 3.0 m/s; no quantitative comparison against FSM-coordinated modular baselines. Training data volume, training duration, and hardware specifications are not mentioned.

## Engineering Insights

When reproducing, prioritize checking three points: **gating threshold** (|g_z + 1| > 0.6)—whether the gravity signal distribution on the target robot remains bimodal; if sensor noise or mounting deviations shift the distribution, recalibration is needed; **velocity-conditioned interpolation** (v̂_t)—the v_max setting directly affects the sampling ratio between walking/running references and must match the commanded velocity range; **reference data retargeting quality**—with only 3 clips, if inverse kinematics retargeting to the target robot's joint space introduces kinematic distortions, the discriminator will learn incorrect priors. The most common pitfall is the difference between gating active during training and omitted at inference: ensure no mode variables remain in the deployment code, otherwise the policy input distribution will mismatch training. Downstream teams seeking to extend the velocity range or add new behavioral modes should prioritize adjusting the v̂_t conditioning scheme rather than increasing the number of discriminators.

## 개요

본 논문은 상태 의존 적대적 운동 사전(State-Dependent Adversarial Motion Priors)에 기반한 통합 휴머노이드 로봇 제어 프레임워크를 제안한다. 단일 actor 정책 하에서 게이팅 함수를 통해 복구 및 운동 판별기를 전환하여 보행, 달리기, 낙상 복구를 종단 간 학습한다. 저자는 Unitree G1에서 엎드린/누운 자세 복구부터 보행/달리기까지의 전체 시퀀스를 배포 시 명시적 모드 전환 없이 검증했다.

## 무엇이 바뀌었는가

기존 휴머노이드 로봇 제어는 보행, 달리기, 복구를 독립적인 모듈로 간주하고 수작업 FSM으로 조정하여 모드 경계가 취약하고 전환 품질이 저하되는 문제가 있었다. 기존 AMP 방법은 종단 간 학습이 가능하지만, 단일 공유 운동 사전으로는 고속 달리기와 복구의 동역학적 특성을 동시에 포착할 수 없었다—달리기의 통계적 특성이 손실되거나 복구와 운동 분포가 혼동되었다. 본 논문이 실제로 바꾼 것은 '모드'를 아키텍처 설계 문제에서 판별기 조건화 문제로 전환한 것이다. 속도 명령으로 보행/달리기 참조 분포를 연속적으로 보간하고, 중력 투영으로 복구 모드를 게이팅하여 단일 정책이 추론 시 어떤 모드 로직도 없이 세 가지 행동을 모두 커버할 수 있게 했다. 이는 수작업 튜닝이 필요한 전환 로직을 제거하고 모드 전환을 학습된 암시적 행동으로 만들었다.

## 방법 분석

### 전체 아키텍처
- 단일 actor 정책 πθ(a_t|o_t), 종단 간 훈련, 동결된 ONNX 모델로 배포, 런타임 모드 로직 없음.
- 관측 o_t는 96차원(신체 각속도, 투영 중력, 명령 속도, 상대 관절 위치/속도, 이전 행동)이며, 4프레임 히스토리를 연결하여 384차원 입력 생성; 행동 a_t ∈ ℝ²⁹는 목표 관절 위치이며, 하위 PD 컨트롤러가 추적.

### 이중 판별기 설계
- **복구 판별기** D_φ^rec: 낙상 복구 참조 클립(fallAndGetUp2_subject2)에서만 훈련.
- **속도 조건화 운동 판별기** D_φ^loco: 정규화된 명령 속도 v̂_t = min(v_x^cmd / v_max, 1)를 추가 입력으로 받아 보행(walk1_subject1) 및 달리기(run1_subject2) 참조에서 공동 훈련. 각 판별기 업데이트 시 확률 (1−v̂_t)로 보행 참조, 확률 v̂_t로 달리기 참조를 샘플링하여 조건 일관된 분포 보간 구현.

### 게이팅 메커니즘
- 게이팅 함수 z_t = rec (|g_z + 1| > 0.6), 그 외 loco. 여기서 g_z는 투영 중력 z성분(직립 시 ≈ −1, 낙상 시 0 또는 양수에 근접).
- 임계값은 약 37° 기울기에 해당하며, 중력 신호의 이중 모드 분포에서 점유율이 낮은 영역에 위치하여 학습 없이도 정확한 노이즈 내성을 확보.
- **핵심 설계**: 게이팅은 훈련 시에만 활성화되고, 추론 시 정책은 단일 통합 네트워크로 명시적 모드 변수가 없음.

### 훈련 목표
- 작업 보상(식 1): R_t = w_v·R_cmd + w_s·R_smooth + w_p·R_posture − w_e·C_energy − w_f·C_fall.
- AMP 보상(식 3): R_AMP = −log(1 − D_φ^rec(s_t, s_t+1)) (z_t = rec일 때); R_AMP = −log(1 − D_φ^loco(s_t, s_t+1|v̂_t)) (z_t = loco일 때).
- 총 보상(식 4): R_t^total = R_t + λ_amp·R_AMP, λ_amp = 0.5.
- 훈련 알고리즘: PPO, 3층 MLP, Isaac Lab 시뮬레이션.

## 핵심 혁신

1. **속도 조건화 판별기가 다중 모드 사전을 대체**: v̂_t를 통해 보행/달리기 참조 간 연속 보간하여 여러 운동 판별기 훈련이나 수작업 모드 경계 정의를 피함. 이는 AMP 프레임워크의 핵심 확장—행동 모드를 이산 모듈이 아닌 조건 변수로 인코딩.
2. **중력 투영 게이팅의 단순성**: 중력 신호의 자연스러운 이중 모드 분포 특성을 활용하여 고정 임계값(37°)으로 복구/운동 판별기 전환을 구현. 게이팅 네트워크 학습이 필요 없고, 훈련 시 활성화·추론 시 은닉되어 배포 정책의 순수성을 보장.
3. **극소 참조 데이터**: LAFAN1의 3개 클립(보행 1 + 달리기 1 + 복구 1)만으로 세 가지 행동을 커버할 수 있어, 조건화 판별기가 제한된 참조 데이터를 효율적으로 활용하고 대규모 운동 라이브러리 의존도를 낮춤.

## 실험 및 결과

| 실험 내용 | 설정 | 핵심 결과 |
|---------|------|---------|
| 시뮬레이션 훈련 | Isaac Lab, PPO, λ_amp=0.5, 3층 MLP | 속도 추적 범위 일반 [−0.5, 1.0] m/s, 고속 [−1.5, 3.0] m/s |
| 하드웨어 배포 | Unitree G1, 50 Hz, ONNX 모델 | 엎드린/누운 자세 복구 성공, 동일 컨트롤러에서 보행→달리기 부드러운 전환 |
| 보행 특성 | 시뮬레이션 관찰 | 보행: 규칙적인 발뒤꿈치-발가락 접촉; 달리기: 보폭 증가, 이중 지지상 단축 |
| 도메인 전이 | 표준 시뮬레이션 파라미터 무작위화만 적용 | sim-to-real 적응이나 도메인 무작위화 미세 조정 없이 직접 배포 성공 |

결과 의미: 하드웨어 실험은 단일 정책이 모드 전환 명령 없이 '복구→보행→달리기' 전체 시퀀스를 완료할 수 있음을 증명했으며, 보행 특성은 생체역학적 기대에 부합. 고속 모드는 명시적 운영자 명령이 필요하며, 저자는 이를 '의도적 안전 제약'이라고 설명.

## 경계 및 한계

논문은 한계를 명시적으로 논의하지 않음. 식별 가능한 경계: 고속 모드는 명시적 운영자 명령에 의존(자율 결정 아님); 게이팅 임계값(37°)에 대한 체계적 민감도 분석 없음; 옆으로 누운 자세 등 더 다양한 낙상 자세 미검증, 계단/경사 등 복잡한 지형 미테스트; 속도 범위가 3.0 m/s를 초과하지 않음; FSM 조정 모듈식 베이스라인과의 정량적 비교 없음. 훈련 데이터 양, 훈련 시간, 하드웨어 사양은 언급되지 않음.

## 공학적 시사점

재현 시 다음 세 가지를 우선 확인: **게이팅 임계값**(|g_z + 1| > 0.6)이 대상 로봇의 중력 신호 분포에서 여전히 이중 모드인지—센서 노이즈나 설치 편차로 분포가 이동하면 재보정 필요; **속도 조건 보간**(v̂_t)의 v_max 설정이 보행/달리기 참조 샘플링 비율에 직접 영향을 주므로 명령 속도 범위와 일치해야 함; **참조 데이터 리타겟팅 품질**—클립이 3개뿐이므로 역운동학으로 대상 로봇 관절 공간에 리타겟팅할 때 운동학적 왜곡이 발생하면 판별기가 잘못된 사전을 학습하게 됨. 가장 흔한 함정은 훈련 시 게이팅 활성화와 추론 시 은닉의 차이: 배포 코드에 모드 변수가 남아 있지 않은지 확인해야 함—그렇지 않으면 정책 입력 분포가 훈련과 불일치. 하위 팀이 속도 범위를 확장하거나 새 행동 모드를 추가해야 한다면 판별기 수를 늘리는 대신 v̂_t 조건화 방식을 조정하는 것이 우선.
