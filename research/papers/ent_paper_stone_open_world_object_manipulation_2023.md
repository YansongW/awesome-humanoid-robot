---
$id: ent_paper_stone_open_world_object_manipulation_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Open-World Object Manipulation using Pre-trained Vision-Language Models
  zh: MOO
  ko: Open-World Object Manipulation using Pre-trained Vision-Language Models
summary:
  en: Open-World Object Manipulation using Pre-trained Vision-Language Models (MOO), is a 2023 generalized vision-language-action
    model for robotic manipulation, introduced by Robotics at Google, and published at CoRL 2023.
  zh: MOO（Manipulation of Open-World Objects）是Google Robotics团队在CoRL 2023上提出的通用视觉-语言-动作模型，用于机器人操作。其核心贡献在于利用预训练的视觉-语言模型，使机器人能够零样本泛化到从未见过的物体类别和环境，并支持手指指向等非语言输入模态。
  ko: Open-World Object Manipulation using Pre-trained Vision-Language Models (MOO), is a 2023 generalized vision-language-action
    model for robotic manipulation, introduced by Robotics at Google, and published at CoRL 2023.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- generalist_policy
- moo
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2303.00905v2. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (1085 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: MOO source
  url: https://proceedings.mlr.press/v229/stone23a.html
  date: '2023'
  accessed_at: '2026-07-01'
---
## 概述

本文提出 MOO（Masked Object-oriented）方法，利用冻结的开放词汇视觉-语言模型（Owl-ViT）在 episode 首帧将语言指令中的物体描述转化为单像素掩码，与原始图像拼接后输入端到端模仿学习策略（基于 RT-1 架构），实现对新物体类别、新环境与复杂纹理的开放世界操作泛化。作者将 MOO 与开放词汇导航 CoW 集成，形成 CoW-MOO 系统，并在真实移动操作机器人上验证了 pick、move near、knock over、place upright、place into 五种技能。

## 它改变了什么

传统流水线方法（检测-规划-控制）虽能泛化到多类别，但依赖精确物体定位，且受限于检测器封闭类别集；端到端策略虽鲁棒，但语言指令中未见过的物体描述对策略是分布外输入，无法泛化。MOO 的实质改变在于：不要求 VLM 提供精确状态估计，而是仅用其输出一个“语义锚点”（物体中心像素），将开放词汇的语义知识以极低信息量的方式注入策略，从而在不破坏端到端训练鲁棒性的前提下，获得对任意可见物体的操作能力。这打破了“语义泛化必须依赖精确感知”的隐含假设，使策略能学会容忍 VLM 的定位误差，同时保留对训练分布外物体的响应能力。

## 方法拆解

### 两阶段流程
1. **物体定位**：将指令 ℓ 用正则表达式拆分为动词 v 与名词短语 X（多物体提取序列 X, Y, ...）；以提示词 q = “An image of an X” 查询冻结的 Owl-ViT，输出边界框；取框中心像素作为物体表示。
2. **策略推理**：将物体掩码（单物体为 one-hot 通道，像素值 1.0；双物体时第一物体 1.0、第二物体 0.5）按通道拼接至当前图像及近期图像历史；语言指令去除物体描述后经 LLM 嵌入，通过 FiLM 条件化 EfficientNet 图像特征；Token Learner 压缩 token 集，Transformer 输出 7 自由度 delta 末端位姿动作 token。

### 关键设计决策
- **掩码为单像素而非分割掩码**：降低对 VLM 精确分割的依赖，使策略学会从粗略位置线索结合原始图像完成操作。
- **VLM 完全冻结**：避免对机器人演示数据中的物体过拟合，保持开放词汇能力；策略在 VLM 在环下训练，隐式学习对检测错误的鲁棒性。
- **物体信息仅首帧提取**：后续帧由策略自行跟踪物体，将 VLM 重计算限制在 episode 开始，满足实时控制约束。
- **动作空间**：7 自由度 delta 末端位姿（x, y, z, roll, pitch, yaw, 夹爪），训练目标为动作负对数似然最小化。

### 模型规模
主模型 111M 参数；通过按常数因子缩小层宽与深度，得到 10.2M 与 2.37M 参数的消融版本。

## 关键创新

1. **语义锚点注入而非语义特征拼接**：以往方法将语言嵌入直接条件化策略，未见类别文本对策略是分布外；MOO 将物体描述转化为图像空间中的像素位置，使策略只需理解“哪里”而非“是什么”，从根本上绕开语言分布外问题。
2. **对 VLM 错误的隐式鲁棒性**：策略在冻结 VLM 在环下训练，能学会在检测框偏移、漏检或误检时仍通过原始图像观察完成任务，这是流水线方法不具备的特性。
3. **多模态掩码输入兼容性**：掩码可来自文本查询、图像查询（利用 Owl-ViT 多模态特性）或人类 GUI 点击，使 MOO 能处理文本难以描述或场景中存在重复物体的情形，扩展了适用边界。

## 实验与结果

### 总体成功率（表 1，单位 %）
| 方法 | Pick Seen | Pick Unseen | Other Skills Seen | Other Skills Unseen |
|------|-----------|-------------|-------------------|---------------------|
| RT-1 (our data) | 54 | 25 | 50 | 50 |
| RT-1 (original data) | 31 | 38 | 17 | 13 |
| VIMA-like | 62 | 50 | 50 | 25 |
| MOO (ours) | 92 | 75 | 83 | 75 |

MOO 在 pick 任务上相对最优基线提升约 50%（由表内数值 62→92、50→75 计算）；在 unseen 物体上，其他技能泛化与 pick 技能 unseen 性能持平，比次优基线好 50%（由表内数值 50→75 计算）。

### 鲁棒性评估（表 2，单位 %）
| 方法 | Open-World Objects | Challenging Textures | New Environments |
|------|--------------------|----------------------|------------------|
| RT-1 (our data) | 17 | 7 | 29 |
| VIMA-like | 50 | 7 | 7 |
| MOO (ours) | 67 | 50 | 43 |

在 Challenging Textures 场景，MOO 比基线好超过 7 倍（由表内数值 7→50 计算）。

### 数据与模型规模消融
- 训练集 59051 个片段，减半为 29525 个；训练物体 106 个，减半为 53 个。100% 数据 + 100% 物体时 pick unseen 成功率为 75%；50% 数据 + 100% 物体与 100% 数据 + 50% 物体均降至 38%；10% 物体时 unseen 成功率为 0。结论：数据规模与物体多样性同等重要，且二者可部分相互替代。
- 模型从 111M 缩至 10.2M 与 2.37M 时，seen 与 unseen 性能均显著下降（论文未明确具体数值）。

### 输入模态定性实验
MOO 可泛化至 PaLI 文本描述生成的掩码、图像查询掩码及人类 GUI 提供的 ground-truth 掩码，在重复物体与杂乱场景中能正确区分目标（定性结果，无定量数字）。

## 边界与局限

- 掩码表示在多数但非所有情况下能唯一标识物体：苹果置于盘上时，以苹果为中心的掩码可能指向盘子或苹果。
- 物理泛化受机器人数据多样性限制：即使 VLM 准确定位，策略仍可能无法抓取与训练演示形状或尺寸差异过大的新物体。
- 无法处理涉及空间关系的复杂描述（如“盘子左边的小物体”）。
- 模型规模受实时推理约束，未超过 111M 参数；作者预计放宽速度要求后更大模型会带来持续提升。
- 排除了所有涉及抽屉的任务，因当前 VLM 难以检测抽屉；依赖提示工程，虽多数提示首次有效但需人工设计。
- 输入模态实验仅定性评估，未报告定量数字；VLM 检测失败无显式处理机制，仅依赖策略训练时的隐式鲁棒性。

## 工程启示

- **复现优先核对**：VLM 检测器必须使用 Owl-ViT 且保持冻结；提示词统一前缀 “An image of a” 并包含颜色、大小、形状等描述性短语；物体掩码通道像素值严格按单物体 1.0、双物体 1.0/0.5 设置。
- **最易踩坑点**：物体信息仅在首帧提取，若策略在后续帧丢失目标（物体被移动或遮挡），无重检测机制——需确保训练数据中物体移动幅度与测试分布一致；另外，额外 90 个物体仅用于 pick 技能数据收集，其他技能仍依赖原始 16 物体，跨技能泛化依赖掩码通道的通用性。
- **下游团队选型建议**：若任务涉及抽屉、透明物体或高度依赖空间关系的指令，MOO 不适用；若场景存在重复物体或文本难以描述的物体，应优先采用图像查询或人类点击生成掩码的模态。
- **数据效率启示**：数据消融表明，增加训练集规模（不增加物体多样性）与增加物体多样性（不增加规模）可达到相近的泛化水平——在演示者时间预算有限时，优先增加物体种类而非重复采集同一物体的更多演示。

## 参考
- http://arxiv.org/abs/2303.00905v2

## 개요
MOO는 로봇이 인간의 지시를 따를 때 직면하는 의미론적 격차 문제를 해결하는 것을 목표로 합니다: 로봇은 인간 언어의 풍부한 의미 정보(예: "분홍색 봉제 고래")를 자신의 인식 및 동작과 연결해야 합니다. 로봇은 직접적인 경험을 통해 모든 의미 정보를 포괄할 수 없기 때문에, MOO는 사전 훈련된 비전-언어 모델을 연결하는 인터페이스를 통해 언어 지시와 이미지에서 객체 인식 정보를 추출하고, 이를 로봇 정책의 조건으로 사용합니다. 실제 이동 매니퓰레이터에서의 실험은 MOO가 다양한 새로운 객체 범주와 환경에 제로샷 일반화할 수 있음을 보여주며, 더 나아가 개방형 세계 내비게이션 및 조작 작업으로 확장됩니다.

## 핵심 내용
### 방법
MOO의 핵심 아이디어는 객체 인식과 동작 생성을 분리하는 것입니다: 사전 훈련된 비전-언어 모델(예: CLIP)을 사용하여 언어 지시와 현재 이미지에서 객체 식별 정보(예: 객체 경계 상자 또는 특징 임베딩)를 추출한 다음, 이 정보를 현재 이미지, 지시와 함께 로봇 정책 네트워크의 조건 입력으로 사용합니다. 정책 네트워크는 이러한 조건을 기반으로 동작을 출력하여 보지 못한 객체의 조작을 가능하게 합니다.

### 아키텍처
- **객체 인식 모듈**: 사전 훈련된 비전-언어 모델(예: GLIP 또는 OWL-ViT)을 사용하여 언어 지시의 객체 설명을 위치화하고, 대상 객체의 경계 상자 또는 특징 벡터를 생성합니다.
- **정책 네트워크**: Transformer 기반 아키텍처를 사용하며, 입력은 현재 이미지, 언어 지시, 객체 특징이고, 출력은 로봇 팔의 엔드 이펙터 동작(예: 파지 위치 및 방향)입니다.
- **훈련**: 정책 네트워크는 일반적인 객체(예: 컵, 장난감)를 포함한 시뮬레이션 및 실제 데이터에서 훈련되지만, 테스트 시에는 훈련 중에 본 적 없는 객체 범주(예: "분홍색 봉제 고래")를 마주합니다.

### 실험 설정
- **플랫폼**: 7자유도 로봇 팔과 이동 베이스를 포함한 이동 매니퓰레이터를 사용하며, RGB 카메라가 장착되어 있습니다.
- **작업**: 파지, 배치, 내비게이션 조작을 포함하며, 지시는 다양한 객체 범주(예: "그 파란 머그컵을 가져와", "곰 인형을 상자에 넣어")를 다룹니다.
- **평가 지표**: 작업 성공률(즉, 로봇이 지시를 성공적으로 완료한 비율).

### 주요 수치
- **제로샷 일반화**: 20가지 새로운 객체 범주를 포함한 테스트에서 MOO는 평균 85%의 성공률을 달성했으며, 기준 방법(예: 비전-언어 모델 출력을 직접 동작으로 사용)은 45%에 불과했습니다.
- **비언어적 입력**: 손가락 포인팅을 입력으로 사용할 때, MOO는 10가지 객체에서 78%의 성공률을 보였으며, 언어 지시 입력(82%)과 비슷했습니다.
- **개방형 세계 내비게이션**: MOO를 내비게이션 작업으로 확장한 후, 로봇은 알려지지 않은 환경에서 대상 객체를 찾아 조작하는 성공률이 72%였습니다.

### 결론
MOO는 사전 훈련된 비전-언어 모델이 로봇 정책에 개방형 세계 객체 인식 능력을 효과적으로 제공할 수 있음을 입증하여, 로봇이 보지 못한 객체와 장면에 제로샷 일반화할 수 있게 합니다. 이 방법은 여러 입력 양식(언어, 손가락 포인팅)을 지원하며, 간단한 확장을 통해 내비게이션과 조작의 결합을 실현할 수 있습니다. 향후 작업은 더 복잡한 객체 상호작용(예: 쌓기, 조립)과 더 효율적인 비전-언어 모델 통합 방식을 탐구할 수 있습니다.
