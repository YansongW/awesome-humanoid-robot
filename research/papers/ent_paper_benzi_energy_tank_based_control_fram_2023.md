---
$id: ent_paper_benzi_energy_tank_based_control_fram_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Energy Tank-based Control Framework for Satisfying the ISO/TS 15066 Constraint
  zh: 满足 ISO/TS 15066 约束的能量罐控制框架
  ko: ISO/TS 15066 제약을 만족시키는 에너지 탱크 기반 제어 프레임워크
summary:
  en: This paper proposes an energy tank-based, passivity-based control framework that directly enforces the energetic bounds
    defined by ISO/TS 15066 for collaborative robots, avoiding conservative velocity limits while preserving stability during
    free motion and human contact. The approach was validated in simulation on a KUKA LWR 4+ 7-DOF force-controlled manipulator.
  zh: 本文提出一种基于能量罐的被动控制框架，用于直接满足ISO/TS 15066对协作机器人的能量约束，避免保守的速度限制，同时保持自由运动与人机接触时的稳定性。该方法在KUKA LWR 4+七自由度力控机械臂上进行了仿真验证。
  ko: 본 논문은 협업 로봇을 위해 ISO/TS 15066에서 정의한 에너지 경계를 직접 적용하는 에너지 탱크 기반 수동성 기반 제어 프레임워크를 제안하여 보수적인 속도 제한을 피하면서 자유 운동 및 인간 접촉 시
    안정성을 유지합니다. 이 접근법은 KUKA LWR 4+ 7자유도 힘 제어 매니퓰레이터 시뮬레이션에서 검증되었습니다.
domains:
- 07_ai_models_algorithms
- 12_policy_regulation_ethics
- 11_applications_markets
layers:
- intelligence
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- energy_tank_control
- passivity_based_control
- iso_ts_15066
- power_force_limiting
- collaborative_robots
- human_robot_interaction
- safety_constraints
- kinetic_energy_bounds
- force_control
- industrial_humanoids
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2304.14059v1. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: Energy Tank-based Control Framework for Satisfying the ISO/TS 15066 Constraint
  url: https://arxiv.org/abs/2304.14059
  date: '2023'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

本文提出一种基于调制能量罐（modulated energy tank）的无源性控制框架，用于直接满足ISO/TS 15066标准中规定的时变能量边界（PFL），无需精确的机器人动力学模型。该方法通过在线调节能量罐下界和可变阻尼器处理外部能量注入，在保证形式化稳定性的同时，显著降低传统速度限制方法的保守性。仿真验证在KUKA LWR 4+ 7-DOF机械臂上进行，展示了在姿态变化和自运动影响下对能量边界的严格遵从。

## 它改变了什么

传统ISO/TS 15066实施方式通过速度限制实现PFL，但速度限制基于完全非弹性碰撞和两体模型的强假设，导致严重保守——机器人实际运动速度可以高于标准计算的速度上限，同时仍满足能量约束。现有CBF方法（如Ferraguti et al. 2020）的安全集定义过于保守（椭圆集合），且因椭球体突然激活/停用导致非平滑行为。更关键的是，Lachner et al. 2021的能量方法严重依赖惯性矩阵等动力学参数的精确知识，这在工业场景中往往不可得或不够准确。

本文真正改变的是：将PFL合规从"速度限制"范式转向"能量限制"范式，且不要求模型知识。它直接处理ISO/TS 15066中定义的动能上限，通过能量罐的功率流控制实现安全约束，同时提供形式化的稳定性保证（Prop. 2和Prop. 3）。这解决了三个核心痛点：保守性（速度限制过严）、模型依赖性（需要精确惯性矩阵）、以及非平滑性（CBF的椭球激活问题）。该方法可作为任何力控机器人的通用安全层，无需修改底层控制器。

## 方法拆解

### 系统建模
- 机器人动力学：n自由度Euler-Lagrange模型，M(q)q̈ + C(q,q̇)q̇ + g(q) = τ(t) + J(q)ᵀFₑ(t)
- 操作空间模型：Λ(x)ẍ + S(x,ẋ)ẋ = F(t)，其中F(t) = -F_c(t) + F_e(t)
- 动能计算：H(t) = ½ ẋ(t)ᵀ Λ(x) ẋ(t) ≥ 0

### 控制架构
- 调制能量罐与机器人互联，输入为笛卡尔速度ẋ(t)，输出为控制力F_c(t)
- 目标：实现期望控制力F_des(t)，同时保证动能上限H(t) ≤ H̄

### 优化问题（离散时间凸优化）
- 基础形式：最小化||F_c(k) - F_des(k)||²，约束为τ F_c(k)ᵀ ẋ(k) + T(k-1) ≥ ε
- 保持方向修正：最小化||α F_des(k) - F_des(k)||²，约束为τ α F_des(k)ᵀ ẋ(k) + T(k-1) ≥ ε，其中F_c = α F_des

### 关键设计决策
- **初始条件**：T(0) = H̄ - H(0) + ε，保证动能上限（Prop. 2）
- **外部能量处理**：罐动力学修改为ẋ_t(t) = (1/x_t(t))[-F_e(t)ᵀ ẋ(t) + b(t) ẋ(t)ᵀ ẋ(t)] + a(t)ᵀ u(t)，其中b(t)为可变阻尼器，仅在F_e(t)ᵀ ẋ(t) > 0且T(t) = ε时激活，用于耗散外部注入能量
- **变能量界限**：当最近身体部位改变时，在线调整罐下界ε(k_i) = T(0) - H_i + H(0)，切换动能上限（Prop. 3保证稳定性）

### 模型无关性
- 方法无需机器人动力学参数（惯性、科里奥利项）知识，仅依赖操作空间动能计算

## 关键创新

1. **模型无关的能量边界合规**：与Lachner et al. 2021不同，该方法不依赖惯性矩阵的精确知识，仅需操作空间动能计算，大幅降低了实际部署的门槛。这是对现有能量方法的关键改进，使得该方法适用于动力学参数不确定或时变的场景。

2. **时变能量边界的在线切换机制**：通过调整罐下界ε(k_i) = T(0) - H_i + H(0)，在最近身体部位改变时平滑切换动能上限，Prop. 3提供了形式化稳定性保证。这解决了多段运动中不同身体部位对应不同能量限制的实际问题，是ISO/TS 15066合规中此前未充分处理的场景。

3. **外部能量注入的主动耗散**：可变阻尼器b(t)仅在外部能量注入且罐能量达到下界时激活，通过耗散项b(t) ẋ(t)ᵀ ẋ(t)主动消除外部能量影响，确保系统在物理交互下仍保持无源性。这一机制使得方法适用于人机协作中的接触场景，而不仅仅是自由运动。

## 实验与结果

### 仿真设置
- 平台：KUKA LWR 4+ 7-DOF力控机械臂，MATLAB建模，Robotics Toolbox计算动力学，采样时间1ms
- 任务：两段运动，第一段最近身体部位为胸部，第二段为肩部

### 关键参数与结果

| 参数 | 第一阶段（胸部） | 第二阶段（肩部） |
|------|-----------------|-----------------|
| 最大力 | 140 N | - |
| 刚度 | 25 N/mm | - |
| 质量 | 40 kg | - |
| 最大能量 E_max | 1.6 J | 2.5 J |
| 速度上限 v_max | 0.29 m/s | 0.37 m/s |
| 罐下界 ε | 3.4 J | 2.5 J |
| 罐初始能量 T(0) | 5.0 J | - |

### 关键行为
- 第一阶段：机器人遵循期望速度曲线，只要动能低于E_max1（罐能量高于ε1）
- 第二阶段：机器人在7.5 sec后减速至低于v_max2，原因是姿态条件和自运动导致动能已达E_max2
- 自由运动时P_ext = 0 W

### 结果含义
- 机器人实际运动速度高于v_max1和v_max2施加的速度值，同时仍满足PFL能量要求——证明速度限制确实保守
- 能量罐机制在边界达到时通过公式(28)近似输入，以牺牲性能为代价保证安全

## 边界与局限

- **仿真验证局限**：所有结果仅在MATLAB仿真环境中获得，未在真实硬件上验证。实际机器人中的模型误差、执行器延迟、传感器噪声可能影响能量罐的功率流计算精度。
- **稳定性证明不完整**：边界切换阶段的稳定性和安全性形式化证明被列为未来工作，当前Prop. 3仅保证切换后的稳定性，未覆盖切换瞬态行为。
- **未与现有方法定量对比**：论文未提供与CBF方法（Ferraguti et al. 2020）或速度限制方法的定量性能对比数据，无法直接评估保守性降低的具体幅度。
- **外部能量处理未充分验证**：可变阻尼器机制仅在理论层面描述，仿真中P_ext = 0 W，未验证实际接触场景下的有效性。
- **未利用运动学冗余**：作者明确将"利用运动学冗余降低表观质量"列为未来工作，当前方法未优化机器人姿态以降低碰撞风险。

## 工程启示

- **优先核对动能计算**：实现时需确保操作空间惯性矩阵Λ(x)的计算准确，虽然方法不依赖完整动力学模型，但动能计算仍需要Λ(x)。建议使用Robotics Toolbox等成熟工具验证Λ(x)的正确性。
- **注意罐下界切换时机**：ε(k_i) = T(0) - H_i + H(0)的切换必须与最近身体部位检测同步，延迟可能导致能量边界违规。建议在控制器中实现身体部位检测与罐参数更新的原子操作。
- **采样时间敏感**：优化问题在离散时间求解，采样时间1ms下性能良好，但若采样时间增大，凸优化求解延迟可能影响实时性。建议评估优化求解器在最坏情况下的计算时间。
- **外部能量耗散参数调优**：可变阻尼器b(t)的增益需要根据实际接触场景调优，过大可能导致系统响应迟钝，过小则无法有效耗散外部能量。建议在仿真中先验证不同b(t)值下的系统行为。
- **最易踩坑点**：初始条件T(0) = H̄ - H(0) + ε的设置必须精确，若H(0)计算有误（如初始速度非零），将导致能量罐初始能量错误，进而影响整个任务的安全保证。建议在启动时执行一次零速度校准。

## 参考
- http://arxiv.org/abs/2304.14059v1

## Overview

This paper proposes a passivity-based control framework using a modulated energy tank to directly satisfy the time-varying energy bounds (PFL) specified in the ISO/TS 15066 standard, without requiring an accurate robot dynamics model. The method handles external energy injection by online adjusting the energy tank lower bound and a variable damper, ensuring formal stability while significantly reducing the conservatism of traditional velocity-limiting approaches. Simulations are conducted on a KUKA LWR 4+ 7-DOF manipulator, demonstrating strict compliance with energy bounds under the influence of posture changes and self-motion.

## What It Changes

Traditional ISO/TS 15066 implementation achieves PFL through velocity limits, but these limits rely on strong assumptions of perfectly inelastic collisions and a two-body model, leading to severe conservatism—the robot's actual motion speed can be higher than the standard-computed velocity upper bound while still satisfying the energy constraint. Existing CBF-based methods (e.g., Ferraguti et al. 2020) define safety sets too conservatively (ellipsoidal sets) and exhibit non-smooth behavior due to sudden activation/deactivation of the ellipsoid. More critically, the energy-based approach of Lachner et al. 2021 heavily depends on accurate knowledge of dynamic parameters such as the inertia matrix, which is often unavailable or inaccurate in industrial scenarios.

What this paper truly changes is shifting PFL compliance from a "velocity-limiting" paradigm to an "energy-limiting" paradigm, without requiring model knowledge. It directly addresses the kinetic energy upper bound defined in ISO/TS 15066, enforcing safety constraints through power-flow control of the energy tank, while providing formal stability guarantees (Prop. 2 and Prop. 3). This resolves three core pain points: conservatism (overly strict velocity limits), model dependence (requiring accurate inertia matrices), and non-smoothness (CBF ellipsoid activation issues). The method can serve as a universal safety layer for any force-controlled robot without modifying the underlying controller.

## Method Breakdown

### System Modeling
- Robot dynamics: n-degree-of-freedom Euler-Lagrange model, M(q)q̈ + C(q,q̇)q̇ + g(q) = τ(t) + J(q)ᵀFₑ(t)
- Operational space model: Λ(x)ẍ + S(x,ẋ)ẋ = F(t), where F(t) = -F_c(t) + F_e(t)
- Kinetic energy computation: H(t) = ½ ẋ(t)ᵀ Λ(x) ẋ(t) ≥ 0

### Control Architecture
- The modulated energy tank is interconnected with the robot, with Cartesian velocity ẋ(t) as input and control force F_c(t) as output
- Objective: achieve the desired control force F_des(t) while guaranteeing the kinetic energy bound H(t) ≤ H̄

### Optimization Problem (Discrete-Time Convex Optimization)
- Basic form: minimize ||F_c(k) - F_des(k)||², subject to τ F_c(k)ᵀ ẋ(k) + T(k-1) ≥ ε
- Direction-preserving modification: minimize ||α F_des(k) - F_des(k)||², subject to τ α F_des(k)ᵀ ẋ(k) + T(k-1) ≥ ε, where F_c = α F_des

### Key Design Decisions
- **Initial condition**: T(0) = H̄ - H(0) + ε, guaranteeing the kinetic energy bound (Prop. 2)
- **External energy handling**: The tank dynamics are modified to ẋ_t(t) = (1/x_t(t))[-F_e(t)ᵀ ẋ(t) + b(t) ẋ(t)ᵀ ẋ(t)] + a(t)ᵀ u(t), where b(t) is a variable damper activated only when F_e(t)ᵀ ẋ(t) > 0 and T(t) = ε, used to dissipate externally injected energy
- **Variable energy bound**: When the closest body part changes, the tank lower bound is adjusted online as ε(k_i) = T(0) - H_i + H(0), switching the kinetic energy upper bound (Prop. 3 guarantees stability)

### Model Independence
- The method does not require knowledge of robot dynamic parameters (inertia, Coriolis terms), relying only on operational space kinetic energy computation

## Key Innovations

1. **Model-independent energy bound compliance**: Unlike Lachner et al. 2021, this method does not depend on accurate knowledge of the inertia matrix, requiring only operational space kinetic energy computation, significantly lowering the barrier to practical deployment. This is a key improvement over existing energy-based methods, making the approach applicable to scenarios with uncertain or time-varying dynamic parameters.

2. **Online switching mechanism for time-varying energy bounds**: By adjusting the tank lower bound ε(k_i) = T(0) - H_i + H(0), the kinetic energy upper bound is smoothly switched when the closest body part changes, with Prop. 3 providing formal stability guarantees. This addresses the practical issue where different body parts correspond to different energy limits in multi-segment motion, a scenario not adequately handled in prior ISO/TS 15066 compliance work.

3. **Active dissipation of external energy injection**: The variable damper b(t) is activated only when external energy is injected and the tank energy reaches its lower bound, actively eliminating external energy effects through the dissipation term b(t) ẋ(t)ᵀ ẋ(t), ensuring the system remains passive under physical interaction. This mechanism makes the method applicable to contact scenarios in human-robot collaboration, not just free motion.

## Experiments and Results

### Simulation Setup
- Platform: KUKA LWR 4+ 7-DOF force-controlled manipulator, MATLAB modeling, Robotics Toolbox for dynamics computation, sampling time 1 ms
- Task: Two-segment motion, with the closest body part being the chest in the first segment and the shoulder in the second

### Key Parameters and Results

| Parameter | Phase 1 (Chest) | Phase 2 (Shoulder) |
|-----------|-----------------|-------------------|
| Maximum force | 140 N | - |
| Stiffness | 25 N/mm | - |
| Mass | 40 kg | - |
| Maximum energy E_max | 1.6 J | 2.5 J |
| Velocity upper bound v_max | 0.29 m/s | 0.37 m/s |
| Tank lower bound ε | 3.4 J | 2.5 J |
| Tank initial energy T(0) | 5.0 J | - |

### Key Behaviors
- Phase 1: The robot follows the desired velocity profile as long as kinetic energy remains below E_max1 (tank energy above ε1)
- Phase 2: The robot decelerates below v_max2 after 7.5 sec, due to posture conditions and self-motion causing kinetic energy to reach E_max2
- P_ext = 0 W during free motion

### Implications of Results
- The robot's actual motion speed exceeds the velocity values imposed by v_max1 and v_max2 while still satisfying the PFL energy requirement—demonstrating that velocity limits are indeed conservative
- The energy tank mechanism approximates the input via equation (28) when the bound is reached, trading performance for safety

## Boundaries and Limitations

- **Simulation-only validation**: All results are obtained in a MATLAB simulation environment and have not been validated on real hardware. Model errors, actuator delays, and sensor noise in actual robots may affect the accuracy of energy tank power-flow computation.
- **Incomplete stability proof**: Formal proof of stability and safety during the boundary switching phase is listed as future work; the current Prop. 3 only guarantees stability after switching and does not cover transient behavior during the switch.
- **No quantitative comparison with existing methods**: The paper does not provide quantitative performance comparisons with CBF-based methods (Ferraguti et al. 2020) or velocity-limiting approaches, making it impossible to directly assess the specific degree of conservatism reduction.
- **Insufficient validation of external energy handling**: The variable damper mechanism is only described theoretically, and since P_ext = 0 W in the simulation, its effectiveness in actual contact scenarios is not validated.
- **Kinematic redundancy not exploited**: The authors explicitly list "exploiting kinematic redundancy to reduce apparent mass" as future work; the current method does not optimize robot posture to reduce collision risk.

## Engineering Insights

- **Prioritize verification of kinetic energy computation**: Implementation must ensure accurate computation of the operational space inertia matrix Λ(x). Although the method does not rely on a full dynamic model, kinetic energy computation still requires Λ(x). It is recommended to use mature tools such as the Robotics Toolbox to verify the correctness of Λ(x).
- **Pay attention to tank lower bound switching timing**: The switching of ε(k_i) = T(0) - H_i + H(0) must be synchronized with closest-body-part detection; delays may lead to energy bound violations. It is recommended to implement atomic operations for body-part detection and tank parameter updates in the controller.
- **Sensitivity to sampling time**: The optimization problem is solved in discrete time and performs well at a 1 ms sampling time, but if the sampling time increases, the convex optimization solution delay may affect real-time performance. It is recommended to evaluate the worst-case computation time of the optimization solver.
- **Tuning of external energy dissipation parameters**: The gain of the variable damper b(t) needs to be tuned according to actual contact scenarios; too large a value may cause sluggish system response, while too small a value may fail to effectively dissipate external energy. It is recommended to first validate system behavior under different b(t) values in simulation.
- **Most common pitfall**: The initial condition T(0) = H̄ - H(0) + ε must be set precisely. If H(0) is computed incorrectly (e.g., non-zero initial velocity), the energy tank's initial energy will be wrong, affecting the safety guarantee for the entire task. It is recommended to perform a zero-velocity calibration at startup.

## 개요

본 논문은 ISO/TS 15066 표준에 명시된 시변 에너지 경계(PFL)를 정밀한 로봇 동역학 모델 없이 직접 충족하기 위한 변조 에너지 탱크(modulated energy tank) 기반의 무源性 제어 프레임워크를 제안한다. 이 방법은 에너지 탱크 하한과 가변 댐퍼를 온라인으로 조절하여 외부 에너지 주입을 처리하며, 형식적 안정성을 보장하면서 기존 속도 제한 방식의 보수성을 크게 줄인다. 시뮬레이션 검증은 KUKA LWR 4+ 7-DOF 로봇 팔에서 수행되었으며, 자세 변화와 자체 운동 영향 하에서 에너지 경계를 엄격히 준수함을 보여준다.

## 그것이 바꾸는 것

기존 ISO/TS 15066 구현 방식은 속도 제한을 통해 PFL을 달성하지만, 속도 제한은 완전 비탄성 충돌과 이체 모델의 강한 가정에 기반하여 심각한 보수성을 초래한다—로봇의 실제 운동 속도는 표준에서 계산된 속도 상한보다 높을 수 있으며 동시에 에너지 제약을 여전히 충족한다. 기존 CBF 방법(예: Ferraguti et al. 2020)의 안전 집합 정의는 지나치게 보수적(타원 집합)이며, 타원체의 갑작스러운 활성화/비활성화로 인해 비평활 동작을 유발한다. 더 중요하게는, Lachner et al. 2021의 에너지 방법은 관성 행렬과 같은 동역학 매개변수의 정확한 지식에 크게 의존하는데, 이는 산업 현장에서 종종 얻을 수 없거나 충분히 정확하지 않다.

본 논문이 진정으로 바꾸는 것은: PFL 준수를 "속도 제한" 패러다임에서 "에너지 제한" 패러다임으로 전환하고, 모델 지식을 요구하지 않는다는 점이다. ISO/TS 15066에서 정의된 운동 에너지 상한을 직접 처리하며, 에너지 탱크의 전력 흐름 제어를 통해 안전 제약을 구현하면서 형식적 안정성 보장(Prop. 2 및 Prop. 3)을 제공한다. 이는 세 가지 핵심 문제점을 해결한다: 보수성(속도 제한이 너무 엄격함), 모델 의존성(정확한 관성 행렬 필요), 비평활성(CBF의 타원 활성화 문제). 이 방법은 하위 수준 제어기를 수정할 필요 없이 모든 힘 제어 로봇의 범용 안전 계층으로 작동할 수 있다.

## 방법 분해

### 시스템 모델링
- 로봇 동역학: n자유도 Euler-Lagrange 모델, M(q)q̈ + C(q,q̇)q̇ + g(q) = τ(t) + J(q)ᵀFₑ(t)
- 작업 공간 모델: Λ(x)ẍ + S(x,ẋ)ẋ = F(t), 여기서 F(t) = -F_c(t) + F_e(t)
- 운동 에너지 계산: H(t) = ½ ẋ(t)ᵀ Λ(x) ẋ(t) ≥ 0

### 제어 아키텍처
- 변조 에너지 탱크는 로봇과 상호 연결되며, 입력은 데카르트 속도 ẋ(t), 출력은 제어 힘 F_c(t)
- 목표: 원하는 제어 힘 F_des(t)를 구현하면서 운동 에너지 상한 H(t) ≤ H̄ 보장

### 최적화 문제(이산 시간 볼록 최적화)
- 기본 형태: ||F_c(k) - F_des(k)||² 최소화, 제약 조건 τ F_c(k)ᵀ ẋ(k) + T(k-1) ≥ ε
- 방향 유지 수정: ||α F_des(k) - F_des(k)||² 최소화, 제약 조건 τ α F_des(k)ᵀ ẋ(k) + T(k-1) ≥ ε, 여기서 F_c = α F_des

### 핵심 설계 결정
- **초기 조건**: T(0) = H̄ - H(0) + ε, 운동 에너지 상한 보장(Prop. 2)
- **외부 에너지 처리**: 탱크 동역학을 ẋ_t(t) = (1/x_t(t))[-F_e(t)ᵀ ẋ(t) + b(t) ẋ(t)ᵀ ẋ(t)] + a(t)ᵀ u(t)로 수정, 여기서 b(t)는 가변 댐퍼이며 F_e(t)ᵀ ẋ(t) > 0이고 T(t) = ε일 때만 활성화되어 외부 주입 에너지를 소산
- **가변 에너지 경계**: 최근 신체 부위가 변경될 때 탱크 하한 ε(k_i) = T(0) - H_i + H(0)을 온라인으로 조정하여 운동 에너지 상한을 전환(Prop. 3이 안정성 보장)

### 모델 무관성
- 이 방법은 로봇 동역학 매개변수(관성, 코리올리 항) 지식이 필요 없으며, 작업 공간 운동 에너지 계산에만 의존

## 핵심 혁신

1. **모델 무관 에너지 경계 준수**: Lachner et al. 2021과 달리, 이 방법은 관성 행렬의 정확한 지식에 의존하지 않고 작업 공간 운동 에너지 계산만 필요로 하여 실제 배포 장벽을 크게 낮춘다. 이는 기존 에너지 방법에 대한 핵심 개선으로, 동역학 매개변수가 불확실하거나 시변하는 시나리오에 적용 가능하게 한다.

2. **시변 에너지 경계의 온라인 전환 메커니즘**: 탱크 하한 ε(k_i) = T(0) - H_i + H(0)을 조정하여 최근 신체 부위가 변경될 때 운동 에너지 상한을 부드럽게 전환하며, Prop. 3이 형식적 안정성 보장을 제공한다. 이는 다중 구간 운동에서 서로 다른 신체 부위가 서로 다른 에너지 제한에 해당하는 실제 문제를 해결하며, ISO/TS 15066 준수에서 이전에 충분히 다루어지지 않은 시나리오이다.

3. **외부 에너지 주입의 능동 소산**: 가변 댐퍼 b(t)는 외부 에너지 주입이 있고 탱크 에너지가 하한에 도달했을 때만 활성화되며, 소산 항 b(t) ẋ(t)ᵀ ẋ(t)을 통해 외부 에너지 영향을 능동적으로 제거하여 물리적 상호작용 하에서도 시스템의 무源性을 보장한다. 이 메커니즘은 자유 운동뿐만 아니라 인간-로봇 협력의 접촉 시나리오에도 방법을 적용 가능하게 한다.

## 실험 및 결과

### 시뮬레이션 설정
- 플랫폼: KUKA LWR 4+ 7-DOF 힘 제어 로봇 팔, MATLAB 모델링, Robotics Toolbox 동역학 계산, 샘플링 시간 1ms
- 작업: 두 구간 운동, 첫 번째 구간의 최근 신체 부위는 가슴, 두 번째는 어깨

### 핵심 매개변수 및 결과

| 매개변수 | 첫 번째 단계(가슴) | 두 번째 단계(어깨) |
|------|-----------------|-----------------|
| 최대 힘 | 140 N | - |
| 강성 | 25 N/mm | - |
| 질량 | 40 kg | - |
| 최대 에너지 E_max | 1.6 J | 2.5 J |
| 속도 상한 v_max | 0.29 m/s | 0.37 m/s |
| 탱크 하한 ε | 3.4 J | 2.5 J |
| 탱크 초기 에너지 T(0) | 5.0 J | - |

### 핵심 동작
- 첫 번째 단계: 로봇은 운동 에너지가 E_max1보다 낮은 한(탱크 에너지가 ε1보다 높은 한) 원하는 속도 곡선을 따름
- 두 번째 단계: 로봇은 7.5초 후 v_max2보다 낮은 속도로 감속, 자세 조건과 자체 운동으로 인해 운동 에너지가 E_max2에 도달했기 때문
- 자유 운동 시 P_ext = 0 W

### 결과 의미
- 로봇의 실제 운동 속도는 v_max1 및 v_max2가 부과하는 속도 값보다 높으면서도 PFL 에너지 요구를 여전히 충족—속도 제한이 실제로 보수적임을 증명
- 에너지 탱크 메커니즘은 경계 도달 시 식(28)을 통해 입력을 근사하여 성능을 희생하면서 안전을 보장

## 경계 및 한계

- **시뮬레이션 검증 한계**: 모든 결과는 MATLAB 시뮬레이션 환경에서만 얻어졌으며 실제 하드웨어에서 검증되지 않았다. 실제 로봇의 모델 오차, 액추에이터 지연, 센서 노이즈는 에너지 탱크의 전력 흐름 계산 정확도에 영향을 미칠 수 있다.
- **안정성 증명 불완전**: 경계 전환 단계의 안정성과 안전성에 대한 형식적 증명은 향후 작업으로 분류되었으며, 현재 Prop. 3은 전환 후 안정성만 보장하고 전환 과도 동작은 다루지 않는다.
- **기존 방법과의 정량적 비교 부재**: 논문은 CBF 방법(Ferraguti et al. 2020) 또는 속도 제한 방법과의 정량적 성능 비교 데이터를 제공하지 않아 보수성 감소의 구체적 규모를 직접 평가할 수 없다.
- **외부 에너지 처리의 불충분한 검증**: 가변 댐퍼 메커니즘은 이론적 수준에서만 설명되었으며, 시뮬레이션에서 P_ext = 0 W로 실제 접촉 시나리오에서의 유효성이 검증되지 않았다.
- **운동학적 여유도 미활용**: 저자는 "운동학적 여유도를 활용한 겉보기 질량 감소"를 향후 작업으로 명시적으로 분류했으며, 현재 방법은 충돌 위험을 줄이기 위한 로봇 자세 최적화를 수행하지 않는다.

## 공학적 시사점

- **운동 에너지 계산 우선 검증**: 구현 시 작업 공간 관성 행렬 Λ(x)의 계산 정확성을 보장해야 한다. 방법이 완전한 동역학 모델에 의존하지 않더라도 운동 에너지 계산에는 여전히 Λ(x)가 필요하다. Robotics Toolbox와 같은 성숙한 도구를 사용하여 Λ(x)의 정확성을 검증할 것을 권장한다.
- **탱크 하한 전환 시점 주의**: ε(k_i) = T(0) - H_i + H(0)의 전환은 최근 신체 부위 감지와 동기화되어야 하며, 지연은 에너지 경계 위반을 초래할 수 있다. 컨트롤러에서 신체 부위 감지와 탱크 매개변수 업데이트의 원자적 작업 구현을 권장한다.
- **샘플링 시간 민감성**: 최적화 문제는 이산 시간에서 해결되며, 샘플링 시간 1ms에서 성능이 양호하지만 샘플링 시간이 증가하면 볼록 최적화 해결 지연이 실시간성에 영향을 미칠 수 있다. 최악의 경우 최적화 솔버 계산 시간을 평가할 것을 권장한다.
- **외부 에너지 소산 매개변수 튜닝**: 가변 댐퍼 b(t)의 이득은 실제 접촉 시나리오에 따라 튜닝해야 하며, 너무 크면 시스템 응답이 둔해지고 너무 작으면 외부 에너지를 효과적으로 소산할 수 없다. 시뮬레이션에서 다양한 b(t) 값에 따른 시스템 동작을 먼저 검증할 것을 권장한다.
- **가장 쉽게 실수하는 지점**: 초기 조건 T(0) = H̄ - H(0) + ε의 설정은 정확해야 하며, H(0) 계산에 오류가 있으면(예: 초기 속도가 0이 아닌 경우) 에너지 탱크 초기 에너지가 잘못되어 전체 작업의 안전 보장에 영향을 미친다. 시작 시 영속도 보정을 한 번 수행할 것을 권장한다.
