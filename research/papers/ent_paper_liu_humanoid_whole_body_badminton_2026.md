---
$id: ent_paper_liu_humanoid_whole_body_badminton_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Humanoid Whole-Body Badminton via Multi-Stage Reinforcement Learning
  zh: 基于多阶段强化学习的人形全身羽毛球运动
  ko: 다단계 강화학습을 통한 휴머노이드 전신 배드민턴
summary:
  en: A unified whole-body reinforcement-learning controller enables a 21-DoF humanoid to play badminton in simulation and
    on hardware, achieving 21-hit rallies in simulation and outgoing shuttle speeds up to 19.1 m/s in real-world tests.
  zh: 本文提出一种基于多阶段强化学习的统一全身控制器，使21自由度人形机器人能在仿真和硬件上打羽毛球。核心贡献包括：无需运动先验或专家演示的三阶段训练流程（步法、挥拍、精调），以及基于EKF的轨迹预测与无预测变体。仿真中实现21拍连续对打，真实世界测试中出球速度达19.1
    m/s。
  ko: 통합된 전신 강화학습 제어기를 통해 21자유도 휴머노이드가 시뮬레이션 및 실제 환경에서 배드민턴을 칠 수 있으며, 시뮬레이션에서는 21회 연속 랠리를, 실제 테스트에서는 초당 최대 19.1m의 셔틀콕 속도를
    달성했다.
domains:
- 07_ai_models_algorithms
- 02_components
- 11_applications_markets
layers:
- intelligence
- upstream
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- reinforcement_learning
- whole_body_control
- sim_to_real
- humanoid_locomotion
- badminton
- ppo
- isaacgym
- extended_kalman_filter
- prediction_free
- dynamic_object_interaction
- curriculum_learning
- phybot_c1
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2511.11218v3. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
sources:
- id: src_001
  type: paper
  title: Humanoid Whole-Body Badminton via Multi-Stage Reinforcement Learning
  url: https://arxiv.org/abs/2511.11218
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

本文提出首个在真实世界打羽毛球的人形机器人系统，由统一全身强化学习策略驱动，通过三阶段课程学习协调步法与挥拍，实现零样本迁移到自研 Phybot C1 硬件。核心贡献在于将敏捷、接触丰富的动态交互从仿真推进到真实部署，验证了多阶段 RL 在亚秒级感知-行动循环中的可行性。

## 它改变了什么

羽毛球任务暴露了现有机器人学习范式的根本缺口：准静态交互假设在真实动态环境中不成立，而乒乓球等既有测试平台又因 2D 简化或缺乏全身扰动而回避了核心难题。本文真正改变的是将"步法与击球必须共同演化"这一物理约束从理论讨论推向工程实践——下肢运动不仅重新定位基底，还通过支撑和动量传递直接决定击球精度，这迫使策略必须在一个统一的优化目标下同时处理平衡维持、快速挥臂和精确计时，而非分层叠加独立控制器。

另一个关键转变在于放弃参考运动或专家示范。作者证明纯通过强化学习即可涌现出类人的后摆-挥拍-恢复动作链，且能自发产生短促修正步、长跨步并行后摆、踮脚伸展等适应不同时间窗口的行为。这暗示对于时间紧迫、接触丰富的全身任务，课程化奖励塑形可能比模仿学习更具扩展性，因为后者难以覆盖连续的动作变体空间。

## 方法拆解

### 三阶段课程训练
- **S1 步法获取**：仅优化 locomotion 奖励，学习在六个采样击球位置间保持稳定、面向前方的步态。主塑形项 `r_track = exp(−4·max(d−0.3, 0))`，d 为基底与目标水平距离，接近 0.3 m 内即算成功。约 1k 迭代收敛。
- **S2 精度引导挥拍**：在 S1 基础上引入仅在预定击球时刻 t* 激活的稀疏奖励 `r_swing = exp(−e_ee_pos²/σ_p)·exp(−e_ee_ori/σ_r) + 0.3·r_v`，其中 `r_v = 1 − exp(−max(0, v_ee·n*)/σ_v)`。关键设计是位置与方向误差耦合相乘，避免策略过度优化单一指标。精度调度从 σ_p=2.0, σ_r=8.0 收紧至 σ_p=0.1, σ_r=1.0，让完整挥拍先涌现再精修。约 20k 迭代后出现平滑动作链。
- **S3 任务聚焦精炼**：移除 S1 的目标接近奖励和大部分步态正则化（feet_orientation、feet_height、feet_distance、sym_contact_forces、sym_step、no_fly），避免与击球目标的梯度干扰。保留安全、能量、硬件约束项，启用域随机化和观测噪声。击球奖励提升 3–5%，能量和扭矩成本降低约 20%。

### 网络与训练配置
- Actor 观察 87 维基础特征 + 短/长历史（5 帧 + 20 帧），总计 1,557 维；Critic 98 维特权观察，额外接收两个后续击球时间、下一目标位置和方向、剩余目标数。
- 动作空间为全部 21 个 DoF 的关节位置目标，统一缩放 0.25，由 500 Hz 低层 PD 控制器跟踪。
- PPO，单张 RTX 4090，4096 并行环境，MLP 512–256–128，ELU 激活，三阶段总耗时 12.4 小时。

### 部署预测
- EKF 状态向量 [x, y, z, v_x, v_y, v_z]^T，使用与训练相同的羽毛球动力学模型 `m·dv/dt = mg − m·‖v‖·v/L`（L=3.4）。当预测高度首次低于击球平面时，线性插值得到击球点和时间。
- 无预测变体仅改变观察空间，用滑动窗口 6 帧羽毛球位置替代显式目标，训练时随机化空气动力学参数，模拟中性能下降很小。

## 关键创新

1. **首个真实世界人形羽毛球系统**：此前工作止步于仿真或简化硬件，本文在 1.28 m、30 kg、21 DoF 的 Phybot C1 上实现零样本部署，无系统辨识或手动调参，验证了域随机化对动态接触任务的有效性。

2. **耦合位置-方向的击球奖励**：相比 [6] 分开处理位置和方向误差，本文将二者相乘耦合，防止策略过度优化其一。这一设计选择在消融中显示更优的精度收敛行为，且更符合羽毛球"位置、速度、方向对齐的短暂窗口"的物理本质。

3. **三阶段课程消除梯度干扰**：S3 移除下肢正则化是关键创新——作者发现步态塑形项与击球目标存在梯度冲突，移除后击球指标继续改善且能耗下降。这提示对于全身动态任务，早期阶段获取的运动技能可能需要主动"遗忘"部分塑形约束才能释放完整性能。

## 实验与结果

| 实验 | 关键结果 |
|------|----------|
| 模拟双机器人对拉 | 最佳连续回球 21 次 |
| 模拟击球精度 | 位置误差 ≤ 0.10 m，方向误差 ≤ 0.2 rad |
| 真实虚拟目标挥拍 | 71 次测试，平均误差 24.29 mm，球拍速度 5.32 m/s |
| 重复性测试 | 20 次独立挥拍，平均误差 23.21 mm，标准差 10.55 mm |
| 真实发球机击球 | 25 次中 24 次成功（1 次击中拍框边缘），回球落点平均 3.5 m |
| EKF 预测精度 | 冲击前 0.6 s 位置误差 < 100 mm，0.3 s 时 < 10 mm；时间误差约 20 ms |

真实世界测试中，拦截区域 98 cm × 42 cm，拦截高度约 1.5 m（机器人身高仅 1.28 m），出球速度最高 10 m/s，挥拍速度约 5.3 m/s，亚秒级时间窗口内完成感知-决策-执行闭环。消融研究显示移除 S1 直接训练发散，S1–S2 可部署但 S3 打破平台期。

## 边界与局限

论文未明确说明策略对超出训练分布的目标位置（如更远距离、更低高度）的泛化边界。当前测试局限于 Mocap 场地内机器发球，未涉及人类或机器人对手的真实对打；无预测变体未部署到硬件。Mocap 光学体积在 2.8 m 以上丢失羽毛球，限制了轨迹弧线高度；真实测试因安全和视野收窄了拦截区域。未建模额外延迟、未拟合执行器网络模型、未进行系统辨识，未使用力或触觉传感器。连续目标间距过大时策略可能失败，超出可用机动时间。

## 工程启示

复现时优先核对三处：一是 PD 增益配置——踝关节 roll 和肩关节 yaw 刚度仅 5 N·m/rad，远低于其他关节的 50–100，这是平衡维持与挥拍灵活性的关键折中；二是 S3 阶段移除的六个正则化项必须完整剔除，否则梯度干扰会显著降低击球精度；三是域随机化参数范围（摩擦 0.5–1.0、推挤间隔 5、基座质心偏移 ±0.08）对零样本迁移至关重要，缺一不可。

最易踩坑的是羽毛球动力学模型参数 L=3.4 的标定——EKF 预测精度高度依赖该值，部署前需用 Mocap 实测轨迹验证。训练数据过滤标准（拦截点 x∈[−0.8,0.8], y∈[−1,0.2], z∈[1.5,1.6]）直接决定策略的覆盖范围，若下游任务需要更大拦截区域，需重新生成轨迹语料库并调整 σ 调度。动作缩放 0.25 意味着策略输出需乘以该系数再送入 PD 控制器，直接使用原始输出会导致关节位置超调。

## 参考
- http://arxiv.org/abs/2511.11218v3

## Overview

This paper presents the first humanoid robot system that plays badminton in the real world, driven by a unified whole-body reinforcement learning policy. Through a three-stage curriculum that coordinates footwork and racket swings, it achieves zero-shot transfer to the custom-built Phybot C1 hardware. The core contribution lies in advancing agile, contact-rich dynamic interaction from simulation to real-world deployment, validating the feasibility of multi-stage RL in sub-second perception-action loops.

## What It Changes

The badminton task exposes a fundamental gap in existing robot learning paradigms: quasi-static interaction assumptions fail in real dynamic environments, while existing testbeds such as table tennis avoid the core challenge through 2D simplifications or lack of whole-body perturbations. What this paper truly changes is pushing the physical constraint that "footwork and striking must co-evolve" from theoretical discussion to engineering practice—lower-limb motion not only repositions the base but directly determines striking accuracy through support and momentum transfer, forcing the policy to simultaneously handle balance maintenance, rapid arm swings, and precise timing under a unified optimization objective, rather than layering independent controllers.

Another key shift is abandoning reference motions or expert demonstrations. The authors show that human-like backswing-swing-recovery action chains can emerge purely through reinforcement learning, along with adaptive behaviors such as short corrective steps, long strides with parallel backswings, and tiptoe extensions tailored to different time windows. This suggests that for time-critical, contact-rich whole-body tasks, curriculum-based reward shaping may be more scalable than imitation learning, as the latter struggles to cover the continuous space of action variants.

## Method Breakdown

### Three-Stage Curriculum Training
- **S1 Footwork Acquisition**: Optimizes only locomotion rewards, learning stable, forward-facing gaits among six sampled strike positions. The primary shaping term is `r_track = exp(−4·max(d−0.3, 0))`, where d is the horizontal distance between the base and the target, with success defined as approaching within 0.3 m. Converges in approximately 1k iterations.
- **S2 Precision-Guided Swinging**: Building on S1, introduces a sparse reward activated only at the predetermined strike time t*: `r_swing = exp(−e_ee_pos²/σ_p)·exp(−e_ee_ori/σ_r) + 0.3·r_v`, where `r_v = 1 − exp(−max(0, v_ee·n*)/σ_v)`. The key design is the multiplicative coupling of position and orientation errors, preventing the policy from over-optimizing a single metric. Precision scheduling tightens from σ_p=2.0, σ_r=8.0 to σ_p=0.1, σ_r=1.0, allowing full swings to emerge first and then refine. Smooth action chains appear after approximately 20k iterations.
- **S3 Task-Focused Refinement**: Removes S1's target-approach rewards and most gait regularizations (feet_orientation, feet_height, feet_distance, sym_contact_forces, sym_step, no_fly) to avoid gradient interference with strike objectives. Retains safety, energy, and hardware constraint terms, and enables domain randomization and observation noise. Strike rewards improve by 3–5%, while energy and torque costs decrease by approximately 20%.

### Network and Training Configuration
- Actor observes 87-dimensional base features plus short/long histories (5 frames + 20 frames), totaling 1,557 dimensions; Critic uses 98-dimensional privileged observations, additionally receiving the next two strike times, next target position and orientation, and remaining target count.
- Action space comprises joint position targets for all 21 DoFs, uniformly scaled by 0.25, tracked by a 500 Hz low-level PD controller.
- PPO, single RTX 4090, 4096 parallel environments, MLP 512–256–128, ELU activation, total training time 12.4 hours across three stages.

### Deployment Prediction
- EKF state vector [x, y, z, v_x, v_y, v_z]^T, using the same badminton dynamics model as training: `m·dv/dt = mg − m·‖v‖·v/L` (L=3.4). When the predicted height first falls below the strike plane, linear interpolation yields the strike point and time.
- The no-prediction variant only changes the observation space, replacing explicit targets with a sliding window of 6 badminton positions, randomizing aerodynamic parameters during training, with minimal performance degradation in simulation.

## Key Innovations

1. **First Real-World Humanoid Badminton System**: Previous work stopped at simulation or simplified hardware; this paper achieves zero-shot deployment on the 1.28 m, 30 kg, 21 DoF Phybot C1 without system identification or manual tuning, validating domain randomization for dynamic contact tasks.

2. **Coupled Position-Orientation Strike Reward**: Unlike [6], which handles position and orientation errors separately, this work multiplies them together, preventing the policy from over-optimizing either. This design choice shows superior precision convergence in ablations and better matches the physical essence of badminton's "brief window of aligned position, velocity, and orientation."

3. **Three-Stage Curriculum Eliminates Gradient Interference**: Removing lower-limb regularizations in S3 is a key innovation—the authors find that gait shaping terms conflict with strike objectives, and their removal continues to improve strike metrics while reducing energy consumption. This suggests that for whole-body dynamic tasks, motor skills acquired in early stages may require actively "forgetting" certain shaping constraints to unlock full performance.

## Experiments and Results

| Experiment | Key Results |
|------------|-------------|
| Simulated dual-robot rally | Best consecutive returns: 21 |
| Simulated strike precision | Position error ≤ 0.10 m, orientation error ≤ 0.2 rad |
| Real virtual-target swings | 71 tests, mean error 24.29 mm, racket speed 5.32 m/s |
| Repeatability test | 20 independent swings, mean error 23.21 mm, std 10.55 mm |
| Real ball-machine strikes | 24/25 successes (1 hit on racket frame edge), average return distance 3.5 m |
| EKF prediction accuracy | Position error < 100 mm at 0.6 s before impact, < 10 mm at 0.3 s; time error approximately 20 ms |

In real-world tests, the interception zone was 98 cm × 42 cm, interception height approximately 1.5 m (robot height only 1.28 m), outgoing ball speeds up to 10 m/s, swing speed approximately 5.3 m/s, completing the perception-decision-execution loop within a sub-second time window. Ablation studies show that removing S1 leads to training divergence, S1–S2 is deployable, and S3 breaks the plateau.

## Boundaries and Limitations

The paper does not explicitly specify the generalization boundary for target positions outside the training distribution (e.g., farther distances, lower heights). Current tests are limited to machine-served balls within a Mocap arena, without real rallies against human or robot opponents; the no-prediction variant was not deployed to hardware. The Mocap optical volume loses the badminton above 2.8 m, limiting trajectory arc heights; real tests narrowed the interception zone due to safety and field-of-view constraints. Additional latency was not modeled, actuator network models were not fitted, system identification was not performed, and no force or tactile sensors were used. The policy may fail when consecutive target spacing is too large, exceeding available maneuver time.

## Engineering Insights

When reproducing, prioritize checking three aspects: first, PD gain configuration—ankle roll and shoulder yaw stiffness is only 5 N·m/rad, far lower than the 50–100 of other joints, a critical trade-off between balance maintenance and swing flexibility; second, the six regularization terms removed in S3 must be fully excluded, otherwise gradient interference significantly reduces strike precision; third, domain randomization ranges (friction 0.5–1.0, push interval 5, base CoM offset ±0.08) are essential for zero-shot transfer and cannot be omitted.

The most common pitfall is calibrating the badminton dynamics parameter L=3.4—EKF prediction accuracy heavily depends on this value, and Mocap-measured trajectories must be used for validation before deployment. The training data filtering criteria (interception points x∈[−0.8,0.8], y∈[−1,0.2], z∈[1.5,1.6]) directly determine policy coverage; if downstream tasks require a larger interception zone, the trajectory corpus must be regenerated and the σ schedule adjusted. Action scaling of 0.25 means policy outputs must be multiplied by this factor before feeding the PD controller; using raw outputs directly causes joint position overshoot.

## 개요

본 논문은 실제 세계에서 배드민턴을 치는 최초의 휴머노이드 로봇 시스템을 제안한다. 통합 전신 강화학습 정책으로 구동되며, 3단계 커리큘럼 학습을 통해 스텝과 스윙을 조화시켜 자체 개발한 Phybot C1 하드웨어에 제로샷 전이를 달성한다. 핵심 기여는 민첩하고 접촉이 많은 동적 상호작용을 시뮬레이션에서 실제 배포로 확장한 것이며, 아초 단위의 인지-행동 루프에서 다단계 RL의 실현 가능성을 검증한 것이다.

## 무엇을 바꾸었는가

배드민턴 과제는 기존 로봇 학습 패러다임의 근본적 결함을 드러낸다: 준정적 상호작용 가정은 실제 동적 환경에서 성립하지 않으며, 탁구와 같은 기존 테스트 플랫폼은 2D 단순화 또는 전신 교란 부재로 핵심 난제를 회피한다. 본 논문이 실제로 바꾼 것은 "스텝과 타구가 함께 진화해야 한다"는 물리적 제약을 이론적 논의에서 공학적 실천으로 끌어올린 것이다—하지 운동은 베이스를 재배치할 뿐만 아니라 지지와 운동량 전달을 통해 타구 정확도를 직접 결정하므로, 정책은 계층적으로 독립 컨트롤러를 쌓는 대신 하나의 통합 최적화 목표 아래 균형 유지, 빠른 스윙, 정밀 타이밍을 동시에 처리해야 한다.

또 다른 핵심 전환은 참조 모션 또는 전문가 데모를 포기한 것이다. 저자들은 순수 강화학습만으로 인간형 백스윙-스윙-리커버리 동작 체인이 나타나고, 짧은 보정 스텝, 긴 크로스 스텝과 병행 백스윙, 발끝 뻗기 등 다양한 시간 창에 적응하는 행동이 자발적으로 생성됨을 증명했다. 이는 시간이 촉박하고 접촉이 많은 전신 과제에서 커리큘럼 기반 보상 형성이 모방 학습보다 확장성이 높을 수 있음을 시사한다. 후자는 연속적인 동작 변이 공간을 포괄하기 어렵기 때문이다.

## 방법 분해

### 3단계 커리큘럼 훈련
- **S1 스텝 획득**: locomotion 보상만 최적화하여 6개의 샘플링된 타구 위치 사이에서 안정적이고 전방을 향한 보행을 학습한다. 주요 형성 항 `r_track = exp(−4·max(d−0.3, 0))`, d는 베이스와 목표의 수평 거리로 0.3 m 이내 접근 시 성공으로 간주한다. 약 1k 반복으로 수렴.
- **S2 정밀도 유도 스윙**: S1 기반 위에 예정된 타구 시점 t*에서만 활성화되는 희소 보상 `r_swing = exp(−e_ee_pos²/σ_p)·exp(−e_ee_ori/σ_r) + 0.3·r_v`를 도입한다. 여기서 `r_v = 1 − exp(−max(0, v_ee·n*)/σ_v)`. 핵심 설계는 위치와 방향 오차를 곱으로 결합하여 정책이 단일 지표를 과도하게 최적화하지 않도록 하는 것이다. 정밀도 스케줄은 σ_p=2.0, σ_r=8.0에서 σ_p=0.1, σ_r=1.0으로 조여져 완전한 스윙이 먼저 나타난 후 정밀화된다. 약 20k 반복 후 부드러운 동작 체인이 나타난다.
- **S3 과제 집중 정제**: S1의 목표 접근 보상과 대부분의 보행 정규화 항(feet_orientation, feet_height, feet_distance, sym_contact_forces, sym_step, no_fly)을 제거하여 타구 목표와의 기울기 간섭을 피한다. 안전, 에너지, 하드웨어 제약 항은 유지하고 도메인 무작위화와 관측 노이즈를 활성화한다. 타구 보상은 3–5% 향상되고 에너지 및 토크 비용은 약 20% 감소한다.

### 네트워크 및 훈련 구성
- Actor는 87차원 기본 특징 + 단기/장기 히스토리(5프레임 + 20프레임)로 총 1,557차원을 관측한다; Critic은 98차원 특권 관측으로 두 개의 후속 타구 시간, 다음 목표 위치 및 방향, 남은 목표 수를 추가로 수신한다.
- 행동 공간은 21개 DoF 전체의 관절 위치 목표이며, 0.25로 균일 스케일링되어 500 Hz 저수준 PD 컨트롤러가 추적한다.
- PPO, 단일 RTX 4090, 4096 병렬 환경, MLP 512–256–128, ELU 활성화, 3단계 총 12.4시간 소요.

### 배포 예측
- EKF 상태 벡터 [x, y, z, v_x, v_y, v_z]^T, 훈련과 동일한 배드민턴 동역학 모델 `m·dv/dt = mg − m·‖v‖·v/L`(L=3.4) 사용. 예측 높이가 처음으로 타구 평면 아래로 내려갈 때 선형 보간으로 타구 지점과 시간을 얻는다.
- 예측 없는 변형은 관측 공간만 변경하며, 슬라이딩 윈도우 6프레임 배드민턴 위치로 명시적 목표를 대체하고, 훈련 시 공기역학 파라미터를 무작위화하여 시뮬레이션 성능 저하가 미미하다.

## 핵심 혁신

1. **최초의 실제 세계 휴머노이드 배드민턴 시스템**: 이전 연구는 시뮬레이션 또는 단순화된 하드웨어에 그쳤으나, 본 논문은 1.28 m, 30 kg, 21 DoF의 Phybot C1에서 시스템 식별이나 수동 튜닝 없이 제로샷 배포를 달성하여 동적 접촉 과제에 대한 도메인 무작위화의 유효성을 검증했다.

2. **위치-방향 결합 타구 보상**: [6]이 위치와 방향 오차를 분리 처리한 것과 달리, 본 논문은 둘을 곱으로 결합하여 정책이 하나만 과도하게 최적화하지 않도록 한다. 이 설계 선택은 절제 실험에서 더 우수한 정밀도 수렴 행동을 보였으며, "위치, 속도, 방향이 정렬되는 짧은 창"이라는 배드민턴의 물리적 본질에 더 부합한다.

3. **3단계 커리큘럼으로 기울기 간섭 제거**: S3에서 하지 정규화를 제거한 것이 핵심 혁신이다—저자들은 보행 형성 항과 타구 목표 사이에 기울기 충돌이 있음을 발견했고, 제거 후 타구 지표가 계속 개선되고 에너지 소비가 감소했다. 이는 전신 동적 과제에서 초기 단계에서 획득한 운동 기술이 완전한 성능을 발휘하려면 일부 형성 제약을 능동적으로 "잊어야" 할 수 있음을 시사한다.

## 실험 및 결과

| 실험 | 핵심 결과 |
|------|----------|
| 시뮬레이션 로봇 간 랠리 | 최고 연속 리턴 21회 |
| 시뮬레이션 타구 정밀도 | 위치 오차 ≤ 0.10 m, 방향 오차 ≤ 0.2 rad |
| 실제 가상 목표 스윙 | 71회 테스트, 평균 오차 24.29 mm, 라켓 속도 5.32 m/s |
| 반복성 테스트 | 20회 독립 스윙, 평균 오차 23.21 mm, 표준편차 10.55 mm |
| 실제 볼 머신 타구 | 25회 중 24회 성공(1회 프레임 가장자리 충돌), 리턴 착지 평균 3.5 m |
| EKF 예측 정밀도 | 충돌 전 0.6 s 위치 오차 < 100 mm, 0.3 s 시 < 10 mm; 시간 오차 약 20 ms |

실제 세계 테스트에서 인터셉트 영역은 98 cm × 42 cm, 인터셉트 높이는 약 1.5 m(로봇 키는 1.28 m에 불과), 출구 볼 속도 최대 10 m/s, 스윙 속도 약 5.3 m/s로 아초 단위 시간 창에서 인지-결정-실행 루프를 완성했다. 절제 연구는 S1 제거 시 직접 발산, S1–S2는 배포 가능하지만 S3가 플랫폼을 깨뜨린다는 것을 보여준다.

## 경계 및 한계

논문은 훈련 분포를 벗어난 목표 위치(예: 더 먼 거리, 더 낮은 높이)에 대한 정책의 일반화 경계를 명확히 밝히지 않았다. 현재 테스트는 Mocap 장소 내 기계 볼 발사에 국한되며 인간 또는 로봇 상대와의 실제 대결은 포함하지 않는다; 예측 없는 변형은 하드웨어에 배포되지 않았다. Mocap 광학 볼륨은 2.8 m 이상에서 배드민턴을 잃어 궤적 아치 높이를 제한한다; 실제 테스트는 안전과 시야 제약으로 인터셉트 영역을 좁혔다. 추가 지연을 모델링하지 않았고, 액추에이터 네트워크 모델을 피팅하지 않았으며, 시스템 식별을 수행하지 않았고, 힘 또는 촉각 센서를 사용하지 않았다. 연속 목표 간격이 너무 크면 정책이 실패할 수 있으며, 가용 기동 시간을 초과한다.

## 공학적 시사점

재현 시 세 가지를 우선 확인해야 한다: 첫째, PD 게인 구성—발목 롤과 어깨 요 스티프니스는 5 N·m/rad에 불과하여 다른 관절의 50–100보다 훨씬 낮다. 이는 균형 유지와 스윙 유연성의 핵심 절충이다; 둘째, S3 단계에서 제거된 여섯 개의 정규화 항을 완전히 제거해야 한다. 그렇지 않으면 기울기 간섭이 타구 정밀도를 크게 떨어뜨린다; 셋째, 도메인 무작위화 파라미터 범위(마찰 0.5–1.0, 밀치기 간격 5, 베이스 질량 중심 오프셋 ±0.08)는 제로샷 전이에 필수적이며 하나라도 빠지면 안 된다.

가장 함정이 많은 부분은 배드민턴 동역학 모델 파라미터 L=3.4의 보정이다—EKF 예측 정밀도는 이 값에 크게 의존하므로 배포 전에 Mocap 실측 궤적으로 검증해야 한다. 훈련 데이터 필터링 기준(인터셉트 지점 x∈[−0.8,0.8], y∈[−1,0.2], z∈[1.5,1.6])은 정책의 커버리지를 직접 결정하므로, 하위 과제가 더 큰 인터셉트 영역을 요구하면 궤적 코퍼스를 재생성하고 σ 스케줄을 조정해야 한다. 행동 스케일링 0.25는 정책 출력에 이 계수를 곱한 후 PD 컨트롤러에 전달해야 함을 의미하며, 원시 출력을 직접 사용하면 관절 위치 오버슈트가 발생한다.
