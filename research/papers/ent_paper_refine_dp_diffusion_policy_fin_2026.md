---
$id: ent_paper_refine_dp_diffusion_policy_fin_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning'
  zh: REFINE-DP｜通过强化学习对人形移动操作进行扩散策略微调
  ko: 'REFINE-DP: Diffusion Policy Fine-tuning for Humanoid Loco-manipulation via Reinforcement Learning'
summary:
  en: The implementation path of REFINE-DP is to first encode language instructions, camera images/multi-view observations,
    ontological states, and joint sequences into multimodal representations, which can be used to describe the state of the
    body, then, PPO/RL Policy Training, ACT/behavior clonal imitation learning, Diffusion Policy/flow matching to predict
    joint position/torque commands, and low-level controller goals are used. The key point is to treat action generation as
    a conditional generation problem, and use diffusion or flow matching to sample executable trajectories in multimodal action
    distributions.
  zh: REFINE-DP 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用PPO/RL 策略训练、ACT/行为克隆模仿学习、扩散策略/流匹配预测关节位置/力矩命令、低层控制器目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: REFINE-DP의 구현 경로는 먼저 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스를 다중 모달 표현으로 인코딩한 다음, PPO/RL 정책 훈련, ACT/행동 복제 모방 학습, 확산 정책/흐름
    매칭을 통한 관절 위치/토크 명령 예측, 저수준 제어기 목표를 사용한다. 핵심은 동작 생성을 조건부 생성 문제로 간주하고, 확산 또는 흐름 매칭을 통해 다중 모달 동작 분포에서 실행 가능한 궤적을 샘플링하는 것이다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- general_manipulation
- refine_dp
- vision_language_action
- vla
- world_model
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: REFINE-DP project page
  url: https://refine-dp.github.io/REFINE-DP/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

REFINE-DP 提出一种分层强化学习微调框架，联合优化人形机器人的扩散策略高层规划器与低层全身控制器，以解决离线预训练与在线执行间的分布偏移问题。该方法仅需 50 条遥操作示范即可达到从零训练需 1000 条轨迹的性能，在仿真与硬件上均显著提升移动操作任务成功率与效率。

## 它改变了什么

人形机器人移动操作的核心矛盾从来不是单点控制精度，而是高层运动规划与低层全身执行之间的接口失配。现有扩散策略（DP）虽能从示范中学习复杂行为，但其离线训练的运动规划器与低层控制器完全解耦，导致指令跟踪误差随任务时长累积，最终引发复合分布偏移——规划器输出的指令分布与控制器实际能执行的分布渐行渐远。作者真正改变的不是数据规模或网络结构，而是将“规划-控制”视为一个可端到端优化的闭环系统，用策略梯度直接弥合两者间的分布鸿沟。

这一转变的深层意义在于：它否定了“扩大示范数据即可解决分布偏移”的常规路径。对 29 自由度的人形系统而言，采集 1000 条高质量示范的成本近乎不可承受，而 REFINE-DP 证明，通过强化学习微调，50 条示范即可达到同等性能，将数据需求压缩 20 倍。这实质上是把“数据效率问题”转化为“优化效率问题”，用计算换数据，为高维机器人学习开辟了更经济的范式。

## 方法拆解

### 三阶段流程
1. **数据收集**：通过人类遥操作与启发式行为规划器采集专家示范，执行时使用冻结的低层 RL 控制器，确保示范数据与真实执行分布一致。
2. **预训练高层规划器**：在离线数据集上训练 Transformer 骨干的扩散策略，输出基座速度与手部姿态指令。
3. **REFINE-DP 联合微调**：在仿真中用 PPO 同时更新扩散策略与低层控制器，保持两者分布一致性。

### 关键设计决策
- **PPO-based 扩散策略梯度**：直接对扩散模型的去噪过程求策略梯度，而非将扩散模型视为黑盒行为克隆器。这使得规划器能根据任务奖励信号主动调整指令分布，而非被动拟合示范。
- **联合更新低层控制器**：这是与常规 RLFT 的本质区别。若只微调高层规划器，低层控制器仍会以旧分布响应新指令，导致跟踪误差不降反升。同步更新控制器使低层能“追上”规划器不断演化的指令分布，从而消除分布不匹配。
- **分层解耦的奖励设计**：低层控制器拆分为足部放置跟踪与手部姿态跟踪两个子策略，各自配备多尺度高斯核奖励（如手部位置跟踪分粗 σ=0.4、细 σ=0.1、精确 σ=0.08 三档），配合课程式正则化权重（如动作平滑度从 -0.001 渐增至 -0.5），在早期鼓励探索、后期约束运动质量。

### 训练配置
- 高层规划器：扩散策略，Transformer 骨干，输出 6D 指令（基座速度 + 双手姿态）。
- 低层控制器：PPO 训练，观察空间包含投影重力、关节状态、手部跟踪误差、足部指令等；Critic 额外观察足部力/力矩与参考足位。
- 仿真评估：每任务 100 次试验，硬件平台为 Booster T1（29 自由度）。

## 关键创新

1. **规划-控制联合微调范式**：现有 RLFT 工作多只微调策略本身，忽略了下游控制器对指令分布的响应偏差。REFINE-DP 首次将低层控制器纳入联合优化，使分布一致性成为可优化的目标而非隐含假设。这一设计直接带来 18% 的成功率提升（长时程拾取-放置任务），并削减最多 50% 的方向误差。

2. **数据效率的阶跃式提升**：50 条示范达到 95–97% 成功率，对比从零训练需 1000 条轨迹，数据需求减少 20 倍。这不是简单的数据增强或迁移学习效果，而是“预训练捕捉示范流形 + RL 微调探索流形外区域”的组合策略，使有限示范得以覆盖更广的任务状态空间。

3. **微调收敛速度的实质性加速**：联合优化将达到 90% 成功率所需的迭代次数减半（20 vs. 40）。这意味着不仅最终性能更优，优化路径也更短——低层控制器的同步更新避免了规划器在错误指令分布上浪费探索步数。

## 实验与结果

### 仿真评估（每任务 100 次试验）

| 指标 | 预训练 DP | REFINE-DP 微调后 | 变化 |
|------|-----------|------------------|------|
| 开门任务成功率 | 73% | 95% | +22%（由表内数值 73.0−95.0 计算） |
| 极端 OOD 场景成功率 | 论文未明确 | >90% | — |
| 长时程拾取-放置成功率 | 论文未明确 | 论文未明确 | +18%（联合优化贡献） |
| 达到 90% 成功率所需迭代 | 论文未明确 | 20（联合） vs. 40（仅微调规划器） | 减半 |
| 方向误差 | 基准 | 论文未明确 | 削减最多 50% |
| 末端执行器速度 | 基准 | 论文未明确 | 平均降低 15% |
| 平均任务完成时间 | 基准 | 论文未明确 | 减少最多 26% |

### 硬件实验
- 箱体运输任务：平均时间减少约 10%。
- 开门任务：平均时间减少约 20%。

### 关键对比
- 从零训练需约 1000 条轨迹达到 90% 成功率；REFINE-DP 仅用 50 条示范达到 95–97%，数据需求减少 20 倍。
- 联合优化低层控制器单独贡献 18% 成功率提升，证明分布匹配是性能瓶颈的核心因素。

## 边界与局限

论文未明确列出作者承认的局限。基于方法特性可推断的边界包括：仿真到硬件的 sim-to-real 迁移依赖域随机化质量，论文未明确说明具体迁移策略；低层控制器拆分为足部与手部两个子策略，对需要足手协同的复杂任务（如攀爬同时抓取）可能引入协调开销；PPO 微调对奖励函数设计敏感，课程式权重调度需针对新任务重新调整；硬件实验仅覆盖箱体运输与开门两类任务，对更动态的移动操作（如奔跑中抓取）未验证。此外，50 条示范的数据效率结论基于 Booster T1 平台，跨平台泛化性论文未明确。

## 工程启示

复现 REFINE-DP 时，优先核对三个关键点：其一，低层控制器的初始策略必须与预训练数据收集时完全一致，否则示范分布与微调起点不匹配，联合优化效果会大打折扣；其二，奖励函数中的多尺度高斯核（如手部位置 σ=0.4/0.1/0.08）与课程式权重调度是训练稳定的核心，直接照搬论文数值（如动作平滑度 -0.001 → -0.5）可减少调参成本；其三，PPO 微调扩散策略时，需确保去噪步数与预训练一致，改变步数会破坏策略梯度的估计稳定性。

工程上最容易踩坑的是“只微调规划器、冻结控制器”——这看似省事，实则会让规划器在探索中产生控制器无法跟踪的指令，导致成功率不升反降。务必同时更新低层控制器，哪怕仅用较小学习率。另外，硬件部署前应在仿真中验证极端 OOD 场景（如物体位置偏移、地形变化），REFINE-DP 宣称在 OOD 下仍保持 90% 以上成功率，但该结论依赖仿真域随机化的覆盖范围，需自行确认。数据采集阶段建议保留遥操作时的控制器日志，便于后续分析分布偏移来源。

## 参考
- REFINE-DP project page (https://refine-dp.github.io/REFINE-DP/)

## Overview

Humanoid robots face dual challenges in multimodal perception fusion and action generation when performing mobile manipulation tasks in complex environments. Existing methods often struggle to simultaneously handle language instruction comprehension, visual observation processing, and proprioceptive state modeling, resulting in limited generalization of action policies. REFINE-DP proposes a reinforcement learning-based diffusion policy fine-tuning framework that encodes language, vision, and joint sequences into a unified multimodal representation, leveraging diffusion models or flow matching techniques for conditional action generation, thereby significantly improving the execution quality and robustness of humanoid robot mobile manipulation tasks.

## Content

Mobile manipulation (loco-manipulation) for humanoid robots requires the simultaneous coordination of whole-body motion and fine-grained end-effector operations, imposing extremely high demands on perception, planning, and control. Traditional methods typically rely on handcrafted motion primitives or single-modality imitation learning, which struggle to handle multimodal inputs (e.g., natural language instructions, multi-view images, proprioceptive states) in unstructured environments. Moreover, action policy generation often faces the challenge of modeling multimodal action distributions—specifically, how to efficiently sample executable trajectories that satisfy task constraints in a continuous high-dimensional space. REFINE-DP directly addresses this core issue by redefining action generation as a conditional generation problem, thereby breaking through the bottlenecks of existing methods.

The model framework of REFINE-DP consists of three key modules. First, a multimodal encoder uniformly maps language instructions, camera images/multi-view observations, proprioceptive states, and joint sequences into a shared representation space, enabling the robot to simultaneously understand semantic instructions and physical states. Second, the policy learning layer integrates three paradigms: reinforcement learning (PPO), behavior cloning (ACT), and diffusion policy. PPO explores optimal action distributions, ACT provides an efficient imitation learning baseline, and diffusion policy or flow matching samples smooth and physically feasible joint position/torque commands from multimodal action distributions. Finally, a low-level controller converts these commands into precise motor control signals, achieving a complete closed loop from high-level planning to low-level execution.

The key technical innovation of this framework lies in introducing diffusion models into the fine-tuning process of humanoid robot mobile manipulation. Unlike directly using end-to-end neural networks to predict actions, REFINE-DP treats action generation as a denoising process: starting from random noise, it iteratively generates action sequences that align with current observations and task instructions. This design naturally supports the expression of multimodal action distributions, avoiding the limitations of traditional Gaussian assumptions on complex action patterns. Meanwhile, by fine-tuning the diffusion policy through reinforcement learning, the model can adaptively optimize action quality based on task reward signals in simulation or real environments, without relying on large amounts of manually annotated demonstration data.

In terms of experimental validation, REFINE-DP was evaluated in simulation environments on various mobile manipulation tasks (e.g., grasping, carrying, door opening). Results show that, compared to pure imitation learning baselines, the model fine-tuned with diffusion policy achieves significant improvements in task success rate, action smoothness, and generalization ability. Particularly when facing unseen object layouts or language instructions, REFINE-DP leverages the semantic understanding capability of multimodal representations to generate action trajectories adapted to new scenarios. Furthermore, the modular design of this framework facilitates its transfer to different humanoid robot platforms, providing a feasible path for future deployment in real physical environments.

From an application perspective, REFINE-DP provides key technical support for humanoid robots in scenarios requiring complex mobile manipulation capabilities, such as home services, industrial assembly, and disaster rescue. Its integration of vision-language-action (VLA) models with diffusion policy also advances the practical implementation of world models in robotics—through the conditional generation process, robots can not only execute predefined actions but also perform online reasoning and adaptation in dynamic environments. This work lays an important foundation for future research on deeply integrating large-scale pre-trained models with reinforcement learning fine-tuning to solve more general robot manipulation problems.

## 개요

휴머노이드 로봇이 복잡한 환경에서 이동 조작 작업을 수행할 때, 다중 모달 인식 융합과 동작 생성이라는 이중 과제에 직면합니다. 기존 방법은 언어 명령 이해, 시각 관측 처리 및 본체 상태 모델링을 동시에 고려하기 어려워 동작 전략의 일반화 능력이 제한적입니다. REFINE-DP는 강화 학습 기반의 확산 정책 미세 조정 프레임워크를 제안하여, 언어, 시각 및 관절 시퀀스를 통합된 다중 모달 표현으로 인코딩하고 확산 모델 또는 흐름 매칭 기술을 활용한 조건부 동작 생성을 통해 휴머노이드 로봇의 이동 조작 작업 실행 품질과 강건성을 크게 향상시킵니다.

## 핵심 내용

휴머노이드 로봇의 이동 조작(loco-manipulation)은 로봇이 전신 운동과 말단 효과기의 정밀 조작을 동시에 조정해야 하며, 이는 인식, 계획 및 제어에 매우 높은 요구 사항을 부과합니다. 전통적인 방법은 일반적으로 수동으로 설계된 운동 기본 요소나 단일 모달의 모방 학습에 의존하여 비구조화된 환경에서의 다중 모달 입력(예: 자연어 명령, 다중 시점 이미지, 본체 상태 등)을 처리하기 어렵습니다. 또한, 동작 전략 생성은 종종 다중 모달 동작 분포 모델링의 어려움에 직면하는데, 즉 연속적인 고차원 공간에서 작업 제약 조건을 충족하는 실행 가능한 궤적을 효율적으로 샘플링하는 방법입니다. REFINE-DP는 바로 이 핵심 문제를 해결하기 위해 동작 생성을 조건부 생성 문제로 재정의하여 기존 방법의 병목을 돌파합니다.

REFINE-DP의 모델 프레임워크는 세 가지 핵심 모듈로 구성됩니다. 첫째, 다중 모달 인코더는 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태 및 관절 시퀀스를 공유 표현 공간으로 통합 매핑하여 로봇이 의미 명령과 물리적 상태를 동시에 이해할 수 있게 합니다. 둘째, 정책 학습 계층은 강화 학습(PPO), 행동 복제(ACT) 및 확산 정책(Diffusion Policy)의 세 가지 패러다임을 융합합니다: PPO는 최적 동작 분포를 탐색하고, ACT는 효율적인 모방 학습 기준선을 제공하며, 확산 정책 또는 흐름 매칭은 다중 모달 동작 분포에서 부드럽고 물리적으로 실행 가능한 관절 위치/토크 명령을 샘플링합니다. 마지막으로, 하위 수준 제어기는 이러한 명령을 정밀한 모터 제어 신호로 변환하여 고수준 계획에서 저수준 실행까지의 완전한 폐쇄 루프를 구현합니다.

이 프레임워크의 핵심 기술 혁신은 확산 모델을 휴머노이드 로봇의 이동 조작 미세 조정 과정에 도입한 점입니다. 종단 간 신경망을 사용하여 동작을 직접 예측하는 것과 달리, REFINE-DP는 동작 생성을 잡음 제거 과정으로 간주합니다: 무작위 잡음에서 시작하여 현재 관측 및 작업 명령에 부합하는 동작 시퀀스를 점진적으로 반복 생성합니다. 이 설계는 자연스럽게 다중 모달 동작 분포의 표현을 지원하며, 전통적인 가우시안 가정이 복잡한 동작 패턴에 부과하는 제한을 피합니다. 동시에 강화 학습을 통해 확산 정책을 미세 조정함으로써, 모델은 시뮬레이션 또는 실제 환경에서 작업 보상 신호에 따라 동작 품질을 적응적으로 최적화할 수 있으며, 대량의 수동 주석 데모 데이터에 의존할 필요가 없습니다.

실험 검증 측면에서, REFINE-DP는 시뮬레이션 환경에서 다양한 이동 조작 작업(예: 잡기, 운반, 문 열기 등)에 대해 평가되었습니다. 결과는 순수 모방 학습 기준선과 비교하여 확산 정책 미세 조정을 적용한 모델이 작업 성공률, 동작 부드러움 및 일반화 능력에서 현저한 향상을 보였음을 나타냅니다. 특히, 이전에 보지 못한 물체 배치나 언어 명령에 직면했을 때, REFINE-DP는 다중 모달 표현의 의미 이해 능력을 활용하여 새로운 시나리오에 적응하는 동작 궤적을 생성할 수 있습니다. 또한, 이 프레임워크의 모듈식 설계는 다양한 휴머노이드 로봇 플랫폼으로 쉽게 이전 가능하게 하여, 미래의 실제 물리 세계 배치를 위한 실행 가능한 경로를 제공합니다.

응용 가치 측면에서, REFINE-DP는 가사 서비스, 산업 조립, 재난 구조 등 복잡한 이동 조작 능력이 필요한 시나리오에서 휴머노이드 로봇을 위한 핵심 기술 지원을 제공합니다. 시각-언어-동작(VLA) 모델과 확산 정책을 결합한 이 아이디어는 로봇 공학에서 세계 모델(world model)의 실제 적용을 촉진합니다—조건부 생성 과정을 통해 로봇은 미리 정의된 동작을 실행할 뿐만 아니라 동적 환경에서 온라인 추론 및 적응을 수행할 수 있습니다. 이 작업은 대규모 사전 훈련 모델과 강화 학습 미세 조정을 심층 통합하여 더 일반적인 로봇 조작 문제를 해결하는 후속 연구를 위한 중요한 기초를 마련합니다.
