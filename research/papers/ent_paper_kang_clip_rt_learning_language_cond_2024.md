---
$id: ent_paper_kang_clip_rt_learning_language_cond_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'CLIP-RT: Learning Language-Conditioned Robotic Policies from Natural Language Supervision'
  zh: CLIP-RT
  ko: 'CLIP-RT: Learning Language-Conditioned Robotic Policies from Natural Language Supervision'
summary:
  en: 'CLIP-RT: Learning Language-Conditioned Robotic Policies from Natural Language Supervision (CLIP-RT), is a 2024 large
    vision-language-action model for robotic manipulation, introduced by Seoul National University, and published at RSS25.'
  zh: CLIP-RT 是首尔国立大学于 2024 年提出的大型视觉-语言-动作模型，用于机器人操作任务。其核心贡献在于利用自然语言监督（如“将手臂向右移动”）让非专家用户也能收集机器人演示数据，并基于此训练语言条件化的策略。该模型在真实世界评估中平均成功率比
    OpenVLA（7B 参数）高出 24%，而参数量仅为后者的七分之一（1B），在 LIBERO 基准上达到 93.1% 的平均成功率与 163 Hz 的推理吞吐量。
  ko: 'CLIP-RT: Learning Language-Conditioned Robotic Policies from Natural Language Supervision (CLIP-RT), is a 2024 large
    vision-language-action model for robotic manipulation, introduced by Seoul National University, and published at RSS25.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- clip_rt
- large_vla_model
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2411.00508v4. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (903 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'CLIP-RT: Learning Language-Conditioned Robotic Policies from Natural Language Supervision (arXiv)'
  url: https://arxiv.org/abs/2411.00508
  date: '2024'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: CLIP-RT source
  url: https://doi.org/10.48550/arXiv.2411.00508
  date: '2024'
  accessed_at: '2026-07-01'
---
## 概述

CLIP-RT 提出用自然语言监督替代传统专家遥操作，让非专家通过语言指令教会机器人新技能。核心贡献是两套机制：基于 LLM 的语言遥操作数据收集框架，以及将 CLIP 判别式架构直接用于机器人动作预测的 CLIP-RT 模型，配合随机轨迹多样化（STD）将少量人类演示扩展一个数量级。在 19 个真实操作任务上，CLIP-RT 以 1B 参数在 Novel 任务上达到 40% 平均成功率，比 7B 的 OpenVLA 高 17 个百分点，且推理速度达 16Hz。

## 它改变了什么

现有 VLA 模型（如 OpenVLA、RT-2）的瓶颈不在模型容量，而在数据收集依赖专家操作机器人或遥操作，这从根本上限制了技能扩展的速度和规模。CLIP-RT 真正改变的是数据获取的入口：把“谁教机器人”从专家换成任何会说话的人，把“教什么”从低层动作向量换成自然语言描述。这不仅是接口替换，而是将策略学习从“动作回归”重构为“语言-上下文匹配”的判别式问题，从而绕开了生成式 VLA 必须自回归解码动作的低效路径。

另一个关键改变是 STD 算法让 10 个 episode 的人类演示能撑起一个可用的策略。它把人类轨迹视为近最优骨架，通过启发式采样中间动作来填充轨迹间的空白，使数据量从 2k 级扩展到 23k 级。这直接回应了“非专家无法大量收集数据”的现实约束——不是让非专家收集更多，而是让每一条专家轨迹发挥更大价值。

## 方法拆解

### 数据收集框架
- **语言遥操作**：人类在特定状态给出自然语言监督（如“move left a lot”），GPT-4 将其翻译为 7 维末端执行器命令（3 维 delta 位置、3 维 delta 旋转、1 维夹爪开合），机器人执行后重复此过程，获得指令-动作配对的演示轨迹。
- **STD 增强**：将专家轨迹按路点分段，对每段计算累积动作 Δa；从集合 S = {0.01, 0.05, 0.1}（对应 1、5、10 cm）中采样不超过剩余动作的增量 δa_n，使新中间动作之和等于 Δa；在夹爪接近路点时引入偏差 δa_dev（z 分量取剩余动作绝对值以保证安全），随后追加恢复动作 δa_rec = -δa_dev。训练时使用 δa 和 δa_rec，省略 δa_dev。

### CLIP-RT 模型
- **架构**：直接复用预训练 CLIP（ViT-H-14-378-quickgelu，1B 参数），图像编码器 f 和文本编码器 g 均保持原始结构，不新增参数。
- **对比模仿学习**：上下文嵌入 c_i = f(v_i) + g(ℓ_i)（当前场景图像 v_i 与语言指令 ℓ_i 之和），语言监督嵌入 z_i = g(u_i)；用带 sigmoid 的二元交叉熵损失优化 c_i 与 z_i 的相似度。动作以自然语言表示，无需指定 token 编码动作，模型为判别式而非生成式。
- **闭环控制**：每个时间步计算上下文与 K 个预定义语言动作类别的相似度，取概率最大者，通过查找表（58 种监督，含 8 种夹爪旋转）翻译为低层命令。单次前向传播即可预测，无需自回归解码。

### 两阶段训练
- **预训练**：在 Open X-Embodiment 数据集（2.4M 轨迹，70 个数据集）上训练，将 7 维动作映射到 50 种预定义自然语言监督，再用 LLM 改写为 899 种变体；训练约 18.1M 实例，4 个 H100 GPU 训练 1 天，batch size 128。
- **域内微调**：使用收集的域内数据（Common 任务 9841 实例，Novel 任务 11578 实例，总计 23k）微调。

## 关键创新

1. **语言作为唯一监督信号**：不同于 RT-H、OLAF 等先在其他接口收集数据再训练策略，CLIP-RT 全程用自然语言作为数据收集和策略学习的接口，彻底移除对遥操作设备的依赖。这是首个将语言监督贯穿数据收集到策略推理全链路的方案。

2. **判别式架构替代生成式**：现有 VLA 均采用自回归生成动作 token，CLIP-RT 将动作预测建模为上下文-语言监督的相似度匹配，单次前向传播即可输出动作。这不仅消除了推理延迟（16Hz vs OpenVLA 的 2Hz），还让模型天然具备对语言指令的语义理解能力，而非机械地映射 token。

3. **STD 的启发式轨迹多样化**：用简单的几何采样（1/5/10 cm 增量）在专家轨迹间生成中间动作，配合偏差-恢复机制模拟人类纠错行为。这一设计让 10 个 episode 的人类监督能扩展到 23k 训练实例，且无需额外人工标注，是数据效率的关键杠杆。

## 实验与结果

实验在 UR5 机械臂（6-DoF + Robotiq 两指夹爪）上进行，共 19 个任务（9 个 Common 与 Open X-Embodiment 对齐，10 个 Novel 为预训练中几乎未见）。每个任务人类收集 10 个 episode，STD 每 episode 增强 3 条轨迹。

| 指标 | CLIP-RT (1B) | OpenVLA (7B) |
|---|---|---|
| Novel 任务平均成功率 | 40% | 23% |
| Common 任务平均成功率 | 54% | 51% |
| 推理速率 (H100) | 16Hz | 2Hz |
| GPU 内存 | 7GB | 论文未明确 |

在简单任务（Point、Pull、Place、Pick、Push）上两者相当，OpenVLA 略优；在复杂任务（Flip、Knock Over、Slide）上 CLIP-RT 显著领先。消融实验显示：CLIP-RT-Action（动作映射到文本 token）普遍落后于 CLIP-RT，说明判别式匹配优于生成式映射；CLIP-RT-Passive（无 STD）在大多数任务上表现挣扎，验证了 STD 的关键作用。附录 E.2 中“Hang the cup”任务两者均取得 0% 成功率，暴露了细粒度对齐的共性困难。

## 边界与局限

论文未设独立局限性章节，但附录 E 揭示了多个边界：语言监督假设用户能提供适当中间抽象级别的指令，非专家可能难以校准指令粒度；模型对对象属性理解不可靠（如被指示指向蓝色骰子却指向红色）；STD 依赖启发式，无法覆盖从失败状态恢复的多样性（如物体从夹爪滑落）；2D 图像输入导致 3D 空间推断困难，单视角存在遮挡问题；模型不预测动作序列，缺乏时间上下文，无法处理需要理解动作历史的任务（如“Shake the water bottle”）；未在需要长期规划或层次推理的任务上测试，未在 ALFRED 数据集上评估。跨本体泛化虽声称可行但未提供实验证据。

## 工程启示

复现时优先核对三点：一是查找表的 58 种监督映射是否与你的机械臂运动学一致，特别是夹爪旋转维度（8D 命令在 OpenVLA 中需降为 7D）；二是 STD 的采样集合 S = {0.01, 0.05, 0.1} 是米制单位，若换用不同尺寸的工作空间需重新标定；三是 LLM 提示词中的坐标系定义（x 为深度、y 为水平、z 为垂直，旋转范围 ±90 度）必须与机器人控制接口严格对齐，否则语言到动作的翻译会产生系统性偏差。

最容易踩坑的是 STD 的偏差-恢复机制：训练时省略 δa_dev 而只用 δa 和 δa_rec，若实现时误将偏差动作也纳入训练，会导致策略学习到错误的纠错行为。另外，预训练数据中 899 种语言监督由 50 种基础监督经 LLM 改写而来，若替换 LLM 需重新生成并验证语义覆盖度。推理时 CLIP-RT 需 7GB GPU 内存，RTX 3090 上可达 8Hz，若需更高频率可考虑量化或编译，但论文未提供相关数据。

## 参考
- http://arxiv.org/abs/2411.00508v4

## 개요
CLIP-RT는 로봇 학습에서 데이터 수집의 진입 장벽이 높은 문제를 해결하고자 자연어를 통해 직관적인 상호작용 인터페이스를 제공합니다. 연구는 두 가지 핵심 측면을 포함합니다: 첫째, 자연어 감독 기반의 데이터 수집 프레임워크를 설계하여 비전문가 사용자가 시연 데이터를 쉽게 수집하고 다양성을 강화할 수 있게 합니다; 둘째, 사전 훈련된 CLIP을适配하는 CLIP-RT 모델을 제안하며, 대조적 모방 학습(contrastive imitation learning)을 활용하여 언어 기반 운동 원시 요소(language-driven motion primitives)를 예측합니다. 모델은 Open X-Embodiment 데이터셋에서 사전 훈련된 후, 프레임워크로 수집된 도메인 내 데이터로 미세 조정됩니다. 실험 결과, CLIP-RT는 실제 환경에서 새로운 조작 기술을 학습하는 능력이 뛰어나며, 소표본 일반화 및 대규모 사전 훈련 모델이나 인간과의 협업 시나리오에서도 우수한 성능을 보입니다.

## 핵심 내용
### 방법
- **데이터 수집 프레임워크**: 자연어 감독 기반의 절차를 설계하여 비전문가 사용자가 간단한 지시(예: "팔을 오른쪽으로 움직여")를 통해 로봇 시연을 수집할 수 있게 합니다. 이 프레임워크는 또한 데이터 증강 기술을 통해 시연 샘플의 다양성을 확장합니다.
- **모델 아키텍처**: CLIP-RT는 사전 훈련된 CLIP 모델을 기반으로 하며, 대조적 모방 학습(contrastive imitation learning)을 통해 언어 조건화된 운동 원시 요소(language-based motion primitives)를 예측합니다. 모델은 시각 입력과 언어 지시를 정렬하여 직접 동작 시퀀스를 출력합니다.

### 실험 설정
- **훈련 데이터**: Open X-Embodiment 데이터셋에서 사전 훈련된 후, 프레임워크로 수집된 도메인 내 데이터로 미세 조정됩니다.
- **평가 환경**: 실제 세계 조작 작업(소표본 일반화 및 협업 시나리오 포함) 및 시뮬레이션 환경(LIBERO 벤치마크).

### 주요 수치 및 결과
- **실제 세계 평가**: CLIP-RT의 평균 성공률은 OpenVLA(7B 파라미터)보다 24% 높으며, 파라미터 수는 1B(7배 더 적음)에 불과합니다.
- **시뮬레이션 환경**: LIBERO 벤치마크에서 평균 성공률 93.1%를 달성하고, 추론 처리량은 163 Hz입니다.
- **일반화 및 협업**: 소표본 설정에서 견고한 성능을 보이며, 대규모 사전 훈련 모델이나 인간과 협업하여 복잡한 작업을 완료할 수 있습니다.

### 결론
CLIP-RT는 자연어 감독을 통해 로봇 데이터 수집의 진입 장벽을 낮추고, 경량 모델로 효율적인 언어 조건화 정책 학습을 구현하여 로봇 조작 분야에서 비전문가 사용자의 참여와 모델 배포에 실현 가능한 솔루션을 제공합니다.
