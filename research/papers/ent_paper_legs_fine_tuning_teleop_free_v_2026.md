---
$id: ent_paper_legs_fine_tuning_teleop_free_v_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'LEGS: Fine-Tuning Teleop-Free VLAs for Humanoid Loco-manipulation in an Embodied Gaussian Splatting World'
  zh: LEGS｜微调无TeleopVLA，用于体现高斯泼溅世界中的人形移动操作
  ko: 'LEGS: Fine-Tuning Teleop-Free VLAs for Humanoid Loco-manipulation in an Embodied Gaussian Splatting World'
summary:
  en: The implementation path of LEGS is to first encode language instructions, camera images/multi-view observations, ontology
    states and joint sequences into multimodal representations, and then use ACT/behavior cloning imitation learning, VLA
    multimodal action model, world model/video prediction to predict low-level controller targets, and terrain/scene representations.
    The key point is to let the video/world model provide predictable physical priors, and then the action head turns the
    semantic goal into continuous control.
  zh: LEGS 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用ACT/行为克隆模仿学习、VLA 多模态动作模型、世界模型/视频预测预测低层控制器目标、地形/场景表征。关键点是让视频/世界模型提供可预测的物理先验，再由动作头把语义目标变成连续控制。
  ko: LEGS의 구현 경로는 먼저 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스를 다중 모달 표현으로 인코딩한 다음, ACT/행동 클로닝 모방 학습, VLA
    다중 모달 동작 모델, 세계 모델/비디오 예측을 통해 저수준 제어기 목표를 예측하고, 지형/장면 표현을 활용한다. 핵심은 비디오/세계 모델이 예측 가능한 물리적 사전 정보를 제공하도록
    하고, 이후 동작 헤드가 의미론적 목표를 연속 제어로 변환하는 것이다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- general_manipulation
- legs
- vision_language_action
- vla
- world_model
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry
    metadata. [2026-08-05] body rewritten as full-text six-section deep read (.staging/deep_read batch2,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
    | WP3 marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek).'
sources:
- id: src_001
  type: website
  title: LEGS project page
  url: https://legsvla.github.io/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

LEGS 是一个无需遥操作即可生成人形机器人 loco-manipulation 训练数据的混合模拟器，由作者团队提出。其核心贡献在于将动态网格前景（机器人、物体）合成在从手持视频重建的 3D 高斯泼溅（3DGS）背景之上，并通过两阶段颜色校准消除合成与真实相机之间的视觉差距，使 VLA 策略在真实 Unitree G1 上取得优于遥操作数据的成功率。

## 它改变了什么

它真正改变的是人形机器人 VLA 策略训练数据获取的范式。此前，训练 loco-manipulation 策略要么依赖昂贵的遥操作演示（受限于操作员劳动和硬件疲劳，成本超线性增长），要么依赖网格渲染的模拟器（存在材质着色不匹配、简化光照和混叠问题，导致视觉编码器产生偏差，sim-to-real 迁移失败）。LEGS 将背景渲染从网格切换到 3DGS，使合成图像在纹理、光照和相机分布上更接近真实传感器观测，从而让程序化生成的数据在真实部署中首次达到甚至超过遥操作数据的有效性。

更重要的是，它改变了“多外观条件覆盖”的成本结构。在遥操作中，每增加一个外观条件（如换一张桌子、换一个物体）都需要重新收集演示；而 LEGS 将运动生成与渲染解耦，记录的运动轨迹可以在新背景下以 GPU 成本重新渲染，使覆盖多种外观变得几乎免费。这直接回应了部署时外观变化导致策略失效的痛点。

## 方法拆解

LEGS 的框架围绕两个解耦组织：物理与渲染解耦、静态背景与动态前景解耦。

### 物理与渲染解耦
- MuJoCo 后端以 500 Hz 解析动力学，运行 SONIC 低层全身控制器，接受 18 维命令（每臂 6 维腕部 SE(3) 姿态 + 连续抓取标量，拼接 4 维基座命令 v_x, v_y, ω_z, h）。
- 独立渲染前端产生图像观测，物理计算与像素生成完全异步。

### 静态背景与动态前景解耦
- 背景是固定的 3DGS 场，仅用于外观渲染，来自约 1–2 分钟手持视频的 COLMAP 姿态估计 + 3DGS 优化（30K 迭代，单工作站 GPU，约 10–15 分钟）。
- 前景是受物理驱动的光栅化网格（机器人 URDF、SAM3D 重建的物体），每帧通过深度测试合成图层。

### 两阶段颜色校准
这是 LEGS 消除视觉差距的关键设计：

1. **Stage 1（每网格对角缩放）**：在 iPhone 空间，对每个网格的线性 RGB 顶点颜色乘以对角缩放因子 s^(c) = clip(μ_T^(c) / μ_S^(c), 0.5, 3.0)，其中 μ_S 是 PBR 渲染内部视图的均值，μ_T 是 SAM2 掩码内 iPhone 像素的均值。选择对角而非完整 3×3 矩阵，因为完整协方差拟合需要更多条目匹配 iPhone 区域方差，会导致裁剪和色调偏移。

2. **Stage 2（全局颜色校正矩阵）**：用 24 色块 ColorChecker 在受控光照下拍摄两台相机，通过最小二乘求解 M⋆ = (Q^T Q)^(−1) Q^T P，将 3DGS 背景和网格前景统一映射到部署相机（RealSense）的色彩空间。在合成层面应用 M⋆ 而非逐物体应用，保持空间一致性，无边界伪影。

### 程序化情节生成
- 利用模拟器特权信息（机器人、物体姿态）程序化生成情节，组合参数化运动原语（Walk、Pick、Place），参数是场景级（目标姿态、物体身份、手臂选择）而非关节级。
- 每个运动以验证检查结束，仅保存验证成功的情节。
- 记录的 18 维命令流独立于渲染输入，因此数据集可在交换背景和网格下仅以 GPU 成本重新渲染。

## 关键创新

1. **3DGS 背景替代网格背景**：这是最核心的创新。传统网格合成管线（如 SAM3D）即使重建质量高，仍因材质着色、光照简化和混叠问题产生视觉偏差。LEGS 将背景替换为 3DGS 场，使合成图像在纹理和光照分布上更接近真实相机观测，直接减少了视觉编码器的分布偏移。

2. **两阶段颜色校准的工程化设计**：Stage 1 针对 SAM3D 反照率回归错误（如盒子从洗白灰色恢复为黑色），Stage 2 将 3DGS 背景和网格前景统一映射到部署相机色彩空间。选择对角缩放而非完整矩阵、在合成层面应用全局矩阵而非逐物体，这些决策都基于对渲染管线和相机 ISP 的深入理解，而非简单的端到端学习。

3. **运动生成与渲染解耦带来的成本重构**：程序化生成器产生的运动轨迹独立于渲染输入，使多外观条件扩展仅需 GPU 重新渲染（约 0.1 小时/条件），而遥操作需要超过 1.5 操作员小时。这使覆盖多种外观在仿真中变得实际可行，直接解决了部署时外观变化的鲁棒性问题。

## 实验与结果

实验在 Unitree G1 上进行三个难度递增的拾取-放置任务，对比 Teleop (50)、SAM3D (200)、LEGS (50)、LEGS (200) 四种数据条件，微调三个 VLA 骨干（ψ0、π0.5、GR00T N1.6），每条件 10 次试验。

**表 2 端任务成功率（成功次数/10 次试验）**：

| 任务 | 骨干 | Teleop (50) | SAM3D (200) | LEGS (50) | LEGS (200) |
|------|------|-------------|-------------|-----------|------------|
| Task 1 | ψ0 | 8 | 7 | 8 | **10** |
| Task 1 | π0.5 | 2 | 1 | 5 | **6** |
| Task 1 | GR00T N1.6 | 4 | 6 | 9 | **9** |
| Task 2 | ψ0 | 6 | 4 | 8 | **9** |
| Task 2 | π0.5 | 0 | 3 | 4 | **5** |
| Task 2 | GR00T N1.6 | 2 | 5 | 7 | **8** |
| Task 3 | ψ0 | 0 | 1 | 3 | **5** |
| Task 3 | π0.5 | 0 | 0 | 1 | **2** |
| Task 3 | GR00T N1.6 | 0 | 3 | 5 | **6** |

关键结果：
- LEGS (200) 在所有九项实验中最佳或并列最佳。
- Task 3 上 Teleop (50) 在所有骨干上崩溃至 0/10，而 LEGS (200) 达到 2–6/10。
- 预算匹配的 LEGS (50) 在每项实验上匹配或超过 Teleop (50)。
- SAM3D (200) 平均 33% 成功率，LEGS (200) 为 67%，平均下降 33 个百分点，大致减半。

**外观随机化（图 5）**：
- 无重新渲染时，三个 50 情节基线在物体和物体+场景条件下崩溃至 0–1/10。
- 重新渲染后，LEGS-aug (200) 在物体+场景移位上达到 Task 1–3 的 100%、80%、40%，而 SAM3D-aug 为 60%、50%、20%。
- 图 5(b) 显示 LEGS-aug (50) 在物体+场景上超过四倍大的 LEGS (200)（50%、40%、30% vs 0%、0%、10%），说明增强多样性比单纯增加数据量更有效。

**成本（表 3）**：
- 初始 50 集 Task 3：遥操作 1.5 操作员小时，LEGS 0.5 GPU 小时（零操作员）。
- 每额外外观条件：遥操作 >1.5 操作员小时，LEGS 约 0.1 GPU 小时，SAM3D 约 0.2 GPU 小时。

## 边界与局限

- 颜色校准依赖固定曝光和固定白平衡（RealSense 曝光 100 μs，白平衡 3000 K），训练策略对原始 3DGS 扫描捕获分布之外的照明变化敏感。
- 适应新场景仍需 1–2 分钟手持视频，比遥操作便宜但非免费。
- 3DGS 重建在高度反射或透明表面上退化，且假设静态背景；有移动人类或其他动态内容的场景需要动态 3DGS 方法，作者未实现。
- 程序化生成器产生可行运动但不优化动态效率，不同于基于轨迹优化或 MPC 的方法。
- 外观随机化套件仅变化相似大小和几何的小型可抓取物体，未测试转移到大幅不同物体类别。
- 仅在单一机器人平台（Unitree G1）、相机（Intel RealSense D435）和全身控制器（SONIC）上评估，未测试跨人形硬件的泛化。
- 论文未明确提及推理频率、遥操作与 LEGS 在曝光/白平衡设置差异对逐像素比较的影响。

## 工程启示

- **先核对颜色校准的两阶段实现**：Stage 1 的对角缩放必须在线性 RGB 中针对 SAM2 掩码区域拟合，Stage 2 的全局矩阵必须用 24 色块 ColorChecker 在受控光照下拍摄。最容易踩坑的是在 sRGB 空间直接做缩放导致双重编码，以及将背景像素混入掩码区域拟合导致色调偏移。
- **运动生成与渲染解耦是复现的核心架构决策**：确保记录的 18 维命令流独立于渲染输入，这样多外观扩展才只需 GPU 重新渲染。如果物理后端与渲染前端耦合，将无法获得 LEGS 的成本优势。
- **外观随机化的收益大于数据量**：图 5(b) 显示 50 集增强数据在物体+场景移位上超过 200 集默认数据，说明在预算有限时，优先覆盖外观多样性而非增加同一分布内的数据量。
- **SONIC 控制器是部署的关键依赖**：相同的 SONIC 二进制在模拟和真实机器人上运行，部署无需更改策略。复现时需确保控制器接口（18 维命令）和 DDS 主题名与部署目标完全一致。
- **注意相机 ISP 设置**：部署时需固定 RealSense 的曝光和自动白平衡（100 μs、3000 K），否则颜色校准矩阵失效。遥操作启用自动曝光/自动白平衡，与 LEGS 的固定 ISP 不同，不能逐像素直接比较。

## 参考

- LEGS project page (https://legsvla.github.io/)

## Overview

LEGS is a hybrid simulator proposed by the authors that generates training data for humanoid robot loco-manipulation without teleoperation. Its core contribution lies in compositing dynamically rasterized foregrounds (robot, objects) onto 3D Gaussian Splatting (3DGS) backgrounds reconstructed from handheld videos, and eliminating the visual gap between synthetic and real cameras through two-stage color calibration, enabling VLA policies to achieve higher success rates on a real Unitree G1 than teleoperation data.

## What It Changes

What it truly changes is the paradigm for acquiring training data for humanoid robot VLA policies. Previously, training loco-manipulation policies either relied on expensive teleoperation demonstrations (limited by operator labor and hardware fatigue, with superlinear cost growth) or mesh-rendered simulators (suffering from material shading mismatches, simplified lighting, and aliasing issues, causing biases in visual encoders and sim-to-real transfer failures). LEGS switches background rendering from meshes to 3DGS, making synthetic images closer to real sensor observations in texture, lighting, and camera distribution, thereby allowing procedurally generated data to match or even exceed the effectiveness of teleoperation data in real-world deployment for the first time.

More importantly, it changes the cost structure of "multi-appearance condition coverage." In teleoperation, each additional appearance condition (e.g., changing a table or an object) requires recollecting demonstrations; LEGS decouples motion generation from rendering, so recorded motion trajectories can be re-rendered under new backgrounds at GPU cost, making coverage of diverse appearances nearly free. This directly addresses the pain point of policy failure due to appearance changes at deployment.

## Method Breakdown

LEGS's framework is organized around two decouplings: physics-rendering decoupling and static background-dynamic foreground decoupling.

### Physics-Rendering Decoupling
- The MuJoCo backend resolves dynamics at 500 Hz, running the SONIC low-level whole-body controller, which accepts 18-dimensional commands (6-DoF wrist SE(3) pose per arm + continuous grasp scalar, concatenated with 4-DoF base commands v_x, v_y, ω_z, h).
- An independent rendering frontend produces image observations, with physics computation and pixel generation fully asynchronous.

### Static Background-Dynamic Foreground Decoupling
- The background is a fixed 3DGS field used solely for appearance rendering, derived from COLMAP pose estimation + 3DGS optimization (30K iterations, single workstation GPU, approximately 10–15 minutes) from about 1–2 minutes of handheld video.
- The foreground is physics-driven rasterized meshes (robot URDF, SAM3D-reconstructed objects), composited as layers per frame via depth testing.

### Two-Stage Color Calibration
This is the key design in LEGS for eliminating visual gaps:

1. **Stage 1 (Per-Mesh Diagonal Scaling)**: In iPhone space, linear RGB vertex colors of each mesh are multiplied by a diagonal scaling factor s^(c) = clip(μ_T^(c) / μ_S^(c), 0.5, 3.0), where μ_S is the mean of the PBR-rendered internal view and μ_T is the mean of iPhone pixels within the SAM2 mask. A diagonal rather than full 3×3 matrix is chosen because full covariance fitting requires more entries to match iPhone region variance, leading to clipping and hue shifts.

2. **Stage 2 (Global Color Correction Matrix)**: A 24-patch ColorChecker is photographed under controlled lighting with both cameras, and the least-squares solution M⋆ = (Q^T Q)^(−1) Q^T P maps both the 3DGS background and mesh foreground uniformly to the deployment camera's (RealSense) color space. Applying M⋆ at the composite level rather than per-object maintains spatial consistency without boundary artifacts.

### Procedural Episode Generation
- Leveraging simulator privileged information (robot, object poses), episodes are procedurally generated by combining parameterized motion primitives (Walk, Pick, Place), with parameters at the scene level (target pose, object identity, arm selection) rather than the joint level.
- Each motion ends with a verification check, and only episodes that pass verification are saved.
- The recorded 18-dimensional command stream is independent of rendering inputs, so datasets can be re-rendered under swapped backgrounds and meshes at GPU cost only.

## Key Innovations

1. **3DGS Background Replacing Mesh Background**: This is the most core innovation. Traditional mesh compositing pipelines (e.g., SAM3D), even with high reconstruction quality, still produce visual biases due to material shading, simplified lighting, and aliasing. LEGS replaces the background with a 3DGS field, making synthetic images closer to real camera observations in texture and lighting distribution, directly reducing distribution shift in visual encoders.

2. **Engineering Design of Two-Stage Color Calibration**: Stage 1 targets SAM3D albedo regression errors (e.g., boxes recovering from washed-out gray to black), and Stage 2 uniformly maps the 3DGS background and mesh foreground to the deployment camera's color space. Choices such as diagonal scaling over full matrices and applying a global matrix at the composite level rather than per-object stem from deep understanding of the rendering pipeline and camera ISP, not simple end-to-end learning.

3. **Cost Restructuring via Motion-Rendering Decoupling**: Motion trajectories from the procedural generator are independent of rendering inputs, making multi-appearance expansion require only GPU re-rendering (approximately 0.1 hours per condition), whereas teleoperation requires over 1.5 operator hours. This makes covering diverse appearances practically feasible in simulation, directly addressing robustness to appearance changes at deployment.

## Experiments and Results

Experiments were conducted on the Unitree G1 across three pick-and-place tasks of increasing difficulty, comparing four data conditions—Teleop (50), SAM3D (200), LEGS (50), LEGS (200)—and fine-tuning three VLA backbones (ψ0, π0.5, GR00T N1.6), with 10 trials per condition.

**Table 2 End-Task Success Rates (successes/10 trials)**:

| Task | Backbone | Teleop (50) | SAM3D (200) | LEGS (50) | LEGS (200) |
|------|----------|-------------|-------------|-----------|------------|
| Task 1 | ψ0 | 8 | 7 | 8 | **10** |
| Task 1 | π0.5 | 2 | 1 | 5 | **6** |
| Task 1 | GR00T N1.6 | 4 | 6 | 9 | **9** |
| Task 2 | ψ0 | 6 | 4 | 8 | **9** |
| Task 2 | π0.5 | 0 | 3 | 4 | **5** |
| Task 2 | GR00T N1.6 | 2 | 5 | 7 | **8** |
| Task 3 | ψ0 | 0 | 1 | 3 | **5** |
| Task 3 | π0.5 | 0 | 0 | 1 | **2** |
| Task 3 | GR00T N1.6 | 0 | 3 | 5 | **6** |

Key results:
- LEGS (200) is best or tied for best across all nine experiments.
- On Task 3, Teleop (50) collapses to 0/10 across all backbones, while LEGS (200) reaches 2–6/10.
- Budget-matched LEGS (50) matches or exceeds Teleop (50) in every experiment.
- SAM3D (200) averages 33% success, LEGS (200) averages 67%, a mean drop of 33 percentage points, roughly halving performance.

**Appearance Randomization (Figure 5)**:
- Without re-rendering, the three 50-episode baselines collapse to 0–1/10 under object and object+scene conditions.
- After re-rendering, LEGS-aug (200) achieves 100%, 80%, 40% on Tasks 1–3 under object+scene shifts, while SAM3D-aug achieves 60%, 50%, 20%.
- Figure 5(b) shows LEGS-aug (50) surpasses the four-times-larger LEGS (200) under object+scene shifts (50%, 40%, 30% vs 0%, 0%, 10%), indicating that augmentation diversity is more effective than simply increasing data volume.

**Cost (Table 3)**:
- Initial 50 episodes of Task 3: teleoperation 1.5 operator hours, LEGS 0.5 GPU hours (zero operator).
- Per additional appearance condition: teleoperation >1.5 operator hours, LEGS approximately 0.1 GPU hours, SAM3D approximately 0.2 GPU hours.

## Boundaries and Limitations

- Color calibration relies on fixed exposure and fixed white balance (RealSense exposure 100 μs, white balance 3000 K), and trained policies are sensitive to lighting variations beyond the distribution captured by the original 3DGS scans.
- Adapting to new scenes still requires 1–2 minutes of handheld video, cheaper than teleoperation but not free.
- 3DGS reconstruction degrades on highly reflective or transparent surfaces and assumes a static background; scenes with moving humans or other dynamic content require dynamic 3DGS methods, which the authors did not implement.
- The procedural generator produces feasible motions but does not optimize dynamic efficiency, differing from trajectory optimization or MPC-based methods.
- The appearance randomization suite only varies small graspable objects of similar size and geometry, without testing transfer to substantially different object categories.
- Evaluation is limited to a single robot platform (Unitree G1), camera (Intel RealSense D435), and whole-body controller (SONIC), without testing generalization across humanoid hardware.
- The paper does not explicitly mention inference frequency or the impact of exposure/white balance differences between teleoperation and LEGS on pixel-wise comparisons.

## Engineering Insights

- **Verify the two-stage color calibration implementation first**: Stage 1's diagonal scaling must be fitted in linear RGB against SAM2 mask regions, and Stage 2's global matrix must be captured with a 24-patch ColorChecker under controlled lighting. The most common pitfalls are performing scaling directly in sRGB space, causing double encoding, and mixing background pixels into mask regions during fitting, causing hue shifts.
- **Motion-rendering decoupling is the core architectural decision for reproduction**: Ensure the recorded 18-dimensional command stream is independent of rendering inputs, so multi-appearance expansion only requires GPU re-rendering. If the physics backend is coupled with the rendering frontend, LEGS's cost advantage will be lost.
- **Appearance randomization yields greater benefits than data volume**: Figure 5(b) shows that 50 augmented episodes outperform 200 default episodes under object+scene shifts, indicating that with limited budgets, prioritizing appearance diversity over increasing data volume within the same distribution is more effective.
- **The SONIC controller is a key dependency for deployment**: The same SONIC binary runs on both simulation and the real robot, requiring no policy changes at deployment. When reproducing, ensure the controller interface (18-dimensional commands) and DDS topic names exactly match the deployment target.
- **Pay attention to camera ISP settings**: At deployment, fix the RealSense exposure and auto white balance (100 μs, 3000 K); otherwise, the color calibration matrix becomes invalid. Teleoperation enables auto exposure/auto white balance, differing from LEGS's fixed ISP, so pixel-wise comparisons cannot be made directly.

## 개요

LEGS는 원격 조작 없이 휴머노이드 로봇의 loco-manipulation 훈련 데이터를 생성하는 하이브리드 시뮬레이터로, 저자 팀이 제안했습니다. 핵심 기여는 손으로 촬영한 비디오에서 재구성된 3D 가우시안 스플래팅(3DGS) 배경 위에 동적 메시 전경(로봇, 물체)을 합성하고, 2단계 색상 보정을 통해 합성과 실제 카메라 간의 시각적 격차를 제거하여 VLA 정책이 실제 Unitree G1에서 원격 조작 데이터보다 우수한 성공률을 달성하게 한 것입니다.

## 무엇을 바꾸었는가

진정으로 바꾼 것은 휴머노이드 로봇 VLA 정책 훈련 데이터 획득의 패러다임입니다. 이전에는 loco-manipulation 정책을 훈련시키기 위해 값비싼 원격 조작 데모(운영자 노동과 하드웨어 피로에 제한되어 비용이 초선형적으로 증가)에 의존하거나, 메시 렌더링 시뮬레이터(재질 음영 불일치, 단순화된 조명, 앨리어싱 문제로 인해 시각 인코더에 편향이 생기고 sim-to-real 전이가 실패)에 의존해야 했습니다. LEGS는 배경 렌더링을 메시에서 3DGS로 전환하여 합성 이미지가 텍스처, 조명, 카메라 분포에서 실제 센서 관측에 더 가깝게 만들어, 절차적으로 생성된 데이터가 실제 배포에서 처음으로 원격 조작 데이터의 효율성에 도달하거나 이를 초과하게 했습니다.

더 중요하게는, "다중 외관 조건 커버리지"의 비용 구조를 바꾸었습니다. 원격 조작에서는 외관 조건(예: 테이블 교체, 물체 교체)을 추가할 때마다 데모를 다시 수집해야 합니다. 반면 LEGS는 모션 생성과 렌더링을 분리하여 기록된 모션 궤적을 새 배경에서 GPU 비용으로 다시 렌더링할 수 있어, 다양한 외관을 커버하는 것이 거의 무료가 됩니다. 이는 배포 시 외관 변화로 인한 정책 실패라는痛点을 직접 해결합니다.

## 방법 분석

LEGS의 프레임워크는 두 가지 분리로 구성됩니다: 물리와 렌더링 분리, 정적 배경과 동적 전경 분리.

### 물리와 렌더링 분리
- MuJoCo 백엔드는 500 Hz로 동역학을 해석하고, SONIC 저수준 전신 컨트롤러를 실행하며, 18차원 명령(각 팔 6차원 손목 SE(3) 자세 + 연속 그랩 스칼라, 4차원 베이스 명령 v_x, v_y, ω_z, h 연결)을 수용합니다.
- 독립적인 렌더링 프론트엔드는 이미지 관측을 생성하며, 물리 계산과 픽셀 생성은 완전히 비동기적입니다.

### 정적 배경과 동적 전경 분리
- 배경은 고정된 3DGS 필드로, 외관 렌더링에만 사용되며, 약 1–2분 손으로 촬영한 비디오의 COLMAP 자세 추정 + 3DGS 최적화(30K 반복, 단일 워크스테이션 GPU, 약 10–15분)에서 생성됩니다.
- 전경은 물리적으로 구동되는 래스터화 메시(로봇 URDF, SAM3D로 재구성된 물체)이며, 매 프레임 깊이 테스트를 통해 레이어가 합성됩니다.

### 2단계 색상 보정
이는 LEGS가 시각적 격차를 제거하는 핵심 설계입니다:

1. **1단계(메시별 대각 스케일링)**: iPhone 공간에서 각 메시의 선형 RGB 정점 색상에 대각 스케일 팩터 s^(c) = clip(μ_T^(c) / μ_S^(c), 0.5, 3.0)를 곱합니다. 여기서 μ_S는 PBR 렌더링 내부 뷰의 평균, μ_T는 SAM2 마스크 내 iPhone 픽셀의 평균입니다. 전체 3×3 행렬 대신 대각을 선택한 이유는 전체 공분산 피팅이 iPhone 영역 분산을 맞추기 위해 더 많은 항목이 필요하여 클리핑과 색조 이동이 발생하기 때문입니다.

2. **2단계(전역 색상 보정 행렬)**: 24색 패치 ColorChecker를 제어된 조명에서 두 카메라로 촬영하고, 최소 제곱으로 M⋆ = (Q^T Q)^(−1) Q^T P를 풀어 3DGS 배경과 메시 전경을 배포 카메라(RealSense)의 색상 공간으로 통일 매핑합니다. 물체별 적용 대신 합성 레벨에서 M⋆를 적용하여 공간 일관성을 유지하고 경계 아티팩트가 없습니다.

### 절차적 에피소드 생성
- 시뮬레이터 특권 정보(로봇, 물체 자세)를 활용하여 에피소드를 절차적으로 생성하고, 파라미터화된 모션 프리미티브(Walk, Pick, Place)를 조합합니다. 파라미터는 장면 레벨(목표 자세, 물체 ID, 팔 선택)이지 관절 레벨이 아닙니다.
- 각 모션은 검증 검사로 끝나며, 검증에 성공한 에피소드만 저장됩니다.
- 기록된 18차원 명령 스트림은 렌더링 입력과 독립적이므로, 데이터셋은 배경과 메시를 교체하여 GPU 비용만으로 다시 렌더링할 수 있습니다.

## 핵심 혁신

1. **3DGS 배경이 메시 배경을 대체**: 이것이 가장 핵심적인 혁신입니다. 전통적인 메시 합성 파이프라인(예: SAM3D)은 재구성 품질이 높아도 재질 음영, 조명 단순화, 앨리어싱 문제로 인해 시각적 편향이 발생합니다. LEGS는 배경을 3DGS 필드로 대체하여 합성 이미지가 텍스처와 조명 분포에서 실제 카메라 관측에 더 가깝게 만들어, 시각 인코더의 분포 이동을 직접 줄입니다.

2. **2단계 색상 보정의 엔지니어링 설계**: 1단계는 SAM3D 알베도 회귀 오류(예: 상자가 흰색 회색에서 검은색으로 복원되는 문제)를 대상으로 하고, 2단계는 3DGS 배경과 메시 전경을 배포 카메라 색상 공간으로 통일 매핑합니다. 전체 행렬 대신 대각 스케일링을 선택하고, 물체별 적용 대신 합성 레벨에서 전역 행렬을 적용하는 결정은 단순한 엔드투엔드 학습이 아닌 렌더링 파이프라인과 카메라 ISP에 대한 깊은 이해에 기반합니다.

3. **모션 생성과 렌더링 분리로 인한 비용 재구성**: 절차적 생성기가 만든 모션 궤적은 렌더링 입력과 독립적이므로, 다중 외관 조건 확장은 GPU 재렌더링(조건당 약 0.1시간)만 필요하며, 원격 조작은 1.5 운영자 시간 이상이 필요합니다. 이는 다양한 외관을 시뮬레이션에서 실제로 커버 가능하게 하여, 배포 시 외관 변화에 대한 강건성 문제를 직접 해결합니다.

## 실험 및 결과

실험은 Unitree G1에서 난이도가 증가하는 세 가지 픽-앤-플레이스 작업으로 수행되었으며, Teleop (50), SAM3D (200), LEGS (50), LEGS (200) 네 가지 데이터 조건을 비교하고, 세 가지 VLA 백본(ψ0, π0.5, GR00T N1.6)을 미세 조정했으며, 조건당 10회 시행했습니다.

**표 2 엔드 태스크 성공률(성공 횟수/10회 시행)**:

| 작업 | 백본 | Teleop (50) | SAM3D (200) | LEGS (50) | LEGS (200) |
|------|------|-------------|-------------|-----------|------------|
| Task 1 | ψ0 | 8 | 7 | 8 | **10** |
| Task 1 | π0.5 | 2 | 1 | 5 | **6** |
| Task 1 | GR00T N1.6 | 4 | 6 | 9 | **9** |
| Task 2 | ψ0 | 6 | 4 | 8 | **9** |
| Task 2 | π0.5 | 0 | 3 | 4 | **5** |
| Task 2 | GR00T N1.6 | 2 | 5 | 7 | **8** |
| Task 3 | ψ0 | 0 | 1 | 3 | **5** |
| Task 3 | π0.5 | 0 | 0 | 1 | **2** |
| Task 3 | GR00T N1.6 | 0 | 3 | 5 | **6** |

핵심 결과:
- LEGS (200)는 9개 실험 모두에서 최고 또는 공동 최고였습니다.
- Task 3에서 Teleop (50)는 모든 백본에서 0/10으로 붕괴한 반면, LEGS (200)는 2–6/10에 도달했습니다.
- 예산이 일치하는 LEGS (50)는 모든 실험에서 Teleop (50)를 일치하거나 초과했습니다.
- SAM3D (200)는 평균 33% 성공률, LEGS (200)는 67%로 평균 33% 포인트 하락, 대략 절반으로 줄었습니다.

**외관 무작위화(그림 5)**:
- 재렌더링 없이 세 가지 50 에피소드 기준선은 물체 및 물체+장면 조건에서 0–1/10으로 붕괴했습니다.
- 재렌더링 후 LEGS-aug (200)는 물체+장면 이동에서 Task 1–3의 100%, 80%, 40%에 도달한 반면, SAM3D-aug는 60%, 50%, 20%였습니다.
- 그림 5(b)는 LEGS-aug (50)가 물체+장면에서 4배 더 큰 LEGS (200)를 초과(50%, 40%, 30% vs 0%, 0%, 10%)하여, 단순히 데이터 양을 늘리는 것보다 증강 다양성이 더 효과적임을 보여줍니다.

**비용(표 3)**:
- 초기 50세트 Task 3: 원격 조작 1.5 운영자 시간, LEGS 0.5 GPU 시간(운영자 0명).
- 추가 외관 조건당: 원격 조작 >1.5 운영자 시간, LEGS 약 0.1 GPU 시간, SAM3D 약 0.2 GPU 시간.

## 경계 및 한계

- 색상 보정은 고정 노출과 고정 화이트 밸런스(RealSense 노출 100 μs, 화이트 밸런스 3000 K)에 의존하며, 훈련된 정책은 원본 3DGS 스캔 캡처 분포를 벗어난 조명 변화에 민감합니다.
- 새 장면 적응에는 여전히 1–2분 손으로 촬영한 비디오가 필요하며, 원격 조작보다 저렴하지만 무료는 아닙니다.
- 3DGS 재구성은 고반사 또는 투명 표면에서 성능이 저하되며, 정적 배경을 가정합니다. 움직이는 인간이나 기타 동적 콘텐츠가 있는 장면은 동적 3DGS 방법이 필요하며, 저자는 구현하지 않았습니다.
- 절차적 생성기는 실행 가능한 모션을 생성하지만 동적 효율성을 최적화하지 않으며, 궤적 최적화나 MPC 기반 방법과 다릅니다.
- 외관 무작위화 키트는 유사한 크기와 기하학을 가진 작은 잡기 가능한 물체만 변화시키며, 크게 다른 물체 카테고리로의 전이는 테스트되지 않았습니다.
- 단일 로봇 플랫폼(Unitree G1), 카메라(Intel RealSense D435), 전신 컨트롤러(SONIC)에서만 평가되었으며, 휴머노이드 하드웨어 전반에 걸친 일반화는 테스트되지 않았습니다.
- 논문은 추론 빈도, 원격 조작과 LEGS의 노출/화이트 밸런스 설정 차이가 픽셀별 비교에 미치는 영향을 명시적으로 언급하지 않았습니다.

## 엔지니어링 시사점

- **색상 보정의 2단계 구현을 먼저 확인**: 1단계의 대각 스케일링은 선형 RGB에서 SAM2 마스크 영역에 맞춰 피팅해야 하며, 2단계의 전역 행렬은 24색 패치 ColorChecker를 제어된 조명에서 촬영해야 합니다. 가장 흔한 실수는 sRGB 공간에서 직접 스케일링하여 이중 인코딩이 발생하거나, 배경 픽셀을 마스크 영역 피팅에 혼합하여 색조 이동이 발생하는 것입니다.
- **모션 생성과 렌더링 분리는 재현의 핵심 아키텍처 결정**: 기록된 18차원 명령 스트림이 렌더링 입력과 독립적인지 확인해야 다중 외관 확장이 GPU 재렌더링만으로 가능합니다. 물리 백엔드와 렌더링 프론트엔드가 결합되어 있으면 LEGS의 비용 이점을 얻을 수 없습니다.
- **외관 무작위화의 이점이 데이터 양보다 큼**: 그림 5(b)는 50세트 증강 데이터가 물체+장면 이동에서 200세트 기본 데이터를 초과하여, 예산이 제한된 경우 동일 분포 내 데이터 양을 늘리는 것보다 외관 다양성 커버를 우선시해야 함을 보여줍니다.
- **SONIC 컨트롤러는 배포의 핵심 의존성**: 동일한 SONIC 바이너리가 시뮬레이션과 실제 로봇에서 실행되며, 배포 시 정책 변경이 필요 없습니다. 재현 시 컨트롤러 인터페이스(18차원 명령)와 DDS 토픽 이름이 배포 대상과 완전히 일치하는지 확인해야 합니다.
- **카메라 ISP 설정 주의**: 배포 시 RealSense의 노출과 자동 화이트 밸런스를 고정(100 μs, 3000 K)해야 하며, 그렇지 않으면 색상 보정 행렬이失效합니다. 원격 조작은 자동 노출/자동 화이트 밸런스를 활성화하며 LEGS의 고정 ISP와 다르므로 픽셀별 직접 비교가 불가능합니다.
