---
$id: ent_paper_e_3_c_video_generation_3d_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'E$^3$C: Video Generation with 3D Environmental Memory and Ego-Exo Human Pose Control'
  zh: 'E$^3$C: Video Generation with 3D Environmental Memory and Ego-Exo Human Pose Control'
  ko: 'E$^3$C: Video Generation with 3D Environmental Memory and Ego-Exo Human Pose Control'
summary:
  en: 'Controllable and physically grounded egocentric video generation is essential for embodied agents to reason about how
    their own and others'' actions manifest and change the world. Institutions per source list: Meta Reality Labs、University
    of Toronto.'
  zh: E$^3$C 是一个可控的自我中心视频生成框架，由研究团队提出，用于具身智能体推理自身及他人动作如何改变世界。其核心贡献在于构建了基于半稠密点云的 3D 环境记忆，并分离了场景结构与人类动态控制，同时支持自我中心（ego）和外部（exo）人体姿态控制。在
    Nymeria 数据集上的实验表明，E$^3$C 在视觉保真度、相机运动准确性、物体一致性及人体控制方面均优于强基线方法。
  ko: 'Controllable and physically grounded egocentric video generation is essential for embodied agents to reason about how
    their own and others'' actions manifest and change the world. Institutions per source list: Meta Reality Labs、University
    of Toronto.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- e
- '3'
- c
- video
- generation
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 281 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2605.26316 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2605.26316v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2605.26316 E$^3$C: Video Generation with 3D Environmental Memory and Ego-Exo Human Pose Control'
  url: https://arxiv.org/abs/2605.26316
  accessed_at: '2026-07-31'
  date: '2026-05-25'
- id: src_002
  type: website
  title: Project page
  url: https://e3c-videogen.github.io/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: 机器人下一代数据入口，可能就是Ego：9篇论文讲透第一视角技术路线
  url: https://mp.weixin.qq.com/s/4JQ1xa-cJ7J1ep_e4txNnA
  accessed_at: '2026-07-31'
---


## 概述

E³C 是一个面向完全可控第一人称视频生成的扩散框架，由 Meta Reality Labs 与多伦多大学合作提出。其核心贡献在于将持久 3D 环境记忆（半稠密点云 + 逐点 VAE 外观特征）与人类驱动动态（ego 姿态令牌 + exo 骨架）解耦，通过双流条件注入（VACE Context Adapter + 姿态交叉注意力）实现几何一致性与人体控制的统一。在 Nymeria 数据集上，E³C 在视觉质量、相机轨迹精度、对象一致性和 ego/exo 人体控制指标上全面超越现有 3D 感知与姿态条件基线。

## 它改变了什么

第一人称视频生成长期被两个脱节的研究路线割裂：姿态条件模型（如 PEVA、EgoControl）能控制人体但缺乏对 3D 场景的持久记忆，导致快速头部运动下背景漂移和几何坍塌；而 3D 一致生成器（如 VMem、Gen3C）针对通用场景设计，不建模自我遮挡与附近人物的耦合运动。E³C 真正改变的是将“场景几何”与“人体动态”作为两个独立但互补的条件流注入扩散模型，而不是试图在一个隐空间中同时隐式建模两者。这一解耦设计使得相机轨迹控制（依赖几何）与人体姿态控制（依赖运动学）可以分别优化，且消融实验证明点云几何条件对 FVD 的改善（从 1380 降至 329）远超任何姿态控制模块，说明几何一致性是第一人称生成的首要瓶颈。

另一个关键改变在于 ego 姿态的表示方式。现有方法将 3D 关节投影为 2D 骨架叠加在 RGB 条件上，但 ego 身体频繁出视野导致绘制稀疏甚至缺失。E³C 引入持久 ego 姿态令牌——将关节与手腕轨迹编码为序列令牌，通过交叉注意力直接注入 DiT——这相当于给模型一个“本体感觉”通道，而非依赖视觉可见性。这一设计将 Hand-F1 从 36.03 提升至 41.44，证明隐式姿态令牌比显式绘制更鲁棒。

## 方法拆解

### 3D 环境记忆构建
- 从上下文帧（前 m=200 帧）运行 SLAM（Project Aria MPS）获得半稠密点云 P ∈ ℝ^(N×3)，每个点分配 RGB 颜色 C（从观测帧采样，体素下采样 0.01m 后平均池化）。
- 逐点外观特征：用 Wan2.1 视频 VAE 编码器 E 编码上下文帧，得到 16 通道潜图（空间分辨率 1/8），将每个 3D 点投影到特征图双线性插值采样，世界空间 0.02m 体素池化聚合多视图。

### 视点对齐渲染与条件注入
- 给定目标相机轨迹 T_(1:n)，将增强点云（颜色 + 特征）投影渲染到每个目标视点，得到 RGB 渲染 c^pc 和特征渲染 c^feat。
- exo 2D 骨架栅格化叠加在 c^pc 上形成 c^ctrl；ego 3D 关节与手腕 SE(3) 变换投影为 2D 骨架和小坐标轴也绘制在 c^ctrl 上。
- 双流编码：视频编码器编码 c^ctrl 得 e^ctrl，轻量 3D 卷积特征编码器（隐藏宽度 1024，输出 96 通道）编码 c^feat 得 e^feat，求和融合后通过 VACE Context Adapter 注入视频 DiT 作为残差条件。
- 关键设计：不显式输入可见性或深度图，模型隐式学习推理遮挡关系。

### 持久 ego 姿态令牌
- 输入：ego 3D 身体关节 p^(e,s) ∈ ℝ^(n×J×3) 和 6DoF 手腕姿态 p^(e,w) ∈ ℝ^(n×2×4×4)（相机坐标系）。
- 特征化：关节用 3D 坐标 + 一阶/二阶时间差分（15 维）；手腕用平移 + 连续 6D 旋转（27 维）。
- 融合：身体与手腕流通过学习门控融合，空间 Transformer + 注意力池化聚合，时间传播。
- 摘要令牌：K=4 个学习令牌交叉关注 n 个姿态令牌，拼接后得 H^e ∈ ℝ^((n+4)×1536)，通过专用姿态交叉注意力条件化 DiT。

### 训练与推理
- 流匹配目标：L = E[ w(t) ||(ε - z_0) - ε_θ(z_t, t; τ, C)||_2^2 ]，z_t = (1-σ_t)z_0 + σ_t ε。
- 从 Wan2.1-VACE-1.3B 初始化，LoRA 秩 256 微调主 DiT 和上下文适配器；ego 姿态编码器、特征编码器、姿态交叉注意力从头训练。
- 推理：50 步 FlowMatch，σ_shift=5.0，CFG ω=5.0。

## 关键创新

1. **双流条件解耦架构**：将 3D 环境记忆（点云渲染 + 逐点特征）与人体动态（exo 骨架 + ego 姿态令牌）作为独立条件流，通过 VACE Adapter 和交叉注意力分别注入。这是首次在第一人称生成中显式分离“静态场景”与“动态人体”的表示，使得相机控制与人体控制可以独立调节，且消融证明两者互补而非冗余。

2. **持久 ego 姿态令牌**：将 ego 关节与手腕轨迹编码为时间序列令牌，通过交叉注意力直接条件化 DiT，而非投影为 2D 绘制。这解决了 ego 身体频繁出视野导致的控制信号稀疏问题，相当于给模型一个不依赖视觉可见性的“本体感觉”通道。Hand-F1 从 36.03 提升至 41.44 是该方法有效性的直接证据。

3. **逐点 VAE 外观特征**：将视频 VAE 的潜特征附加到 3D 点上，渲染后通过轻量特征编码器注入。这比仅用 RGB 点云颜色提供更丰富的纹理先验，LPIPS 从 0.440 降至 0.424，PSNR 从 17.1 提升至 17.9，说明特征级条件比像素级条件更利于生成器的感知质量。

## 实验与结果

在 Nymeria 测试集 100 个片段上评估，与三类基线对比：3D 感知生成器（Splatfacto、Gen3C、VMem）、微调 VACE（points+exo）、第一人称生成器（PEVA、EgoControl、EgoTwin）。

| 指标 | E³C | 最佳微调 VACE | Splatfacto | Gen3C | VMem |
|------|-----|--------------|------------|-------|------|
| FVD ↓ | **249** | 328 | 1040 | 974 | 465 |
| LPIPS ↓ | **0.415** | 0.440 | 0.494 | 0.565 | 0.618 |
| PSNR ↑ | **18.6** | 17.3 | 17.8 | 15.8 | 12.7 |
| TErr (cm) ↓ | **2.40** | 5.41 | 6.60 | 21.37 | 16.82 |
| RErr (°) ↓ | 23.3 | 24.5 | **21.5** | 88.5 | 113.0 |
| Obj-F1 ↑ | **52.85** | 47.95 | 41.05 | 25.66 | 14.74 |
| Exo-F1 ↑ | **39.59** | 36.26 | 15.28 | 12.39 | 8.67 |
| PCK@10% ↑ | **72.88** | 64.08 | 63.13 | 42.08 | 2.55 |
| Hand-F1 ↑ | **41.44** | 34.24 | 21.28 | 25.54 | 26.31 |

消融实验（逐步添加条件）显示：点云几何是最大贡献者（FVD 1380→329），逐点特征改善感知质量（LPIPS 0.440→0.424），exo 骨架提升 Exo-F1（19.87→38.40），编码 ego 令牌在 Hand-F1（36.03→41.44）和 Hand-mIoU（19.36→24.76）上增益最大。与 EgoTwin 对比中，E³C 的 I-FID 31.0 vs 98.2，TErr 2.40cm vs 67.0cm，优势显著。

## 边界与局限

- 空间记忆假设环境大部分静态，仅通过人体运动建模动态，对移动物体或长时程场景变化性能会下降。
- ego/exo 人体控制仅条件化运动，不显式条件化外观——人物离开视野再进入时外观可能漂移。
- 半稠密 SLAM 点云集中在高梯度区域，低纹理区域（如白墙）代表性不足，可能影响无纹理场景的几何一致性。
- 与 EgoTwin 的对比协议无法完全对齐（原始划分和实现不可用），比较结果需谨慎解读。
- 未评估超过 80 帧的长时程生成，也未涉及户外场景专门处理或非人类动态物体控制。

## 工程启示

复现或应用 E³C 时，优先核对以下几点：

1. **SLAM 点云质量是天花板**：消融显示点云条件贡献最大，但半稠密 SLAM 在低纹理区域稀疏。若目标场景纹理贫乏，需考虑补充稠密重建或深度估计，否则几何一致性会显著退化。建议先评估 MPS 点云在目标环境的覆盖率。

2. **特征体素大小需调参**：RGB 点记忆用 0.01m 体素，特征记忆用 0.02m（因特征更平滑）。若场景尺度差异大，这两个参数可能需要重新标定，过小会导致特征碎片化，过大会丢失细节。

3. **ego 姿态令牌的输入坐标系**：关节和手腕姿态在相机坐标系中，且包含时间差分。若从其他动捕系统获取数据，需确保坐标系对齐（尤其手腕 SE(3) 的旋转表示），否则姿态交叉注意力会学到错误的映射。

4. **训练成本高**：64 块 H100、50 epoch、LoRA 秩 256。若资源有限，可先冻结特征编码器只训练姿态分支，消融显示 +Encoded Ego 的增益（Hand-F1 +5.41）相对独立，可能以较低成本获得主要收益。

5. **评估指标陷阱**：PCK@10% 仅对匹配的人体框计算，且与 Exo-F1 联合解读；Hand-mIoU 中空-空帧贡献 1，会虚高。复现评估时需严格遵循其匹配阈值（τ_obj=0.5，τ_exo=0.2）和过滤规则（SAM3 面积 <1536 像素过滤），否则数字不可比。

## 参考
- https://arxiv.org/abs/2605.26316
- https://e3c-videogen.github.io/
- https://mp.weixin.qq.com/s/4JQ1xa-cJ7J1ep_e4txNnA

## Overview

E³C is a diffusion framework for fully controllable first-person video generation, proposed by Meta Reality Labs in collaboration with the University of Toronto. Its core contribution lies in decoupling persistent 3D environment memory (semi-dense point clouds + per-point VAE appearance features) from human-driven dynamics (ego pose tokens + exo skeletons), achieving unified geometric consistency and human control through dual-stream conditional injection (VACE Context Adapter + pose cross-attention). On the Nymeria dataset, E³C comprehensively outperforms existing 3D-aware and pose-conditioned baselines across visual quality, camera trajectory accuracy, object consistency, and ego/exo human control metrics.

## What It Changes

First-person video generation has long been fragmented by two disconnected research lines: pose-conditioned models (e.g., PEVA, EgoControl) can control the human body but lack persistent memory of the 3D scene, leading to background drift and geometric collapse under rapid head motion; while 3D-consistent generators (e.g., VMem, Gen3C) are designed for general scenes and do not model self-occlusion or coupled motion with nearby people. What E³C truly changes is injecting "scene geometry" and "human dynamics" as two independent yet complementary conditional streams into the diffusion model, rather than attempting to implicitly model both in a single latent space. This decoupled design allows camera trajectory control (geometry-dependent) and human pose control (kinematics-dependent) to be optimized separately, and ablation experiments demonstrate that point cloud geometry conditioning improves FVD (from 1380 to 329) far more than any pose control module, indicating that geometric consistency is the primary bottleneck in first-person generation.

Another key change lies in the representation of ego pose. Existing methods project 3D joints into 2D skeletons overlaid on RGB conditioning, but the ego body frequently leaving the field of view results in sparse or even missing renderings. E³C introduces persistent ego pose tokens—encoding joints and wrist trajectories as sequence tokens injected directly into the DiT via cross-attention—which effectively provides the model with a "proprioceptive" channel rather than relying on visual visibility. This design improves Hand-F1 from 36.03 to 41.44, proving that implicit pose tokens are more robust than explicit rendering.

## Method Breakdown

### 3D Environment Memory Construction
- Run SLAM (Project Aria MPS) on context frames (first m=200 frames) to obtain a semi-dense point cloud P ∈ ℝ^(N×3), with each point assigned an RGB color C (sampled from observed frames, voxel-downsampled at 0.01m and average-pooled).
- Per-point appearance features: Encode context frames with the Wan2.1 video VAE encoder E to obtain 16-channel latent maps (spatial resolution 1/8), project each 3D point onto the feature maps with bilinear interpolation sampling, and aggregate multi-view information via 0.02m voxel pooling in world space.

### Viewpoint-Aligned Rendering and Conditional Injection
- Given a target camera trajectory T_(1:n), project and render the augmented point cloud (colors + features) to each target viewpoint, obtaining RGB rendering c^pc and feature rendering c^feat.
- Rasterized exo 2D skeletons are overlaid on c^pc to form c^ctrl; ego 3D joints and wrist SE(3) transforms are projected into 2D skeletons and small coordinate axes, also drawn on c^ctrl.
- Dual-stream encoding: A video encoder encodes c^ctrl to obtain e^ctrl, while a lightweight 3D convolutional feature encoder (hidden width 1024, output 96 channels) encodes c^feat to obtain e^feat. The sum-fused result is injected into the video DiT via the VACE Context Adapter as a residual condition.
- Key design: Visibility or depth maps are not explicitly input; the model implicitly learns to infer occlusion relationships.

### Persistent Ego Pose Tokens
- Input: ego 3D body joints p^(e,s) ∈ ℝ^(n×J×3) and 6DoF wrist poses p^(e,w) ∈ ℝ^(n×2×4×4) (camera coordinate frame).
- Featureization: Joints use 3D coordinates + first/second-order temporal differences (15 dimensions); wrists use translation + continuous 6D rotation (27 dimensions).
- Fusion: Body and wrist streams are fused via learned gating, aggregated with spatial Transformer + attention pooling, and propagated temporally.
- Summary tokens: K=4 learned tokens cross-attend to n pose tokens, concatenated to obtain H^e ∈ ℝ^((n+4)×1536), conditioning the DiT via dedicated pose cross-attention.

### Training and Inference
- Flow matching objective: L = E[ w(t) ||(ε - z_0) - ε_θ(z_t, t; τ, C)||_2^2 ], with z_t = (1-σ_t)z_0 + σ_t ε.
- Initialized from Wan2.1-VACE-1.3B, with LoRA rank 256 fine-tuning of the main DiT and context adapter; ego pose encoder, feature encoder, and pose cross-attention are trained from scratch.
- Inference: 50-step FlowMatch, σ_shift=5.0, CFG ω=5.0.

## Key Innovations

1. **Dual-Stream Conditional Decoupling Architecture**: 3D environment memory (point cloud rendering + per-point features) and human dynamics (exo skeletons + ego pose tokens) are treated as independent conditional streams, injected separately via the VACE Adapter and cross-attention. This is the first time in first-person generation that representations of "static scenes" and "dynamic humans" are explicitly separated, allowing camera control and human control to be independently adjusted, with ablations proving they are complementary rather than redundant.

2. **Persistent Ego Pose Tokens**: Ego joints and wrist trajectories are encoded as temporal sequence tokens and directly condition the DiT via cross-attention, rather than being projected as 2D renderings. This solves the sparse control signal problem caused by the ego body frequently leaving the field of view, effectively providing the model with a "proprioceptive" channel independent of visual visibility. The Hand-F1 improvement from 36.03 to 41.44 is direct evidence of this method's effectiveness.

3. **Per-Point VAE Appearance Features**: Video VAE latent features are attached to 3D points and injected through a lightweight feature encoder after rendering. This provides richer texture priors than RGB point cloud colors alone, with LPIPS dropping from 0.440 to 0.424 and PSNR improving from 17.1 to 17.9, demonstrating that feature-level conditioning benefits generator perceptual quality more than pixel-level conditioning.

## Experiments and Results

Evaluated on 100 clips from the Nymeria test set, compared against three baseline categories: 3D-aware generators (Splatfacto, Gen3C, VMem), fine-tuned VACE (points+exo), and first-person generators (PEVA, EgoControl, EgoTwin).

| Metric | E³C | Best Fine-tuned VACE | Splatfacto | Gen3C | VMem |
|------|-----|--------------|------------|-------|------|
| FVD ↓ | **249** | 328 | 1040 | 974 | 465 |
| LPIPS ↓ | **0.415** | 0.440 | 0.494 | 0.565 | 0.618 |
| PSNR ↑ | **18.6** | 17.3 | 17.8 | 15.8 | 12.7 |
| TErr (cm) ↓ | **2.40** | 5.41 | 6.60 | 21.37 | 16.82 |
| RErr (°) ↓ | 23.3 | 24.5 | **21.5** | 88.5 | 113.0 |
| Obj-F1 ↑ | **52.85** | 47.95 | 41.05 | 25.66 | 14.74 |
| Exo-F1 ↑ | **39.59** | 36.26 | 15.28 | 12.39 | 8.67 |
| PCK@10% ↑ | **72.88** | 64.08 | 63.13 | 42.08 | 2.55 |
| Hand-F1 ↑ | **41.44** | 34.24 | 21.28 | 25.54 | 26.31 |

Ablation experiments (progressively adding conditions) show that point cloud geometry is the largest contributor (FVD 1380→329), per-point features improve perceptual quality (LPIPS 0.440→0.424), exo skeletons improve Exo-F1 (19.87→38.40), and encoded ego tokens yield the largest gains in Hand-F1 (36.03→41.44) and Hand-mIoU (19.36→24.76). In comparison with EgoTwin, E³C achieves I-FID 31.0 vs 98.2 and TErr 2.40cm vs 67.0cm, demonstrating significant advantages.

## Boundaries and Limitations

- Spatial memory assumes a mostly static environment, modeling dynamics only through human motion; performance degrades with moving objects or long-term scene changes.
- Ego/exo human control conditions only motion, not appearance—appearance may drift when a person leaves and re-enters the field of view.
- Semi-dense SLAM point clouds concentrate on high-gradient regions, underrepresenting low-texture areas (e.g., white walls), which may affect geometric consistency in textureless scenes.
- The comparison protocol with EgoTwin cannot be fully aligned (original splits and implementations are unavailable), so results should be interpreted with caution.
- Long-horizon generation beyond 80 frames is not evaluated, nor are outdoor scene-specific handling or non-human dynamic object control.

## Engineering Insights

When reproducing or applying E³C, prioritize checking the following:

1. **SLAM point cloud quality is the ceiling**: Ablations show point cloud conditioning contributes the most, but semi-dense SLAM is sparse in low-texture regions. If the target scene is texture-poor, consider supplementing with dense reconstruction or depth estimation; otherwise, geometric consistency will degrade significantly. It is recommended to first assess MPS point cloud coverage in the target environment.

2. **Feature voxel size requires tuning**: RGB point memory uses 0.01m voxels, while feature memory uses 0.02m (since features are smoother). If scene scale varies significantly, these parameters may need recalibration—too small causes feature fragmentation, too large loses detail.

3. **Input coordinate frame for ego pose tokens**: Joints and wrist poses are in the camera coordinate frame and include temporal differences. If obtaining data from other motion capture systems, ensure coordinate frame alignment (especially the SE(3) rotation representation of wrists); otherwise, pose cross-attention will learn incorrect mappings.

4. **High training cost**: 64 H100 GPUs, 50 epochs, LoRA rank 256. If resources are limited, consider freezing the feature encoder and training only the pose branch; ablations show the +Encoded Ego gain (Hand-F1 +5.41) is relatively independent and may capture most benefits at lower cost.

5. **Evaluation metric pitfalls**: PCK@10% is computed only on matched human boxes and should be interpreted jointly with Exo-F1; Hand-mIoU counts empty-empty frames as 1, which inflates scores. When reproducing evaluations, strictly follow the matching thresholds (τ_obj=0.5, τ_exo=0.2) and filtering rules (SAM3 area <1536 pixels filtered); otherwise, numbers are not comparable.

## 개요

E³C는 Meta Reality Labs와 토론토 대학이 공동 제안한 완전 제어 가능한 1인칭 비디오 생성을 위한 확산 프레임워크입니다. 핵심 기여는 지속적인 3D 환경 메모리(반밀집 포인트 클라우드 + 포인트별 VAE 외관 특징)와 인간 구동 역학(ego 포즈 토큰 + exo 스켈레톤)을 분리하고, 이중 스트림 조건 주입(VACE Context Adapter + 포즈 교차 어텐션)을 통해 기하학적 일관성과 인간 제어의 통합을 실현한 것입니다. Nymeria 데이터셋에서 E³C는 시각적 품질, 카메라 궤적 정밀도, 객체 일관성 및 ego/exo 인간 제어 지표에서 기존 3D 인식 및 포즈 조건 기준선을 전반적으로 능가합니다.

## 무엇을 바꾸었는가

1인칭 비디오 생성은 오랫동안 두 개의 단절된 연구 경로로 분리되어 왔습니다: 포즈 조건 모델(PEVA, EgoControl 등)은 인간을 제어할 수 있지만 3D 장면에 대한 지속적인 메모리가 부족하여 빠른 머리 움직임에서 배경 드리프트와 기하학적 붕괴가 발생합니다. 반면 3D 일관성 생성기(VMem, Gen3C 등)는 일반 장면용으로 설계되어 자기 가림 및 주변 인물의 결합 운동을 모델링하지 않습니다. E³C가 실제로 바꾼 것은 "장면 기하학"과 "인간 역학"을 하나의 잠재 공간에서 동시에 암시적으로 모델링하려는 대신, 두 개의 독립적이지만 상호 보완적인 조건 흐름으로 확산 모델에 주입하는 것입니다. 이 분리 설계는 카메라 궤적 제어(기하학에 의존)와 인간 포즈 제어(운동학에 의존)를 각각 최적화할 수 있게 하며, 소거 실험은 포인트 클라우드 기하학 조건이 FVD 개선(1380에서 329로)에 있어 어떤 포즈 제어 모듈보다 훨씬 크다는 것을 증명하여 기하학적 일관성이 1인칭 생성의 최우선 병목임을 보여줍니다.

또 다른 핵심 변화는 ego 포즈의 표현 방식입니다. 기존 방법은 3D 관절을 2D 스켈레톤으로 투영하여 RGB 조건에 오버레이하지만, ego 신체가 시야를 자주 벗어나 드로잉이 희소하거나 누락됩니다. E³C는 지속적인 ego 포즈 토큰을 도입합니다—관절과 손목 궤적을 시퀀스 토큰으로 인코딩하고 교차 어텐션을 통해 DiT에 직접 주입합니다—이는 모델에 시각적 가시성에 의존하지 않는 "고유 감각" 채널을 제공하는 것과 같습니다. 이 설계는 Hand-F1을 36.03에서 41.44로 향상시켜 암시적 포즈 토큰이 명시적 드로잉보다 더 견고함을 증명합니다.

## 방법 분해

### 3D 환경 메모리 구축
- 컨텍스트 프레임(처음 m=200 프레임)에서 SLAM(Project Aria MPS)을 실행하여 반밀집 포인트 클라우드 P ∈ ℝ^(N×3)를 얻고, 각 포인트에 RGB 색상 C(관측 프레임에서 샘플링, 0.01m 복셀 다운샘플링 후 평균 풀링)를 할당합니다.
- 포인트별 외관 특징: Wan2.1 비디오 VAE 인코더 E로 컨텍스트 프레임을 인코딩하여 16채널 잠재 맵(공간 해상도 1/8)을 얻고, 각 3D 포인트를 특징 맵에 투영하여 쌍선형 보간 샘플링하고, 세계 공간 0.02m 복셀 풀링으로 다중 뷰를 집계합니다.

### 시점 정렬 렌더링 및 조건 주입
- 대상 카메라 궤적 T_(1:n)이 주어지면 향상된 포인트 클라우드(색상 + 특징)를 각 대상 시점에 투영 렌더링하여 RGB 렌더링 c^pc와 특징 렌더링 c^feat를 얻습니다.
- exo 2D 스켈레톤 래스터화를 c^pc에 오버레이하여 c^ctrl을 형성합니다; ego 3D 관절과 손목 SE(3) 변환을 2D 스켈레톤과 작은 좌표축으로 투영하여 c^ctrl에도 그립니다.
- 이중 스트림 인코딩: 비디오 인코더가 c^ctrl을 인코딩하여 e^ctrl을 얻고, 경량 3D 컨볼루션 특징 인코더(숨겨진 너비 1024, 출력 96채널)가 c^feat를 인코딩하여 e^feat를 얻은 후, 합산 융합하여 VACE Context Adapter를 통해 비디오 DiT에 잔차 조건으로 주입합니다.
- 핵심 설계: 가시성 또는 깊이 맵을 명시적으로 입력하지 않고, 모델이 가림 관계를 암시적으로 학습하여 추론합니다.

### 지속적인 ego 포즈 토큰
- 입력: ego 3D 신체 관절 p^(e,s) ∈ ℝ^(n×J×3) 및 6DoF 손목 포즈 p^(e,w) ∈ ℝ^(n×2×4×4)(카메라 좌표계).
- 특징화: 관절은 3D 좌표 + 1차/2차 시간 차분(15차원); 손목은 병진 + 연속 6D 회전(27차원).
- 융합: 신체와 손목 흐름이 학습된 게이팅으로 융합되고, 공간 Transformer + 어텐션 풀링으로 집계되며, 시간 전파됩니다.
- 요약 토큰: K=4개의 학습된 토큰이 n개의 포즈 토큰에 교차 어텐션하고, 연결 후 H^e ∈ ℝ^((n+4)×1536)을 얻어 전용 포즈 교차 어텐션으로 DiT를 조건화합니다.

### 훈련 및 추론
- 흐름 매칭 목표: L = E[ w(t) ||(ε - z_0) - ε_θ(z_t, t; τ, C)||_2^2 ], z_t = (1-σ_t)z_0 + σ_t ε.
- Wan2.1-VACE-1.3B에서 초기화하고, LoRA 랭크 256으로 메인 DiT와 컨텍스트 어댑터를 미세 조정합니다; ego 포즈 인코더, 특징 인코더, 포즈 교차 어텐션은 처음부터 훈련합니다.
- 추론: 50단계 FlowMatch, σ_shift=5.0, CFG ω=5.0.

## 핵심 혁신

1. **이중 스트림 조건 분리 아키텍처**: 3D 환경 메모리(포인트 클라우드 렌더링 + 포인트별 특징)와 인간 역학(exo 스켈레톤 + ego 포즈 토큰)을 독립적인 조건 흐름으로 분리하고, VACE Adapter와 교차 어텐션을 통해 각각 주입합니다. 이는 1인칭 생성에서 "정적 장면"과 "동적 인간"의 표현을 명시적으로 분리한 최초의 사례로, 카메라 제어와 인간 제어를 독립적으로 조정할 수 있으며 소거 실험은 두 가지가 상호 보완적이지 중복이 아님을 증명합니다.

2. **지속적인 ego 포즈 토큰**: ego 관절과 손목 궤적을 시계열 토큰으로 인코딩하고 교차 어텐션으로 DiT를 직접 조건화하며, 2D 드로잉으로 투영하지 않습니다. 이는 ego 신체가 시야를 자주 벗어나 발생하는 제어 신호 희소성 문제를 해결하며, 시각적 가시성에 의존하지 않는 "고유 감각" 채널을 모델에 제공하는 것과 같습니다. Hand-F1이 36.03에서 41.44로 향상된 것은 이 방법의 효과성에 대한 직접적인 증거입니다.

3. **포인트별 VAE 외관 특징**: 비디오 VAE의 잠재 특징을 3D 포인트에 부착하고, 렌더링 후 경량 특징 인코더로 주입합니다. 이는 RGB 포인트 클라우드 색상만 사용하는 것보다 더 풍부한 텍스처 사전을 제공하며, LPIPS가 0.440에서 0.424로 감소하고 PSNR이 17.1에서 17.9로 향상되어 특징 수준 조건이 픽셀 수준 조건보다 생성기의 지각 품질에 더 유리함을 보여줍니다.

## 실험 및 결과

Nymeria 테스트 세트의 100개 클립에서 평가하며, 세 가지 기준선 범주와 비교합니다: 3D 인식 생성기(Splatfacto, Gen3C, VMem), 미세 조정 VACE(points+exo), 1인칭 생성기(PEVA, EgoControl, EgoTwin).

| 지표 | E³C | 최적 미세 조정 VACE | Splatfacto | Gen3C | VMem |
|------|-----|--------------|------------|-------|------|
| FVD ↓ | **249** | 328 | 1040 | 974 | 465 |
| LPIPS ↓ | **0.415** | 0.440 | 0.494 | 0.565 | 0.618 |
| PSNR ↑ | **18.6** | 17.3 | 17.8 | 15.8 | 12.7 |
| TErr (cm) ↓ | **2.40** | 5.41 | 6.60 | 21.37 | 16.82 |
| RErr (°) ↓ | 23.3 | 24.5 | **21.5** | 88.5 | 113.0 |
| Obj-F1 ↑ | **52.85** | 47.95 | 41.05 | 25.66 | 14.74 |
| Exo-F1 ↑ | **39.59** | 36.26 | 15.28 | 12.39 | 8.67 |
| PCK@10% ↑ | **72.88** | 64.08 | 63.13 | 42.08 | 2.55 |
| Hand-F1 ↑ | **41.44** | 34.24 | 21.28 | 25.54 | 26.31 |

소거 실험(조건을 점진적으로 추가)은 포인트 클라우드 기하학이 가장 큰 기여자(FVD 1380→329)이며, 포인트별 특징이 지각 품질을 개선하고(LPIPS 0.440→0.424), exo 스켈레톤이 Exo-F1을 향상시키며(19.87→38.40), 인코딩된 ego 토큰이 Hand-F1(36.03→41.44)과 Hand-mIoU(19.36→24.76)에서 가장 큰 이득을 보여줍니다. EgoTwin과의 비교에서 E³C의 I-FID는 31.0 대 98.2, TErr는 2.40cm 대 67.0cm로 우위가 뚜렷합니다.

## 경계 및 한계

- 공간 메모리는 환경이 대부분 정적이라고 가정하고 인간 운동을 통해서만 동적을 모델링하므로, 움직이는 물체나 장기간 장면 변화에서는 성능이 저하됩니다.
- ego/exo 인간 제어는 운동만 조건화하고 외관은 명시적으로 조건화하지 않습니다—인물이 시야를 벗어났다가 다시 들어올 때 외관이 드리프트할 수 있습니다.
- 반밀집 SLAM 포인트 클라우드는 높은 그래디언트 영역에 집중되어 있어 낮은 텍스처 영역(예: 흰 벽)의 대표성이 부족하며, 텍스처가 없는 장면의 기하학적 일관성에 영향을 줄 수 있습니다.
- EgoTwin과의 비교 프로토콜은 완전히 정렬할 수 없으며(원본 분할 및 구현 사용 불가), 비교 결과는 주의 깊게 해석해야 합니다.
- 80프레임을 초과하는 장기 생성은 평가되지 않았으며, 야외 장면 특수 처리나 비인간 동적 객체 제어도 다루지 않았습니다.

## 엔지니어링 시사점

E³C를 재현하거나 적용할 때 다음 사항을 우선 확인하십시오:

1. **SLAM 포인트 클라우드 품질이 상한선입니다**: 소거 실험은 포인트 클라우드 조건이 가장 큰 기여를 하지만, 반밀집 SLAM은 낮은 텍스처 영역에서 희소합니다. 대상 장면의 텍스처가 빈약하면 밀집 재구성이나 깊이 추정을 보완해야 하며, 그렇지 않으면 기하학적 일관성이 크게 저하됩니다. 먼저 대상 환경에서 MPS 포인트 클라우드의 적용 범위를 평가하는 것이 좋습니다.

2. **특징 복셀 크기 조정이 필요합니다**: RGB 포인트 메모리는 0.01m 복셀, 특징 메모리는 0.02m(특징이 더 매끄럽기 때문)를 사용합니다. 장면 규모 차이가 크면 이 두 매개변수를 다시 보정해야 할 수 있으며, 너무 작으면 특징이 파편화되고 너무 크면 세부 정보가 손실됩니다.

3. **ego 포즈 토큰의 입력 좌표계**: 관절과 손목 포즈는 카메라 좌표계에 있으며 시간 차분을 포함합니다. 다른 모션 캡처 시스템에서 데이터를 얻는 경우 좌표계 정렬(특히 손목 SE(3)의 회전 표현)을 보장해야 하며, 그렇지 않으면 포즈 교차 어텐션이 잘못된 매핑을 학습할 수 있습니다.

4. **훈련 비용이 높습니다**: 64개의 H100, 50 epoch, LoRA 랭크 256. 리소스가 제한된 경우 특징 인코더를 동결하고 포즈 분기만 훈련할 수 있으며, 소거 실험은 +Encoded Ego의 이득(Hand-F1 +5.41)이 상대적으로 독립적이므로 낮은 비용으로 주요 이점을 얻을 수 있습니다.

5. **평가 지표 함정**: PCK@10%는 일치하는 인간 상자에 대해서만 계산되며 Exo-F1과 함께 해석해야 합니다; Hand-mIoU에서 빈-빈 프레임은 1을 기여하여 부풀려질 수 있습니다. 평가를 재현할 때 일치 임계값(τ_obj=0.5, τ_exo=0.2)과 필터링 규칙(SAM3 면적 <1536 픽셀 필터링)을 엄격히 따라야 하며, 그렇지 않으면 숫자를 비교할 수 없습니다.
