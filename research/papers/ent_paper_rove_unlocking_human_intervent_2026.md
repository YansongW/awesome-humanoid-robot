---
$id: ent_paper_rove_unlocking_human_intervent_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'ROVE: Unlocking Human Interventions for Humanoid Manipulation via Reinforcement Learning'
  zh: ROVE｜通过强化学习解锁人形操作的人类干预
  ko: 'ROVE: Unlocking Human Interventions for Humanoid Manipulation via Reinforcement Learning'
summary:
  en: The realization path of Rove is to encode language instructions, camera images/multi-view observations, ontological
    states, and joint sequences into multimodal representations, then, PPO/RL policy training, Diffusion Policy/flow matching,
    and VLA multimodal action model are used to predict executable action commands. The key point is to retain the semantic
    understanding of VLM, while increasing the robot state and action head, so as to avoid remaining in the language planning.
  zh: ROVE 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用PPO/RL 策略训练、扩散策略/流匹配、VLA 多模态动作模型预测可执行动作命令。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: ROVE의 구현 경로는 먼저 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태 및 관절 시퀀스를 다중 모달 표현으로 인코딩한 다음, PPO/RL 정책 훈련, 확산 정책/흐름 매칭, VLA 다중 모달 행동
    모델을 통해 실행 가능한 행동 명령을 예측하는 것이다. 핵심은 VLM의 의미론적 이해를 유지하면서 로봇 상태와 행동 헤드를 추가하여 언어 계획에만 머무르지 않도록 하는 것이다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- general_manipulation
- rove
- vision_language_action
- vla
- world_model
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: ROVE project page
  url: https://xpeng-robotics.github.io/rove
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

ROVE 是一个面向人形机器人 VLA 模型后训练的强化学习框架，核心贡献是提出 Optimistic Value Estimation（OVE）方法，让策略能从人类干预产生的次优轨迹中学习。系统由人类在环数据收集管线与优势条件化策略提取组成，在擦白板和面包入烤面包机两个真实任务上验证了迭代改进效果。

## 它改变了什么

人形操作的后训练长期卡在两个互锁的难题上：一是系统层面，全身运动学与灵巧手的耦合让人类干预的采集本身极难工程化；二是算法层面，干预轨迹天然包含犹豫、试探甚至错误，直接当作专家监督会把这些噪声固化进策略。此前的工作要么回避干预、只用干净演示，要么天真地把干预当最优解，导致学到的策略在部署时表现迟疑。

ROVE 真正改变的是「干预数据该被如何使用」这一前提。它不再假设人类接管是专家行为，而是把干预轨迹视为带有噪声的探索信号，通过价值函数区分其中哪些片段值得模仿、哪些应该忽略。这让人形 VLA 的后训练从「模仿人类动作」转向「从人类干预中提取意图」，在数据质量不可控的真实场景下打开了更实用的训练路径。

## 方法拆解

### 数据收集管线：三段式 episode 分解
每个 episode 以自主 VLA rollout 开始，当失败即将发生时，动捕操作员通过全身与灵巧手遥操作接管。episode 被切分为 rollout、adaptation、recovery 三个阶段，价值标签在 adaptation 结束时设置保守失败边界，避免把操作员犹豫的接管动作误标为 recovery 信号。

### 乐观价值估计（OVE）
- Critic 从三类异构经验学习：自主 rollout、人类干预轨迹、跨具身人类经验视频。
- 先在大规模机器人数据与以自我为中心的人类演示上做 Monte-Carlo 回归预训练，再用 OVE 微调。
- OVE 结合 H 步 TD bootstrap 与 expectile 回归，估计分布内乐观统计量——偏好数据中观察到的更好 recovery，而不查询分布外动作。
- 关键设计：学习状态价值函数而非 Q 函数，使 critic 能吸收动作空间不可直接比较的人类视频。

### 优势条件化策略提取
Critic 为动作块分配优势标签，VLA actor 通过优势条件化微调，强调高价值行为而非均匀模仿所有收集动作。这相当于用价值函数做软过滤，比硬性筛选数据更平滑。

## 关键创新

1. **乐观价值估计的分布内设计**：OVE 用 expectile 回归估计乐观统计量，只偏好数据中真实出现过的更好 recovery，不查询分布外动作。这避免了传统 RL 中价值高估导致的策略漂移，同时保留了对次优干预的纠偏能力。

2. **状态价值函数而非 Q 函数**：这是吸收跨具身人类视频的关键决策。Q 函数需要动作空间对齐，而人形机器人与人类视频的动作维度不可直接比较；状态价值函数绕开了这一限制，让 critic 能从更广泛的数据源学习任务进展。

3. **优势条件化替代数据过滤**：Filtered BC 类方法先过滤再模仿，ROVE 则让策略在训练中根据优势标签动态加权。这保留了负样本的信息，让策略知道「什么不该做」，而不仅仅是「什么该做」。

## 实验与结果

### 任务与基线
两个真实世界任务：Erase the whiteboard（接触丰富）和 Put the bread into the toaster（精细操作）。基线包括 HG-DAgger、Filtered BC、RECAP，以及仅演示训练的 SFT。

### 关键结果
| 对比项 | 结果 |
|--------|------|
| ROVE vs SFT | 两个任务均优于仅演示训练 |
| ROVE vs HG-DAgger | HG-DAgger 在一个任务上低于仅演示基线，策略常表现犹豫 |
| ROVE vs RECAP | 差距反映 critic 质量与后适应干预段优势分配的综合效应 |
| ROVE vs Filtered BC | 表明 RL 式策略学习在 BC 式数据过滤之外提供额外增益 |
| 迭代改进 | 两个任务均在三轮迭代中持续改进 |
| 价值估计 | 加入人类经验后 critic 对不完整擦除状态分配更低价值；OVE 比 MC 估计产生更尖锐的价值曲线 |

### 部署行为
面包未对准槽口时策略后退重试直至插入成功；擦除不完整时重新接触并清除剩余痕迹。这些行为在仅演示策略中很少出现。额外完成螺丝安装任务，要求毫米级精度。

论文未明确各基线具体成功率数值、消融定量表格、推理频率与训练配置。

## 边界与局限

论文未明确列出局限性章节。未提供消融实验的定量表格，未报告各基线具体成功率数值，未披露推理频率、训练配置、硬件平台等工程细节。OVE 的 H 步 TD bootstrap 中 H 的具体取值、expectile 回归的 tau 参数、预训练数据规模均未公开。跨具身人类视频的采集标准与质量筛选方式也未说明。三轮迭代后的收敛行为未知，是否继续改进或过拟合干预噪声无法从现有信息判断。

## 工程启示

复现 ROVE 时先核对三件事：一是动捕遥操作的延迟与接管判定逻辑，这直接决定 adaptation 阶段的价值标签质量；二是 critic 预训练数据中人类视频与机器人数据的比例，跨具身数据的分布偏移可能让 OVE 的乐观估计失真；三是 expectile 回归的 tau 参数，它控制乐观程度，过大会导致价值高估，过小则退化为均值回归。

最容易踩坑的地方在于优势条件化微调时动作块的切分粒度——块太粗会把 recovery 与 rollout 混在一起，块太细则优势信号噪声过大。建议先在小规模数据上验证价值曲线的尖锐度，再上全量训练。另外，HG-DAgger 的失败提示了干预数据直接作为专家监督的危险性，如果你的管线里操作员接管频繁且犹豫，ROVE 的价值函数设计比任何数据清洗都更重要。

## 参考

- ROVE project page (https://xpeng-robotics.github.io/rove)

## Overview

Humanoid robots face a key challenge when performing dexterous manipulation tasks in complex environments: how to effectively combine high-level semantic understanding with low-level motion control. Existing methods often exhibit a gap between the language planning of Vision-Language Models (VLMs) and action execution, making it difficult to achieve an end-to-end closed loop from perception to action. ROVE proposes a reinforcement learning-based framework that encodes language instructions, multi-view visual observations, proprioceptive states, and joint sequences into a unified multimodal representation. By integrating PPO reinforcement learning strategies, diffusion policies, and VLA multimodal action models, it effectively utilizes human intervention signals. The core contribution of this work lies in preserving the semantic understanding capability of VLMs while introducing robot state and action heads, breaking through the limitation of traditional methods that remain at the level of language planning.

## Content

**Research Background and Problem**: The general manipulation capability of humanoid robots is a key step toward autonomous agents. However, existing methods face significant bottlenecks when handling complex, unstructured tasks. On one hand, methods based on Vision-Language Models excel at semantic understanding and task planning, but the generated plans often lack consideration of robot body dynamics and real-time environmental feedback, making it difficult to directly map them into precise joint-level action sequences. On the other hand, while pure reinforcement learning methods can learn robust control policies through trial and error, they suffer from low training efficiency in high-dimensional state-action spaces and struggle to incorporate human prior knowledge. How to organically combine the semantic reasoning advantages of VLMs with the action optimization capabilities of reinforcement learning, while allowing effective human intervention during training and execution, has become a core problem to be solved in this field.

**Method or Model Framework**: ROVE constructs a joint framework for multimodal representation learning and policy optimization. Its input layer encodes language instructions, multi-view camera images, robot proprioceptive states (such as joint angles and end-effector poses), and historical joint sequences into a unified multimodal embedding space. During the policy learning phase, the framework adopts two parallel technical routes: one is reinforcement learning training based on Proximal Policy Optimization (PPO), which continuously optimizes the action policy through interaction with the environment; the other uses diffusion policies or flow matching methods to generate smooth, continuous action trajectories. Finally, the VLA (Vision-Language-Action) multimodal action model fuses the outputs of the above two strategies to predict directly executable action commands. The key to this framework lies in designing a shared semantic-motion representation layer, enabling the semantic features of the VLM to synergize with robot state features in the action space.

**Key Technical Innovations**: The core innovation of ROVE is reflected in the design philosophy of "preserving semantic understanding while adding state-action heads." Traditional VLA methods often directly use the output of the VLM as action commands, resulting in models that can only perform language-level planning without adapting to the dynamic changes of the physical world. By appending a dedicated robot state encoder and action decoding head to the VLM, ROVE enables the model to perceive its own state and generate actions that comply with kinematic and dynamic constraints while maintaining its original semantic understanding capability. Additionally, the introduced human intervention mechanism allows operators to guide policy exploration during training through demonstrations or correction signals, thereby accelerating convergence and improving policy safety. This design allows ROVE to achieve a seamless transition from "understanding" to "execution" without sacrificing language generalization ability.

**Experiments/Validation and Application Value**: Although the paper does not provide specific experimental data, it can be inferred from the framework design that ROVE is suitable for scenarios requiring a combination of fine manipulation and semantic understanding, such as object grasping, tool use, and assembly tasks. By integrating reinforcement learning and diffusion policies under multi-view observations, this method is expected to achieve higher task success rates and sample efficiency in both simulation environments and real robot platforms compared to pure VLM planning or pure RL methods. Its application value lies in providing a scalable general manipulation paradigm for humanoid robots: on one hand, the semantic understanding capability of VLMs enables robots to comprehend natural language instructions and adapt to new tasks; on the other hand, the combination of reinforcement learning and human intervention ensures the physical feasibility and robustness of actions. This framework offers an important technical pathway for the future deployment of humanoid robots in domains such as home service and industrial collaboration.

## 개요

휴머노이드 로봇이 복잡한 환경에서 정밀한 조작 작업을 수행할 때, 고수준의 의미 이해와 저수준의 운동 제어를 효과적으로 결합하는 방법이 핵심 과제로 대두되고 있다. 기존 방법들은 시각-언어 모델(VLM)의 언어 계획과 동작 실행 사이에 괴리가 존재하여, 인식에서 행동으로 이어지는 종단 간 폐쇄 루프를 구현하기 어렵다. ROVE는 강화 학습 기반 프레임워크를 제안하며, 언어 명령, 다중 시점 시각 관측, 본체 상태 및 관절 시퀀스를 통합된 다중 모달 표현으로 인코딩하고, PPO 강화 학습 전략, 확산 전략 및 VLA 다중 모델 동작 모델을 융합하여 인간의 개입 신호를 효과적으로 활용한다. 이 연구의 핵심 기여는 VLM의 의미 이해 능력을 유지하면서 로봇 상태와 동작 헤드를 도입하여, 기존 방법이 언어 계획 수준에 머무르는 한계를 돌파한 점에 있다.

## 핵심 내용

**연구 배경 및 문제점**: 휴머노이드 로봇의 범용 조작 능력은 자율 에이전트로 나아가는 핵심 요소이지만, 기존 방법들은 복잡하고 비구조화된 작업을 처리할 때 현저한 병목 현상에 직면한다. 한편, 시각-언어 모델 기반 방법은 의미 이해와 작업 계획에 뛰어나지만, 생성된 계획은 종종 로봇 본체의 동역학 및 실시간 환경 피드백을 고려하지 않아 정밀한 관절 수준의 동작 시퀀스로 직접 매핑하기 어렵다. 다른 한편, 순수 강화 학습 방법은 시행착오를 통해 강건한 제어 전략을 학습할 수 있지만, 고차원 상태-동작 공간에서 훈련 효율이 낮고 인간의 사전 지식을 통합하기 어렵다. VLM의 의미 추론 장점과 강화 학습의 동작 최적화 능력을 유기적으로 결합하면서, 훈련 및 실행 과정에서 인간이 효과적으로 개입할 수 있도록 하는 것이 이 분야의 핵심 과제로 남아 있다.

**방법 또는 모델 프레임워크**: ROVE는 다중 모달 표현 학습과 전략 최적화를 결합한 통합 프레임워크를 구축한다. 입력 계층은 언어 명령, 다중 시점 카메라 이미지, 로봇 본체 상태(관절 각도, 말단 효과기 자세 등) 및 과거 관절 시퀀스를 인코딩하여 통합된 다중 모달 임베딩 공간을 형성한다. 전략 학습 단계에서는 프레임워크가 두 가지 기술 경로를 병렬로 사용한다. 첫째, 근접 정책 최적화(PPO) 기반 강화 학습 훈련을 통해 환경과 상호작용하며 동작 전략을 지속적으로 최적화한다. 둘째, 확산 전략 또는 흐름 매칭 방법을 활용하여 부드럽고 연속적인 동작 궤적을 생성한다. 최종적으로 VLA(시각-언어-동작) 다중 모달 동작 모델이 위 두 전략의 출력을 융합하여 직접 실행 가능한 동작 명령을 예측한다. 이 프레임워크의 핵심은 공유된 의미-운동 표현 계층을 설계하여 VLM의 의미 특징이 로봇 상태 특징과 동작 공간에서 협력적으로 작용할 수 있도록 하는 데 있다.

**핵심 기술 혁신**: ROVE의 핵심 혁신은 "의미 이해를 유지하고 상태-동작 헤드를 추가"하는 설계 철학에 있다. 기존 VLA 방법은 종종 VLM의 출력을 직접 동작 명령으로 사용하여, 모델이 언어 수준의 계획만 수행할 뿐 물리적 세계의 동적 변화에 적응하지 못하는 한계를 보였다. ROVE는 VLM에 전용 로봇 상태 인코더와 동작 디코딩 헤드를 추가함으로써, 모델이 기존의 의미 이해 능력을 유지하면서 본체 상태를 인식하고 운동학 및 동역학 제약 조건을 충족하는 동작을 생성할 수 있도록 한다. 또한, 프레임워크에 도입된 인간 개입 메커니즘은 작업자가 훈련 과정에서 시범 또는 교정 신호를 통해 전략 탐색을 유도하여, 수렴을 가속화하고 전략의 안전성을 향상시킨다. 이러한 설계는 ROVE가 언어 일반화 능력을 희생하지 않으면서 "이해"에서 "실행"으로의 원활한 연결을 구현할 수 있게 한다.

**실험/검증 및 응용 가치**: 논문이 구체적인 실험 데이터를 제공하지는 않았지만, 프레임워크 설계로 미루어 보아 ROVE는 정밀 조작과 의미 이해가 결합된 시나리오, 예를 들어 물체 잡기, 도구 사용, 조립 작업 등에 적합할 것으로 추정된다. 다중 시점 관측 하에서 강화 학습과 확산 전략을 융합함으로써, 이 방법은 시뮬레이션 환경과 실제 로봇 플랫폼에서 순수 VLM 계획 또는 순수 RL 방법보다 더 높은 작업 성공률과 샘플 효율을 보일 것으로 기대된다. 그 응용 가치는 휴머노이드 로봇에 확장 가능한 범용 조작 패러다임을 제공하는 데 있다. 한편으로 VLM의 의미 이해 능력은 로봇이 자연어 명령을 이해하고 새로운 작업에 적응할 수 있게 하며, 다른 한편으로 강화 학습과 인간 개입의 결합은 동작의 물리적 실현 가능성과 강건성을 보장한다. 이 프레임워크는 향후 휴머노이드 로봇이 가사 서비스, 산업 협력 등 분야에 배치되는 데 중요한 기술적 경로를 제시한다.
