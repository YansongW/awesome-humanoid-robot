---
$id: ent_paper_ho_denoising_diffusion_probabilis_2020
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Denoising Diffusion Probabilistic Models
  zh: 去噪扩散概率模型
  ko: 노이즈 제거 확산 확률 모델
summary:
  en: Ho, Jain, and Abbeel (2020) introduce a denoising diffusion probabilistic model trained with a simplified weighted variational
    bound, achieving high-quality image synthesis on CIFAR-10 and LSUN datasets.
  zh: Ho, Jain 与 Abbeel 于 2020 年提出去噪扩散概率模型，通过简化加权变分界进行训练，在 CIFAR-10 与 LSUN 数据集上实现了高质量图像合成。该模型受非平衡热力学启发，并建立了扩散模型与去噪得分匹配之间的新联系。
  ko: Ho, Jain, Abbeel(2020)는 단순화된 가중 변분 하한으로 학습된 노이즈 제거 확산 확률 모델을 제안하여 CIFAR-10 및 LSUN 데이터셋에서 고품질 이미지 합성을 달성했다.
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- diffusion_model
- generative_model
- denoising
- image_synthesis
- synthetic_data
- score_matching
- langevin_dynamics
- u_net
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2006.11239v2. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: Denoising Diffusion Probabilistic Models
  url: https://arxiv.org/abs/2006.11239
  date: '2020'
  accessed_at: '2026-06-25'
theoretical_depth:
- system
---



## 概述

本文由 Jonathan Ho、Ajay Jain 和 Pieter Abbeel 提出，首次证明去噪扩散概率模型（DDPM）能够生成与 GAN 相当的高质量图像，并建立了扩散模型与去噪得分匹配、退火朗之万动力学及自回归解码之间的理论联系。核心贡献在于通过预测噪声的均值参数化与简化训练目标，将扩散模型的 FID 从 25.32（NCSN）降至 3.17（CIFAR10 无条件生成）。

## 它改变了什么

在 DDPM 之前，扩散模型在生成质量上远逊于 GAN 和自回归模型，NCSN 虽展示了得分匹配的潜力，但其采样器系数是训练后手工设定的，训练过程不直接优化采样质量，且前向过程缺乏与先验的紧密对齐。本文真正改变的是将采样器本身作为潜变量模型直接训练——通过变分下界将 Langevin 式采样器的每一步系数从前向过程的 β_t 严格推导出来，而非事后调整。这使得扩散模型首次在 FID 上超越当时最强的 GAN 变体（StyleGAN2 + ADA 的 FID 3.26 vs 本文 3.17），并揭示了高斯扩散可被视为一种广义比特排序的自回归解码，为生成模型提供了新的理论视角。

## 方法拆解

### 模型定义
- 潜变量模型 pθ(x0) ≔ ∫ pθ(x0:T) dx1:T，x1,…,xT 与数据 x0 同维度。
- 前向过程固定为马尔可夫链：q(xt|xt−1) ≔ N(xt; √(1−βt) xt−1, βt I)，βt 线性从 10⁻⁴ 增至 0.02，T=1000。
- 反向过程为可学习马尔可夫链：pθ(xt−1|xt) ≔ N(xt−1; μθ(xt, t), Σθ(xt, t))。

### 关键设计决策
1. **均值参数化**：不直接预测后验均值 μ̃t，而是预测噪声 εθ(xt, t)，即 μθ = (1/√αt)(xt − (βt/√(1−ᾱt)) εθ)。理由是与朗之万动力学形式相似，且简化变分界。
2. **简化训练目标**：Lsimple(θ) ≔ Et,x0,ε[‖ε − εθ(√ᾱt x0 + √(1−ᾱt) ε, t)‖²]。丢弃变分界的逐项权重，下加权小 t 项，迫使网络聚焦大 t 的更难去噪任务。
3. **方差固定**：反向过程方差 σt² 设为 βt 或 β̃t（未训练），两者结果相似。
4. **数据缩放**：图像整数 {0,…,255} 线性缩放到 [−1,1]，L0 用独立离散解码器计算。

### 采样（Algorithm 2）
xt−1 = (1/√αt)(xt − (1−αt)/√(1−ᾱt) εθ(xt, t)) + σt z，t>1 时 z~N(0,I)，t=1 时 z=0。

### 架构
U-Net 骨干（基于 PixelCNN++），组归一化，Transformer 正弦位置嵌入注入时间 t，16×16 分辨率处使用自注意力。CIFAR10 模型 35.7M 参数，LSUN/CelebA-HQ 模型 114M 参数。

## 关键创新

1. **预测噪声而非后验均值**：这是与先前扩散模型工作的根本区别。预测 ε 使训练目标与去噪得分匹配等价，且采样公式与朗之万动力学形式一致，从而将三个独立研究线（扩散模型、得分匹配、朗之万采样）统一到一个框架下。
2. **简化加权目标**：丢弃变分界的理论权重，看似"不严谨"，却将 FID 从 13.51（完整 L）提升到 3.17。这揭示了一个重要原则：生成质量与似然并非单调对应，聚焦困难去噪任务（大 t）比精确优化似然更有效。
3. **前向过程缩放因子**：每一步按 √(1−βt) 缩放数据，保持方差稳定，确保 DKL(q(xT|x0) ∥ N(0,I)) ≈ 10⁻⁵ bits/dim，使先验与聚合后验紧密匹配，防止采样分布偏移——这是 NCSN 缺失的关键性质。

## 实验与结果

### CIFAR10 无条件生成（Table 1）
| 方法 | IS | FID | NLL (bits/dim) |
|---|---|---|---|
| Ours (Lsimple) | 9.46 ± 0.11 | 3.17 | ≤3.75 |
| Ours (L, 固定各向同性 Σ) | 7.67 ± 0.13 | 13.51 | ≤3.70 |
| NCSN | 8.87 ± 0.12 | 25.32 | - |
| StyleGAN2 + ADA (v1) | 9.74 ± 0.05 | 3.26 | - |
| Gated PixelCNN | 4.60 | 65.93 | 3.03 |

### 消融（Table 2）
| 参数化 | 目标 | Σ | IS | FID |
|---|---|---|---|---|
| μ̃ 预测 | L | 学习对角 | 7.28 | 23.69 |
| μ̃ 预测 | L | 固定各向同性 | 8.06 | 13.22 |
| ε 预测 | L | 固定各向同性 | 7.67 | 13.51 |
| ε 预测 | Lsimple | 固定各向同性 | **9.46** | **3.17** |

### LSUN 256×256（Table 3）
| 数据集 | ProgressiveGAN | StyleGAN2 | Ours (Lsimple) |
|---|---|---|---|
| Bedroom | 8.34 | 2.65 | 4.90 (large) |
| Church | 6.42 | 3.86 | 7.89 |
| Cat | 37.52 | 6.93 | 19.75 |

### 渐进压缩（Section 4.3）
最高质量 CIFAR10 模型：rate = 1.78 bits/dim，distortion = 1.97 bits/dim（RMSE 0.95）。反向过程前 100 步消耗 1.78 bits/dim 中的绝大部分，后 900 步仅贡献 0.004 bits/dim，证实多数比特用于不可感知细节。

### 训练/推理成本
CIFAR10 训练 10.6 小时（TPU v3-8，800k 步），采样 256 张需 17 秒；LSUN 256² 采样 128 张需 300 秒。推理需 T=1000 次网络评估。

## 边界与局限

- 对数似然不与其他基于似然的模型竞争（CIFAR10 NLL 3.75 vs Sparse Transformer 2.80），仅优于能量模型和得分匹配估计。
- 渐进压缩仅为概念验证，依赖最小随机编码，对高维数据不可行，未成为实用压缩系统。
- 未在 ImageNet 等更大规模、更多类别数据集上验证；未测试非图像模态。
- 解码器未使用更强大的条件自回归模型，作者明确留作未来工作。
- 论文未提及对恶意使用（如伪造图像）的缓解措施，仅指出 CNN 生成图像有可检测缺陷。
- 预测 x0 的选项在早期实验中导致更差样本质量，作者未采用。

## 工程启示

复现时优先核对以下三点：**第一**，β_t 调度必须保证 LT ≈ 0（线性 10⁻⁴→0.02 即可），否则先验与聚合后验不匹配会导致采样偏移；**第二**，务必使用 ε 预测 + Lsimple 组合，任何"改进"为完整变分界或学习方差的尝试都会使 FID 恶化 4-10 倍（Table 2 已证实）；**第三**，EMA 衰减 0.9999 和 Adam 学习率 2×10⁻⁴ 是默认安全选择，但 256×256 图像需降至 2×10⁻⁵。最容易踩坑的是 dropout——CIFAR10 需设 0.1，其他数据集设 0，不可统一。推理时 T=1000 步是质量与速度的平衡点，若需加速可尝试缩短 T，但需重新扫描 β_t。LSUN Cat 的 FID 19.75 明显差于 Bedroom 的 4.90，说明该方法对类别内方差大的数据集仍需调参。

## 参考
- http://arxiv.org/abs/2006.11239v2

## Overview

This paper, authored by Jonathan Ho, Ajay Jain, and Pieter Abbeel, is the first to demonstrate that Denoising Diffusion Probabilistic Models (DDPM) can generate high-quality images comparable to GANs, establishing theoretical connections between diffusion models and denoising score matching, annealed Langevin dynamics, and autoregressive decoding. The core contribution lies in the mean parameterization via noise prediction and a simplified training objective, reducing the FID of diffusion models from 25.32 (NCSN) to 3.17 (CIFAR10 unconditional generation).

## What It Changed

Before DDPM, diffusion models lagged far behind GANs and autoregressive models in generation quality. While NCSN demonstrated the potential of score matching, its sampler coefficients were manually set after training, the training process did not directly optimize sampling quality, and the forward process lacked tight alignment with the prior. What this paper truly changed was training the sampler itself directly as a latent variable model—strictly deriving each step's coefficients of the Langevin-style sampler from the forward process's β_t via the variational lower bound, rather than adjusting them post hoc. This allowed diffusion models to surpass the strongest GAN variants of the time on FID for the first time (StyleGAN2 + ADA's FID 3.26 vs. this paper's 3.17), and revealed that Gaussian diffusion can be viewed as a form of generalized bit-order autoregressive decoding, offering a new theoretical perspective for generative models.

## Method Breakdown

### Model Definition
- Latent variable model pθ(x0) ≔ ∫ pθ(x0:T) dx1:T, where x1,…,xT share the same dimensionality as the data x0.
- The forward process is a fixed Markov chain: q(xt|xt−1) ≔ N(xt; √(1−βt) xt−1, βt I), with βt increasing linearly from 10⁻⁴ to 0.02, and T=1000.
- The reverse process is a learnable Markov chain: pθ(xt−1|xt) ≔ N(xt−1; μθ(xt, t), Σθ(xt, t)).

### Key Design Decisions
1. **Mean Parameterization**: Instead of directly predicting the posterior mean μ̃t, the model predicts the noise εθ(xt, t), i.e., μθ = (1/√αt)(xt − (βt/√(1−ᾱt)) εθ). The rationale is its similarity to Langevin dynamics and the simplification of the variational bound.
2. **Simplified Training Objective**: Lsimple(θ) ≔ Et,x0,ε[‖ε − εθ(√ᾱt x0 + √(1−ᾱt) ε, t)‖²]. By discarding the per-term weights of the variational bound, the lower-weight terms for small t are down-weighted, forcing the network to focus on the harder denoising tasks at large t.
3. **Fixed Variance**: The reverse process variance σt² is set to βt or β̃t (not trained), with similar results for both.
4. **Data Scaling**: Image integers {0,…,255} are linearly scaled to [−1,1], and L0 is computed using an independent discrete decoder.

### Sampling (Algorithm 2)
xt−1 = (1/√αt)(xt − (1−αt)/√(1−ᾱt) εθ(xt, t)) + σt z, where z~N(0,I) for t>1 and z=0 for t=1.

### Architecture
U-Net backbone (based on PixelCNN++), group normalization, Transformer sinusoidal positional embeddings for time t, and self-attention at 16×16 resolution. The CIFAR10 model has 35.7M parameters, while the LSUN/CelebA-HQ models have 114M parameters.

## Key Innovations

1. **Predicting noise rather than the posterior mean**: This is a fundamental difference from prior diffusion model work. Predicting ε makes the training objective equivalent to denoising score matching, and the sampling formula aligns with the form of Langevin dynamics, thereby unifying three independent research lines (diffusion models, score matching, Langevin sampling) into a single framework.
2. **Simplified weighted objective**: Discarding the theoretical weights of the variational bound, seemingly "not rigorous," improved FID from 13.51 (full L) to 3.17. This reveals an important principle: generation quality and likelihood are not monotonically related; focusing on difficult denoising tasks (large t) is more effective than precisely optimizing likelihood.
3. **Forward process scaling factor**: Each step scales the data by √(1−βt) to keep the variance stable, ensuring DKL(q(xT|x0) ∥ N(0,I)) ≈ 10⁻⁵ bits/dim, so that the prior closely matches the aggregated posterior, preventing sampling distribution shift—a key property missing in NCSN.

## Experiments and Results

### CIFAR10 Unconditional Generation (Table 1)
| Method | IS | FID | NLL (bits/dim) |
|---|---|---|---|
| Ours (Lsimple) | 9.46 ± 0.11 | 3.17 | ≤3.75 |
| Ours (L, fixed isotropic Σ) | 7.67 ± 0.13 | 13.51 | ≤3.70 |
| NCSN | 8.87 ± 0.12 | 25.32 | - |
| StyleGAN2 + ADA (v1) | 9.74 ± 0.05 | 3.26 | - |
| Gated PixelCNN | 4.60 | 65.93 | 3.03 |

### Ablations (Table 2)
| Parameterization | Objective | Σ | IS | FID |
|---|---|---|---|---|
| μ̃ prediction | L | learned diagonal | 7.28 | 23.69 |
| μ̃ prediction | L | fixed isotropic | 8.06 | 13.22 |
| ε prediction | L | fixed isotropic | 7.67 | 13.51 |
| ε prediction | Lsimple | fixed isotropic | **9.46** | **3.17** |

### Progressive Compression (Section 4.3)
Highest-quality CIFAR10 model: rate = 1.78 bits/dim, distortion = 1.97 bits/dim (RMSE 0.95). The first 100 steps of the reverse process consume the vast majority of the 1.78 bits/dim, while the last 900 steps contribute only 0.004 bits/dim, confirming that most bits are used for imperceptible details.

### Training/Inference Cost
CIFAR10 training takes 10.6 hours (TPU v3-8, 800k steps); sampling 256 images takes 17 seconds; LSUN 256² sampling of 128 images takes 300 seconds. Inference requires T=1000 network evaluations.

## Boundaries and Limitations

- Log-likelihood does not compete with other likelihood-based models (CIFAR10 NLL 3.75 vs Sparse Transformer 2.80), only surpassing energy models and score matching estimates.
- Progressive compression is only a proof of concept, relying on minimal random coding, which is infeasible for high-dimensional data and has not become a practical compression system.
- Not validated on larger-scale, more class-diverse datasets such as ImageNet; non-image modalities were not tested.
- The decoder does not use a more powerful conditional autoregressive model; the authors explicitly leave this as future work.
- The paper does not mention mitigation measures for malicious use (e.g., image forgery), only noting that CNN-generated images have detectable artifacts.
- The option of predicting x0 led to worse sample quality in early experiments and was not adopted by the authors.

## Engineering Insights

When reproducing, prioritize checking the following three points: **First**, the β_t schedule must ensure LT ≈ 0 (linear 10⁻⁴→0.02 suffices); otherwise, a mismatch between the prior and the aggregated posterior will cause sampling shift. **Second**, be sure to use the ε prediction + Lsimple combination; any "improvement" to the full variational bound or learned variance will worsen FID by 4–10× (as confirmed in Table 2). **Third**, EMA decay 0.9999 and Adam learning rate 2×10⁻⁴ are the default safe choices, but for 256×256 images, the learning rate must be reduced to 2×10⁻⁵. The easiest pitfall is dropout—CIFAR10 requires 0.1, while other datasets require 0; it cannot be unified. At inference, T=1000 steps is the balance point between quality and speed; if acceleration is needed, one can try shortening T, but the β_t schedule must be re-scanned. LSUN Cat's FID of 19.75 is significantly worse than Bedroom's 4.90, indicating that the method still requires tuning for datasets with high intra-class variance.

## 개요

본 논문은 Jonathan Ho, Ajay Jain, Pieter Abbeel이 제안한 것으로, denoising diffusion probabilistic model(DDPM)이 GAN과 견줄 만한 고품질 이미지를 생성할 수 있음을 처음으로 입증하고, 확산 모델과 denoising score matching, annealed Langevin dynamics, autoregressive decoding 간의 이론적 연관성을 확립했습니다. 핵심 기여는 노이즈 예측을 통한 평균 매개변수화와 단순화된 훈련 목표를 통해 확산 모델의 FID를 25.32(NCSN)에서 3.17(CIFAR10 무조건 생성)로 낮춘 것입니다.

## 무엇을 바꾸었는가

DDPM 이전에는 확산 모델의 생성 품질이 GAN 및 autoregressive 모델에 크게 미치지 못했습니다. NCSN은 score matching의 잠재력을 보여주었지만, 샘플러 계수는 훈련 후 수동으로 설정되었고, 훈련 과정이 샘플링 품질을 직접 최적화하지 않았으며, 전방 과정이 사전 분포와 긴밀하게 정렬되지 않았습니다. 본 논문이 진정으로 바꾼 것은 샘플러 자체를 잠재 변수 모델로 직접 훈련시킨 것입니다. 즉, 변분 하한을 통해 Langevin 방식 샘플러의 각 단계 계수를 전방 과정의 β_t로부터 엄밀하게 유도했지, 사후 조정이 아닙니다. 이를 통해 확산 모델은 처음으로 FID에서 당시 최강의 GAN 변종(StyleGAN2 + ADA의 FID 3.26 대비 본 논문의 3.17)을 능가했으며, 가우시안 확산이 일반화된 비트 정렬의 autoregressive decoding으로 간주될 수 있음을 밝혀 생성 모델에 새로운 이론적 관점을 제시했습니다.

## 방법 분석

### 모델 정의
- 잠재 변수 모델 pθ(x0) ≔ ∫ pθ(x0:T) dx1:T, x1,…,xT는 데이터 x0와 동일한 차원입니다.
- 전방 과정은 고정된 마르코프 체인: q(xt|xt−1) ≔ N(xt; √(1−βt) xt−1, βt I), βt는 10⁻⁴에서 0.02로 선형적으로 증가하며 T=1000입니다.
- 역방향 과정은 학습 가능한 마르코프 체인: pθ(xt−1|xt) ≔ N(xt−1; μθ(xt, t), Σθ(xt, t))입니다.

### 핵심 설계 결정
1. **평균 매개변수화**: 사후 평균 μ̃t를 직접 예측하지 않고 노이즈 εθ(xt, t)를 예측합니다. 즉, μθ = (1/√αt)(xt − (βt/√(1−ᾱt)) εθ)입니다. 그 이유는 Langevin dynamics 형태와 유사하고 변분 하한을 단순화하기 때문입니다.
2. **단순화된 훈련 목표**: Lsimple(θ) ≔ Et,x0,ε[‖ε − εθ(√ᾱt x0 + √(1−ᾱt) ε, t)‖²]. 변분 하한의 항별 가중치를 버리고 작은 t 항의 가중치를 낮추어 네트워크가 큰 t의 더 어려운 denoising 작업에 집중하도록 강제합니다.
3. **분산 고정**: 역방향 과정 분산 σt²는 βt 또는 β̃t로 설정되며(훈련되지 않음), 두 경우 결과가 유사합니다.
4. **데이터 스케일링**: 이미지 정수 {0,…,255}를 [−1,1]로 선형 스케일링하고, L0는 독립적인 이산 디코더로 계산합니다.

### 샘플링 (Algorithm 2)
xt−1 = (1/√αt)(xt − (1−αt)/√(1−ᾱt) εθ(xt, t)) + σt z, t>1일 때 z~N(0,I), t=1일 때 z=0.

### 아키텍처
U-Net 백본(PixelCNN++ 기반), group normalization, Transformer sinusoidal position embedding으로 시간 t 주입, 16×16 해상도에서 self-attention 사용. CIFAR10 모델은 35.7M 파라미터, LSUN/CelebA-HQ 모델은 114M 파라미터입니다.

## 핵심 혁신

1. **사후 평균이 아닌 노이즈 예측**: 이전 확산 모델 연구와의 근본적인 차이점입니다. ε을 예측하면 훈련 목표가 denoising score matching과 동등해지고, 샘플링 공식이 Langevin dynamics 형태와 일치하여 세 개의 독립적인 연구 계열(확산 모델, score matching, Langevin 샘플링)을 하나의 프레임워크로 통합합니다.
2. **단순화된 가중치 목표**: 변분 하한의 이론적 가중치를 버리는 것은 겉보기에 "엄밀하지 않아" 보이지만, FID를 13.51(전체 L)에서 3.17로 향상시킵니다. 이는 생성 품질과 우도가 단조적으로 대응하지 않으며, 어려운 denoising 작업(큰 t)에 집중하는 것이 우도를 정밀하게 최적화하는 것보다 더 효과적이라는 중요한 원칙을 드러냅니다.
3. **전방 과정 스케일링 인자**: 각 단계에서 데이터를 √(1−βt)로 스케일링하여 분산을 안정적으로 유지하고, DKL(q(xT|x0) ∥ N(0,I)) ≈ 10⁻⁵ bits/dim을 보장하여 사전 분포와 집계된 사후 분포가 긴밀하게 일치하도록 합니다. 이는 샘플링 분포의 이동을 방지하며, NCSN이 갖추지 못한 핵심 속성입니다.

## 실험 및 결과

### CIFAR10 무조건 생성 (Table 1)
| 방법 | IS | FID | NLL (bits/dim) |
|---|---|---|---|
| Ours (Lsimple) | 9.46 ± 0.11 | 3.17 | ≤3.75 |
| Ours (L, 고정 등방성 Σ) | 7.67 ± 0.13 | 13.51 | ≤3.70 |
| NCSN | 8.87 ± 0.12 | 25.32 | - |
| StyleGAN2 + ADA (v1) | 9.74 ± 0.05 | 3.26 | - |
| Gated PixelCNN | 4.60 | 65.93 | 3.03 |

### 절제 연구 (Table 2)
| 매개변수화 | 목표 | Σ | IS | FID |
|---|---|---|---|---|
| μ̃ 예측 | L | 학습 대각 | 7.28 | 23.69 |
| μ̃ 예측 | L | 고정 등방성 | 8.06 | 13.22 |
| ε 예측 | L | 고정 등방성 | 7.67 | 13.51 |
| ε 예측 | Lsimple | 고정 등방성 | **9.46** | **3.17** |

### 점진적 압축 (Section 4.3)
최고 품질 CIFAR10 모델: rate = 1.78 bits/dim, distortion = 1.97 bits/dim (RMSE 0.95). 역방향 과정의 처음 100단계가 1.78 bits/dim의 대부분을 소비하고, 이후 900단계는 0.004 bits/dim만 기여하여 대부분의 비트가 인지 불가능한 세부 사항에 사용됨을 확인했습니다.

### 훈련/추론 비용
CIFAR10 훈련 10.6시간(TPU v3-8, 800k 스텝), 256장 샘플링에 17초; LSUN 256² 128장 샘플링에 300초. 추론에는 T=1000회의 네트워크 평가가 필요합니다.

## 경계 및 한계

- 로그 우도는 다른 우도 기반 모델과 경쟁하지 않습니다(CIFAR10 NLL 3.75 vs Sparse Transformer 2.80), 에너지 모델 및 score matching 추정보다만 우수합니다.
- 점진적 압축은 개념 증명에 불과하며, 최소 랜덤 코딩에 의존하여 고차원 데이터에는 비현실적이며 실용적인 압축 시스템이 되지 못했습니다.
- ImageNet과 같은 더 큰 규모, 더 많은 클래스의 데이터셋에서 검증되지 않았습니다; 비이미지 양식은 테스트되지 않았습니다.
- 디코더는 더 강력한 조건부 autoregressive 모델을 사용하지 않았으며, 저자는 이를 명시적으로 향후 작업으로 남겼습니다.
- 논문은 악의적 사용(예: 이미지 위조)에 대한 완화 조치를 언급하지 않았으며, CNN 생성 이미지에 감지 가능한 결함이 있다는 점만 지적했습니다.
- x0 예측 옵션은 초기 실험에서 더 나쁜 샘플 품질을 초래하여 저자가 채택하지 않았습니다.

## 엔지니어링 시사점

재현 시 다음 세 가지를 우선적으로 확인하십시오: **첫째**, β_t 스케줄은 LT ≈ 0을 보장해야 합니다(선형 10⁻⁴→0.02로 충분). 그렇지 않으면 사전 분포와 집계된 사후 분포의 불일치로 샘플링 이동이 발생합니다; **둘째**, 반드시 ε 예측 + Lsimple 조합을 사용하십시오. 완전한 변분 하한이나 학습 분산으로의 어떤 "개선" 시도도 FID를 4-10배 악화시킵니다(Table 2에서 확인됨); **셋째**, EMA 감쇠 0.9999와 Adam 학습률 2×10⁻⁴이 기본 안전 선택이지만, 256×256 이미지에서는 2×10⁻⁵로 낮춰야 합니다. 가장 함정에 빠지기 쉬운 것은 dropout입니다. CIFAR10에서는 0.1로 설정해야 하지만 다른 데이터셋에서는 0으로 설정해야 하며, 일괄 적용할 수 없습니다. 추론 시 T=1000단계는 품질과 속도의 균형점이며, 가속이 필요하면 T를 줄일 수 있지만 β_t를 다시 스캔해야 합니다. LSUN Cat의 FID 19.75는 Bedroom의 4.90보다 현저히 나빠, 이 방법이 클래스 내 분산이 큰 데이터셋에서는 여전히 하이퍼파라미터 튜닝이 필요함을 시사합니다.
