---
$id: ent_paper_gmt_general_motion_tracking_fo_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'GMT: General Motion Tracking for Humanoid Whole-Body Control'
  zh: GMT｜用于人形全身控制的通用运动跟踪
  ko: 'GMT: General Motion Tracking for Humanoid Whole-Body Control'
summary:
  en: GMT transforms ontology state and joint sequence, simulation interaction data into trackable body targets, and trains
    or combines whole-body strategies through diffusion strategy/flow matching, hierarchical skill/expert strategy, and multi-body
    strategy training, finally, the whole-body trajectory/action sequence, low-level controller target are output. The key
    point is to treat action generation as a conditional generation problem, and use diffusion or flow matching to sample
    executable trajectories in multimodal action distributions.
  zh: GMT 把本体状态与关节序列、仿真交互数据转成可跟踪的身体目标，并通过扩散策略/流匹配、分层技能/专家策略训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: GMT는 본체 상태와 관절 시퀀스, 시뮬레이션 상호작용 데이터를 추적 가능한 신체 목표로 변환하고, 확산 정책/플로우 매칭, 계층적 스킬/전문가 정책 훈련 또는 전신 정책 조합을 통해 최종적으로 전신 궤적/동작
    시퀀스, 저수준 제어기 목표를 출력한다. 핵심은 동작 생성을 조건부 생성 문제로 간주하고, 확산 또는 플로우 매칭을 사용하여 다중 모드 동작 분포에서 실행 가능한 궤적을 샘플링하는 것이다.
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- gmt
- locomotion
- motion_tracking
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: GMT project page
  url: https://gmt-humanoid.github.io
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

GMT 是一个面向真实世界人形机器人全身运动跟踪的统一策略框架，由作者团队提出，核心贡献在于用单一策略覆盖多样化运动谱系。其方法结合自适应采样与运动混合专家架构，旨在解决运动多样性、上下半身协调及策略表达能力不足等挑战，并已在仿真与真实世界验证。

## 它改变了什么

现有工作如 HumanPlus、OmniH2O 聚焦操作任务，下半身运动能力受限；ExBody2 依赖小规模手工筛选数据且需按类别微调；ASAP 虽解决 sim-to-real 差距，但每个单一运动需训练独立策略。这些方案本质上都是“窄场景专用”，无法支撑通用型人形机器人的核心需求——即一个策略在真实世界中连续跟踪风格迥异的全身动作。

GMT 真正改变的，是把“运动跟踪”从“任务定制”推向“通用能力”。它不再为每个动作类别单独训练、单独调参，而是通过训练机制与架构设计，让单一策略在运动流形的不同区域都能保持高保真表现。这意味着下游应用从“选模型”变成“用模型”，对系统集成与部署范式有实质影响。

## 方法拆解

### 自适应采样策略
- 在训练过程中动态调整简单与困难运动的采样比例，避免策略偏向易学样本。
- 关键设计：自动识别困难运动并提高其采样权重，使策略在训练后期仍能持续优化高难度动作，而非过早收敛于“平均表现”。

### 运动混合专家架构
- 采用 MoE 结构，让不同专家网络负责运动流形的不同区域，提升策略对多样运动的表达能力。
- 关键设计：通过门控机制实现运动类别间的软分工，既保留共享表征，又允许局部专业化，避免单一网络容量不足或相互干扰。

### 训练与部署流程
- 训练单一统一策略，无需针对运动类别微调。
- 训练完成后直接部署至真实人形机器人，执行全身运动跟踪。
- 训练配置（优化器、学习率、步数）、数据规模、推理频率、具体硬件型号：论文未明确。

## 关键创新

1. **自适应采样策略**：现有方法多采用均匀采样或人工加权，GMT 让训练过程自动平衡难易样本，这是对课程学习思想的在线化改造，直接缓解了“简单运动主导梯度、困难运动学不动”的常见问题。
2. **运动 MoE 架构**：将 MoE 引入全身运动跟踪，让不同专家自然分化到不同运动流形区域。相比单一网络，它在不增加推理负担的前提下显著提升策略容量，且门控机制可解释性强，便于诊断失败模式。
3. **统一策略真实部署**：不依赖运动类别微调，直接覆盖长技能序列、敏捷技能、舞蹈、风格化运动等广泛谱系。这是从“专用策略集合”到“通用策略”的范式转变，对真实机器人长期运行更具实用价值。

## 实验与结果

实验在仿真与真实世界中进行，验证 GMT 在广泛运动谱系上的跟踪效果，声称达到 state-of-the-art 性能。演示任务包括长技能序列、功夫、空中踢、足球射门、舞蹈、蹲走、拳击、醉酒行走、旋转、投掷、风格化行走等。

| 指标 | 数值 | 说明 |
|------|------|------|
| 基线对比量化结果 | 论文未明确 | 未提供成功率、误差等数值 |
| 参数数量 | 论文未明确 | 未提供模型规模 |
| 训练数据规模 | 论文未明确 | 未提供数据量 |
| 推理频率 | 论文未明确 | 未提供控制频率 |

结果的含义：定性演示覆盖了从敏捷技能到风格化运动的广泛谱系，且单一策略无需微调，这验证了框架的通用性。但缺乏量化对比，无法判断与基线在具体指标上的差距。

## 边界与局限

- 作者观察到同一运动在不同视频中表现不一致，归因于硬件磨损：机器人较新时行为接近仿真，持续使用后性能下降。论文未提及对硬件磨损的补偿机制。
- 未涉及多机器人平台泛化，即框架是否适用于不同型号的人形机器人。
- 未讨论长期部署稳定性，如策略在数小时连续运行后的表现衰减。
- 训练数据规模、仿真环境细节、硬件传感器配置：论文未明确。

## 工程启示

复现 GMT 时，优先核对以下环节：
- **自适应采样的实现细节**：论文未明确采样权重更新公式与频率，这是影响训练稳定性的关键，建议先在小规模运动集上验证其收敛行为。
- **MoE 的门控与专家数量**：论文未明确专家数量与门控网络结构，需根据运动谱系宽度自行调参，注意避免专家坍缩（即多个专家收敛到相同功能）。
- **硬件磨损问题**：作者已指出这是真实部署的主要风险，建议在训练时加入域随机化（如关节阻尼、摩擦系数扰动），或设计在线自适应机制，否则长期运行性能会明显下降。
- **量化评估缺失**：论文未提供基线对比数字，复现时需自行建立评估协议（如跟踪误差、成功率），否则无法判断改进幅度。

## 参考

- GMT project page (https://gmt-humanoid.github.io)

## Overview

Whole-body control of humanoid robots has long faced a core challenge: how to transform high-dimensional perception states and joint sequences into stable, executable motion trajectories, especially when dealing with multimodal action distributions. Existing methods often rely on handcrafted motion templates or task-specific controllers, lacking generalization across diverse motion patterns. To address this, this paper proposes the General Motion Tracking (GMT) framework, which redefines action generation as a conditional generation problem. By leveraging diffusion policy and flow matching techniques, it samples executable trajectories from multimodal action distributions, thereby achieving unified tracking and control of whole-body motion. The main contribution of this work lies in proposing an end-to-end, composable whole-body control paradigm, providing key technical support for building behavioral foundation models.

## Content

### Research Background and Problem

Motion control of humanoid robots involves multiple subtasks such as balancing, walking, and manipulation. Traditional methods typically design separate controllers for each task, leading to system complexity and poor transferability. In recent years, approaches based on reinforcement learning and imitation learning have made progress, but they still suffer from low sampling efficiency and incoherent trajectories when handling multimodal action distributions (e.g., multiple possible joint configurations under the same gait). Moreover, the mapping between proprioceptive states and joint sequences is often nonlinear and limited by the gap between simulation and real environments, resulting in insufficient robustness in motion tracking. GMT addresses these pain points by unifying motion tracking as a conditional generation problem, enhancing the generality and adaptability of whole-body control.

### Method or Model Framework

The core framework of GMT consists of three key modules: a data transformation layer, a generative model layer, and a policy composition layer. First, the data transformation layer uniformly encodes proprioceptive states, joint sequences, and simulation interaction data into "trackable body targets," thereby eliminating heterogeneity across different data sources. Next, the generative model layer employs diffusion policy or flow matching techniques to model action generation as a reverse diffusion process from noise to executable trajectories, enabling efficient sampling from multimodal action distributions. Finally, the policy composition layer, through hierarchical skill and expert strategy training, combines basic motion skills (e.g., balancing, walking) into a whole-body control policy and outputs whole-body trajectories, action sequences, and low-level controller targets.

### Key Technical Innovations

GMT's three major technical innovations significantly enhance the performance of humanoid whole-body control. First, treating action generation as a conditional generation problem allows the model to naturally handle multimodal action distributions, avoiding the limitations of single-trajectory fitting in traditional methods. Second, introducing flow matching as an alternative to diffusion policy reduces computational complexity while maintaining sampling quality, making it more suitable for real-time control scenarios. Third, the proposed training mechanism for hierarchical skills and expert strategies enables independent learning of different motion skills before composition, supporting zero-shot generalization to complex tasks. Additionally, GMT's design is inherently compatible with the behavioral foundation model paradigm, laying the groundwork for future large-scale pretrained motion models.

### Experiments/Validation and Application Value

Although the paper does not provide specific experimental data, it can be reasonably inferred from the framework design that GMT should have significant advantages in whole-body motion tracking tasks within simulation environments. For example, in the joint control of balancing and walking, GMT can generate smooth and stable whole-body trajectories through diffusion sampling, avoiding the jitter caused by mode switching in traditional methods. Furthermore, due to the hierarchical skill training, GMT is expected to quickly adapt to unseen terrains or task scenarios, demonstrating strong generalization capabilities. From an application perspective, GMT provides a unified technical pathway for autonomous motion of humanoid robots in complex environments such as home services and industrial collaboration, particularly suitable for multitask scenarios requiring simultaneous balancing, obstacle avoidance, and manipulation.

## 개요

휴머노이드 로봇의 전신 제어(Whole-Body Control)는 오랫동안 핵심 과제에 직면해 왔습니다. 즉, 고차원의 인지 상태와 관절 시퀀스를 안정적이고 실행 가능한 운동 궤적으로 변환하는 방법, 특히 다중 모드 동작 분포를 다룰 때 더욱 그렇습니다. 기존 방법은 수동으로 설계된 운동 템플릿이나 특정 작업용 컨트롤러에 의존하는 경우가 많아 다양한 운동 패턴에 대한 일반화 능력이 부족합니다. 이에 본 논문은 범용 운동 추적 프레임워크(General Motion Tracking, GMT)를 제안하여 동작 생성을 조건부 생성 문제로 재정의하고, 확산 정책(Diffusion Policy) 및 흐름 매칭(Flow Matching) 기술을 활용하여 다중 모드 동작 분포에서 실행 가능한 궤적을 샘플링함으로써 전신 운동의 통합 추적 및 제어를 실현합니다. 이 연구의 주요 기여는 종단 간(end-to-end)이며 조합 가능한 전신 제어 패러다임을 제안하여 행동 기반 모델(Behavioral Foundation Model) 구축에 핵심 기술적 토대를 제공한 점입니다.

## 핵심 내용

### 연구 배경 및 문제

휴머노이드 로봇의 운동 제어는 균형, 보행, 조작 등 여러 하위 작업을 포함하며, 전통적인 방법은 각 작업마다 개별적으로 컨트롤러를 설계하여 시스템이 복잡해지고 이전이 어렵습니다. 최근 강화 학습 및 모방 학습 기반 방법이 발전했지만, 다중 모드 동작 분포(예: 동일한 보행에서 여러 가능한 관절 구성)를 처리할 때 샘플링 효율이 낮고 궤적이 불연속적인 문제가 여전히 존재합니다. 또한, 본체 상태와 관절 시퀀스 간의 매핑 관계는 종종 비선형적이며, 시뮬레이션과 실제 환경 간의 차이로 인해 운동 추적의 견고성이 부족합니다. GMT는 이러한 문제점을 해결하기 위해 운동 추적을 조건부 생성 문제로 통합하여 전신 제어의 범용성과 적응성을 향상시키는 것을 목표로 합니다.

### 방법 또는 모델 프레임워크

GMT의 핵심 프레임워크는 세 가지 주요 모듈로 구성됩니다: 데이터 변환 계층, 생성 모델 계층 및 정책 조합 계층. 먼저, 데이터 변환 계층은 본체 상태, 관절 시퀀스 및 시뮬레이션 상호작용 데이터를 "추적 가능한 신체 목표"(trackable body targets)로 통합 인코딩하여 데이터 소스 간의 이질성을 제거합니다. 다음으로, 생성 모델 계층은 확산 정책(Diffusion Policy) 또는 흐름 매칭(Flow Matching) 기술을 채택하여 동작 생성을 노이즈에서 실행 가능한 궤적으로의 역확산 과정으로 모델링하며, 다중 모드 동작 분포에서 효율적으로 샘플링할 수 있습니다. 마지막으로, 정책 조합 계층은 계층적 기술(Hierarchical Skill) 및 전문가 정책(Expert Strategy) 훈련을 통해 기본 운동 기술(예: 균형, 보행)을 전신 제어 정책으로 조합하고, 전신 궤적, 동작 시퀀스 및 하위 수준 컨트롤러 목표를 출력합니다.

### 핵심 기술 혁신

GMT의 세 가지 주요 기술 혁신은 휴머노이드 전신 제어의 성능을 크게 향상시킵니다. 첫째, 동작 생성을 조건부 생성 문제로 간주하여 모델이 다중 모드 동작 분포를 자연스럽게 처리할 수 있게 하여, 기존 방법의 단일 궤적 피팅 한계를 피합니다. 둘째, 확산 정책의 대안으로 흐름 매칭을 도입하여 샘플링 품질을 유지하면서 계산 복잡성을 낮추어 실시간 제어 시나리오에 더 적합합니다. 셋째, 계층적 기술과 전문가 정책의 훈련 메커니즘을 제안하여 서로 다른 운동 기술이 독립적으로 학습된 후 조합될 수 있도록 하여, 복잡한 작업에서 제로샷 일반화(zero-shot generalization)를 지원합니다. 또한, GMT의 설계는 행동 기반 모델(Behavioral Foundation Model) 패러다임과 자연스럽게 호환되어, 향후 대규모 사전 훈련 운동 모델의 기반을 마련합니다.

### 실험/검증 및 응용 가치

논문에서 구체적인 실험 데이터를 제공하지는 않았지만, 프레임워크 설계로부터 GMT가 시뮬레이션 환경에서의 전신 운동 추적 작업에서 상당한 이점을 가질 것이라고 합리적으로 추론할 수 있습니다. 예를 들어, 균형과 보행의 결합 제어에서 GMT는 확산 샘플링을 통해 부드럽고 안정적인 전신 궤적을 생성하여, 기존 방법에서 모드 전환으로 인한 떨림을 방지할 수 있습니다. 또한, 계층적 기술 훈련을 채택했기 때문에 GMT는 보지 못한 지형이나 작업 시나리오에서 빠르게 적응할 수 있어 강력한 일반화 능력을 보일 것으로 기대됩니다. 응용 가치 측면에서 GMT는 가사 서비스, 산업 협업 등 복잡한 환경에서 휴머노이드 로봇의 자율 운동을 위한 통합 기술 경로를 제공하며, 특히 균형, 장애물 회피 및 조작을 동시에 고려해야 하는 다중 작업 시나리오에 적합합니다.
