---
$id: ent_paper_gallant_voxel_grid_based_human_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Gallant: Voxel Grid-based Humanoid Locomotion and Local-navigation across 3D Constrained Terrains'
  zh: Gallant｜跨3D约束地形的基于体素网格的人形运动和本地导航
  ko: 'Gallant: Voxel Grid-based Humanoid Locomotion and Local-navigation across 3D Constrained Terrains'
summary:
  en: Gallant first recovers the scene, object, or motion representation from camera images/multi-view observations, ontology
    state and joint sequences, depth/point cloud/altitude maps, and then the image is reconstructed from the depth/point cloud/altitude
    maps, then the strategy network and control module are used to generate the terrain/scene representation, navigation/reaching
    target. The key point is to put the policy network and the control module in the same training/deployment link, reducing
    the discontinuity between high-level goals and low-level actions.
  zh: Gallant 先从相机图像/多视角观测、本体状态与关节序列、深度/点云/高度图恢复场景、目标或运动表征，再用策略网络和控制模块生成地形/场景表征、导航/到达目标。关键点是把策略网络和控制模块放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
  ko: Gallant 先从相机图像/多视角观测、本体状态与关节序列、深度/点云/高度图恢复场景、目标或运动表征，再用策略网络和控制模块生成地形/场景表征、导航/到达目标。关键点是把策略网络和控制模块放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
domains:
- 02_components
- 06_design_engineering
layers:
- upstream
- midstream
functional_roles:
- knowledge
- component
tags:
- deployment
- gallant
- hardware_platform
- real_world
- sensor_suite
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. [2026-08-05]
    body rewritten as full-text six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline.
sources:
- id: src_001
  type: website
  title: Gallant project page
  url: https://gallantloco.github.io/
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

Gallant 是首个将体素网格（voxel grid）作为唯一感知输入、以端到端强化学习训练人形机器人 locomotion 与局部导航策略的工作，由 Unitree 相关团队完成，在 Unitree G1 上实现了零样本 sim-to-real 迁移。其核心贡献在于用 3D 体素表示替代 2.5D 高程图，配合轻量级 LiDAR 仿真管线与域随机化，使单一策略能同时应对地面、侧向与头顶三类空间约束。

## 它改变了什么

现有感知方案的根本缺陷在于“维度降级”：高程图把 3D 点云压成 2.5D 高度场，直接丢弃了悬垂物、低天花板、楼梯底面等垂直结构信息；深度图像则受限于窄 FoV 和有限量程，无法支撑对空间扩展环境的推理。更关键的是，这种感知与控制解耦的架构——高层规划器输出路径、低层策略跟踪——使得策略只能被动跟随，无法主动利用地形几何特征（如提前低头、抬脚跨越）。Gallant 改变了这一范式：它不再问“环境长什么样”，而是问“策略需要看到什么才能做出正确决策”。通过将整个环境编码为二值体素占用网格，并让策略直接从原始几何中学习行为，它证明了端到端学习可以替代传统分层导航，且能覆盖全空间障碍类型。

## 方法拆解

### 问题形式化
将感知运动建模为 POMDP，使用 PPO 训练非对称 actor-critic。Actor 仅接收体素网格，Critic 额外接收高度图作为特权信息，避免部署时依赖延迟敏感通道。

### 体素感知表示
- 感知体积 Ω=[-0.8,0.8]m×[-0.8,0.8]m×[-1.0,1.0]m，分辨率 Δ=0.05m，生成 32×32×40 二值占用张量 X∈{0,1}^{C×H×W}（C=40 为高度切片，H=W=32）。
- 每个体素若至少有一个 LiDAR 点落入则置 1，否则置 0。

### z 分组 2D CNN
将 z 轴作为通道维度，在 x-y 平面应用 2D 卷积，输出公式：
Y_{o,v,u}=σ(Σ_c Σ_{Δv,Δu} W_{o,c,Δv,Δu}·X_{c,v+Δv,u+Δu}+b_o)
相比 3D 核 k³，计算与内存成本约减少因子 k。

### LiDAR 仿真管线（NVIDIA Warp）
- 每个网格在局部坐标系预计算 BVH，仿真时仅变换射线原点，方向仅应用旋转分量：raycast(TM, p, d) = T⁻¹·raycast(M, T⁻¹p, R⁻¹d)。
- 并行 kernel 形状 (N_envs, N_meshes, N_rays)。
- 域随机化：位姿扰动 N(0,1)cm / N(0,(π/180)²)rad；命中位置扰动 N(0,1)cm；延迟 100-200ms；随机掩码 2% 体素。

### 奖励设计（通用、几何感知）
- 目标到达奖励 r_reach = 1/(1+‖P_t‖²) · 𝟙(t>T-T_r)/T_r，T_r=2s。
- 方向速度奖励 r_velocity_direction = (a(p,g)·v_t) / ‖a(p,g)·v_t‖₂，半径 r=1m，κ=0.8。
- 头部高度奖励 r_head_height = exp(−4(H_head_est − H_head)²)，H_head_est 通过沿目标方向前移 0.45m、在 0.5×0.5m 方形内平均地形高度并减去 0.1m 偏移计算。
- 脚部离地奖励 r_feet_clearance = exp(−4(H_feet_est − H_feet)²)，H_feet_est 查询每只脚前方 0.5m 处地形。

### 课程训练
8 种地形（Plane/Ceiling/Forest/Door/Platform/Pile/Upstair/Downstair），每种参数化标量难度 s∈[0,1]，参数插值 p_τ(s)=(1-s)·p_τ^min+s·p_τ^max。成功升级、失败降级。

### 对称性增强
将体素网格沿 y 轴镜像，与翻转后的 proprioceptive 输入对齐，奖励不变，原始与翻转样本共同存入 rollout buffer。

## 关键创新

1. **体素网格作为唯一感知输入的端到端策略**：这是首个证明纯体素表示（无高度图、无深度图、无显式规划器）能支撑人形机器人在复杂 3D 地形上完成 locomotion 与局部导航的工作。相比高程图，体素保留了完整的垂直与多层结构信息，使策略能主动感知头顶约束并调整姿态。
2. **轻量级 LiDAR 仿真管线与域随机化**：通过预计算 BVH、仅变换射线原点、并行 kernel 形状 (N_envs, N_meshes, N_rays)，实现了高效且可扩展的 LiDAR 仿真。对延迟（100-200ms）、噪声、位姿扰动的显式建模，是 sim-to-real 迁移成功的关键——消融实验显示无域随机化（NoDR）在地面级地形上性能显著下降。
3. **z 分组 2D CNN 架构**：将 z 轴作为通道维度，在 x-y 平面应用 2D 卷积，计算成本约为 3D 卷积的 1/k。这一设计在保持 3D 几何信息的同时，大幅降低了计算与内存开销，使策略能在 Orin NX 上实时运行。

## 实验与结果

### 仿真消融（表3，成功率 E_succ↑ / 碰撞动量 E_collision↓）

| 方法 | Ceiling | Forest | Door | Platform | Pile | Upstair | Downstair |
|------|---------|--------|------|----------|------|---------|-----------|
| **Gallant** | 97.1/24.6 | 84.3/311.1 | 98.7/27.7 | 96.1/30.1 | 82.1/113.1 | 96.2/27.0 | 97.9/15.6 |
| w/o-Self-Scan | 28.4/442.7 | 78.1/420.5 | 98.3/152.7 | 22.2/637.6 | 27.2/579.0 | 33.0/305.5 | 96.6/15.2 |
| Sparse-3D-CNN | 86.7/143.5 | 84.1/277.8 | 98.0/74.8 | 88.8/96.8 | 52.4/365.9 | 80.1/107.7 | 97.5/18.9 |
| 3D-CNN | 97.5/20.0 | 73.9/379.0 | 96.1/69.6 | 92.7/65.6 | 65.3/275.4 | 86.0/78.1 | 99.0/12.1 |
| Sparse-2D-CNN | 96.0/26.2 | 80.2/363.1 | 92.7/199.6 | 87.9/100.5 | 57.6/360.3 | 89.1/52.9 | 98.7/4.6 |
| Only-Height-Map | 5.3/1995.3 | 10.5/577.4 | 10.2/717.5 | 96.0/34.3 | 86.2/101.6 | 98.3/11.6 | 98.5/11.2 |
| Only-Voxel-Grid | 96.9/22.4 | 75.9/506.0 | 96.0/281.4 | 94.2/51.0 | 72.3/201.8 | 89.3/46.9 | 98.8/7.0 |
| 10CM | 97.3/24.2 | 77.5/368.0 | 97.5/260.4 | 75.5/63.0 | 65.2/256.3 | 94.1/38.6 | 97.5/13.5 |
| 2.5CM | 13.3/1442.4 | 59.0/642.7 | 64.8/591.0 | 67.2/268.9 | 54.1/400.2 | 86.3/74.8 | 96.6/15.2 |

关键发现：
- **Only-Height-Map 在 Ceiling/Forest/Door 上几乎完全失败**（成功率 5-10%），证明 2.5D 表示无法处理头顶与侧向障碍。
- **w/o-Self-Scan 在 Platform/Pile/Upstair 上大幅下降**（22-33% vs 82-96%），说明自扫描（self-scan）对地面级地形至关重要。
- **2.5CM 分辨率反而导致性能崩溃**（Ceiling 13.3%），表明过密体素引入噪声，5cm 是精度与鲁棒性的最佳平衡点。
- **Sparse-3D-CNN 在 Pile 上仅 52.4%**，低于 Gallant 的 82.1%，验证 z 分组 2D CNN 的优越性。

### 真实世界实验
- 同一策略直接部署到 Unitree G1，无微调。场景包括平坦地形、随机高度天花板、侧向杂乱物、带间隙高平台、踏脚石和楼梯。
- Gallant 在所有真实地形上一致优于 HeightMap 和 NoDR 基线（Fig. 5，15 次试验）。
- HeightMap 在 Ceiling 和 Door 上失败，因 2.5D 表示受限；NoDR 在地面级地形上性能显著下降，因未建模 LiDAR 延迟与噪声。
- 仿真与真实相关性（Fig. 6）：仿真成功率高的地形在硬件上也表现好，验证大规模仿真评估可预测真实性能。

### 关键瓶颈
- Pile 地形上成功率约 80% 平台期；仿真中零 LiDAR 延迟时提升至 90% 以上，表明真实传感器延迟（10Hz 下超 100ms）是关键瓶颈。

## 边界与局限

- 论文未明确列出局限性章节，但实验数据揭示：Pile 地形（堆石）成功率仅 82.1%，是 8 种地形中最低的，且作者承认 LiDAR 延迟是主要瓶颈——10Hz 工作频率下每次扫描因光反射与通信开销产生超 100ms 延迟，限制主动反应能力。
- 未提及在真实世界中对每个场景的具体成功率数字（图 5 仅标注 "15 trials" 但数字未在文本中给出）。
- 未提及与其他非消融方法（如基于深度图像的方法）在真实世界的直接对比实验。
- 未提及策略在完全未知地形类型上的泛化测试。
- 未提及计算延迟的具体毫秒数（仅提及 "lower inference latency"）。
- 未提及具体 PPO 超参数（学习率、batch size、GAE 参数等）与训练总时长。

## 工程启示

1. **先核对 LiDAR 延迟建模**：这是 sim-to-real 迁移成败的关键。NoDR 消融显示，未建模延迟会导致地面级地形成功率显著下降。复现时务必在仿真中显式加入 100-200ms 延迟，并验证延迟分布与真实传感器一致。
2. **体素分辨率 5cm 是甜点**：2.5cm 导致性能崩溃（Ceiling 13.3% vs 97.1%），10cm 在 Platform 上下降（75.5% vs 96.1%）。不要盲目追求高分辨率，噪声与计算开销会抵消精度收益。
3. **自扫描（self-scan）不可省略**：w/o-Self-Scan 在 Platform/Pile/Upstair 上成功率从 82-96% 暴跌至 22-33%。确保策略能感知自身与环境的相对位置，而非仅依赖外部观测。
4. **z 分组 2D CNN 优于 3D CNN**：Sparse-3D-CNN 在 Pile 上仅 52.4% vs Gallant 的 82.1%，且计算成本更低。复现时优先采用 z 分组 2D 卷积架构。
5. **通信优化是部署关键**：TCP 传输 LiDAR 数据与 LCM 内部状态共享导致约 200ms 延迟；通过裁剪 LiDAR 输出、共享内存（体素网格与观测、状态读取与动作命令）可消除几乎所有通信延迟。部署时务必检查端到端延迟链路。
6. **训练配置参考**：8 块 RTX 4090、1024×8 环境数、4000 次迭代、PPO clip 0.2、熵系数 0.003、GAE λ 0.95、γ 0.99、学习率 5e-4。评估规模为 5 次独立评估 × 1000 回合，报告均值±标准差。

## 参考
- Gallant project page (https://gallantloco.github.io/)

## Overview

Gallant is the first work to use a voxel grid as the sole perception input, training humanoid robot locomotion and local navigation policies through end-to-end reinforcement learning. It was developed by the Unitree-related team and achieves zero-shot sim-to-real transfer on the Unitree G1. Its core contribution lies in replacing 2.5D elevation maps with 3D voxel representations, combined with a lightweight LiDAR simulation pipeline and domain randomization, enabling a single policy to simultaneously handle three types of spatial constraints: ground-level, lateral, and overhead.

## What It Changes

The fundamental flaw of existing perception approaches is "dimensionality reduction": elevation maps compress 3D point clouds into 2.5D height fields, directly discarding vertical structure information such as overhangs, low ceilings, and stair undersides; depth images are limited by narrow FoV and finite range, making them insufficient for reasoning about spatially extended environments. More critically, this perception-control decoupled architecture—where a high-level planner outputs paths and a low-level policy tracks them—forces the policy to passively follow rather than actively exploit terrain geometric features (such as ducking early or lifting feet to step over). Gallant changes this paradigm: it no longer asks "what does the environment look like," but rather "what does the policy need to see to make correct decisions." By encoding the entire environment as a binary voxel occupancy grid and letting the policy learn behavior directly from raw geometry, it demonstrates that end-to-end learning can replace traditional hierarchical navigation and cover all spatial obstacle types.

## Method Breakdown

### Problem Formulation
The perception-motor task is modeled as a POMDP, trained with PPO using an asymmetric actor-critic. The actor receives only the voxel grid, while the critic additionally receives a height map as privileged information, avoiding reliance on latency-sensitive channels during deployment.

### Voxel Perception Representation
- Perception volume Ω=[-0.8,0.8]m×[-0.8,0.8]m×[-1.0,1.0]m, resolution Δ=0.05m, producing a 32×32×40 binary occupancy tensor X∈{0,1}^{C×H×W} (C=40 height slices, H=W=32).
- Each voxel is set to 1 if at least one LiDAR point falls within it, otherwise 0.

### z-Grouped 2D CNN
The z-axis is treated as the channel dimension, with 2D convolutions applied in the x-y plane. The output formula is:
Y_{o,v,u}=σ(Σ_c Σ_{Δv,Δu} W_{o,c,Δv,Δu}·X_{c,v+Δv,u+Δu}+b_o)
Compared to a 3D kernel k³, computation and memory costs are reduced by approximately a factor of k.

### LiDAR Simulation Pipeline (NVIDIA Warp)
- A BVH is precomputed for each mesh in the local coordinate frame; during simulation, only the ray origin is transformed, and the direction only applies the rotation component: raycast(TM, p, d) = T⁻¹·raycast(M, T⁻¹p, R⁻¹d).
- Parallel kernel shape (N_envs, N_meshes, N_rays).
- Domain randomization: pose perturbation N(0,1)cm / N(0,(π/180)²)rad; hit position perturbation N(0,1)cm; delay 100-200ms; random masking of 2% of voxels.

### Reward Design (General, Geometry-Aware)
- Goal arrival reward r_reach = 1/(1+‖P_t‖²) · 𝟙(t>T-T_r)/T_r, T_r=2s.
- Directional velocity reward r_velocity_direction = (a(p,g)·v_t) / ‖a(p,g)·v_t‖₂, radius r=1m, κ=0.8.
- Head height reward r_head_height = exp(−4(H_head_est − H_head)²), where H_head_est is computed by moving 0.45m forward along the goal direction, averaging terrain height within a 0.5×0.5m square, and subtracting a 0.1m offset.
- Foot clearance reward r_feet_clearance = exp(−4(H_feet_est − H_feet)²), where H_feet_est queries the terrain 0.5m ahead of each foot.

### Curriculum Training
8 terrain types (Plane/Ceiling/Forest/Door/Platform/Pile/Upstair/Downstair), each parameterized by a scalar difficulty s∈[0,1], with parameter interpolation p_τ(s)=(1-s)·p_τ^min+s·p_τ^max. Success upgrades difficulty, failure downgrades it.

### Symmetry Augmentation
The voxel grid is mirrored along the y-axis, aligned with flipped proprioceptive inputs, with rewards unchanged. Both original and flipped samples are stored in the rollout buffer.

## Key Innovations

1. **End-to-end policy with voxel grid as the sole perception input**: This is the first work demonstrating that a pure voxel representation (no height maps, no depth images, no explicit planner) can support humanoid locomotion and local navigation on complex 3D terrain. Compared to elevation maps, voxels preserve complete vertical and multi-layer structural information, enabling the policy to actively perceive overhead constraints and adjust posture.
2. **Lightweight LiDAR simulation pipeline with domain randomization**: Through precomputed BVH, transforming only ray origins, and parallel kernel shape (N_envs, N_meshes, N_rays), efficient and scalable LiDAR simulation is achieved. Explicit modeling of delay (100-200ms), noise, and pose perturbation is key to successful sim-to-real transfer—ablation experiments show that NoDR (no domain randomization) significantly degrades performance on ground-level terrain.
3. **z-grouped 2D CNN architecture**: The z-axis is treated as the channel dimension with 2D convolutions in the x-y plane, reducing computational cost to approximately 1/k of a 3D convolution. This design maintains 3D geometric information while substantially reducing computation and memory overhead, enabling real-time operation on Orin NX.

## Experiments and Results

### Simulation Ablations (Table 3, success rate E_succ↑ / collision momentum E_collision↓)

| Method | Ceiling | Forest | Door | Platform | Pile | Upstair | Downstair |
|------|---------|--------|------|----------|------|---------|-----------|
| **Gallant** | 97.1/24.6 | 84.3/311.1 | 98.7/27.7 | 96.1/30.1 | 82.1/113.1 | 96.2/27.0 | 97.9/15.6 |
| w/o-Self-Scan | 28.4/442.7 | 78.1/420.5 | 98.3/152.7 | 22.2/637.6 | 27.2/579.0 | 33.0/305.5 | 96.6/15.2 |
| Sparse-3D-CNN | 86.7/143.5 | 84.1/277.8 | 98.0/74.8 | 88.8/96.8 | 52.4/365.9 | 80.1/107.7 | 97.5/18.9 |
| 3D-CNN | 97.5/20.0 | 73.9/379.0 | 96.1/69.6 | 92.7/65.6 | 65.3/275.4 | 86.0/78.1 | 99.0/12.1 |
| Sparse-2D-CNN | 96.0/26.2 | 80.2/363.1 | 92.7/199.6 | 87.9/100.5 | 57.6/360.3 | 89.1/52.9 | 98.7/4.6 |
| Only-Height-Map | 5.3/1995.3 | 10.5/577.4 | 10.2/717.5 | 96.0/34.3 | 86.2/101.6 | 98.3/11.6 | 98.5/11.2 |
| Only-Voxel-Grid | 96.9/22.4 | 75.9/506.0 | 96.0/281.4 | 94.2/51.0 | 72.3/201.8 | 89.3/46.9 | 98.8/7.0 |
| 10CM | 97.3/24.2 | 77.5/368.0 | 97.5/260.4 | 75.5/63.0 | 65.2/256.3 | 94.1/38.6 | 97.5/13.5 |
| 2.5CM | 13.3/1442.4 | 59.0/642.7 | 64.8/591.0 | 67.2/268.9 | 54.1/400.2 | 86.3/74.8 | 96.6/15.2 |

Key findings:
- **Only-Height-Map almost completely fails on Ceiling/Forest/Door** (5-10% success rate), proving that 2.5D representations cannot handle overhead and lateral obstacles.
- **w/o-Self-Scan drops significantly on Platform/Pile/Upstair** (22-33% vs 82-96%), showing that self-scan is critical for ground-level terrain.
- **2.5CM resolution causes performance collapse** (Ceiling 13.3%), indicating that overly dense voxels introduce noise; 5cm is the optimal balance between precision and robustness.
- **Sparse-3D-CNN achieves only 52.4% on Pile**, lower than Gallant's 82.1%, validating the superiority of the z-grouped 2D CNN.

### Real-World Experiments
- The same policy is deployed directly on the Unitree G1 without fine-tuning. Scenarios include flat terrain, random-height ceilings, lateral clutter, elevated platforms with gaps, stepping stones, and stairs.
- Gallant consistently outperforms HeightMap and NoDR baselines on all real-world terrains (Fig. 5, 15 trials).
- HeightMap fails on Ceiling and Door due to 2.5D representation limitations; NoDR degrades significantly on ground-level terrain due to unmodeled LiDAR delay and noise.
- Simulation-to-real correlation (Fig. 6): terrains with high simulation success rates also perform well on hardware, validating that large-scale simulation evaluation can predict real-world performance.

### Key Bottleneck
- Pile terrain plateaus at approximately 80% success rate; with zero LiDAR delay in simulation, it improves to over 90%, indicating that real sensor latency (over 100ms at 10Hz) is the key bottleneck.

## Boundaries and Limitations

- The paper does not explicitly list a limitations section, but experimental data reveals: Pile terrain (rock piles) has a success rate of only 82.1%, the lowest among the 8 terrains, and the authors acknowledge LiDAR latency as the primary bottleneck—at 10Hz operating frequency, each scan incurs over 100ms delay due to light reflection and communication overhead, limiting proactive reaction capability.
- No specific real-world success rate numbers are provided per scenario (Fig. 5 only notes "15 trials" without numerical values in the text).
- No direct real-world comparison experiments with other non-ablation methods (e.g., depth-image-based approaches) are mentioned.
- No generalization tests on completely unseen terrain types are mentioned.
- No specific computational latency in milliseconds is mentioned (only "lower inference latency").
- No specific PPO hyperparameters (learning rate, batch size, GAE parameters, etc.) or total training duration are mentioned.

## Engineering Insights

1. **Verify LiDAR delay modeling first**: This is critical for sim-to-real transfer success. The NoDR ablation shows that unmodeled delay causes significant performance drops on ground-level terrain. When reproducing, explicitly add 100-200ms delay in simulation and verify the delay distribution matches the real sensor.
2. **5cm voxel resolution is the sweet spot**: 2.5cm causes performance collapse (Ceiling 13.3% vs 97.1%), while 10cm degrades Platform performance (75.5% vs 96.1%). Do not blindly pursue higher resolution; noise and computational overhead will offset precision gains.
3. **Self-scan is indispensable**: w/o-Self-Scan drops success rates from 82-96% to 22-33% on Platform/Pile/Upstair. Ensure the policy can perceive the relative position between itself and the environment, rather than relying solely on external observations.
4. **z-grouped 2D CNN outperforms 3D CNN**: Sparse-3D-CNN achieves only 52.4% on Pile vs Gallant's 82.1%, with higher computational cost. When reproducing, prioritize the z-grouped 2D convolution architecture.
5. **Communication optimization is key for deployment**: TCP transmission of LiDAR data and LCM internal state sharing cause approximately 200ms delay; cropping LiDAR output and using shared memory (voxel grid and observations, state reading and action commands) can eliminate almost all communication delay. Always check the end-to-end latency chain during deployment.
6. **Training configuration reference**: 8 RTX 4090 GPUs, 1024×8 environments, 4000 iterations, PPO clip 0.2, entropy coefficient 0.003, GAE λ 0.95, γ 0.99, learning rate 5e-4. Evaluation scale is 5 independent evaluations × 1000 episodes, reporting mean ± standard deviation.

## 개요

Gallant는 복셀 그리드(voxel grid)를 유일한 인식 입력으로 사용하고, 엔드투엔드 강화 학습으로 휴머노이드 로봇의 locomotion 및 로컬 내비게이션 정책을 훈련한 최초의 연구로, Unitree 관련 팀이 수행했으며 Unitree G1에서 제로샷 sim-to-real 전이를 달성했습니다. 핵심 기여는 2.5D 고도 맵을 3D 복셀 표현으로 대체하고, 경량 LiDAR 시뮬레이션 파이프라인과 도메인 무작위화를 결합하여 단일 정책이 지상, 측면, 머리 위의 세 가지 공간 제약을 동시에 처리할 수 있게 한 것입니다.

## 무엇을 바꾸었는가

기존 인식 방식의 근본적 결함은 "차원 축소"에 있습니다. 고도 맵은 3D 포인트 클라우드를 2.5D 높이 필드로 압축하여 돌출물, 낮은 천장, 계단 밑면과 같은 수직 구조 정보를 직접 버립니다. 깊이 이미지는 좁은 FoV와 제한된 범위로 인해 공간적으로 확장된 환경에 대한 추론을 지원할 수 없습니다. 더 중요하게는, 이러한 인식-제어 분리 아키텍처(상위 플래너가 경로를 출력하고 하위 정책이 추종)는 정책이 수동적으로 추종할 수밖에 없어 지형의 기하학적 특징(예: 미리 고개 숙이기, 발을 들어 넘기)을 능동적으로 활용할 수 없습니다. Gallant는 이 패러다임을 바꾸었습니다. "환경이 어떻게 생겼는가"가 아니라 "정책이 올바른 결정을 내리기 위해 무엇을 봐야 하는가"를 묻습니다. 전체 환경을 이진 복셀 점유 그리드로 인코딩하고 정책이 원시 기하학에서 직접 행동을 학습하게 함으로써, 엔드투엔드 학습이 전통적인 계층적 내비게이션을 대체할 수 있고 전체 공간 장애물 유형을 포괄할 수 있음을 증명했습니다.

## 방법 분석

### 문제 정식화
인식-운동을 POMDP로 모델링하고 PPO로 비대칭 actor-critic을 훈련합니다. Actor는 복셀 그리드만 수신하고, Critic은 특권 정보로 고도 맵을 추가로 수신하여 배포 시 지연 민감 채널에 의존하지 않도록 합니다.

### 복셀 인식 표현
- 인식 볼륨 Ω=[-0.8,0.8]m×[-0.8,0.8]m×[-1.0,1.0]m, 해상도 Δ=0.05m, 32×32×40 이진 점유 텐서 X∈{0,1}^{C×H×W} 생성 (C=40은 높이 슬라이스, H=W=32).
- 각 복셀은 최소 하나의 LiDAR 포인트가 포함되면 1, 그렇지 않으면 0으로 설정.

### z 그룹 2D CNN
z축을 채널 차원으로 사용하고 x-y 평면에 2D 컨볼루션을 적용하며, 출력 공식:
Y_{o,v,u}=σ(Σ_c Σ_{Δv,Δu} W_{o,c,Δv,Δu}·X_{c,v+Δv,u+Δu}+b_o)
3D 커널 k³에 비해 계산 및 메모리 비용이 약 k배 감소합니다.

### LiDAR 시뮬레이션 파이프라인 (NVIDIA Warp)
- 각 메시는 로컬 좌표계에서 BVH를 사전 계산하고, 시뮬레이션 시 광선 원점만 변환하며 방향은 회전 성분만 적용: raycast(TM, p, d) = T⁻¹·raycast(M, T⁻¹p, R⁻¹d).
- 병렬 커널 형태 (N_envs, N_meshes, N_rays).
- 도메인 무작위화: 자세 섭동 N(0,1)cm / N(0,(π/180)²)rad; 히트 위치 섭동 N(0,1)cm; 지연 100-200ms; 무작위 마스크 2% 복셀.

### 보상 설계 (범용, 기하학 인식)
- 목표 도달 보상 r_reach = 1/(1+‖P_t‖²) · 𝟙(t>T-T_r)/T_r, T_r=2s.
- 방향 속도 보상 r_velocity_direction = (a(p,g)·v_t) / ‖a(p,g)·v_t‖₂, 반경 r=1m, κ=0.8.
- 머리 높이 보상 r_head_height = exp(−4(H_head_est − H_head)²), H_head_est는 목표 방향으로 0.45m 전진, 0.5×0.5m 정사각형 내 평균 지형 높이에서 0.1m 오프셋을 빼서 계산.
- 발 들림 보상 r_feet_clearance = exp(−4(H_feet_est − H_feet)²), H_feet_est는 각 발 앞 0.5m 지점의 지형을 조회.

### 커리큘럼 훈련
8가지 지형(Plane/Ceiling/Forest/Door/Platform/Pile/Upstair/Downstair), 각각 스칼라 난이도 s∈[0,1]로 매개변수화, 매개변수 보간 p_τ(s)=(1-s)·p_τ^min+s·p_τ^max. 성공 시 승급, 실패 시 강등.

### 대칭성 증강
복셀 그리드를 y축으로 미러링하고, 뒤집힌 고유수용감각 입력과 정렬하며, 보상은 불변, 원본과 뒤집힌 샘플을 함께 rollout 버퍼에 저장.

## 핵심 혁신

1. **복셀 그리드를 유일한 인식 입력으로 하는 엔드투엔드 정책**: 순수 복셀 표현(고도 맵, 깊이 맵, 명시적 플래너 없음)이 휴머노이드 로봇의 복잡한 3D 지형에서 locomotion 및 로컬 내비게이션을 지원할 수 있음을 증명한 최초의 연구. 고도 맵과 달리 복셀은 완전한 수직 및 다층 구조 정보를 보존하여 정책이 머리 위 제약을 능동적으로 인식하고 자세를 조정할 수 있게 합니다.
2. **경량 LiDAR 시뮬레이션 파이프라인과 도메인 무작위화**: BVH 사전 계산, 광선 원점만 변환, 병렬 커널 형태 (N_envs, N_meshes, N_rays)를 통해 효율적이고 확장 가능한 LiDAR 시뮬레이션 구현. 지연(100-200ms), 노이즈, 자세 섭동의 명시적 모델링은 sim-to-real 전이 성공의 핵심 — 소거 실험에서 도메인 무작위화 없음(NoDR)이 지상급 지형에서 성능이 크게 저하됨을 보여줌.
3. **z 그룹 2D CNN 아키텍처**: z축을 채널 차원으로 사용하고 x-y 평면에 2D 컨볼루션을 적용하여 계산 비용이 3D 컨볼루션의 약 1/k. 이 설계는 3D 기하학 정보를 유지하면서 계산 및 메모리 오버헤드를 크게 줄여 Orin NX에서 실시간 실행 가능.

## 실험 및 결과

### 시뮬레이션 소거 (표3, 성공률 E_succ↑ / 충돌 운동량 E_collision↓)

| 방법 | Ceiling | Forest | Door | Platform | Pile | Upstair | Downstair |
|------|---------|--------|------|----------|------|---------|-----------|
| **Gallant** | 97.1/24.6 | 84.3/311.1 | 98.7/27.7 | 96.1/30.1 | 82.1/113.1 | 96.2/27.0 | 97.9/15.6 |
| w/o-Self-Scan | 28.4/442.7 | 78.1/420.5 | 98.3/152.7 | 22.2/637.6 | 27.2/579.0 | 33.0/305.5 | 96.6/15.2 |
| Sparse-3D-CNN | 86.7/143.5 | 84.1/277.8 | 98.0/74.8 | 88.8/96.8 | 52.4/365.9 | 80.1/107.7 | 97.5/18.9 |
| 3D-CNN | 97.5/20.0 | 73.9/379.0 | 96.1/69.6 | 92.7/65.6 | 65.3/275.4 | 86.0/78.1 | 99.0/12.1 |
| Sparse-2D-CNN | 96.0/26.2 | 80.2/363.1 | 92.7/199.6 | 87.9/100.5 | 57.6/360.3 | 89.1/52.9 | 98.7/4.6 |
| Only-Height-Map | 5.3/1995.3 | 10.5/577.4 | 10.2/717.5 | 96.0/34.3 | 86.2/101.6 | 98.3/11.6 | 98.5/11.2 |
| Only-Voxel-Grid | 96.9/22.4 | 75.9/506.0 | 96.0/281.4 | 94.2/51.0 | 72.3/201.8 | 89.3/46.9 | 98.8/7.0 |
| 10CM | 97.3/24.2 | 77.5/368.0 | 97.5/260.4 | 75.5/63.0 | 65.2/256.3 | 94.1/38.6 | 97.5/13.5 |
| 2.5CM | 13.3/1442.4 | 59.0/642.7 | 64.8/591.0 | 67.2/268.9 | 54.1/400.2 | 86.3/74.8 | 96.6/15.2 |

핵심 발견:
- **Only-Height-Map은 Ceiling/Forest/Door에서 거의 완전히 실패** (성공률 5-10%), 2.5D 표현이 머리 위 및 측면 장애물을 처리할 수 없음을 증명.
- **w/o-Self-Scan은 Platform/Pile/Upstair에서 크게 저하** (22-33% vs 82-96%), 자체 스캔(self-scan)이 지상급 지형에 필수적임을 시사.
- **2.5CM 해상도는 오히려 성능 붕괴 초래** (Ceiling 13.3%), 과밀 복셀이 노이즈를 유입하며 5cm가 정밀도와 견고성의 최적 균형점.
- **Sparse-3D-CNN은 Pile에서 52.4%에 불과**, Gallant의 82.1%보다 낮아 z 그룹 2D CNN의 우수성을 검증.

### 실제 세계 실험
- 동일 정책을 Unitree G1에 직접 배포, 미세 조정 없음. 시나리오는 평평한 지형, 무작위 높이 천장, 측면 잡동사니, 간격이 있는 높은 플랫폼, 디딤돌, 계단 포함.
- Gallant는 모든 실제 지형에서 HeightMap 및 NoDR 기준선보다 일관되게 우수 (Fig. 5, 15회 시행).
- HeightMap은 Ceiling과 Door에서 실패, 2.5D 표현의 한계 때문; NoDR은 지상급 지형에서 성능이 크게 저하, LiDAR 지연 및 노이즈 미모델링 때문.
- 시뮬레이션-실제 상관관계 (Fig. 6): 시뮬레이션 성공률이 높은 지형은 하드웨어에서도 좋은 성능을 보여, 대규모 시뮬레이션 평가가 실제 성능을 예측할 수 있음을 검증.

### 핵심 병목
- Pile 지형에서 성공률 약 80% 플랫폼; 시뮬레이션에서 LiDAR 지연이 0일 때 90% 이상으로 상승, 실제 센서 지연(10Hz에서 100ms 초과)이 핵심 병목임을 시사.

## 경계 및 한계

- 논문은 한계 섹션을 명시적으로 나열하지 않았지만, 실험 데이터가 다음을 드러냄: Pile 지형(돌무더기) 성공률 82.1%로 8개 지형 중 최저, 저자는 LiDAR 지연이 주요 병목임을 인정 — 10Hz 작동 주파수에서 각 스캔이 광 반사 및 통신 오버헤드로 100ms 이상 지연되어 능동 반응 능력 제한.
- 실제 세계에서 각 시나리오의 구체적 성공률 수치 미언급 (Fig. 5는 "15 trials"만 표시, 숫자는 텍스트에 없음).
- 다른 비소거 방법(예: 깊이 이미지 기반 방법)과의 실제 세계 직접 비교 실험 미언급.
- 완전히 알려지지 않은 지형 유형에 대한 정책 일반화 테스트 미언급.
- 계산 지연의 구체적 밀리초 수치 미언급 ("lower inference latency"만 언급).
- 구체적 PPO 하이퍼파라미터(학습률, 배치 크기, GAE 매개변수 등) 및 총 훈련 시간 미언급.

## 공학적 시사점

1. **LiDAR 지연 모델링을 먼저 확인**: sim-to-real 전이 성공의 핵심. NoDR 소거는 지연 미모델링이 지상급 지형 성공률을 크게 저하시킴을 보여줌. 재현 시 시뮬레이션에 100-200ms 지연을 명시적으로 추가하고 지연 분포가 실제 센서와 일치하는지 검증.
2. **복셀 해상도 5cm가 최적점**: 2.5cm는 성능 붕괴(Ceiling 13.3% vs 97.1%), 10cm는 Platform에서 저하(75.5% vs 96.1%). 높은 해상도를 맹목적으로 추구하지 말 것, 노이즈와 계산 오버헤드가 정밀도 이득을 상쇄.
3. **자체 스캔(self-scan) 생략 불가**: w/o-Self-Scan은 Platform/Pile/Upstair에서 성공률이 82-96%에서 22-33%로 급락. 정책이 외부 관측에만 의존하지 않고 자신과 환경의 상대적 위치를 인식할 수 있도록 보장.
4. **z 그룹 2D CNN이 3D CNN보다 우수**: Sparse-3D-CNN은 Pile에서 52.4% vs Gallant의 82.1%, 계산 비용도 더 낮음. 재현 시 z 그룹 2D 컨볼루션 아키텍처를 우선 채택.
5. **통신 최적화가 배포의 핵심**: TCP로 LiDAR 데이터를 전송하고 LCM 내부 상태 공유로 약 200ms 지연 발생; LiDAR 출력 트리밍, 공유 메모리(복셀 그리드와 관측, 상태 읽기와 동작 명령)로 거의 모든 통신 지연 제거 가능. 배포 시 엔드투엔드 지연 체인을 반드시 확인.
6. **훈련 구성 참고**: 8개 RTX 4090, 1024×8 환경 수, 4000회 반복, PPO clip 0.2, 엔트로피 계수 0.003, GAE λ 0.95, γ 0.99, 학습률 5e-4. 평가 규모는 5회 독립 평가 × 1000 에피소드, 평균±표준편차 보고.
