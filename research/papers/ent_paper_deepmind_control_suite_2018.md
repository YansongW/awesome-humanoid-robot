---
$id: ent_paper_deepmind_control_suite_2018
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: DeepMind Control Suite
  zh: DeepMind Control Suite
  ko: DeepMind Control Suite
summary:
  en: 'The DeepMind Control Suite is a set of continuous control tasks with a standardised structure and interpretable rewards,
    intended to serve as performance benchmarks for reinforcement learning agents. Institutions per source list: Yuval Tassa、Yotam
    Doron、Alistair Muldal、Tom Erez、Yazhe Li、Diego de Las Casas、David Budden、Abbas Abdolmaleki、Josh Merel、Andrew Lefrancq、Timothy
    Lillicrap、Martin Riedmiller.'
  zh: DeepMind Control Suite 是由 DeepMind 开发的连续控制任务集合，基于 MuJoCo 物理引擎，提供标准化结构和可解释奖励，旨在作为强化学习智能体的性能基准。该套件包含多种学习算法的基准测试，并公开于 GitHub
    仓库。
  ko: 'The DeepMind Control Suite is a set of continuous control tasks with a standardised structure and interpretable rewards,
    intended to serve as performance benchmarks for reinforcement learning agents. Institutions per source list: Yuval Tassa、Yotam
    Doron、Alistair Muldal、Tom Erez、Yazhe Li、Diego de Las Casas、David Budden、Abbas Abdolmaleki、Josh Merel、Andrew Lefrancq、Timothy
    Lillicrap、Martin Riedmiller.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- deepmind
- control
- suite
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 344 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (1801.00690v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: arXiv:1801.00690 DeepMind Control Suite
  url: https://arxiv.org/abs/1801.00690
  accessed_at: '2026-07-31'
  date: '2018-01-02'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

DeepMind Control Suite 是一个基于 MuJoCo 物理引擎的连续控制强化学习基准平台，由 DeepMind 团队开发，旨在提供一套稳定、统一、可解释的物理控制任务集。其核心贡献在于标准化了动作、观测和奖励结构，并提供了 A3C、DDPG、D4PG 三种基线算法的系统评估，填补了连续控制领域缺乏标准基准的空白。

## 它改变了什么

在 ALE 主导离散视频游戏基准、Gym 虽含连续域但结构松散的时代，Control Suite 真正改变了连续控制任务的评估范式。它不再将观测简单拼接为向量，而是分离具有物理单位的量（位置、速度、力），使学习曲线和奖励结构具有跨任务的统一语义——所有任务奖励范围在 [0,1]，评估片段固定 1000 步，最大分数恒为 1000，这让不同任务的性能可直接比较，而非像 Gym 那样各任务奖励尺度迥异、难以聚合。

更重要的是，作者将"任务可解性验证"提升为基准构建的核心环节。他们不是简单发布任务，而是用多种学习智能体迭代测试，确保物理模拟稳定、无"作弊"策略、且至少一个智能体能解决。这种"验证过的基准"理念，使得 Control Suite 成为后续算法比较的可靠起点，而非像某些 Gym 任务那样存在隐藏的脆弱性或非预期解。

## 方法拆解

### 任务架构
- 所有任务用 Python 编写，由 MuJoCo 驱动，物理模型用 MJCF 定义
- 域（物理模型）与任务（特定 MDP 结构）分离，任务按标签组织为 BENCHMARKING 和 EXTRA 元组

### 标准化设计
- **动作**：除 LQR 外，动作限制在单位盒 a ∈ [-1,1]^dim(A)
- **观测**：除 point-mass:hard 外，所有任务强可观测（状态可从单观测恢复），实现为 OrderedDict
- **奖励**：除 LQR 外，奖励 r(s,a) ∈ [0,1]，由 tolerance() 函数生成，支持稀疏（{0,1}）和平滑变体
- **终止**：无终止状态或时间限制，无限时域；实际用折扣公式 ∑γ^i r，γ = e^{-h/τ}

### 关键设计决策
- **积分器选择**：多数域用 MuJoCo 默认半隐式欧拉，少数平滑近能量守恒动力学用四阶龙格-库塔
- **Physics.step() 顺序**：先 mj_step2（依赖控制）再 mj_step1（仅依赖状态），使位置/速度量与当前状态同步，力/加速度量与前一步转移同步
- **初始化多样性**：状态在子集初始化，避免单一状态导致"死记硬背"解
- **LQR 域**：解析转移矩阵和 Riccati 最优策略，因控制和奖励无界而不在基准集

### 基线算法配置
- **A3C**：32 workers，2 层 MLP（256 隐藏单元），RMSProp（α=0.99, ε=0.1），熵正则 β=3e-3，学习率从 5e-5 线性退火到 0
- **DDPG**：actor 300→200，critic 400→300，Adam（η=1e-4），软更新 τ=1e-3，OU 噪声（θ=0.15, σ=0.3），replay buffer 1e6，minibatch 64
- **D4PG**：32 CPU actors + 1 GPU learner，critic 值分布 101 类 [-150,150]，N-step N=5，非均匀采样 α=0.6，硬更新每 100 步，minibatch 256

## 关键创新

1. **统一奖励结构**：所有任务奖励在 [0,1]，配合固定 1000 步评估，使学习曲线 y 轴一致，可直接跨任务比较。这是 Gym 无法提供的——Gym 任务奖励尺度差异巨大，聚合性能无意义。

2. **验证驱动的任务设计**：作者用多种学习智能体迭代测试任务，确保物理稳定、不可利用、至少一个智能体能解决。这避免了基准任务"过易"（可作弊）或"过难"（无算法能解）的陷阱，是基准可信度的关键保障。

3. **像素输入变体的系统评估**：D4PG 像素变体提供 84×84 RGB 帧、2 层 ConvNet（3×3 核、32 通道、ELU、stride 2）、帧堆叠 3 个观测，并发现 actor-critic 共享卷积权重（仅 critic 优化器更新）显著优于分离网络。这为视觉控制任务提供了可复现的架构基线。

## 实验与结果

| 任务 | A3C | D4PG | D4PG (Pixels) | DDPG |
|------|-----|------|---------------|------|
| acrobot swingup | 41.9±1.2 | 297.6±8.4 | 81.7±4.4 | 15.4±0.9 |
| ball_in_cup catch | 104.7±7.8 | 981.2±0.7 | 980.5±0.5 | 984.5±0.3 |
| cartpole balance_sparse | 857.4±7.9 | 1000.0±0.0 | 1000.0±0.0 | 878.5±8.0 |
| cheetah walk | 213.9±1.6 | 736.7±4.4 | 523.8±6.8 | 842.5±1.6 |
| humanoid run | 1.0±0.0 | 463.6±13.8 | 1.4±0.0 | 167.9±4.1 |
| manipulator bring_ball | 0.4±0.0 | 895.9±3.7 | 0.5±0.1 | 0.6±0.1 |
| pendulum swingup | 48.6±5.2 | 836.2±5.0 | 680.9±41.9 | 816.2±4.7 |
| reacher hard | 39.7±2.9 | 973.0±2.0 | 957.1±5.4 | 904.3±6.8 |
| swimmer swimmer6 | 177.8±7.8 | 664.7±11.1 | 194.7±15.9 | 394.0±14.1 |
| walker run | 191.8±1.9 | 839.7±0.7 | 567.2±18.9 | 786.2±0.4 |

（表 1：10^8 训练步后 100 episodes 均值和标准误）

关键发现：
- D4PG 在多数任务上最优，但 DDPG 在 1e7 步前数据效率更高
- 像素变体在 ball_in_cup、cartpole、reacher 上接近状态特征性能，但在 humanoid run、manipulator bring_ball 上完全失败（得分≈1）
- A3C 在 humanoid run 上几乎无法学习（1.0±0.0），凸显连续控制对 off-policy 方法的依赖
- 24 小时训练后（表 2），D4PG 在 acrobot swingup 达 531.1±8.9，作者认为接近上界（摆起时间限制）

## 边界与局限

作者明确承认：同一算法在所有任务使用相同超参数，未做穷举优化，结果不应视为性能上限。像素变体在 swimmer、fish 等任务上完全失败，归因于相机视角不适合像素-only 方案。Control Suite 缺少四足 locomotion、完整操作任务和复杂地形，这些需要推理任务和模型分布。未包含交互式可视化器、C 回调、多线程动力学、TensorFlow op 包装器和 Windows 支持。评估方差受随机目标/初始化影响，未固定跨运行的初始化序列。论文未提及多任务学习、迁移学习、分层 RL 等扩展方向。

## 工程启示

复现时先核对三点：**奖励范围**（确认任务是否在 [0,1]，LQR 除外）、**观测结构**（OrderedDict 中物理量分离，勿拼接为向量）、**积分器类型**（半隐式欧拉 vs 四阶龙格-库塔，影响动力学精度）。最容易踩坑的是 D4PG 像素变体的共享权重设计——分离 actor/critic 网络性能显著更差，必须让 critic 优化器更新共享卷积层。A3C 在连续控制上表现极差（humanoid run 得分≈1），若你的算法是 on-policy 且无经验回放，预期在 Control Suite 上会遭遇类似困难。DDPG 的 OU 噪声参数（θ=0.15, σ=0.3）和软更新 τ=1e-3 是经过验证的默认值，改动需谨慎。评估协议固定 1000 步求和，最大 1000，但许多任务实际最大值远小于 1000（如 acrobot swingup 需多步摆起），比较时需注意任务的理论上界。

## 参考
- https://arxiv.org/abs/1801.00690
- https://github.com/ImChong/Robotics_Notebooks

## Overview

The DeepMind Control Suite is a continuous control reinforcement learning benchmark platform based on the MuJoCo physics engine, developed by the DeepMind team to provide a stable, unified, and interpretable set of physical control tasks. Its core contribution lies in standardizing action, observation, and reward structures, along with systematic evaluations of three baseline algorithms—A3C, DDPG, and D4PG—filling the gap of lacking standard benchmarks in the continuous control domain.

## What It Changed

In an era where ALE dominated discrete video game benchmarks and Gym, though containing continuous domains, had a loose structure, the Control Suite truly transformed the evaluation paradigm for continuous control tasks. Instead of simply concatenating observations into vectors, it separates quantities with physical units (positions, velocities, forces), giving learning curves and reward structures a unified semantic across tasks—all task rewards are in [0,1], evaluation episodes are fixed at 1000 steps, and the maximum score is always 1000, making performance directly comparable across tasks, unlike Gym where reward scales vary drastically and aggregation is difficult.

More importantly, the authors elevated "task solvability verification" to a core component of benchmark construction. Rather than simply releasing tasks, they iteratively tested them with multiple learning agents to ensure stable physics simulation, no "cheating" strategies, and that at least one agent could solve them. This "verified benchmark" philosophy makes the Control Suite a reliable starting point for subsequent algorithm comparisons, unlike some Gym tasks that harbor hidden fragility or unintended solutions.

## Method Breakdown

### Task Architecture
- All tasks are written in Python, driven by MuJoCo, with physics models defined in MJCF
- Domains (physics models) are separated from tasks (specific MDP structures), organized into BENCHMARKING and EXTRA tuples by tags

### Standardization Design
- **Actions**: Except for LQR, actions are constrained to the unit box a ∈ [-1,1]^dim(A)
- **Observations**: Except for point-mass:hard, all tasks are strongly observable (state recoverable from a single observation), implemented as OrderedDict
- **Rewards**: Except for LQR, rewards r(s,a) ∈ [0,1], generated by the tolerance() function, supporting sparse ({0,1}) and smooth variants
- **Termination**: No terminal states or time limits; infinite horizon; practically computed with the discounted sum ∑γ^i r, where γ = e^{-h/τ}

### Key Design Decisions
- **Integrator Selection**: Most domains use MuJoCo's default semi-implicit Euler, while a few with smooth near-energy-conserving dynamics use fourth-order Runge-Kutta
- **Physics.step() Order**: mj_step2 (control-dependent) is called before mj_step1 (state-only), synchronizing position/velocity quantities with the current state and force/acceleration quantities with the previous transition
- **Initialization Diversity**: States are initialized from a subset to avoid "memorization" solutions from a single state
- **LQR Domain**: Uses analytical transition matrices and Riccati optimal policies, excluded from the benchmark set due to unbounded control and rewards

### Baseline Algorithm Configurations
- **A3C**: 32 workers, 2-layer MLP (256 hidden units), RMSProp (α=0.99, ε=0.1), entropy regularization β=3e-3, learning rate linearly annealed from 5e-5 to 0
- **DDPG**: actor 300→200, critic 400→300, Adam (η=1e-4), soft update τ=1e-3, OU noise (θ=0.15, σ=0.3), replay buffer 1e6, minibatch 64
- **D4PG**: 32 CPU actors + 1 GPU learner, critic value distribution with 101 atoms in [-150,150], N-step N=5, non-uniform sampling α=0.6, hard update every 100 steps, minibatch 256

## Key Innovations

1. **Unified Reward Structure**: All task rewards are in [0,1], paired with fixed 1000-step evaluation, making the y-axis of learning curves consistent and enabling direct cross-task comparison. This is something Gym cannot provide—Gym task reward scales vary enormously, making aggregated performance meaningless.

2. **Verification-Driven Task Design**: The authors iteratively tested tasks with multiple learning agents to ensure physical stability, non-exploitability, and that at least one agent could solve them. This avoids the pitfalls of benchmark tasks being "too easy" (cheatable) or "too hard" (unsolvable by any algorithm), serving as a key guarantee of benchmark credibility.

3. **Systematic Evaluation of Pixel-Input Variants**: The D4PG pixel variant provides 84×84 RGB frames, a 2-layer ConvNet (3×3 kernels, 32 channels, ELU, stride 2), frame stacking of 3 observations, and finds that actor-critic sharing convolutional weights (with only the critic optimizer updating) significantly outperforms separate networks. This provides a reproducible architectural baseline for visual control tasks.

## Experiments and Results

| Task | A3C | D4PG | D4PG (Pixels) | DDPG |
|------|-----|------|---------------|------|
| acrobot swingup | 41.9±1.2 | 297.6±8.4 | 81.7±4.4 | 15.4±0.9 |
| ball_in_cup catch | 104.7±7.8 | 981.2±0.7 | 980.5±0.5 | 984.5±0.3 |
| cartpole balance_sparse | 857.4±7.9 | 1000.0±0.0 | 1000.0±0.0 | 878.5±8.0 |
| cheetah walk | 213.9±1.6 | 736.7±4.4 | 523.8±6.8 | 842.5±1.6 |
| humanoid run | 1.0±0.0 | 463.6±13.8 | 1.4±0.0 | 167.9±4.1 |
| manipulator bring_ball | 0.4±0.0 | 895.9±3.7 | 0.5±0.1 | 0.6±0.1 |
| pendulum swingup | 48.6±5.2 | 836.2±5.0 | 680.9±41.9 | 816.2±4.7 |
| reacher hard | 39.7±2.9 | 973.0±2.0 | 957.1±5.4 | 904.3±6.8 |
| swimmer swimmer6 | 177.8±7.8 | 664.7±11.1 | 194.7±15.9 | 394.0±14.1 |
| walker run | 191.8±1.9 | 839.7±0.7 | 567.2±18.9 | 786.2±0.4 |

(Table 1: Mean and standard error over 100 episodes after 10^8 training steps)

Key findings:
- D4PG is optimal on most tasks, but DDPG is more data-efficient before 1e7 steps
- Pixel variants approach state-feature performance on ball_in_cup, cartpole, and reacher, but completely fail on humanoid run and manipulator bring_ball (scores ≈1)
- A3C barely learns on humanoid run (1.0±0.0), highlighting the dependence of continuous control on off-policy methods
- After 24 hours of training (Table 2), D4PG reaches 531.1±8.9 on acrobot swingup, which the authors consider near the upper bound (swing-up time limit)

## Boundaries and Limitations

The authors explicitly acknowledge: the same hyperparameters are used across all tasks for each algorithm without exhaustive optimization, so results should not be viewed as performance ceilings. Pixel variants completely fail on tasks like swimmer and fish, attributed to camera viewpoints unsuitable for pixel-only approaches. The Control Suite lacks quadruped locomotion, full manipulation tasks, and complex terrains, which require reasoning tasks and model distributions. It does not include an interactive visualizer, C callbacks, multi-threaded dynamics, TensorFlow op wrappers, or Windows support. Evaluation variance is affected by random targets/initializations, with no fixed initialization sequence across runs. The paper does not mention extensions such as multi-task learning, transfer learning, or hierarchical RL.

## Engineering Insights

When reproducing, first verify three points: **reward range** (confirm whether the task is in [0,1], except LQR), **observation structure** (physical quantities separated in OrderedDict, do not concatenate into a vector), and **integrator type** (semi-implicit Euler vs. fourth-order Runge-Kutta, affecting dynamics precision). The most common pitfall is the D4PG pixel variant's shared-weight design—separating actor/critic networks performs significantly worse, and the critic optimizer must update the shared convolutional layers. A3C performs extremely poorly on continuous control (humanoid run score ≈1); if your algorithm is on-policy without experience replay, expect similar difficulties on the Control Suite. DDPG's OU noise parameters (θ=0.15, σ=0.3) and soft update τ=1e-3 are validated defaults; changes should be made cautiously. The evaluation protocol fixes 1000-step summation with a maximum of 1000, but many tasks have actual maxima far below 1000 (e.g., acrobot swingup requires multiple steps to swing up), so the theoretical upper bound of each task must be considered when comparing.

## 개요

DeepMind Control Suite는 MuJoCo 물리 엔진 기반의 연속 제어 강화 학습 벤치마크 플랫폼으로, DeepMind 팀이 개발했으며 안정적이고 통일되며 해석 가능한 물리 제어 작업 세트를 제공하는 것을 목표로 합니다. 핵심 기여는 동작, 관측, 보상 구조를 표준화하고 A3C, DDPG, D4PG 세 가지 기준 알고리즘의 체계적 평가를 제공하여 연속 제어 분야의 표준 벤치마크 부재를 해소한 점입니다.

## 그것이 바꾼 것

ALE가 이산 비디오 게임 벤치마크를 주도하고, Gym이 연속 도메인을 포함하지만 구조가 느슨했던 시대에, Control Suite는 연속 제어 작업의 평가 패러다임을 진정으로 바꾸었습니다. 관측을 단순히 벡터로 이어 붙이는 대신, 물리적 단위를 가진 양(위치, 속도, 힘)을 분리하여 학습 곡선과 보상 구조가 작업 간 통일된 의미를 갖도록 했습니다—모든 작업의 보상 범위는 [0,1]이고, 평가 에피소드는 고정 1000스텝이며, 최대 점수는 항상 1000입니다. 이로 인해 서로 다른 작업의 성능을 직접 비교할 수 있으며, Gym처럼 작업마다 보상 척도가 크게 달라 집계가 어려운 문제를 해결했습니다.

더 중요한 것은, 저자가 "작업 해결 가능성 검증"을 벤치마크 구축의 핵심 단계로 승격시킨 점입니다. 단순히 작업을 공개하는 대신, 여러 학습 에이전트로 반복 테스트하여 물리 시뮬레이션이 안정적이고, "치트" 전략이 없으며, 최소 하나의 에이전트가 해결할 수 있음을 보장했습니다. 이러한 "검증된 벤치마크" 철학은 Control Suite를 후속 알고리즘 비교의 신뢰할 수 있는 출발점으로 만들었으며, 일부 Gym 작업처럼 숨겨진 취약성이나 의도치 않은 해법이 있는 문제를 피했습니다.

## 방법 분해

### 작업 아키텍처
- 모든 작업은 Python으로 작성되고 MuJoCo로 구동되며, 물리 모델은 MJCF로 정의됨
- 도메인(물리 모델)과 작업(특정 MDP 구조)이 분리되고, 작업은 BENCHMARKING 및 EXTRA 튜플로 태그에 따라 구성됨

### 표준화 설계
- **동작**: LQR을 제외하고, 동작은 단위 박스 a ∈ [-1,1]^dim(A)로 제한됨
- **관측**: point-mass:hard를 제외한 모든 작업은 강한 관측 가능성(상태가 단일 관측에서 복원 가능)을 가지며, OrderedDict로 구현됨
- **보상**: LQR을 제외하고, 보상 r(s,a) ∈ [0,1]이며, tolerance() 함수로 생성되고, 희소({0,1}) 및 평활 변형을 지원함
- **종료**: 종료 상태나 시간 제한이 없고, 무한 시간 지평; 실제로는 할인 공식 ∑γ^i r, γ = e^{-h/τ} 사용

### 핵심 설계 결정
- **적분기 선택**: 대부분의 도메인은 MuJoCo 기본 반암시적 오일러를 사용하고, 일부 평활한 에너지 보존 역학은 4차 Runge-Kutta를 사용
- **Physics.step() 순서**: 먼저 mj_step2(제어에 의존) 후 mj_step1(상태에만 의존)을 실행하여 위치/속도 양이 현재 상태와 동기화되고, 힘/가속도 양이 이전 전이와 동기화되도록 함
- **초기화 다양성**: 상태는 부분 집합에서 초기화되어 단일 상태로 인한 "암기" 해법을 방지
- **LQR 도메인**: 해석적 전이 행렬과 Riccati 최적 정책을 가지며, 제어와 보상이 무계이므로 벤치마크 집합에 포함되지 않음

### 기준 알고리즘 구성
- **A3C**: 32 workers, 2층 MLP(256 은닉 유닛), RMSProp(α=0.99, ε=0.1), 엔트로피 정규화 β=3e-3, 학습률 5e-5에서 0으로 선형 annealing
- **DDPG**: actor 300→200, critic 400→300, Adam(η=1e-4), 소프트 업데이트 τ=1e-3, OU 노이즈(θ=0.15, σ=0.3), replay buffer 1e6, minibatch 64
- **D4PG**: 32 CPU actors + 1 GPU learner, critic 값 분포 101 클래스 [-150,150], N-step N=5, 비균일 샘플링 α=0.6, 하드 업데이트 매 100스텝, minibatch 256

## 핵심 혁신

1. **통일된 보상 구조**: 모든 작업의 보상이 [0,1]이고, 고정 1000스텝 평가와 결합되어 학습 곡선의 y축이 일관되어 작업 간 직접 비교가 가능합니다. 이는 Gym이 제공할 수 없는 것입니다—Gym 작업은 보상 척도 차이가 커서 성능 집계가 무의미합니다.

2. **검증 기반 작업 설계**: 저자는 여러 학습 에이전트로 작업을 반복 테스트하여 물리적 안정성, 악용 불가능성, 최소 하나의 에이전트 해결 가능성을 보장합니다. 이는 벤치마크 작업이 "너무 쉬움"(치트 가능) 또는 "너무 어려움"(어떤 알고리즘도 해결 불가)의 함정을 피하게 하며, 벤치마크 신뢰도의 핵심 보장입니다.

3. **픽셀 입력 변형의 체계적 평가**: D4PG 픽셀 변형은 84×84 RGB 프레임, 2층 ConvNet(3×3 커널, 32 채널, ELU, stride 2), 프레임 스태킹 3개 관측을 제공하며, actor-critic이 컨볼루션 가중치를 공유할 때(critic 옵티마이저만 업데이트) 분리 네트워크보다 훨씬 우수함을 발견했습니다. 이는 시각 제어 작업에 재현 가능한 아키텍처 기준을 제공합니다.

## 실험 및 결과

| 작업 | A3C | D4PG | D4PG (Pixels) | DDPG |
|------|-----|------|---------------|------|
| acrobot swingup | 41.9±1.2 | 297.6±8.4 | 81.7±4.4 | 15.4±0.9 |
| ball_in_cup catch | 104.7±7.8 | 981.2±0.7 | 980.5±0.5 | 984.5±0.3 |
| cartpole balance_sparse | 857.4±7.9 | 1000.0±0.0 | 1000.0±0.0 | 878.5±8.0 |
| cheetah walk | 213.9±1.6 | 736.7±4.4 | 523.8±6.8 | 842.5±1.6 |
| humanoid run | 1.0±0.0 | 463.6±13.8 | 1.4±0.0 | 167.9±4.1 |
| manipulator bring_ball | 0.4±0.0 | 895.9±3.7 | 0.5±0.1 | 0.6±0.1 |
| pendulum swingup | 48.6±5.2 | 836.2±5.0 | 680.9±41.9 | 816.2±4.7 |
| reacher hard | 39.7±2.9 | 973.0±2.0 | 957.1±5.4 | 904.3±6.8 |
| swimmer swimmer6 | 177.8±7.8 | 664.7±11.1 | 194.7±15.9 | 394.0±14.1 |
| walker run | 191.8±1.9 | 839.7±0.7 | 567.2±18.9 | 786.2±0.4 |

(표 1: 10^8 훈련 스텝 후 100 에피소드 평균 및 표준 오차)

핵심 발견:
- D4PG가 대부분의 작업에서 최적이지만, DDPG는 1e7 스텝 전에 데이터 효율성이 더 높음
- 픽셀 변형은 ball_in_cup, cartpole, reacher에서 상태 특징 성능에 근접하지만, humanoid run, manipulator bring_ball에서는 완전히 실패(점수≈1)
- A3C는 humanoid run에서 거의 학습하지 못함(1.0±0.0), 연속 제어의 off-policy 방법 의존성을 부각
- 24시간 훈련 후(표 2), D4PG는 acrobot swingup에서 531.1±8.9에 도달, 저자는 상한(스윙업 시간 제한)에 근접하다고 판단

## 경계와 한계

저자는 명시적으로 인정합니다: 동일한 알고리즘이 모든 작업에서 동일한 하이퍼파라미터를 사용하고, 철저한 최적화를 수행하지 않았으므로 결과를 성능 상한으로 간주해서는 안 됩니다. 픽셀 변형은 swimmer, fish 등의 작업에서 완전히 실패하며, 이는 카메라 시점이 픽셀 전용 접근에 부적합하기 때문입니다. Control Suite는 사족 보행 locomotion, 완전한 조작 작업, 복잡한 지형이 부족하며, 이러한 작업은 추론 작업과 모델 분포가 필요합니다. 대화형 시각화 도구, C 콜백, 다중 스레드 역학, TensorFlow op 래퍼, Windows 지원은 포함되지 않았습니다. 평가 분산은 무작위 목표/초기화의 영향을 받으며, 실행 간 초기화 시퀀스는 고정되지 않았습니다. 논문은 다중 작업 학습, 전이 학습, 계층적 RL 등의 확장 방향을 언급하지 않았습니다.

## 공학적 시사점

재현 시 세 가지를 먼저 확인하세요: **보상 범위**(작업이 [0,1]인지 확인, LQR 제외), **관측 구조**(OrderedDict에서 물리량 분리, 벡터로 이어 붙이지 말 것), **적분기 유형**(반암시적 오일러 vs 4차 Runge-Kutta, 역학 정밀도에 영향). 가장 함정에 빠지기 쉬운 것은 D4PG 픽셀 변형의 공유 가중치 설계입니다—actor/critic 네트워크를 분리하면 성능이 현저히 나빠지므로, critic 옵티마이저가 공유 컨볼루션 레이어를 업데이트해야 합니다. A3C는 연속 제어에서 매우 나쁜 성능을 보이며(humanoid run 점수≈1), 알고리즘이 on-policy이고 경험 리플레이가 없다면 Control Suite에서 유사한 어려움을 겪을 것으로 예상됩니다. DDPG의 OU 노이즈 파라미터(θ=0.15, σ=0.3)와 소프트 업데이트 τ=1e-3은 검증된 기본값이므로 변경에 주의가 필요합니다. 평가 프로토콜은 고정 1000스텝 합산, 최대 1000이지만, 많은 작업의 실제 최대값은 1000보다 훨씬 작습니다(예: acrobot swingup은 여러 스텝의 스윙업 필요), 비교 시 작업의 이론적 상한에 주의해야 합니다.
