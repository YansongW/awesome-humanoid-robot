---
$id: ent_paper_omnicontact_chaining_meta_skills_contact_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'OmniContact: Chaining Meta-Skills via Contact Flow for Generalizable Humanoid Loco-Manipulation'
  zh: 'OmniContact: Chaining Meta-Skills via Contact Flow for Generalizable Humanoid Loco-Manipulation'
  ko: 'OmniContact: Chaining Meta-Skills via Contact Flow for Generalizable Humanoid Loco-Manipulation'
summary:
  en: 'Learning long-horizon humanoid loco-manipulation poses a dual challenge: it requires not only the robust execution
    of meta-skills but also their seamless, closed-loop chaining equipped with autonomous recovery. Institutions per source
    list: 诺亦腾机器人（Noitom Robotics）、香港科技大学（HKUST）、武汉大学（WHU）、香港大学（HKU）.'
  zh: OmniContact 是一个面向人形机器人全身移动操作任务的层次化框架，由上海交通大学等机构提出。其核心创新在于引入“接触流（Contact Flow）”这一紧凑表示，包含关键身体轨迹与时间序列二值接触信号，并以此为基础构建了低层技能库（CF-Track）与高层规划器（CF-Gen）。实验表明，该方法在长时序任务中成功率显著优于现有基线，并能与
    VLM 结合实现语义驱动的复杂行为。
  ko: 'Learning long-horizon humanoid loco-manipulation poses a dual challenge: it requires not only the robust execution
    of meta-skills but also their seamless, closed-loop chaining equipped with autonomous recovery. Institutions per source
    list: 诺亦腾机器人（Noitom Robotics）、香港科技大学（HKUST）、武汉大学（WHU）、香港大学（HKU）.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- omnicontact
- chaining
- meta
- skills
- contact
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 727 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2606.26201 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2606.26201v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.26201 OmniContact: Chaining Meta-Skills via Contact Flow for Generalizable Humanoid Loco-Manipulation'
  url: https://arxiv.org/abs/2606.26201
  accessed_at: '2026-07-31'
  date: '2026-06-24'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

OmniContact 提出以“接触流”（Contact Flow）作为人形机器人移动操作中高层语义规划与低层全身执行之间的统一接口，通过 CF-Gen 规则规划器与 CF-Track 强化学习控制器分层协作，实现长时程、可恢复的技能链式组合。核心贡献在于用稀疏非均匀采样的身体轨迹加四维二值接触信号，同时兼顾表达力与紧凑性，在仿真中显著超越现有基线。

## 它改变了什么

人形移动操作长期卡在“表示”的选择上：完整的人-物交互（HOI）状态精度高但维度爆炸，难以支撑高层规划；隐式技能嵌入紧凑却不可解释，组合时缺乏可靠性保证。现有强化学习方案要么依赖遥操作开环执行、要么需要逐帧全身参考，本质上都回避了“技能如何被高层任务调用”这一核心问题。OmniContact 真正改变的是将“接触”从执行细节提升为规划原语——它不再让高层规划器去预测稠密运动学，而是只输出物体级目标，由接触流作为中间语言衔接语义与物理。这一改动让 VLM 等基础模型得以介入长时程任务分解，同时保留了低层策略的鲁棒性与恢复能力，是接口设计上的范式转移。

## 方法拆解

### 接触流（Contact Flow）表示
- 形式化：在时间步 t，接触流 F_t = {(b_{t+k}, c_{t+k})}_{k∈T}，其中 T = {0,1,2,3,4,8,12,16,24,32,50} 为非均匀未来帧偏移。
- b 为稀疏身体运动目标（躯干、手腕、踝关节位置与朝向），c ∈ {0,1}^4 为四维二值接触信号，指定末端执行器接触状态。
- 非均匀采样在近处密集、远处稀疏，兼顾短期精度与长期规划视野。

### CF-Track（低层控制器）
- 统一策略跟踪接触流目标，输入为 x_t = [F_t, H_t]，H_t 为 K=5 步历史缓冲。
- 观测包含本体感受（关节运动学、基座朝向、末端执行器位置、上一动作）与物体状态（相对 6D 位姿、包围盒）。
- 奖励：r_t = λ_track·r_t^track + λ_amp·r_t^amp + λ_reg·r_t^reg，最优权重 W_track–W_amp = 0.85–0.15。
- Actor 与 Critic 均采用 Transformer（隐藏维度 [512,256,128]，3 层，8 头），而非 MLP。

### CF-Gen（中层规划器）
- 基于规则的参考合成器：给定物体级目标、当前状态、物体位姿与尺寸，生成密集参考运动。
- 相位模板库：每个元技能分解为有序相位块（如搬运：接近→预抓取→抬升→行走→释放），每相位有明确接触语义。
- 关键帧生成：仅末端执行器放置需精确时用 IK（最多 20 次迭代），纯移动相位仅指定踝关节姿态，其余用默认名义姿态。
- IK 优化变量：骨盆高度 z_pelvis、骨盆俯仰 θ_pitch、所有关节自由度 q，严格排除腰部横滚与偏航。
- 轨迹插值：平移与关节用 LERP，旋转用 SLERP。
- 在线重规划：50 Hz 监控，检测偏差 δ_t 超阈值 ε 时中止当前计划并从当前状态重规划。

### 层次化执行与 VLM 集成
- CF-Gen 将任务目标转化为物体中心接触流片段，CF-Track 执行并持续监控，相位完成或失败时 CF-Gen 更新状态并合成下一片段。
- VLM（Gemini 3.1 Pro Preview）仅做物体级规划：识别物体、分配目标区域/位姿、选择元技能，输出 JSON 格式计划，不预测任何低层运动学。

## 关键创新

1. **接触流作为可组合的中间表示**：将接触信号显式编码为二值时序向量，与稀疏身体轨迹耦合。消融显示，加入接触项（C）将 [T, EE] 基线的成功率从 11.50% 提升至 98.70%，证明接触是技能组合的关键语义锚点，而非冗余细节。
2. **规则规划器与学习控制器的解耦**：CF-Gen 用启发式相位模板与 IK 生成参考，CF-Track 用 RL 跟踪并平滑伪影。这一设计避免了端到端学习对长时程组合的脆弱性，同时让重规划逻辑透明可调试。
3. **VLM 接口的极简约束**：将基础模型的输出限制为物体级子目标列表，不要求预测关节轨迹或接触时序。这使开放词汇语义推理（如“把圆柱体放进篮子”）能直接驱动低层接触丰富的执行，且失败模式可归因（模糊接地、不可行布局、执行偏差）。

## 实验与结果

| 任务 | OmniContact R_succ | 最佳基线 R_succ | OmniContact E_obj^T | 最佳基线 E_obj^T |
|---|---|---|---|---|
| Carry Box | 98.70% (±0.6) | PhysHSI 87.00% (±2.4) | 0.07 (±0.0) | PhysHSI 0.58 (±0.1) |
| Push Suitcase | 82.50% (±2.7) | LessMimic 12.50% (±2.4) | 0.27 (±0.0) | LessMimic 3.14 (±0.1) |
| Stack Boxes | 56.5%（最终阶段） | PhysHSI 0.0%（最终阶段） | - | - |
| Push-Stack Boxes | 76.5%（最终阶段） | LessMimic 0.0%（最终阶段） | - | - |

- 在线重规划显著提升：Push Suitcase 从 82.5% 提升至 94.5%（平均重规划 0.77 次），Stack Boxes 从 56.6% 提升至 80.5%（0.96 次）。
- 消融关键发现：显式接触建模（C）是成功率跃升的主因；加入全关节目标（Dof）反而导致失败（0.00%），说明过度指定自由度会压制策略的适应性。
- 奖励平衡：纯跟踪（1.0–0.0）稳定性仅 46.30%，最优平衡（0.85–0.15）达到 98.70% 成功率且稳定性 58.80%。
- 鲁棒性：Carry Box + Drop 最终成功率 92.5%（平均重规划 1.64 次），Object Pose Offset 下 97.5%。
- 长时程：协议 I（单物体顺序目标）40 分钟存活率 100%，但协议 III（多物体）40 分钟存活率降至 10.0%，平均物体掉落 26.0 次。
- 数据规模：从 10%（2.2h）扩展到 100%（22.3h），成功率与物体精度均提升。

## 边界与局限

论文未明确列出局限性声明，但可从实验推断：欠驱动夹爪限制精细操作，接触流扩展到灵巧手是未竟之事；基于规则的 CF-Gen 在高度动态场景（如快速踢球）中表现不佳，作者设想用可学习数据驱动方法替代启发式接触锚生成。所有定量结果均在仿真（Isaac Lab 训练、MuJoCo 评估）中获得，真实部署仅在 Unitree G1 上做了定性展示，无定量数据。长时程多物体任务（协议 III）成功率衰减严重（40 分钟仅 10%），表明技能链式组合在物体数量增加时仍存在累积误差问题。球类任务成功率较低（Relocate Ball 72.5%，Kick Ball 76.5%），因模拟球光滑刚性易滑动。

## 工程启示

复现时优先核对三点：一是接触流采样偏移 T 与历史窗口 K=5 的配置，这是表示紧凑性的关键；二是奖励权重 W_track–W_amp = 0.85–0.15，偏离此值会显著影响成功率（0.5–0.5 时仅 0.10%）；三是 IK 优化中排除腰部横滚与偏航的约束，这是保持躯干稳定的前提。最容易踩坑的地方在于：加入全关节目标（Dof）会破坏策略适应性（成功率降至 0.00%），因此 CF-Gen 合成参考时务必只指定躯干、手腕、踝关节，其余自由度交给策略自主决策。对下游团队，建议将接触流作为模块间接口标准——高层规划器只输出物体级目标，低层策略只消费接触流，中间不传递稠密运动学。重规划频率 50 Hz 与偏差阈值 ε 的设定需根据任务动态调整，推任务平均重规划 0.77 次即可显著提升成功率，但多物体长时程任务中重规划次数会随掉落事件激增，需预留计算余量。

## 参考
- https://arxiv.org/abs/2606.26201
- https://github.com/ImChong/Robotics_Notebooks

## Overview

OmniContact proposes "Contact Flow" as a unified interface between high-level semantic planning and low-level whole-body execution for humanoid mobile manipulation. Through hierarchical collaboration between the CF-Gen rule-based planner and the CF-Track reinforcement learning controller, it enables long-horizon, recoverable skill chaining. The core contribution lies in using sparsely non-uniformly sampled body trajectories coupled with four-dimensional binary contact signals, balancing expressiveness and compactness, significantly surpassing existing baselines in simulation.

## What It Changes

Humanoid mobile manipulation has long been stuck on the choice of "representation": full human-object interaction (HOI) states offer high precision but suffer from dimensionality explosion, making high-level planning difficult; implicit skill embeddings are compact but uninterpretable, lacking reliability guarantees when composed. Existing RL approaches either rely on teleoperation for open-loop execution or require frame-by-frame full-body references, essentially sidestepping the core question of "how skills are invoked by high-level tasks." What OmniContact truly changes is elevating "contact" from an execution detail to a planning primitive—it no longer requires the high-level planner to predict dense kinematics, but only to output object-level goals, with contact flow serving as an intermediate language bridging semantics and physics. This shift allows foundation models like VLMs to intervene in long-horizon task decomposition while preserving the robustness and recovery capabilities of low-level policies—a paradigm shift in interface design.

## Method Breakdown

### Contact Flow Representation
- Formalization: At time step t, contact flow F_t = {(b_{t+k}, c_{t+k})}_{k∈T}, where T = {0,1,2,3,4,8,12,16,24,32,50} are non-uniform future frame offsets.
- b represents sparse body motion targets (torso, wrist, ankle positions and orientations), c ∈ {0,1}^4 is a four-dimensional binary contact signal specifying end-effector contact states.
- Non-uniform sampling is dense at near horizons and sparse at far horizons, balancing short-term precision with long-term planning horizon.

### CF-Track (Low-Level Controller)
- A unified policy tracks contact flow targets, with input x_t = [F_t, H_t], where H_t is a K=5 step history buffer.
- Observations include proprioception (joint kinematics, base orientation, end-effector positions, previous actions) and object states (relative 6D pose, bounding box).
- Reward: r_t = λ_track·r_t^track + λ_amp·r_t^amp + λ_reg·r_t^reg, with optimal weights W_track–W_amp = 0.85–0.15.
- Both Actor and Critic use Transformers (hidden dimensions [512,256,128], 3 layers, 8 heads) rather than MLPs.

### CF-Gen (Mid-Level Planner)
- Rule-based reference synthesizer: given object-level goals, current state, object pose and dimensions, generates dense reference motion.
- Phase template library: each meta-skill is decomposed into ordered phase blocks (e.g., carrying: approach → pre-grasp → lift → walk → release), with each phase having explicit contact semantics.
- Keyframe generation: IK is used only when precise end-effector placement is required (up to 20 iterations); pure locomotion phases only specify ankle orientations, with default nominal poses for the rest.
- IK optimization variables: pelvis height z_pelvis, pelvis pitch θ_pitch, all joint DOFs q, strictly excluding pelvis roll and yaw.
- Trajectory interpolation: LERP for translation and joints, SLERP for rotation.
- Online replanning: monitored at 50 Hz; when deviation δ_t exceeds threshold ε, the current plan is aborted and replanning occurs from the current state.

### Hierarchical Execution and VLM Integration
- CF-Gen converts task goals into object-centric contact flow segments; CF-Track executes and continuously monitors. Upon phase completion or failure, CF-Gen updates the state and synthesizes the next segment.
- The VLM (Gemini 3.1 Pro Preview) performs only object-level planning: identifying objects, assigning target regions/poses, selecting meta-skills, and outputting plans in JSON format—without predicting any low-level kinematics.

## Key Innovations

1. **Contact flow as a composable intermediate representation**: Explicitly encodes contact signals as binary temporal vectors coupled with sparse body trajectories. Ablations show that adding the contact term (C) improves the [T, EE] baseline success rate from 11.50% to 98.70%, proving that contact is a critical semantic anchor for skill composition rather than redundant detail.
2. **Decoupling rule-based planner from learned controller**: CF-Gen generates references using heuristic phase templates and IK, while CF-Track tracks them with RL and smooths artifacts. This design avoids the fragility of end-to-end learning for long-horizon composition while keeping replanning logic transparent and debuggable.
3. **Minimal constraints on the VLM interface**: Restricts foundation model outputs to a list of object-level subgoals, without requiring prediction of joint trajectories or contact timings. This enables open-vocabulary semantic reasoning (e.g., "put the cylinder in the basket") to directly drive contact-rich low-level execution, with attributable failure modes (vague grounding, infeasible layouts, execution deviations).

## Experiments and Results

| Task | OmniContact R_succ | Best Baseline R_succ | OmniContact E_obj^T | Best Baseline E_obj^T |
|---|---|---|---|---|
| Carry Box | 98.70% (±0.6) | PhysHSI 87.00% (±2.4) | 0.07 (±0.0) | PhysHSI 0.58 (±0.1) |
| Push Suitcase | 82.50% (±2.7) | LessMimic 12.50% (±2.4) | 0.27 (±0.0) | LessMimic 3.14 (±0.1) |
| Stack Boxes | 56.5% (final stage) | PhysHSI 0.0% (final stage) | - | - |
| Push-Stack Boxes | 76.5% (final stage) | LessMimic 0.0% (final stage) | - | - |

- Online replanning provides significant gains: Push Suitcase improves from 82.5% to 94.5% (average 0.77 replans), Stack Boxes from 56.6% to 80.5% (0.96 replans).
- Key ablation findings: Explicit contact modeling (C) is the primary driver of the success rate jump; adding full joint targets (Dof) instead causes failure (0.00%), indicating that over-specifying DOFs suppresses policy adaptability.
- Reward balance: Pure tracking (1.0–0.0) achieves only 46.30% stability, while the optimal balance (0.85–0.15) reaches 98.70% success with 58.80% stability.
- Robustness: Carry Box + Drop achieves 92.5% final success (average 1.64 replans), and 97.5% under Object Pose Offset.
- Long-horizon: Protocol I (single-object sequential goals) achieves 100% survival over 40 minutes, but Protocol III (multi-object) drops to 10.0% survival over 40 minutes, with an average of 26.0 object drops.
- Data scale: Scaling from 10% (2.2h) to 100% (22.3h) improves both success rate and object precision.

## Boundaries and Limitations

The paper does not explicitly list a limitations statement, but inferences can be drawn from experiments: underactuated grippers limit fine manipulation, and extending contact flow to dexterous hands remains unfinished work; the rule-based CF-Gen performs poorly in highly dynamic scenarios (e.g., fast ball kicking), with the authors envisioning learnable data-driven methods to replace heuristic contact anchor generation. All quantitative results are obtained in simulation (trained in Isaac Lab, evaluated in MuJoCo), with real-world deployment only qualitatively demonstrated on Unitree G1 without quantitative data. Long-horizon multi-object tasks (Protocol III) show severe success degradation (only 10% over 40 minutes), indicating that skill chaining still suffers from cumulative errors as object count increases. Ball tasks have relatively low success rates (Relocate Ball 72.5%, Kick Ball 76.5%) due to the smooth rigid simulated balls being prone to sliding.

## Engineering Insights

When reproducing, prioritize verifying three points: first, the contact flow sampling offsets T and history window K=5 configuration, which are key to representation compactness; second, the reward weights W_track–W_amp = 0.85–0.15, as deviation significantly impacts success rate (0.10% at 0.5–0.5); third, the IK optimization constraint excluding pelvis roll and yaw, which is a prerequisite for torso stability. The most common pitfall: adding full joint targets (Dof) destroys policy adaptability (success drops to 0.00%), so CF-Gen must only specify torso, wrist, and ankle when synthesizing references, leaving other DOFs to the policy's autonomous decisions. For downstream teams, we recommend adopting contact flow as the interface standard between modules—high-level planners output only object-level goals, low-level policies consume only contact flow, with no dense kinematics passed in between. The 50 Hz replanning frequency and deviation threshold ε should be tuned according to task dynamics; push tasks achieve significant success gains with an average of 0.77 replans, but multi-object long-horizon tasks see replan counts surge with drop events, requiring reserved computational headroom.

## 개요

OmniContact는 휴머노이드 로봇의 이동 조작에서 고수준 의미론적 계획과 저수준 전신 실행 사이의 통합 인터페이스로 '접촉 흐름'(Contact Flow)을 제안합니다. CF-Gen 규칙 기반 플래너와 CF-Track 강화학습 컨트롤러의 계층적 협업을 통해 장시간 지속 가능하고 복구 가능한 스킬 체인 조합을 구현합니다. 핵심 기여는 희소 비균일 샘플링 신체 궤적과 4차원 이진 접촉 신호를 결합하여 표현력과 압축성을 동시에 확보하고, 시뮬레이션에서 기존 베이스라인을 크게 능가한다는 점입니다.

## 무엇을 바꾸었는가

휴머노이드 이동 조작은 오랫동안 '표현' 선택에 막혀 있었습니다. 완전한 인간-물체 상호작용(HOI) 상태는 정확도가 높지만 차원이 폭발하여 고수준 계획을 지원하기 어렵고, 암시적 스킬 임베딩은 압축적이지만 해석이 불가능하며 조합 시 신뢰성 보장이 부족합니다. 기존 강화학습 접근법은 원격 조작 개루프 실행에 의존하거나 프레임별 전신 참조가 필요하여 본질적으로 '스킬이 고수준 작업에 의해 어떻게 호출되는가'라는 핵심 문제를 회피했습니다. OmniContact가 진정으로 바꾼 것은 '접촉'을 실행 세부사항에서 계획 원시어(primitive)로 승격시킨 것입니다. 더 이상 고수준 플래너가 조밀한 운동학을 예측하도록 요구하지 않고 물체 수준 목표만 출력하며, 접촉 흐름이 의미론과 물리 사이의 중간 언어 역할을 합니다. 이 변경으로 VLM과 같은 기반 모델이 장시간 작업 분해에 개입할 수 있게 되면서도 저수준 정책의 견고성과 복구 능력을 유지하며, 인터페이스 설계의 패러다임 전환입니다.

## 방법 분해

### 접촉 흐름(Contact Flow) 표현
- 형식화: 시간 단계 t에서 접촉 흐름 F_t = {(b_{t+k}, c_{t+k})}_{k∈T}, 여기서 T = {0,1,2,3,4,8,12,16,24,32,50}은 비균일 미래 프레임 오프셋입니다.
- b는 희소 신체 운동 목표(몸통, 손목, 발목 관절 위치 및 방향), c ∈ {0,1}^4는 4차원 이진 접촉 신호로 말단 실행기 접촉 상태를 지정합니다.
- 비균일 샘플링은 가까운 곳은 밀집, 먼 곳은 희소하여 단기 정밀도와 장기 계획 시야를 동시에 고려합니다.

### CF-Track(저수준 컨트롤러)
- 통합 정책이 접촉 흐름 목표를 추적하며, 입력은 x_t = [F_t, H_t], H_t는 K=5 단계 히스토리 버퍼입니다.
- 관측에는 고유수용감각(관절 운동학, 베이스 방향, 말단 실행기 위치, 이전 행동)과 물체 상태(상대 6D 포즈, 경계 상자)가 포함됩니다.
- 보상: r_t = λ_track·r_t^track + λ_amp·r_t^amp + λ_reg·r_t^reg, 최적 가중치 W_track–W_amp = 0.85–0.15.
- Actor와 Critic 모두 MLP가 아닌 Transformer(은닉 차원 [512,256,128], 3계층, 8헤드)를 사용합니다.

### CF-Gen(중간 계층 플래너)
- 규칙 기반 참조 합성기: 물체 수준 목표, 현재 상태, 물체 포즈 및 크기가 주어지면 밀집 참조 운동을 생성합니다.
- 위상 템플릿 라이브러리: 각 메타 스킬은 순서화된 위상 블록으로 분해됩니다(예: 운반: 접근→사전 파지→들어올리기→보행→놓기), 각 위상은 명확한 접촉 의미론을 가집니다.
- 키프레임 생성: 말단 실행기 배치가 정밀해야 하는 경우에만 IK(최대 20회 반복)를 사용하고, 순수 이동 위상은 발목 관절 자세만 지정하며 나머지는 기본 명목 자세를 사용합니다.
- IK 최적화 변수: 골반 높이 z_pelvis, 골반 피치 θ_pitch, 모든 관절 자유도 q, 허리 롤과 요(yaw)는 엄격히 제외합니다.
- 궤적 보간: 병진과 관절은 LERP, 회전은 SLERP를 사용합니다.
- 온라인 재계획: 50Hz 모니터링, 편차 δ_t가 임계값 ε를 초과하면 현재 계획을 중단하고 현재 상태에서 재계획합니다.

### 계층적 실행 및 VLM 통합
- CF-Gen은 작업 목표를 물체 중심 접촉 흐름 세그먼트로 변환하고, CF-Track은 실행하며 지속적으로 모니터링합니다. 위상 완료 또는 실패 시 CF-Gen이 상태를 업데이트하고 다음 세그먼트를 합성합니다.
- VLM(Gemini 3.1 Pro Preview)은 물체 수준 계획만 수행합니다: 물체 인식, 목표 영역/포즈 할당, 메타 스킬 선택, JSON 형식 계획 출력, 저수준 운동학은 전혀 예측하지 않습니다.

## 핵심 혁신

1. **접촉 흐름의 조합 가능한 중간 표현**: 접촉 신호를 이진 시계열 벡터로 명시적으로 인코딩하고 희소 신체 궤적과 결합합니다. 절제 실험에서 접촉 항(C) 추가 시 [T, EE] 베이스라인의 성공률이 11.50%에서 98.70%로 향상되어, 접촉이 스킬 조합의 핵심 의미론적 앵커임을 증명합니다. 중복 세부사항이 아닙니다.
2. **규칙 기반 플래너와 학습 컨트롤러의 분리**: CF-Gen은 휴리스틱 위상 템플릿과 IK로 참조를 생성하고, CF-Track은 RL로 추적하며 아티팩트를 평활화합니다. 이 설계는 엔드투엔드 학습의 장시간 조합 취약성을 피하면서 재계획 로직을 투명하고 디버깅 가능하게 만듭니다.
3. **VLM 인터페이스의 극소 제약**: 기반 모델의 출력을 물체 수준 하위 목표 목록으로 제한하고 관절 궤적이나 접촉 타이밍 예측을 요구하지 않습니다. 이를 통해 개방 어휘 의미론적 추론(예: "원통을 바구니에 넣어")이 저수준 접촉 풍부 실행을 직접 구동할 수 있으며, 실패 모드가 귀인 가능합니다(모호한 접지, 불가능한 배치, 실행 편차).

## 실험 및 결과

| 작업 | OmniContact R_succ | 최고 베이스라인 R_succ | OmniContact E_obj^T | 최고 베이스라인 E_obj^T |
|---|---|---|---|---|
| Carry Box | 98.70% (±0.6) | PhysHSI 87.00% (±2.4) | 0.07 (±0.0) | PhysHSI 0.58 (±0.1) |
| Push Suitcase | 82.50% (±2.7) | LessMimic 12.50% (±2.4) | 0.27 (±0.0) | LessMimic 3.14 (±0.1) |
| Stack Boxes | 56.5%(최종 단계) | PhysHSI 0.0%(최종 단계) | - | - |
| Push-Stack Boxes | 76.5%(최종 단계) | LessMimic 0.0%(최종 단계) | - | - |

- 온라인 재계획의 유의미한 향상: Push Suitcase는 82.5%에서 94.5%(평균 재계획 0.77회), Stack Boxes는 56.6%에서 80.5%(0.96회)로 향상.
- 절제 실험 핵심 발견: 명시적 접촉 모델링(C)이 성공률 도약의 주요 원인; 전관절 목표(Dof) 추가는 오히려 실패(0.00%)를 초래하여, 자유도 과도 지정이 정책의 적응성을 억제함을 시사.
- 보상 균형: 순수 추적(1.0–0.0) 안정성은 46.30%에 불과, 최적 균형(0.85–0.15)은 98.70% 성공률과 58.80% 안정성 달성.
- 견고성: Carry Box + Drop 최종 성공률 92.5%(평균 재계획 1.64회), Object Pose Offset 하에서 97.5%.
- 장시간: 프로토콜 I(단일 물체 순차 목표) 40분 생존율 100%, 그러나 프로토콜 III(다중 물체) 40분 생존율 10.0%로 하락, 평균 물체 낙하 26.0회.
- 데이터 규모: 10%(2.2h)에서 100%(22.3h)로 확장 시 성공률과 물체 정밀도 모두 향상.

## 경계 및 한계

논문은 한계 선언을 명시적으로 나열하지 않았지만 실험에서 추론할 수 있습니다: 저구동 그리퍼는 정밀 조작을 제한하며, 접촉 흐름을 다섯 손가락 로봇 손으로 확장하는 것은 미해결 과제입니다; 규칙 기반 CF-Gen은 고도로 동적인 시나리오(예: 빠른 공 차기)에서 성능이 좋지 않으며, 저자들은 휴리스틱 접촉 앵커 생성을 학습 가능한 데이터 기반 방법으로 대체할 것을 구상합니다. 모든 정량적 결과는 시뮬레이션(Isaac Lab 훈련, MuJoCo 평가)에서 얻었으며, 실제 배포는 Unitree G1에서 정성적 시연만 수행했고 정량적 데이터는 없습니다. 장시간 다중 물체 작업(프로토콜 III)은 성공률 감소가 심각하며(40분에 10%만), 스킬 체인 조합이 물체 수 증가 시 누적 오류 문제가 여전히 존재함을 나타냅니다. 공 작업 성공률은 낮으며(Relocate Ball 72.5%, Kick Ball 76.5%), 시뮬레이션 공이 매끄럽고 강체라 미끄러지기 쉽기 때문입니다.

## 공학적 시사점

재현 시 세 가지를 우선 확인하십시오: 첫째, 접촉 흐름 샘플링 오프셋 T와 히스토리 창 K=5의 구성 — 표현 압축성의 핵심; 둘째, 보상 가중치 W_track–W_amp = 0.85–0.15 — 이 값에서 벗어나면 성공률에 큰 영향(0.5–0.5일 때 0.10%에 불과); 셋째, IK 최적화에서 허리 롤과 요를 제외하는 제약 — 몸통 안정성 유지의 전제. 가장 함정에 빠지기 쉬운 지점: 전관절 목표(Dof) 추가는 정책 적응성을 파괴하므로(성공률 0.00% 하락), CF-Gen 참조 합성 시 반드시 몸통, 손목, 발목만 지정하고 나머지 자유도는 정책의 자율 결정에 맡겨야 합니다. 하류 팀에게는 접촉 흐름을 모듈 간 인터페이스 표준으로 채택할 것을 권장합니다 — 고수준 플래너는 물체 수준 목표만 출력하고, 저수준 정책은 접촉 흐름만 소비하며, 중간에 밀집 운동학을 전달하지 않습니다. 재계획 주파수 50Hz와 편차 임계값 ε의 설정은 작업 동적 특성에 따라 조정해야 하며, 밀기 작업은 평균 재계획 0.77회로 성공률을 크게 높일 수 있지만, 다중 물체 장시간 작업에서는 낙하 이벤트에 따라 재계획 횟수가 급증하므로 계산 여유를 확보해야 합니다.
