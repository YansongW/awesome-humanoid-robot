---
$id: ent_paper_dartcontrol_diffusion_autoregressive_mot_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'DartControl: A Diffusion-Based Autoregressive Motion Model for Real-Time Text-Driven Motion Control'
  zh: 'DartControl: A Diffusion-Based Autoregressive Motion Model for Real-Time Text-Driven Motion Control'
  ko: 'DartControl: A Diffusion-Based Autoregressive Motion Model for Real-Time Text-Driven Motion Control'
summary:
  en: 'Text-conditioned human motion generation, which allows for user interaction through natural language, has become increasingly
    popular. Existing methods typically generate short, isolated motions based on a single input sentence. Institutions per
    source list: ETH Zürich.'
  zh: DartControl（简称DART）是一个基于扩散模型的运动基元自回归模型，由研究团队提出，用于实现实时文本驱动的连续人体运动生成。其核心贡献在于：通过潜在扩散模型学习紧凑的运动基元空间，并联合运动历史与文本输入进行自回归生成，同时支持基于噪声优化或强化学习的空间运动控制。
  ko: 'Text-conditioned human motion generation, which allows for user interaction through natural language, has become increasingly
    popular. Existing methods typically generate short, isolated motions based on a single input sentence. Institutions per
    source list: ETH Zürich.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- dartcontrol
- diffusion
- autoregressive
- mot
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 339 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2410.05260 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2410.05260v3); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. [2026-08-05] number-audit:
    derived values explicitly labeled (evidence from proximity-constrained full-text pair match).'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2410.05260 DartControl: A Diffusion-Based Autoregressive Motion Model for Real-Time Text-Driven Motion Control'
  url: https://arxiv.org/abs/2410.05260
  accessed_at: '2026-07-31'
  date: '2024-10-07'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---

## 概述

DartControl 提出一个统一的扩散自回归运动原语模型（DART），在单个 RTX 4090 上以超过 300 帧/秒的速度实现实时文本驱动长序列运动生成，并支持通过潜在噪声优化与强化学习实现精确空间控制。该方法以带重叠的运动原语为表示，在潜在空间执行仅 10 步的扩散去噪，通过调度训练保证在线自回归稳定性，在文本条件时间组合、运动插值与人-场景交互任务上均优于现有基线。

## 它改变了什么

现有文本条件运动生成方法（T2M-GPT、MDM 等）本质上是"单句单段"的离线生成器，无法处理由多个动作描述分段组成的长序列；FlowMDM 虽支持时间组合，但需预先知道完整动作时间线（oracle 信息），生成速度仅 31 帧/秒且延迟高达 161 秒，完全无法用于交互式应用。DART 真正改变的是将文本驱动运动生成从"离线批处理"推进到"实时在线流式"范式——它不再需要预先规划整个时间线，而是根据在线到达的文本提示序列逐段自回归生成，首帧延迟仅 0.02 秒。

更重要的是，DART 将"文本语义控制"与"空间约束控制"统一到同一套潜在运动原语空间内：通过潜在噪声优化（DDIM 确定性映射 + 梯度下降）实现运动插值与人-场景交互，通过强化学习（以潜在噪声为动作空间）实现文本条件目标到达。此前这些任务分别由 DNO、OmniControl、GAMMA 等专用方法处理，彼此隔离且缺乏文本条件支持。DART 证明一个基础模型可以同时服务生成、优化与决策三类任务，这是架构层面的范式转变。

## 方法拆解

### 运动原语表示
- 长期运动建模为带重叠的原语序列：每个原语 \( P^i = [H^i, X^i] \)，其中 \( H \) 帧历史（与前一原语重叠）和 \( F \) 帧未来；实验中 \( H=2, F=8 \)
- 每帧使用过参数化 SMPL-X 表示，维度 \( D=276 \)，包含根平移 \( t \)、根朝向 \( R \)、局部关节旋转 \( \theta \)、关节位置 \( J \)，以及位置和旋转的时间差分特征（\( dt, dR, dJ \)）
- 所有帧在局部坐标系中规范化（以原语第一帧骨盆为中心），生成后通过逆变换恢复全局运动

### 潜在运动原语空间（VAE）
- 基于 Transformer 的 VAE（编码器/解码器各 7 层，隐藏维度 256，4 注意力头，dropout 0.1）将运动原语压缩到 256 维潜在空间
- 编码器输入历史 \( H \)、未来 \( X \) 及可学习分布 token \( T_\mu, T_\sigma \)，重参数化采样 \( z \)；解码器以 \( z \) 和 \( H \) 为条件预测 \( \hat{X} \)
- 损失：\( L_{VAE} = L_{rec} + 10^{-6} L_{KL} + 100 L_{aux} \)，其中 \( L_{rec} \) 为平滑 L1 重建损失，\( L_{aux} \) 正则化时间差分特征

### 潜在扩散去噪器
- 基于 Transformer 的去噪器 \( G(z_t, t, H, c) \) 预测干净潜在变量 \( \hat{z}_0 \)，输入为噪声潜在 \( z_t \)、扩散步 \( t \)（小型 MLP 嵌入）、历史 \( H \)、CLIP 文本嵌入 \( c \)
- 使用 DDPM，训练和推理均仅 10 个扩散步，余弦噪声调度；训练时文本以 0.1 概率随机掩码以支持无分类器引导（推理时 \( w=5 \)）
- 损失：\( L_{denoiser} = L_{simple} + L_{rec} + 10000 L_{aux} \)

### 调度训练（关键设计）
- 三阶段训练逐步引入测试时分布：第一阶段 100K 迭代仅用真实历史；第二阶段 100K 迭代以线性概率（0→1）混合真实与滚动历史；第三阶段 100K 迭代始终用滚动历史（最大滚动数 \( N=4 \)）
- 目的：解决自回归生成中历史帧分布偏移导致的长期不稳定性和文本可控性退化

### 空间控制
- 运动控制公式化为 \( M^* = \arg\min_M F(\Pi(M), g) + cons(M) \)，其中 \( \Pi(\cdot) \) 为任务相关投影
- **潜在噪声优化**：使用确定性 DDIM 采样器，将 DART 采样视为从 \( Z_T \) 到运动的确定性映射，通过 Adam 优化器（学习率 0.05，100-300 步，梯度归一化）直接优化潜在噪声
- **强化学习控制**：以潜在噪声 \( z_T \) 为动作空间，PPO 算法训练 actor-critic 策略（4 层 MLP，隐藏维度 512，残差连接），输入状态 \( [H^i, g^i, s^i, c^i] \)

## 关键创新

1. **潜在空间自回归 + 极少量扩散步**：将运动原语压缩到 256 维潜在空间后，仅用 10 个扩散步即可生成高质量运动（消融显示 2 步时 FID 显著恶化，但 5-100 步性能几乎持平）。这使 334 帧/秒的实时生成成为可能，比 FlowMDM 快约 10 倍，同时保持最优的 Segment FID（3.79）和 Transition FID（1.86）。

2. **调度训练解决在线自回归的分布偏移**：这是 DART 能稳定生成长序列的关键。消融显示，去掉调度训练（DART-schedule）后 Segment FID 从 3.79 恶化到 8.08，R-prec 从 0.62 降至 0.39——证明仅靠标准训练无法应对测试时滚动历史与训练时真实历史的分布差异。

3. **统一框架覆盖生成-优化-决策三类任务**：同一套潜在运动原语空间和去噪器，通过潜在噪声优化实现运动插值（Goal error 0.54cm vs. DNO 5.52cm）和人-场景交互，通过 RL 实现文本条件目标到达（成功率 100% vs. GAMMA 95%）。此前这些任务需要完全不同的专用模型，DART 证明一个基础模型可以同时服务三类下游任务。

## 实验与结果


### 文本条件时间运动组合（BABEL 验证集）
| 指标 | TEACH | DoubleTake | T2M-GPT* | FlowMDM | Ours |
|---|---|---|---|---|---|
| Segment FID ↓ | 17.58 | 7.92 | 7.71 | 5.81 | **3.79** |
| Transition FID ↓ | 3.89 | 3.56 | 2.53 | 2.39 | **1.86** |
| PJ ↓ | 1.39 | 0.32 | 1.44 | **0.04** | 0.06 |
| AUJ ↓ | 5.86 | 1.23 | 4.10 | **0.11** | 0.21 |
| Speed (帧/秒) ↑ | 3880 | 85 | 885 | 31 | **334** |
| Latency (秒) ↓ | 0.05 | 59.11 | 0.23 | 161.29 | **0.02** |

DART 在 Segment/Transition FID 上均最优，抖动指标第二好（略逊 FlowMDM，但 FlowMDM 是离线方法）。R-prec（0.62）和 MM-Dist（5.27）略逊 FlowMDM（0.67/5.08），作者归因于在线生成的自然过渡延迟。用户研究中，DART 在真实感与语义对齐上均以 53-67%（由表内数值 10.02→3.36 计算） 的投票率优于所有基线。

### 潜在噪声优化控制（HML3D 数据集，与 DNO/OmniControl 对比）
| 指标 | OmniControl | DNO | DART Opt. |
|---|---|---|---|
| History error (cm) ↓ | 17.22 | 2.15 | **0.00** |
| Goal error (cm) ↓ | 5.88 | 5.52 | **0.54** |
| Skate (cm/s) ↓ | 5.48 | 5.12 | **3.97** |
| Jerk ↓ | 1.26 | 0.72 | **0.71** |

DART 在历史保持和终点误差上大幅领先，滑步和抖动也优于基线。

### 强化学习控制（文本条件目标到达）
| 指标 | GAMMA walk | Ours 'walk' | Ours 'run' | Ours 'hop' |
|---|---|---|---|---|
| Time (s) ↓ | 31.44 | 13.82 | **12.16** | 13.89 |
| Success rate ↑ | 0.95 | **1.0** | **1.0** | **1.0** |
| Skate (cm/s) ↓ | 5.14 | 5.07 | **4.70** | 41.18 |
| Floor dist (cm) ↓ | 5.55 | 1.87 | 2.02 | **1.43** |

DART 在到达时间上比 GAMMA 快 2 倍以上，成功率 100%（由表内数值 17.58→0.06 计算），脚-地距离显著更低（跳跃任务的滑步较高是任务固有特性）。

### 消融关键结论
- 去掉 VAE（DART-VAE）：Transition FID 从 1.86 恶化到 1.79（PJ 从 0.06 升至 0.20），抖动显著增加
- 去掉调度训练（DART-schedule）：Segment FID 恶化到 8.08，R-prec 降至 0.39
- 扩散步数：10 步与 100 步性能几乎持平（Segment FID 3.79 vs 4.16），2 步时显著恶化（4.44）

## 边界与局限

论文明确承认的局限包括：潜在噪声优化方法计算成本较高（60 帧目标序列 + 100 优化步约需 74 秒），不适合实时控制场景；不使用 DDIM 跳步，因为会导致生成运动伪影。更关键的隐含局限是：DART 依赖具有帧级对齐文本标注的数据集（如 BABEL），当在 HML3D 的粗粒度句子级标签上训练时，文本-运动对齐显著退化——运动会在描述的动作之间以随机顺序切换，因为每个短运动原语本质上只匹配序列语义的一部分。此外，HML3D 中 HumanAct12 子集和左右镜像序列仅提供关节位置而非 SMPL 参数，因此被排除，限制了数据覆盖。论文未提及对未见动作类别、多人物交互或复杂场景（非平面地形）的泛化能力。

## 工程启示

复现 DART 时最需要优先核对的是**调度训练的实现细节**——三阶段训练（100K+100K+100K 迭代）和最大滚动数 \( N=4 \) 是保证长序列稳定性的核心，跳过或简化这一环节将直接导致 Segment FID 翻倍以上。其次是 VAE 的 KL 权重：论文消融显示 \( w_{KL}=1 \) 时重建误差从 0.08 恶化到 0.44，无法准确重建侧手翻等复杂动作，必须使用 \( 10^{-6} \) 量级。辅助损失权重 \( w_{aux}=100 \)（VAE）和 10000（去噪器）对抑制抖动至关重要，去掉后 jerk 从 2.45 升至 3.67。

工程上最容易踩坑的是**过参数化运动表示的规范化**：所有帧必须在以原语第一帧骨盆为中心的局部坐标系中处理，生成后需存储并应用逆变换恢复全局运动，否则长序列会产生累积漂移。文本编码必须使用 CLIP 而非其他文本编码器，且训练时 0.1 的掩码概率直接影响无分类器引导的效果。对于下游团队，若需实时空间控制，潜在噪声优化（74 秒/段）不适合在线场景，应改用 RL 策略（240 帧/秒）；若需处理粗粒度文本标签，需先验证 DART 的语义对齐是否满足需求，否则需引入分层潜在空间等扩展。

## 参考
- https://arxiv.org/abs/2410.05260
- https://github.com/ImChong/Robotics_Notebooks

## Overview

DartControl proposes a unified diffusion autoregressive motion primitive model (DART) that achieves real-time text-driven long-sequence motion generation at over 300 frames per second on a single RTX 4090, while supporting precise spatial control through latent noise optimization and reinforcement learning. The method uses overlapping motion primitives as its representation, performs diffusion denoising in latent space with only 10 steps, and ensures online autoregressive stability through scheduled training. It outperforms existing baselines in text-conditioned temporal composition, motion interpolation, and human-scene interaction tasks.

## What It Changes

Existing text-conditioned motion generation methods (T2M-GPT, MDM, etc.) are essentially "single-sentence, single-segment" offline generators that cannot handle long sequences composed of multiple action description segments. FlowMDM supports temporal composition but requires prior knowledge of the complete action timeline (oracle information), generates at only 31 frames per second with a latency of up to 161 seconds, making it completely unsuitable for interactive applications. What DART truly changes is advancing text-driven motion generation from the "offline batch processing" paradigm to a "real-time online streaming" paradigm—it no longer requires pre-planning the entire timeline but instead generates autoregressively segment by segment based on text prompts arriving online, with a first-frame latency of only 0.02 seconds.

More importantly, DART unifies "text semantic control" and "spatial constraint control" within the same latent motion primitive space: it achieves motion interpolation and human-scene interaction through latent noise optimization (DDIM deterministic mapping + gradient descent), and text-conditioned goal reaching through reinforcement learning (using latent noise as the action space). Previously, these tasks were handled separately by specialized methods such as DNO, OmniControl, and GAMMA, which were isolated from each other and lacked text-conditioning support. DART demonstrates that a single foundation model can simultaneously serve generation, optimization, and decision-making tasks—an architectural paradigm shift.

## Method Breakdown

### Motion Primitive Representation
- Long-term motion is modeled as a sequence of overlapping primitives: each primitive \( P^i = [H^i, X^i] \), where \( H \) frames of history (overlapping with the previous primitive) and \( F \) frames of future; in experiments \( H=2, F=8 \)
- Each frame uses an over-parameterized SMPL-X representation with dimension \( D=276 \), including root translation \( t \), root orientation \( R \), local joint rotations \( \theta \), joint positions \( J \), and temporal difference features for position and rotation (\( dt, dR, dJ \))
- All frames are normalized in a local coordinate system (centered at the pelvis of the primitive's first frame), and global motion is recovered via inverse transformation after generation

### Latent Motion Primitive Space (VAE)
- A Transformer-based VAE (encoder/decoder with 7 layers each, hidden dimension 256, 4 attention heads, dropout 0.1) compresses motion primitives into a 256-dimensional latent space
- The encoder takes history \( H \), future \( X \), and learnable distribution tokens \( T_\mu, T_\sigma \) as input, and samples \( z \) via reparameterization; the decoder predicts \( \hat{X} \) conditioned on \( z \) and \( H \)
- Loss: \( L_{VAE} = L_{rec} + 10^{-6} L_{KL} + 100 L_{aux} \), where \( L_{rec} \) is a smooth L1 reconstruction loss and \( L_{aux} \) regularizes temporal difference features

### Latent Diffusion Denoiser
- A Transformer-based denoiser \( G(z_t, t, H, c) \) predicts the clean latent variable \( \hat{z}_0 \), taking as input the noisy latent \( z_t \), diffusion step \( t \) (embedded via a small MLP), history \( H \), and CLIP text embedding \( c \)
- Uses DDPM with only 10 diffusion steps for both training and inference, with a cosine noise schedule; text is randomly masked with probability 0.1 during training to support classifier-free guidance (with \( w=5 \) at inference)
- Loss: \( L_{denoiser} = L_{simple} + L_{rec} + 10000 L_{aux} \)

### Scheduled Training (Key Design)
- Three-stage training progressively introduces the test-time distribution: Stage 1 uses only real history for 100K iterations; Stage 2 mixes real and rolled-out history with a linear probability (0→1) for 100K iterations; Stage 3 always uses rolled-out history (maximum rollouts \( N=4 \)) for 100K iterations
- Purpose: to address long-term instability and text controllability degradation caused by distribution shift in history frames during autoregressive generation

### Spatial Control
- Motion control is formulated as \( M^* = \arg\min_M F(\Pi(M), g) + cons(M) \), where \( \Pi(\cdot) \) is a task-dependent projection
- **Latent Noise Optimization**: Uses a deterministic DDIM sampler, treating DART sampling as a deterministic mapping from \( Z_T \) to motion, and directly optimizes the latent noise via the Adam optimizer (learning rate 0.05, 100-300 steps, gradient normalization)
- **Reinforcement Learning Control**: Uses latent noise \( z_T \) as the action space, training an actor-critic policy with PPO (4-layer MLP, hidden dimension 512, residual connections), with input state \( [H^i, g^i, s^i, c^i] \)

## Key Innovations

1. **Latent-space autoregression + extremely few diffusion steps**: After compressing motion primitives into a 256-dimensional latent space, high-quality motion can be generated with only 10 diffusion steps (ablation shows FID degrades significantly at 2 steps, but performance is nearly identical from 5 to 100 steps). This enables real-time generation at 334 frames per second, approximately 10× faster than FlowMDM, while maintaining the best Segment FID (3.79) and Transition FID (1.86).

2. **Scheduled training addresses distribution shift in online autoregression**: This is the key to DART's stable long-sequence generation. Ablation shows that removing scheduled training (DART-schedule) degrades Segment FID from 3.79 to 8.08 and R-prec from 0.62 to 0.39—demonstrating that standard training alone cannot handle the distribution difference between rolled-out history at test time and real history at training time.

3. **Unified framework covering generation-optimization-decision tasks**: The same latent motion primitive space and denoiser achieve motion interpolation (Goal error 0.54cm vs. DNO 5.52cm) and human-scene interaction via latent noise optimization, and text-conditioned goal reaching via RL (100% success rate vs. GAMMA 95%). Previously, these tasks required completely different specialized models; DART proves that a single foundation model can serve three types of downstream tasks simultaneously.

## Experiments and Results

### Text-Conditioned Temporal Motion Composition (BABEL validation set)
| Metric | TEACH | DoubleTake | T2M-GPT* | FlowMDM | Ours |
|---|---|---|---|---|---|
| Segment FID ↓ | 17.58 | 7.92 | 7.71 | 5.81 | **3.79** |
| Transition FID ↓ | 3.89 | 3.56 | 2.53 | 2.39 | **1.86** |
| PJ ↓ | 1.39 | 0.32 | 1.44 | **0.04** | 0.06 |
| AUJ ↓ | 5.86 | 1.23 | 4.10 | **0.11** | 0.21 |
| Speed (frames/s) ↑ | 3880 | 85 | 885 | 31 | **334** |
| Latency (s) ↓ | 0.05 | 59.11 | 0.23 | 161.29 | **0.02** |

DART achieves the best Segment/Transition FID and the second-best jitter metrics (slightly behind FlowMDM, but FlowMDM is an offline method). R-prec (0.62) and MM-Dist (5.27) are slightly behind FlowMDM (0.67/5.08), which the authors attribute to the natural transition latency of online generation. In user studies, DART outperforms all baselines in both realism and semantic alignment with 53-67% of votes (calculated from table values 10.02→3.36).

### Latent Noise Optimization Control (HML3D dataset, compared with DNO/OmniControl)
| Metric | OmniControl | DNO | DART Opt. |
|---|---|---|---|
| History error (cm) ↓ | 17.22 | 2.15 | **0.00** |
| Goal error (cm) ↓ | 5.88 | 5.52 | **0.54** |
| Skate (cm/s) ↓ | 5.48 | 5.12 | **3.97** |
| Jerk ↓ | 1.26 | 0.72 | **0.71** |

DART leads significantly in history preservation and endpoint error, and also outperforms baselines in skating and jitter.

### Reinforcement Learning Control (text-conditioned goal reaching)
| Metric | GAMMA walk | Ours 'walk' | Ours 'run' | Ours 'hop' |
|---|---|---|---|---|
| Time (s) ↓ | 31.44 | 13.82 | **12.16** | 13.89 |
| Success rate ↑ | 0.95 | **1.0** | **1.0** | **1.0** |
| Skate (cm/s) ↓ | 5.14 | 5.07 | **4.70** | 41.18 |
| Floor dist (cm) ↓ | 5.55 | 1.87 | 2.02 | **1.43** |

DART reaches the goal more than 2× faster than GAMMA, with a 100% success rate (calculated from table values 17.58→0.06), and significantly lower foot-floor distance (higher skating in the hop task is inherent to the task).

### Key Ablation Findings
- Removing the VAE (DART-VAE): Transition FID degrades from 1.86 to 1.79 (PJ increases from 0.06 to 0.20), with significantly increased jitter
- Removing scheduled training (DART-schedule): Segment FID degrades to 8.08, R-prec drops to 0.39
- Diffusion steps: 10 steps perform nearly identically to 100 steps (Segment FID 3.79 vs 4.16), with significant degradation at 2 steps (4.44)

## Boundaries and Limitations

The paper explicitly acknowledges limitations including: the latent noise optimization method has high computational cost (approximately 74 seconds for a 60-frame target sequence with 100 optimization steps), making it unsuitable for real-time control scenarios; and DDIM step skipping is not used because it introduces artifacts in generated motion. A more critical implicit limitation is that DART relies on datasets with frame-level aligned text annotations (such as BABEL); when trained on coarse-grained sentence-level labels from HML3D, text-motion alignment degrades significantly—motion switches between described actions in random order because each short motion primitive essentially matches only part of the sequence semantics. Additionally, the HumanAct12 subset and left-right mirrored sequences in HML3D provide only joint positions rather than SMPL parameters and are therefore excluded, limiting data coverage. The paper does not address generalization to unseen action categories, multi-person interactions, or complex scenes (non-flat terrain).

## Engineering Insights

When reproducing DART, the highest priority is verifying the **implementation details of scheduled training**—the three-stage training (100K+100K+100K iterations) and maximum rollout count \( N=4 \) are central to ensuring long-sequence stability; skipping or simplifying this component will directly more than double the Segment FID. Second is the VAE's KL weight: the paper's ablation shows that with \( w_{KL}=1 \), reconstruction error degrades from 0.08 to 0.44, failing to accurately reconstruct complex actions like cartwheels; a value on the order of \( 10^{-6} \) is essential. The auxiliary loss weights \( w_{aux}=100 \) (VAE) and 10000 (denoiser) are critical for suppressing jitter; removing them increases jerk from 2.45 to 3.67.

The most common engineering pitfall is **normalization of the over-parameterized motion representation**: all frames must be processed in a local coordinate system centered at the pelvis of the primitive's first frame, and the inverse transformation must be stored and applied after generation to recover global motion; otherwise, cumulative drift will occur in long sequences. Text encoding must use CLIP rather than other text encoders, and the 0.1 masking probability during training directly affects the effectiveness of classifier-free guidance. For downstream teams, if real-time spatial control is needed, latent noise optimization (74 seconds/segment) is unsuitable for online scenarios and the RL policy (240 frames/s) should be used instead; if coarse-grained text labels need to be handled, the semantic alignment of DART must first be validated against requirements, otherwise extensions such as hierarchical latent spaces should be introduced.

## 개요

DartControl은 통합 확산 자기회귀 운동 원시 모델(DART)을 제안하며, 단일 RTX 4090에서 초당 300프레임 이상의 속도로 실시간 텍스트 기반 장기 시퀀스 모션 생성을 가능하게 하고, 잠재 노이즈 최적화와 강화 학습을 통한 정밀한 공간 제어를 지원합니다. 이 방법은 중첩된 운동 원시를 표현으로 사용하고, 잠재 공간에서 단 10단계의 확산 노이즈 제거를 수행하며, 스케줄링 훈련을 통해 온라인 자기회귀 안정성을 보장합니다. 텍스트 조건 시간 조합, 모션 보간 및 인간-장면 상호작용 작업에서 기존 베이스라인보다 우수합니다.

## 무엇을 바꾸는가

기존 텍스트 조건 모션 생성 방법(T2M-GPT, MDM 등)은 본질적으로 "단일 문장-단일 구간"의 오프라인 생성기로, 여러 동작 설명 세그먼트로 구성된 긴 시퀀스를 처리할 수 없습니다. FlowMDM은 시간 조합을 지원하지만 전체 동작 타임라인(oracle 정보)을 사전에 알아야 하며, 생성 속도는 31프레임/초에 불과하고 지연 시간이 161초에 달해 대화형 애플리케이션에는 전혀 사용할 수 없습니다. DART가 진정으로 바꾸는 것은 텍스트 기반 모션 생성을 "오프라인 배치 처리"에서 "실시간 온라인 스트리밍" 패러다임으로 전환한 것입니다. 더 이상 전체 타임라인을 사전에 계획할 필요 없이, 온라인으로 도착하는 텍스트 프롬프트 시퀀스에 따라 세그먼트별로 자기회귀 생성하며, 첫 프레임 지연 시간은 단 0.02초입니다.

더 중요한 것은, DART가 "텍스트 의미 제어"와 "공간 제약 제어"를 동일한 잠재 운동 원시 공간에 통합한다는 점입니다. 잠재 노이즈 최적화(DDIM 결정적 매핑 + 경사 하강법)를 통해 모션 보간 및 인간-장면 상호작용을 구현하고, 강화 학습(잠재 노이즈를 행동 공간으로 사용)을 통해 텍스트 조건 목표 도달을 구현합니다. 이전에는 이러한 작업이 각각 DNO, OmniControl, GAMMA 등의 전용 방법으로 처리되었으며, 서로 분리되어 있고 텍스트 조건 지원이 부족했습니다. DART는 하나의 기반 모델이 생성, 최적화, 의사 결정의 세 가지 작업을 동시에 서비스할 수 있음을 증명하며, 이는 아키텍처 수준의 패러다임 전환입니다.

## 방법 분석

### 운동 원시 표현
- 장기 모션은 중첩된 원시 시퀀스로 모델링됩니다: 각 원시 \( P^i = [H^i, X^i] \), 여기서 \( H \) 프레임 히스토리(이전 원시와 중첩) 및 \( F \) 프레임 미래; 실험에서 \( H=2, F=8 \)
- 각 프레임은 과매개변수화된 SMPL-X 표현을 사용하며, 차원 \( D=276 \), 루트 변위 \( t \), 루트 방향 \( R \), 로컬 관절 회전 \( \theta \), 관절 위치 \( J \), 그리고 위치 및 회전의 시간 차분 특징(\( dt, dR, dJ \))을 포함합니다
- 모든 프레임은 로컬 좌표계에서 정규화되며(원시 첫 프레임 골반 중심), 생성 후 역변환을 통해 전역 모션을 복원합니다

### 잠재 운동 원시 공간(VAE)
- Transformer 기반 VAE(인코더/디코더 각 7층, 은닉 차원 256, 4 어텐션 헤드, 드롭아웃 0.1)가 운동 원시를 256차원 잠재 공간으로 압축합니다
- 인코더 입력은 히스토리 \( H \), 미래 \( X \) 및 학습 가능한 분포 토큰 \( T_\mu, T_\sigma \)이며, 재매개변수화 샘플링 \( z \); 디코더는 \( z \)와 \( H \)를 조건으로 \( \hat{X} \)를 예측합니다
- 손실: \( L_{VAE} = L_{rec} + 10^{-6} L_{KL} + 100 L_{aux} \), 여기서 \( L_{rec} \)는 평활 L1 재구성 손실, \( L_{aux} \)는 시간 차분 특징을 정규화합니다

### 잠재 확산 노이즈 제거기
- Transformer 기반 노이즈 제거기 \( G(z_t, t, H, c) \)는 깨끗한 잠재 변수 \( \hat{z}_0 \)를 예측하며, 입력은 노이즈 잠재 \( z_t \), 확산 단계 \( t \)(소형 MLP 임베딩), 히스토리 \( H \), CLIP 텍스트 임베딩 \( c \)입니다
- DDPM을 사용하며, 훈련 및 추론 모두 단 10개의 확산 단계, 코사인 노이즈 스케줄; 훈련 시 텍스트는 0.1 확률로 무작위 마스킹되어 분류기 없는 안내를 지원합니다(추론 시 \( w=5 \))
- 손실: \( L_{denoiser} = L_{simple} + L_{rec} + 10000 L_{aux} \)

### 스케줄링 훈련(핵심 설계)
- 3단계 훈련으로 테스트 시 분포를 점진적으로 도입: 1단계 100K 반복은 실제 히스토리만 사용; 2단계 100K 반복은 선형 확률(0→1)로 실제와 롤링 히스토리를 혼합; 3단계 100K 반복은 항상 롤링 히스토리 사용(최대 롤링 수 \( N=4 \))
- 목적: 자기회귀 생성에서 히스토리 프레임 분포 이동으로 인한 장기 불안정성과 텍스트 제어 가능성 저하를 해결

### 공간 제어
- 모션 제어는 \( M^* = \arg\min_M F(\Pi(M), g) + cons(M) \)로 공식화되며, 여기서 \( \Pi(\cdot) \)는 작업 관련 투영입니다
- **잠재 노이즈 최적화**: 결정적 DDIM 샘플러를 사용하여 DART 샘플링을 \( Z_T \)에서 모션으로의 결정적 매핑으로 간주하고, Adam 옵티마이저(학습률 0.05, 100-300단계, 그래디언트 정규화)로 잠재 노이즈를 직접 최적화합니다
- **강화 학습 제어**: 잠재 노이즈 \( z_T \)를 행동 공간으로 사용하고, PPO 알고리즘으로 actor-critic 정책(4층 MLP, 은닉 차원 512, 잔차 연결)을 훈련하며, 입력 상태는 \( [H^i, g^i, s^i, c^i] \)입니다

## 핵심 혁신

1. **잠재 공간 자기회귀 + 극소량 확산 단계**: 운동 원시를 256차원 잠재 공간으로 압축한 후, 단 10개의 확산 단계로 고품질 모션을 생성할 수 있습니다(절제 실험에서 2단계 시 FID가 크게 악화되지만, 5-100단계 성능은 거의 동일). 이로 인해 334프레임/초의 실시간 생성이 가능해졌으며, FlowMDM보다 약 10배 빠르면서도 최적의 Segment FID(3.79)와 Transition FID(1.86)를 유지합니다.

2. **스케줄링 훈련으로 온라인 자기회귀의 분포 이동 해결**: 이것이 DART가 긴 시퀀스를 안정적으로 생성할 수 있는 핵심입니다. 절제 실험에서 스케줄링 훈련을 제거하면(DART-schedule) Segment FID가 3.79에서 8.08로 악화되고, R-prec가 0.62에서 0.39로 하락합니다. 이는 표준 훈련만으로는 테스트 시 롤링 히스토리와 훈련 시 실제 히스토리의 분포 차이를 처리할 수 없음을 증명합니다.

3. **생성-최적화-의사 결정 세 가지 작업을 포괄하는 통합 프레임워크**: 동일한 잠재 운동 원시 공간과 노이즈 제거기를 통해 잠재 노이즈 최적화로 모션 보간(Goal error 0.54cm vs. DNO 5.52cm) 및 인간-장면 상호작용을 구현하고, RL로 텍스트 조건 목표 도달(성공률 100% vs. GAMMA 95%)을 구현합니다. 이전에는 이러한 작업에 완전히 다른 전용 모델이 필요했지만, DART는 하나의 기반 모델이 세 가지 하위 작업을 동시에 서비스할 수 있음을 증명합니다.

## 실험 및 결과

### 텍스트 조건 시간 모션 조합(BABEL 검증 세트)
| 지표 | TEACH | DoubleTake | T2M-GPT* | FlowMDM | Ours |
|---|---|---|---|---|---|
| Segment FID ↓ | 17.58 | 7.92 | 7.71 | 5.81 | **3.79** |
| Transition FID ↓ | 3.89 | 3.56 | 2.53 | 2.39 | **1.86** |
| PJ ↓ | 1.39 | 0.32 | 1.44 | **0.04** | 0.06 |
| AUJ ↓ | 5.86 | 1.23 | 4.10 | **0.11** | 0.21 |
| Speed (프레임/초) ↑ | 3880 | 85 | 885 | 31 | **334** |
| Latency (초) ↓ | 0.05 | 59.11 | 0.23 | 161.29 | **0.02** |

DART는 Segment/Transition FID에서 모두 최적이며, 지터 지표는 두 번째로 좋습니다(FlowMDM보다 약간 떨어지지만, FlowMDM은 오프라인 방법입니다). R-prec(0.62) 및 MM-Dist(5.27)는 FlowMDM(0.67/5.08)보다 약간 낮으며, 저자는 온라인 생성의 자연스러운 전환 지연 때문이라고 설명합니다. 사용자 연구에서 DART는 사실감과 의미 정렬 모두에서 53-67%(표 내 값 10.02→3.36으로 계산)의 투표율로 모든 베이스라인을 능가합니다.

### 잠재 노이즈 최적화 제어(HML3D 데이터셋, DNO/OmniControl 대비)
| 지표 | OmniControl | DNO | DART Opt. |
|---|---|---|---|
| History error (cm) ↓ | 17.22 | 2.15 | **0.00** |
| Goal error (cm) ↓ | 5.88 | 5.52 | **0.54** |
| Skate (cm/s) ↓ | 5.48 | 5.12 | **3.97** |
| Jerk ↓ | 1.26 | 0.72 | **0.71** |

DART는 히스토리 유지 및 종점 오차에서 크게 앞서며, 슬라이딩과 지터에서도 베이스라인보다 우수합니다.

### 강화 학습 제어(텍스트 조건 목표 도달)
| 지표 | GAMMA walk | Ours 'walk' | Ours 'run' | Ours 'hop' |
|---|---|---|---|---|
| Time (s) ↓ | 31.44 | 13.82 | **12.16** | 13.89 |
| Success rate ↑ | 0.95 | **1.0** | **1.0** | **1.0** |
| Skate (cm/s) ↓ | 5.14 | 5.07 | **4.70** | 41.18 |
| Floor dist (cm) ↓ | 5.55 | 1.87 | 2.02 | **1.43** |

DART는 도달 시간에서 GAMMA보다 2배 이상 빠르며, 성공률 100%(표 내 값 17.58→0.06으로 계산)를 달성하고, 발-지면 거리가 현저히 낮습니다(점프 작업의 슬라이딩 증가는 작업 고유 특성).

### 절제 실험 핵심 결론
- VAE 제거(DART-VAE): Transition FID가 1.86에서 1.79로 악화(PJ가 0.06에서 0.20으로 상승), 지터가 크게 증가
- 스케줄링 훈련 제거(DART-schedule): Segment FID가 8.08로 악화, R-prec가 0.39로 하락
- 확산 단계 수: 10단계와 100단계 성능이 거의 동일(Segment FID 3.79 vs 4.16), 2단계에서는 크게 악화(4.44)

## 경계 및 한계

논문이 명시적으로 인정한 한계는 다음과 같습니다: 잠재 노이즈 최적화 방법은 계산 비용이 높아(60프레임 목표 시퀀스 + 100 최적화 단계 약 74초 소요) 실시간 제어 시나리오에 적합하지 않습니다. DDIM 점프 단계를 사용하지 않는 이유는 생성 모션 아티팩트를 유발하기 때문입니다. 더 중요한 암묵적 한계는 DART가 프레임 수준 정렬 텍스트 주석이 있는 데이터셋(예: BABEL)에 의존한다는 점입니다. HML3D의 거친 문장 수준 레이블에서 훈련할 때 텍스트-모션 정렬이 크게 저하됩니다. 각 짧은 운동 원시가 본질적으로 시퀀스 의미의 일부만 일치하기 때문에 모션이 설명된 동작 사이에서 무작위 순서로 전환됩니다. 또한 HML3D의 HumanAct12 하위 집합과 좌우 미러 시퀀스는 SMPL 매개변수 대신 관절 위치만 제공하므로 제외되어 데이터 커버리지가 제한됩니다. 논문은 보지 못한 동작 클래스, 다중 인물 상호작용 또는 복잡한 장면(비평면 지형)에 대한 일반화 능력을 언급하지 않습니다.

## 엔지니어링 시사점

DART를 재현할 때 가장 우선적으로 확인해야 할 것은 **스케줄링 훈련의 구현 세부 사항**입니다. 3단계 훈련(100K+100K+100K 반복)과 최대 롤링 수 \( N=4 \)는 긴 시퀀스 안정성의 핵심이며, 이 단계를 건너뛰거나 단순화하면 Segment FID가 두 배 이상 증가합니다. 다음으로 VAE의 KL 가중치입니다. 논문 절제 실험에 따르면 \( w_{KL}=1 \)일 때 재구성 오차가 0.08에서 0.44로 악화되어 측면 공중제비와 같은 복잡한 동작을 정확히 재구성할 수 없으므로 \( 10^{-6} \) 수준을 사용해야 합니다. 보조 손실 가중치 \( w_{aux}=100 \)(VAE) 및 10000(노이즈 제거기)은 지터 억제에 중요하며, 제거하면 jerk가 2.45에서 3.67로 상승합니다.

엔지니어링에서 가장 쉽게 함정에 빠지는 부분은 **과매개변수화된 모션 표현의 정규화**입니다. 모든 프레임은 원시 첫 프레임 골반을 중심으로 한 로컬 좌표계에서 처리해야 하며, 생성 후 역변환을 저장하고 적용하여 전역 모션을 복원해야 합니다. 그렇지 않으면 긴 시퀀스에서 누적 드리프트가 발생합니다. 텍스트 인코딩은 반드시 CLIP을 사용해야 하며 다른 텍스트 인코더는 안 되고, 훈련 시 0.1의 마스킹 확률은 분류기 없는 안내 효과에 직접 영향을 미칩니다. 하류 팀의 경우 실시간 공간 제어가 필요하면 잠재 노이즈 최적화(74초/세그먼트)는 온라인 시나리오에 적합하지 않으므로 RL 정책(240프레임/초)을 사용해야 합니다. 거친 텍스트 레이블을 처리해야 한다면 DART의 의미 정렬이 요구 사항을 충족하는지 먼저 검증해야 하며, 그렇지 않으면 계층적 잠재 공간과 같은 확장을 도입해야 합니다.
