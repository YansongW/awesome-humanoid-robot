---
$id: ent_paper_enpire_agentic_robot_policy_self_improve_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'ENPIRE: Agentic Robot Policy Self-Improvement in the Real World'
  zh: 'ENPIRE: Agentic Robot Policy Self-Improvement in the Real World'
  ko: 'ENPIRE: Agentic Robot Policy Self-Improvement in the Real World'
summary:
  en: 'Achieving dexterous robotic manipulation in the real world heavily relies on human supervision and algorithm engineering,
    which becomes a central bottleneck in the pursuit of general physical intelligence. Institutions per source list: NVIDIA
    GEAR Lab、CMU LeCAR Lab、UC Berkeley（项目页致谢与公开报道）.'
  zh: ENPIRE 是一个面向真实世界机器人策略自主改进的框架，由研究团队提出，核心贡献在于构建了环境重置、策略执行、结果验证与迭代优化的闭环物理反馈循环。该框架使编码智能体能够自主训练灵巧操作策略，在插针盒整理、扎带紧固和工具使用等任务上达到
    99% 的成功率。
  ko: 'Achieving dexterous robotic manipulation in the real world heavily relies on human supervision and algorithm engineering,
    which becomes a central bottleneck in the pursuit of general physical intelligence. Institutions per source list: NVIDIA
    GEAR Lab、CMU LeCAR Lab、UC Berkeley（项目页致谢与公开报道）.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- enpire
- agentic
- robot
- policy
- self
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 360 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2606.19980 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2606.19980v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.19980 ENPIRE: Agentic Robot Policy Self-Improvement in the Real World'
  url: https://arxiv.org/abs/2606.19980
  accessed_at: '2026-07-31'
  date: '2026-06-18'
- id: src_002
  type: website
  title: Project page
  url: https://research.nvidia.com/labs/gear/enpire/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

ENPIRE 是 NVIDIA 团队提出的现实世界机器人策略自主改进框架，通过编码智能体（coding agent）在真实物理机器人上闭环执行"重置-评估-改进"循环，实现无需人类干预的策略自提升。核心贡献在于构建了可复用的环境接口（EN）、策略改进（PI）、Rollout（R）与进化（E）四模块 harness，并在 8 台双臂机器人上验证了多智能体异步协作的扩展性，将 Push-T 与 Pin insertion 任务的收敛时间分别缩短至约 2 小时与 40 分钟。

## 它改变了什么

它真正改变的是机器人学习研究中"人类照看"这一隐性成本的结构性地位。以往无论 BC 还是 RL 方法，数据收集、奖励设计、重置策略与算法调参都依赖人类在环，这构成了灵巧操作规模化的硬瓶颈。ENPIRE 将这一生命周期整体委托给编码智能体，使"研究吞吐量"不再受限于人类劳动力，而是受限于机器人硬件与 token 预算——这是从"人机协作"到"机器自主研究"的范式跃迁。

更重要的是，它把"环境接口"从固定 API 提升为可被智能体自主构建和优化的对象。传统仿真环境天然提供 reset 与 reward，而现实世界缺乏这种抽象；ENPIRE 通过程序化工具调用合成二元奖励与自动重置，首次让编码智能体在真实物理系统上获得闭环假设检验能力。这改变了"现实世界 RL 难以自动化"的既有认知，但也暴露出 token 成本随车队规模超线性增长的工程代价——自主性并非免费午餐。

## 方法拆解

### 两阶段流程
- **第一阶段（EN）**：编码智能体从人类反馈中构建自主环境接口，离线优化程序化工具调用，建立安全边界、自动重置与验证程序。完成后作为不可变 Gym API 在后续阶段复用，一次性设置成本。
- **第二阶段（PIRE）**：完全自主的自动研究流程，智能体基于现实世界反馈改进策略，由环境自动验证信号引导，无人类干预。

### 核心模块
- **环境模块（EN）**：自动重置与验证。硬安全约束限制机器人配置空间与运动行为，违反即触发任务失败与自动重置。自动验证通过合成二元奖励函数区分任务结果，仅需几分钟成功/失败演示，优化预测准确度与处理延迟（Zip-tie 奖励推理延迟优化至 150ms 以下）。
- **策略改进模块（PI）**：启动策略改进，支持 BC、迭代 BC 与在线 rollout 数据聚合、RL 与 BC 正则化项。
- **Rollout 模块（R）**：单/多并行物理机器人评估策略，策略 30Hz 推理，低层关节控制器 100Hz 通过 CAN 总线。
- **进化模块（E）**：编码智能体分析日志、查阅文献、改进训练基础设施与算法代码，通过 Git 异步协作，可 cherry-pick、复制或合并同伴成功配方。

### 多智能体扩展
- 部署 N 个智能体在 N 个物理机器人上异步测试 N 个假设，从相同基线策略代码库分支，通过 Git 自主协作。
- 两个利用率指标：**MRU**（平均机器人利用率，研究墙钟时间内机器人主动执行实验的时间比例）与 **MTU**（平均 Token 利用率，车队平均 token 消耗量）。
- 成功度量：固定重试次数（8 次）下一次 rollout 中完成任务的机会，同时捕捉精度与上下文恢复能力。

### 关键设计决策
- **不可变环境 API**：确保第二阶段完全自主，避免智能体修改验证逻辑引入偏差。
- **硬安全约束**：限制配置空间到安全操作极限，防止硬件损坏，同时作为情节终止/截断来源。
- **Git 协作**：去中心化知识传播，站点间共享代码、配置、工具与结果，智能体可选择性合并分支。

## 关键创新

1. **现实世界闭环自主研究抽象**：首次将"重置-执行-验证-改进"的完整研究循环封装为可复用的 harness，使编码智能体在真实物理系统上获得闭环假设检验能力。此前编码智能体仅在数字环境（如 GitHub、仿真）中成功，缺乏现实世界接口；ENPIRE 通过程序化工具调用合成奖励与重置，填补了这一空白。

2. **多智能体异步协作的机队架构**：通过 Git 实现去中心化知识共享，智能体可自主合并或 cherry-pick 同伴的成功训练配方。这解决了"在机器人集群上扩展自动研究吞吐量时如何选择与验证假设"的开放问题，使 8 个智能体在 Push-T 上达到 1.0 归一化分数的时间从约 5 小时缩短至约 2 小时。

3. **自动验证信号驱动的策略改进**：编码智能体被要求合成二元奖励函数，仅需几分钟演示即可实现高精度验证（Pin insertion 策略收敛到 100% 速度快于前沿人类参与方法）。这消除了奖励工程这一传统瓶颈，使策略改进完全由环境自动验证信号引导。

## 实验与结果

### 关键结果汇总

| 任务 | 设置 | 关键数字 | 含义 |
|------|------|----------|------|
| Push-T（模拟） | Claude Code/Codex vs Kimi Code | 前两者约 2 小时达 95% 成功率，Kimi 花费两倍时间 | 智能体框架差异显著影响收敛速度 |
| Push-T（现实） | 3 个智能体 | 2 个失败 | 现实世界环境接口构建难度高 |
| Pin insertion | ENPIRE vs 文献 48 | 收敛到 100% 速度更快 | 自主研究可超越人类参与方法 |
| Pin insertion 扩展 | 1→8 智能体 | 接近完美成功率时间从 >1.5h 降至约 40min | 多智能体扩展显著加速 |
| Push-T 扩展 | 1→8 智能体 | 1.0 归一化分数时间从约 5h 降至约 2h | 扩展收益明显 |
| RoboCasa365 | ENPIRE vs GR00T/CaP-X | ENPIRE 优于两者 | 模块化工具使用优于端到端 VLA |
| Zip-tie 奖励延迟 | 优化后 | <150ms | 满足实时性要求 |
| Pin insertion 想法树 | BC 正则化（I37） | +10.8 pp | 算法改进贡献最大 |
| Pin insertion 想法树 | 批量大小调优（I66） | +0.9 pp | 超参数调优贡献有限 |
| Pin insertion 想法树 | 控制器补偿（I76） | +1.3 pp | 低层控制补偿有效 |

### 资源利用
- 从 1 到 8 个智能体扩展时，GPU 利用率和 token 消耗上升，但每机器人利用率（MRU）下降。
- MTU 在最多 4 个智能体时接近线性投影，但在 8 个智能体时急剧上升，token 成本超线性增长。

## 边界与局限

- **资源利用不足**：编码智能体在读取日志、编写代码、调试或等待语言模型骨干时未充分利用机器人资源；随着车队规模扩展，MRU 下降而 GPU 活跃利用率上升。智能体团队花费更多时间总结同伴分支，更少时间实际操作机器人。
- **Token 成本超线性增长**：总 token 预算增长远快于墙钟时间的相应减少，增加车队规模以 token 效率换取更快的策略改进。
- **感知瓶颈**：SAM3 对小型或模糊物体可能返回错误掩码或无可用掩码；分辨率提升（256×256 到 480×640）改善检测但未解决所有失败。
- **迁移机制限制**：迁移实验仅通过书面摘要传递知识，原始轨迹、隐藏日志和检查点被移除，可能限制迁移效果。
- **未做之事**：未提及在更多任务上的验证、长期稳定性评估、不同机器人平台的泛化、安全性的形式化保证、与其他人类参与方法的系统成本对比。具体训练配置（学习率、批次大小、优化器）、训练时长、数据量（episode 数）均未披露。

## 工程启示

- **先核对环境接口的可靠性**：ENPIRE 的成功高度依赖感知 API（SAM3）与程序化工具调用的稳定性。复现时优先验证 SAM3 在目标物体上的检测精度（原始提示 10/20，智能体优化后 17/20），并预留分辨率调整与提示重写的优化空间。
- **注意 token 成本的非线性**：从 4 到 8 个智能体扩展时，token 消耗急剧上升，而墙钟时间收益递减。若 token 预算有限，建议先评估 4 智能体配置的性价比，再决定是否扩展。
- **MRU 下降是必然趋势**：多智能体协作时，智能体花费更多时间在 Git 协作与日志分析上，而非实际操作机器人。若目标是最大化机器人利用率，需设计更高效的协作协议（如减少分支总结频率）。
- **奖励函数合成是最大杠杆**：Pin insertion 中 BC 正则化贡献 +10.8 pp，远超批量大小调优（+0.9 pp）与控制器补偿（+1.3 pp）。复现时应优先让智能体探索算法级改进，而非超参数微调。
- **迁移实验需谨慎**：仅通过书面摘要传递知识可能限制迁移效果，原始轨迹与检查点的移除会削弱跨任务初始化收益。若需迁移，建议保留部分原始数据。

## 参考
- https://arxiv.org/abs/2606.19980
- https://research.nvidia.com/labs/gear/enpire/
- https://github.com/ImChong/Robotics_Notebooks

## Overview

ENPIRE is a framework proposed by the NVIDIA team for autonomous improvement of real-world robot policies. It employs a coding agent to execute a closed-loop "reset-evaluate-improve" cycle on physical robots without human intervention. The core contribution is a reusable harness comprising four modules—Environment (EN), Policy Improvement (PI), Rollout (R), and Evolution (E)—validated on eight dual-arm robots for scalable multi-agent asynchronous collaboration. It reduces convergence times for Push-T and Pin insertion tasks to approximately 2 hours and 40 minutes, respectively.

## What It Changes

What it fundamentally changes is the structural role of the hidden cost of "human supervision" in robot learning research. Previously, both BC and RL methods relied on humans in the loop for data collection, reward design, reset strategies, and algorithm tuning, creating a hard bottleneck for scaling dexterous manipulation. ENPIRE delegates this entire lifecycle to a coding agent, making "research throughput" limited not by human labor but by robot hardware and token budgets—a paradigm shift from "human-robot collaboration" to "autonomous machine research."

More importantly, it elevates the "environment interface" from a fixed API to an object that can be autonomously constructed and optimized by the agent. Traditional simulation environments naturally provide reset and reward, but the real world lacks such abstractions. ENPIRE synthesizes binary rewards and automatic resets through programmatic tool calls, granting coding agents closed-loop hypothesis-testing capabilities on real physical systems for the first time. This challenges the existing notion that "real-world RL is difficult to automate," but also exposes the engineering cost of token consumption growing superlinearly with fleet size—autonomy is not a free lunch.

## Method Breakdown

### Two-Phase Workflow
- **Phase 1 (EN)**: The coding agent builds an autonomous environment interface from human feedback, optimizes programmatic tool calls offline, and establishes safety boundaries, automatic resets, and verification procedures. Once complete, it serves as an immutable Gym API for subsequent phases—a one-time setup cost.
- **Phase 2 (PIRE)**: A fully autonomous research workflow where the agent improves policies based on real-world feedback, guided by automatically verified signals, with no human intervention.

### Core Modules
- **Environment Module (EN)**: Automatic reset and verification. Hard safety constraints limit the robot's configuration space and motion behavior; violations trigger task failure and automatic reset. Automatic verification distinguishes task outcomes via synthesized binary reward functions, requiring only minutes of success/failure demonstrations, optimizing prediction accuracy and processing latency (Zip-tie reward inference latency optimized to below 150ms).
- **Policy Improvement Module (PI)**: Initiates policy improvement, supporting BC, iterative BC with online rollout data aggregation, and RL with BC regularization terms.
- **Rollout Module (R)**: Evaluates policies on single or multiple parallel physical robots, with policy inference at 30Hz and low-level joint controllers at 100Hz over CAN bus.
- **Evolution Module (E)**: The coding agent analyzes logs, reviews literature, and improves training infrastructure and algorithm code, collaborating asynchronously via Git, with the ability to cherry-pick, copy, or merge successful recipes from peers.

### Multi-Agent Scaling
- Deploys N agents on N physical robots to asynchronously test N hypotheses, branching from the same baseline policy codebase and collaborating autonomously via Git.
- Two utilization metrics: **MRU** (Mean Robot Utilization, the fraction of wall-clock research time during which robots actively execute experiments) and **MTU** (Mean Token Utilization, the average token consumption across the fleet).
- Success metric: the chance of completing the task in one rollout within a fixed number of retries (8), capturing both precision and context recovery capability.

### Key Design Decisions
- **Immutable Environment API**: Ensures full autonomy in Phase 2, preventing agents from modifying verification logic and introducing bias.
- **Hard Safety Constraints**: Restrict the configuration space to safe operational limits, preventing hardware damage while serving as a source of episode termination/truncation.
- **Git Collaboration**: Decentralized knowledge propagation, sharing code, configurations, tools, and results across sites, with agents selectively merging branches.

## Key Innovations

1. **Closed-Loop Autonomous Research Abstraction for the Real World**: For the first time, the complete research loop of "reset-execute-verify-improve" is encapsulated as a reusable harness, granting coding agents closed-loop hypothesis-testing capabilities on real physical systems. Previously, coding agents succeeded only in digital environments (e.g., GitHub, simulation) and lacked real-world interfaces; ENPIRE fills this gap by synthesizing rewards and resets through programmatic tool calls.

2. **Fleet Architecture for Multi-Agent Asynchronous Collaboration**: Decentralized knowledge sharing via Git, allowing agents to autonomously merge or cherry-pick successful training recipes from peers. This addresses the open problem of "how to select and validate hypotheses when scaling automated research throughput across robot clusters," reducing the time for 8 agents to reach a normalized score of 1.0 on Push-T from approximately 5 hours to about 2 hours.

3. **Policy Improvement Driven by Automatically Verified Signals**: The coding agent is tasked with synthesizing binary reward functions, achieving high-precision verification with only minutes of demonstrations (Pin insertion policy converges to 100% faster than state-of-the-art human-in-the-loop methods). This eliminates the traditional bottleneck of reward engineering, making policy improvement entirely guided by automatically verified environmental signals.

## Experiments and Results

### Key Results Summary

| Task | Setup | Key Numbers | Implication |
|------|------|----------|------|
| Push-T (Simulation) | Claude Code/Codex vs Kimi Code | The former two reach 95% success in ~2 hours; Kimi takes twice as long | Agent framework differences significantly affect convergence speed |
| Push-T (Real World) | 3 agents | 2 failures | Real-world environment interface construction is challenging |
| Pin insertion | ENPIRE vs Literature 48 | Faster convergence to 100% | Autonomous research can surpass human-in-the-loop methods |
| Pin insertion Scaling | 1→8 agents | Time to near-perfect success drops from >1.5h to ~40min | Multi-agent scaling significantly accelerates |
| Push-T Scaling | 1→8 agents | Time to 1.0 normalized score drops from ~5h to ~2h | Clear scaling benefits |
| RoboCasa365 | ENPIRE vs GR00T/CaP-X | ENPIRE outperforms both | Modular tool use beats end-to-end VLA |
| Zip-tie Reward Latency | After optimization | <150ms | Meets real-time requirements |
| Pin insertion Idea Tree | BC regularization (I37) | +10.8 pp | Algorithmic improvements contribute most |
| Pin insertion Idea Tree | Batch size tuning (I66) | +0.9 pp | Hyperparameter tuning contributes little |
| Pin insertion Idea Tree | Controller compensation (I76) | +1.3 pp | Low-level control compensation is effective |

### Resource Utilization
- Scaling from 1 to 8 agents increases GPU utilization and token consumption, but per-robot utilization (MRU) decreases.
- MTU is close to linear projections up to 4 agents but rises sharply at 8 agents, indicating superlinear token cost growth.

## Boundaries and Limitations

- **Insufficient Resource Utilization**: Coding agents underutilize robot resources while reading logs, writing code, debugging, or waiting for the language model backbone; as fleet size scales, MRU decreases while GPU active utilization rises. Agent teams spend more time summarizing peer branches and less time actually operating robots.
- **Superlinear Token Cost Growth**: Total token budget grows much faster than the corresponding reduction in wall-clock time; increasing fleet size trades token efficiency for faster policy improvement.
- **Perception Bottlenecks**: SAM3 may return incorrect or no masks for small or blurry objects; resolution improvements (256×256 to 480×640) enhance detection but do not resolve all failures.
- **Transfer Mechanism Limitations**: Transfer experiments convey knowledge only through written summaries; raw trajectories, hidden logs, and checkpoints are removed, potentially limiting transfer effectiveness.
- **What Was Not Done**: No validation on more tasks, long-term stability evaluation, generalization across different robot platforms, formal safety guarantees, or systematic cost comparisons with other human-in-the-loop methods. Specific training configurations (learning rate, batch size, optimizer), training duration, and data volume (number of episodes) are not disclosed.

## Engineering Insights

- **Verify Environment Interface Reliability First**: ENPIRE's success heavily depends on the stability of perception APIs (SAM3) and programmatic tool calls. When reproducing, prioritize validating SAM3's detection accuracy on target objects (raw prompts 10/20, improved to 17/20 after agent optimization), and reserve optimization space for resolution adjustments and prompt rewriting.
- **Watch for Nonlinear Token Costs**: Scaling from 4 to 8 agents causes token consumption to rise sharply while wall-clock time gains diminish. If token budgets are limited, evaluate the cost-effectiveness of a 4-agent configuration before deciding to scale further.
- **MRU Decline Is Inevitable**: In multi-agent collaboration, agents spend more time on Git collaboration and log analysis than on actual robot operation. If maximizing robot utilization is the goal, design more efficient collaboration protocols (e.g., reducing branch summary frequency).
- **Reward Function Synthesis Is the Biggest Lever**: In Pin insertion, BC regularization contributes +10.8 pp, far exceeding batch size tuning (+0.9 pp) and controller compensation (+1.3 pp). When reproducing, prioritize having agents explore algorithmic-level improvements over hyperparameter fine-tuning.
- **Transfer Experiments Require Caution**: Conveying knowledge only through written summaries may limit transfer effectiveness; removing raw trajectories and checkpoints weakens cross-task initialization benefits. If transfer is needed, consider retaining some original data.

## 개요

ENPIRE는 NVIDIA 팀이 제안한 현실 세계 로봇 정책 자율 개선 프레임워크로, 코딩 에이전트(coding agent)가 실제 물리 로봇에서 "리셋-평가-개선" 루프를 폐쇄적으로 실행하여 인간 개입 없이 정책을 스스로 향상시킵니다. 핵심 기여는 재사용 가능한 환경 인터페이스(EN), 정책 개선(PI), Rollout(R), 진화(E) 네 가지 모듈로 구성된 harness를 구축하고, 8대의 이중 암 로봇에서 다중 에이전트 비동기 협업의 확장성을 검증하여 Push-T 및 Pin insertion 작업의 수렴 시간을 각각 약 2시간과 40분으로 단축한 것입니다.

## 그것이 바꾼 것

진정으로 바꾼 것은 로봇 학습 연구에서 "인간 감독"이라는 암묵적 비용의 구조적 위상입니다. 기존의 BC 및 RL 방법 모두 데이터 수집, 보상 설계, 리셋 전략, 알고리즘 튜닝이 인간의 개입에 의존했으며, 이는 정밀 조작의 규모화에 있어서의 하드瓶颈이었습니다. ENPIRE는 이 전체 수명 주기를 코딩 에이전트에 위임하여 "연구 처리량"이 더 이상 인간 노동력에 제한되지 않고 로봇 하드웨어와 토큰 예산에 의해 제한되도록 만듭니다——이는 "인간-로봇 협업"에서 "기계 자율 연구"로의 패러다임 전환입니다.

더 중요한 것은 "환경 인터페이스"를 고정 API에서 에이전트가 자율적으로 구축하고 최적화할 수 있는 대상으로 승격시킨 점입니다. 전통적인 시뮬레이션 환경은 자연스럽게 reset과 reward를 제공하지만, 현실 세계에는 이러한 추상화가 부재합니다. ENPIRE는 프로그램적 도구 호출을 통해 이진 보상과 자동 리셋을 합성하여, 코딩 에이전트가 실제 물리 시스템에서 폐쇄 루프 가설 검증 능력을 처음으로 획득하게 했습니다. 이는 "현실 세계 RL 자동화가 어렵다"는 기존 인식을 바꾸었지만, 차량 규모가 커질수록 토큰 비용이 초선형적으로 증가하는 엔지니어링 비용도 드러냈습니다——자율성은 공짜 점심이 아닙니다.

## 방법 분해

### 2단계 프로세스
- **1단계 (EN)**: 코딩 에이전트가 인간 피드백으로부터 자율 환경 인터페이스를 구축하고, 프로그램적 도구 호출을 오프라인으로 최적화하여 안전 경계, 자동 리셋, 검증 절차를 수립합니다. 완료 후 불변 Gym API로 후속 단계에서 재사용되며, 일회성 설정 비용이 발생합니다.
- **2단계 (PIRE)**: 완전 자율 자동 연구 프로세스로, 에이전트가 현실 세계 피드백을 기반으로 정책을 개선하며, 환경 자동 검증 신호에 의해 유도되고 인간 개입이 없습니다.

### 핵심 모듈
- **환경 모듈 (EN)**: 자동 리셋 및 검증. 하드 안전 제약이 로봇 구성 공간과 운동 동작을 제한하며, 위반 시 작업 실패와 자동 리셋이 트리거됩니다. 자동 검증은 합성 이진 보상 함수를 통해 작업 결과를 구분하며, 몇 분의 성공/실패 데모만으로 예측 정확도와 처리 지연 시간을 최적화합니다 (Zip-tie 보상 추론 지연 시간을 150ms 이하로 최적화).
- **정책 개선 모듈 (PI)**: 정책 개선을 시작하며, BC, 반복 BC 및 온라인 rollout 데이터 집계, RL 및 BC 정규화 항을 지원합니다.
- **Rollout 모듈 (R)**: 단일/다중 병렬 물리 로봇에서 정책을 평가하며, 정책은 30Hz 추론, 저수준 관절 컨트롤러는 CAN 버스를 통해 100Hz로 동작합니다.
- **진화 모듈 (E)**: 코딩 에이전트가 로그를 분석하고, 문헌을 참고하며, 훈련 인프라와 알고리즘 코드를 개선합니다. Git을 통해 비동기 협업하며, 동료의 성공 레시피를 cherry-pick, 복사 또는 병합할 수 있습니다.

### 다중 에이전트 확장
- N개의 에이전트를 N개의 물리 로봇에 배포하여 N개의 가설을 비동기적으로 테스트하고, 동일한 기준 정책 코드베이스에서 분기하며 Git을 통해 자율 협업합니다.
- 두 가지 활용 지표: **MRU** (평균 로봇 활용률, 연구 벽시계 시간 동안 로봇이 실험을 적극적으로 실행한 시간 비율) 및 **MTU** (평균 토큰 활용률, 차량 평균 토큰 소비량).
- 성공 측정: 고정 재시도 횟수 (8회) 내에서 한 번의 rollout에서 작업을 완료할 확률로, 정밀도와 컨텍스트 복구 능력을 동시에 포착합니다.

### 핵심 설계 결정
- **불변 환경 API**: 2단계의 완전 자율성을 보장하여 에이전트가 검증 로직을 수정해 편향을 도입하는 것을 방지합니다.
- **하드 안전 제약**: 구성 공간을 안전 작동 한계로 제한하여 하드웨어 손상을 방지하고, 에피소드 종료/중단 소스로도 사용됩니다.
- **Git 협업**: 분산 지식 전파로, 사이트 간 코드, 구성, 도구, 결과를 공유하며 에이전트가 분기를 선택적으로 병합할 수 있습니다.

## 핵심 혁신

1. **현실 세계 폐쇄 루프 자율 연구 추상화**: "리셋-실행-검증-개선"의 전체 연구 루프를 재사용 가능한 harness로 처음 캡슐화하여, 코딩 에이전트가 실제 물리 시스템에서 폐쇄 루프 가설 검증 능력을 획득하게 했습니다. 이전의 코딩 에이전트는 디지털 환경 (예: GitHub, 시뮬레이션)에서만 성공했으며 현실 세계 인터페이스가 부재했습니다. ENPIRE는 프로그램적 도구 호출을 통해 보상과 리셋을 합성하여 이 공백을 메웠습니다.

2. **다중 에이전트 비동기 협업의 차량 아키텍처**: Git을 통한 분산 지식 공유로, 에이전트가 동료의 성공적인 훈련 레시피를 자율적으로 병합하거나 cherry-pick할 수 있습니다. 이는 "로봇 클러스터에서 자동 연구 처리량을 확장할 때 가설을 선택하고 검증하는 방법"이라는 개방형 문제를 해결하여, 8개의 에이전트가 Push-T에서 1.0 정규화 점수에 도달하는 시간을 약 5시간에서 약 2시간으로 단축했습니다.

3. **자동 검증 신호 기반 정책 개선**: 코딩 에이전트가 이진 보상 함수를 합성하도록 요구하며, 몇 분의 데모만으로 높은 정확도의 검증을 달성합니다 (Pin insertion 정책이 최첨단 인간 참여 방법보다 100% 수렴이 더 빠름). 이는 보상 엔지니어링이라는 전통적인 병목을 제거하여, 정책 개선이 전적으로 환경 자동 검증 신호에 의해 유도되도록 합니다.

## 실험 및 결과

### 핵심 결과 요약

| 작업 | 설정 | 핵심 수치 | 의미 |
|------|------|----------|------|
| Push-T (시뮬레이션) | Claude Code/Codex vs Kimi Code | 전자는 약 2시간에 95% 성공률, Kimi는 두 배 시간 소요 | 에이전트 프레임워크 차이가 수렴 속도에 큰 영향 |
| Push-T (현실) | 3개 에이전트 | 2개 실패 | 현실 세계 환경 인터페이스 구축 난이도 높음 |
| Pin insertion | ENPIRE vs 문헌 48 | 100% 수렴 속도 더 빠름 | 자율 연구가 인간 참여 방법을 능가 가능 |
| Pin insertion 확장 | 1→8 에이전트 | 완벽 성공률 도달 시간이 >1.5h에서 약 40분으로 단축 | 다중 에이전트 확장이 크게 가속 |
| Push-T 확장 | 1→8 에이전트 | 1.0 정규화 점수 도달 시간이 약 5h에서 약 2h로 단축 | 확장 이점 명확 |
| RoboCasa365 | ENPIRE vs GR00T/CaP-X | ENPIRE가 둘 다 능가 | 모듈식 도구 사용이 end-to-end VLA보다 우수 |
| Zip-tie 보상 지연 | 최적화 후 | <150ms | 실시간 요구 충족 |
| Pin insertion 아이디어 트리 | BC 정규화 (I37) | +10.8 pp | 알고리즘 개선 기여 최대 |
| Pin insertion 아이디어 트리 | 배치 크기 튜닝 (I66) | +0.9 pp | 하이퍼파라미터 튜닝 기여 제한적 |
| Pin insertion 아이디어 트리 | 컨트롤러 보상 (I76) | +1.3 pp | 저수준 제어 보상 효과적 |

### 자원 활용
- 1개에서 8개 에이전트로 확장 시 GPU 활용률과 토큰 소비는 증가하지만, 로봇당 활용률 (MRU)은 감소합니다.
- MTU는 최대 4개 에이전트까지 선형 투영에 가깝지만, 8개 에이전트에서 급격히 상승하여 토큰 비용이 초선형적으로 증가합니다.

## 경계 및 한계

- **자원 활용 부족**: 코딩 에이전트가 로그 읽기, 코드 작성, 디버깅 또는 언어 모델 백본 대기 중에 로봇 자원을 충분히 활용하지 못합니다. 차량 규모가 커질수록 MRU는 감소하고 GPU 활성 활용률은 상승합니다. 에이전트 팀은 동료 분기를 요약하는 데 더 많은 시간을 보내고, 실제 로봇 조작에는 더 적은 시간을 사용합니다.
- **토큰 비용 초선형 증가**: 총 토큰 예산 증가가 벽시계 시간의 상응하는 감소보다 훨씬 빠르며, 차량 규모를 늘리는 것은 토큰 효율을 희생하여 더 빠른 정책 개선을 얻는 것입니다.
- **인식 병목**: SAM3는 작거나 흐릿한 물체에 대해 잘못된 마스크를 반환하거나 사용 가능한 마스크가 없을 수 있습니다. 해상도 향상 (256×256에서 480×640)은 감지를 개선하지만 모든 실패를 해결하지는 못합니다.
- **전이 메커니즘 제한**: 전이 실험은 서면 요약을 통해서만 지식을 전달하며, 원본 궤적, 숨겨진 로그, 체크포인트가 제거되어 전이 효과가 제한될 수 있습니다.
- **하지 않은 일**: 더 많은 작업에서의 검증, 장기 안정성 평가, 다양한 로봇 플랫폼으로의 일반화, 안전성의 형식적 보장, 다른 인간 참여 방법과의 체계적 비용 비교는 언급되지 않았습니다. 구체적인 훈련 구성 (학습률, 배치 크기, 옵티마이저), 훈련 시간, 데이터 양 (에피소드 수)은 공개되지 않았습니다.

## 엔지니어링 시사점

- **환경 인터페이스 신뢰성을 먼저 확인하라**: ENPIRE의 성공은 인식 API (SAM3)와 프로그램적 도구 호출의 안정성에 크게 의존합니다. 재현 시 SAM3의 대상 물체 감지 정확도를 우선 검증하고 (원본 프롬프트 10/20, 에이전트 최적화 후 17/20), 해상도 조정과 프롬프트 재작성의 최적화 여지를 확보하세요.
- **토큰 비용의 비선형성에 주의하라**: 4개에서 8개 에이전트로 확장 시 토큰 소비가 급격히 증가하고 벽시계 시간 이득은 감소합니다. 토큰 예산이 제한적이라면 4개 에이전트 구성의 비용 효율을 먼저 평가한 후 확장 여부를 결정하세요.
- **MRU 감소는 필연적 추세**: 다중 에이전트 협업 시 에이전트는 Git 협업과 로그 분석에 더 많은 시간을 보내고 실제 로봇 조작에는 더 적은 시간을 사용합니다. 로봇 활용률을 최대화하려면 더 효율적인 협업 프로토콜 (예: 분기 요약 빈도 감소)을 설계해야 합니다.
- **보상 함수 합성이 가장 큰 레버리지**: Pin insertion에서 BC 정규화 기여는 +10.8 pp로, 배치 크기 튜닝 (+0.9 pp)과 컨트롤러 보상 (+1.3 pp)을 훨씬 능가합니다. 재현 시 에이전트가 하이퍼파라미터 미세 조정보다 알고리즘 수준 개선을 탐색하도록 우선 유도하세요.
- **전이 실험은 신중해야 함**: 서면 요약만으로 지식을 전달하면 전이 효과가 제한될 수 있으며, 원본 궤적과 체크포인트 제거는 교차 작업 초기화 이점을 약화시킵니다. 전이가 필요하다면 일부 원본 데이터를 보존하는 것이 좋습니다.
