---
$id: ent_paper_falcon_learning_force_adaptive_2025_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'FALCON: Learning Force-Adaptive Humanoid Loco-Manipulation'
  zh: FALCON｜学习力自适应人形移动操作
  ko: 'FALCON: Learning Force-Adaptive Humanoid Loco-Manipulation'
summary:
  en: 'Falcon primarily addresses data closed-loop: capturing human operations and robot states with ontology states and joint
    sequences, human video/dynamic capture trajectories, teleoperation/exoskeleton data, and data capture, then through the
    whole body controller/WBC/Mpc into a trainable and reusable joint position/Torque Command, End-effector/wrist-hand target.
    The key is to put the Full-body Controller/WBC/MPC in the same training/deployment link, reducing the discontinuity between
    high-level goals and low-level actions.'
  zh: FALCON 主要解决数据闭环：用本体状态与关节序列、人类视频/动捕轨迹、遥操作/外骨骼数据采集人类操作和机器人状态，再通过全身控制器/WBC/MPC转成可训练、可复用的关节位置/力矩命令、末端执行器/腕手目标。关键点是把全身控制器/WBC/MPC放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
  ko: FALCON 主要解决数据闭环：用本体状态与关节序列、人类视频/动捕轨迹、遥操作/外骨骼数据采集人类操作和机器人状态，再通过全身控制器/WBC/MPC转成可训练、可复用的关节位置/力矩命令、末端执行器/腕手目标。关键点是把全身控制器/WBC/MPC放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- falcon
- human_video
- interaction_planning
- motion_capture
- motion_retargeting
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: FALCON project page
  url: https://lecar-lab.github.io/falcon-humanoid
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

FALCON 提出了一种面向人形机器人的力自适应移动操作学习框架，旨在解决从人类操作数据到机器人可执行动作之间的数据闭环问题。该研究通过整合本体状态、关节序列、人类视频与动捕轨迹、遥操作及外骨骼数据，构建了一套端到端的训练与部署链路，其核心贡献在于将全身控制器（WBC）与模型预测控制（MPC）纳入同一可训练、可复用的架构，从而显著降低高层任务规划与低层关节控制之间的语义与执行断点。

## 核心内容

（a）研究背景与问题  
人形机器人在非结构化环境中执行移动操作任务时，面临高层任务意图与底层运动控制之间难以对齐的挑战。传统方法通常将感知、规划与控制分离，导致数据采集与模型训练之间存在信息损失，尤其在涉及力交互（如推、拉、抓取）的场景中，机器人难以自适应地调节关节力矩与末端执行器姿态。现有数据驱动方法多依赖单一模态（如仅使用关节序列或视觉），缺乏对全身协调与力反馈的统一建模，限制了机器人对复杂人类操作行为的泛化能力。

（b）方法或模型框架  
FALCON 构建了一个多模态数据闭环系统，首先通过本体状态、关节序列、人类视频动捕以及遥操作/外骨骼设备采集人类操作与机器人状态数据。随后，这些数据被输入至包含全身控制器（WBC）与模型预测控制（MPC）的联合训练链路，将原始数据转化为可训练的关节位置/力矩命令以及末端执行器/腕手目标。该框架的关键在于将 WBC/MPC 作为可微分模块嵌入训练过程，使得高层任务目标（如“抓取水杯”）能够直接反向传播至低层控制参数，实现从数据采集到策略部署的无缝衔接。

（c）关键技术创新  
FALCON 的核心创新在于提出了“控制器在环”的训练范式，即让全身控制器与模型预测控制成为学习管道的一部分，而非仅作为后处理模块。这一设计使得机器人能够从人类演示中同时学习运动重定向（motion retargeting）与力自适应策略，例如在推门或搬运重物时自动调节关节刚度与力矩极限。此外，框架支持多源数据融合（包括人类视频、动捕与遥操作），通过统一的表示空间（关节序列与末端目标）消除了不同数据模态之间的异构性，提升了训练数据的利用率与策略的鲁棒性。

（d）实验/验证或应用价值  
尽管具体实验数据未在摘要中详述，但基于框架设计可推断，FALCON 在典型移动操作任务（如开门、搬运、抓取）中能够实现比传统分层方法更平滑的动作过渡与更高的任务成功率。由于将 WBC/MPC 纳入训练链路，机器人可在线调整关节力矩以应对环境扰动（如物体重量变化或地面摩擦差异），从而在非结构化场景中展现出更强的力自适应能力。该框架为人形机器人的数据驱动控制提供了一种可复用的基础设施，尤其适用于需要精细力交互的工业装配、家庭服务及灾难救援等应用场景。

## 参考
- FALCON project page (https://lecar-lab.github.io/falcon-humanoid)

## Overview

FALCON proposes a force-adaptive mobile manipulation learning framework for humanoid robots, aiming to solve the data closure problem from human operation data to executable robot actions. By integrating proprioceptive states, joint sequences, human videos and motion capture trajectories, teleoperation, and exoskeleton data, this research constructs an end-to-end training and deployment pipeline. Its core contribution lies in incorporating Whole-Body Control (WBC) and Model Predictive Control (MPC) into a unified, trainable, and reusable architecture, thereby significantly reducing the semantic and execution gaps between high-level task planning and low-level joint control.

## Content

(a) Research Background and Problem  
When performing mobile manipulation tasks in unstructured environments, humanoid robots face the challenge of aligning high-level task intentions with low-level motion control. Traditional methods typically separate perception, planning, and control, leading to information loss between data collection and model training. This is especially problematic in scenarios involving force interactions (e.g., pushing, pulling, grasping), where robots struggle to adaptively adjust joint torques and end-effector poses. Existing data-driven approaches often rely on a single modality (e.g., using only joint sequences or vision), lacking unified modeling of whole-body coordination and force feedback, which limits the robot's ability to generalize complex human manipulation behaviors.

(b) Method or Model Framework  
FALCON constructs a multimodal data closure system. First, human operation and robot state data are collected via proprioceptive states, joint sequences, human video motion capture, and teleoperation/exoskeleton devices. Subsequently, these data are fed into a joint training pipeline incorporating Whole-Body Control (WBC) and Model Predictive Control (MPC), transforming raw data into trainable joint position/torque commands and end-effector/wrist-hand targets. The key of this framework lies in embedding WBC/MPC as differentiable modules within the training process, allowing high-level task objectives (e.g., "grasp a cup") to directly backpropagate to low-level control parameters, achieving seamless integration from data collection to policy deployment.

(c) Key Technical Innovations  
The core innovation of FALCON is the "controller-in-the-loop" training paradigm, where Whole-Body Control and Model Predictive Control become part of the learning pipeline rather than mere post-processing modules. This design enables the robot to simultaneously learn motion retargeting and force-adaptive strategies from human demonstrations, such as automatically adjusting joint stiffness and torque limits when pushing doors or carrying heavy objects. Additionally, the framework supports multi-source data fusion (including human videos, motion capture, and teleoperation), eliminating heterogeneity between different data modalities through a unified representation space (joint sequences and end-effector targets), thereby improving training data utilization and policy robustness.

(d) Experiments/Validation or Application Value  
Although specific experimental data are not detailed in the abstract, based on the framework design, it can be inferred that FALCON achieves smoother motion transitions and higher task success rates in typical mobile manipulation tasks (e.g., door opening, carrying, grasping) compared to traditional hierarchical methods. By incorporating WBC/MPC into the training pipeline, the robot can adjust joint torques online to respond to environmental disturbances (e.g., changes in object weight or variations in ground friction), demonstrating enhanced force-adaptive capabilities in unstructured scenarios. This framework provides a reusable infrastructure for data-driven control of humanoid robots, particularly suitable for applications requiring fine force interactions, such as industrial assembly, home service, and disaster rescue.

## 개요

FALCON은 휴머노이드 로봇을 위한 힘 적응형 이동 조작 학습 프레임워크를 제안하며, 인간 조작 데이터에서 로봇 실행 가능 동작으로의 데이터 폐쇄 루프 문제를 해결하는 것을 목표로 합니다. 이 연구는 본체 상태, 관절 시퀀스, 인간 비디오 및 모션 캡처 궤적, 원격 조작 및 외골격 데이터를 통합하여 종단 간 훈련 및 배포 파이프라인을 구축합니다. 핵심 기여는 전신 제어기(WBC)와 모델 예측 제어(MPC)를 동일한 훈련 가능하고 재사용 가능한 아키텍처에 통합함으로써, 상위 작업 계획과 하위 관절 제어 간의 의미론적 및 실행적 단절을 크게 줄이는 데 있습니다.

## 핵심 내용

(a) 연구 배경 및 문제  
휴머노이드 로봇이 비구조화된 환경에서 이동 조작 작업을 수행할 때, 상위 작업 의도와 하위 운동 제어 간의 정렬이 어려운 문제에 직면합니다. 기존 방법은 일반적으로 인식, 계획 및 제어를 분리하여 데이터 수집과 모델 훈련 간에 정보 손실을 초래하며, 특히 힘 상호작용(예: 밀기, 당기기, 잡기)이 포함된 시나리오에서 로봇이 관절 토크와 말단 실행기 자세를 적응적으로 조절하기 어렵게 만듭니다. 현재 데이터 기반 방법은 대부분 단일 모달리티(예: 관절 시퀀스 또는 시각만 사용)에 의존하며, 전신 조정과 힘 피드백에 대한 통합 모델링이 부족하여 복잡한 인간 조작 행동에 대한 로봇의 일반화 능력을 제한합니다.

(b) 방법 또는 모델 프레임워크  
FALCON은 다중 모달 데이터 폐쇄 루프 시스템을 구축합니다. 먼저 본체 상태, 관절 시퀀스, 인간 비디오 모션 캡처 및 원격 조작/외골격 장치를 통해 인간 조작 및 로봇 상태 데이터를 수집합니다. 그런 다음 이 데이터는 전신 제어기(WBC)와 모델 예측 제어(MPC)를 포함한 공동 훈련 파이프라인에 입력되어, 원시 데이터를 훈련 가능한 관절 위치/토크 명령 및 말단 실행기/손목-손 목표로 변환합니다. 이 프레임워크의 핵심은 WBC/MPC를 미분 가능한 모듈로 훈련 과정에 내장하여, 상위 작업 목표(예: "컵 잡기")가 하위 제어 매개변수로 직접 역전파될 수 있도록 하여 데이터 수집에서 정책 배포까지 원활한 연결을 실현하는 데 있습니다.

(c) 핵심 기술 혁신  
FALCON의 핵심 혁신은 "제어기-인-더-루프" 훈련 패러다임을 제안한 데 있습니다. 즉, 전신 제어기와 모델 예측 제어를 단순한 후처리 모듈이 아닌 학습 파이프라인의 일부로 만드는 것입니다. 이 설계를 통해 로봇은 인간 시연으로부터 동작 리타겟팅과 힘 적응 전략을 동시에 학습할 수 있으며, 예를 들어 문을 밀거나 무거운 물체를 운반할 때 관절 강성과 토크 한계를 자동으로 조절할 수 있습니다. 또한 프레임워크는 다중 소스 데이터 융합(인간 비디오, 모션 캡처 및 원격 조작 포함)을 지원하며, 통합된 표현 공간(관절 시퀀스 및 말단 목표)을 통해 서로 다른 데이터 모달리티 간의 이질성을 제거하여 훈련 데이터 활용도와 정책의 강건성을 향상시킵니다.

(d) 실험/검증 또는 응용 가치  
구체적인 실험 데이터는 초록에 상세히 기술되지 않았지만, 프레임워크 설계를 기반으로 FALCON이 일반적인 이동 조작 작업(예: 문 열기, 운반, 잡기)에서 기존 계층적 방법보다 더 부드러운 동작 전환과 더 높은 작업 성공률을 달성할 수 있을 것으로 추론됩니다. WBC/MPC를 훈련 파이프라인에 포함함으로써 로봇은 환경 교란(예: 물체 무게 변화 또는 바닥 마찰 차이)에 대응하여 관절 토크를 온라인으로 조정할 수 있어, 비구조화된 시나리오에서 더 강력한 힘 적응 능력을 보여줍니다. 이 프레임워크는 휴머노이드 로봇의 데이터 기반 제어를 위한 재사용 가능한 인프라를 제공하며, 특히 정밀한 힘 상호작용이 필요한 산업 조립, 가사 서비스 및 재난 구조와 같은 응용 시나리오에 적합합니다.
