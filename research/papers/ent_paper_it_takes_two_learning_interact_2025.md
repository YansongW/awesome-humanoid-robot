---
$id: ent_paper_it_takes_two_learning_interact_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots'
  zh: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots'
  ko: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots'
summary:
  en: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots is a 2025 work on loco-manipulation and
    whole-body-control for humanoid robots.'
  zh: Harmanoid 是一个面向双人形机器人的全身交互运动模仿框架，由研究团队于2025年提出。其核心贡献在于通过接触感知运动重定向与交互驱动运动控制器，解决了单机器人框架忽略双体动力学导致的接触错位与运动不真实问题。
  ko: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots is a 2025 work on loco-manipulation and
    whole-body-control for humanoid robots.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- humanoid
- it_takes_two
- loco_manipulation
- whole_body_control
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2510.10206v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (711 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'It Takes Two: Learning Interactive Whole-Body Control Between Humanoid Robots (arXiv)'
  url: https://arxiv.org/abs/2510.10206
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

本文提出 CodeBotler 与 RoboEval 两个工具，前者通过 LLM 将自然语言任务描述转化为服务机器人可执行的 Python 嵌入式 DSL 程序，后者提供轻量级模拟器与时间逻辑评估基准，用于系统衡量 LLM 生成程序在动作排序与顺序正确性上的表现。作者在 16 个任务、5 种 LLM 上验证了该评估框架的有效性，并识别出三类典型失败模式。

## 它改变了什么

服务机器人编程长期被简化为“视觉运动策略”或“高层规划”，而真正决定任务成败的往往是动作的精确排序——例如“检查多少会议室没有标记”必须先遍历所有房间再汇总。现有代码生成基准（如数据处理函数）只适合输入-输出单元测试，无法捕捉这种时序依赖。本文改变了这一评估范式：不再问“程序最终输出对不对”，而是问“程序执行轨迹是否满足时间约束”。这迫使 LLM 生成程序不仅要“会调用技能”，还要“在正确的时间调用正确的技能”。

另一个被改变的认知是：验证程序逻辑不需要高保真 3D 模拟。作者用符号模拟器加时间逻辑检查替代了物理仿真，将评估成本从小时级降至秒级，同时保持了对逻辑错误的敏感度。这对实际部署意义重大——团队可以快速迭代提示词与模型，而不必为每个任务搭建仿真环境。

## 方法拆解

### 程序生成管线
- 用户任务描述与提示前缀拼接，前缀包含 8 种技能定义（`get_current_location`、`get_all_rooms`、`is_in_room`、`ask`、`pick`、`place`、`go_to`）及少量示例程序。
- 查询 LLM 生成 Python 程序，支持 OpenAI API、Google PaLM API、HuggingFace 模型（AutoModel 与 Text-Generation-Inference）。
- 关键设计：`get_current_location` 与 `get_all_rooms` 动态查询世界状态，避免硬编码；`is_in_room` 调用零样本 VLM 做感知推理；`ask` 提供结构化多选接口，适配触摸屏或音频交互。

### 执行与部署
- 程序通过 ROS Actionlib 作为客户端运行，遇到技能调用（如 `go_to("lobby")`）即向远程话题（如 `/go_to_server`）发布目标，由机器人端 action server 接收。
- 该设计使 CodeBotler 与具体机器人平台解耦，可机载或外部运行。

### 评估框架
- 模拟器接受世界状态 `w` 与程序 `π`，产生轨迹 `r`（技能序列及参数），记录所有 Python 错误与机器人执行错误。
- 评估器检查轨迹 `r` 是否满足时间约束 `c`，约束以合取范式表达；引入 RoboEval 时间逻辑（RTL），从 LTL 派生，更易书写且可读性更好。
- 基准含 16 个任务，每个任务 5 种不同措辞提示（测试鲁棒性），多个世界状态对应多个时间检查。

## 关键创新

1. **时间逻辑作为评估核心**：将 LTL 简化为 RTL，使非形式化用户任务（如“先检查所有房间再报告”）能精确表达为可自动验证的约束。这是首次将时序正确性作为服务机器人程序生成的首要评估维度，而非仅看最终结果。

2. **符号模拟替代物理仿真**：用 Python 解释器加技能符号模拟，在保证逻辑验证能力的同时将评估成本降低数个数量级。这使得大规模、多提示、多世界状态的鲁棒性测试成为可能，而高保真模拟下这类测试几乎不可行。

3. **提示措辞鲁棒性测试**：每个任务固定 5 种不同措辞，直接暴露 LLM 对自然语言微小变化的敏感性。这一设计在现有基准中罕见，但对实际部署至关重要——用户不会用固定模板描述任务。

## 实验与结果

评估了 GPT-4、GPT-3.5、PaLM2、CodeLLama、StarCoder 五种模型，覆盖 16 个任务（如 Get Drink、Halloween List、Mail Delivery 等）。图 5a 与 5b 为图片，具体数字未提取，但作者明确报告了以下关键结果：

| 指标 | 数值 | 说明 |
|------|------|------|
| 平均 pass@1（最佳模型） | 0.99 | 论文未明确对应具体模型，但为所有任务平均 |
| 平均 pass@1（最差模型） | 0.00 | 论文未明确对应具体模型，但为所有任务平均 |
| 失败模式：Python 运行时错误 | 0.40 | 占所有失败的比例 |
| 失败模式：执行不可行动作 | 0.04 | 占所有失败的比例 |
| 失败模式：满足任务要求错误 | 0.58 | 占所有失败的比例（由 0.40+0.04+0.58 计算，总和 1.02（由表内数值 0.75÷0.75 计算） 因四舍五入） |

失败模式分类显示：满足任务要求错误（即程序能运行但顺序不对）占比最高，验证了时序正确性评估的必要性。作者引入简单拒绝采样程序后，机器人执行错误立即减少，但具体降幅论文未明确。

## 边界与局限

论文未明确列出作者承认的局限。从事实要点推断：未对真实机器人部署做定量评估，未讨论多机器人协同或动态环境干扰；模拟器假设世界状态完全可观测且技能执行无噪声，这在真实场景中不成立。基准任务规模有限（16 个），且提示措辞变化仅 5 种，可能不足以覆盖真实用户表达的多样性。硬件与计算资源未提及，复现时需自行配置。

## 工程启示

复现时先核对三处：一是 ROS Actionlib 话题命名是否与你的机器人接口一致（`/go_to_server` 等），不一致需修改 CodeBotler 客户端映射；二是 `is_in_room` 依赖的 VLM 服务是否可用，零样本推理延迟会直接影响端到端执行时间；三是提示前缀中的示例程序是否与你的技能集匹配，示例质量对 LLM 输出影响极大。

最容易踩坑的是时间约束的书写——RTL 虽比 LTL 易读，但合取范式中的时序依赖（如“先访问所有房间再检查标记”）容易写错，建议先用模拟器对单个任务做 10 次以上轨迹采样验证约束正确性。另外，拒绝采样虽能减少执行错误，但会降低 pass@1，需根据任务容忍度权衡。若下游团队要接入新技能，务必同步更新提示前缀中的技能定义与模拟器中的符号执行逻辑，否则会出现“程序能生成但模拟器无法执行”的脱节。

## 参考
- http://arxiv.org/abs/2510.10206v1

## 개요
Harmanoid 프레임워크는 두 휴머노이드 로봇 간의 물리적 상호작용 시나리오에 특화되어 설계되었으며, 기존 단일 로봇 자율성 연구의 한계를突破了. 이 프레임워크는 두 가지 핵심 모듈을 포함합니다: 접촉 인식 운동 리타게팅 모듈은 SMPL 인체 모델의 접촉점을 로봇 정점에 매핑하여 두 몸체 간의 조화로운 운동을 복원하고, 상호작용 구동 운동 컨트롤러는 상호작용 특정 보상 함수를 활용하여 핵심 지점의 협력과 물리적으로 합리적인 접촉을 강제합니다. 실험 결과, Harmanoid는 상호작용 운동 모방 작업에서 기존 단일 로봇 프레임워크보다 현저히 우수하며, 후자는 이중 몸체 상호작용 시나리오에서 거의 완전히 실패합니다.

## 핵심 내용
### 방법 아키텍처
Harmanoid 프레임워크는 두 가지 핵심 구성 요소로 이루어져 있습니다:
- **접촉 인식 운동 리타게팅**: SMPL 인체 모델의 접촉점과 로봇 정점 간의 매핑 관계를 구축하여 두 로봇 간의 사지 협력 운동을 복원하고, 접촉 오정렬 및 관통 문제를 방지합니다.
- **상호작용 구동 운동 컨트롤러**: 상호작용 특정 보상 함수를 설계하여 두 로봇의 핵심 지점 협력 운동과 물리적으로 합리적인 접촉 행동을 강제하며, 운동학적 충실도와 물리적 현실성을 보장합니다.

### 실험 설정
- 이중 휴머노이드 로봇 플랫폼을 사용하여 상호작용 운동 모방 실험을 수행
- 비교 기준은 기존 단일 로봇 전신 제어 프레임워크
- 평가 지표는 운동학적 충실도(핵심 지점 오차)와 물리적 합리성(접촉 관통률)을 포함

### 핵심 결과
- Harmanoid는 상호작용 운동 모방 작업에서 모든 단일 로봇 기준 방법을 현저히 능가
- 단일 로봇 프레임워크는 이중 몸체 상호작용 시나리오에서 거의 완전히 실패하며, 접촉 오정렬, 관통 및 부자연스러운 운동으로 나타남
- 이중 몸체 접촉과 상호작용 인식 동역학을 명시적으로 모델링함으로써, Harmanoid는 단일 로봇 프레임워크가 처리할 수 없는 결합 행동을 성공적으로 포착

### 결론
Harmanoid는 이중 로봇 간의 접촉과 상호작용 동역학을 명시적으로 모델링하는 것이 물리적으로 합리적인 휴머노이드 로봇 상호작용을 구현하는 데 필수적임을 증명하며, 향후 다중 로봇 협력 작업(예: 운반, 조립)을 위한 기반 프레임워크를 제공합니다.
