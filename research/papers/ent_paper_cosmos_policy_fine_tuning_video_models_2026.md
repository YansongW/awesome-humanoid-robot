---
$id: ent_paper_cosmos_policy_fine_tuning_video_models_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Cosmos Policy: Fine-Tuning Video Models for Visuomotor Control and Planning'
  zh: 'Cosmos Policy: Fine-Tuning Video Models for Visuomotor Control and Planning'
  ko: 'Cosmos Policy: Fine-Tuning Video Models for Visuomotor Control and Planning'
summary:
  en: Recent video generation models demonstrate remarkable ability to capture complex physical interactions and scene evolution
    over time. To leverage their spatiotemporal priors, robotics works have adapted video models for policy learning but introduce
    complexity by requiring multiple stages of post-training and new architectural components for action generation.
  zh: Cosmos Policy 由 NVIDIA 提出，是一种通过单阶段微调将预训练视频模型 Cosmos-Predict2 转化为机器人策略的简洁方法。其核心贡献在于无需修改架构，直接利用视频模型的潜在扩散过程生成动作、未来状态图像和累积奖励值，在
    LIBERO 和 RoboCasa 基准上分别达到 98.5% 和 67.1% 的平均成功率，并在真实世界双臂操作任务中取得最高平均分。
  ko: Recent video generation models demonstrate remarkable ability to capture complex physical interactions and scene evolution
    over time. To leverage their spatiotemporal priors, robotics works have adapted video models for policy learning but introduce
    complexity by requiring multiple stages of post-training and new architectural components for action generation.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- cosmos
- policy
- fine
- tuning
- video
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 777 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2601.16163v1); zh content by DeepSeek from the abstract. Institutions unknown
    (not in source list). [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek
    deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2601.16163 Cosmos Policy: Fine-Tuning Video Models for Visuomotor Control and Planning'
  url: https://arxiv.org/abs/2601.16163
  accessed_at: '2026-07-31'
  date: '2026-01-22'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

NVIDIA 团队提出 Cosmos Policy，通过“潜在帧注入”机制将预训练视频生成模型 Cosmos-Predict2-2B 直接微调为统一机器人策略，无需修改架构即可同时输出动作、未来状态与价值函数。该方法在 LIBERO 平均成功率 98.5%、RoboCasa 67.1%、真实 ALOHA 93.6%，并支持基于模型的最佳 N 采样规划，在挑战性任务中额外提升 12.5 个百分点。

## 它改变了什么

机器人策略学习长期面临“视频先验”与“动作输出”之间的架构鸿沟。此前主流方案要么像 Diffusion Policy 那样从零训练动作扩散模型，浪费了视频生成模型已习得的物理与时空先验；要么像 UVA/UWM 那样设计自定义统一架构，但无法直接加载预训练权重。更麻烦的是，若要利用世界模型做规划，通常需要额外训练逆动力学模型或独立价值网络，形成多阶段、多组件的复杂流水线。

Cosmos Policy 真正改变的是：把“视频生成”与“策略学习”这两个原本需要拼接的模块，压缩为同一个扩散 Transformer 的同一套去噪过程。它证明了预训练视频模型的潜在空间可以天然承载动作、本体感觉、价值函数等非图像模态，且通过简单的潜在帧替换即可实现模态注入，无需任何新架构组件。这意味着一类新的“视频基础模型即策略”范式成为可能——下游机器人团队不再需要设计专用动作头或价值头，只需复用视频模型的生成能力。

## 方法拆解

### 核心机制：潜在帧注入（Latent Frame Injection）
- 将机器人本体感觉、动作块、状态值等非图像模态编码为新的潜在帧，直接插入视频模型的潜在扩散序列中，不修改网络架构。
- 每个模态被归一化到 [−1, +1]，通过重复复制填充至与单个潜在帧相同的 H′×W′×C′ 体积。
- 多相机视角直接在图像序列层面插入额外相机图像。

### 序列设计与条件方案
- 潜在序列顺序为 (s, a, s′, V(s′))，支持从左到右的自回归解码。
- 以两个静态第三视角相机 + 一个腕部相机为例，序列包含 11 个潜在帧：空白占位符、本体感觉、腕部图像、两个第三视角图像、动作块、未来本体感觉、未来腕部图像、未来两个第三视角图像、未来状态值。
- 训练时通过条件掩码决定哪些帧作为条件（干净）、哪些作为生成目标（加噪），从而在同一架构中切换策略/世界模型/价值函数三种训练模式。

### 联合训练目标
- 训练数据 50% 来自演示（训练策略 p(a, s′, V(s′)|s)），50% 来自回滚数据（其中一半训练世界模型 p(s′, V(s′)|s, a)，另一半训练价值函数 p(V(s′)|s, a, s′)）。
- 策略与世界模型均包含辅助目标：策略联合预测未来状态与价值，而非仅预测动作；世界模型联合预测未来状态与价值，而非仅预测未来状态。

### 推理与规划
- 直接策略评估：并行解码，仅提取动作。
- 基于模型规划：best-of-N 采样——策略生成 N 个动作提案，世界模型预测每个提案的未来状态，价值函数评估，选择预测价值最高的动作执行。
- 集成预测：每个动作查询世界模型 3 次、价值函数 5 次，共 15 个值预测，通过“多数均值”聚合。

### 噪声分布调整
- 基础模型的 σ 采样为对数正态分布（P_mean=1.39, P_std=1.2），对高噪声区域权重不足，导致生成初期去噪不良。
- 改为混合分布：0.7 概率从原始对数正态采样，0.3 概率从 [1.0, 85.0] 均匀采样，扩展高 σ 尾部。
- 测试时将 σ_min 从 0.002 提高至 4，经验上提升动作、未来状态与价值的预测准确性。

## 关键创新

1. **潜在帧注入替代架构修改**：这是最关键的创新。以往将非图像模态引入视频模型需要修改注意力机制或添加专用编码器，Cosmos Policy 仅需将模态数据填充为潜在帧并覆盖占位符，完全复用预训练模型的 VAE 与 DiT。这大幅降低了将视频基础模型适配为策略的工程复杂度，且保留了预训练权重中的时空先验。

2. **单阶段联合训练策略、世界模型与价值函数**：通过条件掩码切换训练目标，同一模型在 50/25/25 的批次比例下同时学习三种能力。消融实验显示，移除辅助目标（策略仅预测动作）导致平均成功率下降 1.5 个百分点；完全移除未来状态与价值预测则从 62.5% 骤降至 44.4%，证明联合预测未来状态是策略有效性的核心机制，而非简单的架构红利。

3. **利用策略回滚数据精炼世界模型**：作者发现仅靠演示数据训练的世界模型难以泛化到分布外，因此部署策略收集回滚数据（648 次），以 90% 批次用于精炼世界模型与价值函数。双部署策略（原始检查点作为策略模型，精炼检查点作为规划模型）确保规划模型在 on-policy 数据上训练，这一设计使基于模型的规划在挑战性任务中额外提升 12.5 个百分点。

## 实验与结果

### LIBERO 模拟基准
| 方法 | Spatial | Object | Goal | Long | 平均 |
|------|---------|--------|------|------|------|
| Cosmos Policy | 98.1 | 100.0 | 98.2 | 97.6 | **98.5** |
| CogVLA | - | - | - | - | 97.4 |
| OpenVLA-OFT | - | - | - | - | 97.1 |
| π₀.₅ | - | - | - | - | 96.9 |
| UniVLA | - | - | - | - | 95.2 |
| π₀ | - | - | - | - | 94.2 |
| Dita | - | - | - | - | 92.3 |
| Diffusion Policy | - | - | - | - | 72.4 |

### RoboCasa 模拟基准（仅 50 个人类演示）
| 方法 | 训练演示数 | 平均成功率 |
|------|-----------|-----------|
| Cosmos Policy | 50 | **67.1** |
| GR00T-N1.5 + HAMLET | 300 | 66.4 |
| FLARE | 300 | 66.4 |
| Video Policy | 300 | 66.0 |
| GR00T-N1.5 | 300 | 64.1 |
| π₀ | 300 | 62.5 |
| UWM | 1000 | 60.8 |
| GR00T-N1 + DUST | 300 | 58.5 |
| GR00T-N1 + DreamGen | 300+10000 合成 | 57.6 |
| DP-VLA | 3000 | 57.3 |
| UVA | 50 | 50.0 |
| GR00T-N1 | 300 | 49.6 |

### 真实 ALOHA 机器人（101 次试验）
| 方法 | 平均分数 |
|------|---------|
| Cosmos Policy | **93.6** |
| π₀.₅ | 88.6 |
| π₀ | 77.9 |
| OpenVLA-OFT+ | 62.0 |
| Diffusion Policy | 33.6 |

- 基于模型规划后，两个挑战性任务（“put candies in bowl”和“put candy in ziploc bag”）平均得分提升 12.5 个百分点。
- 消融：移除辅助损失下降 1.5 点；从随机权重训练下降 3.9 点；从零训练变体出现抖动运动，被停止评估。
- 1 去噪步推理：成功率 66.4%（vs 5 步 67.1%），延迟 0.16 秒/动作块（vs 0.61 秒），快近 4 倍。

## 边界与局限

- 基于模型规划的推理延迟显著（约 5 秒/动作块，8 块 H100 并行），限制了动态任务与移动机器人的适用性。
- 有效规划需要大量回放数据（648 次），从更少回放中学习是未解决的开放问题。
- 未进行大规模动作标注机器人数据预训练（与 VLA 模型不同）；未使用输入历史，仅预测 t 和 t+K 时刻的观测。
- 未在 RoboCasa 上使用 MimicGen 生成的额外演示（仅用 50 个人类演示）。
- 真实状态并非完全可观测，世界模型实际预测的是未来观测（本体感觉与相机图像），论文为符号简洁使用“状态”一词。
- 论文未明确：具体训练步数、学习率、批量大小（除附录 A.2 外）、GPU 型号与数量（除规划并行化外）、推理频率的具体数值（除 ALOHA 25 Hz 外）。

## 工程启示

复现时首先核对**潜在帧注入的填充逻辑**——每个模态需要归一化到 [−1, +1] 并复制填充至 H′×W′×C′ 体积，动作块提取时需计算所有副本的平均值再反归一化，这一步最容易出错且直接影响动作精度。其次，**噪声分布调整不可跳过**：默认对数正态分布对高 σ 区域权重不足，会导致生成初期去噪不良和级联误差，务必采用 0.7/0.3 的混合采样并将 σ_min 设为 4。

训练时注意**数据批次比例**：50% 演示 + 50% 回滚（回滚中世界模型与价值函数各半），回滚数据对规划能力至关重要，仅用演示训练的世界模型在分布外泛化上会明显失效。部署时若追求低延迟，1 去噪步（0.16 秒/动作块）仅损失 0.5% 成功率，是实际机器人控制中更现实的选择；若追求最高成功率，则需接受 0.61 秒（模拟）或 0.95 秒（ALOHA）的推理延迟。规划模式需 8 块 GPU 并行，且总耗时约 5 秒，仅适用于非实时任务。

## 参考
- https://arxiv.org/abs/2601.16163
- https://github.com/ImChong/Robotics_Notebooks

## Overview

The NVIDIA team introduces Cosmos Policy, which fine-tunes the pretrained video generation model Cosmos-Predict2-2B directly into a unified robot policy via a "latent frame injection" mechanism, enabling simultaneous output of actions, future states, and value functions without modifying the architecture. The method achieves an average success rate of 98.5% on LIBERO, 67.1% on RoboCasa, and 93.6% on real ALOHA, and supports model-based best-of-N sampling planning, providing an additional 12.5 percentage point improvement on challenging tasks.

## What It Changes

Robot policy learning has long faced an architectural gap between "video priors" and "action output." Previous mainstream approaches either trained action diffusion models from scratch, like Diffusion Policy, wasting the physical and spatiotemporal priors already learned by video generation models, or designed custom unified architectures like UVA/UWM, which could not directly load pretrained weights. More troublesome, leveraging world models for planning typically required additional training of inverse dynamics models or separate value networks, forming complex multi-stage, multi-component pipelines.

What Cosmos Policy truly changes is compressing the two modules that previously needed to be stitched together—"video generation" and "policy learning"—into the same denoising process of the same diffusion transformer. It demonstrates that the latent space of a pretrained video model can naturally accommodate non-image modalities such as actions, proprioception, and value functions, and that modality injection can be achieved through simple latent frame replacement without any new architectural components. This makes possible a new paradigm of "video foundation models as policies"—downstream robotics teams no longer need to design dedicated action heads or value heads, but can simply reuse the generative capabilities of video models.

## Method Breakdown

### Core Mechanism: Latent Frame Injection
- Non-image modalities such as robot proprioception, action chunks, and state values are encoded as new latent frames and directly inserted into the video model's latent diffusion sequence without modifying the network architecture.
- Each modality is normalized to [−1, +1] and replicated to fill a volume identical to a single latent frame's H′×W′×C′.
- Multiple camera views are inserted directly at the image sequence level as additional camera images.

### Sequence Design and Conditioning Scheme
- The latent sequence order is (s, a, s′, V(s′)), supporting left-to-right autoregressive decoding.
- With two static third-person cameras plus one wrist camera as an example, the sequence contains 11 latent frames: blank placeholder, proprioception, wrist image, two third-person images, action chunk, future proprioception, future wrist image, future two third-person images, future state value.
- During training, a conditioning mask determines which frames serve as conditions (clean) and which as generation targets (noisy), enabling switching among policy/world model/value function training modes within the same architecture.

### Joint Training Objective
- Training data is 50% from demonstrations (training policy p(a, s′, V(s′)|s)) and 50% from rollback data (half training the world model p(s′, V(s′)|s, a), half training the value function p(V(s′)|s, a, s′)).
- Both the policy and world model include auxiliary objectives: the policy jointly predicts future states and values rather than only actions; the world model jointly predicts future states and values rather than only future states.

### Inference and Planning
- Direct policy evaluation: parallel decoding, extracting only actions.
- Model-based planning: best-of-N sampling—the policy generates N action proposals, the world model predicts future states for each proposal, the value function evaluates them, and the action with the highest predicted value is executed.
- Ensemble prediction: each action queries the world model 3 times and the value function 5 times, yielding 15 value predictions aggregated via "majority mean."

### Noise Distribution Adjustment
- The base model's σ sampling uses a log-normal distribution (P_mean=1.39, P_std=1.2), which underweights high-noise regions, causing poor denoising in early generation.
- Changed to a mixed distribution: 0.7 probability from the original log-normal sampling, 0.3 probability from uniform sampling over [1.0, 85.0], extending the high-σ tail.
- At test time, σ_min is raised from 0.002 to 4, empirically improving prediction accuracy for actions, future states, and values.

## Key Innovations

1. **Latent frame injection replaces architectural modification**: This is the most critical innovation. Previously, introducing non-image modalities into video models required modifying attention mechanisms or adding dedicated encoders. Cosmos Policy only needs to pad modality data into latent frames and overwrite placeholders, fully reusing the pretrained model's VAE and DiT. This greatly reduces the engineering complexity of adapting video foundation models into policies while preserving the spatiotemporal priors in pretrained weights.

2. **Single-stage joint training of policy, world model, and value function**: Training objectives are switched via conditioning masks, with the same model learning all three capabilities simultaneously under a 50/25/25 batch ratio. Ablation experiments show that removing auxiliary objectives (policy predicting only actions) decreases average success rate by 1.5 percentage points; completely removing future state and value prediction drops performance from 62.5% to 44.4%, proving that joint future state prediction is the core mechanism for policy effectiveness, not a simple architectural benefit.

3. **Refining the world model with policy rollback data**: The authors found that world models trained solely on demonstration data struggle to generalize out-of-distribution, so they deployed the policy to collect rollback data (648 episodes), using 90% of batches to refine the world model and value function. The dual-deployment strategy (original checkpoint as the policy model, refined checkpoint as the planning model) ensures the planning model trains on on-policy data, a design that yields an additional 12.5 percentage point improvement in model-based planning on challenging tasks.

## Experiments and Results

### LIBERO Simulation Benchmark
| Method | Spatial | Object | Goal | Long | Average |
|------|---------|--------|------|------|------|
| Cosmos Policy | 98.1 | 100.0 | 98.2 | 97.6 | **98.5** |
| CogVLA | - | - | - | - | 97.4 |
| OpenVLA-OFT | - | - | - | - | 97.1 |
| π₀.₅ | - | - | - | - | 96.9 |
| UniVLA | - | - | - | - | 95.2 |
| π₀ | - | - | - | - | 94.2 |
| Dita | - | - | - | - | 92.3 |
| Diffusion Policy | - | - | - | - | 72.4 |

### RoboCasa Simulation Benchmark (50 human demonstrations only)
| Method | Training Demonstrations | Average Success Rate |
|------|-----------|-----------|
| Cosmos Policy | 50 | **67.1** |
| GR00T-N1.5 + HAMLET | 300 | 66.4 |
| FLARE | 300 | 66.4 |
| Video Policy | 300 | 66.0 |
| GR00T-N1.5 | 300 | 64.1 |
| π₀ | 300 | 62.5 |
| UWM | 1000 | 60.8 |
| GR00T-N1 + DUST | 300 | 58.5 |
| GR00T-N1 + DreamGen | 300+10000 synthetic | 57.6 |
| DP-VLA | 3000 | 57.3 |
| UVA | 50 | 50.0 |
| GR00T-N1 | 300 | 49.6 |

### Real ALOHA Robot (101 trials)
| Method | Average Score |
|------|---------|
| Cosmos Policy | **93.6** |
| π₀.₅ | 88.6 |
| π₀ | 77.9 |
| OpenVLA-OFT+ | 62.0 |
| Diffusion Policy | 33.6 |

- After model-based planning, the two challenging tasks ("put candies in bowl" and "put candy in ziploc bag") improve by an average of 12.5 percentage points.
- Ablations: removing auxiliary losses decreases by 1.5 points; training from random weights decreases by 3.9 points; the from-scratch variant exhibits jittery motion and is stopped from evaluation.
- 1 denoising step inference: success rate 66.4% (vs 67.1% with 5 steps), latency 0.16 seconds/action chunk (vs 0.61 seconds), nearly 4× faster.

## Boundaries and Limitations

- Model-based planning inference latency is significant (approximately 5 seconds/action chunk with 8 H100 GPUs in parallel), limiting applicability to dynamic tasks and mobile robots.
- Effective planning requires substantial rollback data (648 episodes); learning from fewer rollbacks remains an open problem.
- No large-scale action-annotated robot data pretraining (unlike VLA models); no input history is used, predicting only observations at times t and t+K.
- No additional demonstrations generated by MimicGen are used on RoboCasa (only 50 human demonstrations).
- True states are not fully observable; the world model actually predicts future observations (proprioception and camera images), with the paper using "state" for notational simplicity.
- The paper does not specify: exact training steps, learning rate, batch size (except Appendix A.2), GPU model and count (except planning parallelization), and specific inference frequency values (except ALOHA 25 Hz).

## Engineering Insights

When reproducing, first verify the **latent frame injection padding logic**—each modality needs normalization to [−1, +1] and replication to fill the H′×W′×C′ volume; action chunk extraction requires averaging all copies before denormalization, and this step is the most error-prone and directly impacts action accuracy. Second, **noise distribution adjustment is not optional**: the default log-normal distribution underweights high-σ regions, causing poor early denoising and cascading errors; be sure to use the 0.7/0.3 mixed sampling and set σ_min to 4.

During training, pay attention to **data batch ratios**: 50% demonstrations + 50% rollback (with rollback split evenly between world model and value function); rollback data is critical for planning capability, as world models trained only on demonstrations clearly fail in out-of-distribution generalization. For deployment, if low latency is prioritized, 1 denoising step (0.16 seconds/action chunk) sacrifices only 0.5% success rate and is a more realistic choice for real robot control; if maximum success rate is desired, accept inference latencies of 0.61 seconds (simulation) or 0.95 seconds (ALOHA). Planning mode requires 8 GPUs in parallel with a total time of approximately 5 seconds, suitable only for non-real-time tasks.

## 개요

NVIDIA 팀은 Cosmos Policy를 제안하며, "잠재 프레임 주입" 메커니즘을 통해 사전 훈련된 비디오 생성 모델 Cosmos-Predict2-2B를 아키텍처 수정 없이 직접 통합 로봇 정책으로 미세 조정하여, 동시에 행동, 미래 상태 및 가치 함수를 출력할 수 있게 했다. 이 방법은 LIBERO 평균 성공률 98.5%, RoboCasa 67.1%, 실제 ALOHA 93.6%를 달성했으며, 모델 기반 최적 N 샘플링 계획을 지원하여 도전적인 작업에서 추가로 12.5% 포인트를 향상시켰다.

## 무엇이 바뀌었는가

로봇 정책 학습은 오랫동안 "비디오 사전 지식"과 "행동 출력" 사이의 아키텍처 격차에 직면해 왔다. 기존의 주류 접근 방식은 Diffusion Policy처럼 행동 확산 모델을 처음부터 훈련하여 비디오 생성 모델이 이미 학습한 물리적 및 시공간 사전 지식을 낭비하거나, UVA/UWM처럼 사용자 정의 통합 아키텍처를 설계했지만 사전 훈련된 가중치를 직접 로드할 수 없었다. 더 문제는 세계 모델을 계획에 활용하려면 일반적으로 역동역학 모델이나 별도의 가치 네트워크를 추가로 훈련해야 하여, 다단계·다구성 요소의 복잡한 파이프라인을 형성한다는 점이다.

Cosmos Policy가 진정으로 바꾼 것은 "비디오 생성"과 "정책 학습"이라는 원래 이어 붙여야 했던 두 모듈을 동일한 확산 Transformer의 동일한 노이즈 제거 프로세스로 압축한 것이다. 사전 훈련된 비디오 모델의 잠재 공간이 행동, 고유 수용 감각, 가치 함수 등 비이미지 양식을 자연스럽게 담을 수 있으며, 간단한 잠재 프레임 교체만으로 양식 주입이 가능하고 새로운 아키텍처 구성 요소가 필요 없음을 증명했다. 이는 "비디오 기반 모델이 곧 정책"이라는 새로운 패러다임을 가능하게 한다—하위 로봇 팀은 더 이상 전용 행동 헤드나 가치 헤드를 설계할 필요 없이 비디오 모델의 생성 능력을 재사용하면 된다.

## 방법 분석

### 핵심 메커니즘: 잠재 프레임 주입 (Latent Frame Injection)
- 로봇 고유 수용 감각, 행동 블록, 상태 값 등 비이미지 양식을 새로운 잠재 프레임으로 인코딩하여 비디오 모델의 잠재 확산 시퀀스에 직접 삽입하며, 네트워크 아키텍처는 수정하지 않는다.
- 각 양식은 [−1, +1]로 정규화되고, 반복 복제를 통해 단일 잠재 프레임과 동일한 H′×W′×C′ 볼륨으로 채워진다.
- 다중 카메라 시점은 이미지 시퀀스 수준에서 추가 카메라 이미지를 직접 삽입한다.

### 시퀀스 설계 및 조건 방식
- 잠재 시퀀스 순서는 (s, a, s′, V(s′))이며, 왼쪽에서 오른쪽으로의 자기회귀 디코딩을 지원한다.
- 두 개의 정적 제3자 시점 카메라 + 하나의 손목 카메라를 예로 들면, 시퀀스는 11개의 잠재 프레임을 포함한다: 빈 자리 표시자, 고유 수용 감각, 손목 이미지, 두 개의 제3자 시점 이미지, 행동 블록, 미래 고유 수용 감각, 미래 손목 이미지, 미래 두 개의 제3자 시점 이미지, 미래 상태 값.
- 훈련 시 조건 마스크를 통해 어떤 프레임이 조건(깨끗함)이고 어떤 프레임이 생성 대상(노이즈 추가)인지 결정하여, 동일한 아키텍처에서 정책/세계 모델/가치 함수의 세 가지 훈련 모드를 전환한다.

### 공동 훈련 목표
- 훈련 데이터의 50%는 시연(정책 p(a, s′, V(s′)|s) 훈련)에서, 50%는 롤백 데이터(그중 절반은 세계 모델 p(s′, V(s′)|s, a) 훈련, 나머지 절반은 가치 함수 p(V(s′)|s, a, s′) 훈련)에서 온다.
- 정책과 세계 모델 모두 보조 목표를 포함한다: 정책은 행동만 예측하는 것이 아니라 미래 상태와 가치를 공동 예측하고, 세계 모델은 미래 상태만 예측하는 것이 아니라 미래 상태와 가치를 공동 예측한다.

### 추론 및 계획
- 직접 정책 평가: 병렬 디코딩, 행동만 추출.
- 모델 기반 계획: best-of-N 샘플링—정책이 N개의 행동 제안을 생성하고, 세계 모델이 각 제안의 미래 상태를 예측하며, 가치 함수가 평가하여 예측 가치가 가장 높은 행동을 선택해 실행한다.
- 앙상블 예측: 각 행동에 대해 세계 모델을 3회, 가치 함수를 5회 쿼리하여 총 15개의 값 예측을 만들고, "다수 평균"으로 집계한다.

### 노이즈 분포 조정
- 기본 모델의 σ 샘플링은 로그정규 분포(P_mean=1.39, P_std=1.2)로, 고노이즈 영역에 대한 가중치가 부족하여 생성 초기 노이즈 제거가 불량하다.
- 혼합 분포로 변경: 0.7 확률로 원래 로그정규에서 샘플링, 0.3 확률로 [1.0, 85.0]에서 균일 샘플링하여 고σ 꼬리를 확장한다.
- 테스트 시 σ_min을 0.002에서 4로 높여, 경험적으로 행동, 미래 상태 및 가치 예측 정확도를 향상시킨다.

## 핵심 혁신

1. **아키텍처 수정 대신 잠재 프레임 주입**: 이것이 가장 핵심적인 혁신이다. 과거에는 비이미지 양식을 비디오 모델에 도입하려면 어텐션 메커니즘을 수정하거나 전용 인코더를 추가해야 했지만, Cosmos Policy는 양식 데이터를 잠재 프레임으로 채우고 자리 표시자를 덮어쓸 뿐이며, 사전 훈련된 모델의 VAE와 DiT를 완전히 재사용한다. 이는 비디오 기반 모델을 정책으로 적용하는 엔지니어링 복잡성을 크게 줄이고, 사전 훈련된 가중치의 시공간 사전 지식을 보존한다.

2. **정책, 세계 모델, 가치 함수의 단일 단계 공동 훈련**: 조건 마스크로 훈련 목표를 전환하여, 동일한 모델이 50/25/25 배치 비율로 세 가지 능력을 동시에 학습한다. 절제 실험에 따르면 보조 목표를 제거하면(정책이 행동만 예측) 평균 성공률이 1.5% 포인트 하락하고, 미래 상태와 가치 예측을 완전히 제거하면 62.5%에서 44.4%로 급락하여, 미래 상태 공동 예측이 단순한 아키텍처 이점이 아니라 정책 효과성의 핵심 메커니즘임을 증명한다.

3. **정책 롤백 데이터를 활용한 세계 모델 정제**: 저자는 시연 데이터만으로 훈련된 세계 모델이 분포 외 일반화에 어려움을 겪는다는 것을 발견하고, 배포 정책으로 롤백 데이터(648회)를 수집하여 90% 배치로 세계 모델과 가치 함수를 정제한다. 이중 배포 전략(원본 체크포인트를 정책 모델로, 정제된 체크포인트를 계획 모델로 사용)은 계획 모델이 on-policy 데이터로 훈련되도록 보장하며, 이 설계 덕분에 모델 기반 계획이 도전적인 작업에서 추가로 12.5% 포인트 향상된다.

## 실험 및 결과

### LIBERO 시뮬레이션 벤치마크
| 방법 | Spatial | Object | Goal | Long | 평균 |
|------|---------|--------|------|------|------|
| Cosmos Policy | 98.1 | 100.0 | 98.2 | 97.6 | **98.5** |
| CogVLA | - | - | - | - | 97.4 |
| OpenVLA-OFT | - | - | - | - | 97.1 |
| π₀.₅ | - | - | - | - | 96.9 |
| UniVLA | - | - | - | - | 95.2 |
| π₀ | - | - | - | - | 94.2 |
| Dita | - | - | - | - | 92.3 |
| Diffusion Policy | - | - | - | - | 72.4 |

### RoboCasa 시뮬레이션 벤치마크 (인간 시연 50개만 사용)
| 방법 | 훈련 시연 수 | 평균 성공률 |
|------|-----------|-----------|
| Cosmos Policy | 50 | **67.1** |
| GR00T-N1.5 + HAMLET | 300 | 66.4 |
| FLARE | 300 | 66.4 |
| Video Policy | 300 | 66.0 |
| GR00T-N1.5 | 300 | 64.1 |
| π₀ | 300 | 62.5 |
| UWM | 1000 | 60.8 |
| GR00T-N1 + DUST | 300 | 58.5 |
| GR00T-N1 + DreamGen | 300+10000 합성 | 57.6 |
| DP-VLA | 3000 | 57.3 |
| UVA | 50 | 50.0 |
| GR00T-N1 | 300 | 49.6 |

### 실제 ALOHA 로봇 (101회 시행)
| 방법 | 평균 점수 |
|------|---------|
| Cosmos Policy | **93.6** |
| π₀.₅ | 88.6 |
| π₀ | 77.9 |
| OpenVLA-OFT+ | 62.0 |
| Diffusion Policy | 33.6 |

- 모델 기반 계획 후, 두 개의 도전적 작업("put candies in bowl" 및 "put candy in ziploc bag")의 평균 점수가 12.5% 포인트 향상되었다.
- 절제: 보조 손실 제거 시 1.5점 하락, 무작위 가중치에서 훈련 시 3.9점 하락, 처음부터 훈련한 변형은 떨림 운동이 나타나 평가가 중단되었다.
- 1회 노이즈 제거 단계 추론: 성공률 66.4% (5단계 67.1% 대비), 지연 시간 0.16초/행동 블록 (0.61초 대비), 약 4배 빠름.

## 경계 및 한계

- 모델 기반 계획의 추론 지연 시간이 상당하다(약 5초/행동 블록, H100 8개 병렬), 동적 작업과 이동 로봇 적용성을 제한한다.
- 효과적인 계획에는 많은 재생 데이터(648회)가 필요하며, 더 적은 재생으로 학습하는 것은 해결되지 않은 공개 문제이다.
- 대규모 행동 주석 로봇 데이터 사전 훈련을 수행하지 않았다(VLA 모델과 다름). 입력 히스토리를 사용하지 않고, t와 t+K 시점의 관측만 예측한다.
- RoboCasa에서 MimicGen으로 생성된 추가 시연을 사용하지 않았다(인간 시연 50개만 사용).
- 실제 상태는 완전히 관측 가능하지 않으며, 세계 모델이 실제로 예측하는 것은 미래 관측(고유 수용 감각과 카메라 이미지)이며, 논문은 기호 간결성을 위해 "상태"라는 용어를 사용한다.
- 논문에서 명시하지 않은 것: 구체적인 훈련 단계 수, 학습률, 배치 크기(부록 A.2 제외), GPU 모델 및 수량(계획 병렬화 제외), 추론 빈도의 구체적인 수치(ALOHA 25 Hz 제외).

## 엔지니어링 시사점

재현 시 먼저 **잠재 프레임 주입의 채우기 로직**을 확인해야 한다—각 양식은 [−1, +1]로 정규화하고 H′×W′×C′ 볼륨으로 복제 채워야 하며, 행동 블록 추출 시 모든 복사본의 평균을 계산한 후 역정규화해야 한다. 이 단계가 가장 오류가 발생하기 쉽고 행동 정확도에 직접 영향을 미친다. 둘째, **노이즈 분포 조정은 건너뛸 수 없다**: 기본 로그정규 분포는 고σ 영역에 대한 가중치가 부족하여 생성 초기 노이즈 제거 불량과 계단식 오류를 유발하므로, 반드시 0.7/0.3 혼합 샘플링을 사용하고 σ_min을 4로 설정해야 한다.

훈련 시 **데이터 배치 비율**에 주의하라: 50% 시연 + 50% 롤백(롤백에서 세계 모델과 가치 함수가 각각 절반), 롤백 데이터는 계획 능력에 매우 중요하며, 시연만으로 훈련된 세계 모델은 분포 외 일반화에서 명확히 실패한다. 배포 시 낮은 지연 시간을 추구한다면 1회 노이즈 제거 단계(0.16초/행동 블록)는 성공률 0.5%만 손실하므로 실제 로봇 제어에서 더 현실적인 선택이다. 최고 성공률을 추구한다면 0.61초(시뮬레이션) 또는 0.95초(ALOHA)의 추론 지연 시간을 수용해야 한다. 계획 모드는 GPU 8개 병렬이 필요하며 총 소요 시간이 약 5초이므로 비실시간 작업에만 적합하다.
