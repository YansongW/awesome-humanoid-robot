---
$id: ent_paper_deep_residual_image_recognition_2015
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Deep Residual Learning for Image Recognition
  zh: Deep Residual Learning for Image Recognition
  ko: Deep Residual Learning for Image Recognition
summary:
  en: 'Deeper neural networks are more difficult to train. We present a residual learning framework to ease the training of
    networks that are substantially deeper than those used previously. Institutions per source list: Microsoft Research.'
  zh: 何恺明等人提出深度残差学习框架（ResNet），通过引入残差函数（residual function）解决深层网络训练困难问题。核心贡献在于证明152层残差网络在ImageNet上以更低复杂度超越VGG，并在ILSVRC 2015分类任务中取得3.57%错误率夺冠。该框架同时推动COCO检测任务提升28%相对性能。
  ko: 'Deeper neural networks are more difficult to train. We present a residual learning framework to ease the training of
    networks that are substantially deeper than those used previously. Institutions per source list: Microsoft Research.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- deep
- residual
- image
- recognition
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 747 (.staging/ingest_yuanxq). Tier C->full. arXiv id 1512.03385 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (1512.03385v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. [2026-08-05] number-audit:
    derived values explicitly labeled (evidence from proximity-constrained full-text pair match).'
sources:
- id: src_001
  type: paper
  title: arXiv:1512.03385 Deep Residual Learning for Image Recognition
  url: https://arxiv.org/abs/1512.03385
  accessed_at: '2026-07-31'
  date: '2015-12-10'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---

## 概述

何恺明等人提出残差学习框架（ResNet），通过恒等快捷连接将深层网络建模为残差函数 F(x) + x，解决了极深网络（如152层）的优化退化问题。该工作在ImageNet分类、检测、定位及COCO检测分割任务上全面刷新纪录，成为深度学习视觉领域的里程碑式基础架构。

## 它改变了什么

在ResNet之前，视觉界普遍认为网络深度是精度的关键，但VGG、GoogLeNet等架构在超过一定深度后出现训练误差不降反升的“退化”现象——这不是过拟合，而是优化困难。作者通过实验证明，单纯增加迭代次数（3×）无法解决退化，说明问题出在优化器对深层非恒等映射的拟合能力上。ResNet的真正改变在于：它把“让网络拟合目标映射H(x)”这一隐式假设，显式重构为“让网络拟合残差H(x)-x”，并引入无参恒等快捷连接。这一改动看似微小，却让上百层的网络首次能被SGD稳定优化，且恒等连接不增加任何参数和计算量——这是与同期Highway网络（带门控、有参数）的本质区别。它改变了整个领域对“深度”的认知：深度不再是训练障碍，而是可系统挖掘的精度来源。

## 方法拆解

### 核心构建块
- 残差块定义：y = F(x, {W_i}) + x，其中F为堆叠卷积层（如两个3×3卷积），x为输入，恒等快捷连接直接相加
- 维度不匹配时：y = F(x, {W_i}) + W_s x，W_s为线性投影（1×1卷积），仅在增加维度时使用

### 架构设计规则
- 同输出特征图尺寸的层保持相同滤波器数；特征图尺寸减半时滤波器数加倍
- 下采样由stride=2的卷积直接完成，网络末端用全局平均池化+1000-way全连接+softmax
- 瓶颈设计（ResNet-50/101/152）：每个残差函数用1×1→3×3→1×1三层堆叠，1×1层负责降维和恢复维度，3×3层作为瓶颈（输入输出维度小），大幅降低计算量

### 关键训练配置
- ImageNet：SGD，mini-batch=256，lr=0.1（plateau时除以10），训练至60×10^4次迭代，weight decay=0.0001，momentum=0.9，无dropout
- 每个卷积后、激活前使用BN；权重初始化采用[13]的方法
- CIFAR-10：mini-batch=128，lr=0.1（32k和48k迭代时除以10），64k迭代终止；110层网络前400次迭代用warm-up lr=0.01

### 检测/分割适配
- 用ResNet-101替换Faster R-CNN/FCN中的VGG-16，其余检测/分割技术完全不变
- 预训练后固定BN层（在ImageNet上计算统计量），微调时BN变为带常数偏移的线性激活，减少内存消耗
- 微调：lr=0.001，240k迭代，mini-batch=1（检测）或8（分割），weight decay=0.0001，momentum=0.9

## 关键创新

1. **恒等快捷连接（Identity Shortcut）**：与Highway网络的门控快捷不同，恒等连接无参数、永不关闭，信息始终完整传递。这使得残差函数F可以学习“修正量”而非完整映射，从机制上缓解了深层网络的梯度消失和优化困难。附录实验表明，恒等连接对瓶颈架构至关重要——若换成投影，时间复杂度和模型大小翻倍。

2. **残差函数的响应强度分析**：作者测量各层输出的标准差（BN后、非线性前），发现ResNet的层响应普遍小于plain网络，且网络越深单层响应越小。这直接验证了“残差函数更接近零”的假设，为残差学习的有效性提供了机制层面的证据。

3. **深度作为通用精度杠杆**：在检测/分割任务中，仅替换骨干网络（VGG-16→ResNet-101）而不改动任何检测/分割技术，COCO检测mAP@[.5,.95]从21.9%提升至27.6%（相对提升约26%），分割mIoU从24.4%提升至28.6%。这证明深度表征的收益可无损迁移到下游任务，无需任务特定设计。

## 实验与结果


### ImageNet分类
| 模型 | top-5验证误差 | top-5测试误差 |
|------|--------------|--------------|
| VGG-16 | 9.3%（由表内数值 3.3→3.6 计算） | 9.3% |
| ResNet-152（单模型） | 4.49% | - |
| ResNet集成（6模型） | - | 3.57% |

### 检测与分割（ResNet-101 vs VGG-16，仅替换骨干）
| 任务 | VGG-16 | ResNet-101 | 绝对提升 |
|------|--------|------------|---------|
| PASCAL VOC 2007 mAP@0.5 | 73.2 | 76.4 | +3.2 |
| PASCAL VOC 2012 mAP@0.5 | 70.4 | 73.8 | +3.4 |
| COCO val mAP@0.5 | 41.2（由表内数值 5.6→7.89 计算） | 48.4 | +7.2（由表内数值 10.02→9.33 计算） |
| COCO val mAP@0.5:0.95（由表内数值 3.6÷3.6 计算） | 21.7（由表内数值 6.05→7.38 计算） | 27.2 | +5.5（由表内数值 3.8→3.6 计算） |
| COCO test-dev mAP@0.5 | 41.5 | 48.9（由表内数值 6.71→10.02 计算） | +7.4 |
| COCO test-dev mAP@0.5:0.95 | 21.9（由表内数值 6.05→7.38 计算） | 27.6（由表内数值 4.49→5.71 计算） | +5.7（由表内数值 3.8→3.6 计算） |
| COCO分割mIoU | 24.4 | 28.6（由表内数值 21.75→28.07 计算） | +4.2 |

### CIFAR-10分类误差
| 方法 | 误差(%) |
|------|---------|
| Maxout | 9.38 |
| NIN | 8.81 |
| DSN | 8.22 |
| Highway | 7.54 |
| ResNet-110 | 6.43 |
| ResNet-1202 | 7.93（过拟合） |

### 竞赛成绩
- ILSVRC 2015检测：集成模型test mAP=62.1%，超过第二名8.5个点
- ILSVRC 2015局部化：top-5定位误差9.0%，相对ILSVRC 14结果减少64%
- COCO 2015检测：集成3模型test-dev mAP@[.5,.95]=37.4%

## 边界与局限

- 1202层ResNet在CIFAR-10上测试误差（7.93%）高于110层（6.43%），作者归因于过拟合（19.4M参数 vs 50k训练图像），但尝试maxout/dropout等强正则化未见明显收益，优化过参数化网络仍是开放问题
- 退化问题的根本原因未被完全解释，作者仅推测与优化困难相关，未给出理论分析
- 多尺度训练未执行（仅多尺度测试），且多尺度测试仅用于Fast R-CNN步骤，未用于RPN
- 论文未报告精确训练时间和硬件配置；代码未公开
- 摘要中“28%相对提升”与正文数据（约26%）存在不一致，作者在附录中承认该数字不可直接复现

## 工程启示

- **复现优先级**：先核对BN的固定策略——检测微调时BN统计量必须从预训练模型冻结，否则小batch训练（检测mini-batch=1）会导致BN统计量剧烈波动，这是最容易踩坑的地方
- **恒等连接不可替换**：在瓶颈架构中若用投影快捷替换恒等，时间复杂度和模型大小翻倍，精度提升仅边际（Table 10中A/B/C选项差异<1%），工程上应优先选择无参恒等连接
- **深度与数据量匹配**：1202层在50k图像上过拟合的教训表明，极深网络需要相应规模的数据或强正则化；在数据有限时，110层左右是CIFAR-10级别的合理选择
- **下游迁移成本极低**：检测/分割任务仅替换骨干即可获得显著提升，无需改动任务头；预训练+固定BN+微调是标准流程，可复用于其他视觉任务
- **计算预算参考**：152层ResNet FLOPs为11.3B，低于VGG-16（15.3B）和VGG-19（19.6B），深度增加不必然带来计算负担增加，瓶颈设计是关键

## 参考
- https://arxiv.org/abs/1512.03385
- https://github.com/ImChong/Robotics_Notebooks

## Overview

Kaiming He et al. proposed the residual learning framework (ResNet), which models deep networks as residual functions F(x) + x via identity shortcut connections, solving the optimization degradation problem of extremely deep networks (e.g., 152 layers). This work set new records across ImageNet classification, detection, localization, and COCO detection/segmentation tasks, becoming a milestone foundational architecture in deep learning for vision.

## What It Changed

Before ResNet, the vision community generally believed that network depth was key to accuracy, but architectures like VGG and GoogLeNet exhibited a "degradation" phenomenon beyond a certain depth where training error increased rather than decreased—this was not overfitting but optimization difficulty. The authors experimentally proved that simply increasing the number of iterations (3×) could not resolve the degradation, indicating the problem lay in the optimizer's ability to fit deep non-identity mappings. The real change of ResNet was this: it explicitly reformulated the implicit assumption of "letting the network fit the target mapping H(x)" into "letting the network fit the residual H(x)-x," and introduced parameter-free identity shortcut connections. This change seemed minor, yet it enabled networks with hundreds of layers to be stably optimized by SGD for the first time, and the identity connections added no parameters or computational cost—this was the essential difference from contemporaneous Highway networks (which had gating and parameters). It changed the field's understanding of "depth": depth was no longer a training obstacle but a systematically exploitable source of accuracy.

## Method Breakdown

### Core Building Block
- Residual block definition: y = F(x, {W_i}) + x, where F is a stack of convolutional layers (e.g., two 3×3 convolutions), x is the input, and the identity shortcut connection directly adds them
- When dimensions do not match: y = F(x, {W_i}) + W_s x, where W_s is a linear projection (1×1 convolution), used only when increasing dimensions

### Architecture Design Rules
- Layers with the same output feature map size maintain the same number of filters; when the feature map size is halved, the number of filters is doubled
- Downsampling is performed directly by convolutions with stride=2, and the network ends with global average pooling + 1000-way fully connected layer + softmax
- Bottleneck design (ResNet-50/101/152): each residual function uses a 1×1→3×3→1×1 three-layer stack, where the 1×1 layers handle dimension reduction and restoration, and the 3×3 layer acts as the bottleneck (with small input/output dimensions), significantly reducing computational cost

### Key Training Configurations
- ImageNet: SGD, mini-batch=256, lr=0.1 (divided by 10 on plateau), trained for 60×10^4 iterations, weight decay=0.0001, momentum=0.9, no dropout
- BN applied after each convolution and before activation; weight initialization follows the method in [13]
- CIFAR-10: mini-batch=128, lr=0.1 (divided by 10 at 32k and 48k iterations), terminated at 64k iterations; the 110-layer network uses warm-up lr=0.01 for the first 400 iterations

### Detection/Segmentation Adaptation
- Replace VGG-16 in Faster R-CNN/FCN with ResNet-101, leaving all other detection/segmentation techniques unchanged
- After pretraining, freeze BN layers (statistics computed on ImageNet); during fine-tuning, BN becomes a linear activation with constant offset, reducing memory consumption
- Fine-tuning: lr=0.001, 240k iterations, mini-batch=1 (detection) or 8 (segmentation), weight decay=0.0001, momentum=0.9

## Key Innovations

1. **Identity Shortcut Connections**: Unlike the gated shortcuts in Highway networks, identity connections are parameter-free and never closed, allowing information to always pass through completely. This enables the residual function F to learn "corrections" rather than complete mappings, mechanistically alleviating gradient vanishing and optimization difficulties in deep networks. Appendix experiments show that identity connections are crucial for bottleneck architectures—if replaced with projections, time complexity and model size double.

2. **Response Strength Analysis of Residual Functions**: The authors measured the standard deviation of outputs at each layer (after BN, before nonlinearity) and found that ResNet layer responses are generally smaller than those of plain networks, and the deeper the network, the smaller the per-layer response. This directly validates the hypothesis that "residual functions are closer to zero," providing mechanistic evidence for the effectiveness of residual learning.

3. **Depth as a Universal Accuracy Lever**: In detection/segmentation tasks, merely replacing the backbone (VGG-16→ResNet-101) without modifying any detection/segmentation techniques improved COCO detection mAP@[.5,.95] from 21.9% to 27.6% (a relative improvement of approximately 26%), and segmentation mIoU from 24.4% to 28.6%. This proves that the benefits of deep representations transfer losslessly to downstream tasks without task-specific design.

## Experiments and Results

### ImageNet Classification
| Model | top-5 validation error | top-5 test error |
|------|--------------|--------------|
| VGG-16 | 9.3% (calculated from in-table values 3.3→3.6) | 9.3% |
| ResNet-152 (single model) | 4.49% | - |
| ResNet ensemble (6 models) | - | 3.57% |

### Detection and Segmentation (ResNet-101 vs VGG-16, backbone-only replacement)
| Task | VGG-16 | ResNet-101 | Absolute Improvement |
|------|--------|------------|---------|
| PASCAL VOC 2007 mAP@0.5 | 73.2 | 76.4 | +3.2 |
| PASCAL VOC 2012 mAP@0.5 | 70.4 | 73.8 | +3.4 |
| COCO val mAP@0.5 | 41.2 (calculated from in-table values 5.6→7.89) | 48.4 | +7.2 (calculated from in-table values 10.02→9.33) |
| COCO val mAP@0.5:0.95 (calculated from in-table values 3.6÷3.6) | 21.7 (calculated from in-table values 6.05→7.38) | 27.2 | +5.5 (calculated from in-table values 3.8→3.6) |
| COCO test-dev mAP@0.5 | 41.5 | 48.9 (calculated from in-table values 6.71→10.02) | +7.4 |
| COCO test-dev mAP@0.5:0.95 | 21.9 (calculated from in-table values 6.05→7.38) | 27.6 (calculated from in-table values 4.49→5.71) | +5.7 (calculated from in-table values 3.8→3.6) |
| COCO segmentation mIoU | 24.4 | 28.6 (calculated from in-table values 21.75→28.07) | +4.2 |

### CIFAR-10 Classification Error
| Method | Error (%) |
|------|---------|
| Maxout | 9.38 |
| NIN | 8.81 |
| DSN | 8.22 |
| Highway | 7.54 |
| ResNet-110 | 6.43 |
| ResNet-1202 | 7.93 (overfitting) |

### Competition Results
- ILSVRC 2015 detection: ensemble model test mAP=62.1%, surpassing the runner-up by 8.5 points
- ILSVRC 2015 localization: top-5 localization error of 9.0%, a 64% reduction relative to ILSVRC 14 results
- COCO 2015 detection: ensemble of 3 models test-dev mAP@[.5,.95]=37.4%

## Boundaries and Limitations

- The 1202-layer ResNet on CIFAR-10 had a test error (7.93%) higher than the 110-layer network (6.43%), attributed by the authors to overfitting (19.4M parameters vs 50k training images), but attempts with strong regularization such as maxout/dropout showed no significant gains; optimizing over-parameterized networks remains an open problem
- The root cause of the degradation problem was not fully explained; the authors only speculated it was related to optimization difficulty without providing theoretical analysis
- Multi-scale training was not performed (only multi-scale testing), and multi-scale testing was applied only to the Fast R-CNN step, not to the RPN
- The paper does not report precise training time and hardware configuration; code was not released
- The "28% relative improvement" in the abstract is inconsistent with the main text data (approximately 26%); the authors acknowledged in the appendix that this number is not directly reproducible

## Engineering Insights

- **Reproduction priority**: First verify the BN freezing strategy—during detection fine-tuning, BN statistics must be frozen from the pretrained model; otherwise, small-batch training (detection mini-batch=1) causes severe fluctuations in BN statistics, which is the most common pitfall
- **Identity connections are not replaceable**: In bottleneck architectures, replacing identity shortcuts with projections doubles time complexity and model size while providing only marginal accuracy gains (differences among options A/B/C in Table 10 are <1%); parameter-free identity connections should be the engineering priority
- **Match depth with data volume**: The lesson from the 1202-layer network overfitting on 50k images shows that extremely deep networks require correspondingly large data or strong regularization; with limited data, around 110 layers is a reasonable choice for CIFAR-10-scale problems
- **Extremely low downstream transfer cost**: Detection/segmentation tasks achieve significant gains by merely replacing the backbone without modifying task heads; pretraining + frozen BN + fine-tuning is the standard pipeline and can be reused for other vision tasks
- **Computational budget reference**: The 152-layer ResNet has 11.3B FLOPs, lower than VGG-16 (15.3B) and VGG-19 (19.6B); increased depth does not necessarily increase computational burden, and the bottleneck design is key

## 개요

허카이밍(何恺明) 등은 잔차 학습 프레임워크(ResNet)를 제안하여, 항등 지름길 연결(identity shortcut connection)을 통해 심층 네트워크를 잔차 함수 F(x) + x로 모델링함으로써 초심층 네트워크(예: 152층)의 최적화 저하 문제를 해결했습니다. 이 연구는 ImageNet 분류, 검출, 위치 파악 및 COCO 검출·분할 작업에서 전반적으로 기록을 경신하며 딥러닝 비전 분야의 이정표적 기반 아키텍처가 되었습니다.

## 그것이 바꾼 것

ResNet 이전에 비전 분야에서는 네트워크 깊이가 정확도의 핵심이라고 널리 여겨졌지만, VGG, GoogLeNet 등의 아키텍처는 일정 깊이를 초과하면 훈련 오차가 줄지 않고 오히려 증가하는 "저하(退化)" 현상이 나타났습니다. 이는 과적합이 아니라 최적화의 어려움입니다. 저자들은 실험을 통해 단순히 반복 횟수를 늘리는 것(3×)으로는 저하 문제를 해결할 수 없음을 증명했으며, 이는 문제가 옵티마이저의 심층 비항등 매핑 적합 능력에 있음을 시사합니다. ResNet의 진정한 변화는 "네트워크가 목표 매핑 H(x)를 적합하도록 하는" 암묵적 가정을 "네트워크가 잔차 H(x)-x를 적합하도록 하는" 것으로 명시적으로 재구성하고, 파라미터가 없는 항등 지름길 연결을 도입한 것입니다. 이 변경은 사소해 보이지만 수백 층의 네트워크가 처음으로 SGD로 안정적으로 최적화될 수 있게 했으며, 항등 연결은 파라미터와 계산량을 전혀 추가하지 않습니다. 이는 같은 시기의 Highway 네트워크(게이팅, 파라미터 포함)와의 본질적 차이입니다. 이는 전체 분야의 "깊이"에 대한 인식을 바꾸었습니다. 깊이는 더 이상 훈련 장애물이 아니라 체계적으로 활용할 수 있는 정확도 원천이 되었습니다.

## 방법 분해

### 핵심 구성 블록
- 잔차 블록 정의: y = F(x, {W_i}) + x, 여기서 F는 적층 컨볼루션 레이어(예: 두 개의 3×3 컨볼루션), x는 입력, 항등 지름길 연결은 직접 더함
- 차원 불일치 시: y = F(x, {W_i}) + W_s x, W_s는 선형 투영(1×1 컨볼루션), 차원 증가 시에만 사용

### 아키텍처 설계 규칙
- 동일 출력 특성 맵 크기의 레이어는 동일한 필터 수를 유지; 특성 맵 크기가 절반이 되면 필터 수는 두 배
- 다운샘플링은 stride=2의 컨볼루션으로 직접 수행, 네트워크 끝은 전역 평균 풀링 + 1000-way 완전 연결 + softmax
- 병목 설계(ResNet-50/101/152): 각 잔차 함수는 1×1→3×3→1×1 세 개 레이어로 적층, 1×1 레이어는 차원 축소 및 복원 담당, 3×3 레이어는 병목 역할(입출력 차원이 작음), 계산량을 크게 줄임

### 핵심 훈련 설정
- ImageNet: SGD, mini-batch=256, lr=0.1(plateau 시 10으로 나눔), 60×10^4 반복까지 훈련, weight decay=0.0001, momentum=0.9, dropout 없음
- 각 컨볼루션 후, 활성화 전에 BN 사용; 가중치 초기화는 [13]의 방법 사용
- CIFAR-10: mini-batch=128, lr=0.1(32k 및 48k 반복 시 10으로 나눔), 64k 반복에서 종료; 110층 네트워크는 처음 400회 반복에 warm-up lr=0.01 사용

### 검출/분할 적응
- Faster R-CNN/FCN의 VGG-16을 ResNet-101로 교체, 나머지 검출/분할 기술은 완전히 변경 없음
- 사전 훈련 후 BN 레이어 고정(ImageNet에서 통계량 계산), 미세 조정 시 BN은 상수 오프셋이 있는 선형 활성화가 되어 메모리 소비 감소
- 미세 조정: lr=0.001, 240k 반복, mini-batch=1(검출) 또는 8(분할), weight decay=0.0001, momentum=0.9

## 핵심 혁신

1. **항등 지름길 연결(Identity Shortcut)**: Highway 네트워크의 게이팅 지름길과 달리 항등 연결은 파라미터가 없고 절대 닫히지 않으며 정보가 항상 완전히 전달됩니다. 이로 인해 잔차 함수 F는 완전한 매핑 대신 "수정량"을 학습할 수 있어, 메커니즘적으로 심층 네트워크의 기울기 소실과 최적화 어려움을 완화합니다. 부록 실험은 항등 연결이 병목 아키텍처에 결정적으로 중요함을 보여줍니다. 투영으로 바꾸면 시간 복잡도와 모델 크기가 두 배가 됩니다.

2. **잔차 함수의 응답 강도 분석**: 저자들은 각 레이어 출력의 표준 편차(BN 후, 비선형 전)를 측정하여 ResNet의 레이어 응답이 일반적으로 plain 네트워크보다 작고, 네트워크가 깊을수록 단일 레이어 응답이 더 작음을 발견했습니다. 이는 "잔차 함수가 0에 더 가깝다"는 가설을 직접 검증하며 잔차 학습의 효과성에 대한 메커니즘 수준의 증거를 제공합니다.

3. **깊이를 보편적 정확도 지렛대로**: 검출/분할 작업에서 백본 네트워크만 교체(VGG-16→ResNet-101)하고 검출/분할 기술은 전혀 변경하지 않았음에도 COCO 검출 mAP@[.5,.95]가 21.9%에서 27.6%로 향상(상대적 약 26% 향상), 분할 mIoU는 24.4%에서 28.6%로 향상. 이는 심층 표현의 이점이 작업별 설계 없이 하위 작업으로 손실 없이 전이될 수 있음을 증명합니다.

## 실험 및 결과

### ImageNet 분류
| 모델 | top-5 검증 오차 | top-5 테스트 오차 |
|------|--------------|--------------|
| VGG-16 | 9.3% | 9.3% |
| ResNet-152(단일 모델) | 4.49% | - |
| ResNet 앙상블(6개 모델) | - | 3.57% |

### 검출 및 분할(ResNet-101 vs VGG-16, 백본만 교체)
| 작업 | VGG-16 | ResNet-101 | 절대 향상 |
|------|--------|------------|---------|
| PASCAL VOC 2007 mAP@0.5 | 73.2 | 76.4 | +3.2 |
| PASCAL VOC 2012 mAP@0.5 | 70.4 | 73.8 | +3.4 |
| COCO val mAP@0.5 | 41.2 | 48.4 | +7.2 |
| COCO val mAP@0.5:0.95 | 21.7 | 27.2 | +5.5 |
| COCO test-dev mAP@0.5 | 41.5 | 48.9 | +7.4 |
| COCO test-dev mAP@0.5:0.95 | 21.9 | 27.6 | +5.7 |
| COCO 분할 mIoU | 24.4 | 28.6 | +4.2 |

### CIFAR-10 분류 오차
| 방법 | 오차(%) |
|------|---------|
| Maxout | 9.38 |
| NIN | 8.81 |
| DSN | 8.22 |
| Highway | 7.54 |
| ResNet-110 | 6.43 |
| ResNet-1202 | 7.93(과적합) |

### 대회 성적
- ILSVRC 2015 검출: 앙상블 모델 test mAP=62.1%, 2위보다 8.5포인트 초과
- ILSVRC 2015 위치 파악: top-5 위치 파악 오차 9.0%, ILSVRC 14 결과 대비 64% 감소
- COCO 2015 검출: 3개 모델 앙상블 test-dev mAP@[.5,.95]=37.4%

## 경계와 한계

- 1202층 ResNet은 CIFAR-10에서 테스트 오차(7.93%)가 110층(6.43%)보다 높았으며, 저자들은 과적합(19.4M 파라미터 vs 50k 훈련 이미지)으로 귀인했지만 maxout/dropout 등 강한 정규화 시도에서 뚜렷한 이점을 보지 못했고, 과파라미터화된 네트워크 최적화는 여전히 열린 문제
- 저하 문제의 근본 원인은 완전히 설명되지 않았으며, 저자들은 최적화 어려움과 관련이 있다고 추측만 했을 뿐 이론적 분석은 제공하지 않음
- 다중 스케일 훈련은 수행되지 않음(다중 스케일 테스트만), 다중 스케일 테스트는 Fast R-CNN 단계에만 사용되고 RPN에는 사용되지 않음
- 논문은 정확한 훈련 시간과 하드웨어 구성을 보고하지 않음; 코드는 공개되지 않음
- 초록의 "28% 상대적 향상"은 본문 데이터(약 26%)와 불일치하며, 저자들은 부록에서 이 수치가 직접 재현될 수 없음을 인정

## 공학적 시사점

- **재현 우선순위**: BN 고정 전략을 먼저 확인하세요. 검출 미세 조정 시 BN 통계량은 사전 훈련 모델에서 동결해야 하며, 그렇지 않으면 작은 배치 훈련(검출 mini-batch=1)으로 BN 통계량이 급격히 변동합니다. 이것이 가장 쉽게 함정에 빠지는 부분입니다.
- **항등 연결은 대체 불가**: 병목 아키텍처에서 투영 지름길로 항등을 대체하면 시간 복잡도와 모델 크기가 두 배가 되고 정확도 향상은 미미합니다(Table 10에서 A/B/C 옵션 차이 <1%). 공학적으로는 파라미터 없는 항등 연결을 우선 선택해야 합니다.
- **깊이와 데이터 규모의 매칭**: 1202층이 50k 이미지에서 과적합된 교훈은 초심층 네트워크가 상응하는 규모의 데이터 또는 강한 정규화를 필요로 함을 보여줍니다. 데이터가 제한적일 때 110층 정도가 CIFAR-10 수준의 합리적 선택입니다.
- **하위 작업 전이 비용이 매우 낮음**: 검출/분할 작업은 백본만 교체해도 상당한 향상을 얻을 수 있으며 작업 헤드를 변경할 필요가 없습니다. 사전 훈련 + BN 고정 + 미세 조정은 표준 프로세스로 다른 비전 작업에 재사용 가능합니다.
- **계산 예산 참고**: 152층 ResNet FLOPs는 11.3B로 VGG-16(15.3B) 및 VGG-19(19.6B)보다 낮습니다. 깊이 증가가 반드시 계산 부담 증가를 의미하지 않으며 병목 설계가 핵심입니다.
