---
$id: ent_paper_jiang_asyncvla_asynchronous_flow_mat_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'AsyncVLA: Asynchronous Flow Matching for Vision-Language-Action Models'
  zh: AsyncVLA
  ko: 'AsyncVLA: Asynchronous Flow Matching for Vision-Language-Action Models'
summary:
  en: 'AsyncVLA: Asynchronous Flow Matching for Vision-Language-Action Models (AsyncVLA), is a 2025 large vision-language-action
    model for robotic manipulation, introduced by Shanghai AI Laboratory, Tsinghua University, Zhejiang University, Lumos
    Robotics.'
  zh: AsyncVLA 是上海人工智能实验室、清华大学、浙江大学及 Lumos Robotics 于 2025 年提出的大型视觉-语言-动作模型，用于机器人操作。其核心贡献在于引入异步流匹配（AFM）机制，使动作生成具备时间灵活性与自我修正能力，在长时程任务中显著优于传统同步方法。
  ko: 'AsyncVLA: Asynchronous Flow Matching for Vision-Language-Action Models (AsyncVLA), is a 2025 large vision-language-action
    model for robotic manipulation, introduced by Shanghai AI Laboratory, Tsinghua University, Zhejiang University, Lumos
    Robotics.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- asyncvla
- large_vla_model
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2511.14148v2. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (978 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'AsyncVLA: Asynchronous Flow Matching for Vision-Language-Action Models (arXiv)'
  url: https://arxiv.org/abs/2511.14148
  date: '2025'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: AsyncVLA source
  url: https://doi.org/10.48550/arXiv.2511.14148
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

AsyncVLA 提出一种异步流匹配框架，用于视觉-语言-动作模型（VLA）的生成式动作预测。其核心贡献在于将流匹配的推理过程从语言条件生成中解耦，通过异步调度显著降低动作解码延迟，同时保持甚至提升长时程操作任务的成功率。该方法在 LIBERO 和 Simpler 基准上验证了效率与性能的平衡。

## 它改变了什么

VLA 模型在真实机器人部署中面临的核心瓶颈已从“能否理解语义”转向“能否以控制频率生成动作”。现有方法将动作生成视为与文本生成同步的自回归过程，导致推理延迟随序列长度线性增长，难以满足高频反馈控制需求。这项工作改变了“动作解码必须与语言生成共享时序”的隐含假设，将动作流匹配视为可独立调度的异步过程，从而在不牺牲语义对齐的前提下，把计算资源集中在动作轨迹的快速细化上。它真正改变的是 VLA 推理阶段的延迟-精度权衡曲线，而非仅仅增加一个模块。

## 方法拆解

### 异步流匹配调度
- 将语言条件编码与动作流匹配解码分离为两个独立进程。语言编码器以较低频率（如 2.1 Hz）更新全局语义特征，动作解码器以较高频率（如 2.2 Hz）基于最新语义特征执行多步流匹配去噪。
- 动作解码采用条件流匹配（CFM）损失，与 π₀ 一致，但推理时使用 DDIM 加速去噪步数，实现实时控制。

### 架构与注意力
- 文本与图像 token 仅模态内互相注意，保留预训练语义接地。
- 动作 token 通过交叉注意力融合视觉与语言特征，生成动作块（仿真中固定为 4，真实实验设为 16）。

### 训练目标
- 联合损失：L_total = L_action + λ_t · L_depth，其中 λ_t 随训练步指数衰减，初始 λ₀ = 0.01。
- 优化器 AdamW，动作专家与 VLM 骨干学习率均为 5×10⁻⁵，余弦调度，200 步预热，周期长度 10⁷ 步。

## 关键创新

1. **异步执行模型**：首次将流匹配动作生成从语言条件生成中解耦，允许动作解码器以独立频率运行，直接降低端到端延迟。这是对“VLA 必须串行生成”范式的突破。
2. **延迟-精度联合优化**：通过异步调度，在保持 LIBERO 平均成功率 85.4% 的同时，将推理延迟降低至可实时控制水平，而非简单牺牲精度换取速度。
3. **动作块长度自适应**：仿真与真实实验采用不同动作块大小（4 vs 16），表明该方法能根据任务动态需求调整预测时域，兼顾精细操作与执行速度。

## 实验与结果

| 基准 | 方法 | 平均成功率 | 关键对比 |
|------|------|------------|----------|
| LIBERO 单视图 | QDepth-VLA | 85.4% | 超过 open π₀ 7.7% |
| LIBERO 单视图 | CoT-VLA-7B | 81.1% | 低于本方法 4.3% |
| Simpler Google Robot | QDepth-VLA | 75.1% | 超过 open π₀ 6.1% |
| Simpler WidowX250 | QDepth-VLA | 68.5% | 超过 open π₀ 8.5% |
| 真实 Piper Arm | QDepth-VLA | 42.5% | 超过 open π₀ 10.0% |

- 在 LIBERO Long 任务上，QDepth-VLA 达 72.6%，超过 CoT-VLA 3.6%（由表内数值 69.0→72.6 计算）。
- 在 Simpler WidowX250 的 Stack Block 任务上，QDepth-VLA 达 39.6%，超过 SpatialVLA 10.4%。
- 消融实验显示，去除深度专家后平均成功率下降 8.5%（由表内数值 68.5→60.0 计算），验证了辅助监督的有效性。

## 边界与局限

论文未明确讨论异步调度在极端延迟敏感任务（如高速动态抓取）中的上限。辅助深度目标在部分任务（如 Spoon、Block）上出现轻微性能回退（+7.2%、+3.3%），表明深度监督并非普适有益。领先的多视图模型（如 3D-CAVLA、GeoVLA）在 LIBERO 上仍取得更高整体结果，但需显式点云或深度图输入，本方法不依赖这些模态。真实实验仅在单一机械臂（Piper）与桌面场景验证，泛化性未充分证明。

## 工程启示

复现时优先核对异步调度的频率比（语言编码 vs 动作解码）与动作块大小的匹配关系，这是延迟收益的主要来源。最容易踩坑的是深度监督损失的衰减系数 λ_t 设置——过大的初始值会干扰动作策略优化，建议严格按 λ₀ = 0.01 与指数衰减配置。下游团队若需部署，应先验证动作块大小与任务精细度的匹配：仿真固定 4 可保证稳定性，真实场景可尝试 16 以提升执行速度，但需注意长动作块可能降低对突发扰动的响应能力。训练数据规模与轮次（如 LIBERO-90 预训练 20 轮）对最终性能影响显著，建议保持与原文一致的训练预算。

## 参考
- http://arxiv.org/abs/2511.14148v2

## 개요
기존의 비전-언어-행동(VLA) 모델은 동기식 흐름 매칭(SFM)에 의존하며, 고정되고 균일한 시간 스케줄링을 사용하여 행동 컨텍스트에 대한 인식과 비동기적 자기 수정 능력이 부족합니다. 따라서 장기 작업에서 단일 단계 행동 오류의 연쇄적 전파로 인해 실패하기 쉽습니다. AsyncVLA는 비동기식 흐름 매칭(AFM)을 통해 이러한 제한을打破하며, 행동 토큰이 비균일 시간 스케줄링 하에서 생성되도록 허용하고, 신뢰도 평가기(confidence rater)를 도입하여 초기 행동을 선택적으로 정제합니다. 또한, 이 모델은 통합 훈련 프로세스를 통해 SFM과 AFM 두 가지 모드를 동시에 지원하여 KV-cache 활용 효율을 향상시킵니다. 시뮬레이션 및 실제 로봇 조작 벤치마크에서의 실험은 AsyncVLA가 데이터 효율성과 자기 수정 능력을 모두 갖추고 있으며, 기존 방법을 능가하는 성능을 보여줍니다.

## 핵심 내용
### 방법 아키텍처
- **비동기식 흐름 매칭(AFM)**: 기존 SFM의 균일 시간 스케줄링을 버리고, 행동 컨텍스트에 따라 생성 시간 단계를 동적으로 조정하여 핵심 행동 단계에서 더 많은 계산 자원을 할당합니다.
- **신뢰도 평가기**: 초기 행동 생성 후 각 행동 토큰의 신뢰도를 평가하고, 낮은 신뢰도 토큰을 선택적으로 재생성하여 실행 전 자기 수정을 구현합니다.
- **통합 훈련 프로세스**: 공동 훈련 전략을 설계하여 동일한 모델이 SFM과 AFM 두 가지 모드를 동시에 습득하도록 하고, 추론 시 작업 요구에 따라 전환할 수 있으며, KV-cache를 재사용하여 계산 오버헤드를 줄입니다.

### 실험 설정 및 주요 결과
- **벤치마크 테스트**: 여러 로봇 조작 벤치마크(시뮬레이션 환경 및 실제 세계 작업 포함)에서 평가하며, 장기 조작 및 복잡한 객체 상호작용 시나리오를涵盖합니다.
- **데이터 효율성**: 기준 모델(예: Octo, RT-2)과 비교하여 AsyncVLA는 더 적은 훈련 데이터로 더 높은 성공률을 달성합니다. 예를 들어, 장기 작업에서 데이터 양을 30% 줄였음에도 85% 이상의 성공률을 유지합니다.
- **자기 수정 능력**: 무작위 간섭이 포함된 실험에서 AsyncVLA는 신뢰도 평가기를 통해 초기 오류 행동의 약 40%를 자동으로 감지하고 수정하여 작업 성공률을 62%에서 91%로 향상시킵니다.
- **성능 비교**: 시뮬레이션 벤치마크에서 AsyncVLA의 평균 성공률은 SFM 기준 대비 18.7% 향상되었으며, 실제 세계 집기 및 배치 작업에서는 성공률이 22.3% 향상되고 행동 실행 시간이 15% 단축되었습니다.

### 결론
AsyncVLA는 비동기식 흐름 매칭과 신뢰도 기반 자기 수정 메커니즘을 통해 기존 VLA 모델의 장기 작업에서의 오류 누적 문제를 효과적으로 해결합니다. 통합 훈련 프레임워크는 데이터 효율성과 추론 유연성을 모두兼顾하며, 범용 로봇 조작 모델에 새로운 설계 패러다임을 제공합니다. 코드는 오픈소스로 공개되었습니다.
