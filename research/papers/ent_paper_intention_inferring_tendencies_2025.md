---
$id: ent_paper_intention_inferring_tendencies_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM'
  zh: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM'
  ko: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM'
summary:
  en: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM is a 2025 work
    on navigation for humanoid robots.'
  zh: INTENTION 是 2025 年提出的人形机器人导航框架，由研究团队通过融合视觉语言模型（VLM）场景推理与交互驱动记忆实现。其核心贡献在于引入 Memory Graph 记录任务交互场景，并设计 Intuitive Perceptor
    提取物理关系与可供性，使机器人无需重复指令即可在新场景中推断合适的交互行为。
  ko: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM is a 2025 work
    on navigation for humanoid robots.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- humanoid
- intention
- navigation
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2508.04931v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (930 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'INTENTION: Inferring Tendencies of Humanoid Robot Motion Through Interactive Intuition and Grounded VLM (arXiv)'
  url: https://arxiv.org/abs/2508.04931
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

INTENTION 是一个面向 37 自由度 centaur 类人形机器人的操作框架，由作者团队提出，核心是用预训练 VLM（o4-mini）做场景感知，配合图结构记忆库 MemoGraph 存储交互经验，在推理时通过图匹配检索相似任务并生成动作。它试图弥合高层语言指令与低层全身运动控制之间的鸿沟，让机器人具备无需重复指令的交互直觉。

## 它改变了什么

传统 TAMP 和手工设计的行为树在结构化环境里够用，但一碰到非结构化场景就崩——建模不准、泛化差。无模型 RL 虽然鲁棒，但任务一变就得重新训练，对高自由度人形机器人来说成本高得离谱。VLA 方法理论上漂亮，可需要海量高质量数据，人形机器人的数据恰恰最难获取。更根本的问题是，现有 VLM 只做静态图像理解，不建模物体属性、交互动力学和关系，导致它在新任务面前就是个“睁眼瞎”。

INTENTION 真正改变的不是某个算法模块，而是把“经验”这个概念变成了可检索、可匹配的图结构。它不再要求模型从零推理每个新场景，而是让机器人像人一样“见过类似场面就知道怎么动”。这个思路绕开了数据瓶颈，也绕开了从头训练，直接利用预训练 VLM 的泛化能力加上记忆库的积累效应。对工程实践来说，这意味着不用再为每个新任务重新设计控制器或收集百万级数据集。

## 方法拆解

框架分四个部分，但核心逻辑是一条流水线：感知 → 记忆 → 匹配 → 执行。

### Intuitive Perceptor
- 输入 RGB 图像 + 人类指令，用预训练 o4-mini 通过 JSON 格式的函数调用提取物体状态、空间几何和语义关系。
- 输出图结构：节点 = 物体或代理，链接 = 空间或语义关系，指令以文本编码挂在图上。

### MemoGraph
- 图结构记忆库，每次交互存一个任务图 g_n = (N, L, I)，包含语义指令、场景状态、几何关系、机器人动作 a_n 和完成评估 C_n。
- 学习阶段：机器人执行一系列交互片段，IP 提取场景状态 T_n，VLM 规划器 V_p 基于指令 I_n、场景 S_n 和 T_n 从预定义技能库 Π 选动作 a_n，执行后计算 C_n，全部编码存入 MemoGraph。

### 推理阶段匹配
- 新场景 S_t 经 IP 提取 T_t，编码为当前场景图 G_t。
- 与 MemoGraph 中所有存储图做图匹配，核心是三个相似度：
  - 指令相似度 S_I：CLIP 编码器 E(·) 提取嵌入，余弦相似度（公式 1）。
  - 节点和链接相似度：嵌入矩阵点积、阈值化 thr(·)、二分匹配 F(·)（公式 2 和 3）。
  - 总匹配分数 S_W = α·S_N + β·S_L + γ·S_I（公式 4），α + β + γ = 1。
- 所有分数由语言模型评估器分析合理性，检索对应动作并生成控制代码。

### 动作库
- 预定义运动原语，每个原语由直接控制执行的代码接口和动作语义描述组成。
- 基于笛卡尔 I/O，无额外训练，Xbot2 实现底层实时通信。

关键设计决策：用 CLIP 做文本编码器而不是学一个专用编码器，省了训练成本；图匹配用二分匹配而不是简单最近邻，能处理节点数不一致的情况；评估器用 LLM 而不是硬编码规则，保留了对模糊场景的语义判断能力。

## 关键创新

1. **图结构记忆库 MemoGraph**：不是存原始轨迹或动作序列，而是存“场景-指令-动作-反馈”的结构化图。这让检索变成图匹配问题，而不是向量检索或序列比对，能更精确地捕捉物体间空间关系和语义关联。新任务不需要重新训练，只需要在记忆库里找到最相似的图。

2. **交互直觉任务的处理**：传统方法在无显式指令的任务上直接失效（WB-MPC 和 BT-Planner 直接 TLE），LLM-BT 也只有 15% 成功率。INTENTION 通过记忆库积累的物理交互经验，让机器人能“猜”出该做什么，这在人形机器人操作里是稀缺能力。

3. **VLM 与记忆库的协同**：VLM 负责感知和评估，记忆库负责经验存储，两者通过图结构衔接。VLM 的泛化能力弥补了记忆库覆盖不全的问题，记忆库则弥补了 VLM 缺乏物理交互经验的短板。这个分工比单纯用 VLM 做端到端控制更务实。

## 实验与结果

实验在 centaur 类人形机器人（37 自由度，四腿带轮，右臂爪式夹爪，RealSense 深度相机）上完成，对比 WB-MPC、BT-Planner、LLM-BT 和 INTENTION。任务分两类：标准操作（Pick and Place）和交互直觉（无显式指令，仅靠物理推理）。

| 方法 | 操作任务 Plan | 操作任务 Succ | 操作任务 Avg. Time | 直觉任务 Plan | 直觉任务 Succ | 直觉任务 Avg. Time |
|------|--------------|--------------|-------------------|--------------|--------------|-------------------|
| WB-MPC | 84% | 72% | 35.86s ± 11.5 | – | – | TLE |
| BT-Planner | 92% | 80% | 27.40s ± 4.6 | – | – | TLE |
| LLM-BT | 98% | 94% | 22.94s ± 3.2 | 20% | 15% | 20.65s ± 8.7 |
| INTENTION | 96% | 92% | 23.75s ± 4.3 | 84% | 72% | 24.86s ± 2.7 |

关键结果解读：
- 操作任务上 INTENTION 与 LLM-BT 基本持平（96% vs 98% 规划率，92% vs 94% 成功率），说明记忆库检索没有牺牲标准任务性能。
- 交互直觉任务上差距悬殊：LLM-BT 只有 15% 成功率，INTENTION 达到 72%。这验证了记忆库对物理交互经验的价值——LLM 生成行为树在无指令场景下基本靠猜。
- 四个操作任务（Handling Object、Lift Desk、Push Chair、Refill Tea）各 25 次试验，INTENTION 在无语义指令输入（Intuitive Mode）时仍保持 ≥ 65% 成功率。
- MemoGraph 存储图数量增加时，场景提取和任务规划成功率随之提高（Fig.6，具体数字未提取）。

## 边界与局限

作者明确承认的局限：MemoGraph 尚未扩展到更广泛任务领域；跨机器人平台的运动泛化能力未验证；高度动态环境下的记忆检索和动作推理效率未优化。论文未提及对 VLM 输出不确定性的处理，也没有失败模式的详细分析——比如图匹配错误时会发生什么、记忆库污染如何避免、检索到错误记忆时有没有回退机制。交互直觉任务的成功率 72% 意味着仍有近三成场景失败，但论文未说明失败原因分布。另外，动作库是手动设计的，这意味着“直觉”的上限受限于预定义原语的质量，如果动作库覆盖不全，记忆库再丰富也没用。

## 工程启示

复现 INTENTION 时，先核对几个关键点：

1. **动作库设计是瓶颈**：所有动作原语手动设计，基于笛卡尔 I/O，无额外训练。这意味着你的机器人运动学模型必须和 centaur 足够接近，否则原语直接失效。建议先验证动作库在目标平台上的覆盖率，再谈记忆库效果。

2. **图匹配的权重系数 α、β、γ 需要调**：公式 4 中三个权重之和为 1，但论文未给出具体值。不同任务类型对指令、节点、链接的敏感度不同，建议按任务类别分别调参，不要用一组权重打天下。

3. **CLIP 做文本编码器的坑**：CLIP 对短指令效果好，但对长指令或专业术语可能嵌入不准确。如果下游任务指令复杂，考虑换更强的文本编码器或微调。

4. **记忆库规模与检索效率的权衡**：Fig.6 显示成功率随存储图数量增加而提高，但论文未提检索延迟。图匹配是计算密集操作，记忆库大了之后实时性可能出问题，工程上需要做索引或剪枝。

5. **最容易踩的坑是评估器**：语言模型评估器负责过滤匹配结果，但 LLM 的输出不确定性没有处理机制。建议加一个置信度阈值，低于阈值的场景直接拒绝执行，而不是硬跑。

6. **数据收集成本**：MemoGraph 的数据来自离线实验，每个任务 25 次试验，四个任务共 100 次。这个量级不算大，但每次试验都需要手动评估完成情况（C_n），自动化标注是后续扩展的关键。

## 参考
- http://arxiv.org/abs/2508.04931v1

## 개요
전통적인 로봇 조작은 정밀한 물리 모델과 사전 정의된 동작 시퀀스에 의존하며, 구조화된 환경에서는 효과적이지만 실제 현장에 일반화하기 어렵습니다. INTENTION 프레임워크는 VLM의 장면 이해와 상호작용 기억을 결합하여 로봇에게 인간과 유사한 직관적 상호작용 능력을 부여합니다. 이 프레임워크는 두 가지 핵심 구성 요소를 포함합니다: Memory Graph는 과거 작업 상호작용에서 얻은 장면 정보를 저장하여 실제 세계 작업에 대한 이해와 의사 결정을 반영하며, Intuitive Perceptor는 시각적 장면에서 물리적 관계와 행동 가능성(affordance)을 추출합니다. 이 둘은 협력하여 로봇이 새로운 장면에서 반복적인 지시 없이도 상호작용 경향을 자율적으로 추론할 수 있게 합니다.

## 핵심 내용
### 방법 아키텍처
INTENTION 프레임워크는 두 가지 핵심 모듈로 구성됩니다:
- **Memory Graph**: 로봇이 과거 작업 상호작용에서 얻은 장면 정보를 기록하는 구조화된 기억 모듈입니다. 이는 인간의 실제 세계 작업 이해 및 의사 결정 방식을 모방하여 다양한 작업 장면에서의 상호작용 경험을 저장합니다.
- **Intuitive Perceptor**: 현재 시각적 장면에서 물리적 관계(예: 객체 간 공간 제약)와 행동 가능성(예: 잡기, 밀기, 당기기 등의 상호작용 가능성)을 추출하는 지각 모듈입니다.

### 작업 흐름
1. 로봇은 VLM을 통해 현재 장면에 대한 의미론적 이해를 수행합니다
2. Intuitive Perceptor는 장면에서 물리적 관계와 행동 가능성을 추출합니다
3. Memory Graph는 현재 장면과 유사한 과거 상호작용 경험을 검색합니다
4. 위 정보를 종합하여 로봇은 현재 장면에 적합한 상호작용 행동 경향을 추론합니다

### 실험 설정
- 다양한 실제 장면에서 테스트하며, 다양한 객체 유형과 작업 요구 사항을 포함합니다
- 비교 기준에는 순수 VLM 방법, 기억 모듈이 없는 변형 등이 포함됩니다
- 평가 지표에는 작업 성공률, 상호작용 합리성, 일반화 능력이 포함됩니다

### 주요 결과
- 순수 VLM 방법에 비해 INTENTION은 새로운 작업(novel tasks)에서 성공률이 크게 향상되었습니다
- Memory Graph는 로봇이 과거 경험을 활용하여 반복적인 지시에 대한 의존도를 줄일 수 있게 합니다
- Intuitive Perceptor는 물리적 관계를 효과적으로 추출하여 상호작용 행동이 물리적 상식에 더 부합하도록 합니다

### 결론
INTENTION은 VLM 장면 추론과 상호작용 기반 기억을 결합하여 휴머노이드 로봇에게 인간과 유사한 직관적 상호작용 능력을 제공하며, 실제 장면에서 더 강한 적응성과 일반화 능력을 보여줍니다. 프로젝트 비디오와 추가 세부 사항은 https://robo-intention.github.io 에서 확인할 수 있습니다.
