---
$id: ent_paper_ssr_surefooted_symmetric_traversal_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'SSR: Scaling Surefooted and Symmetric Humanoid Traversal to the Open World'
  zh: 第一视角视觉驱动的人形机器人开放世界稳健穿越
  ko: 'SSR: Scaling Surefooted and Symmetric Humanoid Traversal to the Open World'
summary:
  en: 'Extending humanoid traversal to the open world is key to practical deployment in human environments, but remains challenging.
    The robot must use vision to ensure safe and reliable foot placement on heterogeneous terrain under highly dynamic motion,
    while producing coordinated, natural whole-body behaviors. Institutions per source list: 浙江大学.'
  zh: SSR 是一个面向开放世界的仿人机器人高效端到端视觉导航框架，由研究团队提出。其核心贡献包括：通过想象落脚点引导减少边缘滑移，利用等变潜空间对称增强实现高效双边协调，并采用地形特定多判别器运动先验促进类人行为。实验表明，SSR 在楼梯、宽间隙、高平台等多样真实地形上实现了安全稳定的长距离行走。
  ko: 'Extending humanoid traversal to the open world is key to practical deployment in human environments, but remains challenging.
    The robot must use vision to ensure safe and reliable foot placement on heterogeneous terrain under highly dynamic motion,
    while producing coordinated, natural whole-body behaviors. Institutions per source list: 浙江大学.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- ssr
- scaling
- surefooted
- symmetric
- humanoi
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 10 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2605.30770 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2605.30770v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2605.30770 SSR: Scaling Surefooted and Symmetric Humanoid Traversal to the Open World'
  url: https://arxiv.org/abs/2605.30770
  accessed_at: '2026-07-31'
  date: '2026-05-29'
- id: src_002
  type: website
  title: Project page
  url: https://ssr-humanoid.github.io/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: 万字长文｜人形机器人的运动小脑会不会成为人形机器人的基础设施？
  url: https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA
  accessed_at: '2026-07-31'
---




## 概述

SSR是一个面向开放世界穿越的统一单阶段端到端强化学习框架，由AgiBot团队提出，直接从自我中心深度图像和本体感觉联合学习可靠落脚点与对称自然的全身运动。核心贡献在于将落脚点安全评估从接触时刻前移到摆动阶段（想象落脚点引导）、在潜在空间实现镜像等变对称增强（而非输入级增强），以及用地形特定判别器注入人类运动风格，最终在仿真和实机上实现了对楼梯、间隙、高台等异构地形的近100%成功率与1.3 km户外连续穿越。

## 它改变了什么

此前基于学习的人形穿越方法要么依赖多阶段流水线（如HPL）导致误差累积，要么在输入级做对称增强导致视觉循环策略的训练成本高到难以承受（InpSym峰值内存超24 GB，需48 GB显卡）。更根本的问题是，落脚点安全性评估通常只在接触时刻或接触后发生，摆动腿在空中的动作缺乏密集的早期引导信号，导致策略无法在触地前主动规避边缘和悬空区域。SSR真正改变的是将“安全落脚”从稀疏的接触事件奖励转化为密集的接触前引导，同时把对称性先验从昂贵的输入空间搬移到紧凑的潜在空间，使训练在单张RTX 4090上即可完成（内存从28.8 GB降至23.7 GB）。这解决了此前对称性学习与视觉策略耦合时“计算贵、易脆弱”的痛点，让左右对称的全身协调行为不再依赖大量镜像数据增强。

## 方法拆解

### 问题建模与架构
- 将运动建模为POMDP，PPO优化，非对称actor-critic。策略观测 `o_t = (o_{t-h+1:t}^p, I_t)`，其中本体感觉 `o_t^p ∈ ℝ^72`（基座角速度、投影重力、速度指令、关节位置/速度、上一动作），深度图 `I_t ∈ ℝ^{36×36}`；动作 `a_t ∈ ℝ^21` 为关节位置目标。
- Critic使用特权信息（地面真值速度、接触状态、肢体位置、脚/身体周围高度图）。
- 策略由循环跨模态编码器（CNN编码图像 + MLP编码本体感觉 + GRU融合）、运动状态估计器（预测基座速度 `v̂_t`）和MoE actor组成。编码器三个头 `ẑ_t^f, ẑ_t^b, ẑ_t^p` 分别解码脚部高度图、基座高度图和下一本体感觉（VAE）。

### 想象落脚点引导（Imagined Foothold Guidance）
- 训练中，落脚点想象模型从特权状态 `s_t` 和动作 `a_t` 预测每只脚的预期接触分布 `q_{i,t}(p) = N(p; μ̂_{i,t}, (σ̂_{i,t})²I)`，用高斯负对数似然监督：`L_pred ∝ Σ_i (||p_{i,t}^* - μ̂_{i,t}||² / (2σ̂²) + 2log σ̂)`。
- 支撑缺陷 `ρ(p)` 在22.5 cm×10 cm脚掌补丁上每2.5 cm采样，度量接触点附近支撑重叠比例。落脚点奖励 `r_t^f = exp(-(Σ_i ρ̃_{i,t})² / σ_f)`，其中支撑脚用当前接触点评估，摆动脚用想象分布下的期望缺陷（离散高斯加权采样近似）。
- 关键设计：仅在训练早期步态稳定（地形课程超过预设等级）后才启用接触前引导，避免噪声目标干扰；斜坡地形上强制 `ρ̃ = 0` 避免抑制倾斜接触。

### 等变潜在空间对称增强
- 输入级镜像增强对视觉循环策略代价高（需重新编码镜像图像并展开GRU隐藏状态）。SSR只编码原始观测一次，然后镜像紧凑的actor输入 `o_t^a = [o_t^p, v̂_t, ẑ_t]`。
- 编码器强制镜像等变：时间本体感觉经固定重组算子 `𝒯` 映射为左右分支（各 `ℝ^{42×h}`），镜像变换简化为分支交换 `ℳ_c`；CNN用lift卷积（`K_l = [k_l; ℳ_2D k_l]`）提升图像为成对通道，后续层用约束核 `K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]` 保持对称结构。
- 训练时镜像轨迹 `τ^g = (M_o o_t^a, M_s s_t, M_a a_t)` 附加到原始rollout用于PPO更新，不增加额外前向编码成本。

### 跨地形运动先验
- 为每种地形类型（Slope、Stairs、Discrete、Gap、Platform）各训练一个判别器 `D_i`，从五帧历史 `ψ_t`（每帧63维）建模地形特定参考运动风格，提供AMP风格奖励。数据来自定制动作捕捉和AMASS子集（总时长：Slope 261.3 s，Stairs 295.8 s，Discrete 136.0 s，Gap 174.5 s，Platform 181.6 s）。

## 关键创新

1. **接触前落脚点引导**：将落脚点安全从“接触时评估”变为“摆动中密集引导”，通过想象接触分布和支撑缺陷度量，让策略在触地前就把脚导向平坦区域。这是对HPL/PIM等“接触后反馈”范式的根本改进，直接减少了边缘接触和滑倒。
2. **潜在空间等变对称增强**：通过构造镜像等变编码器（等变MLP/GRU/CNN的完整证明链），将对称性学习从输入级搬到潜在空间，训练内存降低18%（28.8→23.7 GB），使单张RTX 4090可训练，同时保留左右脚交替主导的灵活性（NoSym则固定主导腿）。
3. **地形特定多判别器**：用5个判别器分别建模不同地形的运动风格，而非共享判别器，显著降低峰值脚接触力（如Stairs Up从538.5 N降至507.7 N）和平均功率，使风格先验不被地形差异破坏。

## 实验与结果


### 仿真结果（Isaac Gym，4096个AgiBot X2，每地形1000次试验）
| 方法 | Stairs Up成功率 | Gap成功率 | 备注 |
|---|---|---|---|
| SSR | ~100% | ~100% | 最陡楼梯（22 cm脚仅3 cm余量）仍近100% |
| HPL | 随难度快速退化 | 随难度快速退化 | 多阶段误差累积 |
| PIM | 随难度快速退化 | 随难度快速退化 | 高度图方法 |
| NoFoothold | 最大下降 | 最大下降 | 移除落脚点引导 |
| NoImgn | 最大下降 | 最大下降 | 仅接触时刻评估 |

### 对称性消融
- SSR在八方向速度指令下跟踪更准，原地转向轨迹近镜像对称；NoSym累积漂移、固定主导腿。
- SSR相比InpSym：相同训练时间回报更高、更早达到最大难度、GPU内存28.8→23.7 GB（-18%）。

### 人类运动风格（Table 1，mean ± std）
| 地形 | 指标 | SSR | NoStyle | SglDisc |
|---|---|---|---|---|
| Stairs Up | AP (W) | 381.3±5.4 | 451.6±10.1 | 391.8±5.9 |
| Stairs Up | PFCF (N) | 507.7±12.6 | 538.5±13.5 | 544.7±9.7 |
| Gap | AP (W) | 214.4±5.7 | 317.0±6.5 | 241.1±7.4 |
| Gap | PFCF (N) | 463.4±15.3 | 518.1±16.6 | 481.8±14.7 |

### 实机结果（Table 2，每地形20次）
| 地形 | 成功率 |
|---|---|
| Stairs Up 15/30 cm | 100.0% (20/20) |
| Stairs Down 15/30 cm | 100.0% (20/20) |
| Gap 80 cm | 95.0% (19/20) |
| Platform 40 cm | 100.0% (20/20) |
| Gap-H 90 cm | 85.0% (17/20) |
| Platform-H 45 cm | 95.0% (19/20) |

### 户外与跨平台
- 工业遗产公园1.3 km、40分钟连续穿越，无重置；零样本OOD泛化包括穿孔网格地板、1.3 m宽开式楼梯、螺旋楼梯。
- 跨平台到DEEP Robotics DR02（1.8 m高、70 kg）：Stairs Up/Down 15/40 cm成功率100%，Gap 80 cm成功率100%，Platform 45 cm成功率95%（由表内数值 253.0→12.6 计算）。

## 边界与局限

- 固定前向深度相机（俯仰50°）限制感知覆盖，全向穿越（如侧向或后退）困难；作者建议多视角感知或主动视点调整。
- 严重深度感知失败（如强阳光下镜面反射）仍可能扭曲局部几何，导致落脚点误判；需更鲁棒的感知嵌入。
- 仅关注脚-地面交互，上体接触（如手扶、攀爬）未探索；多接触技能和更丰富的人类演示可进一步扩展机动性。
- 论文未明确：训练数据量、超参数敏感性分析、以及仿真到实机的sim-to-real gap量化。

## 工程启示

- **复现优先级**：先核对地形课程（5种地形×20难度，8 m×4 m区域）和命令范围（Table 4/5），这是训练稳定性的基础。其次检查等变CNN的核约束实现（`K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]`），这是对称性增强的核心，最容易出错。
- **最容易踩坑**：想象落脚点引导的启用时机（课程等级阈值）和斜坡地形特殊处理（`ρ̃=0`）必须严格遵循，否则早期噪声目标会破坏策略；支撑缺陷采样（22.5 cm×10 cm补丁、2.5 cm间隔）的分辨率直接影响奖励质量。
- **硬件与部署**：深度渲染用NVIDIA Warp实现（每像素并行，不重建BVH），实机用Jetson AGX Orin + ONNX Runtime 50 Hz推理；关节增益（Table 14）需按平台调整，跨平台时保留相同训练流程但需重新收集运动先验数据。
- **下游团队**：若目标平台无腰装相机或相机俯仰角不同，需重新训练（感知分布变化）；若追求全向穿越，需先解决感知覆盖问题，SSR的固定前向相机是硬约束。

## 参考
- https://arxiv.org/abs/2605.30770
- https://ssr-humanoid.github.io/
- https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA

## Overview

SSR is a unified single-stage end-to-end reinforcement learning framework for open-world traversal in humanoid robots, proposed by the AgiBot team. It directly learns reliable foothold selection and symmetric, natural whole-body motion from egocentric depth images and proprioception. The core contributions lie in shifting foothold safety assessment from the contact moment to the swing phase (imagined foothold guidance), achieving mirror-equivariant symmetry augmentation in latent space (rather than input-level augmentation), and injecting human motion styles via terrain-specific discriminators. This ultimately achieves near-100% success rates on heterogeneous terrains such as stairs, gaps, and high platforms, along with 1.3 km of continuous outdoor traversal, in both simulation and real-world deployment.

## What It Changes

Previous learning-based humanoid traversal methods either relied on multi-stage pipelines (e.g., HPL) that accumulate errors, or performed input-level symmetry augmentation that made training visual recurrent policies prohibitively expensive (InpSym peaked at over 24 GB of memory, requiring 48 GB GPUs). A more fundamental issue is that foothold safety assessment typically occurs only at or after the contact moment, leaving the swing leg's in-air motion without dense early guidance signals, preventing the policy from proactively avoiding edges and overhanging regions before touchdown. SSR fundamentally changes this by converting "safe foothold" from a sparse contact-event reward into dense pre-contact guidance, while moving the symmetry prior from the expensive input space into a compact latent space, enabling training on a single RTX 4090 (memory reduced from 28.8 GB to 23.7 GB). This resolves the previous pain point where symmetry learning coupled with visual policies was "computationally expensive and fragile," allowing symmetric whole-body coordination without relying on extensive mirrored data augmentation.

## Method Breakdown

### Problem Formulation and Architecture
- The motion is modeled as a POMDP, optimized with PPO, using an asymmetric actor-critic. The policy observation is `o_t = (o_{t-h+1:t}^p, I_t)`, where proprioception `o_t^p ∈ ℝ^72` (base angular velocity, projected gravity, velocity commands, joint positions/velocities, previous actions) and depth image `I_t ∈ ℝ^{36×36}`; the action `a_t ∈ ℝ^21` consists of joint position targets.
- The critic uses privileged information (ground-truth velocity, contact states, limb positions, height maps around feet/body).
- The policy consists of a recurrent cross-modal encoder (CNN encoding images + MLP encoding proprioception + GRU fusion), a motion state estimator (predicting base velocity `v̂_t`), and a Mixture-of-Experts (MoE) actor. The encoder has three heads `ẑ_t^f, ẑ_t^b, ẑ_t^p` that decode foot height maps, base height maps, and next proprioception, respectively (VAE).

### Imagined Foothold Guidance
- During training, a foothold imagination model predicts the expected contact distribution for each foot from privileged state `s_t` and action `a_t`: `q_{i,t}(p) = N(p; μ̂_{i,t}, (σ̂_{i,t})²I)`, supervised by Gaussian negative log-likelihood: `L_pred ∝ Σ_i (||p_{i,t}^* - μ̂_{i,t}||² / (2σ̂²) + 2log σ̂)`.
- Support deficiency `ρ(p)` is sampled every 2.5 cm on a 22.5 cm × 10 cm foot patch, measuring the proportion of support overlap near the contact point. The foothold reward is `r_t^f = exp(-(Σ_i ρ̃_{i,t})² / σ_f)`, where support feet are evaluated at the current contact point and swing feet are evaluated under the expected deficiency from the imagined distribution (approximated via discrete Gaussian-weighted sampling).
- Key design: pre-contact guidance is only enabled after early-stage gait stabilization (terrain curriculum exceeding a preset level) to avoid noisy targets interfering; on sloped terrain, `ρ̃ = 0` is enforced to avoid suppressing inclined contacts.

### Equivariant Latent-Space Symmetry Augmentation
- Input-level mirror augmentation is costly for visual recurrent policies (requiring re-encoding mirrored images and unrolling GRU hidden states). SSR encodes the raw observation only once, then mirrors the compact actor input `o_t^a = [o_t^p, v̂_t, ẑ_t]`.
- The encoder enforces mirror equivariance: temporal proprioception is mapped via a fixed rearrangement operator `𝒯` into left/right branches (each `ℝ^{42×h}`), where the mirror transformation reduces to branch swapping `ℳ_c`; the CNN uses lifted convolutions (`K_l = [k_l; ℳ_2D k_l]`) to lift images into paired channels, with subsequent layers using constrained kernels `K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]` to preserve symmetric structure.
- During training, mirrored trajectories `τ^g = (M_o o_t^a, M_s s_t, M_a a_t)` are appended to the original rollout for PPO updates, without incurring additional forward encoding costs.

### Cross-Terrain Motion Priors
- A separate discriminator `D_i` is trained for each terrain type (Slope, Stairs, Discrete, Gap, Platform), modeling terrain-specific reference motion styles from five-frame histories `ψ_t` (63 dimensions per frame), providing AMP-style rewards. Data comes from custom motion capture and AMASS subsets (total durations: Slope 261.3 s, Stairs 295.8 s, Discrete 136.0 s, Gap 174.5 s, Platform 181.6 s).

## Key Innovations

1. **Pre-Contact Foothold Guidance**: Transforms foothold safety from "assessment at contact" to "dense guidance during swing" by using imagined contact distributions and support deficiency metrics, guiding the foot toward flat regions before touchdown. This is a fundamental improvement over "post-contact feedback" paradigms like HPL/PIM, directly reducing edge contacts and slips.
2. **Latent-Space Equivariant Symmetry Augmentation**: By constructing a mirror-equivariant encoder (with a complete proof chain for equivariant MLP/GRU/CNN), symmetry learning moves from the input space to the latent space, reducing training memory by 18% (28.8 → 23.7 GB), enabling training on a single RTX 4090, while preserving the flexibility of alternating left/right foot dominance (NoSym fixes the dominant leg).
3. **Terrain-Specific Multi-Discriminators**: Using 5 discriminators to model motion styles for different terrains individually, rather than a shared discriminator, significantly reduces peak foot contact forces (e.g., Stairs Up from 538.5 N to 507.7 N) and average power, preventing style priors from being corrupted by terrain differences.

## Experiments and Results

### Simulation Results (Isaac Gym, 4096 AgiBot X2, 1000 trials per terrain)
| Method | Stairs Up Success Rate | Gap Success Rate | Notes |
|---|---|---|---|
| SSR | ~100% | ~100% | Near 100% even on steepest stairs (22 cm foot with only 3 cm clearance) |
| HPL | Rapid degradation with difficulty | Rapid degradation with difficulty | Multi-stage error accumulation |
| PIM | Rapid degradation with difficulty | Rapid degradation with difficulty | Height-map method |
| NoFoothold | Largest drop | Largest drop | Removes foothold guidance |
| NoImgn | Largest drop | Largest drop | Contact-time evaluation only |

### Symmetry Ablation
- SSR tracks more accurately under eight-direction velocity commands, with near-mirror-symmetric in-place turning trajectories; NoSym accumulates drift and fixes the dominant leg.
- SSR vs. InpSym: higher return for the same training time, reaches maximum difficulty earlier, GPU memory 28.8 → 23.7 GB (-18%).

### Human Motion Style (Table 1, mean ± std)
| Terrain | Metric | SSR | NoStyle | SglDisc |
|---|---|---|---|---|
| Stairs Up | AP (W) | 381.3±5.4 | 451.6±10.1 | 391.8±5.9 |
| Stairs Up | PFCF (N) | 507.7±12.6 | 538.5±13.5 | 544.7±9.7 |
| Gap | AP (W) | 214.4±5.7 | 317.0±6.5 | 241.1±7.4 |
| Gap | PFCF (N) | 463.4±15.3 | 518.1±16.6 | 481.8±14.7 |

### Real-Robot Results (Table 2, 20 trials per terrain)
| Terrain | Success Rate |
|---|---|
| Stairs Up 15/30 cm | 100.0% (20/20) |
| Stairs Down 15/30 cm | 100.0% (20/20) |
| Gap 80 cm | 95.0% (19/20) |
| Platform 40 cm | 100.0% (20/20) |
| Gap-H 90 cm | 85.0% (17/20) |
| Platform-H 45 cm | 95.0% (19/20) |

### Outdoor and Cross-Platform
- 1.3 km continuous traversal in an industrial heritage park over 40 minutes, with no resets; zero-shot OOD generalization includes perforated grid flooring, 1.3 m wide open stairs, and spiral staircases.
- Cross-platform to DEEP Robotics DR02 (1.8 m tall, 70 kg): Stairs Up/Down 15/40 cm success rate 100%, Gap 80 cm success rate 100%, Platform 45 cm success rate 95% (calculated from in-table values 253.0 → 12.6).

## Boundaries and Limitations

- The fixed forward-facing depth camera (50° pitch) limits perception coverage, making omnidirectional traversal (e.g., lateral or backward) difficult; the authors suggest multi-view perception or active viewpoint adjustment.
- Severe depth perception failures (e.g., specular reflections under strong sunlight) can still distort local geometry, leading to foothold misjudgments; more robust perception embeddings are needed.
- Only foot-ground interaction is considered; upper-body contacts (e.g., hand support, climbing) are not explored; multi-contact skills and richer human demonstrations could further expand mobility.
- The paper does not specify: training data volume, hyperparameter sensitivity analysis, or quantitative sim-to-real gap.

## Engineering Insights

- **Reproduction Priority**: First verify the terrain curriculum (5 terrain types × 20 difficulty levels, 8 m × 4 m areas) and command ranges (Table 4/5), as these are the foundation of training stability. Next, check the equivariant CNN kernel constraint implementation (`K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]`), which is the core of symmetry augmentation and the most error-prone part.
- **Most Common Pitfalls**: The activation timing of imagined foothold guidance (curriculum level threshold) and the special handling of sloped terrain (`ρ̃=0`) must be strictly followed; otherwise, early noisy targets can destabilize the policy. The resolution of support deficiency sampling (22.5 cm × 10 cm patch, 2.5 cm intervals) directly affects reward quality.
- **Hardware and Deployment**: Depth rendering uses NVIDIA Warp (per-pixel parallel, no BVH reconstruction); real-robot inference uses Jetson AGX Orin + ONNX Runtime at 50 Hz; joint gains (Table 14) need adjustment per platform; cross-platform deployment retains the same training pipeline but requires recollecting motion prior data.
- **Downstream Teams**: If the target platform lacks a waist-mounted camera or has a different camera pitch angle, retraining is required (perception distribution shift); if omnidirectional traversal is desired, perception coverage must be addressed first—SSR's fixed forward camera is a hard constraint.

## 개요

SSR은 AgiBot 팀이 제안한 개방형 세계 횡단을 위한 통합 단일 단계 end-to-end 강화 학습 프레임워크로, 자기중심적 깊이 이미지와 고유 감각을 직접 공동 학습하여 안정적인 착지점과 대칭적이고 자연스러운 전신 운동을 구현합니다. 핵심 기여는 착지점 안전 평가를 접촉 시점 이전의 스윙 단계로 앞당기고(상상 착지점 유도), 잠재 공간에서 미러 등변 대칭 강화를 구현하며(입력 수준 강화가 아닌), 지형 특화 판별기를 통해 인간 운동 스타일을 주입하여, 시뮬레이션과 실물 로봇에서 계단, 틈, 높은 플랫폼 등 이질적인 지형에서 약 100% 성공률과 1.3km 야외 연속 횡단을 달성한 것입니다.

## 무엇을 바꾸었는가

기존의 학습 기반 휴머노이드 횡단 방법은 다단계 파이프라인(예: HPL)에 의존하여 오류가 누적되거나, 입력 수준에서 대칭 강화를 수행하여 시각 순환 정책의 훈련 비용이 감당하기 어려울 정도로 높았습니다(InpSym의 최대 메모리는 24GB를 초과하여 48GB GPU 필요). 더 근본적인 문제는 착지점 안전 평가가 일반적으로 접촉 시점 또는 접촉 후에만 발생하여, 스윙 다리의 공중 동작에 대한 조밀한 초기 유도 신호가 부족하여 정책이 착지 전에 가장자리와 빈 공간을 능동적으로 회피할 수 없다는 점입니다. SSR이 진정으로 바꾼 것은 '안전한 착지'를 희소한 접촉 이벤트 보상에서 조밀한 접촉 전 유도로 전환하고, 대칭성 사전 지식을 값비싼 입력 공간에서 컴팩트한 잠재 공간으로 이동시켜 단일 RTX 4090에서 훈련을 완료할 수 있게 한 것입니다(메모리 28.8GB → 23.7GB). 이는 기존의 대칭성 학습과 시각 정책이 결합될 때 발생하는 '계산 비용이 높고 취약하기 쉬운' 문제를 해결하여, 좌우 대칭의 전신 협조 행동이 더 이상 대량의 미러 데이터 증강에 의존하지 않게 되었습니다.

## 방법 분석

### 문제 모델링 및 아키텍처
- 운동을 POMDP로 모델링하고 PPO로 최적화하며, 비대칭 actor-critic을 사용합니다. 정책 관측 `o_t = (o_{t-h+1:t}^p, I_t)`에서 고유 감각 `o_t^p ∈ ℝ^72`(베이스 각속도, 투영 중력, 속도 명령, 관절 위치/속도, 이전 동작), 깊이 맵 `I_t ∈ ℝ^{36×36}`; 동작 `a_t ∈ ℝ^21`은 관절 위치 목표입니다.
- Critic은 특권 정보(지상 진실 속도, 접촉 상태, 사지 위치, 발/몸 주변 높이 맵)를 사용합니다.
- 정책은 순환 교차 모달 인코더(CNN 이미지 인코딩 + MLP 고유 감각 인코딩 + GRU 융합), 운동 상태 추정기(베이스 속도 `v̂_t` 예측) 및 MoE actor로 구성됩니다. 인코더의 세 헤드 `ẑ_t^f, ẑ_t^b, ẑ_t^p`는 각각 발 높이 맵, 베이스 높이 맵 및 다음 고유 감각(VAE)을 디코딩합니다.

### 상상 착지점 유도(Imagined Foothold Guidance)
- 훈련 중 착지점 상상 모델은 특권 상태 `s_t`와 동작 `a_t`에서 각 발의 예상 접촉 분포 `q_{i,t}(p) = N(p; μ̂_{i,t}, (σ̂_{i,t})²I)`를 예측하며, 가우시안 음의 로그 우도로 감독합니다: `L_pred ∝ Σ_i (||p_{i,t}^* - μ̂_{i,t}||² / (2σ̂²) + 2log σ̂)`.
- 지지 결함 `ρ(p)`은 22.5cm×10cm 발바닥 패치에서 2.5cm 간격으로 샘플링되어 접촉점 주변 지지 중첩 비율을 측정합니다. 착지점 보상 `r_t^f = exp(-(Σ_i ρ̃_{i,t})² / σ_f)`에서 지지 발은 현재 접촉점으로 평가하고, 스윙 발은 상상 분포 하의 기대 결함(이산 가우시안 가중 샘플링 근사)으로 평가합니다.
- 핵심 설계: 훈련 초기 보행 안정화(지형 커리큘럼이 사전 설정된 등급 초과) 후에만 접촉 전 유도를 활성화하여 노이즈 목표가 간섭하지 않도록 하고, 경사 지형에서는 `ρ̃ = 0`을 강제하여 경사 접촉을 억제하지 않도록 합니다.

### 등변 잠재 공간 대칭 강화
- 입력 수준 미러 강화는 시각 순환 정책에 비용이 높습니다(미러 이미지를 다시 인코딩하고 GRU 숨겨진 상태를 펼쳐야 함). SSR은 원본 관측을 한 번만 인코딩한 다음 컴팩트한 actor 입력 `o_t^a = [o_t^p, v̂_t, ẑ_t]`을 미러링합니다.
- 인코더는 미러 등변을 강제합니다: 시간 고유 감각은 고정 재구성 연산자 `𝒯`를 통해 좌우 분기(각 `ℝ^{42×h}`)로 매핑되고, 미러 변환은 분기 교환 `ℳ_c`로 단순화됩니다; CNN은 리프트 컨볼루션(`K_l = [k_l; ℳ_2D k_l]`)을 사용하여 이미지를 쌍 채널로 승격시키고, 후속 레이어는 제약 커널 `K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]`을 사용하여 대칭 구조를 유지합니다.
- 훈련 중 미러 궤적 `τ^g = (M_o o_t^a, M_s s_t, M_a a_t)`이 원본 rollout에 추가되어 PPO 업데이트에 사용되며, 추가 전방 인코딩 비용이 없습니다.

### 지형 간 운동 사전
- 각 지형 유형(Slope, Stairs, Discrete, Gap, Platform)에 대해 판별기 `D_i`를 하나씩 훈련하여 5프레임 기록 `ψ_t`(프레임당 63차원)에서 지형 특정 참조 운동 스타일을 모델링하고 AMP 스타일 보상을 제공합니다. 데이터는 맞춤형 모션 캡처 및 AMASS 하위 집합에서 비롯됩니다(총 시간: Slope 261.3초, Stairs 295.8초, Discrete 136.0초, Gap 174.5초, Platform 181.6초).

## 핵심 혁신

1. **접촉 전 착지점 유도**: 착지점 안전을 '접촉 시 평가'에서 '스윙 중 조밀한 유도'로 전환하여, 상상 접촉 분포와 지지 결함 측정을 통해 정책이 착지 전에 발을 평평한 영역으로 유도합니다. 이는 HPL/PIM 등의 '접촉 후 피드백' 패러다임에 대한 근본적인 개선으로, 가장자리 접촉과 미끄러짐을 직접 줄입니다.
2. **잠재 공간 등변 대칭 강화**: 미러 등변 인코더(등변 MLP/GRU/CNN의 완전한 증명 체인)를 구성하여 대칭성 학습을 입력 수준에서 잠재 공간으로 이동시키고, 훈련 메모리를 18% 감소시켜(28.8→23.7GB) 단일 RTX 4090에서 훈련 가능하게 하면서 좌우 발 교대 지배의 유연성을 유지합니다(NoSym은 고정 지배 다리).
3. **지형 특화 다중 판별기**: 공유 판별기 대신 5개의 판별기를 사용하여 각 지형의 운동 스타일을 개별적으로 모델링하여, 최대 발 접촉력을 크게 줄이고(예: Stairs Up 538.5N → 507.7N) 평균 전력을 줄여 스타일 사전이 지형 차이로 손상되지 않도록 합니다.

## 실험 및 결과

### 시뮬레이션 결과(Isaac Gym, 4096개의 AgiBot X2, 지형당 1000회 시도)
| 방법 | Stairs Up 성공률 | Gap 성공률 | 비고 |
|---|---|---|---|
| SSR | ~100% | ~100% | 가장 가파른 계단(22cm 발, 3cm 여유)에서도 약 100% |
| HPL | 난이도에 따라 급격히 저하 | 난이도에 따라 급격히 저하 | 다단계 오류 누적 |
| PIM | 난이도에 따라 급격히 저하 | 난이도에 따라 급격히 저하 | 높이 맵 방법 |
| NoFoothold | 최대 하락 | 최대 하락 | 착지점 유도 제거 |
| NoImgn | 최대 하락 | 최대 하락 | 접촉 시점 평가만 |

### 대칭성 소거 실험
- SSR은 8방향 속도 명령에서 추적이 더 정확하고, 제자리 회전 궤적이 거의 미러 대칭입니다; NoSym은 누적 드리프트와 고정 지배 다리를 보입니다.
- SSR 대비 InpSym: 동일 훈련 시간에서 보상이 더 높고, 최대 난이도에 더 빨리 도달하며, GPU 메모리 28.8→23.7GB(-18%).

### 인간 운동 스타일(Table 1, mean ± std)
| 지형 | 지표 | SSR | NoStyle | SglDisc |
|---|---|---|---|---|
| Stairs Up | AP (W) | 381.3±5.4 | 451.6±10.1 | 391.8±5.9 |
| Stairs Up | PFCF (N) | 507.7±12.6 | 538.5±13.5 | 544.7±9.7 |
| Gap | AP (W) | 214.4±5.7 | 317.0±6.5 | 241.1±7.4 |
| Gap | PFCF (N) | 463.4±15.3 | 518.1±16.6 | 481.8±14.7 |

### 실물 로봇 결과(Table 2, 지형당 20회)
| 지형 | 성공률 |
|---|---|
| Stairs Up 15/30 cm | 100.0% (20/20) |
| Stairs Down 15/30 cm | 100.0% (20/20) |
| Gap 80 cm | 95.0% (19/20) |
| Platform 40 cm | 100.0% (20/20) |
| Gap-H 90 cm | 85.0% (17/20) |
| Platform-H 45 cm | 95.0% (19/20) |

### 야외 및 크로스 플랫폼
- 산업 유산 공원 1.3km, 40분 연속 횡단, 리셋 없음; 제로샷 OOD 일반화에는 천공 그리드 바닥, 1.3m 너비 개방형 계단, 나선형 계단 포함.
- DEEP Robotics DR02(1.8m 높이, 70kg)로 크로스 플랫폼: Stairs Up/Down 15/40cm 성공률 100%, Gap 80cm 성공률 100%, Platform 45cm 성공률 95%(표 내 수치 253.0→12.6으로 계산).

## 경계 및 한계

- 고정 전방 깊이 카메라(피치 50°)는 인식 범위를 제한하여 전방향 횡단(예: 측면 또는 후진)이 어렵습니다; 저자는 다중 시점 인식 또는 능동 시점 조정을 제안합니다.
- 심각한 깊이 인식 실패(예: 강한 햇빛 아래 거울 반사)는 여전히 로컬 지오메트리를 왜곡하여 착지점 오판을 유발할 수 있습니다; 더 견고한 인식 임베딩이 필요합니다.
- 발-지면 상호작용에만 초점을 맞추며, 상체 접촉(예: 손잡기, 등반)은 탐구되지 않았습니다; 다중 접촉 기술과 더 풍부한 인간 데모가 기동성을 더 확장할 수 있습니다.
- 논문에서 명시되지 않은 사항: 훈련 데이터 양, 하이퍼파라미터 민감도 분석, 시뮬레이션-실물 sim-to-real 격차 정량화.

## 엔지니어링 시사점

- **재현 우선순위**: 먼저 지형 커리큘럼(5가지 지형 × 20 난이도, 8m×4m 영역)과 명령 범위(Table 4/5)를 확인하세요. 이는 훈련 안정성의 기초입니다. 다음으로 등변 CNN의 커널 제약 구현(`K_nl = [[k_a, k_b]; [ℳ_2D k_b, ℳ_2D k_a]]`)을 확인하세요. 이는 대칭 강화의 핵심으로 가장 오류가 발생하기 쉽습니다.
- **가장 함정에 빠지기 쉬운 부분**: 상상 착지점 유도의 활성화 시점(커리큘럼 등급 임계값)과 경사 지형 특수 처리(`ρ̃=0`)를 엄격히 따라야 합니다. 그렇지 않으면 초기 노이즈 목표가 정책을 손상시킵니다; 지지 결함 샘플링(22.5cm×10cm 패치, 2.5cm 간격)의 해상도는 보상 품질에 직접적인 영향을 미칩니다.
- **하드웨어 및 배포**: 깊이 렌더링은 NVIDIA Warp로 구현(픽셀당 병렬, BVH 재구성 없음), 실물 로봇은 Jetson AGX Orin + ONNX Runtime 50Hz 추론; 관절 게인(Table 14)은 플랫폼에 따라 조정해야 하며, 크로스 플랫폼 시 동일한 훈련 절차를 유지하지만 운동 사전 데이터를 다시 수집해야 합니다.
- **하위 팀**: 대상 플랫폼에 허리 장착 카메라가 없거나 카메라 피치 각도가 다르면 재훈련이 필요합니다(인식 분포 변화); 전방향 횡단을 추구한다면 먼저 인식 범위 문제를 해결해야 하며, SSR의 고정 전방 카메라는 하드 제약입니다.
