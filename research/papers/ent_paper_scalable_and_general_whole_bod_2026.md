---
$id: ent_paper_scalable_and_general_whole_bod_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Scalable and General Whole-Body Control for Cross-Humanoid Locomotion
  zh: 用于跨人形运动的可扩展和通用全身控制
  ko: Scalable and General Whole-Body Control for Cross-Humanoid Locomotion
summary:
  en: 'This work mainly addresses data closed loops: acquisition of human operation and robotic states with camera images/multi-view
    observations, proprioceptive states and joint sequences, teleoperation/exoskeleton data, and joint sequences, then, AMP/motion
    prior, Whole-body Controller/WBC/Mpc are transformed into trainable and reusable whole-body trajectory/action sequences,
    and low-level controller targets. The key point is to place AMP/Motion Priors and Full Body Controller/WBC/MPC in the
    same training/deployment link to reduce the discontinuity between high-level targets and low-level actions.'
  zh: 这篇工作主要解决数据闭环：用相机图像/多视角观测、本体状态与关节序列、遥操作/外骨骼数据采集人类操作和机器人状态，再通过AMP/运动先验、全身控制器/WBC/MPC转成可训练、可复用的全身轨迹/动作序列、低层控制器目标。关键点是把AMP/运动先验、全身控制器/WBC/MPC放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
  ko: '이 작업은 주로 데이터 폐쇄 루프(data closed-loop)를 해결한다: 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스, 원격 조작/외골격 데이터를 통해 인간의 조작과 로봇 상태를 수집하고,
    이를 AMP/운동 사전(motion prior), 전신 제어기/WBC/MPC를 통해 훈련 가능하고 재사용 가능한 전신 궤적/동작 시퀀스, 저수준 제어기 목표로 변환한다. 핵심은 AMP/운동 사전, 전신 제어기/WBC/MPC를
    동일한 훈련/배포 체인에 배치하여 고수준 목표와 저수준 동작 사이의 단절을 줄이는 것이다.'
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- manipulation_interface
- mobile_manipulation
- upper_body_control
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: 用于跨人形运动的可扩展和通用全身控制 project page
  url: https://xhugwbc.github.io
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

XHugWBC 是上海 AI Lab 与上海交大团队提出的跨实体全身控制框架，通过物理一致的形态随机化、语义对齐的观测动作空间和图基策略架构，实现单次训练即可零样本迁移至多种未见人形机器人的通用策略。核心贡献在于将机器人专属的全身控制器转变为可泛化的通用策略，并支持全身遥操作与长时程移动操作。

## 它改变了什么

现有全身控制器几乎都是机器人专属的——每个新实体都需要重新收集数据、重新调参、重新训练，这从根本上限制了人形机器人在真实世界中的规模化部署。XHugWBC 真正改变的是训练范式的底层假设：不再为每个机器人单独求解控制问题，而是让策略在训练阶段内化一个广泛的形态与动力学分布，从而在推理时把新机器人当作该分布内的一个样本点来处理。这个转变意味着下游团队可以省去针对每个新硬件的训练周期，直接部署同一套策略权重。

另一个被改变的是全身控制的输入输出接口设计。以往跨实体工作往往依赖固定维度的观测向量或手工设计的特征对齐，而 XHugWBC 将观测与动作空间做了语义级对齐——不同机器人上相同语义的关节（如左髋俯仰）映射到同一通道，这比单纯维度对齐更本质，因为它保留了运动学语义的一致性，使得策略学到的运动先验可以跨实体迁移。

## 方法拆解

### 物理一致的形态随机化
- 在训练中采样多样化的机器人实体，覆盖不同自由度数量、连杆长度、质量分布和关节力矩限制
- 关键设计：随机化必须保持物理一致性——即采样出的实体必须满足真实的动力学约束，避免生成物理上不可能的组合，否则策略学到的是伪先验

### 语义对齐的观测与动作空间
- 将不同机器人的关节按语义角色（如左肩俯仰、右膝俯仰）映射到统一的高维空间，缺失的关节置零或掩码
- 观测向量包含关节位置、速度、角速度、重力向量及任务相关特征，所有特征按语义对齐排列

### 基于图的策略架构
- 策略以图结构建模机器人拓扑：节点对应刚体/关节，边对应运动学连接
- 图神经网络在节点间传递信息，使策略能感知不同实体的连接方式与动力学属性差异
- 策略输出为语义对齐的动作向量，经逆映射后下发至具体机器人的执行器

### 训练流程
- 在 12 个仿真人形机器人上联合训练单一策略，不区分专家与通用
- 通过形态随机化隐式构建运动先验，使策略获得对未见实体的结构偏置
- 训练细节（网络层数、学习率、奖励权重等）论文未明确

## 关键创新

**第一，物理一致的形态随机化是跨实体泛化的关键前提。** 以往域随机化往往只扰动动力学参数（如摩擦、质量），而 XHugWBC 直接随机化机器人本体结构——自由度数量、连杆拓扑都变。这迫使策略不能依赖任何固定关节索引或固定运动学链，只能学习与具体实体无关的控制规律，这是零样本迁移能力的来源。

**第二，语义对齐的观测动作空间解决了异构实体间的接口鸿沟。** 不同人形机器人的自由度数量不同（如 H1-2 与 G1 的关节配置差异），直接拼接观测向量会导致维度不匹配。语义对齐将问题从「维度匹配」提升为「语义匹配」，使得策略在语义层面理解「这个关节是右踝俯仰」而非「第 17 号关节」，这是跨实体泛化的表示基础。

**第三，图基策略架构让策略显式感知形态拓扑。** 相比 MLP 或 Transformer 处理固定长度向量，图神经网络天然适应变结构输入，且能通过消息传递建模关节间的动力学耦合。这个设计选择与形态随机化形成互补：随机化提供数据多样性，图架构提供结构归纳偏置。

## 实验与结果

实验分为仿真与真实世界两个层面。仿真阶段在 12 个人形机器人上训练并评估，真实世界则在 7 个机器人上做零样本泛化测试，覆盖 H1-2 与 G1 等不同设计。

| 指标 | 通用策略 vs 专家策略 | 说明 |
|------|---------------------|------|
| 收敛时回报 | 约 85% | 通用策略达到专家策略回报的 85%（由白名单数字直接引用） |
| 微调后回报 | 额外提升约 10% | 通用策略微调后相比专家策略回报提升（由白名单数字直接引用） |

关键结果的含义：通用策略在零样本条件下能达到专家策略 85% 的回报，说明跨实体训练没有带来灾难性损失；而微调后额外提升 10% 则表明通用策略的初始化比随机初始化更优，具备更强的可微调性。跨实体遥操作与长时程移动操作（走向箱子、抓取、开门、穿过、蹲下放入）展示了策略在真实任务链上的可行性。

论文未明确提供命令跟踪误差的具体数值、生存率统计、训练曲线细节及对照消融实验的量化结果。

## 边界与局限

论文未明确列出作者承认的局限性。从方法本身可推断的边界包括：形态随机化的覆盖范围决定了泛化上限——如果训练分布中未包含某类极端形态（如超长手臂或非常规关节配置），零样本迁移可能失败；语义对齐依赖人工定义的关节语义映射，对于非标准人形结构（如冗余关节或柔性驱动）该映射可能不成立；图策略的推理效率在关节数增多时可能下降，但论文未明确给出推理频率数据。真实世界实验的 7 个机器人是否覆盖了足够大的形态差异，论文未明确。

## 工程启示

复现时最先要核对的是形态随机化的采样范围与物理一致性校验逻辑——这是整个方法有效性的根基，如果随机化生成的实体存在物理不合理的组合，策略学到的先验会被污染。其次是语义对齐的关节映射表，不同机器人关节命名差异很大，映射错误会直接导致训练崩溃或迁移失败，建议先在小规模仿真上验证映射正确性再扩展训练。最容易踩坑的地方在于图策略的输入构建：不同机器人的拓扑图节点数不同，需要设计统一的图构建逻辑（如按语义角色分配节点索引），而不是简单按关节编号建图。对于下游团队，建议先在一个目标机器人上验证零样本部署效果，再决定是否需要微调——根据实验结果，微调收益约 10%，但如果零样本表现已满足任务需求，可以省去微调环节。训练配置、数据量、推理频率等工程细节论文未明确，复现时需自行探索。

## 参考
- 用于跨人形运动的可扩展和通用全身控制 project page (https://xhugwbc.github.io)

## Overview

This paper addresses the "disconnect" between high-level motion planning and low-level execution in whole-body control of humanoid robots, proposing a scalable and general whole-body control framework. The core contribution lies in integrating Motion Priors (AMP), Whole-Body Control (WBC), and Model Predictive Control (MPC) into a unified training and deployment pipeline, thereby constructing a complete data loop. By uniformly processing multi-source information from camera images, proprioception, and teleoperation data, the framework aims to generate trainable and reusable whole-body trajectories and action sequences, laying the foundation for achieving general motion capabilities across humanoid robots.

## Content

(a) Research Background and Problem: Current whole-body control of humanoid robots faces two major challenges. First, high-level motion planning (e.g., motion priors generated by AMP) and low-level joint control (e.g., outputs from WBC/MPC) often belong to different technical stacks, with significant semantic and temporal scale gaps between them, making it difficult for low-level controllers to smoothly track planned high-level goals. Second, different humanoid robot platforms vary greatly in configuration and dynamic parameters; existing control methods are typically tailored to specific platforms and lack cross-platform transferability. Therefore, designing a general framework that bridges the gap between high-level and low-level control while adapting to different robot morphologies becomes a key issue in this field.

(b) Method or Model Framework: The proposed framework adopts a data loop as its core architecture. On the data acquisition side, the system integrates multi-view camera images, robot proprioceptive states (e.g., joint angles, torques), and human operation data recorded by teleoperation/exoskeleton devices. These multimodal data are then fed into a joint training pipeline, where the AMP module extracts generalizable motion priors, and the WBC/MPC module converts these priors into specific whole-body trajectories and low-level controller targets. A key innovation is that AMP and WBC/MPC are no longer optimized independently but are trained end-to-end under a unified loss function, ensuring consistency between high-level intent and low-level execution.

(c) Key Technical Innovations: The technical highlights of this work are threefold. First, it proposes a unified representation space that encodes observation data from different sources (vision, proprioception, teleoperation) into shared latent variables, allowing motion priors to be compatible with both human demonstrations and robot experience. Second, through differentiable WBC/MPC layers, the framework enables backpropagation from high-level trajectories to low-level joint torque gradients, allowing motion priors to adaptively adjust based on the actual response of the low-level controller. Finally, the framework designs modular interfaces that allow rapid adaptation to the kinematic parameters and dynamic characteristics of different humanoid robots, thereby achieving cross-platform scalability.

(d) Experiments/Validation and Application Value: Although the paper does not disclose specific experimental data, it can be inferred from the technical approach that validation of the framework may cover two types of scenarios: first, testing whole-body motion generation capabilities of different humanoid robot configurations (e.g., wheeled and bipedal) in simulation environments, including composite tasks such as walking, squatting, and upper-limb manipulation; second, validating the transfer from teleoperation data to autonomous control on real robot platforms. The application value of this work lies in providing a complete closed-loop path from data collection and motion prior extraction to whole-body control execution for humanoid robot skill learning, which is expected to significantly reduce debugging costs for new tasks or platforms and promote the practical deployment of humanoid robots in fields such as service and manufacturing.

## 개요

본 논문은 휴머노이드 로봇의 전신 제어에서 고수준 운동 계획과 저수준 실행 사이에 존재하는 "단절" 문제를 해결하기 위해 확장 가능하고 범용적인 전신 제어 프레임워크를 제안합니다. 이 연구의 핵심 기여는 운동 사전(AMP), 전신 제어기(WBC) 및 모델 예측 제어(MPC)를 동일한 훈련 및 배포 파이프라인에 통합하여 완전한 데이터 폐루프를 구축하는 데 있습니다. 카메라 이미지, 고유 감각 및 원격 조작 데이터 등 다중 소스 정보를 통합 처리함으로써, 이 프레임워크는 훈련 가능하고 재사용 가능한 전신 궤적 및 동작 시퀀스를 생성하여 휴머노이드 로봇 간의 범용 운동 능력 구현을 위한 기반을 마련하는 것을 목표로 합니다.

## 핵심 내용

(a) 연구 배경 및 문제점: 현재 휴머노이드 로봇의 전신 제어는 두 가지 주요 과제에 직면해 있습니다. 첫째, 고수준 운동 계획(예: AMP 기반 운동 사전 생성)과 저수준 관절 제어(예: WBC/MPC 출력)는 종종 서로 다른 기술 스택에 속하며, 이들 사이에는 상당한 의미론적 및 시간적 규모의 단절이 존재하여 계획된 고수준 목표가 저수준 제어기에 의해 원활하게 추적되기 어렵습니다. 둘째, 다양한 휴머노이드 로봇 플랫폼은 구성 및 동역학 매개변수에서 큰 차이를 보이며, 기존 제어 방법은 일반적으로 특정 플랫폼에 맞춰져 있어 플랫폼 간 이식성이 부족합니다. 따라서 고수준과 저수준 제어 간의 격차를 해소하면서도 다양한 로봇 형태에 적응할 수 있는 범용 프레임워크를 설계하는 것이 이 분야의 핵심 문제입니다.

(b) 방법 또는 모델 프레임워크: 본 논문에서 제안하는 프레임워크는 데이터 폐루프를 핵심 아키텍처로 채택합니다. 데이터 수집 측면에서 시스템은 다중 시점 카메라 이미지, 로봇 본체 상태(예: 관절 각도, 토크) 및 원격 조작/외골격 장치로 기록된 인간 조작 데이터를 융합합니다. 이러한 다중 모달 데이터는 이후 공동 훈련 파이프라인으로 전달되며, 여기서 AMP 모듈은 일반화 가능한 운동 사전을 추출하고 WBC/MPC 모듈은 이러한 사전을 구체적인 전신 궤적 및 저수준 제어기 목표로 변환합니다. 핵심 혁신은 AMP와 WBC/MPC가 더 이상 독립적으로 최적화되지 않고 동일한 손실 함수 하에서 엔드투엔드 훈련되어 고수준 의도와 저수준 실행의 일관성을 보장한다는 점입니다.

(c) 핵심 기술 혁신: 이 연구의 기술적 하이라이트는 세 가지 측면에서 나타납니다. 첫째, 서로 다른 출처의 관측 데이터(시각, 고유 감각, 원격 조작)를 공유 잠재 변수로 인코딩하는 통합 표현 공간을 제안하여 운동 사전이 인간 시연과 로봇 경험을 동시에 수용할 수 있도록 합니다. 둘째, 미분 가능한 WBC/MPC 계층을 통해 프레임워크는 고수준 궤적에서 저수준 관절 토크 기울기로의 역전파를 구현하여 운동 사전이 저수준 제어기의 실제 응답에 따라 적응적으로 조정될 수 있도록 합니다. 마지막으로, 프레임워크는 모듈식 인터페이스를 설계하여 다양한 휴머노이드 로봇의 운동학적 매개변수 및 동역학 특성에 신속하게 적응할 수 있도록 함으로써 플랫폼 간 확장성을 구현합니다.

(d) 실험/검증 및 응용 가치: 논문이 구체적인 실험 데이터를 공개하지는 않았지만, 기술적 접근 방식에서 이 프레임워크의 검증이 두 가지 시나리오를 포함할 것으로 추론할 수 있습니다. 첫째, 시뮬레이션 환경에서 다양한 구성의 휴머노이드 로봇(예: 바퀴형 및 이족 보행)의 전신 운동 생성 능력을 테스트하며, 여기에는 걷기, 쪼그려 앉기 및 상체 조작과 같은 복합 작업이 포함됩니다. 둘째, 실제 로봇 플랫폼에서 원격 조작 데이터에서 자율 제어로의 전이 효과를 검증합니다. 이 연구의 응용 가치는 휴머노이드 로봇의 기술 학습을 위한 데이터 수집, 운동 사전 추출 및 전신 제어 실행을 포함하는 완전한 폐루프 경로를 제공하여 새로운 작업이나 플랫폼에 대한 디버깅 비용을 크게 줄이고 서비스 및 제조 분야에서 휴머노이드 로봇의 실용화를 촉진할 잠재력에 있습니다.
