---
$id: ent_paper_hy_motion_1_0_scaling_flow_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HY-Motion 1.0: Scaling Flow Matching Models for Text-To-Motion Generation'
  zh: 'HY-Motion 1.0: Scaling Flow Matching Models for Text-To-Motion Generation'
  ko: 'HY-Motion 1.0: Scaling Flow Matching Models for Text-To-Motion Generation'
summary:
  en: We present HY-Motion 1.0, a series of state-of-the-art, large-scale, motion generation models capable of generating
    3D human motions from textual descriptions.
  zh: HY-Motion 1.0 是一系列基于 Diffusion Transformer (DiT) 的流匹配模型，首次将文本到3D人体运动生成模型扩展至十亿参数规模。该模型通过全阶段训练范式（包括3000小时大规模预训练、400小时高质量微调及强化学习）实现指令跟随能力，覆盖6大类200余种运动类别，显著超越现有开源基准。
  ko: We present HY-Motion 1.0, a series of state-of-the-art, large-scale, motion generation models capable of generating
    3D human motions from textual descriptions.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- hy
- motion
- '1'
- '0'
- scaling
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 677 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2512.23464 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2512.23464v1); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. [2026-08-05] number-audit:
    derived values explicitly labeled (evidence from proximity-constrained full-text pair match).'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2512.23464 HY-Motion 1.0: Scaling Flow Matching Models for Text-To-Motion Generation'
  url: https://arxiv.org/abs/2512.23464
  accessed_at: '2026-07-31'
  date: '2025-12-29'
- id: src_002
  type: website
  title: Project page
  url: https://github.com/Tencent-Hunyuan/HY-Motion-1.0
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

HY-Motion 1.0 是首个将基于 DiT 的流匹配模型扩展到十亿参数规模的文本到动作生成系统，由腾讯等机构完成。它通过构建超过 3000 小时的多源动作数据管线（野外视频、动捕、3D 资产），结合层次化双条件文本编码与三阶段训练（预训练-微调-RL），在指令跟随（SSAE 78.6%）和动作质量（3.43/5）上显著超越现有开源基准。

## 它改变了什么

这个工作的真正改变在于把"数据规模"从配角推到了主角位置。此前领域内主流做法（MoMask、GoToZero、LoM）要么依赖 HumanML3D 这类小规模标注集，要么用离散分词器牺牲动作平滑性换取语义多样性。HY-Motion 1.0 证明了：当动作数据达到数千小时量级并经过系统化清洗标注时，纯连续表示的流匹配模型可以同时解决"语义对齐差"和"物理伪影多"这两个长期痛点——它没有发明新的数学框架，而是把 LLM 领域已被验证的"数据+规模"路线成功迁移到了动作生成。

更关键的是，它改变了行业对"动作生成 scaling 曲线"的认知。作者通过消融实验揭示了一个非平凡结论：指令跟随能力随模型规模单调提升（0.05B→1B 平均分 3.10→3.34），但动作质量在 0.46B 后饱和（3.26→3.34 仅微增）。这意味着过去社区盲目堆参数的做法是低效的——真正的瓶颈在数据多样性和语义覆盖，而非模型容量。这一发现直接为后续工作指明了资源分配方向。

## 方法拆解

### 数据管线（核心壁垒）
- **多源采集**：1200 万野外视频片段（经镜头边界检测+人体检测器筛选）→ GVHMR 重建 SMPL-X 轨迹；另加约 500 小时动捕/3D 动画资产。
- **统一处理**：全部重定向至中性 SMPL-H（22 关节无手部），三步过滤（去重、异常姿态/速度/位移检测、脚滑伪影检测），重采样至 30fps，>12 秒序列切分。
- **标注增强**：视频源直接用原始视频，3D 数据渲染成合成视频；Gemini-2.5-Pro 生成初步标注，人工验证高质量子集，LLM 标准化结构并多样化改写。

### 动作表示
每帧向量 f ∈ ℝ²⁰¹：全局根平移 t ∈ ℝ³、全局朝向 r ∈ ℝ⁶、局部关节旋转 jr ∈ ℝ²¹ˣ⁶、局部关节位置 jp ∈ ℝ²²ˣ³。关键决策：**移除速度/脚接触标签**（作者观察到收敛更快），旋转用连续 6D 表示（兼容动画工作流，不同于 HumanML3D）。

### HY-Motion DiT 架构
- **混合 Transformer**：双流块（动作/文本独立 QKV+MLP，联合注意力交互）与单流块（拼接序列，并行空间+通道注意力）交替。
- **非对称注意力掩码**：动作 token 全局关注文本，文本 token 被掩码不关注动作——防止文本表征被动作噪声污染。
- **窄带时间掩码**：注意力限制在 121 帧滑动窗口内（30fps 下约 4 秒），降低长序列计算复杂度。
- **层次化双条件**：Qwen3-8B 提取 token 级语义（经 Bidirectional Token Refiner 转双向），CLIP-L 提供全局嵌入，通过分离的 AdaLN 注入。

### 训练策略
- **流匹配目标**：最优传输路径 x_t = (1−t)x₀ + tx₁，损失 L_FM = E[||v_θ(x_t, c, t) − (x₁−x₀)||²]。
- **三阶段**：3000+ 小时预训练（常数 LR η_pre）→ 400 小时精选微调（LR=0.1η_pre）→ 两阶段 RL（DPO 精选 9228 对 + Flow-GRPO 整合 TMR 语义奖励与脚滑/根漂移物理惩罚）。
- **时长预测**：Qwen3-30B-A3B 两阶段训练（SFT 生成"优化提示+时长"，GRPO 用 Qwen3-235B 评判语义与时间合理性）。

## 关键创新

1. **首次验证动作生成模型的 scaling law**：通过 0.05B/0.46B/1B 三档 DiT 的系统对比，证明指令跟随能力随规模持续提升，但动作质量存在饱和点。这一发现直接指导了"轻量版 Lite（0.46B）"的发布策略——不是所有场景都需要 1B 模型。

2. **数据工程成为第一生产力**：将 LLM 领域的"数据配比+清洗+增强"方法论完整迁移到动作领域。特别是"视频→3D 重建→统一骨架→VLM 标注→人工精修→LLM 改写"的闭环管线，解决了此前动作数据集规模小、标注不一致的根本瓶颈。3000 小时预训练数据是现有公开集（HumanML3D 约 14 小时）的 200 倍量级。

3. **非对称注意力+窄带时间掩码**：这个架构设计看似简单但很实用——文本 token 不关注动作避免了语义表征被动作噪声干扰，而窄带掩码在 121 帧窗口内保持了局部时间连贯性，同时将计算复杂度从 O(T²) 降为 O(T·W)。这是工程上"用掩码换效率"的典型范例。

## 实验与结果


### 与 SOTA 对比（2000+ 提示，人工 1-5 分）

| 模型 | 指令跟随 | SSAE | 动作质量 |
|------|---------|------|---------|
| MoMask | 2.31 | 58.0% | 2.79 |
| GoToZero | 2.19 | 52.7% | 2.86 |
| DART | 2.31 | 42.7% | 3.11 |
| LoM | 2.17 | 48.9% | 3.06 |
| **HY-Motion 1.0** | **3.24** | **78.6%** | **3.43** |

HY-Motion 在指令跟随上的优势（+0.93（由表内数值 2.1÷2.2 计算） vs 最强基线）远大于动作质量（+0.32（由表内数值 1.0→1.0 计算）），说明规模化主要解决的是"理解指令"而非"生成动作"。

### 分类别表现（HY-Motion 最强项）
- 指令跟随：移动类 (a) 3.76 最高，游戏角色 (f) 3.01 最低——复杂角色动作仍是短板。
- 动作质量：移动类 3.59 最佳，健身类 (c) 3.28 最弱。

### 规模消融（表 3/4）

| 模型 | 指令跟随 | 动作质量 |
|------|---------|---------|
| DiT-0.05B | 3.10 | 2.91 |
| DiT-0.46B | 3.20 | 3.26 |
| DiT-0.46B-400h | 3.05 | 3.31 |
| DiT-1B | **3.34** | **3.34** |

关键发现：DiT-0.46B-400h（仅 400h 数据）动作质量 3.31 接近 1B 的 3.34，但指令跟随仅 3.05 远低于 1B 的 3.34——**数据规模对语义理解的影响远大于模型容量**。

## 边界与局限

- **复杂指令仍是硬伤**：作者明确承认对高度详细/组合指令的理解不充分，根因是标注管线难以对细微动作生成完整准确的文本描述——这是数据工程的固有天花板，不是模型架构能单独解决的。
- **人-物交互（HOI）缺失**：当前表示不含物体几何信息，生成持工具、推车等交互动作时无法保证物理接触点准确。这是表示层面的根本限制，不是训练技巧能弥补的。
- **未报告标准基准指标**：论文未提供 HumanML3D 上的 FID、R-Precision 等定量对比，无法与历史工作直接对齐。作者可能有意回避——因为其数据分布和评估协议与 HumanML3D 差异较大。
- **未披露关键工程细节**：训练步数、batch size、硬件配置、推理延迟均未给出，复现门槛较高。

## 工程启示

- **先核对数据管线再谈模型**：HY-Motion 的核心壁垒是 3000 小时清洗数据。如果复现，优先验证 GVHMR 重建质量（脚滑过滤阈值、异常姿态检测参数）和 VLM 标注一致性——这两步决定了后续所有训练的上限。
- **规模选择有讲究**：如果目标是动作质量优先（如影视预演），0.46B Lite 版足够；如果目标是复杂指令跟随（如虚拟人助手），必须上 1B。不要盲目追求大模型，先评估下游任务对语义理解的需求强度。
- **最容易踩坑的地方**：① 移除速度/脚接触标签后，需要确保物理奖励（Flow-GRPO 中的脚滑惩罚）足够强，否则动作平滑性会退化；② 非对称注意力掩码实现时容易出错——文本 token 的 attention mask 必须显式置零，否则训练不稳定；③ 窄带时间掩码的窗口大小（121 帧）与采样率强相关，换采样率必须重新调参。
- **RL 阶段性价比高**：DPO（9228 对）和 Flow-GRPO 带来的提升在论文中虽未单独消融，但从 0.46B-400h 与 0.46B 的对比看，数据量对指令跟随的影响远大于 RL——建议先保证预训练数据质量，再投入 RL 调优。

## 参考
- https://arxiv.org/abs/2512.23464
- https://github.com/Tencent-Hunyuan/HY-Motion-1.0
- https://github.com/ImChong/Robotics_Notebooks

## Overview

HY-Motion 1.0 is the first text-to-motion generation system that scales DiT-based flow matching models to the billion-parameter level, developed by Tencent and other institutions. It constructs a multi-source motion data pipeline exceeding 3,000 hours (wild videos, motion capture, 3D assets), combined with hierarchical dual-condition text encoding and a three-stage training paradigm (pretraining-finetuning-RL), significantly surpassing existing open-source baselines in instruction following (SSAE 78.6%) and motion quality (3.43/5).

## What It Changes

The real shift in this work is moving "data scale" from a supporting role to the spotlight. Previous mainstream approaches in this field (MoMask, GoToZero, LoM) either relied on small-scale annotated datasets like HumanML3D or used discrete tokenizers that sacrificed motion smoothness for semantic diversity. HY-Motion 1.0 demonstrates that when motion data reaches the scale of thousands of hours and undergoes systematic cleaning and annotation, flow matching models with purely continuous representations can simultaneously address the two long-standing pain points of "poor semantic alignment" and "numerous physical artifacts"—it does not invent a new mathematical framework but successfully transfers the "data + scale" paradigm validated in the LLM domain to motion generation.

More critically, it reshapes the industry's understanding of the "motion generation scaling curve." Through ablation studies, the authors reveal a non-trivial finding: instruction-following capability improves monotonically with model scale (0.05B→1B average score 3.10→3.34), but motion quality saturates after 0.46B (3.26→3.34, only marginal gains). This implies that the community's past practice of blindly stacking parameters is inefficient—the real bottleneck lies in data diversity and semantic coverage, not model capacity. This finding directly points subsequent work toward resource allocation priorities.

## Method Breakdown

### Data Pipeline (Core Moat)
- **Multi-source collection**: 12 million wild video clips (filtered via shot boundary detection + human detector) → GVHMR reconstructs SMPL-X trajectories; plus approximately 500 hours of motion capture/3D animation assets.
- **Unified processing**: All data retargeted to neutral SMPL-H (22 joints, no hands), with a three-step filtering process (deduplication, abnormal pose/velocity/displacement detection, foot-sliding artifact detection), resampled to 30fps, and sequences longer than 12 seconds segmented.
- **Annotation enhancement**: Video sources use the original videos directly; 3D data is rendered into synthetic videos; Gemini-2.5-Pro generates initial annotations, human validation on high-quality subsets, LLM standardizes structure and diversifies paraphrases.

### Motion Representation
Per-frame vector f ∈ ℝ²⁰¹: global root translation t ∈ ℝ³, global orientation r ∈ ℝ⁶, local joint rotations jr ∈ ℝ²¹ˣ⁶, local joint positions jp ∈ ℝ²²ˣ³. Key decision: **removal of velocity/foot-contact labels** (authors observed faster convergence), with rotations using continuous 6D representation (compatible with animation workflows, unlike HumanML3D).

### HY-Motion DiT Architecture
- **Hybrid Transformer**: Alternating dual-stream blocks (action/text independent QKV+MLP with joint attention interaction) and single-stream blocks (concatenated sequences, parallel spatial + channel attention).
- **Asymmetric attention mask**: Action tokens attend globally to text; text tokens are masked from attending to actions—preventing text representations from being polluted by motion noise.
- **Narrow-band temporal mask**: Attention restricted to a 121-frame sliding window (~4 seconds at 30fps), reducing computational complexity for long sequences.
- **Hierarchical dual conditioning**: Qwen3-8B extracts token-level semantics (converted to bidirectional via Bidirectional Token Refiner), CLIP-L provides global embeddings, injected through separate AdaLN.

### Training Strategy
- **Flow matching objective**: Optimal transport path x_t = (1−t)x₀ + tx₁, loss L_FM = E[||v_θ(x_t, c, t) − (x₁−x₀)||²].
- **Three stages**: 3000+ hours pretraining (constant LR η_pre) → 400 hours curated finetuning (LR=0.1η_pre) → two-stage RL (DPO on 9,228 curated pairs + Flow-GRPO integrating TMR semantic rewards with foot-sliding/root-drift physical penalties).
- **Duration prediction**: Qwen3-30B-A3B two-stage training (SFT generates "optimized prompt + duration", GRPO uses Qwen3-235B to judge semantic and temporal reasonableness).

## Key Innovations

1. **First validation of scaling laws for motion generation models**: Through systematic comparison of DiT at 0.05B/0.46B/1B scales, it proves that instruction-following capability continuously improves with scale, but motion quality has a saturation point. This finding directly informs the release strategy of the "lightweight Lite (0.46B)" version—not all scenarios require a 1B model.

2. **Data engineering as primary productivity**: Fully transfers the "data mixing + cleaning + augmentation" methodology from the LLM domain to motion. In particular, the closed-loop pipeline of "video → 3D reconstruction → unified skeleton → VLM annotation → human refinement → LLM rewriting" solves the fundamental bottleneck of small-scale and inconsistent annotation in previous motion datasets. The 3,000-hour pretraining data is roughly 200 times the scale of existing public datasets (HumanML3D at ~14 hours).

3. **Asymmetric attention + narrow-band temporal mask**: This architectural design appears simple but is highly practical—text tokens not attending to actions prevents semantic representations from being disturbed by motion noise, while the narrow-band mask maintains local temporal coherence within a 121-frame window while reducing computational complexity from O(T²) to O(T·W). This is a typical example of "trading masks for efficiency" in engineering.

## Experiments and Results

### Comparison with SOTA (2000+ prompts, human rating 1-5)

| Model | Instruction Following | SSAE | Motion Quality |
|-------|----------------------|------|----------------|
| MoMask | 2.31 | 58.0% | 2.79 |
| GoToZero | 2.19 | 52.7% | 2.86 |
| DART | 2.31 | 42.7% | 3.11 |
| LoM | 2.17 | 48.9% | 3.06 |
| **HY-Motion 1.0** | **3.24** | **78.6%** | **3.43** |

HY-Motion's advantage in instruction following (+0.93 (calculated from table values 2.1÷2.2) vs strongest baseline) far exceeds that in motion quality (+0.32 (calculated from table values 1.0→1.0)), indicating that scaling primarily addresses "understanding instructions" rather than "generating motion."

### Category-wise Performance (HY-Motion's Strongest Areas)
- Instruction following: locomotion (a) highest at 3.76, game characters (f) lowest at 3.01—complex character motions remain a weakness.
- Motion quality: locomotion best at 3.59, fitness (c) weakest at 3.28.

### Scale Ablation (Tables 3/4)

| Model | Instruction Following | Motion Quality |
|-------|----------------------|----------------|
| DiT-0.05B | 3.10 | 2.91 |
| DiT-0.46B | 3.20 | 3.26 |
| DiT-0.46B-400h | 3.05 | 3.31 |
| DiT-1B | **3.34** | **3.34** |

Key finding: DiT-0.46B-400h (only 400h data) achieves motion quality of 3.31, close to 1B's 3.34, but instruction following of only 3.05, far below 1B's 3.34—**data scale has a much greater impact on semantic understanding than model capacity**.

## Boundaries and Limitations

- **Complex instructions remain a hard problem**: The authors explicitly acknowledge insufficient understanding of highly detailed/compositional instructions, rooted in the annotation pipeline's difficulty in generating complete and accurate text descriptions for subtle motions—this is an inherent ceiling of data engineering, not something model architecture alone can solve.
- **Missing human-object interaction (HOI)**: The current representation lacks object geometry information, making it impossible to guarantee accurate physical contact points when generating interactions like tool use or cart pushing. This is a fundamental limitation at the representation level, not addressable by training tricks.
- **No standard benchmark metrics reported**: The paper does not provide quantitative comparisons such as FID or R-Precision on HumanML3D, preventing direct alignment with historical work. The authors may have deliberately avoided this—given the significant differences in data distribution and evaluation protocols from HumanML3D.
- **Key engineering details undisclosed**: Training steps, batch size, hardware configuration, and inference latency are all omitted, raising the reproduction barrier.

## Engineering Insights

- **Validate the data pipeline before discussing models**: HY-Motion's core moat is 3,000 hours of cleaned data. For reproduction, prioritize verifying GVHMR reconstruction quality (foot-sliding filtering thresholds, abnormal pose detection parameters) and VLM annotation consistency—these two steps determine the ceiling for all subsequent training.
- **Scale selection matters**: If the goal is motion quality priority (e.g., film previsualization), the 0.46B Lite version suffices; if the goal is complex instruction following (e.g., virtual human assistants), the 1B model is necessary. Do not blindly pursue larger models—first assess the downstream task's demand for semantic understanding.
- **Most common pitfalls**: ① After removing velocity/foot-contact labels, ensure physical rewards (foot-sliding penalties in Flow-GRPO) are strong enough, otherwise motion smoothness degrades; ② Asymmetric attention mask implementation is error-prone—the attention mask for text tokens must be explicitly zeroed, otherwise training becomes unstable; ③ The narrow-band temporal mask's window size (121 frames) is strongly coupled to the sampling rate—changing the sampling rate requires re-tuning.
- **RL stage offers high cost-effectiveness**: Although DPO (9,228 pairs) and Flow-GRPO improvements are not individually ablated in the paper, the comparison between 0.46B-400h and 0.46B suggests that data volume has a much larger impact on instruction following than RL—prioritize pretraining data quality before investing in RL tuning.

## 개요

HY-Motion 1.0은 DiT 기반 플로우 매칭 모델을 10억 파라미터 규모로 확장한 최초의 텍스트-모션 생성 시스템으로, 텐센트 등 기관이 완성했습니다. 3000시간 이상의 다중 소스 모션 데이터 파이프라인(야외 비디오, 모션 캡처, 3D 에셋)을 구축하고, 계층적 이중 조건 텍스트 인코딩과 3단계 훈련(사전 훈련-미세 조정-RL)을 결합하여 명령 수행(SSAE 78.6%) 및 모션 품질(3.43/5)에서 기존 오픈소스 벤치마크를 크게 능가합니다.

## 무엇을 바꾸었는가

이 작업의 진정한 변화는 "데이터 규모"를 조연에서 주연으로 끌어올린 것입니다. 기존 이 분야의 주류 접근 방식(MoMask, GoToZero, LoM)은 HumanML3D와 같은 소규모 주석 데이터셋에 의존하거나, 이산 토크나이저를 사용하여 모션의 부드러움을 희생하면서 의미적 다양성을 확보했습니다. HY-Motion 1.0은 모션 데이터가 수천 시간 규모에 도달하고 체계적으로 정제·주석 처리되었을 때, 순수 연속 표현의 플로우 매칭 모델이 "의미 정렬 불량"과 "물리적 아티팩트 과다"라는 두 가지 장기적 문제를 동시에 해결할 수 있음을 증명했습니다. 새로운 수학적 프레임워크를 발명한 것이 아니라, LLM 분야에서 이미 검증된 "데이터+규모" 경로를 모션 생성에 성공적으로 이식한 것입니다.

더 중요하게는, 이는 업계의 "모션 생성 스케일링 곡선"에 대한 인식을 바꾸었습니다. 저자들은 절제 실험을 통해 비자명한 결론을 밝혔습니다: 명령 수행 능력은 모델 규모에 따라 단조 증가하지만(0.05B→1B 평균 점수 3.10→3.34), 모션 품질은 0.46B 이후 포화됩니다(3.26→3.34로 미미한 증가). 이는 과거 커뮤니티의 맹목적인 파라미터 증가 방식이 비효율적이었음을 의미합니다. 진정한 병목은 데이터 다양성과 의미적 커버리지에 있으며, 모델 용량이 아닙니다. 이 발견은 후속 작업에 자원 할당 방향을 직접적으로 제시합니다.

## 방법 분해

### 데이터 파이프라인 (핵심 장벽)
- **다중 소스 수집**: 1200만 개의 야외 비디오 클립(샷 경계 감지+사람 감지기 필터링) → GVHMR로 SMPL-X 궤적 재구성; 추가로 약 500시간의 모션 캡처/3D 애니메이션 에셋.
- **통합 처리**: 모두 중립 SMPL-H(손 제외 22개 관절)로 리타겟팅, 3단계 필터링(중복 제거, 이상 자세/속도/변위 감지, 발 미끄러짐 아티팩트 감지), 30fps로 리샘플링, 12초 초과 시퀀스 분할.
- **주석 강화**: 비디오 소스는 원본 비디오를 직접 사용하고, 3D 데이터는 합성 비디오로 렌더링; Gemini-2.5-Pro로 초기 주석 생성, 고품질 하위 집합은 수동 검증, LLM으로 구조 표준화 및 다양화 재작성.

### 모션 표현
각 프레임 벡터 f ∈ ℝ²⁰¹: 전역 루트 이동 t ∈ ℝ³, 전역 방향 r ∈ ℝ⁶, 로컬 관절 회전 jr ∈ ℝ²¹ˣ⁶, 로컬 관절 위치 jp ∈ ℝ²²ˣ³. 핵심 결정: **속도/발 접촉 라벨 제거**(저자들은 수렴이 더 빠르다는 것을 관찰), 회전은 연속 6D 표현 사용(애니메이션 워크플로우와 호환, HumanML3D와 다름).

### HY-Motion DiT 아키텍처
- **하이브리드 트랜스포머**: 이중 스트림 블록(모션/텍스트 독립 QKV+MLP, 공동 어텐션 상호작용)과 단일 스트림 블록(시퀀스 연결, 병렬 공간+채널 어텐션)이 교대로 배치.
- **비대칭 어텐션 마스크**: 모션 토큰은 텍스트를 전역적으로 주시하지만, 텍스트 토큰은 마스킹되어 모션을 주시하지 않음 — 텍스트 표현이 모션 노이즈로 오염되는 것을 방지.
- **협대역 시간 마스크**: 어텐션을 121프레임 슬라이딩 윈도우(30fps에서 약 4초)로 제한하여 긴 시퀀스의 계산 복잡도 감소.
- **계층적 이중 조건**: Qwen3-8B가 토큰 수준 의미 추출(Bidirectional Token Refiner를 통해 양방향 전환), CLIP-L이 전역 임베딩 제공, 분리된 AdaLN으로 주입.

### 훈련 전략
- **플로우 매칭 목표**: 최적 전송 경로 x_t = (1−t)x₀ + tx₁, 손실 L_FM = E[||v_θ(x_t, c, t) − (x₁−x₀)||²].
- **3단계**: 3000+시간 사전 훈련(상수 LR η_pre) → 400시간 정제 미세 조정(LR=0.1η_pre) → 2단계 RL(DPO 정제 9228쌍 + Flow-GRPO가 TMR 의미 보상과 발 미끄러짐/루트 드리프트 물리 페널티 통합).
- **지속 시간 예측**: Qwen3-30B-A3B 2단계 훈련(SFT가 "최적화된 프롬프트+지속 시간" 생성, GRPO가 Qwen3-235B로 의미 및 시간 합리성 평가).

## 핵심 혁신

1. **모션 생성 모델의 스케일링 법칙 최초 검증**: 0.05B/0.46B/1B 세 가지 DiT의 체계적 비교를 통해 명령 수행 능력은 규모에 따라 지속적으로 향상되지만, 모션 품질은 포화점이 있음을 증명. 이 발견은 "라이트 버전 Lite(0.46B)" 출시 전략을 직접적으로 안내 — 모든 시나리오에 1B 모델이 필요한 것은 아님.

2. **데이터 엔지니어링이 최우선 생산력**: LLM 분야의 "데이터 배합+정제+증강" 방법론을 모션 분야에 완전히 이식. 특히 "비디오→3D 재구성→통합 스켈레톤→VLM 주석→수동 정제→LLM 재작성"의 폐쇄 루프 파이프라인은 기존 모션 데이터셋의 규모 부족과 주석 불일치라는 근본 병목을 해결. 3000시간 사전 훈련 데이터는 기존 공개 데이터셋(HumanML3D 약 14시간)의 약 200배 규모.

3. **비대칭 어텐션+협대역 시간 마스크**: 이 아키텍처 설계는 단순해 보이지만 매우 실용적 — 텍스트 토큰이 모션을 주시하지 않아 의미 표현이 모션 노이즈로부터 보호되고, 협대역 마스크는 121프레임 윈도우 내에서 로컬 시간 연속성을 유지하면서 계산 복잡도를 O(T²)에서 O(T·W)로 감소. 이는 공학적으로 "마스크로 효율성을 얻는" 전형적인 사례.

## 실험 및 결과

### SOTA 비교 (2000+ 프롬프트, 인간 1-5점)

| 모델 | 명령 수행 | SSAE | 모션 품질 |
|------|---------|------|---------|
| MoMask | 2.31 | 58.0% | 2.79 |
| GoToZero | 2.19 | 52.7% | 2.86 |
| DART | 2.31 | 42.7% | 3.11 |
| LoM | 2.17 | 48.9% | 3.06 |
| **HY-Motion 1.0** | **3.24** | **78.6%** | **3.43** |

HY-Motion의 명령 수행 우위(+0.93 (표 내 값 2.1÷2.2로 계산) vs 최강 기준선)는 모션 품질(+0.32 (표 내 값 1.0→1.0으로 계산))보다 훨씬 크며, 이는 규모화가 주로 "명령 이해"를 해결한다는 것을 의미합니다("모션 생성"이 아니라).

### 카테고리별 성능 (HY-Motion 최강 항목)
- 명령 수행: 이동류 (a) 3.76 최고, 게임 캐릭터 (f) 3.01 최저 — 복잡한 캐릭터 동작은 여전히 약점.
- 모션 품질: 이동류 3.59 최고, 피트니스류 (c) 3.28 최약.

### 규모 절제 (표 3/4)

| 모델 | 명령 수행 | 모션 품질 |
|------|---------|---------|
| DiT-0.05B | 3.10 | 2.91 |
| DiT-0.46B | 3.20 | 3.26 |
| DiT-0.46B-400h | 3.05 | 3.31 |
| DiT-1B | **3.34** | **3.34** |

핵심 발견: DiT-0.46B-400h(400h 데이터만)의 모션 품질 3.31은 1B의 3.34에 근접하지만, 명령 수행은 3.05로 1B의 3.34에 훨씬 못 미침 — **데이터 규모가 의미 이해에 미치는 영향은 모델 용량보다 훨씬 큼**.

## 경계 및 한계

- **복잡한 명령은 여전히 난제**: 저자들은 고도로 상세한/조합적 명령에 대한 이해가 불충분함을 명시적으로 인정. 근본 원인은 주석 파이프라인이 미세한 동작에 대해 완전하고 정확한 텍스트 설명을 생성하기 어렵기 때문 — 이는 데이터 엔지니어링의 고유한 한계이며, 모델 아키텍처만으로 해결할 수 없음.
- **사람-물체 상호작용(HOI) 부재**: 현재 표현에는 물체 기하 정보가 포함되지 않아, 도구 사용, 카트 밀기 등의 상호작용 동작 생성 시 물리적 접촉점 정확성을 보장할 수 없음. 이는 표현 수준의 근본적 제한이며, 훈련 기법으로 보완할 수 없음.
- **표준 벤치마크 지표 미보고**: 논문은 HumanML3D에서의 FID, R-Precision 등의 정량적 비교를 제공하지 않아, 기존 연구와 직접 정렬할 수 없음. 저자들이 의도적으로 회피했을 가능성 — 데이터 분포와 평가 프로토콜이 HumanML3D와 크게 다르기 때문.
- **핵심 엔지니어링 세부 사항 미공개**: 훈련 스텝 수, 배치 크기, 하드웨어 구성, 추론 지연 시간이 모두 제공되지 않아 재현 장벽이 높음.

## 엔지니어링 시사점

- **모델 논의 전에 데이터 파이프라인부터 검증**: HY-Motion의 핵심 장벽은 3000시간 정제 데이터. 재현한다면 GVHMR 재구성 품질(발 미끄러짐 필터 임계값, 이상 자세 감지 파라미터)과 VLM 주석 일관성을 우선 검증 — 이 두 단계가 이후 모든 훈련의 상한을 결정.
- **규모 선택에 신중**: 목표가 모션 품질 우선(예: 영화 프리비즈)이라면 0.46B Lite 버전으로 충분; 목표가 복잡한 명령 수행(예: 가상 인간 어시스턴트)이라면 반드시 1B 사용. 대형 모델을 맹목적으로 추구하지 말고, 먼저 다운스트림 작업의 의미 이해 요구 강도를 평가.
- **가장 함정에 빠지기 쉬운 부분**: ① 속도/발 접촉 라벨 제거 후, 물리 보상(Flow-GRPO의 발 미끄러짐 페널티)이 충분히 강해야 함, 그렇지 않으면 모션 부드러움이 저하; ② 비대칭 어텐션 마스크 구현 시 오류 발생 가능 — 텍스트 토큰의 어텐션 마스크는 명시적으로 0으로 설정해야 하며, 그렇지 않으면 훈련이 불안정; ③ 협대역 시간 마스크의 윈도우 크기(121프레임)는 샘플링 속도와 강하게 연관되므로, 샘플링 속도를 변경하면 재조정 필요.
- **RL 단계의 비용 효율성 높음**: DPO(9228쌍)와 Flow-GRPO의 개선 효과는 논문에서 개별 절제되지는 않았지만, 0.46B-400h와 0.46B의 비교에서 데이터 양이 명령 수행에 미치는 영향이 RL보다 훨씬 큼 — 사전 훈련 데이터 품질을 먼저 보장한 후 RL 튜닝에 투자할 것을 권장.
