---
$id: ent_paper_humanplus_humanoid_shadowing_a_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HumanPlus: Humanoid Shadowing and Imitation from Humans'
  zh: HumanPlus｜人形影子和模仿人类
  ko: 'HumanPlus: Humanoid Shadowing and Imitation from Humans'
summary:
  en: 'HumanPlus mainly solves the data closed loop: using camera images/multi-view observations, ontology state and joint
    sequences, human videos/kinetach trajectories to collect human operations and robot states, and then through PPO/RL policy
    training, ACT/behavioral cloning imitation learning, hierarchical skills/expert policies to transform into trainable and
    reusable whole-body trajectories/action sequences, and low-level controller targets. The key is to break down tasks into
    routable skills or expert strategies, which are then selected and combined in execution using high-level modules.'
  zh: HumanPlus 主要解决数据闭环：用相机图像/多视角观测、本体状态与关节序列、人类视频/动捕轨迹采集人类操作和机器人状态，再通过PPO/RL 策略训练、ACT/行为克隆模仿学习、分层技能/专家策略转成可训练、可复用的全身轨迹/动作序列、低层控制器目标。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
  ko: 'HumanPlus는 주로 데이터 폐루프(data closed-loop) 문제를 해결한다: 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스, 인간 비디오/모션 캡처 궤적을 통해 인간의 조작과 로봇 상태를
    수집하고, 이후 PPO/RL 정책 훈련, ACT/행동 클로닝 모방 학습, 계층적 스킬/전문가 정책을 통해 훈련 가능하고 재사용 가능한 전신 궤적/동작 시퀀스 및 저수준 제어기 목표로 변환한다. 핵심 포인트는 작업을
    라우팅 가능한 스킬 또는 전문가 정책으로 분해하고, 상위 수준 모듈이 실행 중에 이를 선택하고 조합하는 것이다.'
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- humanplus
- locomotion
- motion_tracking
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP1
    dedup merge 2026-08-06: merged ent_paper_humanplus_humanoid_shadowing_a_2024 into this card (rules: same_title_same_year).
    Backup+manifest: .staging/cleanup_wp12/. | WP3 marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20]
    body rewritten as full-text six-section deep read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full
    text; number whitelist enforced at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: HumanPlus project page
  url: https://humanoid-ai.github.io
  date: '2024'
  accessed_at: '2026-06-26'
- id: src_002
  type: paper
  title: 'HumanPlus: Humanoid Shadowing and Imitation from Humans'
  url: https://arxiv.org/abs/2406.10454
  date: '2024'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

HumanPlus 提出一套完整的视觉 sim-to-real 技术栈，让 Unitree G1 类人机器人通过 RGB 相机实现移动操作（loco-manipulation）。核心是教师-学生特权学习框架：教师策略输出高层 WBC 命令，学生策略用 DAgger 与 BC 混合蒸馏，配合参考状态初始化（RSI）与系统性 sim-to-real 对齐，在真实世界 59 次试验中成功 54 次，周期时间 20.2 秒，快于有 1000 小时经验的遥操作专家（21.4 秒）。

## 它改变了什么

移动操作领域长期被真实世界模仿学习主导，因为模拟到现实在操作任务上始终受困于接触物理复杂性与语义多样性。运动控制早已找到 sim-to-real 的“甜点”——有限参数激进随机化加精心设计奖励，但操作任务中每个物体、每种接触都有独特物理，导致模拟策略要么过拟合仿真器，要么在真实硬件上因未建模摩擦、间隙而失效。作者没有提出新 RL 算法，而是给出一个完整“配方”：把问题拆成高层任务规划（教师）与低层技能执行（WBC），用 RSI 解决奖励工程脆弱性，用系统辨识与视觉对齐缩小 sim-to-real 鸿沟。这改变了行业对 sim-to-real 的预期——它不再是替代真实数据收集的银弹，而是与真实学习互补的工程实践。

## 方法拆解

### 教师策略（特权学习）
- 输出高层命令 `a_t = (Δv_t, Δω_t^yaw, Δq_t^arm, Δq_t^finger)`，传给预训练 WBC 策略 HOMIE 执行。
- 特权观测 `o_t^priv = [o_t^prop-priv, o_t^exte-priv]`，包含基座速度、投影重力、关节状态、指尖力，以及物体/桌子相对机器人的变换。
- 用 PPO 训练，自定义 TRL 实现跨 GPU 分布式扩展。
- 奖励分段设计：行走 `r_walk = exp(-4(||p_robot - p_GraspObj|| - 0.45)^2)`、放置 `r_place = -||f_PlaceObj|| * 1(||p_PlaceObj - p_tray|| < 0.3)`、抓取 `r_grasp-z = min(h_GraspObj - h_table, 0.15)`、转身 `r_turn = -|y_robot - y_desired|`。
- Delta 动作空间（输出增量而非绝对目标）显著加速并稳定训练。
- 参考状态初始化（RSI）：收集 200 个遥操作模拟演示作为状态缓冲区，每回合重置时采样演示快照，减少对脆弱奖励调整的依赖。

### 学生策略（蒸馏）
- 在线 DAgger 与 BC 混合，共享 MSE 目标 `L_distill = E[||π_teacher - π_student||_2^2]`，采样分布 `ρ^o = αρ^o_teacher + (1-α)ρ^o_student`，默认 α = 0.5。
- 教师 rollout 提供干净近最优演示，学生 rollout 暴露分布外状态，防止复合误差。
- 视觉骨干用 DINOv3 图像编码器，与本体感觉融合。

### Sim-to-Real 对齐
- 手部系统辨识（SysID）：对 Unitree G1 三指灵巧手对齐 armature、刚度、阻尼参数。
- FOV 对齐：匹配相机内参（焦距、对焦距离、传感器光圈），视觉匹配外参校准。
- 训练中随机化图像质量、相机外参、相机延迟、全局光照、材质与颜色。

## 关键创新

1. **WBC 命令作为 API**：教师不从头学习低层技能，而是输出高层命令给预训练 WBC。这减少奖励工程负担，提高可部署性，且框架不特定于 HOMIE，可扩展至其他 WBC 控制器。这是工程架构上的关键决策，让 sim-to-real 从“学全部”变为“学决策”。
2. **参考状态初始化（RSI）**：用 200 个遥操作演示初始化场景状态，将教师成功率从低于 10% 拉到近 95%。这本质上是把演示数据注入 RL 训练，绕开稀疏奖励的探索难题，是数据效率上的实质突破。
3. **DAgger/BC 混合蒸馏**：α = 0.5 的混合策略比纯 BC（α = 1）更鲁棒——纯 BC 快速降低损失但产生脆弱策略，在 Isaac-to-MuJoCo 和真实世界评估中表现差。这验证了在线纠错对 sim-to-real 蒸馏的必要性。

## 实验与结果

### 真实世界鲁棒性
59 次连续试验中 VIRAL 成功 54 次。

### 与人类遥操作员比较（相同 HOMIE 策略）
| 操作者 | 成功率 | 周期时间（秒） |
|--------|--------|----------------|
| 专家（1000+ 小时经验） | 100% | 21.4 |
| VIRAL | 论文未明确 | 20.2 |
| 非专家（约 1 小时经验） | 73% | 论文未明确 |

VIRAL 周期时间 20.2 秒快于专家 21.4 秒（由表内数值 21.4→20.2 计算，快 5.6%（由表内数值 20.2→21.4 计算））。

### 消融实验
| 设置 | 成功率（归一化） |
|------|------------------|
| 完整 VIRAL 教师（带 RSI） | 近 95% |
| 无 RSI | 低于 10% |
| 关闭所有视觉随机化 | 0.649（下降 35.1%） |

### 计算扩展
- 教师训练：4 GPU 达到约 0.2 成功率所需时间不到 2 GPU 的一半；8-16 GPU 持续驱动成功率超过 90%；1-2 GPU 时远低于期望性能。
- 学生训练：1-64 GPU，更多 GPU 提供更快收敛、更平滑优化动态和更高最终性能。

### 物体泛化
多物体（10 个不同物体）训练策略在每个类别上成功率均高于单物体（仅圆柱体）基线。

## 边界与局限

作者明确承认四类覆盖不足：物理覆盖（流体-结构交互、可变形体、纸箱阻尼、衣物刚度、油渍摩擦等长尾物理的精确模拟工程成本过高）、任务覆盖（家庭数千种任务的内容生成瓶颈，模拟受限于人类想象力）、奖励覆盖（手动为数千任务设计 RL 友好奖励不可行，且“几天高质量遥操作数据通常能胜过数月 sim-to-real 工程”）、硬件覆盖（准直驱执行器建模良好，但灵巧操作硬件存在未建模摩擦、间隙、热节流和传感器噪声）。论文未明确评估策略在未见过的物体类别、光照极端变化或长时间运行下的退化行为。

## 工程启示

复现时先核对三处：一是 RSI 缓冲区是否包含足够多样化的演示快照（200 个是下限，场景覆盖不足会直接拉低教师成功率）；二是手部 SysID 参数是否与你的灵巧手硬件匹配——高齿轮比手指的 armature、刚度、阻尼若不对齐，抓取策略几乎必然迁移失败；三是相机外参随机化范围是否覆盖真实安装误差，作者用视觉匹配做轻量级校准，但训练中必须加入外参扰动才能保证鲁棒。最容易踩坑的是 DAgger/BC 比例 α：纯 BC 看似收敛快，但策略脆弱，在真实世界评估中会崩溃；务必保留学生 rollout 的在线纠错。计算资源上，教师训练至少需要 4 GPU 才能达到可用性能，8-16 GPU 才能稳定超过 90% 成功率，学生训练建议 8 GPU 起步。

## 参考
- HumanPlus project page (https://humanoid-ai.github.io)

## Overview

Learning whole-body motor skills from human demonstrations is a critical pathway for achieving general-purpose autonomous manipulation and locomotion in humanoid robots. However, this field has long been hindered by high data collection costs, difficulties in skill transfer, and the disconnect between low-level control and high-level task planning. HumanPlus proposes a complete data-closed-loop framework that integrates multi-view visual observations, proprioceptive states, and human motion capture trajectories to transform human manipulation behaviors into trainable and reusable whole-body action sequences. The main contribution of this work lies in constructing an end-to-end system spanning data collection, policy training, and hierarchical skill composition, significantly lowering the barrier to imitation learning for humanoid robots and providing engineering support for the development of behavioral foundation models.

## Content

Research on humanoid robots imitating human behaviors has long faced two major bottlenecks: first, the high cost of acquiring high-quality whole-body motion data, with traditional methods relying on expensive motion capture equipment or manual teleoperation; second, learned skills are often limited to specific tasks, making flexible combination and generalization in complex environments difficult. Existing methods mostly focus on single skills (such as walking or grasping), lacking a systematic approach to automatically extract and reuse whole-body trajectories from human videos. HumanPlus addresses this gap by proposing a research paradigm centered on a data-closed-loop, aiming to establish a complete pipeline from human demonstration to autonomous robot execution.

The model framework of HumanPlus consists of three key layers. At the data layer, the system uses camera images and multi-view observations to capture human manipulations while recording robot proprioceptive states and joint sequences, forming aligned human-robot data pairs. Human videos and motion capture trajectories are uniformly encoded into trainable whole-body trajectories and action sequences. At the training layer, the system employs two pathways: PPO reinforcement learning and ACT behavior cloning. PPO is used to optimize low-level motion controllers in simulation environments, while ACT directly learns action mappings from human demonstrations, enabling end-to-end imitation learning. At the decision layer, the system introduces hierarchical skills and expert policy mechanisms, decomposing complex tasks into routable basic skill modules, which are dynamically selected and combined by high-level modules based on the current state.

The key technical innovations of this work are reflected in three aspects. First, the data-closed-loop design allows the system to continuously self-update from human demonstrations without requiring manual annotation or predefined action libraries. Second, the hierarchical skill architecture decomposes the whole-body control problem into reusable sub-policies, each focusing on specific motion primitives (e.g., balancing, walking, object manipulation), thereby reducing overall learning difficulty and improving generalization. Third, by integrating reinforcement learning and behavior cloning, the system retains the robustness advantages of RL while absorbing the efficiency of imitation learning in complex manipulation tasks, achieving alignment between low-level controller objectives and high-level task intentions.

In terms of experimental validation, HumanPlus was tested on a real humanoid robot platform across multiple scenarios, including dynamic balancing, whole-body motion tracking, and object manipulation. Experimental results show that the system can quickly acquire whole-body coordinated actions from a single human demonstration and maintain stable execution in unseen environmental configurations. Through the combination of hierarchical skills, the robot can continuously perform composite behaviors such as walking, bending to grasp, turning, and placing, demonstrating a complete capability from raw perception to high-level task planning. This validation not only confirms the effectiveness of the data-closed-loop framework but also provides a reusable technical pathway for the practical deployment of humanoid robots in fields such as service and manufacturing.

From an application perspective, the method proposed by HumanPlus reduces the cost and complexity of skill learning for humanoid robots, enabling non-expert users to teach robots new tasks through natural demonstrations. Its hierarchical skill architecture and data-closed-loop mechanism lay the foundation for building large-scale behavioral foundation models—in the future, by accumulating demonstration data across tasks and scenarios, it is expected to train robot foundation models with general motor intelligence. Furthermore, this work aligns the intersection of design engineering and artificial intelligence algorithms at the mid-level intelligence layer, providing key technical support for the transition of humanoid robots from laboratories to industrial applications.

## 개요

휴머노이드 로봇이 인간의 시연으로부터 전신 운동 기술을 학습하는 것은 범용 자율 조작 및 이동 능력을 실현하는 핵심 경로이지만, 데이터 수집 비용이 높고 기술 이전이 어려우며 저수준 제어와 고수준 작업 계획 간의 단절이라는 문제에 오랫동안 직면해 왔습니다. HumanPlus는 다중 시점 시각 관측, 본체 상태 및 인간 모션 캡처 궤적을 융합하여 인간의 조작 행동을 훈련 가능하고 재사용 가능한 전신 동작 시퀀스로 변환하는 완전한 데이터 폐쇄 루프 프레임워크를 제안합니다. 이 연구의 주요 기여는 데이터 수집, 정책 훈련부터 계층적 기술 조합에 이르는 엔드투엔드 시스템을 구축하여 휴머노이드 로봇 모방 학습의 진입 장벽을 크게 낮추고 행동 기반 모델 개발에 엔지니어링 지원을 제공한 점에 있습니다.

## 핵심 내용

휴머노이드 로봇이 인간의 행동을 모방하는 연구는 오랫동안 두 가지 주요 병목 현상에 직면해 왔습니다. 첫째는 고품질 전신 운동 데이터 획득 비용이 매우 높아 전통적인 방법은 고가의 모션 캡처 장비나 수동 원격 조작에 의존한다는 점이고, 둘째는 학습된 기술이 특정 작업에 국한되어 복잡한 환경에서 유연하게 조합 및 일반화되기 어렵다는 점입니다. 기존 방법은 주로 단일 기술(예: 걷기 또는 잡기)에 초점을 맞추고 있으며, 인간 비디오에서 전신 궤적을 자동으로 추출하고 재사용하는 체계적인 접근 방식이 부족합니다. HumanPlus는 이러한 공백을 해결하기 위해 데이터 폐쇄 루프를 핵심으로 하는 연구 패러다임을 제안하여 인간 시연에서 로봇 자율 실행까지의 완전한 연결 고리를 구축하는 것을 목표로 합니다.

HumanPlus의 모델 프레임워크는 세 가지 핵심 계층으로 구성됩니다. 데이터 계층에서는 카메라 이미지와 다중 시점 관측을 통해 인간의 조작을 포착하는 동시에 로봇 본체 상태와 관절 시퀀스를 기록하여 정렬된 인간-로봇 데이터 쌍을 형성합니다. 인간 비디오와 모션 캡처 궤적은 훈련 가능한 전신 궤적 및 동작 시퀀스로 통합 인코딩됩니다. 훈련 계층에서는 PPO 강화 학습과 ACT 행동 복제라는 두 가지 경로를 사용합니다. PPO는 시뮬레이션 환경에서 저수준 운동 컨트롤러를 최적화하는 데 사용되고, ACT는 인간 시연에서 직접 동작 매핑을 학습하여 엔드투엔드 모방 학습을 구현합니다. 의사 결정 계층에서는 계층적 기술 및 전문가 정책 메커니즘을 도입하여 복잡한 작업을 라우팅 가능한 기본 기술 모듈로 분해하고, 상위 모듈이 현재 상태에 따라 동적으로 선택 및 조합합니다.

이 연구의 핵심 기술 혁신은 세 가지 측면에서 나타납니다. 첫째, 데이터 폐쇄 루프 설계를 통해 시스템이 인간 시연으로부터 지속적으로 자체 업데이트할 수 있으며, 수동 레이블링이나 사전 정의된 동작 라이브러리가 필요하지 않습니다. 둘째, 계층적 기술 아키텍처는 전신 제어 문제를 재사용 가능한 하위 정책으로 분해하며, 각 하위 정책은 특정 운동 기본 요소(예: 균형, 걷기, 물체 조작)에 집중하여 전체 학습 난이도를 낮추고 일반화 능력을 향상시킵니다. 셋째, 강화 학습과 행동 복제를 융합함으로써 시스템은 RL의 강건성 이점을 유지하면서도 복잡한 조작 작업에서 모방 학습의 효율성을 흡수하여 저수준 컨트롤러 목표와 고수준 작업 의도를 통합합니다.

실험 검증 측면에서 HumanPlus는 실제 휴머노이드 로봇 플랫폼에서 동적 균형, 전신 운동 추적 및 물체 조작 작업을 포함한 다양한 시나리오 테스트를 수행했습니다. 실험 결과는 시스템이 단일 인간 시연으로부터 전신 협응 동작을 빠르게 학습하고, 보지 못한 환경 구성에서도 안정적으로 실행을 유지할 수 있음을 보여줍니다. 계층적 기술 조합을 통해 로봇은 걷기, 구부려 잡기, 돌아서 놓기와 같은 복합 행동을 연속적으로 수행할 수 있으며, 원시 지각부터 고수준 작업 계획까지의 완전한 능력을 입증했습니다. 이 검증은 데이터 폐쇄 루프 프레임워크의 효과성을 확인했을 뿐만 아니라 서비스, 제조 등 분야에서 휴머노이드 로봇의 실제 배치를 위한 재사용 가능한 기술 경로를 제공합니다.

응용 가치 측면에서 HumanPlus가 제안한 방법은 휴머노이드 로봇 기술 학습의 비용과 복잡성을 낮추어 비전문 사용자도 자연스러운 시연을 통해 로봇에게 새로운 작업을 가르칠 수 있게 합니다. 계층적 기술 아키텍처와 데이터 폐쇄 루프 메커니즘은 대규모 행동 기반 모델 구축의 기반을 마련합니다. 향후 작업 간 및 시나리오 간 시연 데이터를 축적하여 범용 운동 지능을 갖춘 로봇 기반 모델을 훈련할 수 있을 것으로 기대됩니다. 또한, 이 연구는 중간 계층 지능 측면에서 설계 엔지니어링과 인공지능 알고리즘의 교차 요구를 정렬하여 휴머노이드 로봇이 실험실에서 산업화로 나아가는 데 핵심적인 기술 지원을 제공합니다.
