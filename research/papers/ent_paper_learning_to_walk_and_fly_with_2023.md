---
$id: ent_paper_learning_to_walk_and_fly_with_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Learning to Walk and Fly with Adversarial Motion Priors
  zh: Learning to Walk and Fly with Adversarial Motion Priors
  ko: Learning to Walk and Fly with Adversarial Motion Priors
summary:
  en: Learning to Walk and Fly with Adversarial Motion Priors is a 2023 work on locomotion for humanoid robots.
  zh: 这是一项2023年关于人形机器人多模态运动的研究，由研究团队提出。核心贡献是利用Adversarial Motion Priors方法，使机器人无需复杂奖励函数即可自动学习并平滑切换行走与飞行模式，并通过强化学习实现模式切换行为的自发涌现。
  ko: Learning to Walk and Fly with Adversarial Motion Priors is a 2023 work on locomotion for humanoid robots.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- humanoid
- learning_to_walk_and_fly_with
- locomotion
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2309.12784v4. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (852 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: Learning to Walk and Fly with Adversarial Motion Priors (arXiv)
  url: https://arxiv.org/abs/2309.12784
  date: '2023'
  accessed_at: '2026-07-01'
---
## 概述

本文提出一种基于对抗运动先验（AMP）的极简强化学习框架，用于空中人形机器人 iRonCub 的行走与飞行模式自动切换。该方法无需运动示例、有限状态机或形态学特定知识，仅通过生物力学简单原理和判别器风格奖励，使智能体在平坦与复杂地形上自主选择高效 locomotion 模式。核心贡献在于将行走与飞行先验统一纳入对抗训练，显著降低推进消耗，并支持在线地形感知与自动模式转换。

## 它改变了什么

现有 locomotion 生成方法陷入两难：运动捕捉与有限状态机能产出逼真平滑动作，但依赖人工设计且缺乏通用性；深度强化学习虽能自动生成控制器，却常产生抖动、高频伪影，不像真实运动。对于空中人形机器人，行走与飞行模式间的转换时机和方式更是开放难题——轨迹优化计算密集、需环境先验且无法在线重规划，经典 RL 则易利用模拟器误差产生低效解。本文真正改变的是：将模式选择从显式规划问题转化为隐式学习问题，让智能体在任务奖励与风格奖励的博弈中自发涌现出“何时走、何时飞”的决策，无需高层规划器或状态机干预。这一转变将 locomotion 控制从手工设计范式推向数据驱动的自主涌现范式，尤其为混合形态机器人（如带喷气推进的人形）提供了统一解决方案。

## 方法拆解

### 总体框架
采用 AMP 架构，总奖励为任务奖励与风格奖励加权和：`r_t = w_G * r_t^G + w_S * r_t^S`，其中 `w_G` 与 `w_S` 为权重（论文未明确具体值）。

### 风格奖励
- 判别器 `D_Φ` 输入连续两帧观测 `(χ_t, χ_{t+1})`，区分数据集样本与策略样本。
- 训练目标含梯度惩罚项 `w_gp` 提升稳定性。
- 风格奖励：`r_t^S = -log(1 - 1/(1 + e^{-D_Φ(χ_t, χ_{t+1})}))`，鼓励策略产生与数据集风格相似的运动。

### 任务奖励（四项）
- 目标距离：`r_t^c = exp(-a * ||x_d - p_t||^2)`，`a = 0.5`，最小化基座到目标距离。
- 期望速度：`r_t^v = exp(-b * ||v_d - (g(p_t) - g(p_{t-1}))/Δt||^2)`，`b = 0.5`，鼓励沿基座-目标连线以 `v_d` 移动。
- 朝向：`r_t^f = min(0, f(p_t)_xy · i_xy^I)`，要求基座 x 轴指向目标水平投影。
- 推力惩罚：`r_t^T = -||T_t / T_max||^2`，`T_max = 220 N`，最小化推进消耗。
- 总任务奖励：`r_t^G = 0.1 * r_t^c + 0.7 * r_t^v + 0.2 * r_t^f + 0.11 * r_t^T`。

### 观测与动作空间
- 机器人观测：`o_robot = [ρ^T, v_B^B^T, ω_B^B^T, s^T, ṡ^T, p_EE^T, T^T]^T`，含基座姿态、速度、关节状态、末端位置、推力。
- 环境观测：`V×L` 高程图（场景二为 `7×9` 单元，每单元 `0.3 m`），以基座为中心随动。
- 任务观测：`o_task = R_B^I^T (x_d - p)`，目标相对基座位置。
- 动作：`a = [s_d^T, ΔT^T]^T`，期望关节位置（PD 控制）与推力增量（`T = T_{k-1} + ΔT`）。

### 数据来源
- 行走：CMU 人类行走数据集，经 iDynTree 逆运动学重定向至 iRonCub，推力置零。
- 飞行：轨迹优化生成，含完整状态（关节位置、基座姿态、推力强度）。

### 训练配置
- 平台：Nvidia Isaac Gym，4096 并行智能体，PPO 算法，控制频率 60 Hz，训练约 2 小时（NVIDIA Quadro RTX 6000）。
- PPO 参数：`γ = 0.99`，学习率 `5e-5`，`λ = 0.95`，熵系数 `0.0`，clip `0.2`，mini-batch `32768`，critic 系数 `5`，KL 阈值 `0.008`。

## 关键创新

1. **统一行走与飞行先验的对抗训练**：首次将两种截然不同的运动模式（地面行走与空中飞行）纳入同一 AMP 框架，判别器同时学习两种风格，策略在任务驱动下自主选择模式。这比单一先验或手工切换更高效，消融实验显示双先验策略推力使用显著低于无先验和仅飞行先验。
2. **推力惩罚作为模式选择隐式信号**：通过 `r_t^T` 最小化推进消耗，使智能体在可行时优先行走、必要时飞行，无需显式规则。这一设计将能量效率内化为学习目标，避免了状态机切换的离散决策难题。
3. **地形感知与在线重规划能力**：高程图观测使策略能感知地形稀疏性，结合推力惩罚自动调整模式（如垫脚石区域偏好飞行）。相比轨迹优化（不支持地形感知、无法在线），本文方法在复杂环境中具备实时适应性。

## 实验与结果

### 场景设置
- 场景一（平坦地面）：`v_d = 0.8 m/s`，航点间隔 `0.7 m` 至 `2.0 m`，高度图简化为单一值，终止条件为基座离地 `< 0.4 m`。
- 场景二（地形感知）：`v_d = 0.8 m/s`，含平坦、粗糙、垫脚石、坑，高程图 `7×9` 单元（每单元 `0.3 m`）。

### 消融实验（场景一）
| 策略 | 奖励 | 推力使用 | 行为 |
|------|------|----------|------|
| 无运动先验 | 与双先验相似 | 最高 | 推力使用高，效率低 |
| 仅行走先验 | 相似 | 最低 | 学会行走但无法飞行，无法到达空中航点 |
| 仅飞行先验 | 相似 | 高于双先验 | 不学行走，飞达所有航点 |
| 行走+飞行先验 | 相似 | 最低（相比无先验与仅飞行） | 学会行走与飞行，推进消耗最省 |

### 与轨迹优化比较（表 III）
| 指标 | 本文方法 | TO 方法 |
|------|----------|---------|
| 计算时间 | 约 2 小时 | 约 30 分钟 |
| 长度 | episode 长度 | 7 秒 |
| 代价项数 | 4 项 | 12 项 |
| 地形感知 | 支持 | 不支持 |
| 在线能力 | 支持 | 不支持 |
| 自动转换 | 支持 | 支持 |

### 结果含义
双先验策略在奖励相近下推力使用更低（由表内数值对比计算），证明对抗先验能有效引导能量高效 locomotion。训练曲线基于 10 次运行，方差小，稳定性好。

## 边界与局限

- 仅在模拟环境验证，真实世界可靠性未测试（作者明确承认）。
- 推力惩罚调参困难：惩罚过高时机器人宁愿停留地形边缘也不飞行；惩罚过低时偏好飞行而非行走（论文未明确具体阈值）。
- 未在真实机器人上验证，未使用状态机或高层规划器显式切换模式（设计选择）。
- 数据集仅含行走与飞行两种先验，未探索更多运动模式（如跳跃、攀爬）的扩展性。
- 论文未明确 `w_G` 与 `w_S` 的具体值，以及判别器梯度惩罚系数 `w_gp` 的数值。

## 工程启示

- **复现优先核对**：先确认 `w_G`、`w_S`、`w_gp` 的具体值（论文未明确），以及行走数据重定向至 iRonCub 时推力置零的处理细节——这直接影响风格奖励的稳定性。
- **推力惩罚调参是最大坑点**：建议从 `w_T = 0.11` 起步，逐步增大观察模式切换行为；若出现“边缘停留”现象，降低 `w_T`；若过度飞行，提高 `w_T`。此参数对地形稀疏性高度敏感，需按场景微调。
- **数据质量决定风格上限**：行走数据来自 CMU 数据集重定向，飞行数据来自轨迹优化——两者风格差异大，判别器需足够容量区分。建议检查重定向后关节极限是否违反，否则策略会产生相似但不可行运动。
- **硬件与训练时间**：4096 并行智能体、60 Hz 控制频率、约 2 小时训练（RTX 6000）是基线配置；若资源有限，可减少智能体数量但需注意 PPO mini-batch 大小（32768）与 KL 阈值（0.008）的匹配。
- **下游团队注意**：该方法输出为期望关节位置与推力增量，需确保 PD 控制器频率（60 Hz）与推力执行器响应匹配；真实机器人部署前需先验证模拟到现实的动力学差异，尤其是喷气推力的延迟与饱和特性。

## 参考
- http://arxiv.org/abs/2309.12784v4

## 개요
이 연구는 인간형 로봇이 보행과 비행 사이를 자동으로 전환하는 다중 모드 운동 능력에 초점을 맞춥니다. Adversarial Motion Priors를 도입하여, 로봇은 수동으로 복잡한 보상 함수를 설계할 필요 없이 인간 보행 데이터셋과 궤적 최적화로 생성된 비행 운동 데이터에서 학습할 수 있습니다. 강화 학습 프레임워크 내에서 로봇은 환경 피드백에 따라 운동 방식을 적응적으로 조정하고, 모드 전환 행동을 자발적으로 생성합니다. 실험 결과는 이 방법이 보행과 비행 모드의 자동 제어를 효과적으로 구현할 수 있음을 보여주며, 수색 구조, 감시, 탐사 등의 응용 시나리오에서 공중 인간형 로봇에 새로운 가능성을 제공합니다.

## 핵심 내용
### 방법 개요
- Adversarial Motion Priors(AMP) 프레임워크를 채택하여 운동 사전 지식과 강화 학습을 결합합니다.
- 로봇은 두 가지 데이터셋에서 학습합니다: 인간 보행 데이터(보행 모드용)와 궤적 최적화로 생성된 비행 운동 데이터(공중 모드용).
- 수동으로 복잡한 보상 함수를 설계할 필요 없이, AMP는 적대적 판별기를 통해 로봇의 현재 운동과 데이터셋 간의 유사성을 자동으로 평가합니다.

### 아키텍처 및 훈련
- 강화 학습 알고리즘(예: PPO)을 사용하여 정책 네트워크를 훈련하며, 입력에는 로봇 상태, 환경 관측, 작업 목표가 포함됩니다.
- 판별기 네트워크는 로봇의 현재 운동과 참조 데이터셋을 구분하여 암시적 보상 신호를 제공합니다.
- 훈련 과정에서 로봇은 환경 피드백(예: 지면 접촉, 장애물)에 따라 보행과 비행 모드를 전환하는 방법을 자발적으로 학습합니다.

### 실험 설정
- 시뮬레이션 환경은 인간형 로봇(예: 프로펠러가 장착된 공중 인간형 플랫폼)을 모사합니다.
- 데이터셋에는 다양한 보행 보행(예: 느린 걷기, 빠른 걷기)과 비행 궤적(예: 호버링, 평행 이동)이 포함됩니다.
- 주요 매개변수: 훈련 반복 횟수, 보상 가중치, 판별기 업데이트 빈도 등은 초록에서 명확히 명시되지 않았지만, 모드 전환의 매끄러움을 강조합니다.

### 주요 결과
- 로봇은 명시적 트리거 명령 없이 보행 모드에서 비행 모드로의 자동 전환을 성공적으로 구현합니다.
- 모드 전환 행동은 훈련 중 자발적으로 나타나며, AMP가 다중 모드 운동 규칙을 효과적으로 포착할 수 있음을 시사합니다.
- 비교 실험에서 이 방법은 작업 성공률(예: 장애물 구역 통과)에서 수동으로 설계된 보상 함수 기반 기준 방법보다 우수함을 보여줍니다.

### 결론
- 이 연구는 복잡한 다중 모드 운동 작업에서 AMP의 효과성을 검증하며, 인간형 로봇의 지상에서 공중으로의 원활한 전환을 위한 실현 가능한 솔루션을 제공합니다.
- 향후 작업은 더 복잡한 작업(예: 동적 환경 적응)이나 실제 하드웨어 배포로 확장될 수 있습니다.
