---
$id: ent_paper_wococo_learning_whole_body_hum_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'WoCoCo: Learning Whole-Body Humanoid Control with Sequential Contacts'
  zh: WoCoCo｜通过顺序接触学习全身人形控制
  ko: 'WoCoCo: Learning Whole-Body Humanoid Control with Sequential Contacts'
summary:
  en: WoCoCo first recovers scene, target or motion representations from ontology state and joint sequences, human video/motion
    capture trajectories, and simulation interaction data, and then uses PPO/RL strategy training, AMP/motion priors, diffusion
    strategy/flow matching to generate whole-body trajectories/action sequences, end effectors/wrist targets. The key point
    is to view action generation as a conditional generation problem and use diffusion or flow matching to sample executable
    trajectories in a multimodal action distribution.
  zh: WoCoCo 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、AMP/运动先验、扩散策略/流匹配生成全身轨迹/动作序列、末端执行器/腕手目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: WoCoCo 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、AMP/运动先验、扩散策略/流匹配生成全身轨迹/动作序列、末端执行器/腕手目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- human_video
- interaction_planning
- motion_capture
- motion_retargeting
- wococo
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: WoCoCo project page
  url: https://lecar-lab.github.io/wococo/
  date: '2024'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人在复杂环境中执行全身协调运动，如行走、抓取或搬运，面临的核心挑战在于如何从多模态数据中学习并生成兼具物理可行性与任务适应性的动作序列。现有方法往往依赖于单一数据源或简化接触模型，难以处理现实世界中频繁发生的顺序接触（sequential contacts）问题。WoCoCo 提出了一种全新的学习框架，通过融合本体状态、人类视频/动捕轨迹以及仿真交互数据，将动作生成建模为条件生成问题，并利用扩散模型或流匹配技术从多模态动作分布中采样可执行轨迹，从而显著提升了人形机器人全身控制的鲁棒性与泛化能力。

## 核心内容

人形机器人全身控制的研究背景可追溯至经典的运动规划与最优控制理论，但传统方法在面对高自由度、非连续接触动力学以及复杂环境交互时，往往陷入维数灾难或局部最优。近年来，基于强化学习（RL）和模仿学习的方法取得了显著进展，例如通过 PPO 算法训练策略网络，或利用对抗运动先验（AMP）从人类数据中提取运动风格。然而，这些方法通常假设动作分布是单模态的，且对接触事件的建模较为粗糙，导致在需要顺序接触的任务（如爬楼梯、开门、搬运长物体）中，机器人容易产生不连贯或物理上不合理的动作。

WoCoCo 的核心框架由三个关键模块构成。首先，系统从本体状态与关节序列、人类视频或动捕轨迹、以及仿真交互数据中分别恢复场景、目标或运动表征，形成统一的多模态特征空间。这一过程涉及运动重定向（motion retargeting）与交互规划（interaction planning）的深度融合，确保不同来源的数据在表征层面具有可比性。其次，框架采用 PPO/RL 策略训练作为基础控制器，同时引入 AMP 运动先验来约束生成动作的人类相似性，避免机器人产生非自然的姿态。最后，WoCoCo 将动作生成视为一个条件生成问题，利用扩散策略或流匹配方法在由多模态数据定义的动作分布中采样，从而生成全身轨迹与末端执行器（如腕手）的目标位姿。

在技术创新方面，WoCoCo 的核心贡献在于将顺序接触的时序依赖性与条件生成模型相结合。传统扩散策略通常假设动作序列是独立同分布的，而 WoCoCo 通过引入接触事件的时间戳作为条件变量，使模型能够显式地学习接触切换的时机与动力学特性。此外，框架中的流匹配技术相比标准扩散模型，在采样效率与轨迹平滑性上具有优势，尤其适用于需要实时响应的全身控制场景。另一个关键创新在于多模态数据融合策略：WoCoCo 并非简单地将不同数据源拼接，而是通过共享的隐空间编码器与对抗性判别器，自动对齐人类运动与机器人运动之间的动力学差异，从而减少了对精确物理仿真模型的依赖。

实验验证方面，WoCoCo 在多种仿真环境与真实机器人平台上进行了评估，涵盖行走、搬运、操作等典型任务。结果表明，与基线方法（如仅使用 PPO 或 AMP 的变体）相比，WoCoCo 在任务成功率、运动自然度以及接触事件预测准确性上均有显著提升。特别是在需要连续切换接触点的任务（如从地面拾取物体并放置到高架）中，WoCoCo 生成的轨迹展现出更平滑的力过渡与更低的关节扭矩峰值。该框架的应用价值不仅限于实验室场景，还可推广至服务机器人、工业装配以及灾难救援等领域，为人形机器人在非结构化环境中的自主作业提供了可行的技术路径。

## 参考

- WoCoCo project page (https://lecar-lab.github.io/wococo/)

## Overview

The core challenge for humanoid robots performing whole-body coordinated motions in complex environments—such as walking, grasping, or carrying—lies in learning and generating action sequences from multimodal data that are both physically feasible and task-adaptive. Existing methods often rely on a single data source or simplified contact models, making it difficult to handle the sequential contacts frequently occurring in the real world. WoCoCo proposes a novel learning framework that integrates proprioceptive states, human video/motion capture trajectories, and simulation interaction data, modeling action generation as a conditional generation problem. It leverages diffusion models or flow matching techniques to sample executable trajectories from multimodal action distributions, thereby significantly enhancing the robustness and generalization of whole-body control for humanoid robots.

## Content

The research background of whole-body control for humanoid robots traces back to classical motion planning and optimal control theory. However, traditional methods often suffer from the curse of dimensionality or local optima when dealing with high degrees of freedom, discontinuous contact dynamics, and complex environmental interactions. In recent years, methods based on reinforcement learning (RL) and imitation learning have made significant progress, such as training policy networks with the PPO algorithm or extracting motion styles from human data using Adversarial Motion Priors (AMP). Nevertheless, these methods typically assume that action distributions are unimodal and model contact events coarsely, leading to incoherent or physically implausible motions in tasks requiring sequential contacts (e.g., climbing stairs, opening doors, carrying long objects).

The core framework of WoCoCo consists of three key modules. First, the system recovers scene, object, or motion representations from proprioceptive states and joint sequences, human videos or motion capture trajectories, and simulation interaction data, forming a unified multimodal feature space. This process involves deep integration of motion retargeting and interaction planning, ensuring comparability across data sources at the representation level. Second, the framework employs PPO/RL policy training as the base controller while introducing AMP motion priors to constrain the human-likeness of generated actions, preventing unnatural poses. Finally, WoCoCo treats action generation as a conditional generation problem, using diffusion policies or flow matching methods to sample from action distributions defined by multimodal data, thereby generating whole-body trajectories and target poses for end-effectors (e.g., wrists and hands).

In terms of technical innovation, WoCoCo's core contribution lies in combining the temporal dependencies of sequential contacts with conditional generation models. Traditional diffusion policies typically assume that action sequences are independent and identically distributed, whereas WoCoCo introduces contact event timestamps as conditional variables, enabling the model to explicitly learn the timing and dynamics of contact transitions. Additionally, the flow matching technique in the framework offers advantages in sampling efficiency and trajectory smoothness over standard diffusion models, particularly suitable for whole-body control scenarios requiring real-time responses. Another key innovation is the multimodal data fusion strategy: rather than simply concatenating different data sources, WoCoCo uses a shared latent space encoder and adversarial discriminator to automatically align the dynamic differences between human and robot motions, thereby reducing reliance on accurate physical simulation models.

In terms of experimental validation, WoCoCo has been evaluated across various simulation environments and real robot platforms, covering typical tasks such as walking, carrying, and manipulation. Results show that compared to baseline methods (e.g., variants using only PPO or AMP), WoCoCo achieves significant improvements in task success rate, motion naturalness, and contact event prediction accuracy. Particularly in tasks requiring continuous contact transitions (e.g., picking up an object from the ground and placing it on a high shelf), the trajectories generated by WoCoCo exhibit smoother force transitions and lower joint torque peaks. The application value of this framework extends beyond laboratory settings to service robots, industrial assembly, and disaster rescue, providing a feasible technical pathway for humanoid robots to operate autonomously in unstructured environments.

## 개요

휴머노이드 로봇이 걷기, 잡기, 운반과 같은 복잡한 환경에서 전신 협응 동작을 수행할 때 직면하는 핵심 과제는 다중 모달 데이터로부터 물리적 실현 가능성과 작업 적응성을 모두 갖춘 동작 시퀀스를 학습하고 생성하는 방법에 있습니다. 기존 방법은 종종 단일 데이터 소스나 단순화된 접촉 모델에 의존하여 현실 세계에서 빈번하게 발생하는 순차적 접촉(sequential contacts) 문제를 처리하기 어렵습니다. WoCoCo는 본체 상태, 인간 비디오/모션 캡처 궤적, 시뮬레이션 상호작용 데이터를 융합하여 동작 생성을 조건부 생성 문제로 모델링하고, 확산 모델 또는 흐름 매칭 기술을 활용하여 다중 모달 동작 분포에서 실행 가능한 궤적을 샘플링함으로써 휴머노이드 로봇 전신 제어의 강건성과 일반화 능력을 크게 향상시키는 새로운 학습 프레임워크를 제안합니다.

## 핵심 내용

휴머노이드 로봇 전신 제어의 연구 배경은 고전적인 운동 계획 및 최적 제어 이론으로 거슬러 올라갈 수 있지만, 전통적인 방법은 높은 자유도, 불연속 접촉 동역학, 복잡한 환경 상호작용에 직면했을 때 종종 차원의 저주나 지역 최적해에 빠집니다. 최근에는 강화 학습(RL) 및 모방 학습 기반 방법이 상당한 진전을 이루었으며, 예를 들어 PPO 알고리즘을 통해 정책 네트워크를 훈련하거나 적대적 운동 사전(AMP)을 활용하여 인간 데이터에서 운동 스타일을 추출합니다. 그러나 이러한 방법은 일반적으로 동작 분포가 단일 모달이라고 가정하고 접촉 이벤트 모델링이 다소 거칠어, 순차적 접촉이 필요한 작업(예: 계단 오르기, 문 열기, 긴 물체 운반)에서 로봇이 불연속적이거나 물리적으로 타당하지 않은 동작을 생성하기 쉽습니다.

WoCoCo의 핵심 프레임워크는 세 가지 주요 모듈로 구성됩니다. 첫째, 시스템은 본체 상태와 관절 시퀀스, 인간 비디오 또는 모션 캡처 궤적, 시뮬레이션 상호작용 데이터로부터 각각 장면, 목표 또는 운동 표현을 복원하여 통합된 다중 모달 특징 공간을 형성합니다. 이 과정은 운동 재타겟팅(motion retargeting)과 상호작용 계획(interaction planning)의 심층 융합을 포함하여 서로 다른 소스의 데이터가 표현 수준에서 비교 가능하도록 보장합니다. 둘째, 프레임워크는 PPO/RL 정책 훈련을 기본 제어기로 사용하면서 AMP 운동 사전을 도입하여 생성된 동작의 인간 유사성을 제약하고 로봇이 부자연스러운 자세를 생성하는 것을 방지합니다. 마지막으로 WoCoCo는 동작 생성을 조건부 생성 문제로 간주하고, 확산 정책 또는 흐름 매칭 방법을 활용하여 다중 모달 데이터로 정의된 동작 분포에서 샘플링함으로써 전신 궤적과 말단 효과기(예: 손목-손)의 목표 자세를 생성합니다.

기술 혁신 측면에서 WoCoCo의 핵심 기여는 순차적 접촉의 시간적 의존성과 조건부 생성 모델을 결합한 데 있습니다. 전통적인 확산 정책은 일반적으로 동작 시퀀스가 독립적이고 동일하게 분포되어 있다고 가정하지만, WoCoCo는 접촉 이벤트의 타임스탬프를 조건 변수로 도입하여 모델이 접촉 전환의 시기와 동역학 특성을 명시적으로 학습할 수 있도록 합니다. 또한 프레임워크의 흐름 매칭 기술은 표준 확산 모델에 비해 샘플링 효율성과 궤적 평활성에서 장점을 가지며, 특히 실시간 응답이 필요한 전신 제어 시나리오에 적합합니다. 또 다른 핵심 혁신은 다중 모달 데이터 융합 전략입니다. WoCoCo는 단순히 서로 다른 데이터 소스를 연결하는 것이 아니라 공유된 잠재 공간 인코더와 적대적 판별기를 통해 인간 운동과 로봇 운동 간의 동역학 차이를 자동으로 정렬함으로써 정확한 물리 시뮬레이션 모델에 대한 의존성을 줄입니다.

실험 검증 측면에서 WoCoCo는 다양한 시뮬레이션 환경과 실제 로봇 플랫폼에서 평가되었으며, 걷기, 운반, 조작 등 전형적인 작업을 포함합니다. 결과는 기준 방법(예: PPO 또는 AMP 변형만 사용)과 비교하여 WoCoCo가 작업 성공률, 운동 자연스러움, 접촉 이벤트 예측 정확도에서 현저한 향상을 보여줍니다. 특히 접촉점을 연속적으로 전환해야 하는 작업(예: 바닥에서 물체를 집어 올려 높은 곳에 배치)에서 WoCoCo가 생성한 궤적은 더 부드러운 힘 전이와 더 낮은 관절 토크 피크를 나타냅니다. 이 프레임워크의 응용 가치는 실험실 시나리오에 국한되지 않고 서비스 로봇, 산업 조립, 재난 구조 등 분야로 확장될 수 있으며, 비구조화된 환경에서 휴머노이드 로봇의 자율 작업을 위한 실현 가능한 기술 경로를 제공합니다.
