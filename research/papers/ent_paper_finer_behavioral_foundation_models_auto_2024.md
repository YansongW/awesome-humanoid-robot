---
$id: ent_paper_finer_behavioral_foundation_models_auto_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Finer Behavioral Foundation Models via Auto-Regressive Features and Advantage Weighting
  zh: Finer Behavioral Foundation Models via Auto-Regressive Features and Advantage Weighting
  ko: Finer Behavioral Foundation Models via Auto-Regressive Features and Advantage Weighting
summary:
  en: The forward-backward representation (FB) is a recently proposed framework (Touati et al., 2023; Touati & Ollivier, 2021)
    to train behavior foundation models (BFMs) that aim at providing zero-shot efficient policies for any new task specified
    in a given reinforcement learning (RL) environment, without training for each new task.
  zh: 本文针对前向-后向表示（FB）框架的两大核心局限提出改进：一是通过引入自回归特征打破任务编码的线性限制，使细粒度任务特征可依赖粗粒度信息，从而支持任意非线性任务编码；二是将离线RL技术适配至FB框架，解决其在部分数据集（如DMC Humanoid）中性能停滞的问题。改进后的FB模型在D4RL基准测试中达到单任务离线智能体（IQL、XQL）的同等水平，自回归特征在空间精度与任务泛化场景中贡献显著。
  ko: The forward-backward representation (FB) is a recently proposed framework (Touati et al., 2023; Touati & Ollivier, 2021)
    to train behavior foundation models (BFMs) that aim at providing zero-shot efficient policies for any new task specified
    in a given reinforcement learning (RL) environment, without training for each new task.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- finer
- behavioral
- foundation
- models
- auto
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 125 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2412.04368 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2412.04368v1); zh content by DeepSeek from the abstract. Institutions unknown (not in source list). [2026-08-04]
    body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI
    全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。 | WP4 trilingual backfill 2026-08-10: en body retranslated from zh deep-read
    (3458 chars, DeepSeek).'
sources:
- id: src_001
  type: paper
  title: arXiv:2412.04368 Finer Behavioral Foundation Models via Auto-Regressive Features and Advantage Weighting
  url: https://arxiv.org/abs/2412.04368
  accessed_at: '2026-07-31'
  date: '2024-12-05'
- id: src_002
  type: website
  title: 智元、众擎都在卷的人形机器人运控基座：41篇论文看懂BFM
  url: https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g
  accessed_at: '2026-07-31'
---
## 概述

本文提出FB-AWARE，一个结合自回归特征与优势加权的无奖励行为基础模型（BFM），旨在解决vanilla FB框架在任务编码线性限制与离线数据集策略优化上的双重缺陷。作者通过理论证明与D4RL、DMC、Jaco等基准实验，展示了该模型在零样本下游任务上匹配甚至超越单任务离线智能体的性能，并系统分析了自回归特征带来的适度但一致的提升。

## 它改变了什么

FB框架（Touati & Ollivier 2021）的核心假设是任务编码z与特征B之间呈线性关系，这严重限制了其对复杂奖励函数的表达能力——所有基于successor features的方法都受此约束。更关键的是，vanilla FB在由少量近最优示例组成的离线数据集上策略优化表现极差，这与在线RL算法直接用于离线设置时遇到的分布偏移问题如出一辙。本文真正改变的是：**将FB从“线性任务编码+在线探索数据”的窄适用域，推进到“非线性任务流形+混合离线数据”的通用场景**。作者证明了自回归特征能将可优化奖励集合从线性子空间扩展为非线性流形，同时引入advantage weighting使FB能在低质量、混合行为的数据集上稳定训练。这不仅是算法修补，而是对FB理论框架表达能力边界的实质性拓展——尽管实验显示这种拓展在标准基准上带来的收益是“正面但适度”的，但它为BFM在更复杂任务上的应用扫清了理论障碍。

## 方法拆解

### 自回归特征分解
- 将任务编码z与特征B分解为K个块：z=(z₁,…,z_K)，B=(B₁,…,B_K)
- 逐块计算：z₁=𝔼[r(s)B₁(s)]，z₂=𝔼[r(s)B₂(s,z₁)]，z₃=𝔼[r(s)B₃(s,z₁,z₂)]，依此类推
- 定义3.1（自回归特征映射）：B(s,z)的第i个分量只依赖(z₁,…,z_{i-1})，不依赖后续分量
- 网络实现：通过在全连接层上施加掩码，允许单次前向传播计算B(s,z)，每层可访问所有前序块的先前层

### 理论保证
- 定理3.2：若B是自回归的，则不动点z_r=𝔼[r(s)B(s,z_r)]总是存在且可直接逐分量计算，π_{z_r}为最优策略
- 定理A.3：两块自回归FB足以表示任意连续任务编码r↦z_r，而vanilla FB仅限于线性编码

### 优势加权策略优化
- 目标：argmax_θ 𝔼[Σᵢ w(sᵢ,aᵢ)logπ_θ(aᵢ|sᵢ)]，权重w∝exp(A(s,a)/β)
- 关键设计1：对每个样本独立采样zᵢ（而非minibatch单一z），经验上学习更快
- 关键设计2：使用改进加权重要性采样（IWIS），偏差从O(n⁻¹)降至O(n⁻²)
- 关键设计3：训练两个并行前向网络F₁、F₂，Bellman目标用平均值而非min（对后继度量，min的保守性解释不直接）

### 训练细节
- z采样：50%从高斯分布，50%从数据状态B表示，归一化到范数√d
- 自回归FB每32步计算一次B表示（普通FB每步），整个batch使用单一z（k=1）
- 正交正则化损失确保B近似正交矩阵
- 残差自回归归一化策略f_ar：第一个分量自归一化，后续分量残差归一化，保留组间相对幅度

## 关键创新

1. **自回归特征打破线性任务编码限制**：这是对FB理论框架的根本性扩展。vanilla FB中所有策略共享同一特征B(s)，任务编码被限制在特征张成的线性子空间；自回归特征使B(s,z)依赖z，任务编码映射变为非线性流形，理论上可表示任意连续任务编码（定理A.3）。这一创新将FB从“只能处理线性可分解奖励”提升到“可处理任意连续奖励函数”，是表达能力的质变。

2. **优势加权使FB适配离线混合数据**：vanilla FB在离线数据集上策略优化失败的核心原因是分布偏移——训练策略与行为策略不一致导致Q值估计失真。引入advantage-weighted regression（结合IWIS和双网络集成）后，FB-AW在D4RL locomotion上从总分22.3跃升至696.2，接近单任务SOTA XQL（752.9）。这解决了FB从“玩具问题”走向“真实离线基准”的关键工程障碍。

3. **理论完备性与工程可行性的平衡**：作者不仅证明自回归FB的表达能力（定理A.3），还解决了其训练中的实际困难——z采样方差、B归一化、奖励尺度不变性等。特别是残差自回归归一化策略，在无需完整z向量的情况下保持尺度不变性，这是自回归FB能实际训练的关键设计。

## 实验与结果

### Jaco Arm（RND数据集，d=128）
- FB平均：33.3±12.6；FB-AW平均：72.2±11.6；FB-AWARE(4)平均：82.5±12.6；FB-AWARE(8)平均：71.8±11.4
- 自回归特征带来约15%相对提升（82.5 vs 72.2）

### DMC Locomotion（MOOD数据集）
- Humanoid上vanilla FB性能接近零（6.5±5.7），FB-AW提升至596.7±16.9，FB-AWARE(4)为594.8±17.6
- 自回归组件在out-of-dataset任务上提供轻微提升（如quadruped skip：FB-AW 654.6 vs FB-AWARE(4) 835.2）

### 关键结论
- 优势加权是获得任何像样性能的必要条件（"In many setups, the offline techniques are needed to get any decent performance at all"）
- 自回归特征影响“正面但适度”，集中在需要空间精度和任务泛化超出训练集行为的任务上
- FB-AW和FB-AWARE整体性能与XQL、IQL等任务特定SOTA匹配

## 边界与局限

- **自回归效果有限**：作者明确承认自回归特征带来的改进是“positive but moderate”，并非所有任务上都有显著提升，可能表明BFM表达能力并非这些基准任务的关键限制因素
- **环境限制**：所有实验均为无噪声、马尔可夫（无历史依赖）的连续控制环境，未涉及部分可观测环境或随机奖励
- **数据集依赖**：在低质量RND数据集上，advantage weighting反而损害性能（Table 5），最佳算法取决于可用数据质量
- **理论代价**：自回归FB仅在z=z_r时提供Q函数信息，而非自回归FB为所有z提供Q函数——额外表达能力以获取更少关于其他Q函数的信息为代价
- **计算开销**：测试时计算不动点z_r需要多次前向传播通过B网络，但作者称与vanilla FB相比开销很小；训练时单z-per-batch导致方差增加和训练时间更长
- **未提及**：更大规模环境表现、超参数敏感性分析、具体计算成本量化对比

## 工程启示

- **先核对数据质量**：FB-AW在MOOD（混合目标）数据集上表现优异，但在RND（低质量探索）数据集上可能损害性能。复现时务必先评估数据集的探索充分性和行为多样性，再决定是否启用advantage weighting
- **自回归块数选择**：Jaco实验中4块优于8块（82.5 vs 71.8），D4RL用8块。建议从4块开始，根据任务的空间精度需求调整；自回归特征在需要精确goal-reaching的任务上收益最大
- **最易踩坑点**：z采样策略（50%高斯+50%状态B表示）和残差自回归归一化是训练稳定的关键，缺失会导致训练发散或性能平线；整个batch使用单一z（k=1）虽增加方差但收敛性能更好，不要轻易改为k>1
- **双网络集成用平均值而非min**：对后继度量，min的保守性解释不直接（对奖励r取min可能编码min，但对-r则编码max），这是与Q函数集成的关键区别
- **评估协议**：每个任务采样100,000个状态计算任务编码z_r，平均100个episode的累积奖励，重复5个随机种子——复现时严格遵循此协议以保证可比性
- **Humanoid是试金石**：vanilla FB在Humanoid上性能接近零，若你的方法在Humanoid上无法超越此基线，说明离线训练技术未正确实现

## 参考
- https://arxiv.org/abs/2412.04368
- https://mp.weixin.qq.com/s/Ei32la_vo0UW9Y_QCAqB2g

## 개요

본 논문은 FB-AWARE를 제안한다. 이는 자기회귀 특성과 이점 가중치를 결합한 보상 없는 행동 기반 모델(BFM)로, vanilla FB 프레임워크가 가진 작업 인코딩의 선형적 제한과 오프라인 데이터셋에서의 정책 최적화라는 이중 결함을 해결하는 것을 목표로 한다. 저자들은 이론적 증명과 D4RL, DMC, Jaco 등의 벤치마크 실험을 통해, 이 모델이 제로샷 다운스트림 작업에서 단일 작업 오프라인 에이전트와 동등하거나 더 나은 성능을 보임을 입증하고, 자기회귀 특성이 가져오는 적절하지만 일관된 개선을 체계적으로 분석한다.

## 무엇을 바꾸었는가

FB 프레임워크(Touati & Ollivier 2021)의 핵심 가정은 작업 인코딩 z와 특성 B 사이의 선형 관계이며, 이는 복잡한 보상 함수에 대한 표현력을 심각하게 제한한다—모든 successor features 기반 방법이 이 제약을 받는다. 더욱 중요한 것은, vanilla FB가 소수의 근사 최적 예시로 구성된 오프라인 데이터셋에서 정책 최적화 성능이 매우 나쁘다는 점인데, 이는 온라인 RL 알고리즘을 오프라인 설정에 직접 적용할 때 발생하는 분포 이동 문제와 동일하다. 본 논문이 실제로 바꾸는 것은: **FB를 "선형 작업 인코딩 + 온라인 탐색 데이터"라는 좁은 적용 범위에서 "비선형 작업 다양체 + 혼합 오프라인 데이터"라는 일반적인 시나리오로 확장한 것이다**. 저자들은 자기회귀 특성이 최적화 가능한 보상 집합을 선형 부분공간에서 비선형 다양체로 확장할 수 있음을 증명하고, 동시에 이점 가중치를 도입하여 FB가 저품질·혼합 행동 데이터셋에서 안정적으로 훈련될 수 있게 한다. 이는 단순한 알고리즘 패치가 아니라 FB 이론 프레임워크의 표현력 경계에 대한 실질적인 확장이다—실험에서 이러한 확장이 표준 벤치마크에서 "긍정적이지만 적절한" 이득을 가져온다는 것을 보여주지만, 더 복잡한 작업에서 BFM 적용을 위한 이론적 장애물을 제거한다.

## 방법 분해

### 자기회귀 특성 분해
- 작업 인코딩 z와 특성 B를 K개의 블록으로 분해: z=(z₁,…,z_K), B=(B₁,…,B_K)
- 블록별 계산: z₁=𝔼[r(s)B₁(s)], z₂=𝔼[r(s)B₂(s,z₁)], z₃=𝔼[r(s)B₃(s,z₁,z₂)], 이런 식으로 계속
- 정의 3.1(자기회귀 특성 매핑): B(s,z)의 i번째 구성 요소는 (z₁,…,z_{i-1})에만 의존하고 후속 구성 요소에는 의존하지 않음
- 네트워크 구현: 완전 연결 계층에 마스크를 적용하여 단일 순방향 전파로 B(s,z)를 계산할 수 있으며, 각 계층은 모든 이전 블록의 이전 계층에 접근 가능

### 이론적 보장
- 정리 3.2: B가 자기회귀적이면, 고정점 z_r=𝔼[r(s)B(s,z_r)]은 항상 존재하고 직접 구성 요소별로 계산할 수 있으며, π_{z_r}은 최적 정책이다
- 정리 A.3: 두 블록 자기회귀 FB는 임의의 연속 작업 인코딩 r↦z_r을 표현하기에 충분하며, vanilla FB는 선형 인코딩에만 국한된다

### 이점 가중 정책 최적화
- 목표: argmax_θ 𝔼[Σᵢ w(sᵢ,aᵢ)logπ_θ(aᵢ|sᵢ)], 가중치 w∝exp(A(s,a)/β)
- 핵심 설계 1: 각 샘플에 대해 zᵢ를 독립적으로 샘플링(미니배치 단일 z 대신), 경험적으로 학습이 더 빠름
- 핵심 설계 2: 개선된 가중 중요도 샘플링(IWIS) 사용, 편향이 O(n⁻¹)에서 O(n⁻²)로 감소
- 핵심 설계 3: 두 개의 병렬 순방향 네트워크 F₁, F₂를 훈련하고, Bellman 목표는 min 대신 평균값 사용(후속 측도에 대해 min의 보수적 해석은 직접적이지 않음)

### 훈련 세부 사항
- z 샘플링: 50%는 가우시안 분포에서, 50%는 데이터 상태 B 표현에서, 노름 √d로 정규화
- 자기회귀 FB는 32단계마다 B 표현 계산(일반 FB는 매 단계), 전체 배치에 단일 z 사용(k=1)
- 직교 정규화 손실로 B가 근사 직교 행렬이 되도록 보장
- 잔차 자기회귀 정규화 전략 f_ar: 첫 번째 구성 요소는 자기 정규화, 후속 구성 요소는 잔차 정규화, 그룹 간 상대적 크기 보존

## 핵심 혁신

1. **자기회귀 특성이 선형 작업 인코딩 제한을 깨다**: 이는 FB 이론 프레임워크의 근본적 확장이다. vanilla FB에서 모든 정책은 동일한 특성 B(s)를 공유하고 작업 인코딩은 특성이 생성하는 선형 부분공간으로 제한된다; 자기회귀 특성은 B(s,z)가 z에 의존하게 하여 작업 인코딩 매핑이 비선형 다양체가 되고, 이론적으로 임의의 연속 작업 인코딩을 표현할 수 있다(정리 A.3). 이 혁신은 FB를 "선형 분해 가능한 보상만 처리 가능"에서 "임의의 연속 보상 함수 처리 가능"으로 끌어올려 표현력의 질적 변화를 가져온다.

2. **이점 가중치로 FB가 오프라인 혼합 데이터에 적응**: vanilla FB가 오프라인 데이터셋에서 정책 최적화에 실패하는 핵심 원인은 분포 이동—훈련 정책과 행동 정책의 불일치로 Q값 추정이 왜곡되는 것이다. 이점 가중 회귀(IWIS와 이중 네트워크 앙상블 결합)를 도입한 후, FB-AW는 D4RL locomotion에서 총점 22.3에서 696.2로 급등하여 단일 작업 SOTA XQL(752.9)에 근접한다. 이는 FB가 "장난감 문제"에서 "실제 오프라인 벤치마크"로 나아가는 핵심 공학적 장애물을 해결한다.

3. **이론적 완전성과 공학적 실현 가능성의 균형**: 저자들은 자기회귀 FB의 표현력을 증명(정리 A.3)할 뿐만 아니라 훈련 중 실제 어려움—z 샘플링 분산, B 정규화, 보상 스케일 불변성 등을 해결한다. 특히 잔차 자기회귀 정규화 전략은 완전한 z 벡터 없이 스케일 불변성을 유지하며, 이는 자기회귀 FB가 실제로 훈련될 수 있는 핵심 설계이다.

## 실험 및 결과

### Jaco Arm(RND 데이터셋, d=128)
- FB 평균: 33.3±12.6; FB-AW 평균: 72.2±11.6; FB-AWARE(4) 평균: 82.5±12.6; FB-AWARE(8) 평균: 71.8±11.4
- 자기회귀 특성은 약 15% 상대적 개선 제공(82.5 vs 72.2)

### DMC Locomotion(MOOD 데이터셋)
- Humanoid에서 vanilla FB 성능은 거의 0(6.5±5.7), FB-AW는 596.7±16.9로 향상, FB-AWARE(4)는 594.8±17.6
- 자기회귀 구성 요소는 데이터셋 외 작업에서 약간의 개선 제공(예: quadruped skip: FB-AW 654.6 vs FB-AWARE(4) 835.2)

### 핵심 결론
- 이점 가중치는 어떤 괜찮은 성능을 얻기 위한 필수 조건이다("In many setups, the offline techniques are needed to get any decent performance at all")
- 자기회귀 특성의 영향은 "긍정적이지만 적절"하며, 공간 정밀도와 훈련 집합 행동을 초과하는 작업 일반화가 필요한 작업에 집중된다
- FB-AW와 FB-AWARE의 전반적 성능은 XQL, IQL 등 작업 특정 SOTA와 일치한다

## 경계 및 한계

- **자기회귀 효과 제한적**: 저자들은 자기회귀 특성이 가져오는 개선이 "positive but moderate"임을 명시적으로 인정하며, 모든 작업에서 유의미한 향상이 있는 것은 아니다. 이는 BFM 표현력이 이러한 벤치마크 작업의 핵심 제한 요소가 아닐 수 있음을 시사한다
- **환경 제한**: 모든 실험은 노이즈가 없고 마르코프(히스토리 의존 없음)인 연속 제어 환경이며, 부분 관측 가능 환경이나 확률적 보상은 다루지 않음
- **데이터셋 의존성**: 저품질 RND 데이터셋에서 이점 가중치는 오히려 성능을 해친다(Table 5), 최적 알고리즘은 사용 가능한 데이터 품질에 따라 달라진다
- **이론적 비용**: 자기회귀 FB는 z=z_r일 때만 Q 함수 정보를 제공하지만, 비자기회귀 FB는 모든 z에 대해 Q 함수를 제공한다—추가 표현력은 다른 Q 함수에 대한 정보를 덜 얻는 대가로 얻어진다
- **계산 오버헤드**: 테스트 시 고정점 z_r 계산은 B 네트워크를 통한 여러 순방향 전파가 필요하지만, 저자들은 vanilla FB와 비교해 오버헤드가 작다고 주장한다; 훈련 시 배치당 단일 z는 분산 증가와 더 긴 훈련 시간을 초래한다
- **미언급 사항**: 더 큰 규모 환경에서의 성능, 하이퍼파라미터 민감도 분석, 구체적 계산 비용 정량 비교

## 공학적 시사점

- **먼저 데이터 품질 확인**: FB-AW는 MOOD(혼합 목표) 데이터셋에서 우수한 성능을 보이지만 RND(저품질 탐색) 데이터셋에서는 성능을 해칠 수 있다. 재현 시 데이터셋의 탐색 충분성과 행동 다양성을 먼저 평가한 후 이점 가중치 활성화 여부를 결정하라
- **자기회귀 블록 수 선택**: Jaco 실험에서 4블록이 8블록보다 우수(82.5 vs 71.8), D4RL은 8블록 사용. 4블록에서 시작하여 작업의 공간 정밀도 요구에 따라 조정하라; 자기회귀 특성은 정확한 goal-reaching이 필요한 작업에서 가장 큰 이득을 제공한다
- **가장 쉽게 빠지는 함정**: z 샘플링 전략(50% 가우시안 + 50% 상태 B 표현)과 잔차 자기회귀 정규화는 훈련 안정성의 핵심이며, 누락 시 훈련 발산 또는 성능 평탄화가 발생한다; 전체 배치에 단일 z 사용(k=1)은 분산을 증가시키지만 수렴 성능이 더 좋으므로 k>1로 쉽게 변경하지 말라
- **이중 네트워크 앙상블은 min 대신 평균 사용**: 후속 측도에 대해 min의 보수적 해석은 직접적이지 않다(보상 r에 min을 취하면 min을 인코딩할 수 있지만 -r에는 max를 인코딩), 이는 Q 함수 앙상블과의 핵심 차이점이다
- **평가 프로토콜**: 각 작업에 대해 100,000개 상태를 샘플링하여 작업 인코딩 z_r을 계산하고, 100개 에피소드의 누적 보상을 평균하며, 5개 무작위 시드를 반복—재현 시 이 프로토콜을 엄격히 따라 비교 가능성을 보장하라
- **Humanoid는 시금석**: vanilla FB는 Humanoid에서 성능이 거의 0이며, 당신의 방법이 Humanoid에서 이 기준선을 초과하지 못한다면 오프라인 훈련 기술이 제대로 구현되지 않은 것이다

## Overview

This paper proposes FB-AWARE, a reward-free behavior foundation model (BFM) that combines autoregressive features with advantage weighting, aiming to address the dual shortcomings of the vanilla FB framework: the linear limitation of task encoding and poor policy optimization on offline datasets. Through theoretical proofs and benchmark experiments on D4RL, DMC, Jaco, etc., the authors demonstrate that this model matches or even surpasses single-task offline agents on zero-shot downstream tasks, and systematically analyze the modest but consistent improvements brought by autoregressive features.

## What It Changes

The core assumption of the FB framework (Touati & Ollivier 2021) is a linear relationship between task encoding z and features B, which severely limits its expressive power for complex reward functions—all methods based on successor features are subject to this constraint. More critically, vanilla FB performs poorly in policy optimization on offline datasets composed of a small number of near-optimal examples, which is exactly the distribution shift problem encountered when online RL algorithms are directly applied to offline settings. What this paper truly changes is: **advancing FB from the narrow applicability of "linear task encoding + online exploration data" to the general scenario of "nonlinear task manifolds + mixed offline data"**. The authors prove that autoregressive features can expand the optimizable reward set from a linear subspace to a nonlinear manifold, while introducing advantage weighting enables FB to train stably on low-quality, mixed-behavior datasets. This is not merely an algorithmic patch but a substantive expansion of the expressive boundaries of the FB theoretical framework—although experiments show the gains on standard benchmarks are "positive but modest," it clears theoretical obstacles for BFM applications on more complex tasks.

## Method Breakdown

### Autoregressive Feature Decomposition
- Decompose task encoding z and features B into K blocks: z=(z₁,…,z_K), B=(B₁,…,B_K)
- Compute block by block: z₁=𝔼[r(s)B₁(s)], z₂=𝔼[r(s)B₂(s,z₁)], z₃=𝔼[r(s)B₃(s,z₁,z₂)], and so on
- Definition 3.1 (Autoregressive feature map): The i-th component of B(s,z) depends only on (z₁,…,z_{i-1}), not on subsequent components
- Network implementation: By applying masks on fully connected layers, B(s,z) can be computed in a single forward pass, with each layer accessing previous layers of all preceding blocks

### Theoretical Guarantees
- Theorem 3.2: If B is autoregressive, then the fixed point z_r=𝔼[r(s)B(s,z_r)] always exists and can be computed component-wise directly, with π_{z_r} being the optimal policy
- Theorem A.3: Two-block autoregressive FB suffices to represent any continuous task encoding r↦z_r, whereas vanilla FB is limited to linear encodings

### Advantage-Weighted Policy Optimization
- Objective: argmax_θ 𝔼[Σᵢ w(sᵢ,aᵢ)logπ_θ(aᵢ|sᵢ)], with weights w∝exp(A(s,a)/β)
- Key design 1: Sample zᵢ independently for each sample (rather than a single z per minibatch), empirically leading to faster learning
- Key design 2: Use improved weighted importance sampling (IWIS), reducing bias from O(n⁻¹) to O(n⁻²)
- Key design 3: Train two parallel forward networks F₁, F₂, using the average rather than the min for the Bellman target (for successor measures, the conservative interpretation of min is not direct)

### Training Details
- z sampling: 50% from Gaussian distribution, 50% from data-state B representations, normalized to norm √d
- Autoregressive FB computes B representations every 32 steps (vanilla FB every step), using a single z for the entire batch (k=1)
- Orthogonal regularization loss ensures B approximates an orthogonal matrix
- Residual autoregressive normalization strategy f_ar: the first component is self-normalized, subsequent components are residual-normalized, preserving relative magnitudes across groups

## Key Innovations

1. **Autoregressive features break the linear task encoding limitation**: This is a fundamental extension of the FB theoretical framework. In vanilla FB, all policies share the same feature B(s), and task encodings are restricted to the linear subspace spanned by the features; autoregressive features make B(s,z) depend on z, turning the task encoding map into a nonlinear manifold that can theoretically represent any continuous task encoding (Theorem A.3). This innovation elevates FB from "only handling linearly decomposable rewards" to "handling arbitrary continuous reward functions"—a qualitative leap in expressive power.

2. **Advantage weighting adapts FB to offline mixed data**: The core reason vanilla FB fails in policy optimization on offline datasets is distribution shift—the mismatch between training and behavior policies distorts Q-value estimates. With the introduction of advantage-weighted regression (combining IWIS and dual-network ensembles), FB-AW jumps from a total score of 22.3 to 696.2 on D4RL locomotion, approaching the single-task SOTA XQL (752.9). This resolves the key engineering obstacle preventing FB from moving from "toy problems" to "real offline benchmarks."

3. **Balance between theoretical completeness and engineering feasibility**: The authors not only prove the expressive power of autoregressive FB (Theorem A.3) but also address practical training difficulties—z sampling variance, B normalization, reward scale invariance, etc. In particular, the residual autoregressive normalization strategy maintains scale invariance without requiring the full z vector, which is a critical design enabling autoregressive FB to be trained in practice.

## Experiments and Results

### Jaco Arm (RND dataset, d=128)
- FB average: 33.3±12.6; FB-AW average: 72.2±11.6; FB-AWARE(4) average: 82.5±12.6; FB-AWARE(8) average: 71.8±11.4
- Autoregressive features yield approximately 15% relative improvement (82.5 vs 72.2)

### DMC Locomotion (MOOD dataset)
- On Humanoid, vanilla FB performance is near zero (6.5±5.7), FB-AW improves to 596.7±16.9, FB-AWARE(4) reaches 594.8±17.6
- The autoregressive component provides slight improvements on out-of-dataset tasks (e.g., quadruped skip: FB-AW 654.6 vs FB-AWARE(4) 835.2)

### Key Conclusions
- Advantage weighting is necessary to obtain any decent performance ("In many setups, the offline techniques are needed to get any decent performance at all")
- The impact of autoregressive features is "positive but moderate," concentrated on tasks requiring spatial precision and task generalization beyond training-set behavior
- FB-AW and FB-AWARE overall performance matches task-specific SOTA methods such as XQL and IQL

## Boundaries and Limitations

- **Limited autoregressive gains**: The authors explicitly acknowledge that improvements from autoregressive features are "positive but moderate," not significant across all tasks, possibly indicating that BFM expressive power is not the key limiting factor on these benchmark tasks
- **Environment restrictions**: All experiments are in noise-free, Markovian (no history dependence) continuous control environments, without partial observability or stochastic rewards
- **Dataset dependence**: On low-quality RND datasets, advantage weighting actually hurts performance (Table 5); the best algorithm depends on the quality of available data
- **Theoretical cost**: Autoregressive FB provides Q-function information only at z=z_r, whereas non-autoregressive FB provides Q-functions for all z—the additional expressive power comes at the cost of obtaining less information about other Q-functions
- **Computational overhead**: Computing the fixed point z_r at test time requires multiple forward passes through the B network, though the authors claim the overhead is small compared to vanilla FB; during training, single-z-per-batch leads to increased variance and longer training time
- **Not addressed**: Performance on larger-scale environments, hyperparameter sensitivity analysis, quantitative comparison of specific computational costs

## Engineering Insights

- **Check data quality first**: FB-AW performs excellently on MOOD (mixed-goal) datasets but may hurt performance on RND (low-quality exploration) datasets. When reproducing, always assess the exploration sufficiency and behavioral diversity of the dataset before deciding whether to enable advantage weighting
- **Choosing the number of autoregressive blocks**: In Jaco experiments, 4 blocks outperform 8 blocks (82.5 vs 71.8), while D4RL uses 8 blocks. It is recommended to start with 4 blocks and adjust based on the spatial precision requirements of the task; autoregressive features yield the greatest gains on tasks requiring precise goal-reaching
- **Most common pitfalls**: The z sampling strategy (50% Gaussian + 50% state B representations) and residual autoregressive normalization are critical for training stability; omitting them leads to training divergence or performance plateaus. Using a single z for the entire batch (k=1) increases variance but yields better convergence performance—do not casually switch to k>1
- **Use the average rather than the min for dual-network ensembles**: For successor measures, the conservative interpretation of min is not direct (taking min over rewards r may encode min, but for -r it encodes max)—this is a key difference from Q-function ensembles
- **Evaluation protocol**: Sample 100,000 states per task to compute the task encoding z_r, average cumulative reward over 100 episodes, repeated across 5 random seeds—strictly follow this protocol when reproducing to ensure comparability
- **Humanoid is the litmus test**: Vanilla FB performance is near zero on Humanoid; if your method cannot surpass this baseline on Humanoid, the offline training techniques are not correctly implemented
