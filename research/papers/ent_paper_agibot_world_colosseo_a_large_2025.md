---
$id: ent_paper_agibot_world_colosseo_a_large_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'AgiBot World Colosseo: A Large-scale Manipulation Platform for Scalable and Intelligent Embodied Systems'
  zh: AgiBot World Colosseo｜用于可扩展和智能具身系统的大规模操作平台
  ko: 'AgiBot World Colosseo: A Large-scale Manipulation Platform for Scalable and Intelligent Embodied Systems'
summary:
  en: The implementation path of AgiBot World Colosseo is to first encode language instructions, camera images/multi-view
    observations, ontology states and joint sequences into multimodal representations, and then use VLM semantic planning/routing,
    latent variables/action tokens, MM-DiT/Transformer action heads to predict whole-body trajectories/action sequences, end
    effectors/wrist targets, and action chunk/tokens. The key is to break down tasks into routable skills or expert strategies,
    which are then selected and combined in execution using high-level modules.
  zh: AgiBot World Colosseo 的实现路径是先把语言指令、相机图像/多视角观测、本体状态与关节序列编码成多模态表征，再用VLM 语义规划/路由、潜变量/动作 token、MM-DiT/Transformer 动作头预测全身轨迹/动作序列、末端执行器/腕手目标、动作
    chunk/token。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
  ko: AgiBot World Colosseo의 구현 경로는 먼저 언어 명령, 카메라 이미지/다중 시점 관측, 본체 상태 및 관절 시퀀스를 다중 모달 표현으로 인코딩한 다음, VLM
    의미론적 계획/라우팅, 잠재 변수/동작 토큰, MM-DiT/Transformer 동작 헤드를 사용하여 전신 궤적/동작 시퀀스, 말단 실행기/손목-손 목표, 동작 청크/토큰을 예측한다.
    핵심은 작업을 라우팅 가능한 스킬 또는 전문가 정책으로 분해하고, 상위 모듈이 실행 중에 이를 선택하고 조합하는 것이다.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- agibot_world_colosseo
- general_manipulation
- vision_language_action
- vla
- world_model
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry
    metadata. [2026-08-05] body rewritten as full-text six-section deep read (.staging/deep_read batch2,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.
    | WP1 dedup merge 2026-08-06: merged ent_paper_agibot_world_colosseo_a_large_2025 into this card (rules:
    same_title_same_year). Backup+manifest: .staging/cleanup_wp12/. 深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI
    全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。 | WP3 marker fix 2026-08-10: summary.ko translated zh->ko
    (DeepSeek).'
sources:
- id: src_001
  type: website
  title: AgiBot World Colosseo project page
  url: https://github.com/OpenDriveLab/AgiBot-World
  date: '2025'
  accessed_at: '2026-06-26'
- id: src_002
  type: paper
  title: 'AgiBot World Colosseo: A Large-scale Manipulation Platform for Scalable and Intelligent Embodied Systems'
  url: https://arxiv.org/abs/2503.06669
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

AgiBot World Colosseo 是一个大规模真实世界机器人操作平台，由智元机器人（AgiBot）团队构建，包含 100 台同构双人形机器人、4000 平方米设施和超过 100 万条轨迹数据。其核心贡献在于通过标准化采集流程、人类在环质量控制以及配套的 GO-1（ViLLA 框架）策略模型，系统性解决了机器人操作数据碎片化和质量不可控的问题，并展示了数据规模与策略性能之间的幂律缩放关系。

## 它改变了什么

机器人操作领域长期受困于"数据饥渴"与"数据脏乱"的双重困境。现有数据集如 Open X-Embodiment 虽然规模庞大，但本质上是多个实验室数据的简单拼接——异构硬件、非标准化采集协议、缺乏人类反馈的质量控制，导致模型训练时不得不花费大量容量去适应数据分布差异而非学习操作技能本身。DROID 尝试用众包解决规模问题，但固定单臂硬件和无人验证的轨迹质量使其泛化能力受限。AgiBot World 真正改变的是"数据生产"的范式：它不再假设数据是现成的、可被动收集的，而是将数据采集本身视为一个需要工程化设计的核心环节——从硬件同构化、遥操作标准化到人类在环的迭代质量闭环，每一步都在消除数据中的系统性噪声。这种"数据工厂"思路使得后续模型训练可以专注于算法创新而非数据清洗，这是与之前所有数据集最本质的差异。

## 方法拆解

### 数据采集基础设施
- **硬件同构化**：100 台 AgiBot G1 人形机器人，双 7-DoF 手臂 + 移动底盘 + 可调腰部，末端执行器模块化（标准夹爪或 6-DoF 灵巧手），8 摄像头配置（1 RGB-D + 3 鱼眼前置 + 每腕部 RGB-D/鱼眼 + 2 后置鱼眼），30 Hz 同步记录图像与本体感觉。
- **遥操作双通道**：VR 头显（手势→末端执行器位姿→逆运动学→关节角）与全身动捕（人体关节含手指→机器人姿态映射），后者支持单指运动和躯干姿态，为灵巧操作提供数据基础。

### 三阶段采集流程
1. **预采集验证**：确认任务可行性，制定采集标准（如轨迹时长、动作规范性）。
2. **熟练操作员采集**：按标准执行，本地验证完整性后上传云端。
3. **后处理标注**：标注员逐集验证是否符合标准，并附加语言标注。

### 关键设计决策
- **失败恢复数据保留**：约 1% 的轨迹包含操作员出错后恢复并完成的片段，手动标注失败原因和时间戳。作者认为这类数据对策略的失败反思和鲁棒性对齐不可或缺。
- **人类在环迭代**：采集小规模演示→训练策略→部署评估→根据失败模式修订采集协议。例如发现模型动作起始时长时间停顿后，引入后处理步骤消除空闲帧。

### GO-1 模型（ViLLA 框架）三阶段训练
- **Stage 1 潜在动作模型（LAM）**：在互联网规模异构数据（含 Ego4D 人类视频）上训练编码器-解码器。编码器为带因果时间掩码的时空 transformer，解码器为空间 transformer，通过 VQ-VAE 目标量化得到潜在动作 token，码本大小 |C|，token 数 k=4。
- **Stage 2 潜在规划器**：以 InternVL2.5-2B 为 VLM 骨干（24 层 transformer，全双向注意力），输入多视角图像（头部、左腕、右腕）和语言指令，预测潜在动作 token。
- **Stage 3 动作专家**：扩散目标建模低层动作连续分布，解码动作块 A_t = [a_t, ..., a_{t+H}]，H=30。条件包括多视角图像、本体感觉状态 p_t 和语言指令。

设计理由：潜在动作空间比 OpenVLA 的离散低层动作空间小数个数量级，使通用 VLM 能高效适配机器人策略；InternVL2.5-2B 的 2B 参数规模在初步实验中被证明在性能和计算成本间取得平衡。

## 关键创新

1. **数据质量作为一等公民**：AgiBot World 是首个将"人类在环验证"和"失败恢复数据"系统化纳入数据集设计的平台。之前的数据集要么完全依赖自动采集（DROID），要么缺乏质量反馈机制，而这里的三阶段验证流程和失败轨迹标注直接提升了数据信噪比——消融实验显示人工验证的 528 条轨迹比未验证的 482 条提升 0.18 完成分数，证明质量比数量更关键。
2. **潜在动作空间作为 VLM 与机器人策略的桥梁**：GO-1 的 LAM 将高维连续动作压缩为离散潜在 token，使 InternVL2.5-2B 这类通用 VLM 无需修改架构即可用于机器人控制。这一设计避免了 OpenVLA 中直接离散化低层动作导致的语义鸿沟，是"VLM 即策略"路线的重要工程突破。
3. **同构硬件 + 标准化流程的可扩展性验证**：100 台同构机器人采集的数据消除了跨硬件域差异，使得缩放规律研究成为可能——性能与轨迹数呈幂律关系（Pearson r=0.97），这是首个在真实世界机器人操作数据上验证的清晰缩放信号，为后续数据规模决策提供了定量依据。

## 实验与结果

### 数据集对比（表 I）
| 数据集 | 轨迹数 |
|--------|--------|
| Open X-Embodiment | 1.4M（正文提及 >2.4M） |
| **AgiBot World** | **1,001,552** |
| DROID | 76k |
| RoboSet | 98.5k |
| BridgeData V2 | 60.1k |
| RoboMIND | 55k |
| RT-1 | 130k |
| RH20T | 13k |

AgiBot World 是唯一同时具备详细标注、相机标定、双臂、灵巧手、失败恢复和人类在环的数据集。

### 数据集质量对比（图 6）
在 AgiBot World 上预训练的策略：
- 分布内场景：平均完成分数 **0.77** vs OXE 的 **0.47**（+0.30）
- 分布外场景：**0.67** vs **0.38**（+0.29）

### 数据量效率
AgiBot World alpha（约 236 小时）远小于 OXE（约 2000 小时）仍取得更高成功率；仅用 OXE 数据量 1/10 时，预训练策略泛化性提升 **18%**。

### GO-1 对比实验（图 5）
5 个任务 × 30 次试验（10 次已见 + 20 次变化/干扰），GO-1 显著优于 RDT，加入潜在规划器平均提升 **0.12** 任务完成分数。

### 缩放规律（图 7a）
训练轨迹数从 9.2k（alpha 的 10%）到 1M（完整 beta），性能与轨迹数呈幂律缩放，Pearson 相关系数 **r = 0.97**。

### 数据质量消融（图 7b）
"Wipe Table" 任务：人工验证的 528 条轨迹 vs 未验证的 482 条，完成分数提升 **0.18**。

### 评估任务
6 个任务（Restock Bag、Table Bussing、Pour Water、Restock Beverage、Fold Shorts、Wipe Table），每任务 2 个未见场景（位置泛化、视觉干扰、语言泛化），每场景 10 次 rollout 取平均。

## 边界与局限

- **仿真环境缺失**：所有评估均在真实世界进行，模拟环境仍在开发中，这限制了快速可复现的基准测试和消融研究效率。
- **灵巧手任务未单独评估**：虽然硬件支持 6-DoF 灵巧手，但论文未提供灵巧手任务的定量结果，其实际增益尚不明确。
- **多机器人协同未量化**：平台支持协作任务，但未给出多机器人协同的评估数据。
- **失败恢复数据的比例极低**：仅约 1%，其在不同任务类型上的适用性和最优比例未做系统探索。
- **论文未明确**：具体 GPU 型号、训练时长、推理延迟、模型总参数量（除 VLM 骨干 2B 外）等工程细节。

## 工程启示

- **先核对数据质量协议**：如果复现或使用该数据集，务必保留其"人工验证"和"失败恢复"标注字段——消融实验证明这两项对性能贡献显著（+0.18 完成分数），丢弃它们等于浪费数据价值。
- **关注潜在动作空间而非直接微调 VLM**：GO-1 的核心在于 LAM 将动作压缩为 token，直接对低层动作做离散化（如 OpenVLA 路线）会丢失语义信息。复现时应优先复现 LAM 的 VQ-VAE 训练，而非直接跳到 Stage 2/3。
- **缩放规律是双刃剑**：r=0.97 的幂律关系意味着数据量翻倍带来的收益递减，但同时也说明当前数据规模尚未饱和。下游团队应根据自身任务复杂度决定是继续采集数据还是优化模型架构。
- **最容易踩坑的地方**：轨迹时长分布差异极大（30-60 秒为主，部分超 2 分钟），而 DROID/OXE 多为 5-20 秒。如果直接混合训练，需要处理时间尺度不匹配问题；建议按任务类型分组或使用动作块（H=30）时注意时间步对齐。
- **硬件同构性是隐性前提**：所有结论基于 100 台同构机器人。如果下游团队使用异构硬件，数据集的预训练权重可能无法直接迁移，需要额外的域适应步骤。

## 参考
- AgiBot World Colosseo project page (https://github.com/OpenDriveLab/AgiBot-World)

## Overview

AgiBot World Colosseo is a large-scale real-world robot manipulation platform built by the AgiBot team, comprising 100 homogeneous dual-humanoid robots, a 4,000-square-meter facility, and over 1 million trajectory data entries. Its core contribution lies in systematically addressing the issues of fragmented robot manipulation data and uncontrollable quality through standardized collection pipelines, human-in-the-loop quality control, and the accompanying GO-1 (ViLLA framework) policy model, while demonstrating a power-law scaling relationship between data scale and policy performance.

## What It Changes

The robot manipulation field has long been trapped in the dual dilemma of "data hunger" and "data messiness." Existing datasets such as Open X-Embodiment, despite their large scale, are essentially simple concatenations of data from multiple labs—heterogeneous hardware, non-standardized collection protocols, and quality control lacking human feedback force models to expend significant capacity adapting to data distribution differences rather than learning manipulation skills themselves. DROID attempts to solve the scale problem through crowdsourcing, but its fixed single-arm hardware and unverified trajectory quality limit its generalization capability. What AgiBot World truly changes is the paradigm of "data production": it no longer assumes data is readily available and passively collectible, but instead treats data collection itself as a core component requiring engineering design—from hardware homogenization and teleoperation standardization to iterative human-in-the-loop quality loops, each step eliminating systematic noise in the data. This "data factory" approach allows subsequent model training to focus on algorithmic innovation rather than data cleaning, which is the most fundamental difference from all previous datasets.

## Method Breakdown

### Data Collection Infrastructure
- **Hardware Homogenization**: 100 AgiBot G1 humanoid robots, dual 7-DoF arms + mobile base + adjustable waist, modular end effectors (standard gripper or 6-DoF dexterous hand), 8-camera configuration (1 RGB-D + 3 front fisheye + per-wrist RGB-D/fisheye + 2 rear fisheye), synchronized recording of images and proprioception at 30 Hz.
- **Dual-Channel Teleoperation**: VR headset (gesture → end-effector pose → inverse kinematics → joint angles) and full-body motion capture (human joints including fingers → robot pose mapping), with the latter supporting individual finger movement and torso posture, providing the data foundation for dexterous manipulation.

### Three-Stage Collection Pipeline
1. **Pre-collection Validation**: Confirm task feasibility and establish collection standards (e.g., trajectory duration, action规范性).
2. **Skilled Operator Collection**: Execute according to standards, verify completeness locally, then upload to the cloud.
3. **Post-processing Annotation**: Annotators verify episode-by-episode compliance with standards and attach language annotations.

### Key Design Decisions
- **Failure Recovery Data Retention**: Approximately 1% of trajectories include segments where operators made errors, recovered, and completed the task, with failure causes and timestamps manually annotated. The authors argue this data is indispensable for policy failure reflection and robustness alignment.
- **Human-in-the-Loop Iteration**: Collect small-scale demonstrations → train policy → deploy and evaluate → revise collection protocols based on failure modes. For example, after discovering prolonged pauses at action initiation, a post-processing step was introduced to eliminate idle frames.

### GO-1 Model (ViLLA Framework) Three-Stage Training
- **Stage 1 Latent Action Model (LAM)**: Train an encoder-decoder on internet-scale heterogeneous data (including Ego4D human videos). The encoder is a spatiotemporal transformer with causal temporal masking, and the decoder is a spatial transformer, quantizing via VQ-VAE objective to obtain latent action tokens, with codebook size |C| and k=4 tokens.
- **Stage 2 Latent Planner**: Uses InternVL2.5-2B as the VLM backbone (24-layer transformer, fully bidirectional attention), taking multi-view images (head, left wrist, right wrist) and language instructions as input to predict latent action tokens.
- **Stage 3 Action Expert**: Diffusion objective models the continuous distribution of low-level actions, decoding action chunks A_t = [a_t, ..., a_{t+H}] with H=30. Conditions include multi-view images, proprioceptive state p_t, and language instructions.

Design Rationale: The latent action space is orders of magnitude smaller than OpenVLA's discrete low-level action space, enabling general-purpose VLMs to efficiently adapt to robot policies; the 2B parameter scale of InternVL2.5-2B was shown in preliminary experiments to balance performance and computational cost.

## Key Innovations

1. **Data Quality as a First-Class Citizen**: AgiBot World is the first platform to systematically incorporate "human-in-the-loop validation" and "failure recovery data" into dataset design. Previous datasets either relied entirely on automated collection (DROID) or lacked quality feedback mechanisms, whereas the three-stage validation pipeline and failure trajectory annotation here directly improve the data signal-to-noise ratio—ablation experiments show that 528 human-validated trajectories outperform 482 unvalidated ones by 0.18 completion score, proving quality matters more than quantity.
2. **Latent Action Space as a Bridge Between VLM and Robot Policies**: GO-1's LAM compresses high-dimensional continuous actions into discrete latent tokens, allowing general-purpose VLMs like InternVL2.5-2B to be used for robot control without architectural modification. This design avoids the semantic gap caused by directly discretizing low-level actions in OpenVLA, representing a significant engineering breakthrough in the "VLM-as-policy" approach.
3. **Scalability Validation of Homogeneous Hardware + Standardized Pipeline**: Data collected from 100 homogeneous robots eliminates cross-hardware domain differences, making scaling law research possible—performance follows a power-law relationship with trajectory count (Pearson r=0.97), marking the first clear scaling signal validated on real-world robot manipulation data and providing quantitative evidence for future data scale decisions.

## Experiments and Results

### Dataset Comparison (Table I)
| Dataset | Trajectory Count |
|---------|------------------|
| Open X-Embodiment | 1.4M (mentioned as >2.4M in text) |
| **AgiBot World** | **1,001,552** |
| DROID | 76k |
| RoboSet | 98.5k |
| BridgeData V2 | 60.1k |
| RoboMIND | 55k |
| RT-1 | 130k |
| RH20T | 13k |

AgiBot World is the only dataset that simultaneously possesses detailed annotations, camera calibration, dual arms, dexterous hands, failure recovery, and human-in-the-loop validation.

### Dataset Quality Comparison (Figure 6)
Policies pretrained on AgiBot World:
- In-distribution scenarios: average completion score **0.77** vs OXE's **0.47** (+0.30)
- Out-of-distribution scenarios: **0.67** vs **0.38** (+0.29)

### Data Volume Efficiency
AgiBot World alpha (approximately 236 hours) is far smaller than OXE (approximately 2000 hours) yet achieves higher success rates; with only 1/10 of OXE's data volume, pretrained policy generalization improves by **18%**.

### GO-1 Comparative Experiments (Figure 5)
5 tasks × 30 trials (10 seen + 20 varied/disturbed), GO-1 significantly outperforms RDT, with the latent planner adding an average improvement of **0.12** in task completion score.

### Scaling Law (Figure 7a)
Training trajectory counts from 9.2k (10% of alpha) to 1M (full beta), performance scales as a power law with trajectory count, Pearson correlation coefficient **r = 0.97**.

### Data Quality Ablation (Figure 7b)
"Wipe Table" task: 528 human-validated trajectories vs 482 unvalidated ones, completion score improvement of **0.18**.

### Evaluation Tasks
6 tasks (Restock Bag, Table Bussing, Pour Water, Restock Beverage, Fold Shorts, Wipe Table), each with 2 unseen scenarios (position generalization, visual disturbance, language generalization), averaging 10 rollouts per scenario.

## Boundaries and Limitations

- **Missing Simulation Environment**: All evaluations are conducted in the real world; the simulation environment is still under development, limiting the efficiency of rapidly reproducible benchmarks and ablation studies.
- **Dexterous Hand Tasks Not Individually Evaluated**: Although the hardware supports 6-DoF dexterous hands, the paper does not provide quantitative results for dexterous hand tasks, leaving their actual gains unclear.
- **Multi-Robot Collaboration Not Quantified**: The platform supports collaborative tasks, but no evaluation data for multi-robot collaboration is provided.
- **Extremely Low Proportion of Failure Recovery Data**: Only approximately 1%, with no systematic exploration of its applicability across different task types or optimal proportions.
- **Not Explicitly Stated in the Paper**: Specific GPU models, training duration, inference latency, total model parameter count (beyond the 2B VLM backbone), and other engineering details.

## Engineering Insights

- **First Verify Data Quality Protocols**: When reproducing or using this dataset, be sure to retain its "human validation" and "failure recovery" annotation fields—ablation experiments prove these contribute significantly to performance (+0.18 completion score); discarding them wastes data value.
- **Focus on Latent Action Space Rather Than Direct VLM Fine-Tuning**: The core of GO-1 lies in LAM compressing actions into tokens; directly discretizing low-level actions (as in the OpenVLA approach) loses semantic information. When reproducing, prioritize reproducing LAM's VQ-VAE training rather than jumping directly to Stage 2/3.
- **The Scaling Law Is a Double-Edged Sword**: The power-law relationship with r=0.97 implies diminishing returns from doubling data volume, but it also indicates the current data scale has not yet saturated. Downstream teams should decide based on their task complexity whether to continue collecting data or optimize model architecture.
- **Most Likely Pitfall**: Trajectory duration distribution varies greatly (30-60 seconds dominant, some exceeding 2 minutes), whereas DROID/OXE mostly range from 5-20 seconds. If mixing training directly, time-scale mismatches need to be addressed; it is recommended to group by task type or ensure temporal alignment when using action chunks (H=30).
- **Hardware Homogeneity Is an Implicit Prerequisite**: All conclusions are based on 100 homogeneous robots. If downstream teams use heterogeneous hardware, the dataset's pretrained weights may not transfer directly, requiring additional domain adaptation steps.

## 개요

AgiBot World Colosseo는 지유안 로봇(AgiBot) 팀이 구축한 대규모 실제 세계 로봇 조작 플랫폼으로, 100대의 동일한 사양의 휴머노이드 로봇, 4,000제곱미터 시설, 100만 개 이상의 궤적 데이터를 포함합니다. 핵심 기여는 표준화된 수집 프로세스, 인간 개입 품질 관리, 그리고配套 GO-1(ViLLA 프레임워크) 정책 모델을 통해 로봇 조작 데이터의 파편화와 품질 불안정 문제를 체계적으로 해결하고, 데이터 규모와 정책 성능 간의 멱법칙 스케일링 관계를 입증한 데 있습니다.

## 무엇을 바꾸었는가

로봇 조작 분야는 오랫동안 '데이터 기아'와 '데이터 오염'이라는 이중적 곤경에 시달려 왔습니다. Open X-Embodiment와 같은 기존 데이터셋은 규모가 방대하지만, 본질적으로 여러 연구소 데이터의 단순拼接에 불과합니다—이기종 하드웨어, 비표준화 수집 프로토콜, 인간 피드백 기반 품질 관리 부재로 인해 모델 훈련 시 데이터 분포 차이에 적응하는 데 많은 용량을 소비해야 하며, 조작 기술 자체를 학습하는 데 집중하지 못합니다. DROID는 크라우드소싱으로 규모 문제를 해결하려 했지만, 고정된 단일 암 하드웨어와 검증되지 않은 궤적 품질로 인해 일반화 능력이 제한적입니다. AgiBot World가 진정으로 바꾼 것은 '데이터 생산'의 패러다임입니다: 더 이상 데이터가 기성품으로 수동적으로 수집 가능하다고 가정하지 않고, 데이터 수집 자체를 공학적 설계가 필요한 핵심 단계로 간주합니다—하드웨어 동질화, 원격 조작 표준화부터 인간 개입의 반복적 품질 폐루프까지, 각 단계에서 데이터의 체계적 노이즈를 제거합니다. 이러한 '데이터 공장' 접근 방식은 이후 모델 훈련이 데이터 정제가 아닌 알고리즘 혁신에 집중할 수 있게 하며, 이는 이전의 모든 데이터셋과의 가장 본질적인 차이입니다.

## 방법 분해

### 데이터 수집 인프라
- **하드웨어 동질화**: 100대의 AgiBot G1 휴머노이드 로봇, 이중 7-DoF 암 + 이동 베이스 + 조절 가능한 허리, 모듈식 엔드 이펙터(표준 그리퍼 또는 6-DoF 절제 손), 8개 카메라 구성(1 RGB-D + 3 어안 전면 + 각 손목 RGB-D/어안 + 2 후면 어안), 30Hz 동기화 이미지 및 고유 감각 기록.
- **원격 조작 이중 채널**: VR 헤드셋(제스처→엔드 이펙터 포즈→역운동학→관절 각도) 및 전신 모션 캡처(인체 관절 포함 손가락→로봇 자세 매핑), 후자는 단일 손가락 운동과 몸통 자세를 지원하여 절제 조작을 위한 데이터 기반을 제공합니다.

### 3단계 수집 프로세스
1. **사전 수집 검증**: 작업 가능성 확인, 수집 표준(궤적 시간, 동작 규범 등) 수립.
2. **숙련된 운영자 수집**: 표준에 따라 실행, 로컬 검증 완료 후 클라우드 업로드.
3. **후처리 주석**: 주석자가 에피소드별로 표준 충족 여부를 검증하고 언어 주석을 추가.

### 핵심 설계 결정
- **실패 복구 데이터 보존**: 약 1%의 궤적에는 운영자가 오류 후 복구하여 완료한 세그먼트가 포함되며, 실패 원인과 타임스탬프를 수동으로 주석 처리합니다. 저자는 이러한 데이터가 정책의 실패 반성과 견고성 정렬에 필수적이라고 주장합니다.
- **인간 개입 반복**: 소규모 데모 수집→정책 훈련→배포 평가→실패 패턴에 따른 수집 프로토콜 수정. 예를 들어 모델 동작 시작 시 긴 정지가 발견된 후, 유휴 프레임을 제거하는 후처리 단계를 도입했습니다.

### GO-1 모델(ViLLA 프레임워크) 3단계 훈련
- **Stage 1 잠재 동작 모델(LAM)**: 인터넷 규모 이기종 데이터(Ego4D 인간 비디오 포함)에서 인코더-디코더 훈련. 인코더는 인과적 시간 마스크가 있는 시공간 트랜스포머, 디코더는 공간 트랜스포머로, VQ-VAE 목표를 통해 잠재 동작 토큰을 양자화하며, 코드북 크기 |C|, 토큰 수 k=4.
- **Stage 2 잠재 플래너**: InternVL2.5-2B를 VLM 백본(24층 트랜스포머, 전방향 주의)으로 사용, 다중 시점 이미지(머리, 왼쪽 손목, 오른쪽 손목)와 언어 명령을 입력으로 잠재 동작 토큰 예측.
- **Stage 3 동작 전문가**: 확산 목표로 저수준 동작의 연속 분포를 모델링, 동작 블록 A_t = [a_t, ..., a_{t+H}] 디코딩, H=30. 조건에는 다중 시점 이미지, 고유 감각 상태 p_t 및 언어 명령 포함.

설계 근거: 잠재 동작 공간은 OpenVLA의 이산 저수준 동작 공간보다 수 자릿수 작아, 범용 VLM이 로봇 정책에 효율적으로 적응할 수 있게 합니다; InternVL2.5-2B의 2B 파라미터 규모는 초기 실험에서 성능과 계산 비용 간의 균형을 이루는 것으로 입증되었습니다.

## 핵심 혁신

1. **데이터 품질을 일급 시민으로**: AgiBot World는 '인간 개입 검증'과 '실패 복구 데이터'를 체계적으로 데이터셋 설계에 통합한 최초의 플랫폼입니다. 이전 데이터셋은 자동 수집(DROID)에 전적으로 의존하거나 품질 피드백 메커니즘이 부재했지만, 여기의 3단계 검증 프로세스와 실패 궤적 주석은 데이터 신호 대 잡음 비를 직접 향상시킵니다—절제 실험에서 인간 검증된 528개 궤적이 검증되지 않은 482개보다 0.18 완료 점수 향상을 보여, 품질이 수량보다 더 중요함을 입증합니다.
2. **잠재 동작 공간을 VLM과 로봇 정책의 브리지로**: GO-1의 LAM은 고차원 연속 동작을 이산 잠재 토큰으로 압축하여 InternVL2.5-2B와 같은 범용 VLM이 아키텍처 수정 없이 로봇 제어에 사용될 수 있게 합니다. 이 설계는 OpenVLA에서 저수준 동작을 직접 이산화할 때 발생하는 의미론적 격차를 피하며, 'VLM 곧 정책' 경로의 중요한 공학적 돌파구입니다.
3. **동일 하드웨어 + 표준화 프로세스의 확장성 검증**: 100대의 동일 로봇에서 수집된 데이터는 하드웨어 간 도메인 차이를 제거하여 스케일링 법칙 연구를 가능하게 합니다—성능과 궤적 수 간의 멱법칙 관계(Pearson r=0.97)는 실제 세계 로봇 조작 데이터에서 검증된 최초의 명확한 스케일링 신호로, 향후 데이터 규모 결정에 정량적 근거를 제공합니다.

## 실험 및 결과

### 데이터셋 비교(표 I)
| 데이터셋 | 궤적 수 |
|--------|--------|
| Open X-Embodiment | 1.4M(본문에서 >2.4M 언급) |
| **AgiBot World** | **1,001,552** |
| DROID | 76k |
| RoboSet | 98.5k |
| BridgeData V2 | 60.1k |
| RoboMIND | 55k |
| RT-1 | 130k |
| RH20T | 13k |

AgiBot World는 상세 주석, 카메라 캘리브레이션, 이중 암, 절제 손, 실패 복구 및 인간 개입을 동시에 갖춘 유일한 데이터셋입니다.

### 데이터셋 품질 비교(그림 6)
AgiBot World에서 사전 훈련된 정책:
- 분포 내 시나리오: 평균 완료 점수 **0.77** vs OXE의 **0.47**(+0.30)
- 분포 외 시나리오: **0.67** vs **0.38**(+0.29)

### 데이터량 효율성
AgiBot World alpha(약 236시간)는 OXE(약 2000시간)보다 훨씬 작음에도 더 높은 성공률을 달성; OXE 데이터량의 1/10만 사용했을 때 사전 훈련 정책 일반화 성능 **18%** 향상.

### GO-1 비교 실험(그림 5)
5개 작업 × 30회 시도(10회 기존 + 20회 변형/간섭), GO-1이 RDT보다 유의미하게 우수, 잠재 플래너 추가 시 평균 **0.12** 작업 완료 점수 향상.

### 스케일링 법칙(그림 7a)
훈련 궤적 수가 9.2k(alpha의 10%)에서 1M(전체 beta)까지 증가할 때, 성능과 궤적 수 간 멱법칙 스케일링, Pearson 상관 계수 **r = 0.97**.

### 데이터 품질 절제(그림 7b)
'Wipe Table' 작업: 인간 검증된 528개 궤적 vs 검증되지 않은 482개, 완료 점수 **0.18** 향상.

### 평가 작업
6개 작업(Restock Bag, Table Bussing, Pour Water, Restock Beverage, Fold Shorts, Wipe Table), 각 작업당 2개 미경험 시나리오(위치 일반화, 시각적 간섭, 언어 일반화), 각 시나리오당 10회 rollout 평균.

## 경계 및 한계

- **시뮬레이션 환경 부재**: 모든 평가는 실제 세계에서 수행되었으며, 시뮬레이션 환경은 개발 중입니다. 이는 빠른 재현 가능한 벤치마크 테스트와 절제 연구 효율성을 제한합니다.
- **절제 손 작업 별도 평가 없음**: 하드웨어가 6-DoF 절제 손을 지원하지만, 논문은 절제 손 작업의 정량적 결과를 제공하지 않아 실제 이점이 명확하지 않습니다.
- **다중 로봇 협업 미정량화**: 플랫폼이 협업 작업을 지원하지만, 다중 로봇 협업 평가 데이터는 제공되지 않았습니다.
- **실패 복구 데이터 비율 극히 낮음**: 약 1%에 불과하며, 다양한 작업 유형에서의 적용 가능성과 최적 비율은 체계적으로 탐구되지 않았습니다.
- **논문에서 명시되지 않음**: 구체적인 GPU 모델, 훈련 시간, 추론 지연 시간, 모델 총 파라미터 수(VLM 백본 2B 제외) 등의 공학적 세부 사항.

## 공학적 시사점

- **데이터 품질 프로토콜 먼저 확인**: 이 데이터셋을 재현하거나 사용할 경우, '인간 검증' 및 '실패 복구' 주석 필드를 반드시 유지하세요—절제 실험에서 이 두 항목이 성능에 유의미한 기여(+0.18 완료 점수)를 하며, 이를 버리는 것은 데이터 가치를 낭비하는 것입니다.
- **잠재 동작 공간에 주목, VLM 직접 미세 조정 지양**: GO-1의 핵심은 LAM이 동작을 토큰으로 압축하는 것이며, 저수준 동작을 직접 이산화(OpenVLA 경로)하면 의미 정보가 손실됩니다. 재현 시 Stage 2/3로 건너뛰지 말고 LAM의 VQ-VAE 훈련을 우선 재현해야 합니다.
- **스케일링 법칙은 양날의 검**: r=0.97의 멱법칙 관계는 데이터량이 두 배가 되어도 수익이 감소함을 의미하지만, 동시에 현재 데이터 규모가 아직 포화되지 않았음을 시사합니다. 하류 팀은 자체 작업 복잡도에 따라 데이터 수집을 계속할지 모델 아키텍처를 최적화할지 결정해야 합니다.
- **가장 함정에 빠지기 쉬운 부분**: 궤적 시간 분포 차이가 매우 큽니다(30-60초가 주, 일부 2분 초과), 반면 DROID/OXE는 대부분 5-20초입니다. 직접 혼합 훈련 시 시간 스케일 불일치 문제를 처리해야 하며, 작업 유형별 그룹화 또는 동작 블록(H=30) 사용 시 시간 단계 정렬에 주의해야 합니다.
- **하드웨어 동질성은 암묵적 전제**: 모든 결론은 100대의 동일 로봇을 기반으로 합니다. 하류 팀이 이기종 하드웨어를 사용할 경우, 데이터셋의 사전 훈련 가중치가 직접 전이되지 않을 수 있으며 추가 도메인 적응 단계가 필요합니다.
