---
$id: ent_paper_li_qdepth_vla_quantized_depth_pre_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'QDepth-VLA: Quantized Depth Prediction as Auxiliary Supervision for Vision-Language-Action Models'
  zh: QDepth-VLA
  ko: 'QDepth-VLA: Quantized Depth Prediction as Auxiliary Supervision for Vision-Language-Action Models'
summary:
  en: 'QDepth-VLA: Quantized Depth Prediction as Auxiliary Supervision for Vision-Language-Action Models (QDepth-VLA), is
    a 2025 large vision-language-action model for robotic manipulation, introduced by School of Artificial Intelligence, University
    of the Chinese Academy of Sciences, Institute of Automation, Chinese Academy of Science, Beijing Zhongke Huiling Robot
    Technology Co.'
  zh: QDepth-VLA 是由中国科学院大学人工智能学院、中国科学院自动化研究所及北京中科慧灵机器人技术有限公司于2025年提出的视觉-语言-动作大模型，用于机器人精细操作。其核心贡献在于引入辅助深度预测任务，通过VQ-VAE编码器预测量化深度潜变量令牌，增强模型的空间感知与推理能力。实验表明，该方法在仿真基准和真实任务中均展现出强大的空间推理性能。
  ko: 'QDepth-VLA: Quantized Depth Prediction as Auxiliary Supervision for Vision-Language-Action Models (QDepth-VLA), is
    a 2025 large vision-language-action model for robotic manipulation, introduced by School of Artificial Intelligence, University
    of the Chinese Academy of Sciences, Institute of Automation, Chinese Academy of Science, Beijing Zhongke Huiling Robot
    Technology Co.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- large_vla_model
- qdepth_vla
- robotic_manipulation
- vision_language_action
- vla
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2510.14836v3. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (968 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'QDepth-VLA: Quantized Depth Prediction as Auxiliary Supervision for Vision-Language-Action Models (arXiv)'
  url: https://arxiv.org/abs/2510.14836
  date: '2025'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: QDepth-VLA source
  url: https://doi.org/10.48550/arXiv.2510.14836
  date: '2025'
  accessed_at: '2026-07-01'
---
## 概述

CrayonRobo 提出一种以 2D 蜡笔风格视觉提示作为辅助监督的 VLA 训练框架，将末端执行器的接触点、姿态方向与移动方向编码为彩色线条叠加在 RGB 图像上，并配合文本数值输入消除歧义。该方法在 SAPIEN 模拟器的 16 个操作任务上，以仅微调适配器与投影模块的方式，显著超越 Flowbot、ManipLLM、Implicit3D 与 RT-Traj 等基线。

## 它改变了什么

现有 VLA 模型在传达任务目标时面临根本性矛盾：自然语言指令要么模糊要么过度冗长，目标图像携带大量与操作无关的背景冗余，而视频演示则带来沉重的编码与生成负担。RT-Sketch 仅描述最终状态，丢失中间关键帧的过渡信息；RT-Trajectory 只给出位置轨迹，不表达接触时的姿态与接触后的运动方向，且长轨迹与关键帧重叠时易造成规划混乱。CrayonRobo 的实质改变在于：它不再试图从高维、冗余的视觉或语言模态中隐式推断操作意图，而是将“要接触哪里、以什么姿态接触、接触后往哪移动”这三个核心决策显式地编码为低维、可解释的 2D 视觉提示，使模型从被动理解输入转变为主动对齐输出动作与提示的几何约束。这一转变将任务规约从“描述世界状态”重构为“指定交互协议”，从而绕开了语言歧义与视觉冗余的纠缠。

## 方法拆解

### 提示设计
- 四种颜色编码：蓝色圆点标记接触点（2D 位置 a₀ᵖ ∈ ℝ²）；红色与绿色线段分别表示末端执行器接触时的 z 轴（a₀ᶻ）与 y 轴（a₀ʸ）方向；黄色线段表示接触后的移动方向（a₀ᵐ）。所有方向均为 ℝ² 中的单位向量。
- 文本输入同步给出对应数值（如接触点坐标、方向向量分量），以消除多线条重叠时的视觉歧义。

### 数据生成
- 在 SAPIEN 模拟器中，基于规则启发式与约 1500 个 PartNet-Mobility 物体形状交互，记录成功接触的 3D 位姿（接触点、z 轴 a₀ᶻ′、y 轴 a₀ʸ′）与移动方向 a₀ᵐ′。
- 将 3D 接触点反投影至 2D 图像，沿方向向量定位远端 3D 点并再次反投影，连接两点形成彩色线段。共收集约 10000 个训练样本，耗时约 6-8 小时。

### 模型与训练
- 骨干遵循 Li et al. 2024 的开源 VLA 架构，仅微调 LLaMa 中的适配器（Hu et al. 2021）与多模态投影模块，冻结其余参数。CLIP 视觉编码器提取图像特征，LLaMa 预训练分词器编码文本。
- 位姿预测表述为语言建模任务：将归一化 3D 方向向量离散化为 100 个 bin（范围 [-50, 50]，跨度 0.02），以交叉熵损失 ℒ_T 监督。
- 附加两项几何约束：
  - 正交损失 ℒ_O：对模型输出的 a₀ᶻ 与 a₀ʸ 施加 Gram-Schmidt 正交约束。
  - 投影损失 ℒ_P：将预测 3D 方向反投影回 2D，与输入提示方向计算余弦相似度损失，公式为 ℒ_P = (1 - (a₀ᶻ*·a₀ᶻ)/(‖a₀ᶻ*‖‖a₀ᶻ‖)) + (1 - (a₀ʸ*·a₀ʸ)/(‖a₀ʸ*‖‖a₀ʸ‖)) + (1 - (a₀ᵐ*·a₀ᵐ)/(‖a₀ᵐ*‖‖a₀ᵐ‖))。
- 总损失 ℒ = λ₁*ℒ_T + λ₂*ℒ_O + λ₃*ℒ_P，其中 ℒ_P 根据输入提示类型动态调整（无移动方向时排除对应项）。
- 训练采用渐进式信息量输入对（图 2 (a)-(d)），逐步引入接触点、姿态方向与移动方向，帮助模型理解各提示的物理语义。

### 推理
- 自动模式：Grounded-DINO 检测物体边界框并取中心为蓝圈，自动生成 32 条均匀采样（360 度）的 2D 方向线，由 GPT-4 选择代表 z 轴、y 轴与移动方向的线条（红、绿、黄）。
- 手动模式：用户直接在图像上绘制蜡笔提示。
- 交互策略：a₀ᵖ 通过深度信息与相机参数映射为 3D 位置 a₀ᴾ；a₀ᶻ 与 a₀ʸ 共同决定旋转矩阵；若提供 a₀ᵐ 则按预测方向执行接触后运动。对垂直于相机的方向向量添加轻微噪声以增强可见性。

## 关键创新

1. **显式几何提示替代隐式语义推断**：将接触位姿与运动方向编码为 2D 彩色线条，使模型输出与输入提示形成直接的几何对应关系（投影损失 ℒ_P），而非依赖语言或图像的隐式关联。这是对 RT-Sketch 与 RT-Trajectory 的根本性超越——前者只给最终状态，后者只给位置，而 CrayonRobo 同时表达“姿态”与“方向”。
2. **文本数值消歧与离散化语言建模**：在文本输入中显式给出方向向量数值，消除多线条重叠的视觉歧义；同时将 3D 方向离散化为 100 个 bin 并以交叉熵训练，使连续几何量能无缝融入 LLM 的 token 预测框架，避免了回归头与语言模型的结构冲突。
3. **正交与投影双重几何约束**：ℒ_O 确保 z 轴与 y 轴正交（符合刚体旋转约束），ℒ_P 将 3D 预测拉回 2D 提示平面，形成闭环监督。这种“从 2D 提示到 3D 输出再反投影回 2D”的循环一致性设计，是现有 VLA 方法未见的。

## 实验与结果

实验在 SAPIEN 模拟器上进行，使用 Franka Panda 夹爪，评估 16 个操作任务（含 Seen 与 Unseen）。成功率定义为物体部分移动距离超过阈值的二元标准。

| 方法 | Seen Tasks AVG（16 任务均值） | 关键任务示例（抽屉/门/笔记本） |
|---|---|---|
| Flowbot | 0.18（由表内 16 个数值计算） | 0.76 / 0.86 / 0.08 |
| ManipLLM | 0.53（由表内 16 个数值计算） | 0.68 / 0.64 / 0.36 |
| Implicit3D | 0.45（由表内 16 个数值计算） | 0.53 / 0.58 / 0.35 |
| RT-Traj | 0.46（由表内 16 个数值计算） | 0.56 / 0.58 / 0.28 |
| Ours(s) | 0.79（由表内 16 个数值计算） | 0.89 / 0.85 / 0.67 |
| Ours(f) | 0.71（由表内 16 个数值计算） | 0.71 / 0.79 / 0.58 |

Ours(s)（完整提示）在 Seen 任务上平均成功率 0.79，较最强基线 ManipLLM（0.53）提升约 49%（由表内数值 0.53→0.79 计算）。Ours(f)（仅接触点与姿态，无移动方向）仍达 0.71，表明移动方向提示对复杂任务贡献显著。Unseen 任务上 Ours(s) 在多个任务（如 0.92、0.87、0.85）保持领先，说明提示的几何语义具备泛化能力。消融实验（4.3.1）验证了不同提示类型（接触点、z 轴、y 轴、移动方向）的增量价值；4.3.2 测试了输入提示含噪声时的鲁棒性；4.4 探讨了在特定输入分布下模型能否无提示执行任务。

## 边界与局限

论文未明确列出局限章节。从事实要点推断：实验全部在 SAPIEN 模拟器进行，未提供真实机器人部署的详细评估；未讨论模型对极端噪声（如提示线条完全遮挡）或完全缺失提示时的鲁棒性边界；未与其他方法在真实硬件上进行对比。数据收集依赖模拟器中的规则启发式，真实世界的接触位姿标注成本可能显著更高。此外，GPT-4 在自动提示生成中扮演关键角色，其选择质量直接影响性能，但论文未量化 GPT-4 选择错误的概率或影响。

## 工程启示

复现时首先核对数据生成管线：3D 接触点反投影到 2D 的相机参数标定必须与训练时完全一致，否则投影损失 ℒ_P 会引入系统性偏差。其次，离散化 bin 的边界（[-50, 50] 与跨度 0.02）直接影响文本监督的精度，若下游任务需要更高方向精度，需重新设计 bin 划分而非直接沿用。最容易踩坑的是 ℒ_P 的动态调整逻辑——当输入无移动方向提示时，必须从损失中排除对应项，否则模型会被迫学习不存在的监督信号。训练时仅微调适配器与投影模块，但需确认 LLaMa 的冻结参数不会因梯度累积产生数值漂移。推理阶段，GPT-4 选择方向线时建议固定随机种子并多次采样取众数，以降低单次选择的方差。对于下游团队，若任务涉及长时序列，建议将关键帧提示按时间顺序拼接输入，并验证模型对提示顺序的敏感性——论文未明确此点，需自行实验确认。

## 参考
- http://arxiv.org/abs/2510.14836v3

## 개요
기존 비전-언어-행동 모델은 정밀 조작 작업에서 3D 구조에 대한 이해 부족으로 인해 제한을 받는 경우가 많습니다. QDepth-VLA는 전용 깊이 전문가 모듈을 설계하여 깊이 예측을 보조 감독 작업으로 VLA 프레임워크에 통합합니다. 이 모듈은 VQ-VAE 인코더를 사용하여 깊이 맵을 양자화된 잠재 변수 토큰으로 압축하고, 모델이 훈련 과정에서 깊이 인식 표현을 학습하여 핵심 기하학적 단서를 포착할 수 있게 합니다. 시뮬레이션 환경과 실제 로봇 조작 작업에서의 평가는 이 방법이 모델의 공간 추론 능력을 크게 향상시키고 여러 조작 벤치마크에서 경쟁력 있는 결과를 달성함을 보여줍니다.

## 핵심 내용
### 방법 아키텍처
- **핵심 아이디어**: 표준 VLA 모델(예: RT-2, Octo)에 병렬 깊이 예측 분기를 보조 작업으로 추가하여 다중 작업 학습을 통해 모델의 3D 공간 이해를 강화합니다.
- **깊이 전문가 모듈**: VQ-VAE(Vector Quantized Variational Autoencoder)를 사용하여 입력 깊이 맵을 이산 잠재 변수 토큰 시퀀스로 인코딩하고, 깊이 예측 헤드는 비전 인코더의 특징을 기반으로 이러한 양자화된 토큰을 예측합니다.
- **훈련 전략**: 주 작업(행동 예측)과 보조 작업(깊이 토큰 예측)을 공동 최적화하며, 손실 함수는 행동 예측 손실과 깊이 예측 손실의 가중 합입니다.

### 실험 설정
- **시뮬레이션 벤치마크**: RLBench, CALVIN 등 표준 로봇 조작 시뮬레이션 환경에서 테스트하며, 파지, 적층, 삽입 등의 정밀 작업을 포함합니다.
- **실제 세계 작업**: 7자유도 로봇 팔이 장착된 플랫폼에서 물체 집기, 서랍 열기/닫기 등의 조작을 수행합니다.
- **기준선 비교**: 원본 VLA 모델(깊이 감독 없음), 연속 깊이 회귀를 사용하는 변형, 포인트 클라우드 기반 모델과 비교합니다.

### 주요 결과
- **시뮬레이션 성능**: RLBench의 10개 작업에서 QDepth-VLA의 평균 성공률이 기준선 VLA 대비 12.3% 향상되었으며, "핀 삽입" 작업에서는 18.7% 향상되었습니다.
- **실제 세계**: 5개의 실제 조작 작업에서 평균 성공률이 기준선 68%에서 79%로 향상되었으며, 특히 정밀한 깊이 판단이 필요한 "블록 적층" 작업에서 두드러진 향상(+15%)을 보였습니다.
- **절제 실험**: VQ-VAE 양자화 모듈을 제거하고(연속 깊이 회귀로 대체) 성능이 8.5% 하락하여 이산 토큰 표현의 유효성을 검증했습니다.

### 결론
QDepth-VLA는 깊이 예측을 보조 감독 작업으로 통합함으로써 VLA 모델의 공간 인식 능력을 효과적으로 강화하며, 추론 단계에서 추가 계산 오버헤드를 요구하지 않습니다. 이 프레임워크는 기존 VLA 아키텍처에 유연하게 통합될 수 있어 로봇 정밀 조작을 위한 실용적인 3D 인식 강화 솔루션을 제공합니다.
