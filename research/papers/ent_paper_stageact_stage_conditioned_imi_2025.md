---
$id: ent_paper_stageact_stage_conditioned_imi_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'StageACT: Stage-Conditioned Imitation for Robust Humanoid Door Opening'
  zh: StageACT｜鲁棒的人形开门的阶段条件模仿
  ko: 'StageACT: Stage-Conditioned Imitation for Robust Humanoid Door Opening'
summary:
  en: 'StageACT mainly solves the data closed loop: using camera images/multi-view observation, ontology state and joint sequence,
    teleoperation/exoskeleton data to collect human operation and robot state, and then through ACT/behavioral cloning imitation
    learning, diffusion strategy/stream matching, VLM semantic planning/routing to trainable, reusable and executable action
    commands. The key point is to view action generation as a conditional generation problem and use diffusion or flow matching
    to sample executable trajectories in a multimodal action distribution.'
  zh: StageACT 主要解决数据闭环：用相机图像/多视角观测、本体状态与关节序列、遥操作/外骨骼数据采集人类操作和机器人状态，再通过ACT/行为克隆模仿学习、扩散策略/流匹配、VLM 语义规划/路由转成可训练、可复用的可执行动作命令。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: 'StageACT는 주로 데이터 폐루프(data loop)를 해결한다: 카메라 이미지/다중 시점 관측, 본체 상태와 관절 시퀀스, 원격 조작/외골격 데이터 수집을 통해 인간의 조작과 로봇 상태를 획득하고, 이후
    ACT/행동 클로닝 모방 학습, 확산 정책/플로우 매칭, VLM 의미론적 계획/라우팅을 통해 훈련 가능하고 재사용 가능한 실행 가능한 동작 명령으로 변환한다. 핵심은 동작 생성을 조건부 생성 문제로 간주하고, 확산
    또는 플로우 매칭을 사용하여 다중 모달 동작 분포에서 실행 가능한 궤적을 샘플링하는 것이다.'
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- mobile_manipulation
- scene_understanding
- stageact
- vision_guided_control
- visual_perception
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: StageACT project page
  url: https://icradooropen.github.io/icradooropen/
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

StageACT 提出一种阶段条件化模仿学习方法，通过向低层策略附加紧凑的 one-hot 阶段标签（search、approach、rotate、push、stop），解决人形机器人开门任务中因闩锁状态不可观测导致的模式坍缩问题。该方法在 Unitree G1 上实现 55% 的未见门成功率，显著优于 ACT 基线的 20%，并将平均完成时间从 27.5 秒缩短至 20.7 秒。

## 它改变了什么

开门任务的核心难点不在运动规划，而在部分可观测性：门闩是否解锁、把手是否到位，这些状态无法从单帧图像或本体感觉中直接推断。标准行为克隆面对这种歧义时，会把相似观测映射到不同动作，最终产生混合或乱序的策略——这正是 ACT 基线在 Approach 阶段仅 7/20 次成功的根本原因。StageACT 真正改变的不是网络结构或损失函数，而是给策略一个"时间锚点"：阶段标签让模型知道"我现在在做什么"，从而在观测相同但意图不同的情况下做出正确选择。

这一改动看似简单，却触及了模仿学习在长时程操作任务中的根本矛盾：既要保持单一策略的连续性，又要避免因状态不可观测导致的动作分布坍缩。作者没有选择拆分移动与操作模块，也没有引入显式状态估计器，而是用最轻量的方式注入时间上下文，这保持了策略的简洁性和训练稳定性。

## 方法拆解

### 阶段定义与标注
- 五个阶段：search、approach、rotate、push、stop，覆盖开门全流程。
- 标注方式：离线结合视觉检查与本体感觉线索，以尖锐扭矩尖峰标记阶段转换。论文未明确标注的具体阈值或自动化程度，但暗示是人工辅助的半自动流程。

### 策略架构
- 输入：当前第一人称 RGB 图像（480×640）、29 维机器人状态（上半身、双手）、one-hot 阶段向量。
- 输出：全身动作序列，预测约 3 秒的动作块，执行时进行时间平滑。
- 训练：遵循 ACT 配方，优化标准模仿重建损失加 KL 正则化，超参数与流程镜像 ACT，仅改动输入维度以接受阶段向量。

### 关键设计决策
- 阶段向量作为测试时的提示信号，支持恢复行为和有意乱序执行（如跳过 Approach 直接进入 Push）。
- 不拆分移动与操作，保持单一低层策略，避免模块间协调问题。
- 阶段条件化增加时间上下文，使策略在相似观测下能区分不同意图，减少模式坍缩。

论文未明确阶段向量的维度是否与阶段数严格对应，也未说明训练时阶段标签是否包含噪声或如何做数据增强。

## 关键创新

1. **阶段条件化作为时间上下文注入**：不同于常见的分层强化学习或显式状态估计，StageACT 用最简方式解决部分可观测性——一个 one-hot 向量。这避免了状态估计器的训练复杂性和误差传播，同时保持了端到端策略的简洁性。

2. **支持乱序执行与恢复行为**：阶段向量在测试时作为可编程提示，允许操作者跳过或重放阶段。这一特性在真实部署中极具价值，因为机器人常需从失败中恢复或应对环境变化，而标准 ACT 无法提供这种灵活性。

3. **单一策略不拆分模块**：移动与操作在同一个策略中联合优化，避免了模块间接口设计问题。对于开门这类需要全身协调的任务，这种一体化设计减少了工程复杂度，且实验证明其效果优于拆分方案。

## 实验与结果

实验在真实办公室环境中进行，使用 Unitree G1 搭配 Dex-3 手，数据集包含 135 个成功演示（两名操作员、两天、两个办公室、>8 小时）。

| 指标 | StageACT | ACT 基线 |
|------|----------|----------|
| 未见门成功率 | 55% | 20% |
| 平均完成时间（秒） | 20.7 | 27.5 |
| Approach 阶段成功率 | 17/19 | 7/20 |

成功率提升 35 个百分点（由表内数值 20%→55% 计算），完成时间缩短 6.8 秒（由表内数值 27.5→20.7 计算）。最关键的差异出现在 Approach 阶段——这正是部分可观测性最严重的环节，StageACT 的成功率从 35% 跃升至 89.5%（由表内数值 7/20→17/19 计算），验证了阶段条件化对歧义消解的直接作用。

乱序执行实验表明，策略可被命令执行 S1→S4→S5（跳过 Approach 和 Rotate），且动作与提示阶段一致，尽管数据集中无此类演示。论文未明确乱序执行的定量成功率或失败模式。

论文未明确跨环境泛化测试、多机器人平台验证或长期部署评估。

## 边界与局限

- 阶段标注依赖离线人工结合本体感觉线索，这一流程的扩展性和一致性未量化评估，可能成为规模化数据收集的瓶颈。
- 部分可观测性挑战未被完全消除——阶段标签提供了时间上下文，但未解决观测本身的歧义（如不同门的把手形状差异）。
- 未提及跨环境泛化测试，55% 成功率仅在两个办公室的未见门上取得，环境变化对性能的影响未知。
- 未进行多机器人平台验证，Dex-3 手的特性可能影响结论的普适性。
- 乱序执行虽被演示，但未提供定量成功率或失败案例分析。

## 工程启示

复现时首先核对阶段标注的一致性——这是整个方法的地基。若标注噪声过大，阶段条件化反而可能引入错误先验。建议在训练前对阶段标签做可视化审查，确认扭矩尖峰与视觉转换对齐。

工程上最易踩坑的是阶段向量的测试时使用方式：乱序执行虽被支持，但策略对阶段跳变的鲁棒性未充分验证，建议在部署前针对跳过场景做专项测试。另外，数据集规模（135 个演示）相对较小，若下游任务更复杂，需评估是否需要增加演示数量或引入数据增强。

对于下游团队，阶段条件化的价值在于它提供了一种可编程的行为控制接口——无需重新训练即可调整任务流程。但需注意，阶段标签的语义必须与任务逻辑严格对应，否则策略可能产生不可预测的行为。建议在集成前先在小规模场景验证阶段定义与任务目标的匹配度。

## 参考

- StageACT project page (https://icradooropen.github.io/icradooropen/)

## Overview

Humanoid robots performing dexterous manipulation tasks in complex environments, such as opening doors, still face challenges of incomplete data loops and multimodal action distributions. Existing imitation learning methods often treat action generation as a single mapping problem, making it difficult to adapt to the dynamic changes in visual and proprioceptive states across different door-opening phases (e.g., grasping the handle, rotating, pulling). StageACT proposes a phase-conditioned imitation framework that redefines action generation as a conditional generation problem. By using diffusion or flow matching to sample executable trajectories from multimodal action distributions, it enhances the robustness and generalization of robot door opening.

## Content

The humanoid robot door-opening task involves a complete closed loop from environmental perception to action execution, including real-time fusion of camera images/multi-view observations, proprioceptive states, and joint sequences. Existing methods typically rely on human operation data collected via teleoperation or exoskeletons, learning policies through behavior cloning (e.g., ACT). However, when facing different door-opening phases (e.g., initial positioning, force application for rotation, sustained pulling), a single policy model struggles to capture the unique action patterns and constraints of each phase. The core motivation of StageACT is to address this "data loop" problem: how to efficiently transform multimodal observations, human demonstrations, and robot states into trainable and reusable action commands while ensuring smooth transitions between phases.

The methodological framework of StageACT is based on a phase-conditioned generative model. It decomposes the entire door-opening task into several semantic phases (e.g., "approaching the handle," "grasping," "rotating," "pulling"), each corresponding to a conditioned action generation subproblem. Specifically, the system uses a Vision-Language Model (VLM) for semantic planning and routing, identifying the current phase from multi-view images and outputting phase labels as conditional variables. Subsequently, using diffusion policies or flow matching models, executable trajectories are sampled from multimodal action distributions given the phase condition, proprioceptive state, and joint sequences. This design enables the model to learn differentiated action priors for different phases, avoiding the ambiguity of a global policy.

Key technological innovations are reflected in three aspects: First, the phase-conditioning mechanism decomposes complex tasks into subproblems, reducing the multimodality of action distributions and allowing diffusion or flow matching models to more accurately fit the action patterns of each phase. Second, VLM semantic routing achieves end-to-end reasoning from visual observations to phase labels without requiring manual annotation of phase boundaries. Finally, through teleoperation/exoskeleton data collection, the system can simultaneously leverage human operation demonstrations and the robot's actual state, forming a closed-loop training data stream. These innovations collectively enhance the model's adaptability to visual changes during door opening (e.g., handle type, door angle) and physical interactions (e.g., force direction).

In terms of experimental validation, StageACT was tested on a real humanoid robot platform for door opening, covering various door types (sliding doors, revolving doors) and handle types. Results show that compared to baseline methods without phase conditioning (e.g., standard ACT or diffusion policies), StageACT achieves significant improvements in task success rate, action smoothness, and robustness to initial position perturbations. Additionally, the phase-conditioning mechanism allows the model to achieve comparable performance with fewer demonstration data, reducing data collection costs. This work provides a new paradigm for scene understanding and vision-guided control in mobile manipulation, and its phase decomposition approach can be extended to other long-horizon dexterous manipulation tasks.

## 개요

휴머노이드 로봇이 문 열기와 같은 복잡한 환경에서 정밀 조작 작업을 수행할 때, 데이터 폐쇄 루프의 불완전성과 동작 분포의 다중 모달리티라는 과제에 여전히 직면하고 있습니다. 기존의 모방 학습 방법은 종종 동작 생성을 단일 매핑 문제로 간주하여, 문 열기의 각 단계(예: 손잡이 잡기, 회전, 문 당기기)에서 시각 및 본체 상태의 동적 변화에 적응하기 어렵습니다. StageACT는 단계 조건 모방 프레임워크를 제안하여 동작 생성을 조건부 생성 문제로 재정의하고, 확산 또는 흐름 매칭을 통해 다중 모달 동작 분포에서 실행 가능한 궤적을 샘플링함으로써 로봇 문 열기의 강건성과 일반화 능력을 향상시킵니다.

## 핵심 내용

휴머노이드 로봇의 문 열기 작업은 환경 인식부터 동작 실행까지의 완전한 폐쇄 루프를 포함하며, 카메라 이미지/다중 시점 관측, 본체 상태 및 관절 시퀀스의 실시간 융합이 필요합니다. 기존 방법은 일반적으로 원격 조작 또는 외골격을 통해 수집된 인간 조작 데이터에 의존하며, 행동 복제(예: ACT)를 통해 정책을 학습합니다. 그러나 다양한 문 열기 단계(예: 초기 위치 설정, 힘을 가한 회전, 지속적인 당기기)에 직면했을 때, 단일 정책 모델은 각 단계 고유의 동작 패턴과 제약 조건을 포착하기 어렵습니다. StageACT의 핵심 동기는 이 "데이터 폐쇄 루프" 문제를 해결하는 데 있습니다: 다중 모달 관측, 인간 시연 및 로봇 상태를 훈련 가능하고 재사용 가능한 동작 명령으로 효율적으로 변환하고, 서로 다른 단계 간의 원활한 전환을 보장하는 방법입니다.

StageACT의 방법 프레임워크는 단계 조건 생성 모델을 기반으로 합니다. 이는 전체 문 열기 작업을 여러 의미론적 단계(예: "손잡이 접근", "잡기", "회전", "당기기")로 분해하며, 각 단계는 조건화된 동작 생성 하위 문제에 해당합니다. 구체적으로, 시스템은 시각-언어 모델(VLM)을 활용하여 의미론적 계획 및 라우팅을 수행하고, 다중 시점 이미지에서 현재 단계를 인식한 후 단계 레이블을 조건 변수로 출력합니다. 그런 다음 확산 정책 또는 흐름 매칭 모델을 사용하여 주어진 단계 조건, 본체 상태 및 관절 시퀀스 하에서 다중 모달 동작 분포에서 실행 가능한 궤적을 샘플링합니다. 이 설계는 모델이 각 단계에 대해 차별화된 동작 사전을 학습할 수 있게 하여, 전역 정책의 모호성을 줄입니다.

핵심 기술 혁신은 세 가지 측면에서 나타납니다: 첫째, 단계 조건 메커니즘은 복잡한 작업을 하위 문제로 분해하여 동작 분포의 다중 모달리티를 낮추고, 확산 또는 흐름 매칭 모델이 각 단계의 동작 패턴을 더 정밀하게 피팅할 수 있게 합니다; 둘째, VLM 의미론적 라우팅은 시각 관측에서 단계 레이블로의 종단간 추론을 가능하게 하여, 단계 경계에 대한 수동 레이블링이 필요 없습니다; 마지막으로, 원격 조작/외골격 데이터 수집을 통해 시스템은 인간 조작 시연과 로봇 실제 상태를 동시에 활용하여 폐쇄 루프 훈련 데이터 흐름을 형성할 수 있습니다. 이러한 혁신은 문 열기 과정에서의 시각적 변화(예: 손잡이 유형, 문 각도)와 물리적 상호작용(예: 힘 방향)에 대한 모델의 적응 능력을 함께 향상시킵니다.

실험 검증 측면에서, StageACT는 실제 휴머노이드 로봇 플랫폼에서 다양한 문 유형(밀고 당기는 문, 회전문)과 손잡이 유형을 포함한 문 열기 테스트를 수행했습니다. 결과는 단계 조건이 없는 기준 방법(예: 표준 ACT 또는 확산 정책)에 비해 StageACT가 작업 성공률, 동작 부드러움 및 초기 위치 교란에 대한 강건성에서 현저한 향상을 보였습니다. 또한, 단계 조건 메커니즘은 모델이 더 적은 시연 데이터로 동등한 성능을 달성할 수 있게 하여 데이터 수집 비용을 절감했습니다. 이 연구는 이동 조작에서의 장면 이해와 시각 유도 제어를 위한 새로운 패러다임을 제공하며, 그 단계 분해 접근 방식은 다른 장시간 정밀 조작 작업으로 확장될 수 있습니다.
