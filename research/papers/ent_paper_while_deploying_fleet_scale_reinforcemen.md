---
$id: ent_paper_while_deploying_fleet_scale_reinforcemen
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Learning while Deploying: Fleet-Scale Reinforcement Learning for Generalist Robot Policies'
  zh: 'Learning while Deploying: Fleet-Scale Reinforcement Learning for Generalist Robot Policies'
  ko: 'Learning while Deploying: Fleet-Scale Reinforcement Learning for Generalist Robot Policies'
summary:
  en: 'Learning while Deploying: Fleet-Scale Reinforcement Learning for Generalist Robot Policies - Research - AGIBOT Finch
    Research Join us Research Latest news Join Us Hot Research LWD Learning while Deploying: Fleet-Scale Reinforcement Learning
    for Generalist Robot Policies Apr 30, 2026 Read Paper Imagi Institutions per source list: AGIBOT Research.'
  zh: LWD（Learning while Deploying）是由AGIBOT Finch Research提出的一个面向通用机器人策略的车队规模离线到在线强化学习框架。其核心贡献在于将部署过程本身转化为持续训练循环，利用整个机器人车队产生的异构经验（包括成功、失败和人工干预）来持续改进单一通用VLA策略，而非仅依赖专家演示。
  ko: 'Learning while Deploying: Fleet-Scale Reinforcement Learning for Generalist Robot Policies - Research - AGIBOT Finch
    Research Join us Research Latest news Join Us Hot Research LWD Learning while Deploying: Fleet-Scale Reinforcement Learning
    for Generalist Robot Policies Apr 30, 2026 Read Paper Imagi Institutions per source list: AGIBOT Research.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- while
- deploying
- fleet
- scale
- reinforcemen
- project_page_sourced
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: Full ingest from Yuanxq lab paper list row 708 (.staging/ingest_yuanxq). Tier B->page. Content compiled by DeepSeek
    from the fetched project page (https://finch.agibot.com/research/lwd). Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    project-page source only, limited); en/ko sections regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: website
  title: Project page
  url: https://finch.agibot.com/research/lwd
  accessed_at: '2026-07-31'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

本文提出 Learning while Deploying（LWD），一个面向通用 VLA 机器人策略的车队级离线到在线强化学习框架，由 Agibot 团队在 G1 双臂机器人上实现。核心贡献在于将部署从评估终点转变为训练循环，通过 DIVL（分布隐式价值学习）与 QAM（伴随匹配 Q 学习）两个组件，解决异构车队回放与生成式动作头两大挑战，在 8 个真实操作任务上持续优于先前后训练基线。

## 它改变了什么

过去几年，VLA 预训练模型让机器人获得了强大的先验，但社区默认的范式是：预训练后冻结或仅用少量专家示范微调，部署只是收集更多示范的飞轮。LWD 真正改变的是这个隐含假设——它不再把部署当作数据采集的附属品，而是把整个车队当作一个持续运转的强化学习环境。这意味着失败轨迹、部分进展、人工干预这些过去被丢弃或仅用于对比学习的信号，现在都成为价值学习的监督来源。

更深层的改变在于后训练的对象。先前的后训练系统（如 RT-X 类工作）主要针对专用策略，每个任务一个控制器，而 LWD 坚持训练单一通用策略覆盖所有任务。这不仅是工程简化，而是对「通用性」的重新定义：通用不是预训练时的静态属性，而是部署中通过异构经验持续打磨出来的动态属性。作者明确抛弃了模仿学习作为后训练手段，转向离线到在线 RL，这要求价值函数在稀疏延迟奖励和高度异构的数据分布下依然稳定——这是此前系统刻意回避的难点。

## 方法拆解

LWD 的架构是一个闭环：预训练策略部署到车队 → 自主 rollout 与人工干预聚合到共享回放缓冲区 → 集中式 learner 更新策略 → 周期推回车队。

### 两阶段训练流程
- **离线阶段**：用先前收集的机器人数据（专家示范、历史 rollout、失败模式附近的探索数据）做离线 RL 初始化，为策略和 critic 提供稳定起点。这一步的关键是避免从零开始在线探索的冷启动问题。
- **在线阶段**：当前策略部署到车队，每台机器人执行真实任务并上传轨迹到共享在线缓冲区；learner 在静态离线缓冲区与增长的在线缓冲区混合数据上训练，周期性推送更新。

### 组件一：DIVL（Distributional Implicit Value Learning）
- 不回归单一标量值，而是学习数据集中动作值的**分布**，提取分位数统计量作为 TD bootstrap 目标。
- 保留 IQL 的分布内价值学习原则，避免分布外最大化导致的过估计——这对异构回放尤其重要，因为不同任务的奖励稀疏度和成功率差异极大。
- 对长 horizon 任务使用多步 TD 目标，加速稀疏终止奖励的传播。

### 组件二：QAM（Q-learning with Adjoint Matching）
- 针对 flow-matching 动作头设计。现代 VLA 策略通过多步去噪生成动作，无解析似然，直接策略梯度不稳定。
- 利用 critic 梯度引导策略提取，将引导优化重构为沿 flow 轨迹的**局部回归**，避免通过完整生成过程反向传播。

### 整体解耦
DIVL 负责策略评估（从异构回放中学价值），QAM 负责策略提取（将价值转化为 flow 策略更新）。两者解耦使得价值学习可以独立于动作头结构，策略更新可以独立于价值估计的噪声。

论文未明确：DIVL 的分位数参数选择、QAM 的具体回归目标形式、离线与在线数据的混合比例、learner 的更新频率与推送周期。

## 关键创新

1. **将部署本身变成 RL 训练信号源**：此前数据飞轮只利用成功轨迹，LWD 明确将失败、部分进展、人工干预全部纳入价值学习。这不是简单的数据增广，而是对「什么构成学习信号」的重新定义——失败轨迹在 TD 学习中提供负向价值监督，人工干预提供接近成功状态的引导。

2. **DIVL 的分布价值学习适配异构回放**：IQL 此前主要用于同质数据，LWD 将其扩展为分布形式，用分位数统计量作为 bootstrap 目标。这个设计直接回应了车队数据的核心矛盾——不同任务的成功率、奖励密度、干预频率差异巨大，单一标量值无法捕捉这种变异性，而分布形式天然具备鲁棒性。

3. **QAM 解决生成式动作头的策略提取**：flow-matching 动作头是当前 VLA 的主流选择，但无法计算似然，导致基于最大似然的策略提取和直接策略梯度都不可行。QAM 将 critic 引导重构为沿 flow 轨迹的局部回归，绕开了完整生成过程的反向传播——这是对生成式策略 RL 训练的一个实用解法，且与动作头结构解耦。

## 实验与结果

实验在 Agibot G1 双臂机器人车队上进行，覆盖 8 个真实操作任务，包括多个持续 3-5 分钟的长 horizon 任务（冲泡功夫茶、调制鸡尾酒、制作果汁、打包鞋子、杂货补货）。训练单一通用策略覆盖所有任务。

| 指标 | LWD 表现 | 对比基线 |
|------|---------|---------|
| 成功率 | 持续优于先前后训练基线 | 论文未给出具体数值 |
| 平均周期时间（长 horizon） | 降低 | 论文未给出具体数值 |
| 长 horizon 任务成功率 | 优势尤其明显 | 论文未给出具体数值 |

所有实验视频以 1 倍速展示，排除加速误导。

论文未明确：具体成功率数值、周期时间数值、基线方法名称、非长 horizon 任务上的改进幅度、车队规模、数据量、训练配置。

结果的含义：LWD 同时提升成功率和效率（周期时间下降），说明离线到在线 RL 不仅让策略更可靠，还让解决方案更优——这不是简单的「多试几次成功」，而是价值学习确实找到了更高效的执行路径。

## 边界与局限

论文未明确列出局限性讨论段落。从事实要点可推断的边界包括：

- **未与专用策略对比**：没有给出 LWD 通用策略与每任务专用策略的对比细节，因此无法判断通用性带来的性能损失幅度。
- **未验证大规模部署**：仅提出未来方向，未在更多家庭、商店、工厂场景验证。
- **非长 horizon 任务改进幅度未提及**：如果短任务改进有限，则框架价值可能集中在长 horizon 场景。
- **车队规模未提及**：无法判断框架在多大车队规模下有效，以及通信、同步开销如何随规模增长。

论文未明确：LWD 在短 horizon 任务上的表现、对人工干预频率的敏感性、离线数据质量对最终性能的影响。

## 工程启示

复现 LWD 时，最先要核对的是**价值学习的稳定性**——DIVL 的分位数参数和 IQL 的期望分位数选择直接决定 TD 目标的噪声水平，这可能是整个框架最敏感的部分。建议先在小规模离线数据上验证 DIVL 能否稳定收敛，再引入在线数据。

最容易踩坑的地方是**异构回放的过拟合**：在线缓冲区增长快，如果混合比例不当，价值函数会迅速偏向近期数据，导致灾难性遗忘。作者提到「避免过拟合瞬时在线数据」但未给出具体比例，复现时需自行实验确定离线/在线数据配比。

对下游团队的实际指导：

- 如果已有 VLA 预训练模型和车队部署，LWD 的价值在于把现有部署数据（包括失败）转化为策略改进，无需重新设计数据采集流程。
- QAM 的局部回归设计意味着你可以替换动作头结构而不必重写价值学习部分——架构解耦是工程上的实际红利。
- 长 horizon 任务是 LWD 的优势区，如果业务场景主要是短任务，收益可能有限，先做小规模验证再决定是否全量投入。
- 联系 Agibot（research@agibot.com）获取更多实现细节，但需注意论文未公开训练配置，复现成本可能较高。

## 参考
- https://finch.agibot.com/research/lwd
- https://github.com/ImChong/Robotics_Notebooks

## Overview

This paper proposes Learning while Deploying (LWD), a fleet-level offline-to-online reinforcement learning framework for general-purpose VLA robot policies, implemented by the Agibot team on G1 dual-arm robots. The core contribution lies in transforming deployment from an evaluation endpoint into a training loop, leveraging two components—DIVL (Distributional Implicit Value Learning) and QAM (Q-learning with Adjoint Matching)—to address the two major challenges of heterogeneous fleet replay and generative action heads, consistently outperforming prior post-training baselines across 8 real-world manipulation tasks.

## What It Changes

Over the past few years, VLA pretrained models have endowed robots with strong priors, but the community's default paradigm has been: freeze after pretraining or fine-tune with a small amount of expert demonstrations, with deployment serving merely as a flywheel for collecting more demonstrations. What LWD truly changes is this implicit assumption—it no longer treats deployment as an accessory to data collection, but instead treats the entire fleet as a continuously operating reinforcement learning environment. This means that failure trajectories, partial progress, and human interventions—signals that were previously discarded or used only for contrastive learning—now become supervision sources for value learning.

The deeper change lies in the object of post-training. Previous post-training systems (such as RT-X style work) primarily targeted specialized policies, with one controller per task, whereas LWD insists on training a single generalist policy covering all tasks. This is not merely an engineering simplification but a redefinition of "generality": generality is not a static property from pretraining, but a dynamic property continuously refined through heterogeneous experience during deployment. The authors explicitly abandon imitation learning as a post-training mechanism, pivoting to offline-to-online RL, which requires the value function to remain stable under sparse delayed rewards and highly heterogeneous data distributions—a difficulty previous systems deliberately avoided.

## Method Breakdown

LWD's architecture is a closed loop: pretrained policy deployed to the fleet → autonomous rollouts and human interventions aggregated into a shared replay buffer → centralized learner updates the policy → periodic pushback to the fleet.

### Two-Phase Training Pipeline
- **Offline phase**: Uses previously collected robot data (expert demonstrations, historical rollouts, exploration data near failure modes) for offline RL initialization, providing a stable starting point for the policy and critic. The key here is avoiding the cold-start problem of online exploration from scratch.
- **Online phase**: The current policy is deployed to the fleet, each robot executes real tasks and uploads trajectories to a shared online buffer; the learner trains on a mixture of the static offline buffer and the growing online buffer, with periodic policy updates pushed back.

### Component One: DIVL (Distributional Implicit Value Learning)
- Instead of regressing a single scalar value, it learns the **distribution** of action values in the dataset, extracting quantile statistics as TD bootstrap targets.
- Preserves IQL's in-distribution value learning principle, avoiding overestimation from out-of-distribution maximization—especially critical for heterogeneous replay, where reward sparsity and success rates vary drastically across tasks.
- Uses multi-step TD targets for long-horizon tasks to accelerate propagation of sparse terminal rewards.

### Component Two: QAM (Q-learning with Adjoint Matching)
- Designed for flow-matching action heads. Modern VLA policies generate actions through multi-step denoising without tractable likelihoods, making direct policy gradient unstable.
- Uses critic gradients to guide policy extraction, reformulating the guided optimization as **local regression** along the flow trajectory, avoiding backpropagation through the full generative process.

### Overall Decoupling
DIVL handles policy evaluation (learning values from heterogeneous replay), while QAM handles policy extraction (converting values into flow policy updates). This decoupling allows value learning to be independent of action head architecture, and policy updates to be independent of noise in value estimates.

Not specified in the paper: DIVL's quantile parameter choices, QAM's specific regression target form, the mixing ratio of offline to online data, and the learner's update frequency and pushback period.

## Key Innovations

1. **Turning deployment itself into an RL training signal source**: Previous data flywheels only utilized successful trajectories; LWD explicitly incorporates failures, partial progress, and human interventions into value learning. This is not simple data augmentation but a redefinition of "what constitutes a learning signal"—failure trajectories provide negative value supervision in TD learning, and human interventions provide guidance near successful states.

2. **DIVL's distributional value learning adapted to heterogeneous replay**: IQL was previously used primarily with homogeneous data; LWD extends it to distributional form, using quantile statistics as bootstrap targets. This design directly addresses the core contradiction of fleet data—success rates, reward densities, and intervention frequencies vary enormously across tasks, and a single scalar value cannot capture this variability, whereas the distributional form is naturally robust.

3. **QAM solving policy extraction for generative action heads**: Flow-matching action heads are the current mainstream choice for VLA, but they lack tractable likelihoods, making both maximum-likelihood-based policy extraction and direct policy gradient infeasible. QAM reformulates critic guidance as local regression along the flow trajectory, bypassing backpropagation through the full generative process—a practical solution for RL training of generative policies that is decoupled from action head architecture.

## Experiments and Results

Experiments were conducted on a fleet of Agibot G1 dual-arm robots, covering 8 real-world manipulation tasks, including multiple long-horizon tasks lasting 3-5 minutes (brewing Kung Fu tea, mixing cocktails, making juice, packing shoes, grocery restocking). A single generalist policy was trained to cover all tasks.

| Metric | LWD Performance | Comparison Baseline |
|--------|----------------|---------------------|
| Success rate | Consistently outperforms prior post-training baselines | Specific numbers not provided in paper |
| Average cycle time (long-horizon) | Reduced | Specific numbers not provided in paper |
| Long-horizon task success rate | Advantage especially pronounced | Specific numbers not provided in paper |

All experiment videos are shown at 1x speed to rule out acceleration artifacts.

Not specified in the paper: specific success rate values, cycle time values, baseline method names, improvement magnitude on non-long-horizon tasks, fleet size, data volume, and training configurations.

Implication of results: LWD simultaneously improves success rate and efficiency (reduced cycle time), indicating that offline-to-online RL not only makes the policy more reliable but also finds better solutions—this is not simply "succeeding after more attempts" but rather value learning genuinely discovering more efficient execution paths.

## Boundaries and Limitations

The paper does not explicitly include a limitations discussion section. Inferred boundaries from factual points include:

- **No comparison with specialized policies**: No detailed comparison between LWD's generalist policy and per-task specialized policies is provided, making it impossible to assess the performance cost of generality.
- **No large-scale deployment validation**: Only proposed as future direction, not validated in more home, store, or factory scenarios.
- **Improvement on non-long-horizon tasks not mentioned**: If improvements on short tasks are limited, the framework's value may be concentrated in long-horizon scenarios.
- **Fleet size not mentioned**: Impossible to determine at what fleet scale the framework remains effective, or how communication and synchronization overhead scale.

Not specified in the paper: LWD's performance on short-horizon tasks, sensitivity to human intervention frequency, and the impact of offline data quality on final performance.

## Engineering Insights

When reproducing LWD, the first thing to verify is **value learning stability**—DIVL's quantile parameters and IQL's expectation quantile choices directly determine the noise level of TD targets, which may be the most sensitive part of the entire framework. It is recommended to first validate whether DIVL converges stably on small-scale offline data before introducing online data.

The most likely pitfall is **overfitting to heterogeneous replay**: the online buffer grows quickly, and if the mixing ratio is inappropriate, the value function will rapidly bias toward recent data, causing catastrophic forgetting. The authors mention "avoiding overfitting to transient online data" but do not provide specific ratios; reproduction requires self-experimentation to determine the offline/online data mix.

Practical guidance for downstream teams:

- If you already have a pretrained VLA model and fleet deployment, LWD's value lies in converting existing deployment data (including failures) into policy improvements without redesigning the data collection pipeline.
- QAM's local regression design means you can swap action head architectures without rewriting the value learning component—architectural decoupling is a practical engineering dividend.
- Long-horizon tasks are LWD's advantage zone; if your business scenarios are primarily short tasks, the gains may be limited—run small-scale validation before committing full resources.
- Contact Agibot (research@agibot.com) for more implementation details, but note that training configurations are not publicly disclosed, so reproduction costs may be high.

## 개요

본 논문은 Agibot 팀이 G1 이중 팔 로봇에서 구현한, 범용 VLA 로봇 정책을 위한 차량군 단위 오프라인-투-온라인 강화학습 프레임워크인 Learning while Deploying(LWD)을 제안합니다. 핵심 기여는 배포를 평가의 종착점이 아닌 훈련 루프로 전환한 것이며, DIVL(분포적 암시적 가치 학습)과 QAM(수반 매칭 Q-러닝) 두 구성 요소를 통해 이기종 차량군 재생과 생성적 액션 헤드라는 두 가지 과제를 해결하고, 8가지 실제 조작 작업에서 기존 사전 훈련 후 훈련 기준선을 지속적으로 능가합니다.

## 무엇을 바꾸었는가

지난 몇 년간 VLA 사전 훈련 모델은 로봇에게 강력한 사전 지식을 제공했지만, 커뮤니티의 기본 패러다임은 사전 훈련 후 동결하거나 소량의 전문가 시연으로 미세 조정하고, 배포는 단지 더 많은 시연을 수집하는 플라이휠에 불과했습니다. LWD가 진정으로 바꾼 것은 이 암묵적 가정입니다——더 이상 배포를 데이터 수집의 부산물로 보지 않고, 전체 차량군을 지속적으로 작동하는 강화학습 환경으로 간주합니다. 이는 실패 궤적, 부분적 진행, 인간 개입과 같이 과거에 폐기되거나 대조 학습에만 사용되던 신호들이 이제 가치 학습의 감독 소스가 된다는 것을 의미합니다.

더 깊은 변화는 후훈련 대상에 있습니다. 이전 후훈련 시스템(예: RT-X 계열 작업)은 주로 전용 정책, 즉 작업당 하나의 컨트롤러를 대상으로 했지만, LWD는 모든 작업을 포괄하는 단일 범용 정책 훈련을 고수합니다. 이는 단순한 엔지니어링 단순화가 아니라 '범용성'에 대한 재정의입니다: 범용성은 사전 훈련 시의 정적 속성이 아니라, 배포 중 이기종 경험을 통해 지속적으로 다듬어지는 동적 속성입니다. 저자들은 후훈련 수단으로서의 모방 학습을 명시적으로 포기하고 오프라인-투-온라인 RL로 전환했으며, 이는 가치 함수가 희소 지연 보상과 고도로 이기종적인 데이터 분포 하에서도 안정적으로 유지되어야 함을 요구합니다——이는 이전 시스템들이 의도적으로 회피했던 난점입니다.

## 방법 분석

LWD의 아키텍처는 폐루프입니다: 사전 훈련 정책을 차량군에 배포 → 자율 롤아웃과 인간 개입을 공유 재생 버퍼에 집계 → 중앙 집중식 learner가 정책 업데이트 → 주기적으로 차량군에 푸시.

### 2단계 훈련 프로세스
- **오프라인 단계**: 이전에 수집된 로봇 데이터(전문가 시연, 과거 롤아웃, 실패 모드 근처의 탐색 데이터)로 오프라인 RL 초기화를 수행하여 정책과 critic에 안정적인 출발점을 제공합니다. 이 단계의 핵심은 처음부터 온라인 탐색을 시작할 때의 콜드 스타트 문제를 피하는 것입니다.
- **온라인 단계**: 현재 정책을 차량군에 배포하고, 각 로봇이 실제 작업을 수행하며 궤적을 공유 온라인 버퍼에 업로드합니다; learner는 정적 오프라인 버퍼와 증가하는 온라인 버퍼의 혼합 데이터로 훈련하고 주기적으로 업데이트를 푸시합니다.

### 구성 요소 1: DIVL(분포적 암시적 가치 학습)
- 단일 스칼라 값을 회귀하는 대신 데이터셋 내 행동 값의 **분포**를 학습하고, 분위수 통계량을 TD 부트스트랩 목표로 추출합니다.
- IQL의 분포 내 가치 학습 원칙을 유지하여 분포 외 최대화로 인한 과대평가를 피합니다——이는 이기종 재생에서 특히 중요한데, 작업별 보상 희소성과 성공률 차이가 극심하기 때문입니다.
- 긴 horizon 작업에는 다단계 TD 목표를 사용하여 희소 종료 보상의 전파를 가속화합니다.

### 구성 요소 2: QAM(수반 매칭 Q-러닝)
- flow-matching 액션 헤드를 대상으로 설계되었습니다. 현대 VLA 정책은 다단계 노이즈 제거로 행동을 생성하며 해석적 우도를 가지지 않아 직접 정책 경사가 불안정합니다.
- critic 경사를 활용한 정책 추출을 flow 궤적을 따른 **국소 회귀**로 재구성하여 전체 생성 과정을 통한 역전파를 피합니다.

### 전체 분리
DIVL은 정책 평가(이기종 재생에서 가치 학습)를 담당하고, QAM은 정책 추출(가치를 flow 정책 업데이트로 변환)을 담당합니다. 이 둘의 분리로 가치 학습은 액션 헤드 구조와 독립적일 수 있고, 정책 업데이트는 가치 추정의 노이즈와 독립적일 수 있습니다.

논문에서 명시되지 않은 사항: DIVL의 분위수 파라미터 선택, QAM의 구체적 회귀 목표 형태, 오프라인/온라인 데이터 혼합 비율, learner의 업데이트 빈도와 푸시 주기.

## 핵심 혁신

1. **배포 자체를 RL 훈련 신호 소스로 전환**: 이전 데이터 플라이휠은 성공 궤적만 활용했지만, LWD는 실패, 부분적 진행, 인간 개입을 모두 가치 학습에 명시적으로 포함합니다. 이는 단순한 데이터 증강이 아니라 '무엇이 학습 신호를 구성하는가'에 대한 재정의입니다——실패 궤적은 TD 학습에서 부정적 가치 감독을 제공하고, 인간 개입은 성공 상태에 가까운 안내를 제공합니다.

2. **DIVL의 분포 가치 학습이 이기종 재생에 적응**: IQL은 이전에 주로 동질 데이터에 사용되었지만, LWD는 이를 분포 형태로 확장하여 분위수 통계량을 부트스트랩 목표로 사용합니다. 이 설계는 차량군 데이터의 핵심 모순에 직접 대응합니다——작업별 성공률, 보상 밀도, 개입 빈도 차이가 크며, 단일 스칼라 값은 이러한 변동성을 포착할 수 없지만 분포 형태는 자연스럽게 견고성을 갖습니다.

3. **QAM이 생성적 액션 헤드의 정책 추출 문제 해결**: flow-matching 액션 헤드는 현재 VLA의 주류 선택이지만 우도를 계산할 수 없어 최대 우도 기반 정책 추출과 직접 정책 경사 모두 불가능합니다. QAM은 critic 안내를 flow 궤적을 따른 국소 회귀로 재구성하여 전체 생성 과정의 역전파를 우회합니다——이는 생성적 정책의 RL 훈련에 대한 실용적 해법이며 액션 헤드 구조와 분리되어 있습니다.

## 실험 및 결과

실험은 Agibot G1 이중 팔 로봇 차량군에서 수행되었으며, 8가지 실제 조작 작업을 포함하고 여러 3-5분 지속의 긴 horizon 작업(쿵푸 차 만들기, 칵테일 제조, 주스 만들기, 신발 포장, 식료품 보충)을 포함합니다. 모든 작업을 포괄하는 단일 범용 정책을 훈련합니다.

| 지표 | LWD 성과 | 비교 기준선 |
|------|---------|---------|
| 성공률 | 기존 사전 훈련 후 훈련 기준선을 지속적으로 능가 | 논문에서 구체적 수치 미제시 |
| 평균 주기 시간(긴 horizon) | 감소 | 논문에서 구체적 수치 미제시 |
| 긴 horizon 작업 성공률 | 우위가 특히 두드러짐 | 논문에서 구체적 수치 미제시 |

모든 실험 영상은 1배속으로 표시되어 가속에 의한 오해를 배제합니다.

논문에서 명시되지 않은 사항: 구체적 성공률 수치, 주기 시간 수치, 기준선 방법 이름, 비-긴 horizon 작업에서의 개선 폭, 차량군 규모, 데이터 양, 훈련 설정.

결과의 의미: LWD는 성공률과 효율성(주기 시간 감소)을 동시에 향상시키며, 이는 오프라인-투-온라인 RL이 정책을 더 신뢰할 수 있게 할 뿐만 아니라 솔루션을 더 최적으로 만든다는 것을 의미합니다——이는 단순한 '여러 번 시도해서 성공'이 아니라 가치 학습이 실제로 더 효율적인 실행 경로를 찾았다는 것입니다.

## 경계와 한계

논문은 한계 논의 섹션을 명시적으로 제시하지 않았습니다. 사실적 요점에서 추론할 수 있는 경계는 다음과 같습니다:

- **전용 정책과의 비교 없음**: LWD 범용 정책과 작업별 전용 정책의 비교 세부 사항이 제공되지 않아 범용성으로 인한 성능 손실 폭을 판단할 수 없습니다.
- **대규모 배포 검증 없음**: 미래 방향으로만 제안되었으며, 더 많은 가정, 상점, 공장 시나리오에서 검증되지 않았습니다.
- **비-긴 horizon 작업 개선 폭 미언급**: 짧은 작업에서 개선이 제한적이라면 프레임워크의 가치는 긴 horizon 시나리오에 집중될 수 있습니다.
- **차량군 규모 미언급**: 프레임워크가 어느 정도의 차량군 규모에서 효과적인지, 통신 및 동기화 오버헤드가 규모에 따라 어떻게 증가하는지 판단할 수 없습니다.

논문에서 명시되지 않은 사항: LWD의 짧은 horizon 작업에서의 성능, 인간 개입 빈도에 대한 민감도, 오프라인 데이터 품질이 최종 성능에 미치는 영향.

## 엔지니어링 시사점

LWD를 재현할 때 가장 먼저 확인해야 할 것은 **가치 학습의 안정성**입니다——DIVL의 분위수 파라미터와 IQL의 기대 분위수 선택이 TD 목표의 노이즈 수준을 직접 결정하며, 이는 전체 프레임워크에서 가장 민감한 부분일 수 있습니다. 소규모 오프라인 데이터에서 DIVL이 안정적으로 수렴하는지 먼저 검증한 후 온라인 데이터를 도입하는 것을 권장합니다.

가장 함정에 빠지기 쉬운 부분은 **이기종 재생의 과적합**입니다: 온라인 버퍼가 빠르게 증가하므로 혼합 비율이 부적절하면 가치 함수가 최근 데이터에 급격히 편향되어 치명적 망각을 초래할 수 있습니다. 저자는 '순간 온라인 데이터의 과적합 방지'를 언급했지만 구체적 비율을 제시하지 않았으므로, 재현 시 오프라인/온라인 데이터 비율을 직접 실험으로 결정해야 합니다.

하위 팀에 대한 실질적 지침:

- 이미 VLA 사전 훈련 모델과 차량군 배포가 있다면, LWD의 가치는 기존 배포 데이터(실패 포함)를 정책 개선으로 전환하는 데 있으며 데이터 수집 프로세스를 재설계할 필요가 없습니다.
- QAM의 국소 회귀 설계는 가치 학습 부분을 다시 작성하지 않고 액션 헤드 구조를 교체할 수 있음을 의미합니다——아키텍처 분리는 엔지니어링상의 실질적 이점입니다.
- 긴 horizon 작업은 LWD의 강점 영역이며, 비즈니스 시나리오가 주로 짧은 작업이라면 이점이 제한적일 수 있으므로 소규모 검증 후 전체 투자 여부를 결정하십시오.
- Agibot(research@agibot.com)에 문의하여 더 많은 구현 세부 사항을 얻을 수 있지만, 논문이 훈련 설정을 공개하지 않았으므로 재현 비용이 높을 수 있습니다.
