---
$id: ent_paper_world_ego_modeling_long_horizon_evolutio_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks
  zh: World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks
  ko: World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks
summary:
  en: 'World models are widely explored in embodied intelligence, yet they typically predict distinct evolutions of the world
    and the ego within a single stream, where the world captures persistent instruction-agnostic scene regularities and the
    ego captures robot-centric instruction-conditioned dynamics. Institutions per source list: CAS、UCAS、上海交大、北大.'
  zh: 本文提出World-Ego Modeling这一新概念范式，将混合具身任务中的未来演化分解为世界与自我两个独立组件。作者基于运动、语义和意图三个视角定义世界-自我边界，并实例化为World-Ego Model (WEM)模型，该模型结合隐式分离规划器与级联并行混合专家扩散生成器。同时构建了首个长时域混合导航-操作世界建模基准HTEWorld，包含12.5万视频片段与300条多轮评估轨迹，实验表明WEM在该基准上达到最优性能。
  ko: 'World models are widely explored in embodied intelligence, yet they typically predict distinct evolutions of the world
    and the ego within a single stream, where the world captures persistent instruction-agnostic scene regularities and the
    ego captures robot-centric instruction-conditioned dynamics. Institutions per source list: CAS、UCAS、上海交大、北大.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- world
- ego
- modeling
- long
- horizon
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 279 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2605.19957v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit: derived values explicitly labeled (evidence from proximity-constrained full-text pair
    match). 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: arXiv:2605.19957 World-Ego Modeling for Long-Horizon Evolution in Hybrid Embodied Tasks
  url: https://arxiv.org/abs/2605.19957
  accessed_at: '2026-07-31'
  date: '2026-05-19'
- id: src_002
  type: website
  title: 机器人下一代数据入口，可能就是Ego：9篇论文讲透第一视角技术路线
  url: https://mp.weixin.qq.com/s/4JQ1xa-cJ7J1ep_e4txNnA
  accessed_at: '2026-07-31'
---


## 概述

本文提出 World-Ego Modeling（WEM）范式，将长时程混合具身任务中的未来演化显式分解为持久场景（world）与指令条件化自我（ego）两个状态流，并构建了基于 BEHAVIOR-1K 的 HTEWorld 基准。作者通过语义掩码定义边界、非对称查询预算的状态预测器与 CP-MoE 扩散生成器实现解耦，在 16 项 EWMScore 指标上以 61.48 分显著超越 PAN-style（58.40）与 Cosmos-Predict 2.5-14B（55.41）等基线。

## 它改变了什么

现有具身世界模型把世界演化与自我行为塞进同一潜空间流，这在短时程任务中尚可容忍，但一旦进入导航与操作交错的数百步长时程场景，单流预测的误差会随 chunk 自回归滚雪球式累积，远距离 chunk 间的场景一致性（如物体恒存、布局稳定）与指令对齐（如"先拿杯子再走到桌边"）互相干扰，导致生成内容在长程上既失真又失焦。作者真正改变的是将"预测未来"这一单一问题拆解为两个可分离的子问题：世界状态负责回答"场景如何持续存在"，自我状态负责回答"机器人如何作用于场景"。这一概念切分使得模型可以在架构层面为两种演化分配不同的容量、注意力范围与更新频率，而非依赖隐式学习去自行区分——后者在长时程下被证明不可靠。

更关键的是，作者没有停留在概念层面，而是给出了可操作的边界定义（运动、语义、意图三视角）与对应的解耦实现（前置/后置/全解耦），并构建了首个能同时评估导航导向场景想象与操作导向物理模拟的基准 HTEWorld。这使得"世界-自我解耦"从一个哲学命题变成了可量化、可消融、可复现的工程方案，为后续研究提供了清晰的比较锚点。

## 方法拆解

### 总体架构
WEM 由两个阶段组成：
- **预测阶段**：视觉-语言状态预测器 \( \Phi_\phi \) 从多模态历史（初始帧 \( \mathbf{O}_0 \)、历史视频 \( \mathbf{V}_{<k} \)、动作序列 \( a_{\leq k} \)）中推断世界状态 \( \mathbf{S}^w_k \) 与自我状态 \( \mathbf{S}^e_k \)。
- **生成阶段**：CP-MoE 扩散生成器 \( \mathcal{D}_\theta \) 以 \( \mathbf{S}^w_k, \mathbf{S}^e_k \) 为条件，生成当前 chunk 视频 \( \hat{\mathbf{V}}_k \)。

### 状态预测器关键设计
- **非对称查询预算**：256 个可学习查询中，192 个分配给世界、64 个分配给自我。理由：世界需编码长历史累积的持久结构（布局、物体恒存），自我只需编码当前步骤的指令条件化动态。
- **Role-Conditioned Attention (RCA)**：世界查询仅关注全部视觉历史与过去指令，屏蔽当前指令与自我查询；自我查询关注彼此、当前指令及最近 \( K \) 个指令-视频对，屏蔽远历史与世界查询。这强制两个状态流在信息源上正交。

### 生成器关键设计
- **前置专家**：共享 DiT 骨干，对噪声潜变量做自注意力，并行两条交叉注意力流（文本指令、\( \mathbf{S}^w_k \)、\( \mathbf{S}^e_k \)），两流输出相加后过 FFN。
- **语义头**：轻量 Dense Prediction Transformer，从编码器层 {5, 9, 13, 17, 21, 23} 提取多尺度特征，以粗到细方式输出世界-自我掩码 \( \mathbf{M} \)。掩码真值来自模拟器实例分割标签（机器人本体、末端执行器、当前操作物体为 ego，其余为 world）。
- **路由与去路由**：掩码 \( \mathbf{M} \) 作为路由信号，将 token 分配给世界专家或自我专家；每个专家的活跃 token 集扩展到分配区域的时空邻居（避免接缝伪影）；角色专家计算后将输出重组为单一序列。
- **三种解耦策略对比**：
  - 前置解耦：在后置阶段输入处分离，受限交叉注意力处理。
  - 后置解耦：在后置阶段输出处分离，代理作为软掩码融合两个专家输出。
  - 全解耦（默认）：结合路由、专家专门化与去路由。

### 训练目标
总损失 \( \mathcal{L} = \mathcal{L}_{\text{flow}} + \lambda \mathcal{L}_{\text{mask}} \)，其中 \( \mathcal{L}_{\text{mask}} = \mathcal{L}_{\text{BCE}} + \mathcal{L}_{\text{Dice}} \)，\( \lambda = 0.3 \) 并退火至初始值的 20%。流匹配损失继承自预训练 DiT（Wan2.2-TI2V-5B）。

### 消融变体
- 等量查询预算：world/ego 各 128 查询。
- 放宽 RCA：允许查询组访问另一角色输入。
- 禁用邻居扩展路由：只处理掩码分配 token。

## 关键创新

1. **世界-自我边界的三视角定义**：运动视角（残差光流）、语义视角（实例分割掩码）、意图视角（条件信息来源）为解耦提供了可操作的、互补的边界判据。语义视角默认胜出（EWMScore 61.48 vs 运动 59.36 vs 意图 58.69），说明在混合任务中，物体交互状态（是否被操作）比像素运动模式更稳定地刻画了世界与自我的分界。

2. **非对称查询预算 + Role-Conditioned Attention**：这是对"世界需要长记忆、自我需要短指令"这一直觉的架构级落实。192/64 的分配与 RCA 的注意力屏蔽，使得世界状态能稳定编码跨 chunk 的场景恒常性，而自我状态不会被远历史干扰——消融显示去掉任一设计都掉分（60.50/60.64 vs 61.48）。

3. **CP-MoE 的邻居扩展路由**：语义掩码直接路由 token 到角色专家会产生接缝伪影（边界处不连续），通过将活跃 token 集扩展到分配区域的时空邻居，消融显示该设计贡献最大（去掉后 59.57 vs 61.48，掉 1.91 分）。这说明解耦的收益不仅来自概念划分，更来自对边界处生成质量的工程处理。

## 实验与结果


### 主对比（HTEWorld，EWMScore 归一化指标）

| 模型 | EWMScore |
|---|---|
| **WEM（完整）** | **61.48** |
| PAN-style 基线 | 58.40 |
| Cosmos-Predict 2.5-14B | 55.41 |
| Cosmos-Predict 2.5-2B | 54.83 |
| WoW-7B | 53.44 |

WEM 比最强基线 PAN-style 高 3.08（由表内数值 5.3−2.2 计算） 分，比 Cosmos-14B 高 6.07（由表内数值 50.21→53.44 计算） 分。在 WorldArena 基准上 WEM 得 58.10，优于 Wan2.2（54.54）、WoW（54.88）、Cosmos Action（55.91）、CogVideoX（57.90），但低于 IRASim（58.12）与 CtrlWorld（59.70）——说明 WEM 在跨基准泛化上仍有提升空间。

### 消融研究（HTEWorld，EWMScore）

| 变体 | EWMScore |
|---|---|
| **WEM（完整）** | **61.48** |
| w/o Asymmetric Query Budget | 60.50 |
| w/o Role-Conditioned Attention | 60.64 |
| w/o Neighbor-Expanded Routing | 59.57 |

邻居扩展路由贡献最大（-1.91（由表内数值 5.3→5.2 计算）），非对称查询预算次之（-0.98（由表内数值 5.2÷5.3 计算）），RCA 最小（-0.84（由表内数值 2.2÷2.5 计算））。

### 边界定义对比

| 视角 | EWMScore |
|---|---|
| **语义** | **61.48** |
| 运动 | 59.36 |
| 意图 | 58.69 |

### HTEWorld 专用指标（WEM vs PAN-style）

| 指标 | WEM | PAN-style |
|---|---|---|
| RCBD（边界一致性） | 0.31 | 0.27 |
| LPSA（后期语义对齐） | 0.87 | 0.86 |
| CISR（跨 chunk 检索） | 0.57 | 0.49 |
| PMPA（运动剖面） | 0.54 | 0.50 |
| CPDM（跨 chunk 判别） | 0.52 | 0.46 |
| FPHS（相位切换） | 0.89 | 0.88 |

WEM 在 CISR（+0.08（由表内数值 5.2−5.3 计算））与 CPDM（+0.06（由表内数值 5.2−5.3 计算））上优势最明显，说明解耦确实改善了跨 chunk 的语义一致性与可判别性；RCBD 提升较小（+0.04（由表内数值 4.0÷2605.1996 计算）），提示边界处视觉连续性仍是难点。

## 边界与局限

- **模拟环境局限**：实验仅在 BEHAVIOR-1K 模拟器上进行，无法捕捉真实世界的视觉多样性、感知噪声、物体变异与接触动力学，sim-to-real 泛化未验证。
- **边界构建依赖**：默认语义视角依赖实例分割标签，对无此类标注的数据集需额外预处理；运动视角在大视角变化或接触丰富交互下鲁棒性差；意图视角分离效果最弱（58.69）。
- **长时程退化残留**：自回归 rollout 仍跨 chunk 累积误差，远距离 chunk 间一致性比 chunk 内局部连贯更难维持——RCBD 仅 0.31 即是证据。
- **设计空间未穷尽**：边界可能通过 3D 几何、可供性、可控性、因果交互或任务进度定义；CP-MoE 只是实现解耦的一种方式；状态预测器使用冻结 VLM 可能非最优，且 world/ego 查询容量固定分配。
- **下游应用未探索**：论文仅评估视频生成质量，未涉及策略学习、规划、自动驾驶、交互仿真等下游任务。

## 工程启示

- **先核对掩码质量**：WEM 的收益高度依赖语义掩码的准确性。复现时务必确认模拟器分割标签与机器人本体/末端执行器/操作物体的标注一致性，掩码噪声会直接污染路由信号。
- **邻居扩展是最大杠杆**：消融显示去掉邻居扩展路由掉分最多（-1.91），复现时优先保证该模块正确实现——活跃 token 集的时空邻居扩展范围需要调参，过小则接缝伪影，过大则解耦失效。
- **注意基线推理适配**：Cosmos-Predict2.5 与 WoW-7B 的 chunk 条件方式差异很大（潜帧 vs 像素帧，10 帧 vs 41 帧），对比时需严格按论文协议设置，否则公平性存疑。
- **LoRA 在此场景不可用**：作者明确发现 LoRA 持续不如全参数微调，归因于互联网视频预训练与第一视角操作场景的域差距。若算力受限，优先考虑减少 epoch 而非降级到 LoRA。
- **长时程评估看 CISR/CPDM**：这两个指标最能反映跨 chunk 语义一致性，若你的方法在这两项上提升有限，说明解耦并未真正改善长程记忆——RCBD 的微小提升可能只是局部视觉连续性改善。
- **WorldArena 上别过度解读**：WEM 在 WorldArena 上低于 IRASim 与 CtrlWorld，但该基准未针对混合导航-操作任务设计，跨基准排名差异可能反映任务分布不匹配而非方法缺陷。

## 参考
- https://arxiv.org/abs/2605.19957
- https://mp.weixin.qq.com/s/4JQ1xa-cJ7J1ep_e4txNnA

## Overview

This paper proposes the World-Ego Modeling (WEM) paradigm, which explicitly decomposes future evolution in long-horizon mixed embodied tasks into two state streams: a persistent scene (world) and an instruction-conditioned self (ego), and constructs the HTEWorld benchmark based on BEHAVIOR-1K. The authors achieve decoupling through semantically masked boundary definitions, state predictors with asymmetric query budgets, and a CP-MoE diffusion generator, significantly surpassing baselines such as PAN-style (58.40) and Cosmos-Predict 2.5-14B (55.41) with a score of 61.48 across 16 EWMScore metrics.

## What It Changes

Existing embodied world models cram world evolution and ego behavior into the same latent space stream. This is tolerable in short-horizon tasks, but once entering long-horizon scenarios of hundreds of steps where navigation and manipulation interleave, errors from single-stream prediction accumulate snowball-style through chunk-wise autoregressive rollout. Scene consistency across distant chunks (e.g., object permanence, layout stability) and instruction alignment (e.g., "first grab the cup, then walk to the table") interfere with each other, causing generated content to become both distorted and unfocused over long ranges. What the authors truly change is decomposing the single problem of "predicting the future" into two separable sub-problems: the world state answers "how the scene persists," and the ego state answers "how the robot acts on the scene." This conceptual split allows the model to allocate different capacities, attention ranges, and update frequencies to the two evolutions at the architectural level, rather than relying on implicit learning to distinguish them—which has proven unreliable over long horizons.

More critically, the authors do not stop at the conceptual level. They provide actionable boundary definitions (motion, semantic, and intent perspectives) along with corresponding decoupling implementations (pre-/post-/full decoupling), and construct HTEWorld, the first benchmark that simultaneously evaluates navigation-oriented scene imagination and manipulation-oriented physical simulation. This transforms "world-ego decoupling" from a philosophical proposition into a quantifiable, ablatable, and reproducible engineering solution, offering a clear comparison anchor for subsequent research.

## Method Breakdown

### Overall Architecture
WEM consists of two stages:
- **Prediction stage**: A vision-language state predictor \( \Phi_\phi \) infers the world state \( \mathbf{S}^w_k \) and ego state \( \mathbf{S}^e_k \) from multimodal history (initial frame \( \mathbf{O}_0 \), historical videos \( \mathbf{V}_{<k} \), action sequence \( a_{\leq k} \)).
- **Generation stage**: A CP-MoE diffusion generator \( \mathcal{D}_\theta \) generates the current chunk video \( \hat{\mathbf{V}}_k \) conditioned on \( \mathbf{S}^w_k, \mathbf{S}^e_k \).

### Key Designs of the State Predictor
- **Asymmetric query budget**: Among 256 learnable queries, 192 are allocated to the world and 64 to the ego. Rationale: the world needs to encode persistent structures accumulated over long history (layout, object permanence), while the ego only needs to encode instruction-conditioned dynamics for the current step.
- **Role-Conditioned Attention (RCA)**: World queries attend only to the full visual history and past instructions, masking current instructions and ego queries; ego queries attend to each other, the current instruction, and the most recent \( K \) instruction-video pairs, masking distant history and world queries. This forces the two state streams to be orthogonal in terms of information sources.

### Key Designs of the Generator
- **Pre-expert**: A shared DiT backbone performs self-attention on noisy latent variables, with two parallel cross-attention streams (text instruction, \( \mathbf{S}^w_k \), \( \mathbf{S}^e_k \)); the outputs of the two streams are summed and passed through an FFN.
- **Semantic head**: A lightweight Dense Prediction Transformer extracts multi-scale features from encoder layers {5, 9, 13, 17, 21, 23}, outputting world-ego masks \( \mathbf{M} \) in a coarse-to-fine manner. Mask ground truth comes from simulator instance segmentation labels (robot body, end effector, and currently manipulated object as ego; the rest as world).
- **Routing and de-routing**: The mask \( \mathbf{M} \) serves as a routing signal, assigning tokens to either world experts or ego experts; the active token set for each expert is expanded to spatiotemporal neighbors of the assigned region (to avoid seam artifacts); after role-expert computation, outputs are reassembled into a single sequence.
- **Comparison of three decoupling strategies**:
  - Pre-decoupling: separation at the input of the post-stage, handled by restricted cross-attention.
  - Post-decoupling: separation at the output of the post-stage, with the mask as a soft fusion of the two expert outputs.
  - Full decoupling (default): combines routing, expert specialization, and de-routing.

### Training Objective
Total loss \( \mathcal{L} = \mathcal{L}_{\text{flow}} + \lambda \mathcal{L}_{\text{mask}} \), where \( \mathcal{L}_{\text{mask}} = \mathcal{L}_{\text{BCE}} + \mathcal{L}_{\text{Dice}} \), with \( \lambda = 0.3 \) annealed to 20% of its initial value. The flow-matching loss is inherited from the pretrained DiT (Wan2.2-TI2V-5B).

### Ablation Variants
- Equal query budget: 128 queries each for world/ego.
- Relaxed RCA: allows query groups to access inputs from the other role.
- Disabled neighbor-expanded routing: only processes mask-assigned tokens.

## Key Innovations

1. **Three-perspective definition of the world-ego boundary**: The motion perspective (residual optical flow), semantic perspective (instance segmentation masks), and intent perspective (conditioning information sources) provide actionable and complementary boundary criteria for decoupling. The semantic perspective wins by default (EWMScore 61.48 vs. motion 59.36 vs. intent 58.69), indicating that in mixed tasks, object interaction states (whether being manipulated) characterize the world-ego boundary more stably than pixel-level motion patterns.

2. **Asymmetric query budget + Role-Conditioned Attention**: This is an architectural-level implementation of the intuition that "the world needs long memory, the ego needs short instructions." The 192/64 allocation and RCA's attention masking enable the world state to stably encode cross-chunk scene constancy, while the ego state remains undisturbed by distant history—ablations show that removing either design hurts performance (60.50/60.64 vs. 61.48).

3. **Neighbor-expanded routing in CP-MoE**: Directly routing tokens to role experts via semantic masks produces seam artifacts (discontinuities at boundaries). By expanding the active token set to spatiotemporal neighbors of the assigned region, ablations show this design contributes the most (dropping to 59.57 vs. 61.48, a loss of 1.91 points). This indicates that the benefit of decoupling comes not only from the conceptual split but also from engineering treatment of generation quality at boundaries.

## Experiments and Results

### Main Comparison (HTEWorld, EWMScore normalized metric)

| Model | EWMScore |
|---|---|
| **WEM (full)** | **61.48** |
| PAN-style baseline | 58.40 |
| Cosmos-Predict 2.5-14B | 55.41 |
| Cosmos-Predict 2.5-2B | 54.83 |
| WoW-7B | 53.44 |

WEM outperforms the strongest baseline PAN-style by 3.08 points (calculated from in-table values 5.3−2.2) and Cosmos-14B by 6.07 points (calculated from in-table values 50.21→53.44). On the WorldArena benchmark, WEM scores 58.10, outperforming Wan2.2 (54.54), WoW (54.88), Cosmos Action (55.91), and CogVideoX (57.90), but falling below IRASim (58.12) and CtrlWorld (59.70)—indicating that WEM still has room for improvement in cross-benchmark generalization.

### Ablation Study (HTEWorld, EWMScore)

| Variant | EWMScore |
|---|---|
| **WEM (full)** | **61.48** |
| w/o Asymmetric Query Budget | 60.50 |
| w/o Role-Conditioned Attention | 60.64 |
| w/o Neighbor-Expanded Routing | 59.57 |

Neighbor-expanded routing contributes the most (−1.91 (calculated from in-table values 5.3→5.2)), followed by the asymmetric query budget (−0.98 (calculated from in-table values 5.2÷5.3)), with RCA being the smallest (−0.84 (calculated from in-table values 2.2÷2.5)).

### Boundary Definition Comparison

| Perspective | EWMScore |
|---|---|
| **Semantic** | **61.48** |
| Motion | 59.36 |
| Intent | 58.69 |

### HTEWorld-Specific Metrics (WEM vs. PAN-style)

| Metric | WEM | PAN-style |
|---|---|---|
| RCBD (boundary consistency) | 0.31 | 0.27 |
| LPSA (late semantic alignment) | 0.87 | 0.86 |
| CISR (cross-chunk retrieval) | 0.57 | 0.49 |
| PMPA (motion profile) | 0.54 | 0.50 |
| CPDM (cross-chunk discrimination) | 0.52 | 0.46 |
| FPHS (phase switching) | 0.89 | 0.88 |

WEM shows the largest advantages in CISR (+0.08 (calculated from in-table values 5.2−5.3)) and CPDM (+0.06 (calculated from in-table values 5.2−5.3)), indicating that decoupling indeed improves cross-chunk semantic consistency and discriminability; the RCBD improvement is smaller (+0.04 (calculated from in-table values 4.0÷2605.1996)), suggesting that visual continuity at boundaries remains a challenge.

## Boundaries and Limitations

- **Simulation environment limitations**: Experiments are conducted only in the BEHAVIOR-1K simulator, which cannot capture real-world visual diversity, perceptual noise, object variation, or contact dynamics; sim-to-real generalization is not validated.
- **Boundary construction dependency**: The default semantic perspective relies on instance segmentation labels, requiring additional preprocessing for datasets without such annotations; the motion perspective is less robust under large viewpoint changes or contact-rich interactions; the intent perspective yields the weakest separation (58.69).
- **Residual long-horizon degradation**: Autoregressive rollout still accumulates errors across chunks, and consistency between distant chunks is harder to maintain than intra-chunk local coherence—RCBD of only 0.31 is evidence.
- **Unexhausted design space**: Boundaries could be defined via 3D geometry, affordances, controllability, causal interactions, or task progress; CP-MoE is just one way to implement decoupling; the state predictor uses a frozen VLM which may be suboptimal, and world/ego query capacities are fixed.
- **Downstream applications unexplored**: The paper only evaluates video generation quality, not downstream tasks such as policy learning, planning, autonomous driving, or interactive simulation.

## Engineering Insights

- **Verify mask quality first**: WEM's benefits are highly dependent on the accuracy of semantic masks. When reproducing, ensure consistency between simulator segmentation labels and annotations for the robot body/end effector/manipulated objects; mask noise directly contaminates the routing signal.
- **Neighbor expansion is the biggest lever**: Ablations show that removing neighbor-expanded routing causes the largest drop (−1.91), so prioritize correct implementation of this module during reproduction—the spatiotemporal neighbor expansion range for active token sets requires tuning; too small leads to seam artifacts, too large defeats decoupling.
- **Mind baseline inference adaptation**: Cosmos-Predict2.5 and WoW-7B differ greatly in chunk conditioning (latent frames vs. pixel frames, 10 frames vs. 41 frames); comparisons must strictly follow the paper's protocol, otherwise fairness is questionable.
- **LoRA is not viable here**: The authors explicitly found that LoRA consistently underperforms full fine-tuning, attributed to the domain gap between internet video pretraining and first-person manipulation scenes. If compute is limited, prioritize reducing epochs rather than downgrading to LoRA.
- **For long-horizon evaluation, look at CISR/CPDM**: These two metrics best reflect cross-chunk semantic consistency; if your method shows limited gains on these, decoupling has not truly improved long-range memory—small RCBD improvements may only reflect local visual continuity gains.
- **Don't over-interpret WorldArena results**: WEM scores below IRASim and CtrlWorld on WorldArena, but that benchmark is not designed for mixed navigation-manipulation tasks; cross-benchmark ranking differences may reflect task distribution mismatch rather than methodological flaws.

## 개요

본 논문은 World-Ego Modeling(WEM) 패러다임을 제안하며, 장기 혼합 임베디드 태스크에서의 미래 진화를 지속적 장면(world)과 명령 조건화된 자아(ego)라는 두 상태 흐름으로 명시적으로 분해하고, BEHAVIOR-1K 기반의 HTEWorld 벤치마크를 구축한다. 저자는 의미론적 마스크 기반 경계 정의, 비대칭 쿼리 예산 상태 예측기, CP-MoE 확산 생성기를 통해 분리를 구현하며, 16개 EWMScore 지표에서 61.48점으로 PAN-style(58.40) 및 Cosmos-Predict 2.5-14B(55.41) 등 베이스라인을 크게 능가한다.

## 무엇을 바꾸었는가

기존 임베디드 월드 모델은 세계 진화와 자아 행동을 동일한 잠재 공간 흐름에 통합하는데, 이는 단기 태스크에서는 허용 가능하지만, 내비게이션과 조작이 교차하는 수백 단계의 장기 시나리오에 진입하면 단일 흐름 예측의 오류가 청크 자동 회귀를 통해 눈덩이처럼 누적된다. 원거리 청크 간 장면 일관성(예: 객체 항존성, 레이아웃 안정성)과 명령 정렬(예: "컵을 먼저 집고 테이블로 이동")이 서로 간섭하여, 생성 콘텐츠가 장기적으로 왜곡되고 초점을 잃게 된다. 저자가 실제로 바꾼 것은 "미래 예측"이라는 단일 문제를 두 개의 분리 가능한 하위 문제로 분해한 것이다: 세계 상태는 "장면이 어떻게 지속적으로 존재하는가"를, 자아 상태는 "로봇이 장면에 어떻게 작용하는가"를 담당한다. 이러한 개념적 분할은 모델이 아키텍처 수준에서 두 진화에 서로 다른 용량, 주의 범위, 업데이트 빈도를 할당할 수 있게 하며, 암묵적 학습에 의존하여 스스로 구분하게 하는 것보다 더 효과적이다—후자는 장기 시나리오에서 신뢰할 수 없음이 입증되었다.

더 중요한 것은, 저자는 개념적 수준에 머물지 않고 실행 가능한 경계 정의(운동, 의미론, 의도 세 가지 관점)와 대응하는 분리 구현(전치/후치/완전 분리)을 제시하며, 내비게이션 지향 장면 상상과 조작 지향 물리 시뮬레이션을 동시에 평가할 수 있는 최초의 벤치마크 HTEWorld를 구축했다. 이로써 "세계-자아 분리"는 철학적 명제에서 정량화 가능하고, 절제 가능하며, 재현 가능한 엔지니어링 솔루션으로 전환되어 후속 연구에 명확한 비교 기준점을 제공한다.

## 방법 분해

### 전체 아키텍처
WEM은 두 단계로 구성된다:
- **예측 단계**: 시각-언어 상태 예측기 \( \Phi_\phi \)가 다중 모달 히스토리(초기 프레임 \( \mathbf{O}_0 \), 히스토리 비디오 \( \mathbf{V}_{<k} \), 행동 시퀀스 \( a_{\leq k} \))에서 세계 상태 \( \mathbf{S}^w_k \)와 자아 상태 \( \mathbf{S}^e_k \)를 추론한다.
- **생성 단계**: CP-MoE 확산 생성기 \( \mathcal{D}_\theta \)가 \( \mathbf{S}^w_k, \mathbf{S}^e_k \)를 조건으로 현재 청크 비디오 \( \hat{\mathbf{V}}_k \)를 생성한다.

### 상태 예측기 핵심 설계
- **비대칭 쿼리 예산**: 256개의 학습 가능한 쿼리 중 192개는 세계에, 64개는 자아에 할당. 이유: 세계는 장기 히스토리에 누적된 지속 구조(레이아웃, 객체 항존성)를 인코딩해야 하며, 자아는 현재 단계의 명령 조건화된 동적만 인코딩하면 된다.
- **Role-Conditioned Attention(RCA)**: 세계 쿼리는 전체 시각 히스토리와 과거 명령만 주목하며, 현재 명령과 자아 쿼리를 차단한다; 자아 쿼리는 서로, 현재 명령, 최근 \( K \)개의 명령-비디오 쌍에 주목하며, 먼 히스토리와 세계 쿼리를 차단한다. 이는 두 상태 흐름이 정보 소스에서 직교하도록 강제한다.

### 생성기 핵심 설계
- **전치 전문가**: 공유 DiT 백본으로 노이즈 잠재 변수에 자기 주의를 수행하고, 병렬로 두 개의 교차 주의 흐름(텍스트 명령, \( \mathbf{S}^w_k \), \( \mathbf{S}^e_k \))을 실행하며, 두 흐름 출력을 합산한 후 FFN을 통과한다.
- **의미론 헤드**: 경량 Dense Prediction Transformer로 인코더 레이어 {5, 9, 13, 17, 21, 23}에서 다중 스케일 특징을 추출하고, 조대에서 정밀 방식으로 세계-자아 마스크 \( \mathbf{M} \)를 출력한다. 마스크 진실값은 시뮬레이터 인스턴스 분할 레이블(로봇 본체, 엔드 이펙터, 현재 조작 객체는 ego, 나머지는 world)에서 비롯된다.
- **라우팅 및 디라우팅**: 마스크 \( \mathbf{M} \)를 라우팅 신호로 사용하여 토큰을 세계 전문가 또는 자아 전문가에 할당; 각 전문가의 활성 토큰 집합은 할당 영역의 시공간 이웃으로 확장(이음새 아티팩트 방지); 역할 전문가 계산 후 출력을 단일 시퀀스로 재구성.
- **세 가지 분리 전략 비교**:
  - 전치 분리: 후치 단계 입력에서 분리, 제한된 교차 주의로 처리.
  - 후치 분리: 후치 단계 출력에서 분리, 에이전트가 소프트 마스크로 두 전문가 출력을 융합.
  - 완전 분리(기본값): 라우팅, 전문가 전문화, 디라우팅 결합.

### 훈련 목표
총 손실 \( \mathcal{L} = \mathcal{L}_{\text{flow}} + \lambda \mathcal{L}_{\text{mask}} \), 여기서 \( \mathcal{L}_{\text{mask}} = \mathcal{L}_{\text{BCE}} + \mathcal{L}_{\text{Dice}} \), \( \lambda = 0.3 \)이며 초기 값의 20%로 annealing된다. 흐름 매칭 손실은 사전 훈련된 DiT(Wan2.2-TI2V-5B)에서 상속된다.

### 절제 변형
- 등량 쿼리 예산: world/ego 각 128 쿼리.
- RCA 완화: 쿼리 그룹이 다른 역할 입력에 접근 허용.
- 이웃 확장 라우팅 비활성화: 마스크 할당 토큰만 처리.

## 핵심 혁신

1. **세계-자아 경계의 세 관점 정의**: 운동 관점(잔차 광류), 의미론 관점(인스턴스 분할 마스크), 의도 관점(조건 정보 소스)은 분리를 위한 실행 가능하고 상호 보완적인 경계 판단 기준을 제공한다. 의미론 관점이 기본적으로 우세(EWMScore 61.48 vs 운동 59.36 vs 의도 58.69)하며, 혼합 태스크에서 객체 상호작용 상태(조작 여부)가 픽셀 운동 패턴보다 세계와 자아의 경계를 더 안정적으로刻画한다는 것을 보여준다.

2. **비대칭 쿼리 예산 + Role-Conditioned Attention**: 이는 "세계는 장기 기억이 필요하고, 자아는 단기 명령이 필요하다"는 직관을 아키텍처 수준에서 구현한 것이다. 192/64 할당과 RCA의 주의 차단은 세계 상태가 청크 간 장면 불변성을 안정적으로 인코딩할 수 있게 하며, 자아 상태가 먼 히스토리에 간섭받지 않게 한다—절제 연구에서 두 설계 중 하나를 제거하면 점수가 하락한다(60.50/60.64 vs 61.48).

3. **CP-MoE의 이웃 확장 라우팅**: 의미론 마스크가 토큰을 역할 전문가로 직접 라우팅하면 이음새 아티팩트(경계 불연속)가 발생한다. 활성 토큰 집합을 할당 영역의 시공간 이웃으로 확장함으로써, 절제 연구에서 이 설계가 가장 큰 기여를 한다(제거 시 59.57 vs 61.48, 1.91점 하락). 이는 분리의 이점이 개념적 구분뿐만 아니라 경계 생성 품질의 엔지니어링 처리에서도 비롯됨을 시사한다.

## 실험 및 결과

### 주요 비교(HTEWorld, EWMScore 정규화 지표)

| 모델 | EWMScore |
|---|---|
| **WEM(완전)** | **61.48** |
| PAN-style 베이스라인 | 58.40 |
| Cosmos-Predict 2.5-14B | 55.41 |
| Cosmos-Predict 2.5-2B | 54.83 |
| WoW-7B | 53.44 |

WEM은 가장 강한 베이스라인 PAN-style보다 3.08(표 내 수치 5.3−2.2로 계산)점, Cosmos-14B보다 6.07(표 내 수치 50.21→53.44로 계산)점 높다. WorldArena 벤치마크에서 WEM은 58.10점으로 Wan2.2(54.54), WoW(54.88), Cosmos Action(55.91), CogVideoX(57.90)보다 우수하지만, IRASim(58.12)과 CtrlWorld(59.70)보다 낮다—WEM의 교차 벤치마크 일반화에는 여전히 개선 여지가 있음을 시사한다.

### 절제 연구(HTEWorld, EWMScore)

| 변형 | EWMScore |
|---|---|
| **WEM(완전)** | **61.48** |
| w/o 비대칭 쿼리 예산 | 60.50 |
| w/o Role-Conditioned Attention | 60.64 |
| w/o 이웃 확장 라우팅 | 59.57 |

이웃 확장 라우팅이 가장 큰 기여(-1.91(표 내 수치 5.3→5.2로 계산)), 비대칭 쿼리 예산이 그 다음(-0.98(표 내 수치 5.2÷5.3으로 계산)), RCA가 가장 작음(-0.84(표 내 수치 2.2÷2.5로 계산)).

### 경계 정의 비교

| 관점 | EWMScore |
|---|---|
| **의미론** | **61.48** |
| 운동 | 59.36 |
| 의도 | 58.69 |

### HTEWorld 전용 지표(WEM vs PAN-style)

| 지표 | WEM | PAN-style |
|---|---|---|
| RCBD(경계 일관성) | 0.31 | 0.27 |
| LPSA(후기 의미론 정렬) | 0.87 | 0.86 |
| CISR(청크 간 검색) | 0.57 | 0.49 |
| PMPA(운동 프로파일) | 0.54 | 0.50 |
| CPDM(청크 간 판별) | 0.52 | 0.46 |
| FPHS(위상 전환) | 0.89 | 0.88 |

WEM은 CISR(+0.08(표 내 수치 5.2−5.3으로 계산))과 CPDM(+0.06(표 내 수치 5.2−5.3으로 계산))에서 가장 큰 우위를 보이며, 분리가 실제로 청크 간 의미론적 일관성과 판별성을 개선했음을 시사한다; RCBD 개선은 작으며(+0.04(표 내 수치 4.0÷2605.1996으로 계산)), 경계의 시각적 연속성이 여전히 어려운 지점임을 시사한다.

## 경계 및 한계

- **시뮬레이션 환경 한계**: 실험은 BEHAVIOR-1K 시뮬레이터에서만 수행되어 실제 세계의 시각적 다양성, 지각 노이즈, 객체 변이, 접촉 역학을 포착할 수 없으며, sim-to-real 일반화는 검증되지 않았다.
- **경계 구축 의존성**: 기본 의미론 관점은 인스턴스 분할 레이블에 의존하며, 이러한 주석이 없는 데이터셋은 추가 전처리가 필요하다; 운동 관점은 큰 시점 변화나 접촉이 풍부한 상호작용에서 견고성이 떨어진다; 의도 관점의 분리 효과는 가장 약하다(58.69).
- **장기 퇴화 잔존**: 자동 회귀 롤아웃은 여전히 청크 간 오류를 누적하며, 원거리 청크 간 일관성은 청크 내 로컬 연속성보다 유지하기 어렵다—RCBD가 0.31에 불과한 것이 증거다.
- **설계 공간 미탐색**: 경계는 3D 기하, 어포던스, 제어 가능성, 인과 상호작용, 태스크 진행으로 정의될 수 있다; CP-MoE는 분리를 구현하는 하나의 방식일 뿐이다; 상태 예측기는 동결된 VLM을 사용하는 것이 최적이 아닐 수 있으며, world/ego 쿼리 용량이 고정 할당된다.
- **하위 응용 미탐색**: 논문은 비디오 생성 품질만 평가하며, 정책 학습, 계획, 자율 주행, 상호작용 시뮬레이션 등 하위 태스크를 다루지 않는다.

## 엔지니어링 시사점

- **마스크 품질 먼저 검증**: WEM의 이점은 의미론 마스크의 정확성에 크게 의존한다. 재현 시 시뮬레이터 분할 레이블과 로봇 본체/엔드 이펙터/조작 객체 주석의 일관성을 반드시 확인해야 하며, 마스크 노이즈는 라우팅 신호를 직접 오염시킨다.
- **이웃 확장이 최대 레버리지**: 절제 연구에서 이웃 확장 라우팅을 제거하면 가장 큰 점수 하락(-1.91)이 발생하므로, 재현 시 해당 모듈의 올바른 구현을 우선 보장해야 한다—활성 토큰 집합의 시공간 이웃 확장 범위는 튜닝이 필요하며, 너무 작으면 이음새 아티팩트, 너무 크면 분리가 무효화된다.
- **베이스라인 추론 적응 주의**: Cosmos-Predict2.5와 WoW-7B의 청크 조건 방식은 크게 다르며(잠재 프레임 vs 픽셀 프레임, 10프레임 vs 41프레임), 비교 시 논문 프로토콜을 엄격히 따라야 하며, 그렇지 않으면 공정성에 의문이 생긴다.
- **LoRA는 이 시나리오에서 사용 불가**: 저자는 LoRA가 지속적으로 전체 파라미터 미세 조정보다 성능이 낮다는 것을 명확히 발견했으며, 인터넷 비디오 사전 훈련과 1인칭 조작 장면의 도메인 격차에 기인한다. 계산 자원이 제한된 경우, LoRA로 다운그레이드하기보다 epoch 수를 줄이는 것을 우선 고려하라.
- **장기 평가는 CISR/CPDM 확인**: 이 두 지표는 청크 간 의미론적 일관성을 가장 잘 반영한다. 이 두 항목에서 개선이 제한적이라면, 분리가 실제로 장기 기억을 개선하지 못했다는 뜻이다—RCBD의 미세한 개선은 로컬 시각 연속성 개선에 불과할 수 있다.
- **WorldArena에서 과도한 해석 금지**: WEM은 WorldArena에서 IRASim과 CtrlWorld보다 낮지만, 해당 벤치마크는 혼합 내비게이션-조작 태스크를 위해 설계되지 않았으며, 교차 벤치마크 순위 차이는 태스크 분포 불일치를 반영할 수 있으며 방법 결함이 아닐 수 있다.
