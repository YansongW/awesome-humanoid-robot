---
$id: ent_paper_vlk_humanoid_loco_manipulation_synthetic_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'VLK: Learning Humanoid Loco-Manipulation from Synthetic Interactions in Reconstructed Scenes'
  zh: 'VLK: Learning Humanoid Loco-Manipulation from Synthetic Interactions in Reconstructed Scenes'
  ko: 'VLK: Learning Humanoid Loco-Manipulation from Synthetic Interactions in Reconstructed Scenes'
summary:
  en: 'Perception-based humanoid loco-manipulation requires connecting egocentric observations and task instructions to whole-body
    motion. Learning this mapping requires synchronized egocentric images, language commands, and robot-compatible kinematic
    trajectories, yet no existing data source provides this complete tuple at scale. Institutions per source list: UC Berkeley、CMU、Shanghai
    AI Lab.'
  zh: VLK 提出一种从合成交互中学习人形机器人全身运动控制的方法，由多机构联合完成。核心贡献是通过 3D Gaussian Splatting 重建室内场景，自动生成 48,000 条视觉-语言-运动学配对轨迹，无需人工标注。该方法在
    Unitree G1 实体机器人上验证了导航与单物体搬运任务的有效性。
  ko: 'Perception-based humanoid loco-manipulation requires connecting egocentric observations and task instructions to whole-body
    motion. Learning this mapping requires synchronized egocentric images, language commands, and robot-compatible kinematic
    trajectories, yet no existing data source provides this complete tuple at scale. Institutions per source list: UC Berkeley、CMU、Shanghai
    AI Lab.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- vlk
- humanoid
- loco
- manipulation
- synthetic
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 814 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2606.30645v1); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit final pass: derivation labels corrected/completed after full-text re-verification.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.30645 VLK: Learning Humanoid Loco-Manipulation from Synthetic Interactions in Reconstructed Scenes'
  url: https://arxiv.org/abs/2606.30645
  accessed_at: '2026-07-31'
  date: '2026-06-29'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---

## 概述

本文提出VLK系统，通过真实到仿真再到真实的管线，在3DGS重建场景中合成大规模视觉-语言-运动学三元组数据，训练Unitree G1人形机器人的感知-定位-操作策略。核心贡献在于将3DGS场景重建与全身运动跟踪连接为完整数据生成系统，实现无需人工演示的端到端语言指令驱动操作，并在120次真实世界试验中验证了可行性。

## 它改变了什么

人形机器人定位-操作学习长期受困于数据稀缺：真实遥操作成本高且难以扩展，人类动捕缺乏机器人自我中心观测，自我中心视频又缺少机器人兼容运动学。作者真正改变的是数据生成范式——不再依赖人工演示或真实数据采集，而是通过3DGS重建真实场景、合成交互轨迹、渲染自我中心观测，将三个碎片化方案（场景重建、运动合成、视觉渲染）整合为闭环数据工厂。这一转变使得训练数据可以按需生成、无限扩展，且天然具备视觉-语言-运动学对齐，解决了此前数据源无法同时提供三元组的根本矛盾。

## 方法拆解

### 数据生成管线三阶段
1. **场景重建与标注**：iPhone 14 Pro Polycam扫描（RGB+LiDAR深度）重建度量尺度3DGS场景；提取点云，用viser工具手动标注语义3D边界框（物体类别如chair/table/box）和可行走区域多边形。
2. **交互合成**：导航模型用BONES-SEED的G1运动训练；交互模型用OMOMO序列经OmniRetarget重定向到G1训练。交互合成基于CHOIS/HiHi条件扩散公式，额外条件包括初始物体状态、物体几何（BPS表示+MLP投影）、期望相对手腕位姿（物体坐标系定义）和手腕接触标签。
3. **自我中心渲染**：Isaac Sim中回放合成G1轨迹，加载3DGS场景，虚拟ZED 2i相机安装在头部（外参校准匹配真实相机）；应用域随机化（光照变化、相机外参和焦距扰动、图像空间增强）。

### 运动表示与策略架构
- **运动学状态**：x_τ = [Δx_τ, Δy_τ, Δψ_τ, h_τ, R^root_τ, sin(q_τ), cos(q_τ), c_τ]，其中c_τ为左右手腕-物体接触标签（∈{0,1}），部署时自回归预测而非直接观测。
- **物体轨迹**：X_obj = [o, R_obj]，o为全局物体位置，R_obj为相对输入物体几何的6D旋转。
- **VLK策略**：输入当前自我中心RGB观测o_t、语言指令ℓ、当前运动学状态x_t；输出H=30帧未来轨迹（30Hz下1秒）。从预训练π_0.5初始化，全模型微调；流匹配x_0预测目标，重建损失L_traj = ||x̂ − x||²₂。
- **辅助损失**：脚-地面接触预测（焦点损失）、累积根轨迹一致性、前向运动学精度（踝/腕，权重1.0）、脚滑动正则化（权重0.05）。

### 后处理与部署
- **脚滑修正**：用预测脚接触标签识别接触阶段，每个阶段内通过逆运动学约束脚固定在接触位置。
- **手-物接触优化**：改编EgoAllo优化方法，驱动腕部位置和旋转到物体局部坐标系中输入的腕部姿态。
- **部署系统**：Triton推理后端，VLK在RTX 5090（端到端31ms），全身跟踪在RTX 5000 Ada（50Hz，每tick 4.3ms）；相邻动作块10帧重叠；基于拉普拉斯方差的锐度帧选择应对运动模糊。

## 关键创新

1. **合成数据闭环**：首次将3DGS场景重建与全身运动跟踪连接为完整数据生成系统，实现真实到仿真再到真实的闭环。相比此前碎片化方案，这一系统能按需生成任意规模的配对数据，且天然保证视觉-语言-运动学对齐。
2. **接触标签自回归预测**：部署时无需直接观测手腕-物体接触，而是由策略自回归预测接触标签，避免了额外传感器依赖，同时保持对物体交互的稳定控制。
3. **可微正向运动学层**：在训练损失中引入可微FK层，对全局身体和末端执行器位置施加几何约束，显著提升生成轨迹的物理合理性和腕部放置精度，这是纯关节角空间训练难以实现的。

## 实验与结果


### 真实世界成功率（120次试验，每模式20次）
| 任务 | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 20/20 | 19/20 |
| Turn Around | 20/20 | 18/20 |
| Pick (Floor) | 16/20 | 18/20 |
| Put (Floor) | 20/20 | 20/20 |
| Pick (Surface) | 11/20 | 13/20 |
| Put (Surface) | 8/20 | 15/20 |

### 仿真评估（MuJoCo+IsaacSim，每模式1000次rollout）
| 任务 | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 994/1000 | 948/1000 |
| Turn Around | 843/1000 | 813/1000 |
| Pick (Floor) | 731/1000 | 749/1000 |
| Put (Floor) | 991/1000 | 987/1000 |
| Pick (Surface) | 458/1000 | 521/1000 |
| Put (Surface) | 569/1000 | 722/1000 |

### 关键消融
- **接触标签必要性**：无接触标签的Pick (Floor)在真实世界两个场景成功率均为0/5，证明接触标签对操作稳定性至关重要。
- **数据量影响**：Pick (Surface)从10%数据时0%成功率提升到全数据集时46%成功率，显示数据规模对复杂任务的决定性作用。
- **域随机化**：行走模式成功率从无随机化41%提升到全随机化90%，其中光照随机化单项贡献最大（87%，原文 Table 数值）。

### 延迟分解（Table 4）
| 组件 | 耗时 | 占比 |
|------|------|------|
| 图像获取 | 5.4ms | 9% |
| 观测打包 | 7.5ms | 12% |
| 服务器往返+采样 | 37.0ms | 59% |
| 输出反归一化 | 4.2ms | 7% |
| 世界帧变换+FK | 7.2ms | 11% |
| 参考合并 | 3.4ms | 5% |
| **总计** | **63.0ms** | **100%** |

重规划周期约555ms，总延迟63ms留有8.8×余量，重规划频率约1.8Hz。

## 边界与局限

交互合成受OMOMO覆盖范围限制，仅包含有限集合的大物体交互，生成行为更适合双手搬运箱状物体而非抓取杯子或工具等小物体。接触感知跟踪器稳定大物体操作但不处理精确物体抓取。表面级交互（Pick/Put Surface）可靠性较低，部分原因是重定向交互数据对不同支撑面高度的箱体操作覆盖有限。论文未明确讨论真实世界失败案例分类、对其他机器人平台的泛化性、未见物体类别或场景布局的泛化能力，以及长时间部署的稳定性问题。

## 工程启示

复现时优先核对三点：一是场景重建质量——3DGS优化是否收敛、语义标注是否准确，这直接影响合成数据质量；二是域随机化配置——光照随机化对成功率提升贡献最大（41%→87%），务必完整实现；三是接触标签的生成与预测——这是操作稳定性的关键，无接触标签时Pick (Floor)真实世界成功率直接归零。最容易踩坑的地方包括：OMOMO重定向到G1的运动质量（建议先验证重定向后轨迹的物理合理性）、部署时运动模糊处理（锐度帧选择窗口0.3s需与推理频率匹配）、以及异步块合并的时序同步（合并步骤贡献3.4ms，需确保不阻塞50Hz跟踪循环）。数据生成成本约600 GPU小时/环境，建议先在小规模验证管线正确性再全量生成。

## 参考
- https://arxiv.org/abs/2606.30645
- https://github.com/ImChong/Robotics_Notebooks

## Overview

This paper proposes the VLK system, which synthesizes large-scale vision-language-kinematics triplet data in 3DGS-reconstructed scenes through a real-to-sim-to-real pipeline to train perception-locomotion-manipulation policies for the Unitree G1 humanoid robot. The core contribution is connecting 3DGS scene reconstruction with whole-body motion tracking into a complete data generation system, enabling end-to-end language-instruction-driven manipulation without human demonstrations, validated across 120 real-world trials.

## What It Changes

Humanoid robot locomotion-manipulation learning has long been constrained by data scarcity: real teleoperation is costly and difficult to scale, human motion capture lacks robot egocentric observations, and egocentric videos lack robot-compatible kinematics. What the authors truly change is the data generation paradigm—no longer relying on human demonstrations or real data collection, but instead reconstructing real scenes via 3DGS, synthesizing interaction trajectories, and rendering egocentric observations, integrating three fragmented approaches (scene reconstruction, motion synthesis, visual rendering) into a closed-loop data factory. This shift enables on-demand, infinitely scalable training data with inherent vision-language-kinematics alignment, resolving the fundamental contradiction where previous data sources could not simultaneously provide all three modalities.

## Method Breakdown

### Three-Stage Data Generation Pipeline
1. **Scene Reconstruction and Annotation**: iPhone 14 Pro Polycam scanning (RGB + LiDAR depth) reconstructs metric-scale 3DGS scenes; point clouds are extracted and semantic 3D bounding boxes (object categories such as chair/table/box) and walkable region polygons are manually annotated using the viser tool.
2. **Interaction Synthesis**: The navigation model is trained with G1 motions from BONES-SEED; the interaction model is trained with OMOMO sequences retargeted to G1 via OmniRetarget. Interaction synthesis builds on CHOIS/HiHi conditional diffusion formulations, with additional conditions including initial object state, object geometry (BPS representation + MLP projection), desired relative wrist pose (defined in the object coordinate frame), and wrist contact labels.
3. **Egocentric Rendering**: Synthesized G1 trajectories are replayed in Isaac Sim with the 3DGS scene loaded, and a virtual ZED 2i camera is mounted on the head (extrinsics calibrated to match the real camera); domain randomization is applied (lighting variation, camera extrinsics and focal length perturbation, image-space augmentation).

### Motion Representation and Policy Architecture
- **Kinematic State**: x_τ = [Δx_τ, Δy_τ, Δψ_τ, h_τ, R^root_τ, sin(q_τ), cos(q_τ), c_τ], where c_τ denotes left/right wrist-object contact labels (∈{0,1}), predicted autoregressively at deployment rather than directly observed.
- **Object Trajectory**: X_obj = [o, R_obj], where o is the global object position and R_obj is the 6D rotation relative to the input object geometry.
- **VLK Policy**: Inputs are the current egocentric RGB observation o_t, language instruction ℓ, and current kinematic state x_t; outputs H=30 frames of future trajectory (1 second at 30Hz). Initialized from pretrained π_0.5 and fine-tuned across the full model; flow matching with x_0 target prediction, reconstruction loss L_traj = ||x̂ − x||²₂.
- **Auxiliary Losses**: Foot-ground contact prediction (focal loss), cumulative root trajectory consistency, forward kinematics accuracy (ankle/wrist, weight 1.0), and foot sliding regularization (weight 0.05).

### Post-Processing and Deployment
- **Foot Slip Correction**: Predicted foot contact labels identify contact phases, and inverse kinematics constrains feet to fixed contact positions within each phase.
- **Hand-Object Contact Optimization**: Adapted from the EgoAllo optimization method, driving wrist positions and rotations to the input wrist poses in the object local coordinate frame.
- **Deployment System**: Triton inference backend, VLK on RTX 5090 (31ms end-to-end), whole-body tracking on RTX 5000 Ada (50Hz, 4.3ms per tick); adjacent action chunks overlap by 10 frames; sharpness-based frame selection using Laplacian variance addresses motion blur.

## Key Innovations

1. **Synthetic Data Closed Loop**: First to connect 3DGS scene reconstruction with whole-body motion tracking into a complete data generation system, achieving a real-to-sim-to-real closed loop. Compared to prior fragmented approaches, this system generates arbitrarily scaled paired data on demand with inherent vision-language-kinematics alignment.
2. **Autoregressive Contact Label Prediction**: At deployment, wrist-object contact is not directly observed but predicted autoregressively by the policy, eliminating additional sensor dependencies while maintaining stable object interaction control.
3. **Differentiable Forward Kinematics Layer**: A differentiable FK layer in the training loss imposes geometric constraints on global body and end-effector positions, significantly improving the physical plausibility of generated trajectories and wrist placement accuracy—something difficult to achieve with pure joint-angle-space training.

## Experiments and Results

### Real-World Success Rates (120 trials, 20 per mode)
| Task | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 20/20 | 19/20 |
| Turn Around | 20/20 | 18/20 |
| Pick (Floor) | 16/20 | 18/20 |
| Put (Floor) | 20/20 | 20/20 |
| Pick (Surface) | 11/20 | 13/20 |
| Put (Surface) | 8/20 | 15/20 |

### Simulation Evaluation (MuJoCo+IsaacSim, 1000 rollouts per mode)
| Task | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 994/1000 | 948/1000 |
| Turn Around | 843/1000 | 813/1000 |
| Pick (Floor) | 731/1000 | 749/1000 |
| Put (Floor) | 991/1000 | 987/1000 |
| Pick (Surface) | 458/1000 | 521/1000 |
| Put (Surface) | 569/1000 | 722/1000 |

### Key Ablations
- **Contact Label Necessity**: Without contact labels, Pick (Floor) achieved 0/5 success in both real-world scenes, demonstrating that contact labels are critical for manipulation stability.
- **Data Scale Impact**: Pick (Surface) improved from 0% success with 10% data to 46% success with the full dataset, showing the decisive role of data scale for complex tasks.
- **Domain Randomization**: Walking mode success improved from 41% without randomization to 90% with full randomization, with lighting randomization alone contributing the most (87%, per original paper Table values).

### Latency Breakdown (Table 4)
| Component | Time | Percentage |
|------|------|------|
| Image acquisition | 5.4ms | 9% |
| Observation packing | 7.5ms | 12% |
| Server round-trip + sampling | 37.0ms | 59% |
| Output denormalization | 4.2ms | 7% |
| World frame transform + FK | 7.2ms | 11% |
| Reference merging | 3.4ms | 5% |
| **Total** | **63.0ms** | **100%** |

The replanning period is approximately 555ms, leaving an 8.8× margin over the 63ms total latency, with a replanning frequency of approximately 1.8Hz.

## Boundaries and Limitations

Interaction synthesis is limited by OMOMO coverage, which includes only a finite set of large-object interactions, generating behaviors better suited for two-handed box carrying rather than grasping cups or tools. The contact-aware tracker stabilizes large-object manipulation but does not handle precise object grasping. Surface-level interactions (Pick/Put Surface) show lower reliability, partly due to limited retargeted interaction data for box manipulation across different support surface heights. The paper does not explicitly discuss real-world failure case categorization, generalization to other robot platforms, generalization to unseen object categories or scene layouts, or long-duration deployment stability.

## Engineering Insights

When reproducing, prioritize three aspects: first, scene reconstruction quality—whether 3DGS optimization converges and semantic annotations are accurate, as this directly affects synthetic data quality; second, domain randomization configuration—lighting randomization contributes the most to success rate improvement (41%→87%) and must be fully implemented; third, contact label generation and prediction—this is key to manipulation stability, as Pick (Floor) real-world success drops to zero without contact labels. Common pitfalls include: OMOMO retargeting quality to G1 (recommend validating physical plausibility of retargeted trajectories first), motion blur handling at deployment (sharpness frame selection window of 0.3s must match inference frequency), and temporal synchronization of asynchronous chunk merging (merging contributes 3.4ms and must not block the 50Hz tracking loop). Data generation costs approximately 600 GPU hours per environment; it is recommended to validate pipeline correctness at small scale before full-scale generation.

## 개요

본 논문은 VLK 시스템을 제안하며, 실제-시뮬레이션-실제 파이프라인을 통해 3DGS 재구성 장면에서 대규모 시각-언어-운동학 삼중항 데이터를 합성하여 Unitree G1 휴머노이드 로봇의 인식-위치추정-조작 정책을 훈련한다. 핵심 기여는 3DGS 장면 재구성과 전신 운동 추적을 완전한 데이터 생성 시스템으로 연결하여, 인간 시연 없이 종단 간 언어 명령 기반 조작을 구현하고 120회의 실제 세계 실험에서 타당성을 검증한 것이다.

## 무엇을 바꾸었는가

휴머노이드 로봇의 위치추정-조작 학습은 오랫동안 데이터 부족에 시달려 왔다: 실제 원격 조작은 비용이 높고 확장이 어려우며, 인간 모션 캡처는 로봇의 자기중심 관측이 부족하고, 자기중심 비디오는 로봇 호환 운동학이 부족하다. 저자가 실제로 바꾼 것은 데이터 생성 패러다임이다—더 이상 인간 시연이나 실제 데이터 수집에 의존하지 않고, 3DGS로 실제 장면을 재구성하고, 상호작용 궤적을 합성하며, 자기중심 관측을 렌더링하여 세 가지 파편화된 솔루션(장면 재구성, 운동 합성, 시각 렌더링)을 폐쇄 루프 데이터 공장으로 통합한다. 이러한 전환은 훈련 데이터를 필요에 따라 생성하고 무한히 확장할 수 있게 하며, 자연스럽게 시각-언어-운동학 정렬을 갖추어, 이전 데이터 소스가 삼중항을 동시에 제공할 수 없던 근본적 모순을 해결한다.

## 방법 분석

### 데이터 생성 파이프라인 3단계
1. **장면 재구성 및 주석**: iPhone 14 Pro Polycam 스캔(RGB+LiDAR 깊이)으로 미터 단위 3DGS 장면 재구성; 포인트 클라우드 추출, viser 도구로 수동 의미론적 3D 경계 상자(의자/테이블/상자 등 객체 클래스) 및 보행 가능 영역 다각형 주석.
2. **상호작용 합성**: 내비게이션 모델은 BONES-SEED의 G1 운동으로 훈련; 상호작용 모델은 OMOMO 시퀀스를 OmniRetarget으로 G1에 리타겟팅하여 훈련. 상호작용 합성은 CHOIS/HiHi 조건 확산 공식 기반, 추가 조건에는 초기 객체 상태, 객체 기하( BPS 표현+MLP 투영), 기대 상대 손목 자세(객체 좌표계 정의) 및 손목 접촉 라벨 포함.
3. **자기중심 렌더링**: Isaac Sim에서 합성 G1 궤적 재생, 3DGS 장면 로드, 가상 ZED 2i 카메라를 머리에 장착(외부 파라미터 보정이 실제 카메라와 일치); 도메인 무작위화 적용(조명 변화, 카메라 외부 파라미터 및 초점 거리 섭동, 이미지 공간 증강).

### 운동 표현 및 정책 아키텍처
- **운동학 상태**: x_τ = [Δx_τ, Δy_τ, Δψ_τ, h_τ, R^root_τ, sin(q_τ), cos(q_τ), c_τ], 여기서 c_τ는 좌우 손목-객체 접촉 라벨(∈{0,1}), 배포 시 자기회귀 예측이지 직접 관측이 아님.
- **객체 궤적**: X_obj = [o, R_obj], o는 전역 객체 위치, R_obj는 입력 객체 기하에 상대적인 6D 회전.
- **VLK 정책**: 입력은 현재 자기중심 RGB 관측 o_t, 언어 명령 ℓ, 현재 운동학 상태 x_t; 출력은 H=30프레임 미래 궤적(30Hz에서 1초). 사전 훈련된 π_0.5에서 초기화, 전체 모델 미세 조정; 흐름 매칭 x_0 예측 목표, 재구성 손실 L_traj = ||x̂ − x||²₂.
- **보조 손실**: 발-지면 접촉 예측(포컬 손실), 누적 루트 궤적 일관성, 순방향 운동학 정밀도(발목/손목, 가중치 1.0), 발 미끄러짐 정규화(가중치 0.05).

### 후처리 및 배포
- **발 미끄러짐 보정**: 예측 발 접촉 라벨로 접촉 단계 식별, 각 단계 내에서 역운동학으로 발이 접촉 위치에 고정되도록 제약.
- **손-객체 접촉 최적화**: EgoAllo 최적화 방법을 각색, 손목 위치와 회전을 객체 로컬 좌표계의 입력 손목 자세로 구동.
- **배포 시스템**: Triton 추론 백엔드, VLK는 RTX 5090(종단 간 31ms), 전신 추적은 RTX 5000 Ada(50Hz, 틱당 4.3ms); 인접 동작 블록 10프레임 중첩; 라플라시안 분산 기반 선명도 프레임 선택으로 모션 블러 대응.

## 핵심 혁신

1. **합성 데이터 폐쇄 루프**: 처음으로 3DGS 장면 재구성과 전신 운동 추적을 완전한 데이터 생성 시스템으로 연결하여 실제-시뮬레이션-실제 폐쇄 루프를 구현. 이전 파편화 솔루션과 달리, 이 시스템은 필요에 따라 임의 규모의 쌍 데이터를 생성하고 자연스럽게 시각-언어-운동학 정렬을 보장.
2. **접촉 라벨 자기회귀 예측**: 배포 시 손목-객체 접촉을 직접 관측할 필요 없이 정책이 자기회귀로 접촉 라벨을 예측하여 추가 센서 의존을 피하면서 객체 상호작용의 안정적 제어를 유지.
3. **미분 가능 순방향 운동학 계층**: 훈련 손실에 미분 가능 FK 계층을 도입하여 전역 신체 및 말단 실행기 위치에 기하학적 제약을 부과, 생성 궤적의 물리적 타당성과 손목 배치 정밀도를 크게 향상—순수 관절 각도 공간 훈련으로는 달성하기 어렵다.

## 실험 및 결과

### 실제 세계 성공률(120회 시험, 모드당 20회)
| 작업 | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 20/20 | 19/20 |
| Turn Around | 20/20 | 18/20 |
| Pick (Floor) | 16/20 | 18/20 |
| Put (Floor) | 20/20 | 20/20 |
| Pick (Surface) | 11/20 | 13/20 |
| Put (Surface) | 8/20 | 15/20 |

### 시뮬레이션 평가(MuJoCo+IsaacSim, 모드당 1000회 롤아웃)
| 작업 | Lab Scene | Apartment Scene |
|------|-----------|-----------------|
| Walk To | 994/1000 | 948/1000 |
| Turn Around | 843/1000 | 813/1000 |
| Pick (Floor) | 731/1000 | 749/1000 |
| Put (Floor) | 991/1000 | 987/1000 |
| Pick (Surface) | 458/1000 | 521/1000 |
| Put (Surface) | 569/1000 | 722/1000 |

### 핵심 소거 실험
- **접촉 라벨 필요성**: 접촉 라벨이 없는 Pick (Floor)는 실제 세계 두 장면 모두 성공률 0/5, 접촉 라벨이 조작 안정성에 결정적임을 증명.
- **데이터량 영향**: Pick (Surface)는 데이터 10%에서 성공률 0%에서 전체 데이터셋에서 46%로 향상, 복잡한 작업에 대한 데이터 규모의 결정적 역할을 보여줌.
- **도메인 무작위화**: 보행 모드 성공률이 무작위화 없음 41%에서 전체 무작위화 90%로 향상, 조명 무작위화 단일 기여가 가장 큼(87%, 원문 Table 수치).

### 지연 분해(Table 4)
| 구성 요소 | 소요 시간 | 비율 |
|------|------|------|
| 이미지 획득 | 5.4ms | 9% |
| 관측 패킹 | 7.5ms | 12% |
| 서버 왕복+샘플링 | 37.0ms | 59% |
| 출력 역정규화 | 4.2ms | 7% |
| 세계 프레임 변환+FK | 7.2ms | 11% |
| 참조 병합 | 3.4ms | 5% |
| **총계** | **63.0ms** | **100%** |

재계획 주기는 약 555ms, 총 지연 63ms는 8.8배 여유를 남기며, 재계획 빈도는 약 1.8Hz.

## 경계 및 한계

상호작용 합성은 OMOMO 적용 범위 제한으로 유한한 대형 객체 상호작용만 포함하며, 생성된 행동은 컵이나 도구 같은 소형 객체를 잡기보다 양손으로 상자형 객체를 운반하는 데 더 적합. 접촉 인식 추적기는 대형 객체 조작을 안정화하지만 정밀한 객체 파지에는 대응하지 않음. 표면 수준 상호작용(Pick/Put Surface) 신뢰성은 상대적으로 낮으며, 부분적으로는 리타겟팅된 상호작용 데이터가 다양한 지지면 높이의 상자 조작에 대한 적용 범위가 제한적이기 때문. 논문은 실제 세계 실패 사례 분류, 다른 로봇 플랫폼으로의 일반화, 보지 못한 객체 클래스나 장면 레이아웃에 대한 일반화 능력, 장시간 배포 안정성 문제를 명시적으로 논의하지 않음.

## 공학적 시사점

재현 시 세 가지를 우선 확인: 첫째, 장면 재구성 품질—3DGS 최적화 수렴 여부, 의미론적 주석 정확성, 이는 합성 데이터 품질에 직접 영향; 둘째, 도메인 무작위화 구성—조명 무작위화가 성공률 향상에 가장 큰 기여(41%→87%), 반드시 완전히 구현; 셋째, 접촉 라벨 생성 및 예측—이는 조작 안정성의 핵심, 접촉 라벨이 없으면 Pick (Floor) 실제 세계 성공률이 직접 0이 됨. 가장 함정에 빠지기 쉬운 부분: OMOMO의 G1 리타겟팅 운동 품질(리타겟팅 후 궤적의 물리적 타당성 먼저 검증 권장), 배포 시 모션 블러 처리(선명도 프레임 선택 창 0.3s는 추론 빈도와 일치해야 함), 비동기 블록 병합의 타이밍 동기화(병합 단계는 3.4ms 기여, 50Hz 추적 루프를 차단하지 않도록 보장). 데이터 생성 비용은 환경당 약 600 GPU 시간, 소규모로 파이프라인 정확성을 먼저 검증한 후 전체 생성 권장.
