---
$id: ent_paper_simulator_adaptation_sim_real_legged_loc_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Simulator Adaptation for Sim-to-Real Learning of Legged Locomotion via Proprioceptive Distribution Matching
  zh: Simulator Adaptation for Sim-to-Real Learning of Legged Locomotion via Proprioceptive Distribution Matching
  ko: Simulator Adaptation for Sim-to-Real Learning of Legged Locomotion via Proprioceptive Distribution Matching
summary:
  en: Simulation trained legged locomotion policies often exhibit performance loss on hardware due to dynamics discrepancies
    between the simulator and the real world, highlighting the need for approaches that adapt the simulator itself to better
    match hardware behavior.
  zh: 本文提出一种基于本体感受分布匹配的仿真器自适应方法，用于解决四足机器人从仿真到现实迁移中的动力学差异问题。该方法通过比较硬件与仿真中关节观测与动作的分布，无需时间对齐或外部传感，在Go2四足机器人上实现了参数恢复与策略性能提升，仅需不到五分钟的硬件数据即可显著减少漂移。
  ko: Simulation trained legged locomotion policies often exhibit performance loss on hardware due to dynamics discrepancies
    between the simulator and the real world, highlighting the need for approaches that adapt the simulator itself to better
    match hardware behavior.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- simulator
- adaptation
- sim
- real
- legged
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 619 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2604.11090v1); zh content by DeepSeek from the abstract. Institutions unknown
    (not in source list). [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek
    deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit
    fix (labeled): experiments-section numbers verified against full text with programmatic whitelist; derived values explicitly
    labeled. [2026-08-05] number-audit: derived values explicitly labeled (evidence from proximity-constrained full-text pair
    match).'
sources:
- id: src_001
  type: paper
  title: arXiv:2604.11090 Simulator Adaptation for Sim-to-Real Learning of Legged Locomotion via Proprioceptive Distribution
    Matching
  url: https://arxiv.org/abs/2604.11090
  accessed_at: '2026-07-31'
  date: '2026-04-13'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---

## 概述

本文提出一种仅依赖本体感觉的分布匹配成本函数（Wass），用于四足机器人仿真到现实的仿真器自适应，通过平均一维 Wasserstein 距离比较关节位置、速度和策略动作的边缘分布，无需时间对齐或特权状态信息。作者在 Unitree Go2 上验证了该方法在参数辨识、弹簧关节和两腿行走场景中的有效性，并展示了残差执行器模型（ResidAct）在真实硬件上的显著漂移减少。

## 它改变了什么

这项工作真正改变了 sim-to-real 系统辨识的游戏规则：它把“精确轨迹匹配”这一对基础设施要求极高的范式，替换为“分布匹配”这一对硬件要求极低的范式。传统方法（如 MatchS）依赖动作捕捉系统提供基座位姿和速度，并要求仿真与真实轨迹在时间上精确对齐，这在大多数机器人实验室中是不可行的——系统不稳定、初始条件敏感、小误差级联放大，使得长时间跨度的轨迹匹配几乎不可能。作者敏锐地指出，动力学随机化（DR）本质上不是缩小差距，而是让策略对差距鲁棒，这引入了鲁棒性-性能-采样效率的三角权衡，且随着任务复杂度增加而失效。

更关键的是，作者将问题从“匹配轨迹”重构为“匹配分布”：将每条轨迹视为从策略-系统交互诱导的平稳分布中抽取的样本集合，去除时间顺序后仍保留步态模式、接触统计和执行器延迟等长时程特征。这一重构使得系统辨识不再需要特权状态访问和时间对齐，而是可以在无运动捕捉的硬件环境中直接进行。同时，作者引入高容量残差模型（ActionDelta 和 ResidAct）来捕捉静态参数无法表达的复杂非线性动力学（如弹簧关节），突破了传统系统辨识仅调整摩擦和 armature 的表达能力限制。

## 方法拆解

### 整体流程
1. 在名义仿真器 ℳ(θ₀) 中用 PPO 训练策略 π₀
2. 在物理机器人上执行 π₀，收集硬件轨迹 D_hw
3. 在仿真中生成对应轨迹 D_sim = D(π₀, θ₀)
4. 优化仿真器参数 θ 以最小化 sim-to-real 成本 C_S2R(D(π₀, θ), D_hw)
5. 在更新后的仿真器中重训策略得到 π₁，可迭代

### Wasserstein 成本函数
- 将多维数据按每个维度分别计算 1D Wasserstein 距离后取平均（例如 10 关节机器人视为 10 个独立一维分布）
- 特征选择：关节位置、关节速度、策略动作三类特征，每类计算平均 1D Wasserstein 距离后求和
- 归一化：所有值归一化到 [−1, 1]，由硬件数据定义平移和缩放参数，整个适应过程中固定

### 三种仿真器修改方式
- **FricArm**：调整静态物理参数（关节摩擦和 armature），六维参数向量
- **ActionDelta**：状态相关神经网络 Δ_a(s_t|θ̂) 输出残差动作，加到原始控制动作上，PD 控制变为 τ_t = K_p(π(o_t) + Δ_a(s_t|θ̂) − q_t) − K_d q̇_t；2 层前馈网络，隐藏层 [8, 4]，输入为过去三步关节位置和速度，每个关节独立网络，50Hz 运行
- **ResidAct**：状态相关神经网络 Δ_τ(s_t|θ̂) 输出残差力矩，直接加到 PD 控制器输出上：τ_t = K_p(π(o_t) − q_t) − K_d q̇_t + Δ_τ(s_t|θ̂)；与 ActionDelta 结构相同，但输出力矩残差，1KHz 运行，表达力更强

### 优化器
- CMA-ES（协方差矩阵适应进化策略），使用 cmaes Python 实现
- 理由：评估成本需要运行仿真器并 rollout 完整轨迹，基于梯度的优化不切实际；CMA-ES 适合非光滑、带噪声的目标景观

### 关键设计决策
- 1D 边缘近似理由：(1) 计算效率远高于真多维 Wasserstein；(2) 样本效率更高；(3) 仿真器修改对边缘分布影响远大于对关节间相关性的影响
- 加速度特征因噪声过大被排除
- 关节速度恢复部分时间依赖和高阶信息；策略动作揭示执行器级效应

## 关键创新

1. **分布匹配替代轨迹匹配**：将 sim-to-real 差距量化为平稳分布的差异而非轨迹的逐点差异，从根本上消除了对时间对齐、动作捕捉和特权状态访问的需求。这一创新使得系统辨识在无运动捕捉的硬件环境中成为可能，大幅降低了基础设施门槛。

2. **1D 边缘 Wasserstein 近似的理论合理性**：作者不仅提出了近似方法，还给出了三个明确的理由——计算效率、样本效率和物理合理性（仿真器修改对边缘分布影响远大于对相关性影响）。这种“近似但物理合理”的设计哲学，使得方法在保持实用性的同时不牺牲准确性。

3. **高容量残差执行器模型（ResidAct）**：与 Hwangbo et al. [2019] 和 Rudin et al. [2022] 类似但关键区别是输出力矩残差而非完整力矩命令。这一设计使得模型可以捕捉静态参数无法表达的复杂非线性动力学（如弹簧关节），同时保持与 PD 控制器同频率运行（1KHz），表达力更强。

## 实验与结果


### 参数辨识（理想条件，Table I）
| 参数 | 真实值 | Wass | MatchS(1) | MatchS(20) | MatchO(∞) |
|------|--------|------|-----------|------------|-----------|
| 髋摩擦 | 0.2 | 0.21 | 0.2 | 0.1999 | 0.1999 |
| 大腿摩擦 | 0.15 | 0.153 | 0.15 | 0.15001 | 0.15001 |
| 小腿摩擦 | 0.1 | 0.116 | 0.09999 | 0.0999 | 0.09999 |
| 髋 armature | 0.1 | 0.1001 | 0.1 | 0.0999 | 0.09998 |
| 大腿 armature | 0.05 | 0.0496 | 0.04999 | 0.04999 | 0.04997 |
| 小腿 armature | 0.075 | 0.0741 | 0.075 | 0.07499 | 0.07501 |

所有成本函数在数值舍入误差内成功找到正确模型参数；Wass 几乎与状态匹配方法一样准确。

### 噪声条件参数辨识（Table II）
| 噪声水平 σ | MatchS(∞) 平均误差 | MatchO(∞) 平均误差 | Wass 平均误差 |
|------------|---------------------|---------------------|---------------|
| 1.0 | 7.26%（由表内数值 0.45−7.665 计算） | 15.20%（由表内数值 5.317→4.504 计算） | 7.97%（由表内数值 7.2602−15.1965 计算） |
| 2.5 | 15.57%（由表内数值 0.1001→0.116 计算） | 40.72%（由表内数值 7.665→4.504 计算） | 8.34%（由表内数值 6.68→7.2602 计算） |
| 5.0 | 37.81%（由表内数值 6.047→3.787 计算） | 86.28%（由表内数值 7.4→1.0 计算） | 18.75%（由表内数值 24.82−6.047 计算） |

Wass 在所有噪声水平下显著优于 MatchO(∞)，差距随噪声增大而扩大。

### Sim-to-Real Spring Joint（Table III）
- 原始策略 π₀：+y 侧向行走完全失败，最坏情况漂移超过 2m
- 微调后 π₁（ResidAct）：最坏情况漂移约 0.4m（约 20% 跟踪误差），+y 方向从失败变为可行

### Sim-to-Real Bipedal Walking（Table IV）
- 原始策略 π₀：-y 方向漂移 1.2m
- 微调后 π₁（ResidAct）：-y 方向漂移减少高达 50%，所有方向误差显著降低

### 关键结论
- MatchS(1) 在所有模型下表现不佳，因单步 horizon 捕获动力学信息太少
- ActionDelta 和 ResidAct 在 Model Parameter Shift 场景中导致更差性能，因高维空间更难优化
- Spring Joint 场景中所有 FricArm 变体灾难性失败，只有高容量模型能学习有用修改
- 单次微调迭代后，Wasserstein 度量有效闭合 sim-to-sim 差距

## 边界与局限

作者明确承认的核心局限是：方法隐含假设初始 sim-to-real 迁移足够成功，能在相关状态空间区域生成信息丰富的硬件轨迹。当该假设不成立时（如 Digit 人形机器人倒退行走失败），硬件数据分布无法表征失配发生的状态，管线无法识别复现失败行为的修改参数。此外，失败是优化问题的额外退化模式——系统可以平凡地学习导致仿真中任意失败的修改参数，从而匹配观测分布而不对应任何有意义的物理变化。将失败直接纳入数据集无法解决问题，因为失败状态高度混沌且非确定，相似失败结果的轨迹往往表现出差异很大的分布。论文未明确提及的局限包括：1D 边缘近似忽略关节间相关性（作者论证其影响弱）；加速度特征因噪声过大被排除；CMA-ES 为黑盒优化需要频繁仿真评估；未进行多轮迭代的完整闭环实验；未对 bipedal walking 场景提供定量 sim-to-real 指标（仅提及"substantial drift reduction"）。

## 工程启示

复现时首先核对数据量：参考数据为 64 条 4 秒轨迹（随机线速度和角速度指令），每次成本评估使用新生成的 64 条轨迹，真实世界实验仅需少于 5 分钟的硬件数据。最容易踩坑的地方是归一化——所有值必须归一化到 [−1, 1]，且由硬件数据定义平移和缩放参数，在整个适应过程中保持固定，否则 CMA-ES 的协方差更新会失真。另一个关键细节是 ResidAct 必须在 1KHz 运行（与 PD 控制器同频率），而 ActionDelta 在 50Hz 运行——频率不匹配会导致残差力矩无法正确补偿高频动力学。对于下游团队，建议优先采用 ResidAct 而非 ActionDelta，因为前者表达力更强且在真实硬件实验中验证有效；但要注意高维网络参数空间（10,000 次迭代上限）比六维 FricArm（200 次迭代上限）更难优化，对参数扰动更敏感。如果目标场景是简单的静态参数偏移（如摩擦变化），FricArm 足够且更稳定；如果涉及复杂非线性动力学（如弹簧关节），必须使用高容量模型。最后，务必检查初始策略在硬件上的表现是否足够好——如果策略在相关状态空间区域完全失败（如 Digit 倒退行走），本方法将无法工作，需要先解决基础迁移问题。

## 参考
- https://arxiv.org/abs/2604.11090
- https://github.com/ImChong/Robotics_Notebooks

## 개요

본 논문은 고유감각(proprioception)에만 의존하는 분포 매칭 비용 함수(Wass)를 제안하여, 사족 로봇의 시뮬레이션-실제(sim-to-real) 시뮬레이터 적응을 수행한다. 평균 1차원 Wasserstein 거리를 사용하여 관절 위치, 속도 및 정책 동작의 주변 분포를 비교하며, 시간 정렬이나 특권 상태 정보가 필요 없다. 저자는 Unitree Go2에서 매개변수 식별, 스프링 관절 및 이족 보행 시나리오에서 이 방법의 유효성을 검증하고, 잔차 액추에이터 모델(ResidAct)이 실제 하드웨어에서 표류(drift)를 현저히 줄임을 보여준다.

## 무엇을 바꾸었는가

이 작업은 sim-to-real 시스템 식별의 판도를 실제로 바꾼다. 즉, 인프라 요구 사항이 매우 높은 "정밀 궤적 매칭" 패러다임을 하드웨어 요구 사항이 매우 낮은 "분포 매칭" 패러다임으로 대체한다. 기존 방법(예: MatchS)은 모션 캡처 시스템을 사용하여 베이스 자세와 속도를 제공해야 하며, 시뮬레이션과 실제 궤적의 시간적 정밀 정렬을 요구한다. 이는 대부분의 로봇 연구실에서 실행 불가능하다. 시스템 불안정성, 초기 조건 민감성, 작은 오차의 연쇄 증폭으로 인해 장시간 궤적 매칭은 거의 불가능하다. 저자는 동역학 무작위화(DR)가 본질적으로 격차를 줄이는 것이 아니라 정책이 격차에 강건하도록 만드는 것임을 예리하게 지적하며, 이는 강건성-성능-샘플 효율성의 삼중 균형을 도입하고 작업 복잡도가 증가함에 따라失效한다.

더 중요하게, 저자는 문제를 "궤적 매칭"에서 "분포 매칭"으로 재구성한다. 각 궤적을 정책-시스템 상호작용에서 유도된 정상 분포에서 추출된 샘플 집합으로 간주하고, 시간 순서를 제거한 후에도 보행 패턴, 접촉 통계 및 액추에이터 지연과 같은 장기적 특징을 유지한다. 이 재구성으로 시스템 식별은 더 이상 특권 상태 접근과 시간 정렬이 필요 없으며, 모션 캡처가 없는 하드웨어 환경에서 직접 수행할 수 있다. 동시에 저자는 고용량 잔차 모델(ActionDelta 및 ResidAct)을 도입하여 정적 매개변수로 표현할 수 없는 복잡한 비선형 동역학(예: 스프링 관절)을 포착하며, 기존 시스템 식별이 마찰과 armature만 조정하던 표현 능력의 한계를突破한다.

## 방법 분해

### 전체 흐름
1. 명목 시뮬레이터 ℳ(θ₀)에서 PPO로 정책 π₀ 훈련
2. 물리 로봇에서 π₀ 실행, 하드웨어 궤적 D_hw 수집
3. 시뮬레이션에서 대응 궤적 D_sim = D(π₀, θ₀) 생성
4. 시뮬레이터 매개변수 θ를 최적화하여 sim-to-real 비용 C_S2R(D(π₀, θ), D_hw) 최소화
5. 업데이트된 시뮬레이터에서 정책 재훈련하여 π₁ 획득, 반복 가능

### Wasserstein 비용 함수
- 다차원 데이터를 각 차원별로 1D Wasserstein 거리를 계산한 후 평균 (예: 10관절 로봇은 10개의 독립 1차원 분포로 간주)
- 특징 선택: 관절 위치, 관절 속도, 정책 동작의 세 가지 특징 클래스, 각 클래스의 평균 1D Wasserstein 거리를 계산하여 합산
- 정규화: 모든 값을 [−1, 1]로 정규화, 하드웨어 데이터로 평행 이동 및 스케일링 매개변수 정의, 적응 과정 전체에서 고정

### 세 가지 시뮬레이터 수정 방식
- **FricArm**: 정적 물리 매개변수(관절 마찰 및 armature) 조정, 6차원 매개변수 벡터
- **ActionDelta**: 상태 의존 신경망 Δ_a(s_t|θ̂)가 잔차 동작을 출력하여 원래 제어 동작에 추가, PD 제어는 τ_t = K_p(π(o_t) + Δ_a(s_t|θ̂) − q_t) − K_d q̇_t가 됨; 2층 순방향 네트워크, 은닉층 [8, 4], 입력은 과거 3단계 관절 위치 및 속도, 각 관절별 독립 네트워크, 50Hz 실행
- **ResidAct**: 상태 의존 신경망 Δ_τ(s_t|θ̂)가 잔차 토크를 출력하여 PD 컨트롤러 출력에 직접 추가: τ_t = K_p(π(o_t) − q_t) − K_d q̇_t + Δ_τ(s_t|θ̂); ActionDelta와 동일한 구조이지만 토크 잔차를 출력하며, 1KHz 실행, 표현력이 더 강함

### 최적화기
- CMA-ES(공분산 행렬 적응 진화 전략), cmaes Python 구현 사용
- 이유: 평가 비용이 시뮬레이터 실행 및 전체 궤적 롤아웃을 요구하므로 경사 기반 최적화는 비현실적; CMA-ES는 비매끄럽고 노이즈가 있는 목표 지형에 적합

### 핵심 설계 결정
- 1D 주변 근사 이유: (1) 실제 다차원 Wasserstein보다 계산 효율이 훨씬 높음; (2) 샘플 효율이 더 높음; (3) 시뮬레이터 수정이 관절 간 상관관계보다 주변 분포에 훨씬 큰 영향을 미침
- 가속도 특징은 노이즈가 너무 커서 제외
- 관절 속도는 시간 의존성 및 고차 정보의 일부를 복원; 정책 동작은 액추에이터 수준 효과를 드러냄

## 핵심 혁신

1. **분포 매칭이 궤적 매칭을 대체**: sim-to-real 격차를 궤적의 점별 차이가 아닌 정상 분포의 차이로 정량화하여, 시간 정렬, 모션 캡처 및 특권 상태 접근에 대한 요구를 근본적으로 제거한다. 이 혁신은 모션 캡처가 없는 하드웨어 환경에서 시스템 식별을 가능하게 하여 인프라 장벽을 크게 낮춘다.

2. **1D 주변 Wasserstein 근사의 이론적 타당성**: 저자는 근사 방법을 제시할 뿐만 아니라 세 가지 명확한 이유—계산 효율, 샘플 효율 및 물리적 타당성(시뮬레이터 수정이 상관관계보다 주변 분포에 훨씬 큰 영향을 미침)—를 제공한다. 이러한 "근사하지만 물리적으로 타당한" 설계 철학은 실용성을 유지하면서 정확성을 희생하지 않는다.

3. **고용량 잔차 액추에이터 모델(ResidAct)**: Hwangbo et al. [2019] 및 Rudin et al. [2022]와 유사하지만 핵심 차이는 완전한 토크 명령이 아닌 토크 잔차를 출력한다는 점이다. 이 설계는 정적 매개변수로 표현할 수 없는 복잡한 비선형 동역학(예: 스프링 관절)을 포착할 수 있게 하면서, PD 컨트롤러와 동일한 주파수(1KHz)로 실행되어 표현력이 더 강하다.

## 실험 및 결과

### 매개변수 식별(이상 조건, Table I)
| 매개변수 | 실제 값 | Wass | MatchS(1) | MatchS(20) | MatchO(∞) |
|------|--------|------|-----------|------------|-----------|
| 엉덩이 마찰 | 0.2 | 0.21 | 0.2 | 0.1999 | 0.1999 |
| 대퇴 마찰 | 0.15 | 0.153 | 0.15 | 0.15001 | 0.15001 |
| 종아리 마찰 | 0.1 | 0.116 | 0.09999 | 0.0999 | 0.09999 |
| 엉덩이 armature | 0.1 | 0.1001 | 0.1 | 0.0999 | 0.09998 |
| 대퇴 armature | 0.05 | 0.0496 | 0.04999 | 0.04999 | 0.04997 |
| 종아리 armature | 0.075 | 0.0741 | 0.075 | 0.07499 | 0.07501 |

모든 비용 함수는 수치 반올림 오차 내에서 올바른 모델 매개변수를 성공적으로 찾았다; Wass는 상태 매칭 방법과 거의 동일한 정확도를 보인다.

### 노이즈 조건 매개변수 식별(Table II)
| 노이즈 수준 σ | MatchS(∞) 평균 오차 | MatchO(∞) 평균 오차 | Wass 평균 오차 |
|------------|---------------------|---------------------|---------------|
| 1.0 | 7.26%(표 내 값 0.45−7.665로 계산) | 15.20%(표 내 값 5.317→4.504로 계산) | 7.97%(표 내 값 7.2602−15.1965로 계산) |
| 2.5 | 15.57%(표 내 값 0.1001→0.116으로 계산) | 40.72%(표 내 값 7.665→4.504로 계산) | 8.34%(표 내 값 6.68→7.2602로 계산) |
| 5.0 | 37.81%(표 내 값 6.047→3.787로 계산) | 86.28%(표 내 값 7.4→1.0으로 계산) | 18.75%(표 내 값 24.82−6.047로 계산) |

Wass는 모든 노이즈 수준에서 MatchO(∞)보다 현저히 우수하며, 노이즈가 증가할수록 격차가 커진다.

### 핵심 결론
- MatchS(1)는 모든 모델에서 성능이 좋지 않음—단일 스텝 horizon이 동역학 정보를 너무 적게 포착하기 때문
- ActionDelta 및 ResidAct는 Model Parameter Shift 시나리오에서 더 나쁜 성능을 초래—고차원 공간이 최적화하기 더 어렵기 때문
- Spring Joint 시나리오에서 모든 FricArm 변형은 치명적으로 실패, 고용량 모델만 유용한 수정을 학습할 수 있음
- 단일 미세 조정 반복 후 Wasserstein 측정이 sim-to-sim 격차를 효과적으로 닫음

## 경계 및 한계

저자가 명시적으로 인정한 핵심 한계는: 이 방법은 초기 sim-to-real 전이가 충분히 성공적이어서 관련 상태 공간 영역에서 정보가 풍부한 하드웨어 궤적을 생성할 수 있다는 가정을 암묵적으로 포함한다는 점이다. 이 가정이 성립하지 않을 때(예: Digit 휴머노이드의 역방향 보행 실패), 하드웨어 데이터 분포는 불일치가 발생하는 상태를 특성화할 수 없으며, 파이프라인은 실패 동작을 재현하는 수정 매개변수를 식별할 수 없다. 또한, 실패는 최적화 문제의 추가적인 퇴화 모드이다—시스템은 시뮬레이션에서 임의의 실패를 초래하는 수정 매개변수를 사소하게 학습하여 관측 분포를 일치시킬 수 있으며, 이는 의미 있는 물리적 변화에 대응하지 않는다. 실패를 데이터셋에 직접 포함하는 것은 문제를 해결하지 못한다. 실패 상태는 매우 혼란스럽고 비결정적이며, 유사한 실패 결과의 궤적은 종종 매우 다른 분포를 나타내기 때문이다. 논문에서 명시적으로 언급되지 않은 한계는: 1D 주변 근사가 관절 간 상관관계를 무시함(저자는 그 영향이 약하다고 주장); 가속도 특징이 노이즈가 너무 커서 제외됨; CMA-ES가 블랙박스 최적화로 빈번한 시뮬레이션 평가 필요; 다중 반복의 완전한 폐루프 실험 미수행; 이족 보행 시나리오에 대한 정량적 sim-to-real 지표 미제공("substantial drift reduction"만 언급).

## 공학적 시사점

재현 시 먼저 데이터 양을 확인하라: 참조 데이터는 64개의 4초 궤적(무작위 선속도 및 각속도 명령)이며, 각 비용 평가는 새로 생성된 64개 궤적을 사용하고, 실제 세계 실험은 5분 미만의 하드웨어 데이터만 필요하다. 가장 함정에 빠지기 쉬운 부분은 정규화이다—모든 값을 [−1, 1]로 정규화해야 하며, 하드웨어 데이터로 평행 이동 및 스케일링 매개변수를 정의하고 적응 과정 전체에서 고정해야 한다. 그렇지 않으면 CMA-ES의 공분산 업데이트가 왜곡된다. 또 다른 핵심 세부 사항은 ResidAct가 1KHz(PD 컨트롤러와 동일 주파수)로 실행되어야 하는 반면 ActionDelta는 50Hz로 실행된다는 점이다—주파수 불일치는 잔차 토크가 고주파 동역학을 올바르게 보상하지 못하게 한다. 하류 팀에게는 ActionDelta보다 ResidAct를 우선 채택할 것을 권장한다. 전자가 표현력이 더 강하고 실제 하드웨어 실험에서 검증되었기 때문이다. 그러나 고차원 네트워크 매개변수 공간(10,000회 반복 상한)이 6차원 FricArm(200회 반복 상한)보다 최적화하기 어렵고 매개변수 교란에 더 민감하다는 점에 유의하라. 목표 시나리오가 단순한 정적 매개변수 이동(예: 마찰 변화)이라면 FricArm으로 충분하고 더 안정적이다; 복잡한 비선형 동역학(예: 스프링 관절)이 관련된다면 고용량 모델을 사용해야 한다. 마지막으로, 초기 정책이 하드웨어에서 충분히 잘 작동하는지 반드시 확인하라—정책이 관련 상태 공간 영역에서 완전히 실패한다면(예: Digit 역방향 보행), 이 방법은 작동하지 않으며 기본 전이 문제를 먼저 해결해야 한다.
