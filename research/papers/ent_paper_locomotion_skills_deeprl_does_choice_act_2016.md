---
$id: ent_paper_locomotion_skills_deeprl_does_choice_act_2016
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Learning Locomotion Skills Using DeepRL: Does the Choice of Action Space Matter?'
  zh: 'Learning Locomotion Skills Using DeepRL: Does the Choice of Action Space Matter?'
  ko: 'Learning Locomotion Skills Using DeepRL: Does the Choice of Action Space Matter?'
summary:
  en: 'The use of deep reinforcement learning allows for high-dimensional state descriptors, but little is known about how
    the choice of action representation impacts the learning difficulty and the resulting performance. Institutions per source
    list: Xue Bin Peng、Michiel van de Panne.'
  zh: 本文研究了深度强化学习中动作空间选择对运动技能学习的影响。作者对比了力矩、肌肉激活、目标关节角度和目标关节角速度四种动作参数化方法，在多个平面关节图形的步态模仿任务上评估了学习时间、策略鲁棒性、运动质量和查询频率。结果表明，高层动作参数化提供的局部反馈能显著影响学习效果、鲁棒性和策略质量。
  ko: 'The use of deep reinforcement learning allows for high-dimensional state descriptors, but little is known about how
    the choice of action representation impacts the learning difficulty and the resulting performance. Institutions per source
    list: Xue Bin Peng、Michiel van de Panne.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- locomotion
- skills
- deeprl
- does
- choice
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 341 (.staging/ingest_yuanxq). Tier C->full. arXiv id 1611.01055 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (1611.01055v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:1611.01055 Learning Locomotion Skills Using DeepRL: Does the Choice of Action Space Matter?'
  url: https://arxiv.org/abs/1611.01055
  accessed_at: '2026-07-31'
  date: '2016-11-03'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

本文系统比较了深度强化学习（DeepRL）中四种动作空间参数化方式（力矩、目标关节角速度、目标关节角、肌肉激活）对运动模仿学习的影响，覆盖双足、迅猛龙和狗三种角色。核心贡献在于揭示了动作表示对学习速度、最终性能、鲁棒性和策略查询率的显著影响，并提出了一种交替优化策略参数与执行器参数的启发式方法，使神经网络能有效控制复杂的肌肉骨骼模型。

## 它改变了什么

这项工作的价值不在于提出新的强化学习算法，而在于它把“动作空间设计”这个在工程实践中常被当作超参数随手处理的问题，提升到了决定学习成败的核心因素的高度。此前DeepRL研究多聚焦于网络架构、奖励塑形或算法改进，而动作表示的选择往往被默认视为“细节”，本文用系统的对照实验证明了这个“细节”对学习难度和最终策略质量的支配性影响——尤其是在复杂角色（狗、迅猛龙）和高维肌肉模型上，动作表示的选择甚至比算法本身更能决定成败。

它真正改变的是我们对“智能从何而来”的理解：作者通过对比肌肉激活（MTU）与简化动作抽象（PD）的表现，实证了生物力学结构本身（被动弹性、阻尼）承担了部分控制计算，即“具身智能”或“mechanical intelligence”在DeepRL框架下是可量化、可利用的。这为后续研究打开了一个新方向：与其只优化策略网络，不如同时优化“身体”参数（执行器特性），让控制与力学协同进化。

## 方法拆解

### 任务框架
- 标准actor-critic结构，策略为高斯分布 π_θ(s,a) = N(μ_θ(s), Σ)，Σ固定为手动指定的对角协方差。
- 目标为最大化期望累积奖励 J(π_θ) = E[Σ γ^t r_t]，γ=0.9。
- 状态特征 s_t = (Φ(q_t, q̇_t), Φ(q*_t, q̇*_t))，即当前姿态特征加参考姿态特征，Φ包含根部高度、连杆相对位置和质心速度。

### 四种动作模型
- **PD（目标关节角）**：动作指定目标角度 q̂，力矩 τ = k_p(q̂ − q) + k_d(q̇̂ − q̇)，其中 q̇̂=0，k_p、k_d手动设定。
- **Vel（目标关节速度）**：动作指定目标速度 q̇̂，力矩 τ = k_d(q̇̂ − q̇)，k_d与PD相同。
- **Tor（力矩）**：动作直接指定关节力矩，受手动设定的力矩限制，无需额外执行器参数。
- **MTU（肌肉激活）**：动作指定肌腱单元激活 a_MTU ∈ [0,1]，力计算 F_MTU = F_SE = F_CE + F_PE，其中 F_CE = a_MTU · F_0 · f_l(l_CE) · f_v(v_CE)。肌肉成拮抗对出现，动作维度至少是其他模型的两倍。

### 关键训练技术
- **正时序差分（PTD）更新**：优势函数 A(s,a) = I[δ > 0]，δ = r + γV(s') − V(s)。仅当TD误差为正时更新actor，避免负TD更新将策略均值推向未知的劣质动作。
- **有界动作空间处理**：采用修正的inverting gradients方法——当策略均值 μ^i(s) 超出边界 [l^i, u^i] 且梯度方向指向边界外时，将梯度替换为 l^i − μ^i(s) 或 u^i − μ^i(s)；边界内则保留原始梯度。
- **探索退火**：训练时 λ ~ Ber(ε)，ε从1线性退火至0.2（500k次迭代），仅 λ=1 的元组用于actor更新，critic可用全部元组。
- **执行器参数优化（算法2）**：交替进行策略学习（算法1，250k次迭代）和CMA优化执行器参数 ψ（250代，每代16个样本，每个样本通过16次10s rollout评估），共6轮，约50小时。

### 奖励函数
r = 0.5·r_pose + 0.05·r_vel + 0.15·r_end + 0.1·r_root + 0.2·r_com，各分量均为指数型奖励，惩罚姿态、速度、末端执行器、根部和质心偏差。

## 关键创新

1. **首次系统量化动作空间选择对DeepRL运动学习的影响**：在统一框架下（相同网络结构、奖励、算法）对比四种动作表示，覆盖三个角色、七种运动，提供了可复现的基准数据。此前研究多局限于单一动作空间，无法回答“哪种表示更好、为什么”这一基本工程问题。

2. **提出策略-执行器交替优化方法**：将执行器参数（肌肉最大等长力、肌腱静息长度、力臂等）纳入优化循环，类比生物进化中“身体”与“控制”的协同适应。这是对“具身智能”理念的工程化实现——不是把身体当作固定约束，而是作为可优化的自由参数，使神经网络能有效驾驭高维肌肉模型。

3. **PTD更新与有界梯度修正的组合**：PTD避免了负TD更新导致的高斯策略均值漂移问题，而有界梯度修正比Hausknecht & Stone (2015)的inverting gradients更保守——仅在边界被违反时改变梯度，边界内保留原始梯度，减少了信息损失。两者结合使训练在多种动作空间下均能稳定收敛。

## 实验与结果

### 最终性能（NCR，归一化累积奖励）与学习速度（AUC）

| 角色+运动 | Tor | Vel | PD | MTU |
|-----------|-----|-----|-----|-----|
| Biped Walk | 0.7662 | 0.9520 | 0.9524 | **0.9584** |
| Biped March | 0.9353 | **0.9784** | 0.9767 | 0.9484 |
| Biped Run | 0.9032 | 0.9070 | 0.9057 | 0.8988 |
| Raptor Run (Sim) | 0.7265 | 0.9612 | **0.9863** | 0.9708 |
| Raptor Run | 0.6141 | 0.8732 | **0.9548** | 0.9533 |
| Dog Bound (Sim) | 0.8016 | 0.9788 | **0.9797** | 0.9033 |
| Dog Rear-Up | 0.8151 | 0.7364 | **0.9565** | 0.8744 |

### 关键发现
- **PD表现最稳定**：在7个运动中的5个学习速度最快（AUC最高），最终性能在所有运动中均达到可比最佳。
- **Tor性能最差**：在简单双足上尚可，但在狗和迅猛龙上显著恶化（如Raptor Run NCR仅0.6141）。
- **MTU学习最慢但潜力大**：AUC普遍最低（如Biped Run仅0.5360），但优化执行器参数后，在Biped Walk上取得最佳NCR（0.9584），且产生更自然的运动。
- **鲁棒性**：PD在所有运动中对外力扰动最鲁棒；Tor在双足行走中显著脆弱；MTU在迅猛龙奔跑中最不鲁棒。不规则地形测试中，狗的Tor策略因“不自然抖动”反而意外鲁棒（作者怀疑该行为规避了跌倒检测）。
- **查询率**：PD和Vel在低查询率（15Hz）下表现更好，Tor退化最快；MTU在低查询率下更好、高查询率下更差。

## 边界与局限

- **仅限2D平面模拟**：所有结论基于平面铰接角色，未验证3D场景，而3D中力矩控制与肌肉模型的差异可能更大。
- **MTU参数优化可能未达最优**：作者承认CMA优化所得执行器参数“可能仍非任务最优”，因此MTU与其他模型的比较可能低估了其潜力。
- **优化针对单一运动**：执行器参数针对特定运动优化，未考虑多任务适应性，而自然界期望更广泛的运动能力。
- **超参数敏感性**：探索率、协方差Σ、奖励权重等超参数对结果影响显著，作者认为总体趋势可能泛化，但无法给出确定性结论。
- **未做之事**：未测试其他RL算法（如PPO、SAC）或运动控制任务；未开发执行器力学与控制能力的协同设计算法。

## 工程启示

- **优先选择PD或Vel动作空间**：如果你的目标是快速获得稳定策略，PD（目标关节角）是最安全的选择——学习快、鲁棒性好、对查询率不敏感。Vel是PD的轻量替代，在简单任务上性能接近。
- **避免直接使用力矩控制**：除非任务极其简单（如2D双足），否则Tor策略在复杂角色上性能退化严重，且对扰动极脆弱。若必须使用，需配合更保守的探索策略和更长的训练时间。
- **MTU需配合执行器优化**：直接使用肌肉模型几乎总是学习最慢的，但优化执行器参数后可能获得更自然的运动。复现时注意：MTU优化约需50小时（6轮CMA），且每轮需重新训练策略，计算成本高。
- **核对关键超参数**：PTD更新（仅正TD误差更新actor）和探索退火（ε从1到0.2）是稳定训练的关键，不要随意改动。有界梯度修正需与动作边界定义一致，否则会导致策略均值卡在边界。
- **查询率选择**：若控制频率受限（如嵌入式系统），优先选择PD或Vel；若使用MTU，低查询率反而可能更好，但原因尚不明确，需自行验证。
- **最易踩坑处**：MTU的动作维度是其他模型的两倍以上（如Dog MTU动作44维 vs PD 20维），状态维度也更高（214 vs 170），网络输出层和协方差Σ需相应调整；执行器参数（表1）有114-282个，手动设定几乎不可能，必须使用CMA优化。

## 参考
- https://arxiv.org/abs/1611.01055
- https://github.com/ImChong/Robotics_Notebooks

## Overview

This paper systematically compares the effects of four action space parameterizations in deep reinforcement learning (DeepRL)—torque, target joint angular velocity, target joint angle, and muscle activation—on motion imitation learning, covering three characters: a biped, a velociraptor, and a dog. The core contribution lies in revealing the significant impact of action representation on learning speed, final performance, robustness, and policy query rate, and in proposing a heuristic method that alternately optimizes policy parameters and actuator parameters, enabling neural networks to effectively control complex musculoskeletal models.

## What It Changes

The value of this work lies not in proposing a new reinforcement learning algorithm, but in elevating "action space design"—a problem often treated as a hyperparameter in engineering practice—to the level of a core factor determining learning success or failure. Previous DeepRL research has mostly focused on network architecture, reward shaping, or algorithmic improvements, while the choice of action representation has often been implicitly treated as a "detail." This paper uses systematic controlled experiments to demonstrate the dominant influence of this "detail" on learning difficulty and final policy quality—especially on complex characters (dog, velociraptor) and high-dimensional muscle models, where the choice of action representation can matter more than the algorithm itself.

What it truly changes is our understanding of "where intelligence comes from": by comparing muscle activation (MTU) with simplified action abstractions (PD), the authors empirically demonstrate that biomechanical structure itself (passive elasticity, damping) bears part of the control computation—that is, "embodied intelligence" or "mechanical intelligence" is quantifiable and exploitable within the DeepRL framework. This opens a new direction for subsequent research: rather than only optimizing the policy network, one can also optimize "body" parameters (actuator characteristics), allowing control and mechanics to co-evolve.

## Method Breakdown

### Task Framework
- Standard actor-critic structure, with the policy being a Gaussian distribution π_θ(s,a) = N(μ_θ(s), Σ), where Σ is a fixed, manually specified diagonal covariance.
- Objective: maximize expected cumulative reward J(π_θ) = E[Σ γ^t r_t], with γ=0.9.
- State features s_t = (Φ(q_t, q̇_t), Φ(q*_t, q̇*_t)), i.e., current pose features plus reference pose features, where Φ includes root height, relative link positions, and center-of-mass velocity.

### Four Action Models
- **PD (target joint angle)**: The action specifies a target angle q̂, with torque τ = k_p(q̂ − q) + k_d(q̇̂ − q̇), where q̇̂=0 and k_p, k_d are manually set.
- **Vel (target joint velocity)**: The action specifies a target velocity q̇̂, with torque τ = k_d(q̇̂ − q̇), where k_d is the same as in PD.
- **Tor (torque)**: The action directly specifies joint torque, subject to manually set torque limits, requiring no additional actuator parameters.
- **MTU (muscle activation)**: The action specifies tendon unit activation a_MTU ∈ [0,1], with force computed as F_MTU = F_SE = F_CE + F_PE, where F_CE = a_MTU · F_0 · f_l(l_CE) · f_v(v_CE). Muscles appear in antagonistic pairs, and the action dimension is at least twice that of the other models.

### Key Training Techniques
- **Positive temporal difference (PTD) update**: Advantage function A(s,a) = I[δ > 0], where δ = r + γV(s') − V(s). The actor is updated only when the TD error is positive, avoiding negative TD updates that push the policy mean toward unknown, poor actions.
- **Bounded action space handling**: A modified inverting gradients method is used—when the policy mean μ^i(s) exceeds the bounds [l^i, u^i] and the gradient direction points outside the bounds, the gradient is replaced with l^i − μ^i(s) or u^i − μ^i(s); within the bounds, the original gradient is retained.
- **Exploration annealing**: During training, λ ~ Ber(ε), with ε annealed linearly from 1 to 0.2 (over 500k iterations); only tuples with λ=1 are used for actor updates, while the critic can use all tuples.
- **Actuator parameter optimization (Algorithm 2)**: Alternates between policy learning (Algorithm 1, 250k iterations) and CMA optimization of actuator parameters ψ (250 generations, 16 samples per generation, each sample evaluated via 16 10-second rollouts), for a total of 6 rounds, approximately 50 hours.

### Reward Function
r = 0.5·r_pose + 0.05·r_vel + 0.15·r_end + 0.1·r_root + 0.2·r_com, where each component is an exponential reward penalizing deviations in pose, velocity, end-effector, root, and center of mass.

## Key Innovations

1. **First systematic quantification of the impact of action space choice on DeepRL motion learning**: Under a unified framework (same network structure, reward, and algorithm), four action representations are compared across three characters and seven motions, providing reproducible benchmark data. Previous studies were mostly limited to a single action space and could not answer the fundamental engineering question of "which representation is better and why."

2. **Proposal of a policy-actuator alternating optimization method**: Actuator parameters (muscle maximum isometric force, tendon rest length, moment arms, etc.) are incorporated into the optimization loop, analogous to the co-adaptation of "body" and "control" in biological evolution. This is an engineering implementation of the "embodied intelligence" concept—treating the body not as a fixed constraint but as optimizable free parameters, enabling neural networks to effectively control high-dimensional muscle models.

3. **Combination of PTD updates and bounded gradient correction**: PTD avoids the problem of Gaussian policy mean drift caused by negative TD updates, while bounded gradient correction is more conservative than the inverting gradients of Hausknecht & Stone (2015)—it only alters the gradient when bounds are violated, retaining the original gradient within bounds, thereby reducing information loss. Together, they enable stable convergence across multiple action spaces.

## Experiments and Results

### Final Performance (NCR, normalized cumulative reward) and Learning Speed (AUC)

| Character + Motion | Tor | Vel | PD | MTU |
|-----------|-----|-----|-----|-----|
| Biped Walk | 0.7662 | 0.9520 | 0.9524 | **0.9584** |
| Biped March | 0.9353 | **0.9784** | 0.9767 | 0.9484 |
| Biped Run | 0.9032 | 0.9070 | 0.9057 | 0.8988 |
| Raptor Run (Sim) | 0.7265 | 0.9612 | **0.9863** | 0.9708 |
| Raptor Run | 0.6141 | 0.8732 | **0.9548** | 0.9533 |
| Dog Bound (Sim) | 0.8016 | 0.9788 | **0.9797** | 0.9033 |
| Dog Rear-Up | 0.8151 | 0.7364 | **0.9565** | 0.8744 |

### Key Findings
- **PD is the most stable**: It has the fastest learning speed (highest AUC) in 5 of 7 motions, and its final performance is comparably best across all motions.
- **Tor performs worst**: It is acceptable on simple bipeds but degrades significantly on dogs and velociraptors (e.g., Raptor Run NCR is only 0.6141).
- **MTU learns slowest but has high potential**: Its AUC is generally the lowest (e.g., Biped Run at only 0.5360), but after optimizing actuator parameters, it achieves the best NCR on Biped Walk (0.9584) and produces more natural motion.
- **Robustness**: PD is the most robust to external force perturbations across all motions; Tor is notably fragile in bipedal walking; MTU is the least robust in velociraptor running. In irregular terrain tests, the dog's Tor policy was unexpectedly robust due to "unnatural jitter" (the authors suspect this behavior circumvented fall detection).
- **Query rate**: PD and Vel perform better at low query rates (15Hz), while Tor degrades fastest; MTU performs better at low query rates and worse at high query rates.

## Boundaries and Limitations

- **2D planar simulation only**: All conclusions are based on planar articulated characters; 3D scenarios are not validated, and differences between torque control and muscle models may be even larger in 3D.
- **MTU parameter optimization may not be optimal**: The authors acknowledge that the actuator parameters obtained via CMA optimization "may still not be task-optimal," so comparisons between MTU and other models may underestimate its potential.
- **Optimization for a single motion**: Actuator parameters are optimized for specific motions and do not consider multi-task adaptability, whereas nature expects a broader range of motor abilities.
- **Hyperparameter sensitivity**: Hyperparameters such as exploration rate, covariance Σ, and reward weights significantly affect results. The authors believe the overall trends may generalize but cannot provide definitive conclusions.
- **What was not done**: Other RL algorithms (e.g., PPO, SAC) or motor control tasks were not tested; no co-design algorithm for actuator mechanics and control capability was developed.

## Engineering Implications

- **Prefer PD or Vel action spaces**: If your goal is to quickly obtain a stable policy, PD (target joint angle) is the safest choice—fast learning, good robustness, and insensitive to query rate. Vel is a lightweight alternative to PD with comparable performance on simple tasks.
- **Avoid direct torque control**: Unless the task is extremely simple (e.g., 2D biped), Tor policies degrade severely on complex characters and are highly fragile to perturbations. If it must be used, pair it with more conservative exploration strategies and longer training times.
- **MTU requires actuator optimization**: Directly using muscle models is almost always the slowest to learn, but optimizing actuator parameters can yield more natural motion. When reproducing, note that MTU optimization takes approximately 50 hours (6 rounds of CMA), and each round requires retraining the policy, incurring high computational cost.
- **Verify key hyperparameters**: PTD updates (updating the actor only on positive TD errors) and exploration annealing (ε from 1 to 0.2) are critical for stable training; do not modify them arbitrarily. Bounded gradient correction must be consistent with the definition of action bounds; otherwise, the policy mean may get stuck at the boundary.
- **Query rate selection**: If the control frequency is limited (e.g., embedded systems), prefer PD or Vel; if using MTU, lower query rates may actually be better, but the reason is not yet clear and requires your own validation.
- **Most common pitfalls**: The action dimension of MTU is more than twice that of other models (e.g., Dog MTU has 44 action dimensions vs. 20 for PD), and the state dimension is also higher (214 vs. 170); the network output layer and covariance Σ must be adjusted accordingly. Actuator parameters (Table 1) number 114–282, making manual setting nearly impossible; CMA optimization is mandatory.

## 개요

본 논문은 심층 강화 학습(DeepRL)에서 네 가지 동작 공간 파라미터화 방식(토크, 목표 관절 각속도, 목표 관절 각도, 근육 활성화)이 운동 모방 학습에 미치는 영향을 이족 보행, 랩터, 개 세 가지 캐릭터에 걸쳐 체계적으로 비교한다. 핵심 기여는 동작 표현이 학습 속도, 최종 성능, 강건성 및 정책 쿼리 빈도에 미치는 유의미한 영향을 밝히고, 정책 파라미터와 실행기 파라미터를 교대로 최적화하는 휴리스틱 방법을 제안하여 신경망이 복잡한 근골격 모델을 효과적으로 제어할 수 있게 한 점이다.

## 무엇을 바꾸었는가

이 연구의 가치는 새로운 강화 학습 알고리즘을 제안하는 것이 아니라, 공학 실무에서 종종 하이퍼파라미터처럼 취급되던 '동작 공간 설계' 문제를 학습 성패를 결정짓는 핵심 요소로 끌어올린 데 있다. 기존 DeepRL 연구는 주로 네트워크 아키텍처, 보상 형성 또는 알고리즘 개선에 집중했으며, 동작 표현의 선택은 암묵적으로 '세부 사항'으로 간주되었다. 본 논문은 체계적인 대조 실험을 통해 이 '세부 사항'이 학습 난이도와 최종 정책 품질에 지배적인 영향을 미친다는 것을 증명했다—특히 복잡한 캐릭터(개, 랩터)와 고차원 근육 모델에서는 동작 표현의 선택이 알고리즘 자체보다 성패를 더 크게 좌우한다.

이 연구가 진정으로 바꾼 것은 '지능이 어디서 오는가'에 대한 이해다: 저자들은 근육 활성화(MTU)와 단순화된 동작 추상화(PD)의 성능을 비교함으로써, 생체역학적 구조 자체(수동 탄성, 감쇠)가 일부 제어 계산을 담당한다는 것, 즉 '구현된 지능(embodied intelligence)' 또는 '기계적 지능(mechanical intelligence)'이 DeepRL 프레임워크에서 정량화 가능하고 활용 가능하다는 것을 실증했다. 이는 후속 연구에 새로운 방향을 열어준다: 정책 네트워크만 최적화하는 대신 '신체' 파라미터(실행기 특성)도 함께 최적화하여 제어와 역학이 공진화하도록 하는 것이다.

## 방법 분해

### 작업 프레임워크
- 표준 actor-critic 구조, 정책은 가우시안 분포 π_θ(s,a) = N(μ_θ(s), Σ), Σ는 수동으로 지정된 대각 공분산으로 고정.
- 목표는 기대 누적 보상 J(π_θ) = E[Σ γ^t r_t] 최대화, γ=0.9.
- 상태 특징 s_t = (Φ(q_t, q̇_t), Φ(q*_t, q̇*_t)), 즉 현재 자세 특징과 참조 자세 특징의 결합, Φ는 루트 높이, 링크 상대 위치, 질량 중심 속도를 포함.

### 네 가지 동작 모델
- **PD(목표 관절 각도)**: 동작이 목표 각도 q̂를 지정, 토크 τ = k_p(q̂ − q) + k_d(q̇̂ − q̇), 여기서 q̇̂=0, k_p, k_d는 수동 설정.
- **Vel(목표 관절 속도)**: 동작이 목표 속도 q̇̂를 지정, 토크 τ = k_d(q̇̂ − q̇), k_d는 PD와 동일.
- **Tor(토크)**: 동작이 관절 토크를 직접 지정, 수동 설정된 토크 제한을 받으며 추가 실행기 파라미터 불필요.
- **MTU(근육 활성화)**: 동작이 힘줄 단위 활성화 a_MTU ∈ [0,1]를 지정, 힘 계산 F_MTU = F_SE = F_CE + F_PE, 여기서 F_CE = a_MTU · F_0 · f_l(l_CE) · f_v(v_CE). 근육은 길항 쌍으로 나타나며, 동작 차원은 다른 모델의 최소 두 배.

### 핵심 훈련 기술
- **양의 시간차(PTD) 업데이트**: 이점 함수 A(s,a) = I[δ > 0], δ = r + γV(s') − V(s). TD 오차가 양수일 때만 actor를 업데이트하여, 음의 TD 업데이트가 정책 평균을 알 수 없는 열등한 동작으로 밀어내는 것을 방지.
- **유계 동작 공간 처리**: 수정된 inverting gradients 방법 사용—정책 평균 μ^i(s)가 경계 [l^i, u^i]를 벗어나고 그래디언트 방향이 경계 밖을 가리킬 때, 그래디언트를 l^i − μ^i(s) 또는 u^i − μ^i(s)로 대체; 경계 내에서는 원래 그래디언트 유지.
- **탐색 어닐링**: 훈련 시 λ ~ Ber(ε), ε는 1에서 0.2로 선형 어닐링(500k 반복), λ=1인 튜플만 actor 업데이트에 사용, critic은 전체 튜플 사용 가능.
- **실행기 파라미터 최적화(알고리즘 2)**: 정책 학습(알고리즘 1, 250k 반복)과 CMA 실행기 파라미터 ψ 최적화(250세대, 각 세대 16개 샘플, 각 샘플은 16회 10초 rollout 평가)를 교대로 수행, 총 6라운드, 약 50시간.

### 보상 함수
r = 0.5·r_pose + 0.05·r_vel + 0.15·r_end + 0.1·r_root + 0.2·r_com, 각 구성 요소는 지수형 보상으로 자세, 속도, 말단 실행기, 루트 및 질량 중심 편차를 페널티.

## 핵심 혁신

1. **동작 공간 선택이 DeepRL 운동 학습에 미치는 영향의 최초 체계적 정량화**: 통일된 프레임워크(동일한 네트워크 구조, 보상, 알고리즘)에서 네 가지 동작 표현을 비교하고, 세 캐릭터, 일곱 가지 운동을 포괄하며 재현 가능한 벤치마크 데이터를 제공. 기존 연구는 대부분 단일 동작 공간에 국한되어 '어떤 표현이 더 나은가, 왜 그런가'라는 기본적인 공학적 질문에 답할 수 없었다.

2. **정책-실행기 교대 최적화 방법 제안**: 실행기 파라미터(근육 최대 등척성 힘, 힘줄 휴지 길이, 모멘트 팔 등)를 최적화 루프에 포함시켜 생물학적 진화에서 '신체'와 '제어'의 협응 적응을 유추. 이는 '구현된 지능' 개념의 공학적 구현—신체를 고정된 제약이 아닌 최적화 가능한 자유 파라미터로 취급하여 신경망이 고차원 근육 모델을 효과적으로 다룰 수 있게 함.

3. **PTD 업데이트와 유계 그래디언트 수정의 결합**: PTD는 음의 TD 업데이트로 인한 가우시안 정책 평균 드리프트 문제를 방지하고, 유계 그래디언트 수정은 Hausknecht & Stone (2015)의 inverting gradients보다 보수적—경계가 위반될 때만 그래디언트를 변경하고 경계 내에서는 원래 그래디언트를 유지하여 정보 손실을 줄임. 두 기법의 결합으로 다양한 동작 공간에서 훈련이 안정적으로 수렴.

## 실험 및 결과

### 최종 성능(NCR, 정규화 누적 보상) 및 학습 속도(AUC)

| 캐릭터+운동 | Tor | Vel | PD | MTU |
|-----------|-----|-----|-----|-----|
| Biped Walk | 0.7662 | 0.9520 | 0.9524 | **0.9584** |
| Biped March | 0.9353 | **0.9784** | 0.9767 | 0.9484 |
| Biped Run | 0.9032 | 0.9070 | 0.9057 | 0.8988 |
| Raptor Run (Sim) | 0.7265 | 0.9612 | **0.9863** | 0.9708 |
| Raptor Run | 0.6141 | 0.8732 | **0.9548** | 0.9533 |
| Dog Bound (Sim) | 0.8016 | 0.9788 | **0.9797** | 0.9033 |
| Dog Rear-Up | 0.8151 | 0.7364 | **0.9565** | 0.8744 |

### 핵심 발견
- **PD가 가장 안정적**: 7개 운동 중 5개에서 학습 속도가 가장 빠르고(AUC 최고), 최종 성능은 모든 운동에서 비교 가능한 최고 수준.
- **Tor 성능 최악**: 단순 이족 보행에서는 그럭저럭이지만, 개와 랩터에서는 현저히 악화(Raptor Run NCR 0.6141에 불과).
- **MTU 학습 가장 느리지만 잠재력 큼**: AUC가 전반적으로 가장 낮지만(Biped Run 0.5360), 실행기 파라미터 최적화 후 Biped Walk에서 최고 NCR(0.9584) 달성, 더 자연스러운 운동 생성.
- **강건성**: PD는 모든 운동에서 외력 교란에 가장 강건; Tor는 이족 보행에서 현저히 취약; MTU는 랩터 달리기에서 가장 비강건. 불규칙 지형 테스트에서 개의 Tor 정책은 '부자연스러운 떨림'으로 인해 오히려 예상외로 강건(저자들은 이 행동이 낙상 감지를 회피한다고 의심).
- **쿼리 빈도**: PD와 Vel은 낮은 쿼리 빈도(15Hz)에서 더 우수, Tor는 가장 빠르게 성능 저하; MTU는 낮은 쿼리 빈도에서 더 좋고 높은 쿼리 빈도에서 더 나쁨.

## 경계 및 한계

- **2D 평면 시뮬레이션에만 국한**: 모든 결론은 평면 관절 캐릭터 기반, 3D 장면은 검증되지 않았으며 3D에서 토크 제어와 근육 모델의 차이는 더 클 수 있음.
- **MTU 파라미터 최적화가 최적이 아닐 수 있음**: 저자들은 CMA 최적화로 얻은 실행기 파라미터가 '여전히 작업 최적이 아닐 수 있다'고 인정, 따라서 MTU와 다른 모델의 비교는 그 잠재력을 과소평가했을 수 있음.
- **단일 운동에 대한 최적화**: 실행기 파라미터는 특정 운동에 대해 최적화되었으며 다중 작업 적응성을 고려하지 않음, 자연계에서는 더 넓은 운동 능력이 기대됨.
- **하이퍼파라미터 민감성**: 탐색률, 공분산 Σ, 보상 가중치 등의 하이퍼파라미터가 결과에 유의미한 영향을 미치며, 저자들은 전체 경향이 일반화될 수 있다고 보지만 확정적 결론은 내릴 수 없음.
- **수행하지 않은 것**: 다른 RL 알고리즘(PPO, SAC 등)이나 운동 제어 작업을 테스트하지 않음; 실행기 역학과 제어 능력의 공동 설계 알고리즘을 개발하지 않음.

## 공학적 시사점

- **PD 또는 Vel 동작 공간 우선 선택**: 빠르게 안정적인 정책을 얻는 것이 목표라면 PD(목표 관절 각도)가 가장 안전한 선택—학습이 빠르고, 강건성이 좋으며, 쿼리 빈도에 둔감. Vel은 PD의 경량 대안으로 단순 작업에서 성능이 유사.
- **직접 토크 제어 회피**: 작업이 극도로 단순하지 않다면(2D 이족 보행 등), Tor 정책은 복잡한 캐릭터에서 성능 저하가 심하고 교란에 극도로 취약. 반드시 사용해야 한다면 더 보수적인 탐색 전략과 더 긴 훈련 시간 필요.
- **MTU는 실행기 최적화와 병행 필요**: 근육 모델을 직접 사용하면 거의 항상 학습이 가장 느리지만, 실행기 파라미터 최적화 후 더 자연스러운 운동을 얻을 수 있음. 재현 시 주의: MTU 최적화는 약 50시간(6라운드 CMA)이 소요되며 각 라운드마다 정책을 재훈련해야 하므로 계산 비용이 높음.
- **핵심 하이퍼파라미터 확인**: PTD 업데이트(양의 TD 오차만으로 actor 업데이트)와 탐색 어닐링(ε 1에서 0.2)은 안정적 훈련의 핵심이므로 임의로 변경하지 말 것. 유계 그래디언트 수정은 동작 경계 정의와 일치해야 하며, 그렇지 않으면 정책 평균이 경계에 갇힐 수 있음.
- **쿼리 빈도 선택**: 제어 빈도가 제한된 경우(임베디드 시스템 등), PD 또는 Vel을 우선 선택; MTU를 사용한다면 낮은 쿼리 빈도가 오히려 더 나을 수 있지만 원인은 아직 불명확하므로 직접 검증 필요.
- **가장 흔한 함정**: MTU의 동작 차원은 다른 모델의 두 배 이상(Dog MTU 동작 44차원 vs PD 20차원), 상태 차원도 더 높음(214 vs 170), 네트워크 출력 레이어와 공분산 Σ를 그에 맞게 조정해야 함; 실행기 파라미터(표 1)는 114-282개로 수동 설정이 거의 불가능하므로 반드시 CMA 최적화 사용.
