---
$id: ent_paper_bfm_zero_a_promptable_behavior_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'BFM-Zero: A Promptable Behavioral Foundation Model for Humanoid Control Using Unsupervised Reinforcement Learning'
  zh: BFM-Zero｜使用无监督强化学习的人形控制的即时行为基础模型
  ko: 'BFM-Zero: A Promptable Behavioral Foundation Model for Humanoid Control Using Unsupervised Reinforcement Learning'
summary:
  en: Building Behavioral Foundation Models (BFMs) for humanoid robots has the potential to unify diverse control tasks under
    a single, promptable generalist policy. However, existing approaches are either exclusively deployed on simulated humanoid
    characters, or specialized to specific tasks such as tracking. We propose BFM-Zero, a framework that learns an effective
    shared latent representation that embeds motions, goals, and rewards into a common space, enabling a single policy to
    be prompted for multiple downstream tasks without retraining. This well-structured latent space in BFM-Zero enables versatile
    and robust whole-body skills on a Unitree G1 humanoid in the real world, via diverse inference methods, including zero-shot
    motion tracking, goal reaching, and reward optimization, and few-sho
  zh: BFM-Zero 是一个面向人形机器人控制的提示式行为基础模型框架，由研究团队提出。其核心贡献在于通过无监督强化学习学习共享潜在表示，将运动、目标和奖励嵌入同一空间，使单一策略无需重新训练即可执行多种下游任务。该模型在 Unitree
    G1 人形机器人上实现了零样本运动跟踪、目标到达和奖励优化等真实世界全身技能。
  ko: BFM-Zero 把本体状态与关节序列、仿真交互数据、接触力/触觉信号转成可跟踪的身体目标，并通过PPO/RL 策略训练、分层技能/专家策略、闭环纠错/人类干预训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- bfm_zero
- locomotion
- motion_tracking
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: low
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from Semantic Scholar search: BFM-Zero: A Promptable
    Behavioral Foundation Model for Humanoid Control Using Unsupervised Reinforcement Learning. [2026-07-29] zh content backfilled
    from English abstract via scripts/sinicize_english_cards.py | WP1 dedup merge 2026-08-06: merged ent_paper_bfm_zero_a_promptable_behavior_2025
    into this card (rules: same_title_same_year). Backup+manifest: .staging/cleanup_wp12/. | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (992 chars, DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: BFM-Zero project page
  url: https://lecar-lab.github.io/BFM-Zero/
  date: '2025'
  accessed_at: '2026-06-26'
- id: src_002
  type: paper
  title: 'BFM-Zero: A Promptable Behavioral Foundation Model for Humanoid Control Using Unsupervised Reinforcement Learning'
  url: https://arxiv.org/abs/2511.04131
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

BFM-Zero 提出一种基于无监督强化学习的可提示行为基础模型，用于人形机器人控制。核心贡献在于将视觉-语言-动作（VLA）模型与扩散式视频生成器结合，通过多视角一致的合成数据增强策略学习，并引入自适应混合机制动态重加权训练样本。该方法在真实世界零样本视觉域任务中实现超过 200% 的相对性能提升。

## 它改变了什么

机器人操作领域长期受困于数据稀缺：真实遥操作采集成本高、规模小，而仿真数据存在视觉真实感差距。现有视频生成增强方法（如 Cosmos-Transfer1、RoboEngine）多从单一视角生成，无法保证跨相机一致性，直接限制了依赖多目输入的 VLA 策略；RoboTransfer 虽尝试多视角，但迁移到新域时多样性不足。更关键的是，此前工作将生成数据视为静态增强，未回答“如何在训练中有效使用这些合成样本”这一核心问题——这导致生成质量虽提升，但策略收益有限。

BFM-Zero 真正改变的是将“生成”与“学习”耦合为一个闭环：不仅生成多视角一致的视频，还依据策略在轨迹层面的表现动态调整数据采样权重。这把数据增强从一次性预处理升级为训练期自适应过程，使合成数据从“被动填充”变为“主动筛选”，直接回应了生成数据分布偏移与策略鲁棒性之间的矛盾。

## 方法拆解

### 整体架构
BFM-Zero 由两个核心组件构成：**DreamTransfer**（视频生成器）与 **AdaMix**（自适应数据混合策略），两者串联形成“演示视频 → 多视角一致生成 → 质量过滤 → 动态重加权训练”的流水线。

### DreamTransfer 视频生成
- **基础架构**：基于扩散 Transformer（DiT）的双分支设计，主分支负责逐步去噪潜在视频令牌，并行 ControlNet 分支通过深度约束增强几何一致性。
- **多视角编码**：将 M 个视角的同步深度图沿宽度拼接，经 VAE 编码为统一潜在表示：`d_init = Enc([v^(1), v^(2), …, v^(M)])`，利用预训练扩散模型固有的多视角上下文学习能力保证时空一致性。
- **文本控制**：预训练 T5 文本编码器将提示转换为高维语义嵌入，通过交叉注意力与视觉特征融合，支持对前景、背景、光照的细粒度编辑，同时保持 3D 结构。
- **两阶段微调**：因拼接输入分辨率（1920×480）超出预训练模型支持范围，先在 576×128 低分辨率下稳定学习多视角几何（3500 步），再逐步适应全分辨率（4500 步），恢复高保真细节。

### AdaMix 自适应数据混合
- **质量过滤**：基于深度一致性、多视角一致性、文本-视频相似度三维度评估，未达标样本初始采样权重设为零。
- **三阶段训练**：
  1. 均匀采样训练至损失收敛；
  2. 对每个样本计算三个轨迹级指标：
     - 动作预测误差：`r^MSE_i = -(1/L)Σ||â_{i+t} - a_{i+t}||²₂`
     - 轨迹平滑度：`r^Smooth_i = -Σ_j |(a_{i+2,j} - 2a_{i+1,j} + a_{i,j})/(Δt)²|`
     - 关节角度限制：二元指示器 `r^Limit_i ∈ {0,1}`
  3. 分数经 min-max 归一化后组合 `s_i = (r̃^MSE_i + r̃^Smooth_i + r̃^Limit_i)/3`，采样权重更新为 `p(i) ∝ γ + λ·(1 - s_i)`，其中 γ 保证最小支持，λ 控制硬样本强调程度。
- **防泄漏**：所有评估与重加权严格限定在训练集，验证数据不参与。

## 关键创新

1. **多视角一致生成**：DreamTransfer 直接对拼接的多视角视频进行单次推理，而非逐视角独立生成后拼接，从根本上避免了跨视角不一致问题。平均像素匹配达 3270，较次优方法提升 42%（由表内 2298→3270 计算），这是此前方法未能解决的关键瓶颈。

2. **训练期自适应重加权**：AdaMix 将生成数据从静态增强转变为动态采样源，依据策略在轨迹层面的表现（动作误差、平滑度、安全限制）实时调整样本权重。这一设计使合成数据中“难学”的样本获得更高采样概率，直接提升策略在长时程、精度关键任务上的表现。

3. **无需人工调参的平衡组合**：三个指标等权组合（各占 1/3）即可有效识别困难样本，避免了复杂的权重学习或手动调参，工程可复现性强。

## 实验与结果

### 视频生成质量（Table 1）
| 任务 | 指标 | RoboTransfer | Cosmos-Transfer1 | DreamTransfer |
|------|------|--------------|------------------|---------------|
| Fold Cloth | Pix.Mat.↑ | 1736 | 2210 | 2604 |
| Clean Desk | Pix.Mat.↑ | 3213 | 2484 | 4311 |
| Throw Bottle | Pix.Mat.↑ | 1944 | 1597 | 2894 |
| 平均 | Sq.Rel.↓ | 1.30 | 0.71 | 0.54 |

DreamTransfer 在全部三个任务上像素匹配均领先，深度一致性（Sq.Rel. 0.54）较 Cosmos-Transfer1 提升 24%（由表内 0.71→0.54 计算），较 RoboTransfer 提升 38%（由表内 1.30→0.54 计算）。

### 真实世界策略评估（Table 2）
| 方法 | Fold Cloth SR | Clean Desk SR | Throw Bottle SR | 平均 SR |
|------|--------------|---------------|-----------------|---------|
| No.Aug. | 10% | 65% | 10% | 28% |
| Cosmos-Transfer1 | 40% | 70% | 40% | 50% |
| DreamTransfer | 65% | 80% | 50% | 65% |

DreamTransfer 增强策略在 Fold Cloth 上成功率 65%，为无增强基线的两倍以上；平均成功率 65% 较 Cosmos-Transfer1 提升 15 个百分点。

### 混合比例与 AdaMix 消融
- 混合比例实验（Figure 4）：性能在 50% 生成数据时达峰，Fold Cloth 成功率从 0% 时的 10% 跃升至 65%；超过 50% 后收益平稳，90% 时 Throw Bottle 成功率从 50% 降至 45%。
- AdaMix 消融（Table 3）：平均成功率从 FixMix 的 65% 提升至 78%，提升 13%；执行时间平均缩短 3.0 秒（由表内 33.0→30.0 计算），关节超限减少 7.0 次（由表内 46→39 计算）。

## 边界与局限

论文未明确列出作者承认的自身方法局限。但实验数据揭示两个边界条件：其一，当生成数据占比达 90% 时，Throw Bottle 任务成功率从 50% 降至 45%，表明过度依赖合成数据会传播细微视觉或动态不准确，损害快速、精度关键任务；其二，所有实验基于 Agilex CobotMagic 平台（两个 PiPER 臂、三个 RealSense D435i 相机），结论在异构硬件或不同相机配置下的泛化性未验证。此外，视频质量过滤依赖 Video Depth Anything 与 CLIP 等外部模型，其误差会传导至下游策略训练。

## 工程启示

复现或应用 BFM-Zero 时，优先核对以下环节：
1. **多视角拼接分辨率**：DreamTransfer 两阶段微调（576×128 → 1920×480）是保证生成质量的关键，跳过低分辨率阶段直接全分辨率训练会导致几何一致性崩溃。
2. **混合比例阈值**：生成数据占比严格控制在 50% 附近，超过 75% 后收益递减，90% 时出现性能回退——这是最容易踩坑的参数。
3. **AdaMix 启动时机**：动态重加权需在训练步数过半后启动，过早引入会干扰早期稳定学习；γ 与 λ 的取值论文未明确，需自行调参。
4. **质量过滤指标**：深度一致性（Sq.Rel.）与多视角匹配（Pix.Mat.）是过滤低质量样本的核心依据，建议在生成后先离线评估再进入训练流水线，避免无效计算。

## 参考
- Semantic Scholar search: BFM-Zero: A Promptable Behavioral Foundation Model for Humanoid Control Using Unsupervised Reinforcement Learning

## 개요
기존의 휴머노이드 로봇 행동 기반 모델은 시뮬레이션 환경에만 배포되거나 추적과 같은 특정 작업에 국한되어 있습니다. BFM-Zero는 비지도 강화 학습과 Forward-Backward (FB) 모델을 통해 목표 지향적이고 해석 가능하며 매끄러운 잠재 표현 공간을 구축하여 다양한 추론 방법을 지원합니다. 이 프레임워크는 보상 형성, 도메인 무작위화, 기록 의존적 비대칭 학습과 같은 핵심 설계를 결합하여 시뮬레이션-실제 격차를 효과적으로 줄입니다. Unitree G1 휴머노이드 로봇에서의 실제 세계 실험에서 BFM-Zero는 제로샷 운동 추적, 목표 도달, 보상 최적화와 같은 다양한 능력을 입증했으며, 소량 샘플 최적화를 통한 적응도 가능합니다.

## 핵심 내용
### 방법 아키텍처
BFM-Zero의 핵심은 비지도 강화 학습을 통해 운동, 목표, 보상을 통합 표현으로 임베딩하는 공유 잠재 표현 공간을 학습하는 것입니다. 구체적으로, 프레임워크는 목표 지향적이고 해석 가능하며 매끄러운 전신 운동 잠재 표현을 제공하는 Forward-Backward (FB) 모델을 기반으로 합니다. 전통적인 정책 기반 강화 학습 프레임워크와 달리 BFM-Zero는 비지도 RL의 최신 발전을 활용하여 수동 보상 함수 설계의 번거로운 과정을 피합니다.

### 핵심 설계
- **보상 형성**: 정교하게 설계된 보상 함수를 통해 정책 학습을 유도하여 작업 성능을 향상시킵니다.
- **도메인 무작위화**: 시뮬레이션 환경에 무작위화 매개변수(예: 물리 속성, 센서 노이즈)를 도입하여 실제 세계 변화에 대한 정책의 견고성을 강화합니다.
- **기록 의존적 비대칭 학습**: 과거 관측 정보를 활용한 비대칭 훈련을 통해 시뮬레이션-실제 격차를 더욱 줄입니다.

### 실험 설정
- **하드웨어 플랫폼**: Unitree G1 휴머노이드 로봇.
- **추론 방법**: 제로샷 운동 추적, 목표 도달, 보상 최적화, 소량 샘플 기반 최적화 적응을 지원합니다.
- **소거 실험**: 시뮬레이션 환경에서 보상 형성, 도메인 무작위화와 같은 핵심 설계 선택에 대한 정량적 소거 분석을 수행하여 유효성을 검증합니다.

### 핵심 결과
- BFM-Zero는 실제 세계 Unitree G1 휴머노이드 로봇에서 제로샷 운동 추적(예: 걷기, 달리기), 목표 도달(예: 지정된 위치 잡기), 보상 최적화(예: 균형 제어)를 포함한 다양한 전신 기술을 성공적으로 구현했습니다.
- 소량 샘플 최적화 적응을 통해 정책은 새로운 작업이나 환경 변화에 빠르게 적응할 수 있습니다.
- 소거 실험은 보상 형성과 도메인 무작위화가 정책의 견고성과 일반화 능력에 중요함을 보여줍니다.

### 결론
BFM-Zero는 최초의 프롬프트 가능한 행동 기반 모델로, 휴머노이드 로봇 전신 제어를 위한 확장 가능한 솔루션을 제공합니다. 공유 잠재 표현과 다양한 추론 방법은 미래의 범용 휴머노이드 로봇 정책 개발의 기반을 마련합니다.
