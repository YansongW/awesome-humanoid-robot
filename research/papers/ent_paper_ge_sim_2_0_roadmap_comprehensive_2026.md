---
$id: ent_paper_ge_sim_2_0_roadmap_comprehensive_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'GE-Sim 2.0: A Roadmap Towards Comprehensive Closed-loop Video World Simulators for Robotic Manipulation'
  zh: 'GE-Sim 2.0: A Roadmap Towards Comprehensive Closed-loop Video World Simulators for Robotic Manipulation'
  ko: 'GE-Sim 2.0: A Roadmap Towards Comprehensive Closed-loop Video World Simulators for Robotic Manipulation'
summary:
  en: We introduce GE-Sim 2.0 (Genie Envisioner World Simulator 2.0), a closed-loop video world simulator for robotic manipulation.
    Building on the action-conditioned video generation framework of Genie Envisioner, GE-Sim 2.0 is re-trained on thousands
    of hours of real-world robot data spanning teleoperation, contact-rich interaction, and on-robot policy deployment, substantially
    improving action-following fidelity and tra
  zh: GE-Sim 2.0 是一个面向机器人操作的闭环视频世界模拟器，由 Genie Envisioner 团队开发。其核心贡献在于通过三个新模块（状态专家、世界裁判、加速框架）将视频模拟与策略学习闭环连接，仅用 2B 参数即登顶 WorldArena
    排行榜，并实现真实世界策略性能提升。
  ko: We introduce GE-Sim 2.0 (Genie Envisioner World Simulator 2.0), a closed-loop video world simulator for robotic manipulation.
    Building on the action-conditioned video generation framework of Genie Envisioner, GE-Sim 2.0 is re-trained on thousands
    of hours of real-world robot data spanning teleoperation, contact-rich interaction, and on-robot policy deployment, substantially
    improving action-following fidelity and tra
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- ge
- sim
- '2'
- '0'
- roadmap
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: low
  notes: 'Full ingest from Yuanxq lab paper list row 372 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2605.27491v1); zh content by DeepSeek from the abstract. Institutions unknown
    (not in source list). [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek
    deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2605.27491 GE-Sim 2.0: A Roadmap Towards Comprehensive Closed-loop Video World Simulators for Robotic Manipulation'
  url: https://arxiv.org/abs/2605.27491
  accessed_at: '2026-07-31'
  date: '2026-05-26'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

GE-Sim 2.0 是 NVIDIA 团队提出的闭环视频世界模拟器，在 2B 参数的动作条件视频生成骨干上，新增本体感觉状态专家、世界裁判和 DMD2 蒸馏加速三大模块，将视频生成从"仅视觉预测"推进为"可闭环评估与学习"的完整模拟器。核心贡献在于首次将状态预测、奖励评估和高效推理集成于单一框架，并在 WorldArena 排行榜登顶，闭环成功率与真实机器人一致性在 1 pp 以内。

## 它改变了什么

机器人学习正从"算法驱动"转向"数据驱动"，但评估环节仍是瓶颈：真实机器人基准测试缓慢、难以复现，而经典物理模拟器（MuJoCo、Isaac Gym 等）在接触动力学、可变形物体和视觉真实感上存在根本性短板。生成式视频模型提供了新路径，但现有视频世界模拟器（包括 GE-Sim 初版）有三个致命缺陷：只预测视觉、不输出本体感觉状态；只生成 rollout、不提供可验证的成功信号；推理吞吐量远低于规模化并行评估需求。

GE-Sim 2.0 真正改变的是"视频世界模型能做什么"的边界——它不再是一个被动的视频生成器，而是一个主动的闭环评估器。通过将本体感觉状态预测、奖励判断和加速推理集成进单一框架，它让"在模拟器中训练和评估策略"这件事第一次有了接近真实世界的可信度。这不仅是工程改进，而是重新定义了视频世界模型在机器人学习流水线中的角色：从"可视化工具"升级为"可验证的模拟环境"。

## 方法拆解

### 整体架构
保留 GE-Sim 的动作条件视频生成骨干（Cosmos-Predict2-2B-Video2World DiT），在数千小时真实机器人数据上重新训练，新增三个并行模块：视觉专家（Vision Expert）、本体感觉状态专家（Proprioceptive State Expert）、世界裁判（World Judge），以及 DMD2 蒸馏加速框架。

### 视觉专家
- 条件输入：`z_cond = [z_noisy; R_ray; M_pose; m_cond]`，其中 `R_ray` 为 6 通道逐像素射线图（原点+单位方向），`M_pose` 为 3 通道 EE 位姿图，`m_cond` 为 1 通道二进制掩码区分记忆帧与待预测帧。
- 所有输入归一化到 [-1,1]，避免扩散训练尺度不匹配。
- 训练损失：流匹配目标 `L_video = w(τ) ||(v_θ - (ε - l)) ⊙ (1 - M)||_2^2`，仅在待预测帧上计算。
- 记忆帧增强：渐进噪声混合（σ_mem=0.2，首帧 σ_first=0.5）、局部高斯模糊（核大小 [1,5]）、多视角同步颜色抖动（概率 0.3），全部由外部激活概率 0.8 门控。

### 本体感觉状态专家
- 轻量 Transformer 分支，从视觉专家所有 L 层输出加权融合（可学习标量 `α_l` 初始化为 1）后经 LayerNorm：`H_fuse = LayerNorm(Σ α_l h_l_video)`。
- 状态表示：16 维 `s_t = [θ_t^L, g_t^L, θ_t^R, g_t^R]`（7 维关节角 + 夹爪开度）。
- 训练损失：`L_proprio_state = E ||v_φ(s_τ, τ, H_fuse) - (ε - s_0)||_2^2`，视觉专家冻结。
- 历史状态增强：delta 索引偏移（Δ~Uniform{−3,…,3}）和时间重采样（n_prev→n_prev−1→n_prev 低通插值），各以概率 0.5 独立应用。

### 世界裁判
- 基于 Robometer 的视觉语言奖励模型，冻结视觉编码器，仅训练语言模型和预测头。
- 每帧单独输入，附加专用每帧 token，取隐藏状态 `f_i` 作为帧表示。
- 仅训练逐帧成功预测目标（放弃进度预测），理由：真实轨迹含错误恢复行为，进度标签有噪声且非单调。
- 损失：类平衡二元交叉熵 `L_judge = Σ m_i w_i BCE(σ(s_hat_i), y_i) / Σ m_i w_i`。

### DMD2 蒸馏加速
- 三步蒸馏：冻结教师（主训练阶段视觉专家）、少步学生（目标 4 步）、可训练 fake-score critic。
- 分布匹配梯度：`∇_DMD = (x₀^fake − x₀^teacher) / ‖x₀^student − x₀^teacher‖`。
- 学生每 5 步更新一次，critic 其余 4 步更新；学生固定 sigma 调度 [1.0, 0.9375, 0.8333, 0.625]。
- 随机步长训练：每个 chunk 帧以随机时间步长采样，推理时可跳帧，覆盖最多 4 倍时间跨度。

## 关键创新

1. **状态预测与视觉生成的联合建模**：首次将本体感觉状态解码器与视频生成器并行集成，且通过可学习权重 `α_l` 实现跨层视觉上下文融合。这解决了纯视觉模拟器中"命令动作作为噪声代理会漂移"的根本问题，使闭环 rollout 中状态累积误差可控。

2. **世界裁判的"稀疏成功信号"设计**：放弃进度预测、仅用逐帧成功二分类，是对真实机器人数据特性的深刻洞察——轨迹中普遍存在绕路、重试、暂时回退，进度标签非单调且有噪声。这一设计让奖励信号在 WM rollout 上准确率达 79%，比 122B 参数的 Qwen3.5 高 19 pp。

3. **DMD2 蒸馏 + 随机步长训练的组合加速**：将 100 帧 rollout 从分钟级压缩到 2.3 秒（单 H100，4 步），同时随机步长训练支持推理时 4 倍帧跳过。这使大规模并行评估首次成为可能，是"闭环模拟器"从概念走向工程可用的关键一步。

## 实验与结果

### WorldArena 排行榜
GE-Sim 2.0 以 2B 参数登顶公开排行榜，超越专用机器人世界模型（Ctrl-World、DreamDojo、GigaWorld、ABot）和闭源通用视频生成器（Sora、Veo）。

### 逐任务回放指标（表 2，头视角）
| 指标 | Ctrl-World | DreamDojo | GE-Sim 2.0 |
|------|-----------|-----------|------------|
| PSNR | 19.09 | 17.38 | **23.05** |
| SSIM | 0.786 | 0.728 | **0.846** |
| LPIPS | 0.296 | 0.353 | **0.145** |
| FID | 62.70 | 79.82 | **32.28** |
| FVD | 1083.7 | 1155.0 | **481.3** |

### 长程时间鲁棒性（图 6）
GE-Sim 2.0 在 50 秒回放中 PSNR 从 24.84 dB 衰减到 21.08 dB（摆动 <4 dB）；Ctrl-World 和 DreamDojo 第一段后急剧下降至 16.16 和 15.44 dB。

### 世界裁判奖励质量（表 3，平均）
| 模式 | GE-Sim 2.0 准确率 | Qwen3.5-122B 准确率 | 事件距离（帧） |
|------|------------------|---------------------|----------------|
| WM rollout | **79%** | 60% | 28.2 vs. 57.8 |
| GT 视频 | **87%** | 58% | 15.7 vs. 64.7 |

### 闭环一致性（图 8、10）
- 带状态条件时拟合趋势斜率接近 1，小负偏移；去掉状态条件后趋势更浅、正偏移。
- 片段级准确率：带状态 0.81 / 不带 0.74 / Ctrl-World 0.63；召回率：0.82 / 0.67 / 0.25。

### WM 过滤行为克隆（图 9）
真实机器人成功率：Pour water 0.40→0.55，Fold towels 0.40→0.50，Pull out plug 0.45→0.65；平均 0.417→0.567（绝对增益 0.150）。

### 消融
- 去掉状态专家：片段级准确率 0.81→0.74，召回率 0.82→0.67。
- 用 Qwen 替换世界裁判：WM 模式准确率降 19 pp，事件距离增加 30 帧。

## 边界与局限

- 接触丰富任务（Pull out plug、Command grasp & release）上仍存在假阳性和假阴性，细粒度接触状态和长程状态累积仍是挑战。
- Pour water 和 Borrow flame 上检测小尺度视觉信号（液位、火焰传递）仍是开放问题。
- 基础模型仅在固定具身的双臂遥操作和机器人回放数据上训练，未扩展到跨具身、ego-view、UMI 风格数据。
- 世界裁判是外部模块，未统一到单一世界模型中（作者列为未来方向）。
- 实验仅验证离线过滤行为克隆，未进行模拟器内在线策略学习或强化学习。
- 闭环和消融仅使用单一 VLA 策略家族（π_0.5），未在更广泛策略范围上验证。
- 论文未提及数据规模具体数值、训练总时长、批大小等工程细节。

## 工程启示

复现或采用 GE-Sim 2.0 时，优先核对以下环节：

1. **记忆帧增强参数**：外部激活概率 0.8、渐进噪声 σ_mem=0.2、首帧 σ_first=0.5、局部模糊核 [1,5]、颜色抖动概率 0.3——这些参数直接决定自回归 rollout 的分布偏移控制效果，建议严格按论文设置，不要随意调整。

2. **DMD2 蒸馏的 sigma 调度**：固定 [1.0, 0.9375, 0.8333, 0.625] 集中在高噪声区，生成器更新 shift=3、critic 更新 shift=5，critic 损失权重 1/σ²——这些细节对蒸馏稳定性至关重要，偏离可能导致学生模型崩溃。

3. **历史状态增强的验证时禁用**：delta 索引偏移和时间重采样仅在训练时启用，验证时必须关闭，否则会污染评估结果。

4. **最易踩坑处**：状态专家训练时视觉专家必须完全冻结；EE 位姿图渲染器在训练和推理时必须保持一致（论文明确强调）；世界裁判每帧单独输入、不沿时间轴平均，且文本条件用当前块匹配的子任务标题而非完整任务指令。

5. **硬件门槛**：单 H100 生成 100 帧 rollout 约 2.3 秒（4 步），这是加速后的结果；未蒸馏的教师模型推理成本会高一个数量级，规划算力时需预留蒸馏训练开销。

## 参考
- https://arxiv.org/abs/2605.27491
- https://github.com/ImChong/Robotics_Notebooks

## Overview

GE-Sim 2.0 is a closed-loop video world simulator proposed by the NVIDIA team. Built on a 2B-parameter action-conditioned video generation backbone, it adds three major modules: a proprioceptive state expert, a world judge, and DMD2 distillation acceleration, advancing video generation from "vision-only prediction" to a "closed-loop evaluable and learnable" full simulator. The core contribution lies in integrating state prediction, reward evaluation, and efficient inference into a single framework for the first time, topping the WorldArena leaderboard with closed-loop success rates and real-robot consistency within 1 pp.

## What It Changes

Robot learning is shifting from "algorithm-driven" to "data-driven," but evaluation remains a bottleneck: real-robot benchmarking is slow and hard to reproduce, while classical physics simulators (MuJoCo, Isaac Gym, etc.) have fundamental shortcomings in contact dynamics, deformable objects, and visual realism. Generative video models offer a new path, but existing video world simulators (including the initial GE-Sim) have three fatal flaws: they only predict vision without outputting proprioceptive states; they only generate rollouts without providing verifiable success signals; and their inference throughput is far below the demands of large-scale parallel evaluation.

What GE-Sim 2.0 truly changes is the boundary of "what video world models can do"—it is no longer a passive video generator but an active closed-loop evaluator. By integrating proprioceptive state prediction, reward judgment, and accelerated inference into a single framework, it gives "training and evaluating policies in a simulator" a credibility close to the real world for the first time. This is not just an engineering improvement; it redefines the role of video world models in the robot learning pipeline: from a "visualization tool" to a "verifiable simulation environment."

## Method Breakdown

### Overall Architecture
It retains GE-Sim's action-conditioned video generation backbone (Cosmos-Predict2-2B-Video2World DiT), retrained on thousands of hours of real robot data, with three new parallel modules: a Vision Expert, a Proprioceptive State Expert, a World Judge, and a DMD2 distillation acceleration framework.

### Vision Expert
- Conditioning inputs: `z_cond = [z_noisy; R_ray; M_pose; m_cond]`, where `R_ray` is a 6-channel per-pixel ray map (origin + unit direction), `M_pose` is a 3-channel EE pose map, and `m_cond` is a 1-channel binary mask distinguishing memory frames from frames to predict.
- All inputs are normalized to [-1, 1] to avoid diffusion training scale mismatches.
- Training loss: flow matching objective `L_video = w(τ) ||(v_θ - (ε - l)) ⊙ (1 - M)||_2^2`, computed only on frames to predict.
- Memory frame augmentation: progressive noise mixing (σ_mem=0.2, first frame σ_first=0.5), local Gaussian blur (kernel size [1,5]), multi-view synchronized color jitter (probability 0.3), all gated by an external activation probability of 0.8.

### Proprioceptive State Expert
- A lightweight Transformer branch that fuses outputs from all L layers of the Vision Expert with learnable weights (`α_l` initialized to 1) followed by LayerNorm: `H_fuse = LayerNorm(Σ α_l h_l_video)`.
- State representation: 16-dimensional `s_t = [θ_t^L, g_t^L, θ_t^R, g_t^R]` (7 joint angles + gripper opening).
- Training loss: `L_proprio_state = E ||v_φ(s_τ, τ, H_fuse) - (ε - s_0)||_2^2`, with the Vision Expert frozen.
- Historical state augmentation: delta index offset (Δ~Uniform{−3,…,3}) and temporal resampling (n_prev→n_prev−1→n_prev low-pass interpolation), each applied independently with probability 0.5.

### World Judge
- A Robometer-based vision-language reward model with a frozen vision encoder, training only the language model and prediction head.
- Each frame is input individually with a dedicated per-frame token, and the hidden state `f_i` is taken as the frame representation.
- Only per-frame success prediction is trained (progress prediction is discarded), with the rationale: real trajectories contain error recovery behaviors, and progress labels are noisy and non-monotonic.
- Loss: class-balanced binary cross-entropy `L_judge = Σ m_i w_i BCE(σ(s_hat_i), y_i) / Σ m_i w_i`.

### DMD2 Distillation Acceleration
- Three-step distillation: frozen teacher (Vision Expert from the main training phase), few-step student (target 4 steps), and a trainable fake-score critic.
- Distribution matching gradient: `∇_DMD = (x₀^fake − x₀^teacher) / ‖x₀^student − x₀^teacher‖`.
- The student updates every 5 steps, and the critic updates the remaining 4 steps; the student uses a fixed sigma schedule [1.0, 0.9375, 0.8333, 0.625].
- Random step-size training: each chunk frame is sampled with random time steps, and inference can skip frames, covering up to 4× the time span.

## Key Innovations

1. **Joint modeling of state prediction and visual generation**: For the first time, a proprioceptive state decoder is integrated in parallel with the video generator, with cross-layer visual context fusion via learnable weights `α_l`. This solves the fundamental problem in pure visual simulators where "commanding actions as noise proxies drifts," making state accumulation errors controllable in closed-loop rollouts.

2. **The World Judge's "sparse success signal" design**: Discarding progress prediction and using only per-frame success binary classification reflects a deep insight into real robot data—trajectories commonly contain detours, retries, and temporary regressions, making progress labels non-monotonic and noisy. This design achieves 79% accuracy on WM rollouts, 19 pp higher than Qwen3.5 with 122B parameters.

3. **Combined DMD2 distillation + random step-size training acceleration**: Compresses 100-frame rollouts from minutes to 2.3 seconds (single H100, 4 steps), while random step-size training supports 4× frame skipping at inference. This makes large-scale parallel evaluation feasible for the first time and is the key step in moving the "closed-loop simulator" from concept to engineering practicality.

## Experiments and Results

### WorldArena Leaderboard
GE-Sim 2.0 tops the public leaderboard with 2B parameters, surpassing dedicated robot world models (Ctrl-World, DreamDojo, GigaWorld, ABot) and closed-source general video generators (Sora, Veo).

### Per-Task Replay Metrics (Table 2, head view)
| Metric | Ctrl-World | DreamDojo | GE-Sim 2.0 |
|--------|-----------|-----------|------------|
| PSNR | 19.09 | 17.38 | **23.05** |
| SSIM | 0.786 | 0.728 | **0.846** |
| LPIPS | 0.296 | 0.353 | **0.145** |
| FID | 62.70 | 79.82 | **32.28** |
| FVD | 1083.7 | 1155.0 | **481.3** |

### Long-Horizon Temporal Robustness (Figure 6)
GE-Sim 2.0 decays from 24.84 dB to 21.08 dB PSNR over 50-second replays (fluctuation <4 dB); Ctrl-World and DreamDojo drop sharply to 16.16 and 15.44 dB after the first segment.

### World Judge Reward Quality (Table 3, average)
| Mode | GE-Sim 2.0 Accuracy | Qwen3.5-122B Accuracy | Event Distance (frames) |
|------|---------------------|------------------------|--------------------------|
| WM rollout | **79%** | 60% | 28.2 vs. 57.8 |
| GT video | **87%** | 58% | 15.7 vs. 64.7 |

### Closed-Loop Consistency (Figures 8, 10)
- With state conditioning, the fitted trend slope is close to 1 with a small negative offset; removing state conditioning yields a shallower trend with a positive offset.
- Segment-level accuracy: with state 0.81 / without 0.74 / Ctrl-World 0.63; recall: 0.82 / 0.67 / 0.25.

### WM-Filtered Behavior Cloning (Figure 9)
Real-robot success rates: Pour water 0.40→0.55, Fold towels 0.40→0.50, Pull out plug 0.45→0.65; average 0.417→0.567 (absolute gain 0.150).

### Ablations
- Removing the state expert: segment-level accuracy 0.81→0.74, recall 0.82→0.67.
- Replacing the World Judge with Qwen: WM-mode accuracy drops 19 pp, event distance increases by 30 frames.

## Boundaries and Limitations

- Contact-rich tasks (Pull out plug, Command grasp & release) still exhibit false positives and false negatives; fine-grained contact states and long-horizon state accumulation remain challenges.
- Detecting small-scale visual signals (liquid level, flame transfer) in Pour water and Borrow flame remains an open problem.
- The base model is trained only on fixed-embodiment dual-arm teleoperation and robot replay data, not extended to cross-embodiment, ego-view, or UMI-style data.
- The World Judge is an external module and is not unified into a single world model (listed by the authors as future work).
- Experiments only validate offline filtered behavior cloning, not in-simulator online policy learning or reinforcement learning.
- Closed-loop and ablation studies use only a single VLA policy family (π_0.5), not validated across a broader policy range.
- The paper does not mention specific data scale values, total training duration, batch size, or other engineering details.

## Engineering Insights

When reproducing or adopting GE-Sim 2.0, prioritize checking the following:

1. **Memory frame augmentation parameters**: external activation probability 0.8, progressive noise σ_mem=0.2, first frame σ_first=0.5, local blur kernel [1,5], color jitter probability 0.3—these parameters directly determine distribution shift control in autoregressive rollouts. It is recommended to follow the paper strictly and not adjust them arbitrarily.

2. **DMD2 distillation sigma schedule**: fixed [1.0, 0.9375, 0.8333, 0.625] concentrated in the high-noise region, generator update shift=3, critic update shift=5, critic loss weight 1/σ²—these details are critical for distillation stability, and deviations may cause student model collapse.

3. **Disable historical state augmentation during validation**: delta index offset and temporal resampling are enabled only during training and must be turned off during validation; otherwise, evaluation results will be contaminated.

4. **Most common pitfalls**: the Vision Expert must be fully frozen during state expert training; the EE pose map renderer must remain consistent between training and inference (explicitly emphasized in the paper); the World Judge takes each frame individually without temporal averaging, and the text condition uses the subtask title matched to the current chunk rather than the full task instruction.

5. **Hardware threshold**: generating a 100-frame rollout on a single H100 takes about 2.3 seconds (4 steps), which is the accelerated result; the undistilled teacher model's inference cost is an order of magnitude higher, so plan compute resources with distillation training overhead in mind.

## 개요

GE-Sim 2.0은 NVIDIA 팀이 제안한 폐루프 비디오 세계 시뮬레이터로, 2B 파라미터의 동작 조건부 비디오 생성 백본에 고유수용감각 상태 전문가, 세계 심판, DMD2 증류 가속의 세 가지 모듈을 추가하여 비디오 생성을 "시각적 예측만 하는 것"에서 "폐루프 평가와 학습이 가능한 완전한 시뮬레이터"로 발전시켰습니다. 핵심 기여는 상태 예측, 보상 평가, 고효율 추론을 단일 프레임워크에 처음으로 통합한 것이며, WorldArena 순위표에서 1위를 차지했고 폐루프 성공률과 실제 로봇 간의 일관성은 1 pp 이내입니다.

## 그것이 바꾼 것

로봇 학습은 "알고리즘 중심"에서 "데이터 중심"으로 전환되고 있지만, 평가 단계는 여전히 병목입니다. 실제 로봇 벤치마크 테스트는 느리고 재현이 어려우며, 고전 물리 시뮬레이터(MuJoCo, Isaac Gym 등)는 접촉 역학, 변형 가능한 물체, 시각적 사실성에서 근본적인 한계가 있습니다. 생성형 비디오 모델은 새로운 경로를 제공하지만, 기존 비디오 세계 시뮬레이터(GE-Sim 초기 버전 포함)에는 세 가지 치명적인 결함이 있습니다. 시각만 예측하고 고유수용감각 상태를 출력하지 않으며, rollout만 생성하고 검증 가능한 성공 신호를 제공하지 않으며, 추론 처리량이 대규모 병렬 평가 요구보다 훨씬 낮습니다.

GE-Sim 2.0이 진정으로 바꾼 것은 "비디오 세계 모델이 할 수 있는 것"의 경계입니다. 더 이상 수동적인 비디오 생성기가 아니라 능동적인 폐루프 평가기입니다. 고유수용감각 상태 예측, 판단 보상, 가속 추론을 단일 프레임워크에 통합함으로써 "시뮬레이터에서 정책을 훈련하고 평가하는 것"이 처음으로 실제 세계에 가까운 신뢰성을 갖게 되었습니다. 이는 단순한 엔지니어링 개선이 아니라 비디오 세계 모델의 로봇 학습 파이프라인 내 역할을 "시각화 도구"에서 "검증 가능한 시뮬레이션 환경"으로 재정의한 것입니다.

## 방법 분석

### 전체 아키텍처
GE-Sim의 동작 조건부 비디오 생성 백본(Cosmos-Predict2-2B-Video2World DiT)을 유지하고 수천 시간의 실제 로봇 데이터로 재훈련했으며, 세 가지 병렬 모듈을 추가했습니다. 시각 전문가(Visual Expert), 고유수용감각 상태 전문가(Proprioceptive State Expert), 세계 심판(World Judge), 그리고 DMD2 증류 가속 프레임워크입니다.

### 시각 전문가
- 조건 입력: `z_cond = [z_noisy; R_ray; M_pose; m_cond]`, 여기서 `R_ray`는 6채널 픽셀별 레이 맵(원점+단위 방향), `M_pose`는 3채널 EE 포즈 맵, `m_cond`는 메모리 프레임과 예측 대상 프레임을 구분하는 1채널 이진 마스크입니다.
- 모든 입력은 [-1,1]로 정규화되어 확산 훈련 스케일 불일치를 방지합니다.
- 훈련 손실: 흐름 매칭 목표 `L_video = w(τ) ||(v_θ - (ε - l)) ⊙ (1 - M)||_2^2`, 예측 대상 프레임에서만 계산됩니다.
- 메모리 프레임 강화: 점진적 노이즈 혼합(σ_mem=0.2, 첫 프레임 σ_first=0.5), 국소 가우시안 블러(커널 크기 [1,5]), 다중 시점 동기 색상 지터(확률 0.3), 모두 외부 활성화 확률 0.8로 게이트됩니다.

### 고유수용감각 상태 전문가
- 경량 Transformer 분기로, 시각 전문가의 모든 L 레이어 출력을 가중 융합(학습 가능한 스칼라 `α_l` 초기값 1)한 후 LayerNorm을 적용합니다. `H_fuse = LayerNorm(Σ α_l h_l_video)`.
- 상태 표현: 16차원 `s_t = [θ_t^L, g_t^L, θ_t^R, g_t^R]`(7차원 관절 각도 + 그리퍼 개폐도).
- 훈련 손실: `L_proprio_state = E ||v_φ(s_τ, τ, H_fuse) - (ε - s_0)||_2^2`, 시각 전문가는 동결됩니다.
- 과거 상태 강화: 델타 인덱스 오프셋(Δ~Uniform{−3,…,3}) 및 시간 리샘플링(n_prev→n_prev−1→n_prev 저역 통과 보간), 각각 확률 0.5로 독립 적용됩니다.

### 세계 심판
- Robometer 기반의 시각-언어 보상 모델로, 시각 인코더는 동결하고 언어 모델과 예측 헤드만 훈련합니다.
- 각 프레임을 개별 입력으로 처리하고 전용 프레임별 토큰을 추가하며, 숨겨진 상태 `f_i`를 프레임 표현으로 사용합니다.
- 프레임별 성공 예측 목표만 훈련합니다(진행도 예측 포기). 이유: 실제 궤적에는 오류 복구 동작이 포함되어 있어 진행도 레이블이 노이즈가 많고 비단조적입니다.
- 손실: 클래스 균형 이진 교차 엔트로피 `L_judge = Σ m_i w_i BCE(σ(s_hat_i), y_i) / Σ m_i w_i`.

### DMD2 증류 가속
- 3단계 증류: 동결된 교사(주 훈련 단계의 시각 전문가), 소수 스텝 학생(목표 4스텝), 훈련 가능한 fake-score critic.
- 분포 정합 기울기: `∇_DMD = (x₀^fake − x₀^teacher) / ‖x₀^student − x₀^teacher‖`.
- 학생은 5스텝마다 업데이트되고 critic은 나머지 4스텝에서 업데이트됩니다. 학생의 고정 sigma 스케줄은 [1.0, 0.9375, 0.8333, 0.625]입니다.
- 무작위 스텝 크기 훈련: 각 chunk 프레임을 무작위 시간 스텝 크기로 샘플링하고, 추론 시 프레임을 건너뛸 수 있어 최대 4배 시간 범위를 커버합니다.

## 핵심 혁신

1. **상태 예측과 시각 생성의 결합 모델링**: 고유수용감각 상태 디코더를 비디오 생성기와 병렬로 처음 통합했으며, 학습 가능한 가중치 `α_l`을 통해 레이어 간 시각 컨텍스트 융합을 구현합니다. 이는 순수 시각 시뮬레이터에서 "명령 동작이 노이즈 대리자로 사용되어 드리프트하는" 근본적인 문제를 해결하여 폐루프 rollout에서 상태 누적 오차를 제어 가능하게 만듭니다.

2. **세계 심판의 "희소 성공 신호" 설계**: 진행도 예측을 포기하고 프레임별 성공 이진 분류만 사용하는 것은 실제 로봇 데이터 특성에 대한 깊은 통찰입니다. 궤적에는 우회, 재시도, 일시적 후퇴가 일반적이며 진행도 레이블은 비단조적이고 노이즈가 있습니다. 이 설계로 WM rollout에서 보상 신호 정확도가 79%에 달하며, 122B 파라미터의 Qwen3.5보다 19 pp 높습니다.

3. **DMD2 증류 + 무작위 스텝 크기 훈련의 결합 가속**: 100프레임 rollout을 분 단위에서 2.3초(단일 H100, 4스텝)로 압축했으며, 무작위 스텝 크기 훈련은 추론 시 4배 프레임 건너뛰기를 지원합니다. 이는 대규모 병렬 평가를 처음으로 가능하게 만든 "폐루프 시뮬레이터"가 개념에서 엔지니어링 실용으로 전환되는 핵심 단계입니다.

## 실험 및 결과

### WorldArena 순위표
GE-Sim 2.0은 2B 파라미터로 공개 순위표에서 1위를 차지하며 전용 로봇 세계 모델(Ctrl-World, DreamDojo, GigaWorld, ABot)과 폐쇄형 범용 비디오 생성기(Sora, Veo)를 능가했습니다.

### 작업별 재생 지표(표 2, 헤드 뷰)
| 지표 | Ctrl-World | DreamDojo | GE-Sim 2.0 |
|------|-----------|-----------|------------|
| PSNR | 19.09 | 17.38 | **23.05** |
| SSIM | 0.786 | 0.728 | **0.846** |
| LPIPS | 0.296 | 0.353 | **0.145** |
| FID | 62.70 | 79.82 | **32.28** |
| FVD | 1083.7 | 1155.0 | **481.3** |

### 장기 시간 강건성(그림 6)
GE-Sim 2.0은 50초 재생에서 PSNR이 24.84 dB에서 21.08 dB로 감소했습니다(변동 <4 dB). Ctrl-World와 DreamDojo는 첫 구간 이후 16.16 및 15.44 dB로 급격히 하락했습니다.

### 세계 심판 보상 품질(표 3, 평균)
| 모드 | GE-Sim 2.0 정확도 | Qwen3.5-122B 정확도 | 이벤트 거리(프레임) |
|------|------------------|---------------------|----------------|
| WM rollout | **79%** | 60% | 28.2 vs. 57.8 |
| GT 비디오 | **87%** | 58% | 15.7 vs. 64.7 |

### 폐루프 일관성(그림 8, 10)
- 상태 조건 포함 시 피팅 추세 기울기가 1에 가깝고 작은 음의 오프셋이 있으며, 상태 조건 제거 시 추세가 더 얕아지고 양의 오프셋이 발생합니다.
- 세그먼트 수준 정확도: 상태 포함 0.81 / 미포함 0.74 / Ctrl-World 0.63; 재현율: 0.82 / 0.67 / 0.25.

### WM 필터링 행동 클로닝(그림 9)
실제 로봇 성공률: Pour water 0.40→0.55, Fold towels 0.40→0.50, Pull out plug 0.45→0.65; 평균 0.417→0.567(절대 증가 0.150).

### 절제
- 상태 전문가 제거: 세그먼트 수준 정확도 0.81→0.74, 재현율 0.82→0.67.
- 세계 심판을 Qwen으로 교체: WM 모드 정확도 19 pp 하락, 이벤트 거리 30프레임 증가.

## 경계 및 한계

- 접촉이 많은 작업(Pull out plug, Command grasp & release)에서는 여전히 거짓 양성 및 거짓 음성이 존재하며, 세밀한 접촉 상태와 장기 상태 누적은 여전히 과제입니다.
- Pour water 및 Borrow flame에서 소규모 시각 신호(액체 레벨, 화염 전달) 감지는 여전히 미해결 문제입니다.
- 기본 모델은 고정된 체형의 이팔 원격 조작 및 로봇 재생 데이터에서만 훈련되었으며, 체형 간, ego-view, UMI 스타일 데이터로 확장되지 않았습니다.
- 세계 심판은 외부 모듈로, 단일 세계 모델로 통합되지 않았습니다(저자가 향후 방향으로 명시).
- 실험은 오프라인 필터링 행동 클로닝만 검증했으며, 시뮬레이터 내 온라인 정책 학습이나 강화 학습은 수행하지 않았습니다.
- 폐루프 및 절제는 단일 VLA 정책 계열(π_0.5)만 사용했으며, 더 넓은 정책 범위에서 검증되지 않았습니다.
- 논문은 데이터 규모의 구체적 수치, 총 훈련 시간, 배치 크기 등의 엔지니어링 세부 사항을 언급하지 않았습니다.

## 엔지니어링 시사점

GE-Sim 2.0을 재현하거나 채택할 때 다음 항목을 우선적으로 확인하십시오.

1. **메모리 프레임 강화 파라미터**: 외부 활성화 확률 0.8, 점진적 노이즈 σ_mem=0.2, 첫 프레임 σ_first=0.5, 국소 블러 커널 [1,5], 색상 지터 확률 0.3 — 이러한 파라미터는 자기회귀 rollout의 분포 오프셋 제어 효과를 직접 결정하므로 논문 설정을 엄격히 따르고 임의로 조정하지 마십시오.

2. **DMD2 증류의 sigma 스케줄**: 고정 [1.0, 0.9375, 0.8333, 0.625]는 고노이즈 영역에 집중되어 있으며, 생성기 업데이트 shift=3, critic 업데이트 shift=5, critic 손실 가중치 1/σ² — 이러한 세부 사항은 증류 안정성에 중요하며, 이탈 시 학생 모델 붕괴로 이어질 수 있습니다.

3. **과거 상태 강화의 검증 시 비활성화**: 델타 인덱스 오프셋과 시간 리샘플링은 훈련 시에만 활성화되며, 검증 시 반드시 꺼야 합니다. 그렇지 않으면 평가 결과가 오염됩니다.

4. **가장 함정에 빠지기 쉬운 부분**: 상태 전문가 훈련 시 시각 전문가는 완전히 동결해야 합니다. EE 포즈 맵 렌더러는 훈련과 추론에서 일관성을 유지해야 합니다(논문에서 명시적으로 강조). 세계 심판은 각 프레임을 개별 입력으로 처리하고 시간 축을 따라 평균하지 않으며, 텍스트 조건은 전체 작업 지시가 아닌 현재 청크와 일치하는 하위 작업 제목을 사용합니다.

5. **하드웨어 요구 사항**: 단일 H100에서 100프레임 rollout 생성은 약 2.3초(4스텝)이며, 이는 가속 후 결과입니다. 증류되지 않은 교사 모델의 추론 비용은 한 자릿수 더 높으므로, 연산 계획 시 증류 훈련 비용을 예비로 확보해야 합니다.
