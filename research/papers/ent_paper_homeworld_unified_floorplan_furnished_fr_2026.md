---
$id: ent_paper_homeworld_unified_floorplan_furnished_fr_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HomeWorld: A Unified Floorplan-to-Furnished Framework for Generating Controllable, Densely Interactive Whole-Home Scenes'
  zh: 'HomeWorld: A Unified Floorplan-to-Furnished Framework for Generating Controllable, Densely Interactive Whole-Home Scenes'
  ko: 'HomeWorld: A Unified Floorplan-to-Furnished Framework for Generating Controllable, Densely Interactive Whole-Home Scenes'
summary:
  en: 'Indoor scene generation is crucial for robot simulation and modern interior design. However, complex layouts together
    with scarce 3D scene data make learning-based generation challenging. Institutions per source list: Ace Robotics、CUHK
    MMLab、Shenzhen Loop Area Institute（*Equal contribution、†Project lead）.'
  zh: HomeWorld 是一个由研究团队提出的统一分层框架，用于生成可控且密集交互的全屋场景。其核心贡献包括：构建了包含 30 万真实住宅平面图的大规模数据集，并利用大语言模型实现细粒度平面图生成；结合图像生成模型与 VLM 优化器，实现从家具布局到小物体放置的完整室内场景合成，最终输出可直接用于具身
    AI 模拟的 3D 场景。
  ko: 'Indoor scene generation is crucial for robot simulation and modern interior design. However, complex layouts together
    with scarce 3D scene data make learning-based generation challenging. Institutions per source list: Ace Robotics、CUHK
    MMLab、Shenzhen Loop Area Institute（*Equal contribution、†Project lead）.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- homeworld
- unified
- floorplan
- furnished
- fr
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 383 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2606.06390 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2606.06390v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.06390 HomeWorld: A Unified Floorplan-to-Furnished Framework for Generating Controllable, Densely Interactive
    Whole-Home Scenes'
  url: https://arxiv.org/abs/2606.06390
  accessed_at: '2026-07-31'
  date: '2026-06-04'
- id: src_002
  type: website
  title: Project page
  url: https://kairos-homeworld.github.io/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

HomeWorld 是一个从自然语言提示生成可控、密集交互整屋场景的统一分层框架，由研究团队提出。其核心贡献在于将整屋生成分解为平面图合成、分层漫游布置、递归细化和可操作物体放置四个阶段，结合大规模真实平面图数据训练的 LLM 与显式 3D 锚定，解决了现有方法在全局连贯性、物理可行性和仿真就绪性上的不足。

## 它改变了什么

整屋场景生成长期被割裂为平面图合成、单房间布局、物体摆放等孤立子任务，导致生成的场景缺乏全局拓扑一致性和跨房间的语义连贯性。现有方法要么依赖手工规则（如 ProcTHOR）产生重复且有偏的布局，要么直接进行 3D 学习但受限于训练数据稀缺和计算成本，要么采用 2D 提升（如 Text2Room）但缺乏强 3D 先验锚定，在扩展到整屋时产生几何碎片化。HomeWorld 真正改变的是将"生成"重新定义为"可控的层次化实例化"过程：先用 LLM 在结构化表示上合成拓扑正确的平面图，再通过显式 3D 外壳约束下的 2D 修复逐步填充家具，最后用 VLM 递归校正物理违规。这一范式将数据稀缺的 3D 生成问题转化为数据相对充足的 2D 生成与 3D 验证问题，使得生成结果在拓扑有效性、物体密度和物理合理性上同时得到提升。

## 方法拆解

### 阶段 1：平面图生成（LLM + K-D 树）
- 数据构建：从在线房地产仓库收集 108.4 万张平面图图像，用 2K 张手动标注图像训练检测模型识别门、窗、墙和尺寸链，检测框转中心线并合并邻近端点重建矢量化结构；OCR 提取房间标签和尺寸，经质量过滤后得到 31.4 万张验证平面图。
- 表示设计：将平面图编码为二叉 K-D 树，内部节点存储分割轴和坐标，叶节点对应空间单元并分配房间语义；门、窗等附件在 JSON 中单独表示。相比直接预测多边形坐标，K-D 树输出空间更结构化，确定性避免多边形重叠和空洞。
- 训练：使用 Qwen3-4B-Instruct，LoRA 监督微调，数据划分 281K/15K/15K。

### 阶段 2：分层漫游布置（2D 提升 + 3D 锚定）
- 外壳实例化：在 Blender 中创建空的无家具外壳，带基本光照和材质，作为漫游的显式 3D 约束。
- 全局布局（俯视图）：渲染俯视图，标注门、窗及门相邻房间，用图像修复模型（Gemini 3.0 Image Pro Preview）合成带家具俯视图，SAM-3 提取实例掩码和 2D 框，VLM 进行开放词汇类别识别。
- 细节细化（自我中心视图）：基于热图的视点选择策略，将地板离散化为网格，贪心选择覆盖最少观测区域的视点；对每个视点渲染自我中心图像并修复合成次要物体，SAM-3D 重建 3D 几何，几何对齐模块集成到房间。
- 漫游顺序：先俯视图放置大型功能家具（床、沙发），再自我中心视图逐步丰富小物品。

### 阶段 3：递归布局细化（VLM 顺序决策）
- 将细化表述为顺序决策过程：VLM 细化器观察当前布局状态，预测结构化纠正动作（平移、旋转或组合变换），动作确定性应用于 3D 边界框，重新渲染检查，循环直到通过验证或达到迭代限制。
- 微调数据构建：损坏布局构造（注入边界违规、碰撞、门道阻塞等扰动）、Oracle 标记修复动作（按残余误差、新引入违规、移动成本和语义合理性评分选真值）、模型在环样本（初始细化器滚动，中间状态由 oracle 重新标记）。

### 阶段 4：可操作物体放置
- 根据语义类别和几何属性（水平表面存在性和足够可用面积）识别可支撑小物体的家具。
- 为每个家具构建局部表面画布，用修复模型合成表面级排列。
- 使用 PhysX-Anything 推断物理属性（密度、杨氏模量、泊松比），密度和网格体积估计质量。
- VLM 将合成表面图像解析为可操作物体集合（语义类别、尺度、相对位置和方向），变换到全局坐标系。
- 插入前进行轻量物理过滤：移除超出支撑边界、严重碰撞、漂浮或违反类别级支撑约束的物体，保留显式支撑关系。

## 关键创新

1. **K-D 树平面图表示**：将平面图生成从自由多边形预测转化为结构化递归分割预测，从根本上消除了重叠、空洞和不连通等拓扑错误。这是对输出空间的重构而非简单约束，使得 LLM 生成在结构上天然合法，且可确定性转换回传统表示。
2. **分层漫游（Hierarchical Roaming）**：先俯视图全局布局再自我中心视图细节细化，配合显式 3D 外壳约束，解决了纯 2D 提升方法在整屋尺度上的几何不一致问题。这一设计将 2D 生成先验与 3D 锚定有效结合，避免了直接 3D 学习的数据瓶颈。
3. **VLM 递归细化器**：将布局校正建模为顺序决策过程，通过损坏布局构造 + Oracle 标记 + 模型在环的三阶段数据构建，训练 VLM 输出可执行的 3D 边界框编辑动作。这使得细化过程可验证、可迭代，而非一次性生成。

## 实验与结果

### 平面图生成（表 2）
| 指标 | ProcTHOR-10K | Ours |
|------|-------------|------|
| 图有效性 | 72.6% | 87.7% |
| 类型图多样性 | 6.2 | 22.4 |
| 非类型图多样性 | 2.3 | 6.9 |

### 布局生成（表 4，4 个房间类别每类 25 样本）
| 方法 | CR | OOB | VD | FOD |
|------|-----|-----|-----|-----|
| LayoutGPT | 0.05 | 0.01 | 0.22 | 1.99 |
| Holodeck | 0.07 | 0.02 | 0.22 | 2.10 |
| LayoutVLM | 0.20 | 0.01 | 0.27 | 2.15 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

### 用户研究（表 5，30 参与者，10 样本/方法）
| 方法 | Reasonability | Aesthetics | Complexity | Average |
|------|--------------|------------|------------|---------|
| LayoutGPT | 0.202 | 0.184 | 0.153 | 0.180 |
| Holodeck | 0.395 | 0.412 | 0.452 | 0.420 |
| LayoutVLM | 0.252 | 0.260 | 0.283 | 0.265 |
| Ours | 0.807 | 0.827 | 0.797 | 0.811 |

### 消融（表 6）
| 变体 | CR | OOB | VD | FOD |
|------|-----|-----|-----|-----|
| w/o Top-down Roaming | 0.09 | 0.02 | 0.22 | 2.6 |
| w/o Ego-centric Roaming | 0.07 | 0.02 | 0.20 | 3.44 |
| w/o VLM-LLM Refiner | 0.20 | 0.05 | 0.33 | 3.02 |
| w/o Manipulable Placement | 0.05 | 0.01 | 0.33 | 1.82 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

关键含义：Ours 在 FOD（足迹物体密度）上显著领先（4.16 vs 基线最高 2.15），说明场景更密集；消融显示 VLM-LLM Refiner 对 CR 影响最大（移除后 CR 从 0.05 升至 0.20），验证了递归校正在物理合理性中的核心作用。

## 边界与局限

论文未明确讨论局限性，但从事实要点可推断：外壳纹理生成采用简单策略（复用 3D-FRONT 资产或 2D 生成模型合成），未引入专用材质管线；光照采用规则生成而非学习式方法。未提及对生成场景的物理仿真验证结果、与真实世界场景的泛化性测试、生成时间/计算成本的具体数字、用户研究的详细统计结果。未提及 VLM 细化器的迭代限制具体数值、视点选择的具体参数（网格分辨率、视场角、安全距离）。未提及可操作物体数量的具体统计（除表 1 中 >15 外）。此外，LayoutGPT 仅支持 living room 和 bedroom，因此在 kitchen 和 bathroom 比较中未评估，可能影响基线对比的完整性。

## 工程启示

复现或采用 HomeWorld 时，优先核对以下环节：**K-D 树表示的正确实现**是平面图生成质量的基石，消融显示 canonical representation 会导致 9.2% 重叠、2.0% 空洞和 16.2% 不连通，而 K-D 树将不连通降至 12.3%，但仍非零，需关注叶节点语义分配的约束。**VLM 细化器的数据构建**是最易踩坑处——损坏布局注入的扰动类型和幅度直接影响 oracle 标记质量，建议严格按论文的边界违规、碰撞、门道阻塞、旋转、尺度、类别级错误六类扰动构造，并保留模型在环样本以覆盖中间状态。**分层漫游的视点选择**对最终物体密度影响显著（移除后 FOD 从 4.16 降至 2.6），热图覆盖策略的网格分辨率需根据房间尺度调参。**可操作物体放置**依赖 PhysX-Anything 的物理属性推断，建议先验证密度和网格体积估计的准确性，否则质量估计偏差会导致物理过滤误删。最后，数据集发布（300K 平面图 + 5K 场景）是复现的关键依赖，若无法获取，可先用 31.4 万验证平面图子集训练 LLM，但需注意分布差异。

## 参考
- https://arxiv.org/abs/2606.06390
- https://kairos-homeworld.github.io/
- https://github.com/ImChong/Robotics_Notebooks

## Overview

HomeWorld is a unified hierarchical framework for generating controllable, dense, interactive whole-house scenes from natural language prompts, proposed by the research team. Its core contribution lies in decomposing whole-house generation into four stages—floor plan synthesis, hierarchical roaming arrangement, recursive refinement, and manipulable object placement—combining LLMs trained on large-scale real floor plan data with explicit 3D anchoring to address shortcomings in existing methods regarding global coherence, physical feasibility, and simulation readiness.

## What It Changes

Whole-house scene generation has long been fragmented into isolated subtasks such as floor plan synthesis, single-room layout, and object placement, resulting in generated scenes lacking global topological consistency and cross-room semantic coherence. Existing methods either rely on handcrafted rules (e.g., ProcTHOR) producing repetitive and biased layouts, or directly learn in 3D but are limited by scarce training data and computational costs, or employ 2D lifting (e.g., Text2Room) but lack strong 3D prior anchoring, leading to geometric fragmentation when scaling to whole houses. What HomeWorld truly changes is redefining "generation" as a "controllable hierarchical instantiation" process: first, an LLM synthesizes topologically correct floor plans on structured representations; then, furniture is progressively filled via 2D inpainting under explicit 3D shell constraints; finally, a VLM recursively corrects physical violations. This paradigm transforms the data-scarce 3D generation problem into a relatively data-sufficient 2D generation and 3D verification problem, simultaneously improving topological validity, object density, and physical plausibility of generated results.

## Method Breakdown

### Stage 1: Floor Plan Generation (LLM + K-D Tree)
- Data construction: Collected 1.084 million floor plan images from online real estate repositories, trained a detection model on 2K manually annotated images to identify doors, windows, walls, and dimension chains; detection boxes were converted to centerlines and merged with neighboring endpoints to reconstruct vectorized structures; OCR extracted room labels and dimensions, yielding 314K validated floor plans after quality filtering.
- Representation design: Encoded floor plans as binary K-D trees, with internal nodes storing split axes and coordinates, leaf nodes corresponding to spatial units with assigned room semantics; attachments such as doors and windows are separately represented in JSON. Compared to directly predicting polygon coordinates, the K-D tree output space is more structured, deterministically avoiding polygon overlaps and holes.
- Training: Used Qwen3-4B-Instruct with LoRA supervised fine-tuning, with data split of 281K/15K/15K.

### Stage 2: Hierarchical Roaming Arrangement (2D Lifting + 3D Anchoring)
- Shell instantiation: Created empty unfurnished shells in Blender with basic lighting and materials, serving as explicit 3D constraints for roaming.
- Global layout (top-down view): Rendered top-down views, annotated doors, windows, and rooms adjacent to doors, used an image inpainting model (Gemini 3.0 Image Pro Preview) to synthesize furnished top-down views, SAM-3 extracted instance masks and 2D boxes, and a VLM performed open-vocabulary category recognition.
- Detail refinement (egocentric view): A heatmap-based viewpoint selection strategy discretized the floor into a grid, greedily selecting viewpoints covering the least observed regions; for each viewpoint, egocentric images were rendered and inpainted to synthesize secondary objects, SAM-3D reconstructed 3D geometry, and a geometry alignment module integrated objects into the room.
- Roaming order: Large functional furniture (beds, sofas) placed first via top-down views, then small items progressively enriched via egocentric views.

### Stage 3: Recursive Layout Refinement (VLM Sequential Decision)
- Formulated refinement as a sequential decision process: a VLM refiner observes the current layout state and predicts structured corrective actions (translation, rotation, or combined transformations), actions are deterministically applied to 3D bounding boxes, re-rendered for verification, and the loop continues until validation passes or iteration limits are reached.
- Fine-tuning data construction: Corrupted layout construction (injecting perturbations such as boundary violations, collisions, doorway blockages), oracle-labeled repair actions (scoring ground truth by residual error, newly introduced violations, movement cost, and semantic plausibility), and model-in-the-loop samples (initial refiner rollouts with intermediate states re-labeled by the oracle).

### Stage 4: Manipulable Object Placement
- Identified furniture capable of supporting small objects based on semantic categories and geometric properties (presence of horizontal surfaces and sufficient usable area).
- Constructed local surface canvases for each furniture piece, using inpainting models to synthesize surface-level arrangements.
- Used PhysX-Anything to infer physical properties (density, Young's modulus, Poisson's ratio), estimating mass from density and mesh volume.
- A VLM parsed synthesized surface images into manipulable object sets (semantic categories, scales, relative positions, and orientations), transformed into the global coordinate system.
- Applied lightweight physical filtering before insertion: removed objects exceeding support boundaries, severely colliding, floating, or violating category-level support constraints, preserving explicit support relationships.

## Key Innovations

1. **K-D Tree Floor Plan Representation**: Transformed floor plan generation from free polygon prediction into structured recursive split prediction, fundamentally eliminating topological errors such as overlaps, holes, and disconnections. This is a reconstruction of the output space rather than a simple constraint, making LLM generation structurally valid by design and deterministically convertible back to traditional representations.
2. **Hierarchical Roaming**: Combining top-down global layout first with egocentric view detail refinement, coupled with explicit 3D shell constraints, resolves geometric inconsistencies of pure 2D lifting methods at whole-house scales. This design effectively integrates 2D generation priors with 3D anchoring, avoiding the data bottleneck of direct 3D learning.
3. **VLM Recursive Refiner**: Models layout correction as a sequential decision process, training a VLM to output executable 3D bounding box editing actions through a three-stage data construction of corrupted layout construction + oracle labeling + model-in-the-loop. This makes the refinement process verifiable and iterative, rather than one-shot generation.

## Experiments and Results

### Floor Plan Generation (Table 2)
| Metric | ProcTHOR-10K | Ours |
|--------|-------------|------|
| Graph validity | 72.6% | 87.7% |
| Typed graph diversity | 6.2 | 22.4 |
| Untyped graph diversity | 2.3 | 6.9 |

### Layout Generation (Table 4, 25 samples per 4 room categories)
| Method | CR | OOB | VD | FOD |
|--------|-----|-----|-----|-----|
| LayoutGPT | 0.05 | 0.01 | 0.22 | 1.99 |
| Holodeck | 0.07 | 0.02 | 0.22 | 2.10 |
| LayoutVLM | 0.20 | 0.01 | 0.27 | 2.15 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

### User Study (Table 5, 30 participants, 10 samples/method)
| Method | Reasonability | Aesthetics | Complexity | Average |
|--------|--------------|------------|------------|---------|
| LayoutGPT | 0.202 | 0.184 | 0.153 | 0.180 |
| Holodeck | 0.395 | 0.412 | 0.452 | 0.420 |
| LayoutVLM | 0.252 | 0.260 | 0.283 | 0.265 |
| Ours | 0.807 | 0.827 | 0.797 | 0.811 |

### Ablation (Table 6)
| Variant | CR | OOB | VD | FOD |
|---------|-----|-----|-----|-----|
| w/o Top-down Roaming | 0.09 | 0.02 | 0.22 | 2.6 |
| w/o Ego-centric Roaming | 0.07 | 0.02 | 0.20 | 3.44 |
| w/o VLM-LLM Refiner | 0.20 | 0.05 | 0.33 | 3.02 |
| w/o Manipulable Placement | 0.05 | 0.01 | 0.33 | 1.82 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

Key implications: Ours significantly leads in FOD (footprint object density) (4.16 vs. 2.15 for the best baseline), indicating denser scenes; ablation shows the VLM-LLM Refiner has the greatest impact on CR (CR rises from 0.05 to 0.20 when removed), validating the core role of recursive correction in physical plausibility.

## Boundaries and Limitations

The paper does not explicitly discuss limitations, but inferences from factual points include: shell texture generation adopts simple strategies (reusing 3D-FRONT assets or synthesizing via 2D generation models) without introducing a dedicated material pipeline; lighting uses rule-based generation rather than learned methods. No mention is made of physical simulation validation results for generated scenes, generalization tests against real-world scenes, specific numbers for generation time/computational cost, or detailed statistical results from the user study. No mention of specific iteration limits for the VLM refiner, specific parameters for viewpoint selection (grid resolution, field of view, safety distance), or specific statistics on manipulable object counts (beyond >15 in Table 1). Additionally, LayoutGPT only supports living rooms and bedrooms, so it was not evaluated in kitchen and bathroom comparisons, potentially affecting the completeness of baseline comparisons.

## Engineering Insights

When reproducing or adopting HomeWorld, prioritize verifying the following: **Correct implementation of the K-D tree representation** is the cornerstone of floor plan generation quality; ablation shows that canonical representation leads to 9.2% overlaps, 2.0% holes, and 16.2% disconnections, while K-D trees reduce disconnections to 12.3% but not to zero, requiring attention to constraints on leaf node semantic assignment. **Data construction for the VLM refiner** is the most error-prone area—the types and magnitudes of injected perturbations in corrupted layouts directly affect oracle labeling quality; it is recommended to strictly follow the paper's six perturbation categories (boundary violations, collisions, doorway blockages, rotation, scale, category-level errors) and retain model-in-the-loop samples to cover intermediate states. **Viewpoint selection in hierarchical roaming** significantly impacts final object density (FOD drops from 4.16 to 2.6 when removed); the grid resolution of the heatmap coverage strategy needs tuning based on room scale. **Manipulable object placement** relies on PhysX-Anything's physical property inference; it is advisable to first validate the accuracy of density and mesh volume estimation, otherwise mass estimation biases can cause physical filtering to erroneously remove objects. Finally, dataset release (300K floor plans + 5K scenes) is a key dependency for reproduction; if unavailable, one can initially train the LLM on the 314K validated floor plan subset, but distribution differences must be noted.

## 개요

HomeWorld는 연구팀이 제안한 자연어 프롬프트에서 제어 가능하고 밀집된 상호작용형 전체 주택 장면을 생성하는 통합 계층적 프레임워크입니다. 핵심 기여는 전체 주택 생성을 평면도 합성, 계층적 로밍 배치, 재귀적 세분화, 조작 가능한 객체 배치의 네 단계로 분해하고, 대규모 실제 평면도 데이터로 훈련된 LLM과 명시적 3D 앵커링을 결합하여 기존 방법의 전역적 일관성, 물리적 실현 가능성, 시뮬레이션 준비성 부족 문제를 해결한 것입니다.

## 무엇을 바꾸었는가

전체 주택 장면 생성은 오랫동안 평면도 합성, 단일 방 레이아웃, 객체 배치 등의 고립된 하위 작업으로 분리되어, 생성된 장면은 전역적 위상 일관성과 방 간 의미적 연속성이 부족했습니다. 기존 방법은 수동 규칙(예: ProcTHOR)에 의존하여 반복적이고 편향된 레이아웃을 생성하거나, 직접 3D 학습을 수행하지만 훈련 데이터 부족과 계산 비용에 제약을 받거나, 2D 승격(예: Text2Room)을 채택하지만 강력한 3D 사전 앵커링이 부족하여 전체 주택으로 확장 시 기하학적 파편화가 발생했습니다. HomeWorld가 진정으로 바꾼 것은 "생성"을 "제어 가능한 계층적 인스턴스화" 프로세스로 재정의한 것입니다: 먼저 LLM을 사용하여 구조화된 표현에서 위상적으로 올바른 평면도를 합성하고, 명시적 3D 셸 제약 하의 2D 인페인팅으로 점진적으로 가구를 채운 다음, VLM으로 물리적 위반을 재귀적으로 교정합니다. 이 패러다임은 데이터가 부족한 3D 생성 문제를 데이터가 상대적으로 풍부한 2D 생성 및 3D 검증 문제로 변환하여, 생성 결과의 위상적 유효성, 객체 밀도, 물리적 합리성을 동시에 향상시킵니다.

## 방법 분해

### 단계 1: 평면도 생성 (LLM + K-D 트리)
- 데이터 구축: 온라인 부동산 저장소에서 108.4만 장의 평면도 이미지를 수집하고, 2K장의 수동 주석 이미지로 검출 모델을 훈련하여 문, 창, 벽, 치수선을 식별합니다. 검출 박스를 중심선으로 변환하고 인접 끝점을 병합하여 벡터화 구조를 재구성합니다. OCR로 방 레이블과 치수를 추출하고 품질 필터링 후 31.4만 장의 검증 평면도를 얻습니다.
- 표현 설계: 평면도를 이진 K-D 트리로 인코딩하며, 내부 노드는 분할 축과 좌표를 저장하고, 리프 노드는 공간 단위에 해당하며 방 의미를 할당합니다. 문, 창 등의 부속물은 JSON에서 별도로 표현됩니다. 다각형 좌표를 직접 예측하는 것보다 K-D 트리 출력 공간이 더 구조화되어 있으며, 결정론적으로 다각형 겹침과 빈 공간을 방지합니다.
- 훈련: Qwen3-4B-Instruct를 사용하고 LoRA 지도 미세 조정, 데이터 분할 281K/15K/15K.

### 단계 2: 계층적 로밍 배치 (2D 승격 + 3D 앵커링)
- 셸 인스턴스화: Blender에서 기본 조명과 재질을 갖춘 빈 무가구 셸을 생성하여 로밍의 명시적 3D 제약으로 사용합니다.
- 전역 레이아웃(탑뷰): 탑뷰를 렌더링하고 문, 창, 문 인접 방을 주석 처리한 후, 이미지 인페인팅 모델(Gemini 3.0 Image Pro Preview)로 가구가 있는 탑뷰를 합성하고, SAM-3로 인스턴스 마스크와 2D 박스를 추출하며, VLM으로 개방 어휘 카테고리 인식을 수행합니다.
- 세부 세분화(자아 중심 뷰): 히트맵 기반 시점 선택 전략으로 바닥을 그리드로 이산화하고, 가장 적게 관측된 영역을 덮는 시점을 탐욕적으로 선택합니다. 각 시점에 대해 자아 중심 이미지를 렌더링하고 인페인팅으로 보조 객체를 합성하며, SAM-3D로 3D 기하학을 재구성하고 기하학 정렬 모듈로 방에 통합합니다.
- 로밍 순서: 먼저 탑뷰로 대형 기능 가구(침대, 소파)를 배치한 다음, 자아 중심 뷰로 소형 물품을 점진적으로 풍부하게 합니다.

### 단계 3: 재귀적 레이아웃 세분화 (VLM 순차 결정)
- 세분화를 순차 결정 프로세스로 공식화: VLM 세분화기가 현재 레이아웃 상태를 관찰하고 구조화된 교정 동작(이동, 회전 또는 결합 변환)을 예측하며, 동작은 3D 경계 상자에 결정론적으로 적용되고, 재렌더링 후 검사하며, 검증 통과 또는 반복 제한에 도달할 때까지 루프합니다.
- 미세 조정 데이터 구축: 손상된 레이아웃 구성(경계 위반, 충돌, 출입구 막힘 등 교란 주입), Oracle 교정 동작 레이블링(잔여 오류, 새로 도입된 위반, 이동 비용, 의미적 합리성 점수로 진실값 선택), 모델 인 더 루프 샘플(초기 세분화기 롤링, 중간 상태를 oracle로 재레이블링).

### 단계 4: 조작 가능한 객체 배치
- 의미적 카테고리와 기하학적 속성(수평 표면 존재성과 충분한 사용 가능 면적)에 따라 소형 객체를 지지할 수 있는 가구를 식별합니다.
- 각 가구에 대해 로컬 표면 캔버스를 구축하고 인페인팅 모델로 표면 수준 배열을 합성합니다.
- PhysX-Anything으로 물리 속성(밀도, 영률, 푸아송 비)을 추론하고, 밀도와 메시 부피로 질량을 추정합니다.
- VLM이 합성 표면 이미지를 조작 가능한 객체 집합(의미적 카테고리, 스케일, 상대 위치 및 방향)으로 파싱하고 전역 좌표계로 변환합니다.
- 삽입 전 경량 물리 필터링: 지지 경계를 벗어나거나, 심각한 충돌, 부유, 또는 카테고리 수준 지지 제약을 위반하는 객체를 제거하고 명시적 지지 관계를 유지합니다.

## 핵심 혁신

1. **K-D 트리 평면도 표현**: 평면도 생성을 자유 다각형 예측에서 구조화된 재귀 분할 예측으로 변환하여 위상 오류(겹침, 빈 공간, 비연결성)를 근본적으로 제거합니다. 이는 단순 제약이 아닌 출력 공간의 재구성으로, LLM 생성이 구조적으로 자연스럽게 유효하며 결정론적으로 기존 표현으로 변환 가능합니다.
2. **계층적 로밍(Hierarchical Roaming)**: 먼저 탑뷰 전역 레이아웃, 그다음 자아 중심 뷰 세부 세분화를 수행하고 명시적 3D 셸 제약을 결합하여 순수 2D 승격 방법의 전체 주택 규모 기하학적 불일치 문제를 해결합니다. 이 설계는 2D 생성 사전과 3D 앵커링을 효과적으로 결합하여 직접 3D 학습의 데이터 병목을 피합니다.
3. **VLM 재귀 세분화기**: 레이아웃 교정을 순차 결정 프로세스로 모델링하고, 손상된 레이아웃 구성 + Oracle 레이블링 + 모델 인 더 루프의 3단계 데이터 구축을 통해 VLM이 실행 가능한 3D 경계 상자 편집 동작을 출력하도록 훈련합니다. 이로써 세분화 프로세스가 일회성 생성이 아닌 검증 가능하고 반복 가능해집니다.

## 실험 및 결과

### 평면도 생성 (표 2)
| 지표 | ProcTHOR-10K | Ours |
|------|-------------|------|
| 그래프 유효성 | 72.6% | 87.7% |
| 유형 그래프 다양성 | 6.2 | 22.4 |
| 비유형 그래프 다양성 | 2.3 | 6.9 |

### 레이아웃 생성 (표 4, 4개 방 카테고리 각 25개 샘플)
| 방법 | CR | OOB | VD | FOD |
|------|-----|-----|-----|-----|
| LayoutGPT | 0.05 | 0.01 | 0.22 | 1.99 |
| Holodeck | 0.07 | 0.02 | 0.22 | 2.10 |
| LayoutVLM | 0.20 | 0.01 | 0.27 | 2.15 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

### 사용자 연구 (표 5, 30명 참가자, 방법당 10개 샘플)
| 방법 | 합리성 | 미학 | 복잡성 | 평균 |
|------|--------------|------------|------------|---------|
| LayoutGPT | 0.202 | 0.184 | 0.153 | 0.180 |
| Holodeck | 0.395 | 0.412 | 0.452 | 0.420 |
| LayoutVLM | 0.252 | 0.260 | 0.283 | 0.265 |
| Ours | 0.807 | 0.827 | 0.797 | 0.811 |

### 절제 연구 (표 6)
| 변형 | CR | OOB | VD | FOD |
|------|-----|-----|-----|-----|
| w/o 탑다운 로밍 | 0.09 | 0.02 | 0.22 | 2.6 |
| w/o 자아 중심 로밍 | 0.07 | 0.02 | 0.20 | 3.44 |
| w/o VLM-LLM 세분화기 | 0.20 | 0.05 | 0.33 | 3.02 |
| w/o 조작 가능한 배치 | 0.05 | 0.01 | 0.33 | 1.82 |
| Ours | 0.05 | 0.01 | 0.35 | 4.16 |

핵심 의미: Ours는 FOD(발자국 객체 밀도)에서 현저히 앞서며(4.16 vs 기준선 최고 2.15), 장면이 더 밀집되어 있음을 나타냅니다. 절제 연구는 VLM-LLM 세분화기가 CR에 가장 큰 영향을 미친다는 것을 보여주며(제거 시 CR이 0.05에서 0.20으로 상승), 재귀적 교정이 물리적 합리성의 핵심 역할임을 검증합니다.

## 경계 및 한계

논문은 한계를 명시적으로 논의하지 않았지만, 사실적 요점에서 추론할 수 있습니다: 셸 텍스처 생성은 간단한 전략(3D-FRONT 자산 재사용 또는 2D 생성 모델 합성)을 채택하고 전용 재질 파이프라인을 도입하지 않았습니다. 조명은 규칙 기반 생성이며 학습 기반 방법이 아닙니다. 생성된 장면의 물리 시뮬레이션 검증 결과, 실제 세계 장면과의 일반화 테스트, 생성 시간/계산 비용의 구체적 수치, 사용자 연구의 상세 통계 결과는 언급되지 않았습니다. VLM 세분화기의 반복 제한 구체적 값, 시점 선택의 구체적 매개변수(그리드 해상도, 시야각, 안전 거리)도 언급되지 않았습니다. 조작 가능한 객체 수의 구체적 통계(표 1의 >15 제외)도 언급되지 않았습니다. 또한 LayoutGPT는 living room과 bedroom만 지원하므로 kitchen과 bathroom 비교에서 평가되지 않아 기준선 비교의 완전성에 영향을 미칠 수 있습니다.

## 공학적 시사점

HomeWorld를 재현하거나 채택할 때 다음 단계를 우선 확인하십시오: **K-D 트리 표현의 올바른 구현**은 평면도 생성 품질의 기초이며, 절제 연구는 정규 표현이 9.2% 겹침, 2.0% 빈 공간, 16.2% 비연결성을 초래하는 반면 K-D 트리는 비연결성을 12.3%로 낮추지만 여전히 0이 아니므로 리프 노드 의미 할당의 제약에 주의해야 합니다. **VLM 세분화기의 데이터 구축**은 가장 함정이 많은 부분입니다—손상된 레이아웃에 주입된 교란 유형과 크기는 oracle 레이블링 품질에 직접 영향을 미치므로, 논문의 경계 위반, 충돌, 출입구 막힘, 회전, 스케일, 카테고리 수준 오류의 6가지 교란 유형을 엄격히 따르고 중간 상태를 덮기 위해 모델 인 더 루프 샘플을 유지하는 것이 좋습니다. **계층적 로밍의 시점 선택**은 최종 객체 밀도에 큰 영향을 미치며(제거 시 FOD가 4.16에서 2.6으로 하락), 히트맵 커버리지 전략의 그리드 해상도는 방 규모에 따라 조정해야 합니다. **조작 가능한 객체 배치**는 PhysX-Anything의 물리 속성 추론에 의존하므로, 밀도와 메시 부피 추정의 정확성을 먼저 검증하는 것이 좋습니다. 그렇지 않으면 질량 추정 편차로 물리 필터링이 잘못 삭제할 수 있습니다. 마지막으로, 데이터셋 공개(300K 평면도 + 5K 장면)는 재현의 핵심 의존성이며, 확보할 수 없으면 31.4만 검증 평면도 하위 집합으로 LLM을 훈련할 수 있지만 분포 차이에 주의해야 합니다.
