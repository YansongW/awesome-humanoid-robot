---
$id: ent_paper_green_vla_staged_vision_language_action_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Green-VLA: Staged Vision-Language-Action Model for Generalist Robots'
  zh: 'Green-VLA: Staged Vision-Language-Action Model for Generalist Robots'
  ko: 'Green-VLA: Staged Vision-Language-Action Model for Generalist Robots'
summary:
  en: 'We introduce Green-VLA, a staged Vision-Language-Action (VLA) framework for real-world deployment on the Green humanoid
    robot while maintaining generalization across diverse embodiments. Institutions per source list: Sber Robotics Center.'
  zh: Green-VLA 是一个面向通用机器人的分阶段视觉-语言-动作（VLA）框架，由研究团队开发，旨在部署于 Green 人形机器人并保持跨不同形态的泛化能力。其核心贡献包括五阶段课程学习、可扩展数据处理流水线（3000小时演示数据）以及统一形态感知动作接口，并通过强化学习对齐显著提升成功率、鲁棒性和长时任务效率。
  ko: 'We introduce Green-VLA, a staged Vision-Language-Action (VLA) framework for real-world deployment on the Green humanoid
    robot while maintaining generalization across diverse embodiments. Institutions per source list: Sber Robotics Center.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- green
- vla
- staged
- vision
- language
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 378 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2602.00919 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2602.00919v2); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (rewritten): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2602.00919 Green-VLA: Staged Vision-Language-Action Model for Generalist Robots'
  url: https://arxiv.org/abs/2602.00919
  accessed_at: '2026-07-31'
  date: '2026-01-31'
- id: src_002
  type: website
  title: Project page
  url: https://greenvla.github.io/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---




## 概述

Green-VLA 是一个约 4B 参数的视觉-语言-动作（VLA）模型，由 Green 团队提出，面向通用机器人操作。其核心贡献在于提出五阶段训练课程（L0→L1→R0→R1→R2），将互联网规模预训练、跨本体机器人预训练、本体微调与强化学习对齐系统化整合，并配套统一动作空间、数据质量流水线（DataQA）与目标平衡采样等工程手段，在 ALOHA、SimplerEnv 及人形机器人等多项基准上显著超越 π₀、OpenVLA 等强基线。

## 它改变了什么

它真正改变的是“VLA 训练范式”的底层逻辑——不再把数据规模当作第一推动力，而是把“质量对齐、动作统一、RL 优化”作为与规模并列甚至更优先的杠杆。此前领域内主流做法（如 π₀、OpenVLA）依赖大规模异构数据混合 + 行为克隆（BC），但 BC 会迅速饱和，且多本体混合训练时高动量优化器（β₁=0.95–0.98）的动量缓冲区会被大数据集梯度主导，导致稀有本体被“冲刷掉”。Green-VLA 用目标平衡采样（αₜ 从 0 线性增至 1）从机制上缓解了模式遗忘，而非仅靠调采样权重。

同时，它把“推理延迟”和“系统吞吐量”从工程细节提升为与成功率并列的一等公民。现有显式推理 VLA（如 EO-1、WALL-OSS）依赖自回归循环，延迟高；Green-VLA 用流匹配动作专家 + SDPA 注意力内核 + 减少去噪步数，在保持高成功率的同时压缩推理开销。此外，R2 阶段的 RL 对齐方式（轨迹优化 + 源分布优化）绕开了流匹配模型在传统 on-policy/off-policy RL 中的对数概率估计难题和值梯度反向传播不稳定性，这是对“VLA 如何做 RL”这一开放问题的务实回答。

## 方法拆解

### 五阶段训练课程
- **L0（基础 VLM）**：底层视觉-语言模型（PaliGemma 3B），无机器人动作能力。
- **L1（网络预训练）**：24M 非机器人多模态样本，构建物理世界理解与语义基础。
- **R0（通用机器人预训练）**：184M 机器人领域样本（>3,000 小时，跨人形与机械臂），学习跨本体通用先验。在 48 块 H100 上训练 >10⁵ 步。
- **R1（本体特定微调）**：目标本体高质量数据集 SFT，将通用能力转化为具体技能。
- **R2（RL 对齐）**：结合 BC 先验与 RL 目标，提升长期成功率、鲁棒性与平均链长（ACL）。

### 统一动作空间 (𝒜ᵤ)
- 用固定语义布局的 64 维统一动作空间替代朴素填充，通过映射函数 Φₑ 与二进制掩码 mₑ 实现多本体控制，消除填充产生的虚假梯度。
- 动态本体与控制类型提示 cₑ（手臂数量、手数量、夹爪/灵巧手、关节/笛卡尔、移动/静态、槽位使用）使动作映射显式化。

### 目标平衡采样（Target-balanced sampling）
- 采样权重：Wᵢ^(t) = wᵢ^αₜ / Σⱼ wⱼ^αₜ，αₜ ∈ [0,1]，α₀=0，α_T=1。
- 从均匀混合（α=0）逐渐收敛到偏斜分布（α=1），防止早期坍缩到主导本体，让模型先学共享结构再专门化。

### 动作对齐（Action alignment）
- 用单调三次样条重采样轨迹并插值动作，归一化每步有效运动。
- 通过腕部相机平均光流幅度估计各数据集执行速度：低采集频率数据集（Bridge、Fractal）帧间光流大，需加密动作；高频率但慢速数据集（AgiBot DexHand）则降采样加速。

### 时间尺度条件化（Temporal Scale Conditioning）
- 对每个样本抽取标量速度因子 v ~ p(v)，用插值/重采样扭曲目标轨迹：v>1 减慢，v<1 压缩。
- 对动作专家隐藏状态施加 RMS 风格调制：ĥₜ = γ(v)RMSNorm(hₜ) + β(v)，γ、β 为 v 的小型学习函数。
- 推理时 v 为超参数，权衡局部精度与长时程效率。

### 情节进度（Episode progress）
- 流匹配动作专家联合预测归一化进度信号 ρ̂ₜ ∈ [0,1]，训练目标 ρₜ = t/T，暴露给下游规划器以请求更新指令。

### OOD 检测器
- 在线检测器建模为训练集状态上的高斯混合模型：p_train(s) = Σₖ φₖ 𝒩(s|μₖ, Σₖ)。
- 若预测动作导致 p_train(s) < τ_ood，则重算动作至修正状态 s + α∇p_train(s)，α=0.2。

### 联合预测模块（JPM）
- 专用 VLM 预测 2D 可操作点 (u,v)，经深度 d、内参 K、相机位姿 T_c^w 提升至 3D：[p⋆; 1] = T_c^w [d(u,v)K⁻¹[u,v,1]ᵀ; 1]。
- 求解 IK 得可行关节配置 q⋆，用伪逆引导（ΠGDM）在每个去噪步修改速度场，保留先验同时满足目标约束。

### R2 RL 微调——轨迹优化
- 在 R1 数据上训练小型独立评论家（Implicit Q-Learning）：L_V(ψ) = 𝔼[L₂^τ(Q_θ̂(s,a) − V_ψ(s))]，L_Q(θ) = 𝔼[(r + γV_ψ(s′) − Q_θ)²]。
- 基模型生成轨迹后，计算 ∇ₐQ(s,a)，归一化后按 η 缩放加到动作：a ← a + η·∇ₐQ/‖∇ₐQ‖，重复 N 次。
- 因 Q 非最优，优化后轨迹需在环境中验证（操作员恢复初始状态，执行改进轨迹并保存）。

### R2 RL 微调——源分布优化
- 用 actor-critic 训练小型独立 actor 网络，生成输入基模型的随机噪声，最大化环境回报。
- 基模型生成动作倾向保持在训练数据附近，即使噪声分布偏移也相对安全。

## 关键创新

1. **五阶段训练课程的系统化设计**：将互联网预训练（L1）、跨本体机器人预训练（R0）、本体微调（R1）与 RL 对齐（R2）显式分层，每阶段有明确的数据规模与目标。这不是简单堆叠，而是让“通用先验”与“本体专用”解耦——R0 用 184M 样本学跨本体共性，R1 用高质量数据转技能，R2 用 RL 对齐长期目标。相比 π₀ 的端到端混合训练，这种分层使每个阶段可独立调试与评估。

2. **目标平衡采样（αₜ 退火）**：直接针对多本体混合训练中的“模式遗忘”问题。从均匀混合（α=0）开始，让稀有本体在早期贡献表征，再逐渐过渡到偏斜分布（α=1）。这是对高动量优化器（β₁=0.95–0.98）行为特性的针对性设计——早期平衡确保动量缓冲区不被大数据集主导。

3. **R2 的“非侵入式”RL 微调**：轨迹优化（∇ₐQ 引导）与源分布优化（actor 生成噪声）都绕开了直接修改基模型权重的 RL 梯度路径。这避免了流匹配模型在传统 RL 中的两个核心难题：on-policy 方法（PPO/GRPO）的对数概率估计困难，以及 off-policy 方法值梯度反向传播通过迭代生成过程的不稳定性。工程上更稳健，且保留了 BC 先验。

## 实验与结果


对照实验覆盖从单臂机械臂、双臂移动平台到全尺寸人形机器人的多实施例范围，基线包括 π₀、GR00T N1、AgiBot GO-1、WALL-OSS、OpenVLA、RT-1-X、Flower、DB-MemVLA 等。评估指标以成功率（SR）为主，辅以平均完成时间、平均链长（ACL）等。训练采用五阶段课程：L0（基础 VLM）→ L1（网络预训练，24M 非机器人多模态样本）→ R0（通用机器人预训练，184M 机器人领域样本，总计 3,000 小时）→ R1（实施例 SFT）→ R2（RL 对齐）。R0 在 48 块 H100 GPU 上训练超过 10⁵ 优化步，数据量显著少于 π₀ 的超过 10,000 小时（论文未明确 π₀ 具体数据量，仅给出 `> 10,000 hours`）。下表汇总各阶段关键结果。

| 任务/环境 | 指标 | π₀ | GR00T N1 | AgiBot GO-1 | Green-VLA(R0) | Green-VLA(R1) | Green-VLA(R2) |
|---|---|---|---|---|---|---|---|
| ALOHA 桌面清理 | First item SR (%) | 35.6 | 33.2 | 38.4 | 69.5 | — | — |
| ALOHA 桌面清理 | AVG Time | 2m59s | >5m | 3m57s | 1m35s | — | — |
| Simpler Google Robot | Average SR (%) | 56.8 | — | — | 60.2 | 61.3 | 71.8 |
| Simpler WidowX | AVG Grasp (%) | 53.1 | — | — | 75.0 | 76.1 | 91.7 |
| Simpler WidowX | AVG Success (%) | 27.1 | — | — | 45.0 | 55.2 | 79.1 |

**结果含义要点：**

- **R0 预训练阶段即展现强泛化**：在 ALOHA 桌面清理中，Green-VLA(R0) 的首物品成功率 69.5%，显著高于 π₀ 的 35.6%（由表内数值 35.6→69.5 计算，相对提升约 95%），且平均完成时间 1m35s 远快于 π₀ 的 2m59s。在 Simpler Google Robot 上，R0 平均成功率 60.2% 已超过 π₀ 微调后的 56.8%，且无需针对特定实施例额外调优。这表明统一动作空间与掩码 BC 目标有效消除了朴素零填充的"伪惩罚"梯度，使多实施例预训练数据能够互相促进而非干扰。

- **R1→R2 的 RL 对齐带来持续且显著的增益**：在 Simpler WidowX 上，从 R0 到 R1 再到 R2，平均抓取成功率从 75.0% 提升至 76.1% 再跃升至 91.7%，平均任务成功率从 45.0% 提升至 55.2% 再跃升至 79.1%。在 Simpler BRIDGE WidowX 环境中，R2 对齐将 R1 模型成功率提升了绝对 24%（论文原文 `an absolute 24%`）。在 Google Robot 上，R2 平均成功率 71.8% 相比 R1 的 61.3% 提升明显（由表内数值 61.3→71.8 计算）。作者指出，行为克隆在长时程和接触丰富操作中会快速饱和，而 R2 的奖励塑形精炼有效克服了这一瓶颈，尤其在长时程一致性、错误恢复和精确任务遵循方面。

- **数据规模与多样性是预训练质量的关键杠杆**：R0 使用 3,000 小时统一多实施例数据，显著少于 π₀ 的超过 10,000 小时（论文未明确 π₀ 具体数据量，仅给出 `> 10,000 hours`），却在多个基准上达到或超越 π₀ 微调性能。数据混合中，AgiBot twofinger 因最大（774 小时）且视觉多样性最高（D_vis=0.149）获得最高采样权重 0.210；DROID 以 501 小时、D_vis=0.140 和 0.129 的采样权重次之；Galaxea 以 477 小时、D_vis=0.168 在 ALOHA 类数据中视觉多样性最高。自采 Green Humanoid 数据经镜像和时间反转增强，从 48 小时原始数据扩展至 167 训练小时，以 0.089 的采样权重参与训练。这种基于多样性指标的加权采样策略，使得有限数据量下仍能覆盖广泛的任务和技能分布。

## 边界与局限

- **重定向保真度依赖**：性能仍受 retargeting fidelity、残余数据集偏差及灵巧技能覆盖度限制。若源机器人动作到目标本体的映射不精确，R0 学到的先验可能带偏 R1。
- **多语言与推理模块缺失**：未实现多语言指令跟随，未集成轻量级任务分解推理模块（思维链规划与实时执行结合），未加入具身记忆与轨迹回放。
- **v 为超参数**：推理时时间尺度因子 v 需人工设定，未来才由高层规划器自动控制。
- **R2 轨迹优化不保证改进**：作者明确承认 Q 函数非最优且超参数（η、N）启发式调优，优化后轨迹可能恶化，需环境验证。
- **未明确参数**：JPM 中 VLM 具体型号、OOD 阈值 τ_ood、η 与 N 具体数值、v 的采样分布 p(v) 均未在论文中给出。

## 工程启示

- **先核对采样退火曲线**：目标平衡采样（αₜ 从 0 到 1）是防止多本体模式遗忘的关键。复现时先确认 αₜ 的调度步数与总训练步数匹配——若 α 增长过快，稀有本体仍会被冲刷；过慢则大本体欠拟合。
- **动作对齐是隐性瓶颈**：不同数据集执行速度差异（用腕部相机光流估计）直接影响混合训练效果。复现时先对每个数据集计算平均光流幅度，再决定插值/降采样策略，否则模型对动力学幅度的预期会混乱。
- **R2 的验证闭环不可跳过**：轨迹优化（∇ₐQ 引导）不保证改进，必须部署环境验证环节（恢复初始状态、执行改进轨迹、保存结果）。若跳过此步，可能把恶化轨迹混入 R1 数据集，造成累积污染。
- **统一动作空间的掩码设计**：64 维统一动作空间依赖 Φₑ 与 mₑ 的正确实现。朴素填充会产生虚假梯度，复现时先验证不同本体间动作槽位的语义对齐是否一致。
- **推理优化关注点**：SDPA 注意力内核与减少流匹配去噪步数是压缩延迟的主要手段。若下游对实时性敏感，优先调这两项而非模型规模。
- **数据质量流水线（DataQA）指标**：抖动（J）、清晰度（S）、多样性（D）、状态方差（σ²）的阈值需按本体调整——例如 AgiBot twofinger 的 D_state=3975 远高于 DROID 的 3.10，直接套用统一阈值会误删有效数据。

## 参考
- https://arxiv.org/abs/2602.00919
- https://greenvla.github.io/
- https://github.com/ImChong/Robotics_Notebooks

## Overview

Green-VLA is a vision-language-action (VLA) model with approximately 4B parameters, proposed by the Green team for general-purpose robot manipulation. Its core contribution lies in proposing a five-stage training curriculum (L0→L1→R0→R1→R2) that systematically integrates internet-scale pretraining, cross-embodiment robot pretraining, embodiment-specific fine-tuning, and reinforcement learning alignment, accompanied by engineering techniques such as a unified action space, a data quality pipeline (DataQA), and target-balanced sampling. It significantly outperforms strong baselines such as π₀ and OpenVLA across multiple benchmarks including ALOHA, SimplerEnv, and humanoid robots.

## What It Changes

What it truly changes is the underlying logic of the "VLA training paradigm"—it no longer treats data scale as the primary driving force, but instead regards "quality alignment, action unification, and RL optimization" as levers that are on par with or even prioritized over scale. Previously, mainstream approaches in the field (e.g., π₀, OpenVLA) relied on large-scale heterogeneous data mixing plus behavioral cloning (BC), but BC saturates quickly, and during multi-embodiment mixed training, the momentum buffers of high-momentum optimizers (β₁=0.95–0.98) become dominated by gradients from large datasets, causing rare embodiments to be "washed out." Green-VLA mitigates mode forgetting mechanistically via target-balanced sampling (αₜ linearly increasing from 0 to 1), rather than merely tuning sampling weights.

At the same time, it elevates "inference latency" and "system throughput" from engineering details to first-class citizens alongside success rate. Existing explicit-reasoning VLAs (e.g., EO-1, WALL-OSS) rely on autoregressive loops, which incur high latency; Green-VLA uses a flow-matching action expert plus an SDPA attention kernel and reduced denoising steps to compress inference overhead while maintaining high success rates. Furthermore, the RL alignment approach in the R2 stage (trajectory optimization + source distribution optimization) circumvents the log-probability estimation difficulties and value-gradient backpropagation instability of flow-matching models in traditional on-policy/off-policy RL, offering a pragmatic answer to the open question of "how to do RL for VLAs."

## Method Breakdown

### Five-Stage Training Curriculum
- **L0 (Base VLM)**: Underlying vision-language model (PaliGemma 3B), without robot action capabilities.
- **L1 (Web Pretraining)**: 24M non-robot multimodal samples, building physical world understanding and semantic foundations.
- **R0 (General Robot Pretraining)**: 184M robot-domain samples (>3,000 hours, spanning humanoids and manipulators), learning cross-embodiment general priors. Trained on 48 H100 GPUs for >10⁵ steps.
- **R1 (Embodiment-Specific Fine-Tuning)**: SFT on high-quality target-embodiment datasets, converting general capabilities into concrete skills.
- **R2 (RL Alignment)**: Combining BC priors with RL objectives to improve long-horizon success rates, robustness, and average chain length (ACL).

### Unified Action Space (𝒜ᵤ)
- Replaces naive padding with a 64-dimensional unified action space with a fixed semantic layout, enabling multi-embodiment control via a mapping function Φₑ and binary mask mₑ, eliminating spurious gradients from padding.
- Dynamic embodiment and control type prompts cₑ (number of arms, number of hands, gripper/dexterous hand, joint/Cartesian, mobile/static, slot usage) make action mapping explicit.

### Target-Balanced Sampling
- Sampling weight: Wᵢ^(t) = wᵢ^αₜ / Σⱼ wⱼ^αₜ, αₜ ∈ [0,1], α₀=0, α_T=1.
- Gradually converges from uniform mixing (α=0) to a skewed distribution (α=1), preventing early collapse to the dominant embodiment and allowing the model to first learn shared structure before specializing.

### Action Alignment
- Uses monotone cubic splines to resample trajectories and interpolate actions, normalizing per-step effective motion.
- Estimates execution speed for each dataset via average optical flow magnitude from wrist cameras: low-frequency datasets (Bridge, Fractal) have large inter-frame optical flow and require denser actions; high-frequency but slow datasets (AgiBot DexHand) are downsampled and accelerated.

### Temporal Scale Conditioning
- Draws a scalar speed factor v ~ p(v) for each sample, warping the target trajectory via interpolation/resampling: v>1 slows down, v<1 compresses.
- Applies RMS-style modulation to action expert hidden states: ĥₜ = γ(v)RMSNorm(hₜ) + β(v), where γ and β are small learned functions of v.
- At inference, v is a hyperparameter balancing local precision and long-horizon efficiency.

### Episode Progress
- The flow-matching action expert jointly predicts a normalized progress signal ρ̂ₜ ∈ [0,1], with training target ρₜ = t/T, exposed to downstream planners to request updated instructions.

### OOD Detector
- The online detector is modeled as a Gaussian mixture model over training states: p_train(s) = Σₖ φₖ 𝒩(s|μₖ, Σₖ).
- If the predicted action leads to p_train(s) < τ_ood, the action is recomputed toward a corrected state s + α∇p_train(s), with α=0.2.

### Joint Prediction Module (JPM)
- A dedicated VLM predicts 2D actionable points (u,v), lifted to 3D via depth d, intrinsics K, and camera pose T_c^w: [p⋆; 1] = T_c^w [d(u,v)K⁻¹[u,v,1]ᵀ; 1].
- Solves IK to obtain feasible joint configurations q⋆, using pseudo-inverse guidance (ΠGDM) to modify the velocity field at each denoising step, preserving the prior while satisfying target constraints.

### R2 RL Fine-Tuning—Trajectory Optimization
- Trains a small independent critic (Implicit Q-Learning) on R1 data: L_V(ψ) = 𝔼[L₂^τ(Q_θ̂(s,a) − V_ψ(s))], L_Q(θ) = 𝔼[(r + γV_ψ(s′) − Q_θ)²].
- After the base model generates trajectories, computes ∇ₐQ(s,a), normalizes it, scales by η, and adds it to the action: a ← a + η·∇ₐQ/‖∇ₐQ‖, repeated N times.
- Since Q is not optimal, optimized trajectories must be validated in the environment (operator resets the initial state, executes the improved trajectory, and saves it).

### R2 RL Fine-Tuning—Source Distribution Optimization
- Trains a small independent actor network via actor-critic to generate random noise fed into the base model, maximizing environment reward.
- The base model's action generation tends to stay near training data, remaining relatively safe even if the noise distribution shifts.

## Key Innovations

1. **Systematic Design of the Five-Stage Training Curriculum**: Explicitly layers web pretraining (L1), cross-embodiment robot pretraining (R0), embodiment fine-tuning (R1), and RL alignment (R2), with clear data scales and objectives at each stage. This is not simple stacking but decoupling "general priors" from "embodiment-specific" knowledge—R0 uses 184M samples to learn cross-embodiment commonalities, R1 uses high-quality data to convert skills, and R2 uses RL to align long-term objectives. Compared to π₀'s end-to-end mixed training, this layering allows each stage to be independently debugged and evaluated.

2. **Target-Balanced Sampling (αₜ Annealing)**: Directly targets the "mode forgetting" problem in multi-embodiment mixed training. Starting from uniform mixing (α=0) lets rare embodiments contribute representations early, then gradually transitions to a skewed distribution (α=1). This is a targeted design for the behavior of high-momentum optimizers (β₁=0.95–0.98)—early balance ensures the momentum buffer is not dominated by large datasets.

3. **"Non-Invasive" RL Fine-Tuning in R2**: Both trajectory optimization (∇ₐQ guidance) and source distribution optimization (actor-generated noise) bypass directly modifying base model weights via RL gradient paths. This avoids two core difficulties of flow-matching models in traditional RL: log-probability estimation in on-policy methods (PPO/GRPO) and instability of value-gradient backpropagation through iterative generation in off-policy methods. It is more robust engineering-wise and preserves BC priors.

## Experiments and Results

The comparative experiments cover a multi-embodiment range from single-arm manipulators and dual-arm mobile platforms to full-size humanoid robots, with baselines including π₀, GR00T N1, AgiBot GO-1, WALL-OSS, OpenVLA, RT-1-X, Flower, and DB-MemVLA. Evaluation metrics focus on success rate (SR), supplemented by average completion time and average chain length (ACL). Training follows the five-stage curriculum: L0 (base VLM) → L1 (web pretraining, 24M non-robot multimodal samples) → R0 (general robot pretraining, 184M robot-domain samples totaling 3,000 hours) → R1 (embodiment SFT) → R2 (RL alignment). R0 is trained on 48 H100 GPUs for over 10⁵ optimization steps, with significantly less data than π₀'s over 10,000 hours (the paper does not specify π₀'s exact data volume, only giving `> 10,000 hours`). The table below summarizes key results across stages.

| Task/Environment | Metric | π₀ | GR00T N1 | AgiBot GO-1 | Green-VLA(R0) | Green-VLA(R1) | Green-VLA(R2) |
|---|---|---|---|---|---|---|---|
| ALOHA Table Cleanup | First item SR (%) | 35.6 | 33.2 | 38.4 | 69.5 | — | — |
| ALOHA Table Cleanup | AVG Time | 2m59s | >5m | 3m57s | 1m35s | — | — |
| Simpler Google Robot | Average SR (%) | 56.8 | — | — | 60.2 | 61.3 | 71.8 |
| Simpler WidowX | AVG Grasp (%) | 53.1 | — | — | 75.0 | 76.1 | 91.7 |
| Simpler WidowX | AVG Success (%) | 27.1 | — | — | 45.0 | 55.2 | 79.1 |

**Key Takeaways from Results:**

- **R0 pretraining already exhibits strong generalization**: In ALOHA table cleanup, Green-VLA(R0) achieves a first-item success rate of 69.5%, significantly higher than π₀'s 35.6% (calculated from table values 35.6→69.5, a relative improvement of ~95%), with an average completion time of 1m35s far faster than π₀'s 2m59s. On Simpler Google Robot, R0's average success rate of 60.2% already exceeds π₀'s fine-tuned 56.8%, without additional embodiment-specific tuning. This indicates that the unified action space and masked BC objective effectively eliminate the "pseudo-penalty" gradients of naive zero-padding, allowing multi-embodiment pretraining data to mutually reinforce rather than interfere.

- **R1→R2 RL alignment brings sustained and significant gains**: On Simpler WidowX, from R0 to R1 to R2, average grasp success rises from 75.0% to 76.1% and then jumps to 91.7%, while average task success rises from 45.0% to 55.2% and then jumps to 79.1%. In the Simpler BRIDGE WidowX environment, R2 alignment improves the R1 model's success rate by an absolute 24% (paper states `an absolute 24%`). On Google Robot, R2's average success rate of 71.8% shows a clear improvement over R1's 61.3% (calculated from table values 61.3→71.8). The authors note that behavioral cloning saturates quickly in long-horizon and contact-rich manipulation, and R2's reward-shaping refinement effectively overcomes this bottleneck, particularly in long-horizon consistency, error recovery, and precise task following.

- **Data scale and diversity are key levers for pretraining quality**: R0 uses 3,000 hours of unified multi-embodiment data, significantly less than π₀'s over 10,000 hours (the paper does not specify π₀'s exact data volume, only giving `> 10,000 hours`), yet achieves or exceeds π₀'s fine-tuned performance on multiple benchmarks. In the data mix, AgiBot twofinger receives the highest sampling weight of 0.210 due to its largest size (774 hours) and highest visual diversity (D_vis=0.149); DROID follows with 501 hours, D_vis=0.140, and a sampling weight of 0.129; Galaxea, with 477 hours and D_vis=0.168, has the highest visual diversity among ALOHA-type data. Self-collected Green Humanoid data, augmented via mirroring and time reversal, expands from 48 raw hours to 167 training hours, participating with a sampling weight of 0.089. This diversity-based weighted sampling strategy ensures broad coverage of tasks and skill distributions even with limited data volume.

## Boundaries and Limitations

- **Retargeting fidelity dependence**: Performance remains limited by retargeting fidelity, residual dataset bias, and dexterous skill coverage. If the mapping from source robot actions to target embodiment is imprecise, priors learned in R0 may bias R1.
- **Missing multilingual and reasoning modules**: No multilingual instruction following, no integrated lightweight task decomposition reasoning module (combining chain-of-thought planning with real-time execution), and no embodied memory or trajectory replay.
- **v as a hyperparameter**: The temporal scale factor v at inference requires manual setting; future work will have high-level planners control it automatically.
- **R2 trajectory optimization does not guarantee improvement**: The authors explicitly acknowledge that the Q function is suboptimal and hyperparameters (η, N) are heuristically tuned; optimized trajectories may worsen and require environment validation.
- **Unspecified parameters**: The specific VLM model in JPM, OOD threshold τ_ood, exact values of η and N, and the sampling distribution p(v) for v are not provided in the paper.

## Engineering Insights

- **First verify the sampling annealing curve**: Target-balanced sampling (αₜ from 0 to 1) is key to preventing multi-embodiment mode forgetting. When reproducing, first confirm that the αₜ schedule steps match total training steps—if α grows too fast, rare embodiments will still be washed out; too slow, and large embodiments underfit.
- **Action alignment is a hidden bottleneck**: Differences in execution speed across datasets (estimated via wrist-camera optical flow) directly affect mixed training quality. When reproducing, first compute average optical flow magnitude per dataset, then decide interpolation/downsampling strategies; otherwise, the model's expectations of dynamics magnitude become confused.
- **The R2 validation loop cannot be skipped**: Trajectory optimization (∇ₐQ guidance) does not guarantee improvement; an environment validation step (resetting initial state, executing improved trajectories, saving results) is mandatory. Skipping it may mix worsened trajectories into the R1 dataset, causing cumulative contamination.
- **Mask design in the unified action space**: The 64-dimensional unified action space relies on correct implementation of Φₑ and mₑ. Naive padding produces spurious gradients; when reproducing, first verify that semantic alignment of action slots is consistent across embodiments.
- **Inference optimization focus**: The SDPA attention kernel and reduced flow-matching denoising steps are the primary means of compressing latency. If downstream applications are latency-sensitive, prioritize tuning these two over model scale.
- **DataQA metrics**: Thresholds for jitter (J), sharpness (S), diversity (D), and state variance (σ²) need adjustment per embodiment—for example, AgiBot twofinger's D_state=3975 is far higher than DROID's 3.10, and directly applying a unified threshold would incorrectly discard valid data.

## 개요

Green-VLA는 Green 팀이 제안한 약 4B 파라미터 규모의 비전-언어-행동(VLA) 모델로, 범용 로봇 조작을 대상으로 한다. 핵심 기여는 5단계 훈련 커리큘럼(L0→L1→R0→R1→R2)을 제안하여 인터넷 규모 사전 훈련, 교차-본체 로봇 사전 훈련, 본체 미세 조정, 강화 학습 정렬을 체계적으로 통합하고, 통합 행동 공간, 데이터 품질 파이프라인(DataQA), 목표 균형 샘플링 등의 엔지니어링 수단을 함께 제공하여 ALOHA, SimplerEnv 및 휴머노이드 로봇 등 여러 벤치마크에서 π₀, OpenVLA 등 강력한 베이스라인을 크게 능가한다.

## 무엇을 바꾸었는가

진정으로 바꾼 것은 "VLA 훈련 패러다임"의 근본 논리이다. 더 이상 데이터 규모를 최우선 동력으로 삼지 않고, "품질 정렬, 행동 통일, RL 최적화"를 규모와 동등하거나 더 우선하는 지렛대로 삼는다. 기존 분야의 주류 접근법(예: π₀, OpenVLA)은 대규모 이질적 데이터 혼합 + 행동 클로닝(BC)에 의존했지만, BC는 빠르게 포화되고, 다중 본체 혼합 훈련 시 고모멘텀 최적화기(β₁=0.95–0.98)의 모멘텀 버퍼가 대규모 데이터셋의 그래디언트에 지배되어 희귀 본체가 "씻겨 나가는" 문제가 있었다. Green-VLA는 목표 균형 샘플링(αₜ를 0에서 1로 선형 증가)을 통해 샘플링 가중치 조정에만 의존하지 않고 메커니즘 수준에서 모드 망각을 완화한다.

동시에 "추론 지연"과 "시스템 처리량"을 엔지니어링 세부 사항에서 성공률과 동등한 일급 시민으로 승격시켰다. 기존의 명시적 추론 VLA(예: EO-1, WALL-OSS)는 자기회귀 루프에 의존하여 지연이 높다. Green-VLA는 플로우 매칭 행동 전문가 + SDPA 어텐션 커널 + 노이즈 제거 스텝 감소를 통해 높은 성공률을 유지하면서 추론 오버헤드를 압축한다. 또한 R2 단계의 RL 정렬 방식(궤적 최적화 + 소스 분포 최적화)은 플로우 매칭 모델이 전통적인 on-policy/off-policy RL에서 겪는 로그 확률 추정 문제와 값 그래디언트 역전파 불안정성을 우회하는데, 이는 "VLA에 RL을 어떻게 적용할 것인가"라는 공개 문제에 대한 실용적인 답변이다.

## 방법 분석

### 5단계 훈련 커리큘럼
- **L0(기본 VLM)**: 하위 비전-언어 모델(PaliGemma 3B), 로봇 행동 능력 없음.
- **L1(웹 사전 훈련)**: 2,400만 개의 비로봇 멀티모달 샘플로 물리 세계 이해와 의미론적 기반 구축.
- **R0(범용 로봇 사전 훈련)**: 1억 8,400만 개의 로봇 도메인 샘플(3,000시간 이상, 휴머노이드 및 로봇 팔 교차), 교차 본체 범용 사전 지식 학습. 48대 H100에서 10⁵ 스텝 이상 훈련.
- **R1(본체 특정 미세 조정)**: 목표 본체의 고품질 데이터셋 SFT로 범용 능력을 구체적 스킬로 전환.
- **R2(RL 정렬)**: BC 사전 지식과 RL 목표를 결합하여 장기 성공률, 견고성, 평균 체인 길이(ACL) 향상.

### 통합 행동 공간 (𝒜ᵤ)
- 고정 의미론적 레이아웃의 64차원 통합 행동 공간으로 단순 패딩을 대체하고, 매핑 함수 Φₑ와 이진 마스크 mₑ를 통해 다중 본체 제어를 구현하여 패딩으로 인한 허위 그래디언트 제거.
- 동적 본체 및 제어 유형 프롬프트 cₑ(팔 수, 손 수, 그리퍼/다섯 손가락 손, 관절/데카르트, 이동/정적, 슬롯 사용)로 행동 매핑을 명시화.

### 목표 균형 샘플링(Target-balanced sampling)
- 샘플링 가중치: Wᵢ^(t) = wᵢ^αₜ / Σⱼ wⱼ^αₜ, αₜ ∈ [0,1], α₀=0, α_T=1.
- 균일 혼합(α=0)에서 편향 분포(α=1)로 점진적 수렴하여 초기 붕괴를 지배적 본체로 방지하고, 모델이 먼저 공유 구조를 학습한 후 전문화하도록 함.

### 행동 정렬(Action alignment)
- 단조 3차 스플라인으로 궤적을 리샘플링하고 행동을 보간하여 각 스텝의 유효 운동을 정규화.
- 손목 카메라 평균 광류 크기로 각 데이터셋의 실행 속도 추정: 낮은 수집 빈도 데이터셋(Bridge, Fractal)은 프레임 간 광류가 커서 행동 밀집화 필요, 높은 빈도지만 느린 데이터셋(AgiBot DexHand)은 다운샘플링으로 가속.

### 시간 스케일 조건화(Temporal Scale Conditioning)
- 각 샘플에 스칼라 속도 인자 v ~ p(v)를 추출하고, 보간/리샘플링으로 목표 궤적 왜곡: v>1은 느리게, v<1은 압축.
- 행동 전문가 은닉 상태에 RMS 스타일 변조 적용: ĥₜ = γ(v)RMSNorm(hₜ) + β(v), γ, β는 v의 소형 학습 함수.
- 추론 시 v는 하이퍼파라미터로, 로컬 정밀도와 장기 효율성 간의 균형을 조정.

### 에피소드 진행도(Episode progress)
- 플로우 매칭 행동 전문가가 정규화된 진행 신호 ρ̂ₜ ∈ [0,1]을 공동 예측하고, 훈련 목표 ρₜ = t/T를 하위 계획자에 노출하여 지시 업데이트 요청.

### OOD 탐지기
- 온라인 탐지기는 훈련 세트 상태에 대한 가우시안 혼합 모델로 모델링: p_train(s) = Σₖ φₖ 𝒩(s|μₖ, Σₖ).
- 예측 행동이 p_train(s) < τ_ood를 초래하면, 수정 상태 s + α∇p_train(s), α=0.2로 행동 재계산.

### 공동 예측 모듈(JPM)
- 전용 VLM이 2D 조작 가능 지점 (u,v)을 예측하고, 깊이 d, 내부 파라미터 K, 카메라 포즈 T_c^w를 통해 3D로 승격: [p⋆; 1] = T_c^w [d(u,v)K⁻¹[u,v,1]ᵀ; 1].
- IK를 풀어 실현 가능한 관절 구성 q⋆를 얻고, 의사 역행렬 안내(ΠGDM)로 각 노이즈 제거 스텝에서 속도 필드를 수정하여 사전 지식을 유지하면서 목표 제약을 충족.

### R2 RL 미세 조정——궤적 최적화
- R1 데이터에서 소형 독립 평가자(Implicit Q-Learning) 훈련: L_V(ψ) = 𝔼[L₂^τ(Q_θ̂(s,a) − V_ψ(s))], L_Q(θ) = 𝔼[(r + γV_ψ(s′) − Q_θ)²].
- 기본 모델이 궤적 생성 후, ∇ₐQ(s,a)를 계산하고 정규화한 뒤 η 배율로 행동에 추가: a ← a + η·∇ₐQ/‖∇ₐQ‖, N회 반복.
- Q가 최적이 아니므로, 최적화된 궤적은 환경에서 검증 필요(운영자가 초기 상태 복원, 개선 궤적 실행 및 저장).

### R2 RL 미세 조정——소스 분포 최적화
- actor-critic으로 소형 독립 actor 네트워크를 훈련하여 기본 모델에 입력되는 랜덤 노이즈를 생성하고 환경 보상을 최대화.
- 기본 모델의 행동 생성 경향은 훈련 데이터 근처에 유지되므로, 노이즈 분포가 편향되어도 상대적으로 안전.

## 핵심 혁신

1. **5단계 훈련 커리큘럼의 체계적 설계**: 인터넷 사전 훈련(L1), 교차 본체 로봇 사전 훈련(R0), 본체 미세 조정(R1), RL 정렬(R2)을 명시적으로 계층화하고, 각 단계에 명확한 데이터 규모와 목표를 부여. 단순한 적층이 아니라 "범용 사전 지식"과 "본체 전용"을 분리——R0는 1억 8,400만 샘플로 교차 본체 공통성을 학습하고, R1은 고품질 데이터로 스킬 전환, R2는 RL로 장기 목표 정렬. π₀의 엔드투엔드 혼합 훈련과 달리, 이 계층화는 각 단계를 독립적으로 디버깅하고 평가할 수 있게 한다.

2. **목표 균형 샘플링(αₜ 어닐링)**: 다중 본체 혼합 훈련의 "모드 망각" 문제를 직접 겨냥. 균일 혼합(α=0)에서 시작하여 희귀 본체가 초기 표현 학습에 기여하도록 한 후, 점진적으로 편향 분포(α=1)로 전환. 이는 고모멘텀 최적화기(β₁=0.95–0.98)의 동작 특성에 대한 맞춤형 설계——초기 균형은 모멘텀 버퍼가 대규모 데이터셋에 지배되지 않도록 보장.

3. **R2의 "비침습적" RL 미세 조정**: 궤적 최적화(∇ₐQ 안내)와 소스 분포 최적화(actor 노이즈 생성) 모두 기본 모델 가중치를 직접 수정하는 RL 그래디언트 경로를 우회. 이는 플로우 매칭 모델이 전통적 RL에서 겪는 두 가지 핵심 난제를 회피: on-policy 방법(PPO/GRPO)의 로그 확률 추정 어려움, off-policy 방법의 값 그래디언트가 반복 생성 과정을 통한 역전파 불안정성. 엔지니어링적으로 더 견고하고 BC 사전 지식을 보존.

## 실험 및 결과

대조 실험은 단일 팔 로봇 팔, 이중 팔 이동 플랫폼에서 전신 휴머노이드까지 다양한 구현체 범위를 포괄하며, 베이스라인에는 π₀, GR00T N1, AgiBot GO-1, WALL-OSS, OpenVLA, RT-1-X, Flower, DB-MemVLA 등이 포함된다. 평가 지표는 성공률(SR)을 중심으로 평균 완료 시간, 평균 체인 길이(ACL) 등을 보조로 사용. 훈련은 5단계 커리큘럼을 따름: L0(기본 VLM) → L1(웹 사전 훈련, 2,400만 비로봇 멀티모달 샘플) → R0(범용 로봇 사전 훈련, 1억 8,400만 로봇 도메인 샘플, 총 3,000시간) → R1(구현체 SFT) → R2(RL 정렬). R0는 48대 H100 GPU에서 10⁵ 최적화 스텝 이상 훈련하며, 데이터량은 π₀의 10,000시간 이상(논문은 π₀의 구체적 데이터량을 명시하지 않고 `> 10,000 hours`만 제공)보다 현저히 적다. 아래 표는 각 단계의 핵심 결과를 요약한다.

| 작업/환경 | 지표 | π₀ | GR00T N1 | AgiBot GO-1 | Green-VLA(R0) | Green-VLA(R1) | Green-VLA(R2) |
|---|---|---|---|---|---|---|---|
| ALOHA 데스크톱 정리 | 첫 번째 물품 SR (%) | 35.6 | 33.2 | 38.4 | 69.5 | — | — |
| ALOHA 데스크톱 정리 | 평균 시간 | 2m59s | >5m | 3m57s | 1m35s | — | — |
| Simpler Google Robot | 평균 SR (%) | 56.8 | — | — | 60.2 | 61.3 | 71.8 |
| Simpler WidowX | 평균 그립 (%) | 53.1 | — | — | 75.0 | 76.1 | 91.7 |
| Simpler WidowX | 평균 성공 (%) | 27.1 | — | — | 45.0 | 55.2 | 79.1 |

**결과 의미 요점:**

- **R0 사전 훈련 단계에서 강력한 일반화 발휘**: ALOHA 데스크톱 정리에서 Green-VLA(R0)의 첫 번째 물품 성공률 69.5%는 π₀의 35.6%보다 현저히 높으며(표 내 수치 35.6→69.5로 계산, 상대적 향상 약 95%), 평균 완료 시간 1m35s는 π₀의 2m59s보다 훨씬 빠르다. Simpler Google Robot에서 R0 평균 성공률 60.2%는 이미 π₀ 미세 조정 후의 56.8%를 초과하며, 특정 구현체에 대한 추가 튜닝이 필요 없다. 이는 통합 행동 공간과 마스크 BC 목표가 단순 제로 패딩의 "가짜 페널티" 그래디언트를 효과적으로 제거하여 다중 구현체 사전 훈련 데이터가 서로 방해하지 않고 상호 촉진할 수 있음을 시사한다.

- **R1→R2의 RL 정렬로 지속적이고 현저한 이득**: Simpler WidowX에서 R0에서 R1, R2로 갈수록 평균 그립 성공률이 75.0% → 76.1% → 91.7%로, 평균 작업 성공률이 45.0% → 55.2% → 79.1%로 상승. Simpler BRIDGE WidowX 환경에서 R2 정렬은 R1 모델 성공률을 절대 24% 향상(논문 원문 `an absolute 24%`). Google Robot에서 R2 평균 성공률 71.8%는 R1의 61.3% 대비 뚜렷한 향상(표 내 수치 61.3→71.8로 계산). 저자는 행동 클로닝이 장기 및 접촉이 풍부한 조작에서 빠르게 포화되지만, R2의 보상 형성 정제가 특히 장기 일관성, 오류 복구, 정밀 작업 준수 측면에서 이 병목을 효과적으로 극복한다고 지적한다.

- **데이터 규모와 다양성은 사전 훈련 품질의 핵심 지렛대**: R0는 3,000시간의 통합 다중 구현체 데이터를 사용하며, π₀의 10,000시간 이상(논문은 π₀의 구체적 데이터량을 명시하지 않고 `> 10,000 hours`만 제공)보다 현저히 적지만 여러 벤치마크에서 π₀ 미세 조정 성능에 도달하거나 초과. 데이터 혼합에서 AgiBot twofinger는 가장 크고(774시간) 시각적 다양성이 가장 높아(D_vis=0.149) 최고 샘플링 가중치 0.210을 획득; DROID는 501시간, D_vis=0.140 및 0.129의 샘플링 가중치로 다음; Galaxea는 477시간, D_vis=0.168로 ALOHA 계열 데이터 중 시각적 다양성이 가장 높음. 자체 수집 Green Humanoid 데이터는 미러 및 시간 반전 증강으로 48시간 원본 데이터에서 167 훈련 시간으로 확장되어 0.089의 샘플링 가중치로 훈련에 참여. 이러한 다양성 지표 기반 가중 샘플링 전략은 제한된 데이터량에서도 광범위한 작업 및 스킬 분포를 포괄할 수 있게 한다.

## 경계와 한계

- **리타게팅 충실도 의존성**: 성능은 여전히 retargeting fidelity, 잔여 데이터셋 편향, 손재주 스킬 커버리지에 의해 제한. 소스 로봇 행동에서 목표 본체로의 매핑이 정확하지 않으면 R0에서 학습한 사전 지식이 R1을 편향시킬 수 있음.
- **다국어 및 추론 모듈 부재**: 다국어 지시 따르기 미구현, 경량 작업 분해 추론 모듈(사고 사슬 계획과 실시간 실행 결합) 미통합, 구현체 메모리 및 궤적 재생 미포함.
- **v는 하이퍼파라미터**: 추론 시 시간 스케일 인자 v는 수동 설정 필요, 향후 상위 계획자가 자동 제어할 예정.
- **R2 궤적 최적화는 개선을 보장하지 않음**: 저자는 Q 함수가 최적이 아니고 하이퍼파라미터(η, N)가 휴리스틱하게 튜닝되었음을 명시적으로 인정하며, 최적화된 궤적이 악화될 수 있어 환경 검증이 필요.
- **명시되지 않은 파라미터**: JPM의 VLM 구체적 모델, OOD 임계값 τ_ood, η와 N의 구체적 수치, v의 샘플링 분포 p(v)는 논문에 제공되지 않음.

## 엔지니어링 시사점

- **먼저 샘플링 어닐링 곡선 확인**: 목표 균형 샘플링(αₜ 0에서 1)은 다중 본체 모드 망각 방지의 핵심. 재현 시 αₜ의 스케줄 스텝 수와 총 훈련 스텝 수가 일치하는지 먼저 확인——α 증가가 너무 빠르면 희귀 본체가 여전히 씻겨 나가고, 너무 느리면 대형 본체가 과소적합.
- **행동 정렬은 암묵적 병목**: 데이터셋 간 실행 속도 차이(손목 카메라 광류로 추정)는 혼합 훈련 효과에 직접 영향. 재현 시 각 데이터셋의 평균 광류 크기를 먼저 계산한 후 보간/다운샘플링 전략을 결정해야 하며, 그렇지 않으면 모델의 역학 규모 기대가 혼란.
- **R2의 검증 루프는 건너뛸 수 없음**: 궤적 최적화(∇ₐQ 안내)는 개선을 보장하지 않으므로 환경 검증 단계(초기 상태 복원, 개선 궤적 실행, 결과 저장)를 반드시 배포해야 함. 이 단계를 건너뛰면 악화된 궤적이 R1 데이터셋에 혼입되어 누적 오염을 초래할 수 있음.
- **통합 행동 공간의 마스크 설계**: 64차원 통합 행동 공간은 Φₑ와 mₑ의 올바른 구현에 의존. 단순 패딩은 허위 그래디언트를 생성하므로, 재현 시 먼저 서로 다른 본체 간 행동 슬롯의 의미론적 정렬이 일관적인지 검증.
- **추론 최적화 관심 지점**: SDPA 어텐션 커널과 플로우 매칭 노이즈 제거 스텝 감소는 지연 압축의 주요 수단. 하위 시스템이 실시간에 민감하면 모델 규모보다 이 두 가지를 우선 조정.
- **데이터 품질 파이프라인(DataQA) 지표**: 지터(J), 선명도(S), 다양성(D), 상태 분산(σ²)의 임계값은 본체별로 조정 필요——예: AgiBot twofinger의 D_state=3975는 DROID의 3.10보다 훨씬 높아, 통일 임계값을 직접 적용하면 유효 데이터가 잘못 삭제될 수 있음.
