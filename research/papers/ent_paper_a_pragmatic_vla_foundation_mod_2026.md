---
$id: ent_paper_a_pragmatic_vla_foundation_mod_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: A Pragmatic VLA Foundation Model
  zh: LingBot-VLA｜务实的VLA基础模型
  ko: A Pragmatic VLA Foundation Model
summary:
  en: The implementation path of LingBot-VLA is to first encode language instructions, camera images/multi-view observations
    into multimodal representations, and then use VLA multimodal action models and VLM semantic planning/routing prediction
    to execute action commands. The key point is to preserve the semantic understanding of VLM while adding robot states and
    action heads to avoid staying only at language planning.
  zh: LingBot-VLA 的实现路径是先把语言指令、相机图像/多视角观测编码成多模态表征，再用VLA 多模态动作模型、VLM 语义规划/路由预测可执行动作命令。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
  ko: LingBot-VLA 的实现路径是先把语言指令、相机图像/多视角观测编码成多模态表征，再用VLA 多模态动作模型、VLM 语义规划/路由预测可执行动作命令。关键点是保留 VLM 的语义理解，同时增加机器人状态和动作头，避免只停留在语言规划。
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- general_manipulation
- lingbot_vla
- vision_language_action
- vla
- world_model
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. [2026-08-05]
    body rewritten as full-text six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML
    full text); en/ko sections regenerated by translate pipeline.
sources:
- id: src_001
  type: website
  title: LingBot-VLA project page
  url: https://github.com/robbyant/lingbot-vla
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

本文提出 LingBot-VLA，一个面向真实世界双臂操作任务的视觉-语言-动作（VLA）基础模型，由 Robbyant 团队开发。其核心贡献在于系统性地研究了 VLA 模型在 20,000 小时真实机器人数据上的扩展规律，并通过 Mixture-of-Transformers 架构、Flow Matching 动作建模、视觉深度蒸馏以及分布式训练优化，在 GM-100 基准上显著超越现有方法。

## 它改变了什么

该工作真正改变的是社区对 VLA 模型"数据扩展性"的认知方式。此前，VLA 研究大多停留在小规模数据或仿真环境中的验证，缺乏在统一框架下、跨多平台、大规模真实数据上的严格实证。LingBot-VLA 用 20,000 小时、9 种机器人配置的数据证明了：真实机器人性能随预训练数据规模增长而持续提升，且在 20,000 小时时未见饱和——这一结论直接回应了"数据是否仍是 VLA 的第一性瓶颈"这一核心争议。

其次，它改变了 VLA 训练工程的标准。现有开源代码库（如 π_0 等）在多节点扩展时受限于数据 I/O 和通信开销，而 LingBot-VLA 通过 FSDP+HSDP 混合分片策略和算子级优化，将 8-GPU 吞吐量提升至每 GPU 每秒 261 样本，加速 1.5~2.8 倍。这意味着大规模 VLA 预训练不再是少数拥有超算中心的机构的特权，而是可复现的工程实践。

## 方法拆解

### 模型架构：Mixture-of-Transformers (MoT)
- 集成预训练 VLM（Qwen2.5-VL）与随机初始化的动作专家（action expert），通过共享自注意力进行层间统一序列建模。
- 观测上下文定义为 O_t = [I_t^1, I_t^2, I_t^3, T_t, s_t]，包含三视角图像、任务指令和机器人状态。
- 动作序列 A_t = [a_t, ..., a_{t+T-1}]，预训练阶段动作块长度 T=50。

### 动作建模：Flow Matching
- 训练目标为条件流匹配损失：L_FM = E ||v_θ(A_{t,s}, O_t, s) - (A_t - ε)||²，其中中间动作 A_{t,s} = sA_t + (1-s)ε。
- 相比离散 token 化或高斯混合，Flow Matching 提供更平滑的连续动作分布建模，适合高精度操作。

### 注意力机制：块状因果掩码
- 序列分为三个功能块：[图像、指令]、[状态]、[动作块]。
- 块间因果掩码，块内双向注意力，防止未来动作信息泄漏到观测表示。

### 空间感知：视觉深度蒸馏
- 将 VLM 的可学习查询 Q_t 与 LingBot-Depth 的深度令牌 D_t 对齐，最小化 L_distill = E|Proj(Q_t) - D_t|。
- 该设计直接注入几何信息，解决 VLA 在精确深度推理上的固有缺陷。

### 分布式与算子优化
- FSDP（ZeRO）分片优化器状态、参数和梯度；借鉴 HSDP 为动作专家构建特定"分片组"以减少通信开销。
- 混合精度：归约用 float32，存储和通信用 bfloat16。
- FlexAttention 优化稀疏注意力，torch.compile 算子融合减少内核启动开销。

### 数据标注流程
- 人工将多视角视频按原子动作分段，去除静态帧。
- 使用 Qwen3-VL-235B-A22B 自动标注任务和子任务指令。

## 关键创新

1. **首个大规模真实世界 VLA 扩展性实证**：在 20,000 小时、9 种双臂配置数据上，系统展示了性能随数据规模持续增长且未饱和。这为"数据规模是 VLA 核心杠杆"提供了迄今最有力的证据，而非仅靠架构创新。

2. **视觉深度蒸馏作为通用空间感知模块**：将深度信息以蒸馏方式注入 VLM 查询，而非简单拼接或修改输入通道。该设计在真实世界三个平台上平均 SR 提升 4.28%（相对 π_0.5），且消融实验显示其收益在随机化场景中更为显著（+9.92%），说明深度信息对泛化至关重要。

3. **面向 VLA 的分布式训练系统优化**：HSDP 分片组设计针对 MoT 架构中动作专家与 VLM 的异构通信模式，配合 FlexAttention 和 torch.compile，实现了 1.5~2.8 倍吞吐量提升。这是首个将系统级优化与 VLA 架构特性深度耦合的公开工作。

## 实验与结果

### 真实世界基准（GM-100，25 台机器人，100 任务）
| 方法 | 平均 SR (%) | 平均 PS (%) |
|------|------------|------------|
| WALL-OSS | 4.05 | 10.35 |
| GR00T N1.6 | 7.59 | 15.99 |
| π_0.5 | 13.02 | 27.65 |
| Ours w/o depth | 15.74 | 33.69 |
| Ours w/ depth | **17.30** | **35.41** |

- 深度蒸馏带来平均 SR +4.28%、PS +7.76% 的提升（相对 π_0.5）。
- 在 Galaxea R1Pro 上提升最大（SR +6.88%），AgileX 上 PS 提升达 +5.54%。

### 模拟基准（RoboTwin 2.0，50 任务）
| 设置 | π_0.5 SR (%) | Ours w/o depth SR (%) | Ours w/ depth SR (%) |
|------|-------------|----------------------|---------------------|
| 干净场景 | 82.74 | 86.50 | 88.56 |
| 随机化场景 | 76.76 | 85.34 | 86.68 |

- 随机化场景中绝对 SR 提升达 9.92%（w/ depth），远超干净场景的 5.82%，表明深度信息显著增强了对视觉扰动的鲁棒性。

### 扩展性与数据效率
- 预训练数据从 3,000 小时增至 20,000 小时，下游成功率持续提升，未见饱和。
- 在每任务仅 80 条演示的有限预算下，LingBot-VLA 的 SR 和 PS 均优于使用完整 130 条演示的 π_0.5，说明模型的数据效率更高。

### 训练吞吐量
- 8-GPU 设置下，每 GPU 每秒 261 个样本，相比现有 VLA 代码库加速 1.5~2.8 倍。

## 边界与局限

- **平台覆盖有限**：仅评估双臂、固定底盘/腰部配置，未涉及单臂或移动机器人平台，也未评估非桌面场景。
- **深度蒸馏的边际收益在部分任务上不稳定**：附录中如 Click Bell 任务，Ours w/ depth 在随机化场景 SR 仅 36%，低于 π_0.5 的 66%，说明深度信息并非在所有任务上均有益。
- **数据标注依赖人工分段**：视频分段由人工标注者完成，虽指令标注自动化，但原子动作划分仍存在主观性，可能影响跨平台一致性。
- **推理频率未报告**：论文未明确模型在真实机器人上的控制频率，这直接影响部署可行性评估。
- **扩展性实验未探索 20,000 小时以上**：虽未见饱和，但无法推断更极端数据规模下的行为。

## 工程启示

- **复现优先核对数据管线**：20,000 小时数据的标注流程（人工分段+Qwen3-VL 指令标注）是性能基础，建议先复现数据格式与动作块定义（T=50），再关注模型架构。
- **深度蒸馏是低成本高收益模块**：仅需一个预训练深度模型（LingBot-Depth）和投影层，即可带来平均 4.28% SR 提升。下游团队可优先集成此模块，尤其针对视觉随机化场景。
- **分布式训练配置是关键瓶颈**：HSDP 分片组设计依赖 MoT 架构的专家划分，若替换 VLM 或动作专家结构，需重新调优分片策略。建议先在小规模（8 GPU）验证吞吐量，再扩展至多节点。
- **数据效率优势值得利用**：在演示预算有限（如每任务 80 条）时，LingBot-VLA 仍优于 π_0.5 全量训练，说明其架构对数据利用更充分。下游场景数据稀缺时，此特性可显著降低采集成本。
- **注意任务级性能波动**：深度蒸馏在部分任务（如 Click Bell）上可能带来负收益，部署前需按任务类型评估，而非仅看平均指标。

## 参考

- LingBot-VLA project page (https://github.com/robbyant/lingbot-vla)

## Overview

This paper introduces LingBot-VLA, a vision-language-action (VLA) foundation model for real-world bimanual manipulation tasks, developed by the Robbyant team. Its core contribution lies in systematically studying the scaling laws of VLA models on 20,000 hours of real robot data, and significantly surpassing existing methods on the GM-100 benchmark through the Mixture-of-Transformers architecture, Flow Matching action modeling, visual depth distillation, and distributed training optimization.

## What It Changes

What this work truly changes is the community's perception of "data scalability" for VLA models. Previously, VLA research mostly remained at the level of validation on small-scale data or simulation environments, lacking rigorous empirical evidence on large-scale real data across multiple platforms under a unified framework. LingBot-VLA demonstrates with 20,000 hours and 9 robot configurations that real robot performance continuously improves with increasing pretraining data scale, with no saturation observed at 20,000 hours—this conclusion directly addresses the core debate of "whether data remains the first-principle bottleneck for VLA."

Second, it changes the standard for VLA training engineering. Existing open-source codebases (such as π_0) are limited by data I/O and communication overhead in multi-node scaling, whereas LingBot-VLA achieves 261 samples per GPU per second on 8 GPUs through FSDP+HSDP hybrid sharding strategies and operator-level optimizations, a 1.5–2.8× speedup. This means large-scale VLA pretraining is no longer a privilege of institutions with supercomputing centers, but a reproducible engineering practice.

## Method Breakdown

### Model Architecture: Mixture-of-Transformers (MoT)
- Integrates a pretrained VLM (Qwen2.5-VL) with a randomly initialized action expert, performing unified sequence modeling across layers through shared self-attention.
- Observation context is defined as O_t = [I_t^1, I_t^2, I_t^3, T_t, s_t], containing three-view images, task instructions, and robot state.
- Action sequence A_t = [a_t, ..., a_{t+T-1}], with action chunk length T=50 during pretraining.

### Action Modeling: Flow Matching
- Training objective is the conditional flow matching loss: L_FM = E ||v_θ(A_{t,s}, O_t, s) - (A_t - ε)||², where intermediate action A_{t,s} = sA_t + (1-s)ε.
- Compared to discrete tokenization or Gaussian mixtures, Flow Matching provides smoother continuous action distribution modeling, suitable for high-precision manipulation.

### Attention Mechanism: Blockwise Causal Masking
- The sequence is divided into three functional blocks: [image, instruction], [state], [action chunk].
- Inter-block causal masking with intra-block bidirectional attention prevents future action information from leaking into observation representations.

### Spatial Awareness: Visual Depth Distillation
- Aligns the VLM's learnable queries Q_t with LingBot-Depth's depth tokens D_t, minimizing L_distill = E|Proj(Q_t) - D_t|.
- This design directly injects geometric information, addressing VLA's inherent deficiency in precise depth reasoning.

### Distributed and Operator Optimization
- FSDP (ZeRO) shards optimizer states, parameters, and gradients; draws on HSDP to construct dedicated "shard groups" for the action expert to reduce communication overhead.
- Mixed precision: float32 for reductions, bfloat16 for storage and communication.
- FlexAttention optimizes sparse attention, torch.compile fuses operators to reduce kernel launch overhead.

### Data Annotation Pipeline
- Human annotators segment multi-view videos into atomic actions, removing static frames.
- Qwen3-VL-235B-A22B is used to automatically annotate task and subtask instructions.

## Key Innovations

1. **First large-scale real-world VLA scaling empirical study**: Systematically demonstrates on 20,000 hours and 9 bimanual configurations that performance continuously improves with data scale without saturation. This provides the strongest evidence to date that "data scale is the core lever for VLA," rather than relying solely on architectural innovation.

2. **Visual depth distillation as a general spatial awareness module**: Depth information is injected into VLM queries via distillation, rather than simple concatenation or input channel modification. This design achieves an average SR improvement of 4.28% over π_0.5 across three real-world platforms, and ablation studies show the gains are more pronounced in randomized scenarios (+9.92%), indicating depth information is critical for generalization.

3. **Distributed training system optimization tailored for VLA**: The HSDP shard group design targets the heterogeneous communication patterns between the action expert and VLM in the MoT architecture, combined with FlexAttention and torch.compile, achieving 1.5–2.8× throughput improvement. This is the first public work that deeply couples system-level optimization with VLA architectural characteristics.

## Experiments and Results

### Real-World Benchmark (GM-100, 25 robots, 100 tasks)
| Method | Avg SR (%) | Avg PS (%) |
|--------|------------|------------|
| WALL-OSS | 4.05 | 10.35 |
| GR00T N1.6 | 7.59 | 15.99 |
| π_0.5 | 13.02 | 27.65 |
| Ours w/o depth | 15.74 | 33.69 |
| Ours w/ depth | **17.30** | **35.41** |

- Depth distillation yields average SR +4.28% and PS +7.76% improvements (relative to π_0.5).
- Largest improvement on Galaxea R1Pro (SR +6.88%), with PS improvement reaching +5.54% on AgileX.

### Simulation Benchmark (RoboTwin 2.0, 50 tasks)
| Setting | π_0.5 SR (%) | Ours w/o depth SR (%) | Ours w/ depth SR (%) |
|---------|-------------|----------------------|---------------------|
| Clean scenes | 82.74 | 86.50 | 88.56 |
| Randomized scenes | 76.76 | 85.34 | 86.68 |

- Absolute SR improvement in randomized scenes reaches 9.92% (w/ depth), far exceeding the 5.82% in clean scenes, indicating depth information significantly enhances robustness to visual perturbations.

### Scaling and Data Efficiency
- Increasing pretraining data from 3,000 to 20,000 hours yields continuous downstream success rate improvements with no saturation observed.
- Under a limited budget of only 80 demonstrations per task, LingBot-VLA outperforms π_0.5 trained on the full 130 demonstrations in both SR and PS, indicating higher data efficiency.

### Training Throughput
- On 8-GPU setups, 261 samples per GPU per second, a 1.5–2.8× speedup over existing VLA codebases.

## Boundaries and Limitations

- **Limited platform coverage**: Only bimanual, fixed-base/waist configurations were evaluated; single-arm or mobile robot platforms were not covered, nor were non-desktop scenarios.
- **Marginal gains from depth distillation are unstable on some tasks**: In the appendix, for tasks like Click Bell, Ours w/ depth achieves only 36% SR in randomized scenes, lower than π_0.5's 66%, indicating depth information is not beneficial across all tasks.
- **Data annotation relies on manual segmentation**: Video segmentation is performed by human annotators. Although instruction annotation is automated, atomic action segmentation still involves subjectivity, potentially affecting cross-platform consistency.
- **Inference frequency not reported**: The paper does not specify the control frequency on real robots, which directly affects deployment feasibility assessment.
- **Scaling experiments do not explore beyond 20,000 hours**: Although no saturation is observed, behavior at more extreme data scales cannot be inferred.

## Engineering Insights

- **Prioritize reproducing the data pipeline**: The annotation pipeline for 20,000 hours of data (manual segmentation + Qwen3-VL instruction annotation) is the foundation of performance. It is recommended to first reproduce the data format and action chunk definition (T=50) before focusing on the model architecture.
- **Depth distillation is a low-cost, high-reward module**: Only a pretrained depth model (LingBot-Depth) and a projection layer are needed to achieve an average 4.28% SR improvement. Downstream teams can prioritize integrating this module, especially for visually randomized scenarios.
- **Distributed training configuration is a key bottleneck**: The HSDP shard group design depends on the expert partitioning of the MoT architecture. If the VLM or action expert structure is replaced, the sharding strategy needs to be re-tuned. It is recommended to first validate throughput at small scale (8 GPUs) before scaling to multiple nodes.
- **Data efficiency advantage is worth exploiting**: With limited demonstration budgets (e.g., 80 per task), LingBot-VLA still outperforms π_0.5 trained on full data, indicating its architecture utilizes data more effectively. This characteristic can significantly reduce collection costs in downstream scenarios with scarce data.
- **Watch for task-level performance fluctuations**: Depth distillation may yield negative returns on certain tasks (e.g., Click Bell). Deployment decisions should be evaluated per task type rather than relying solely on average metrics.

## 개요

본 논문은 Robbyant 팀이 개발한 실제 세계 이중 팔 조작 작업을 위한 비전-언어-동작(VLA) 기반 모델인 LingBot-VLA를 제안합니다. 핵심 기여는 20,000시간의 실제 로봇 데이터에 대한 VLA 모델의 확장 법칙을 체계적으로 연구하고, Mixture-of-Transformers 아키텍처, Flow Matching 동작 모델링, 시각적 깊이 증류 및 분산 훈련 최적화를 통해 GM-100 벤치마크에서 기존 방법을 크게 능가한다는 점입니다.

## 그것이 바꾸는 것

이 작업이 진정으로 바꾸는 것은 VLA 모델의 "데이터 확장성"에 대한 커뮤니티의 인식 방식입니다. 이전의 VLA 연구는 대부분 소규모 데이터나 시뮬레이션 환경에서의 검증에 머물렀으며, 통합된 프레임워크에서 다중 플랫폼, 대규모 실제 데이터에 대한 엄격한 실증이 부족했습니다. LingBot-VLA는 20,000시간, 9가지 로봇 구성의 데이터를 통해 실제 로봇 성능이 사전 훈련 데이터 규모가 증가함에 따라 지속적으로 향상되고 20,000시간에서도 포화되지 않음을 증명했습니다. 이 결론은 "데이터가 여전히 VLA의 일차적 병목인가"라는 핵심 논쟁에 직접적으로 답합니다.

둘째, VLA 훈련 엔지니어링의 표준을 변화시킵니다. 기존 오픈소스 코드베이스(예: π_0 등)는 다중 노드 확장 시 데이터 I/O 및 통신 오버헤드에 제한을 받지만, LingBot-VLA는 FSDP+HSDP 혼합 분할 전략과 연산자 수준 최적화를 통해 8-GPU 처리량을 GPU당 초당 261개 샘플로 향상시켜 1.5~2.8배 가속화했습니다. 이는 대규모 VLA 사전 훈련이 더 이상 소수의 슈퍼컴퓨팅 센터를 보유한 기관만의 특권이 아니라 재현 가능한 엔지니어링 실천이 되었음을 의미합니다.

## 방법 분석

### 모델 아키텍처: Mixture-of-Transformers (MoT)
- 사전 훈련된 VLM(Qwen2.5-VL)과 무작위 초기화된 동작 전문가(action expert)를 통합하고, 공유 자기 주의를 통해 레이어 간 통합 시퀀스 모델링을 수행합니다.
- 관측 컨텍스트는 O_t = [I_t^1, I_t^2, I_t^3, T_t, s_t]로 정의되며, 세 가지 시점의 이미지, 작업 지시문 및 로봇 상태를 포함합니다.
- 동작 시퀀스 A_t = [a_t, ..., a_{t+T-1}], 사전 훈련 단계의 동작 블록 길이 T=50.

### 동작 모델링: Flow Matching
- 훈련 목표는 조건부 흐름 매칭 손실: L_FM = E ||v_θ(A_{t,s}, O_t, s) - (A_t - ε)||², 여기서 중간 동작 A_{t,s} = sA_t + (1-s)ε.
- 이산 토큰화나 가우시안 혼합에 비해 Flow Matching은 더 부드러운 연속 동작 분포 모델링을 제공하여 고정밀 조작에 적합합니다.

### 주의 메커니즘: 블록 인과 마스크
- 시퀀스는 [이미지, 지시문], [상태], [동작 블록]의 세 가지 기능 블록으로 나뉩니다.
- 블록 간 인과 마스크, 블록 내 양방향 주의를 통해 미래 동작 정보가 관측 표현으로 누출되는 것을 방지합니다.

### 공간 인식: 시각적 깊이 증류
- VLM의 학습 가능한 쿼리 Q_t와 LingBot-Depth의 깊이 토큰 D_t를 정렬하여 L_distill = E|Proj(Q_t) - D_t|를 최소화합니다.
- 이 설계는 기하학적 정보를 직접 주입하여 정밀한 깊이 추론에서 VLA의 고유한 결함을 해결합니다.

### 분산 및 연산자 최적화
- FSDP(ZeRO)는 옵티마이저 상태, 파라미터 및 그래디언트를 분할합니다. HSDP를 참고하여 동작 전문가를 위한 특정 "분할 그룹"을 구성하여 통신 오버헤드를 줄입니다.
- 혼합 정밀도: 리덕션은 float32, 저장 및 통신은 bfloat16.
- FlexAttention으로 희소 주의를 최적화하고, torch.compile 연산자 융합으로 커널 시작 오버헤드를 줄입니다.

### 데이터 주석 파이프라인
- 사람이 다중 시점 비디오를 원자 동작 단위로 분할하고 정적 프레임을 제거합니다.
- Qwen3-VL-235B-A22B를 사용하여 작업 및 하위 작업 지시문을 자동으로 주석 처리합니다.

## 핵심 혁신

1. **최초의 대규모 실제 세계 VLA 확장성 실증**: 20,000시간, 9가지 이중 팔 구성 데이터에서 성능이 데이터 규모에 따라 지속적으로 증가하고 포화되지 않음을 체계적으로 보여줍니다. 이는 "데이터 규모가 VLA의 핵심 레버리지"라는 주장에 대해 아키텍처 혁신만이 아닌 가장 강력한 증거를 제공합니다.

2. **범용 공간 인식 모듈로서의 시각적 깊이 증류**: 깊이 정보를 단순히 연결하거나 입력 채널을 수정하는 대신 증류 방식으로 VLM 쿼리에 주입합니다. 이 설계는 실제 세계 세 플랫폼에서 평균 SR 4.28% 향상(π_0.5 대비)을 보였으며, 절제 실험에서 무작위화된 시나리오에서 그 이점이 더 두드러짐(+9.92%)을 보여 깊이 정보가 일반화에 중요함을 시사합니다.

3. **VLA를 위한 분산 훈련 시스템 최적화**: HSDP 분할 그룹 설계는 MoT 아키텍처에서 동작 전문가와 VLM의 이기종 통신 패턴을 대상으로 하며, FlexAttention 및 torch.compile과 결합하여 1.5~2.8배 처리량 향상을 달성했습니다. 이는 시스템 수준 최적화와 VLA 아키텍처 특성을 깊이 결합한 최초의 공개 작업입니다.

## 실험 및 결과

### 실제 세계 벤치마크 (GM-100, 25대 로봇, 100개 작업)
| 방법 | 평균 SR (%) | 평균 PS (%) |
|------|------------|------------|
| WALL-OSS | 4.05 | 10.35 |
| GR00T N1.6 | 7.59 | 15.99 |
| π_0.5 | 13.02 | 27.65 |
| Ours w/o depth | 15.74 | 33.69 |
| Ours w/ depth | **17.30** | **35.41** |

- 깊이 증류는 평균 SR +4.28%, PS +7.76% 향상(π_0.5 대비).
- Galaxea R1Pro에서 가장 큰 향상(SR +6.88%), AgileX에서 PS 향상 +5.54%.

### 시뮬레이션 벤치마크 (RoboTwin 2.0, 50개 작업)
| 설정 | π_0.5 SR (%) | Ours w/o depth SR (%) | Ours w/ depth SR (%) |
|------|-------------|----------------------|---------------------|
| 깨끗한 시나리오 | 82.74 | 86.50 | 88.56 |
| 무작위화된 시나리오 | 76.76 | 85.34 | 86.68 |

- 무작위화된 시나리오에서 절대 SR 향상 9.92%(w/ depth)로 깨끗한 시나리오의 5.82%를 크게 능가하며, 깊이 정보가 시각적 교란에 대한 견고성을 크게 강화함을 보여줍니다.

### 확장성 및 데이터 효율성
- 사전 훈련 데이터가 3,000시간에서 20,000시간으로 증가함에 따라 다운스트림 성공률이 지속적으로 향상되며 포화가 관찰되지 않습니다.
- 작업당 80개의 데모만 있는 제한된 예산에서 LingBot-VLA의 SR과 PS는 모두 전체 130개 데모를 사용하는 π_0.5보다 우수하여 모델의 데이터 효율성이 더 높음을 보여줍니다.

### 훈련 처리량
- 8-GPU 설정에서 GPU당 초당 261개 샘플로 기존 VLA 코드베이스 대비 1.5~2.8배 가속.

## 경계 및 한계

- **플랫폼 범위 제한**: 이중 팔, 고정 섀시/허리 구성만 평가했으며, 단일 팔 또는 이동 로봇 플랫폼은 포함하지 않았고 비데스크탑 시나리오도 평가하지 않았습니다.
- **깊이 증류의 한계 수익이 일부 작업에서 불안정**: 부록의 Click Bell 작업과 같이 Ours w/ depth는 무작위화된 시나리오에서 SR이 36%에 불과하여 π_0.5의 66%보다 낮습니다. 이는 깊이 정보가 모든 작업에 유익한 것은 아님을 보여줍니다.
- **데이터 주석이 수동 분할에 의존**: 비디오 분할은 수동 주석자가 수행하며, 지시문 주석은 자동화되었지만 원자 동작 분할에는 여전히 주관성이 존재하여 플랫폼 간 일관성에 영향을 미칠 수 있습니다.
- **추론 빈도 미보고**: 논문은 실제 로봇에서 모델의 제어 빈도를 명확히 하지 않아 배포 가능성 평가에 직접적인 영향을 미칩니다.
- **확장성 실험은 20,000시간 이상을 탐구하지 않음**: 포화가 관찰되지 않았지만 더 극단적인 데이터 규모에서의 동작을 추론할 수 없습니다.

## 엔지니어링 시사점

- **재현 시 데이터 파이프라인 우선 확인**: 20,000시간 데이터의 주석 프로세스(수동 분할 + Qwen3-VL 지시문 주석)는 성능의 기반이므로, 모델 아키텍처보다 데이터 형식과 동작 블록 정의(T=50)를 먼저 재현하는 것이 좋습니다.
- **깊이 증류는 저비용 고수익 모듈**: 사전 훈련된 깊이 모델(LingBot-Depth)과 투영 레이어만 있으면 평균 4.28% SR 향상을 얻을 수 있습니다. 다운스트림 팀은 특히 시각적 무작위화 시나리오에서 이 모듈을 우선 통합할 수 있습니다.
- **분산 훈련 구성이 핵심 병목**: HSDP 분할 그룹 설계는 MoT 아키텍처의 전문가 분할에 의존하므로, VLM이나 동작 전문가 구조를 교체하면 분할 전략을 다시 튜닝해야 합니다. 소규모(8 GPU)에서 처리량을 먼저 검증한 후 다중 노드로 확장하는 것이 좋습니다.
- **데이터 효율성 이점 활용**: 데모 예산이 제한된 경우(예: 작업당 80개)에도 LingBot-VLA는 π_0.5 전체 훈련보다 우수하여 아키텍처가 데이터를 더 효율적으로 활용함을 보여줍니다. 다운스트림 시나리오에서 데이터가 부족할 때 이 특성은 수집 비용을 크게 줄일 수 있습니다.
- **작업 수준 성능 변동 주의**: 깊이 증류는 일부 작업(예: Click Bell)에서 부정적 수익을 가져올 수 있으므로, 평균 지표만 보지 말고 배포 전에 작업 유형별로 평가해야 합니다.
