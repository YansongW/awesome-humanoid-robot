---
$id: ent_paper_neto_high_level_robot_programming_b_2013
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'High-level robot programming based on CAD: dealing with unpredictable environments'
  zh: 基于CAD的高级机器人编程：应对不可预测环境
  ko: 'CAD 기반 고수준 로봇 프로그래밍: 예측 불가능한 환경 대응'
summary:
  en: This paper presents a CAD-based human-robot interface that lets non-expert users program industrial robots by drawing
    paths in Autodesk Inventor, and compensates for environmental uncertainty using online feedback from a laser camera and
    a force/torque sensor.
  zh: 本文提出一种基于CAD的人机交互界面，使非专业用户能通过Autodesk Inventor绘制路径来编程工业机器人，并利用激光相机和力/力矩传感器的在线反馈补偿环境不确定性。核心贡献在于直接从通用CAD软件生成机器人程序，无需专业机器人编程技能，且成本低、设置时间短。
  ko: 본 논문은 Autodesk Inventor에서 경로를 그려 비전문 사용자가 산업용 로봇을 프로그래밍할 수 있는 CAD 기반 인간-로봇 인터페이스를 제안하며, 레이저 카메라와 힘/토크 센서의 온라인 피드백을 통해
    환경 불확실성을 보상한다.
domains:
- 08_software_middleware
- 11_applications_markets
- 02_components
layers:
- intelligence
- upstream
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- cad_based_programming
- robot_programming
- teach_by_demonstration
- sensory_feedback
- online_path_correction
- seam_tracking
- force_controlled_profile_following
- industrial_robot
- autodesk_inventor
- laser_camera
- force_torque_sensor
- human_robot_interface
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/1309.2086v1. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_001
  type: paper
  title: 'High-level robot programming based on CAD: dealing with unpredictable environments'
  url: https://arxiv.org/abs/1309.2086
  date: '2013'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---


## 概述

本文提出一种基于通用3D CAD软件（Autodesk Inventor）的高层机器人编程方法，用户直接在CAD环境中绘制路径并定义工具姿态，系统自动生成机器人程序，并通过外部传感器（激光相机、力/扭矩传感器）在线调整路径以应对不可预测环境。作者来自葡萄牙科英布拉大学（Pedro Neto等），核心贡献在于将CAD集成、程序自动生成与感官反馈闭环结合，为中小企业提供低成本、易用的机器人编程方案。

## 它改变了什么

传统机器人编程依赖示教器，耗时且需专业技术，中小企业难以负担熟练操作员。现有CAD-based机器人编程研究多为局部方案，缺乏从CAD到机器人程序的“全局”自动化，且未充分处理动态环境中的不确定性（如模型误差、校准偏差、外来物体）。本文真正改变的是：将编程入口从“机器人坐标系下的示教”提升到“CAD装配模型中的路径绘制”，并首次在CAD集成方案中系统引入外部传感器（激光相机、力/扭矩）进行在线路径修正，使非专家用户能以“画图”方式编程，同时让机器人具备应对真实环境偏差的能力。这不同于商业CAR软件（需独立CAM/CAR平台），也不同于纯离线编程（无法处理在线扰动）。

## 方法拆解

### 用户流程
1. 在Autodesk Inventor中“绘制”期望路径（直线、样条、圆弧），沿路径放置简化工具模型以定义末端执行器姿态（位置+方向）。
2. 通过Inventor API提取路径点坐标及每个零件模型的变换矩阵（相对于装配原点{U}）。
3. 将提取信息转换为机器人程序（运动指令、参考框架、工具参数）。

### 坐标变换
- 校准定义框架{B}相对于{U}：`BCT = BUT · UCT`（公式1）。
- 计算`BUT`时，旋转矩阵`BUR = UBT^T`，位置`BUorgP = -UBT^T · UBorgP`（公式6、7）。
- 关键：用户需在CAD中定义框架，并在真实场景中教导机器人框架姿态（离线到在线映射）。

### 插值方法（针对风险区域）
- 识别风险路径（末端执行器方向突变处）。
- 等间距离散化，计算每个插值点方向。
- **位置插值**：方向向量`W = Pj+1 - Pj`，速度分布`vi(k) = (Wi/|W|) · v(k)`（公式10），采样宽度`Δt`，插值位置`r(k) = r(0) + vi(k) · k · Δt`（公式13）。
- **方向插值**：四元数球面线性插值（Slerp），公式14、15。

### 程序生成
- 两阶段：先定义和参数化位置/方向、参考框架、工具（公式16），再生成程序主体，根据CAD线条类型选择运动指令（线性、关节、圆弧或样条）。

### 感官反馈
- 激光相机（焊接）和力/扭矩传感器（轮廓跟踪）提供在线路径调整。
- 采用闭环位置控制或力控制（Fuzzy-PI和PI推理）。

## 关键创新

1. **CAD即编程界面**：用户无需学习机器人编程语言，直接在CAD中“画路径”即可生成程序，学习曲线短，且CAD建模与编程在同一平台，避免CAM/CAR软件的高成本和转换损失。
2. **全局解决方案**：不同于现有研究仅处理局部环节，本文覆盖从CAD路径绘制、坐标变换、插值平滑到程序生成、传感器在线调整的完整链路，且基于通用CAD包（Inventor）而非专用软件。
3. **在线感官反馈集成**：将激光相机和力/扭矩传感器纳入CAD-based编程框架，使离线生成的路径能实时修正，应对校准误差、模型不准确和外来物体等不可预测因素——这是现有CAD-based研究普遍缺失的。

## 实验与结果

| 实验 | 机器人/控制器 | 传感器 | 采样率 | 关键参数 | 结果 |
|------|--------------|--------|--------|----------|------|
| 焊缝跟踪 | ABB IRB 2400 / S4C+/M2000 | DIGI-I/S激光相机（串口） | 5 Hz | 速度10 mm/s，距离0-80 mm | 校正路径0-2 mm（理想情况为灰色，虚线为偏离） |
| 轮廓跟踪 | Motoman HP6 / NX100 | JR3六自由度F/T传感器（以太网100 Mbps） | 20 Hz | 力设定点-15 N | 轴向力-30至0 N，位置-5至10 mm（0-200秒），Fuzzy-PI和PI控制器 |

- 实验1中，机器人路径调整频率限制为5 Hz，较高焊接速度下校正不平滑；机器人分辨率0.01 mm使调整更突兀。
- 实验2中，力波动源于表面粗糙度和F/T数据噪声。

## 边界与局限

- 系统设计用于特定技术应用（焊接、轮廓跟踪），非通用解决方案。
- 实验1中5 Hz调整频率限制导致高速焊接时校正不平滑；0.01 mm分辨率使调整过程突兀。
- 外部传感器在线调整可能遇到：传感器与工作空间碰撞、机器人臂发送到工作区外、运动学奇异、过程参数选择不当、传感器与控制器通信延迟。
- 操作员需确保工件在机器人工作区内、无碰撞、识别运动学奇异。
- 任务失败时，中止后重启系统可能复杂（如弧焊需将焊枪放回停止点）。
- 论文未明确数据量、训练配置（无机器学习），也未提及多传感器融合策略或复杂曲面（非直线/圆弧/样条）的处理。

## 工程启示

- **复现优先核对**：坐标变换公式（公式1、6、7）是离线到在线映射的核心，校准精度直接影响路径准确性；实验中有意进行粗略校准以模拟不可预测环境，实际应用中应评估校准误差容忍度。
- **插值参数敏感**：风险区域识别和Slerp插值（公式14、15）对方向突变处理至关重要，需根据实际路径曲率调整离散化宽度`Δt`和速度分布`vi(k)`，否则可能产生路径抖动。
- **传感器集成是最大坑**：5 Hz调整频率（实验1）和20 Hz（实验2）差异显著，高频应用需考虑通信延迟和控制器带宽；力控制中Fuzzy-PI参数需针对表面粗糙度调优，否则力波动明显。
- **下游团队注意**：系统依赖Autodesk Inventor API（VB/C#/C++），需独立应用程序提取CAD信息（Autodesk Apprentice Server显示模型）；通信组件（PcRob、MotomanLib、JR3PCI）均为特定硬件绑定，更换机器人或传感器需重写接口。
- **安全冗余**：操作员必须人工确认工作区、碰撞和奇异点，系统无自动避障或奇异规避，实际部署需增加安全逻辑。

## 参考
- http://arxiv.org/abs/1309.2086v1

## Overview

This paper proposes a high-level robot programming method based on a general-purpose 3D CAD software (Autodesk Inventor), where users directly draw paths and define tool poses in the CAD environment, and the system automatically generates robot programs, with online path adjustments via external sensors (laser camera, force/torque sensor) to handle unpredictable environments. The authors are from the University of Coimbra, Portugal (Pedro Neto et al.), and the core contribution lies in combining CAD integration, automatic program generation, and closed-loop sensory feedback, offering a low-cost, easy-to-use robot programming solution for small and medium-sized enterprises.

## What It Changes

Traditional robot programming relies on teach pendants, which is time-consuming and requires specialized skills, making it difficult for SMEs to afford skilled operators. Existing CAD-based robot programming research is mostly partial solutions, lacking "global" automation from CAD to robot programs, and insufficiently addressing uncertainties in dynamic environments (e.g., model errors, calibration deviations, foreign objects). What this paper truly changes is: elevating the programming entry point from "teaching in the robot coordinate system" to "path drawing in the CAD assembly model," and, for the first time, systematically introducing external sensors (laser camera, force/torque) into a CAD-integrated framework for online path correction, enabling non-expert users to program via "drawing" while equipping robots to handle real-world environmental deviations. This differs from commercial CAR software (which requires standalone CAM/CAR platforms) and from pure offline programming (which cannot handle online disturbances).

## Method Breakdown

### User Workflow
1. "Draw" the desired path (lines, splines, arcs) in Autodesk Inventor, and place simplified tool models along the path to define end-effector poses (position + orientation).
2. Extract path point coordinates and the transformation matrix of each part model (relative to the assembly origin {U}) via the Inventor API.
3. Convert the extracted information into robot programs (motion commands, reference frames, tool parameters).

### Coordinate Transformations
- Calibration defines frame {B} relative to {U}: `BCT = BUT · UCT` (Equation 1).
- When computing `BUT`, the rotation matrix `BUR = UBT^T`, and the position `BUorgP = -UBT^T · UBorgP` (Equations 6, 7).
- Key point: users must define frames in CAD and teach the frame pose to the robot in the real scene (offline-to-online mapping).

### Interpolation Methods (for Risk Zones)
- Identify risky paths (where end-effector orientation changes abruptly).
- Discretize with equal spacing and compute the orientation at each interpolation point.
- **Position interpolation**: direction vector `W = Pj+1 - Pj`, velocity distribution `vi(k) = (Wi/|W|) · v(k)` (Equation 10), sampling width `Δt`, interpolated position `r(k) = r(0) + vi(k) · k · Δt` (Equation 13).
- **Orientation interpolation**: quaternion spherical linear interpolation (Slerp), Equations 14, 15.

### Program Generation
- Two stages: first define and parameterize positions/orientations, reference frames, and tools (Equation 16), then generate the program body, selecting motion commands (linear, joint, circular, or spline) based on the CAD line type.

### Sensory Feedback
- Laser camera (welding) and force/torque sensor (contour tracking) provide online path adjustments.
- Closed-loop position control or force control (Fuzzy-PI and PI inference) is employed.

## Key Innovations

1. **CAD as the Programming Interface**: Users do not need to learn robot programming languages; they simply "draw paths" in CAD to generate programs, resulting in a short learning curve, and CAD modeling and programming occur on the same platform, avoiding the high costs and conversion losses of CAM/CAR software.
2. **Global Solution**: Unlike existing research that addresses only partial aspects, this paper covers the complete chain from CAD path drawing, coordinate transformations, interpolation smoothing, to program generation and sensor-based online adjustment, based on a general-purpose CAD package (Inventor) rather than specialized software.
3. **Online Sensory Feedback Integration**: Laser camera and force/torque sensors are incorporated into the CAD-based programming framework, allowing offline-generated paths to be corrected in real time to handle unpredictable factors such as calibration errors, model inaccuracies, and foreign objects—a capability generally missing in existing CAD-based research.

## Experiments and Results

| Experiment | Robot/Controller | Sensor | Sampling Rate | Key Parameters | Results |
|------------|------------------|--------|----------------|----------------|---------|
| Weld seam tracking | ABB IRB 2400 / S4C+/M2000 | DIGI-I/S laser camera (serial port) | 5 Hz | Speed 10 mm/s, distance 0-80 mm | Corrected path 0-2 mm (ideal case in gray, dashed line shows deviation) |
| Contour tracking | Motoman HP6 / NX100 | JR3 6-DOF F/T sensor (Ethernet 100 Mbps) | 20 Hz | Force setpoint -15 N | Axial force -30 to 0 N, position -5 to 10 mm (0-200 s), Fuzzy-PI and PI controllers |

- In Experiment 1, the robot path adjustment frequency was limited to 5 Hz, causing non-smooth corrections at higher welding speeds; the robot resolution of 0.01 mm made adjustments more abrupt.
- In Experiment 2, force fluctuations arose from surface roughness and F/T data noise.

## Boundaries and Limitations

- The system is designed for specific technical applications (welding, contour tracking), not a general-purpose solution.
- In Experiment 1, the 5 Hz adjustment frequency limit led to non-smooth corrections at high welding speeds; the 0.01 mm resolution made the adjustment process abrupt.
- External sensor online adjustment may encounter: sensor collisions with the workspace, robot arm sent outside the work area, kinematic singularities, improper process parameter selection, and communication delays between sensor and controller.
- Operators must ensure the workpiece is within the robot's work area, collision-free, and identify kinematic singularities.
- On task failure, restarting the system after aborting may be complex (e.g., arc welding requires returning the torch to the stop point).
- The paper does not specify data volume, training configurations (no machine learning), nor does it mention multi-sensor fusion strategies or handling of complex surfaces (non-linear/arc/spline).

## Engineering Insights

- **Prioritize Verification for Reproduction**: The coordinate transformation formulas (Equations 1, 6, 7) are central to offline-to-online mapping; calibration accuracy directly affects path precision; in experiments, deliberate rough calibration was used to simulate unpredictable environments, so in practice, tolerance to calibration errors should be assessed.
- **Interpolation Parameters Are Sensitive**: Risk zone identification and Slerp interpolation (Equations 14, 15) are critical for handling abrupt orientation changes; the discretization width `Δt` and velocity distribution `vi(k)` must be adjusted based on actual path curvature, otherwise path jitter may occur.
- **Sensor Integration Is the Biggest Pitfall**: The difference between 5 Hz adjustment frequency (Experiment 1) and 20 Hz (Experiment 2) is significant; high-frequency applications require consideration of communication delays and controller bandwidth; in force control, Fuzzy-PI parameters need tuning for surface roughness, otherwise force fluctuations are pronounced.
- **Downstream Teams Should Note**: The system relies on the Autodesk Inventor API (VB/C#/C++), requiring a standalone application to extract CAD information (Autodesk Apprentice Server displays the model); communication components (PcRob, MotomanLib, JR3PCI) are all hardware-specific, so changing robots or sensors requires rewriting interfaces.
- **Safety Redundancy**: Operators must manually confirm the work area, collisions, and singularities; the system has no automatic obstacle avoidance or singularity avoidance, so additional safety logic is needed for actual deployment.

## 개요

본 논문은 범용 3D CAD 소프트웨어(Autodesk Inventor)를 기반으로 한 고수준 로봇 프로그래밍 방법을 제안한다. 사용자는 CAD 환경에서 직접 경로를 그리고 도구 자세를 정의하며, 시스템은 자동으로 로봇 프로그램을 생성하고, 외부 센서(레이저 카메라, 힘/토크 센서)를 통해 예측 불가능한 환경에 대응하여 경로를 온라인으로 조정한다. 저자는 포르투갈 코임브라 대학교(Pedro Neto 등) 소속이며, 핵심 기여는 CAD 통합, 프로그램 자동 생성, 감각 피드백 폐루프를 결합하여 중소기업에 저비용·사용 용이한 로봇 프로그래밍 솔루션을 제공하는 것이다.

## 그것이 바꾸는 것

전통적인 로봇 프로그래밍은 티치 펜던트에 의존하며, 시간이 많이 소요되고 전문 기술이 필요하여 중소기업이 숙련된 작업자를 고용하기 어렵다. 기존 CAD 기반 로봇 프로그래밍 연구는 대부분 부분적 솔루션에 그치며, CAD에서 로봇 프로그램으로의 '전체적' 자동화가 부족하고, 동적 환경의 불확실성(모델 오차, 캘리브레이션 편차, 외부 물체)을 충분히 처리하지 못한다. 본 논문이 실제로 바꾸는 것은 프로그래밍 진입점을 '로봇 좌표계에서의 티칭'에서 'CAD 어셈블리 모델에서의 경로 그리기'로 끌어올리고, CAD 통합 솔루션에 외부 센서(레이저 카메라, 힘/토크)를 체계적으로 도입하여 온라인 경로 수정을 가능하게 하여, 비전문가 사용자가 '그림 그리기' 방식으로 프로그래밍하면서도 로봇이 실제 환경 편차에 대응할 수 있게 한다. 이는 상용 CAR 소프트웨어(독립 CAM/CAR 플랫폼 필요)와도 다르고, 온라인 교란을 처리할 수 없는 순수 오프라인 프로그래밍과도 다르다.

## 방법 분해

### 사용자 흐름
1. Autodesk Inventor에서 원하는 경로(직선, 스플라인, 원호)를 '그린다'. 경로를 따라 단순화된 도구 모델을 배치하여 엔드 이펙터 자세(위치+방향)를 정의한다.
2. Inventor API를 통해 경로 점 좌표와 각 부품 모델의 변환 행렬(어셈블리 원점 {U} 기준)을 추출한다.
3. 추출된 정보를 로봇 프로그램(모션 명령, 기준 프레임, 도구 파라미터)으로 변환한다.

### 좌표 변환
- 캘리브레이션 정의 프레임 {B}를 {U} 기준으로: `BCT = BUT · UCT` (수식 1).
- `BUT` 계산 시, 회전 행렬 `BUR = UBT^T`, 위치 `BUorgP = -UBT^T · UBorgP` (수식 6, 7).
- 핵심: 사용자는 CAD에서 프레임을 정의하고, 실제 현장에서 로봇에 프레임 자세를 티칭해야 한다(오프라인에서 온라인으로의 매핑).

### 보간 방법(위험 영역 대상)
- 위험 경로(엔드 이펙터 방향 급변 지점)를 식별한다.
- 등간격 이산화하고, 각 보간 점의 방향을 계산한다.
- **위치 보간**: 방향 벡터 `W = Pj+1 - Pj`, 속도 분포 `vi(k) = (Wi/|W|) · v(k)` (수식 10), 샘플링 폭 `Δt`, 보간 위치 `r(k) = r(0) + vi(k) · k · Δt` (수식 13).
- **방향 보간**: 쿼터니언 구면 선형 보간(Slerp), 수식 14, 15.

### 프로그램 생성
- 두 단계: 먼저 위치/방향, 기준 프레임, 도구를 정의하고 파라미터화(수식 16)한 후, 프로그램 본문을 생성하며 CAD 선 유형에 따라 모션 명령(선형, 관절, 원호 또는 스플라인)을 선택한다.

### 감각 피드백
- 레이저 카메라(용접)와 힘/토크 센서(윤곽 추적)가 온라인 경로 조정을 제공한다.
- 폐루프 위치 제어 또는 힘 제어(Fuzzy-PI 및 PI 추론)를 사용한다.

## 핵심 혁신

1. **CAD를 프로그래밍 인터페이스로**: 사용자는 로봇 프로그래밍 언어를 배울 필요 없이 CAD에서 직접 '경로를 그려' 프로그램을 생성할 수 있으며, 학습 곡선이 짧고 CAD 모델링과 프로그래밍이 동일 플랫폼에서 이루어져 CAM/CAR 소프트웨어의 고비용과 변환 손실을 피한다.
2. **전체적 솔루션**: 기존 연구가 부분적 단계만 다루는 것과 달리, 본 논문은 CAD 경로 그리기, 좌표 변환, 보간 평활화에서 프로그램 생성, 센서 온라인 조정까지의 완전한 체인을 다루며, 전용 소프트웨어가 아닌 범용 CAD 패키지(Inventor)를 기반으로 한다.
3. **온라인 감각 피드백 통합**: 레이저 카메라와 힘/토크 센서를 CAD 기반 프로그래밍 프레임워크에 포함시켜, 오프라인 생성 경로가 실시간으로 수정되어 캘리브레이션 오차, 모델 부정확성, 외부 물체 등 예측 불가능한 요소에 대응할 수 있게 한다——이는 기존 CAD 기반 연구에서 일반적으로 누락된 부분이다.

## 실험 및 결과

| 실험 | 로봇/컨트롤러 | 센서 | 샘플링 주기 | 핵심 파라미터 | 결과 |
|------|--------------|--------|--------|----------|------|
| 용접선 추적 | ABB IRB 2400 / S4C+/M2000 | DIGI-I/S 레이저 카메라(시리얼) | 5 Hz | 속도 10 mm/s, 거리 0-80 mm | 보정 경로 0-2 mm(이상적 경우는 회색, 점선은 이탈) |
| 윤곽 추적 | Motoman HP6 / NX100 | JR3 6자유도 F/T 센서(이더넷 100 Mbps) | 20 Hz | 힘 설정점 -15 N | 축 방향 힘 -30~0 N, 위치 -5~10 mm(0-200초), Fuzzy-PI 및 PI 컨트롤러 |

- 실험 1에서 로봇 경로 조정 주파수는 5 Hz로 제한되어, 높은 용접 속도에서 보정이 매끄럽지 않다. 로봇 분해능 0.01 mm로 조정이 더욱 급격해진다.
- 실험 2에서 힘 변동은 표면 거칠기와 F/T 데이터 노이즈에서 비롯된다.

## 경계 및 한계

- 시스템은 특정 기술 응용(용접, 윤곽 추적)을 위해 설계되었으며, 범용 솔루션이 아니다.
- 실험 1의 5 Hz 조정 주파수 제한으로 고속 용접 시 보정이 매끄럽지 않다. 0.01 mm 분해능으로 조정 과정이 급격해진다.
- 외부 센서 온라인 조정은 다음 문제에 직면할 수 있다: 센서와 작업 공간의 충돌, 로봇 팔이 작업 영역 밖으로 이동, 운동학적 특이점, 공정 파라미터 선택 부적절, 센서와 컨트롤러 간 통신 지연.
- 작업자는 공작물이 로봇 작업 영역 내에 있고, 충돌이 없으며, 운동학적 특이점을 식별했는지 확인해야 한다.
- 작업 실패 시, 중단 후 시스템 재시작이 복잡할 수 있다(예: 아크 용접의 경우 용접 토치를 중단 지점으로 되돌려야 함).
- 논문은 데이터 양, 훈련 구성(머신러닝 없음)을 명시하지 않았으며, 다중 센서 융합 전략이나 복잡한 곡면(직선/원호/스플라인이 아닌) 처리도 언급하지 않았다.

## 공학적 시사점

- **재현 시 우선 확인**: 좌표 변환 수식(수식 1, 6, 7)은 오프라인에서 온라인으로의 매핑 핵심이며, 캘리브레이션 정밀도가 경로 정확도에 직접 영향을 미친다. 실험에서는 예측 불가능한 환경을 시뮬레이션하기 위해 의도적으로 대략적인 캘리브레이션을 수행했으며, 실제 응용에서는 캘리브레이션 오차 허용 범위를 평가해야 한다.
- **보간 파라미터 민감성**: 위험 영역 식별과 Slerp 보간(수식 14, 15)은 방향 급변 처리에 중요하며, 실제 경로 곡률에 따라 이산화 폭 `Δt`와 속도 분포 `vi(k)`를 조정해야 한다. 그렇지 않으면 경로 떨림이 발생할 수 있다.
- **센서 통합이 가장 큰 함정**: 5 Hz 조정 주파수(실험 1)와 20 Hz(실험 2)의 차이가 크며, 고주파 응용에서는 통신 지연과 컨트롤러 대역폭을 고려해야 한다. 힘 제어에서 Fuzzy-PI 파라미터는 표면 거칠기에 맞춰 튜닝해야 하며, 그렇지 않으면 힘 변동이 뚜렷하다.
- **하류 팀 주의**: 시스템은 Autodesk Inventor API(VB/C#/C++)에 의존하며, CAD 정보 추출을 위해 독립 응용 프로그램(Autodesk Apprentice Server가 모델 표시)이 필요하다. 통신 구성 요소(PcRob, MotomanLib, JR3PCI)는 모두 특정 하드웨어에 바인딩되어 있어, 로봇이나 센서를 교체하면 인터페이스를 다시 작성해야 한다.
- **안전 중복**: 작업자는 작업 영역, 충돌, 특이점을 수동으로 확인해야 하며, 시스템에는 자동 충돌 회피나 특이점 회피가 없으므로 실제 배포 시 안전 로직을 추가해야 한다.
