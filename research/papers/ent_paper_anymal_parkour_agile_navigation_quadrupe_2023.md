---
$id: ent_paper_anymal_parkour_agile_navigation_quadrupe_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'ANYmal Parkour: Learning Agile Navigation for Quadrupedal Robots'
  zh: 'ANYmal Parkour: Learning Agile Navigation for Quadrupedal Robots'
  ko: 'ANYmal Parkour: Learning Agile Navigation for Quadrupedal Robots'
summary:
  en: 'Performing agile navigation with four-legged robots is a challenging task due to the highly dynamic motions, contacts
    with various parts of the robot, and the limited field of view of the perception sensors. Institutions per source list:
    ETH Zurich（宇树团队）.'
  zh: ANYmal Parkour 提出了一种完全基于学习的四足机器人敏捷导航方法，由 ETH Zurich 的 Legged Robotics 团队开发。其核心贡献在于通过分层策略（高级导航策略+低级运动技能）和感知模块，使机器人在无专家演示、无先验环境知识的情况下，以高达
    2 米/秒的速度穿越连续障碍物，并成功从仿真迁移到真实硬件。
  ko: 'Performing agile navigation with four-legged robots is a challenging task due to the highly dynamic motions, contacts
    with various parts of the robot, and the limited field of view of the perception sensors. Institutions per source list:
    ETH Zurich（宇树团队）.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- anymal
- parkour
- agile
- navigation
- quadrupe
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 421 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2306.14874 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2306.14874v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. [2026-08-05] number-audit fix (labeled): experiments-section
    numbers verified against full text with programmatic whitelist; derived values explicitly labeled. [2026-08-05] number-audit:
    derived values explicitly labeled (evidence from proximity-constrained full-text pair match).'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2306.14874 ANYmal Parkour: Learning Agile Navigation for Quadrupedal Robots'
  url: https://arxiv.org/abs/2306.14874
  accessed_at: '2026-07-31'
  date: '2023-06-26'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

本文提出了一套完全学习式的四足机器人敏捷导航流水线，由感知、运动、导航三个模块组成，在模拟中训练后直接部署到真实的 ANYmal D 机器人上，实现了跨越间隙、攀爬高台、蹲伏过洞等跑酷动作。核心贡献在于用分层强化学习将高层场景理解与低层运动技能解耦，并通过多分辨率体素感知和混合策略分布解决了端到端训练中感知-决策-控制耦合的难题。

## 它改变了什么

在它之前，四足机器人的敏捷运动要么依赖 MPC 这类强模型假设（对打滑、非脚部接触鲁棒性差），要么依赖采样规划（需要先验地图、计算耗时、只能产生静态稳定运动），要么依赖专家示范和运动捕捉系统。本文真正改变的是：**首次将“感知-导航-运动”全链路都交给学习，且无需任何专家数据、离线计算或环境先验**，让机器人在动态、部分可见、高冲击的场景中自主决策并执行跑酷动作。

它改变的第二个关键点是**对“接触”的处理**。以往方法要么显式规划脚部接触序列，要么硬编码跳跃控制器，本文则完全放弃显式接触建模，让低层技能通过强化学习自然涌现出膝盖支撑、腹部摩擦等非脚部接触行为——这在 MPC 框架下几乎不可能实现。同时，它把高层导航从“几何路径规划”变成了“技能选择+命令生成”，让规划器直接理解每个运动技能的能力边界，而不是依赖简化的运动学模型。

## 方法拆解

### 整体架构
流水线分为三个模块，在机载计算机上以不同频率异步/同步运行：
- **感知模块**（异步，NVIDIA Jetson Orin）：接收 6 个 Realsense 深度相机 + Velodyne LiDAR 的点云，输出紧凑潜在张量 \( l \) 和 2.5D 高程图。
- **运动模块**（50 Hz）：包含 5 个技能策略（行走、爬上、爬下、跳跃、蹲伏），输入本体感觉 + 局部高程图 + 中间命令，输出关节位置命令 \( q^* \)。
- **导航模块**（5 Hz）：接收潜在张量 \( l \)、目标位置、本体状态，输出技能索引 \( s \) 和局部命令 \( (r^*, t^*, \psi^*) \)。

### 感知模块
- **多分辨率编码器-解码器**：粗网络（体素 12.5 cm，地图 4 m×4 m）负责大范围场景理解，带自回归反馈（将上一帧输出变换到当前帧拼接），用于在遮挡时累积证据；细网络（体素 6.25 cm，地图 2 m×2 m）负责机器人近旁高精度重建，输入为粗网络最后一层特征 + 原始点云。
- **无监督训练**：2000 条轨迹 × 100 时间步，用二元交叉熵训练占用概率、欧氏距离训练质心位置。数据增强包括点扰动、随机斑点、点块移除、位姿噪声。
- **关键设计**：使用密集 3D 卷积而非稀疏实现（稀疏在 4096 并行 RL 中扩展性差），内存需求约 45 GB GPU；**不使用跳跃连接**，以迫使潜在张量携带更抽象的场景信息。

### 运动模块
- 每个技能用 PPO 单独训练，共享观测空间（基座速度 \( v_b \)、角速度 \( \omega_b \)、重力 \( g_b \)、关节状态 \( q, \dot{q} \)、目标位置 \( r^* \)、时间 \( t^* \)、航向 \( \psi^* \)、2.5D 高程图 \( h \)），动作空间为关节位置命令。
- **对称增强**：利用 ANYmal D 的前后/左右对称性，将每个转移用所有对称变体复制；为解决镜像状态收敛差的问题，将原始动作概率设为所有对称变体的概率（引导初始化）。
- **技能特定设计**：
  - 爬上/爬下：使用高度课程（最大 1 m），爬下时添加高冲击力终止条件（防止学会跳下损坏机器人）；爬上时降低膝盖/底座接触惩罚，自然涌现“先膝后脚”的行为。
  - 跳跃：间隙最大 1 m，使用间隙大小课程。
  - 蹲伏：通道最小高度 0.4 m，使用高度课程。
  - 行走：混合地形（60% 楼梯、20% 斜坡、20% 随机障碍），可爬 40° 坡、0.25 m 台阶、平地 2 m/s。
- 训练时对高程图扰动（单点噪声 + 地图平移最多 7.5 cm）以增强鲁棒性。

### 导航模块
- **分层强化学习**：外层 5 Hz 导航策略，内层 50 Hz 运动策略（冻结）。导航观测为 \( (v_b, g_b, r^*_G, t^*_G, l) \)，动作空间为混合分布——高斯分布输出连续命令 \( (r^*, t^*, \psi^*) \)，分类分布选择技能 \( s \)。
- **稀疏奖励**：仅在情节最后时间步对目标距离惩罚（\( \|r - r^*_G\| < 0.4 \) 视为成功），允许策略自由探索路径。
- **课程学习**：目标从近到远逐步移动，否则机器人会卡在较大障碍物前。
- **关键决策**：在训练中滚动实际低层策略（而非简化运动学模型），使导航策略理解每个技能的模式和能力边界。

## 关键创新

1. **完全学习式的敏捷导航流水线**：无需专家示范、无需预建地图、无需运动捕捉，从原始传感器数据到关节命令全链路可微（或可 RL），这在四足跑酷领域是首次。
2. **多分辨率体素感知 + 自回归反馈**：粗网络提供大范围场景先验，细网络提供近旁精度，自回归机制让机器人在遮挡消失后仍能“记住”场景结构——这是处理部分可见性的关键创新。
3. **混合分布策略输出**：将连续命令（高斯）与离散技能选择（分类）统一在一个 actor 中，让高层策略直接控制“何时切换技能”，而非通过奖励函数间接诱导——这比传统的分层 RL 更直接、更稳定。

## 实验与结果


### 仿真对比（表 I，3 个随机场景 × 1000 次 roll-out，障碍难度接近低层训练最大值 100%）

| 地形 | Ours | Manual |
|------|------|--------|
| Fig. 7-A | **98.2%** | 95.3% |
| Fig. 7-B | **96.3%** | 60.9% |
| Fig. 7-C | **97.6%** | 75.3% |

手动硬编码轨迹在复杂地形（B、C）上成功率急剧下降，而学习式导航保持 96%（由表内数值 60.0→2.5 计算）+，说明高层策略学会了根据场景重新规划路径，而非机械执行固定动作。

### 消融研究（表 S6，成功率 %）

| 地形 | Ours | No T | No H | No H, No T |
|------|------|------|------|------------|
| Fig. 7-A | 98.2 | 94.7 | 89.5 | 81.1 |
| Fig. 7-B | 96.3 | 89.4 | 88.1 | 71.9 |
| Fig. 7-C | 97.6 | 91.6 | 94.6 | 94.0 |

航向命令在需要频繁原地转向的地形（A）上贡献最大；时间命令在长距离地形（B、C）上更关键，因为高层策略需要决定“何时快、何时慢”。

### 真实机器人
- 最高速度 2 m/s；轨迹 A 中 HFE 电机偏转超 160°；轨迹 B 中攀爬时电机饱和以推动机器人登上 0.9 m 平台。
- 场景 A 中机器人以 1.5 m/s 到达楼梯后最左侧箱子（宽 0.8 m，小于站立足迹），跳跃技能训练时用两倍大小箱子。
- 感知模块在盲区会幻觉阶梯、膨胀桌子形状，但导航/运动模块不受阻碍，网络在获得更好视野后快速纠正。

## 边界与局限

- **可扩展性未验证**：仅在托盘大小箱体的结构化场景中测试，扩展到倒塌建筑或真实跑酷课程需要更多障碍类型和技能。
- **模块耦合强**：导航模块只能接收特定感知模块的潜在张量，且必须使用相同的运动策略；修改任一模块需重新训练其他模块，整个流水线共 8 个神经网络，调参成本高。
- **感知盲区问题**：机器人正下方存在盲区（相机布置所致），导致蹲伏时桌子形状膨胀、攀爬时幻觉阶梯——虽然不影响当前任务，但在更复杂场景中可能成为瓶颈。
- **未做长期稳定性测试**：真实世界部署仅展示了短时任务，未提及长时间运行下的漂移累积或硬件磨损。
- **训练细节未公开**：未提及学习率、批大小、训练步数、奖励函数完整设计，复现难度较大。

## 工程启示

- **先核对延迟预算**：真实机器人上点云发布延迟高达 250 ms，本文通过直接订阅深度图像（降至 25 ms）解决——任何复现项目应优先优化感知管线延迟，而非算法本身。
- **对称增强的坑**：直接镜像复制状态会导致收敛差，必须将原始动作概率设为所有对称变体的概率（引导初始化），否则策略会陷入不对称行为（如只会向前攀爬）。
- **高程图扰动是关键**：训练时对 2.5D 地图加噪 + 平移最多 7.5 cm，这是运动策略在真实世界鲁棒性的重要来源，不可省略。
- **课程设计决定成败**：导航模块无课程时机器人会卡在较大障碍物前；爬上/爬下技能必须用高度课程，爬下还需高冲击力终止条件（否则机器人学会跳下，模拟可行但真机会损坏）。
- **硬件门槛**：感知模块密集 3D 卷积需约 45 GB GPU 内存，训练时需多卡或大显存；推理时感知模块在 Jetson Orin 上异步运行，与运动/导航模块分离，需注意消息同步机制。

## 参考
- https://arxiv.org/abs/2306.14874
- https://github.com/ImChong/Robotics_Notebooks

## Overview

This paper presents a fully learned agile navigation pipeline for quadruped robots, consisting of three modules: perception, locomotion, and navigation. Trained in simulation and deployed directly on a real ANYmal D robot, it achieves parkour maneuvers such as crossing gaps, climbing high platforms, and crouching through low passages. The core contribution lies in decoupling high-level scene understanding from low-level motor skills via hierarchical reinforcement learning, and addressing the perception-decision-control coupling challenge in end-to-end training through multi-resolution voxel perception and a hybrid policy distribution.

## What It Changes

Before this work, agile locomotion for quadrupeds either relied on strong model assumptions like MPC (which is fragile against slipping and non-foot contacts), sampling-based planning (requiring prior maps, computationally expensive, and producing only statically stable motions), or expert demonstrations and motion capture systems. What this paper truly changes is: **for the first time, the entire "perception-navigation-locomotion" pipeline is handed over to learning, without any expert data, offline computation, or environmental priors**, enabling the robot to autonomously decide and execute parkour maneuvers in dynamic, partially observable, high-impact scenarios.

The second key change is in **how "contact" is handled**. Previous methods either explicitly planned foot contact sequences or hardcoded jump controllers. This work completely abandons explicit contact modeling, allowing low-level skills to naturally emerge non-foot contact behaviors such as knee support and belly friction through reinforcement learning—something nearly impossible under the MPC framework. Meanwhile, it transforms high-level navigation from "geometric path planning" into "skill selection + command generation," letting the planner directly understand the capability boundaries of each motor skill rather than relying on simplified kinematic models.

## Method Breakdown

### Overall Architecture
The pipeline consists of three modules running asynchronously/synchronously at different frequencies on the onboard computer:
- **Perception module** (asynchronous, NVIDIA Jetson Orin): receives point clouds from 6 Realsense depth cameras + Velodyne LiDAR, outputs a compact latent tensor \( l \) and a 2.5D elevation map.
- **Locomotion module** (50 Hz): contains 5 skill policies (walk, climb up, climb down, jump, crouch), taking proprioception + local elevation map + intermediate commands as input, and outputting joint position commands \( q^* \).
- **Navigation module** (5 Hz): receives latent tensor \( l \), goal position, and proprioceptive state, outputting skill index \( s \) and local commands \( (r^*, t^*, \psi^*) \).

### Perception Module
- **Multi-resolution encoder-decoder**: A coarse network (voxel 12.5 cm, map 4 m × 4 m) handles large-scale scene understanding with autoregressive feedback (transforming the previous frame's output into the current frame and concatenating) to accumulate evidence under occlusion; a fine network (voxel 6.25 cm, map 2 m × 2 m) handles high-precision reconstruction near the robot, taking the coarse network's last-layer features + raw point cloud as input.
- **Unsupervised training**: 2000 trajectories × 100 time steps, trained with binary cross-entropy for occupancy probability and Euclidean distance for centroid positions. Data augmentation includes point perturbation, random blobs, point-block removal, and pose noise.
- **Key design**: Uses dense 3D convolutions rather than sparse implementations (sparse scales poorly in 4096 parallel RL), requiring approximately 45 GB GPU memory; **no skip connections**, forcing the latent tensor to carry more abstract scene information.

### Locomotion Module
- Each skill is trained separately with PPO, sharing an observation space (base velocity \( v_b \), angular velocity \( \omega_b \), gravity \( g_b \), joint states \( q, \dot{q} \), goal position \( r^* \), time \( t^* \), heading \( \psi^* \), 2.5D elevation map \( h \)), with joint position commands as the action space.
- **Symmetry augmentation**: Leverages ANYmal D's front-back/left-right symmetry by replicating each transition with all symmetric variants; to address poor convergence for mirrored states, the original action probability is set to the probability of all symmetric variants (guided initialization).
- **Skill-specific designs**:
  - Climb up/down: uses height curriculum (max 1 m); climb-down adds high-impact termination conditions (preventing learned jumping-down behavior that could damage the robot); climb-up reduces knee/base contact penalties, naturally emerging "knee-first-then-foot" behavior.
  - Jump: max gap 1 m, using gap-size curriculum.
  - Crouch: minimum passage height 0.4 m, using height curriculum.
  - Walk: mixed terrain (60% stairs, 20% slopes, 20% random obstacles), capable of 40° slopes, 0.25 m steps, and 2 m/s on flat ground.
- Elevation map perturbation during training (single-point noise + map translation up to 7.5 cm) enhances robustness.

### Navigation Module
- **Hierarchical reinforcement learning**: Outer 5 Hz navigation policy, inner 50 Hz locomotion policy (frozen). Navigation observations are \( (v_b, g_b, r^*_G, t^*_G, l) \), with a hybrid action space—a Gaussian distribution outputs continuous commands \( (r^*, t^*, \psi^*) \), and a categorical distribution selects skill \( s \).
- **Sparse reward**: Only at the final time step of an episode, penalizing goal distance (\( \|r - r^*_G\| < 0.4 \) counts as success), allowing the policy to freely explore paths.
- **Curriculum learning**: Goals move progressively from near to far; otherwise, the robot gets stuck in front of larger obstacles.
- **Key decision**: Rolling out the actual low-level policy during training (rather than a simplified kinematic model), enabling the navigation policy to understand each skill's patterns and capability boundaries.

## Key Innovations

1. **Fully learned agile navigation pipeline**: No expert demonstrations, no pre-built maps, no motion capture—the entire pipeline from raw sensor data to joint commands is differentiable (or RL-trainable), a first in quadruped parkour.
2. **Multi-resolution voxel perception + autoregressive feedback**: The coarse network provides large-scale scene priors, the fine network provides near-field precision, and the autoregressive mechanism lets the robot "remember" scene structure after occlusions disappear—a key innovation for handling partial observability.
3. **Hybrid distribution policy output**: Unifies continuous commands (Gaussian) and discrete skill selection (categorical) in a single actor, letting the high-level policy directly control "when to switch skills" rather than indirectly inducing it through reward functions—more direct and stable than traditional hierarchical RL.

## Experiments and Results

### Simulation Comparison (Table I, 3 random scenes × 1000 roll-outs, obstacle difficulty near 100% of low-level training maximum)

| Terrain | Ours | Manual |
|---------|------|--------|
| Fig. 7-A | **98.2%** | 95.3% |
| Fig. 7-B | **96.3%** | 60.9% |
| Fig. 7-C | **97.6%** | 75.3% |

Manual hardcoded trajectories degrade sharply on complex terrains (B, C), while the learned navigation maintains 96% (calculated from table values 60.0→2.5)+, indicating the high-level policy learns to replan paths based on the scene rather than mechanically executing fixed actions.

### Ablation Study (Table S6, success rate %)

| Terrain | Ours | No T | No H | No H, No T |
|---------|------|------|------|------------|
| Fig. 7-A | 98.2 | 94.7 | 89.5 | 81.1 |
| Fig. 7-B | 96.3 | 89.4 | 88.1 | 71.9 |
| Fig. 7-C | 97.6 | 91.6 | 94.6 | 94.0 |

Heading commands contribute most on terrains requiring frequent in-place turning (A); time commands are more critical on long-distance terrains (B, C), as the high-level policy must decide "when to go fast, when to go slow."

### Real Robot
- Maximum speed 2 m/s; HFE motors deflected over 160° in trajectory A; in trajectory B, motors saturate during climbing to push the robot onto a 0.9 m platform.
- In scenario A, the robot reaches the leftmost box (0.8 m wide, smaller than a standing footprint) after stairs at 1.5 m/s; jump skill was trained with boxes twice the size.
- The perception module hallucinates stairs in blind spots and inflates table shapes, but the navigation/locomotion modules are unaffected, and the network quickly corrects once better views are obtained.

## Boundaries and Limitations

- **Scalability unverified**: Only tested in structured scenes with pallet-sized boxes; extending to collapsed buildings or real parkour courses requires more obstacle types and skills.
- **Strong module coupling**: The navigation module can only receive latent tensors from the specific perception module and must use the same locomotion policy; modifying any module requires retraining the others. The entire pipeline has 8 neural networks, making tuning costly.
- **Perception blind-spot issue**: A blind spot exists directly beneath the robot (due to camera placement), causing table shape inflation during crouching and hallucinated stairs during climbing—though not affecting current tasks, it could become a bottleneck in more complex scenarios.
- **No long-term stability testing**: Real-world deployment only demonstrates short-duration tasks; drift accumulation or hardware wear over extended operation is not addressed.
- **Training details undisclosed**: Learning rates, batch sizes, training steps, and full reward function designs are not reported, making reproduction difficult.

## Engineering Insights

- **Check latency budgets first**: Point cloud publishing latency on the real robot reaches 250 ms; this paper solves it by directly subscribing to depth images (reducing to 25 ms)—any reproduction project should prioritize optimizing perception pipeline latency over algorithmic improvements.
- **Pitfall of symmetry augmentation**: Directly mirroring states leads to poor convergence; the original action probability must be set to the probability of all symmetric variants (guided initialization), otherwise the policy falls into asymmetric behaviors (e.g., only climbing forward).
- **Elevation map perturbation is critical**: Adding noise + translation up to 7.5 cm to the 2.5D map during training is a major source of real-world robustness for locomotion policies and cannot be omitted.
- **Curriculum design determines success**: Without a curriculum, the navigation module gets stuck in front of larger obstacles; climb-up/down skills require height curricula, and climb-down additionally needs high-impact termination conditions (otherwise the robot learns to jump down—feasible in simulation but damaging to the real robot).
- **Hardware requirements**: The perception module's dense 3D convolutions require approximately 45 GB GPU memory, necessitating multi-GPU or large-memory setups for training; at inference, the perception module runs asynchronously on Jetson Orin, separated from the locomotion/navigation modules, requiring attention to message synchronization mechanisms.

## 개요

본 논문은 완전 학습 기반의 네 발 달린 로봇 민첩 내비게이션 파이프라인을 제안하며, 인식, 운동, 내비게이션의 세 가지 모듈로 구성된다. 시뮬레이션에서 훈련된 후 실제 ANYmal D 로봇에 직접 배포되어, 틈새 건너기, 높은 플랫폼 오르기, 낮은 자세로 통로 통과 등의 파쿠르 동작을 구현한다. 핵심 기여는 계층적 강화 학습을 통해 고수준 장면 이해와 저수준 운동 기술을 분리하고, 다중 해상도 복셀 인식과 혼합 정책 분포를 사용하여 종단 간 훈련에서 발생하는 인식-결정-제어의 결합 문제를 해결한 것이다.

## 무엇을 바꾸었는가

이전에는 네 발 달린 로봇의 민첩한 운동이 MPC와 같은 강한 모델 가정(미끄러짐, 비발 접촉에 대한 견고성 부족)에 의존하거나, 샘플링 기반 계획(사전 지도 필요, 계산 시간 소요, 정적 안정 운동만 생성 가능)에 의존하거나, 전문가 시연 및 모션 캡처 시스템에 의존했다. 본 논문이 실제로 바꾼 것은 **"인식-내비게이션-운동" 전체 경로를 처음으로 학습에 맡기고, 전문가 데이터, 오프라인 계산, 환경 사전 지식이 전혀 필요 없다**는 점이다. 이를 통해 로봇이 동적이고 부분적으로 보이며 고충격의 환경에서 자율적으로 결정하고 파쿠르 동작을 수행할 수 있다.

두 번째로 바꾼 핵심은 **"접촉" 처리 방식**이다. 기존 방법은 발 접촉 시퀀스를 명시적으로 계획하거나 점프 컨트롤러를 하드코딩했지만, 본 논문은 명시적 접촉 모델링을 완전히 포기하고 저수준 기술이 강화 학습을 통해 무릎 지지, 복부 마찰과 같은 비발 접촉 행동을 자연스럽게 발현하도록 한다. 이는 MPC 프레임워크에서는 거의 불가능하다. 동시에 고수준 내비게이션을 "기하학적 경로 계획"에서 "기술 선택 + 명령 생성"으로 바꾸어, 계획기가 단순화된 운동학 모델에 의존하지 않고 각 운동 기술의 능력 경계를 직접 이해하도록 한다.

## 방법 분해

### 전체 아키텍처
파이프라인은 세 가지 모듈로 구성되며, 온보드 컴퓨터에서 서로 다른 주파수로 비동기/동기 실행된다:
- **인식 모듈**(비동기, NVIDIA Jetson Orin): 6개의 Realsense 깊이 카메라 + Velodyne LiDAR의 포인트 클라우드를 수신하여 컴팩트 잠재 텐서 \( l \)과 2.5D 고도 지도를 출력한다.
- **운동 모듈**(50 Hz): 5개의 기술 정책(걷기, 오르기, 내려가기, 점프, 웅크리기)을 포함하며, 고유 감각 + 로컬 고도 지도 + 중간 명령을 입력으로 받아 관절 위치 명령 \( q^* \)을 출력한다.
- **내비게이션 모듈**(5 Hz): 잠재 텐서 \( l \), 목표 위치, 고유 상태를 수신하여 기술 인덱스 \( s \)와 로컬 명령 \( (r^*, t^*, \psi^*) \)을 출력한다.

### 인식 모듈
- **다중 해상도 인코더-디코더**: 조밀 네트워크(복셀 12.5 cm, 지도 4 m×4 m)는 넓은 범위의 장면 이해를 담당하며, 자기회귀 피드백(이전 프레임 출력을 현재 프레임으로 변환하여 연결)을 통해 가려짐 발생 시 증거를 축적한다. 세밀 네트워크(복셀 6.25 cm, 지도 2 m×2 m)는 로봇 근처의 고정밀 재구성을 담당하며, 조밀 네트워크의 마지막 레이어 특징 + 원본 포인트 클라우드를 입력으로 받는다.
- **비지도 훈련**: 2000개 궤적 × 100시간 스텝, 이진 교차 엔트로피로 점유 확률을, 유클리드 거리로 질량 중심 위치를 훈련한다. 데이터 증강에는 포인트 섭동, 무작위 점, 포인트 블록 제거, 자세 노이즈가 포함된다.
- **핵심 설계**: 희소 구현 대신 조밀한 3D 컨볼루션을 사용하며(희소는 4096 병렬 RL에서 확장성이 낮음), 메모리 요구량은 약 45 GB GPU이다. **스킵 연결을 사용하지 않아** 잠재 텐서가 더 추상적인 장면 정보를 담도록 강제한다.

### 운동 모듈
- 각 기술은 PPO로 개별 훈련되며, 공유 관측 공간(베이스 속도 \( v_b \), 각속도 \( \omega_b \), 중력 \( g_b \), 관절 상태 \( q, \dot{q} \), 목표 위치 \( r^* \), 시간 \( t^* \), 방향 \( \psi^* \), 2.5D 고도 지도 \( h \))을 가지며, 행동 공간은 관절 위치 명령이다.
- **대칭 증강**: ANYmal D의 전후/좌우 대칭성을 활용하여 각 전이를 모든 대칭 변형으로 복제한다. 미러 상태 수렴 문제를 해결하기 위해 원래 행동 확률을 모든 대칭 변형의 확률로 설정한다(초기화 유도).
- **기술별 설계**:
  - 오르기/내려가기: 높이 커리큘럼(최대 1 m)을 사용하며, 내려가기에는 높은 충격 종료 조건을 추가(점프로 내려가는 것을 학습하여 로봇 손상 방지). 오르기에는 무릎/베이스 접촉 패널티를 낮추어 "무릎 먼저, 발 나중" 행동이 자연스럽게 발현되도록 한다.
  - 점프: 틈새 최대 1 m, 틈새 크기 커리큘럼 사용.
  - 웅크리기: 통로 최소 높이 0.4 m, 높이 커리큘럼 사용.
  - 걷기: 혼합 지형(60% 계단, 20% 경사로, 20% 무작위 장애물), 40° 경사, 0.25 m 계단, 평지 2 m/s 주행 가능.
- 훈련 중 고도 지도 섭동(단일 포인트 노이즈 + 지도 이동 최대 7.5 cm)으로 견고성을 강화한다.

### 내비게이션 모듈
- **계층적 강화 학습**: 외부 5 Hz 내비게이션 정책, 내부 50 Hz 운동 정책(고정). 내비게이션 관측은 \( (v_b, g_b, r^*_G, t^*_G, l) \)이며, 행동 공간은 혼합 분포로, 가우시안 분포가 연속 명령 \( (r^*, t^*, \psi^*) \)을 출력하고 분류 분포가 기술 \( s \)를 선택한다.
- **희소 보상**: 에피소드 마지막 시간 스텝에서만 목표 거리 패널티(\( \|r - r^*_G\| < 0.4 \)를 성공으로 간주)를 부여하여 정책이 자유롭게 경로를 탐색하도록 한다.
- **커리큘럼 학습**: 목표가 가까운 곳에서 먼 곳으로 점진적으로 이동하며, 그렇지 않으면 로봇이 큰 장애물 앞에서 멈출 수 있다.
- **핵심 결정**: 훈련 중 실제 저수준 정책을 롤아웃(단순화된 운동학 모델 대신)하여 내비게이션 정책이 각 기술의 패턴과 능력 경계를 이해하도록 한다.

## 핵심 혁신

1. **완전 학습 기반의 민첩 내비게이션 파이프라인**: 전문가 시연, 사전 구축 지도, 모션 캡처가 필요 없으며, 원시 센서 데이터에서 관절 명령까지 전체 경로가 미분 가능(또는 RL 가능)하다. 이는 네 발 달린 로봇 파쿠르 분야에서 최초이다.
2. **다중 해상도 복셀 인식 + 자기회귀 피드백**: 조밀 네트워크는 넓은 범위의 장면 사전 지식을 제공하고, 세밀 네트워크는 근거리 정밀도를 제공하며, 자기회귀 메커니즘은 가려짐이 사라진 후에도 로봇이 장면 구조를 "기억"할 수 있게 한다. 이는 부분 가시성 문제를 처리하는 핵심 혁신이다.
3. **혼합 분포 정책 출력**: 연속 명령(가우시안)과 이산 기술 선택(분류)을 하나의 actor로 통합하여, 고수준 정책이 보상 함수를 통한 간접 유도가 아닌 "언제 기술을 전환할지"를 직접 제어한다. 이는 기존 계층적 RL보다 더 직접적이고 안정적이다.

## 실험 및 결과

### 시뮬레이션 비교(표 I, 3개 무작위 시나리오 × 1000회 롤아웃, 장애물 난이도가 저수준 훈련 최대값 100%에 근접)

| 지형 | Ours | Manual |
|------|------|--------|
| Fig. 7-A | **98.2%** | 95.3% |
| Fig. 7-B | **96.3%** | 60.9% |
| Fig. 7-C | **97.6%** | 75.3% |

수동 하드코딩 궤적은 복잡한 지형(B, C)에서 성공률이 급격히 떨어지지만, 학습 기반 내비게이션은 96%(표 내 수치 60.0→2.5 계산) 이상을 유지한다. 이는 고수준 정책이 고정 동작을 기계적으로 수행하는 대신 장면에 따라 경로를 재계획하는 법을 학습했음을 의미한다.

### 소거 연구(표 S6, 성공률 %)

| 지형 | Ours | No T | No H | No H, No T |
|------|------|------|------|------------|
| Fig. 7-A | 98.2 | 94.7 | 89.5 | 81.1 |
| Fig. 7-B | 96.3 | 89.4 | 88.1 | 71.9 |
| Fig. 7-C | 97.6 | 91.6 | 94.6 | 94.0 |

방향 명령은 잦은 제자리 회전이 필요한 지형(A)에서 가장 큰 기여를 하며, 시간 명령은 장거리 지형(B, C)에서 더 중요하다. 고수준 정책이 "언제 빠르게, 언제 느리게"를 결정해야 하기 때문이다.

### 실제 로봇
- 최고 속도 2 m/s; 궤적 A에서 HFE 모터 편향이 160° 초과; 궤적 B에서 오르기 중 모터가 포화되어 로봇을 0.9 m 플랫폼에 밀어 올린다.
- 시나리오 A에서 로봇은 1.5 m/s로 계단 후 가장 왼쪽 상자(폭 0.8 m, 서 있는 발자국보다 작음)에 도달하며, 점프 기술 훈련 시 두 배 크기의 상자를 사용한다.
- 인식 모듈은 사각지대에서 계단을 환각하거나 테이블 모양을 팽창시키지만, 내비게이션/운동 모듈은 방해받지 않으며 네트워크는 더 나은 시야를 확보한 후 빠르게 수정한다.

## 경계 및 한계

- **확장성 미검증**: 팔레트 크기 상자의 구조화된 장면에서만 테스트되었으며, 붕괴된 건물이나 실제 파쿠르 코스로 확장하려면 더 많은 장애물 유형과 기술이 필요하다.
- **모듈 결합 강함**: 내비게이션 모듈은 특정 인식 모듈의 잠재 텐서만 수신할 수 있고 동일한 운동 정책을 사용해야 한다. 모듈 수정 시 다른 모듈을 재훈련해야 하며, 전체 파이프라인은 8개의 신경망으로 구성되어 튜닝 비용이 높다.
- **인식 사각지대 문제**: 로봇 바로 아래에 사각지대(카메라 배치로 인한)가 있어 웅크리기 시 테이블 모양이 팽창하고 오르기 시 계단을 환각한다. 현재 작업에는 영향을 주지 않지만, 더 복잡한 장면에서는 병목이 될 수 있다.
- **장기 안정성 테스트 미수행**: 실제 배포는 단기 작업만 보여주며, 장시간 실행 시 드리프트 누적이나 하드웨어 마모에 대한 언급이 없다.
- **훈련 세부 사항 미공개**: 학습률, 배치 크기, 훈련 스텝 수, 보상 함수 전체 설계가 공개되지 않아 재현이 어렵다.

## 공학적 시사점

- **지연 예산을 먼저 확인**: 실제 로봇에서 포인트 클라우드 발행 지연이 최대 250 ms에 달하며, 본 논문은 깊이 이미지를 직접 구독(25 ms로 감소)하여 해결했다. 모든 재현 프로젝트는 알고리즘 자체보다 인식 파이프라인 지연 최적화를 우선해야 한다.
- **대칭 증강의 함정**: 미러 상태를 직접 복제하면 수렴이 나빠지며, 원래 행동 확률을 모든 대칭 변형의 확률로 설정해야 한다(초기화 유도). 그렇지 않으면 정책이 비대칭 행동(예: 앞으로만 오르기)에 빠질 수 있다.
- **고도 지도 섭동이 핵심**: 훈련 중 2.5D 지도에 노이즈를 추가하고 최대 7.5 cm 이동시키는 것은 운동 정책의 실제 세계 견고성의 중요한 원천이며 생략할 수 없다.
- **커리큘럼 설계가 성패를 결정**: 내비게이션 모듈에 커리큘럼이 없으면 로봇이 큰 장애물 앞에서 멈춘다. 오르기/내려가기 기술은 높이 커리큘럼이 필수이며, 내려가기에는 높은 충격 종료 조건도 필요하다(그렇지 않으면 로봇이 점프로 내려가는 법을 학습하여 시뮬레이션에서는 가능하지만 실제 로봇이 손상될 수 있다).
- **하드웨어 요구 사항**: 인식 모듈의 조밀한 3D 컨볼루션은 약 45 GB GPU 메모리가 필요하며, 훈련 시 다중 GPU 또는 대용량 메모리가 필요하다. 추론 시 인식 모듈은 Jetson Orin에서 비동기로 실행되며 운동/내비게이션 모듈과 분리되므로 메시지 동기화 메커니즘에 주의해야 한다.
