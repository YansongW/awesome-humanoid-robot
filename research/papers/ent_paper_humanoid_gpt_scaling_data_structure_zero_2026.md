---
$id: ent_paper_humanoid_gpt_scaling_data_structure_zero_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Humanoid-GPT: Scaling Data and Structure for Zero-Shot Motion Tracking'
  zh: 亿帧数据驱动的人形零样本动作跟踪
  ko: 'Humanoid-GPT: Scaling Data and Structure for Zero-Shot Motion Tracking'
summary:
  en: 'We introduce Humanoid-GPT, a GPT-style Transformer with causal attention trained on a billion-scale motion corpus for
    whole-body control. Unlike prior shallow MLP trackers constrained by scarce data and an agility-generalization trade-off,
    Humanoid-GPT is pre-trained on a 2B-frame retargeted corpus that unifies all major mocap datasets with large-scale in-house
    recordings. Institutions per source list: 清华大学、银河通用、上海交大、北京大学、上海期智研究院.'
  zh: Humanoid-GPT 是一个基于因果注意力的 GPT 风格 Transformer 模型，由研究团队在十亿级运动语料上预训练，用于全身控制。其核心贡献在于通过扩展数据（20亿帧重定向语料）和模型容量，实现了对高度动态行为的零样本运动跟踪，并展现出前所未有的泛化能力。
  ko: 'We introduce Humanoid-GPT, a GPT-style Transformer with causal attention trained on a billion-scale motion corpus for
    whole-body control. Unlike prior shallow MLP trackers constrained by scarce data and an agility-generalization trade-off,
    Humanoid-GPT is pre-trained on a 2B-frame retargeted corpus that unifies all major mocap datasets with large-scale in-house
    recordings. Institutions per source list: 清华大学、银河通用、上海交大、北京大学、上海期智研究院.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- humanoid
- gpt
- scaling
- data
- structure
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 29 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2606.03985 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2606.03985v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.03985 Humanoid-GPT: Scaling Data and Structure for Zero-Shot Motion Tracking'
  url: https://arxiv.org/abs/2606.03985
  accessed_at: '2026-07-31'
  date: '2026-06-02'
- id: src_002
  type: website
  title: Project page
  url: https://github.com/GalaxyGeneralRobotics/Humanoid-GPT/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: Project page (fetched)
  url: https://raw.githubusercontent.com/GalaxyGeneralRobotics/Humanoid-GPT/HEAD/README.md
  accessed_at: '2026-07-31'
- id: src_004
  type: website
  title: 万字长文｜人形机器人的运动小脑会不会成为人形机器人的基础设施？
  url: https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA
  accessed_at: '2026-07-31'
---


## 概述

Humanoid-GPT 是一个基于 GPT 风格因果注意力 Transformer 的通用在线人形运动跟踪器，通过将训练数据扩展至 2B 帧（比先前工作大 200 倍以上）并配合 Harmonic Motion Embedding (HME) 数据策展与专家蒸馏流程，实现了对未见舞蹈动作的零样本运动跟踪。该系统在 Unitree-G1 上以 50 Hz 闭环控制运行，端到端推理延迟低于 1.5ms，在仿真与真实世界实验中均显著超越现有基线。

## 它改变了什么

运动跟踪领域长期被浅层 MLP 主导，训练数据停留在 10^4 量级轨迹（约 7.2M 帧），这造成了一个看似不可调和的「敏捷性-泛化性」权衡：域内敏捷动作的跟踪锐度与未见风格上的泛化能力难以兼得。作者的核心判断是，这一权衡并非根本性限制，而是数据规模不足与训练设计不匹配的症状——语言/视觉领域的大规模扩展路径在运动控制中从未被认真尝试。

真正被改变的是「规模假设」在运动跟踪中的确立。作者不仅将数据扩大了两个数量级，更重要的是证明了：当数据从百万帧增长到十亿帧时，模型结构的选择（因果 Transformer vs MLP）决定了性能是持续提升还是早期饱和。这打破了运动跟踪领域「小数据+浅模型」的默认范式，将问题重新定义为数据策展、结构匹配与训练稳定性三者的联合优化，而非单纯的算法调参。

## 方法拆解

### 数据策展与 HME 采样
- 聚合 AMASS、LAFAN1、MotionMillion、PHUMA 及内部采集数据，经运动重定向映射至 Unitree-G1 的 29-DoF 空间
- 过滤含物体交互序列（坐椅、游泳等），应用时间扭曲增强（均匀加减速）将数据扩展约 5 倍，最终获得 2B 帧语料
- 训练多个 Periodic Autoencoders 提取每关节周期幅度/频率，聚合为 HME 向量；K-Means 聚类产生约 300 个运动簇（每簇 1k–2k 序列）
- 关键设计：HME 用于多样性感知、分布平衡采样——多样性不足导致过拟合频繁模式，平衡性不足限制能力上限

### 运动专家训练
- 每簇训练 PPO 策略 π: 𝒢 × 𝒮 ↦ 𝒜，状态含关节位置/速度、根部角速度、投影重力、先前动作；输出经 PD 控制器转为扭矩
- 奖励在身体关键点级计算：R_kpt(t) = R_pos(t) + R_rot(t) + R_vel(t) + R_penal(t)，其中 R_pos(t) = Σ w_k exp(−α_pos ‖e^pos‖₁)，α_pos=1.0，α_rot=2.0，α_vel=0.03；下半身权重 1.5，上半身 0.75
- 训练后仅保留高保真、长时稳定的专家（约 384 个为最优平衡点）

### DAgger 蒸馏至通用策略
- 采用 DAgger 框架：迭代中 rollout 学生策略并查询专家目标动作，训练学生匹配专家输出
- 输入 token e_t = [s_t; q_t^ref]（本体感受 + 目标参考姿态拼接），长度 H=32 的序列经时间因果掩码 Transformer
- 损失为 SmoothL1Loss，公式 (2)：â = ⋃ concat_{k∈[-H+1,0]} t_i(s_{t-k}, g_{t-k})，l = ℒ(G_θ(e_{t-H+1:t}), â)
- 推理时维护 H 个历史 token 队列，取最后位置输出作为控制目标；batch size 不小于专家数量以避免模式崩溃

### 架构选择理由
- 运动跟踪本质因果（测试时无未来观测），GPT 风格因果注意力与部署约束对齐
- Transformer 相比 MLP 和非因果变体随数据/模型规模更干净地扩展
- 并行序列监督 + 自回归时间预测利用 Transformer 固有优势，隐式学习位置不变的时间预测，使 episode 起始（历史稀缺）也能稳定输出

## 关键创新

1. **数据规模跨越临界点**：2B 帧训练语料比先前最大工作（SONIC 的 100M 帧）大 20 倍，比主流基线（7.2M 帧）大 200 倍以上。这不是增量改进，而是首次将运动跟踪推入「数据受限」区域——实验显示 200M→2B 边际增益下降，表明模型容量成为新瓶颈，这在小数据 regime 从未被观察到。

2. **HME 驱动的分布平衡采样**：现有工作要么随机采样（多样性失控），要么均匀采样（频繁模式过拟合）。HME 通过周期自编码器提取谐波特征并聚类，实现「多样性感知 + 分布平衡」的双重约束。作者明确强调两者缺一不可——这是对数据策展方法论的重要补充，而非简单的数据堆砌。

3. **因果 Transformer 作为控制架构**：虽然 Transformer 在运动生成中已有应用，但将其作为在线跟踪器的核心架构并系统验证「因果性 + 规模扩展」的协同效应是新的。关键洞察是：因果注意力不仅满足部署约束，还通过隐式时间预测解决了 episode 起始阶段的冷启动问题——这是 MLP 和 TCN 均不具备的特性。

## 实验与结果

### 仿真结果（AMASS-test 分割）

| 模型 | Tokens | 参数 | SR (%) | MPJPE | MPJVE | RootVelErr | MPKPE (mm) |
|------|--------|------|--------|-------|-------|------------|------------|
| MLP (3-layer) | 2M | 0.25M | 76.89 | 0.1191 | 0.6081 | 0.2304 | 100.49 |
| TCN (8-layer) | 2M | 0.65M | 81.48 | 0.0885 | 0.5716 | 0.2266 | 79.75 |
| Humanoid-GPT-S | 2M | 5.7M | 83.26 | 0.0853 | 0.5492 | 0.2049 | 62.65 |
| Humanoid-GPT-S | 20M | 5.7M | 86.02 | 0.0802 | 0.5210 | 0.1868 | 46.49 |
| Humanoid-GPT-B | 200M | 22.1M | 88.27 | 0.0793 | 0.5076 | 0.1820 | 44.78 |
| Humanoid-GPT-B | 2B | 22.1M | 90.43 | 0.0768 | 0.4891 | 0.1756 | 41.49 |
| Humanoid-GPT-L | 2B | 80.4M | 92.58 | 0.0735 | 0.4820 | 0.1785 | 40.99 |

关键对比：MLP-L 在 2M tokens 仅 75.25% SR（低于 MLP-S 的 76.89%），TCN-L 在 2M tokens 为 79.85%（低于 TCN-S 的 81.48%）——浅层模型增大容量反而退化；最佳基线 TCN-L 的 MPKPE 为 56.15mm，比 Humanoid-GPT-S（43.25mm）差 30%。

### 真实世界零样本舞蹈跟踪（MPJPE/MPJVE）

| 动作 | GMT | TWIST | Any2Track | GPT-S | GPT-B |
|------|-----|-------|-----------|-------|-------|
| Can Do Can Go! | 0.1087/1.2560 | 0.1253/1.1637 | 0.1039/1.1828 | 0.1024/1.0572 | **0.0974/0.9813** |
| Gokuraku Joudo | 0.1098/1.2865 | 0.1162/1.2731 | 0.1136/1.2366 | 0.1180/1.2362 | **0.1075/1.2257** |
| HuoYuanJia/Fearless | 0.0921/0.7054 | 0.1079/0.7821 | 0.0956/0.6410 | **0.0825/0.6209** | 0.0858/0.6158 |
| PokerFace | 0.0994/0.8217 | 0.1047/0.8893 | 0.0928/0.8641 | 0.0903/0.8476 | **0.0856/0.7325** |

数据多样性分析：作者策展数据集的 log-volume 比 AMASS 增加约 4–5 倍。推理延迟：RTX 4090 上端到端 <1.5ms，比 TWIST 快约 5 倍。

## 边界与局限

论文未明确提及失败案例的具体分析、特定运动类别的性能下限，以及真实世界部署中硬件磨损或安全性的讨论。作者承认的边界包括：未纳入接触、视觉或语言等更丰富模态；未扩展到交互或多智能体场景；未与长时程规划或 VLA 风格指令耦合。

值得注意的隐含局限：所有真实世界实验均基于舞蹈动作（高动态但无物体交互），对需要环境接触的任务（如操作、攀爬）可能不适用——数据策展阶段明确过滤了此类序列。此外，专家数量（约 384 个）与聚类数高度耦合，若目标运动分布与训练分布差异过大，HME 聚类的覆盖性可能不足。

## 工程启示

复现或借鉴此工作，建议优先核对以下环节：

1. **数据管线是最大工程风险**：2B 帧语料的获取、重定向、过滤与增强流程占整体工作量的比重远超模型设计。若无法复现同等规模数据，可先验证 HME 采样策略在 100M 帧量级是否仍优于随机采样——这决定了数据策展的边际价值。

2. **专家数量与蒸馏稳定性**：约 384 个专家是多样性、簇内一致性与计算成本的平衡点（128 过粗，1024 成本过高）。DAgger 蒸馏时 batch size 必须不小于专家数量，否则易模式崩溃——这是最容易踩坑的细节。

3. **历史长度 H=32 是计算与性能的折中**：实验显示 64 帧历史持续提升，但计算量随序列长度二次增长。若下游任务对延迟敏感，可先验证 16 帧是否可接受；若追求极致精度，可尝试 64 帧并接受推理延迟增加。

4. **域随机化参数需按平台调整**：地面摩擦 𝒰(0.3, 2.0)、外力间隔 𝒰(5.0, 10.0)、躯干 CoM 变化 𝒰(−0.15, 0.15) 等参数是针对 G1 调优的。迁移到其他硬件时，建议先做敏感性分析，特别是 armature 缩放（𝒰(1.0, 1.05)）对仿真-现实差异的影响。

5. **部署栈已开源**：ONNX 导出 + TensorRT 优化（因果注意力与融合 MLP 内核）+ C++ 流式管线，50 Hz 闭环控制。若复现时推理延迟超标，优先检查 TensorRT 是否真正融合了因果注意力掩码——这是 1.5ms 延迟的关键。

## 参考
- https://arxiv.org/abs/2606.03985
- https://github.com/GalaxyGeneralRobotics/Humanoid-GPT/
- https://raw.githubusercontent.com/GalaxyGeneralRobotics/Humanoid-GPT/HEAD/README.md
- https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA

## Overview

Humanoid-GPT is a general-purpose online humanoid motion tracker based on a GPT-style causal attention Transformer. By scaling training data to 2B frames (over 200x larger than prior work) and pairing it with Harmonic Motion Embedding (HME) data curation and expert distillation pipelines, it achieves zero-shot motion tracking of unseen dance movements. The system runs closed-loop control on the Unitree-G1 at 50 Hz with end-to-end inference latency below 1.5ms, significantly outperforming existing baselines in both simulation and real-world experiments.

## What It Changes

Motion tracking has long been dominated by shallow MLPs, with training data stuck at 10^4 trajectories (approximately 7.2M frames). This created a seemingly irreconcilable "agility-generalization" trade-off: the tracking sharpness of in-domain agile motions and the ability to generalize to unseen styles were difficult to achieve simultaneously. The authors' core judgment is that this trade-off is not a fundamental limitation but a symptom of insufficient data scale and mismatched training design—the large-scale scaling path from the language/vision domains has never been seriously attempted in motion control.

What truly changes is the establishment of the "scaling hypothesis" in motion tracking. The authors not only expanded data by two orders of magnitude but, more importantly, demonstrated that when data grows from millions to billions of frames, the choice of model architecture (causal Transformer vs MLP) determines whether performance keeps improving or saturates early. This breaks the default "small data + shallow model" paradigm in motion tracking, reframing the problem as a joint optimization of data curation, architecture matching, and training stability, rather than mere algorithmic tuning.

## Method Breakdown

### Data Curation and HME Sampling
- Aggregates AMASS, LAFAN1, MotionMillion, PHUMA, and internally collected data, mapped via motion retargeting to the Unitree-G1's 29-DoF space
- Filters out sequences with object interactions (chair sitting, swimming, etc.), applies time-warping augmentation (uniform acceleration/deceleration) to expand data approximately 5x, yielding a 2B-frame corpus
- Trains multiple Periodic Autoencoders to extract per-joint periodic amplitude/frequency, aggregated into HME vectors; K-Means clustering produces approximately 300 motion clusters (1k–2k sequences each)
- Key design: HME enables diversity-aware, distribution-balanced sampling—insufficient diversity causes overfitting to frequent patterns, while insufficient balance limits the capability ceiling

### Motion Expert Training
- Trains a PPO policy per cluster π: 𝒢 × 𝒮 ↦ 𝒜, with state comprising joint positions/velocities, root angular velocity, projected gravity, and previous actions; outputs are converted to torques via a PD controller
- Rewards are computed at the body keypoint level: R_kpt(t) = R_pos(t) + R_rot(t) + R_vel(t) + R_penal(t), where R_pos(t) = Σ w_k exp(−α_pos ‖e^pos‖₁), α_pos=1.0, α_rot=2.0, α_vel=0.03; lower-body weights are 1.5, upper-body 0.75
- After training, only high-fidelity, long-term stable experts are retained (approximately 384 as the optimal balance point)

### DAgger Distillation to a Generalist Policy
- Employs the DAgger framework: iteratively rolls out the student policy and queries expert target actions, training the student to match expert outputs
- Input token e_t = [s_t; q_t^ref] (proprioception + target reference pose concatenated), with a sequence of length H=32 passed through a time-causal masked Transformer
- Loss is SmoothL1Loss, Equation (2): â = ⋃ concat_{k∈[-H+1,0]} t_i(s_{t-k}, g_{t-k}), l = ℒ(G_θ(e_{t-H+1:t}), â)
- At inference, maintains a queue of H historical tokens, taking the last position's output as the control target; batch size must be no smaller than the number of experts to avoid mode collapse

### Architecture Selection Rationale
- Motion tracking is inherently causal (no future observations at test time), aligning GPT-style causal attention with deployment constraints
- Transformers scale more cleanly with data/model size compared to MLPs and non-causal variants
- Parallel sequence supervision + autoregressive temporal prediction leverages the Transformer's inherent strengths, implicitly learning position-invariant temporal prediction, enabling stable outputs even at episode start (when history is scarce)

## Key Innovations

1. **Data Scale Crossing a Critical Threshold**: The 2B-frame training corpus is 20x larger than the largest prior work (SONIC's 100M frames) and over 200x larger than mainstream baselines (7.2M frames). This is not an incremental improvement but the first push of motion tracking into the "data-limited" regime—experiments show diminishing marginal gains from 200M→2B, indicating model capacity becomes the new bottleneck, a phenomenon never observed in the small-data regime.

2. **HME-Driven Distribution-Balanced Sampling**: Existing work either samples randomly (uncontrolled diversity) or uniformly (overfitting to frequent patterns). HME extracts harmonic features via periodic autoencoders and clusters them, achieving dual constraints of "diversity-aware + distribution-balanced." The authors explicitly emphasize that both are indispensable—this is an important addition to data curation methodology, not mere data stacking.

3. **Causal Transformer as Control Architecture**: While Transformers have been applied in motion generation, using them as the core architecture for an online tracker and systematically validating the synergy of "causality + scaling" is novel. The key insight is that causal attention not only satisfies deployment constraints but also solves the cold-start problem at episode start through implicit temporal prediction—a property absent in both MLPs and TCNs.

## Experiments and Results

### Simulation Results (AMASS-test split)

| Model | Tokens | Params | SR (%) | MPJPE | MPJVE | RootVelErr | MPKPE (mm) |
|-------|--------|--------|-------|-------|-------|------------|------------|
| MLP (3-layer) | 2M | 0.25M | 76.89 | 0.1191 | 0.6081 | 0.2304 | 100.49 |
| TCN (8-layer) | 2M | 0.65M | 81.48 | 0.0885 | 0.5716 | 0.2266 | 79.75 |
| Humanoid-GPT-S | 2M | 5.7M | 83.26 | 0.0853 | 0.5492 | 0.2049 | 62.65 |
| Humanoid-GPT-S | 20M | 5.7M | 86.02 | 0.0802 | 0.5210 | 0.1868 | 46.49 |
| Humanoid-GPT-B | 200M | 22.1M | 88.27 | 0.0793 | 0.5076 | 0.1820 | 44.78 |
| Humanoid-GPT-B | 2B | 22.1M | 90.43 | 0.0768 | 0.4891 | 0.1756 | 41.49 |
| Humanoid-GPT-L | 2B | 80.4M | 92.58 | 0.0735 | 0.4820 | 0.1785 | 40.99 |

Key comparison: MLP-L at 2M tokens achieves only 75.25% SR (below MLP-S's 76.89%), and TCN-L at 2M tokens achieves 79.85% (below TCN-S's 81.48%)—shallow models degrade when capacity increases; the best baseline TCN-L has an MPKPE of 56.15mm, 30% worse than Humanoid-GPT-S (43.25mm).

### Real-World Zero-Shot Dance Tracking (MPJPE/MPJVE)

| Motion | GMT | TWIST | Any2Track | GPT-S | GPT-B |
|--------|-----|-------|-----------|-------|-------|
| Can Do Can Go! | 0.1087/1.2560 | 0.1253/1.1637 | 0.1039/1.1828 | 0.1024/1.0572 | **0.0974/0.9813** |
| Gokuraku Joudo | 0.1098/1.2865 | 0.1162/1.2731 | 0.1136/1.2366 | 0.1180/1.2362 | **0.1075/1.2257** |
| HuoYuanJia/Fearless | 0.0921/0.7054 | 0.1079/0.7821 | 0.0956/0.6410 | **0.0825/0.6209** | 0.0858/0.6158 |
| PokerFace | 0.0994/0.8217 | 0.1047/0.8893 | 0.0928/0.8641 | 0.0903/0.8476 | **0.0856/0.7325** |

Data diversity analysis: The curated dataset's log-volume is approximately 4–5x larger than AMASS. Inference latency: end-to-end <1.5ms on RTX 4090, approximately 5x faster than TWIST.

## Boundaries and Limitations

The paper does not explicitly mention detailed failure case analysis, performance floors for specific motion categories, or discussions of hardware wear or safety in real-world deployment. Acknowledged boundaries include: not incorporating richer modalities such as contact, vision, or language; not extending to interactive or multi-agent scenarios; and not coupling with long-horizon planning or VLA-style instructions.

Notable implicit limitations: All real-world experiments are based on dance motions (highly dynamic but without object interaction), which may not apply to tasks requiring environmental contact (e.g., manipulation, climbing)—such sequences were explicitly filtered during data curation. Additionally, the number of experts (approximately 384) is tightly coupled with the number of clusters; if the target motion distribution deviates significantly from the training distribution, HME clustering coverage may be insufficient.

## Engineering Insights

For reproduction or adaptation, the following aspects should be prioritized:

1. **Data pipeline is the biggest engineering risk**: The acquisition, retargeting, filtering, and augmentation pipeline for the 2B-frame corpus accounts for a far larger share of total effort than model design. If reproducing the same data scale is infeasible, first verify whether the HME sampling strategy still outperforms random sampling at the 100M-frame scale—this determines the marginal value of data curation.

2. **Expert count and distillation stability**: Approximately 384 experts is the balance point among diversity, intra-cluster consistency, and computational cost (128 is too coarse, 1024 too expensive). During DAgger distillation, batch size must be no smaller than the number of experts, otherwise mode collapse is likely—this is the most common pitfall.

3. **History length H=32 is a compromise between computation and performance**: Experiments show that 64 frames of history continue to improve performance, but computational cost grows quadratically with sequence length. If the downstream task is latency-sensitive, first verify whether 16 frames are acceptable; if pursuing maximum precision, try 64 frames and accept increased inference latency.

4. **Domain randomization parameters need platform-specific tuning**: Parameters such as ground friction 𝒰(0.3, 2.0), external force intervals 𝒰(5.0, 10.0), and torso CoM variation 𝒰(−0.15, 0.15) are tuned for the G1. When porting to other hardware, sensitivity analysis is recommended first, particularly regarding the impact of armature scaling (𝒰(1.0, 1.05)) on sim-to-real gaps.

5. **Deployment stack is open-sourced**: ONNX export + TensorRT optimization (causal attention and fused MLP kernels) + C++ streaming pipeline, with 50 Hz closed-loop control. If inference latency exceeds targets during reproduction, first check whether TensorRT has truly fused the causal attention mask—this is the key to the 1.5ms latency.

## 개요

Humanoid-GPT는 GPT 스타일의 인과적 어텐션 트랜스포머를 기반으로 하는 범용 온라인 휴머노이드 모션 트래킹 시스템입니다. 훈련 데이터를 2B 프레임(이전 연구보다 200배 이상)으로 확장하고 Harmonic Motion Embedding(HME) 데이터 큐레이션 및 전문가 증류 프로세스를 결합하여, 본 적 없는 춤 동작에 대한 제로샷 모션 트래킹을 구현했습니다. 이 시스템은 Unitree-G1에서 50Hz 폐루프 제어로 작동하며, 엔드투엔드 추론 지연 시간이 1.5ms 미만으로, 시뮬레이션 및 실제 세계 실험 모두에서 기존 베이스라인을 크게 능가합니다.

## 무엇이 바뀌었는가

모션 트래킹 분야는 오랫동안 얕은 MLP가 지배해 왔으며, 훈련 데이터는 10^4 규모의 궤적(약 7.2M 프레임)에 머물러 있었습니다. 이로 인해 '민첩성-일반화' 트레이드오프라는 해결하기 어려워 보이는 문제가 발생했습니다: 도메인 내 민첩한 동작의 트래킹 정밀도와 본 적 없는 스타일에 대한 일반화 능력을 동시에 달성하기 어려웠습니다. 저자들의 핵심 판단은 이 트레이드오프가 근본적인 한계가 아니라 데이터 규모 부족과 훈련 설계 불일치의 증상이라는 것입니다—언어/비전 분야의 대규모 확장 경로는 모션 제어에서 진지하게 시도된 적이 없었습니다.

실제로 바뀐 것은 모션 트래킹에서 '스케일 가설'의 확립입니다. 저자들은 데이터를 두 자릿수 이상 확장했을 뿐만 아니라, 데이터가 백만 프레임에서 십억 프레임으로 성장할 때 모델 구조 선택(인과적 트랜스포머 vs MLP)이 성능의 지속적 향상 또는 조기 포화를 결정한다는 것을 증명했습니다. 이는 모션 트래킹 분야의 '소규모 데이터 + 얕은 모델'이라는 기본 패러다임을 깨고, 문제를 단순한 알고리즘 튜닝이 아닌 데이터 큐레이션, 구조 매칭, 훈련 안정성의 공동 최적화로 재정의했습니다.

## 방법 분석

### 데이터 큐레이션 및 HME 샘플링
- AMASS, LAFAN1, MotionMillion, PHUMA 및 내부 수집 데이터를 통합하고, 모션 리타겟팅을 통해 Unitree-G1의 29-DoF 공간으로 매핑
- 객체 상호작용 시퀀스(의자 앉기, 수영 등)를 필터링하고, 시간 왜곡 증강(균일 가속/감속)을 적용하여 데이터를 약 5배 확장, 최종적으로 2B 프레임 코퍼스 확보
- 여러 Periodic Autoencoder를 훈련하여 각 관절의 주기적 진폭/주파수를 추출하고 HME 벡터로 집계; K-Means 클러스터링으로 약 300개의 모션 클러스터 생성(각 클러스터 1k–2k 시퀀스)
- 핵심 설계: HME는 다양성 인식, 분포 균형 샘플링에 사용—다양성 부족은 빈번한 패턴에 대한 과적합을 유발하고, 균형 부족은 능력 상한을 제한

### 모션 전문가 훈련
- 각 클러스터에 대해 PPO 정책 π: 𝒢 × 𝒮 ↦ 𝒜 훈련, 상태는 관절 위치/속도, 루트 각속도, 투영된 중력, 이전 행동 포함; 출력은 PD 컨트롤러를 통해 토크로 변환
- 보상은 신체 키포인트 수준에서 계산: R_kpt(t) = R_pos(t) + R_rot(t) + R_vel(t) + R_penal(t), 여기서 R_pos(t) = Σ w_k exp(−α_pos ‖e^pos‖₁), α_pos=1.0, α_rot=2.0, α_vel=0.03; 하체 가중치 1.5, 상체 0.75
- 훈련 후 고충실도, 장기 안정적 전문가만 유지(약 384개가 최적 균형점)

### DAgger 증류를 통한 범용 정책
- DAgger 프레임워크 사용: 반복 과정에서 학생 정책을 롤아웃하고 전문가 목표 동작을 쿼리하여 학생이 전문가 출력을 매칭하도록 훈련
- 입력 토큰 `e_t = [s_t; q_t^ref]`（고유수용감각 + 목표 참조 자세 연결）, 길이 H=32의 시퀀스가 시간적 인과 마스크 트랜스포머를 통과
- 손실은 SmoothL1Loss, 수식 (2): â = ⋃ concat_{k∈[-H+1,0]} t_i(s_{t-k}, g_{t-k}), l = ℒ(G_θ(e_{t-H+1:t}), â)
- 추론 시 H개의 히스토리 토큰 큐를 유지하고 마지막 위치 출력을 제어 목표로 사용; 배치 크기는 모드 붕괴를 피하기 위해 전문가 수 이상이어야 함

### 아키텍처 선택 이유
- 모션 트래킹은 본질적으로 인과적(테스트 시 미래 관측 없음), GPT 스타일 인과 어텐션은 배포 제약과 정렬
- 트랜스포머는 MLP 및 비인과 변형보다 데이터/모델 규모에 따라 더 깔끔하게 확장
- 병렬 시퀀스 감독 + 자기회귀 시간 예측은 트랜스포머의 고유한 장점을 활용, 위치 불변 시간 예측을 암시적으로 학습하여 에피소드 시작(히스토리 부족)에서도 안정적 출력 가능

## 핵심 혁신

1. **데이터 규모 임계점 돌파**: 2B 프레임 훈련 코퍼스는 이전 최대 연구(SONIC의 100M 프레임)보다 20배, 주류 베이스라인(7.2M 프레임)보다 200배 이상 큽니다. 이는 점진적 개선이 아니라 모션 트래킹을 처음으로 '데이터 제한' 영역으로 밀어넣은 것입니다—실험에서 200M→2B의 한계 이득 감소가 관찰되어 모델 용량이 새로운 병목이 되었음을 보여주며, 이는 소규모 데이터 영역에서는 관찰된 적이 없습니다.

2. **HME 기반 분포 균형 샘플링**: 기존 연구는 무작위 샘플링(다양성 통제 불가) 또는 균일 샘플링(빈번한 패턴 과적합)을 사용했습니다. HME는 주기적 오토인코더를 통해 고조파 특징을 추출하고 클러스터링하여 '다양성 인식 + 분포 균형'의 이중 제약을 구현합니다. 저자들은 둘 다 필수적임을 명확히 강조합니다—이는 단순한 데이터 축적이 아닌 데이터 큐레이션 방법론에 대한 중요한 기여입니다.

3. **인과적 트랜스포머를 제어 아키텍처로**: 트랜스포머는 모션 생성에 이미 적용되었지만, 온라인 트래커의 핵심 아키텍처로 사용하고 '인과성 + 규모 확장'의 시너지 효과를 체계적으로 검증한 것은 새로운 시도입니다. 핵심 통찰: 인과 어텐션은 배포 제약을 충족할 뿐만 아니라, 암시적 시간 예측을 통해 에피소드 시작 단계의 콜드 스타트 문제를 해결합니다—이는 MLP와 TCN 모두가 가지지 못한 특성입니다.

## 실험 및 결과

### 시뮬레이션 결과(AMASS-test 분할)

| 모델 | 토큰 | 파라미터 | SR (%) | MPJPE | MPJVE | RootVelErr | MPKPE (mm) |
|------|--------|------|--------|-------|-------|------------|------------|
| MLP (3-layer) | 2M | 0.25M | 76.89 | 0.1191 | 0.6081 | 0.2304 | 100.49 |
| TCN (8-layer) | 2M | 0.65M | 81.48 | 0.0885 | 0.5716 | 0.2266 | 79.75 |
| Humanoid-GPT-S | 2M | 5.7M | 83.26 | 0.0853 | 0.5492 | 0.2049 | 62.65 |
| Humanoid-GPT-S | 20M | 5.7M | 86.02 | 0.0802 | 0.5210 | 0.1868 | 46.49 |
| Humanoid-GPT-B | 200M | 22.1M | 88.27 | 0.0793 | 0.5076 | 0.1820 | 44.78 |
| Humanoid-GPT-B | 2B | 22.1M | 90.43 | 0.0768 | 0.4891 | 0.1756 | 41.49 |
| Humanoid-GPT-L | 2B | 80.4M | 92.58 | 0.0735 | 0.4820 | 0.1785 | 40.99 |

핵심 비교: MLP-L은 2M 토큰에서 75.25% SR(MLP-S의 76.89%보다 낮음), TCN-L은 2M 토큰에서 79.85%(TCN-S의 81.48%보다 낮음)—얕은 모델은 용량을 늘려도 오히려 성능이 저하됨; 최고 베이스라인 TCN-L의 MPKPE는 56.15mm로 Humanoid-GPT-S(43.25mm)보다 30% 나쁨.

### 실제 세계 제로샷 춤 트래킹(MPJPE/MPJVE)

| 동작 | GMT | TWIST | Any2Track | GPT-S | GPT-B |
|------|-----|-------|-----------|-------|-------|
| Can Do Can Go! | 0.1087/1.2560 | 0.1253/1.1637 | 0.1039/1.1828 | 0.1024/1.0572 | **0.0974/0.9813** |
| Gokuraku Joudo | 0.1098/1.2865 | 0.1162/1.2731 | 0.1136/1.2366 | 0.1180/1.2362 | **0.1075/1.2257** |
| HuoYuanJia/Fearless | 0.0921/0.7054 | 0.1079/0.7821 | 0.0956/0.6410 | **0.0825/0.6209** | 0.0858/0.6158 |
| PokerFace | 0.0994/0.8217 | 0.1047/0.8893 | 0.0928/0.8641 | 0.0903/0.8476 | **0.0856/0.7325** |

데이터 다양성 분석: 저자들이 큐레이션한 데이터셋의 log-volume은 AMASS보다 약 4–5배 증가. 추론 지연 시간: RTX 4090에서 엔드투엔드 <1.5ms, TWIST보다 약 5배 빠름.

## 경계 및 한계

논문은 실패 사례에 대한 구체적 분석, 특정 모션 카테고리의 성능 하한, 실제 세계 배포에서 하드웨어 마모 또는 안전성에 대한 논의를 명시적으로 다루지 않았습니다. 저자들이 인정한 경계: 접촉, 시각 또는 언어와 같은 더 풍부한 모달리티 미포함; 상호작용 또는 다중 에이전트 시나리오로 확장되지 않음; 장기 계획 또는 VLA 스타일 명령과 결합되지 않음.

주목할 만한 암시적 한계: 모든 실제 세계 실험은 춤 동작(고역동적이지만 객체 상호작용 없음)을 기반으로 하며, 환경 접촉이 필요한 작업(예: 조작, 등반)에는 적용되지 않을 수 있음—데이터 큐레이션 단계에서 이러한 시퀀스를 명시적으로 필터링했습니다. 또한 전문가 수(약 384개)는 클러스터 수와 높은 연관성을 가지며, 목표 모션 분포가 훈련 분포와 크게 다를 경우 HME 클러스터링의 커버리지가 부족할 수 있습니다.

## 엔지니어링 시사점

이 작업을 재현하거나 참고하려면 다음 단계를 우선적으로 검토하는 것이 좋습니다:

1. **데이터 파이프라인이 가장 큰 엔지니어링 리스크**: 2B 프레임 코퍼스의 획득, 리타겟팅, 필터링 및 증강 프로세스는 모델 설계보다 훨씬 큰 비중을 차지합니다. 동일한 규모의 데이터를 재현할 수 없다면, HME 샘플링 전략이 100M 프레임 규모에서도 무작위 샘플링보다 우수한지 먼저 검증하세요—이는 데이터 큐레이션의 한계 가치를 결정합니다.

2. **전문가 수와 증류 안정성**: 약 384개의 전문가는 다양성, 클러스터 내 일관성 및 계산 비용의 균형점입니다(128은 너무 거칠고, 1024는 비용이 높음). DAgger 증류 시 배치 크기는 반드시 전문가 수 이상이어야 하며, 그렇지 않으면 모드 붕괴가 발생하기 쉽습니다—가장 쉽게 함정에 빠지는 세부 사항입니다.

3. **히스토리 길이 H=32는 계산과 성능의 절충**: 실험에서 64프레임 히스토리가 지속적으로 성능을 향상시키지만, 계산량은 시퀀스 길이에 따라 2차적으로 증가합니다. 하위 작업이 지연 시간에 민감하다면 16프레임이 허용 가능한지 먼저 검증하세요; 최고 정밀도를 추구한다면 64프레임을 시도하고 추론 지연 시간 증가를 수용하세요.

4. **도메인 무작위화 파라미터는 플랫폼에 따라 조정 필요**: 지면 마찰 𝒰(0.3, 2.0), 외력 간격 𝒰(5.0, 10.0), 몸통 CoM 변화 𝒰(−0.15, 0.15) 등의 파라미터는 G1에 맞게 튜닝되었습니다. 다른 하드웨어로 이전할 때는 특히 armature 스케일링(𝒰(1.0, 1.05))이 시뮬레이션-실제 차이에 미치는 영향에 대한 민감도 분석을 먼저 수행하는 것이 좋습니다.

5. **배포 스택은 오픈소스**: ONNX 내보내기 + TensorRT 최적화(인과 어텐션 및 융합 MLP 커널) + C++ 스트리밍 파이프라인, 50Hz 폐루프 제어. 재현 시 추론 지연 시간이 기준을 초과하면 TensorRT가 인과 어텐션 마스크를 실제로 융합했는지 먼저 확인하세요—이것이 1.5ms 지연 시간의 핵심입니다.
