---
$id: ent_paper_resmimic_from_general_motion_t_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'ResMimic: From General Motion Tracking to Humanoid Whole-body Loco-Manipulation via Residual Learning'
  zh: ResMimic｜从一般运动跟踪到通过残差学习的人形全身移动操作
  ko: 'ResMimic: From General Motion Tracking to Humanoid Whole-body Loco-Manipulation via Residual Learning'
summary:
  en: RESMIMIC first recovers the scene, target or motion representation from the ontology state and joint sequence, human
    video/motion capture trajectory, and simulation interaction data, and then uses the policy network and control module
    to generate the whole-body trajectory/action sequence. The key point is to put the policy network and the control module
    in the same training/deployment link, reducing the discontinuity between high-level goals and low-level actions.
  zh: ResMimic 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用策略网络和控制模块生成全身轨迹/动作序列。关键点是把策略网络和控制模块放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
  ko: ResMimic 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用策略网络和控制模块生成全身轨迹/动作序列。关键点是把策略网络和控制模块放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- human_video
- interaction_planning
- motion_capture
- motion_retargeting
- resmimic
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: ResMimic project page
  url: https://resmimic.github.io/
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人在复杂环境中实现全身协调的移动与操作（Loco-Manipulation）一直是机器人学领域的核心挑战，其难点在于如何弥合高层任务规划与低层运动控制之间的语义鸿沟。现有方法通常将运动跟踪与操作控制分离处理，导致策略在从仿真迁移到真实场景时出现显著的性能下降。为此，本文提出ResMimic框架，通过引入残差学习机制，将策略网络与控制模块整合至统一的训练与部署链路中，从而有效减少高层目标到低层动作之间的断点，实现从一般运动跟踪到全身移动操作的高效迁移。

## 核心内容

研究背景与问题方面，人形机器人全身移动操作面临两大核心难题：一是如何从多种异构数据源（如人类视频、运动捕捉轨迹、仿真交互数据）中提取鲁棒的运动表征；二是如何确保高层策略规划出的动作序列能够被底层控制器精确执行。传统方法往往将运动重定向（Motion Retargeting）与操作控制作为独立模块设计，导致策略网络输出的抽象指令与电机层面的关节指令之间存在不连续性，进而引发动作抖动、任务失败等问题。ResMimic正是针对这一“策略-控制断点”问题而设计。

在方法或模型框架层面，ResMimic构建了一个端到端的残差学习架构。其核心流程分为两个阶段：首先，系统从本体状态与关节序列、人类视频/动捕轨迹以及仿真交互数据中恢复场景、目标或运动表征，形成统一的隐式表示；随后，策略网络与控制模块在同一训练链路中联合优化，策略网络负责生成全身轨迹的粗粒度规划，而控制模块则通过残差学习方式对规划结果进行细粒度修正。这种设计使得高层语义目标（如“抓取物体”）能够直接映射为低层关节力矩指令，避免了传统分层架构中的信息损失。

关键技术创新主要体现在三个方面。第一，残差学习机制的引入使得控制模块可以专注于补偿策略网络输出与真实物理动态之间的偏差，而非从零开始学习全部控制映射，显著提升了样本效率与泛化能力。第二，ResMimic支持多模态数据源融合，能够同时利用人类演示视频中的语义信息与仿真环境中的物理约束，从而在缺乏精确动力学模型的情况下仍能生成可行动作。第三，框架将策略网络与控制模块置于同一条训练/部署链路中，实现了从感知到动作的端到端梯度传播，从根本上消除了模块间的不连续性。

实验验证与应用价值方面，ResMimic在多种人形机器人全身移动操作任务上进行了评估，包括动态行走中抓取物体、跨越障碍物后操作工具等复杂场景。实验结果表明，相比基线方法（如独立训练的运动重定向与控制器组合），ResMimic在任务成功率、动作平滑度以及对外部扰动的鲁棒性上均取得显著提升。此外，该框架对数据源的依赖性较低，即使仅使用单目视频作为输入，也能生成具有合理物理特性的全身动作序列，这为人形机器人在非结构化环境中的实际部署提供了可行路径。其应用价值不仅局限于实验室场景，更可拓展至灾害救援、家庭服务、工业协作等需要全身协调操作的领域。

## 参考
- ResMimic project page (https://resmimic.github.io/)

## Overview

Achieving whole-body coordinated locomotion and manipulation (Loco-Manipulation) for humanoid robots in complex environments has long been a core challenge in robotics. The difficulty lies in bridging the semantic gap between high-level task planning and low-level motion control. Existing methods typically treat motion tracking and manipulation control as separate processes, leading to significant performance degradation when policies are transferred from simulation to real-world scenarios. To address this, the paper proposes the ResMimic framework, which introduces a residual learning mechanism to integrate the policy network and control module into a unified training and deployment pipeline. This effectively reduces the discontinuities between high-level objectives and low-level actions, enabling efficient transfer from general motion tracking to whole-body loco-manipulation.

## Content

In terms of research background and problem, whole-body loco-manipulation for humanoid robots faces two core challenges: first, how to extract robust motion representations from diverse heterogeneous data sources (e.g., human videos, motion capture trajectories, simulation interaction data); second, how to ensure that action sequences planned by high-level policies can be precisely executed by low-level controllers. Traditional methods often design motion retargeting and manipulation control as independent modules, leading to discontinuities between the abstract instructions output by the policy network and the joint-level commands at the motor level, resulting in issues such as action jitter and task failure. ResMimic is specifically designed to address this "policy-control discontinuity" problem.

At the methodological or model framework level, ResMimic constructs an end-to-end residual learning architecture. Its core process consists of two stages: first, the system recovers scene, object, or motion representations from proprioceptive states and joint sequences, human videos/motion capture trajectories, and simulation interaction data, forming a unified implicit representation; subsequently, the policy network and control module are jointly optimized within the same training pipeline. The policy network generates coarse-grained plans for whole-body trajectories, while the control module performs fine-grained corrections on these plans through residual learning. This design allows high-level semantic objectives (e.g., "grasp an object") to be directly mapped to low-level joint torque commands, avoiding information loss inherent in traditional hierarchical architectures.

Key technological innovations are primarily reflected in three aspects. First, the introduction of the residual learning mechanism enables the control module to focus on compensating for deviations between the policy network's output and real physical dynamics, rather than learning the entire control mapping from scratch, significantly improving sample efficiency and generalization capability. Second, ResMimic supports the fusion of multimodal data sources, simultaneously leveraging semantic information from human demonstration videos and physical constraints from simulation environments, thereby generating feasible actions even in the absence of accurate dynamic models. Third, the framework places the policy network and control module within the same training/deployment pipeline, achieving end-to-end gradient propagation from perception to action, fundamentally eliminating discontinuities between modules.

In terms of experimental validation and application value, ResMimic was evaluated on various whole-body loco-manipulation tasks for humanoid robots, including grasping objects during dynamic walking and operating tools after crossing obstacles. Experimental results show that compared to baseline methods (e.g., independently trained motion retargeting and controller combinations), ResMimic achieves significant improvements in task success rate, action smoothness, and robustness to external disturbances. Furthermore, the framework has low dependency on data sources; even when using only monocular video as input, it can generate whole-body action sequences with reasonable physical characteristics, providing a feasible path for deploying humanoid robots in unstructured environments. Its application value extends beyond laboratory settings to fields requiring whole-body coordinated operations, such as disaster rescue, home services, and industrial collaboration.

## 개요

휴머노이드 로봇이 복잡한 환경에서 전신 조화를 이루며 이동과 조작(Loco-Manipulation)을 수행하는 것은 로봇공학 분야의 핵심 과제로, 그 난점은 고수준 작업 계획과 저수준 운동 제어 사이의 의미론적 간극을 어떻게 해소할 것인가에 있습니다. 기존 방법은 일반적으로 운동 추적과 조작 제어를 분리하여 처리하기 때문에, 정책이 시뮬레이션에서 실제 환경으로 전이될 때 성능이 현저히 저하됩니다. 이에 본 논문은 ResMimic 프레임워크를 제안하며, 잔차 학습 메커니즘을 도입하여 정책 네트워크와 제어 모듈을 통합된 훈련 및 배포 파이프라인으로 결합함으로써 고수준 목표에서 저수준 동작 사이의 단절을 효과적으로 줄이고, 일반적인 운동 추적에서 전신 이동 조작으로의 효율적인 전이를 실현합니다.

## 핵심 내용

연구 배경 및 문제 측면에서, 휴머노이드 로봇의 전신 이동 조작은 두 가지 핵심 난제에 직면합니다. 첫째는 다양한 이종 데이터 소스(예: 인간 비디오, 모션 캡처 궤적, 시뮬레이션 상호작용 데이터)로부터 강건한 운동 표현을 추출하는 방법이고, 둘째는 고수준 정책이 계획한 동작 시퀀스를 저수준 제어기가 정확히 실행할 수 있도록 보장하는 방법입니다. 전통적인 방법은 종종 모션 리타겟팅(Motion Retargeting)과 조작 제어를 독립적인 모듈로 설계하여, 정책 네트워크가 출력하는 추상 명령과 모터 수준의 관절 명령 사이에 불연속성이 발생하고, 이로 인해 동작 떨림, 작업 실패 등의 문제가 초래됩니다. ResMimic은 바로 이러한 '정책-제어 단절' 문제를 해결하기 위해 설계되었습니다.

방법 또는 모델 프레임워크 측면에서, ResMimic은 종단 간 잔차 학습 아키텍처를 구축합니다. 핵심 프로세스는 두 단계로 나뉩니다. 먼저, 시스템은 자체 상태와 관절 시퀀스, 인간 비디오/모션 캡처 궤적, 시뮬레이션 상호작용 데이터로부터 장면, 목표 또는 운동 표현을 복원하여 통합된 잠재 표현을 형성합니다. 이후, 정책 네트워크와 제어 모듈이 동일한 훈련 파이프라인에서 공동으로 최적화되며, 정책 네트워크는 전신 궤적의 조잡한 계획을 생성하고 제어 모듈은 잔차 학습 방식을 통해 계획 결과를 세밀하게 수정합니다. 이러한 설계는 '물체 잡기'와 같은 고수준 의미론적 목표를 저수준 관절 토크 명령으로 직접 매핑할 수 있게 하여, 전통적인 계층 구조에서의 정보 손실을 방지합니다.

핵심 기술 혁신은 주로 세 가지 측면에서 나타납니다. 첫째, 잔차 학습 메커니즘의 도입으로 제어 모듈은 처음부터 모든 제어 매핑을 학습하는 대신 정책 네트워크 출력과 실제 물리적 동역학 간의 편차를 보상하는 데 집중할 수 있어, 샘플 효율성과 일반화 능력이 크게 향상됩니다. 둘째, ResMimic은 다중 모달 데이터 소스 융합을 지원하여 인간 시연 비디오의 의미론적 정보와 시뮬레이션 환경의 물리적 제약을 동시에 활용함으로써, 정확한 동역학 모델이 없는 상황에서도 실행 가능한 동작을 생성할 수 있습니다. 셋째, 프레임워크는 정책 네트워크와 제어 모듈을 동일한 훈련/배포 파이프라인에 배치하여 인식에서 동작까지의 종단 간 그래디언트 전파를 실현함으로써, 모듈 간 불연속성을 근본적으로 제거합니다.

실험 검증 및 응용 가치 측면에서, ResMimic은 동적 보행 중 물체 잡기, 장애물 넘기 후 도구 조작 등 다양한 휴머노이드 로봇 전신 이동 조작 작업에서 평가되었습니다. 실험 결과, 기준 방법(예: 독립적으로 훈련된 모션 리타겟팅과 제어기 조합)과 비교하여 ResMimic은 작업 성공률, 동작 부드러움, 외부 교란에 대한 강건성에서 현저한 향상을 보였습니다. 또한, 이 프레임워크는 데이터 소스에 대한 의존도가 낮아, 단안 비디오만을 입력으로 사용하더라도 합리적인 물리적 특성을 가진 전신 동작 시퀀스를 생성할 수 있으며, 이는 비구조화된 환경에서 휴머노이드 로봇의 실제 배포를 위한 실현 가능한 경로를 제공합니다. 그 응용 가치는 실험실 환경에 국한되지 않고, 전신 조화 작업이 필요한 재난 구조, 가사 서비스, 산업 협력 등의 분야로 확장될 수 있습니다.
