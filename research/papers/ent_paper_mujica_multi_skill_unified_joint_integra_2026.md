---
$id: ent_paper_mujica_multi_skill_unified_joint_integra_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'MUJICA: Multi-skill Unified Joint Integration of Control Architecture for Wheeled-Legged Robots'
  zh: 'MUJICA: Multi-skill Unified Joint Integration of Control Architecture for Wheeled-Legged Robots'
  ko: 'MUJICA: Multi-skill Unified Joint Integration of Control Architecture for Wheeled-Legged Robots'
summary:
  en: 'Wheeled-legged robots hold promise for traversing complex terrains and offer superior mobility compared to legged robots.
    However, wheeled-legged robots must effectively balance both wheeled driving and legged control. Institutions per source
    list: 复旦大学智能机器人与先进制造学院等.'
  zh: MUJICA 是一种为轮腿机器人设计的统一全本体感知控制架构，由研究团队提出。其核心贡献在于将多种低级技能（如全向移动、高台攀爬、跌倒恢复）集成于单一策略中，并通过精确的直流电机约束建模和高级技能选择器，实现了从仿真到现实的高鲁棒性自适应运动。在
    Unitree Go2-W 机器人上的实验验证了其在非结构化环境中适应性和任务成功率的显著提升。
  ko: 'Wheeled-legged robots hold promise for traversing complex terrains and offer superior mobility compared to legged robots.
    However, wheeled-legged robots must effectively balance both wheeled driving and legged control. Institutions per source
    list: 复旦大学智能机器人与先进制造学院等.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- mujica
- multi
- skill
- unified
- joint
verification:
  status: verified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 724 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2605.13058 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2605.13058v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline. 深读+数字白名单复核通过 2026-08-10（批量一）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2605.13058 MUJICA: Multi-skill Unified Joint Integration of Control Architecture for Wheeled-Legged Robots'
  url: https://arxiv.org/abs/2605.13058
  accessed_at: '2026-07-31'
  date: '2026-05-13'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

MUJICA 是一个面向轮足机器人的统一控制架构，由研究团队提出，旨在将全向移动、高平台攀爬和跌倒恢复等多种动力学差异显著的技能整合进单一本体感知策略中，并通过高层技能选择器实现无需外部感知的自动切换。其核心贡献在于引入精确的 DC 电机约束建模与安全感知的约束强化学习框架，在提升电机性能利用率的同时，保证了 sim-to-real 迁移的可靠性与实机部署的安全性。

## 它改变了什么

轮足机器人领域长期存在一个结构性矛盾：轮式驱动的高效性与腿部越障的灵活性在控制层面难以统一。现有框架大多从足式机器人方法直接修改而来，这导致它们继承了足式机器人对动力学差异的“钝感”——当技能从平地行走扩展到疾驰、溜蹄或攀爬时，这些方法要么只能处理相似技能，要么依赖手动切换，无法在单一策略中自适应地整合。更关键的是，大多数学习方法在建模执行器时采用简化驱动约束，完全忽略了 DC 电机扭矩随速度变化以及随关节位置变化的物理限制。这种简化在仿真中或许无碍，但在实机上会导致策略频繁请求超出电机物理极限的扭矩，轻则性能退化，重则触发保护机制甚至损坏硬件。

MUJICA 真正改变的不是“又提出一个多技能 RL 框架”，而是将“电机物理约束”从训练后的工程修补提升为训练中的一等公民。它把问题重新建模为约束部分可观测马尔可夫决策过程（C-POMDP），让策略在优化长期回报的同时必须满足 k 个硬约束。这一转变意味着，策略不再被允许“幻想”超出电机能力的动作，而是在约束边界内寻找最优解。同时，通过引入技能指示变量将不同技能的观测空间沿特定维度解耦，MUJICA 让单一策略网络能够容纳动力学差异巨大的技能，而无需为每个技能单独训练网络——这为轮足机器人在复杂真实环境中的自主连续作业提供了架构层面的可能性。

## 方法拆解

MUJICA 采用分层架构，底层为多技能统一策略，高层为技能选择器，两层均基于本体感知训练。

### 问题建模与观测/动作空间
- 问题建模为 C-POMDP，目标为最大化长期回报同时满足 k 个约束（式1）。
- 观测向量仅含本体感觉：`o_t = [ω_t^b, g_t^b, cmd_t, q_t, q̇_t, a_t, ζ_t]^T`（式2），其中 `ζ_t` 为技能指示变量，每个技能关联唯一指示值，使各技能观测空间沿该维度解耦。
- 特权状态 `s_t = [o_t, v_t, c_t, u_t, h_t]^T`（式3），包含基座线速度、碰撞概率、轮地距离和潜在状态，仅训练时可用。
- 动作空间：腿部关节输出相对默认姿态的角度偏移，轮关节输出期望电机速度；通过 PD 控制器转换：轮关节 `τ = K_d(q̇ - a)`，其他关节 `τ = K_p(q - a) - K_d·q̇`（式4）。

### 状态估计器
- 基于过去 H=6 帧观测，使用 GRU 网络处理时间依赖，预测潜在状态、基座线速度、各部件碰撞概率和轮地距离（式5）。
- 引入参考编码器映射后继观测到潜在状态作为监督目标，使用 SwAV 对比学习损失。
- 总损失 `L_Estimate = L_Pred + L_SwAV(e_t, ê_t)`（式6），其中 `L_Pred` 包含速度 MSE、碰撞 BCE 和轮地距离 MSE（式7）。

### 非对称 Actor-Critic 与约束优化
- 策略网络接收在线编码器的历史嵌入 `f_t` 和当前观测 `o_t`；奖励评论家和约束评论家接收特权观测。
- 采用 P3O 框架处理约束优化，损失为 `L_P3O(θ) = L_R_CLIP(θ) + κ·Σ_i max{0, L_Ci_CLIP(θ)}`（式8），其中 `κ` 为惩罚因子。

### DC 电机约束建模
- 每个执行器受速度相关扭矩限制：低速时最大扭矩恒定，高速时随速度线性下降。
- 小腿关节扭矩受关节速度和关节位置共同影响，最大扭矩在固定速度下随关节位置呈余弦依赖。
- 数值来自 Unitree 官方电机手册，确保约束与真实硬件一致。

### 技能选择器
- 高层选择器接收最近 H 帧观测（不含 `ζ_t`），输出预定义技能集合上的概率分布。
- 使用非对称 actor-critic 网络在冻结的低层技能策略之上训练（Training S2），通过统一速度跟踪奖励学习。

### 课程学习
- 构建 33×20 网格多任务环境，4,096 个机器人分配到不同行（对应特定地形和任务），每列代表课程难度级别。
- 全向移动和高平台攀爬在成功维持速度跟踪后推进课程；跌倒恢复在机器人接近标准站立姿态时推进课程。

## 关键创新

1. **精确 DC 电机约束的端到端嵌入**：将速度相关和位置相关的扭矩限制直接建模进 C-POMDP 的约束条件中，而非事后裁剪。这是对现有 RL 控制框架的根本性修正——策略在训练阶段就学会在电机物理边界内动作，消融实验显示无约束时超过 90% 的样本位于可行电机区域之外，有约束时违规率降至 3.5% 以下。这一设计直接决定了 sim-to-real 迁移的安全性。

2. **技能指示变量驱动的观测空间解耦**：通过 `ζ_t` 将不同技能的观测空间沿单一维度分离，使单一策略网络能够容纳动力学差异巨大的技能（如跌倒恢复与疾驰），而无需为每个技能单独训练网络或手动切换。这一机制的可扩展性意味着后续新增技能只需分配新的指示值，无需重新设计架构。

3. **高层技能选择器的纯本体感知自动切换**：选择器仅基于最近 H 帧观测（不含 `ζ_t`）输出技能概率分布，在冻结的低层策略之上训练。这使得机器人能够在真实环境中自主完成“跌倒→恢复→攀爬→移动”的连续任务链，无需外部感知或人工干预，实机演示中成功完成了从随机跌倒姿态到攀爬 60 cm 高平台的连续任务。

## 实验与结果

实验在 IsaacLab 仿真平台进行，4,096 个并行环境，单张 NVIDIA RTX 4090 GPU，机器人平台为 Unitree Go2-W。低层训练 30,000 次迭代，高层训练 10,000 次迭代，控制频率 50 Hz，物理仿真 200 Hz。测试设置包含 5 个代表性任务、10 个递增难度级别（级别间隔相对训练加倍）、4 个随机种子。

### 技能选择器评估（表 III）
单集串联三个中等难度任务（难度 5/10）：楼梯恢复、楼梯攀爬、高平台攀爬，100 集测试。

| 方法 | 楼梯恢复成功率 | 楼梯攀爬成功率 | 高平台攀爬成功率 |
|------|--------------|--------------|----------------|
| w/o indicator（共享奖励） | ✖ | ✖ | ✖ |
| w/o indicator（分离奖励） | 72% | 72% | 38% |
| with skill selector（ours） | 95% | 95% | 91% |

### DC 电机约束消融
| 配置 | 可行电机区域外样本比例 |
|------|---------------------|
| 无约束 | >90% |
| 有约束 | <3.5% |

### 实机实验
- 楼梯恢复：在 30° 楼梯上从倒置状态成功恢复。
- 楼梯攀爬：成功攀爬不同踏步高度和不规则边缘的楼梯。
- 高平台攀爬：成功攀爬 80 cm 高室外平台和 1 m 高室内平台（接近机械和驱动极限）。

结果表明，技能指示变量的引入对多技能切换至关重要——共享奖励下三项任务全部失败，分离奖励下高平台攀爬仅 38%，而完整 MUJICA 将三项任务成功率均提升至 90% 以上。DC 电机约束的消融验证了约束建模对策略安全性的决定性作用。

## 边界与局限

论文未明确讨论局限性。从事实要点可推断的边界包括：实机实验仅验证了楼梯恢复、楼梯攀爬和高平台攀爬，未在真实世界中测试跌倒恢复技能；技能选择器仅在难度 5/10 级别评估，更高难度下的切换鲁棒性未知；仅验证了 Unitree Go2-W 单一平台，未推广至其他轮足机器人；未提供计算资源需求或推理延迟的详细分析，实机计算平台为 NVIDIA Jetson Orin NX，但未说明其资源占用裕度。此外，DC 电机约束数值来自 Unitree 官方手册，若迁移至其他电机型号需重新标定。

## 工程启示

复现 MUJICA 时，优先核对以下环节：第一，DC 电机约束的数值必须与目标机器人官方手册严格一致，这是消融实验中违规率从 90% 降至 3.5% 的关键，也是 sim-to-real 安全性的根基——若电机手册数据缺失，建议先通过实测拟合扭矩-速度-位置曲线。第二，技能指示变量 `ζ_t` 的编码方式直接影响多技能解耦效果，表 III 显示共享奖励下全部失败，分离奖励下高平台攀爬仅 38%，因此务必为每个技能分配唯一指示值，并在观测向量中正确传递。第三，课程学习的推进条件需精确复现：全向移动和高平台攀爬在成功维持速度跟踪后推进，跌倒恢复在接近标准站立姿态时推进，推进条件过松或过紧都会影响训练稳定性。第四，高层技能选择器训练时低层策略必须冻结，否则会破坏已学技能。最容易踩坑的地方在于状态估计器的 SwAV 对比损失——参考编码器与在线编码器的同步更新机制若不正确，会导致潜在状态漂移，进而影响碰撞概率和轮地距离的预测精度，最终削弱约束满足能力。建议在仿真中先复现 DC 电机约束消融实验（无约束 vs 有约束），确认违规率指标达标后再进行完整训练。

## 参考
- https://arxiv.org/abs/2605.13058
- https://github.com/ImChong/Robotics_Notebooks

## Overview

MUJICA is a unified control architecture for wheel-legged robots proposed by the research team, aiming to integrate multiple skills with significantly different dynamics—such as omnidirectional locomotion, high-platform climbing, and fall recovery—into a single proprioceptive policy, with a high-level skill selector enabling automatic switching without external perception. Its core contribution lies in introducing precise DC motor constraint modeling and a safety-aware constrained reinforcement learning framework, which improves motor performance utilization while ensuring reliable sim-to-real transfer and safe real-world deployment.

## What It Changes

The wheel-legged robot field has long faced a structural contradiction: the efficiency of wheeled driving and the flexibility of legged obstacle traversal are difficult to unify at the control level. Most existing frameworks are directly adapted from legged robot methods, inheriting their "insensitivity" to dynamic differences—when skills expand from flat-ground walking to sprinting, pacing, or climbing, these methods either handle only similar skills or rely on manual switching, failing to adaptively integrate them within a single policy. More critically, most learning methods model actuators with simplified drive constraints, completely ignoring the physical limits of DC motor torque varying with speed and joint position. Such simplifications may be harmless in simulation, but on real hardware they cause policies to frequently request torques beyond motor physical limits, leading to performance degradation at best, or triggering protection mechanisms and even hardware damage at worst.

What MUJICA truly changes is not "yet another multi-skill RL framework," but elevating "motor physical constraints" from post-training engineering patches to first-class citizens during training. It reformulates the problem as a constrained partially observable Markov decision process (C-POMDP), requiring the policy to satisfy k hard constraints while optimizing long-term rewards. This shift means the policy is no longer allowed to "fantasize" actions beyond motor capabilities, but must find optimal solutions within constraint boundaries. Meanwhile, by introducing a skill indicator variable to decouple observation spaces of different skills along a specific dimension, MUJICA enables a single policy network to accommodate skills with vastly different dynamics without training separate networks for each skill—providing architectural possibilities for autonomous continuous operation of wheel-legged robots in complex real-world environments.

## Method Breakdown

MUJICA adopts a hierarchical architecture: the low level is a multi-skill unified policy, and the high level is a skill selector, both trained based on proprioception.

### Problem Formulation and Observation/Action Spaces
- The problem is formulated as a C-POMDP, aiming to maximize long-term rewards while satisfying k constraints (Eq. 1).
- The observation vector contains only proprioceptive signals: `o_t = [ω_t^b, g_t^b, cmd_t, q_t, q̇_t, a_t, ζ_t]^T` (Eq. 2), where `ζ_t` is the skill indicator variable, with each skill associated with a unique indicator value, decoupling observation spaces of different skills along this dimension.
- The privileged state `s_t = [o_t, v_t, c_t, u_t, h_t]^T` (Eq. 3) includes base linear velocity, collision probability, wheel-ground distance, and latent state, available only during training.
- Action space: leg joints output angular offsets relative to default postures, wheel joints output desired motor speeds; converted via PD controllers: wheel joints `τ = K_d(q̇ - a)`, other joints `τ = K_p(q - a) - K_d·q̇` (Eq. 4).

### State Estimator
- Based on the past H=6 frames of observations, a GRU network processes temporal dependencies to predict latent states, base linear velocity, per-part collision probabilities, and wheel-ground distance (Eq. 5).
- A reference encoder maps subsequent observations to latent states as supervision targets, using SwAV contrastive learning loss.
- Total loss `L_Estimate = L_Pred + L_SwAV(e_t, ê_t)` (Eq. 6), where `L_Pred` includes velocity MSE, collision BCE, and wheel-ground distance MSE (Eq. 7).

### Asymmetric Actor-Critic and Constrained Optimization
- The policy network receives historical embeddings `f_t` from the online encoder and current observation `o_t`; the reward critic and constraint critics receive privileged observations.
- The P3O framework handles constrained optimization, with loss `L_P3O(θ) = L_R_CLIP(θ) + κ·Σ_i max{0, L_Ci_CLIP(θ)}` (Eq. 8), where `κ` is the penalty factor.

### DC Motor Constraint Modeling
- Each actuator is subject to speed-dependent torque limits: maximum torque is constant at low speeds and decreases linearly with speed at high speeds.
- Shank joint torque is affected by both joint speed and joint position; maximum torque exhibits cosine dependence on joint position at fixed speeds.
- Numerical values are sourced from the Unitree official motor manual, ensuring constraints align with real hardware.

### Skill Selector
- The high-level selector receives the most recent H frames of observations (excluding `ζ_t`) and outputs a probability distribution over a predefined skill set.
- Trained using an asymmetric actor-critic network on top of the frozen low-level skill policy (Training S2), learning through a unified velocity tracking reward.

### Curriculum Learning
- A 33×20 grid multi-task environment is constructed, with 4,096 robots assigned to different rows (corresponding to specific terrains and tasks), each column representing a curriculum difficulty level.
- Omnidirectional locomotion and high-platform climbing advance the curriculum after successfully maintaining velocity tracking; fall recovery advances when the robot approaches a standard standing posture.

## Key Innovations

1. **End-to-End Embedding of Precise DC Motor Constraints**: Speed-dependent and position-dependent torque limits are directly modeled into the C-POMDP constraints rather than post-hoc clipping. This is a fundamental correction to existing RL control frameworks—the policy learns to act within motor physical boundaries during training. Ablation experiments show that without constraints, over 90% of samples fall outside the feasible motor region, while with constraints, the violation rate drops below 3.5%. This design directly determines the safety of sim-to-real transfer.

2. **Skill Indicator Variable-Driven Observation Space Decoupling**: By separating observation spaces of different skills along a single dimension via `ζ_t`, a single policy network can accommodate skills with vastly different dynamics (e.g., fall recovery vs. sprinting) without training separate networks or manual switching. The scalability of this mechanism means future skills only require assigning new indicator values without redesigning the architecture.

3. **Pure Proprioceptive Automatic Switching via High-Level Skill Selector**: The selector outputs skill probability distributions based solely on the most recent H frames of observations (excluding `ζ_t`), trained on top of the frozen low-level policy. This enables the robot to autonomously complete the continuous task chain of "fall → recover → climb → move" in real environments without external perception or human intervention. Real-world demonstrations successfully completed continuous tasks from random fall postures to climbing a 60 cm high platform.

## Experiments and Results

Experiments were conducted on the IsaacLab simulation platform with 4,096 parallel environments on a single NVIDIA RTX 4090 GPU, using the Unitree Go2-W robot platform. Low-level training ran for 30,000 iterations, high-level training for 10,000 iterations, with a control frequency of 50 Hz and physics simulation at 200 Hz. Test setups included 5 representative tasks, 10 increasing difficulty levels (level intervals doubled relative to training), and 4 random seeds.

### Skill Selector Evaluation (Table III)
Single episodes concatenating three medium-difficulty tasks (difficulty 5/10): stair recovery, stair climbing, and high-platform climbing, tested over 100 episodes.

| Method | Stair Recovery Success Rate | Stair Climbing Success Rate | High-Platform Climbing Success Rate |
|--------|---------------------------|---------------------------|-----------------------------------|
| w/o indicator (shared reward) | ✖ | ✖ | ✖ |
| w/o indicator (separate rewards) | 72% | 72% | 38% |
| with skill selector (ours) | 95% | 95% | 91% |

### DC Motor Constraint Ablation
| Configuration | Proportion of Samples Outside Feasible Motor Region |
|---------------|-----------------------------------------------------|
| Without constraints | >90% |
| With constraints | <3.5% |

### Real-World Experiments
- Stair recovery: Successfully recovered from an inverted state on a 30° staircase.
- Stair climbing: Successfully climbed stairs with varying step heights and irregular edges.
- High-platform climbing: Successfully climbed an 80 cm outdoor platform and a 1 m indoor platform (near mechanical and actuation limits).

Results demonstrate that the skill indicator variable is critical for multi-skill switching—all three tasks failed under shared rewards, high-platform climbing reached only 38% under separate rewards, while the full MUJICA improved success rates across all three tasks to over 90%. The DC motor constraint ablation validates the decisive role of constraint modeling in policy safety.

## Boundaries and Limitations

The paper does not explicitly discuss limitations. Inferred boundaries from factual points include: real-world experiments only validated stair recovery, stair climbing, and high-platform climbing, without testing fall recovery in the real world; the skill selector was only evaluated at difficulty level 5/10, leaving switching robustness at higher difficulties unknown; only the Unitree Go2-W platform was validated, without generalization to other wheel-legged robots; no detailed analysis of computational resource requirements or inference latency was provided, with the real-world computing platform being NVIDIA Jetson Orin NX but without specifying resource utilization margins. Additionally, DC motor constraint values come from the Unitree official manual, requiring recalibration for other motor models.

## Engineering Insights

When reproducing MUJICA, prioritize verifying the following aspects: First, DC motor constraint values must strictly match the target robot's official manual—this is key to reducing violation rates from 90% to 3.5% in ablations and is the foundation of sim-to-real safety; if motor manual data is unavailable, it is recommended to fit torque-speed-position curves through real measurements first. Second, the encoding of the skill indicator variable `ζ_t` directly affects multi-skill decoupling effectiveness; Table III shows complete failure under shared rewards and only 38% for high-platform climbing under separate rewards, so assigning unique indicator values per skill and correctly passing them in the observation vector is essential. Third, curriculum advancement conditions must be precisely reproduced: omnidirectional locomotion and high-platform climbing advance after successfully maintaining velocity tracking, while fall recovery advances when approaching a standard standing posture; overly loose or tight conditions will affect training stability. Fourth, the low-level policy must be frozen during high-level skill selector training, otherwise learned skills will be disrupted. The most error-prone area is the SwAV contrastive loss in the state estimator—incorrect synchronization between the reference encoder and online encoder updates can cause latent state drift, degrading collision probability and wheel-ground distance prediction accuracy, ultimately weakening constraint satisfaction. It is recommended to first reproduce the DC motor constraint ablation in simulation (without vs. with constraints) to confirm violation rate metrics before proceeding to full training.

## 개요

MUJICA는 연구팀이 제안한 바퀴 달린 로봇을 위한 통합 제어 아키텍처로, 전방향 이동, 높은 플랫폼 등반, 낙상 복구 등 동역학적 특성이 크게 다른 여러 기술을 단일 자기수용 정책에 통합하고, 고수준 기술 선택기를 통해 외부 인식 없이 자동으로 전환할 수 있도록 설계되었습니다. 핵심 기여는 정밀한 DC 모터 제약 조건 모델링과 안전 인식 제약 강화 학습 프레임워크를 도입하여 모터 성능 활용도를 높이는 동시에 sim-to-real 전이의 신뢰성과 실기 배치의 안전성을 보장한 것입니다.

## 무엇을 바꾸었는가

바퀴 달린 로봇 분야에는 오랫동안 구조적 모순이 존재해 왔습니다: 바퀴 구동의 효율성과 다리 기반 장애물 통과의 유연성은 제어 계층에서 통합하기 어렵습니다. 기존 프레임워크는 대부분 족식 로봇 방법을 직접 수정한 것으로, 이는 동역학 차이에 대한 "둔감함"을 그대로 물려받았습니다—기술이 평지 보행에서 질주, 속보, 등반으로 확장될 때, 이러한 방법들은 유사한 기술만 처리하거나 수동 전환에 의존하여 단일 정책에서 적응적으로 통합할 수 없습니다. 더 중요한 것은, 대부분의 학습 방법이 액추에이터를 모델링할 때 단순화된 구동 제약을 사용하여 DC 모터 토크가 속도에 따라 변하고 관절 위치에 따라 변하는 물리적 한계를 완전히 무시한다는 점입니다. 이러한 단순화는 시뮬레이션에서는 문제가 없을 수 있지만, 실기에서는 정책이 모터 물리적 한계를 초과하는 토크를 빈번히 요청하여 성능 저하를 초래하거나 보호 메커니즘을 작동시키거나 하드웨어를 손상시킬 수 있습니다.

MUJICA가 실제로 바꾼 것은 "또 하나의 다중 기술 RL 프레임워크를 제안한 것"이 아니라, "모터 물리적 제약"을 훈련 후의 엔지니어링 패치에서 훈련 중의 일급 시민으로 승격시킨 것입니다. 문제를 제약 부분 관측 마르코프 결정 과정(C-POMDP)으로 재모델링하여, 정책이 장기 보상을 최적화하면서 k개의 하드 제약을 충족해야 하도록 했습니다. 이 전환은 정책이 더 이상 모터 능력을 초과하는 동작을 "환상"할 수 없고, 제약 경계 내에서 최적해를 찾아야 함을 의미합니다. 동시에 기술 지시 변수를 도입하여 서로 다른 기술의 관측 공간을 특정 차원을 따라 분리함으로써, MUJICA는 단일 정책 네트워크가 동역학 차이가 큰 기술을 수용할 수 있게 하면서 각 기술마다 별도로 네트워크를 훈련할 필요가 없게 했습니다—이는 복잡한 실제 환경에서 바퀴 달린 로봇의 자율 연속 작업을 위한 아키텍처 수준의 가능성을 제공합니다.

## 방법 분해

MUJICA는 계층적 아키텍처를 채택하며, 하위 계층은 다중 기술 통합 정책, 상위 계층은 기술 선택기로, 두 계층 모두 자기수용 인식 기반으로 훈련됩니다.

### 문제 모델링 및 관측/동작 공간
- 문제를 C-POMDP로 모델링하며, 목표는 장기 보상을 최대화하면서 k개의 제약을 충족하는 것입니다 (식 1).
- 관측 벡터는 자기수용 감각만 포함합니다: `o_t = [ω_t^b, g_t^b, cmd_t, q_t, q̇_t, a_t, ζ_t]^T` (식 2), 여기서 `ζ_t`는 기술 지시 변수로, 각 기술은 고유한 지시 값을 가지며 각 기술의 관측 공간을 해당 차원을 따라 분리합니다.
- 특권 상태 `s_t = [o_t, v_t, c_t, u_t, h_t]^T` (식 3)는 베이스 선속도, 충돌 확률, 바퀴-지면 거리, 잠재 상태를 포함하며 훈련 시에만 사용 가능합니다.
- 동작 공간: 다리 관절은 기본 자세에 대한 각도 오프셋을 출력하고, 바퀴 관절은 목표 모터 속도를 출력합니다; PD 컨트롤러를 통해 변환됩니다: 바퀴 관절 `τ = K_d(q̇ - a)`, 기타 관절 `τ = K_p(q - a) - K_d·q̇` (식 4).

### 상태 추정기
- 과거 H=6 프레임 관측을 기반으로 GRU 네트워크를 사용하여 시간 의존성을 처리하고, 잠재 상태, 베이스 선속도, 각 부품의 충돌 확률, 바퀴-지면 거리를 예측합니다 (식 5).
- 후속 관측을 잠재 상태에 매핑하는 참조 인코더를 감독 목표로 도입하고, SwAV 대비 학습 손실을 사용합니다.
- 총 손실 `L_Estimate = L_Pred + L_SwAV(e_t, ê_t)` (식 6), 여기서 `L_Pred`는 속도 MSE, 충돌 BCE, 바퀴-지면 거리 MSE를 포함합니다 (식 7).

### 비대칭 Actor-Critic 및 제약 최적화
- 정책 네트워크는 온라인 인코더의 히스토리 임베딩 `f_t`와 현재 관측 `o_t`를 수신합니다; 보상 비평가와 제약 비평가는 특권 관측을 수신합니다.
- P3O 프레임워크를 사용하여 제약 최적화를 처리하며, 손실은 `L_P3O(θ) = L_R_CLIP(θ) + κ·Σ_i max{0, L_Ci_CLIP(θ)}` (식 8), 여기서 `κ`는 페널티 계수입니다.

### DC 모터 제약 모델링
- 각 액추에이터는 속도 관련 토크 제한을 받습니다: 저속에서는 최대 토크가 일정하고, 고속에서는 속도에 따라 선형적으로 감소합니다.
- 종아리 관절 토크는 관절 속도와 관절 위치의 영향을 함께 받으며, 최대 토크는 고정 속도에서 관절 위치에 따라 코사인 의존성을 보입니다.
- 수치는 Unitree 공식 모터 매뉴얼에서 가져와 제약이 실제 하드웨어와 일치하도록 보장합니다.

### 기술 선택기
- 고수준 선택기는 최근 H 프레임 관측(`ζ_t` 제외)을 수신하고, 사전 정의된 기술 집합에 대한 확률 분포를 출력합니다.
- 비대칭 actor-critic 네트워크를 사용하여 동결된 저수준 기술 정책 위에서 훈련됩니다 (Training S2), 통합 속도 추적 보상을 통해 학습합니다.

### 커리큘럼 학습
- 33×20 그리드 다중 작업 환경을 구축하고, 4,096개의 로봇을 서로 다른 행(특정 지형 및 작업에 해당)에 할당하며, 각 열은 커리큘럼 난이도 수준을 나타냅니다.
- 전방향 이동과 높은 플랫폼 등반은 속도 추적을 성공적으로 유지한 후 커리큘럼을 진행합니다; 낙상 복구는 로봇이 표준 기립 자세에 근접할 때 커리큘럼을 진행합니다.

## 핵심 혁신

1. **정밀 DC 모터 제약의 엔드투엔드 임베딩**: 속도 관련 및 위치 관련 토크 제한을 사후 클리핑이 아닌 C-POMDP의 제약 조건에 직접 모델링합니다. 이는 기존 RL 제어 프레임워크에 대한 근본적 수정입니다—정책이 훈련 단계에서 모터 물리적 경계 내에서 동작하는 법을 학습하며, 소거 실험에서 제약이 없을 때 90% 이상의 샘플이 실행 가능한 모터 영역 밖에 있고, 제약이 있을 때 위반률이 3.5% 미만으로 감소함을 보여줍니다. 이 설계는 sim-to-real 전이의 안전성을 직접 결정합니다.

2. **기술 지시 변수 기반 관측 공간 분리**: `ζ_t`를 통해 서로 다른 기술의 관측 공간을 단일 차원을 따라 분리하여, 단일 정책 네트워크가 동역학 차이가 큰 기술(예: 낙상 복구와 질주)을 수용할 수 있게 하면서 각 기술마다 별도로 네트워크를 훈련하거나 수동 전환할 필요가 없게 합니다. 이 메커니즘의 확장성은 이후 새 기술을 추가할 때 새 지시 값을 할당하기만 하면 되고 아키텍처를 재설계할 필요가 없음을 의미합니다.

3. **고수준 기술 선택기의 순수 자기수용 자동 전환**: 선택기는 최근 H 프레임 관측(`ζ_t` 제외)만 기반으로 기술 확률 분포를 출력하며, 동결된 저수준 정책 위에서 훈련됩니다. 이를 통해 로봇이 실제 환경에서 "낙상→복구→등반→이동"의 연속 작업 체인을 외부 인식이나 수동 개입 없이 자율적으로 완료할 수 있으며, 실기 시연에서 무작위 낙상 자세에서 60cm 높이 플랫폼 등반까지의 연속 작업을 성공적으로 완료했습니다.

## 실험 및 결과

실험은 IsaacLab 시뮬레이션 플랫폼에서 수행되었으며, 4,096개의 병렬 환경, 단일 NVIDIA RTX 4090 GPU, 로봇 플랫폼은 Unitree Go2-W입니다. 저수준 훈련 30,000회 반복, 고수준 훈련 10,000회 반복, 제어 주파수 50Hz, 물리 시뮬레이션 200Hz. 테스트 설정은 5개의 대표 작업, 10개의 증가 난이도 수준(수준 간격은 훈련 대비 2배), 4개의 무작위 시드를 포함합니다.

### 기술 선택기 평가 (표 III)
단일 에피소드에서 세 개의 중간 난이도 작업(난이도 5/10)을 연결: 계단 복구, 계단 등반, 높은 플랫폼 등반, 100회 에피소드 테스트.

| 방법 | 계단 복구 성공률 | 계단 등반 성공률 | 높은 플랫폼 등반 성공률 |
|------|--------------|--------------|----------------|
| 지시자 없음 (공유 보상) | ✖ | ✖ | ✖ |
| 지시자 없음 (분리 보상) | 72% | 72% | 38% |
| 기술 선택기 포함 (ours) | 95% | 95% | 91% |

### DC 모터 제약 소거
| 구성 | 실행 가능한 모터 영역 밖 샘플 비율 |
|------|---------------------|
| 제약 없음 | >90% |
| 제약 있음 | <3.5% |

### 실기 실험
- 계단 복구: 30° 계단에서 뒤집힌 상태에서 성공적으로 복구.
- 계단 등반: 다양한 디딤 높이와 불규칙한 가장자리를 가진 계단을 성공적으로 등반.
- 높은 플랫폼 등반: 80cm 높이의 실외 플랫폼과 1m 높이의 실내 플랫폼(기계 및 구동 한계에 근접)을 성공적으로 등반.

결과는 기술 지시 변수의 도입이 다중 기술 전환에 결정적임을 보여줍니다—공유 보상에서는 세 작업 모두 실패, 분리 보상에서는 높은 플랫폼 등반이 38%에 불과했지만, 완전한 MUJICA는 세 작업의 성공률을 모두 90% 이상으로 끌어올렸습니다. DC 모터 제약 소거는 제약 모델링이 정책 안전성에 미치는 결정적 역할을 검증합니다.

## 경계 및 한계

논문은 한계를 명시적으로 논의하지 않았습니다. 사실적 요점에서 추론할 수 있는 경계는 다음과 같습니다: 실기 실험은 계단 복구, 계단 등반, 높은 플랫폼 등반만 검증했으며, 실제 세계에서 낙상 복구 기술을 테스트하지 않았습니다; 기술 선택기는 난이도 5/10 수준에서만 평가되었으며, 더 높은 난이도에서의 전환 견고성은 알 수 없습니다; Unitree Go2-W 단일 플랫폼만 검증했으며, 다른 바퀴 달린 로봇으로 일반화되지 않았습니다; 계산 리소스 요구 사항이나 추론 지연 시간에 대한 상세 분석을 제공하지 않았으며, 실기 계산 플랫폼은 NVIDIA Jetson Orin NX이지만 리소스 사용 여유에 대한 설명은 없습니다. 또한 DC 모터 제약 수치는 Unitree 공식 매뉴얼에서 가져온 것으로, 다른 모터 모델로 전환 시 재보정이 필요합니다.

## 공학적 시사점

MUJICA를 재현할 때 다음 단계를 우선 확인하십시오: 첫째, DC 모터 제약 수치는 대상 로봇 공식 매뉴얼과 엄격히 일치해야 합니다. 이는 소거 실험에서 위반률이 90%에서 3.5%로 감소하는 핵심이자 sim-to-real 안전성의 기반입니다—모터 매뉴얼 데이터가 없으면 먼저 실측을 통해 토크-속도-위치 곡선을 피팅하는 것이 좋습니다. 둘째, 기술 지시 변수 `ζ_t`의 인코딩 방식은 다중 기술 분리 효과에 직접 영향을 미치며, 표 III은 공유 보상에서 모두 실패하고 분리 보상에서 높은 플랫폼 등반이 38%에 불과함을 보여주므로, 각 기술에 고유한 지시 값을 할당하고 관측 벡터에서 올바르게 전달해야 합니다. 셋째, 커리큘럼 학습의 진행 조건을 정확히 재현해야 합니다: 전방향 이동과 높은 플랫폼 등반은 속도 추적을 성공적으로 유지한 후 진행하고, 낙상 복구는 표준 기립 자세에 근접할 때 진행하며, 진행 조건이 너무 느슨하거나 빡빡하면 훈련 안정성에 영향을 미칩니다. 넷째, 고수준 기술 선택기 훈련 시 저수준 정책을 반드시 동결해야 하며, 그렇지 않으면 학습된 기술이 손상됩니다. 가장 함정에 빠지기 쉬운 부분은 상태 추정기의 SwAV 대비 손실입니다—참조 인코더와 온라인 인코더의 동기 업데이트 메커니즘이 올바르지 않으면 잠재 상태 드리프트가 발생하여 충돌 확률과 바퀴-지면 거리 예측 정확도에 영향을 미치고, 결국 제약 충족 능력을 약화시킵니다. 시뮬레이션에서 먼저 DC 모터 제약 소거 실험(제약 없음 vs 제약 있음)을 재현하여 위반률 지표가 목표에 도달했는지 확인한 후 전체 훈련을 진행하는 것이 좋습니다.
