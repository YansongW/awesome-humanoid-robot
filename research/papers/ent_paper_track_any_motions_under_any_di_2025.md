---
$id: ent_paper_track_any_motions_under_any_di_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Track Any Motions under Any Disturbances
  zh: 跟踪任何干扰下的任何动作
  ko: Track Any Motions under Any Disturbances
summary:
  en: This work first recovers scene, target, or motion representations from simulated interaction data and contact force/tactile
    signals, and then uses diffusion strategy/flow matching to generate whole-body trajectories/action sequences and low-level
    controller targets. The key point is to view action generation as a conditional generation problem and use diffusion or
    flow matching to sample executable trajectories in a multimodal action distribution.
  zh: 这篇工作先从仿真交互数据、接触力/触觉信号恢复场景、目标或运动表征，再用扩散策略/流匹配生成全身轨迹/动作序列、低层控制器目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
  ko: 这篇工作先从仿真交互数据、接触力/触觉信号恢复场景、目标或运动表征，再用扩散策略/流匹配生成全身轨迹/动作序列、低层控制器目标。关键点是把动作生成看成条件生成问题，用扩散或流匹配在多模态动作分布里采样可执行轨迹。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- balance
- behavioral_foundation_model
- locomotion
- motion_tracking
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: 跟踪任何干扰下的任何动作 project page
  url: https://zzk273.github.io/Any2Track/
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

在人形机器人全身运动控制领域，现有方法通常受限于预设的动作模板或简化的动力学模型，难以在复杂、多变的物理交互中同时实现鲁棒的平衡维持与精确的动作跟踪。本文提出一种名为“Any2Track”的通用框架，其核心创新在于将全身动作生成重新定义为一种条件生成问题，通过从仿真交互数据中恢复场景与运动表征，并利用扩散策略或流匹配技术，在高度多模态的动作分布中采样出可执行的全身轨迹。该工作旨在为构建行为基础模型提供关键技术支撑，使机器人能够在任意干扰下跟踪任意运动，显著提升人形机器人在非结构化环境中的适应性与运动能力。

## 核心内容

当前人形机器人的运动控制研究面临两大核心挑战：一是如何在受到外部推、拉等未知干扰时维持动态平衡，二是如何实时跟踪复杂、非重复性的全身动作（如舞蹈、避障或操作）。传统基于模型预测控制（MPC）的方法虽能处理部分扰动，但通常需要精确的动力学模型和简化的接触假设，难以应对高维、非线性的全身运动。此外，基于强化学习的方法虽然能从仿真中学习鲁棒策略，但其动作空间往往被限制在特定任务或低维基元中，缺乏对任意运动指令的泛化能力。因此，如何构建一个既能处理多模态动作分布，又能对实时干扰做出响应的统一控制框架，成为该领域的关键瓶颈。

本文提出的Any2Track框架将动作生成视为一个条件生成过程。其核心思想是：首先，通过仿真环境中的交互数据（包括接触力、触觉信号及机器人本体状态）来恢复场景、目标或运动的高维表征；然后，利用扩散策略或流匹配模型，在由这些表征条件约束的多模态动作分布中，通过迭代去噪或概率流采样生成可执行的全身轨迹。这种方法避免了传统方法中对动作分布进行显式建模的困难，转而利用生成模型强大的分布拟合能力，从而能够处理包含跳跃、旋转、抓取等复杂动作的连续空间。

该工作的关键技术突破在于将“条件生成”与“全身控制”深度融合。具体而言，扩散策略通过逐步向随机噪声中添加条件信息（如目标运动轨迹、当前接触力状态）来生成动作序列，而流匹配则通过学习一个连续的概率流来连接噪声分布与真实动作分布。这两种方法均能有效处理动作分布中的多模态性（例如，同一目标运动可能有多种不同的平衡策略）。此外，框架通过引入接触力与触觉信号作为条件输入，使得生成的动作能够实时感知并适应外部干扰，从而在保持平衡的同时精确跟踪目标运动。

在实验验证方面，该工作依托高保真仿真环境，构建了包含多种干扰场景（如地面滑动、外力推搡、负载变化）和多样化运动指令（如行走、奔跑、舞蹈动作）的测试集。实验结果表明，基于扩散策略或流匹配生成的全身轨迹，在运动跟踪精度和抗干扰鲁棒性上均显著优于传统MPC基线及基于单一动作基元的强化学习方法。特别是，该框架展现出对未见过的运动指令和干扰模式的零样本泛化能力，这为其在实际机器人平台上的部署奠定了基础。从应用价值看，Any2Track为构建通用的“行为基础模型”提供了关键技术路径，有望推动人形机器人在服务、救援及工业协作等复杂场景中的实用化进程。

## 参考

- 跟踪任何干扰下的任何动作 project page (https://zzk273.github.io/Any2Track/)

## Overview

In the field of whole-body motion control for humanoid robots, existing methods are often constrained by predefined motion templates or simplified dynamic models, making it difficult to simultaneously achieve robust balance maintenance and precise motion tracking in complex, variable physical interactions. This paper proposes a general framework called "Any2Track," whose core innovation lies in redefining whole-body motion generation as a conditional generation problem. By recovering scene and motion representations from simulation interaction data and utilizing diffusion policies or flow matching techniques, executable whole-body trajectories are sampled from highly multimodal motion distributions. This work aims to provide key technical support for building behavioral foundation models, enabling robots to track arbitrary motions under any disturbances, thereby significantly enhancing the adaptability and motor capabilities of humanoid robots in unstructured environments.

## Content

Current research on motion control for humanoid robots faces two core challenges: first, maintaining dynamic balance when subjected to unknown disturbances such as external pushes or pulls, and second, tracking complex, non-repetitive whole-body motions (e.g., dancing, obstacle avoidance, or manipulation) in real time. Although traditional model predictive control (MPC) methods can handle some disturbances, they typically require precise dynamic models and simplified contact assumptions, making it difficult to cope with high-dimensional, nonlinear whole-body motions. Furthermore, while reinforcement learning-based methods can learn robust policies from simulation, their action spaces are often limited to specific tasks or low-dimensional primitives, lacking generalization to arbitrary motion commands. Therefore, constructing a unified control framework that can both handle multimodal action distributions and respond to real-time disturbances has become a key bottleneck in this field.

The Any2Track framework proposed in this paper treats motion generation as a conditional generation process. Its core idea is as follows: first, high-dimensional representations of scenes, targets, or motions are recovered from interaction data in simulation environments (including contact forces, tactile signals, and robot proprioceptive states); then, using diffusion policies or flow matching models, executable whole-body trajectories are generated by iterative denoising or probabilistic flow sampling from multimodal action distributions constrained by these representations. This approach avoids the difficulty of explicitly modeling action distributions in traditional methods, instead leveraging the powerful distribution-fitting capabilities of generative models to handle continuous spaces containing complex motions such as jumping, spinning, and grasping.

The key technical breakthrough of this work lies in the deep integration of "conditional generation" and "whole-body control." Specifically, diffusion policies generate action sequences by gradually adding conditional information (e.g., target motion trajectories, current contact force states) to random noise, while flow matching learns a continuous probability flow to connect the noise distribution with the true action distribution. Both methods can effectively handle multimodality in action distributions (e.g., the same target motion may have multiple different balance strategies). Additionally, by incorporating contact forces and tactile signals as conditional inputs, the framework enables generated actions to perceive and adapt to external disturbances in real time, thereby accurately tracking target motions while maintaining balance.

In terms of experimental validation, this work relies on high-fidelity simulation environments to construct a test set containing various disturbance scenarios (e.g., ground sliding, external pushes, load changes) and diverse motion commands (e.g., walking, running, dance movements). Experimental results show that whole-body trajectories generated by diffusion policies or flow matching significantly outperform traditional MPC baselines and reinforcement learning methods based on single action primitives in terms of motion tracking accuracy and disturbance robustness. In particular, the framework demonstrates zero-shot generalization to unseen motion commands and disturbance patterns, laying the foundation for its deployment on real robotic platforms. From an application perspective, Any2Track provides a key technical pathway for building general "behavioral foundation models," potentially advancing the practical use of humanoid robots in complex scenarios such as service, rescue, and industrial collaboration.

## 개요

휴머노이드 로봇의 전신 운동 제어 분야에서 기존 방법들은 일반적으로 사전 정의된 동작 템플릿이나 단순화된 동역학 모델에 제한되어, 복잡하고 다양한 물리적 상호작용에서 강건한 균형 유지와 정밀한 동작 추적을 동시에 달성하기 어렵습니다. 본 논문은 "Any2Track"이라는 범용 프레임워크를 제안하며, 그 핵심 혁신은 전신 동작 생성을 조건부 생성 문제로 재정의하고, 시뮬레이션 상호작용 데이터에서 장면 및 운동 표현을 복원한 후, 확산 전략(diffusion policy) 또는 흐름 매칭(flow matching) 기술을 활용하여 고도로 다중 모드(multi-modal)인 동작 분포에서 실행 가능한 전신 궤적을 샘플링하는 데 있습니다. 이 연구는 로봇이 임의의 외란 하에서도 임의의 동작을 추적할 수 있도록 하여, 비구조화된 환경에서 휴머노이드 로봇의 적응성과 운동 능력을 크게 향상시키는 행동 기반 모델(behavior foundation model) 구축을 위한 핵심 기술 지원을 제공하는 것을 목표로 합니다.

## 핵심 내용

현재 휴머노이드 로봇의 운동 제어 연구는 두 가지 핵심 과제에 직면해 있습니다. 첫째는 외부 밀기, 당기기 등 알 수 없는 외란을 받을 때 동적 균형을 유지하는 방법이고, 둘째는 복잡하고 비반복적인 전신 동작(예: 춤, 장애물 회피, 조작)을 실시간으로 추적하는 방법입니다. 전통적인 모델 예측 제어(MPC) 기반 방법은 일부 외란을 처리할 수 있지만, 일반적으로 정밀한 동역학 모델과 단순화된 접촉 가정이 필요하여 고차원, 비선형 전신 운동을 다루기 어렵습니다. 또한 강화 학습 기반 방법은 시뮬레이션에서 강건한 정책을 학습할 수 있지만, 동작 공간이 특정 작업이나 저차원 기본 요소로 제한되는 경우가 많아 임의의 동작 명령에 대한 일반화 능력이 부족합니다. 따라서 다중 모드 동작 분포를 처리하면서도 실시간 외란에 대응할 수 있는 통합 제어 프레임워크를 구축하는 것이 이 분야의 핵심 병목이 되고 있습니다.

본 논문에서 제안하는 Any2Track 프레임워크는 동작 생성을 조건부 생성 과정으로 간주합니다. 핵심 아이디어는 다음과 같습니다. 먼저, 시뮬레이션 환경의 상호작용 데이터(접촉력, 촉각 신호 및 로봇 자체 상태 포함)를 통해 장면, 목표 또는 운동의 고차원 표현을 복원합니다. 그런 다음, 확산 전략 또는 흐름 매칭 모델을 사용하여 이러한 표현 조건에 의해 제약된 다중 모드 동작 분포에서 반복적 잡음 제거 또는 확률적 흐름 샘플링을 통해 실행 가능한 전신 궤적을 생성합니다. 이 방법은 전통적인 방법에서 동작 분포를 명시적으로 모델링하는 어려움을 피하고, 대신 생성 모델의 강력한 분포 피팅 능력을 활용하여 점프, 회전, 잡기 등 복잡한 동작을 포함하는 연속 공간을 처리할 수 있습니다.

이 연구의 핵심 기술적 돌파구는 "조건부 생성"과 "전신 제어"의 심층 융합에 있습니다. 구체적으로, 확산 전략은 무작위 잡음에 조건 정보(예: 목표 운동 궤적, 현재 접촉력 상태)를 점진적으로 추가하여 동작 시퀀스를 생성하는 반면, 흐름 매칭은 잡음 분포와 실제 동작 분포를 연결하는 연속적인 확률적 흐름을 학습합니다. 두 방법 모두 동작 분포의 다중 모드성(예: 동일한 목표 운동에 대해 여러 가지 다른 균형 전략이 존재할 수 있음)을 효과적으로 처리할 수 있습니다. 또한 프레임워크는 접촉력과 촉각 신호를 조건 입력으로 도입하여 생성된 동작이 실시간으로 외부 외란을 감지하고 적응할 수 있도록 함으로써 균형을 유지하면서 목표 운동을 정밀하게 추적할 수 있게 합니다.

실험 검증 측면에서, 이 연구는 고충실도 시뮬레이션 환경을 기반으로 다양한 외란 시나리오(예: 지면 미끄러짐, 외부 밀기, 부하 변화)와 다양한 운동 명령(예: 걷기, 달리기, 춤 동작)을 포함하는 테스트 세트를 구축했습니다. 실험 결과, 확산 전략 또는 흐름 매칭을 기반으로 생성된 전신 궤적은 운동 추적 정밀도와 외란 저항 강건성에서 전통적인 MPC 기준선 및 단일 동작 기본 요소 기반 강화 학습 방법보다 현저히 우수함을 보여주었습니다. 특히, 이 프레임워크는 보지 못한 운동 명령과 외란 패턴에 대한 제로샷 일반화(zero-shot generalization) 능력을 나타내어, 실제 로봇 플랫폼에 배포할 수 있는 기반을 마련했습니다. 응용 가치 측면에서 Any2Track은 범용 "행동 기반 모델" 구축을 위한 핵심 기술 경로를 제공하며, 서비스, 구조 및 산업 협력 등 복잡한 시나리오에서 휴머노이드 로봇의 실용화를 촉진할 것으로 기대됩니다.
