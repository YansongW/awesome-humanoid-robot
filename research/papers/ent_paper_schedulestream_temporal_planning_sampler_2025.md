---
$id: ent_paper_schedulestream_temporal_planning_sampler_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'ScheduleStream: Temporal Planning with Samplers for GPU-Accelerated Multi-Arm Task and Motion Planning & Scheduling'
  zh: 'ScheduleStream: Temporal Planning with Samplers for GPU-Accelerated Multi-Arm Task and Motion Planning & Scheduling'
  ko: 'ScheduleStream: Temporal Planning with Samplers for GPU-Accelerated Multi-Arm Task and Motion Planning & Scheduling'
summary:
  en: 'Bimanual and humanoid robots are appealing because of their human-like ability to leverage multiple arms to efficiently
    complete tasks. However, controlling multiple arms at once is computationally challenging due to the growth in the hybrid
    discrete-continuous action space. Institutions per source list: NVIDIA Research（Caelan Garrett）、University of Sydney（Fabio
    Ramos）.'
  zh: ScheduleStream 是首个通用型规划与调度框架，由研究团队提出，用于解决双臂及人形机器人的多臂并行任务与运动规划调度问题。其核心贡献在于通过混合持续动作建模时间动态，并利用 GPU 加速采样器提升规划效率，在仿真和真实场景中均优于现有方法。
  ko: 'Bimanual and humanoid robots are appealing because of their human-like ability to leverage multiple arms to efficiently
    complete tasks. However, controlling multiple arms at once is computationally challenging due to the growth in the hybrid
    discrete-continuous action space. Institutions per source list: NVIDIA Research（Caelan Garrett）、University of Sydney（Fabio
    Ramos）.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- schedulestream
- temporal
- planning
- sampler
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 763 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2511.04758v2); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2511.04758 ScheduleStream: Temporal Planning with Samplers for GPU-Accelerated Multi-Arm Task and Motion Planning
    & Scheduling'
  url: https://arxiv.org/abs/2511.04758
  accessed_at: '2026-07-31'
  date: '2025-11-06'
- id: src_002
  type: website
  title: Project page
  url: https://schedulestream.github.io
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

ScheduleStream 是一个完全基于 Python 构建的通用规划与调度语言，由作者团队提出，旨在超越传统 TAMP 框架（如 PDDLStream）仅支持顺序计划的局限，直接支持动作在时间上异步重叠的调度（schedules）。其核心贡献在于将声明式规划语言与过程式 Python 编程无缝对接，并引入连续采样操作（流）来实例化无限常量域中的动作参数，同时提出 Eager Stream Scheduling 算法求解最小 makespan 调度。

## 它改变了什么

它真正改变的是 TAMP 问题的“解空间形态”。以往 PDDLStream 等框架的输出是动作序列，隐含了“一个动作完成、下一个动作开始”的串行约束，这在多机械臂协同场景中严重浪费了并行硬件能力。ScheduleStream 将解定义为带时间戳的调度 τ，允许动作重叠执行，这直接改变了规划问题的组合结构——从“排列动作”变为“在时间轴上安排动作的起止”，并引入 makespan 作为优化目标。此外，它打破了声明式语言（PDDL）与过程式语言（Python）之间的鸿沟，让用户可以用 Python 函数直接定义谓词（如碰撞检测）和采样器（如轨迹规划），这解决了 PDDL 无法表达无限连续参数空间（如 SE(3) 配置）的痛点。

## 方法拆解

### 语言核心：函数式状态与重载操作符
- 状态被定义为对**函数**（functions）的赋值，例如 `At(arm1)<=q1` 表示机械臂 1 当前配置为 q1。采用封闭世界假设变体：未声明的函数求值为 None，未声明的谓词为 False。
- 通过重载 Python 的 `<=`（赋值）和 `==`（测试）操作符，将声明式建模语法嵌入 Python，使得状态更新和条件检查在代码层面直接可读。

### 动作模型：瞬时与持续
- **瞬时动作**（如 `pick`）：类似 STRIPS，有前置条件（如 `Kin(...)`、`Attached("?obj")==None`）和效果（如 `Holding("?arm")<="?obj"`）。
- **持续动作**（如 `move`）：扩展为有开始条件/效果、进行中条件、结束条件/效果和持续时间。例如 `move` 的开始效果是 `At("?arm")<="?t"`（中间轨迹点），结束效果是 `At("?arm")<="?q2"`，持续时间由过程式函数 `Duration("?t")` 计算。

### 流（Streams）作为连续采样器
- 流是 PDDLStream 的扩展，用于生成无限多的连续参数值。例如 `grasps` 流生成抓取位姿，`ik_qs` 生成逆解配置，`motions` 规划轨迹。
- 每个流有条件生成器，通过 `next(grasps.gen(arm1))` 调用，返回新常量或 None。流输出被加入“流计划” ψ，并更新初始状态，诱导下一轮求解。

### Eager Stream Scheduling 算法
1. 用当前常量集求解有限调度问题（子程序 `schedule`）。
2. 若失败，实例化流 s ∈ 𝒮 为 s(x)，调用生成器产生新常量 y。
3. 若 y 有效且未被探索，将流输出谓词 p(x,y) 加入 ψ，更新初始状态 I，重新求解。
4. 重复直至找到调度或流耗尽。

### Schedule 子程序：从顺序计划到调度
- 将每个持续动作 a 编译为两个瞬时动作：`a.start` 和 `a.end`。引入谓词 `Ongoing`（动作进行中）和函数 `Remaining`（剩余时间）。
- 求解顺序规划得到计划 π 后，按时间戳构建调度：遇到 `a.start` 设置开始时间 T[a(x)] ← t*；遇到 `a.end` 计算 Δt ← a(x).duration − (t* − T[a(x)])，更新 t* ← t* + max(0, Δt)，并记录 ⟨T[a(x)], a(x), t*⟩。

## 关键创新

1. **解空间从序列到调度**：这是对 TAMP 问题定义的根本性扩展。允许动作重叠意味着规划器必须处理并发约束（如机械臂碰撞避免），而不仅仅是顺序因果链。这直接提升了多机械臂场景的吞吐量上限。
2. **声明式与过程式的无缝融合**：通过 Python 函数定义谓词（如 `collision_cond`）和持续时间（`Duration`），使得复杂几何计算（碰撞检测、逆运动学）可以直接嵌入规划逻辑，无需像 PDDL 那样用外部工具预计算所有可能值。这是对 PDDLStream 的“流”概念的实用化升级。
3. **Eager 流实例化策略**：与 PDDLStream 的“先采样后规划”不同，Eager 策略在求解失败时才按需生成新常量，避免了无效采样爆炸，且与调度求解器紧密耦合，能更快收敛到可行调度。

## 实验与结果

论文片段仅包含三个说明性示例问题，未提供任何定量实验结果。

| 问题 | 场景描述 | 关键点 |
|------|---------|--------|
| Problem 1 | 机械臂可并行拾取物体 | 展示调度允许动作重叠 |
| Problem 2 | 机械臂只能顺序拾取物体 | 展示调度退化为顺序计划 |
| Problem 3 | 一个机械臂拾取后必须后退为另一个腾出空间 | 展示进行中条件（collision_cond）的作用 |

**注意**：论文未提及基线方法、成功率、计算时间、makespan 对比等任何量化指标。因此，无法从事实要点中得出任何关于性能优劣的结论。

## 边界与局限

- **未提供实验验证**：论文片段完全没有实验部分，无法判断该方法在真实机器人系统上的可行性、鲁棒性或计算效率。
- **未讨论最优性保证**：Eager Stream Scheduling 是贪心式的流实例化策略，论文未说明其与最优调度的差距，也未提及任何剪枝或启发式来保证解的质量。
- **未处理资源冲突的全局优化**：调度子程序通过顺序规划编译而来，可能无法捕捉动作间复杂的资源竞争（如共享工作空间），论文未明确说明如何处理此类全局约束。
- **论文未明确**：作者未讨论扩展到高维连续空间（如 7-DOF 机械臂）时的采样效率、未提及对动态环境（移动障碍物）的适应性、未讨论与学习方法的结合。

## 工程启示

- **复现前先核对核心接口**：重点检查 `<=` 和 `==` 操作符的重载是否与 Python 对象模型兼容，特别是当函数值本身是 PyTorch 张量时，`==` 的语义需要自定义（张量比较返回张量，需转换为布尔）。
- **流生成器的实现是最大坑点**：`next(s.gen(x))` 必须保证在有限时间内返回有效常量或 None，且生成器内部不能有状态泄漏（如缓存未清空导致重复输出）。建议为每个流实例维护独立的生成器对象。
- **调度子程序的编译逻辑需仔细验证**：将持续动作编译为 start/end 两个瞬时动作时，`Remaining` 函数的更新逻辑容易出错，特别是当多个动作并行时，`t*` 的推进必须严格按时间戳排序处理。
- **对下游团队的指导**：如果目标是多机械臂协同装配，建议先用 Problem 1 和 3 的场景做单元测试，验证调度器能否正确产生重叠动作；若发现规划时间过长，优先检查流实例化是否过于频繁（Eager 策略可能在某些问题上退化为穷举采样）。
- **注意封闭世界假设的副作用**：未声明的函数求值为 None，这意味着任何遗漏的状态初始化都会导致规划失败，且错误难以追踪。建议在建模时显式初始化所有可能涉及的函数。

## 参考
- https://arxiv.org/abs/2511.04758
- https://schedulestream.github.io
- https://github.com/ImChong/Robotics_Notebooks

## Overview

ScheduleStream is a general-purpose planning and scheduling language built entirely on Python, proposed by the author team to overcome the limitation of traditional TAMP frameworks (such as PDDLStream) that only support sequential plans, directly supporting schedules where actions overlap asynchronously in time. Its core contribution lies in seamlessly integrating a declarative planning language with procedural Python programming, introducing continuous sampling operations (streams) to instantiate action parameters in infinite constant domains, and proposing the Eager Stream Scheduling algorithm to solve minimal makespan schedules.

## What It Changes

What it truly changes is the "solution space shape" of TAMP problems. Previously, frameworks like PDDLStream output action sequences, implying the serial constraint of "one action completes, the next begins," which severely wastes parallel hardware capabilities in multi-arm coordination scenarios. ScheduleStream defines solutions as timestamped schedules τ, allowing actions to overlap, directly altering the combinatorial structure of the planning problem—from "arranging actions" to "arranging action start and end times on a timeline"—and introducing makespan as an optimization objective. Additionally, it bridges the gap between declarative languages (PDDL) and procedural languages (Python), allowing users to directly define predicates (such as collision detection) and samplers (such as trajectory planning) as Python functions, addressing PDDL's inability to express infinite continuous parameter spaces (such as SE(3) configurations).

## Method Breakdown

### Language Core: Functional State and Overloaded Operators
- State is defined as assignments to **functions**, e.g., `At(arm1)<=q1` indicates that arm 1's current configuration is q1. A variant of the closed-world assumption is adopted: undeclared functions evaluate to None, and undeclared predicates are False.
- By overloading Python's `<=` (assignment) and `==` (test) operators, declarative modeling syntax is embedded into Python, making state updates and condition checks directly readable at the code level.

### Action Models: Instantaneous and Durative
- **Instantaneous actions** (e.g., `pick`): Similar to STRIPS, with preconditions (e.g., `Kin(...)`, `Attached("?obj")==None`) and effects (e.g., `Holding("?arm")<="?obj"`).
- **Durative actions** (e.g., `move`): Extended with start conditions/effects, invariant conditions, end conditions/effects, and duration. For example, `move`'s start effect is `At("?arm")<="?t"` (intermediate trajectory point), its end effect is `At("?arm")<="?q2"`, and its duration is computed by the procedural function `Duration("?t")`.

### Streams as Continuous Samplers
- Streams are an extension of PDDLStream, used to generate infinitely many continuous parameter values. For example, the `grasps` stream generates grasp poses, `ik_qs` generates inverse kinematics solutions, and `motions` plans trajectories.
- Each stream has a conditional generator, invoked via `next(grasps.gen(arm1))`, returning a new constant or None. Stream outputs are added to the "stream plan" ψ, updating the initial state and inducing the next round of solving.

### Eager Stream Scheduling Algorithm
1. Solve a finite scheduling problem with the current constant set (subroutine `schedule`).
2. If it fails, instantiate stream s ∈ 𝒮 as s(x), invoke the generator to produce a new constant y.
3. If y is valid and unexplored, add the stream output predicate p(x,y) to ψ, update the initial state I, and re-solve.
4. Repeat until a schedule is found or streams are exhausted.

### Schedule Subroutine: From Sequential Plans to Schedules
- Compile each durative action a into two instantaneous actions: `a.start` and `a.end`. Introduce the predicate `Ongoing` (action in progress) and the function `Remaining` (remaining time).
- After solving a sequential plan π, construct the schedule by timestamps: upon encountering `a.start`, set start time T[a(x)] ← t*; upon encountering `a.end`, compute Δt ← a(x).duration − (t* − T[a(x)]), update t* ← t* + max(0, Δt), and record ⟨T[a(x)], a(x), t*⟩.

## Key Innovations

1. **Solution space from sequences to schedules**: This is a fundamental extension of the TAMP problem definition. Allowing action overlap means the planner must handle concurrency constraints (such as arm collision avoidance), not just sequential causal chains. This directly raises the throughput ceiling in multi-arm scenarios.
2. **Seamless fusion of declarative and procedural**: By defining predicates (e.g., `collision_cond`) and durations (`Duration`) as Python functions, complex geometric computations (collision detection, inverse kinematics) can be directly embedded into planning logic, without needing to precompute all possible values with external tools as in PDDL. This is a practical upgrade of PDDLStream's "stream" concept.
3. **Eager stream instantiation strategy**: Unlike PDDLStream's "sample-then-plan" approach, the Eager strategy generates new constants on demand only when solving fails, avoiding invalid sampling explosions, and is tightly coupled with the scheduling solver, converging faster to feasible schedules.

## Experiments and Results

The paper excerpt contains only three illustrative example problems and provides no quantitative experimental results.

| Problem | Scenario Description | Key Point |
|---------|---------------------|-----------|
| Problem 1 | Arm can pick objects in parallel | Demonstrates schedules allowing action overlap |
| Problem 2 | Arm can only pick objects sequentially | Demonstrates schedules degenerating to sequential plans |
| Problem 3 | One arm must retreat after picking to make space for another | Demonstrates the role of invariant conditions (collision_cond) |

**Note**: The paper does not mention any quantitative metrics such as baseline methods, success rates, computation time, or makespan comparisons. Therefore, no conclusions about performance superiority or inferiority can be drawn from the factual points.

## Boundaries and Limitations

- **No experimental validation provided**: The paper excerpt completely lacks an experimental section, making it impossible to judge the method's feasibility, robustness, or computational efficiency on real robotic systems.
- **No optimality guarantees discussed**: Eager Stream Scheduling is a greedy stream instantiation strategy; the paper does not explain its gap from optimal schedules, nor does it mention any pruning or heuristics to ensure solution quality.
- **No global optimization of resource conflicts**: The scheduling subroutine is compiled from sequential planning and may fail to capture complex resource competition between actions (such as shared workspaces); the paper does not clearly state how such global constraints are handled.
- **Not explicitly addressed by the paper**: The authors do not discuss sampling efficiency when scaling to high-dimensional continuous spaces (e.g., 7-DOF arms), adaptability to dynamic environments (moving obstacles), or integration with learning methods.

## Engineering Insights

- **Verify core interfaces before reproduction**: Focus on checking whether the overloading of `<=` and `==` operators is compatible with Python's object model, especially when function values are PyTorch tensors—the semantics of `==` need customization (tensor comparisons return tensors and must be converted to booleans).
- **Stream generator implementation is the biggest pitfall**: `next(s.gen(x))` must guarantee returning a valid constant or None within finite time, and the generator must not have state leakage (e.g., stale caches causing repeated outputs). It is recommended to maintain independent generator objects for each stream instance.
- **Carefully validate the scheduling subroutine's compilation logic**: When compiling durative actions into start/end instantaneous actions, the update logic of the `Remaining` function is error-prone, especially when multiple actions run in parallel—the advancement of `t*` must be strictly processed in timestamp order.
- **Guidance for downstream teams**: If the goal is multi-arm collaborative assembly, it is recommended to first use Problem 1 and 3 scenarios for unit testing to verify whether the scheduler correctly produces overlapping actions; if planning time is excessive, first check whether stream instantiation is too frequent (the Eager strategy may degenerate into exhaustive sampling on certain problems).
- **Be aware of closed-world assumption side effects**: Undeclared functions evaluate to None, meaning any omitted state initialization will cause planning failure, and errors are hard to trace. It is recommended to explicitly initialize all potentially involved functions during modeling.

## 개요

ScheduleStream은 저자 팀이 제안한 완전히 Python 기반으로 구축된 범용 계획 및 스케줄링 언어로, 기존 TAMP 프레임워크(예: PDDLStream)가 순차적 계획만 지원하는 한계를 넘어, 동작이 시간적으로 비동기적으로 겹치는 스케줄을 직접 지원하는 것을 목표로 합니다. 핵심 기여는 선언적 계획 언어와 절차적 Python 프로그래밍을 원활하게 연결하고, 연속 샘플링 연산(스트림)을 도입하여 무한 상수 도메인에서 동작 매개변수를 인스턴스화하며, 최소 makespan 스케줄을求解하는 Eager Stream Scheduling 알고리즘을 제안한 것입니다.

## 그것이 바꾸는 것

진정으로 바뀌는 것은 TAMP 문제의 "해 공간 형태"입니다. 기존 PDDLStream과 같은 프레임워크의 출력은 동작 시퀀스로, "하나의 동작이 완료되고 다음 동작이 시작되는" 직렬 제약을 암시하며, 이는 다중 로봇 팔 협업 시나리오에서 병렬 하드웨어 성능을 심각하게 낭비합니다. ScheduleStream은 해를 타임스탬프가 있는 스케줄 τ로 정의하여 동작이 겹쳐 실행될 수 있게 하며, 이는 계획 문제의 조합 구조를 직접 변경합니다—"동작 배열"에서 "시간 축에서 동작의 시작과 종료를 배치"하는 것으로 바뀌고, makespan을 최적화 목표로 도입합니다. 또한, 선언적 언어(PDDL)와 절차적 언어(Python) 사이의 간극을 허물어, 사용자가 Python 함수로 술어(예: 충돌 감지)와 샘플러(예: 궤적 계획)를 직접 정의할 수 있게 하여, PDDL이 표현할 수 없는 무한 연속 매개변수 공간(예: SE(3) 구성)의痛点을 해결합니다.

## 방법 분석

### 언어 핵심: 함수형 상태와 연산자 오버로딩
- 상태는 **함수**에 대한 할당으로 정의됩니다. 예: `At(arm1)<=q1`은 로봇 팔 1의 현재 구성이 q1임을 나타냅니다. 폐쇄 세계 가정 변형을 채택: 선언되지 않은 함수는 None으로 평가되고, 선언되지 않은 술어는 False입니다.
- Python의 `<=`(할당) 및 `==`(테스트) 연산자를 오버로딩하여 선언적 모델링 구문을 Python에 내장하고, 상태 업데이트와 조건 검사를 코드 수준에서 직접 읽을 수 있게 합니다.

### 동작 모델: 순간적 및 지속적
- **순간 동작**(예: `pick`): STRIPS와 유사하며, 전제 조건(예: `Kin(...)`, `Attached("?obj")==None`)과 효과(예: `Holding("?arm")<="?obj"`)가 있습니다.
- **지속 동작**(예: `move`): 시작 조건/효과, 진행 중 조건, 종료 조건/효과 및 지속 시간으로 확장됩니다. 예를 들어 `move`의 시작 효과는 `At("?arm")<="?t"`(중간 궤적 점)이고, 종료 효과는 `At("?arm")<="?q2"`이며, 지속 시간은 절차적 함수 `Duration("?t")`로 계산됩니다.

### 스트림(Streams)으로서의 연속 샘플러
- 스트림은 PDDLStream의 확장으로, 무한히 많은 연속 매개변수 값을 생성하는 데 사용됩니다. 예를 들어 `grasps` 스트림은 그리퍼 포즈를 생성하고, `ik_qs`는 역운동학 구성을 생성하며, `motions`는 궤적을 계획합니다.
- 각 스트림에는 조건부 생성기가 있으며, `next(grasps.gen(arm1))`를 통해 호출되어 새 상수 또는 None을 반환합니다. 스트림 출력은 "스트림 계획" ψ에 추가되고 초기 상태를 업데이트하여 다음 라운드의 해결을 유도합니다.

### Eager Stream Scheduling 알고리즘
1. 현재 상수 집합으로 유한 스케줄링 문제를 해결합니다(하위 프로시저 `schedule`).
2. 실패하면 스트림 s ∈ 𝒮를 s(x)로 인스턴스화하고 생성기를 호출하여 새 상수 y를 생성합니다.
3. y가 유효하고 탐색되지 않았다면 스트림 출력 술어 p(x,y)를 ψ에 추가하고 초기 상태 I를 업데이트한 후 다시 해결합니다.
4. 스케줄을 찾거나 스트림이 소진될 때까지 반복합니다.

### Schedule 하위 프로시저: 순차 계획에서 스케줄로
- 각 지속 동작 a를 두 개의 순간 동작으로 컴파일합니다: `a.start` 및 `a.end`. 술어 `Ongoing`(동작 진행 중)과 함수 `Remaining`(남은 시간)을 도입합니다.
- 순차 계획을 해결하여 계획 π를 얻은 후, 타임스탬프에 따라 스케줄을 구성합니다: `a.start`를 만나면 시작 시간 T[a(x)] ← t*를 설정하고; `a.end`를 만나면 Δt ← a(x).duration − (t* − T[a(x)])를 계산하고, t* ← t* + max(0, Δt)로 업데이트하며, ⟨T[a(x)], a(x), t*⟩를 기록합니다.

## 핵심 혁신

1. **해 공간이 시퀀스에서 스케줄로**: 이는 TAMP 문제 정의에 대한 근본적인 확장입니다. 동작이 겹칠 수 있다는 것은 플래너가 순차적 인과 체인뿐만 아니라 동시성 제약(예: 로봇 팔 충돌 회피)을 처리해야 함을 의미합니다. 이는 다중 로봇 팔 시나리오의 처리량 상한을 직접 향상시킵니다.
2. **선언적 및 절차적 방식의 원활한 융합**: Python 함수로 술어(예: `collision_cond`)와 지속 시간(`Duration`)을 정의함으로써 복잡한 기하학적 계산(충돌 감지, 역운동학)을 계획 로직에 직접 내장할 수 있으며, PDDL처럼 외부 도구로 모든 가능한 값을 사전 계산할 필요가 없습니다. 이는 PDDLStream의 "스트림" 개념에 대한 실용적 업그레이드입니다.
3. **Eager 스트림 인스턴스화 전략**: PDDLStream의 "먼저 샘플링 후 계획"과 달리, Eager 전략은 해결 실패 시에만 필요에 따라 새 상수를 생성하여 비효율적인 샘플링 폭발을 피하고, 스케줄링 솔버와 긴밀하게 결합되어 실행 가능한 스케줄로 더 빠르게 수렴할 수 있습니다.

## 실험 및 결과

논문 발췌문에는 세 가지 설명적 예제 문제만 포함되어 있으며, 정량적 실험 결과는 제공되지 않습니다.

| 문제 | 시나리오 설명 | 핵심 포인트 |
|------|---------|--------|
| Problem 1 | 로봇 팔이 물체를 병렬로 집을 수 있음 | 스케줄이 동작 겹침을 허용함을 보여줌 |
| Problem 2 | 로봇 팔이 물체를 순차적으로만 집을 수 있음 | 스케줄이 순차 계획으로 퇴화함을 보여줌 |
| Problem 3 | 한 로봇 팔이 집은 후 다른 팔을 위해 공간을 비워야 함 | 진행 중 조건(collision_cond)의 역할을 보여줌 |

**주의**: 논문은 기준 방법, 성공률, 계산 시간, makespan 비교 등 어떤 정량적 지표도 언급하지 않습니다. 따라서 사실적 요점만으로는 성능 우열에 대한 어떤 결론도 도출할 수 없습니다.

## 경계 및 한계

- **실험 검증 미제공**: 논문 발췌문에는 실험 부분이 전혀 없어, 실제 로봇 시스템에서 이 방법의 실행 가능성, 견고성 또는 계산 효율성을 판단할 수 없습니다.
- **최적성 보장 미논의**: Eager Stream Scheduling은 탐욕적 스트림 인스턴스화 전략이며, 논문은 최적 스케줄과의 차이를 설명하지 않고, 해 품질을 보장하기 위한 가지치기나 휴리스틱도 언급하지 않습니다.
- **자원 충돌의 전역 최적화 미처리**: 스케줄 하위 프로시저는 순차 계획 컴파일을 통해 생성되므로 동작 간 복잡한 자원 경쟁(예: 공유 작업 공간)을 포착하지 못할 수 있으며, 논문은 이러한 전역 제약을 어떻게 처리하는지 명확히 설명하지 않습니다.
- **논문 미명시 사항**: 저자는 고차원 연속 공간(예: 7-DOF 로봇 팔)으로 확장 시 샘플링 효율성, 동적 환경(이동 장애물) 적응성, 학습 방법과의 결합에 대해 논의하지 않았습니다.

## 공학적 시사점

- **재현 전 핵심 인터페이스 확인**: `<=` 및 `==` 연산자 오버로딩이 Python 객체 모델과 호환되는지 중점적으로 확인해야 하며, 특히 함수 값 자체가 PyTorch 텐서인 경우 `==`의 의미는 사용자 정의가 필요합니다(텐서 비교는 텐서를 반환하므로 부울로 변환해야 함).
- **스트림 생성기 구현이 가장 큰 함정**: `next(s.gen(x))`는 유한 시간 내에 유효한 상수 또는 None을 반환해야 하며, 생성기 내부에 상태 누출(예: 캐시 미정리로 인한 중복 출력)이 없어야 합니다. 각 스트림 인스턴스에 대해 독립적인 생성기 객체를 유지하는 것이 좋습니다.
- **스케줄 하위 프로시저의 컴파일 로직을 신중히 검증**: 지속 동작을 start/end 두 개의 순간 동작으로 컴파일할 때 `Remaining` 함수의 업데이트 로직은 오류가 발생하기 쉬우며, 특히 여러 동작이 병렬로 실행될 때 t*의 진행은 타임스탬프 순서에 따라 엄격히 처리해야 합니다.
- **하위 팀에 대한 지침**: 목표가 다중 로봇 팔 협업 조립이라면 Problem 1과 3의 시나리오로 단위 테스트를 먼저 수행하여 스케줄러가 겹치는 동작을 올바르게 생성하는지 검증하는 것이 좋습니다. 계획 시간이 너무 길다면 스트림 인스턴스화가 너무 빈번한지 우선 확인하십시오(Eager 전략은 특정 문제에서 완전 샘플링으로 퇴화할 수 있음).
- **폐쇄 세계 가정의 부작용 주의**: 선언되지 않은 함수는 None으로 평가되므로, 누락된 상태 초기화는 계획 실패를 초래하고 오류 추적이 어렵습니다. 모델링 시 관련된 모든 함수를 명시적으로 초기화하는 것이 좋습니다.
