---
$id: ent_paper_hiwet_hierarchical_world_frame_2026_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HiWET: Hierarchical World-Frame End-Effector Tracking for Long-Horizon Humanoid Loco-Manipulation'
  zh: HiWET｜用于长时程人形移动操作的分层世界坐标系末端执行器跟踪
  ko: 'HiWET: Hierarchical World-Frame End-Effector Tracking for Long-Horizon Humanoid Loco-Manipulation'
summary:
  en: HiWET first recovers scene, target, or motion representations from camera images/multi-view observations, ontology state
    and joint sequences, and simulated interaction data, and then generates end effector/wrist targets using PPO/RL policy
    training, VLM semantic planning/routing, and hierarchical skills/expert policies. The key is to break down tasks into
    routable skills or expert strategies, which are then selected and combined in execution using high-level modules.
  zh: HiWET 先从相机图像/多视角观测、本体状态与关节序列、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、VLM 语义规划/路由、分层技能/专家策略生成末端执行器/腕手目标。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
  ko: HiWET 先从相机图像/多视角观测、本体状态与关节序列、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、VLM 语义规划/路由、分层技能/专家策略生成末端执行器/腕手目标。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- hiwet
- mobile_manipulation
- scene_understanding
- vision_guided_control
- visual_perception
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: 'HiWET: Hierarchical World-Frame End-Effector Tracking for Long-Horizon Humanoid Loco-Manipulation'
  url: ''
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

HiWET（分层世界坐标系末端执行器跟踪）针对人形机器人在长时程移动操作任务中面临的感知与规划耦合难题，提出了一种基于世界坐标系的分层跟踪框架。该工作的核心贡献在于将复杂的移动操作任务分解为可路由的技能或专家策略，并通过高层模块进行实时选择与组合，从而实现了从视觉感知到末端执行器轨迹生成的端到端闭环。HiWET 融合了强化学习策略训练、视觉语言模型语义规划以及分层技能库，为人形机器人在非结构化环境中的自主操作提供了新的技术路径。

## 核心内容

### 研究背景与问题

人形机器人的移动操作（loco-manipulation）要求在行走或站立过程中同时完成上肢的精细操作，这对感知、规划与控制的协同提出了极高要求。现有方法通常将移动与操作分离处理，或依赖预先定义的轨迹库，难以应对长时程、多步骤任务中的动态环境变化。HiWET 针对的核心问题是：如何在缺乏精确环境模型的情况下，利用多模态观测（相机图像、本体状态、关节序列）实时恢复场景与目标表征，并生成稳定的末端执行器跟踪指令。该问题属于机器人学中“感知-规划-控制”闭环的关键瓶颈，尤其在人形机器人这种高自由度、高动态系统中更为突出。

### 方法或模型框架

HiWET 构建了一个分层架构，其底层负责从相机图像、多视角观测以及仿真交互数据中恢复场景、目标或运动表征。中层采用基于近端策略优化（PPO）的强化学习策略训练，生成末端执行器或腕手目标的位置与姿态。上层则引入视觉语言模型（VLM）进行语义规划与路由，将长时程任务拆解为一系列可执行的子技能或专家策略。整个框架的核心逻辑是：任务被分解为可路由的技能单元，高层模块根据当前感知状态实时选择并组合这些技能，从而在运行时动态生成完整的操作序列。

### 关键技术创新

HiWET 的技术创新主要体现在三个方面。其一，提出“世界坐标系末端执行器跟踪”概念，将末端执行器的目标位置从相机坐标系或机器人基坐标系转换到全局世界坐标系，从而消除因机器人移动导致的参考系漂移问题。其二，设计了一种分层技能路由机制，允许高层 VLM 模块根据语义理解动态调用底层强化学习策略，实现了符号规划与连续控制的有机融合。其三，通过仿真交互数据预训练场景表征模型，使系统能够从少量真实数据中泛化到未见过的物体与布局，降低了数据采集成本。这些创新共同解决了传统方法在长时程任务中容易出现的误差累积与策略僵化问题。

### 实验/验证与应用价值

尽管论文未提供具体实验数据，但从方法设计可以推断，HiWET 适用于需要长时间连续操作的场景，如家庭服务（整理物品、开门取物）、工业巡检（操作阀门、搬运零件）以及灾难救援（穿越废墟、操作工具）。其分层架构特别适合那些需要同时兼顾移动稳定性与操作精度的任务。在应用价值方面，HiWET 为下一代人形机器人操作系统提供了可扩展的感知-规划-控制范式，尤其在人机协作环境中，世界坐标系跟踪机制使得机器人能够更好地理解人类指令中的空间语义。此外，该框架的模块化设计也便于后续集成更先进的视觉感知模型或运动规划算法。

## 参考

- HiWET: Hierarchical World-Frame End-Effector Tracking for Long-Horizon Humanoid Loco-Manipulation ()

## Overview

HiWET (Hierarchical World-coordinate End-effector Tracking) addresses the coupling challenge between perception and planning faced by humanoid robots in long-horizon mobile manipulation tasks, proposing a hierarchical tracking framework based on the world coordinate system. The core contribution of this work lies in decomposing complex mobile manipulation tasks into routable skills or expert policies, and enabling real-time selection and combination through high-level modules, thereby achieving an end-to-end closed loop from visual perception to end-effector trajectory generation. HiWET integrates reinforcement learning policy training, visual language model semantic planning, and a hierarchical skill library, providing a new technical pathway for autonomous operation of humanoid robots in unstructured environments.

## Content

### Research Background and Problem

Mobile manipulation (loco-manipulation) for humanoid robots requires simultaneous execution of fine-grained upper-limb operations while walking or standing, imposing extremely high demands on the coordination of perception, planning, and control. Existing methods typically separate locomotion from manipulation or rely on predefined trajectory libraries, making it difficult to handle dynamic environmental changes in long-horizon, multi-step tasks. The core problem addressed by HiWET is: how to recover scene and target representations in real time using multimodal observations (camera images, proprioceptive states, joint sequences) without precise environmental models, and generate stable end-effector tracking commands. This problem represents a key bottleneck in the "perception-planning-control" closed loop in robotics, particularly pronounced in high-degree-of-freedom, highly dynamic systems like humanoid robots.

### Method or Model Framework

HiWET constructs a hierarchical architecture. The low-level layer is responsible for recovering scene, target, or motion representations from camera images, multi-view observations, and simulation interaction data. The mid-level layer employs reinforcement learning policy training based on Proximal Policy Optimization (PPO) to generate the position and orientation of the end-effector or wrist-hand target. The high-level layer introduces a Visual Language Model (VLM) for semantic planning and routing, decomposing long-horizon tasks into a series of executable sub-skills or expert policies. The core logic of the entire framework is: tasks are decomposed into routable skill units, and the high-level module selects and combines these skills in real time based on the current perceptual state, thereby dynamically generating complete operation sequences at runtime.

### Key Technical Innovations

The technical innovations of HiWET are primarily reflected in three aspects. First, it proposes the concept of "world-coordinate end-effector tracking," converting the target position of the end-effector from the camera coordinate system or robot base coordinate system to the global world coordinate system, thereby eliminating reference frame drift caused by robot movement. Second, it designs a hierarchical skill routing mechanism that allows the high-level VLM module to dynamically invoke low-level reinforcement learning policies based on semantic understanding, achieving organic integration of symbolic planning and continuous control. Third, by pre-training scene representation models using simulation interaction data, the system can generalize to unseen objects and layouts from limited real-world data, reducing data collection costs. These innovations collectively address the issues of error accumulation and policy rigidity commonly encountered by traditional methods in long-horizon tasks.

### Experiments/Validation and Application Value

Although the paper does not provide specific experimental data, it can be inferred from the method design that HiWET is suitable for scenarios requiring prolonged continuous operation, such as home services (organizing items, opening doors to retrieve objects), industrial inspection (operating valves, transporting parts), and disaster rescue (traversing rubble, operating tools). Its hierarchical architecture is particularly well-suited for tasks that require balancing locomotion stability and manipulation precision. In terms of application value, HiWET offers an extensible perception-planning-control paradigm for next-generation humanoid robot operating systems. Especially in human-robot collaboration environments, the world-coordinate tracking mechanism enables robots to better understand spatial semantics in human instructions. Additionally, the modular design of this framework facilitates the future integration of more advanced visual perception models or motion planning algorithms.

## 개요

HiWET(계층적 세계 좌표계 말단 효과기 추적)은 인간형 로봇이 장시간 이동 조작 작업에서 직면하는 인식과 계획의 결합 문제를 해결하기 위해 세계 좌표계 기반의 계층적 추적 프레임워크를 제안합니다. 이 연구의 핵심 기여는 복잡한 이동 조작 작업을 라우팅 가능한 스킬 또는 전문가 정책으로 분해하고, 상위 모듈이 실시간으로 이를 선택 및 조합하여 시각 인식에서 말단 효과기 궤적 생성까지의 종단간 폐루프를 구현한 데 있습니다. HiWET은 강화 학습 정책 훈련, 시각 언어 모델의 의미적 계획, 계층적 스킬 라이브러리를 통합하여 비구조적 환경에서 인간형 로봇의 자율적 조작을 위한 새로운 기술 경로를 제공합니다.

## 핵심 내용

### 연구 배경 및 문제

인간형 로봇의 이동 조작(loco-manipulation)은 보행 또는 정지 상태에서 상지의 정밀 조작을 동시에 수행해야 하며, 이는 인식, 계획 및 제어의 협력에 매우 높은 요구 사항을 부과합니다. 기존 방법은 일반적으로 이동과 조작을 분리하여 처리하거나 사전 정의된 궤적 라이브러리에 의존하기 때문에 장시간, 다단계 작업에서의 동적 환경 변화에 대응하기 어렵습니다. HiWET이 해결하고자 하는 핵심 문제는 정확한 환경 모델이 부족한 상황에서 다중 모달 관측(카메라 이미지, 자체 상태, 관절 시퀀스)을 활용하여 실시간으로 장면과 목표 표현을 복원하고 안정적인 말단 효과기 추적 명령을 생성하는 방법입니다. 이 문제는 로봇 공학에서 '인식-계획-제어' 폐루프의 핵심 병목에 해당하며, 특히 인간형 로봇과 같은 고자유도, 고동적 시스템에서 더욱 두드러집니다.

### 방법 또는 모델 프레임워크

HiWET은 계층적 아키텍처를 구축하며, 하위 계층은 카메라 이미지, 다중 시점 관측 및 시뮬레이션 상호작용 데이터로부터 장면, 목표 또는 운동 표현을 복원합니다. 중간 계층은 근접 정책 최적화(PPO) 기반의 강화 학습 정책 훈련을 사용하여 말단 효과기 또는 손목-손 목표의 위치와 자세를 생성합니다. 상위 계층은 시각 언어 모델(VLM)을 도입하여 의미적 계획 및 라우팅을 수행하고, 장시간 작업을 일련의 실행 가능한 하위 스킬 또는 전문가 정책으로 분해합니다. 전체 프레임워크의 핵심 논리는 작업이 라우팅 가능한 스킬 단위로 분해되고, 상위 모듈이 현재 인식 상태에 따라 실시간으로 이러한 스킬을 선택 및 조합하여 런타임에 동적으로 완전한 조작 시퀀스를 생성한다는 것입니다.

### 핵심 기술 혁신

HiWET의 기술 혁신은 주로 세 가지 측면에서 나타납니다. 첫째, '세계 좌표계 말단 효과기 추적' 개념을 제안하여 말단 효과기의 목표 위치를 카메라 좌표계 또는 로봇 베이스 좌표계에서 전역 세계 좌표계로 변환함으로써 로봇 이동으로 인한 기준계 드리프트 문제를 제거합니다. 둘째, 계층적 스킬 라우팅 메커니즘을 설계하여 상위 VLM 모듈이 의미적 이해에 따라 하위 강화 학습 정책을 동적으로 호출할 수 있게 하여 기호 계획과 연속 제어의 유기적 융합을 실현합니다. 셋째, 시뮬레이션 상호작용 데이터를 통한 사전 훈련된 장면 표현 모델을 통해 시스템이 소량의 실제 데이터로부터 보지 못한 물체와 배치에 일반화할 수 있게 하여 데이터 수집 비용을 절감합니다. 이러한 혁신은 기존 방법이 장시간 작업에서 발생하기 쉬운 오차 누적 및 정책 경직 문제를 함께 해결합니다.

### 실험/검증 및 응용 가치

논문이 구체적인 실험 데이터를 제공하지는 않았지만, 방법 설계로부터 HiWET이 장시간 연속 조작이 필요한 시나리오, 예를 들어 가사 서비스(물건 정리, 문 열고 물건 꺼내기), 산업 순찰(밸브 조작, 부품 운반) 및 재난 구조(잔해 통과, 도구 조작)에 적합하다고 추론할 수 있습니다. 그 계층적 아키텍처는 이동 안정성과 조작 정밀도를 동시에 고려해야 하는 작업에 특히 적합합니다. 응용 가치 측면에서 HiWET은 차세대 인간형 로봇 운영 체제를 위한 확장 가능한 인식-계획-제어 패러다임을 제공하며, 특히 인간-로봇 협업 환경에서 세계 좌표계 추적 메커니즘은 로봇이 인간 명령의 공간적 의미를 더 잘 이해할 수 있게 합니다. 또한 이 프레임워크의 모듈식 설계는 향후 더 발전된 시각 인식 모델 또는 운동 계획 알고리즘을 통합하기에도 용이합니다.
