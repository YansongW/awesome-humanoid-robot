---
$id: ent_paper_wang_wam_survey_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'World Action Models: The Next Frontier in Embodied AI'
  zh: 世界动作模型：具身智能的下一个前沿
  ko: '월드 액션 모델: 엠바디드 AI의 다음 프론티어'
summary:
  en: A 2026 survey that defines World Action Models (WAMs) as embodied foundation models unifying predictive world modeling
    with action generation, and organizes existing methods into Cascaded and Joint architectures.
  zh: World Action Models (WAMs) 是2026年提出的一类具身基础模型，旨在统一预测性世界建模与动作生成。该综述将现有方法划分为级联架构与联合架构，并系统梳理了数据生态与评估协议，为具身智能领域提供了首个系统性框架。
  ko: 2026년 서베이로, World Action Models(WAMs)를 예측적 월드 모델링과 액션 생성을 통합하는 엠바디드 파욍데이션 모델로 정의하고, 기존 방법을 Cascaded 및 Joint 아키텍처로 분류함.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
- 09_data_datasets
- 10_evaluation_benchmarks
layers:
- intelligence
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- wam
- world_action_model
- world_model
- vla
- survey
- cascaded_wam
- joint_wam
- diffusion
- autoregressive
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2605.12090v1. [2026-07-29] zh
    content backfilled from English abstract via scripts/sinicize_english_cards.py [2026-08-05] body rewritten as full-text
    six-section deep read (.staging/deep_read batch2, DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections
    regenerated by translate pipeline.
    深读+数字白名单复核通过 2026-08-10（批量二）；等级 ai_fulltext_verified（AI 全文核验），schema v1 status 枚举不含该值，按数据纪律记为 verified。
sources:
- id: src_wam_paper
  type: paper
  title: 'World Action Models: The Next Frontier in Embodied AI'
  url: https://arxiv.org/abs/2605.12090
  date: '2026-05-12'
  accessed_at: '2026-06-22'
- id: src_wam_repo
  type: website
  title: OpenMOSS/Awesome-WAM
  url: https://github.com/OpenMOSS/Awesome-WAM
  date: '2026-05-13'
  accessed_at: '2026-06-22'
related_entities:
- id: ent_paper_wang_vla_survey_2026
  relationship: is_based_on
  description:
    en: The WAM survey extends the data-centric VLA survey by focusing on models that jointly predict future states and actions.
    zh: WAM 综述在关注联合预测未来状态与动作的模型这一点上，扩展了以数据为中心的 VLA 综述。
    ko: WAM 서베이는 미래 상태와 액션을 공동으로 예측하는 모델에 초점을 맞추며 데이터 중심 VLA 서베이를 확장함.
- id: ent_dataset_omniaction
  relationship: cites
  description:
    en: The WAM survey discusses omni-modal and internet-scale data ecosystems fueling WAM development.
    zh: WAM 综述讨论了推动 WAM 发展的全模态与互联网规模数据生态。
    ko: WAM 서베이는 WAM 개발을 뒷받침하는 옴니모달 및 인터넷 규모 데이터 생태계를 논의함.
- id: ent_benchmark_libero_plus
  relationship: cites
  description:
    en: The WAM survey cites robustness benchmarks such as LIBERO-plus as part of the emerging WAM evaluation landscape.
    zh: WAM 综述将 LIBERO-plus 等鲁棒性基准引用为新兴 WAM 评测格局的一部分。
    ko: WAM 서베이는 LIBERO-plus와 같은 견고성 벤치마크를 새로운 WAM 평가 환경의 일부로 인용함.
theoretical_depth:
- system
---


## 概述

本文由复旦大学、上海创新研究院与新加坡国立大学联合撰写，首次系统性地提出并定义了World Action Models（WAMs）这一具身智能新范式——即统一预测性状态建模与动作生成的具身基础模型。文章给出了WAM的形式化定义、两大核心判据、Cascaded与Joint两大架构分类体系，并全面梳理了数据生态、评估协议与代表性工作，旨在为这一碎片化研究领域提供统一的概念框架。

## 它改变了什么

这篇综述真正改变的是对具身智能模型本质的认知定位。长期以来，VLA模型被视作「观测到动作」的反应式映射器，世界模型则被当作外部模拟器或数据增强工具，两者在架构、目标和应用场景上彼此割裂。作者将世界模型从外部工具（监督者）重新定位为策略架构内部的预测核心，确立了「World」（预测物理）与「Action」（运动控制）作为平等组件的全新范式。这一概念重构的意义在于：它首次为散落各处的「预测+控制」类工作提供了统一的分类坐标系，使研究者能够清晰辨识不同工作在架构谱系中的位置，而非在碎片化的术语中各自为战。

更重要的是，作者通过区分WAM与Video Action Models（VAMs）、Video Policies、Action World Models（AWMs）等邻近概念，厘清了长期存在的术语混淆。WAM是模态无关的超集，视频仅是建模世界的一种代理；Video Policies仅需继承预训练时空表征映射观测到动作，而WAM必须有主动预测承诺——下一状态的合成必须是模型推理和输出的显式组成部分。这种概念边界的划定，为后续研究提供了清晰的准入标准。

## 方法拆解

### WAM形式化定义
WAM目标分布为联合分布 $p(o', a | o, l)$，目标函数：
$$\mathcal{L}_{WAM} = \mathbb{E}_{(o,l,o',a)\sim\mathcal{D}}[-\log p(o', a | o, l)]$$

### 两大核心判据
1. **前向预测建模**：必须生成或利用未来状态 $o'$ 的可量化表征（显式视觉预测如像素级视频帧、稠密光流，或隐式物理表征如物理接地潜空间）
2. **耦合动作生成**：动作 $a$ 必须严格与预期未来状态 $o'$ 对齐（联合概率输出或级联/统一潜架构中的策略条件化）

### 两大架构类别
- **Cascaded WAM**：显式分解目标 $p(o', a|o, l) = p(a|o', o, l)p(o'|o, l)$，先合成未来状态表征再推导动作
  - **Explicit（像素空间）**：学习式动作提取（UniPi的IDM、π0.7的VLA替代IDM）、几何式动作提取（AVDC的光流+SE(3)解析推导、3DFlowAction的三维流场）
  - **Implicit（潜在表示）**：VPP的VAE编码+单步扩散预测、S-VAM的自蒸馏压缩、MWM的未来语义掩码替代RGB预测
- **Joint WAM**：直接建模联合分布，状态预测与动作生成在共享表征空间中共优化
  - **Autoregressive**：GR-1/GR-2的显式解耦表示、CoT-VLA的混合注意力路由、WorldVLA的模态特定因果掩码
  - **Diffusion-based**：Unified Stream（UWM的独立噪声水平控制、Cosmos Policy的潜帧注入）与Multi-Stream（DUST的独立flow-matching损失、DiT4DiT的配对Video DiT–Action DiT架构）

### 关键设计决策
- 级联分解提供自然归纳偏置：世界模型无需推理机器人运动学，动作模型无需解决长时程场景预测
- 隐式潜在规划（VPP、S-VAM）旨在绕过像素级视频合成的计算瓶颈，实现实时控制兼容的推理速度
- 自回归联合预测面临灾难性误差传播与顺序解码计算瓶颈之间的张力，推动扩散与并行解码路径的发展

## 关键创新

**1. 首次给出WAM的形式化定义与分类体系**：此前世界模型与动作生成的整合工作在架构、学习目标和应用场景上碎片化，缺乏统一概念框架。作者提出的两大核心判据（前向预测建模+耦合动作生成）和Cascaded/Joint二分法，为整个领域提供了可操作的分类坐标，使研究者能够精确定位自身工作的创新点与相邻工作的差异。

**2. 厘清WAM与邻近概念的边界**：与VAMs的模态无关超集关系、与Video Policies的「主动预测承诺」区分、与AWMs的「Agent主要类别」重新定位——这些概念辨析并非术语游戏，而是直接决定了模型设计的最低要求：仅继承预训练时空表征映射观测到动作不足以称为WAM，必须有显式的下一状态合成作为推理和输出的组成部分。

**3. 系统梳理隐式潜在规划路径**：作者明确指出像素级视频合成的计算开销是实时部署的主要瓶颈，并系统整理了绕过像素重建的三条技术路线——RSSM/TSSM状态空间模型、JEPA范式预测潜嵌入、以及VPP/S-VAM/MWM等工作的轻量潜在预测。这一梳理为实时部署导向的研究提供了清晰的技术选型地图。

## 实验与结果

本文为综述论文，未报告新的实验结果或基线对比数字。但文中汇总了代表性工作的关键性能数据：

| 模型 | 参数规模 | 仿真基准 | 真实平台 | 关键结果 |
|------|---------|---------|---------|---------|
| GR-1 | 195M | CALVIN | Kinova Gen2 | 视频预测作为动作生成正则化器 |
| GR-2 | 30-719M | CALVIN | Kinova Gen3 | VQGAN离散视觉流水线 |
| CoT-VLA | 7B | LIBERO、Bridge-V2 | 无 | 混合注意力路由 |
| WorldVLA | 7B | LIBERO | 无 | 模态特定因果掩码 |
| ℱ1 | 4.2B | LIBERO、SimplerEnv | Genie-1、Franka | MoT解耦预测路径 |
| VLA-JEPA | 2B | LIBERO、SimplerEnv、LIBERO-Plus | Franka | 潜空间联合序列建模 |
| VideoVLA | 5B | SIMPLER | Realman | 复用CogVideoX-5B骨干 |
| DreamZero | 14B | PolaRiS、GenieSim3.0 | AgiBot-G1、Franka | KV-cache观测替换+系统级优化 |
| Cosmos Policy | 2B | LIBERO、RoboCase | ALOHA | 单一checkpoint同时作为策略、世界模型和值函数 |
| UD-VLA | 8B | CALVIN、LIBERO、SimplerEnv | UR5e | 统一离散token空间mask-and-predict |
| Motus | 8B | Robotwin 2.0、LIBERO-Long | AC-One、Aloha-2 | Tri-modal Joint Attention |
| AdaWorldPolicy | 2.8B | LIBERO-10、Variant PushT、CALVIN | INOVO | 力预测器第三专家+LoRA在线自适应 |

此外，**Ctrl-World**将π0.5-DROID策略在下游任务成功率提升44.7%；**RoboScape**合成数据上训练的VLA策略达到与真实演示训练相当的性能；**DREMA**实现真实机器人一次性策略学习。消融实验确认：移除未来图像预测或视频协同训练会降低控制性能。

## 边界与局限

作者明确承认的局限包括：自回归视频世界模型存在严重误差累积问题且难以建模高度多模态分布；扩散视频世界模型计算密集，像素级扩散去噪在RGB空间中计算代价高昂；当前评估协议在评估WAM相关能力方面存在缺口。此外，作者指出传统模拟器（IsaacGym、MuJoCo）难以捕捉真实环境的复杂性与变异性，导致显著sim-to-real差距。

论文未提及对自身综述方法的系统性局限分析或未来工作方向的具体建议（Sec. 7讨论关键障碍与未来方向但未展开）。训练配置、数据量、推理频率、硬件细节均未提及，这限制了读者对文中各模型实际部署可行性的直接判断。值得注意的是，文中大量模型仅在仿真环境验证（如RoboEnvision、This&That、TesserAct、CoT-VLA、WorldVLA），其真实世界泛化能力尚未得到证实。

## 工程启示

对工程复现与选型的直接指导：

**先核对三件事**：一是明确你的任务是否需要「主动预测承诺」——如果仅需反应式映射，标准VLA即可，无需引入WAM的额外复杂度；二是评估实时性预算——像素级视频合成（显式Cascaded路径）在RGB空间去噪的计算代价极高，若需闭环高频控制，应优先考虑隐式潜在规划（VPP、S-VAM路线）或Joint WAM的扩散并行解码；三是数据条件——标准VLA严格要求配对的 $(o_t, a_t)$ 轨迹，若你的数据以无动作视频为主，UWM的独立噪声水平控制或PAD的token序列扩展策略可纳入无动作互联网视频数据。

**最容易踩坑的地方**：一是自回归联合预测的灾难性误差传播——早期视觉幻觉级联导致后续动作失败，需关注CoT-VLA的混合注意力路由或AIM的intent-causal注意力掩码等缓解机制；二是「像素匹配捷径」——模型将容量浪费在重建无关背景变化上而非学习可操作的转移物理，MWM的几何信息瓶颈或LDA-1B的DINO潜空间预测是值得借鉴的规避策略；三是纯离散控制的方差较高，若采用离散动作token需配套方差缓解措施；四是级联架构中两阶段误差累积——UniPi的单次长时程生成被发现存在语义漂移与复合误差，VLP的语义级干预（限制误差在单个片段内累积）或Veo-Act的在线交互检测器（检测到即将接触时移交控制给反应式VLA）是有效的工程缓解手段。

## 参考
- http://arxiv.org/abs/2605.12090v1

## Overview

This paper, jointly authored by Fudan University, Shanghai Innovation Institute, and the National University of Singapore, is the first to systematically propose and define World Action Models (WAMs), a new paradigm in embodied intelligence—namely, embodied foundation models that unify predictive state modeling and action generation. The paper provides a formal definition of WAMs, two core criteria, a taxonomy of two major architectural categories (Cascaded and Joint), and a comprehensive review of the data ecosystem, evaluation protocols, and representative works, aiming to offer a unified conceptual framework for this fragmented research area.

## What It Changes

What this survey truly changes is the conceptual positioning of embodied intelligence models. For a long time, VLA models have been viewed as reactive mappers from "observations to actions," while world models have been treated as external simulators or data augmentation tools, with the two being disconnected in architecture, objectives, and application scenarios. The authors reposition world models from external tools (supervisors) to the predictive core within the policy architecture, establishing a new paradigm where "World" (predicting physics) and "Action" (motor control) are equal components. The significance of this conceptual reframing lies in the fact that it provides, for the first time, a unified classification coordinate system for the scattered "prediction + control" works, enabling researchers to clearly identify where different works sit within the architectural spectrum, rather than fighting in fragmented terminology.

More importantly, the authors clarify long-standing terminological confusion by distinguishing WAMs from adjacent concepts such as Video Action Models (VAMs), Video Policies, and Action World Models (AWMs). WAM is a modality-agnostic superset, where video is merely one proxy for modeling the world; Video Policies only need to inherit pretrained spatiotemporal representations to map observations to actions, whereas WAMs must have an explicit predictive commitment—the synthesis of the next state must be an explicit component of the model's reasoning and output. This delineation of conceptual boundaries provides clear admission criteria for subsequent research.

## Method Decomposition

### Formal Definition of WAM
The WAM target distribution is the joint distribution $p(o', a | o, l)$, with the objective function:
$$\mathcal{L}_{WAM} = \mathbb{E}_{(o,l,o',a)\sim\mathcal{D}}[-\log p(o', a | o, l)]$$

### Two Core Criteria
1. **Forward predictive modeling**: Must generate or utilize a quantifiable representation of the future state $o'$ (explicit visual prediction such as pixel-level video frames, dense optical flow, or implicit physical representations such as physics-grounded latent spaces)
2. **Coupled action generation**: The action $a$ must be strictly aligned with the expected future state $o'$ (joint probability output or policy conditioning in cascaded/unified latent architectures)

### Two Architectural Categories
- **Cascaded WAM**: Explicitly decomposes the objective $p(o', a|o, l) = p(a|o', o, l)p(o'|o, l)$, first synthesizing future state representations and then deriving actions
  - **Explicit (pixel space)**: Learned action extraction (UniPi's IDM, π0.7's VLA-replacing IDM), geometric action extraction (AVDC's optical flow + SE(3) analytical derivation, 3DFlowAction's 3D flow fields)
  - **Implicit (latent representations)**: VPP's VAE encoding + single-step diffusion prediction, S-VAM's self-distillation compression, MWM's future semantic masks replacing RGB prediction
- **Joint WAM**: Directly models the joint distribution, with state prediction and action generation co-optimized in a shared representation space
  - **Autoregressive**: GR-1/GR-2's explicit decoupled representations, CoT-VLA's hybrid attention routing, WorldVLA's modality-specific causal masks
  - **Diffusion-based**: Unified Stream (UWM's independent noise level control, Cosmos Policy's latent frame injection) and Multi-Stream (DUST's independent flow-matching losses, DiT4DiT's paired Video DiT–Action DiT architecture)

### Key Design Decisions
- Cascaded decomposition provides a natural inductive bias: the world model need not reason about robot kinematics, and the action model need not solve long-horizon scene prediction
- Implicit latent planning (VPP, S-VAM) aims to bypass the computational bottleneck of pixel-level video synthesis, achieving inference speeds compatible with real-time control
- Autoregressive joint prediction faces a tension between catastrophic error propagation and the computational bottleneck of sequential decoding, driving the development of diffusion and parallel decoding paths

## Key Innovations

**1. First formal definition and taxonomy of WAMs**: Previously, works integrating world models and action generation were fragmented in architecture, learning objectives, and application scenarios, lacking a unified conceptual framework. The authors' two core criteria (forward predictive modeling + coupled action generation) and the Cascaded/Joint dichotomy provide an actionable classification coordinate system for the entire field, enabling researchers to precisely locate their own contributions and differences from adjacent works.

**2. Clarifying the boundaries between WAMs and adjacent concepts**: The modality-agnostic superset relationship with VAMs, the "explicit predictive commitment" distinction from Video Policies, and the repositioning of AWMs as a "major agent category"—these conceptual distinctions are not mere terminological games but directly determine the minimum requirements for model design: merely inheriting pretrained spatiotemporal representations to map observations to actions is insufficient to qualify as a WAM; explicit next-state synthesis must be a component of reasoning and output.

**3. Systematic review of implicit latent planning paths**: The authors explicitly identify the computational overhead of pixel-level video synthesis as the primary bottleneck for real-time deployment and systematically organize three technical routes that bypass pixel reconstruction—RSSM/TSSM state-space models, JEPA-paradigm latent embedding prediction, and lightweight latent prediction in works such as VPP/S-VAM/MWM. This review provides a clear technology selection map for real-time deployment-oriented research.

## Experiments and Results

This paper is a survey and does not report new experimental results or baseline comparison numbers. However, it summarizes key performance data from representative works:

| Model | Parameter Scale | Simulation Benchmarks | Real Platforms | Key Results |
|------|---------|---------|---------|---------|
| GR-1 | 195M | CALVIN | Kinova Gen2 | Video prediction as action generation regularizer |
| GR-2 | 30-719M | CALVIN | Kinova Gen3 | VQGAN discrete visual pipeline |
| CoT-VLA | 7B | LIBERO, Bridge-V2 | None | Hybrid attention routing |
| WorldVLA | 7B | LIBERO | None | Modality-specific causal masks |
| ℱ1 | 4.2B | LIBERO, SimplerEnv | Genie-1, Franka | MoT decoupled prediction paths |
| VLA-JEPA | 2B | LIBERO, SimplerEnv, LIBERO-Plus | Franka | Latent-space joint sequence modeling |
| VideoVLA | 5B | SIMPLER | Realman | Reuses CogVideoX-5B backbone |
| DreamZero | 14B | PolaRiS, GenieSim3.0 | AgiBot-G1, Franka | KV-cache observation replacement + system-level optimization |
| Cosmos Policy | 2B | LIBERO, RoboCase | ALOHA | Single checkpoint serving as policy, world model, and value function |
| UD-VLA | 8B | CALVIN, LIBERO, SimplerEnv | UR5e | Unified discrete token space mask-and-predict |
| Motus | 8B | Robotwin 2.0, LIBERO-Long | AC-One, Aloha-2 | Tri-modal Joint Attention |
| AdaWorldPolicy | 2.8B | LIBERO-10, Variant PushT, CALVIN | INOVO | Force predictor third expert + LoRA online adaptation |

Additionally, **Ctrl-World** improved the downstream task success rate of the π0.5-DROID policy by 44.7%; **RoboScape** VLA policies trained on synthetic data achieved performance comparable to those trained on real demonstrations; **DREMA** achieved one-shot policy learning on real robots. Ablation experiments confirm that removing future image prediction or video co-training degrades control performance.

## Boundaries and Limitations

Limitations explicitly acknowledged by the authors include: autoregressive video world models suffer from severe error accumulation and struggle to model highly multimodal distributions; diffusion video world models are computationally intensive, with pixel-level diffusion denoising being costly in RGB space; and current evaluation protocols have gaps in assessing WAM-related capabilities. Additionally, the authors note that traditional simulators (IsaacGym, MuJoCo) struggle to capture the complexity and variability of real environments, leading to significant sim-to-real gaps.

The paper does not mention a systematic analysis of the limitations of its own survey methodology or specific recommendations for future work directions (Sec. 7 discusses key obstacles and future directions but does not elaborate). Training configurations, data volumes, inference frequencies, and hardware details are not mentioned, which limits readers' ability to directly judge the practical deployment feasibility of the models discussed. Notably, many models in the paper are validated only in simulation environments (e.g., RoboEnvision, This&That, TesserAct, CoT-VLA, WorldVLA), and their real-world generalization capabilities remain unverified.

## Engineering Implications

Direct guidance for engineering reproduction and model selection:

**First, verify three things**: First, determine whether your task requires an "explicit predictive commitment"—if reactive mapping suffices, a standard VLA is adequate without introducing the additional complexity of WAMs; second, assess your real-time budget—pixel-level video synthesis (explicit Cascaded path) incurs extremely high computational costs for denoising in RGB space; if closed-loop high-frequency control is needed, prioritize implicit latent planning (VPP, S-VAM routes) or Joint WAM with diffusion parallel decoding; third, consider data conditions—standard VLAs strictly require paired $(o_t, a_t)$ trajectories; if your data is predominantly action-free video, UWM's independent noise level control or PAD's token sequence expansion strategy can incorporate action-free internet video data.

**Most common pitfalls**: First, catastrophic error propagation in autoregressive joint prediction—early visual hallucination cascades lead to subsequent action failures; pay attention to mitigation mechanisms such as CoT-VLA's hybrid attention routing or AIM's intent-causal attention masks; second, the "pixel-matching shortcut"—models waste capacity on reconstructing irrelevant background changes rather than learning actionable transition physics; MWM's geometric information bottleneck or LDA-1B's DINO latent-space prediction are worthwhile strategies to avoid this; third, pure discrete control exhibits high variance; if discrete action tokens are adopted, accompanying variance mitigation measures are needed; fourth, two-stage error accumulation in cascaded architectures—UniPi's single long-horizon generation has been found to suffer from semantic drift and compound errors; VLP's semantic-level intervention (limiting error accumulation within a single segment) or Veo-Act's online contact detector (handing control to a reactive VLA upon detecting imminent contact) are effective engineering mitigations.

## 개요

본 논문은复旦大学, 상하이 혁신 연구원, 싱가포르 국립대학이 공동으로 작성하였으며, 처음으로 체계적으로 World Action Models(WAMs)이라는 새로운 패러다임을 제안하고 정의한다. WAM은 예측적 상태 모델링과 행동 생성을 통합하는 임베디드 인공지능 기반 모델이다. 논문은 WAM의 형식적 정의, 두 가지 핵심 판단 기준, Cascaded 및 Joint 두 가지 아키텍처 분류 체계를 제시하고, 데이터 생태계, 평가 프로토콜 및 대표적 연구를 종합적으로 정리하여, 이 파편화된 연구 분야에 통일된 개념적 프레임워크를 제공하는 것을 목표로 한다.

## 무엇을 바꾸었는가

이 리뷰가 진정으로 바꾼 것은 임베디드 인공지능 모델의 본질에 대한 인식의 전환이다. 오랫동안 VLA 모델은 '관찰에서 행동으로'의 반응형 매퍼로 간주되었고, 세계 모델은 외부 시뮬레이터나 데이터 증강 도구로 취급되어, 두 모델은 아키텍처, 목표, 응용 시나리오에서 서로 분리되어 있었다. 저자들은 세계 모델을 외부 도구(감독자)에서 정책 아키텍처 내부의 예측 핵심으로 재배치하고, 'World'(물리 예측)와 'Action'(운동 제어)을 동등한 구성 요소로 하는 새로운 패러다임을 확립했다. 이 개념적 재구성의 의미는: 처음으로 흩어져 있던 '예측+제어' 유형의 연구에 통일된 분류 좌표계를 제공하여, 연구자들이 파편화된 용어 속에서 각자 싸우는 대신 서로 다른 연구가 아키텍처 스펙트럼에서 차지하는 위치를 명확히 식별할 수 있게 한 것이다.

더 중요한 것은, 저자들이 WAM과 Video Action Models(VAMs), Video Policies, Action World Models(AWMs) 등 인접 개념을 구분함으로써 오랫동안 존재해 온 용어 혼란을 해소했다는 점이다. WAM은 모달리티에 구애받지 않는 상위 집합이며, 비디오는 세계를 모델링하는 하나의 대리 수단일 뿐이다. Video Policies는 사전 훈련된 시공간 표현을 상속받아 관찰을 행동에 매핑하기만 하면 되지만, WAM은 능동적 예측 약속을 가져야 한다—다음 상태의 합성이 모델 추론 및 출력의 명시적 구성 요소여야 한다. 이러한 개념적 경계 설정은 후속 연구에 명확한 진입 기준을 제공한다.

## 방법 분해

### WAM 형식적 정의
WAM의 목표 분포는 결합 분포 $p(o', a | o, l)$이며, 목표 함수는:
$$\mathcal{L}_{WAM} = \mathbb{E}_{(o,l,o',a)\sim\mathcal{D}}[-\log p(o', a | o, l)]$$

### 두 가지 핵심 판단 기준
1. **전방 예측 모델링**: 미래 상태 $o'$의 정량화 가능한 표현을 생성하거나 활용해야 함(픽셀 수준 비디오 프레임, 조밀한 광학 흐름과 같은 명시적 시각적 예측, 또는 물리적 접지 잠재 공간과 같은 암시적 물리적 표현)
2. **결합된 행동 생성**: 행동 $a$는 엄격하게 기대되는 미래 상태 $o'$와 정렬되어야 함(결합 확률 출력 또는 캐스케이드/통합 잠재 아키텍처에서의 정책 조건화)

### 두 가지 아키텍처 범주
- **Cascaded WAM**: 목표를 명시적으로 분해 $p(o', a|o, l) = p(a|o', o, l)p(o'|o, l)$, 먼저 미래 상태 표현을 합성한 다음 행동을 도출
  - **Explicit(픽셀 공간)**: 학습 기반 행동 추출(UniPi의 IDM, π0.7의 VLA 대체 IDM), 기하학 기반 행동 추출(AVDC의 광학 흐름+SE(3) 해석적 유도, 3DFlowAction의 3차원 흐름장)
  - **Implicit(잠재 표현)**: VPP의 VAE 인코딩+단일 단계 확산 예측, S-VAM의 자기 증류 압축, MWM의 미래 의미 마스크로 RGB 예측 대체
- **Joint WAM**: 결합 분포를 직접 모델링, 상태 예측과 행동 생성이 공유 표현 공간에서 공동 최적화
  - **Autoregressive**: GR-1/GR-2의 명시적 분리 표현, CoT-VLA의 혼합 어텐션 라우팅, WorldVLA의 모달리티 특정 인과 마스크
  - **Diffusion 기반**: Unified Stream(UWM의 독립 노이즈 수준 제어, Cosmos Policy의 잠재 프레임 주입) 및 Multi-Stream(DUST의 독립 flow-matching 손실, DiT4DiT의 쌍 Video DiT–Action DiT 아키텍처)

### 핵심 설계 결정
- 캐스케이드 분해는 자연스러운 귀납적 편향을 제공: 세계 모델은 로봇 운동학을 추론할 필요가 없고, 행동 모델은 장기간 장면 예측을 해결할 필요가 없음
- 암시적 잠재 계획(VPP, S-VAM)은 픽셀 수준 비디오 합성의 계산 병목을 우회하여 실시간 제어와 호환되는 추론 속도를 목표로 함
- 자기회귀적 결합 예측은 치명적 오류 전파와 순차 디코딩 계산 병목 사이의 긴장에 직면하여, 확산 및 병렬 디코딩 경로의 발전을 촉진

## 핵심 혁신

**1. WAM의 형식적 정의와 분류 체계를 처음으로 제시**: 이전의 세계 모델과 행동 생성 통합 연구는 아키텍처, 학습 목표, 응용 시나리오에서 파편화되어 통일된 개념 프레임워크가 부족했다. 저자들이 제안한 두 가지 핵심 판단 기준(전방 예측 모델링+결합 행동 생성)과 Cascaded/Joint 이분법은 전체 분야에 실행 가능한 분류 좌표를 제공하여, 연구자들이 자신의 연구 혁신점과 인접 연구와의 차이를 정확히 위치시킬 수 있게 한다.

**2. WAM과 인접 개념의 경계를 명확히 함**: VAMs와의 모달리티 무관 상위 집합 관계, Video Policies와의 '능동적 예측 약속' 구분, AWMs와의 '에이전트 주요 범주' 재배치—이러한 개념 변별은 단순한 용어 게임이 아니라 모델 설계의 최소 요구 사항을 직접 결정한다: 사전 훈련된 시공간 표현을 상속받아 관찰을 행동에 매핑하는 것만으로는 WAM이라고 할 수 없으며, 명시적 다음 상태 합성이 추론 및 출력의 구성 요소여야 한다.

**3. 암시적 잠재 계획 경로를 체계적으로 정리**: 저자들은 픽셀 수준 비디오 합성의 계산 비용이 실시간 배포의 주요 병목임을 명확히 지적하고, 픽셀 재구성을 우회하는 세 가지 기술 경로를 체계적으로 정리했다—RSSM/TSSM 상태 공간 모델, JEPA 패러다임의 잠재 임베딩 예측, VPP/S-VAM/MWM 등의 경량 잠재 예측. 이 정리는 실시간 배포 지향 연구에 명확한 기술 선택 지도를 제공한다.

## 실험 및 결과

본 논문은 리뷰 논문으로, 새로운 실험 결과나 기준 비교 수치를 보고하지 않는다. 그러나 대표적 연구의 핵심 성능 데이터를 요약했다:

| 모델 | 파라미터 규모 | 시뮬레이션 벤치마크 | 실제 플랫폼 | 핵심 결과 |
|------|---------|---------|---------|---------|
| GR-1 | 195M | CALVIN | Kinova Gen2 | 비디오 예측을 행동 생성 정규화기로 사용 |
| GR-2 | 30-719M | CALVIN | Kinova Gen3 | VQGAN 이산 시각 파이프라인 |
| CoT-VLA | 7B | LIBERO, Bridge-V2 | 없음 | 혼합 어텐션 라우팅 |
| WorldVLA | 7B | LIBERO | 없음 | 모달리티 특정 인과 마스크 |
| ℱ1 | 4.2B | LIBERO, SimplerEnv | Genie-1, Franka | MoT 분리 예측 경로 |
| VLA-JEPA | 2B | LIBERO, SimplerEnv, LIBERO-Plus | Franka | 잠재 공간 결합 시퀀스 모델링 |
| VideoVLA | 5B | SIMPLER | Realman | CogVideoX-5B 백본 재사용 |
| DreamZero | 14B | PolaRiS, GenieSim3.0 | AgiBot-G1, Franka | KV-cache 관찰 교체+시스템 수준 최적화 |
| Cosmos Policy | 2B | LIBERO, RoboCase | ALOHA | 단일 checkpoint가 정책, 세계 모델, 가치 함수로 동시에 작동 |
| UD-VLA | 8B | CALVIN, LIBERO, SimplerEnv | UR5e | 통일된 이산 토큰 공간 mask-and-predict |
| Motus | 8B | Robotwin 2.0, LIBERO-Long | AC-One, Aloha-2 | Tri-modal Joint Attention |
| AdaWorldPolicy | 2.8B | LIBERO-10, Variant PushT, CALVIN | INOVO | 힘 예측기 제3 전문가+LoRA 온라인 적응 |

또한, **Ctrl-World**는 π0.5-DROID 정책의 다운스트림 작업 성공률을 44.7% 향상시켰고; **RoboScape** 합성 데이터로 훈련된 VLA 정책은 실제 시연 훈련과 동등한 성능을 달성했으며; **DREMA**는 실제 로봇에서 원샷 정책 학습을 구현했다. 절제 실험은 미래 이미지 예측 또는 비디오 공동 훈련을 제거하면 제어 성능이 저하됨을 확인했다.

## 경계와 한계

저자들이 명시적으로 인정한 한계는: 자기회귀 비디오 세계 모델은 심각한 오류 누적 문제가 있고 고도로 다중 모달 분포를 모델링하기 어렵다; 확산 비디오 세계 모델은 계산 집약적이며, 픽셀 수준 확산 노이즈 제거는 RGB 공간에서 계산 비용이 높다; 현재 평가 프로토콜은 WAM 관련 능력 평가에 공백이 있다. 또한, 저자들은 전통적 시뮬레이터(IsaacGym, MuJoCo)가 실제 환경의 복잡성과 변동성을 포착하기 어려워 상당한 sim-to-real 격차를 초래한다고 지적한다.

논문은 자체 리뷰 방법론에 대한 체계적 한계 분석이나 미래 작업 방향에 대한 구체적 제안을 언급하지 않는다(Sec. 7에서 핵심 장애물과 미래 방향을 논의하지만 확장하지 않음). 훈련 구성, 데이터 양, 추론 빈도, 하드웨어 세부 사항은 언급되지 않아, 독자가 각 모델의 실제 배포 가능성을 직접 판단하는 데 제한이 있다. 주목할 점은, 논문의 많은 모델이 시뮬레이션 환경에서만 검증되었으며(RoboEnvision, This&That, TesserAct, CoT-VLA, WorldVLA 등), 실제 세계 일반화 능력은 아직 입증되지 않았다.

## 공학적 시사점

공학적 재현 및 선택에 대한 직접적 지침:

**먼저 세 가지를 확인하라**: 첫째, 작업에 '능동적 예측 약속'이 필요한지 명확히 하라—반응형 매핑만 필요하다면 표준 VLA로 충분하며, WAM의 추가 복잡성을 도입할 필요가 없다; 둘째, 실시간 예산을 평가하라—픽셀 수준 비디오 합성(명시적 Cascaded 경로)의 RGB 공간 노이즈 제거 계산 비용이 매우 높으므로, 폐쇄 루프 고주파 제어가 필요하다면 암시적 잠재 계획(VPP, S-VAM 경로) 또는 Joint WAM의 확산 병렬 디코딩을 우선 고려해야 한다; 셋째, 데이터 조건—표준 VLA는 엄격하게 쌍을 이루는 $(o_t, a_t)$ 궤적을 요구하지만, 데이터가 주로 행동 없는 비디오라면 UWM의 독립 노이즈 수준 제어 또는 PAD의 토큰 시퀀스 확장 전략을 통해 행동 없는 인터넷 비디오 데이터를 통합할 수 있다.

**가장 함정에 빠지기 쉬운 부분**: 첫째, 자기회귀 결합 예측의 치명적 오류 전파—초기 시각적 환각 캐스케이드가 후속 행동 실패를 초래하므로, CoT-VLA의 혼합 어텐션 라우팅 또는 AIM의 intent-causal 어텐션 마스크와 같은 완화 메커니즘에 주목해야 한다; 둘째, '픽셀 매칭 지름길'—모델이 작동 가능한 전이 물리학을 학습하는 대신 무관한 배경 변화를 재구성하는 데 용량을 낭비하는 경우, MWM의 기하학적 정보 병목 또는 LDA-1B의 DINO 잠재 공간 예측은 참고할 만한 회피 전략이다; 셋째, 순수 이산 제어의 분산이 높으므로, 이산 행동 토큰을 사용한다면 분산 완화 조치가 필요하다; 넷째, 캐스케이드 아키텍처의 2단계 오류 누적—UniPi의 단일 장기 생성은 의미 드리프트와 복합 오류가 발견되었으며, VLP의 의미 수준 개입(오류를 단일 세그먼트 내로 제한) 또는 Veo-Act의 온라인 상호작용 감지기(접촉이 임박했을 때 제어를 반응형 VLA에 넘김)는 효과적인 공학적 완화 수단이다.
