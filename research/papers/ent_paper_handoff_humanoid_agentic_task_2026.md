---
$id: ent_paper_handoff_humanoid_agentic_task_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HANDOFF: Humanoid Agentic Task-Space Whole-Body Control via Distilled Complementary Teachers'
  zh: HANDOFF｜通过精炼补充教师进行人形智能体任务空间全身控制
  ko: 'HANDOFF: Humanoid Agentic Task-Space Whole-Body Control via Distilled Complementary Teachers'
summary:
  en: Handoff transforms ontological states and joint sequences into trackable body targets, and through teacher-student knowledge
    transfer, diffusion strategy/flow matching, VLM semantic planning/routing training, or combined whole-body strategies,
    the HANDOFF can be implemented in the context of multi-agent systems, finally, the whole body trajectory/action sequence
    and low-level controller target are output. The key is to train the teacher's strategy with privileged information and
    distill the ability to use only the student's strategy of deploying observations.
  zh: HANDOFF 把本体状态与关节序列转成可跟踪的身体目标，并通过教师-学生知识迁移、扩散策略/流匹配、VLM 语义规划/路由训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标。关键点是用特权信息训练教师策略，再把能力蒸馏到只能使用部署观测的学生策略。
  ko: HANDOFF 把本体状态与关节序列转成可跟踪的身体目标，并通过教师-学生知识迁移、扩散策略/流匹配、VLM 语义规划/路由训练或组合全身策略，最终输出全身轨迹/动作序列、低层控制器目标。关键点是用特权信息训练教师策略，再把能力蒸馏到只能使用部署观测的学生策略。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- handoff
- manipulation_interface
- mobile_manipulation
- upper_body_control
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: HANDOFF project page
  url: https://lzyang2000.github.io/HANDOFF/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

针对人形机器人在复杂任务中面临的全身协调控制与感知-行动闭环难题，本文提出 HANDOFF 框架，通过教师-学生知识蒸馏策略，将依赖特权信息的全身控制能力迁移至仅依赖可部署观测的学生策略。该方法创新性地融合扩散策略、流匹配与大视觉语言模型（VLM）语义规划，实现了从本体状态与关节序列到可跟踪身体目标的转换，最终输出全身轨迹与低层控制器指令。HANDOFF 为人形机器人从仿真训练到真实部署提供了一条高效、可扩展的技术路径。

## 核心内容

人形机器人的全身控制（Whole-Body Control）长期受困于高维状态空间与复杂动力学耦合问题。传统方法或依赖精确动力学模型，或需要大量人工调参，难以适应非结构化环境中的移动操作任务。现有强化学习策略虽能在仿真中取得良好表现，但其对特权信息（如物体质量、地面摩擦系数、未来接触力等）的依赖，导致策略难以直接迁移至仅能获取本体感知与视觉观测的真实机器人上。这一“仿真-现实”鸿沟成为制约人形机器人自主作业的关键瓶颈。

HANDOFF 的核心框架采用教师-学生知识蒸馏范式。首先，在仿真环境中利用特权信息训练一个“教师策略”，该策略能够基于完整的环境状态生成最优的全身关节轨迹与身体目标。随后，通过蒸馏技术，将教师策略的决策能力压缩至一个“学生策略”，该学生策略仅能访问部署时可获得的观测信息（如关节编码器读数、IMU数据、RGB-D图像等）。蒸馏过程中，HANDOFF 引入扩散策略与流匹配（Flow Matching）技术，使得学生策略能够从教师策略的轨迹分布中高效采样，而非简单拟合均值，从而保留动作的多样性与鲁棒性。

在技术创新层面，HANDOFF 提出了“可跟踪身体目标”（Trackable Body Targets）这一中间表征，将高维的本体状态与关节序列转化为低维、物理可解释的参考点（如质心位置、末端执行器姿态、躯干朝向等）。这一设计不仅降低了蒸馏难度，还使得学生策略能够与底层全身控制器（如QP求解器或阻抗控制器）无缝对接。此外，HANDOFF 集成了大视觉语言模型（VLM）进行高层语义规划与路由训练，使机器人能够理解自然语言指令并分解为子任务序列，再由全身控制策略逐级执行，实现了从语义到运动的端到端贯通。

实验验证方面，HANDOFF 在多种人形机器人移动操作任务中进行了评估，包括物体抓取、开门、搬运等。结果表明，蒸馏后的学生策略在仅使用部署观测的条件下，能够复现教师策略在特权信息下的任务成功率，且动作平滑度与执行效率接近教师水平。在零样本泛化测试中，HANDOFF 展现出对未见过物体形状、地面材质变化及光照条件的良好适应性。该框架的意义在于，它为人形机器人的全身控制提供了一条“先仿真学习、后蒸馏部署”的实用路线，显著降低了真实机器人的调试成本与安全风险，有望推动人形机器人在家庭服务、工业装配等场景中的实际落地。

## 参考

- HANDOFF project page (https://lzyang2000.github.io/HANDOFF/)

## Overview

To address the challenges of whole-body coordination and perception-action loops in complex tasks for humanoid robots, this paper proposes the HANDOFF framework. It employs a teacher-student knowledge distillation strategy to transfer whole-body control capabilities that rely on privileged information to a student policy that only uses deployable observations. This method innovatively integrates diffusion policy, flow matching, and large vision-language model (VLM) semantic planning, enabling the conversion from proprioceptive states and joint sequences to trackable body targets, ultimately outputting whole-body trajectories and low-level controller commands. HANDOFF provides an efficient and scalable technical pathway for humanoid robots from simulation training to real-world deployment.

## Content

Whole-body control of humanoid robots has long been plagued by high-dimensional state spaces and complex dynamic coupling issues. Traditional methods either rely on precise dynamic models or require extensive manual parameter tuning, making them difficult to adapt to mobile manipulation tasks in unstructured environments. Although existing reinforcement learning policies perform well in simulation, their dependence on privileged information (e.g., object mass, ground friction coefficient, future contact forces) hinders direct transfer to real robots that can only access proprioceptive and visual observations. This "sim-to-real" gap is a key bottleneck limiting autonomous operation of humanoid robots.

The core framework of HANDOFF adopts a teacher-student knowledge distillation paradigm. First, a "teacher policy" is trained in simulation using privileged information, capable of generating optimal whole-body joint trajectories and body targets based on complete environmental states. Subsequently, through distillation, the decision-making capability of the teacher policy is compressed into a "student policy" that only accesses observations available during deployment (e.g., joint encoder readings, IMU data, RGB-D images). During distillation, HANDOFF introduces diffusion policy and flow matching techniques, enabling the student policy to efficiently sample from the trajectory distribution of the teacher policy rather than simply fitting the mean, thereby preserving action diversity and robustness.

In terms of technical innovation, HANDOFF proposes the intermediate representation of "trackable body targets," converting high-dimensional proprioceptive states and joint sequences into low-dimensional, physically interpretable reference points (e.g., center of mass position, end-effector pose, torso orientation). This design not only reduces the difficulty of distillation but also allows the student policy to seamlessly interface with low-level whole-body controllers (e.g., QP solvers or impedance controllers). Additionally, HANDOFF integrates large vision-language models (VLMs) for high-level semantic planning and routing training, enabling the robot to understand natural language instructions and decompose them into sub-task sequences, which are then executed stepwise by the whole-body control policy, achieving end-to-end integration from semantics to motion.

In experimental validation, HANDOFF was evaluated on various humanoid robot mobile manipulation tasks, including object grasping, door opening, and carrying. Results show that the distilled student policy, using only deployment observations, can replicate the task success rate of the teacher policy under privileged information, with motion smoothness and execution efficiency close to teacher levels. In zero-shot generalization tests, HANDOFF demonstrated good adaptability to unseen object shapes, ground material variations, and lighting conditions. The significance of this framework lies in providing a practical "simulation learning first, then distillation deployment" route for whole-body control of humanoid robots, significantly reducing debugging costs and safety risks for real robots, and potentially advancing the practical deployment of humanoid robots in scenarios such as home services and industrial assembly.

## 개요

휴머노이드 로봇이 복잡한 작업에서 직면하는 전신 협조 제어와 인지-행동 폐쇄 루프 문제를 해결하기 위해, 본 논문은 HANDOFF 프레임워크를 제안한다. 이는 교사-학생 지식 증류 전략을 통해 특권 정보에 의존하는 전신 제어 능력을 배치 가능한 관측만을 사용하는 학생 정책으로 이전한다. 이 방법은 확산 정책, 흐름 매칭 및 대규모 시각-언어 모델(VLM)의 의미론적 계획을 혁신적으로 통합하여, 본체 상태와 관절 시퀀스를 추적 가능한 신체 목표로 변환하고, 최종적으로 전신 궤적과 저수준 제어기 명령을 출력한다. HANDOFF는 휴머노이드 로봇의 시뮬레이션 훈련에서 실제 배포까지의 효율적이고 확장 가능한 기술 경로를 제공한다.

## 핵심 내용

휴머노이드 로봇의 전신 제어는 오랫동안 고차원 상태 공간과 복잡한 동역학 결합 문제에 어려움을 겪어 왔다. 기존 방법은 정밀한 동역학 모델에 의존하거나 많은 수동 매개변수 조정이 필요하여, 비구조적 환경에서의 이동 조작 작업에 적응하기 어렵다. 기존 강화 학습 정책은 시뮬레이션에서 좋은 성과를 보이지만, 특권 정보(예: 물체 질량, 지면 마찰 계수, 미래 접촉력 등)에 의존하여 본체 감각과 시각 관측만 가능한 실제 로봇으로의 직접 이전이 어렵다. 이러한 '시뮬레이션-실제' 격차는 휴머노이드 로봇의 자율 작업을 제한하는 주요 병목 현상이다.

HANDOFF의 핵심 프레임워크는 교사-학생 지식 증류 패러다임을 채택한다. 먼저, 시뮬레이션 환경에서 특권 정보를 활용하여 '교사 정책'을 훈련한다. 이 정책은 완전한 환경 상태를 기반으로 최적의 전신 관절 궤적과 신체 목표를 생성한다. 그런 다음, 증류 기술을 통해 교사 정책의 의사 결정 능력을 '학생 정책'으로 압축한다. 학생 정책은 배포 시 얻을 수 있는 관측 정보(예: 관절 엔코더 판독값, IMU 데이터, RGB-D 이미지 등)만 접근할 수 있다. 증류 과정에서 HANDOFF는 확산 정책과 흐름 매칭 기술을 도입하여, 학생 정책이 단순히 평균을 맞추는 대신 교사 정책의 궤적 분포에서 효율적으로 샘플링할 수 있게 하여, 행동의 다양성과 강건성을 유지한다.

기술 혁신 측면에서 HANDOFF는 '추적 가능한 신체 목표'라는 중간 표현을 제안한다. 이는 고차원의 본체 상태와 관절 시퀀스를 저차원의 물리적으로 해석 가능한 참조점(예: 질량 중심 위치, 말단 효과기 자세, 몸통 방향 등)으로 변환한다. 이 설계는 증류 난이도를 낮출 뿐만 아니라, 학생 정책이 저수준 전신 제어기(예: QP 솔버 또는 임피던스 제어기)와 원활하게 연결될 수 있게 한다. 또한, HANDOFF는 대규모 시각-언어 모델(VLM)을 통합하여 고수준 의미론적 계획 및 경로 훈련을 수행함으로써, 로봇이 자연어 명령을 이해하고 하위 작업 시퀀스로 분해한 후, 전신 제어 정책이 단계적으로 실행하여 의미론에서 운동까지의 종단 간 연결을 실현한다.

실험 검증 측면에서 HANDOFF는 물체 잡기, 문 열기, 운반 등 다양한 휴머노이드 로봇 이동 조작 작업에서 평가되었다. 결과는 증류된 학생 정책이 배포 관측만을 사용하여 특권 정보 하의 교사 정책의 작업 성공률을 재현할 수 있으며, 동작의 부드러움과 실행 효율이 교사 수준에 근접함을 보여준다. 제로샷 일반화 테스트에서 HANDOFF는 보지 못한 물체 형태, 지면 재질 변화 및 조명 조건에 대한 우수한 적응성을 나타냈다. 이 프레임워크의 의의는 휴머노이드 로봇의 전신 제어를 위한 '시뮬레이션 학습 후 증류 배포'라는 실용적인 경로를 제공하여, 실제 로봇의 디버깅 비용과 안전 위험을 크게 줄이며, 가사 서비스, 산업 조립 등 현장에서의 휴머노이드 로봇 실용화를 촉진할 것으로 기대된다.
