---
$id: ent_paper_learning_human_humanoid_coordi_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Learning Human-Humanoid Coordination for Collaborative Object Carrying
  zh: COLA｜学习人机协调以协作搬运物体
  ko: Learning Human-Humanoid Coordination for Collaborative Object Carrying
summary:
  en: COLA first recovers scene, target or motion representations from ontology state and joint sequences, human video/motion
    capture trajectories, and simulation interaction data, and then uses PPO/RL policy training, world model/video prediction,
    closed-loop error correction/human intervention to generate whole-body trajectories/action sequences and low-level controller
    targets. The key point is to put PPO/RL policy training and world model/video prediction in the same training/deployment
    chain to reduce the breakpoints between high-level goals and low-level actions.
  zh: COLA 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、世界模型/视频预测、闭环纠错/人类干预生成全身轨迹/动作序列、低层控制器目标。关键点是把PPO/RL 策略训练、世界模型/视频预测放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
  ko: COLA 先从本体状态与关节序列、人类视频/动捕轨迹、仿真交互数据恢复场景、目标或运动表征，再用PPO/RL 策略训练、世界模型/视频预测、闭环纠错/人类干预生成全身轨迹/动作序列、低层控制器目标。关键点是把PPO/RL 策略训练、世界模型/视频预测放在同一条训练/部署链路里，减少高层目标到低层动作之间的断点。
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- cola
- manipulation_interface
- mobile_manipulation
- upper_body_control
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata.
sources:
- id: src_001
  type: website
  title: COLA project page
  url: https://yushi-du.github.io/COLA/
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

人形机器人与人类协作搬运物体是服务机器人走向实际应用的关键场景，但现有方法常因高层任务规划与低层运动控制之间存在断点而难以实现流畅协调。本文提出的COLA（Coordination Learning Architecture）框架，通过将PPO/RL策略训练与世界模型/视频预测整合至同一训练与部署链路，有效弥合了高层目标与低层动作之间的鸿沟。该工作从本体状态、人类动捕轨迹及仿真交互数据中联合恢复场景与运动表征，并引入闭环纠错与人类干预机制，为提升人机协调的鲁棒性与自然性提供了系统化解决方案。

## 核心内容

在人形机器人协作搬运任务中，核心挑战在于如何让机器人理解人类意图并实时生成与之协调的全身运动。传统方法通常将高层任务规划（如目标位置设定）与低层运动控制（如关节轨迹生成）分离处理，导致规划结果难以直接映射为可执行的动作序列，尤其在动态交互场景下容易出现响应滞后或动作失配。COLA正是针对这一“规划-控制断点”问题而设计，旨在通过统一的学习框架实现从感知到动作的端到端协调。

COLA的模型框架包含三个核心模块：表征恢复模块、策略学习模块与闭环执行模块。首先，表征恢复模块从多种数据源——包括机器人本体状态与关节序列、人类视频或动捕轨迹、以及仿真环境中的交互数据——中提取场景语义、目标位姿与运动基元等高层表征。这些表征作为策略学习的输入，使模型能够理解当前交互状态与人类意图。

在技术创新层面，COLA的关键贡献在于将PPO/RL策略训练与世界模型/视频预测置于同一条训练与部署链路中。具体而言，世界模型负责预测未来若干步的视觉与状态演化，为RL策略提供前瞻性的价值估计；而RL策略则根据世界模型的预测结果生成全身轨迹与动作序列，并输出低层控制器目标。这种联合训练方式使得策略能够同时考虑长期回报与短期运动可行性，显著减少了高层规划与低层执行之间的语义与时间尺度断点。此外，框架还集成了闭环纠错机制，允许在部署阶段根据实时传感器反馈调整动作，并支持人类通过干预信号修正机器人行为。

实验验证方面，COLA在仿真环境与真实人形机器人平台上进行了协作搬运任务测试。结果表明，与基线方法（如单独使用RL策略或仅依赖世界模型规划）相比，COLA在任务成功率、人机协调流畅度以及对外部扰动的鲁棒性上均有显著提升。特别是在需要实时适应人类运动速度变化或路径调整的场景中，COLA的联合训练链路能够更快地生成匹配的全身运动，减少了人机之间的力交互冲突。该框架为家庭服务、仓储物流等需要人机紧密协作的领域提供了可部署的技术方案，其设计理念也对未来人形机器人通用协调能力的构建具有参考价值。

## 参考
- COLA project page (https://yushi-du.github.io/COLA/)

## Overview

Humanoid robots collaborating with humans to carry objects is a key scenario for service robots moving toward practical applications. However, existing methods often struggle to achieve smooth coordination due to the disconnect between high-level task planning and low-level motion control. The COLA (Coordination Learning Architecture) framework proposed in this paper effectively bridges the gap between high-level goals and low-level actions by integrating PPO/RL policy training with world models/video prediction into a unified training and deployment pipeline. This work jointly recovers scene and motion representations from proprioceptive states, human motion capture trajectories, and simulation interaction data, and introduces closed-loop error correction and human intervention mechanisms, providing a systematic solution for enhancing the robustness and naturalness of human-robot coordination.

## Content

In humanoid robot collaborative carrying tasks, the core challenge lies in enabling the robot to understand human intentions and generate coordinated whole-body motions in real time. Traditional methods typically separate high-level task planning (e.g., setting target positions) from low-level motion control (e.g., generating joint trajectories), resulting in planning outcomes that are difficult to directly map to executable action sequences, especially in dynamic interaction scenarios where response delays or motion mismatches occur. COLA is specifically designed to address this "planning-control gap" by achieving end-to-end coordination from perception to action through a unified learning framework.

The COLA model framework comprises three core modules: a representation recovery module, a policy learning module, and a closed-loop execution module. First, the representation recovery module extracts high-level representations such as scene semantics, target poses, and motion primitives from multiple data sources—including robot proprioceptive states and joint sequences, human videos or motion capture trajectories, and interaction data from simulation environments. These representations serve as inputs for policy learning, enabling the model to understand the current interaction state and human intentions.

At the technical innovation level, COLA's key contribution lies in placing PPO/RL policy training and world models/video prediction within the same training and deployment pipeline. Specifically, the world model predicts future visual and state evolutions over several steps, providing forward-looking value estimates for the RL policy; the RL policy then generates whole-body trajectories and action sequences based on the world model's predictions, outputting targets for low-level controllers. This joint training approach allows the policy to simultaneously consider long-term rewards and short-term motion feasibility, significantly reducing the semantic and temporal gaps between high-level planning and low-level execution. Additionally, the framework integrates a closed-loop error correction mechanism, enabling adjustments to actions based on real-time sensor feedback during deployment and supporting human intervention signals to correct robot behavior.

In terms of experimental validation, COLA was tested on collaborative carrying tasks in both simulation environments and real humanoid robot platforms. Results show that compared to baseline methods (e.g., using RL policies alone or relying solely on world model planning), COLA achieves significant improvements in task success rate, human-robot coordination smoothness, and robustness to external disturbances. Particularly in scenarios requiring real-time adaptation to changes in human movement speed or path adjustments, COLA's joint training pipeline generates matching whole-body motions more quickly, reducing force interaction conflicts between humans and robots. This framework provides a deployable technical solution for fields requiring close human-robot collaboration, such as home services and warehouse logistics, and its design philosophy offers valuable insights for building general coordination capabilities in future humanoid robots.

## 개요

휴머노이드 로봇과 인간이 협력하여 물체를 운반하는 것은 서비스 로봇이 실제 응용으로 나아가는 핵심 시나리오이지만, 기존 방법은 고수준 작업 계획과 저수준 운동 제어 사이에 단절이 존재하여 원활한 협조를 달성하기 어려운 경우가 많습니다. 본 논문에서 제안하는 COLA(Coordination Learning Architecture) 프레임워크는 PPO/RL 정책 훈련과 세계 모델/비디오 예측을 동일한 훈련 및 배포 파이프라인에 통합함으로써 고수준 목표와 저수준 동작 사이의 간극을 효과적으로 메웁니다. 이 연구는 본체 상태, 인간 모션 캡처 궤적 및 시뮬레이션 상호작용 데이터로부터 장면과 운동 표현을 공동으로 복원하고, 폐루프 오류 수정 및 인간 개입 메커니즘을 도입하여 인간-로봇 협조의 견고성과 자연스러움을 향상시키는 체계적인 솔루션을 제공합니다.

## 핵심 내용

휴머노이드 로봇 협력 운반 작업에서 핵심 과제는 로봇이 인간의 의도를 이해하고 이에 맞춰 실시간으로 전신 운동을 생성하는 방법입니다. 전통적인 방법은 일반적으로 고수준 작업 계획(예: 목표 위치 설정)과 저수준 운동 제어(예: 관절 궤적 생성)를 분리하여 처리하므로, 계획 결과를 실행 가능한 동작 시퀀스로 직접 매핑하기 어렵고, 특히 동적 상호작용 시나리오에서 응답 지연이나 동작 불일치가 발생하기 쉽습니다. COLA는 바로 이러한 '계획-제어 단절' 문제를 해결하기 위해 설계되었으며, 통합된 학습 프레임워크를 통해 인식에서 동작까지의 종단간 협조를 목표로 합니다.

COLA의 모델 프레임워크는 세 가지 핵심 모듈로 구성됩니다: 표현 복원 모듈, 정책 학습 모듈 및 폐루프 실행 모듈. 먼저, 표현 복원 모듈은 로봇 본체 상태 및 관절 시퀀스, 인간 비디오 또는 모션 캡처 궤적, 시뮬레이션 환경의 상호작용 데이터 등 다양한 데이터 소스로부터 장면 의미, 목표 자세 및 운동 프리미티브와 같은 고수준 표현을 추출합니다. 이러한 표현은 정책 학습의 입력으로 사용되어 모델이 현재 상호작용 상태와 인간 의도를 이해할 수 있게 합니다.

기술 혁신 측면에서 COLA의 핵심 기여는 PPO/RL 정책 훈련과 세계 모델/비디오 예측을 동일한 훈련 및 배포 파이프라인에 배치한 점입니다. 구체적으로, 세계 모델은 미래 여러 단계의 시각 및 상태 진화를 예측하여 RL 정책에 선견지명 있는 가치 추정을 제공하고, RL 정책은 세계 모델의 예측 결과에 따라 전신 궤적과 동작 시퀀스를 생성하며 저수준 제어기 목표를 출력합니다. 이러한 공동 훈련 방식은 정책이 장기 보상과 단기 운동 가능성을 동시에 고려할 수 있게 하여, 고수준 계획과 저수준 실행 사이의 의미 및 시간 척도 단절을 크게 줄입니다. 또한, 프레임워크는 폐루프 오류 수정 메커니즘을 통합하여 배포 단계에서 실시간 센서 피드백에 따라 동작을 조정하고, 인간이 개입 신호를 통해 로봇 행동을 수정할 수 있도록 지원합니다.

실험 검증 측면에서 COLA는 시뮬레이션 환경과 실제 휴머노이드 로봇 플랫폼에서 협력 운반 작업 테스트를 수행했습니다. 결과는 기준 방법(예: RL 정책 단독 사용 또는 세계 모델 계획에만 의존)과 비교하여 COLA가 작업 성공률, 인간-로봇 협조의 원활함 및 외부 교란에 대한 견고성에서 현저한 향상을 보였습니다. 특히 인간의 운동 속도 변화나 경로 조정에 실시간으로 적응해야 하는 시나리오에서 COLA의 공동 훈련 파이프라인은 일치하는 전신 운동을 더 빠르게 생성하여 인간-로봇 간의 힘 상호작용 충돌을 줄였습니다. 이 프레임워크는 가사 서비스, 창고 물류 등 인간-로봇 긴밀 협력이 필요한 분야에 배포 가능한 기술 솔루션을 제공하며, 그 설계 개념은 미래 휴머노이드 로봇의 일반 협조 능력 구축에 참고 가치가 있습니다.
