---
$id: ent_paper_humanx_toward_agile_and_genera_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HumanX: Toward Agile and Generalizable Humanoid Interaction Skills from Human Videos'
  zh: HumanX｜从人类视频中走向敏捷和可推广的人形交互技能
  ko: 'HumanX: Toward Agile and Generalizable Humanoid Interaction Skills from Human Videos'
summary:
  en: HumanX first recovers scene, target, or motion representations from human videos/move-capture trajectories, and then
    generates actionable action commands using ACT/behavior cloning imitation learning, IK/action redirection, and hierarchical
    skills/expert strategies. The key is to break down tasks into routable skills or expert strategies, which are then selected
    and combined in execution using high-level modules.
  zh: HumanX 先从人类视频/动捕轨迹恢复场景、目标或运动表征，再用ACT/行为克隆模仿学习、IK/动作重定向、分层技能/专家策略生成可执行动作命令。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
  ko: HumanX는 먼저 인간 비디오/모션 캡처 궤적으로부터 장면, 목표 또는 운동 표현을 복원한 다음, ACT/행동 복제 모방 학습, IK/동작 리타게팅, 계층적 스킬/전문가 정책을 사용하여 실행 가능한 동작 명령을
    생성한다. 핵심은 작업을 라우팅 가능한 스킬 또는 전문가 정책으로 분해하고, 고수준 모듈이 실행 중에 이를 선택하고 조합하는 것이다.
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- human_video
- humanx
- interaction_planning
- motion_capture
- motion_retargeting
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP3
    marker fix 2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, project-page source only, limited; number whitelist enforced
    at generation); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: HumanX project page
  url: https://wyhuai.github.io/human-x/
  date: '2026'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

HumanX 提出 RynnVLA-001，一种通过三阶段渐进式训练将大规模第一人称人类视频中的操作技能迁移到人形机器人控制的新框架。核心思路是先训练视频生成模型，再引入轨迹感知中间阶段，最后在机器人数据上微调，以弥合视觉预测与低层动作空间之间的差距。该方法在相同机器人数据上微调时，相比 GR00T N1.5 和 Pi0 取得更高成功率。

## 它改变了什么

机器人操作领域长期受困于一个结构性矛盾：VLA 模型的性能天花板由数据规模决定，而机器人操作数据的获取成本远高于文本和图像数据。遥操作采集轨迹不仅劳动密集，且难以覆盖真实世界的长尾分布。现有最大规模的开源机器人数据集，如 O’Neill et al. 2024、Khazatsky et al. 2024 等，其体量仍比 LLM 和 VLM 的训练语料小数个数量级。这意味着单纯堆砌机器人数据无法突破泛化瓶颈，必须寻找替代数据源。

HumanX 真正改变的是数据策略的底层逻辑：不再将人类视频视为辅助正则化信号，而是将其作为预训练的主战场。通过视频生成任务隐式提取第一人称演示中的操作先验，再以轨迹预测为桥梁，最后用少量机器人数据完成领域对齐。这一思路将数据瓶颈从「机器人轨迹采集」转移为「人类视频筛选」，后者在互联网上近乎无限。如果成立，它实际上重新定义了 VLA 训练的数据经济性——用 12M 人类视频换取对 244K 机器人数据更充分的利用。

## 方法拆解

### 三阶段渐进式训练流程

**阶段一：Ego-Centric Video Generative Pretraining**
- 训练 Image-to-Video (I2V) 模型，输入单张图像和语言指令，自回归预测后续帧
- 架构基于 AR 图像生成模型 Chameleon 扩展，采用自回归 Transformer
- 训练数据：12M 个第一人称人类操作视频（网络来源筛选）+ 244K 个机器人操作视频（开源数据集筛选）
- 输入序列结构为 `[language tokens, visual tokens_t, language tokens, visual tokens_{t+1}, ...]`，语言与视觉 token 交错排列以对齐 VLA 推理过程
- 损失函数：离散视觉 token 和语言 token 上的交叉熵损失

**阶段二：Human-Centric Trajectory-Aware Video Modeling**
- 在带手部关键点标注的第一人称视频上继续训练 I2V 模型
- 除预测未来帧外，同时预测未来帧中的关键点轨迹
- 关键设计理由：手部关键点轨迹与机器人动作空间具有结构相似性，可作为视觉动态到低层动作的中间表征

**阶段三：Robot-Centric Vision-Language-Action Modeling**
- 继承前两阶段权重，在自收集的机器人数据集上微调
- 输入：语言指令 + 当前观测（双视角 + 关节状态）
- 优化目标：预测未来帧和动作嵌入

### ActionVAE
- 变分自编码器，将动作块（action chunks）编码为紧凑嵌入
- 训练后固定，推理时模型输出单个动作嵌入，由 ActionVAE 解码为可执行动作序列
- 作用：将高维连续动作空间压缩为离散 token 空间，与视频生成的 token 预测框架统一

### 关键设计决策
多阶段课程学习的逻辑在于：纯视觉预测与动作生成之间存在表征鸿沟，直接跳转会浪费预训练信号。轨迹预测阶段作为「语义桥梁」，让模型先学会理解手部运动学，再映射到机器人执行空间。这一设计使三个阶段的知识传递损失最小化。

## 关键创新

**1. 以视频生成为预训练主任务而非辅助目标**
现有 VLA 工作多将人类视频作为辅助损失或数据增强，HumanX 将其作为第一阶段的主优化目标。视频生成迫使模型学习完整的物理动态和操作因果链，而非仅仅提取静态特征。这是从「看视频学表征」到「生成视频学动态」的范式转变。

**2. 轨迹感知中间阶段弥合视觉-动作鸿沟**
手部关键点轨迹预测作为显式中间监督，比纯视觉预测更接近动作空间，又比直接预测机器人动作更易获得大规模标注。这一设计选择在课程学习中扮演「锚点」角色，使阶段一到阶段三的迁移不是黑箱跳跃，而是有明确语义对应的渐进过程。

**3. 交错 token 序列统一 VLA 推理格式**
将语言、视觉、动作统一为离散 token 序列，使模型可以用单一自回归框架处理三种模态。这一设计不仅简化了架构，还使预训练的视频生成任务与下游的动作预测任务共享完全相同的优化目标形式，最大化知识迁移效率。

## 实验与结果

论文片段未包含具体实验任务、基线对比的定量数字或成功率数据。提及的对比设置如下：

| 对比项 | 设置 | 结果 |
|--------|------|------|
| 基线模型 | GR00T N1.5 (Bjorck et al. 2025a)、Pi0 (Black et al. 2024) | 在同一机器人操作数据上微调 |
| 训练数据 | 12M 第一人称人类视频 + 244K 机器人视频 | 论文未明确 |
| 评估指标 | 成功率 | RynnVLA-001 一致更高，具体数值论文未明确 |

关键含义：在相同机器人数据条件下，RynnVLA-001 优于两个强基线，说明性能增益来自预训练策略而非数据量优势。但缺乏消融实验数据（如去掉阶段二的效果、不同人类视频规模的影响），无法判断各阶段的独立贡献。论文未明确。

## 边界与局限

论文片段未明确列出作者承认的局限性。从方法本身可推断的边界包括：第一，阶段二依赖手部关键点标注，其标注质量与覆盖范围直接影响轨迹预测的泛化，但标注规模与筛选标准论文未明确；第二，12M 人类视频的筛选流程（如视角一致性、任务多样性、质量过滤）在片段中标注为「将在 Sec. 4 提供」，实际细节缺失；第三，ActionVAE 的动作块编码能力受限于训练数据的动作分布，对未见过的操作模式可能解码失真；第四，实验对比仅提及两个基线，未涉及与纯机器人数据训练（无人类视频预训练）的消融，无法量化人类视频预训练的净增益。论文未明确。

## 工程启示

复现或借鉴此方法时，优先核对三件事：第一，12M 人类视频的筛选标准——来源分布、视角一致性、任务类型覆盖，这直接决定预训练质量，但论文未给出细节，需自行设计或联系作者；第二，阶段二手部关键点标注的获取方式——是自动检测还是人工标注，标注噪声对轨迹预测的影响有多大，这决定了中间阶段的实际收益；第三，ActionVAE 的压缩比与动作块长度——压缩过紧会丢失精细操作，过松则无法与 token 预测框架有效对齐。

最容易踩坑的地方在于阶段一到阶段二的衔接：如果手部关键点轨迹与机器人动作空间的映射关系设计不当，中间阶段可能不仅无益，反而引入偏置。建议在阶段二训练后先做小规模机器人数据验证，确认轨迹预测确实提升了动作预测质量，再投入全量微调。另外，交错 token 序列的长度管理需要谨慎——语言、视觉、动作 token 的比例失衡可能导致训练不稳定，建议在阶段一就固定序列格式并在全流程保持一致。

## 参考

- HumanX project page (https://wyhuai.github.io/human-x/)

## Overview

HumanX proposes a novel framework for learning agile interaction skills for humanoid robots from human videos, aiming to address the bottleneck in current humanoid robot skill acquisition, which relies heavily on extensive manual programming and expensive motion capture data, hindering generalization to new scenarios. The core contribution of this work lies in constructing a complete data and policy pipeline from human demonstrations to executable robot actions: by recovering scene, object, and motion representations from videos or motion capture trajectories, and then combining imitation learning, inverse kinematics retargeting, and hierarchical skill policies, the robot can flexibly compose and execute complex interaction tasks. HumanX provides systematic methodological support for enhancing the autonomy and adaptability of humanoid robots in unstructured environments.

## Content

The core challenge in current humanoid robot research is how to efficiently acquire and generalize interaction skills. Traditional methods either rely on manually designed motion primitives or require large amounts of precise motion capture data, leading to high skill library construction costs and poor transferability. Humans, on the other hand, can quickly learn new actions by observing videos, inspiring researchers to explore extracting robot-usable motion representations directly from human videos. However, differences in kinematic structure and dynamic characteristics between humans and robots, along with issues of intent and scene understanding implicit in videos, make this transformation process difficult. HumanX is specifically designed to bridge this "human-to-robot" skill transfer gap.

The HumanX framework comprises three key stages. First, the system recovers scene geometry, interaction targets, and human motion representations from human videos or motion capture trajectories, using computer vision and motion capture techniques to transform raw observations into structured information. Second, based on the recovered representations, it employs ACT (Action Chunking with Transformers) and behavioral cloning imitation learning to generate initial action commands, then uses inverse kinematics (IK) and action retargeting techniques to map human motions to the kinematic constraints of the humanoid robot, ensuring the generated actions are physically feasible. Finally, the system introduces hierarchical skills and expert policy mechanisms, decomposing complex tasks into a series of routable sub-skills (e.g., grasping, pushing/pulling, gait adjustment), with a high-level module dynamically selecting and combining these skills based on the current scene state to achieve flexible task execution.

The key technological innovations of HumanX are reflected in three aspects. First, it proposes an end-to-end trainable pipeline from video to robot actions, avoiding the tedious manual feature design of traditional methods. Second, by decomposing tasks into composable expert policies, the system gains generalization ability for new scenarios—even without seeing a complete task, it can adapt by recombining existing skill modules. Third, in the action retargeting stage, HumanX integrates kinematic constraints and physical plausibility checks, significantly reducing common issues like joint limit violations and self-collisions when directly transferring human motions. These designs enable HumanX to maintain motion agility while greatly enhancing skill transferability.

In experimental validation, HumanX was evaluated on various humanoid robot interaction tasks, including object grasping, tool use, and human-robot collaboration. Results show that compared to pure motion capture-driven baseline methods, HumanX achieves significant improvements in task success rates, especially in scenarios requiring rapid adaptation to new objects or layouts. Additionally, the system demonstrates compatibility with different humanoid robot platforms, verifying the generality of its skill representation and retargeting modules. From an application perspective, HumanX provides a low-cost, high-efficiency skill acquisition pathway for scenarios such as service robots, home assistants, and industrial collaboration—requiring only a small number of human videos to teach robots new interactive behaviors, greatly lowering the deployment barrier.

## 개요

HumanX는 인간 영상으로부터 휴머노이드 로봇의 민첩한 상호작용 기술을 학습하는 새로운 프레임워크를 제안합니다. 이는 현재 휴머노이드 로봇 기술 습득이 대규모 수동 프로그래밍과 고가의 모션 캡처 데이터에 의존하여 새로운 시나리오로의 일반화가 어려운 한계를 해결하는 데 목표를 둡니다. 이 연구의 핵심 기여는 인간 시연에서 로봇 실행 가능 동작까지의 완전한 데이터 및 정책 경로를 구축한 데 있습니다. 비디오나 모션 캡처 궤적으로부터 장면, 목표 및 운동 표현을 복원한 후, 모방 학습, 역운동학 재지정 및 계층적 기술 정책을 결합하여 로봇이 복잡한 상호작용 작업을 유연하게 조합하고 실행할 수 있도록 합니다. HumanX는 비구조화된 환경에서 휴머노이드 로봇의 자율성과 적응성을 향상시키기 위한 체계적인 방법론적 기반을 제공합니다.

## 핵심 내용

현재 휴머노이드 로봇 연구가 직면한 핵심 과제는 상호작용 기술을 효율적으로 습득하고 일반화하는 방법입니다. 전통적인 방법은 수동으로 설계된 운동 원시 요소에 의존하거나 대량의 정밀한 모션 캡처 데이터가 필요하여 기술 라이브러리 구축 비용이 높고 이전성이 낮습니다. 반면 인간은 비디오를 관찰하여 새로운 동작을 빠르게 학습할 수 있으며, 이는 연구자들이 인간 비디오에서 직접 로봇이 사용 가능한 운동 표현을 추출하는 방법을 탐구하도록 영감을 주었습니다. 그러나 인간과 로봇의 운동학적 구조, 동역학적 특성의 차이, 그리고 비디오에 내재된 의도와 장면 이해 문제로 인해 이러한 변환 과정은 많은 어려움을 수반합니다. HumanX는 바로 이러한 '인간에서 로봇으로'의 기술 이전 격차를 해결하기 위해 설계되었습니다.

HumanX의 프레임워크는 세 가지 주요 단계로 구성됩니다. 첫째, 시스템은 인간 비디오나 모션 캡처 궤적으로부터 장면 기하학, 상호작용 목표 및 인간 운동 표현을 복원하며, 이 과정은 컴퓨터 비전과 모션 캡처 기술을 활용하여 원시 관찰을 구조화된 정보로 변환합니다. 둘째, 복원된 표현을 기반으로 ACT(Action Chunking with Transformers)와 행동 복제 모방 학습을 사용하여 초기 동작 명령을 생성한 후, 역운동학(IK)과 동작 재지정 기술을 통해 인간의 운동을 휴머노이드 로봇의 운동학적 제약 조건에 매핑하여 생성된 동작이 물리적으로 실행 가능하도록 보장합니다. 마지막으로, 시스템은 계층적 기술과 전문가 정책 메커니즘을 도입하여 복잡한 작업을 일련의 라우팅 가능한 하위 기술(예: 잡기, 밀기/당기기, 보행 조정)로 분해하고, 상위 모듈이 현재 장면 상태에 따라 이러한 기술을 동적으로 선택하고 조합하여 유연한 작업 실행을 구현합니다.

HumanX의 핵심 기술 혁신은 세 가지 측면에서 나타납니다. 첫째, 비디오에서 로봇 동작까지의 종단간 훈련 가능한 파이프라인을 제안하여 전통적인 방법에서의 번거로운 수동 특징 설계를 피합니다. 둘째, 작업을 조합 가능한 전문가 정책으로 분해함으로써 시스템은 새로운 장면에 대응하는 일반화 능력을 갖추게 됩니다. 즉, 완전한 작업을 본 적이 없더라도 기존 기술 모듈의 재편성을 통해 적응할 수 있습니다. 셋째, 동작 재지정 단계에서 HumanX는 운동학적 제약 조건과 물리적 타당성 검사를 통합하여 인간 동작을 직접 이전할 때 흔히 발생하는 관절 한계 초과 및 자체 충돌 문제를 크게 줄입니다. 이러한 설계는 HumanX가 동작의 민첩성을 유지하면서 기술의 이전 가능성을 크게 향상시킵니다.

실험 검증 측면에서 HumanX는 물체 잡기, 도구 사용, 인간과의 협력 등 다양한 휴머노이드 로봇 상호작용 작업에서 평가되었습니다. 결과는 순수 모션 캡처 기반의 기준 방법과 비교하여 HumanX가 작업 성공률에서 특히 새로운 물체나 새로운 배치에 빠르게 적응해야 하는 시나리오에서 현저한 향상을 보였습니다. 또한 시스템은 다양한 휴머노이드 로봇 플랫폼과의 호환성을 입증하여 기술 표현 및 재지정 모듈의 범용성을 검증했습니다. 응용 가치 측면에서 HumanX는 서비스 로봇, 가정용 어시스턴트 및 산업 협력 등 시나리오에 저비용, 고효율의 기술 습득 경로를 제공합니다. 소량의 인간 비디오만으로 로봇이 새로운 상호작용 행동을 학습할 수 있어 배포 장벽을 크게 낮춥니다.
