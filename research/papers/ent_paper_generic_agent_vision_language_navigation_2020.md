---
$id: ent_paper_generic_agent_vision_language_navigation_2020
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: Towards Learning a Generic Agent for Vision-and-Language Navigation via Pre-training
  zh: Towards Learning a Generic Agent for Vision-and-Language Navigation via Pre-training
  ko: Towards Learning a Generic Agent for Vision-and-Language Navigation via Pre-training
summary:
  en: 'Learning to navigate in a visual environment following natural-language instructions is a challenging task, because
    the multimodal inputs to the agent are highly variable, and the training data on a new task is often limited. Institutions
    per source list: 杜克大学、微软研究院.'
  zh: 本文提出了首个面向视觉-语言导航（VLN）任务的预训练与微调范式。通过在大规模图像-文本-动作三元组上进行自监督学习，模型（Prevalent）习得了视觉环境与语言指令的通用表征，可直接嵌入现有VLN框架。在Room-to-Room基准上，该模型将路径加权成功率从47%提升至51%，并在视觉-对话导航和"Help,
    Anna!"等任务上刷新了最优结果。
  ko: 'Learning to navigate in a visual environment following natural-language instructions is a challenging task, because
    the multimodal inputs to the agent are highly variable, and the training data on a new task is often limited. Institutions
    per source list: 杜克大学、微软研究院.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- generic
- agent
- vision
- language
- navigation
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 819 (.staging/ingest_yuanxq). Tier A->full. Title guard: substring (score
    1.0). Abstract and metadata from arXiv API (2002.10638v2); zh content by DeepSeek from the abstract. Institutions as given
    in the source list, not verified. [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1,
    DeepSeek deepseek-chat T<=0.3, arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: arXiv:2002.10638 Towards Learning a Generic Agent for Vision-and-Language Navigation via Pre-training
  url: https://arxiv.org/abs/2002.10638
  accessed_at: '2026-07-31'
  date: '2020-02-25'
- id: src_002
  type: website
  title: Project page
  url: https://github.com/weituo12321/PREVALENT
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

本文提出 Prevalent，首个面向视觉语言导航（VLN）的预训练-微调范式。作者利用 Matterport3D 模拟器构建 658 万图像-文本-动作三元组（其中 98.4% 由 Speaker 模型合成），通过图像注意的掩码语言建模（MLM）与动作预测（AP）两个自监督目标训练跨模态编码器，并在 R2R、CVDN、HANNA 三个下游任务上验证其作为即插即用组件的有效性，显著超越现有基线。

## 它改变了什么

VLN 领域长期受困于数据稀缺——最大数据集 R2R 仅 104K 样本，比典型视觉语言预训练语料小一个数量级。现有方法要么从零训练序列到序列模型，要么直接套用 BERT/ViLBERT 等通用 VLP 模型，但后者依赖 Faster R-CNN 区域特征，与 VLN 的全景视图输入（每视图 36 张图像）在计算上不兼容。Prevalent 真正改变的是：将预训练从"语言中心"转向"导航中心"，首次在图像-文本-动作三元组上自监督学习，使编码器同时编码视觉环境、语言指令与动作轨迹的联合分布。这意味着下游模型不再需要从零学习视觉-语言对齐，而是直接继承一个已经理解"看到什么、说什么、往哪走"三者关系的通用表示，从而显著缩小 seen 与 unseen 环境的性能差距（如 HANNA 上 SPL 差距从 38.42 降至 30.66）。

## 方法拆解

### 输入表示
- **视觉嵌入**：每个全景视图 36 张图像（12 角度 × 3 俯仰），每张图像为 2176 维向量 = 2048 维 ResNet 特征 + 128 维方向特征（[sinψ; cosψ; sinω; cosω] 重复 32 次），经 LayerNorm(W_e·s + b_e) 映射至 d_h=768。
- **文本嵌入**：标准 Transformer token 嵌入 + 位置嵌入，LayerNorm 后输入。

### 编码器架构（三层 Transformer 堆叠）
1. **单模态文本编码器**（L_text=9）：标准自注意力，掩码矩阵 M 全零。
2. **单模态视觉编码器**（L_vision=1）：标准自注意力，处理 36 张全景图像。
3. **跨模态编码器**（L_cross=3）：查询来自另一模态前层输出，键值来自当前模态 H_{ℓ-1}，实现跨模态全连接；后接自注意力与 FFN。

注意力计算：A_ℓ = Softmax(QK^T/√d_k + M)V，M 中 0 允许注意，−∞ 禁止。

### 预训练目标
- **图像注意的 MLM**：15% 概率掩码词，基于周围词与全部图像预测，损失 L_MLM = −E log p(x_i | x_{\i}, s)。
- **动作预测（AP）**：在 [CLS] token 输出上接 FC 层预测动作，损失 L_AP = −E log p(a | x_[CLS], s)。
- 总损失 L = L_MLM + L_AP。作者尝试掩码图像建模但效果不佳，故排除。

### 预训练数据
- R2R 训练集：104K 三元组。
- Speaker 模型（seq2seq 自回归）合成：6,482K 三元组（占 98.4%）。
- 总计 6,582K 三元组，在 Matterport3D Simulator 上收集最短路径轨迹。

### 微调设计
- **R2R**：预训练词嵌入输入 LSTM 编码器-解码器；两阶段——先基于特征训练，再微调交叉注意力层（20k 迭代，lr=2×10⁻⁶）。
- **CVDN**：序列长度限制 300，超出部分截断。
- **HANNA**：触发帮助时用预训练模型编码指令。

## 关键创新

1. **首个 VLN 专用预训练范式**：不同于 BERT/ViLBERT 等通用 VLP，Prevalent 在图像-文本-动作三元组上预训练，直接对齐"视觉-语言-动作"三者关系，而非仅视觉-语言。这使得预训练表示天然适配导航决策，而非需要额外适配层。
2. **图像注意的 MLM**：与 BERT 的纯文本 MLM 不同，掩码词预测时跨模态编码器可关注全部 36 张全景图像，使语言理解与空间视觉上下文绑定。消融实验证明该设计在 CVDN 上优于 BERT 预训练（Mixed 指标 3.15 vs 2.64）和 BERT 微调（3.15 vs 2.28）。
3. **合成数据规模化**：利用 Speaker 模型将预训练数据扩大 63 倍（104K→6,582K），解决 VLN 数据稀缺瓶颈。合成数据占 98.4%，且实验证明其有效性——预训练词嵌入在 R2R 上适应速度显著快于随机初始化（Figure 4）。

## 实验与结果

### R2R 结果（Test Unseen）
| 方法 | TL | NE | SR | SPL |
|------|-----|-----|-----|------|
| EnvDrop | - | - | 51 | 47 |
| Press | - | - | 49 | 45 |
| Prevalent（单指令） | 10.51 | 5.30 | 54 | 51 |
| Prevalent（多指令） | 10.21 | 4.52 | 59 | 56 |
| Human | 11.85 | 1.61 | 86 | 76 |

SPL 从最先进 47% 提升至 51%（单指令），多指令下 SPL=56 优于 Press 的 53。

### CVDN 结果（Test Unseen）
| 方法 | Oracle | Navigator | Mixed |
|------|--------|-----------|-------|
| Seq2Seq | 1.25 | 2.11 | 2.35 |
| Prevalent | 1.67 | 2.39 | 2.44 |

Prevalent 在所有设置下显著优于 Seq2Seq。

### HANNA 结果
| 方法 | SEEN-ENV SR/SPL/NE/#R | UNSEEN-ALL SR/SPL/NE/#R |
|------|------------------------|--------------------------|
| Anna | 88.37/63.92/1.33/2.9 | 47.45/25.50/7.67/5.8 |
| Prevalent | 83.82/59.38/1.47/3.4 | 52.91/28.72/5.29/6.6 |

Prevalent 在 unseen 上全面优于 Anna，seen-unseen 差距更小（SPL 差距 30.66 vs 38.42）。

### 消融实验
- **CVDN 预训练目标**（Mixed 指标）：完整目标 3.15 > 仅 MLM 3.14 > BERT 预训练 2.64 > BERT 微调 2.28。
- **R2R 特征提取 vs 微调**：微调 SR=54/SPL=51 优于特征提取 SR=52/SPL=49。
- **HANNA 目标消融**：完整目标 UNSEEN-ALL SR=52.91 显著优于仅 MLM 44.29、BERT 特征 24.12、BERT 微调 26.36。

## 边界与局限

- 掩码图像建模被尝试但效果不佳，已排除，说明视觉重建目标对导航任务可能非必要。
- 未在真实物理机器人上验证，所有实验基于 Matterport3D 模拟器。
- 未对合成数据质量进行系统性评估，也未分析合成指令与人工指令的分布差异。
- 未讨论 CVDN 序列长度限制 300 可能造成的信息丢失。
- 未分析 HANNA 中 #R 略高（6.6 vs 5.8）的原因，可能影响帮助请求效率。
- 未报告预训练总计算量（GPU 小时数），仅提及 8×V100、batch 96、20 轮。

## 工程启示

复现时优先核对三点：**预训练数据构建**——Speaker 模型生成质量直接决定预训练效果，建议先在小规模合成数据上验证指令与轨迹对齐；**跨模态编码器层数配置**（L_text=9, L_vision=1, L_cross=3）是经验值，若下游任务视觉信息更复杂（如连续导航），需重新调优；**微调两阶段策略**——R2R 上先特征提取后微调交叉注意力层（lr=2×10⁻⁶）是性能关键，跳过特征提取阶段直接微调可能导致过拟合。最易踩坑的是合成数据规模：6,482K 三元组占 98.4%，若计算资源有限缩减合成数据，需注意预训练收益可能非线性下降。对于 CVDN，序列长度 300 的截断策略会丢失长对话历史，下游任务若涉及多轮交互，建议改用滑动窗口或层次编码。

## 参考
- https://arxiv.org/abs/2002.10638
- https://github.com/weituo12321/PREVALENT
- https://github.com/ImChong/Robotics_Notebooks

## Overview

This paper introduces Prevalent, the first pretraining-finetuning paradigm for Vision-and-Language Navigation (VLN). The authors leverage the Matterport3D simulator to construct 6.58 million image-text-action triplets (98.4% synthesized by a Speaker model), train a cross-modal encoder using two self-supervised objectives—image-attended masked language modeling (MLM) and action prediction (AP)—and validate its effectiveness as a plug-and-play component on three downstream tasks: R2R, CVDN, and HANNA, significantly surpassing existing baselines.

## What It Changes

The VLN field has long been constrained by data scarcity—the largest dataset, R2R, contains only 104K samples, an order of magnitude smaller than typical vision-language pretraining corpora. Existing methods either train sequence-to-sequence models from scratch or directly apply general VLP models like BERT/ViLBERT, but the latter rely on Faster R-CNN region features, which are computationally incompatible with VLN's panoramic view inputs (36 images per view). What Prevalent truly changes is shifting pretraining from "language-centric" to "navigation-centric," performing self-supervised learning on image-text-action triplets for the first time, enabling the encoder to jointly encode the visual environment, language instructions, and action trajectories. This means downstream models no longer need to learn vision-language alignment from scratch but instead inherit a general representation that already understands the relationship among "what is seen, what is said, and where to go," significantly narrowing the performance gap between seen and unseen environments (e.g., the SPL gap on HANNA drops from 38.42 to 30.66).

## Method Breakdown

### Input Representation
- **Visual Embeddings**: Each panoramic view consists of 36 images (12 angles × 3 pitches), each represented as a 2176-dimensional vector = 2048-dim ResNet features + 128-dim directional features ([sinψ; cosψ; sinω; cosω] repeated 32 times), mapped to d_h=768 via LayerNorm(W_e·s + b_e).
- **Text Embeddings**: Standard Transformer token embeddings + positional embeddings, passed through LayerNorm.

### Encoder Architecture (Three-Layer Transformer Stack)
1. **Single-Modal Text Encoder** (L_text=9): Standard self-attention with a zero mask matrix M.
2. **Single-Modal Vision Encoder** (L_vision=1): Standard self-attention processing 36 panoramic images.
3. **Cross-Modal Encoder** (L_cross=3): Queries come from the previous layer output of the other modality, keys/values from the current modality H_{ℓ-1}, enabling full cross-modal connectivity; followed by self-attention and FFN.

Attention computation: A_ℓ = Softmax(QK^T/√d_k + M)V, where 0 in M allows attention and −∞ prohibits it.

### Pretraining Objectives
- **Image-Attended MLM**: Masks words with 15% probability, predicts based on surrounding words and all images, loss L_MLM = −E log p(x_i | x_{\i}, s).
- **Action Prediction (AP)**: An FC layer on the [CLS] token output predicts actions, loss L_AP = −E log p(a | x_[CLS], s).
- Total loss L = L_MLM + L_AP. The authors attempted masked image modeling but excluded it due to poor performance.

### Pretraining Data
- R2R training set: 104K triplets.
- Speaker model (seq2seq autoregressive) synthesized: 6,482K triplets (98.4%).
- Total: 6,582K triplets, collected on Matterport3D Simulator using shortest-path trajectories.

### Finetuning Design
- **R2R**: Pretrained word embeddings feed an LSTM encoder-decoder; two-stage—first train on features, then finetune cross-attention layers (20k iterations, lr=2×10⁻⁶).
- **CVDN**: Sequence length capped at 300, truncating beyond that.
- **HANNA**: The pretrained model encodes instructions when help is triggered.

## Key Innovations

1. **First VLN-Specific Pretraining Paradigm**: Unlike general VLPs such as BERT/ViLBERT, Prevalent pretrains on image-text-action triplets, directly aligning the "vision-language-action" relationship rather than vision-language alone. This makes the pretrained representation naturally suited for navigation decisions without requiring additional adaptation layers.
2. **Image-Attended MLM**: Unlike BERT's text-only MLM, the cross-modal encoder can attend to all 36 panoramic images during masked word prediction, binding language understanding to spatial visual context. Ablation studies show this design outperforms BERT pretraining (Mixed metric 3.15 vs 2.64) and BERT finetuning (3.15 vs 2.28) on CVDN.
3. **Synthetic Data Scaling**: The Speaker model expands pretraining data 63-fold (104K→6,582K), addressing the VLN data scarcity bottleneck. Synthetic data accounts for 98.4%, and experiments confirm its effectiveness—pretrained word embeddings adapt significantly faster on R2R than random initialization (Figure 4).

## Experiments and Results

### R2R Results (Test Unseen)
| Method | TL | NE | SR | SPL |
|--------|-----|-----|-----|------|
| EnvDrop | - | - | 51 | 47 |
| Press | - | - | 49 | 45 |
| Prevalent (single instruction) | 10.51 | 5.30 | 54 | 51 |
| Prevalent (multiple instructions) | 10.21 | 4.52 | 59 | 56 |
| Human | 11.85 | 1.61 | 86 | 76 |

SPL improves from state-of-the-art 47% to 51% (single instruction), with SPL=56 under multiple instructions outperforming Press's 53.

### CVDN Results (Test Unseen)
| Method | Oracle | Navigator | Mixed |
|--------|--------|-----------|-------|
| Seq2Seq | 1.25 | 2.11 | 2.35 |
| Prevalent | 1.67 | 2.39 | 2.44 |

Prevalent significantly outperforms Seq2Seq across all settings.

### HANNA Results
| Method | SEEN-ENV SR/SPL/NE/#R | UNSEEN-ALL SR/SPL/NE/#R |
|--------|------------------------|--------------------------|
| Anna | 88.37/63.92/1.33/2.9 | 47.45/25.50/7.67/5.8 |
| Prevalent | 83.82/59.38/1.47/3.4 | 52.91/28.72/5.29/6.6 |

Prevalent outperforms Anna comprehensively on unseen environments with a smaller seen-unseen gap (SPL gap 30.66 vs 38.42).

### Ablation Studies
- **CVDN pretraining objectives** (Mixed metric): Full objective 3.15 > MLM-only 3.14 > BERT pretraining 2.64 > BERT finetuning 2.28.
- **R2R feature extraction vs finetuning**: Finetuning SR=54/SPL=51 outperforms feature extraction SR=52/SPL=49.
- **HANNA objective ablation**: Full objective UNSEEN-ALL SR=52.91 significantly outperforms MLM-only 44.29, BERT features 24.12, and BERT finetuning 26.36.

## Boundaries and Limitations

- Masked image modeling was attempted but excluded due to poor performance, suggesting visual reconstruction objectives may be unnecessary for navigation tasks.
- No validation on real physical robots; all experiments are based on the Matterport3D simulator.
- No systematic evaluation of synthetic data quality, nor analysis of distribution differences between synthetic and human instructions.
- No discussion of potential information loss from the CVDN sequence length cap of 300.
- No analysis of why #R is slightly higher on HANNA (6.6 vs 5.8), which may affect help-request efficiency.
- No report of total pretraining compute (GPU hours), only mentioning 8×V100, batch size 96, and 20 epochs.

## Engineering Insights

When reproducing, prioritize verifying three points: **pretraining data construction**—Speaker model generation quality directly determines pretraining effectiveness, so validate instruction-trajectory alignment on small-scale synthetic data first; **cross-modal encoder layer configuration** (L_text=9, L_vision=1, L_cross=3) is empirical—if downstream tasks involve more complex visual information (e.g., continuous navigation), re-tuning is needed; **two-stage finetuning strategy**—on R2R, feature extraction followed by cross-attention layer finetuning (lr=2×10⁻⁶) is critical for performance; skipping feature extraction and finetuning directly may cause overfitting. The most common pitfall is synthetic data scale: 6,482K triplets account for 98.4%, and if computational resources limit synthetic data reduction, note that pretraining gains may decline nonlinearly. For CVDN, the sequence length truncation at 300 loses long dialogue history; for downstream tasks involving multi-turn interaction, consider sliding windows or hierarchical encoding instead.

## 개요

본 논문은 Prevalent를 제안한다. 이는 시각-언어 내비게이션(VLN)을 위한 최초의 사전학습-미세조정 패러다임이다. 저자들은 Matterport3D 시뮬레이터를 활용하여 658만 개의 이미지-텍스트-행동 삼중항(그중 98.4%는 Speaker 모델이 합성)을 구축하고, 이미지 주의 마스크 언어 모델링(MLM)과 행동 예측(AP)이라는 두 가지 자기지도 학습 목표를 통해 교차양상 인코더를 훈련시킨다. R2R, CVDN, HANNA 세 가지 하위 과제에서 플러그 앤 플레이 구성 요소로서의 효용성을 검증하며, 기존 기준선을 크게 능가한다.

## 그것이 바꾼 것

VLN 분야는 오랫동안 데이터 희소성에 시달려 왔다. 가장 큰 데이터셋인 R2R은 단 104K 샘플로, 전형적인 시각-언어 사전학습 말뭉치보다 한 자릿수 작다. 기존 방법들은 시퀀스-투-시퀀스 모델을 처음부터 훈련하거나 BERT/ViLBERT 같은 범용 VLP 모델을 직접 적용했지만, 후자는 Faster R-CNN 영역 특징에 의존하여 VLN의 파노라마 뷰 입력(뷰당 36장의 이미지)과 계산적으로 호환되지 않는다. Prevalent가 진정으로 바꾼 것은 사전학습을 "언어 중심"에서 "내비게이션 중심"으로 전환한 것이다. 이미지-텍스트-행동 삼중항에 대해 최초로 자기지도 학습을 수행하여 인코더가 시각 환경, 언어 명령, 행동 궤적의 결합 분포를 동시에 인코딩하게 한다. 이는 하위 모델이 더 이상 시각-언어 정렬을 처음부터 학습할 필요 없이, "무엇을 보고, 무엇을 말하고, 어디로 가는지"의 관계를 이미 이해하는 범용 표현을 직접 상속받아 seen과 unseen 환경 간 성능 격차를 크게 줄인다는 것을 의미한다(예: HANNA에서 SPL 격차가 38.42에서 30.66으로 감소).

## 방법 분해

### 입력 표현
- **시각 임베딩**: 각 파노라마 뷰는 36장의 이미지(12 각도 × 3 피치)로 구성되며, 각 이미지는 2176차원 벡터 = 2048차원 ResNet 특징 + 128차원 방향 특징([sinψ; cosψ; sinω; cosω]를 32회 반복)이며, LayerNorm(W_e·s + b_e)을 통해 d_h=768로 매핑된다.
- **텍스트 임베딩**: 표준 Transformer 토큰 임베딩 + 위치 임베딩, LayerNorm 후 입력.

### 인코더 아키텍처(3계층 Transformer 적층)
1. **단일양상 텍스트 인코더**(L_text=9): 표준 자기주의, 마스크 행렬 M은 전부 0.
2. **단일양상 시각 인코더**(L_vision=1): 표준 자기주의, 36장의 파노라마 이미지 처리.
3. **교차양상 인코더**(L_cross=3): 쿼리는 다른 양상의 이전 계층 출력에서, 키-값은 현재 양상 H_{ℓ-1}에서 가져와 교차양상 완전 연결을 구현; 이후 자기주의와 FFN이 이어진다.

주의 계산: A_ℓ = Softmax(QK^T/√d_k + M)V, M에서 0은 주의 허용, −∞는 금지.

### 사전학습 목표
- **이미지 주의 MLM**: 15% 확률로 단어 마스킹, 주변 단어와 전체 이미지를 기반으로 예측, 손실 L_MLM = −E log p(x_i | x_{\i}, s).
- **행동 예측(AP)**: [CLS] 토큰 출력에 FC 계층을 연결하여 행동 예측, 손실 L_AP = −E log p(a | x_[CLS], s).
- 총 손실 L = L_MLM + L_AP. 저자들은 마스크 이미지 모델링을 시도했지만 효과가 좋지 않아 제외했다.

### 사전학습 데이터
- R2R 훈련 세트: 104K 삼중항.
- Speaker 모델(seq2seq 자기회귀) 합성: 6,482K 삼중항(98.4% 차지).
- 총 6,582K 삼중항, Matterport3D 시뮬레이터에서 최단 경로 궤적 수집.

### 미세조정 설계
- **R2R**: 사전학습 단어 임베딩을 LSTM 인코더-디코더에 입력; 두 단계로 진행 — 먼저 특징 기반 훈련, 그다음 교차 주의 계층 미세조정(20k 반복, lr=2×10⁻⁶).
- **CVDN**: 시퀀스 길이를 300으로 제한, 초과분은 절단.
- **HANNA**: 도움 요청 시 사전학습 모델로 명령 인코딩.

## 핵심 혁신

1. **최초의 VLN 전용 사전학습 패러다임**: BERT/ViLBERT 같은 범용 VLP와 달리, Prevalent는 이미지-텍스트-행동 삼중항에서 사전학습하여 "시각-언어-행동"의 관계를 직접 정렬하며, 단순한 시각-언어 정렬이 아니다. 이는 사전학습 표현이 추가 적응 계층 없이 내비게이션 결정에 자연스럽게 적합하게 한다.
2. **이미지 주의 MLM**: BERT의 순수 텍스트 MLM과 달리, 마스킹된 단어 예측 시 교차양상 인코더가 전체 36장의 파노라마 이미지를 주의할 수 있어 언어 이해가 공간적 시각 맥락과 결합된다. 절제 실험은 이 설계가 CVDN에서 BERT 사전학습(Mixed 지표 3.15 vs 2.64)과 BERT 미세조정(3.15 vs 2.28)보다 우수함을 증명한다.
3. **합성 데이터 규모화**: Speaker 모델을 활용하여 사전학습 데이터를 63배 확장(104K→6,582K), VLN 데이터 희소성 병목을 해결. 합성 데이터가 98.4%를 차지하며, 실험을 통해 그 효용성이 입증됨 — 사전학습 단어 임베딩은 R2R에서 무작위 초기화보다 적응 속도가 현저히 빠름(Figure 4).

## 실험 및 결과

### R2R 결과(Test Unseen)
| 방법 | TL | NE | SR | SPL |
|------|-----|-----|-----|------|
| EnvDrop | - | - | 51 | 47 |
| Press | - | - | 49 | 45 |
| Prevalent(단일 명령) | 10.51 | 5.30 | 54 | 51 |
| Prevalent(다중 명령) | 10.21 | 4.52 | 59 | 56 |
| Human | 11.85 | 1.61 | 86 | 76 |

SPL이 최신 기술 수준인 47%에서 51%(단일 명령)로 향상, 다중 명령에서 SPL=56으로 Press의 53을 능가.

### CVDN 결과(Test Unseen)
| 방법 | Oracle | Navigator | Mixed |
|------|--------|-----------|-------|
| Seq2Seq | 1.25 | 2.11 | 2.35 |
| Prevalent | 1.67 | 2.39 | 2.44 |

Prevalent는 모든 설정에서 Seq2Seq를 현저히 능가.

### HANNA 결과
| 방법 | SEEN-ENV SR/SPL/NE/#R | UNSEEN-ALL SR/SPL/NE/#R |
|------|------------------------|--------------------------|
| Anna | 88.37/63.92/1.33/2.9 | 47.45/25.50/7.67/5.8 |
| Prevalent | 83.82/59.38/1.47/3.4 | 52.91/28.72/5.29/6.6 |

Prevalent는 unseen에서 Anna를 전반적으로 능가하며, seen-unseen 격차가 더 작다(SPL 격차 30.66 vs 38.42).

### 절제 실험
- **CVDN 사전학습 목표**(Mixed 지표): 전체 목표 3.15 > MLM만 3.14 > BERT 사전학습 2.64 > BERT 미세조정 2.28.
- **R2R 특징 추출 vs 미세조정**: 미세조정 SR=54/SPL=51이 특징 추출 SR=52/SPL=49보다 우수.
- **HANNA 목표 절제**: 전체 목표 UNSEEN-ALL SR=52.91이 MLM만 44.29, BERT 특징 24.12, BERT 미세조정 26.36보다 현저히 우수.

## 경계와 한계

- 마스크 이미지 모델링은 시도되었지만 효과가 좋지 않아 제외되었으며, 이는 시각 재구성 목표가 내비게이션 작업에 필수적이지 않을 수 있음을 시사.
- 실제 물리적 로봇에서 검증되지 않았으며, 모든 실험은 Matterport3D 시뮬레이터 기반.
- 합성 데이터 품질에 대한 체계적 평가가 없으며, 합성 명령과 인간 명령의 분포 차이도 분석되지 않음.
- CVDN 시퀀스 길이 제한 300으로 인한 정보 손실 가능성에 대해 논의하지 않음.
- HANNA에서 #R이 다소 높은(6.6 vs 5.8) 원인을 분석하지 않았으며, 이는 도움 요청 효율성에 영향을 미칠 수 있음.
- 사전학습 총 계산량(GPU 시간)을 보고하지 않았으며, 8×V100, batch 96, 20 에포크만 언급.

## 공학적 시사점

재현 시 세 가지를 우선 확인해야 한다: **사전학습 데이터 구축** — Speaker 모델 생성 품질이 사전학습 효과를 직접 결정하므로, 소규모 합성 데이터에서 명령-궤적 정렬을 먼저 검증할 것; **교차양상 인코더 계층 구성**(L_text=9, L_vision=1, L_cross=3)은 경험적 값으로, 하위 작업의 시각 정보가 더 복잡하면(예: 연속 내비게이션) 재조정 필요; **미세조정 2단계 전략** — R2R에서 특징 추출 후 교차 주의 계층 미세조정(lr=2×10⁻⁶)이 성능의 핵심이며, 특징 추출 단계를 건너뛰고 직접 미세조정하면 과적합이 발생할 수 있다. 가장 함정에 빠지기 쉬운 부분은 합성 데이터 규모다: 6,482K 삼중항이 98.4%를 차지하므로, 계산 자원이 제한되어 합성 데이터를 줄이면 사전학습 이득이 비선형적으로 감소할 수 있음을 유의해야 한다. CVDN의 경우 시퀀스 길이 300 절단 전략은 긴 대화 기록을 잃을 수 있으므로, 하위 작업이 다중 턴 상호작용을 포함한다면 슬라이딩 윈도우나 계층적 인코딩을 권장한다.
