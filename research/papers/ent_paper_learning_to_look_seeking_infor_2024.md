---
$id: ent_paper_learning_to_look_seeking_infor_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization'
  zh: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization'
  ko: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization'
summary:
  en: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization is a 2024 work on manipulation for
    humanoid robots.'
  zh: '《Learning to Look: Seeking Information for Decision Making via Policy Factorization》是2024年关于人形机器人操作的研究。作者提出因子化上下文马尔可夫决策过程（factorized
    Contextual Markov Decision Processes）问题，并设计双策略框架DISaM，通过信息搜索策略与信息接收策略的分离训练，实现操作任务中的主动探索与决策。该方法在五项需要信息搜索行为的操作任务中显著优于现有方法。'
  ko: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization is a 2024 work on manipulation for
    humanoid robots.'
domains:
- 07_ai_models_algorithms
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- humanoid
- learning_to_look
- manipulation
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-14'
  confidence: medium
  notes: 'Abstract backfilled by scripts/backfill_paper_abstracts.py from http://arxiv.org/abs/2410.18964v1. [2026-07-29]
    zh content backfilled from English abstract via scripts/sinicize_english_cards.py | WP4 trilingual backfill 2026-08-10:
    ko body retranslated from zh deep-read (936 chars, DeepSeek). [2026-08-21] body rewritten as full-text six-section deep
    read (scripts/deep_read_cards.py, DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation);
    en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization (arXiv)'
  url: https://arxiv.org/abs/2410.18964
  date: '2024'
  accessed_at: '2026-07-01'
- id: src_002
  type: website
  title: 'Learning to Look: Seeking Information for Decision Making via Policy Factorization project page'
  url: https://robin-lab.cs.utexas.edu/learning2look/
  date: '2024'
  accessed_at: '2026-07-01'
---
## 概述

本文提出一种面向高速越野自动驾驶的在线自适应决策框架，核心是将车辆动力学建模为混合参数化与学习模型，并仅对学习模型最后一层FNN权重的线性组合进行在线卡尔曼滤波适应，同时通过离线元学习优化基函数与滤波器参数。该方法在真实全尺寸车辆上验证，相比基线在预测精度与安全指标上更优。

## 它改变了什么

越野自动驾驶的难点在于地形-车辆交互难以精确建模，且真实数据学习的动力学模型对未见地形泛化差。现有混合方法（第一性原理+学习）在标称条件下有效，但面对运行中动态变化的地形，其固定参数无法实时调整；而间接自适应控制依赖人工设计的基函数与调参学习率，用户负担重且易陷入次优。本文真正改变的是：将“在线适应哪些参数”与“如何适应”这两个问题解耦——不是适应全部网络参数（过慢），也不是仅适应标量增益（表达力不足），而是适应一个精心设计的低维线性子空间，该子空间由离线元学习确定。这使得在线更新既快又稳，且能捕捉复杂地形交互的主要变化模式。

## 方法拆解

### 动力学模型
- 离散非线性系统：`x_{t+1}=f(x_t,u_t,y_t)`，状态 `x_t=[p_t,v_t,z_t]∈ℝ¹⁰`（位置/偏航、速度/偏航率、刹车/转向/发动机），控制 `u_t∈ℝ³`（油门/刹车/转向），外部输入 `y_t∈ℝ¹⁴`（四轮侧倾/俯仰/法向量）。
- 加速度预测：`v̇_t=M⁻¹X(x_t)[F_t+ζ(η_t;φ,θ_t)]+D_t`，其中 `ζ` 为LSTM+FNN混合网络，`φ` 为固定学习参数，`θ_t` 为可适应参数，`η_t=[x_t,u_t,y_t,F_t]`。

### 可适应参数设计
- 仅适应FNN最后一层权重的线性组合，表示为张量 `W∈ℝ^{n_w×n_out×n_in}`。
- 学习模型输出：`ζ(η;φ,θ)=(φ^w+θ^w)ᵀWΦ(η;φ^l)+φ^b+θ^b`。
- 关键性质：动力学是适应参数 `θ` 的线性函数（Remark 2），`W` 是基函数集合，其权重由 `θ^w` 适应动态调整（Remark 3）。

### 在线自适应（Algorithm 1）
- 每 `h` 个时间步用卡尔曼滤波器更新一次参数（慢于动力学传播），以减轻噪声、捕捉长期趋势。
- 假设1：`θ_{t+h}=θ_t+w_t^θ`，`w_t^θ∼𝒩(0,Q)`；假设2：`x_{t+1}=f(x_t,u_t,y_t;θ_t)+w_t^x`，`w_t^x∼𝒩(0,R)`。
- 多步雅可比递归：`H_{t+i}=ℱ^x_{t+i}H_{t+i-1}+ℱ^θ_{t+i}`，`H_t=ℱ^θ_t`。
- 测量选择矩阵 `C∈ℝ^{3×10}` 仅选速度 `v` 更新，忽略位置 `p` 与 `z`（理由：单步内位置变化小于噪声，`z` 传播不依赖适应参数）。
- 更新缩放因子 `γ_t=‖v_t‖²₂/(‖v_t‖²₂+ε)`，防止低速时无效更新。

### 离线元学习（Algorithm 2）
- 目标：优化基函数 `W` 与滤波器参数 `P_s,Q,R,ε`，最大化在线适应有效性。
- 数据按离散runs收集，每条轨迹 `{x_{t-τ:t+T},u_{t-τ:t+T},y_{t-τ:t+T}}`，`τ` 为适应长度，`T` 为MPPI预测长度。

## 关键创新

1. **参数适应子空间的可学习性**：将在线适应限制在FNN最后一层权重的线性组合上，且该组合的基函数由离线元学习获得。这不同于固定最后一层或全参数适应，既保证了更新速度（低维），又保留了表达力（基函数针对任务优化）。
2. **多步雅可比与测量选择**：在卡尔曼滤波中显式计算多步雅可比 `H`，并仅用速度测量更新，忽略位置与执行器状态。这一设计决策基于对噪声特性与参数依赖性的分析，显著降低了计算负担且不损失估计精度。
3. **元学习与在线适应的闭环**：离线阶段直接优化在线滤波器的超参数（`Q,R,ε`）与基函数，使在线适应过程本身成为元学习的目标，而非事后调参。这解决了传统自适应方法中“适应规则与任务脱节”的根本问题。

## 实验与结果

论文提及在全尺寸自主越野车辆上进行了真实世界测试，验证方法在预测精度、性能和安全指标上优于基线，尤其在安全关键场景中。但论文片段未提供具体实验表格数字（该表为图片，数字未提取），因此无法给出定量对比。视频链接：https://youtu.be/cCKHHrDRQEA。

| 指标 | 本文方法 | 基线 | 说明 |
|------|---------|------|------|
| 预测精度 | 论文未明确 | 论文未明确 | 原文表格为图片 |
| 安全指标 | 论文未明确 | 论文未明确 | 原文表格为图片 |
| 性能指标 | 论文未明确 | 论文未明确 | 原文表格为图片 |

## 边界与局限

论文片段未明确列出作者承认的局限与未做之事。从方法本身推断，可能的边界包括：适应参数仅覆盖FNN最后一层线性组合，若地形交互模式超出该子空间表达范围，适应效果将受限；卡尔曼滤波假设高斯噪声与随机游走参数演化，在极端非线性或突变地形下可能失效；测量选择仅用速度，若速度测量噪声大或不可靠，估计精度会下降。此外，离线元学习依赖多样地形数据，若部署环境与训练分布差异过大，基函数可能不匹配。

## 工程启示

复现时先核对三点：一是确认动力学模型中 `ζ` 的LSTM+FNN结构及 `W` 张量的维度初始化方式，这直接影响适应子空间的表达能力；二是卡尔曼滤波的 `h` 步更新间隔与 `ε` 缩放因子的取值，`ε` 过小会导致低速时频繁无效更新，过大则削弱高速适应能力；三是离线元学习的数据采集需覆盖足够多样的地形与速度范围，否则基函数 `W` 会偏向训练分布。最容易踩坑的地方在于多步雅可比 `H` 的递归计算——若忽略 `∂ζ/∂x≈0` 假设的适用条件（即状态变化对学习模型输出的影响），会导致雅可比计算错误。建议先在仿真中验证 `H` 的数值正确性，再上实车。

## 参考
- http://arxiv.org/abs/2410.18964v1

## 개요
본 연구는 로봇 조작에서 능동적 또는 상호작용적 탐색 행동이 필요한 작업을 위해, 분해된 맥락 마르코프 결정 과정(factorized Contextual Markov Decision Processes) 문제 모델을 제안한다. 저자들은 DISaM 이중 정책 프레임워크를 설계했으며, 이는 관련 맥락 정보를 획득하기 위해 환경을 탐색하는 정보 탐색 정책과, 해당 맥락을 활용하여 조작 목표를 완수하는 정보 수신 정책을 포함한다. 두 정책을 분리하여 훈련함으로써, 정보 수신 정책은 정보 탐색 정책에 보상 신호를 제공할 수 있다. 테스트 단계에서는 이중 에이전트가 조작 정책의 다음 최적 행동에 대한 불확실성에 기반하여 탐색과 활용의 균형을 조정한다. 실험은 정보 탐색 행동이 필요한 다섯 가지 조작 작업(시뮬레이션 및 실제 환경 포함)에서 DISaM의 우수한 성능을 검증했다.

## 핵심 내용
### 문제 정의
- 분해된 맥락 마르코프 결정 과정(factorized Contextual MDPs)을 제안하여 작업을 정보 탐색 단계와 정보 활용 단계로 분해
- 능동적 탐색이 필요한 로봇 조작 시나리오에 적용 가능. 예: 로봇 머리 움직임을 통한 조작 관련 정보 탐색, 또는 다중 로봇 시스템에서 정찰 로봇이 정보를 탐색하여 의사결정 로봇에 제공

### 방법 아키텍처: DISaM
- **이중 정책 프레임워크**:
  - 정보 탐색 정책(information-seeking policy): 환경을 탐색하여 관련 맥락 정보를 찾는 역할
  - 정보 수신 정책(information-receiving policy): 획득한 맥락 정보를 활용하여 조작 목표를 완수하는 역할
- **훈련 메커니즘**:
  - 두 정책은 분리하여 훈련 가능하며, 정보 수신 정책이 정보 탐색 정책에 보상 신호를 제공
  - 테스트 시 조작 정책의 다음 최적 행동에 대한 불확실성에 기반하여 탐색과 활용을 동적으로 균형 조정

### 실험 설정
- 정보 탐색 행동이 필요한 다섯 가지 조작 작업으로, 시뮬레이션 환경과 실제 세계 시나리오를 포함
- 기존 방법과 비교하여 DISaM은 모든 작업에서 현저히 우수한 성능을 보임

### 주요 결과
- 시뮬레이션 및 실제 환경 모두에서 DISaM은 더 강력한 정보 탐색 및 조작 의사결정 능력을 입증
- 이중 정책 분해 설계는 탐색과 활용의 균형 문제를 효과적으로 해결

### 결론
- 분해된 맥락 MDP는 능동적 탐색이 필요한 조작 작업을 위한 통합 모델링 프레임워크를 제공
- DISaM의 이중 정책 분리 훈련 방법은 확장 가능하며, 다양한 로봇 조작 시나리오에 적용 가능

더 많은 정보는 프로젝트 홈페이지에서 확인: https://robin-lab.cs.utexas.edu/learning2look/
