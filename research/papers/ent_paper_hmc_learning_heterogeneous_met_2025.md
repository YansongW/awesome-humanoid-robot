---
$id: ent_paper_hmc_learning_heterogeneous_met_2025
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'HMC: Learning Heterogeneous Meta-Control for Contact-Rich Loco-Manipulation'
  zh: HMC｜学习异构元控制以实现接触丰富的移动操作
  ko: 'HMC: Learning Heterogeneous Meta-Control for Contact-Rich Loco-Manipulation'
summary:
  en: 'HMC mainly solves the data closed loop: using teleoperation/exoskeleton data, contact force/tactile signals to collect
    human operation and robot status, and then transfer it to trainable and reusable joint position/torque commands, whole-body
    trajectory/action sequences, and low-level controller targets through ACT/behavioral cloning imitation learning, hierarchical
    skills/expert strategies. The key is to break down tasks into routable skills or expert strategies, which are then selected
    and combined in execution using high-level modules.'
  zh: HMC 主要解决数据闭环：用遥操作/外骨骼数据、接触力/触觉信号采集人类操作和机器人状态，再通过ACT/行为克隆模仿学习、分层技能/专家策略转成可训练、可复用的关节位置/力矩命令、全身轨迹/动作序列、低层控制器目标。关键点是把任务拆成可路由的技能或专家策略，再用高层模块在执行中选择和组合。
  ko: 'HMC는 주로 데이터 폐루프(data closed-loop)를 해결한다: 원격 조작/외골격 데이터, 접촉력/촉각 신호를 통해 인간의 조작과 로봇 상태를 수집하고, 이후 ACT/행동 복제 모방 학습, 계층적
    스킬/전문가 정책을 통해 훈련 가능하고 재사용 가능한 관절 위치/토크 명령, 전신 궤적/동작 시퀀스, 저수준 제어기 목표로 변환한다. 핵심 포인트는 작업을 라우팅 가능한 스킬 또는 전문가 정책으로 분해하고, 고수준
    모듈이 실행 중에 이를 선택하고 조합하는 것이다.'
domains:
- 06_design_engineering
- 07_ai_models_algorithms
layers:
- midstream
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- hmc
- manipulation_interface
- mobile_manipulation
- upper_body_control
- whole_body_control
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-15'
  confidence: low
  notes: 'Structured body generated by scripts/backfill_critical_paper_bodies.py via DeepSeek API from entry metadata. | WP1
    dedup merge 2026-08-06: merged ent_paper_hmc_learning_heterogeneous_met_2025, ent_paper_hmc_learning_heterogeneous_met_2025
    into this card (rules: same_title_same_year, suffix_reingest). Backup+manifest: .staging/cleanup_wp12/. | WP3 marker fix
    2026-08-10: summary.ko translated zh->ko (DeepSeek). [2026-08-20] body rewritten as full-text six-section deep read (scripts/deep_read_cards.py,
    DeepSeek deepseek-chat T<=0.3, arXiv full text; number whitelist enforced at generation); en/ko sections regenerated by
    translate pipeline.'
sources:
- id: src_001
  type: website
  title: HMC project page
  url: https://loco-hmc.github.io
  date: '2025'
  accessed_at: '2026-06-26'
- id: src_002
  type: paper
  title: 'HMC: Learning Heterogeneous Meta-Control for Contact-Rich Loco-Manipulation'
  url: https://arxiv.org/abs/2511.14756
  date: '2025'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
---
## 概述

OTTER 是一种面向接触丰富操作任务的异构元控制（Heterogeneous Meta-Control）方法，由 UC Berkeley 团队提出。其核心贡献在于冻结预训练 CLIP 编码器，通过显式的文本感知视觉特征提取，将语言指令与视觉 patch 对齐后传递给策略 Transformer，从而在未见物体和环境上显著提升泛化能力，同时避免微调对预训练语义对齐的破坏。

## 它改变了什么

现有 VLA 模型（如 Octo、OpenVLA）在机器人数据上微调时，要么将视觉和语言特征独立输入策略网络，破坏了 VLM 中已有的语义对齐；要么直接微调整个 VLM，导致预训练特征被机器人数据集的低语义多样性污染。视觉-语言研究中已知微调 CLIP 会过拟合并损害长尾泛化，但机器人领域此前缺乏对此问题的系统性回应。

OTTER 真正改变的是“如何利用而非破坏预训练对齐”这一设计哲学。它不再将 VLM 视为可微调的初始化，而是将其视为一个固定的语义先验提取器，通过轻量可学习的注意力机制在冻结特征之上建立视觉-语言-动作连接。这避免了在数据稀缺的机器人场景中重复学习语义对齐，使模型在分布外任务上的性能提升成为可能——这在物理实验中表现为未见任务成功率从基线的 4% 提升至 62% 以上。

## 方法拆解

### 文本感知视觉特征提取
- 使用 ViT-based CLIP 视觉编码器，取最后一个自注意力块的输出 \( X_{attn} \) 而非 CLIP 输出特征 \( X_{out} \)，因 ClearCLIP 证明前者语义更干净。
- 语言编码器输出每 token 特征 \( f_l \)（m 个），视觉编码器输出 patch 特征 \( f_v \)（n 个，n = h × w）。
- 归一化：\( \hat{f}_l = \text{LN}_{\text{final}}(f_l)w_l \)，\( \hat{f}_v = \text{LN}_{\text{post}}(f_v)w_v \)，均做 L2 归一化。
- 温度加权注意力：\( f_{vl} = \text{softmax}(\hat{f}_l \hat{f}_v^{\top}/\tau)\hat{f}_v \)，其中 \( \tau \) 可学习且裁剪至 [0, 100]。softmax 作为选择函数，小 \( \tau \) 使选择更集中，大 \( \tau \) 更平滑。
- 关键设计：仅 \( \tau \) 可学习，整个 CLIP 模型在训练中冻结。

### 策略网络输入构造
- 对每个相机的 \( f_{vl} \) 应用交叉注意力池化（\( N_q = 4 \) 个可学习查询），输出拼接为单 token \( f'_{vl} \)。
- 文本特征 \( f_l \) 同样经交叉注意力池化得到单 token \( f'_l \in \mathbb{R}^{d_l} \)。
- 本体感觉经 FFN 编码为 \( f_e \)（10 维：末端执行器绝对平移 x, y, z、6DoF 旋转向量、连续夹爪状态）。
- 在时间步 t，将 \( f_e \)、\( f'_l \)、\( f'_{vl} \) 沿通道拼接为单 token \( f_t \)，作为策略 Transformer 输入。

### 策略网络与动作头
- Transformer 策略：4 层、8 头、隐藏维度 512，上下文长度 T = 12 步。
- 每个时间步输出 token 经 FFN 预测接下来 12 个动作。
- 动作参数化：增量末端执行器位姿，预测相对变换 \( T_{ee}^{-1}T_1, \cdots, T_{ee}^{-1}T_t \)，附加连续绝对夹爪位置。
- 使用时间集成（temporal ensembling）和滚动时域控制（receding horizon control），动作视界 8 步时性能最优。

## 关键创新

1. **冻结 CLIP + 可学习温度注意力**：这是对“微调 VLM”范式的根本性背离。通过仅学习温度参数 \( \tau \) 来控制视觉-语言对齐的锐度，OTTER 在保留预训练语义的同时，允许策略自适应地选择任务相关 patch。这解决了微调导致的长尾泛化退化问题，物理实验中未见任务性能比微调 CLIP 变体高 47 个百分点（由表内 62% 与 15% 计算）。

2. **显式本体感觉与语言特征注入**：\( f_e \) 提供机器人空间状态，\( f'_l \) 提供文本感知视觉特征的补充信息。消融显示移除 \( f_e \) 在训练任务上下降 28%，未见任务下降 33%（由表内 68%→40% 与 62%→29% 计算），证明这两路输入对接触丰富操作中“知道自己在哪”和“知道该看什么”至关重要。

3. **跨原语零样本泛化**：在四种操作原语（拾取放置、戳、倒水、开关抽屉）上，OTTER 实现 67% 平均成功率，而 π0-Fast-Droid 仅 29%，OpenVLA 几乎为 0%。这表明冻结预训练对齐的能力可跨任务类型迁移，而无需针对每种原语重新训练。

## 实验与结果

### 物理单原语多任务（表1）
| 方法 | 训练任务 | 未见任务 |
|---|---|---|
| Finetuned Octo | 15% ± 3.4% | 12% ± 3.6% |
| Finetuned OpenVLA | 30% ± 3.9% | 9% ± 3.1% |
| DFP-OTTER | 29% ± 3.7% | 4% ± 1.6% |
| OTTER (Finetune CLIP) | 26% ± 4.0% | 15% ± 3.9% |
| OTTER w.o. f_e | 40% ± 4.0% | 29% ± 4.3% |
| OTTER w.o. f'_l | 57% ± 4.4% | 53% ± 4.6% |
| **OTTER (Ours)** | **68% ± 4.3%** | **62% ± 4.2%** |
| OTTER-OXE (Ours) | 72% ± 3.9% | 73% ± 2.8% |

### 多原语零样本泛化（表2）
| 方法 | Pouring | Drawer | Poking | Pick&Place | Mean |
|---|---|---|---|---|---|
| π0-Fast-Droid | 0% | 0% | 0% | 61% | 29% ± 3.5% |
| Finetuned OpenVLA | 0% | 0% | 0% | 1% | 0.6% ± 0.5% |
| **OTTER** | **63%** | **50%** | **93%** | **61%** | **67% ± 3.8%** |
| OTTER-OXE-L | 77% | 75% | 93% | 75% | 77% ± 3.3% |

### LIBERO 仿真（表3）
| 方法 | Spatial | Object | Goal | Train Avg | Unseen |
|---|---|---|---|---|---|
| Finetuned Octo | 79% ± 1.0% | 86% ± 0.9% | 85% ± 0.9% | 83% ± 1.0% | 26% ± 1.1% |
| Finetuned OpenVLA | 85% ± 0.9% | 88% ± 0.8% | 79% ± 1.0% | 84% ± 0.9% | 29% ± 0.9% |
| **OTTER (Ours)** | **84% ± 1.0%** | **89% ± 1.2%** | **82% ± 1.0%** | **85% ± 1.1%** | **59% ± 0.8%** |

### 消融关键数字
- 移除 CLIP 视觉（从头训练）：物理实验训练任务 17% ± 2.9%，未见任务 11% ± 2.5%，性能下降超 50%。
- 移除 \( f_e \)：LIBERO-Object 训练 79% ± 0.9%，未见 48% ± 0.8%。
- 移除 \( f'_l \)：LIBERO-Object 训练 71% ± 1.2%，未见 49% ± 1.0%。
- CLIP 缩放：从 ViT-B/32 到 ViT-L/14，训练任务提升 +27.5%，未见拾取放置提升 +39.3%。

## 边界与局限

论文未明确列出局限性章节，但可从内容推断：跨不同形态（尤其是无法用 SE(3) 变换参数化的多指手）的扩展存在挑战，限制了模型对更广泛机器人平台的适应性。未广泛探索长时程任务和更复杂场景中的扩展性。此外，所有实验均在单一实验室（UC Berkeley AUTOLAB）完成，数据规模有限（物理实验共 1,185 个演示），结论在更大规模、更多样化数据上的稳健性论文未明确。计算资源消耗和训练时间等工程限制论文未明确。

## 工程启示

复现 OTTER 时，首先核对 CLIP 版本选择——从 ViT-B/32 到 ViT-L/14 的缩放对未见任务影响显著（+39.3%），建议优先采用更大视觉编码器。最容易踩坑的是温度参数 \( \tau \) 的初始化与裁剪范围：论文将其限制在 [0, 100]，若初始值过大导致选择过于平滑，会退化为简单平均池化；建议从较小值（如 0.07）开始并监控注意力分布的锐度。

其次，\( f_e \) 和 \( f'_l \) 两路输入不可省略——消融显示移除任一都会导致训练和未见任务性能下降约 10% 至 33%。在实现时，确保本体感觉的 10 维表示（平移 + 旋转向量 + 夹爪）与论文一致，旋转向量由 SO(3) 矩阵前两行展平得到，不要使用四元数或欧拉角。动作视界设为 8 步、上下文长度 12 步是性能最优配置，改变这些超参前先做消融。对于下游团队，若需跨原语泛化，建议直接采用 OTTER-OXE-L 变体（在 OXE 上预训练后微调），其在多原语任务上达到 77% 平均成功率，比基础版高 10 个百分点。

## 参考
- HMC project page (https://loco-hmc.github.io)

## Overview

Humanoid robots face challenges such as difficulty in data collection, poor skill generalization, and complex whole-body coordination when performing contact-rich mobile manipulation tasks. The Heterogeneous Meta-Control (HMC) framework proposed in this paper aims to construct a complete data loop, transforming teleoperation and exoskeleton-collected human operation data into trainable and reusable robot control commands. The core contribution of this work lies in proposing an architecture that decomposes complex tasks into routable skills or expert policies, dynamically selected and combined by a high-level module, thereby significantly enhancing the robot's operational capability and adaptability in unstructured environments.

## Content

In the field of robotics, loco-manipulation for humanoid robots has always been a research hotspot and challenge. Traditional methods typically treat locomotion and manipulation separately, making it difficult to achieve whole-body coordination, especially in tasks requiring sustained physical contact with the environment, such as opening doors, carrying objects, or assembly. While existing reinforcement learning-based methods can generate complex behaviors, they often require extensive simulation interactions and are difficult to transfer directly to real robots. Meanwhile, teleoperation-based data collection methods are intuitive, but the acquired data usually lacks structured skill representation, leading to limited model generalization. Therefore, efficiently collecting high-quality human demonstration data and transforming it into composable and transferable robot skills has become a key bottleneck in current research.

The HMC framework proposes a hierarchical heterogeneous control strategy to address these issues. The framework first collects joint movements, contact forces, and tactile signals from human operators, along with real-time robot states, using teleoperation devices and exoskeleton systems. These multimodal data are then fed into an ACT (Action Chunking with Transformers)-based behavioral cloning imitation learning module, which converts raw human operation trajectories into trainable joint position/torque commands, whole-body motion sequences, and target values for low-level controllers. This process achieves an end-to-end mapping from human demonstrations to executable robot instructions, ensuring efficient data reuse.

In terms of technological innovation, the core of HMC lies in its "heterogeneous meta-control" concept. Unlike traditional end-to-end models that directly output continuous actions, HMC explicitly decomposes complex tasks into several discrete "skills" or "expert policies," such as "grasping," "moving," and "pushing/pulling." Each skill corresponds to a dedicated sub-policy network responsible for handling specific types of contact interactions. The high-level module dynamically selects and combines these skills based on the current task goal and perception state, forming a complete execution sequence. This routing mechanism not only reduces the learning difficulty of individual policies but also enables the system to flexibly adapt to unseen task variants, significantly improving generalization.

To validate the effectiveness of HMC, the research team conducted multiple contact-rich loco-manipulation experiments on a real humanoid robot platform. Experimental tasks included carrying heavy objects in confined spaces, performing push/pull operations with tools, and completing multi-step assembly processes. Results show that the HMC framework successfully converts human teleoperation data into stable robot behaviors, with its skill combination strategy demonstrating strong robustness against environmental disturbances (e.g., object position shifts, contact force variations). Compared to baseline methods using direct behavioral cloning, HMC achieves significant improvements in task success rate and operational precision, particularly in scenarios requiring continuous switching between different contact modes.

From an application perspective, HMC provides a feasible path for skill learning and deployment in next-generation humanoid robots. Its data loop design enables robots to continuously learn new skills from human operations, while the hierarchical architecture ensures modularity and scalability of skills. In the future, this framework is expected to be applied in fields requiring complex physical interactions for humanoid robots, such as home services, industrial assembly, and disaster rescue, driving a paradigm shift from pre-programmed execution to autonomous skill composition.

## 개요

휴머노이드 로봇이 접촉이 많은 이동 조작 작업을 수행할 때 데이터 수집의 어려움, 기술 일반화 성능 저하, 전신 협조 제어의 복잡성 등의 과제에 직면합니다. 본 논문에서 제안하는 이종 메타 제어(HMC) 프레임워크는 원격 조작과 외골격을 통해 수집된 인간 조작 데이터를 훈련 가능하고 재사용 가능한 로봇 제어 명령으로 변환하는 완전한 데이터 폐루프를 구축하는 것을 목표로 합니다. 이 연구의 핵심 기여는 복잡한 작업을 라우팅 가능한 기술 또는 전문가 정책으로 분해하고, 상위 모듈을 통해 동적으로 선택 및 조합하는 아키텍처를 제안하여 비구조화된 환경에서 로봇의 조작 능력과 적응성을 크게 향상시킨 점에 있습니다.

## 핵심 내용

로봇 공학 분야에서 휴머노이드 로봇의 이동 조작(Loco-Manipulation)은 항상 연구의 핵심이자 난제였습니다. 전통적인 방법은 일반적으로 이동과 조작을 분리하여 처리하므로, 특히 문 열기, 운반, 조립 등 환경과 지속적인 물리적 접촉이 필요한 작업에서 전신 협조를 달성하기 어렵습니다. 기존 강화 학습 기반 방법은 복잡한 행동을 생성할 수 있지만, 대량의 시뮬레이션 상호작용이 필요하고 실제 로봇에 직접 이전하기 어려운 경우가 많습니다. 동시에 원격 조작 기반 데이터 수집 방식은 직관적이지만, 획득된 데이터는 일반적으로 구조화된 기술 표현이 부족하여 모델의 일반화 능력이 제한됩니다. 따라서 고품질의 인간 시연 데이터를 효율적으로 수집하고 이를 조합 가능하고 이전 가능한 로봇 기술로 변환하는 것이 현재 연구의 핵심 병목 현상입니다.

HMC 프레임워크는 이러한 문제를 해결하기 위해 계층적 이종 제어 전략을 제안합니다. 이 프레임워크는 먼저 원격 조작 장치와 외골격 시스템을 통해 인간 조작자의 관절 움직임, 접촉력 및 촉각 신호를 수집하고, 동시에 로봇의 실시간 상태를 기록합니다. 이러한 다중 모드 데이터는 이후 ACT(Action Chunking with Transformers) 기반 행동 복제 모방 학습 모듈로 전송되며, 이 모듈은 원시 인간 조작 궤적을 훈련 가능한 관절 위치/토크 명령, 전신 운동 시퀀스 및 하위 제어기의 목표 값으로 변환합니다. 이 과정은 인간 시연에서 로봇 실행 가능 명령으로의 종단 간 매핑을 실현하고 데이터의 효율적인 재사용을 보장합니다.

기술 혁신 측면에서 HMC의 핵심은 "이종 메타 제어" 개념에 있습니다. 전통적인 종단 간 모델이 연속적인 동작을 직접 출력하는 것과 달리, HMC는 복잡한 작업을 명시적으로 여러 개의 이산적인 "기술" 또는 "전문가 정책"(예: "잡기", "이동", "밀기/당기기")으로 분해합니다. 각 기술은 특정 유형의 접촉 상호작용을 처리하는 전용 하위 정책 네트워크에 해당합니다. 상위 모듈은 현재 작업 목표와 인식 상태에 따라 이러한 기술을 동적으로 선택하고 조합하여 완전한 실행 시퀀스를 형성합니다. 이러한 라우팅 메커니즘은 단일 정책의 학습 난이도를 낮출 뿐만 아니라 시스템이 보지 못한 작업 변형에 유연하게 대응할 수 있게 하여 일반화 능력을 크게 향상시킵니다.

HMC의 효과를 검증하기 위해 연구팀은 실제 휴머노이드 로봇 플랫폼에서 여러 가지 접촉이 많은 이동 조작 실험을 수행했습니다. 실험 작업에는 제한된 공간에서 무거운 물체 운반, 도구를 사용한 밀기/당기기 작업, 다단계 조립 프로세스 완료 등이 포함됩니다. 결과는 HMC 프레임워크가 인간 원격 조작 데이터를 안정적인 로봇 행동으로 성공적으로 변환할 수 있음을 보여주었으며, 기술 조합 전략은 환경 교란(예: 물체 위치 이동, 접촉력 변화)에 대해 강력한 강건성을 나타냈습니다. 행동 복제를 직접 사용하는 기준 방법과 비교하여 HMC는 작업 성공률과 조작 정밀도에서 현저한 향상을 보였으며, 특히 서로 다른 접촉 모드를 연속적으로 전환해야 하는 시나리오에서 더욱 두드러진 장점을 보였습니다.

응용 가치 측면에서 HMC는 차세대 휴머노이드 로봇의 기술 학습 및 배치를 위한 실행 가능한 경로를 제공합니다. 데이터 폐루프 설계는 로봇이 인간 조작으로부터 지속적으로 새로운 기술을 학습할 수 있게 하며, 계층적 아키텍처는 기술의 모듈성과 확장성을 보장합니다. 향후 이 프레임워크는 가사 서비스, 산업 조립, 재난 구조 등 휴머노이드 로봇이 복잡한 물리적 상호작용을 수행해야 하는 분야에 적용되어, 로봇이 사전 설정된 프로그램 실행에서 자율적인 기술 조합으로의 패러다임 전환을 촉진할 것으로 기대됩니다.
