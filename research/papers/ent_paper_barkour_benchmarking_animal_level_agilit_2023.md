---
$id: ent_paper_barkour_benchmarking_animal_level_agilit_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'Barkour: Benchmarking Animal-level Agility with Quadruped Robots'
  zh: 'Barkour: Benchmarking Animal-level Agility with Quadruped Robots'
  ko: 'Barkour: Benchmarking Animal-level Agility with Quadruped Robots'
summary:
  en: 'Animals have evolved various agile locomotion strategies, such as sprinting, leaping, and jumping. There is a growing
    interest in developing legged robots that move like their biological counterparts and show various agile skills to navigate
    complex environments quickly. Institutions per source list: Google DeepMind.'
  zh: Barkour 是一个受犬类敏捷性比赛启发的四足机器人基准测试，由研究团队提出，用于系统评估机器人的敏捷控制能力。该基准包含多样化障碍物和基于时间的评分机制，并提供了两种控制方法作为基线：基于强化学习的专业技能组合策略，以及基于 Transformer
    的通用运动策略 Locomotion-Transformer。实验表明，该方法能使定制四足机器人以犬类一半的速度完成赛道。
  ko: 'Animals have evolved various agile locomotion strategies, such as sprinting, leaping, and jumping. There is a growing
    interest in developing legged robots that move like their biological counterparts and show various agile skills to navigate
    complex environments quickly. Institutions per source list: Google DeepMind.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- barkour
- benchmarking
- animal
- level
- agilit
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 314 (.staging/ingest_yuanxq). Tier C->full. arXiv id 2305.14654 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2305.14654v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2305.14654 Barkour: Benchmarking Animal-level Agility with Quadruped Robots'
  url: https://arxiv.org/abs/2305.14654
  accessed_at: '2026-07-31'
  date: '2023-05-24'
- id: src_002
  type: website
  title: GitHub仓库 Robotics_Notebooks
  url: https://github.com/ImChong/Robotics_Notebooks
  accessed_at: '2026-07-31'
---


## 概述

Barkour 是由 Google Research 等团队提出的四足机器人敏捷性基准，灵感源自狗敏捷比赛，在 5m×5m 场地内设置暂停台、绕桩、A 型架和宽跳四种障碍，并给出与 AKC 规则对齐的量化评分公式。论文同时提供了两条基线路径：手动状态机切换三个专家策略（全向行走、斜坡攀爬、跳跃），以及将专家策略蒸馏为单一 Transformer 通用策略（Locomotion-Transformer），并在自研 11.5kg 四足机器人上完成了约 3600 次真实课程尝试。

## 它改变了什么

腿式机器人领域不缺控制器，缺的是能横向比较控制策略与硬件的统一标尺。此前评估多为临时性指标，各实验室自说自话，导致“敏捷”无从量化。Barkour 的实质贡献是把“动物级敏捷”从口号变成可计算、可复现的分数——它定义的不是一个任务，而是一套包含障碍物规格、计时规则、扣分细则和成功判定的完整协议，让任何团队都能在相同条件下对策略打分。

更关键的是，论文用行动回答了“单一 RL 策略能否覆盖多技能课程”这一悬而未决的问题。作者没有只给基准，而是同时给出两条可复现的基线：专家策略组合与蒸馏通用策略，并诚实展示了两者与真实狗的差距（0.77/0.73 vs 1.0）。这改变了领域内“刷单任务 SOTA”的惯性，迫使研究者直面多技能迁移、sim-to-real 高速域差距等真问题。

## 方法拆解

### 基准设计
- 场地 5m×5m，四种障碍物：暂停台（1m×1m×0.1m）、绕桩（5 根柔性杆，间距 0.5–0.8m）、A 型架（30° 斜坡，顶高 1m）、宽跳（0.5m 长板，与地齐平）。
- 评分公式：`R_agility = 1.0 − max(t_run − t_allotted, 0) × 0.01 − penalties`，其中 `t_allotted = Σ(d_obstacle / v_target)`，`v_target = 1.69 m/s`；全程限定时间 10.64s，全程名义距离 18m。
- 成功判定细化到躯干中心位移（下桌 0.7m、上桌 0.4m）、绕桩中心与杆距（0.1m）、A 型架穿越底线、宽跳不触板。

### 专家策略（Specialist Policies）
- 三个策略均在 IsaacGym 中用 PPO 训练，共享观测空间（期望速度 `v̄`、本体感知 `s^p`、地形高度场 `s^v`、上一动作 `a_{t−1}`），动作空间为目标电机角度。
- 全向行走策略（OWP）：不平地形课程，速度指令范围 `v_x∈[-1.5,1.5] m/s`、`v_y∈[-1,1] m/s`、`ω_z∈[-2.5,2.5] rad/s`。
- 斜坡攀爬策略（SCP）：坡度从 5° 递增至 33°，速度指令 `v_x∈[-1.0,1.5] m/s`。
- 跳跃策略（JP）：三阶段课程——平地冲刺（最高 2.25 m/s）→ 间隙 0.3–0.6m → 固定 0.6m 间隙微调；终止条件为膝盖在间隙上方高度 < 0.06m。
- 域随机化关键：默认参数在 <1 m/s 时有效，但 >2 m/s 时 sim-to-real 差距显著，需额外随机化躯干惯量（40%–165%）、电机建模和关节静摩擦（0–0.7）。

### 通用策略 Locomotion-Transformer
- 通过行为克隆将专家策略蒸馏为因果 Transformer：输入序列 `{s^p_0, a_0, …, s^p_{t−1}, a_{t−1}, s^p_t, s^v_t}`，预测下一动作 `a_t`，L2 回归损失。
- 架构：两层 Transformer，上下文长度 15（对应 0.3s），内部表示 256-d；高度场经两层卷积（核 3、通道 8）编码为 64-d 嵌入；本体感知与动作各经 256-d 投影 token 化；ReLU 激活。
- 数据收集：17,636 个模拟回合（57.58 机器人小时），覆盖随机台阶、楼梯、间隙、斜坡四种环境；对不接收速度命令的策略，用 1s 后平均实际速度作为有效命令。

### 高层导航控制器
- 状态机设计，访问完整环境状态与真实位姿，输出速度指令；路点含目标位置、朝向和误差容限（绕杆 `d_lim=0.3m`、`ψ_lim=0.2rad`；跳跃前收紧至 `0.1m`、`0.17rad`）。
- 速度命令公式：`v_x = α·sin(φ)·‖d‖`，`v_y = β·cos(φ)·‖d‖`，`ω = γ·Δ(ψ)`。

## 关键创新

1. **把狗敏捷比赛变成可计算的机器人基准**：Barkour 不是又一个仿真任务，而是包含物理障碍物规格、计时规则、扣分细则和成功判定的完整协议，且评分公式与 AKC 规则对齐，让“动物级敏捷”有了可横向比较的量化分数。这是领域内首次将动物竞技规则系统化移植到机器人评估。

2. **蒸馏式通用策略替代手工切换**：Locomotion-Transformer 通过行为克隆将三个专家策略融合为单一网络，部署时无需显式告知障碍物类型，仅需速度指令。消融显示 MLP 在相同数据上显著差于 Transformer，且上下文长度 15 是关键——这验证了时序信息对多技能平滑切换的必要性。

3. **高速域 sim-to-real 的系统性处理**：作者明确指出默认域随机化在 >2 m/s 时失效，并针对跳跃/爬坡场景增加躯干惯量、电机建模和关节静摩擦随机化。这一发现对任何追求高速敏捷策略的团队都有直接指导意义，而非仅适用于本基准。

## 实验与结果

| 指标 | 专家策略 | Locomotion-Transformer | 真实狗 |
|---|---|---|---|
| Barkour 得分 | 0.77 ± 0.064 | 0.73 ± 0.062 | 1.00 ± 0 |
| 完成时间 (s) | 24.6 ± 1.1 | 25.8 ± 1.6 | 9.02 ± 0.65 |
| 前进速度 (m/s) | 0.74 ± 0.04 | 0.69 ± 0.04 | — |

- 单障碍物表现：绕桩成功率 100%（9.27s），A 型架成功率 100%（7.95s），宽跳成功率仅 38%（2.45s，速度 1.7 m/s）——宽跳是明显瓶颈。
- 层级控制器鲁棒性：71 次运行中 6 次摔倒，40 次触碰跳板（得分约 0.73），25 次完成全部障碍（平均 0.87）。
- 消融：1% 数据集（约 176 episodes）不足；10% 与 100% 性能相似；更长上下文更有效；增大模型尺寸可提升但受限于推理延迟。
- 多任务 RL 直接训练失败：混合三种地形的单一策略无法爬上 A-Frame 也无法完成宽跳，证明陡坡与跳跃需要专家级训练。
- 单次最佳运行：20.4s 完成，得分 0.91；跳跃段最高速度 2.3 m/s。

## 边界与局限

- 基线方法使用特权信息：环境 CAD 模型和机器人位置来自外部动作捕捉系统，未探索纯机载传感器方案。
- 宽跳成功率仅 38%，且课程特定策略在真实环境中未完全清除间隙，偶尔跳过或错过绕桩。
- 斜坡穿越策略在仿真中表现良好但迁移到真实 A-Frame 时失败，说明仿真地形与真实障碍物（如人造草皮摩擦、间隙）存在未建模差异。
- 作者未进行多机器人平台泛化实验，也未测试策略在未见障碍物配置上的表现；长期稳定性与退化测试未提及。
- 真实狗从未失败且显著快于基线，作者承认这凸显了硬件（如柔性脊柱）与算法的双重改进空间。

## 工程启示

- **先核对评分公式与障碍物规格**：Barkour 的分数高度依赖 `v_target=1.69 m/s` 和障碍物名义尺寸，复现时务必按附录 B 精确搭建（如 A 型架地板与斜坡间隙 <20mm、宽跳板与地齐平），否则分数不可比。
- **高速域域随机化是最大坑**：默认参数在 <1 m/s 有效，但跳跃/爬坡需额外随机化躯干惯量（40%–165%）和关节静摩擦（0–0.7），否则 sim-to-real 差距会直接导致宽跳失败。
- **Transformer 蒸馏值得优先复现**：Locomotion-Transformer 性能仅略低于专家组合（0.73 vs 0.77），但部署时无需显式障碍物类型，且消融显示 10% 数据即可达到接近满数据性能——对算力有限的团队是更经济的选择。
- **宽跳是瓶颈，优先优化跳跃策略**：成功率 38% 是全场最低，且失败多因触碰跳板边缘。建议从 JP 的三阶段课程（平地冲刺→间隙→固定间隙微调）入手，并检查膝盖高度终止条件（<0.06m）是否过于严格。
- **注意推理频率与硬件匹配**：策略位置指令 50 Hz、PD 环 1 kHz，PD 增益 (20 N·m/rad, 0.5 N·m·s/rad)；若更换执行器（如 T-Motor AK80-6 峰值 12 N·m），需重新做系统辨识（可微优化关节增益与阻尼）。

## 参考
- https://arxiv.org/abs/2305.14654
- https://github.com/ImChong/Robotics_Notebooks

## Overview

Barkour is an agility benchmark for quadruped robots proposed by teams including Google Research, inspired by dog agility competitions. It sets up four obstacles—a pause box, weave poles, an A-frame, and a broad jump—within a 5m×5m area, and provides a quantitative scoring formula aligned with AKC rules. The paper also offers two baseline approaches: a manual state machine switching among three expert policies (omnidirectional walking, slope climbing, jumping), and distilling the expert policies into a single Transformer generalist policy (Locomotion-Transformer), with approximately 3,600 real course attempts completed on a custom 11.5kg quadruped robot.

## What It Changes

The legged robotics field does not lack controllers; it lacks a unified metric for horizontally comparing control strategies and hardware. Previous evaluations were mostly ad hoc metrics, with each lab reporting its own results, making "agility" impossible to quantify. Barkour's substantive contribution is turning "animal-level agility" from a slogan into a computable, reproducible score—it defines not a single task but a complete protocol including obstacle specifications, timing rules, penalty details, and success criteria, allowing any team to score strategies under identical conditions.

More critically, the paper answers the open question of "whether a single RL policy can cover a multi-skill course" through action. The authors do not merely provide a benchmark; they also provide two reproducible baselines—expert policy ensembles and distilled generalist policies—and honestly demonstrate their gap compared to real dogs (0.77/0.73 vs 1.0). This shifts the field's inertia of "SOTA on single tasks," forcing researchers to confront real problems such as multi-skill transfer and high-speed sim-to-real gaps.

## Method Breakdown

### Benchmark Design
- Area: 5m×5m, four obstacles: pause box (1m×1m×0.1m), weave poles (5 flexible poles, spacing 0.5–0.8m), A-frame (30° slope, 1m peak height), broad jump (0.5m long board, flush with ground).
- Scoring formula: `R_agility = 1.0 − max(t_run − t_allotted, 0) × 0.01 − penalties`, where `t_allotted = Σ(d_obstacle / v_target)`, `v_target = 1.69 m/s`; total time limit 10.64s, total nominal distance 18m.
- Success criteria detailed down to torso center displacement (0.7m off the table, 0.4m onto the table), distance from weave pole centers (0.1m), crossing the A-frame bottom line, and not touching the broad jump board.

### Specialist Policies
- All three policies trained in IsaacGym with PPO, sharing observation space (desired velocity `v̄`, proprioception `s^p`, terrain height field `s^v`, previous action `a_{t−1}`), with action space as target motor angles.
- Omnidirectional Walking Policy (OWP): uneven terrain course, velocity commands `v_x∈[-1.5,1.5] m/s`, `v_y∈[-1,1] m/s`, `ω_z∈[-2.5,2.5] rad/s`.
- Slope Climbing Policy (SCP): slopes increasing from 5° to 33°, velocity commands `v_x∈[-1.0,1.5] m/s`.
- Jumping Policy (JP): three-stage course—flat sprint (up to 2.25 m/s) → gaps 0.3–0.6m → fixed 0.6m gap fine-tuning; termination condition: knee height above gap < 0.06m.
- Key domain randomization: default parameters effective below 1 m/s, but sim-to-real gap significant above 2 m/s, requiring additional randomization of torso inertia (40%–165%), motor modeling, and joint static friction (0–0.7).

### Generalist Policy: Locomotion-Transformer
- Distills expert policies via behavior cloning into a causal Transformer: input sequence `{s^p_0, a_0, …, s^p_{t−1}, a_{t−1}, s^p_t, s^v_t}`, predicting next action `a_t` with L2 regression loss.
- Architecture: two-layer Transformer, context length 15 (corresponding to 0.3s), internal representation 256-d; height field encoded via two convolutional layers (kernel 3, channels 8) into 64-d embedding; proprioception and actions each tokenized via 256-d projections; ReLU activation.
- Data collection: 17,636 simulated episodes (57.58 robot hours), covering four environments—random steps, stairs, gaps, slopes; for policies not receiving velocity commands, the average actual velocity over 1s is used as the effective command.

### High-Level Navigation Controller
- State machine design, accessing full environment state and true pose, outputting velocity commands; waypoints include target position, orientation, and error tolerances (weave poles `d_lim=0.3m`, `ψ_lim=0.2rad`; tightened to `0.1m`, `0.17rad` before jumps).
- Velocity command formula: `v_x = α·sin(φ)·‖d‖`, `v_y = β·cos(φ)·‖d‖`, `ω = γ·Δ(ψ)`.

## Key Innovations

1. **Turning dog agility competitions into a computable robot benchmark**: Barkour is not just another simulation task but a complete protocol with physical obstacle specifications, timing rules, penalty details, and success criteria, with a scoring formula aligned with AKC rules, giving "animal-level agility" a horizontally comparable quantitative score. This is the first systematic transplantation of animal competition rules into robot evaluation in the field.

2. **Distilled generalist policy replacing manual switching**: Locomotion-Transformer fuses three expert policies into a single network via behavior cloning, requiring no explicit obstacle type at deployment—only velocity commands. Ablations show MLP performs significantly worse than Transformer on the same data, and context length 15 is critical—validating the necessity of temporal information for smooth multi-skill switching.

3. **Systematic handling of high-speed sim-to-real**: The authors explicitly identify that default domain randomization fails above 2 m/s and add torso inertia, motor modeling, and joint static friction randomization for jumping/climbing scenarios. This finding has direct guidance for any team pursuing high-speed agile policies, not just for this benchmark.

## Experiments and Results

| Metric | Expert Policies | Locomotion-Transformer | Real Dog |
|---|---|---|---|
| Barkour Score | 0.77 ± 0.064 | 0.73 ± 0.062 | 1.00 ± 0 |
| Completion Time (s) | 24.6 ± 1.1 | 25.8 ± 1.6 | 9.02 ± 0.65 |
| Forward Velocity (m/s) | 0.74 ± 0.04 | 0.69 ± 0.04 | — |

- Single-obstacle performance: weave poles 100% success (9.27s), A-frame 100% success (7.95s), broad jump only 38% success (2.45s, velocity 1.7 m/s)—broad jump is the clear bottleneck.
- Hierarchical controller robustness: 6 falls in 71 runs, 40 board touches (score ≈ 0.73), 25 full course completions (average 0.87).
- Ablations: 1% dataset (≈176 episodes) insufficient; 10% and 100% perform similarly; longer context more effective; larger model size helps but limited by inference latency.
- Direct multi-task RL training fails: a single policy trained on mixed three terrains cannot climb the A-frame nor complete the broad jump, proving steep slopes and jumps require expert-level training.
- Best single run: 20.4s completion, score 0.91; peak velocity 2.3 m/s during jump segment.

## Boundaries and Limitations

- Baseline methods use privileged information: environment CAD models and robot position from external motion capture systems; purely onboard sensor solutions not explored.
- Broad jump success rate only 38%, and course-specific policies did not fully clear gaps in real environments, occasionally skipping or missing weave poles.
- Slope traversal policy performs well in simulation but fails when transferred to the real A-frame, indicating unmodeled differences between simulated terrain and real obstacles (e.g., artificial turf friction, gaps).
- Authors did not conduct multi-robot platform generalization experiments, nor test policies on unseen obstacle configurations; long-term stability and degradation tests not mentioned.
- Real dogs never failed and were significantly faster than baselines; authors acknowledge this highlights dual improvement space in hardware (e.g., flexible spine) and algorithms.

## Engineering Insights

- **Verify scoring formula and obstacle specifications first**: Barkour scores heavily depend on `v_target=1.69 m/s` and nominal obstacle dimensions; reproduction must precisely follow Appendix B (e.g., A-frame floor-to-ramp gap <20mm, broad jump board flush with ground), otherwise scores are incomparable.
- **High-speed domain randomization is the biggest pitfall**: Default parameters work below 1 m/s, but jumping/climbing requires additional randomization of torso inertia (40%–165%) and joint static friction (0–0.7), otherwise sim-to-real gaps directly cause broad jump failures.
- **Transformer distillation is worth prioritizing for reproduction**: Locomotion-Transformer performs only slightly below the expert ensemble (0.73 vs 0.77), but requires no explicit obstacle type at deployment, and ablations show 10% data achieves near-full-data performance—a more economical choice for teams with limited compute.
- **Broad jump is the bottleneck; prioritize jump policy optimization**: 38% success is the lowest across the course, with most failures from touching the board edge. Start with JP's three-stage curriculum (flat sprint → gaps → fixed gap fine-tuning) and check whether the knee height termination condition (<0.06m) is overly strict.
- **Match inference frequency with hardware**: Policy position commands at 50 Hz, PD loop at 1 kHz, PD gains (20 N·m/rad, 0.5 N·m·s/rad); if changing actuators (e.g., T-Motor AK80-6 peak 12 N·m), redo system identification (differentiable optimization of joint gains and damping).

## 개요

Barkour는 Google Research 등 팀이 제안한 네 발 로봇 민첩성 벤치마크로, 개 민첩성 대회에서 영감을 받아 5m×5m 공간에 정지대, 지그재그 장애물, A형 프레임, 넓이뛰기 네 가지 장애물을 배치하고 AKC 규칙과 정렬된 정량적 점수 공식을 제공합니다. 논문은 또한 두 가지 기준선 경로를 제시합니다: 수동 상태 머신이 세 가지 전문가 정책(전방향 보행, 경사 등반, 점프)을 전환하는 방식과, 전문가 정책을 단일 Transformer 범용 정책(Locomotion-Transformer)으로 증류하는 방식이며, 자체 개발한 11.5kg 네 발 로봇에서 약 3600회의 실제 코스 시도를 완료했습니다.

## 무엇을 바꾸었는가

다리 로봇 분야에는 컨트롤러가 부족하지 않지만, 제어 정책과 하드웨어를 가로로 비교할 수 있는 통일된 척도가 부족했습니다. 이전 평가는 대부분 임시 지표에 의존하여 각 실험실이 제각각 주장하여 '민첩성'을 정량화할 수 없었습니다. Barkour의 실질적 기여는 '동물 수준의 민첩성'을 구호에서 계산 가능하고 재현 가능한 점수로 바꾼 것입니다—이는 단일 작업을 정의하는 것이 아니라 장애물 사양, 타이밍 규칙, 감점 세부 사항, 성공 판정을 포함한 완전한 프로토콜을 정의하여 어떤 팀이든 동일한 조건에서 정책을 평가할 수 있게 합니다.

더 중요하게, 논문은 '단일 RL 정책이 다중 기술 코스를 커버할 수 있는가'라는 미해결 질문에 행동으로 답했습니다. 저자는 벤치마크만 제공하지 않고 전문가 정책 조합과 증류 범용 정책이라는 두 가지 재현 가능한 기준선을 함께 제시하고, 실제 개와의 격차(0.77/0.73 vs 1.0)를 정직하게 보여주었습니다. 이는 분야 내 '단일 작업 SOTA 갱신' 관성을 바꾸어 연구자들이 다중 기술 전이, sim-to-real 고속 영역 격차 같은 진짜 문제를 직시하도록 강제합니다.

## 방법 분해

### 벤치마크 설계
- 공간 5m×5m, 네 가지 장애물: 정지대(1m×1m×0.1m), 지그재그 장애물(5개 유연한 막대, 간격 0.5–0.8m), A형 프레임(30° 경사, 정상 높이 1m), 넓이뛰기(0.5m 길이 판, 지면과 평평).
- 점수 공식: `R_agility = 1.0 − max(t_run − t_allotted, 0) × 0.01 − penalties`, 여기서 `t_allotted = Σ(d_obstacle / v_target)`, `v_target = 1.69 m/s`; 전체 제한 시간 10.64초, 전체 명목 거리 18m.
- 성공 판정은 몸통 중심 변위(테이블 하강 0.7m, 상승 0.4m), 지그재그 중심과 막대 거리(0.1m), A형 프레임 하단선 통과, 넓이뛰기 판 비접촉으로 세분화.

### 전문가 정책(Specialist Policies)
- 세 정책 모두 IsaacGym에서 PPO로 훈련, 관측 공간 공유(목표 속도 `v̄`, 고유 감각 `s^p`, 지형 높이 필드 `s^v`, 이전 행동 `a_{t−1}`), 행동 공간은 목표 모터 각도.
- 전방향 보행 정책(OWP): 불규칙 지형 코스, 속도 명령 범위 `v_x∈[-1.5,1.5] m/s`, `v_y∈[-1,1] m/s`, `ω_z∈[-2.5,2.5] rad/s`.
- 경사 등반 정책(SCP): 경사 5°에서 33°로 증가, 속도 명령 `v_x∈[-1.0,1.5] m/s`.
- 점프 정책(JP): 3단계 코스—평지 질주(최대 2.25 m/s) → 간격 0.3–0.6m → 고정 0.6m 간격 미세 조정; 종료 조건은 무릎이 간격 위 높이 < 0.06m.
- 도메인 무작위화 핵심: 기본 매개변수는 <1 m/s에서 유효하지만 >2 m/s에서는 sim-to-real 격차가 크므로, 몸통 관성(40%–165%), 모터 모델링, 관절 정지 마찰(0–0.7) 추가 무작위화 필요.

### 범용 정책 Locomotion-Transformer
- 행동 복제를 통해 전문가 정책을 인과 Transformer로 증류: 입력 시퀀스 `{s^p_0, a_0, …, s^p_{t−1}, a_{t−1}, s^p_t, s^v_t}`, 다음 행동 `a_t` 예측, L2 회귀 손실.
- 아키텍처: 2계층 Transformer, 컨텍스트 길이 15(0.3초에 해당), 내부 표현 256-d; 높이 필드는 2계층 합성곱(커널 3, 채널 8)으로 64-d 임베딩 인코딩; 고유 감각과 행동은 각각 256-d 프로젝션으로 토큰화; ReLU 활성화.
- 데이터 수집: 17,636개 시뮬레이션 에피소드(57.58 로봇 시간), 무작위 계단, 계단, 간격, 경사 네 가지 환경 포함; 속도 명령을 받지 않는 정책의 경우 1초 후 평균 실제 속도를 유효 명령으로 사용.

### 상위 수준 내비게이션 컨트롤러
- 상태 머신 설계, 전체 환경 상태와 실제 자세 접근, 속도 명령 출력; 웨이포인트는 목표 위치, 방향, 오차 허용 한계 포함(지그재그 `d_lim=0.3m`, `ψ_lim=0.2rad`; 점프 전 0.1m, 0.17rad로 강화).
- 속도 명령 공식: `v_x = α·sin(φ)·‖d‖`, `v_y = β·cos(φ)·‖d‖`, `ω = γ·Δ(ψ)`.

## 핵심 혁신

1. **개 민첩성 대회를 계산 가능한 로봇 벤치마크로 변환**: Barkour는 또 다른 시뮬레이션 작업이 아니라 물리적 장애물 사양, 타이밍 규칙, 감점 세부 사항, 성공 판정을 포함한 완전한 프로토콜이며, 점수 공식이 AKC 규칙과 정렬되어 '동물 수준의 민첩성'에 가로 비교 가능한 정량적 점수를 부여합니다. 이는 분야에서 동물 경기 규칙을 로봇 평가에 체계적으로 이식한 최초의 사례입니다.

2. **증류 기반 범용 정책이 수동 전환 대체**: Locomotion-Transformer는 행동 복제를 통해 세 전문가 정책을 단일 네트워크로 융합하며, 배포 시 장애물 유형을 명시적으로 알릴 필요 없이 속도 명령만 필요합니다. 절제 실험에서 MLP는 동일 데이터에서 Transformer보다 현저히 낮은 성능을 보였고, 컨텍스트 길이 15가 핵심임을 확인—이는 다중 기술의 부드러운 전환에 시계열 정보가 필수적임을 검증합니다.

3. **고속 영역 sim-to-real의 체계적 처리**: 저자는 기본 도메인 무작위화가 >2 m/s에서失效함을 명확히 지적하고, 점프/등반 시나리오에 몸통 관성, 모터 모델링, 관절 정지 마찰 무작위화를 추가했습니다. 이 발견은 고속 민첩성 정책을 추구하는 모든 팀에 직접적 지침을 제공하며, 본 벤치마크에만 국한되지 않습니다.

## 실험 및 결과

| 지표 | 전문가 정책 | Locomotion-Transformer | 실제 개 |
|---|---|---|---|
| Barkour 점수 | 0.77 ± 0.064 | 0.73 ± 0.062 | 1.00 ± 0 |
| 완료 시간 (초) | 24.6 ± 1.1 | 25.8 ± 1.6 | 9.02 ± 0.65 |
| 전진 속도 (m/s) | 0.74 ± 0.04 | 0.69 ± 0.04 | — |

- 단일 장애물 성능: 지그재그 성공률 100%(9.27초), A형 프레임 성공률 100%(7.95초), 넓이뛰기 성공률 38%(2.45초, 속도 1.7 m/s)—넓이뛰기가 명백한 병목.
- 계층적 컨트롤러 견고성: 71회 실행 중 6회 넘어짐, 40회 점프판 접촉(점수 약 0.73), 25회 전체 장애물 완료(평균 0.87).
- 절제: 1% 데이터셋(약 176 에피소드) 불충분; 10%와 100% 성능 유사; 더 긴 컨텍스트가 더 효과적; 모델 크기 증가는 성능 향상 가능하나 추론 지연으로 제한.
- 다중 작업 RL 직접 훈련 실패: 세 지형 혼합 단일 정책은 A-Frame 등반도 넓이뛰기도 완료하지 못해, 급경사와 점프는 전문가 수준 훈련이 필요함을 증명.
- 단일 최고 실행: 20.4초 완료, 점수 0.91; 점프 구간 최고 속도 2.3 m/s.

## 경계 및 한계

- 기준선 방법은 특권 정보 사용: 환경 CAD 모델과 로봇 위치는 외부 모션 캡처 시스템에서 제공, 순수 온보드 센서 방식은 탐구되지 않음.
- 넓이뛰기 성공률 38%에 불과하며, 코스 특화 정책은 실제 환경에서 간격을 완전히 제거하지 못해 가끔 건너뛰거나 지그재그를 놓침.
- 경사 횡단 정책은 시뮬레이션에서 우수하지만 실제 A-Frame 전이 시 실패하여, 시뮬레이션 지형과 실제 장애물(인조 잔디 마찰, 간격 등) 간 미모델링 차이가 존재함을 시사.
- 저자는 다중 로봇 플랫폼 일반화 실험을 수행하지 않았고, 미지의 장애물 구성에서 정책 성능도 테스트하지 않음; 장기 안정성 및 성능 저하 테스트는 언급되지 않음.
- 실제 개는 실패한 적이 없고 기준선보다 현저히 빨라, 저자는 하드웨어(예: 유연한 척추)와 알고리즘의 이중 개선 여지가 있음을 인정.

## 공학적 시사점

- **먼저 점수 공식과 장애물 사양을 검증**: Barkour 점수는 `v_target=1.69 m/s`와 장애물 명목 치수에 크게 의존하므로, 재현 시 부록 B에 따라 정확히 구축해야 함(예: A형 프레임 바닥과 경사 간격 <20mm, 넓이뛰기 판이 지면과 평평), 그렇지 않으면 점수를 비교할 수 없음.
- **고속 영역 도메인 무작위화가 최대 함정**: 기본 매개변수는 <1 m/s에서 유효하지만, 점프/등반에는 몸통 관성(40%–165%)과 관절 정지 마찰(0–0.7) 추가 무작위화가 필요하며, 그렇지 않으면 sim-to-real 격차가 넓이뛰기 실패로 직접 이어짐.
- **Transformer 증류가 우선 재현 가치 있음**: Locomotion-Transformer 성능은 전문가 조합보다 약간 낮지만(0.73 vs 0.77), 배포 시 장애물 유형을 명시적으로 알릴 필요가 없고, 절제 실험에서 10% 데이터로도 거의 전체 데이터 성능에 도달—계산 자원이 제한된 팀에게 더 경제적 선택.
- **넓이뛰기가 병목, 점프 정책 우선 최적화**: 성공률 38%는 전체 최저이며, 실패는 대부분 점프판 가장자리 접촉 때문. JP의 3단계 코스(평지 질주→간격→고정 간격 미세 조정)에서 시작하고, 무릎 높이 종료 조건(<0.06m)이 너무 엄격한지 확인 권장.
- **추론 주파수와 하드웨어 일치 주의**: 정책 위치 명령 50 Hz, PD 루프 1 kHz, PD 이득 (20 N·m/rad, 0.5 N·m·s/rad); 액추에이터 교체 시(예: T-Motor AK80-6 최대 12 N·m), 시스템 식별 재수행 필요(관절 이득과 감쇠 미분 최적화).
