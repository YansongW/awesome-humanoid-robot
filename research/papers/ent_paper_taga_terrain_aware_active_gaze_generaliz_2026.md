---
$id: ent_paper_taga_terrain_aware_active_gaze_generaliz_2026
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: paper
names:
  en: 'TAGA: Terrain-aware Active Gaze Learning for Generalizable Agile Humanoid Locomotion'
  zh: 地形感知主动凝视的人形敏捷运动控制
  ko: 'TAGA: Terrain-aware Active Gaze Learning for Generalizable Agile Humanoid Locomotion'
summary:
  en: 'Agile humanoid locomotion across diverse challenging terrain demands both wide perceptual coverage and precise local
    geometry understanding. Motivated by the way humans selectively look at relevant terrain during locomotion, we introduce
    TAGA, a Terrain-aware Active Gaze learning framework for Attention-based humanoid control. Institutions per source list:
    新加坡国立大学Marmot实验室、浙江大学、华南理工大学.'
  zh: TAGA 是一个面向注意力机制人形机器人控制的主动注视学习框架，由研究团队提出，旨在通过融合视觉、本体感知与运动指令，让机器人自主关注地形关键区域。其核心贡献在于仅通过强化学习即可自然涌现类人注视行为，无需额外监督，并在真实环境中实现了
    1.2 米的最大间隙跨越距离。
  ko: 'Agile humanoid locomotion across diverse challenging terrain demands both wide perceptual coverage and precise local
    geometry understanding. Motivated by the way humans selectively look at relevant terrain during locomotion, we introduce
    TAGA, a Terrain-aware Active Gaze learning framework for Attention-based humanoid control. Institutions per source list:
    新加坡国立大学Marmot实验室、浙江大学、华南理工大学.'
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
theoretical_depth:
- system
tags:
- taga
- terrain
- aware
- active
- gaze
verification:
  status: unverified
  reviewed_by: ai
  reviewed_at: '2026-07-31'
  confidence: medium
  notes: 'Full ingest from Yuanxq lab paper list row 9 (.staging/ingest_yuanxq). Tier B->full. arXiv id 2606.05880 recovered
    programmatically (strict title match/page scan). Title guard: substring (score 1.0). Abstract and metadata from arXiv
    API (2606.05880v1); zh content by DeepSeek from the abstract. Institutions as given in the source list, not verified.
    [2026-08-04] body rewritten as full-text six-section deep read (.staging/deep_read batch1, DeepSeek deepseek-chat T<=0.3,
    arXiv HTML full text); en/ko sections regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'arXiv:2606.05880 TAGA: Terrain-aware Active Gaze Learning for Generalizable Agile Humanoid Locomotion'
  url: https://arxiv.org/abs/2606.05880
  accessed_at: '2026-07-31'
  date: '2026-06-04'
- id: src_002
  type: website
  title: Project page
  url: https://marmotlab.github.io/taga-humanoid/
  accessed_at: '2026-07-31'
- id: src_003
  type: website
  title: 万字长文｜人形机器人的运动小脑会不会成为人形机器人的基础设施？
  url: https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA
  accessed_at: '2026-07-31'
---


## 概述

TAGA 是一个面向人形机器人敏捷 locomotion 的主动感知框架，由研究团队在 Isaac Lab 仿真与 Unitree G1 真机上实现。其核心贡献在于将“看哪里”的主动注视机制与多模态感知融合相结合，通过可微分的注视点预测与交叉注意力机制，在显著降低训练成本的同时实现了与全图扫描相当甚至更优的泛化性能。

## 它改变了什么

现有感知 locomotion 方法陷入了一个两难：基于高度图的方法在扩大感知范围时计算成本急剧上升，而基于前视深度图的方法虽然看得远却会遗漏脚部附近的关键地形细节。更根本的问题在于，这两种感知范式——视觉的远见与高度图的局部精度——本质上是互补的，但现有方法要么依赖单一来源，要么只是松散耦合，从未真正解决“何时看哪里”这一主动感知问题。人类和动物在复杂地形上行走时，目光会主动投向任务相关的区域，而不是均匀扫描整个视野。TAGA 改变了这一范式：它不再将感知视为被动的信息收集，而是将其建模为一个可学习的决策过程，让策略自主决定哪些局部地形信息对下一步动作最关键。

这一改变的实际意义在于，它打破了感知范围与计算成本之间的线性绑定。通过将注意力集中在高度图的一个紧凑区域（K=11 的裁剪块），TAGA 在保持前瞻视觉线索的同时，将局部几何处理的复杂度降低了近一个数量级。更重要的是，这种主动注视机制不是手工设计的启发式规则，而是通过端到端强化学习自动涌现的——策略学会了在不同地形上应该注视哪里，这比任何固定策略都更具适应性。

## 方法拆解

TAGA 的整体框架是一个 POMDP 策略，观测空间由三部分构成：前视深度图 d_t ∈ ℝ^{1×36×64}、以自我为中心的高度图 h_t^{xyz} ∈ ℝ^{3×21×21}、以及 5 帧本体感觉历史 p_t^H。动作空间为 29 维目标关节位置，以 50 Hz 输出，由 200 Hz 的低级 PD 控制器跟踪。

### 任务相关主动注视模块
- 轻量级预测头 f_roi 接收视觉嵌入 e_t^d 与本体感觉嵌入 e_t^p 的拼接，输出归一化二维注视位置 r_t ∈ [0,1]²
- 将 r_t 映射到高度图网格坐标 (u_t, v_t) = (⌊r_t^x M⌋, ⌊r_t^y M⌋)，其中 M=21
- 通过可微分的双线性网格采样裁剪出 K=11 的紧凑地形块 h̃_t^{xyz}
- 边界惩罚 L_roi 防止注视点退化到高度图边界，裕度 m=0.05

### 视觉运动融合编码器
- 裁剪地形块经轻量级 CNN 与空间坐标 MLP 编码，得到逐点地形嵌入 E_t^m ∈ ℝ^{K×K×128}，作为 key 和 value
- 投影头 f_proj 从视觉与本体感觉嵌入生成 query
- 多头交叉注意力融合：e_t^{pg} = MHA(f_proj([e_t^p, e_t^d]), E_t^m, E_t^m)，输出 ℝ¹²⁸

### 动作解码与训练
- 混合专家（MoE）模块，N_e=5 个专家，门控网络 g(·) 生成软路由权重 α_t^i = Softmax(g(e_t^π))_i，最终动作 a_t = Σ α_t^i E_i(e_t^π)
- 非对称 actor-critic PPO 训练，critic 接收特权信息（真实线速度、完整高度图）
- 两阶段训练：第一阶段 30k 次迭代无噪声训练核心技能，第二阶段 10k 次迭代降低熵系数并引入部署导向域随机化
- 对比损失 L_con 通过 Sinkhorn 平衡分配将 MoE 门控嵌入与完整高度图地形嵌入在共享原型空间中对齐，鼓励专家地形专业化
- 对称增强利用人形机器人左右对称性，镜像观测与动作并纳入 PPO 更新

## 关键创新

**主动注视作为可学习决策**：将“看哪里”建模为端到端可微的预测头，而非手工规则或固定策略。这是首次将主动感知机制与强化学习策略联合优化，使注视行为本身成为策略的一部分。其重要性在于，策略学会了在不同地形上自适应地分配感知资源——在间隙跨越时注视落脚点，在稀疏踏脚石上注视下一个支撑面。

**裁剪式注意力替代全图处理**：通过可微分的双线性采样从 21×21 高度图中裁剪出 11×11 的紧凑块，将局部几何处理的输入规模缩减约 73%。这不仅降低了计算成本，更重要的是通过信息瓶颈迫使策略聚焦于任务相关区域，避免了全图处理中的信息冗余与过拟合。实验证明，这种裁剪式注意力在 OOD 地形上的表现与全图扫描相当，但训练成本降低 65.2%。

**MoE 与地形对比学习的协同**：通过对比损失将 MoE 门控嵌入与完整高度图的地形嵌入在共享原型空间中对齐，使专家网络自发地 specialize 到不同地形类型。这解决了 MoE 在强化学习中常见的专家退化问题，同时为策略提供了地形感知的模块化决策能力。

## 实验与结果

仿真评估在 Isaac Lab 中进行，8,000 个并行环境，4 块 RTX 5090 GPU，训练总耗时约 17 GPU 天。每种地形评估 1,000 次试验。

| 方法 | Gaps | Stepping Stones | Beam | High Platform | Terrain C1 | Terrain C2 | 训练成本 |
|------|------|-----------------|------|---------------|------------|------------|----------|
| CReF | 97.40% | 52.30% | 96.50% | 98.70% | 85.20% | 43.10% | ~10 GPU-days |
| TAGA-HSOnly | 93.10% | 92.50% | 98.30% | 99.60% | 90.50% | 91.50% | ~17 GPU-days |
| TAGA-InactiveGaze | 57.10% | 83.20% | 95.60% | 100.00% | 72.70% | 48.80% | ~14 GPU-days |
| TAGA-FullScan | 99.50% | 98.00% | 97.50% | 100.00% | 93.40% | 92.50% | ~49 GPU-days |
| TAGA-NoAMP | 96.40% | 97.20% | 97.60% | 99.50% | 89.80% | 91.60% | ~16 GPU-days |
| **TAGA (Ours)** | **98.30%** | **97.90%** | **98.50%** | **100.00%** | **93.70%** | **93.90%** | **~17 GPU-days** |

关键对比结论：
- TAGA 在 OOD 地形 C1、C2 上成功率分别为 93.70% 和 93.90%，显著优于 CReF（85.20%/43.10%）和 TAGA-InactiveGaze（72.70%/48.80%）
- TAGA 与 TAGA-FullScan 性能相当，但训练成本降低 65.2%（17 vs 49 GPU-days）
- TAGA-InactiveGaze 在 Gaps 上仅 57.10%，证明主动注视机制对间隙跨越至关重要
- 真实世界评估：Unitree G1 成功跨越 120 cm 间隙（对比方法最佳为 90 cm），稀疏踏脚石间距 50–70 cm 且高度变化 10 cm，并在室外、低光照、踢踹推搡下保持稳定

## 边界与局限

作者明确承认执行器热负荷问题：TAGA 实现的动态动作（爆发性跳跃、精确落脚、动态平衡恢复）在长时间运行中对执行器产生高热负荷，可能导致控制精度逐渐下降，表现为跳跃距离缩短或错过落脚点。此外，方法高度依赖高度图质量——在复杂地形上，高度图重建质量差会直接导致步态不当或失败。论文未明确提及数据量（AMP 参考数据的具体规模）以及在不同机器人平台上的迁移能力。值得注意的是，真实世界评估中的对比方法（HPL、PIM、CMoE 等）并非在同一实验条件下复现，而是引用了各自论文中的最佳结果，因此跨方法比较需谨慎解读。

## 工程启示

复现 TAGA 时，首先需要核对的是**注视预测头的初始化与边界惩罚的权重**——从消融实验看，TAGA-InactiveGaze（固定注视点）在 Gaps 上性能骤降至 57.10%，说明注视机制对间隙类地形至关重要，但若边界惩罚过强可能导致注视点过早收敛到中心区域，削弱自适应能力。其次，**两阶段训练策略不可跳过**：第一阶段的无噪声训练建立了核心技能，第二阶段的域随机化微调（特别是深度相机退化模拟与高度图噪声）是真实世界泛化的关键。最容易踩坑的地方在于**对比损失 L_con 的 Sinkhorn 平衡分配实现**——若原型字典 C 的更新频率或温度系数 T_con 设置不当，可能导致 MoE 专家退化而非专业化。对于下游团队，建议先在小规模地形集（如仅间隙与踏脚石）上验证注视行为是否合理，再扩展到全地形训练；训练成本约 17 GPU 天（4×RTX 5090），若资源有限可考虑减少并行环境数至 4,000 并观察性能衰减。部署时注意执行器热管理——TAGA 的爆发性动作在长时间运行中可能触发过热保护，建议在真机测试中加入执行器温度监控与降级策略。

## 参考
- https://arxiv.org/abs/2606.05880
- https://marmotlab.github.io/taga-humanoid/
- https://mp.weixin.qq.com/s/Kx9myecE1Z0eGqOapoqQnA

## Overview

TAGA is an active perception framework for agile locomotion in humanoid robots, implemented by the research team in Isaac Lab simulation and on the Unitree G1 real robot. Its core contribution lies in combining an active gaze mechanism for "where to look" with multimodal perception fusion. Through differentiable gaze point prediction and cross-attention mechanisms, it achieves comparable or even superior generalization performance to full-image scanning while significantly reducing training costs.

## What It Changes

Existing perception-based locomotion methods face a dilemma: height-map-based approaches incur sharply rising computational costs as the perception range expands, while forward-looking depth-map methods can see far but miss critical terrain details near the feet. More fundamentally, these two perception paradigms—visual foresight and local precision from height maps—are inherently complementary, yet existing methods either rely on a single source or only loosely couple them, never truly addressing the active perception problem of "when and where to look." When humans and animals walk over complex terrain, their gaze actively shifts to task-relevant regions rather than uniformly scanning the entire field of view. TAGA changes this paradigm: instead of treating perception as passive information collection, it models perception as a learnable decision process, allowing the policy to autonomously determine which local terrain information is most critical for the next action.

The practical significance of this change is that it breaks the linear coupling between perception range and computational cost. By focusing attention on a compact region of the height map (a K=11 cropped patch), TAGA reduces the complexity of local geometry processing by nearly an order of magnitude while retaining forward-looking visual cues. More importantly, this active gaze mechanism is not a hand-designed heuristic rule but emerges automatically through end-to-end reinforcement learning—the policy learns where to look on different terrains, which is more adaptive than any fixed strategy.

## Method Breakdown

The overall framework of TAGA is a POMDP policy. The observation space consists of three parts: a forward-looking depth map d_t ∈ ℝ^{1×36×64}, an ego-centric height map h_t^{xyz} ∈ ℝ^{3×21×21}, and a 5-frame proprioceptive history p_t^H. The action space is 29-dimensional target joint positions, output at 50 Hz and tracked by a low-level PD controller at 200 Hz.

### Task-Relevant Active Gaze Module
- A lightweight prediction head f_roi receives the concatenation of visual embedding e_t^d and proprioceptive embedding e_t^p, and outputs a normalized 2D gaze position r_t ∈ [0,1]²
- Maps r_t to height map grid coordinates (u_t, v_t) = (⌊r_t^x M⌋, ⌊r_t^y M⌋), where M=21
- Crops a compact terrain patch h̃_t^{xyz} of size K=11 via differentiable bilinear grid sampling
- A boundary penalty L_roi prevents the gaze point from degrading to the height map boundary, with margin m=0.05

### Visual-Motor Fusion Encoder
- The cropped terrain patch is encoded via a lightweight CNN and a spatial coordinate MLP, producing a pointwise terrain embedding E_t^m ∈ ℝ^{K×K×128}, used as key and value
- A projection head f_proj generates the query from visual and proprioceptive embeddings
- Multi-head cross-attention fusion: e_t^{pg} = MHA(f_proj([e_t^p, e_t^d]), E_t^m, E_t^m), outputting ℝ¹²⁸

### Action Decoding and Training
- Mixture-of-Experts (MoE) module with N_e=5 experts; a gating network g(·) generates soft routing weights α_t^i = Softmax(g(e_t^π))_i, and the final action is a_t = Σ α_t^i E_i(e_t^π)
- Asymmetric actor-critic PPO training; the critic receives privileged information (true linear velocity, full height map)
- Two-stage training: Stage 1 runs 30k iterations of noise-free training to learn core skills; Stage 2 runs 10k iterations with reduced entropy coefficient and deployment-oriented domain randomization
- A contrastive loss L_con aligns MoE gating embeddings with full height map terrain embeddings in a shared prototype space via Sinkhorn balanced assignment, encouraging expert terrain specialization
- Symmetry augmentation exploits the left-right symmetry of humanoid robots by mirroring observations and actions and incorporating them into PPO updates

## Key Innovations

**Active gaze as a learnable decision**: "Where to look" is modeled as an end-to-end differentiable prediction head rather than hand-crafted rules or fixed strategies. This is the first time an active perception mechanism is jointly optimized with a reinforcement learning policy, making the gaze behavior itself part of the policy. Its importance lies in the policy learning to adaptively allocate perception resources across different terrains—gazing at footholds during gap crossing and at the next support surface on sparse stepping stones.

**Cropped attention replacing full-image processing**: A compact 11×11 patch is cropped from the 21×21 height map via differentiable bilinear sampling, reducing the input scale of local geometry processing by approximately 73%. This not only lowers computational cost but, more importantly, forces the policy to focus on task-relevant regions through an information bottleneck, avoiding information redundancy and overfitting in full-image processing. Experiments show that this cropped attention performs comparably to full-image scanning on OOD terrains while reducing training cost by 65.2%.

**Synergy between MoE and terrain contrastive learning**: A contrastive loss aligns MoE gating embeddings with terrain embeddings from the full height map in a shared prototype space, causing expert networks to spontaneously specialize to different terrain types. This resolves the common expert degeneration problem of MoE in reinforcement learning while providing the policy with terrain-aware modular decision-making capabilities.

## Experiments and Results

Simulation evaluation was conducted in Isaac Lab with 8,000 parallel environments on 4 RTX 5090 GPUs, with a total training time of approximately 17 GPU-days. Each terrain was evaluated over 1,000 trials.

| Method | Gaps | Stepping Stones | Beam | High Platform | Terrain C1 | Terrain C2 | Training Cost |
|------|------|-----------------|------|---------------|------------|------------|----------|
| CReF | 97.40% | 52.30% | 96.50% | 98.70% | 85.20% | 43.10% | ~10 GPU-days |
| TAGA-HSOnly | 93.10% | 92.50% | 98.30% | 99.60% | 90.50% | 91.50% | ~17 GPU-days |
| TAGA-InactiveGaze | 57.10% | 83.20% | 95.60% | 100.00% | 72.70% | 48.80% | ~14 GPU-days |
| TAGA-FullScan | 99.50% | 98.00% | 97.50% | 100.00% | 93.40% | 92.50% | ~49 GPU-days |
| TAGA-NoAMP | 96.40% | 97.20% | 97.60% | 99.50% | 89.80% | 91.60% | ~16 GPU-days |
| **TAGA (Ours)** | **98.30%** | **97.90%** | **98.50%** | **100.00%** | **93.70%** | **93.90%** | **~17 GPU-days** |

Key comparative conclusions:
- TAGA achieves success rates of 93.70% and 93.90% on OOD terrains C1 and C2, significantly outperforming CReF (85.20%/43.10%) and TAGA-InactiveGaze (72.70%/48.80%)
- TAGA performs comparably to TAGA-FullScan but with a 65.2% reduction in training cost (17 vs. 49 GPU-days)
- TAGA-InactiveGaze achieves only 57.10% on Gaps, demonstrating that the active gaze mechanism is critical for gap crossing
- Real-world evaluation: the Unitree G1 successfully crossed a 120 cm gap (the best baseline achieved 90 cm), navigated sparse stepping stones with 50–70 cm spacing and 10 cm height variations, and remained stable outdoors, under low-light conditions, and under kicks and pushes

## Boundaries and Limitations

The authors explicitly acknowledge the actuator thermal load issue: the dynamic actions implemented by TAGA (explosive jumps, precise footholds, dynamic balance recovery) impose high thermal loads on actuators during prolonged operation, which may lead to gradually degrading control precision, manifesting as shortened jump distances or missed footholds. Additionally, the method relies heavily on height map quality—on complex terrain, poor height map reconstruction directly leads to improper gaits or failures. The paper does not explicitly mention the data volume (the specific scale of AMP reference data) or transferability to different robot platforms. Notably, the baseline methods in the real-world evaluation (HPL, PIM, CMoE, etc.) were not reproduced under identical experimental conditions but rather cited from their respective papers' best results; therefore, cross-method comparisons should be interpreted with caution.

## Engineering Insights

When reproducing TAGA, the first things to verify are **the initialization of the gaze prediction head and the weight of the boundary penalty**—from the ablation experiments, TAGA-InactiveGaze (fixed gaze point) drops sharply to 57.10% on Gaps, indicating that the gaze mechanism is crucial for gap-type terrains, but an overly strong boundary penalty may cause the gaze point to converge prematurely to the central region, weakening adaptability. Second, **the two-stage training strategy cannot be skipped**: the noise-free training in Stage 1 establishes core skills, and the domain randomization fine-tuning in Stage 2 (especially depth camera degradation simulation and height map noise) is key to real-world generalization. The most likely pitfall lies in **the Sinkhorn balanced assignment implementation of the contrastive loss L_con**—if the update frequency of the prototype dictionary C or the temperature coefficient T_con is set improperly, MoE experts may degenerate rather than specialize. For downstream teams, it is recommended to first validate gaze behavior on a small-scale terrain set (e.g., only gaps and stepping stones) before scaling to full-terrain training; the training cost is approximately 17 GPU-days (4×RTX 5090), and if resources are limited, consider reducing the number of parallel environments to 4,000 and observing performance degradation. During deployment, pay attention to actuator thermal management—TAGA's explosive actions may trigger overheating protection during prolonged operation; it is recommended to include actuator temperature monitoring and degradation strategies in real-robot testing.

## 개요

TAGA는 인간형 로봇의 민첩한 보행(locomotion)을 위한 능동적 지각(active perception) 프레임워크로, 연구팀이 Isaac Lab 시뮬레이션과 Unitree G1 실물 로봇에서 구현했습니다. 핵심 기여는 "어디를 볼지"에 대한 능동적 주시 메커니즘과 다중 모달 지각 융합을 결합한 것으로, 미분 가능한 주시점 예측과 교차 주의 메커니즘을 통해 훈련 비용을 크게 줄이면서도 전체 이미지 스캔과 동등하거나 더 나은 일반화 성능을 달성했습니다.

## 무엇을 바꾸었는가

기존의 지각 기반 보행 방법은 딜레마에 빠져 있습니다: 높이 맵 기반 방법은 지각 범위를 넓힐 때 계산 비용이 급격히 증가하고, 전방 깊이 맵 기반 방법은 멀리 볼 수 있지만 발 근처의 중요한 지형 세부 정보를 놓칩니다. 더 근본적인 문제는 이 두 지각 패러다임(시각적 원시성과 높이 맵의 국소적 정밀도)이 본질적으로 상호 보완적이지만, 기존 방법은 단일 소스에 의존하거나 단순히 느슨하게 결합할 뿐 "언제 어디를 볼지"라는 능동적 지각 문제를 진정으로 해결하지 못한다는 점입니다. 인간과 동물은 복잡한 지형을 걸을 때 시선을 작업 관련 영역에 능동적으로 향하게 하며, 전체 시야를 균일하게 스캔하지 않습니다. TAGA는 이 패러다임을 바꿉니다: 지각을 수동적 정보 수집으로 보지 않고 학습 가능한 의사 결정 과정으로 모델링하여, 정책이 다음 동작에 어떤 국소 지형 정보가 가장 중요한지 스스로 결정하게 합니다.

이 변화의 실질적 의미는 지각 범위와 계산 비용 사이의 선형적 결합을 깨뜨린다는 점입니다. 높이 맵의 컴팩트한 영역(K=11 크롭 블록)에 주의를 집중함으로써, TAGA는 전방 시각 단서를 유지하면서 국소 기하 처리를 위한 복잡도를 약 한 자릿수 줄입니다. 더 중요한 것은, 이 능동적 주시 메커니즘은 수작업으로 설계된 휴리스틱 규칙이 아니라 엔드투엔드 강화 학습을 통해 자동으로 출현한다는 점입니다. 정책은 다양한 지형에서 어디를 주시해야 하는지 학습하며, 이는 어떤 고정 전략보다 적응력이 뛰어납니다.

## 방법 분해

TAGA의 전체 프레임워크는 POMDP 정책이며, 관측 공간은 세 부분으로 구성됩니다: 전방 깊이 맵 d_t ∈ ℝ^{1×36×64}, 자기 중심 높이 맵 h_t^{xyz} ∈ ℝ^{3×21×21}, 그리고 5프레임 고유 수용 감각 이력 p_t^H. 행동 공간은 29차원 목표 관절 위치로 50 Hz로 출력되며, 200 Hz의 저수준 PD 컨트롤러가 추적합니다.

### 작업 관련 능동 주시 모듈
- 경량 예측 헤드 f_roi는 시각 임베딩 e_t^d와 고유 수용 감각 임베딩 e_t^p의 연결을 입력받아 정규화된 2D 주시 위치 r_t ∈ [0,1]²를 출력
- r_t를 높이 맵 그리드 좌표 (u_t, v_t) = (⌊r_t^x M⌋, ⌊r_t^y M⌋)로 매핑, 여기서 M=21
- 미분 가능한 이중선형 그리드 샘플링을 통해 K=11의 컴팩트한 지형 블록 h̃_t^{xyz}를 크롭
- 경계 페널티 L_roi는 주시점이 높이 맵 경계로 퇴화하는 것을 방지, 마진 m=0.05

### 시각-운동 융합 인코더
- 크롭된 지형 블록은 경량 CNN과 공간 좌표 MLP로 인코딩되어 점별 지형 임베딩 E_t^m ∈ ℝ^{K×K×128}을 생성, key와 value로 사용
- 프로젝션 헤드 f_proj는 시각 및 고유 수용 감각 임베딩에서 query를 생성
- 멀티헤드 교차 주의 융합: e_t^{pg} = MHA(f_proj([e_t^p, e_t^d]), E_t^m, E_t^m), 출력 ℝ¹²⁸

### 행동 디코딩 및 훈련
- 혼합 전문가(MoE) 모듈, N_e=5개의 전문가, 게이팅 네트워크 g(·)가 소프트 라우팅 가중치 α_t^i = Softmax(g(e_t^π))_i를 생성, 최종 행동 a_t = Σ α_t^i E_i(e_t^π)
- 비대칭 actor-critic PPO 훈련, critic은 특권 정보(실제 선속도, 전체 높이 맵)를 수신
- 2단계 훈련: 1단계 30k 반복은 노이즈 없이 핵심 기술을 훈련, 2단계 10k 반복은 엔트로피 계수를 낮추고 배포 지향 도메인 무작위화 도입
- 대비 손실 L_con은 Sinkhorn 균형 할당을 통해 MoE 게이팅 임베딩과 전체 높이 맵 지형 임베딩을 공유 프로토타입 공간에서 정렬, 전문가의 지형 전문화를 장려
- 대칭 증강은 인간형 로봇의 좌우 대칭성을 활용하여 관측과 행동을 미러링하고 PPO 업데이트에 포함

## 핵심 혁신

**학습 가능한 결정으로서의 능동 주시**: "어디를 볼지"를 수작업 규칙이나 고정 전략이 아닌 엔드투엔드 미분 가능한 예측 헤드로 모델링합니다. 이는 능동 지각 메커니즘과 강화 학습 정책을 공동으로 최적화한 최초의 사례로, 주시 행동 자체가 정책의 일부가 됩니다. 그 중요성은 정책이 다양한 지형에서 지각 자원을 적응적으로 할당하는 법을 학습한다는 점입니다—간격을 넘을 때 착지점을 주시하고, 흩어진 디딤돌에서 다음 지지면을 주시합니다.

**전체 이미지 처리를 대체하는 크롭 기반 주의**: 미분 가능한 이중선형 샘플링을 통해 21×21 높이 맵에서 11×11 컴팩트 블록을 크롭하여 국소 기하 처리의 입력 규모를 약 73% 줄입니다. 이는 계산 비용을 낮출 뿐만 아니라, 더 중요하게는 정보 병목을 통해 정책이 작업 관련 영역에 집중하도록 강제하여 전체 이미지 처리에서의 정보 중복과 과적합을 피합니다. 실험 결과, 이러한 크롭 기반 주의는 OOD 지형에서 전체 이미지 스캔과 동등한 성능을 보이면서 훈련 비용은 65.2% 절감합니다.

**MoE와 지형 대비 학습의 시너지**: 대비 손실을 통해 MoE 게이팅 임베딩과 전체 높이 맵의 지형 임베딩을 공유 프로토타입 공간에서 정렬함으로써, 전문가 네트워크가 서로 다른 지형 유형에 자발적으로 특화되도록 합니다. 이는 강화 학습에서 MoE가 흔히 겪는 전문가 퇴화 문제를 해결하면서, 정책에 지형 인식 모듈식 의사 결정 능력을 제공합니다.

## 실험 및 결과

시뮬레이션 평가는 Isaac Lab에서 수행되었으며, 8,000개의 병렬 환경, 4개의 RTX 5090 GPU, 총 훈련 시간 약 17 GPU-일. 각 지형에서 1,000회 시행 평가.

| 방법 | Gaps | Stepping Stones | Beam | High Platform | Terrain C1 | Terrain C2 | 훈련 비용 |
|------|------|-----------------|------|---------------|------------|------------|----------|
| CReF | 97.40% | 52.30% | 96.50% | 98.70% | 85.20% | 43.10% | ~10 GPU-일 |
| TAGA-HSOnly | 93.10% | 92.50% | 98.30% | 99.60% | 90.50% | 91.50% | ~17 GPU-일 |
| TAGA-InactiveGaze | 57.10% | 83.20% | 95.60% | 100.00% | 72.70% | 48.80% | ~14 GPU-일 |
| TAGA-FullScan | 99.50% | 98.00% | 97.50% | 100.00% | 93.40% | 92.50% | ~49 GPU-일 |
| TAGA-NoAMP | 96.40% | 97.20% | 97.60% | 99.50% | 89.80% | 91.60% | ~16 GPU-일 |
| **TAGA (Ours)** | **98.30%** | **97.90%** | **98.50%** | **100.00%** | **93.70%** | **93.90%** | **~17 GPU-일** |

핵심 비교 결론:
- TAGA는 OOD 지형 C1, C2에서 성공률 93.70%와 93.90%로, CReF(85.20%/43.10%)와 TAGA-InactiveGaze(72.70%/48.80%)를 크게 능가
- TAGA는 TAGA-FullScan과 성능이 동등하지만 훈련 비용은 65.2% 절감(17 vs 49 GPU-일)
- TAGA-InactiveGaze는 Gaps에서 57.10%에 불과, 능동 주시 메커니즘이 간격 넘기에 필수적임을 증명
- 실제 세계 평가: Unitree G1이 120 cm 간격을 성공적으로 넘음(비교 방법 최고 90 cm), 흩어진 디딤돌 간격 50–70 cm 및 높이 변화 10 cm, 야외, 저조도, 발차기·밀치기 상황에서도 안정성 유지

## 경계 및 한계

저자는 액추에이터 열 부하 문제를 명시적으로 인정합니다: TAGA가 구현하는 동적 동작(폭발적 점프, 정밀 착지, 동적 균형 회복)은 장시간 실행 시 액추에이터에 높은 열 부하를 발생시켜 제어 정밀도가 점진적으로 저하될 수 있으며, 점프 거리 단축이나 착지점 놓침으로 나타날 수 있습니다. 또한, 이 방법은 높이 맵 품질에 크게 의존합니다—복잡한 지형에서 높이 맵 재구성 품질이 낮으면 보행 자세 오류나 실패로 직접 이어집니다. 논문은 데이터 양(AMP 참조 데이터의 구체적 규모)과 다른 로봇 플랫폼으로의 이전 능력에 대해 명시적으로 언급하지 않습니다. 주목할 점은 실제 세계 평가의 비교 방법(HPL, PIM, CMoE 등)이 동일한 실험 조건에서 재현된 것이 아니라 각 논문의 최상의 결과를 인용한 것이므로, 방법 간 비교는 신중하게 해석해야 합니다.

## 공학적 시사점

TAGA를 재현할 때 먼저 확인해야 할 것은 **주시 예측 헤드의 초기화와 경계 페널티의 가중치**입니다—절제 실험에서 TAGA-InactiveGaze(고정 주시점)는 Gaps에서 성능이 57.10%로 급락하여 주시 메커니즘이 간격 유형 지형에 필수적임을 보여주지만, 경계 페널티가 너무 강하면 주시점이 중앙 영역으로 조기 수렴하여 적응 능력이 약화될 수 있습니다. 둘째, **2단계 훈련 전략은 건너뛸 수 없습니다**: 1단계의 노이즈 없는 훈련은 핵심 기술을 구축하고, 2단계의 도메인 무작위화 미세 조정(특히 깊이 카메라 열화 시뮬레이션과 높이 맵 노이즈)은 실제 세계 일반화의 핵심입니다. 가장 함정에 빠지기 쉬운 부분은 **대비 손실 L_con의 Sinkhorn 균형 할당 구현**입니다—프로토타입 사전 C의 업데이트 빈도나 온도 계수 T_con 설정이 부적절하면 MoE 전문가가 전문화가 아닌 퇴화로 이어질 수 있습니다. 하류 팀에게는 소규모 지형 세트(예: 간격과 디딤돌만)에서 주시 행동이 합리적인지 먼저 검증한 후 전체 지형 훈련으로 확장할 것을 권장합니다. 훈련 비용은 약 17 GPU-일(4×RTX 5090)이며, 자원이 제한된 경우 병렬 환경 수를 4,000으로 줄이고 성능 저하를 관찰할 수 있습니다. 배포 시 액추에이터 열 관리에 주의하세요—TAGA의 폭발적 동작은 장시간 실행 시 과열 보호를 유발할 수 있으므로, 실물 테스트에 액추에이터 온도 모니터링과 성능 저하 전략을 포함하는 것이 좋습니다.
