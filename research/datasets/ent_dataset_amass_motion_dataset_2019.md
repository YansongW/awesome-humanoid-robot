---
$id: ent_dataset_amass_motion_dataset_2019
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: dataset
names:
  en: AMASS Motion Dataset
  zh: AMASS 动作数据集
  ko: AMASS 모션 데이터셋
summary:
  en: Large-scale clean motion-capture dataset of human body shapes and poses, widely used for humanoid sim pre-training.
  zh: 大规模干净的动作捕捉数据集，包含人体形状和姿态，广泛用于人形机器人仿真预训练。
  ko: 인체 형태와 자세를 포함한 대규모 클린 모션 캡처 데이터셋, 휨로봇 시뮬레이션 사전 학습에 널리 사용.
domains:
- 09_data_datasets
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- amass
- dataset
- human_motion
- motion_capture
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-13]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b5): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: AMASS Motion Dataset
  url: https://amass.is.tue.mpg.de/
  date: '2019'
  accessed_at: '2026-07-02'
---
## 概述

AMASS（Archive of Motion Capture as Surface Shapes）是一个大规模、干净的人体动作捕捉数据集，通过统一参数化框架整合了 15 个不同的光学标记点动捕数据库，包含超过 40 小时、覆盖 300 多名受试者、超过 11000 条运动序列的全身姿态与形状数据。它真正改变的不是"动作捕捉"本身，而是让人类运动数据从零散、异构的孤岛变成了可规模化训练深度模型的统一原料——这是人形机器人全身运动策略仿真预训练的事实标准来源。

## 核心内容

### 是什么：准确定义

AMASS 动作数据集属于 **数据集** 类型，所属领域包括数据与数据集（`09_data_datasets`）、AI 模型与算法（`07_ai_models_algorithms`），价值链层级为智能层。其全称为 **Archive of Motion Capture as Surface Shapes**，由 Naureen Mahmood、Nima Ghorbani、Nikolaus F. Troje、Gerard Pons-Moll 与 Michael J. Black 于 ICCV 2019 发表。

AMASS 的核心贡献在于：**将 15 个不同光学标记点动捕数据集统一到同一个参数化框架（SMPL 人体模型）中**。此前每个动捕数据集使用不同的身体参数化方式，难以整合为单一元数据集；AMASS 通过新方法 MoSh++ 将动捕数据转换为由 SMPL 表示的逼真 3D 人体网格，该方法适用于任意标记点配置，同时恢复软组织动力学与逼真的手部运动。

### 为什么存在：痛点与历史定位

在 AMASS 出现之前，深度学习驱动的计算机视觉研究依赖大规模数据集，而现有的人体动捕数据集普遍存在两个结构性缺陷：

1. **规模小、动作受限**：单个动捕数据集通常只有数小时数据，动作类型单一，难以支撑学习通用人体运动模型。
2. **参数化不统一**：不同数据集使用不同的身体模型与标记点协议，彼此之间无法直接合并训练。

AMASS 的历史定位正是**"统一者"**——它不采集新数据，而是把已有的 15 个光学动捕数据集"翻译"成同一种语言（SMPL 参数），从而让数据规模产生质变。这种"整合优于采集"的思路，与 ARIO 全机器人合一数据集提出的统一数据标准一脉相承，但 AMASS 更早、更成熟，且专注于人体而非机器人本体。

### 原理拆解

**① MoSh++：从标记点到 SMPL 网格**

MoSh++ 是 AMASS 的核心技术方法。其输入是光学动捕系统输出的标记点三维轨迹，输出是 SMPL 人体模型的参数（姿态参数 θ 与形状参数 β）。SMPL 提供标准骨骼表示与完全绑定的表面网格，其数学形式可概括为：

$$
M(\beta, \theta) = W(T(\beta, \theta), J(\beta), \theta, \mathcal{W})
$$

其中 $T(\beta, \theta)$ 为根据形状与姿态变形后的模板网格，$J(\beta)$ 为关节位置，$W$ 为线性混合蒙皮函数，$\mathcal{W}$ 为皮肤权重。MoSh++ 的关键创新在于：它不要求固定标记点配置，而是通过优化同时拟合形状、姿态与软组织形变，从而兼容来自不同动捕实验室的异构标记点协议。

**② 统一参数化的数据工程价值**

统一参数化带来的直接收益是**数据可拼接**。15 个数据集在 SMPL 参数空间中对齐后，可以合并为单一训练集，无需担心特征空间不一致。这种"先统一、再训练"的管线，正是 AMASS 作为"干净"数据集的含义——干净不是指无噪声，而是指**格式一致、可直接消费**。

### 关键参数与规格

| 参数 | 数值 | 说明 |
|------|------|------|
| 整合数据集数量 | 15 个 | 不同光学标记点动捕数据集 |
| 运动数据总时长 | 超过 40 小时 | 显著超过此前任何人体运动数据集 |
| 受试者数量 | 超过 300 人 | 覆盖广泛的人群多样性 |
| 运动序列数量 | 超过 11000 条 | 每条为一次完整动作记录 |
| 人体模型 | SMPL | 标准骨骼表示 + 完全绑定的表面网格 |
| 发表会议 | ICCV 2019 | 论文页码 5442–5451 |

（以上数字均来自 AMASS 官方论文摘要与引用信息）

### 横向对比

| 数据集 | 数据类型 | 规模 | 统一参数化 | 人形机器人适用性 |
|--------|---------|------|-----------|-----------------|
| AMASS | 光学动捕人体运动 | 40+ 小时，11000+ 序列 | 是（SMPL） | 高：全身运动策略预训练 |
| Ego4D | 第一视角视频 | 3000+ 小时 | 否 | 中：手-物交互与第一人称技能 |
| Epic-Kitchens | 第一视角厨房视频 | 需自行确认 | 否 | 中：操作序列理解 |
| HumanPlus Shadowing | 机器人影子跟随数据 | 需自行确认 | 否 | 高：全身模仿与遥操作 |

AMASS 与 HumanPlus Shadowing 的差异值得注意：AMASS 是**纯人体数据**，不含机器人本体状态；HumanPlus 则把人体演示与机器人全身状态绑定。二者在训练管线中互补——AMASS 提供大规模运动先验，HumanPlus 提供具身对齐数据。

### 谁在用·应用案例

**① 人形机器人仿真预训练**

在人形机器人领域，AMASS 的标准用法是：**先在 AMASS 上训练低层全身跟踪策略，再迁移到真实机器人**。这是因为 AMASS 提供的是纯人体运动数据，不包含机器人动力学约束；策略先在仿真中学会"人体应该怎么动"，再通过域随机化与重定向适配到具体机器人平台。

**② 动画与可视化**

AMASS 官方定位即"可直接用于动画、可视化与生成深度学习训练数据"。其 SMPL 参数化格式天然适配图形学管线，可用于生成角色动画或作为数据增强工具。

**③ 跨本体正迁移研究**

AMASS 作为大规模人类数据源，是验证"用人类数据学习任务语义、用机器人数据解决具身落地"这一跨数据量级鸿沟路径的关键资源。其百万小时级的人类数据规模（工程判断：40 小时为动捕数据，若计入衍生增强数据则规模更大）为学习运动先验提供了充足原料。

### 局限与边界

1. **缺乏物体交互与力觉信息**：AMASS 是纯运动学数据，不包含操作类任务所需的物体交互与力觉信息。因此它通常与遥操作数据互补使用，而非相互替代。
2. **光学动捕的采集约束**：AMASS 依赖光学标记点动捕，需要专业场地与设备维护标定，且难以覆盖野外场景。
3. **无机器人本体状态**：AMASS 不包含机器人关节力矩、电机电流等本体感受数据，无法直接用于训练需要力控的策略。
4. **SMPL 模型的手部细节**：虽然 MoSh++ 恢复逼真手部运动，但 SMPL 的手部自由度表达仍不如专用手部模型精细（工程判断）。

### 常见误区

1. **"AMASS 是机器人数据集"**——错。AMASS 是纯人体运动数据集，不包含任何机器人本体数据。它用于人形机器人训练的前提是"先在仿真中预训练、再迁移"的管线设计。
2. **"AMASS 数据可以直接用于真实机器人"**——错。AMASS 数据缺少机器人动力学约束，直接部署会导致策略无视关节限位与力矩限制。必须经过重定向与仿真适配。
3. **"AMASS 与 HumanPlus 是竞争关系"**——错。二者互补：AMASS 提供大规模运动先验，HumanPlus 提供人体-机器人状态绑定数据。实际训练管线常同时使用。
4. **"数据集就是基准"**——错。数据集提供训练原料；基准（benchmark）额外规定任务划分、评估协议与指标。AMASS 是数据集，不是基准。

### 相关知识

- `ent_software_mujoco_physics_engine_2022` — MuJoCo 是 AMASS 预训练策略常用的仿真环境，提供高保真接触动力学。
- `ent_technology_optitrack_motion_capture_2024` — OptiTrack 是 AMASS 所整合的光学动捕数据的典型采集设备，实现亚毫米级追踪。
- `ent_dataset_ego4d_egocentric_video_dataset_2022` — Ego4D 与 AMASS 同属"人类侧"数据资源，但提供第一视角视频而非动捕数据。
- `ent_technology_humanplus_shadowing_system_2024` — HumanPlus 与 AMASS 互补，提供人体-机器人状态绑定的影子跟随数据。
- `ent_dataset_epic_kitchens_dataset_2018` — Epic-Kitchens 与 AMASS 同属人类数据资源，聚焦第一视角厨房交互视频。

## 参考

- [AMASS Motion Dataset](https://amass.is.tue.mpg.de/)
- [Chapter 21: 数据与数据集](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-21.md)
- [Chapter 02: 实体类型与领域](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)

## Overview

AMASS (Archive of Motion Capture as Surface Shapes) is a large-scale, clean human motion capture dataset that integrates 15 different optical marker-based motion capture databases through a unified parameterization framework, containing over 40 hours of full-body pose and shape data from more than 300 subjects and over 11,000 motion sequences. What it truly changes is not "motion capture" itself, but rather transforming human motion data from scattered, heterogeneous silos into unified raw material that can be used to train deep models at scale—this is the de facto standard source for simulation-based pretraining of full-body motion policies for humanoid robots.

## Content

### What It Is: Precise Definition

The AMASS motion dataset belongs to the **dataset** type, with domains including Data and Datasets (`09_data_datasets`) and AI Models and Algorithms (`07_ai_models_algorithms`), positioned at the intelligence layer of the value chain. Its full name is **Archive of Motion Capture as Surface Shapes**, published by Naureen Mahmood, Nima Ghorbani, Nikolaus F. Troje, Gerard Pons-Moll, and Michael J. Black at ICCV 2019.

The core contribution of AMASS is: **unifying 15 different optical marker-based motion capture datasets into a single parameterized framework (the SMPL human body model)**. Previously, each motion capture dataset used different body parameterizations, making it difficult to integrate them into a single meta-dataset; AMASS uses the new method MoSh++ to convert motion capture data into realistic 3D human meshes represented by SMPL, a method that works with arbitrary marker configurations while recovering soft-tissue dynamics and realistic hand motion.

### Why It Exists: Pain Points and Historical Positioning

Before AMASS, deep learning-driven computer vision research relied on large-scale datasets, yet existing human motion capture datasets generally suffered from two structural deficiencies:

1. **Small scale and limited actions**: Individual motion capture datasets typically contain only a few hours of data with limited action types, insufficient for learning general human motion models.
2. **Non-uniform parameterization**: Different datasets use different body models and marker protocols, making them impossible to directly merge for training.

AMASS's historical positioning is precisely that of a **"unifier"**—it does not collect new data but rather "translates" 15 existing optical motion capture datasets into the same language (SMPL parameters), thereby achieving a qualitative leap in data scale. This "integration over collection" approach aligns with the unified data standards proposed by the ARIO all-robot unified dataset, but AMASS is earlier, more mature, and focused on humans rather than robot bodies.

### Principle Breakdown

**① MoSh++: From Markers to SMPL Meshes**

MoSh++ is the core technical method of AMASS. Its input is the 3D marker trajectory output by optical motion capture systems, and its output is the parameters of the SMPL human body model (pose parameters θ and shape parameters β). SMPL provides a standard skeletal representation with a fully registered surface mesh, whose mathematical form can be summarized as:

$$
M(\beta, \theta) = W(T(\beta, \theta), J(\beta), \theta, \mathcal{W})
$$

where $T(\beta, \theta)$ is the template mesh deformed by shape and pose, $J(\beta)$ is the joint locations, $W$ is the linear blend skinning function, and $\mathcal{W}$ is the skin weights. The key innovation of MoSh++ is that it does not require a fixed marker configuration; instead, it simultaneously fits shape, pose, and soft-tissue deformation through optimization, thereby accommodating heterogeneous marker protocols from different motion capture laboratories.

**② The Data Engineering Value of Unified Parameterization**

The direct benefit of unified parameterization is **data composability**. After the 15 datasets are aligned in SMPL parameter space, they can be merged into a single training set without concerns about inconsistent feature spaces. This "unify first, then train" pipeline is precisely what makes AMASS a "clean" dataset—clean does not mean noise-free, but rather **format-consistent and directly consumable**.

### Key Parameters and Specifications

| Parameter | Value | Description |
|-----------|-------|-------------|
| Number of integrated datasets | 15 | Different optical marker-based motion capture datasets |
| Total motion data duration | Over 40 hours | Significantly exceeds any previous human motion dataset |
| Number of subjects | Over 300 | Covers broad population diversity |
| Number of motion sequences | Over 11,000 | Each is a complete action recording |
| Human body model | SMPL | Standard skeletal representation + fully registered surface mesh |
| Publication venue | ICCV 2019 | Paper pages 5442–5451 |

(All figures above are from the AMASS official paper abstract and citation information)

### Horizontal Comparison

| Dataset | Data Type | Scale | Unified Parameterization | Humanoid Robot Suitability |
|---------|-----------|-------|--------------------------|----------------------------|
| AMASS | Optical motion capture human motion | 40+ hours, 11,000+ sequences | Yes (SMPL) | High: full-body motion policy pretraining |
| Ego4D | First-person video | 3,000+ hours | No | Medium: hand-object interaction and first-person skills |
| Epic-Kitchens | First-person kitchen video | To be confirmed | No | Medium: manipulation sequence understanding |
| HumanPlus Shadowing | Robot shadowing data | To be confirmed | No | High: full-body imitation and teleoperation |

The difference between AMASS and HumanPlus Shadowing is worth noting: AMASS is **purely human data**, containing no robot body states; HumanPlus, by contrast, binds human demonstrations with full robot body states. The two are complementary in the training pipeline—AMASS provides large-scale motion priors, while HumanPlus provides embodiment-aligned data.

### Who Uses It: Application Cases

**① Humanoid Robot Simulation Pretraining**

In the humanoid robot domain, the standard usage of AMASS is: **first train low-level full-body tracking policies on AMASS, then transfer to real robots**. This is because AMASS provides purely human motion data without robot dynamics constraints; the policy first learns "how the human body should move" in simulation, then adapts to specific robot platforms through domain randomization and retargeting.

**② Animation and Visualization**

The official positioning of AMASS is "directly usable for animation, visualization, and generating deep learning training data." Its SMPL parameterized format naturally fits graphics pipelines and can be used to generate character animations or serve as a data augmentation tool.

**③ Cross-Embodiment Positive Transfer Research**

As a large-scale human data source, AMASS is a key resource for validating the path of "learning task semantics from human data and solving embodiment deployment with robot data" across the data scale gap. Its million-hour-scale human data volume (engineering judgment: 40 hours is motion capture data; if derived augmented data is included, the scale is larger) provides ample raw material for learning motion priors.

### Limitations and Boundaries

1. **Lack of object interaction and force feedback information**: AMASS is purely kinematic data and does not contain object interaction or force feedback information required for manipulation tasks. It is therefore typically used complementarily with teleoperation data rather than as a substitute.
2. **Acquisition constraints of optical motion capture**: AMASS relies on optical marker-based motion capture, requiring professional studio space and equipment maintenance for calibration, and it is difficult to cover outdoor scenarios.
3. **No robot body states**: AMASS does not contain proprioceptive data such as joint torques or motor currents, making it impossible to directly train policies requiring force control.
4. **Hand details of the SMPL model**: Although MoSh++ recovers realistic hand motion, the hand DOF expression of SMPL is still less refined than dedicated hand models (engineering judgment).

### Common Misconceptions

1. **"AMASS is a robot dataset"**—Wrong. AMASS is a purely human motion dataset containing no robot body data. Its use in humanoid robot training presupposes a "pretrain in simulation, then transfer" pipeline design.
2. **"AMASS data can be directly used on real robots"**—Wrong. AMASS data lacks robot dynamics constraints; direct deployment would cause policies to ignore joint limits and torque limits. Retargeting and simulation adaptation are mandatory.
3. **"AMASS and HumanPlus are competitors"**—Wrong. They are complementary: AMASS provides large-scale motion priors, while HumanPlus provides human-robot state binding data. Real training pipelines often use both simultaneously.
4. **"A dataset is a benchmark"**—Wrong. Datasets provide training raw material; benchmarks additionally specify task splits, evaluation protocols, and metrics. AMASS is a dataset, not a benchmark.

### Related Knowledge

- `ent_software_mujoco_physics_engine_2022` — MuJoCo is a commonly used simulation environment for AMASS pretrained policies, providing high-fidelity contact dynamics.
- `ent_technology_optitrack_motion_capture_2024` — OptiTrack is a typical acquisition device for the optical motion capture data integrated in AMASS, achieving sub-millimeter tracking.
- `ent_dataset_ego4d_egocentric_video_dataset_2022` — Ego4D and AMASS both belong to "human-side" data resources, but Ego4D provides first-person video rather than motion capture data.
- `ent_technology_humanplus_shadowing_system_2024` — HumanPlus complements AMASS by providing shadowing data with human-robot state binding.
- `ent_dataset_epic_kitchens_dataset_2018` — Epic-Kitchens and AMASS both belong to human data resources, focusing on first-person kitchen interaction videos.

## 개요

AMASS(Archive of Motion Capture as Surface Shapes)는 대규모의 깨끗한 인체 모션 캡처 데이터셋으로, 통합 파라미터화 프레임워크를 통해 15개의 서로 다른 광학 마커 모션 캡처 데이터베이스를 통합하며, 40시간 이상, 300명 이상의 피험자, 11,000개 이상의 모션 시퀀스에 걸친 전신 자세 및 형태 데이터를 포함합니다. 이 데이터셋이 진정으로 바꾼 것은 "모션 캡처" 자체가 아니라, 인간 모션 데이터를 파편화되고 이질적인 고립된 데이터에서 대규모 딥러닝 모델 훈련이 가능한 통합 원자재로 만든 것입니다. 이는 휴머노이드 로봇 전신 모션 전략 시뮬레이션 사전 훈련의 사실상 표준 데이터 소스입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

AMASS 모션 데이터셋은 **데이터셋** 유형에 속하며, 관련 분야는 데이터 및 데이터셋(`09_data_datasets`), AI 모델 및 알고리즘(`07_ai_models_algorithms`)이며, 가치 사슬 계층은 지능 계층입니다. 전체 명칭은 **Archive of Motion Capture as Surface Shapes**이며, Naureen Mahmood, Nima Ghorbani, Nikolaus F. Troje, Gerard Pons-Moll 및 Michael J. Black이 ICCV 2019에서 발표했습니다.

AMASS의 핵심 기여는 다음과 같습니다: **15개의 서로 다른 광학 마커 모션 캡처 데이터셋을 동일한 파라미터화 프레임워크(SMPL 인체 모델)로 통합한 것**입니다. 이전에는 각 모션 캡처 데이터셋이 서로 다른 신체 파라미터화 방식을 사용하여 단일 메타데이터셋으로 통합하기 어려웠습니다. AMASS는 새로운 방법인 MoSh++를 통해 모션 캡처 데이터를 SMPL로 표현된 사실적인 3D 인체 메시로 변환하며, 이 방법은 임의의 마커 구성에 적용 가능하고 연조직 역학과 사실적인 손 움직임을 동시에 복원합니다.

### 왜 존재하는가: 문제점과 역사적 위치

AMASS가 등장하기 전, 딥러닝 기반 컴퓨터 비전 연구는 대규모 데이터셋에 의존했지만, 기존 인체 모션 캡처 데이터셋은 일반적으로 두 가지 구조적 결함을 가지고 있었습니다:

1. **규모가 작고 동작이 제한적**: 개별 모션 캡처 데이터셋은 일반적으로 수 시간의 데이터만 있으며, 동작 유형이 단일하여 일반적인 인체 모션 모델 학습을 지원하기 어렵습니다.
2. **파라미터화 불일치**: 서로 다른 데이터셋은 서로 다른 신체 모델과 마커 프로토콜을 사용하여 직접 병합 훈련이 불가능합니다.

AMASS의 역사적 위치는 바로 **"통합자"**입니다. 새로운 데이터를 수집하는 것이 아니라 기존의 15개 광학 모션 캡처 데이터셋을 동일한 언어(SMPL 파라미터)로 "번역"하여 데이터 규모의 질적 변화를 이끌어냅니다. 이러한 "수집보다 통합"의 접근 방식은 ARIO 전 로봇 통합 데이터셋이 제안한 통합 데이터 표준과 맥을 같이하지만, AMASS는 더 일찍 등장했고 더 성숙하며, 로봇 본체가 아닌 인체에 특화되어 있습니다.

### 원리 분석

**① MoSh++: 마커에서 SMPL 메시까지**

MoSh++는 AMASS의 핵심 기술 방법입니다. 입력은 광학 모션 캡처 시스템이 출력하는 마커의 3D 궤적이고, 출력은 SMPL 인체 모델의 파라미터(자세 파라미터 θ 및 형태 파라미터 β)입니다. SMPL은 표준 골격 표현과 완전히 바인딩된 표면 메시를 제공하며, 수학적 형태는 다음과 같이 요약할 수 있습니다:

$$
M(\beta, \theta) = W(T(\beta, \theta), J(\beta), \theta, \mathcal{W})
$$

여기서 $T(\beta, \theta)$는 형태와 자세에 따라 변형된 템플릿 메시, $J(\beta)$는 관절 위치, $W$는 선형 블렌드 스키닝 함수, $\mathcal{W}$는 스킨 가중치입니다. MoSh++의 핵심 혁신은 고정된 마커 구성을 요구하지 않고, 최적화를 통해 형태, 자세 및 연조직 변형을 동시에 피팅하여 서로 다른 모션 캡처 실험실의 이질적인 마커 프로토콜과 호환된다는 점입니다.

**② 통합 파라미터화의 데이터 엔지니어링 가치**

통합 파라미터화가 가져오는 직접적인 이점은 **데이터 연결 가능성**입니다. 15개 데이터셋이 SMPL 파라미터 공간에서 정렬된 후 단일 훈련 세트로 병합할 수 있으며, 특징 공간 불일치를 걱정할 필요가 없습니다. 이러한 "먼저 통합, 그다음 훈련"의 파이프라인은 AMASS가 "깨끗한" 데이터셋이라는 의미입니다. 깨끗함은 노이즈가 없다는 것이 아니라 **형식이 일관되고 직접 소비 가능**하다는 의미입니다.

### 주요 파라미터 및 사양

| 파라미터 | 값 | 설명 |
|------|------|------|
| 통합 데이터셋 수 | 15개 | 서로 다른 광학 마커 모션 캡처 데이터셋 |
| 모션 데이터 총 시간 | 40시간 이상 | 이전의 어떤 인체 모션 데이터셋보다 현저히 큼 |
| 피험자 수 | 300명 이상 | 광범위한 인구 다양성 포함 |
| 모션 시퀀스 수 | 11,000개 이상 | 각각은 완전한 동작 기록 |
| 인체 모델 | SMPL | 표준 골격 표현 + 완전히 바인딩된 표면 메시 |
| 발표 학회 | ICCV 2019 | 논문 페이지 5442–5451 |

(위 수치는 모두 AMASS 공식 논문 초록 및 인용 정보에서 가져온 것입니다)

### 수평 비교

| 데이터셋 | 데이터 유형 | 규모 | 통합 파라미터화 | 휴머노이드 로봇 적용성 |
|--------|---------|------|-----------|-----------------|
| AMASS | 광학 모션 캡처 인체 모션 | 40+ 시간, 11,000+ 시퀀스 | 예(SMPL) | 높음: 전신 모션 전략 사전 훈련 |
| Ego4D | 1인칭 비디오 | 3,000+ 시간 | 아니요 | 중간: 손-물체 상호작용 및 1인칭 기술 |
| Epic-Kitchens | 1인칭 주방 비디오 | 직접 확인 필요 | 아니요 | 중간: 조작 시퀀스 이해 |
| HumanPlus Shadowing | 로봇 섀도우 팔로우 데이터 | 직접 확인 필요 | 아니요 | 높음: 전신 모방 및 원격 조작 |

AMASS와 HumanPlus Shadowing의 차이점은 주목할 만합니다. AMASS는 **순수 인체 데이터**로 로봇 본체 상태를 포함하지 않습니다. 반면 HumanPlus는 인체 데모와 로봇 전신 상태를 바인딩합니다. 두 데이터셋은 훈련 파이프라인에서 상호 보완적입니다. AMASS는 대규모 모션 사전 지식을 제공하고, HumanPlus는 구현 정렬 데이터를 제공합니다.

### 사용처·응용 사례

**① 휴머노이드 로봇 시뮬레이션 사전 훈련**

휴머노이드 로봇 분야에서 AMASS의 표준 사용법은 다음과 같습니다: **먼저 AMASS에서 저수준 전신 추적 전략을 훈련한 다음 실제 로봇으로 전이**하는 것입니다. 이는 AMASS가 순수 인체 모션 데이터를 제공하고 로봇 역학 제약을 포함하지 않기 때문입니다. 전략은 먼저 시뮬레이션에서 "인체가 어떻게 움직여야 하는지"를 학습한 다음, 도메인 무작위화와 리타게팅을 통해 특정 로봇 플랫폼에 적응합니다.

**② 애니메이션 및 시각화**

AMASS의 공식 포지셔닝은 "애니메이션, 시각화 및 생성 딥러닝 훈련 데이터에 직접 사용 가능"입니다. SMPL 파라미터화 형식은 그래픽스 파이프라인에 자연스럽게 적합하여 캐릭터 애니메이션 생성 또는 데이터 증강 도구로 사용할 수 있습니다.

**③ 교차 본체 정전이 연구**

AMASS는 대규모 인간 데이터 소스로서 "인간 데이터로 작업 의미론을 학습하고, 로봇 데이터로 구현을 해결"하는 교차 데이터 규모 격차 경로를 검증하는 핵심 리소스입니다. 수백만 시간 규모의 인간 데이터(엔지니어링 판단: 40시간은 모션 캡처 데이터이며, 파생 증강 데이터를 포함하면 규모가 더 큼)는 모션 사전 지식 학습에 충분한 원자재를 제공합니다.

### 한계 및 경계

1. **물체 상호작용 및 힘 감각 정보 부족**: AMASS는 순수 운동학 데이터로, 조작 작업에 필요한 물체 상호작용 및 힘 감각 정보를 포함하지 않습니다. 따라서 일반적으로 원격 조작 데이터와 상호 보완적으로 사용되며, 상호 대체가 아닙니다.
2. **광학 모션 캡처의 수집 제약**: AMASS는 광학 마커 모션 캡처에 의존하므로 전문 공간과 장비 유지보수 및 캘리브레이션이 필요하며, 야외 시나리오를 다루기 어렵습니다.
3. **로봇 본체 상태 없음**: AMASS는 로봇 관절 토크, 모터 전류 등의 고유 수용 데이터를 포함하지 않아 힘 제어가 필요한 전략을 직접 훈련할 수 없습니다.
4. **SMPL 모델의 손 세부 표현**: MoSh++가 사실적인 손 움직임을 복원하지만, SMPL의 손 자유도 표현은 전용 손 모델보다 정밀하지 않습니다(엔지니어링 판단).

### 일반적인 오해

1. **"AMASS는 로봇 데이터셋이다"** — 틀림. AMASS는 순수 인체 모션 데이터셋으로 로봇 본체 데이터를 포함하지 않습니다. 휴머노이드 로봇 훈련에 사용되는 전제는 "시뮬레이션에서 사전 훈련 후 전이"하는 파이프라인 설계입니다.
2. **"AMASS 데이터를 실제 로봇에 직접 사용할 수 있다"** — 틀림. AMASS 데이터는 로봇 역학 제약이 없어 직접 배포하면 관절 한계와 토크 제한을 무시하는 전략이 됩니다. 반드시 리타게팅과 시뮬레이션 적응을 거쳐야 합니다.
3. **"AMASS와 HumanPlus는 경쟁 관계다"** — 틀림. 두 데이터셋은 상호 보완적입니다. AMASS는 대규모 모션 사전 지식을 제공하고, HumanPlus는 인체-로봇 상태 바인딩 데이터를 제공합니다. 실제 훈련 파이프라인은 종종 두 데이터를 동시에 사용합니다.
4. **"데이터셋이 곧 벤치마크다"** — 틀림. 데이터셋은 훈련 원자재를 제공합니다. 벤치마크는 추가로 작업 분할, 평가 프로토콜 및 지표를 규정합니다. AMASS는 데이터셋이지 벤치마크가 아닙니다.

### 관련 지식

- `ent_software_mujoco_physics_engine_2022` — MuJoCo는 AMASS 사전 훈련 전략에 일반적으로 사용되는 시뮬레이션 환경으로, 고충실도 접촉 역학을 제공합니다.
- `ent_technology_optitrack_motion_capture_2024` — OptiTrack은 AMASS가 통합한 광학 모션 캡처 데이터의 대표적인 수집 장비로, 서브밀리미터급 추적을 구현합니다.
- `ent_dataset_ego4d_egocentric_video_dataset_2022` — Ego4D는 AMASS와 함께 "인간 측" 데이터 리소스에 속하지만, 모션 캡처 데이터가 아닌 1인칭 비디오를 제공합니다.
- `ent_technology_humanplus_shadowing_system_2024` — HumanPlus는 AMASS와 상호 보완적이며, 인체-로봇 상태 바인딩 섀도우 팔로우 데이터를 제공합니다.
- `ent_dataset_epic_kitchens_dataset_2018` — Epic-Kitchens는 AMASS와 함께 인간 데이터 리소스에 속하며, 1인칭 주방 상호작용 비디오에 초점을 맞춥니다.
