---
$id: ent_dataset_bridgedata_v2_2023
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: dataset
names:
  en: BridgeData V2
  zh: BridgeData V2
  ko: BridgeData V2
summary:
  en: Widely used tabletop manipulation dataset with language instructions for imitation learning.
  zh: 广泛用于模仿学习的带语言指令的桌面操作数据集。
  ko: 모방 학습을 위한 언어 지시가 포함된 널리 사용되는 탁상 조작 데이터셋.
domains:
- 09_data_datasets
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- bridge_data
- dataset
- language_instructions
- manipulation
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-13]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b5): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: BridgeData V2
  url: https://github.com/rail-berkeley/bridge_data_v2
  date: '2023'
  accessed_at: '2026-07-02'
---
## 概述

BridgeData V2 是一个带语言指令标注的大规模桌面操作数据集，由加州大学伯克利分校等机构收集，包含 60,096 条轨迹，覆盖 24 种环境与 13 种技能，广泛用于模仿学习与视觉-语言-动作模型（VLA）的跨具身预训练。它真正改变的不是数据量本身，而是让"语言条件策略"有了一个可复现、可比较的公共基准。

## 核心内容

### 是什么：准确定义

BridgeData V2 是一个面向可扩展机器人学习研究的大规模、多样化机器人操作行为数据集。它兼容开放词表（open-vocabulary）、多任务学习方法，这些方法以目标图像或自然语言指令为条件。从该数据中学到的技能能够泛化到新物体、新环境，并跨机构迁移。

从数据工程视角看，它属于**跨具身预训练数据**中的关键一环。在 VLA 训练的数据配方中，它与 Open X-Embodiment、DROID、AgiBot World Colosseo 并列，为模型提供"操作物理直觉"——即与具体机器人本体解耦的、关于物体如何被操作的基础知识。

### 为什么存在：痛点与历史定位

在 BridgeData V2 出现之前，机器人学习社区面临一个结构性困境：每个实验室各自收集数据，环境、相机视角、物体设置各不相同，导致策略难以跨机构复现与比较。更关键的是，早期数据集大多缺乏语言标注，策略只能以目标图像为条件，无法利用自然语言指令这一灵活的接口。

BridgeData V2 的定位正是填补这一空白：它用统一的协议收集大规模、多环境、带语言标注的操作数据，使研究者能够训练以自然语言为条件的策略，并验证其在新物体、新环境中的泛化能力。它同时是 VLA 路线兴起的"燃料"之一——没有这类带语义标注的大规模数据，RT-2、OpenVLA 等模型的跨具身预训练就无从谈起。

### 原理拆解

**① 数据构成：规模与多样性**

BridgeData V2 的核心设计原则是"以多样性支撑泛化"。为支持广泛泛化，数据在多种环境中针对大量任务收集，并在物体、相机视角和工作空间位置上引入变化。每条轨迹都标注了与机器人正在执行任务对应的自然语言指令。

数据集总量为 **60,096 条轨迹**，其中 **50,365 条为遥操作演示**，**9,731 条来自脚本化 pick-and-place 策略的 rollout**。数据覆盖 **24 种环境**与 **13 种技能**。

**② 环境与技能分布**

24 种环境分为 4 大类。大部分数据来自 7 个不同的玩具厨房，包含水槽、灶台和微波炉等元素的组合；其余环境来自多种来源，包括各种桌面、独立玩具水槽、玩具洗衣机等。

技能分布上，大部分数据来自基础物体操作任务，如 pick-and-place、推（pushing）和扫（sweeping）；另有环境操作数据，包括开关门和抽屉；其余来自更复杂的任务，如叠积木、折叠布料和清扫颗粒状介质。部分数据段包含这些类别的混合。

**③ 相机视角配置**

数据集包含多种相机视角："Over-the-shoulder"指主固定相机，"randomized"指数据收集者每 50 条轨迹随机化的两个备用相机视角，"Depth"（当存在时）与主固定相机同视角，"Wrist"指广角腕载相机。更多相机在后续收集中被加入。

**④ 与 VLA 训练的关系**

在 VLA 预训练中，BridgeData V2 作为真实机器人演示数据源之一，与人类视频、仿真数据、互联网图文共同构成混合数据配方。其语言标注使模型能够学习"指令-动作"的映射关系，这是纯视觉数据无法提供的。

### 关键参数与规格

| 参数 | 数值 | 备注 |
|------|------|------|
| 总轨迹数 | 60,096 条 | 含遥操作与脚本策略数据 |
| 遥操作演示 | 50,365 条 | 人工遥操作收集 |
| 脚本策略 rollout | 9,731 条 | 脚本化 pick-and-place 策略 |
| 环境数 | 24 种 | 分为 4 大类 |
| 技能数 | 13 种 | 含基础操作、环境操作、复杂任务 |
| 玩具厨房数 | 7 个 | 主要数据来源 |
| 相机视角 | 4 类 | 主固定、随机化、深度、腕载 |
| 语言标注 | 每条轨迹 | 自然语言指令 |
| 数据格式 | JPEG 图像 | 以 zip 文件分开放置 |

### 横向对比

与同类型数据集相比，BridgeData V2 的差异化定位清晰：

- **vs. DROID**：DROID 强调跨实验室、跨真实家庭环境的分布式收集，以场景与视角多样性著称；BridgeData V2 则聚焦桌面操作，环境以玩具厨房为主，但语言标注更系统化。
- **vs. Open X-Embodiment**：后者是 20 余家机构聚合的开放数据集，覆盖 20 余种机器人本体、百万级片段，是更大的公共底座；BridgeData V2 是其中的重要组成部分，但规模更小、本体更单一。
- **vs. AgiBot World Colosseo**：后者面向全尺寸人形机器人，提供稀缺的高质量真机数据；BridgeData V2 以桌面夹爪操作为主，不涉及全身控制。

工程判断：BridgeData V2 的价值不在"最大"，而在"最标准"——它用统一的协议和语言标注，为模仿学习研究提供了一个可复现的基准。

### 谁在用·应用案例

**① 模仿学习研究**

BridgeData V2 是模仿学习研究的常用基准数据。研究者在其上训练策略，评估语言条件操作能力，并测试对新物体、新环境的泛化。

**② VLA 跨具身预训练**

在 VLA 的数据配方中，BridgeData V2 与 Open X-Embodiment、DROID 等共同构成预训练数据混合。模型先在混合数据上预训练，再通过 LoRA 或全量微调适配下游任务。

**③ 跨机构泛化验证**

数据集设计目标之一是"跨机构泛化"——在一个机构数据上训练的策略，能在另一机构的环境和机器人上工作。这使其成为验证策略鲁棒性的标准测试平台。

### 局限与边界

**① 任务范围有限**：数据以桌面操作为主，不涉及移动操作、全身操作或人形机器人特有的浮动基座问题。对于需要行走中操作或全身协调的任务，需补充其他数据源。

**② 本体单一**：数据主要来自固定基座或轮式平台的夹爪操作，与人形机器人的灵巧手操作存在差距。工程判断：从 BridgeData V2 学到的策略迁移到人形平台时，需要额外的适配或微调。

**③ 语言标注粒度**：每条轨迹标注一个自然语言指令，但指令的语义粒度可能不足以覆盖复杂任务中的子步骤分解。

**④ 数据分布偏差**：大部分数据来自玩具厨房环境，真实家庭或工业场景的覆盖有限。

### 常见误区

1. **"BridgeData V2 是最大的操作数据集"**——错。Open X-Embodiment 覆盖百万级片段，规模远大于它；BridgeData V2 的价值在于语言标注的规范性和环境的可控性。

2. **"有了它就能训练通用人形机器人策略"**——错。它不包含人形机器人数据，也不涉及全身控制；它解决的是桌面操作的语言条件策略问题。

3. **"语言标注只是附加信息"**——错。语言标注是数据设计的核心：它使策略能够以自然语言为条件，这是开放词表泛化的基础。

4. **"脚本策略数据质量低，应该丢弃"**——错。9,731 条脚本 rollout 提供了遥操作难以覆盖的多样性和规模，是数据配方的重要组成部分。

### 相关知识

- `ent_dataset_droid` — 同属 VLA 预训练数据混合的分布式操作数据集，强调场景与视角多样性。
- `ent_dataset_omniaction` — 大规模多模态操作数据集，融合视觉、语音与环境声音，与 BridgeData V2 的语言条件形成互补。
- `ent_paper_agibot_world_colosseo_a_large_2025` — 面向全尺寸人形机器人的大规模操作数据集，补充 BridgeData V2 缺少的人形本体数据。
- `ent_paper_wang_vla_survey_2026` — VLA 综述，系统梳理了包括 BridgeData V2 在内的数据工程与模型架构演进。
- `ent_paper_world_action_models_embodied_brains_road_2026` — 提出世界行动模型与数据记录协议，为数据集语义标准化提供新框架。

## 参考

- [BridgeData V2](https://github.com/rail-berkeley/bridge_data_v2)
- [Chapter 19: VLA 与数据工程](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-19.md)
- [Chapter 16: 操作与抓取](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-16.md)

## Overview

BridgeData V2 is a large-scale desktop manipulation dataset annotated with language instructions, collected by UC Berkeley and other institutions. It contains 60,096 trajectories covering 24 environments and 13 skills, and is widely used for imitation learning and cross-embodiment pretraining of Vision-Language-Action models (VLAs). What it truly changes is not the data volume itself, but rather providing a reproducible, comparable public benchmark for "language-conditioned policies."

## Content

### What It Is: Precise Definition

BridgeData V2 is a large-scale, diverse robot manipulation behavior dataset designed for scalable robot learning research. It is compatible with open-vocabulary, multi-task learning methods that condition on goal images or natural language instructions. Skills learned from this data can generalize to new objects, new environments, and transfer across institutions.

From a data engineering perspective, it is a key component of **cross-embodiment pretraining data**. In the data recipe for VLA training, it stands alongside Open X-Embodiment, DROID, and AgiBot World Colosseo, providing models with "manipulation physical intuition"—that is, foundational knowledge about how objects are manipulated, decoupled from specific robot embodiments.

### Why It Exists: Pain Points and Historical Positioning

Before BridgeData V2, the robot learning community faced a structural dilemma: each lab collected its own data with different environments, camera viewpoints, and object setups, making policies difficult to reproduce and compare across institutions. More critically, early datasets mostly lacked language annotations, so policies could only condition on goal images and could not leverage natural language instructions as a flexible interface.

BridgeData V2's positioning is precisely to fill this gap: it collects large-scale, multi-environment, language-annotated manipulation data using a unified protocol, enabling researchers to train policies conditioned on natural language and verify their generalization capabilities on new objects and environments. It is also one of the "fuels" for the rise of the VLA paradigm—without such large-scale semantically annotated data, cross-embodiment pretraining of models like RT-2 and OpenVLA would be impossible.

### Principle Breakdown

**① Data Composition: Scale and Diversity**

The core design principle of BridgeData V2 is "diversity supports generalization." To support broad generalization, data is collected across many environments for a large number of tasks, with variations introduced in objects, camera viewpoints, and workspace positions. Each trajectory is annotated with a natural language instruction corresponding to the task the robot is performing.

The dataset contains a total of **60,096 trajectories**, of which **50,365 are teleoperated demonstrations** and **9,731 come from rollouts of scripted pick-and-place policies**. The data covers **24 environments** and **13 skills**.

**② Environment and Skill Distribution**

The 24 environments fall into 4 major categories. Most data comes from 7 different toy kitchens containing combinations of elements such as sinks, stoves, and microwaves; the remaining environments come from various sources, including various tabletops, standalone toy sinks, toy washing machines, and more.

In terms of skill distribution, most data comes from basic object manipulation tasks such as pick-and-place, pushing, and sweeping; there is also environment interaction data, including opening and closing doors and drawers; the rest comes from more complex tasks such as stacking blocks, folding cloth, and sweeping granular media. Some data segments contain mixtures of these categories.

**③ Camera Viewpoint Configuration**

The dataset includes multiple camera viewpoints: "Over-the-shoulder" refers to the primary fixed camera, "randomized" refers to two alternate camera viewpoints that data collectors randomized every 50 trajectories, "Depth" (when present) shares the same viewpoint as the primary fixed camera, and "Wrist" refers to a wide-angle wrist-mounted camera. Additional cameras were added in later collection rounds.

**④ Relationship to VLA Training**

In VLA pretraining, BridgeData V2 serves as one source of real robot demonstration data, forming a mixed data recipe together with human videos, simulation data, and internet images and text. Its language annotations enable models to learn "instruction-action" mappings, which purely visual data cannot provide.

### Key Parameters and Specifications

| Parameter | Value | Notes |
|-----------|-------|-------|
| Total trajectories | 60,096 | Includes teleoperation and scripted policy data |
| Teleoperated demonstrations | 50,365 | Collected via human teleoperation |
| Scripted policy rollouts | 9,731 | Scripted pick-and-place policies |
| Number of environments | 24 | Divided into 4 major categories |
| Number of skills | 13 | Includes basic manipulation, environment interaction, complex tasks |
| Number of toy kitchens | 7 | Primary data source |
| Camera viewpoints | 4 types | Primary fixed, randomized, depth, wrist-mounted |
| Language annotations | Per trajectory | Natural language instructions |
| Data format | JPEG images | Stored separately in zip files |

### Horizontal Comparison

Compared with similar datasets, BridgeData V2's differentiated positioning is clear:

- **vs. DROID**: DROID emphasizes distributed collection across labs and real home environments, known for scene and viewpoint diversity; BridgeData V2 focuses on tabletop manipulation with toy kitchens as the primary environment, but with more systematic language annotations.
- **vs. Open X-Embodiment**: The latter is an open dataset aggregated from over 20 institutions, covering 20+ robot embodiments and millions of episodes, serving as a larger public foundation; BridgeData V2 is an important component of it, but smaller in scale and more homogeneous in embodiment.
- **vs. AgiBot World Colosseo**: The latter targets full-size humanoid robots, providing scarce high-quality real-robot data; BridgeData V2 focuses on tabletop gripper manipulation and does not involve whole-body control.

Engineering judgment: BridgeData V2's value lies not in being the "largest," but in being the "most standardized"—it provides a reproducible benchmark for imitation learning research through unified protocols and language annotations.

### Who Uses It: Application Cases

**① Imitation Learning Research**

BridgeData V2 is a common benchmark dataset for imitation learning research. Researchers train policies on it, evaluate language-conditioned manipulation capabilities, and test generalization to new objects and environments.

**② VLA Cross-Embodiment Pretraining**

In VLA data recipes, BridgeData V2, together with Open X-Embodiment, DROID, and others, forms the pretraining data mixture. Models are first pretrained on the mixed data, then adapted to downstream tasks via LoRA or full fine-tuning.

**③ Cross-Institution Generalization Validation**

One of the dataset's design goals is "cross-institution generalization"—policies trained on data from one institution should work in another institution's environments and robots. This makes it a standard testbed for validating policy robustness.

### Limitations and Boundaries

**① Limited Task Scope**: The data focuses on tabletop manipulation and does not cover mobile manipulation, whole-body manipulation, or the floating-base issues unique to humanoid robots. For tasks requiring manipulation during locomotion or whole-body coordination, additional data sources are needed.

**② Homogeneous Embodiment**: The data primarily comes from gripper manipulation on fixed-base or wheeled platforms, which differs from dexterous hand manipulation on humanoid robots. Engineering judgment: policies learned from BridgeData V2 require additional adaptation or fine-tuning when transferred to humanoid platforms.

**③ Language Annotation Granularity**: Each trajectory is annotated with one natural language instruction, but the semantic granularity of the instructions may be insufficient to cover sub-step decomposition in complex tasks.

**④ Data Distribution Bias**: Most data comes from toy kitchen environments, with limited coverage of real home or industrial scenarios.

### Common Misconceptions

1. **"BridgeData V2 is the largest manipulation dataset"**—Incorrect. Open X-Embodiment covers millions of episodes and is far larger; BridgeData V2's value lies in the standardization of its language annotations and the controllability of its environments.

2. **"With it, you can train general-purpose humanoid robot policies"**—Incorrect. It does not contain humanoid robot data and does not involve whole-body control; it addresses the problem of language-conditioned policies for tabletop manipulation.

3. **"Language annotations are just auxiliary information"**—Incorrect. Language annotations are central to the dataset design: they enable policies to condition on natural language, which is the foundation of open-vocabulary generalization.

4. **"Scripted policy data is low quality and should be discarded"**—Incorrect. The 9,731 scripted rollouts provide diversity and scale that teleoperation struggles to cover, making them an important component of the data recipe.

### Related Knowledge

- `ent_dataset_droid` — A distributed manipulation dataset in the same VLA pretraining data mixture, emphasizing scene and viewpoint diversity.
- `ent_dataset_omniaction` — A large-scale multimodal manipulation dataset integrating vision, speech, and environmental sound, complementing BridgeData V2's language conditioning.
- `ent_paper_agibot_world_colosseo_a_large_2025` — A large-scale manipulation dataset for full-size humanoid robots, supplementing the humanoid embodiment data missing from BridgeData V2.
- `ent_paper_wang_vla_survey_2026` — A VLA survey that systematically reviews the evolution of data engineering and model architectures, including BridgeData V2.
- `ent_paper_world_action_models_embodied_brains_road_2026` — Proposes world action models and data recording protocols, providing a new framework for semantic standardization of datasets.

## 개요

BridgeData V2는 언어 지시가 주석으로 달린 대규모 탁상 조작 데이터셋으로, 캘리포니아 대학교 버클리 캠퍼스 등 기관에서 수집했으며, 60,096개의 궤적을 포함하고 24가지 환경과 13가지 스킬을 포괄합니다. 모방 학습과 시각-언어-행동 모델(VLA)의 교차 체현 사전 학습에 널리 사용됩니다. 이 데이터셋이 실제로 바꾼 것은 데이터 규모 자체가 아니라, '언어 조건 정책'에 재현 가능하고 비교 가능한 공공 벤치마크를 제공했다는 점입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

BridgeData V2는 확장 가능한 로봇 학습 연구를 위한 대규모의 다양한 로봇 조작 행동 데이터셋입니다. 목표 이미지나 자연어 지시를 조건으로 하는 개방형 어휘(open-vocabulary), 다중 작업 학습 방법과 호환됩니다. 이 데이터에서 학습된 스킬은 새로운 물체, 새로운 환경으로 일반화되며 기관 간 전이도 가능합니다.

데이터 엔지니어링 관점에서 보면, 이는 **교차 체현 사전 학습 데이터**의 핵심 요소 중 하나입니다. VLA 훈련의 데이터 레시피에서 Open X-Embodiment, DROID, AgiBot World Colosseo와 함께 모델에 '조작 물리 직관'—즉, 특정 로봇 본체와 분리된, 물체가 어떻게 조작되는지에 대한 기초 지식—을 제공합니다.

### 왜 존재하는가: 문제점과 역사적 위치

BridgeData V2가 등장하기 전, 로봇 학습 커뮤니티는 구조적 딜레마에 직면해 있었습니다. 각 연구실이 자체적으로 데이터를 수집했고, 환경, 카메라 시점, 물체 설정이 모두 달라 정책의 기관 간 재현과 비교가 어려웠습니다. 더 중요하게는, 초기 데이터셋 대부분이 언어 주석이 부족하여 정책이 목표 이미지에만 조건화될 수 있었고, 자연어 지시라는 유연한 인터페이스를 활용할 수 없었습니다.

BridgeData V2의 위치는 바로 이 공백을 메우는 것입니다. 통일된 프로토콜로 대규모, 다중 환경, 언어 주석이 포함된 조작 데이터를 수집하여 연구자들이 자연어를 조건으로 하는 정책을 훈련하고 새로운 물체와 환경에서의 일반화 능력을 검증할 수 있게 합니다. 이는 또한 VLA 노선이 부상하는 데 필요한 '연료' 중 하나입니다—이런 의미적 주석이 있는 대규모 데이터가 없었다면 RT-2, OpenVLA 등의 모델의 교차 체현 사전 학습은 불가능했을 것입니다.

### 원리 분석

**① 데이터 구성: 규모와 다양성**

BridgeData V2의 핵심 설계 원칙은 '다양성으로 일반화를 뒷받침한다'는 것입니다. 광범위한 일반화를 지원하기 위해 데이터는 다양한 환경에서 많은 작업에 대해 수집되었으며, 물체, 카메라 시점, 작업 공간 위치에 변화를 도입했습니다. 각 궤적에는 로봇이 수행 중인 작업에 해당하는 자연어 지시가 주석으로 달려 있습니다.

데이터셋 총량은 **60,096개의 궤적**이며, 그중 **50,365개는 원격 조작 데모**, **9,731개는 스크립트 기반 pick-and-place 정책의 롤아웃**입니다. 데이터는 **24가지 환경**과 **13가지 스킬**을 포괄합니다.

**② 환경 및 스킬 분포**

24가지 환경은 4가지 주요 범주로 나뉩니다. 대부분의 데이터는 싱크대, 스토브, 전자레인지 등의 요소가 결합된 7개의 서로 다른 장난감 주방에서 비롯됩니다. 나머지 환경은 다양한 탁상, 독립형 장난감 싱크대, 장난감 세탁기 등 여러 출처에서 왔습니다.

스킬 분포 측면에서, 대부분의 데이터는 pick-and-place, 밀기(pushing), 쓸기(sweeping)와 같은 기본 물체 조작 작업에서 비롯됩니다. 또한 문과 서랍 열고 닫기를 포함한 환경 조작 데이터가 있으며, 나머지는 블록 쌓기, 천 접기, 입자 매체 청소와 같은 더 복잡한 작업에서 비롯됩니다. 일부 데이터 세그먼트에는 이러한 범주의 혼합이 포함되어 있습니다.

**③ 카메라 시점 구성**

데이터셋에는 여러 카메라 시점이 포함됩니다. 'Over-the-shoulder'는 주요 고정 카메라를 의미하고, 'randomized'는 데이터 수집자가 50개 궤적마다 무작위화하는 두 개의 예비 카메라 시점을 의미하며, 'Depth'(존재하는 경우)는 주요 고정 카메라와 동일한 시점이고, 'Wrist'는 광각 손목 장착 카메라를 의미합니다. 더 많은 카메라는 이후 수집에서 추가되었습니다.

**④ VLA 훈련과의 관계**

VLA 사전 학습에서 BridgeData V2는 실제 로봇 데모 데이터 소스 중 하나로, 인간 비디오, 시뮬레이션 데이터, 인터넷 이미지-텍스트와 함께 혼합 데이터 레시피를 구성합니다. 언어 주석은 모델이 '지시-행동' 매핑 관계를 학습할 수 있게 하며, 이는 순수 시각 데이터로는 제공할 수 없는 것입니다.

### 핵심 매개변수 및 사양

| 매개변수 | 값 | 비고 |
|------|------|------|
| 총 궤적 수 | 60,096개 | 원격 조작 및 스크립트 정책 데이터 포함 |
| 원격 조작 데모 | 50,365개 | 수동 원격 조작으로 수집 |
| 스크립트 정책 롤아웃 | 9,731개 | 스크립트화된 pick-and-place 정책 |
| 환경 수 | 24가지 | 4가지 주요 범주로 구분 |
| 스킬 수 | 13가지 | 기본 조작, 환경 조작, 복잡한 작업 포함 |
| 장난감 주방 수 | 7개 | 주요 데이터 소스 |
| 카메라 시점 | 4가지 유형 | 주요 고정, 무작위화, 깊이, 손목 장착 |
| 언어 주석 | 각 궤적 | 자연어 지시 |
| 데이터 형식 | JPEG 이미지 | zip 파일로 분리 배치 |

### 수평 비교

동일 유형의 데이터셋과 비교할 때 BridgeData V2의 차별화된 위치는 명확합니다.

- **vs. DROID**: DROID는 실험실 간, 실제 가정 환경에서의 분산 수집을 강조하며 장면과 시점 다양성으로 유명합니다. BridgeData V2는 탁상 조작에 초점을 맞추고 환경은 주로 장난감 주방이지만 언어 주석은 더 체계적입니다.
- **vs. Open X-Embodiment**: 후자는 20여 개 기관이 집계한 개방형 데이터셋으로, 20여 종의 로봇 본체와 수백만 개의 에피소드를 포괄하는 더 큰 공공 기반입니다. BridgeData V2는 그 중요한 구성 요소이지만 규모는 더 작고 본체는 더 단일합니다.
- **vs. AgiBot World Colosseo**: 후자는 전신 크기 휴머노이드 로봇을 대상으로 하며 희소한 고품질 실제 로봇 데이터를 제공합니다. BridgeData V2는 탁상 그리퍼 조작이 주를 이루며 전신 제어를 다루지 않습니다.

엔지니어링 판단: BridgeData V2의 가치는 '가장 큰 것'이 아니라 '가장 표준적인 것'에 있습니다. 통일된 프로토콜과 언어 주석으로 모방 학습 연구에 재현 가능한 벤치마크를 제공합니다.

### 사용처·적용 사례

**① 모방 학습 연구**

BridgeData V2는 모방 학습 연구의 일반적인 벤치마크 데이터입니다. 연구자들은 이 데이터에서 정책을 훈련하고 언어 조건 조작 능력을 평가하며 새로운 물체와 환경에 대한 일반화를 테스트합니다.

**② VLA 교차 체현 사전 학습**

VLA 데이터 레시피에서 BridgeData V2는 Open X-Embodiment, DROID 등과 함께 사전 학습 데이터 혼합을 구성합니다. 모델은 먼저 혼합 데이터에서 사전 학습된 후 LoRA 또는 전체 미세 조정을 통해 하위 작업에 적응합니다.

**③ 교차 기관 일반화 검증**

데이터셋의 설계 목표 중 하나는 '교차 기관 일반화'입니다—한 기관의 데이터로 훈련된 정책이 다른 기관의 환경과 로봇에서 작동해야 합니다. 이는 정책 견고성을 검증하는 표준 테스트 플랫폼이 되게 합니다.

### 한계와 경계

**① 작업 범위 제한**: 데이터는 주로 탁상 조작에 초점을 맞추며, 이동 조작, 전신 조작 또는 휴머노이드 로봇 고유의 부유 베이스 문제를 다루지 않습니다. 걷기 중 조작이나 전신 조정이 필요한 작업에는 다른 데이터 소스를 보충해야 합니다.

**② 본체 단일성**: 데이터는 주로 고정 베이스 또는 바퀴형 플랫폼의 그리퍼 조작에서 비롯되며, 휴머노이드 로봇의 손재주 있는 손 조작과는 차이가 있습니다. 엔지니어링 판단: BridgeData V2에서 학습된 정책을 휴머노이드 플랫폼으로 전이하려면 추가 적응 또는 미세 조정이 필요합니다.

**③ 언어 주석 세분성**: 각 궤적에 하나의 자연어 지시가 주석으로 달리지만, 지시의 의미적 세분성은 복잡한 작업의 하위 단계 분해를 포괄하기에 충분하지 않을 수 있습니다.

**④ 데이터 분포 편향**: 대부분의 데이터는 장난감 주방 환경에서 비롯되며, 실제 가정이나 산업 현장의 포괄 범위는 제한적입니다.

### 일반적인 오해

1. **"BridgeData V2는 가장 큰 조작 데이터셋이다"** — 틀렸습니다. Open X-Embodiment는 수백만 개의 에피소드를 포괄하며 규모가 훨씬 큽니다. BridgeData V2의 가치는 언어 주석의 규범성과 환경의 통제 가능성에 있습니다.

2. **"이것만 있으면 범용 휴머노이드 로봇 정책을 훈련할 수 있다"** — 틀렸습니다. 휴머노이드 로봇 데이터를 포함하지 않으며 전신 제어도 다루지 않습니다. 해결하는 것은 탁상 조작의 언어 조건 정책 문제입니다.

3. **"언어 주석은 단지 부가 정보일 뿐이다"** — 틀렸습니다. 언어 주석은 데이터 설계의 핵심입니다. 정책이 자연어를 조건으로 할 수 있게 하며, 이는 개방형 어휘 일반화의 기초입니다.

4. **"스크립트 정책 데이터는 품질이 낮으니 버려야 한다"** — 틀렸습니다. 9,731개의 스크립트 롤아웃은 원격 조작으로는 포괄하기 어려운 다양성과 규모를 제공하며 데이터 레시피의 중요한 구성 요소입니다.

### 관련 지식

- `ent_dataset_droid` — VLA 사전 학습 데이터 혼합에 속하는 분산 조작 데이터셋으로, 장면과 시점 다양성을 강조합니다.
- `ent_dataset_omniaction` — 대규모 다중 모달 조작 데이터셋으로, 시각, 음성, 환경 소리를 융합하며 BridgeData V2의 언어 조건과 상호 보완적입니다.
- `ent_paper_agibot_world_colosseo_a_large_2025` — 전신 크기 휴머노이드 로봇을 위한 대규모 조작 데이터셋으로, BridgeData V2에 없는 휴머노이드 본체 데이터를 보완합니다.
- `ent_paper_wang_vla_survey_2026` — VLA 서베이로, BridgeData V2를 포함한 데이터 엔지니어링과 모델 아키텍처의 진화를 체계적으로 정리합니다.
- `ent_paper_world_action_models_embodied_brains_road_2026` — 세계 행동 모델과 데이터 기록 프로토콜을 제안하며 데이터셋 의미적 표준화의 새로운 프레임워크를 제공합니다.
