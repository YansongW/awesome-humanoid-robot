---
$id: ent_benchmark_libero
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: benchmark
names:
  en: LIBERO
  zh: LIBERO
  ko: LIBERO
summary:
  en: A benchmark and dataset for learning library-generalizable robot manipulation skills from short-horizon tabletop tasks
    with procedural object and scene variations.
  zh: 一个用于从短程桌面任务中学习可泛化机器人操作技能的基准与数据集，支持程序化的物体与场景变化。
  ko: 프로시저적 객체 및 장면 변화를 가진 단기 테이블탑 작업에서 라이브러리 일반화 가능한 로봇 조작 기술을 학습하기 위한 벤치마크 및 데이터셋.
domains:
- 09_data_datasets
- 10_evaluation_benchmarks
layers:
- intelligence
- validation_markets
functional_roles:
- knowledge
- intelligence
tags:
- robot_learning
- vla
- tabletop
- benchmark
- dataset
- short_horizon
- generalization
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-06-22'
  confidence: high
  notes: 'Scope confirmed by original NeurIPS 2023 paper and Wang et al. 2026 VLA survey. Body backfilled from entity metadata
    by scripts/backfill_critical_entities.py.  [2026-08-13] body upgraded to textbook-grade (.staging/textbook_grade_run/b5):
    zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters + first-hand sources (number whitelist audit passed);
    en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_libero_paper
  type: paper
  title: 'LIBERO: Benchmarking Knowledge Transfer for Lifelong Robot Learning'
  url: https://arxiv.org/abs/2304.13470
  date: '2023-04-26'
  accessed_at: '2026-06-22'
- id: src_libero_code
  type: website
  title: LIBERO GitHub Repository
  url: https://github.com/Lifelong-Robot-Learning/LIBERO
  date: '2023-04-26'
  accessed_at: '2026-06-22'
related_entities:
- id: ent_paper_wang_vla_survey_2026
  relationship: cites
  description:
    en: Wang et al. 2026 survey cites LIBERO as a representative short-horizon tabletop VLA benchmark.
    zh: Wang 等人 2026 综述将 LIBERO 引用为代表性短程桌面 VLA 基准。
    ko: Wang et al. 2026 서베이는 LIBERO를 대표적인 단기 테이블탑 VLA 벤치마크로 인용함.
theoretical_depth:
- system
---
## 概述

LIBERO（*Benchmarking Knowledge Transfer for Lifelong Robot Learning*，NeurIPS 2023）是一个面向终身机器人学习的基准与数据集，通过程序化生成流水线构建桌面短程操作任务，将"泛化"解耦为空间布局、物体类别、目标指令与长时序组合四个可独立测量的维度。它提供 130 个任务、四个任务套件与三种视觉运动策略架构，已成为 VLA（视觉-语言-动作）模型报告泛化性能的默认评测选项之一。

## 核心内容

### 是什么：定义与定位

LIBERO 是一个用于研究多任务与终身机器人学习中**知识迁移**问题的基准与数据集。它同时提供两类知识的学习载体：关于物体与空间关系的**陈述性知识**（declarative knowledge），以及关于运动与行为的**程序性知识**（procedural knowledge）。其核心研究问题不是单任务成功率，而是机器人学习器在一系列任务上顺序学习时，能否把先前任务的知识用于加速后续学习、并避免灾难性遗忘。

LIBERO 属于评测基准类型，英文名称为 *LIBERO*，全称为 *Benchmarking Knowledge Transfer for Lifelong Robot Learning*，发表于 NeurIPS 2023（arXiv:2304.13470）。它提供程序化的物体与场景变化机制，并按变化维度组织任务套件，从而把"泛化"从模糊口号拆成可独立测量的维度。它真正改变的不是单任务成功率的记录方式，而是**知识迁移能力的可量化评估范式**——让"学得快不快"第一次有了标准化的受控测试平台。

### 为什么存在：痛点与历史定位

在 LIBERO 出现之前，机器人操作基准面临三个结构性痛点：

1. **泛化无法归因**：传统基准只报告平均成功率，无法回答"模型失败是因为空间推理弱、物体识别差，还是指令理解不足"。
2. **任务生成不可扩展**：人工设计任务场景成本高、数量有限，难以支撑大规模策略训练与评测。
3. **终身学习缺乏受控载体**：持续学习研究需要"任务序列"式的实验设计，而当时缺乏共享物体集与程序化场景变化的标准化任务套件。

LIBERO 的历史定位在于：它首次将"知识迁移"作为基准设计的中心目标，通过**受控分布偏移**（controlled distribution shifts）将泛化能力拆解为可独立测量的维度。这一设计思想被后续基准广泛继承，成为 VLA 时代评测方法论的基石之一。

### 原理拆解

**① 程序化生成流水线：无限任务的来源**

LIBERO 的核心机制是一条程序化生成流水线（procedural generation pipeline），它能够"原则上生成无限多个操作任务"。流水线通过组合物体类型、空间布局、目标指令等要素，自动生成带标注的任务场景与演示数据。这一设计解决了人工任务设计的扩展性瓶颈，使研究者可以按需生成大规模、多样化的训练与评测任务。

**② 四任务套件：泛化维度的解耦**

LIBERO 提供 130 个任务，分为四个任务套件：

| 套件名称 | 变化维度 | 考察能力 | 设计意图 |
|----------|----------|----------|----------|
| LIBERO-Spatial | 空间布局变化 | 空间推理与坐标变换 | 受控分布偏移，要求特定类型知识迁移 |
| LIBERO-Object | 物体类别变化 | 视觉-语义迁移 | 受控分布偏移，要求特定类型知识迁移 |
| LIBERO-Goal | 目标（指令）变化 | 语言理解与目标达成 | 受控分布偏移，要求特定类型知识迁移 |
| LIBERO-100 | 多维度纠缠变化 | 综合知识迁移 | 100 个任务，需迁移纠缠知识 |

其中 LIBERO-100 进一步拆分为 **LIBERO-90**（用于预训练策略）与 **LIBERO-10**（用于测试智能体下游终身学习性能）。前三个套件具有受控分布偏移，意味着它们要求迁移**特定类型**的知识；而 LIBERO-100 要求迁移**纠缠的**知识，更接近真实世界的复杂迁移场景。

**③ 评测协议：从训练到评估的完整闭环**

LIBERO 提供五个研究主题、三种视觉运动策略网络架构、三种终身学习算法，以及顺序微调（sequential finetuning）与多任务学习（multitask learning）基线。研究者可以在统一的代码框架内完成数据加载、策略训练与评测，保证了跨论文结果的可比性。

### 关键参数与规格

| 参数项 | 数值/内容 | 来源 |
|--------|-----------|------|
| 任务总数 | 130 个 | GitHub 仓库 |
| 任务套件数 | 4 个（Spatial / Object / Goal / 100） | GitHub 仓库 |
| LIBERO-100 拆分 | LIBERO-90（预训练）+ LIBERO-10（终身学习测试） | GitHub 仓库 |
| 研究主题数 | 5 个 | GitHub 仓库 |
| 视觉运动策略架构 | 3 种 | GitHub 仓库 |
| 终身学习算法 | 3 种 + 2 个基线 | GitHub 仓库 |
| 发表信息 | NeurIPS 2023，arXiv:2304.13470 | 语料 |
| 环境形态 | 桌面单臂 | 语料 |
| 主要评测对象 | 知识迁移/终身学习 | 语料 |

### 横向对比

| 基准 | 主要评测对象 | 形态 | 任务规模 | 特色 |
|------|--------------|------|----------|------|
| HumanoidBench | 全身运动 + 操作策略 | 双足人形（Unitree H1） | 40+ 任务 | 人形专用、受控比较 |
| LIBERO | 知识迁移/终身学习 | 桌面单臂 | 130 任务 | 变化维度解耦 |
| LIBERO-Plus | VLA 鲁棒性 | 桌面单臂 | 10,030 任务 | 七维扰动扫描 |
| ManiSkill | 可泛化操作技能 | 多形态 | 大量任务 | 高吞吐、真实感观测 |
| Isaac Gym 基准 | RL 训练与评测吞吐 | 多形态 | 任务集 | GPU 端到端加速 |

需要警惕的是**基准不可通约性**：不同基准的成功率数字没有横向可比性，任何"A 模型在基准 X 上 90%，B 模型在基准 Y 上 80%"式的跨基准比较都是无效论证。跨模型比较必须锚定同一基准、同一协议、同一评测种子分布。

### 谁在用：应用案例

**① VLA 模型的标准评测场**

LIBERO 对 VLA 时代的影响是深远的：它提供的标准化任务套件与评测协议，成为大量 VLA 模型论文报告泛化性能的默认选项之一。例如，OpenVLA（约 70 亿参数，采用 DINOv2+SigLIP 双视觉编码器与 Llama 系语言骨干，在 Open X-Embodiment 数据集约 97 万条真实机器人片段上预训练）即在 LIBERO 等基准上超越此前的开源基线。其价值不在于某个单项指标，而在于全开放：权重、训练代码与数据配方全部公开，且支持 LoRA 等参数高效微调。

**② 泛化维度的系统评测**

VLA 评测通常按维度组织：**指令内泛化**（同任务新表述）、**视觉泛化**（新物体/新背景/新视角）、**语义泛化**（新指令组合）、**物理泛化**（新物体属性如重量、摩擦）。LIBERO 的四任务套件正是这一维度化思想的具体实现——LIBERO-SPATIAL 对应空间泛化，LIBERO-OBJECT 对应物体泛化，LIBERO-GOAL 对应指令泛化，LIBERO-LONG 对应长时程目标保持与恢复。

**③ 开源生态的验证阶梯**

对工程团队而言，LIBERO/LIBERO-Plus 构成从仿真到真机的验证阶梯的一部分。务实的技术选型路径通常是：先用 LeRobot + SmolVLA 在低成本臂上跑通数据闭环，再以 OpenVLA 或 π0 类模型作为性能基线，最后在目标人形平台上做双系统化改造与机载部署优化——LIBERO 在此路径中承担"标准化仿真评测"的角色。

### 局限与边界

1. **桌面场景限制**：LIBERO 局限于桌面单臂操作，不涉及移动、全身协调或长时程任务组合（需自行确认其是否覆盖多阶段任务，语料中 LIBERO-LONG 涉及长时程，但整体仍属桌面范畴）。
2. **仿真-真机差距**：仿真成功率与真机成功率之间的相关性并不总是成立，尤其在接触富集任务上。严谨的评估流程应当以仿真做大规模筛选、以真机做最终确认。
3. **基准过拟合风险**：任何公开基准一旦成为社区焦点，都会触发古德哈特定律（"指标成为目标时便不再是好指标"）。模型开始针对基准任务过拟合，基准分数与实际能力的相关性衰减。LIBERO-Plus 式的扰动扩展是缓解手段之一。
4. **不覆盖场景级评测**：LIBERO 属于 L3 系统级评测，不涉及 L4 场景级指标（如连续 8 小时班次作业、MTBF、非受控环境泛化）。

### 常见误区

1. **"LIBERO 是单任务成功率基准"**——错。它的核心研究问题是知识迁移与终身学习，单任务成功率只是手段而非目的。其设计重心在于"按变化维度解耦"的受控分布偏移。
2. **"LIBERO 的成功率可以跨基准比较"**——错。不同基准的成功率数字没有横向可比性，跨基准比较必须锚定同一基准、同一协议、同一评测种子分布。
3. **"LIBERO 分数高就代表真机表现好"**——错。仿真成功率与真机成功率之间的相关性并不总是成立，尤其在接触富集任务上。仿真做大规模筛选、真机做最终确认才是严谨流程。
4. **"LIBERO 只有四个任务套件"**——部分正确。四个套件是核心，但 LIBERO-100 内部还拆分为 LIBERO-90 与 LIBERO-10，且程序化生成流水线原则上可生成无限任务。

### 相关知识

- `ent_paper_uncovering_mitigating_positional_blind_s_2026` — 该工作揭示 VLA 模型在工作空间内存在系统性失败区域，挑战了 LIBERO 默认"能力空间均匀"的评测假设，将评估粒度从任务级推进到空间单元级。
- `ent_paper_chung_rethinking_progression_of_memo_2025` — 该工作提出 Embodied-SlotSSM 并在 LIBERO-Mem 及 LIBERO 通用任务上验证了非马尔可夫推理能力，是 LIBERO 生态向记忆维度扩展的代表性工作。
- `ent_paper_gate_not_cache_gate_provenance_bounds_2026` — 该工作针对 VLA 闭环控制中的 token 跳过机制，在 LIBERO 类任务上证明门控信号来源而非跳过机制本身决定可靠性，对 LIBERO 评测协议中的推理效率维度有方法论意义。
- `ent_paper_guo_on_robustness_of_vision_langua_2025` — 该工作系统研究 VLA 模型的鲁棒性，与 LIBERO-Plus 的七维扰动扫描思想一脉相承，共同推动评测从"任务数量"转向"扰动维度的系统性"。
- `ent_paper_xu_vla_cache_efficient_vision_lan_2025` — 该工作提出 KV 缓存复用加速 VLA 推理，其评测工作点（如 0.9 skip ratio）多在 LIBERO 类基准上报告，是 LIBERO 生态中推理效率维度的代表工作。

## 参考

- [LIBERO: Benchmarking Knowledge Transfer for Lifelong Robot Learning (arXiv:2304.13470)](https://arxiv.org/abs/2304.13470)
- [LIBERO GitHub 仓库](https://github.com/Lifelong-Robot-Learning/LIBERO)
- [第 25 章：人形机器人评测方法论](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-25.md)
- [第 19 章：VLA 模型与开源生态](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-19.md)

## Overview

LIBERO (*Benchmarking Knowledge Transfer for Lifelong Robot Learning*, NeurIPS 2023) is a benchmark and dataset for lifelong robot learning. It constructs tabletop short-horizon manipulation tasks through a procedural generation pipeline, decoupling "generalization" into four independently measurable dimensions: spatial layout, object category, goal instruction, and long-horizon composition. It provides 130 tasks, four task suites, and three visuomotor policy architectures, and has become one of the default evaluation options for VLA (Vision-Language-Action) models reporting generalization performance.

## Content

### What it is: Definition and Positioning

LIBERO is a benchmark and dataset for studying **knowledge transfer** in multi-task and lifelong robot learning. It simultaneously provides learning vehicles for two types of knowledge: **declarative knowledge** about object and spatial relationships, and **procedural knowledge** about motion and behavior. Its core research question is not single-task success rate, but whether a robot learner, when sequentially learning across a series of tasks, can leverage knowledge from previous tasks to accelerate subsequent learning while avoiding catastrophic forgetting.

LIBERO is a benchmark of the evaluation type, with the English name *LIBERO*, full name *Benchmarking Knowledge Transfer for Lifelong Robot Learning*, published at NeurIPS 2023 (arXiv:2304.13470). It provides procedural object and scene variation mechanisms and organizes task suites by variation dimension, thereby breaking "generalization" down from a vague slogan into independently measurable dimensions. What it truly changes is not how single-task success rates are recorded, but the **quantifiable evaluation paradigm for knowledge transfer capability**—providing, for the first time, a standardized controlled testbed for "how quickly something is learned."

### Why it exists: Pain Points and Historical Positioning

Before LIBERO, robot manipulation benchmarks faced three structural pain points:

1. **Generalization cannot be attributed**: Traditional benchmarks only report average success rates, unable to answer "whether a model fails due to weak spatial reasoning, poor object recognition, or insufficient instruction understanding."
2. **Task generation is not scalable**: Manually designing task scenarios is costly and limited in quantity, making it difficult to support large-scale policy training and evaluation.
3. **Lifelong learning lacks a controlled vehicle**: Continual learning research requires "task sequence"-style experimental designs, but standardized task suites with shared object sets and procedural scene variations were lacking at the time.

LIBERO's historical positioning lies in this: it was the first to make "knowledge transfer" the central goal of benchmark design, decomposing generalization capability into independently measurable dimensions through **controlled distribution shifts**. This design philosophy has been widely inherited by subsequent benchmarks and has become one of the cornerstones of evaluation methodology in the VLA era.

### Mechanism Breakdown

**① Procedural Generation Pipeline: The Source of Unlimited Tasks**

LIBERO's core mechanism is a procedural generation pipeline that can "in principle generate infinitely many manipulation tasks." The pipeline automatically generates annotated task scenes and demonstration data by combining elements such as object types, spatial layouts, and goal instructions. This design solves the scalability bottleneck of manual task design, allowing researchers to generate large-scale, diverse training and evaluation tasks on demand.

**② Four Task Suites: Decoupling Generalization Dimensions**

LIBERO provides 130 tasks, organized into four task suites:

| Suite Name | Variation Dimension | Capability Assessed | Design Intent |
|------------|---------------------|---------------------|---------------|
| LIBERO-Spatial | Spatial layout variation | Spatial reasoning and coordinate transformation | Controlled distribution shift, requiring specific types of knowledge transfer |
| LIBERO-Object | Object category variation | Visual-semantic transfer | Controlled distribution shift, requiring specific types of knowledge transfer |
| LIBERO-Goal | Goal (instruction) variation | Language understanding and goal achievement | Controlled distribution shift, requiring specific types of knowledge transfer |
| LIBERO-100 | Multi-dimensional entangled variation | Comprehensive knowledge transfer | 100 tasks, requiring transfer of entangled knowledge |

Among these, LIBERO-100 is further split into **LIBERO-90** (for pretraining policies) and **LIBERO-10** (for testing an agent's downstream lifelong learning performance). The first three suites feature controlled distribution shifts, meaning they require transferring **specific types** of knowledge; LIBERO-100, by contrast, requires transferring **entangled** knowledge, closer to the complex transfer scenarios of the real world.

**③ Evaluation Protocol: A Complete Loop from Training to Assessment**

LIBERO provides five research topics, three visuomotor policy network architectures, three lifelong learning algorithms, and sequential finetuning and multitask learning baselines. Researchers can complete data loading, policy training, and evaluation within a unified code framework, ensuring cross-paper comparability of results.

### Key Parameters and Specifications

| Parameter | Value/Content | Source |
|-----------|---------------|--------|
| Total tasks | 130 | GitHub repository |
| Number of task suites | 4 (Spatial / Object / Goal / 100) | GitHub repository |
| LIBERO-100 split | LIBERO-90 (pretraining) + LIBERO-10 (lifelong learning test) | GitHub repository |
| Number of research topics | 5 | GitHub repository |
| Visuomotor policy architectures | 3 | GitHub repository |
| Lifelong learning algorithms | 3 + 2 baselines | GitHub repository |
| Publication info | NeurIPS 2023, arXiv:2304.13470 | Corpus |
| Environment form | Tabletop single-arm | Corpus |
| Primary evaluation target | Knowledge transfer / lifelong learning | Corpus |

### Horizontal Comparison

| Benchmark | Primary Evaluation Target | Form | Task Scale | Features |
|-----------|---------------------------|------|------------|----------|
| HumanoidBench | Whole-body motion + manipulation policies | Bipedal humanoid (Unitree H1) | 40+ tasks | Humanoid-specific, controlled comparison |
| LIBERO | Knowledge transfer / lifelong learning | Tabletop single-arm | 130 tasks | Decoupled variation dimensions |
| LIBERO-Plus | VLA robustness | Tabletop single-arm | 10,030 tasks | Seven-dimensional perturbation sweep |
| ManiSkill | Generalizable manipulation skills | Multi-form | Large task set | High throughput, realistic observations |
| Isaac Gym Benchmark | RL training and evaluation throughput | Multi-form | Task set | GPU end-to-end acceleration |

A key caveat is **benchmark incommensurability**: success rate numbers across different benchmarks are not horizontally comparable, and any cross-benchmark comparison such as "Model A achieves 90% on Benchmark X, Model B achieves 80% on Benchmark Y" is invalid. Cross-model comparisons must be anchored to the same benchmark, the same protocol, and the same evaluation seed distribution.

### Who uses it: Application Cases

**① Standard Evaluation Ground for VLA Models**

LIBERO's impact on the VLA era is profound: its standardized task suites and evaluation protocols have become one of the default options for VLA model papers reporting generalization performance. For example, OpenVLA (approximately 7B parameters, employing a DINOv2+SigLIP dual vision encoder and a Llama-family language backbone, pretrained on approximately 970K real robot episodes from the Open X-Embodiment dataset) surpasses previous open-source baselines on benchmarks such as LIBERO. Its value lies not in any single metric but in full openness: weights, training code, and data recipes are all publicly released, and it supports parameter-efficient fine-tuning such as LoRA.

**② Systematic Evaluation of Generalization Dimensions**

VLA evaluation is typically organized by dimension: **intra-instruction generalization** (new phrasing of the same task), **visual generalization** (new objects/backgrounds/viewpoints), **semantic generalization** (new instruction combinations), and **physical generalization** (new object properties such as weight and friction). LIBERO's four task suites are a concrete implementation of this dimensionalized thinking—LIBERO-SPATIAL corresponds to spatial generalization, LIBERO-OBJECT to object generalization, LIBERO-GOAL to instruction generalization, and LIBERO-LONG to long-horizon goal maintenance and recovery.

**③ Validation Ladder for the Open-Source Ecosystem**

For engineering teams, LIBERO/LIBERO-Plus form part of a validation ladder from simulation to real robots. A pragmatic technology selection path typically proceeds as follows: first use LeRobot + SmolVLA on a low-cost arm to close the data loop, then use OpenVLA or π0-class models as performance baselines, and finally perform dual-system transformation and onboard deployment optimization on the target humanoid platform—LIBERO plays the role of "standardized simulation evaluation" in this path.

### Limitations and Boundaries

1. **Tabletop scenario limitations**: LIBERO is confined to tabletop single-arm manipulation, not involving mobility, whole-body coordination, or long-horizon task composition (whether it covers multi-stage tasks requires confirmation; LIBERO-LONG in the corpus involves long horizons, but the overall scope remains tabletop).
2. **Sim-to-real gap**: The correlation between simulation success rates and real-robot success rates does not always hold, especially on contact-rich tasks. A rigorous evaluation process should use simulation for large-scale screening and real robots for final confirmation.
3. **Benchmark overfitting risk**: Any public benchmark that becomes a community focus triggers Goodhart's law ("when a metric becomes a target, it ceases to be a good metric"). Models begin overfitting to benchmark tasks, and the correlation between benchmark scores and actual capability decays. Perturbation extensions like LIBERO-Plus are one mitigation approach.
4. **No coverage of scenario-level evaluation**: LIBERO belongs to L3 system-level evaluation and does not cover L4 scenario-level metrics (such as continuous 8-hour shift operations, MTBF, or uncontrolled environment generalization).

### Common Misconceptions

1. **"LIBERO is a single-task success rate benchmark"**—Wrong. Its core research question is knowledge transfer and lifelong learning; single-task success rate is a means, not an end. Its design focus lies in "decoupling by variation dimension" through controlled distribution shifts.
2. **"LIBERO success rates can be compared across benchmarks"**—Wrong. Success rate numbers across different benchmarks are not horizontally comparable; cross-benchmark comparisons must be anchored to the same benchmark, the same protocol, and the same evaluation seed distribution.
3. **"High LIBERO scores imply good real-robot performance"**—Wrong. The correlation between simulation success rates and real-robot success rates does not always hold, especially on contact-rich tasks. Using simulation for large-scale screening and real robots for final confirmation is the rigorous process.
4. **"LIBERO has only four task suites"**—Partially correct. The four suites are the core, but LIBERO-100 is internally split into LIBERO-90 and LIBERO-10, and the procedural generation pipeline can in principle generate unlimited tasks.

### Related Knowledge

- `ent_paper_uncovering_mitigating_positional_blind_s_2026` — This work reveals that VLA models exhibit systematic failure regions within the workspace, challenging LIBERO's default assumption of "uniform capability space" and advancing evaluation granularity from the task level to the spatial unit level.
- `ent_paper_chung_rethinking_progression_of_memo_2025` — This work proposes Embodied-SlotSSM and validates non-Markovian reasoning capabilities on LIBERO-Mem and general LIBERO tasks, representing a notable effort extending the LIBERO ecosystem toward the memory dimension.
- `ent_paper_gate_not_cache_gate_provenance_bounds_2026` — This work addresses token skipping mechanisms in VLA closed-loop control, demonstrating on LIBERO-like tasks that the provenance of gating signals, rather than the skipping mechanism itself, determines reliability—carrying methodological significance for the inference efficiency dimension of LIBERO evaluation protocols.
- `ent_paper_guo_on_robustness_of_vision_langua_2025` — This work systematically studies VLA model robustness, sharing intellectual lineage with LIBERO-Plus's seven-dimensional perturbation sweep, jointly advancing evaluation from "task quantity" toward "systematicity of perturbation dimensions."
- `ent_paper_xu_vla_cache_efficient_vision_lan_2025` — This work proposes KV cache reuse to accelerate VLA inference, with evaluation operating points (e.g., 0.9 skip ratio) mostly reported on LIBERO-like benchmarks, representing the inference efficiency dimension within the LIBERO ecosystem.

## 개요

LIBERO(*Benchmarking Knowledge Transfer for Lifelong Robot Learning*, NeurIPS 2023)는 평생 로봇 학습을 위한 벤치마크 및 데이터셋으로, 프로그램적 생성 파이프라인을 통해 데스크톱 단거리 조작 작업을 구축하고 "일반화"를 공간 레이아웃, 객체 범주, 목표 지시, 장기 시퀀스 조합의 네 가지 독립적으로 측정 가능한 차원으로 분해합니다. 130개의 작업, 네 가지 작업 스위트, 세 가지 비주얼-운동 정책 아키텍처를 제공하며, VLA(비전-언어-행동) 모델의 일반화 성능을 보고하는 기본 평가 옵션 중 하나가 되었습니다.

## 핵심 내용

### 무엇인가: 정의 및 위치

LIBERO는 다중 작업 및 평생 로봇 학습에서 **지식 전이** 문제를 연구하기 위한 벤치마크이자 데이터셋입니다. 객체와 공간 관계에 관한 **서술적 지식**(declarative knowledge)과 운동 및 행동에 관한 **절차적 지식**(procedural knowledge)이라는 두 가지 유형의 지식 학습 매개체를 동시에 제공합니다. 핵심 연구 문제는 단일 작업 성공률이 아니라, 로봇 학습자가 일련의 작업을 순차적으로 학습할 때 이전 작업의 지식을 활용하여 후속 학습을 가속화하고 치명적 망각을 피할 수 있는지 여부입니다.

LIBERO는 평가 벤치마크 유형에 속하며, 영문 명칭은 *LIBERO*, 전체 명칭은 *Benchmarking Knowledge Transfer for Lifelong Robot Learning*으로, NeurIPS 2023(arXiv:2304.13470)에 게재되었습니다. 프로그램적 객체 및 장면 변화 메커니즘을 제공하고 변화 차원에 따라 작업 스위트를 구성하여 "일반화"를 모호한 구호에서 독립적으로 측정 가능한 차원으로 분해합니다. 실제로 변화시킨 것은 단일 작업 성공률의 기록 방식이 아니라 **지식 전이 능력의 정량적 평가 패러다임**입니다. "얼마나 빨리 배우는가"에 대해 처음으로 표준화된 통제 테스트 플랫폼을 제공한 것입니다.

### 왜 존재하는가: 문제점 및 역사적 위치

LIBERO가 등장하기 전, 로봇 조작 벤치마크는 세 가지 구조적 문제점에 직면했습니다:

1. **일반화 귀인 불가**: 기존 벤치마크는 평균 성공률만 보고하여 "모델 실패가 공간 추론 부족, 객체 인식 부족, 또는 지시 이해 부족 때문인지"에 답할 수 없었습니다.
2. **작업 생성 확장성 부족**: 수작업으로 설계된 작업 장면은 비용이 높고 수량이 제한적이어서 대규모 정책 훈련 및 평가를 지원하기 어려웠습니다.
3. **평생 학습의 통제된 매개체 부재**: 지속 학습 연구는 "작업 시퀀스" 형태의 실험 설계가 필요했지만, 공유 객체 집합과 프로그램적 장면 변화를 갖춘 표준화된 작업 스위트가 부재했습니다.

LIBERO의 역사적 위치는 "지식 전이"를 벤치마크 설계의 중심 목표로 처음 삼고, **통제된 분포 이동**(controlled distribution shifts)을 통해 일반화 능력을 독립적으로 측정 가능한 차원으로 분해한 데 있습니다. 이 설계 사상은 이후 벤치마크에 널리 계승되어 VLA 시대 평가 방법론의 초석 중 하나가 되었습니다.

### 원리 분해

**① 프로그램적 생성 파이프라인: 무한 작업의 원천**

LIBERO의 핵심 메커니즘은 "원칙적으로 무한한 조작 작업을 생성할 수 있는" 프로그램적 생성 파이프라인(procedural generation pipeline)입니다. 파이프라인은 객체 유형, 공간 레이아웃, 목표 지시 등의 요소를 조합하여 주석이 달린 작업 장면과 시연 데이터를 자동으로 생성합니다. 이 설계는 수작업 작업 설계의 확장성 병목을 해결하여 연구자가 필요에 따라 대규모의 다양한 훈련 및 평가 작업을 생성할 수 있게 합니다.

**② 네 가지 작업 스위트: 일반화 차원의 분해**

LIBERO는 130개의 작업을 제공하며, 네 가지 작업 스위트로 나뉩니다:

| 스위트 이름 | 변화 차원 | 평가 능력 | 설계 의도 |
|----------|----------|----------|----------|
| LIBERO-Spatial | 공간 레이아웃 변화 | 공간 추론 및 좌표 변환 | 통제된 분포 이동, 특정 유형의 지식 전이 요구 |
| LIBERO-Object | 객체 범주 변화 | 시각-의미 전이 | 통제된 분포 이동, 특정 유형의 지식 전이 요구 |
| LIBERO-Goal | 목표(지시) 변화 | 언어 이해 및 목표 달성 | 통제된 분포 이동, 특정 유형의 지식 전이 요구 |
| LIBERO-100 | 다차원 얽힌 변화 | 종합 지식 전이 | 100개 작업, 얽힌 지식 전이 필요 |

여기서 LIBERO-100은 **LIBERO-90**(사전 훈련 정책용)과 **LIBERO-10**(에이전트의 하위 평생 학습 성능 테스트용)으로 더 세분화됩니다. 처음 세 스위트는 통제된 분포 이동을 가지므로 **특정 유형**의 지식 전이를 요구합니다. 반면 LIBERO-100은 **얽힌** 지식 전이를 요구하여 실제 세계의 복잡한 전이 시나리오에 더 가깝습니다.

**③ 평가 프로토콜: 훈련부터 평가까지의 완전한 폐루프**

LIBERO는 5가지 연구 주제, 3가지 비주얼-운동 정책 네트워크 아키텍처, 3가지 평생 학습 알고리즘, 그리고 순차 미세 조정(sequential finetuning) 및 다중 작업 학습(multitask learning) 기준선을 제공합니다. 연구자는 통일된 코드 프레임워크 내에서 데이터 로딩, 정책 훈련 및 평가를 완료할 수 있어 논문 간 결과의 비교 가능성을 보장합니다.

### 주요 매개변수 및 사양

| 매개변수 항목 | 값/내용 | 출처 |
|--------|-----------|------|
| 총 작업 수 | 130개 | GitHub 저장소 |
| 작업 스위트 수 | 4개(Spatial / Object / Goal / 100) | GitHub 저장소 |
| LIBERO-100 분할 | LIBERO-90(사전 훈련) + LIBERO-10(평생 학습 테스트) | GitHub 저장소 |
| 연구 주제 수 | 5개 | GitHub 저장소 |
| 비주얼-운동 정책 아키텍처 | 3가지 | GitHub 저장소 |
| 평생 학습 알고리즘 | 3가지 + 2개 기준선 | GitHub 저장소 |
| 게재 정보 | NeurIPS 2023, arXiv:2304.13470 | 말뭉치 |
| 환경 형태 | 데스크톱 단일 팔 | 말뭉치 |
| 주요 평가 대상 | 지식 전이/평생 학습 | 말뭉치 |

### 횡적 비교

| 벤치마크 | 주요 평가 대상 | 형태 | 작업 규모 | 특징 |
|------|--------------|------|----------|------|
| HumanoidBench | 전신 운동 + 조작 정책 | 이족 보행 휴머노이드(Unitree H1) | 40+ 작업 | 휴머노이드 전용, 통제 비교 |
| LIBERO | 지식 전이/평생 학습 | 데스크톱 단일 팔 | 130개 작업 | 변화 차원 분해 |
| LIBERO-Plus | VLA 견고성 | 데스크톱 단일 팔 | 10,030개 작업 | 7차원 교란 스캔 |
| ManiSkill | 일반화 가능한 조작 기술 | 다중 형태 | 대량 작업 | 고처리량, 사실적 관측 |
| Isaac Gym 벤치마크 | RL 훈련 및 평가 처리량 | 다중 형태 | 작업 집합 | GPU 종단 간 가속 |

주의해야 할 점은 **벤치마크 비공약성**(benchmark incommensurability)입니다: 서로 다른 벤치마크의 성공률 수치는 횡적 비교가 불가능하며, "모델 A가 벤치마크 X에서 90%, 모델 B가 벤치마크 Y에서 80%"와 같은 교차 벤치마크 비교는 무효한 논증입니다. 교차 모델 비교는 동일한 벤치마크, 동일한 프로토콜, 동일한 평가 시드 분포에 고정되어야 합니다.

### 누가 사용하는가: 적용 사례

**① VLA 모델의 표준 평가장**

LIBERO가 VLA 시대에 미친 영향은 깊습니다: 제공하는 표준화된 작업 스위트와 평가 프로토콜은 많은 VLA 모델 논문이 일반화 성능을 보고하는 기본 옵션 중 하나가 되었습니다. 예를 들어, OpenVLA(약 70억 파라미터, DINOv2+SigLIP 이중 비전 인코더와 Llama 계열 언어 백본을 사용, Open X-Embodiment 데이터셋의 약 97만 개 실제 로봇 클립에서 사전 훈련)는 LIBERO 등 벤치마크에서 이전의 오픈소스 기준선을 능가했습니다. 그 가치는 특정 단일 지표가 아니라 완전한 개방성에 있습니다: 가중치, 훈련 코드, 데이터 레시피가 모두 공개되어 있으며 LoRA와 같은 파라미터 효율적 미세 조정을 지원합니다.

**② 일반화 차원의 체계적 평가**

VLA 평가는 일반적으로 차원별로 구성됩니다: **지시 내 일반화**(동일 작업의 새로운 표현), **시각적 일반화**(새 객체/새 배경/새 시점), **의미적 일반화**(새 지시 조합), **물리적 일반화**(무게, 마찰과 같은 새 객체 속성). LIBERO의 네 가지 작업 스위트는 바로 이러한 차원화 사상의 구체적 구현입니다. LIBERO-SPATIAL은 공간 일반화, LIBERO-OBJECT는 객체 일반화, LIBERO-GOAL은 지시 일반화, LIBERO-LONG은 장기 목표 유지 및 복구에 해당합니다.

**③ 오픈소스 생태계의 검증 사다리**

엔지니어링 팀에게 LIBERO/LIBERO-Plus는 시뮬레이션에서 실제 로봇까지의 검증 사다리의 일부를 구성합니다. 실용적인 기술 선택 경로는 일반적으로: 먼저 LeRobot + SmolVLA로 저비용 팔에서 데이터 폐루프를 구동하고, 그다음 OpenVLA 또는 π0 유형 모델을 성능 기준선으로 사용하며, 마지막으로 목표 휴머노이드 플랫폼에서 이중 시스템화 개조와 온보드 배포 최적화를 수행합니다. LIBERO는 이 경로에서 "표준화된 시뮬레이션 평가" 역할을 담당합니다.

### 한계 및 경계

1. **데스크톱 장면 제한**: LIBERO는 데스크톱 단일 팔 조작에 국한되며, 이동, 전신 협응 또는 장기 작업 조합을 다루지 않습니다(다단계 작업을 포함하는지 자체 확인 필요, 말뭉치에서 LIBERO-LONG은 장기 시퀀스를 다루지만 전체적으로 여전히 데스크톱 범주에 속함).
2. **시뮬레이션-실제 격차**: 시뮬레이션 성공률과 실제 로봇 성공률 간의 상관관계는 항상 성립하지 않으며, 특히 접촉이 풍부한 작업에서 그렇습니다. 엄격한 평가 프로세스는 시뮬레이션으로 대규모 선별을 하고 실제 로봇으로 최종 확인을 해야 합니다.
3. **벤치마크 과적합 위험**: 공개 벤치마크는 커뮤니티의 초점이 되면 굿하트 법칙("지표가 목표가 되면 더 이상 좋은 지표가 아니다")을 촉발합니다. 모델이 벤치마크 작업에 과적합되기 시작하면 벤치마크 점수와 실제 능력 간의 상관관계가 감소합니다. LIBERO-Plus 유형의 교란 확장은 완화 수단 중 하나입니다.
4. **장면 수준 평가 미포함**: LIBERO는 L3 시스템 수준 평가에 속하며, L4 장면 수준 지표(예: 연속 8시간 교대 작업, MTBF, 비통제 환경 일반화)를 다루지 않습니다.

### 일반적인 오해

1. **"LIBERO는 단일 작업 성공률 벤치마크다"** — 틀림. 핵심 연구 문제는 지식 전이와 평생 학습이며, 단일 작업 성공률은 수단이지 목적이 아닙니다. 설계 중심은 "변화 차원별 분해"의 통제된 분포 이동에 있습니다.
2. **"LIBERO 성공률은 교차 벤치마크 비교가 가능하다"** — 틀림. 서로 다른 벤치마크의 성공률 수치는 횡적 비교가 불가능하며, 교차 벤치마크 비교는 동일한 벤치마크, 동일한 프로토콜, 동일한 평가 시드 분포에 고정되어야 합니다.
3. **"LIBERO 점수가 높으면 실제 로봇 성능도 좋다는 뜻이다"** — 틀림. 시뮬레이션 성공률과 실제 로봇 성공률 간의 상관관계는 항상 성립하지 않으며, 특히 접촉이 풍부한 작업에서 그렇습니다. 시뮬레이션으로 대규모 선별, 실제 로봇으로 최종 확인이 엄격한 프로세스입니다.
4. **"LIBERO에는 네 가지 작업 스위트만 있다"** — 부분적으로 맞음. 네 가지 스위트가 핵심이지만, LIBERO-100 내부는 LIBERO-90과 LIBERO-10으로 더 세분화되며, 프로그램적 생성 파이프라인은 원칙적으로 무한한 작업을 생성할 수 있습니다.

### 관련 지식

- `ent_paper_uncovering_mitigating_positional_blind_s_2026` — 이 연구는 VLA 모델이 작업 공간 내에서 체계적 실패 영역을 가진다는 것을 밝혀내어 LIBERO의 기본 가정인 "능력 공간 균일성"에 도전하고, 평가 세분성을 작업 수준에서 공간 단위 수준으로 발전시켰습니다.
- `ent_paper_chung_rethinking_progression_of_memo_2025` — 이 연구는 Embodied-SlotSSM을 제안하고 LIBERO-Mem 및 LIBERO 일반 작업에서 비마르코프 추론 능력을 검증했으며, LIBERO 생태계가 메모리 차원으로 확장된 대표적 연구입니다.
- `ent_paper_gate_not_cache_gate_provenance_bounds_2026` — 이 연구는 VLA 폐루프 제어의 토큰 건너뛰기 메커니즘을 대상으로, LIBERO 유형 작업에서 게이팅 신호의 출처가 건너뛰기 메커니즘 자체보다 신뢰성을 결정한다는 것을 증명했으며, LIBERO 평가 프로토콜의 추론 효율성 차원에 방법론적 의미가 있습니다.
- `ent_paper_guo_on_robustness_of_vision_langua_2025` — 이 연구는 VLA 모델의 견고성을 체계적으로 연구하며, LIBERO-Plus의 7차원 교란 스캔 사상과 맥을 같이하여 평가가 "작업 수"에서 "교란 차원의 체계성"으로 전환되도록 공동으로 추진했습니다.
- `ent_paper_xu_vla_cache_efficient_vision_lan_2025` — 이 연구는 KV 캐시 재사용으로 VLA 추론을 가속화하며, 평가 작업 지점(예: 0.9 skip ratio)이 주로 LIBERO 유형 벤치마크에서 보고되어 LIBERO 생태계의 추론 효율성 차원을 대표하는 연구입니다.
