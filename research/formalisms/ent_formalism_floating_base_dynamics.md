---
$id: ent_formalism_floating_base_dynamics
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: formalism
names:
  en: Floating-Base Dynamics
  zh: 浮动基动力学
  ko: 부유 기반 동역학
summary:
  en: A rigid-body dynamics formalism for robots with an unactuated or underactuated base, such as bipeds, where the base
    pose is part of the generalized coordinates.
  zh: 人形机器人不同于固定基座的工业机械臂，其基座（躯干）可在空间中自由移动。因此需要用浮动基（floating base）坐标描述系统位形：
  ko: 이족 로봇처럼 비구동·저구동 기반을 가진 로봇을 위해 기반 자세를 일반화 좌표에 포함시키는 강체 동역학 공식화.
domains:
- 06_design_engineering
layers:
- midstream
functional_roles:
- knowledge
tags:
- formalism
- chapter_8
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py.  [2026-08-12] body upgraded to
    textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters +
    first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

浮动基动力学（Floating-Base Dynamics）是描述基座（躯干）可在空间中自由平移和旋转的机器人系统运动方程的数学框架。与固定基座的工业机械臂不同，人形机器人的基座没有直接执行器驱动，其 6 个自由度只能通过脚与地面的接触力间接控制，因此本质上是一个欠驱动系统。浮动基动力学方程将基座加速度与接触力直接关联，是步态规划、全身 QP 控制与动态平衡控制的理论基石。

## 核心内容

### 是什么：准确定义

浮动基动力学是研究基座不受机械约束、可在空间中自由运动的机器人系统运动规律的动力学形式化方法。它通过一组广义坐标描述系统位形：

$$
\mathbf{q} = (\mathbf{p}_{\text{base}}, \mathbf{R}_{\text{base}}, \mathbf{q}_{\text{joints}})
$$

其中 $\mathbf{p}_{\text{base}} \in \mathbb{R}^3$ 为基座位置，$\mathbf{R}_{\text{base}} \in SO(3)$ 为基座姿态，$\mathbf{q}_{\text{joints}} \in \mathbb{R}^{n_j}$ 为关节角。这套坐标体系与固定基座机器人的本质区别在于：基座的 6 个自由度（3 个平移 + 3 个旋转）没有对应的执行器，它们是被动自由度。

### 为什么存在：痛点与历史定位

在浮动基动力学出现之前，机器人动力学研究主要面向工业机械臂——基座被螺栓固定在地面或基座上，所有自由度均由电机直接驱动，系统完全驱动（fully actuated）。但人形机器人打破了这一前提：它的躯干悬浮在空中，没有任何物理支撑将其锚定到惯性系。如果沿用固定基座动力学建模，会得到错误的结论——例如，无法解释为什么机器人站立时脚底需要产生特定大小的地面反力，也无法回答"躯干倾斜时如何通过踝关节力矩恢复平衡"这类核心问题。

浮动基动力学的历史定位在于：它把"接触力"从约束条件提升为状态变量。在固定基座模型中，接触力是末端执行器与环境交互的产物；而在浮动基模型中，接触力是维持基座姿态的唯一手段。这一视角转换使人形机器人的控制问题从"跟踪轨迹"转变为"管理接触力"，直接催生了 ZMP 稳定判据、捕获点理论和全身 QP 控制等现代平衡控制方法。

### 原理拆解

**① 质量矩阵分块：把系统拆成"上块"与"下块"**

浮动基动力学方程的标准形式为：

$$
\mathbf{M}(\mathbf{q}) \dot{\mathbf{v}} + \mathbf{C}(\mathbf{q}, \mathbf{v}) \mathbf{v} + \mathbf{g}(\mathbf{q}) = \mathbf{S}^T \boldsymbol{\tau} + \sum_{c} \mathbf{J}_c^T(\mathbf{q}) \mathbf{F}_c
$$

其中 $\mathbf{M}(\mathbf{q})$ 为包含浮动基与关节的 $n \times n$ 质量矩阵，$\mathbf{C}(\mathbf{q}, \mathbf{v})$ 为科氏力与离心力项，$\mathbf{g}(\mathbf{q})$ 为重力项，$\mathbf{S}$ 为选择矩阵（将关节力矩 $\boldsymbol{\tau}$ 映射到广义坐标空间，仅作用于关节，不作用于浮动基），$\mathbf{J}_c$ 为第 $c$ 个接触点对应的雅可比矩阵，$\mathbf{F}_c$ 为接触力。

将质量矩阵按浮动基（下标 $b$）与关节（下标 $j$）分块后，方程可拆为两组：

$$
\begin{bmatrix} \mathbf{M}_{bb} & \mathbf{M}_{bj} \\ \mathbf{M}_{jb} & \mathbf{M}_{jj} \end{bmatrix} \begin{bmatrix} \dot{\mathbf{v}}_b \\ \dot{\mathbf{v}}_j \end{bmatrix} + \begin{bmatrix} \mathbf{C}_b \\ \mathbf{C}_j \end{bmatrix} + \begin{bmatrix} \mathbf{g}_b \\ \mathbf{g}_j \end{bmatrix} = \begin{bmatrix} \mathbf{0} \\ \boldsymbol{\tau} \end{bmatrix} + \sum_{c} \begin{bmatrix} \mathbf{J}_{c,b}^T \\ \mathbf{J}_{c,j}^T \end{bmatrix} \mathbf{F}_c
$$

**② 欠驱动的数学体现：上块没有力矩项**

注意上块（浮动基）方程中 $\boldsymbol{\tau}$ 的系数为零——这正是欠驱动的数学体现。基座加速度 $\dot{\mathbf{v}}_b$ 完全由接触力 $\mathbf{F}_c$ 决定。这意味着，人形机器人无法像工业机械臂那样"直接命令"躯干运动，只能通过脚底与地面的接触力间接改变躯干姿态。这一约束贯穿所有平衡控制算法：无论是 ZMP 判据还是捕获点方法，本质上都是在回答"如何产生合适的接触力来驱动基座"。

**③ 接触力是"输入"而非"约束"**

在浮动基框架中，接触力 $\mathbf{F}_c$ 不是被动的约束反力，而是需要主动求解的变量。它们必须满足摩擦锥约束（防止打滑）和单向约束（脚只能推地、不能拉地）。这些约束与浮动基动力学联立求解，构成全身 QP 控制中的核心等式约束来源。

### 关键参数与规格

| 参数 | 符号 | 含义 | 典型特征 |
|------|------|------|----------|
| 基座位置 | $\mathbf{p}_{\text{base}}$ | 浮动基坐标系原点在惯性系中的位置 | $\in \mathbb{R}^3$ |
| 基座姿态 | $\mathbf{R}_{\text{base}}$ | 浮动基坐标系相对于惯性系的旋转 | $\in SO(3)$ |
| 广义坐标 | $\mathbf{q}$ | 描述系统位形的最小独立变量集合 | 维度 $6 + n_j$ |
| 质量矩阵 | $\mathbf{M}(\mathbf{q})$ | 包含浮动基与关节的惯性矩阵 | $n \times n$ 对称正定 |
| 选择矩阵 | $\mathbf{S}$ | 把关节力矩映射到广义坐标空间 | 仅作用于关节分量 |
| 接触雅可比 | $\mathbf{J}_c$ | 第 $c$ 个接触点的速度映射 | 行数等于接触力维度 |
| 欠驱动自由度 | — | 基座的 6 个无执行器自由度 | 3 平移 + 3 旋转 |

### 横向对比

| 维度 | 固定基座动力学 | 浮动基动力学 |
|------|---------------|-------------|
| 基座状态 | 固定不动，无基座坐标 | 6 个自由漂浮自由度 |
| 驱动特性 | 完全驱动（所有自由度有执行器） | 欠驱动（基座无执行器） |
| 接触力角色 | 末端执行器的交互产物 | 控制基座的核心输入 |
| 典型应用 | 工业机械臂轨迹跟踪 | 人形机器人步态与平衡控制 |
| 方程结构 | $\mathbf{M}\ddot{\mathbf{q}} + \mathbf{C}\dot{\mathbf{q}} + \mathbf{g} = \boldsymbol{\tau}$ | $\mathbf{M}\dot{\mathbf{v}} + \mathbf{C}\mathbf{v} + \mathbf{g} = \mathbf{S}^T\boldsymbol{\tau} + \sum \mathbf{J}_c^T \mathbf{F}_c$ |

### 谁在用·应用案例

**全身 QP 控制**：现代人形机器人控制器（如 Atlas、Digit 等平台）在每个控制周期求解一个二次规划问题，其动力学等式约束正是浮动基方程。任务跟踪目标 + 动力学等式 + 摩擦锥不等式 + 力矩限，构成与现代人形机器人控制器完全一致的核心结构。实时 QP 求解器（如 OSQP、qpOASES、ProxQP）在控制周期内完成求解。

**步态规划**：浮动基动力学揭示了"基座加速度完全由接触力决定"这一本质，因此步态规划的核心任务变为设计足底接触力的时间序列，使基座沿期望轨迹运动。

**动态平衡控制**：捕获点方法把动态平衡转化为落脚点几何问题，其底层模型正是浮动基动力学——通过控制质心角动量与接触力，使机器人从任意扰动状态恢复到稳定站立。

### 局限与边界

浮动基动力学是理想化模型，存在以下边界：

1. **刚体假设**：方程假设所有连杆为刚体，未考虑结构柔性。实际人形机器人的连杆在高速运动下会产生弹性变形，导致模型失配（工程判断）。
2. **接触模型简化**：点接触或面接触假设忽略了脚底与地面之间的复杂摩擦分布和形变。
3. **计算开销**：质量矩阵求逆与科氏力项计算在自由度较高时计算量显著，实时控制需借助递归牛顿-欧拉算法或预计算技术。
4. **不适用于飞行阶段**：当机器人双脚离地（如跳跃、飞行）时，接触力项消失，系统变为完全欠驱动，需切换为质心动量模型描述。

### 常见误区

1. **"浮动基就是自由漂浮"**——错。浮动基指基座无机械约束，但机器人仍通过接触力与环境交互。自由漂浮（如太空机器人）是接触力为零的特例。
2. **"欠驱动意味着不可控"**——错。欠驱动指执行器数量少于自由度数量，但通过接触力的间接控制，系统仍可达到稳定平衡——这正是人类行走的本质。
3. **"浮动基方程与固定基座方程可以互换"**——错。固定基座方程中 $\mathbf{S} = \mathbf{I}$（单位矩阵），而浮动基方程中 $\mathbf{S}$ 将力矩仅映射到关节分量，两者结构有本质差异。
4. **"接触力是约束反力，不需要主动求解"**——错。在浮动基框架中，接触力是需要与运动方程联立求解的变量，且必须满足摩擦锥等不等式约束。

### 相关知识

- `ent_formalism_inverse_dynamics_qp` — 逆动力学 QP 以浮动基方程为等式约束，求解满足任务与约束的关节力矩与接触力。
- `ent_method_model_predictive_control` — MPC 在每个控制周期求解有限时域最优控制问题，其预测模型常采用浮动基动力学。
- `ent_paper_arachchige_dynamic_modeling_and_validatio_2023` — 该论文对双足机器人进行动态建模与验证，展示了浮动基动力学的实际应用流程。
- `ent_paper_li_design_of_a_flying_humanoid_ro_2021` — 飞行人形机器人的推力矢量控制需与浮动基动力学联立，处理飞行与地面运动的混合模式。
- `ent_paper_the_duke_humanoid_design_and_c_2024` — Duke Humanoid 的强化学习行走策略在仿真与硬件中均以浮动基动力学为环境模型。

## 参考

- [项目 Wiki 第 8 章：人形机器人设计原理](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [项目 Wiki 第 2 章：实体类型与理论深度](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)
- [项目 Wiki 第 14 章：全身控制与优化](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-14.md)

## Overview

Floating-Base Dynamics is a mathematical framework for describing the equations of motion of robotic systems whose base (torso) can translate and rotate freely in space. Unlike fixed-base industrial manipulators, the base of a humanoid robot has no direct actuators; its 6 degrees of freedom can only be controlled indirectly through contact forces with the ground, making it inherently an underactuated system. The floating-base dynamics equations directly relate base acceleration to contact forces, serving as the theoretical cornerstone for gait planning, whole-body QP control, and dynamic balance control.

## Content

### What It Is: Precise Definition

Floating-base dynamics is a formalization method for studying the motion of robotic systems whose base is not mechanically constrained and can move freely in space. It describes the system configuration using a set of generalized coordinates:

$$
\mathbf{q} = (\mathbf{p}_{\text{base}}, \mathbf{R}_{\text{base}}, \mathbf{q}_{\text{joints}})
$$

where $\mathbf{p}_{\text{base}} \in \mathbb{R}^3$ is the base position, $\mathbf{R}_{\text{base}} \in SO(3)$ is the base orientation, and $\mathbf{q}_{\text{joints}} \in \mathbb{R}^{n_j}$ are the joint angles. The essential difference from fixed-base robots is that the base's 6 degrees of freedom (3 translational + 3 rotational) have no corresponding actuators; they are passive degrees of freedom.

### Why It Exists: Pain Points and Historical Context

Before floating-base dynamics emerged, robot dynamics research primarily targeted industrial manipulators—bases bolted to the ground or a fixture, with all degrees of freedom directly driven by motors, making the system fully actuated. Humanoid robots broke this premise: their torso floats in the air with no physical support anchoring it to the inertial frame. Continuing to use fixed-base dynamics modeling would yield incorrect conclusions—for example, it cannot explain why a standing robot's feet must generate specific ground reaction forces, nor can it answer core questions like "how to recover balance through ankle torques when the torso tilts."

The historical significance of floating-base dynamics lies in elevating "contact forces" from constraint conditions to state variables. In fixed-base models, contact forces are byproducts of end-effector interaction with the environment; in floating-base models, contact forces are the only means of maintaining base posture. This shift in perspective transformed humanoid control from "trajectory tracking" to "contact force management," directly giving rise to modern balance control methods such as the ZMP stability criterion, capture point theory, and whole-body QP control.

### Principle Breakdown

**① Mass Matrix Partitioning: Splitting the System into "Upper" and "Lower" Blocks**

The standard form of the floating-base dynamics equation is:

$$
\mathbf{M}(\mathbf{q}) \dot{\mathbf{v}} + \mathbf{C}(\mathbf{q}, \mathbf{v}) \mathbf{v} + \mathbf{g}(\mathbf{q}) = \mathbf{S}^T \boldsymbol{\tau} + \sum_{c} \mathbf{J}_c^T(\mathbf{q}) \mathbf{F}_c
$$

where $\mathbf{M}(\mathbf{q})$ is the $n \times n$ mass matrix including the floating base and joints, $\mathbf{C}(\mathbf{q}, \mathbf{v})$ is the Coriolis and centrifugal term, $\mathbf{g}(\mathbf{q})$ is the gravity term, $\mathbf{S}$ is the selection matrix (mapping joint torques $\boldsymbol{\tau}$ to generalized coordinate space, acting only on joints, not on the floating base), $\mathbf{J}_c$ is the Jacobian for the $c$-th contact point, and $\mathbf{F}_c$ is the contact force.

Partitioning the mass matrix into floating base (subscript $b$) and joints (subscript $j$) blocks, the equation splits into two groups:

$$
\begin{bmatrix} \mathbf{M}_{bb} & \mathbf{M}_{bj} \\ \mathbf{M}_{jb} & \mathbf{M}_{jj} \end{bmatrix} \begin{bmatrix} \dot{\mathbf{v}}_b \\ \dot{\mathbf{v}}_j \end{bmatrix} + \begin{bmatrix} \mathbf{C}_b \\ \mathbf{C}_j \end{bmatrix} + \begin{bmatrix} \mathbf{g}_b \\ \mathbf{g}_j \end{bmatrix} = \begin{bmatrix} \mathbf{0} \\ \boldsymbol{\tau} \end{bmatrix} + \sum_{c} \begin{bmatrix} \mathbf{J}_{c,b}^T \\ \mathbf{J}_{c,j}^T \end{bmatrix} \mathbf{F}_c
$$

**② Mathematical Manifestation of Underactuation: No Torque Term in the Upper Block**

Note that the coefficient of $\boldsymbol{\tau}$ in the upper block (floating base) equation is zero—this is the mathematical manifestation of underactuation. The base acceleration $\dot{\mathbf{v}}_b$ is entirely determined by contact forces $\mathbf{F}_c$. This means a humanoid robot cannot "directly command" torso motion like an industrial manipulator; it can only indirectly alter torso posture through contact forces between the feet and the ground. This constraint permeates all balance control algorithms: whether ZMP criteria or capture point methods, they all fundamentally answer "how to generate appropriate contact forces to drive the base."

**③ Contact Forces as "Inputs" Rather Than "Constraints"**

In the floating-base framework, contact forces $\mathbf{F}_c$ are not passive constraint reactions but variables that must be actively solved. They must satisfy friction cone constraints (to prevent slipping) and unilateral constraints (feet can only push the ground, not pull). These constraints, solved jointly with the floating-base dynamics, constitute the core equality constraints in whole-body QP control.

### Key Parameters and Specifications

| Parameter | Symbol | Meaning | Typical Characteristics |
|-----------|--------|---------|------------------------|
| Base position | $\mathbf{p}_{\text{base}}$ | Position of the floating-base frame origin in the inertial frame | $\in \mathbb{R}^3$ |
| Base orientation | $\mathbf{R}_{\text{base}}$ | Rotation of the floating-base frame relative to the inertial frame | $\in SO(3)$ |
| Generalized coordinates | $\mathbf{q}$ | Minimal set of independent variables describing system configuration | Dimension $6 + n_j$ |
| Mass matrix | $\mathbf{M}(\mathbf{q})$ | Inertia matrix including floating base and joints | $n \times n$ symmetric positive definite |
| Selection matrix | $\mathbf{S}$ | Maps joint torques to generalized coordinate space | Acts only on joint components |
| Contact Jacobian | $\mathbf{J}_c$ | Velocity mapping for the $c$-th contact point | Number of rows equals contact force dimension |
| Underactuated DOFs | — | 6 actuator-free DOFs of the base | 3 translational + 3 rotational |

### Horizontal Comparison

| Dimension | Fixed-Base Dynamics | Floating-Base Dynamics |
|-----------|--------------------|------------------------|
| Base state | Fixed, no base coordinates | 6 free-floating DOFs |
| Actuation characteristics | Fully actuated (all DOFs have actuators) | Underactuated (base has no actuators) |
| Role of contact forces | Byproducts of end-effector interaction | Core inputs for controlling the base |
| Typical applications | Industrial manipulator trajectory tracking | Humanoid gait and balance control |
| Equation structure | $\mathbf{M}\ddot{\mathbf{q}} + \mathbf{C}\dot{\mathbf{q}} + \mathbf{g} = \boldsymbol{\tau}$ | $\mathbf{M}\dot{\mathbf{v}} + \mathbf{C}\mathbf{v} + \mathbf{g} = \mathbf{S}^T\boldsymbol{\tau} + \sum \mathbf{J}_c^T \mathbf{F}_c$ |

### Who Uses It: Application Cases

**Whole-Body QP Control**: Modern humanoid controllers (e.g., Atlas, Digit platforms) solve a quadratic programming problem at each control cycle, with the floating-base equation serving as the dynamic equality constraint. Task tracking objectives + dynamic equality + friction cone inequalities + torque limits form the core structure identical to modern humanoid controllers. Real-time QP solvers (e.g., OSQP, qpOASES, ProxQP) complete the solution within the control cycle.

**Gait Planning**: Floating-base dynamics reveals the essence that "base acceleration is entirely determined by contact forces," so the core task of gait planning becomes designing the time sequence of foot contact forces to drive the base along the desired trajectory.

**Dynamic Balance Control**: Capture point methods transform dynamic balance into a foot placement geometry problem, with floating-base dynamics as the underlying model—by controlling center-of-mass angular momentum and contact forces, the robot recovers from arbitrary disturbance states to stable standing.

### Limitations and Boundaries

Floating-base dynamics is an idealized model with the following boundaries:

1. **Rigid body assumption**: The equations assume all links are rigid, neglecting structural flexibility. Actual humanoid robot links undergo elastic deformation under high-speed motion, leading to model mismatch (engineering judgment).
2. **Simplified contact models**: Point or planar contact assumptions ignore the complex friction distribution and deformation between the foot and the ground.
3. **Computational cost**: Mass matrix inversion and Coriolis term computation become significantly expensive at high degrees of freedom; real-time control requires recursive Newton-Euler algorithms or precomputation techniques.
4. **Not applicable to flight phases**: When both feet leave the ground (e.g., jumping, flying), the contact force terms vanish, the system becomes fully underactuated, and a switch to the centroidal momentum model is required.

### Common Misconceptions

1. **"Floating base means free-floating"**—Wrong. Floating base means the base has no mechanical constraints, but the robot still interacts with the environment through contact forces. Free-floating (e.g., space robots) is a special case with zero contact forces.
2. **"Underactuated means uncontrollable"**—Wrong. Underactuated means the number of actuators is fewer than the number of DOFs, but through indirect control via contact forces, the system can still achieve stable equilibrium—this is the essence of human walking.
3. **"Floating-base and fixed-base equations are interchangeable"**—Wrong. In fixed-base equations, $\mathbf{S} = \mathbf{I}$ (identity matrix), while in floating-base equations, $\mathbf{S}$ maps torques only to joint components; the two have fundamentally different structures.
4. **"Contact forces are constraint reactions that don't need active solving"**—Wrong. In the floating-base framework, contact forces are variables that must be solved jointly with the equations of motion and must satisfy inequality constraints such as the friction cone.

### Related Knowledge

- `ent_formalism_inverse_dynamics_qp` — Inverse dynamics QP uses the floating-base equation as an equality constraint to solve for joint torques and contact forces satisfying tasks and constraints.
- `ent_method_model_predictive_control` — MPC solves a finite-horizon optimal control problem at each control cycle, with the prediction model often employing floating-base dynamics.
- `ent_paper_arachchige_dynamic_modeling_and_validatio_2023` — This paper performs dynamic modeling and validation of a bipedal robot, demonstrating the practical application workflow of floating-base dynamics.
- `ent_paper_li_design_of_a_flying_humanoid_ro_2021` — Thrust vector control of flying humanoid robots must be coupled with floating-base dynamics to handle hybrid modes of flight and ground locomotion.
- `ent_paper_the_duke_humanoid_design_and_c_2024` — Duke Humanoid's reinforcement learning walking policies use floating-base dynamics as the environment model in both simulation and hardware.

## 개요

부동 기반 동역학(Floating-Base Dynamics)은 베이스(몸통)가 공간에서 자유롭게 병진 및 회전할 수 있는 로봇 시스템의 운동 방정식을 설명하는 수학적 프레임워크입니다. 고정 기반 산업용 로봇 팔과 달리, 휴머노이드 로봇의 베이스는 직접적인 액추에이터 구동이 없으며, 그 6자유도는 발과 지면 사이의 접촉력을 통해서만 간접적으로 제어됩니다. 따라서 본질적으로 과소구동 시스템입니다. 부동 기반 동역학 방정식은 베이스 가속도와 접촉력을 직접 연관시키며, 보행 계획, 전신 QP 제어 및 동적 평형 제어의 이론적 기초입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

부동 기반 동역학은 베이스가 기계적 구속 없이 공간에서 자유롭게 움직일 수 있는 로봇 시스템의 운동 법칙을 연구하는 동역학적 형식화 방법입니다. 일반화 좌표 집합을 통해 시스템의 형상을 설명합니다:

$$
\mathbf{q} = (\mathbf{p}_{\text{base}}, \mathbf{R}_{\text{base}}, \mathbf{q}_{\text{joints}})
$$

여기서 $\mathbf{p}_{\text{base}} \in \mathbb{R}^3$는 베이스 위치, $\mathbf{R}_{\text{base}} \in SO(3)$는 베이스 자세, $\mathbf{q}_{\text{joints}} \in \mathbb{R}^{n_j}$는 관절 각도입니다. 이 좌표계가 고정 기반 로봇과 본질적으로 다른 점은 베이스의 6자유도(3 병진 + 3 회전)에 해당하는 액추에이터가 없으며, 이들은 수동 자유도라는 것입니다.

### 왜 존재하는가: 문제점과 역사적 위치

부동 기반 동역학이 등장하기 전, 로봇 동역학 연구는 주로 산업용 로봇 팔을 대상으로 했습니다——베이스가 바닥이나 기반에 볼트로 고정되어 있고, 모든 자유도가 모터로 직접 구동되며, 시스템은 완전 구동(fully actuated)입니다. 그러나 휴머노이드 로봇은 이 전제를 깨뜨렸습니다: 몸통이 공중에 떠 있어 관성계에 고정시키는 물리적 지지대가 없습니다. 고정 기반 동역학 모델링을 그대로 적용하면 잘못된 결론에 도달합니다——예를 들어, 로봇이 서 있을 때 발바닥에 특정 크기의 지면 반력이 필요한 이유를 설명할 수 없고, "몸통이 기울어졌을 때 발목 토크로 평형을 회복하는 방법"과 같은 핵심 질문에 답할 수 없습니다.

부동 기반 동역학의 역사적 위치는 "접촉력"을 구속 조건에서 상태 변수로 승격시킨 데 있습니다. 고정 기반 모델에서 접촉력은 말단 효과기가 환경과 상호작용한 결과물입니다. 반면 부동 기반 모델에서 접촉력은 베이스 자세를 유지하는 유일한 수단입니다. 이러한 관점 전환은 휴머노이드 로봇의 제어 문제를 "궤적 추적"에서 "접촉력 관리"로 바꾸었고, ZMP 안정 판정, 캡처 포인트 이론, 전신 QP 제어와 같은 현대 평형 제어 방법을 직접 탄생시켰습니다.

### 원리 분해

**① 질량 행렬 분할: 시스템을 "상부 블록"과 "하부 블록"으로 분리**

부동 기반 동역학 방정식의 표준 형태는 다음과 같습니다:

$$
\mathbf{M}(\mathbf{q}) \dot{\mathbf{v}} + \mathbf{C}(\mathbf{q}, \mathbf{v}) \mathbf{v} + \mathbf{g}(\mathbf{q}) = \mathbf{S}^T \boldsymbol{\tau} + \sum_{c} \mathbf{J}_c^T(\mathbf{q}) \mathbf{F}_c
$$

여기서 $\mathbf{M}(\mathbf{q})$는 부동 기반과 관절을 포함하는 $n \times n$ 질량 행렬, $\mathbf{C}(\mathbf{q}, \mathbf{v})$는 코리올리 및 원심력 항, $\mathbf{g}(\mathbf{q})$는 중력 항, $\mathbf{S}$는 선택 행렬(관절 토크 $\boldsymbol{\tau}$를 일반화 좌표 공간에 매핑하며, 관절에만 작용하고 부동 기반에는 작용하지 않음), $\mathbf{J}_c$는 $c$번째 접촉점에 해당하는 야코비 행렬, $\mathbf{F}_c$는 접촉력입니다.

질량 행렬을 부동 기반(아래첨자 $b$)과 관절(아래첨자 $j$)로 분할하면, 방정식은 두 그룹으로 나눌 수 있습니다:

$$
\begin{bmatrix} \mathbf{M}_{bb} & \mathbf{M}_{bj} \\ \mathbf{M}_{jb} & \mathbf{M}_{jj} \end{bmatrix} \begin{bmatrix} \dot{\mathbf{v}}_b \\ \dot{\mathbf{v}}_j \end{bmatrix} + \begin{bmatrix} \mathbf{C}_b \\ \mathbf{C}_j \end{bmatrix} + \begin{bmatrix} \mathbf{g}_b \\ \mathbf{g}_j \end{bmatrix} = \begin{bmatrix} \mathbf{0} \\ \boldsymbol{\tau} \end{bmatrix} + \sum_{c} \begin{bmatrix} \mathbf{J}_{c,b}^T \\ \mathbf{J}_{c,j}^T \end{bmatrix} \mathbf{F}_c
$$

**② 과소구동의 수학적 표현: 상부 블록에는 토크 항이 없음**

상부 블록(부동 기반) 방정식에서 $\boldsymbol{\tau}$의 계수가 0이라는 점에 주목하세요——이것이 과소구동의 수학적 표현입니다. 베이스 가속도 $\dot{\mathbf{v}}_b$는 전적으로 접촉력 $\mathbf{F}_c$에 의해 결정됩니다. 이는 휴머노이드 로봇이 산업용 로봇 팔처럼 몸통 운동을 "직접 명령"할 수 없고, 발바닥과 지면 사이의 접촉력을 통해서만 간접적으로 몸통 자세를 바꿀 수 있음을 의미합니다. 이 구속은 모든 평형 제어 알고리즘에 스며들어 있습니다: ZMP 판정이든 캡처 포인트 방법이든, 본질적으로 "베이스를 구동하기 위한 적절한 접촉력을 어떻게 생성할 것인가"에 답하는 것입니다.

**③ 접촉력은 "입력"이지 "구속"이 아님**

부동 기반 프레임워크에서 접촉력 $\mathbf{F}_c$는 수동적인 구속 반력이 아니라 능동적으로 풀어야 할 변수입니다. 이들은 마찰 원뿔 구속(미끄러짐 방지)과 단방향 구속(발은 지면을 밀 수 있지만 당길 수 없음)을 충족해야 합니다. 이러한 구속은 부동 기반 동역학과 연립하여 풀리며, 전신 QP 제어의 핵심 등식 구속 조건의 원천이 됩니다.

### 주요 매개변수 및 사양

| 매개변수 | 기호 | 의미 | 일반적 특징 |
|------|------|------|----------|
| 베이스 위치 | $\mathbf{p}_{\text{base}}$ | 부동 기반 좌표계 원점의 관성계 내 위치 | $\in \mathbb{R}^3$ |
| 베이스 자세 | $\mathbf{R}_{\text{base}}$ | 관성계에 대한 부동 기반 좌표계의 회전 | $\in SO(3)$ |
| 일반화 좌표 | $\mathbf{q}$ | 시스템 형상을 설명하는 최소 독립 변수 집합 | 차원 $6 + n_j$ |
| 질량 행렬 | $\mathbf{M}(\mathbf{q})$ | 부동 기반과 관절을 포함하는 관성 행렬 | $n \times n$ 대칭 양정치 |
| 선택 행렬 | $\mathbf{S}$ | 관절 토크를 일반화 좌표 공간에 매핑 | 관절 성분에만 작용 |
| 접촉 야코비 | $\mathbf{J}_c$ | $c$번째 접촉점의 속도 매핑 | 행 수는 접촉력 차원과 동일 |
| 과소구동 자유도 | — | 베이스의 액추에이터 없는 6자유도 | 3 병진 + 3 회전 |

### 횡적 비교

| 차원 | 고정 기반 동역학 | 부동 기반 동역학 |
|------|---------------|-------------|
| 베이스 상태 | 고정, 베이스 좌표 없음 | 6개의 자유 부유 자유도 |
| 구동 특성 | 완전 구동(모든 자유도에 액추에이터) | 과소구동(베이스에 액추에이터 없음) |
| 접촉력 역할 | 말단 효과기의 상호작용 결과물 | 베이스 제어의 핵심 입력 |
| 일반적 응용 | 산업용 로봇 팔 궤적 추적 | 휴머노이드 로봇 보행 및 평형 제어 |
| 방정식 구조 | $\mathbf{M}\ddot{\mathbf{q}} + \mathbf{C}\dot{\mathbf{q}} + \mathbf{g} = \boldsymbol{\tau}$ | $\mathbf{M}\dot{\mathbf{v}} + \mathbf{C}\mathbf{v} + \mathbf{g} = \mathbf{S}^T\boldsymbol{\tau} + \sum \mathbf{J}_c^T \mathbf{F}_c$ |

### 누가 사용하는가·응용 사례

**전신 QP 제어**: 현대 휴머노이드 로봇 제어기(예: Atlas, Digit 등 플랫폼)는 각 제어 주기마다 2차 계획 문제를 풀며, 그 동역학 등식 구속 조건이 바로 부동 기반 방정식입니다. 작업 추적 목표 + 동역학 등식 + 마찰 원뿔 부등식 + 토크 제한이 현대 휴머노이드 로봇 제어기와 완전히 일치하는 핵심 구조를 구성합니다. 실시간 QP 솔버(예: OSQP, qpOASES, ProxQP)가 제어 주기 내에 해를 구합니다.

**보행 계획**: 부동 기반 동역학은 "베이스 가속도가 전적으로 접촉력에 의해 결정된다"는 본질을 밝혔으므로, 보행 계획의 핵심 작업은 베이스가 원하는 궤적을 따라 움직이도록 발바닥 접촉력의 시간 시퀀스를 설계하는 것이 됩니다.

**동적 평형 제어**: 캡처 포인트 방법은 동적 평형을 착지점 기하 문제로 변환하며, 그 기저 모델이 바로 부동 기반 동역학입니다——질량 중심 각운동량과 접촉력을 제어하여 로봇이 임의의 교란 상태에서 안정적인 기립으로 복귀하도록 합니다.

### 한계와 경계

부동 기반 동역학은 이상화된 모델이며 다음과 같은 경계가 있습니다:

1. **강체 가정**: 방정식은 모든 링크가 강체라고 가정하며 구조적 유연성을 고려하지 않습니다. 실제 휴머노이드 로봇의 링크는 고속 운동에서 탄성 변형이 발생하여 모델 불일치를 초래할 수 있습니다(공학적 판단).
2. **접촉 모델 단순화**: 점 접촉 또는 면 접촉 가정은 발바닥과 지면 사이의 복잡한 마찰 분포와 변형을 무시합니다.
3. **계산 비용**: 질량 행렬 역산과 코리올리 항 계산은 자유도가 높을 때 계산량이 크며, 실시간 제어는 재귀 뉴턴-오일러 알고리즘 또는 사전 계산 기술이 필요합니다.
4. **비행 단계에는 부적합**: 로봇의 양발이 지면에서 떨어질 때(점프, 비행 등) 접촉력 항이 사라지고 시스템은 완전 과소구동이 되며, 질량 중심 운동량 모델로 전환해야 합니다.

### 일반적인 오해

1. **"부동 기반은 자유 부유와 같다"**——틀림. 부동 기반은 베이스에 기계적 구속이 없음을 의미하지만, 로봇은 여전히 접촉력을 통해 환경과 상호작용합니다. 자유 부유(예: 우주 로봇)는 접촉력이 0인 특수한 경우입니다.
2. **"과소구동은 제어 불가능을 의미한다"**——틀림. 과소구동은 액추에이터 수가 자유도 수보다 적음을 의미하지만, 접촉력을 통한 간접 제어로 시스템은 여전히 안정적인 평형에 도달할 수 있습니다——이것이 인간 보행의 본질입니다.
3. **"부동 기반 방정식과 고정 기반 방정식은 서로 바꿔 쓸 수 있다"**——틀림. 고정 기반 방정식에서 $\mathbf{S} = \mathbf{I}$(단위 행렬)인 반면, 부동 기반 방정식에서 $\mathbf{S}$는 토크를 관절 성분에만 매핑하며, 두 구조는 본질적으로 다릅니다.
4. **"접촉력은 구속 반력이므로 능동적으로 풀 필요가 없다"**——틀림. 부동 기반 프레임워크에서 접촉력은 운동 방정식과 연립하여 풀어야 하는 변수이며, 마찰 원뿔과 같은 부등식 구속을 충족해야 합니다.

### 관련 지식

- `ent_formalism_inverse_dynamics_qp` — 역동역학 QP는 부동 기반 방정식을 등식 구속으로 사용하여 작업과 구속을 충족하는 관절 토크와 접촉력을 계산합니다.
- `ent_method_model_predictive_control` — MPC는 각 제어 주기마다 유한 시간 최적 제어 문제를 풀며, 예측 모델로 부동 기반 동역학을 자주 사용합니다.
- `ent_paper_arachchige_dynamic_modeling_and_validatio_2023` — 이 논문은 이족 보행 로봇의 동적 모델링과 검증을 수행하여 부동 기반 동역학의 실제 적용 절차를 보여줍니다.
- `ent_paper_li_design_of_a_flying_humanoid_ro_2021` — 비행 휴머노이드 로봇의 추력 벡터 제어는 부동 기반 동역학과 연립하여 비행과 지면 운동의 혼합 모드를 처리해야 합니다.
- `ent_paper_the_duke_humanoid_design_and_c_2024` — Duke Humanoid의 강화 학습 보행 정책은 시뮬레이션과 하드웨어 모두에서 부동 기반 동역학을 환경 모델로 사용합니다.
