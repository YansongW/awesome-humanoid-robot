---
$id: ent_software_rt_preempt_linux
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: software_platform
names:
  en: Linux RT-PREEMPT
  zh: Linux RT-PREEMPT
  ko: Linux RT-PREEMPT
summary:
  en: A Linux kernel patchset that makes most of the kernel preemptible to provide deterministic, low-latency real-time behavior
    for robot control.
  zh: 使Linux内核大部分可抢占以提供确定性低延迟实时行为的内核补丁集，常用于机器人控制。
  ko: Linux 커널 대부분을 선점 가능하게 만들어 결정적·저지연 실시간 동작을 제공하는 커널 패치 집합.
domains:
- 08_software_middleware
layers:
- intelligence
functional_roles:
- knowledge
tags:
- software_platform
- chapter_22
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body backfilled from chapter-06.md#6.4.7 实时 Linux：PREEMPT_RT、cyclictest 与线程优先级 by scripts/backfill_nonpaper_entries.py.
    Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12] body upgraded to textbook-grade
    (.staging/textbook_grade_run/b2_b2): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters + first-hand
    sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

Linux RT-PREEMPT 是一个使 Linux 内核大部分可抢占、中断线程化的内核补丁集，旨在提供确定性的低延迟实时行为，是当前人形机器人主控计算机实时核的主流方案。它完整保留 Linux 生态（驱动、ROS 2、容器工具链），把最坏情况延迟从毫秒级压到数十微秒量级，真正改变的不是"Linux 能否实时"，而是"机器人主控能否既跑 AI 又跑控制"。

## 核心内容

### 是什么：准确定义

Linux RT-PREEMPT（又称 PREEMPT_RT）是一个针对标准 Linux 内核的补丁集，其核心目标是通过**内核抢占**与**中断线程化**两大机制，将通用 Linux 内核改造为具备硬实时能力的操作系统。它并非一个独立的操作系统，而是对主线 Linux 内核的持续补丁流——社区长期维护，并逐步将实时功能合入主线。

从软件分层角度看，Linux RT-PREEMPT 位于硬件之上、机器人中间件（如 ROS 2）之下，属于操作系统与驱动层。在人形机器人典型的异构计算架构中，它通常与标准 Linux 核、QNX 实时核配合使用：RT-PREEMPT 实时核跑控制回路，标准 Linux 核跑感知与 AI 推理，两核之间以共享内存或确定性以太网通道交换数据。

### 为什么存在：痛点与历史定位

通用操作系统（如标准 Linux）并非为硬实时设计：内核不可抢占区、中断处理与调度器的非确定性会带来不可控的延迟尖峰。人形机器人对时序的要求极为苛刻——全身上下数十个执行器以毫秒级周期闭环，安全相关功能（急停、力矩限制、跌倒保护）强制要求确定性响应。若控制回路遭遇延迟尖峰，轻则抖动影响步态质量，重则导致失稳甚至安全事故。

在 RT-PREEMPT 出现之前，工程师面临两难选择：要么使用 QNX 等商用 RTOS，牺牲生态丰富度并承担授权成本；要么忍受标准 Linux 的时序不确定性。Linux RT-PREEMPT 的历史定位正是**填补这一空白**——它让开发者无需离开 Linux 生态即可获得接近硬实时的确定性，把最坏情况延迟从毫秒级压到数十微秒量级（工程判断）。它真正改变的不是"Linux 能不能实时"，而是"机器人主控能否既跑 AI 又跑控制"——将原本需要两颗异构芯片分别承担的任务，收敛到同一颗多核处理器上。

### 原理拆解

**① 内核抢占：缩小不可抢占区**

标准 Linux 内核中存在大量不可抢占的临界区（如自旋锁保护的代码段），实时任务在这些区间内无法被调度。RT-PREEMPT 通过以下手段将绝大多数临界区转换为可抢占：

- 将自旋锁替换为可睡眠的互斥锁（rtmutex），使高优先级任务可以抢占持有锁的低优先级任务；
- 引入优先级继承协议，防止优先级反转导致的高优先级任务被无限阻塞；
- 使内核代码路径本身可被抢占，中断处理程序可被调度器挂起。

**② 中断线程化：把中断变成可调度实体**

传统 Linux 中，中断处理程序（ISR）运行在不可抢占的上下文，其执行时间不可控。RT-PREEMPT 将中断处理程序转换为内核线程（IRQ threads），赋予其优先级属性，使中断响应时间变得可预测。硬实时要求极高的中断（如 EtherCAT 同步中断）仍可保留为不可线程化的快速处理路径。

**③ 配套工程实践：确定性不是补丁单独给的**

RT-PREEMPT 补丁本身只提供"可能性"，工程实践才能兑现确定性。语料明确列出的配套手段包括：

- **CPU 隔离（isolcpus）**：将实时任务绑定的 CPU 核从通用调度器中隔离出来，避免被普通进程抢占；
- **中断亲和性绑定**：将关键中断（如 EtherCAT 主站中断）绑定到特定 CPU 核，与实时任务同核或异核部署；
- **内存锁定（mlockall）**：防止实时线程因页面换入换出产生不可预测的延迟；
- **实时优先级（SCHED_FIFO）**：为实时线程分配固定优先级，确保其优先于普通进程调度。

典型的调度设计是：EtherCAT 主站与全身控制（WBC）任务绑定独立 CPU 核、最高 SCHED_FIFO 优先级并锁定内存；策略推理与普通进程共享其余核，通过 cgroups 限制其抢占干扰；所有实时线程禁用动态内存分配与非确定性系统调用。

### 关键参数与规格

| 维度 | Linux RT-PREEMPT | Xenomai | QNX | Zephyr |
|---|---|---|---|---|
| 调度延迟量级 | 数十微秒 | 微秒级 | 微秒级 | 微秒级 |
| 生态丰富度 | 极高（完整 Linux） | 高（复用 Linux 用户态） | 中 | 低（嵌入式） |
| 维护成本 | 低 | 高 | 中（商用授权） | 低 |
| 安全认证基础 | 需自建 | 需自建 | 有成熟认证案例 | 部分 |
| 典型安装部位 | 主控计算机 | 主控计算机 | 安全域控制器 | 关节/传感器节点 |

上表来自语料中的四者工程对比。选型时需注意：RT-PREEMPT 的"数十微秒"调度延迟是**典型量级**而非硬保证，实际值取决于硬件平台、内核配置与负载情况（工程判断）。若应用需要微秒级确定性且预算充足，QNX 是更稳妥的选择；若面向资源受限的关节/传感器节点，Zephyr 更合适。

### 横向对比

**与 QNX 对比**：QNX 是商用微内核 RTOS，文件系统与网络栈均为用户态服务，内核只保留最小功能，驱动故障不会蔓延至内核，具备功能安全认证资质；代价是生态与授权成本。RT-PREEMPT 的优势是完整保留 Linux 生态（驱动、ROS 2、容器工具链），劣势是安全认证需自建。典型人形机器人采用"QNX 或 RT-PREEMPT 实时核跑控制 + 标准 Linux 核跑感知与 AI"的异构组合，安全相关功能强制落在实时侧。

**与 Xenomai 对比**：Xenomai 是双内核方案，实时任务运行于 Cobalt 实时核，延迟可低至微秒级，但维护复杂。RT-PREEMPT 是单内核方案，维护成本低，但延迟量级为数十微秒。两者都复用 Linux 用户态生态，但 Xenomai 的实时任务需使用专用 API，而 RT-PREEMPT 的实时任务直接使用标准 POSIX 接口。

**与 Zephyr 对比**：Zephyr 是面向资源受限嵌入式设备的开源 RTOS，常用于传感器节点与电机控制器，生态丰富度低。RT-PREEMPT 面向主控计算机，两者安装部位不同，通常共存于同一台机器人——RT-PREEMPT 跑主控，Zephyr 跑关节/传感器节点。

### 谁在用·应用案例

Linux RT-PREEMPT 是当前人形机器人实时核的主流方案，广泛应用于原型验证、学术研究与早期商业化产品中。语料给出的典型部署模式：

- **主控计算机**：RT-PREEMPT 实时核承载 EtherCAT 主站与 WBC 任务，绑定独立 CPU 核、最高 SCHED_FIFO 优先级并锁定内存；
- **异构组合**：与标准 Linux 核配合，实时核跑控制，标准核跑感知与 AI，两核之间以共享内存或确定性以太网通道交换数据；
- **安全功能落地**：急停、力矩限制、跌倒保护等安全相关功能强制落在实时侧。

配套的工程实践还包括：策略推理与普通进程共享其余核，通过 cgroups 限制其抢占干扰；所有实时线程禁用动态内存分配与非确定性系统调用。

### 局限与边界

1. **延迟量级的天花板**：RT-PREEMPT 的调度延迟为数十微秒量级，低于 Xenomai 和 QNX 的微秒级。对于需要微秒级确定性的应用（如某些安全关键控制回路），它可能不够。
2. **安全认证需自建**：与 QNX 有成熟认证案例不同，RT-PREEMPT 的安全认证基础是"需自建"——这意味着在功能安全标准（如 IEC 61508、ISO 13849）约束下，使用 RT-PREEMPT 需要额外的认证工作。
3. **确定性依赖工程实践**：补丁本身不保证确定性，CPU 隔离、中断亲和性绑定、内存锁定等配套手段缺一不可。配置不当仍可能出现延迟尖峰。
4. **实时线程的编程约束**：所有实时线程必须禁用动态内存分配与非确定性系统调用，这对开发者的编程习惯提出要求。

### 常见误区

1. **"打了 RT-PREEMPT 补丁就实时了"**——错。补丁只提供"可能性"，CPU 隔离、中断亲和性绑定、内存锁定、SCHED_FIFO 优先级等工程实践缺一不可。语料明确将后者列为"配套的工程实践"。
2. **"RT-PREEMPT 延迟比 QNX 低"**——错。语料给出的对比表中，RT-PREEMPT 的调度延迟量级为"数十微秒"，而 QNX 为"微秒级"。RT-PREEMPT 的优势在生态，不在延迟绝对值。
3. **"RT-PREEMPT 和标准 Linux 只能二选一"**——错。典型部署是异构组合：RT-PREEMPT 实时核跑控制，标准 Linux 核跑感知与 AI，两核之间以共享内存或确定性以太网通道交换数据。
4. **"实时线程可以随便用动态内存分配"**——错。所有实时线程必须禁用动态内存分配与非确定性系统调用，否则会产生不可预测的延迟。

### 相关知识

- `ent_software_ros_2_middleware_2024` — ROS 2 中间件运行于 RT-PREEMPT 之上，其发布/订阅通信的确定性依赖底层操作系统的实时能力。
- `ent_software_qnx` — QNX 是与 RT-PREEMPT 并列的实时操作系统选项，微内核架构与安全认证资质是其差异化优势。
- `ent_paper_gutierrez_real_time_linux_communications_2018` — 该研究在 PREEMPT-RT Linux 上评估 UDP 通信实时性能，验证了 CPU/IRQ 隔离对保持延迟边界的有效性。

## 参考

- [人形机器人全流程开发工作流](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [第 22 章：软件中间件](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-22.md)
- [第 24 章：端到端软件栈](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-24.md)

## Overview

Linux RT-PREEMPT is a patch set that makes most of the Linux kernel preemptible and interrupt handling threaded, aiming to provide deterministic low-latency real-time behavior. It is currently the mainstream solution for the real-time core of humanoid robot main control computers. It fully preserves the Linux ecosystem (drivers, ROS 2, container toolchains), compressing worst-case latency from the millisecond range down to tens of microseconds. What it truly changes is not "whether Linux can be real-time," but "whether a robot's main controller can run both AI and control."

## Content

### What It Is: A Precise Definition

Linux RT-PREEMPT (also known as PREEMPT_RT) is a patch set for the standard Linux kernel. Its core goal is to transform the general-purpose Linux kernel into an operating system with hard real-time capabilities through two key mechanisms: **kernel preemption** and **interrupt threading**. It is not a standalone operating system but an ongoing stream of patches applied to the mainline Linux kernel—maintained by the community over the long term, with real-time features gradually being merged upstream.

From a software layering perspective, Linux RT-PREEMPT sits above the hardware and below robot middleware (such as ROS 2), belonging to the operating system and driver layer. In a typical heterogeneous computing architecture for humanoid robots, it is usually used alongside a standard Linux core and a QNX real-time core: the RT-PREEMPT real-time core runs control loops, the standard Linux core runs perception and AI inference, and the two cores exchange data via shared memory or deterministic Ethernet channels.

### Why It Exists: Pain Points and Historical Positioning

General-purpose operating systems (such as standard Linux) are not designed for hard real-time: non-preemptible regions in the kernel, interrupt handling, and scheduler non-determinism introduce uncontrollable latency spikes. Humanoid robots impose extremely stringent timing requirements—dozens of actuators across the body close loops at millisecond-level cycles, and safety-critical functions (emergency stop, torque limiting, fall protection) mandate deterministic responses. If a control loop encounters a latency spike, the consequences range from jitter degrading gait quality to instability or even safety incidents.

Before RT-PREEMPT, engineers faced a dilemma: either use a commercial RTOS like QNX, sacrificing ecosystem richness and bearing licensing costs, or tolerate the timing non-determinism of standard Linux. The historical positioning of Linux RT-PREEMPT is precisely to **fill this gap**—it allows developers to achieve near-hard-real-time determinism without leaving the Linux ecosystem, compressing worst-case latency from the millisecond range down to tens of microseconds (engineering judgment). What it truly changes is not "whether Linux can be real-time," but "whether a robot's main controller can run both AI and control"—converging tasks that previously required two heterogeneous chips onto a single multi-core processor.

### Principle Breakdown

**① Kernel Preemption: Shrinking Non-Preemptible Regions**

The standard Linux kernel contains numerous non-preemptible critical sections (such as code protected by spinlocks), within which real-time tasks cannot be scheduled. RT-PREEMPT converts the vast majority of these critical sections into preemptible ones through the following means:

- Replacing spinlocks with sleepable mutexes (rtmutex), allowing higher-priority tasks to preempt lower-priority tasks holding the lock;
- Introducing a priority inheritance protocol to prevent priority inversion from indefinitely blocking high-priority tasks;
- Making kernel code paths themselves preemptible, allowing interrupt handlers to be suspended by the scheduler.

**② Interrupt Threading: Turning Interrupts into Schedulable Entities**

In traditional Linux, interrupt handlers (ISRs) run in a non-preemptible context, and their execution time is uncontrollable. RT-PREEMPT converts interrupt handlers into kernel threads (IRQ threads), assigning them priority attributes so that interrupt response times become predictable. Interrupts with extremely stringent hard real-time requirements (such as EtherCAT synchronization interrupts) can still be preserved as non-threaded fast-path handlers.

**③ Supporting Engineering Practices: Determinism Is Not Given by the Patch Alone**

The RT-PREEMPT patch itself only provides "possibility"; engineering practices deliver determinism. The supporting measures explicitly listed in the source material include:

- **CPU isolation (isolcpus)**: Isolating CPU cores bound to real-time tasks from the general scheduler to prevent preemption by ordinary processes;
- **Interrupt affinity binding**: Binding critical interrupts (such as the EtherCAT master interrupt) to specific CPU cores, deployed on the same core as or a different core from real-time tasks;
- **Memory locking (mlockall)**: Preventing real-time threads from experiencing unpredictable latency due to page swapping;
- **Real-time priority (SCHED_FIFO)**: Assigning fixed priorities to real-time threads to ensure they are scheduled ahead of ordinary processes.

A typical scheduling design is: the EtherCAT master and whole-body control (WBC) tasks are bound to a dedicated CPU core with the highest SCHED_FIFO priority and locked memory; policy inference and ordinary processes share the remaining cores, with cgroups limiting their preemption interference; all real-time threads disable dynamic memory allocation and non-deterministic system calls.

### Key Parameters and Specifications

| Dimension | Linux RT-PREEMPT | Xenomai | QNX | Zephyr |
|---|---|---|---|---|
| Scheduling latency magnitude | Tens of microseconds | Microsecond-level | Microsecond-level | Microsecond-level |
| Ecosystem richness | Extremely high (full Linux) | High (reuses Linux user space) | Medium | Low (embedded) |
| Maintenance cost | Low | High | Medium (commercial license) | Low |
| Safety certification basis | Requires self-building | Requires self-building | Mature certification cases available | Partial |
| Typical installation location | Main control computer | Main control computer | Safety domain controller | Joint/sensor nodes |

The table above comes from the four-way engineering comparison in the source material. When selecting, note that RT-PREEMPT's "tens of microseconds" scheduling latency is a **typical magnitude** rather than a hard guarantee; the actual value depends on the hardware platform, kernel configuration, and load conditions (engineering judgment). If an application requires microsecond-level determinism and the budget allows, QNX is a more reliable choice; if targeting resource-constrained joint/sensor nodes, Zephyr is more suitable.

### Horizontal Comparison

**Comparison with QNX**: QNX is a commercial microkernel RTOS where the file system and network stack run as user-space services, the kernel retains only minimal functionality, driver failures do not propagate to the kernel, and it possesses functional safety certification credentials; the cost is ecosystem and licensing. RT-PREEMPT's advantage is fully preserving the Linux ecosystem (drivers, ROS 2, container toolchains), while its disadvantage is that safety certification must be self-built. A typical humanoid robot employs a heterogeneous combination of "QNX or RT-PREEMPT real-time core running control + standard Linux core running perception and AI," with safety-critical functions mandatorily placed on the real-time side.

**Comparison with Xenomai**: Xenomai is a dual-kernel solution where real-time tasks run on the Cobalt real-time core, achieving latency as low as the microsecond level, but with complex maintenance. RT-PREEMPT is a single-kernel solution with lower maintenance costs, but latency is on the order of tens of microseconds. Both reuse the Linux user-space ecosystem, but Xenomai's real-time tasks require a dedicated API, whereas RT-PREEMPT's real-time tasks directly use standard POSIX interfaces.

**Comparison with Zephyr**: Zephyr is an open-source RTOS for resource-constrained embedded devices, commonly used in sensor nodes and motor controllers, with low ecosystem richness. RT-PREEMPT targets main control computers; the two have different installation locations and typically coexist on the same robot—RT-PREEMPT runs the main controller, while Zephyr runs joint/sensor nodes.

### Who Uses It: Application Cases

Linux RT-PREEMPT is the mainstream solution for the real-time core of humanoid robots, widely used in prototype validation, academic research, and early commercial products. Typical deployment patterns from the source material:

- **Main control computer**: The RT-PREEMPT real-time core hosts the EtherCAT master and WBC tasks, bound to a dedicated CPU core with the highest SCHED_FIFO priority and locked memory;
- **Heterogeneous combination**: Paired with a standard Linux core, the real-time core runs control, the standard core runs perception and AI, and the two cores exchange data via shared memory or deterministic Ethernet channels;
- **Safety function implementation**: Safety-critical functions such as emergency stop, torque limiting, and fall protection are mandatorily placed on the real-time side.

Supporting engineering practices also include: policy inference and ordinary processes share the remaining cores, with cgroups limiting their preemption interference; all real-time threads disable dynamic memory allocation and non-deterministic system calls.

### Limitations and Boundaries

1. **Latency magnitude ceiling**: RT-PREEMPT's scheduling latency is on the order of tens of microseconds, lower than the microsecond-level of Xenomai and QNX. For applications requiring microsecond-level determinism (such as certain safety-critical control loops), it may be insufficient.
2. **Safety certification must be self-built**: Unlike QNX, which has mature certification cases, RT-PREEMPT's safety certification basis is "requires self-building"—meaning that under functional safety standards (such as IEC 61508, ISO 13849), using RT-PREEMPT entails additional certification work.
3. **Determinism depends on engineering practices**: The patch itself does not guarantee determinism; supporting measures such as CPU isolation, interrupt affinity binding, and memory locking are indispensable. Improper configuration can still result in latency spikes.
4. **Programming constraints for real-time threads**: All real-time threads must disable dynamic memory allocation and non-deterministic system calls, imposing requirements on developers' programming habits.

### Common Misconceptions

1. **"Applying the RT-PREEMPT patch makes it real-time"**—Wrong. The patch only provides "possibility"; engineering practices such as CPU isolation, interrupt affinity binding, memory locking, and SCHED_FIFO priority are indispensable. The source material explicitly lists these as "supporting engineering practices."
2. **"RT-PREEMPT has lower latency than QNX"**—Wrong. In the comparison table from the source material, RT-PREEMPT's scheduling latency magnitude is "tens of microseconds," while QNX is "microsecond-level." RT-PREEMPT's advantage lies in the ecosystem, not in absolute latency.
3. **"RT-PREEMPT and standard Linux are mutually exclusive"**—Wrong. Typical deployment is a heterogeneous combination: the RT-PREEMPT real-time core runs control, the standard Linux core runs perception and AI, and the two cores exchange data via shared memory or deterministic Ethernet channels.
4. **"Real-time threads can freely use dynamic memory allocation"**—Wrong. All real-time threads must disable dynamic memory allocation and non-deterministic system calls; otherwise, unpredictable latency will occur.

### Related Knowledge

- `ent_software_ros_2_middleware_2024` — ROS 2 middleware runs on top of RT-PREEMPT, and the determinism of its publish/subscribe communication depends on the real-time capabilities of the underlying operating system.
- `ent_software_qnx` — QNX is a real-time operating system option alongside RT-PREEMPT, with its microkernel architecture and safety certification credentials as differentiating advantages.
- `ent_paper_gutierrez_real_time_linux_communications_2018` — This study evaluates UDP communication real-time performance on PREEMPT-RT Linux, validating the effectiveness of CPU/IRQ isolation in maintaining latency bounds.

## 개요

Linux RT-PREEMPT는 Linux 커널의 대부분을 선점 가능하게 만들고 인터럽트를 스레드화하는 커널 패치 세트로, 결정론적 저지연 실시간 동작을 제공하는 것을 목표로 하며, 현재 휴머노이드 로봇 메인 제어 컴퓨터의 실시간 코어를 위한 주류 솔루션입니다. Linux 생태계(드라이버, ROS 2, 컨테이너 도구 체인)를 완전히 보존하면서 최악의 경우 지연 시간을 밀리초 단위에서 수십 마이크로초 수준으로 낮춥니다. 실제로 바꾸는 것은 "Linux가 실시간이 될 수 있는가"가 아니라 "로봇 메인 제어가 AI와 제어를 동시에 실행할 수 있는가"입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

Linux RT-PREEMPT(PREEMPT_RT라고도 함)는 표준 Linux 커널을 위한 패치 세트로, **커널 선점**과 **인터럽트 스레드화**라는 두 가지 메커니즘을 통해 범용 Linux 커널을 하드 실시간 기능을 갖춘 운영 체제로 변환하는 것을 핵심 목표로 합니다. 독립적인 운영 체제가 아니라 메인라인 Linux 커널에 대한 지속적인 패치 흐름입니다. 커뮤니티가 장기간 유지 관리하며 실시간 기능을 점진적으로 메인라인에 통합합니다.

소프트웨어 계층 관점에서 Linux RT-PREEMPT는 하드웨어 위, 로봇 미들웨어(예: ROS 2) 아래에 위치하며 운영 체제 및 드라이버 계층에 속합니다. 휴머노이드 로봇의 전형적인 이기종 컴퓨팅 아키텍처에서 일반적으로 표준 Linux 코어, QNX 실시간 코어와 함께 사용됩니다. RT-PREEMPT 실시간 코어는 제어 루프를 실행하고, 표준 Linux 코어는 인식 및 AI 추론을 실행하며, 두 코어 간에는 공유 메모리 또는 결정론적 이더넷 채널을 통해 데이터를 교환합니다.

### 왜 존재하는가: 문제점과 역사적 위치

범용 운영 체제(예: 표준 Linux)는 하드 실시간용으로 설계되지 않았습니다. 커널의 비선점 영역, 인터럽트 처리 및 스케줄러의 비결정성은 제어할 수 없는 지연 스파이크를 유발할 수 있습니다. 휴머노이드 로봇은 타이밍 요구 사항이 매우 엄격합니다. 전신의 수십 개 액추에이터가 밀리초 주기로 폐루프를 형성하며, 안전 관련 기능(비상 정지, 토크 제한, 낙상 보호)은 결정론적 응답을 강제합니다. 제어 루프가 지연 스파이크를 겪으면 가벼운 경우 보행 품질에 영향을 주는 떨림이 발생하고, 심한 경우 불안정 또는 안전 사고로 이어질 수 있습니다.

RT-PREEMPT가 등장하기 전에는 엔지니어들이 두 가지 선택지 사이에서 딜레마에 빠졌습니다. QNX와 같은 상용 RTOS를 사용하여 생태계 풍부함을 희생하고 라이선스 비용을 부담하거나, 표준 Linux의 타이밍 불확실성을 감수하는 것이었습니다. Linux RT-PREEMPT의 역사적 위치는 바로 **이 공백을 메우는 것**입니다. 개발자가 Linux 생태계를 떠나지 않고도 하드 실시간에 가까운 결정론을 얻을 수 있게 하여 최악의 경우 지연 시간을 밀리초 단위에서 수십 마이크로초 수준으로 낮춥니다(공학적 판단). 실제로 바꾸는 것은 "Linux가 실시간이 될 수 있는가"가 아니라 "로봇 메인 제어가 AI와 제어를 동시에 실행할 수 있는가"입니다. 즉, 원래 두 개의 이기종 칩이 각각 담당하던 작업을 동일한 멀티코어 프로세서로 수렴시킵니다.

### 원리 분석

**① 커널 선점: 비선점 영역 축소**

표준 Linux 커널에는 많은 비선점 임계 영역(예: 스핀락으로 보호되는 코드 섹션)이 존재하며, 실시간 작업은 이러한 영역에서 스케줄링될 수 없습니다. RT-PREEMPT는 다음 수단을 통해 대부분의 임계 영역을 선점 가능하게 변환합니다.

- 스핀락을 절전 가능한 뮤텍스(rtmutex)로 교체하여 고우선순위 작업이 락을 보유한 저우선순위 작업을 선점할 수 있게 합니다.
- 우선순위 상속 프로토콜을 도입하여 우선순위 역전으로 인한 고우선순위 작업의 무한 차단을 방지합니다.
- 커널 코드 경로 자체를 선점 가능하게 만들고, 인터럽트 핸들러를 스케줄러가 일시 중단할 수 있게 합니다.

**② 인터럽트 스레드화: 인터럽트를 스케줄 가능한 엔터티로 변환**

기존 Linux에서 인터럽트 핸들러(ISR)는 비선점 컨텍스트에서 실행되며 실행 시간을 제어할 수 없습니다. RT-PREEMPT는 인터럽트 핸들러를 커널 스레드(IRQ threads)로 변환하고 우선순위 속성을 부여하여 인터럽트 응답 시간을 예측 가능하게 만듭니다. 매우 높은 실시간 요구 사항이 있는 인터럽트(예: EtherCAT 동기 인터럽트)는 스레드화되지 않은 빠른 처리 경로로 유지될 수 있습니다.

**③ 병행 엔지니어링 실무: 결정론은 패치만으로 주어지지 않음**

RT-PREEMPT 패치 자체는 "가능성"만 제공하며, 엔지니어링 실무가 결정론을 실현합니다. 자료에 명시된 병행 수단은 다음과 같습니다.

- **CPU 격리(isolcpus)**: 실시간 작업이 바인딩된 CPU 코어를 범용 스케줄러에서 격리하여 일반 프로세스의 선점을 방지합니다.
- **인터럽트 친화성 바인딩**: 중요 인터럽트(예: EtherCAT 마스터 인터럽트)를 특정 CPU 코어에 바인딩하여 실시간 작업과 동일 코어 또는 다른 코어에 배포합니다.
- **메모리 잠금(mlockall)**: 실시간 스레드의 페이지 스왑 인/아웃으로 인한 예측 불가능한 지연을 방지합니다.
- **실시간 우선순위(SCHED_FIFO)**: 실시간 스레드에 고정 우선순위를 할당하여 일반 프로세스보다 우선적으로 스케줄링되도록 보장합니다.

전형적인 스케줄링 설계는 다음과 같습니다. EtherCAT 마스터와 전신 제어(WBC) 작업을 독립 CPU 코어에 바인딩하고, 최고 SCHED_FIFO 우선순위를 부여하며 메모리를 잠급니다. 정책 추론과 일반 프로세스는 나머지 코어를 공유하며 cgroups를 통해 선점 간섭을 제한합니다. 모든 실시간 스레드는 동적 메모리 할당과 비결정론적 시스템 호출을 비활성화합니다.

### 주요 매개변수 및 사양

| 차원 | Linux RT-PREEMPT | Xenomai | QNX | Zephyr |
|---|---|---|---|---|
| 스케줄링 지연 수준 | 수십 마이크로초 | 마이크로초 수준 | 마이크로초 수준 | 마이크로초 수준 |
| 생태계 풍부함 | 매우 높음(완전한 Linux) | 높음(Linux 사용자 공간 재사용) | 중간 | 낮음(임베디드) |
| 유지보수 비용 | 낮음 | 높음 | 중간(상용 라이선스) | 낮음 |
| 안전 인증 기반 | 자체 구축 필요 | 자체 구축 필요 | 성숙한 인증 사례 있음 | 일부 |
| 전형적인 설치 위치 | 메인 제어 컴퓨터 | 메인 제어 컴퓨터 | 안전 도메인 컨트롤러 | 관절/센서 노드 |

위 표는 자료의 네 가지 엔지니어링 비교에서 가져온 것입니다. 선택 시 주의할 점: RT-PREEMPT의 "수십 마이크로초" 스케줄링 지연은 **전형적인 수준**이지 하드 보장이 아니며, 실제 값은 하드웨어 플랫폼, 커널 구성 및 부하 상황에 따라 달라집니다(공학적 판단). 마이크로초 수준의 결정론이 필요하고 예산이 충분하다면 QNX가 더 안정적인 선택입니다. 리소스가 제한된 관절/센서 노드를 대상으로 한다면 Zephyr가 더 적합합니다.

### 수평 비교

**QNX와 비교**: QNX는 상용 마이크로커널 RTOS로, 파일 시스템과 네트워크 스택이 모두 사용자 공간 서비스이며 커널은 최소 기능만 유지합니다. 드라이버 오류가 커널로 확산되지 않으며 기능 안전 인증 자격을 갖추고 있습니다. 대가는 생태계와 라이선스 비용입니다. RT-PREEMPT의 장점은 Linux 생태계(드라이버, ROS 2, 컨테이너 도구 체인)를 완전히 보존하는 것이고, 단점은 안전 인증을 자체 구축해야 한다는 것입니다. 전형적인 휴머노이드 로봇은 "QNX 또는 RT-PREEMPT 실시간 코어로 제어 실행 + 표준 Linux 코어로 인식 및 AI 실행"의 이기종 조합을 채택하며, 안전 관련 기능은 실시간 측에 강제로 배치됩니다.

**Xenomai와 비교**: Xenomai는 이중 커널 솔루션으로, 실시간 작업은 Cobalt 실시간 코어에서 실행되며 지연 시간이 마이크로초 수준까지 낮을 수 있지만 유지보수가 복잡합니다. RT-PREEMPT는 단일 커널 솔루션으로 유지보수 비용이 낮지만 지연 수준은 수십 마이크로초입니다. 둘 다 Linux 사용자 공간 생태계를 재사용하지만, Xenomai의 실시간 작업은 전용 API를 사용해야 하는 반면 RT-PREEMPT의 실시간 작업은 표준 POSIX 인터페이스를 직접 사용합니다.

**Zephyr와 비교**: Zephyr는 리소스가 제한된 임베디드 장치를 위한 오픈 소스 RTOS로, 센서 노드와 모터 컨트롤러에 자주 사용되며 생태계 풍부함이 낮습니다. RT-PREEMPT는 메인 제어 컴퓨터를 대상으로 하며, 둘의 설치 위치가 다르므로 일반적으로 동일한 로봇에 공존합니다. RT-PREEMPT는 메인 제어를 실행하고 Zephyr는 관절/센서 노드를 실행합니다.

### 누가 사용하는가·응용 사례

Linux RT-PREEMPT는 현재 휴머노이드 로봇 실시간 코어의 주류 솔루션으로, 프로토타입 검증, 학술 연구 및 초기 상용 제품에 널리 사용됩니다. 자료에 제시된 전형적인 배포 패턴:

- **메인 제어 컴퓨터**: RT-PREEMPT 실시간 코어가 EtherCAT 마스터와 WBC 작업을 담당하며, 독립 CPU 코어에 바인딩되고 최고 SCHED_FIFO 우선순위를 가지며 메모리를 잠급니다.
- **이기종 조합**: 표준 Linux 코어와 함께 사용되며, 실시간 코어는 제어를 실행하고 표준 코어는 인식 및 AI를 실행하며, 두 코어 간에는 공유 메모리 또는 결정론적 이더넷 채널을 통해 데이터를 교환합니다.
- **안전 기능 구현**: 비상 정지, 토크 제한, 낙상 보호 등 안전 관련 기능은 실시간 측에 강제로 배치됩니다.

병행 엔지니어링 실무에는 다음도 포함됩니다. 정책 추론과 일반 프로세스는 나머지 코어를 공유하며 cgroups를 통해 선점 간섭을 제한합니다. 모든 실시간 스레드는 동적 메모리 할당과 비결정론적 시스템 호출을 비활성화합니다.

### 한계와 경계

1. **지연 수준의 상한**: RT-PREEMPT의 스케줄링 지연은 수십 마이크로초 수준으로 Xenomai와 QNX의 마이크로초 수준보다 낮습니다. 마이크로초 수준의 결정론이 필요한 응용(예: 일부 안전 중요 제어 루프)에는 충분하지 않을 수 있습니다.
2. **안전 인증 자체 구축 필요**: QNX에 성숙한 인증 사례가 있는 것과 달리 RT-PREEMPT의 안전 인증 기반은 "자체 구축 필요"입니다. 즉, 기능 안전 표준(예: IEC 61508, ISO 13849)의 제약 하에서 RT-PREEMPT를 사용하려면 추가 인증 작업이 필요합니다.
3. **결정론은 엔지니어링 실무에 의존**: 패치 자체는 결정론을 보장하지 않으며, CPU 격리, 인터럽트 친화성 바인딩, 메모리 잠금 등의 병행 수단이 모두 필수입니다. 구성이 잘못되면 여전히 지연 스파이크가 발생할 수 있습니다.
4. **실시간 스레드의 프로그래밍 제약**: 모든 실시간 스레드는 동적 메모리 할당과 비결정론적 시스템 호출을 비활성화해야 하며, 이는 개발자의 프로그래밍 습관에 대한 요구 사항입니다.

### 일반적인 오해

1. **"RT-PREEMPT 패치를 적용하면 실시간이 된다"** — 틀림. 패치는 "가능성"만 제공하며, CPU 격리, 인터럽트 친화성 바인딩, 메모리 잠금, SCHED_FIFO 우선순위 등의 엔지니어링 실무가 모두 필수입니다. 자료는 후자를 "병행 엔지니어링 실무"로 명시합니다.
2. **"RT-PREEMPT 지연이 QNX보다 낮다"** — 틀림. 자료의 비교표에서 RT-PREEMPT의 스케줄링 지연 수준은 "수십 마이크로초"이고 QNX는 "마이크로초 수준"입니다. RT-PREEMPT의 장점은 생태계에 있으며 지연 절대값에 있지 않습니다.
3. **"RT-PREEMPT와 표준 Linux는 둘 중 하나만 선택해야 한다"** — 틀림. 전형적인 배포는 이기종 조합입니다. RT-PREEMPT 실시간 코어는 제어를 실행하고 표준 Linux 코어는 인식 및 AI를 실행하며, 두 코어 간에는 공유 메모리 또는 결정론적 이더넷 채널을 통해 데이터를 교환합니다.
4. **"실시간 스레드는 동적 메모리 할당을 자유롭게 사용할 수 있다"** — 틀림. 모든 실시간 스레드는 동적 메모리 할당과 비결정론적 시스템 호출을 비활성화해야 하며, 그렇지 않으면 예측 불가능한 지연이 발생합니다.

### 관련 지식

- `ent_software_ros_2_middleware_2024` — ROS 2 미들웨어는 RT-PREEMPT 위에서 실행되며, 게시/구독 통신의 결정론은 하위 운영 체제의 실시간 기능에 의존합니다.
- `ent_software_qnx` — QNX는 RT-PREEMPT와 나란히 선택할 수 있는 실시간 운영 체제 옵션으로, 마이크로커널 아키텍처와 안전 인증 자격이 차별화된 장점입니다.
- `ent_paper_gutierrez_real_time_linux_communications_2018` — 이 연구는 PREEMPT-RT Linux에서 UDP 통신 실시간 성능을 평가하여 CPU/IRQ 격리가 지연 경계를 유지하는 데 효과적임을 검증했습니다.
