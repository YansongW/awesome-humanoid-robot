---
$id: ent_software_ros_2_middleware_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: software_platform
names:
  en: ROS 2 Middleware
  zh: ROS 2 中间件
  ko: ROS 2 미들웨어
summary:
  en: De facto robot middleware with DDS-based pub/sub and real-time support.
  zh: 基于DDS的发布/订阅和实时支持的事实标准机器人中间件。
  ko: DDS 기반 pub/sub 및 실시간 지원을 갖춘 사실상의 로봇 미들웨어.
domains:
- 08_software_middleware
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- dds
- middleware
- real_time
- ros2
- software_platform
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b2_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: ROS 2 Middleware
  url: https://www.ros.org/
  date: '2024'
  accessed_at: '2026-07-02'
---
## 概述

ROS 2 中间件是基于 DDS（Data Distribution Service）标准实现发布/订阅通信与实时支持的机器人软件层，位于操作系统与机器人应用之间，是人形机器人系统中连接感知、决策与执行模块的"神经系统"。它通过 RMW（ROS Middleware）抽象层屏蔽底层 DDS 实现的差异，使开发者无需修改应用代码即可切换不同通信后端。

## 核心内容

### 是什么：准确定义

ROS 2 中间件是 ROS 2（Robot Operating System 2）框架中负责分布式通信的核心软件层。它建立在 OMG（Object Management Group）制定的 DDS 标准之上，采用发布-订阅（publish-subscribe）语义：发送方（发布者）和接收方（订阅者）通过主题（topic）解耦，无需彼此知道对方存在。这种设计使机器人系统中的多个计算节点——如视觉感知节点、运动规划节点、关节控制节点——能够以松耦合方式协同工作。

从软件分层角度看，ROS 2 中间件位于操作系统（Linux、QNX 等）与机器人应用之间，提供通信、数据序列化、时间同步、服务发现等通用服务。它真正改变的不是"机器人能做什么"，而是"机器人各模块如何高效协作"——将原本紧耦合的单体程序拆解为可独立开发、部署和扩展的分布式组件。

### 为什么存在：痛点与历史定位

在 ROS 2 出现之前，ROS 1 的通信机制存在根本性局限：其基于 TCPROS/UDPROS 的自研协议缺乏实时性保证，无法满足人形机器人对确定性通信的严苛需求。一个典型双足机器人可能同时运行：几十赫兹的多相机感知流、千赫兹的关节力控环、十赫兹以上的高层规划，以及跨机身各模块的时间同步、故障诊断和日志记录。这些流量在带宽、延迟、抖动、可靠性和安全性上的需求差异极大，无法用单一网络或协议满足。

ROS 1 的另一个痛点是缺乏安全性机制——没有内置的加密、认证和访问控制，这在工业与商业场景中不可接受。DDS 标准恰好提供了完整的 QoS（Quality of Service）策略体系、DDS-Security 安全规范以及成熟的商业与开源实现。ROS 2 选择 DDS 作为底层通信标准，本质上是将机器人通信从"自研协议时代"推进到"标准化工业通信时代"。

### 原理拆解

**① 发布-订阅与 DDS 全局数据空间**

DDS 的核心模型是"全局数据空间"（Global Data Space）：所有参与者（DomainParticipant）加入同一个域（Domain），通过主题发布和订阅数据。每个主题关联一种数据类型，发布者写入数据，订阅者异步接收。这种模型天然支持一对多、多对一、多对多的通信拓扑，且无需中心服务器。

**② RMW 抽象层：屏蔽 DDS 差异**

ROS 2 通过 RMW 接口屏蔽底层 DDS 实现差异。`rmw_fastrtps_cpp`、`rmw_cyclonedds_cpp`、`rmw_connextdds` 等是不同 DDS 的适配层。切换 RMW 通常只需设置环境变量 `RMW_IMPLEMENTATION`，无需修改应用代码。这一设计使 ROS 2 既能运行在开源 DDS 上，也能切换到商业 DDS 以满足高安全场景需求。

**③ Composition 与零拷贝：突破性能瓶颈**

对于千赫兹级关节力控环，传统 DDS 网络通信的拷贝开销不可接受。ROS 2 提供 Composition 机制——把多个节点放入同一进程，通过进程内通信（intra-process communication）绕过 DDS 网络层，直接进行指针传递（pointer passing），实现零拷贝（zero-copy）数据传输。这一机制使同一计算单元内的节点间通信延迟降低一个数量级以上（工程判断）。

**④ 实时性支持：从"尽力而为"到"确定性"**

ROS 2 中间件通过 QoS 策略（如 BEST_EFFORT 与 RELIABLE 可靠性策略、DEADLINE 截止时间策略）为不同流量提供差异化服务。对于关节力控这类硬实时流量，可配置 RELIABLE + 高优先级 QoS；对于感知流这类大带宽容忍丢失的流量，可配置 BEST_EFFORT。配合底层确定性总线（如 TSN、EtherCAT、CAN-FD），ROS 2 中间件能够在同一系统中同时承载软实时与硬实时流量。

### 关键参数与规格

| 实现 | 许可证 | ROS 2 默认 RMW | 共享内存 | DDS-Security | 主要工具链 | 典型适用场景 |
|---|---|---|---|---|---|---|
| Eclipse CycloneDDS | EPL 2.0 | Rolling/Jazzy 默认 | 支持 | 部分支持 | `cyclonedds` CLI | 开源机器人、学术项目 |
| eProsima Fast DDS | Apache 2.0 | Humble 默认 | 优秀 | 支持 | Fast DDS Monitor | 商业机器人、同机零拷贝 |
| RTI Connext DDS | 商业 | 可选 `rmw_connextdds` | 支持 | 完整 | RTI Admin Console、Monitor | 航空航天、汽车、高安全 |
| GurumDDS | 商业 | 可选 | 支持 | 完整 | Gurum Net | 韩国工业/机器人市场 |
| OpenDDS | 开源（OCI） | 无官方 RMW | 支持 | 支持 | OpenDDS Monitor | 已有 OpenDDS 生态 |

选型要点：开源学术项目优先 CycloneDDS（简洁高性能）或 Fast DDS（功能丰富）；高安全工业场景选 RTI Connext DDS（DDS-Security 完整）；韩国市场可考虑 GurumDDS。需注意 OpenDDS 无官方 RMW，集成成本较高（工程判断）。

### 横向对比

| 维度 | ROS 2 中间件（DDS 方案） | ROS 1（TCPROS/UDPROS） | 自研通信层 |
|---|---|---|---|
| 实时性 | 支持 QoS 策略，可配合确定性总线 | 无实时保证 | 需自行实现 |
| 安全性 | DDS-Security（部分实现完整） | 无内置安全 | 需自行实现 |
| 生态 | 丰富（ROS 2 工具链、MoveIt 2、Nav2） | 成熟但停止演进 | 无生态 |
| 学习成本 | 中等（需理解 DDS 概念） | 低 | 高 |
| 适用场景 | 人形机器人、多机器人系统、工业 | 学术原型 | 特定嵌入式场景 |

ROS 2 中间件的核心优势在于"标准化 + 生态"：它不仅是通信层，更是整个 ROS 2 生态（ros2_control、MoveIt 2、Nav2、Behavior Trees）的通信底座。相比之下，自研通信层虽可针对特定硬件深度优化，但无法获得社区持续维护的算法与工具。

### 谁在用·应用案例

ROS 2 中间件已广泛应用于人形机器人的原型验证、学术研究与早期商业化产品中。典型场景包括：

- **学术研究**：高校人形机器人实验室使用 ROS 2 + CycloneDDS 搭建感知-规划-控制全栈，利用 Composition 机制将视觉与力控节点放入同一进程实现低延迟通信。
- **商业机器人**：采用 Fast DDS 作为默认 RMW，利用其优秀的共享内存支持实现同机零拷贝，满足关节力控环的千赫兹通信需求。
- **高安全场景**：航空航天与汽车领域选用 RTI Connext DDS，利用其完整的 DDS-Security 实现加密与访问控制。

### 局限与边界

1. **实时性上限**：ROS 2 中间件本身不提供硬实时保证——它依赖底层操作系统（如 QNX、PREEMPT_RT Linux）与确定性总线（TSN、EtherCAT）共同实现。纯软件层面的 ROS 2 通信无法满足微秒级确定性需求。
2. **DDS 实现差异**：不同 DDS 实现虽遵循同一标准，但在性能、工具链、安全特性上差异显著。切换 RMW 虽无需改代码，但性能特征可能大幅变化，需重新验证。
3. **资源开销**：DDS 的完整功能（服务发现、QoS 管理、安全）带来额外内存与 CPU 开销，在资源受限的嵌入式控制器上可能成为瓶颈。
4. **学习曲线**：DDS 的 QoS 策略体系复杂，初学者容易配置错误导致通信异常（如 RELIABLE 策略下订阅者离线导致发布者阻塞）。

### 常见误区

1. **"ROS 2 中间件 = DDS"**——不准确。ROS 2 中间件是 ROS 2 框架中基于 DDS 的通信层，包含 RMW 抽象、Composition、消息序列化等自有机制；DDS 是底层通信标准，两者是包含关系而非等同关系。
2. **"切换 RMW 后性能不变"**——错误。不同 DDS 实现在延迟、吞吐、内存占用上差异显著，切换后必须重新进行性能验证。
3. **"零拷贝 = 所有场景都更快"**——错误。零拷贝（指针传递）仅在 Composition（同进程）场景下有效；跨进程或跨设备通信仍需序列化与网络传输。
4. **"ROS 2 中间件能保证硬实时"**——错误。它提供 QoS 机制与实时性支持，但硬实时保证需要操作系统与底层总线协同，ROS 2 中间件只是其中一环。

### 相关知识

- `ent_paper_kronauer_latency_analysis_of_ros2_multi_2021` — 对 ROS 2 多机通信延迟的系统性分析，揭示不同 RMW 与 QoS 配置下的延迟特征
- `ent_paper_paul_performance_evaluation_of_ros2_2024` — 评估 ROS 2 在不同硬件与网络条件下的性能表现，为选型提供数据支撑
- `ent_paper_bravo_arrabal_strengthening_multi_robot_syst_2025` — 探讨如何基于 ROS 2 强化多机器人系统的通信可靠性与安全性
- `ent_software_qnx` — QNX 作为硬实时操作系统，与 ROS 2 中间件配合可满足关节力控等确定性通信需求
- `ent_paper_gao_real_time_service_subscription_2025` — 车载边缘计算中的实时服务订阅与卸载，与 ROS 2 中间件的 QoS 机制在思想上相通
- `ent_paper_koubaa_service_oriented_robotic_compu_2019` — 面向服务的机器人计算框架，与 ROS 2 发布-订阅模型同属分布式通信范式

## 参考

- [ROS 2 Middleware](https://www.ros.org/)
- [Humanoid Full Development Workflow](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Chapter 06: Communication and Middleware](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-06.md)

## Overview

The ROS 2 middleware is a robotics software layer based on the DDS (Data Distribution Service) standard that implements publish/subscribe communication and real-time support. It sits between the operating system and robot applications, serving as the "nervous system" connecting perception, decision-making, and execution modules in humanoid robot systems. Through the RMW (ROS Middleware) abstraction layer, it shields differences in underlying DDS implementations, allowing developers to switch between different communication backends without modifying application code.

## Content

### What It Is: A Precise Definition

The ROS 2 middleware is the core software layer responsible for distributed communication within the ROS 2 (Robot Operating System 2) framework. It is built on the DDS standard established by OMG (Object Management Group) and employs publish-subscribe semantics: senders (publishers) and receivers (subscribers) are decoupled through topics, without needing to know of each other's existence. This design enables multiple computational nodes in a robot system—such as vision perception nodes, motion planning nodes, and joint control nodes—to collaborate in a loosely coupled manner.

From a software layering perspective, the ROS 2 middleware resides between the operating system (Linux, QNX, etc.) and robot applications, providing common services such as communication, data serialization, time synchronization, and service discovery. What it truly changes is not "what robots can do," but "how efficiently robot modules collaborate"—decomposing what were tightly coupled monolithic programs into distributed components that can be independently developed, deployed, and scaled.

### Why It Exists: Pain Points and Historical Positioning

Before ROS 2, the communication mechanism of ROS 1 had fundamental limitations: its proprietary protocol based on TCPROS/UDPROS lacked real-time guarantees and could not meet the stringent deterministic communication requirements of humanoid robots. A typical bipedal robot might simultaneously run: multi-camera perception streams at tens of hertz, joint force control loops at kilohertz, high-level planning above ten hertz, as well as time synchronization, fault diagnosis, and logging across modules throughout the body. These traffic flows have vastly different requirements in bandwidth, latency, jitter, reliability, and security, which cannot be satisfied by a single network or protocol.

Another pain point of ROS 1 was the lack of security mechanisms—no built-in encryption, authentication, or access control, which was unacceptable in industrial and commercial scenarios. The DDS standard happened to provide a complete QoS (Quality of Service) policy framework, the DDS-Security specification, and mature commercial and open-source implementations. ROS 2's choice of DDS as the underlying communication standard essentially advanced robot communication from the "proprietary protocol era" to the "standardized industrial communication era."

### Principle Breakdown

**① Publish-Subscribe and the DDS Global Data Space**

The core model of DDS is the "Global Data Space": all participants (DomainParticipants) join the same domain and publish and subscribe to data through topics. Each topic is associated with a data type; publishers write data, and subscribers receive it asynchronously. This model naturally supports one-to-many, many-to-one, and many-to-many communication topologies without requiring a central server.

**② RMW Abstraction Layer: Shielding DDS Differences**

ROS 2 shields differences in underlying DDS implementations through the RMW interface. `rmw_fastrtps_cpp`, `rmw_cyclonedds_cpp`, `rmw_connextdds`, and others are adapter layers for different DDS implementations. Switching RMW typically only requires setting the environment variable `RMW_IMPLEMENTATION`, with no need to modify application code. This design allows ROS 2 to run on open-source DDS or switch to commercial DDS to meet high-safety scenario requirements.

**③ Composition and Zero-Copy: Breaking Through Performance Bottlenecks**

For kilohertz-level joint force control loops, the copy overhead of traditional DDS network communication is unacceptable. ROS 2 provides the Composition mechanism—placing multiple nodes in the same process and using intra-process communication to bypass the DDS network layer, directly performing pointer passing for zero-copy data transfer. This mechanism reduces communication latency between nodes within the same computational unit by more than an order of magnitude (engineering judgment).

**④ Real-Time Support: From "Best Effort" to "Deterministic"**

The ROS 2 middleware provides differentiated services for various traffic types through QoS policies (such as BEST_EFFORT and RELIABLE reliability policies, and the DEADLINE deadline policy). For hard real-time traffic like joint force control, RELIABLE + high-priority QoS can be configured; for perception streams with large bandwidth and tolerance for loss, BEST_EFFORT can be configured. Combined with underlying deterministic buses (such as TSN, EtherCAT, CAN-FD), the ROS 2 middleware can simultaneously carry both soft and hard real-time traffic within the same system.

### Key Parameters and Specifications

| Implementation | License | ROS 2 Default RMW | Shared Memory | DDS-Security | Primary Toolchain | Typical Use Cases |
|---|---|---|---|---|---|---|
| Eclipse CycloneDDS | EPL 2.0 | Rolling/Jazzy default | Supported | Partial support | `cyclonedds` CLI | Open-source robotics, academic projects |
| eProsima Fast DDS | Apache 2.0 | Humble default | Excellent | Supported | Fast DDS Monitor | Commercial robotics, same-machine zero-copy |
| RTI Connext DDS | Commercial | Optional `rmw_connextdds` | Supported | Complete | RTI Admin Console, Monitor | Aerospace, automotive, high safety |
| GurumDDS | Commercial | Optional | Supported | Complete | Gurum Net | Korean industrial/robotics market |
| OpenDDS | Open source (OCI) | No official RMW | Supported | Supported | OpenDDS Monitor | Existing OpenDDS ecosystem |

Selection guidelines: For open-source academic projects, prioritize CycloneDDS (simple and high-performance) or Fast DDS (feature-rich); for high-safety industrial scenarios, choose RTI Connext DDS (complete DDS-Security); for the Korean market, consider GurumDDS. Note that OpenDDS has no official RMW, resulting in higher integration costs (engineering judgment).

### Horizontal Comparison

| Dimension | ROS 2 Middleware (DDS Solution) | ROS 1 (TCPROS/UDPROS) | Custom Communication Layer |
|---|---|---|---|
| Real-time | Supports QoS policies, can work with deterministic buses | No real-time guarantees | Must be implemented from scratch |
| Security | DDS-Security (partially complete implementations) | No built-in security | Must be implemented from scratch |
| Ecosystem | Rich (ROS 2 toolchain, MoveIt 2, Nav2) | Mature but no longer evolving | No ecosystem |
| Learning curve | Moderate (requires understanding DDS concepts) | Low | High |
| Use cases | Humanoid robots, multi-robot systems, industrial | Academic prototypes | Specific embedded scenarios |

The core advantage of the ROS 2 middleware lies in "standardization + ecosystem": it is not just a communication layer, but the communication foundation for the entire ROS 2 ecosystem (ros2_control, MoveIt 2, Nav2, Behavior Trees). In contrast, although a custom communication layer can be deeply optimized for specific hardware, it cannot benefit from community-maintained algorithms and tools.

### Who Uses It: Application Cases

The ROS 2 middleware has been widely applied in humanoid robot prototyping, academic research, and early commercial products. Typical scenarios include:

- **Academic research**: University humanoid robot laboratories use ROS 2 + CycloneDDS to build full perception-planning-control stacks, leveraging the Composition mechanism to place vision and force control nodes in the same process for low-latency communication.
- **Commercial robotics**: Fast DDS is used as the default RMW, leveraging its excellent shared memory support for same-machine zero-copy to meet kilohertz communication requirements of joint force control loops.
- **High-safety scenarios**: Aerospace and automotive fields choose RTI Connext DDS, utilizing its complete DDS-Security for encryption and access control.

### Limitations and Boundaries

1. **Real-time ceiling**: The ROS 2 middleware itself does not provide hard real-time guarantees—it relies on the underlying operating system (such as QNX, PREEMPT_RT Linux) and deterministic buses (TSN, EtherCAT) to achieve this together. Pure software-level ROS 2 communication cannot meet microsecond-level deterministic requirements.
2. **DDS implementation differences**: Although different DDS implementations follow the same standard, they differ significantly in performance, toolchains, and security features. Switching RMW does not require code changes, but performance characteristics may change dramatically and require revalidation.
3. **Resource overhead**: The full feature set of DDS (service discovery, QoS management, security) incurs additional memory and CPU overhead, which can become a bottleneck on resource-constrained embedded controllers.
4. **Learning curve**: The QoS policy framework of DDS is complex, and beginners can easily misconfigure it, leading to communication anomalies (e.g., a subscriber going offline under the RELIABLE policy causing the publisher to block).

### Common Misconceptions

1. **"ROS 2 middleware = DDS"**—Inaccurate. The ROS 2 middleware is the communication layer within the ROS 2 framework based on DDS, including proprietary mechanisms such as RMW abstraction, Composition, and message serialization; DDS is the underlying communication standard, and the relationship is one of inclusion, not equivalence.
2. **"Performance remains unchanged after switching RMW"**—Incorrect. Different DDS implementations vary significantly in latency, throughput, and memory usage; performance must be revalidated after switching.
3. **"Zero-copy = faster in all scenarios"**—Incorrect. Zero-copy (pointer passing) is only effective in Composition (same-process) scenarios; cross-process or cross-device communication still requires serialization and network transmission.
4. **"The ROS 2 middleware can guarantee hard real-time"**—Incorrect. It provides QoS mechanisms and real-time support, but hard real-time guarantees require coordination between the operating system and the underlying bus; the ROS 2 middleware is only one part of the chain.

### Related Knowledge

- `ent_paper_kronauer_latency_analysis_of_ros2_multi_2021` — A systematic analysis of ROS 2 multi-machine communication latency, revealing latency characteristics under different RMW and QoS configurations
- `ent_paper_paul_performance_evaluation_of_ros2_2024` — Evaluates ROS 2 performance across different hardware and network conditions, providing data support for selection decisions
- `ent_paper_bravo_arrabal_strengthening_multi_robot_syst_2025` — Explores how to strengthen communication reliability and security in multi-robot systems based on ROS 2
- `ent_software_qnx` — QNX as a hard real-time operating system, which, combined with the ROS 2 middleware, can meet deterministic communication requirements such as joint force control
- `ent_paper_gao_real_time_service_subscription_2025` — Real-time service subscription and offloading in vehicular edge computing, conceptually aligned with the QoS mechanisms of the ROS 2 middleware
- `ent_paper_koubaa_service_oriented_robotic_compu_2019` — A service-oriented robotic computing framework, sharing the distributed communication paradigm with the ROS 2 publish-subscribe model

## 개요

ROS 2 미들웨어는 DDS(Data Distribution Service) 표준을 기반으로 발행/구독 통신과 실시간 지원을 구현하는 로봇 소프트웨어 계층으로, 운영체제와 로봇 애플리케이션 사이에 위치하며, 휴머노이드 로봇 시스템에서 인지, 의사결정, 실행 모듈을 연결하는 "신경 시스템"입니다. RMW(ROS Middleware) 추상화 계층을 통해 하위 DDS 구현의 차이를 숨겨, 개발자가 애플리케이션 코드를 수정하지 않고도 다양한 통신 백엔드를 전환할 수 있습니다.

## 핵심 내용

### 무엇인가: 정확한 정의

ROS 2 미들웨어는 ROS 2(Robot Operating System 2) 프레임워크에서 분산 통신을 담당하는 핵심 소프트웨어 계층입니다. OMG(Object Management Group)가 제정한 DDS 표준 위에 구축되었으며, 발행-구독(publish-subscribe) 의미론을 채택합니다: 송신자(발행자)와 수신자(구독자)는 토픽(topic)을 통해 디커플링되어 서로의 존재를 알 필요가 없습니다. 이 설계는 로봇 시스템의 여러 계산 노드——예: 시각 인지 노드, 모션 플래닝 노드, 관절 제어 노드——가 느슨한 결합 방식으로 협력할 수 있게 합니다.

소프트웨어 계층 관점에서 ROS 2 미들웨어는 운영체제(Linux, QNX 등)와 로봇 애플리케이션 사이에 위치하며, 통신, 데이터 직렬화, 시간 동기화, 서비스 발견 등의 공통 서비스를 제공합니다. 이것이 실제로 바꾸는 것은 "로봇이 무엇을 할 수 있는가"가 아니라 "로봇의 각 모듈이 어떻게 효율적으로 협력하는가"입니다——원래 긴밀하게 결합된 모놀리식 프로그램을 독립적으로 개발, 배포, 확장 가능한 분산 컴포넌트로 분해합니다.

### 왜 존재하는가: 문제점과 역사적 위치

ROS 2가 등장하기 전, ROS 1의 통신 메커니즘에는 근본적인 한계가 있었습니다: TCPROS/UDPROS 기반의 자체 프로토콜은 실시간성 보장이 없어 휴머노이드 로봇의 결정적 통신에 대한 엄격한 요구를 충족할 수 없었습니다. 전형적인 이족 보행 로봇은 동시에 실행할 수 있습니다: 수십 헤르츠의 멀티 카메라 인지 스트림, 킬로헤르츠의 관절 힘 제어 루프, 10헤르츠 이상의 고수준 플래닝, 그리고 기체 전체 모듈의 시간 동기화, 장애 진단, 로그 기록. 이러한 트래픽은 대역폭, 지연, 지터, 신뢰성, 보안성 측면에서 요구 사항이 매우 달라 단일 네트워크나 프로토콜로 충족할 수 없습니다.

ROS 1의 또 다른 문제점은 보안 메커니즘 부재——내장된 암호화, 인증, 접근 제어가 없어 산업 및 상업 시나리오에서 수용 불가능했습니다. DDS 표준은恰好 완전한 QoS(Quality of Service) 정책 체계, DDS-Security 보안 규격, 그리고 성숙한 상용 및 오픈소스 구현을 제공합니다. ROS 2가 DDS를 하위 통신 표준으로 선택한 것은 본질적으로 로봇 통신을 "자체 프로토콜 시대"에서 "표준화된 산업 통신 시대"로 전환한 것입니다.

### 원리 분해

**① 발행-구독과 DDS 글로벌 데이터 공간**

DDS의 핵심 모델은 "글로벌 데이터 공간"(Global Data Space)입니다: 모든 참가자(DomainParticipant)는 동일한 도메인(Domain)에 참여하여 토픽을 통해 데이터를 발행하고 구독합니다. 각 토픽은 하나의 데이터 타입과 연결되며, 발행자는 데이터를 쓰고 구독자는 비동기적으로 수신합니다. 이 모델은 자연스럽게 일대다, 다대일, 다대다 통신 토폴로지를 지원하며 중앙 서버가 필요 없습니다.

**② RMW 추상화 계층: DDS 차이 숨기기**

ROS 2는 RMW 인터페이스를 통해 하위 DDS 구현 차이를 숨깁니다. `rmw_fastrtps_cpp`, `rmw_cyclonedds_cpp`, `rmw_connextdds` 등은 서로 다른 DDS의 어댑터 계층입니다. RMW 전환은 일반적으로 환경 변수 `RMW_IMPLEMENTATION`만 설정하면 되며 애플리케이션 코드 수정이 필요 없습니다. 이 설계는 ROS 2가 오픈소스 DDS에서 실행될 수 있을 뿐만 아니라 높은 보안 시나리오 요구를 충족하기 위해 상용 DDS로 전환할 수 있게 합니다.

**③ Composition과 제로 카피: 성능 병목 돌파**

킬로헤르츠급 관절 힘 제어 루프의 경우, 기존 DDS 네트워크 통신의 복사 오버헤드는 수용 불가능합니다. ROS 2는 Composition 메커니즘을 제공합니다——여러 노드를 동일한 프로세스에 배치하고, 프로세스 내 통신(intra-process communication)을 통해 DDS 네트워크 계층을 우회하여 직접 포인터 전달(pointer passing)로 제로 카피(zero-copy) 데이터 전송을 구현합니다. 이 메커니즘은 동일한 계산 유닛 내 노드 간 통신 지연을 한 자릿수 이상 낮춥니다(엔지니어링 판단).

**④ 실시간 지원: "최선 노력"에서 "결정성"으로**

ROS 2 미들웨어는 QoS 정책(예: BEST_EFFORT와 RELIABLE 신뢰성 정책, DEADLINE 데드라인 정책)을 통해 다양한 트래픽에 차별화된 서비스를 제공합니다. 관절 힘 제어와 같은 하드 실시간 트래픽의 경우 RELIABLE + 높은 우선순위 QoS를 구성할 수 있고, 인지 스트림과 같은 대역폭이 크고 손실을 허용하는 트래픽의 경우 BEST_EFFORT를 구성할 수 있습니다. 하위 결정적 버스(예: TSN, EtherCAT, CAN-FD)와 결합하여 ROS 2 미들웨어는 동일한 시스템에서 소프트 실시간과 하드 실시간 트래픽을 동시에 수용할 수 있습니다.

### 주요 매개변수와 사양

| 구현 | 라이선스 | ROS 2 기본 RMW | 공유 메모리 | DDS-Security | 주요 도구 체인 | 전형적 적용 시나리오 |
|---|---|---|---|---|---|---|
| Eclipse CycloneDDS | EPL 2.0 | Rolling/Jazzy 기본 | 지원 | 부분 지원 | `cyclonedds` CLI | 오픈소스 로봇, 학술 프로젝트 |
| eProsima Fast DDS | Apache 2.0 | Humble 기본 | 우수 | 지원 | Fast DDS Monitor | 상용 로봇, 동일 머신 제로 카피 |
| RTI Connext DDS | 상용 | 선택 `rmw_connextdds` | 지원 | 완전 | RTI Admin Console, Monitor | 항공우주, 자동차, 고보안 |
| GurumDDS | 상용 | 선택 | 지원 | 완전 | Gurum Net | 한국 산업/로봇 시장 |
| OpenDDS | 오픈소스(OCI) | 공식 RMW 없음 | 지원 | 지원 | OpenDDS Monitor | 기존 OpenDDS 생태계 |

선정 요점: 오픈소스 학술 프로젝트는 CycloneDDS(간결하고 고성능) 또는 Fast DDS(기능 풍부) 우선; 고보안 산업 시나리오는 RTI Connext DDS(DDS-Security 완전) 선택; 한국 시장은 GurumDDS 고려 가능. OpenDDS는 공식 RMW가 없어 통합 비용이 높다는 점에 유의해야 합니다(엔지니어링 판단).

### 횡적 비교

| 차원 | ROS 2 미들웨어(DDS 방식) | ROS 1(TCPROS/UDPROS) | 자체 개발 통신 계층 |
|---|---|---|---|
| 실시간성 | QoS 정책 지원, 결정적 버스 결합 가능 | 실시간 보장 없음 | 직접 구현 필요 |
| 보안성 | DDS-Security(일부 구현 완전) | 내장 보안 없음 | 직접 구현 필요 |
| 생태계 | 풍부(ROS 2 도구 체인, MoveIt 2, Nav2) | 성숙하나 진화 중단 | 생태계 없음 |
| 학습 비용 | 중간(DDS 개념 이해 필요) | 낮음 | 높음 |
| 적용 시나리오 | 휴머노이드 로봇, 다중 로봇 시스템, 산업 | 학술 프로토타입 | 특정 임베디드 시나리오 |

ROS 2 미들웨어의 핵심 장점은 "표준화 + 생태계"입니다: 단순한 통신 계층이 아니라 전체 ROS 2 생태계(ros2_control, MoveIt 2, Nav2, Behavior Trees)의 통신 기반입니다. 반면 자체 개발 통신 계층은 특정 하드웨어에 대해 심층 최적화가 가능하지만, 커뮤니티가 지속적으로 유지보수하는 알고리즘과 도구를 얻을 수 없습니다.

### 누가 사용하는가·적용 사례

ROS 2 미들웨어는 휴머노이드 로봇의 프로토타입 검증, 학술 연구, 초기 상용화 제품에 널리 적용되었습니다. 전형적인 시나리오는 다음과 같습니다:

- **학술 연구**: 대학 휴머노이드 로봇 연구실은 ROS 2 + CycloneDDS로 인지-플래닝-제어 풀스택을 구축하고, Composition 메커니즘을 활용하여 시각 및 힘 제어 노드를 동일한 프로세스에 배치하여 저지연 통신을 구현합니다.
- **상용 로봇**: Fast DDS를 기본 RMW로 채택하고, 우수한 공유 메모리 지원을 활용하여 동일 머신 제로 카피를 구현하여 관절 힘 제어 루프의 킬로헤르츠 통신 요구를 충족합니다.
- **고보안 시나리오**: 항공우주 및 자동차 분야는 RTI Connext DDS를 선택하여 완전한 DDS-Security로 암호화와 접근 제어를 구현합니다.

### 한계와 경계

1. **실시간성 상한**: ROS 2 미들웨어 자체는 하드 실시간 보장을 제공하지 않습니다——하위 운영체제(예: QNX, PREEMPT_RT Linux)와 결정적 버스(TSN, EtherCAT)에 의존하여 공동 구현합니다. 순수 소프트웨어 수준의 ROS 2 통신은 마이크로초 단위의 결정적 요구를 충족할 수 없습니다.
2. **DDS 구현 차이**: 서로 다른 DDS 구현은 동일한 표준을 따르지만 성능, 도구 체인, 보안 기능에서 차이가显著합니다. RMW 전환은 코드 수정이 필요 없지만 성능 특성이 크게 변할 수 있어 재검증이 필요합니다.
3. **리소스 오버헤드**: DDS의 완전한 기능(서비스 발견, QoS 관리, 보안)은 추가 메모리와 CPU 오버헤드를 발생시켜 리소스가 제한된 임베디드 컨트롤러에서 병목이 될 수 있습니다.
4. **학습 곡선**: DDS의 QoS 정책 체계는 복잡하여 초보자가 설정 오류로 통신 이상을 일으키기 쉽습니다(예: RELIABLE 정책에서 구독자 오프라인 시 발행자 차단).

### 일반적인 오해

1. **"ROS 2 미들웨어 = DDS"**——부정확합니다. ROS 2 미들웨어는 ROS 2 프레임워크에서 DDS 기반의 통신 계층으로, RMW 추상화, Composition, 메시지 직렬화 등의 자체 메커니즘을 포함합니다; DDS는 하위 통신 표준으로, 둘은 포함 관계이지 동등 관계가 아닙니다.
2. **"RMW 전환 후 성능 불변"**——오류입니다. 서로 다른 DDS 구현은 지연, 처리량, 메모리 점유에서 차이가显著하여 전환 후 반드시 성능 재검증이 필요합니다.
3. **"제로 카피 = 모든 시나리오에서 더 빠름"**——오류입니다. 제로 카피(포인터 전달)는 Composition(동일 프로세스) 시나리오에서만 유효합니다; 프로세스 간 또는 장치 간 통신은 여전히 직렬화와 네트워크 전송이 필요합니다.
4. **"ROS 2 미들웨어가 하드 실시간을 보장한다"**——오류입니다. QoS 메커니즘과 실시간 지원을 제공하지만, 하드 실시간 보장은 운영체제와 하위 버스의 협력이 필요하며 ROS 2 미들웨어는 그중 하나의 요소일 뿐입니다.

### 관련 지식

- `ent_paper_kronauer_latency_analysis_of_ros2_multi_2021` — ROS 2 다중 머신 통신 지연에 대한 체계적 분석으로, 다양한 RMW와 QoS 구성에서의 지연 특성을 밝힘
- `ent_paper_paul_performance_evaluation_of_ros2_2024` — 다양한 하드웨어와 네트워크 조건에서 ROS 2의 성능을 평가하여 선정에 데이터 지원 제공
- `ent_paper_bravo_arrabal_strengthening_multi_robot_syst_2025` — ROS 2 기반 다중 로봇 시스템의 통신 신뢰성과 보안성 강화 방법 탐구
- `ent_software_qnx` — QNX는 하드 실시간 운영체제로, ROS 2 미들웨어와 결합하여 관절 힘 제어 등의 결정적 통신 요구 충족 가능
- `ent_paper_gao_real_time_service_subscription_2025` — 차량 엣지 컴퓨팅의 실시간 서비스 구독과 오프로딩으로, ROS 2 미들웨어의 QoS 메커니즘과 사상적으로相通
- `ent_paper_koubaa_service_oriented_robotic_compu_2019` — 서비스 지향 로봇 컴퓨팅 프레임워크로, ROS 2 발행-구독 모델과 동일한 분산 통신 패러다임에 속함
