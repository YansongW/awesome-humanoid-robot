---
$id: ent_software_lerobot_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: software_platform
names:
  en: LeRobot
  zh: LeRobot
  ko: LeRobot
summary:
  en: End-to-end PyTorch library covering dataset formats, teleoperation, training, and deployment.
  zh: 覆盖数据集格式、遥操作、训练和部署的端到端PyTorch库。
  ko: 데이터셋 형식, 텔레오퍼레이션, 학습 및 배포를 포괄하는 엔드투엔드 PyTorch 라이브러리.
domains:
- 08_software_middleware
- 09_data_datasets
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- framework
- lerobot
- pytorch
- software_platform
- teleoperation
- training
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b2_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: LeRobot
  url: https://github.com/huggingface/lerobot
  date: '2024'
  accessed_at: '2026-07-02'
---
## 概述

LeRobot 是 Hugging Face 维护的覆盖数据集格式、遥操作、训练和部署的端到端 PyTorch 库，旨在降低机器人学习（尤其是模仿学习与 VLA 模型）的入门门槛。它真正改变的不是某个单一算法，而是将"从数据到上机"的碎片化流程收敛为统一框架，使学术界与小团队能以标准化方式构建数据管线并复用预训练模型。

## 核心内容

### 是什么：准确定义

LeRobot 是一个硬件无关、Python 原生的开源软件平台，由 Hugging Face 维护，覆盖策略学习栈的四个环节：标准化数据集格式（LeRobotDataset）、遥操作采集接口、主流策略实现（ACT、扩散策略等）与部署推理。其核心目标正如官方仓库所述："降低入门门槛，让每个人都能贡献并受益于共享数据集与预训练模型"。

它属于**软件平台**类型，所属领域包括软件中间件、数据与数据集、AI 模型与算法，价值链层级为智能层。在端到端软件栈的架构中，LeRobot 与 ROS 2、Pinocchio、MoveIt/OMPL 共同构成集成骨架，但定位不同：ROS 2 提供通信中间件，Pinocchio 提供动力学内核，而 LeRobot 专注于**策略学习的数据与训练闭环**。

### 为什么存在：痛点与历史定位

在 LeRobot 出现之前，机器人学习社区面临严重的碎片化问题。每个实验室自建数据格式、自写采集脚本、自训策略，导致：

1. **数据无法互通**：不同团队采集的"裸轨迹"缺乏统一的 schema（本体标识、时间戳、坐标系定义、任务描述），混训与追溯成本极高。
2. **遥操作到训练的鸿沟**：采集端与训练端工具链割裂，从数据采集到策略部署往往需要数周的工程适配。
3. **预训练模型难以复用**：缺乏统一的模型 zoo 与数据集托管平台，跨团队复现成本高。

LeRobot 的历史定位是成为机器人学习领域的"数据与模型枢纽"：它背靠 Hugging Face Hub 的托管能力，把数据集格式、策略实现与部署接口统一起来。正如第 21 章所述，它"已成为学术界与小团队构建数据管线的事实起点"。

### 原理拆解

**① 标准化数据格式：LeRobotDataset**

LeRobotDataset 采用 Parquet + MP4（或图片）的混合存储方案，托管于 Hugging Face Hub。其核心设计原则是：

- **多模态组织**：以标准化 schema 组织多视角视频、状态与动作序列，支持流式读取与增量追加。
- **时间同步**：各模态时间戳显式存储，保证训练时观测与动作的严格对齐。
- **随机访问**：Parquet 列式存储支持高效随机读取，避免加载整个数据集。

**② 遥操作采集接口**

LeRobot 提供硬件无关的遥操作接口，兼容从低成本机械臂（如 SO-100）到人形机器人的多种平台。采集时同步记录全部观测与动作，天然形成"观测—动作"对，可直接用于行为克隆。

**③ 策略训练与部署**

LeRobot 内置多种经真实世界验证的主流策略实现（ACT、扩散策略等），并提供统一的训练与部署推理接口。其价值在于"经过验证的端到端参考实现"——企业可在其数据格式与策略接口上构建私有管线，把工程投入集中在本体适配与安全层。

**④ 数据格式转换的工程架构**

实践中常见的架构是"采集端用企业自建格式保证效率，入湖时转换为 LeRobot/RLDS 等标准格式用于训练"，两种格式之间维护一套经过版本化管理的转换器。LeRobot 格式与 RLDS（Open X-Embodiment 采用）是两条被广泛遵循的标准，前者强调即插即用，后者强调片段级元数据与跨数据集互操作性。

### 关键参数与规格

| 维度 | 规格/说明 |
|------|-----------|
| 底层框架 | PyTorch |
| 数据存储 | Parquet + MP4 或图片 |
| 数据托管 | Hugging Face Hub |
| 支持策略 | ACT、扩散策略等主流方法 |
| 硬件兼容 | 硬件无关，从 SO-100 到人形机器人 |
| 社区规模 | GitHub Star 26.6k，Fork 5.4k，1,702 Commits（按语料数据） |
| 生态集成 | NVIDIA Isaac GR00T 1.7、Isaac Teleop、Cosmos 3（计划中） |

### 横向对比

| 方案 | 代表 | 优势 | 局限 |
|------|------|------|------|
| 标准化研究格式 | LeRobot 数据集格式、RLDS（Open X-Embodiment 采用） | 生态成熟、与开源训练管线即插即用 | 对力觉/触觉等扩展模态的支持需要自行扩展 schema |
| 企业自建格式 | 各数据工厂内部 schema | 贴合自有本体与协议、检索效率高 | 跨机构共享与开源复现成本高 |
| 通用序列化容器 | MCAP、HDF5、Parquet 等 | 工具链通用、性能可控 | 缺少机器人语义层，需要自行定义约定 |

与 ROS 2 相比，LeRobot 不处理通信与实时控制，而是聚焦学习栈；与 MoveIt/OMPL 相比，LeRobot 不提供运动规划，而是提供策略学习与部署。它真正改变的不是机器人底层控制，而是**数据到策略的转化效率**。

### 谁在用·应用案例

**① 学术界与小团队**：LeRobot 已成为构建数据管线的事实起点，其标准化格式与预训练模型降低了冷启动成本。

**② NVIDIA 与 Hugging Face 的生态集成**：NVIDIA 将 Isaac GR00T 1.7（面向人形机器人的推理 VLA 模型）和 Isaac Teleop 框架集成至 LeRobot，并计划加入 Cosmos 3 世界模型，为开发者提供从仿真训练到真实部署的全链路路径。

**③ 安全研究靶标**：Imperio 团队以 smolVLA 模型为靶标，在 LeRobot 平台的真实抓取任务中验证数据投毒攻击，仅需 3 个投毒片段（占 0.94%）即可实现完全拒绝服务攻击——这从侧面说明 LeRobot 已成为社区公认的实验基准平台。

### 局限与边界

1. **扩展模态支持有限**：对力觉/触觉等扩展模态的支持需要自行扩展 schema，标准格式并非万能。
2. **不涉及底层控制**：LeRobot 不提供实时控制、运动规划或动力学计算，需与 ROS 2、Pinocchio、WBC/MPC 等组件配合。
3. **数据质量依赖上游**：时间同步、质检与去重决定有效数据率，LeRobot 提供格式但不保证数据质量。
4. **安全责任在集成方**：LeRobot 提供参考实现，但安全监控通路与本体适配需企业自行构建。

### 常见误区

1. **"LeRobot 是一个完整的机器人操作系统"**——错。它是策略学习栈，不处理通信、实时控制与规划，需与 ROS 2 等中间件配合。
2. **"用了 LeRobot 就能直接训练出可部署策略"**——错。公开数据集的真实价值往往在于预训练底座与基准，而非直接训练出可部署策略；把公开数据当作能力上限以外的补充而非替代，才是现实定位。
3. **"LeRobot 格式是唯一标准"**——错。RLDS 是另一条被广泛遵循的标准，实践中常需维护格式转换器。
4. **"数据格式统一就解决了数据问题"**——错。数据 schema 设计应显式包含本体标识、时间戳、坐标系定义、任务描述与采集协议版本，缺少这些元数据的"裸轨迹"在混训与追溯中会付出高昂代价。

### 相关知识

- `ent_report_blog_nvidia_and_hugging_face_bring_2026` — NVIDIA 将 Isaac GR00T 1.7 与 Isaac Teleop 集成至 LeRobot，扩展其全链路能力
- `ent_paper_imperio_smolvla_the_implicatio_2026` — 以 LeRobot 平台为实验基准，验证数据投毒对 VLA 模型的实际威胁
- `ent_paper_safe_vision_language_action_models_barri_2026` — 在 VLA 模型部署中引入 CBF 安全保证，与 LeRobot 部署流程互补
- `ent_paper_open_aoe_open_egocentric_manipulation_da_2026` — 开放第一人称操作数据集工具链，与 LeRobot 在数据基础设施层面形成对照

## 参考

- [LeRobot GitHub 仓库](https://github.com/huggingface/lerobot)
- [第 21 章：数据基础设施](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-21.md)
- [第 24 章：端到端软件栈](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-24.md)

## Overview

LeRobot is an end-to-end PyTorch library maintained by Hugging Face that covers dataset formats, teleoperation, training, and deployment, aiming to lower the barrier to entry for robot learning (especially imitation learning and VLA models). What it truly changes is not a single algorithm, but rather the convergence of the fragmented "from data to deployment" pipeline into a unified framework, enabling academia and small teams to build data pipelines in a standardized way and reuse pretrained models.

## Content

### What It Is: A Precise Definition

LeRobot is a hardware-agnostic, Python-native open-source software platform maintained by Hugging Face, covering four stages of the policy learning stack: standardized dataset format (LeRobotDataset), teleoperation data collection interfaces, mainstream policy implementations (ACT, diffusion policies, etc.), and deployment inference. Its core goal, as stated in the official repository, is to "lower the barrier to entry so that everyone can contribute to and benefit from shared datasets and pretrained models."

It belongs to the **software platform** category, spanning software middleware, data and datasets, AI models and algorithms, and sits at the intelligence layer of the value chain. Within the end-to-end software stack architecture, LeRobot forms the integration backbone together with ROS 2, Pinocchio, and MoveIt/OMPL, but with distinct roles: ROS 2 provides communication middleware, Pinocchio provides the dynamics kernel, and LeRobot focuses on the **data and training loop for policy learning**.

### Why It Exists: Pain Points and Historical Positioning

Before LeRobot, the robot learning community faced severe fragmentation. Each lab built its own data formats, wrote its own collection scripts, and trained its own policies, leading to:

1. **Data incompatibility**: "Raw trajectories" collected by different teams lacked a unified schema (embodiment identifiers, timestamps, coordinate frame definitions, task descriptions), making mixed training and traceability extremely costly.
2. **The teleoperation-to-training gap**: The toolchains for data collection and training were disconnected, often requiring weeks of engineering adaptation from data collection to policy deployment.
3. **Difficulty reusing pretrained models**: The lack of a unified model zoo and dataset hosting platform made cross-team reproduction costly.

LeRobot's historical positioning is to become the "data and model hub" for robot learning: backed by Hugging Face Hub's hosting capabilities, it unifies dataset formats, policy implementations, and deployment interfaces. As described in Chapter 21, it "has become the de facto starting point for academia and small teams building data pipelines."

### Principle Breakdown

**① Standardized Data Format: LeRobotDataset**

LeRobotDataset uses a hybrid storage scheme of Parquet + MP4 (or images), hosted on Hugging Face Hub. Its core design principles are:

- **Multimodal organization**: Organizes multi-view videos, states, and action sequences using a standardized schema, supporting streaming reads and incremental appends.
- **Time synchronization**: Timestamps for each modality are explicitly stored, ensuring strict alignment between observations and actions during training.
- **Random access**: Parquet's columnar storage supports efficient random reads, avoiding the need to load entire datasets.

**② Teleoperation Data Collection Interface**

LeRobot provides hardware-agnostic teleoperation interfaces compatible with a wide range of platforms, from low-cost robotic arms (e.g., SO-100) to humanoid robots. During collection, all observations and actions are recorded synchronously, naturally forming "observation-action" pairs that can be directly used for behavior cloning.

**③ Policy Training and Deployment**

LeRobot includes multiple mainstream policy implementations validated in the real world (ACT, diffusion policies, etc.) and provides unified training and deployment inference interfaces. Its value lies in being "validated end-to-end reference implementations"—enterprises can build private pipelines on top of its data format and policy interfaces, concentrating engineering effort on embodiment adaptation and safety layers.

**④ Engineering Architecture for Data Format Conversion**

A common architecture in practice is "collection endpoints use enterprise-custom formats for efficiency, then convert to standard formats such as LeRobot/RLDS for training when entering the data lake," with a set of version-managed converters maintained between the two formats. The LeRobot format and RLDS (adopted by Open X-Embodiment) are two widely followed standards: the former emphasizes plug-and-play, while the latter emphasizes episode-level metadata and cross-dataset interoperability.

### Key Parameters and Specifications

| Dimension | Specification/Description |
|-----------|--------------------------|
| Underlying framework | PyTorch |
| Data storage | Parquet + MP4 or images |
| Data hosting | Hugging Face Hub |
| Supported policies | ACT, diffusion policies, and other mainstream methods |
| Hardware compatibility | Hardware-agnostic, from SO-100 to humanoid robots |
| Community scale | 26.6k GitHub Stars, 5.4k Forks, 1,702 Commits (per corpus data) |
| Ecosystem integrations | NVIDIA Isaac GR00T 1.7, Isaac Teleop, Cosmos 3 (planned) |

### Horizontal Comparison

| Approach | Representative | Advantages | Limitations |
|----------|----------------|------------|-------------|
| Standardized research formats | LeRobot dataset format, RLDS (adopted by Open X-Embodiment) | Mature ecosystem, plug-and-play with open-source training pipelines | Support for extended modalities such as force/tactile sensing requires custom schema extensions |
| Enterprise-custom formats | Internal schemas of various data factories | Tailored to proprietary embodiments and protocols, high retrieval efficiency | High cost for cross-institution sharing and open-source reproduction |
| General-purpose serialization containers | MCAP, HDF5, Parquet, etc. | General-purpose toolchains, controllable performance | Lack robot-specific semantic layers, requiring custom conventions |

Compared to ROS 2, LeRobot does not handle communication or real-time control, focusing instead on the learning stack; compared to MoveIt/OMPL, LeRobot does not provide motion planning but rather policy learning and deployment. What it truly changes is not low-level robot control, but the **efficiency of transforming data into policies**.

### Who Uses It · Application Cases

**① Academia and Small Teams**: LeRobot has become the de facto starting point for building data pipelines, with its standardized format and pretrained models lowering cold-start costs.

**② NVIDIA and Hugging Face Ecosystem Integration**: NVIDIA has integrated Isaac GR00T 1.7 (an inference VLA model for humanoid robots) and the Isaac Teleop framework into LeRobot, with plans to add the Cosmos 3 world model, providing developers with an end-to-end path from simulation training to real-world deployment.

**③ Security Research Target**: The Imperio team used the smolVLA model as a target to validate data poisoning attacks in real grasping tasks on the LeRobot platform, achieving complete denial-of-service with just 3 poisoned episodes (0.94% of the data)—which indirectly demonstrates that LeRobot has become a recognized experimental benchmark platform in the community.

### Limitations and Boundaries

1. **Limited support for extended modalities**: Support for extended modalities such as force/tactile sensing requires custom schema extensions; the standard format is not a one-size-fits-all solution.
2. **No low-level control**: LeRobot does not provide real-time control, motion planning, or dynamics computation, requiring integration with components such as ROS 2, Pinocchio, and WBC/MPC.
3. **Data quality depends on upstream**: Time synchronization, quality inspection, and deduplication determine the effective data rate; LeRobot provides the format but does not guarantee data quality.
4. **Safety responsibility lies with the integrator**: LeRobot provides reference implementations, but safety monitoring pathways and embodiment adaptation must be built by the enterprise itself.

### Common Misconceptions

1. **"LeRobot is a complete robot operating system"**—Wrong. It is a policy learning stack that does not handle communication, real-time control, or planning, and must be paired with middleware such as ROS 2.
2. **"Using LeRobot means you can directly train deployable policies"**—Wrong. The real value of public datasets often lies in pretraining foundations and benchmarks, not in directly training deployable policies; treating public data as a supplement beyond one's capability ceiling, rather than a replacement, is the realistic positioning.
3. **"The LeRobot format is the only standard"**—Wrong. RLDS is another widely followed standard, and format converters often need to be maintained in practice.
4. **"Unified data formats solve the data problem"**—Wrong. Data schema design should explicitly include embodiment identifiers, timestamps, coordinate frame definitions, task descriptions, and collection protocol versions; "raw trajectories" lacking these metadata incur high costs in mixed training and traceability.

### Related Knowledge

- `ent_report_blog_nvidia_and_hugging_face_bring_2026` — NVIDIA integrates Isaac GR00T 1.7 and Isaac Teleop into LeRobot, expanding its end-to-end capabilities
- `ent_paper_imperio_smolvla_the_implicatio_2026` — Uses the LeRobot platform as an experimental benchmark to validate the real threat of data poisoning to VLA models
- `ent_paper_safe_vision_language_action_models_barri_2026` — Introduces CBF safety guarantees in VLA model deployment, complementing the LeRobot deployment pipeline
- `ent_paper_open_aoe_open_egocentric_manipulation_da_2026` — Open-source egocentric manipulation dataset toolchain, contrasting with LeRobot at the data infrastructure level

## 개요

LeRobot은 Hugging Face가 유지 관리하는 데이터셋 형식, 원격 조작, 훈련 및 배포를 아우르는 엔드투엔드 PyTorch 라이브러리로, 로봇 학습(특히 모방 학습 및 VLA 모델)의 진입 장벽을 낮추는 것을 목표로 합니다. 이 라이브러리가 실제로 바꾸는 것은 특정 단일 알고리즘이 아니라, "데이터에서 실기 적용까지"의 파편화된 프로세스를 통합 프레임워크로 수렴시켜 학계와 소규모 팀이 표준화된 방식으로 데이터 파이프라인을 구축하고 사전 훈련된 모델을 재사용할 수 있게 하는 것입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

LeRobot은 Hugging Face가 유지 관리하는 하드웨어 독립적이고 Python 기반의 오픈소스 소프트웨어 플랫폼으로, 정책 학습 스택의 네 가지 단계를 포괄합니다: 표준화된 데이터셋 형식(LeRobotDataset), 원격 조작 수집 인터페이스, 주요 정책 구현(ACT, 확산 정책 등) 및 배포 추론. 핵심 목표는 공식 저장소에 명시된 바와 같이 "진입 장벽을 낮추어 모든 사람이 공유 데이터셋과 사전 훈련된 모델에 기여하고 혜택을 받을 수 있게 하는 것"입니다.

이는 **소프트웨어 플랫폼** 유형에 속하며, 관련 분야는 소프트웨어 미들웨어, 데이터 및 데이터셋, AI 모델 및 알고리즘을 포함하고, 가치 사슬 계층은 지능 계층입니다. 엔드투엔드 소프트웨어 스택 아키텍처에서 LeRobot은 ROS 2, Pinocchio, MoveIt/OMPL과 함께 통합 골격을 구성하지만, 역할은 다릅니다: ROS 2는 통신 미들웨어를 제공하고, Pinocchio는 동역학 커널을 제공하며, LeRobot은 **정책 학습의 데이터 및 훈련 루프**에 집중합니다.

### 왜 존재하는가: 문제점과 역사적 위치

LeRobot이 등장하기 전, 로봇 학습 커뮤니티는 심각한 파편화 문제에 직면했습니다. 각 연구실은 자체 데이터 형식을 구축하고, 자체 수집 스크립트를 작성하며, 자체 정책을 훈련하여 다음과 같은 문제가 발생했습니다:

1. **데이터 상호 운용 불가**: 서로 다른 팀이 수집한 "원시 궤적"은 통일된 스키마(본체 식별, 타임스탬프, 좌표계 정의, 작업 설명)가 없어 혼합 훈련과 추적 비용이 매우 높았습니다.
2. **원격 조작에서 훈련까지의 격차**: 수집 측과 훈련 측 도구 체인이 분리되어 데이터 수집에서 정책 배포까지 수 주간의 엔지니어링 적응이 필요했습니다.
3. **사전 훈련된 모델 재사용 어려움**: 통일된 모델 zoo와 데이터셋 호스팅 플랫폼이 없어 팀 간 재현 비용이 높았습니다.

LeRobot의 역사적 위치는 로봇 학습 분야의 "데이터 및 모델 허브"가 되는 것입니다: Hugging Face Hub의 호스팅 역량을 기반으로 데이터셋 형식, 정책 구현 및 배포 인터페이스를 통합합니다. 제21장에서 언급된 바와 같이, 이는 "학계와 소규모 팀이 데이터 파이프라인을 구축하는 사실상의 출발점이 되었습니다."

### 원리 분석

**① 표준화된 데이터 형식: LeRobotDataset**

LeRobotDataset은 Parquet + MP4(또는 이미지)의 혼합 저장 방식을 채택하며, Hugging Face Hub에 호스팅됩니다. 핵심 설계 원칙은 다음과 같습니다:

- **멀티모달 구성**: 표준화된 스키마로 다중 시점 비디오, 상태 및 행동 시퀀스를 구성하며, 스트리밍 읽기와 증분 추가를 지원합니다.
- **시간 동기화**: 각 모달리티의 타임스탬프를 명시적으로 저장하여 훈련 시 관측과 행동의 엄격한 정렬을 보장합니다.
- **임의 접근**: Parquet 열 기반 저장은 효율적인 임의 읽기를 지원하여 전체 데이터셋을 로드하지 않아도 됩니다.

**② 원격 조작 수집 인터페이스**

LeRobot은 하드웨어 독립적인 원격 조작 인터페이스를 제공하며, 저비용 로봇 팔(예: SO-100)부터 휴머노이드 로봇까지 다양한 플랫폼을 지원합니다. 수집 시 모든 관측과 행동을 동기적으로 기록하여 자연스럽게 "관측-행동" 쌍을 형성하며, 행동 복제에 직접 사용할 수 있습니다.

**③ 정책 훈련 및 배포**

LeRobot은 실제 세계에서 검증된 여러 주요 정책 구현(ACT, 확산 정책 등)을 내장하고 있으며, 통일된 훈련 및 배포 추론 인터페이스를 제공합니다. 그 가치는 "검증된 엔드투엔드 참조 구현"에 있습니다—기업은 자체 데이터 형식과 정책 인터페이스 위에 사설 파이프라인을 구축하여 엔지니어링 투자를 본체 적응과 안전 계층에 집중할 수 있습니다.

**④ 데이터 형식 변환의 엔지니어링 아키텍처**

실무에서 흔한 아키텍처는 "수집 측은 기업 자체 형식으로 효율성을 보장하고, 데이터 레이크에 유입 시 LeRobot/RLDS 등 표준 형식으로 변환하여 훈련에 사용"하는 방식이며, 두 형식 간에는 버전 관리되는 변환기 세트를 유지합니다. LeRobot 형식과 RLDS(Open X-Embodiment 채택)는 널리 따르는 두 가지 표준으로, 전자는 플러그 앤 플레이를 강조하고 후자는 에피소드 수준 메타데이터와 교차 데이터셋 상호 운용성을 강조합니다.

### 주요 매개변수 및 사양

| 차원 | 사양/설명 |
|------|-----------|
| 기본 프레임워크 | PyTorch |
| 데이터 저장 | Parquet + MP4 또는 이미지 |
| 데이터 호스팅 | Hugging Face Hub |
| 지원 정책 | ACT, 확산 정책 등 주요 방법 |
| 하드웨어 호환성 | 하드웨어 독립적, SO-100부터 휴머노이드 로봇까지 |
| 커뮤니티 규모 | GitHub Star 26.6k, Fork 5.4k, 1,702 Commits(코퍼스 데이터 기준) |
| 생태계 통합 | NVIDIA Isaac GR00T 1.7, Isaac Teleop, Cosmos 3(계획 중) |

### 횡적 비교

| 방안 | 대표 | 장점 | 한계 |
|------|------|------|------|
| 표준화된 연구 형식 | LeRobot 데이터셋 형식, RLDS(Open X-Embodiment 채택) | 생태계 성숙, 오픈소스 훈련 파이프라인과 플러그 앤 플레이 | 힘/촉각 등 확장 모달리티 지원은 자체 스키마 확장 필요 |
| 기업 자체 형식 | 각 데이터 팩토리 내부 스키마 | 자체 본체 및 프로토콜에 적합, 검색 효율 높음 | 기관 간 공유 및 오픈소스 재현 비용 높음 |
| 범용 직렬화 컨테이너 | MCAP, HDF5, Parquet 등 | 도구 체인 범용, 성능 제어 가능 | 로봇 의미론 계층 부재, 자체 규칙 정의 필요 |

ROS 2와 비교하여 LeRobot은 통신과 실시간 제어를 처리하지 않고 학습 스택에 집중합니다; MoveIt/OMPL과 비교하여 LeRobot은 운동 계획을 제공하지 않고 정책 학습과 배포를 제공합니다. 이 라이브러리가 실제로 바꾸는 것은 로봇의 저수준 제어가 아니라 **데이터에서 정책으로의 변환 효율**입니다.

### 사용자 및 응용 사례

**① 학계 및 소규모 팀**: LeRobot은 데이터 파이프라인 구축의 사실상 출발점이 되었으며, 표준화된 형식과 사전 훈련된 모델은 초기 비용을 낮춥니다.

**② NVIDIA와 Hugging Face의 생태계 통합**: NVIDIA는 Isaac GR00T 1.7(휴머노이드 로봇용 추론 VLA 모델)과 Isaac Teleop 프레임워크를 LeRobot에 통합하고, Cosmos 3 세계 모델 추가를 계획하여 개발자에게 시뮬레이션 훈련부터 실제 배포까지의 전체 경로를 제공합니다.

**③ 보안 연구 표적**: Imperio 팀은 smolVLA 모델을 표적으로 LeRobot 플랫폼의 실제 파지 작업에서 데이터 중독 공격을 검증했으며, 단 3개의 중독 조각(0.94% 차지)으로 완전한 서비스 거부 공격을 달성할 수 있었습니다—이는 LeRobot이 커뮤니티에서 인정받는 실험 벤치마크 플랫폼이 되었음을 보여줍니다.

### 한계 및 경계

1. **확장 모달리티 지원 제한**: 힘/촉각 등 확장 모달리티 지원은 자체 스키마 확장이 필요하며, 표준 형식이 만능은 아닙니다.
2. **저수준 제어 미포함**: LeRobot은 실시간 제어, 운동 계획 또는 동역학 계산을 제공하지 않으며, ROS 2, Pinocchio, WBC/MPC 등 구성 요소와 함께 사용해야 합니다.
3. **데이터 품질은 상류에 의존**: 시간 동기화, 품질 검사 및 중복 제거가 유효 데이터 비율을 결정하며, LeRobot은 형식을 제공하지만 데이터 품질을 보장하지 않습니다.
4. **안전 책임은 통합 측에 있음**: LeRobot은 참조 구현을 제공하지만, 안전 모니터링 경로와 본체 적응은 기업이 자체 구축해야 합니다.

### 흔한 오해

1. **"LeRobot은 완전한 로봇 운영 체제다"** — 틀림. 이는 정책 학습 스택이며, 통신, 실시간 제어 및 계획을 처리하지 않으므로 ROS 2 등 미들웨어와 함께 사용해야 합니다.
2. **"LeRobot을 사용하면 바로 배포 가능한 정책을 훈련할 수 있다"** — 틀림. 공개 데이터셋의 실제 가치는 종종 사전 훈련 기반과 벤치마크에 있으며, 직접 배포 가능한 정책 훈련이 아닙니다; 공개 데이터를 능력 상한을 넘는 보충이 아닌 대체물로 보는 것이 현실적인 위치입니다.
3. **"LeRobot 형식이 유일한 표준이다"** — 틀림. RLDS는 널리 따르는 또 다른 표준이며, 실무에서는 형식 변환기 유지가 자주 필요합니다.
4. **"데이터 형식 통일이 데이터 문제를 해결한다"** — 틀림. 데이터 스키마 설계는 본체 식별, 타임스탬프, 좌표계 정의, 작업 설명 및 수집 프로토콜 버전을 명시적으로 포함해야 하며, 이러한 메타데이터가 없는 "원시 궤적"은 혼합 훈련과 추적에서 높은 비용을 초래합니다.

### 관련 지식

- `ent_report_blog_nvidia_and_hugging_face_bring_2026` — NVIDIA가 Isaac GR00T 1.7과 Isaac Teleop을 LeRobot에 통합하여 전체 경로 역량 확장
- `ent_paper_imperio_smolvla_the_implicatio_2026` — LeRobot 플랫폼을 실험 벤치마크로 사용하여 데이터 중독이 VLA 모델에 미치는 실질적 위협 검증
- `ent_paper_safe_vision_language_action_models_barri_2026` — VLA 모델 배포에 CBF 안전 보장 도입, LeRobot 배포 프로세스와 상호 보완
- `ent_paper_open_aoe_open_egocentric_manipulation_da_2026` — 개방형 1인칭 조작 데이터셋 도구 체인, 데이터 인프라 계층에서 LeRobot과 대조
