---
$id: ent_rom_test
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: benchmark
names:
  en: Range of Motion Test
  zh: 活动范围测试
  ko: 가동 범위 테스트
summary:
  en: A benchmark that measures the reachable joint angles of a robot hand or limb, used to evaluate workspace coverage and
    joint flexibility.
  zh: 一种测量机器人手或肢体可达关节角度的基准，用于评估工作空间覆盖范围和关节灵活性。
  ko: 로봇 손이나 사지의 도달 가능한 관절 각도를 측정하여 작업 공간 커버리지와 관절 유연성을 평가하는 벤치마크입니다.
domains:
- 10_evaluation_benchmarks
- 06_design_engineering
layers:
- validation_markets
- midstream
functional_roles:
- knowledge
- tool_equipment
tags:
- rom
- range_of_motion
- benchmark
- workspace
- dexterous_hand
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-06-25'
  confidence: high
  notes: 'Standard benchmark referenced in the RUKA paper for joint reachability evaluation. Body backfilled from entity metadata
    by scripts/backfill_critical_entities.py.  [2026-08-13] body upgraded to textbook-grade (.staging/textbook_grade_run/b5):
    zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters + first-hand sources (number whitelist audit passed);
    en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'RUKA: Rethinking the Design of Humanoid Hands with Learning'
  url: https://arxiv.org/abs/2504.13165
  date: '2025'
  accessed_at: '2026-06-25'
theoretical_depth:
- system
---
## 概述

活动范围测试（Range of Motion Test, ROM Test）是一种测量机器人手或肢体各关节可达角度范围的基准方法，用于量化工作空间覆盖面积与关节灵活性。它真正改变的不是"能否动"的判断，而是将"动得多广"从定性描述转化为可复现、可对比的定量指标，为机械手设计中的自由度分配、执行器选型与形态优化提供数据支撑。

## 核心内容

### 是什么：活动范围测试的准确定义

活动范围测试源自康复医学中评估人体关节活动度的经典方法，被机器人学界引入后成为一种 **benchmark（基准测试）**。它通过驱动被测关节从极限位置运动到另一极限位置，记录每个关节的最大角度偏移量，进而合成整个末端（指尖或足底）的可达空间包络。

在机器人语境下，ROM Test 包含三个核心要素：

- **被测对象**：单关节（如 MCP 屈曲角）或整条运动链（如五指手的工作空间）；
- **测量手段**：关节编码器读数、运动捕捉系统（如 MANUS 手套）或视觉标记追踪；
- **输出指标**：关节角度范围（度）、末端可达体积（立方厘米或立方米）、各向同性指数等。

该测试属于评测基准领域，同时与设计工程深度耦合——它既是验收手段，也是设计迭代的输入条件。

### 为什么存在：痛点与历史定位

机械手设计长期被一个"不可能三角"困住：直接驱动方案（如 LEAP、Allegro）控制精确但体积大、成本高；腱驱动方案（如 Shadow Hand）形态拟人但非线性弹性建模困难，且成本高达 $100,000 量级。在此背景下，ROM Test 的存在源于三个具体痛点：

**① 设计验证缺乏统一标尺。** 不同团队设计的机械手，自由度数量从 6 到 20 不等，执行器布局各异。没有 ROM 数据，就无法回答"这个手的抓取能力是否配得上它的自由度数量"这一基本问题。

**② 学习式控制需要数据边界。** 以 RUKA 为代表的腱驱动手采用学习式控制器，需要从运动捕捉数据中拟合关节到执行器的映射模型。ROM 测试界定了模型训练的有效输入空间——超出 ROM 范围的关节角度在物理上不可达，模型无需也不应外推。

**③ 形态设计需要量化反馈。** 拇指对掌角度、手指 splay 范围、手掌弓曲度等形态参数，直接影响手的抓取构型多样性。ROM 测试将这些形态特征转化为可比较的数字，使设计者能判断"拟人形态"是否真的带来了"拟人功能"。

### 原理拆解

**① 关节角度测量的几何基础**

ROM 测试的本质是测量相邻连杆之间的相对旋转。对于机器人关节，这一测量通常由编码器直接完成；对于运动捕捉系统，则通过标记点的空间坐标反算关节角。设两个相邻连杆上的标记点分别为 \(P_1\)、\(P_2\)，关节中心为 \(O\)，则关节角 \(\theta\) 由向量夹角给出：

$$
\theta = \arccos\left(\frac{(P_1 - O) \cdot (P_2 - O)}{\|P_1 - O\| \, \|P_2 - O\|}\right)
$$

对于多关节链，末端位置由正运动学累积得到。以五指手为例，每根手指可视为开链结构，指尖位置 \(\mathbf{p}\) 是各关节角 \(\boldsymbol{\theta} = [\theta_1, \theta_2, \ldots, \theta_n]^T\) 的函数：

$$
\mathbf{p} = f(\boldsymbol{\theta}) = \sum_{i=1}^{n} \left( \prod_{j=1}^{i-1} \mathbf{R}_j \right) \mathbf{l}_i
$$

其中 \(\mathbf{R}_j\) 为第 \(j\) 个关节的旋转矩阵，\(\mathbf{l}_i\) 为第 \(i\) 个连杆的长度向量。ROM 测试遍历关节角的极限组合，即可得到末端可达空间的边界。

**② 工作空间包络的构建**

将各关节独立驱动至极限位置并组合采样，得到一系列末端位置点云。这些点的凸包或曲面包络即为工作空间。RUKA 论文中即采用此类方法，通过运动捕捉数据评估手的可达性（reachability），并与其他机械手进行对比。

**③ 与运动学透明度的关系**

ROM 测试不仅测量"能到哪"，还隐含评估"控制是否透明"。对于腱驱动系统，由于腱的弹性与摩擦，实际关节角与执行器位置之间存在偏差。ROM 测试中记录的"指令角度"与"实际角度"之差，反映了系统的运动学透明度——差值越小，控制越精确。

### 关键参数与规格

ROM 测试的关键参数包括：

| 参数 | 典型值/范围 | 说明 |
|------|------------|------|
| 关节角分辨率 | 0.1°–1° | 编码器精度决定，需自行确认 |
| 采样频率 | 100–1000 Hz | 动态 ROM 测量时要求，需自行确认 |
| 自由度数量 | 6–20 | 机械手常见范围，RUKA 为 15 DOF |
| 执行器数量 | 5–16 | 少于自由度时存在耦合驱动，RUKA 为 11 执行器 |
| 末端位置测量精度 | ±0.5–2 mm | 运动捕捉系统典型值，需自行确认 |
| 测试持续时间 | 5–30 分钟 | 完整 ROM 扫描所需时间，需自行确认 |

RUKA 手的具体规格：5 指、15 个欠驱动自由度、11 个执行器，拇指 3 个电机（每关节独立），其余 4 指各 2 个执行器，PIP/DIP 耦合驱动。这些参数直接决定了其 ROM 测试的测量方案——耦合关节需要解耦算法才能得到单关节角度。

### 横向对比

ROM 测试与其他常见机械手评估基准的对比：

| 基准类型 | 测量对象 | 核心指标 | 优势 | 局限 |
|---------|---------|---------|------|------|
| ROM Test | 关节角度/工作空间 | 角度范围、可达体积 | 直观、可复现、成本低 | 不反映动态性能与抓取力 |
| 抓取成功率测试 | 完整抓取任务 | 成功次数/总次数 | 任务导向、端到端 | 依赖控制器质量，难以隔离硬件因素 |
| 负载测试 | 关节力矩 | 最大持续/峰值力矩 | 量化强度指标 | 需要专用夹具，测试复杂 |
| 耐久性测试 | 重复运动 | 失效前循环次数 | 评估寿命 | 耗时长，成本高 |

RUKA 论文的评估即同时采用了 ROM（reachability）、耐久性和强度测试，形成互补。ROM 测试在其中扮演"基础能力筛查"角色——如果可达性不足，后续的抓取与操作测试无从谈起。

### 谁在用：应用案例

**① RUKA 机械手（学术研究）**

RUKA 团队使用 MANUS 手套采集运动捕捉数据，评估手的形态学精度与可达性。其开源设计允许其他研究者复现 ROM 测试流程，建立可比较的基准数据。

**② 灵巧操作实验室（学术研究）**

研究抓取规划的实验室通常将 ROM 测试作为手模型验证的第一步。在仿真环境中，ROM 数据用于校准 URDF 模型；在实物实验中，ROM 数据用于确认装配质量与设计一致性。

**③ 假肢与康复机器人（临床应用）**

仿生假肢手的 ROM 指标直接对应截肢者的功能恢复程度。临床团队使用 ROM 测试评估假肢手是否覆盖了日常生活所需的关键抓取构型。

**④ 工业机械臂（设计验证）**

虽然工业臂的 ROM 通常由规格书直接给出，但实际装配后的 ROM 测试可发现连杆干涉、线缆束缚等设计问题。

### 局限与边界

ROM 测试的边界清晰，但常被忽视：

**① 静态指标，不反映动态能力。** ROM 测量的是极限位置，而非运动过程中的实际可达性。高速运动下，惯性力可能导致实际可达范围缩小。

**② 单关节测量忽略耦合效应。** 多关节协同运动时，由于腱耦合或连杆干涉，实际工作空间可能小于各关节独立 ROM 的笛卡尔积。RUKA 的 PIP/DIP 耦合设计即意味着单关节 ROM 无法直接相加。

**③ 不包含力信息。** 一个关节在 ROM 边界处的输出力矩可能趋近于零。ROM 数据无法回答"在多大范围内能施加多大力量"这一关键问题。

**④ 测量协议影响结果。** 运动速度、负载条件、测量设备精度都会影响 ROM 数值。不同实验室的数据可比性需要统一的测量协议。

### 常见误区

1. **"ROM 越大越好"**——不准确。过大的 ROM 可能牺牲结构刚度或增加重量。RUKA 的设计哲学是"学习友好"而非"极限 ROM"，其价值在于用学习式控制弥补硬件简化带来的非线性。

2. **"ROM 测试可以替代抓取测试"**——错误。ROM 是必要不充分条件。一个 ROM 很大的手可能因控制精度不足而无法完成精细抓取。

3. **"仿真中的 ROM 等于实物 ROM"**——通常不成立。装配公差、腱的弹性、摩擦等因素使实物 ROM 往往小于仿真值。工程判断：设计时应预留 10%–20% 的 ROM 裕量。

4. **"ROM 测试只适用于手"**——不准确。该测试同样适用于机械臂、腿足式机器人的腿部关节，以及人形机器人的全身关节。足底可达区域的可视化即是一种 ROM 测试的应用。

### 相关知识

- `ent_paper_zorin_ruka_rethinking_the_design_of_2025` — RUKA 论文将 ROM 测试作为核心评估手段之一，用于验证腱驱动手的可达性优势。
- `ent_paper_chauhan_exploitation_of_material_conso_2023` — 该研究虽聚焦供应链优化，但其"层级间权衡"思想与 ROM 测试中"自由度数量 vs 控制复杂度"的权衡具有方法论上的类比性。

## 参考

- [RUKA: Rethinking the Design of Humanoid Hands with Learning](https://arxiv.org/abs/2504.13165)
- [Awesome Humanoid Robot - Development Workflow](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Chapter 09: 足底可达区域分析](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-09.md)

## Overview

The Range of Motion Test (ROM Test) is a benchmark method for measuring the achievable angular range of each joint in a robotic hand or limb, used to quantify workspace coverage area and joint flexibility. What it truly changes is not the judgment of "whether it can move," but rather transforming "how widely it moves" from a qualitative description into a reproducible, comparable quantitative metric, providing data support for degree-of-freedom allocation, actuator selection, and morphology optimization in robotic hand design.

## Content

### What It Is: A Precise Definition of the Range of Motion Test

The Range of Motion Test originates from the classic method used in rehabilitation medicine to assess human joint mobility. After being introduced into the robotics community, it became a **benchmark**. It drives the joint under test from one limit position to another, records the maximum angular deviation of each joint, and then synthesizes the reachable spatial envelope of the entire end-effector (fingertip or sole).

In the robotics context, the ROM Test comprises three core elements:

- **Subject under test**: A single joint (e.g., MCP flexion angle) or an entire kinematic chain (e.g., the workspace of a five-fingered hand);
- **Measurement means**: Joint encoder readings, motion capture systems (e.g., MANUS gloves), or visual marker tracking;
- **Output metrics**: Joint angular range (degrees), end-effector reachable volume (cubic centimeters or cubic meters), isotropy index, etc.

This test belongs to the evaluation benchmark domain while being deeply coupled with design engineering—it serves both as an acceptance tool and as input for design iteration.

### Why It Exists: Pain Points and Historical Positioning

Robotic hand design has long been trapped by an "impossible triangle": direct-drive solutions (e.g., LEAP, Allegro) offer precise control but are bulky and costly; tendon-driven solutions (e.g., Shadow Hand) are anthropomorphic in form but suffer from difficult nonlinear elasticity modeling and costs on the order of $100,000. Against this backdrop, the ROM Test exists due to three specific pain points:

**① Lack of a unified yardstick for design validation.** Robotic hands designed by different teams vary in degree-of-freedom count from 6 to 20, with diverse actuator layouts. Without ROM data, the fundamental question of "does this hand's grasping capability justify its number of degrees of freedom" cannot be answered.

**② Learning-based control requires data boundaries.** Tendon-driven hands represented by RUKA employ learning-based controllers that require fitting joint-to-actuator mapping models from motion capture data. The ROM test defines the valid input space for model training—joint angles beyond the ROM range are physically unreachable, and the model neither needs nor should extrapolate beyond them.

**③ Morphological design requires quantitative feedback.** Morphological parameters such as thumb opposition angle, finger splay range, and palm curvature directly influence the diversity of grasp configurations. The ROM test converts these morphological features into comparable numbers, enabling designers to judge whether "anthropomorphic form" truly delivers "anthropomorphic function."

### Principle Breakdown

**① Geometric basis of joint angle measurement**

The essence of the ROM test is measuring the relative rotation between adjacent links. For robotic joints, this measurement is typically performed directly by encoders; for motion capture systems, joint angles are back-calculated from the spatial coordinates of markers. Let the markers on two adjacent links be \(P_1\) and \(P_2\), with the joint center at \(O\). The joint angle \(\theta\) is then given by the angle between vectors:

$$
\theta = \arccos\left(\frac{(P_1 - O) \cdot (P_2 - O)}{\|P_1 - O\| \, \|P_2 - O\|}\right)
$$

For multi-joint chains, the end-effector position is accumulated via forward kinematics. Taking a five-fingered hand as an example, each finger can be viewed as an open-chain structure, where the fingertip position \(\mathbf{p}\) is a function of the joint angles \(\boldsymbol{\theta} = [\theta_1, \theta_2, \ldots, \theta_n]^T\):

$$
\mathbf{p} = f(\boldsymbol{\theta}) = \sum_{i=1}^{n} \left( \prod_{j=1}^{i-1} \mathbf{R}_j \right) \mathbf{l}_i
$$

where \(\mathbf{R}_j\) is the rotation matrix of the \(j\)-th joint and \(\mathbf{l}_i\) is the length vector of the \(i\)-th link. The ROM test traverses extreme combinations of joint angles to obtain the boundary of the end-effector's reachable space.

**② Construction of the workspace envelope**

Driving each joint independently to its limit positions and sampling combinations yields a point cloud of end-effector positions. The convex hull or surface envelope of these points constitutes the workspace. The RUKA paper adopts this approach, using motion capture data to evaluate hand reachability and compare it against other robotic hands.

**③ Relationship with kinematic transparency**

The ROM test not only measures "where it can reach" but also implicitly evaluates "whether control is transparent." For tendon-driven systems, due to tendon elasticity and friction, deviations exist between actual joint angles and actuator positions. The difference between "commanded angle" and "actual angle" recorded during the ROM test reflects the system's kinematic transparency—the smaller the difference, the more precise the control.

### Key Parameters and Specifications

Key parameters of the ROM test include:

| Parameter | Typical Value/Range | Description |
|-----------|--------------------|-------------|
| Joint angle resolution | 0.1°–1° | Determined by encoder precision; to be confirmed |
| Sampling frequency | 100–1000 Hz | Required for dynamic ROM measurement; to be confirmed |
| Number of degrees of freedom | 6–20 | Common range for robotic hands; RUKA has 15 DOF |
| Number of actuators | 5–16 | Coupled actuation exists when fewer than DOF; RUKA has 11 actuators |
| End-effector position measurement accuracy | ±0.5–2 mm | Typical value for motion capture systems; to be confirmed |
| Test duration | 5–30 minutes | Time required for a full ROM scan; to be confirmed |

Specific specifications of the RUKA hand: 5 fingers, 15 underactuated degrees of freedom, 11 actuators, thumb with 3 motors (one per joint independently), and the remaining 4 fingers each with 2 actuators, with PIP/DIP coupled actuation. These parameters directly determine the measurement scheme for its ROM test—coupled joints require decoupling algorithms to obtain individual joint angles.

### Horizontal Comparison

Comparison of the ROM test with other common robotic hand evaluation benchmarks:

| Benchmark Type | Measurement Subject | Core Metrics | Advantages | Limitations |
|---------------|--------------------|--------------|------------|-------------|
| ROM Test | Joint angles/workspace | Angular range, reachable volume | Intuitive, reproducible, low cost | Does not reflect dynamic performance or grasping force |
| Grasp success rate test | Complete grasping tasks | Successes/total attempts | Task-oriented, end-to-end | Depends on controller quality; difficult to isolate hardware factors |
| Load test | Joint torque | Maximum sustained/peak torque | Quantifies strength metrics | Requires specialized fixtures; complex testing |
| Durability test | Repeated motion | Cycles before failure | Evaluates lifespan | Time-consuming, high cost |

The RUKA paper's evaluation simultaneously employs ROM (reachability), durability, and strength tests, forming a complementary set. The ROM test plays the role of "basic capability screening" in this framework—if reachability is insufficient, subsequent grasping and manipulation tests are moot.

### Who Uses It: Application Cases

**① RUKA robotic hand (academic research)**

The RUKA team uses MANUS gloves to collect motion capture data, evaluating the hand's morphological accuracy and reachability. Its open-source design allows other researchers to reproduce the ROM test procedure and establish comparable benchmark data.

**② Dexterous manipulation laboratories (academic research)**

Laboratories researching grasp planning typically use the ROM test as the first step in hand model validation. In simulation environments, ROM data is used to calibrate URDF models; in physical experiments, ROM data is used to confirm assembly quality and design consistency.

**③ Prosthetics and rehabilitation robotics (clinical applications)**

ROM metrics of bionic prosthetic hands directly correspond to the degree of functional recovery for amputees. Clinical teams use the ROM test to assess whether a prosthetic hand covers the key grasp configurations required for daily living.

**④ Industrial robotic arms (design validation)**

Although the ROM of industrial arms is typically provided directly in specifications, ROM testing after actual assembly can reveal design issues such as link interference and cable constraints.

### Limitations and Boundaries

The boundaries of the ROM test are clear but often overlooked:

**① Static metric; does not reflect dynamic capability.** The ROM test measures limit positions, not actual reachability during motion. Under high-speed motion, inertial forces may reduce the actual reachable range.

**② Single-joint measurement ignores coupling effects.** During multi-joint coordinated motion, due to tendon coupling or link interference, the actual workspace may be smaller than the Cartesian product of individual joint ROMs. RUKA's PIP/DIP coupled design means that single-joint ROMs cannot be simply summed.

**③ Does not include force information.** The output torque of a joint near its ROM boundary may approach zero. ROM data cannot answer the critical question of "over what range can how much force be exerted."

**④ Measurement protocol affects results.** Motion speed, load conditions, and measurement device precision all influence ROM values. Comparability of data across different laboratories requires a unified measurement protocol.

### Common Misconceptions

1. **"Bigger ROM is always better"**—Inaccurate. Excessively large ROM may sacrifice structural stiffness or increase weight. RUKA's design philosophy is "learning-friendly" rather than "extreme ROM"; its value lies in using learning-based control to compensate for nonlinearities introduced by hardware simplification.

2. **"The ROM test can replace grasp testing"**—Incorrect. ROM is a necessary but not sufficient condition. A hand with large ROM may fail to perform precision grasps due to insufficient control accuracy.

3. **"ROM in simulation equals ROM in reality"**—Generally not true. Factors such as assembly tolerances, tendon elasticity, and friction cause physical ROM to often be smaller than simulated values. Engineering judgment: a 10%–20% ROM margin should be reserved during design.

4. **"The ROM test only applies to hands"**—Inaccurate. The test equally applies to robotic arms, leg joints of legged robots, and full-body joints of humanoid robots. Visualization of the sole's reachable area is one application of the ROM test.

### Related Knowledge

- `ent_paper_zorin_ruka_rethinking_the_design_of_2025` — The RUKA paper uses the ROM test as one of its core evaluation methods to validate the reachability advantages of the tendon-driven hand.
- `ent_paper_chauhan_exploitation_of_material_conso_2023` — Although this study focuses on supply chain optimization, its "cross-level trade-off" thinking is methodologically analogous to the "number of DOF vs. control complexity" trade-off in the ROM test.

## 개요

관절 가동 범위 테스트(Range of Motion Test, ROM Test)는 로봇 손 또는 팔다리의 각 관절이 도달할 수 있는 각도 범위를 측정하는 벤치마크 방법으로, 작업 공간 커버리지 면적과 관절 유연성을 정량화하는 데 사용됩니다. 이 테스트가 실제로 바꾸는 것은 "움직일 수 있는지"에 대한 판단이 아니라, "얼마나 넓게 움직이는지"를 정성적 설명에서 재현 가능하고 비교 가능한 정량적 지표로 전환하여, 로봇 손 설계에서의 자유도 할당, 액추에이터 선정 및 형태 최적화에 데이터 기반을 제공한다는 점입니다.

## 핵심 내용

### 무엇인가: 가동 범위 테스트의 정확한 정의

가동 범위 테스트는 재활 의학에서 인간 관절의 가동성을 평가하는 고전적 방법에서 유래했으며, 로봇 공학계에 도입된 후 **벤치마크(기준 테스트)**가 되었습니다. 이 테스트는 측정 대상 관절을 한계 위치에서 다른 한계 위치로 구동하여 각 관절의 최대 각도 편차를 기록하고, 이를 통해 말단(손끝 또는 발바닥)의 도달 가능 공간 포락선을 합성합니다.

로봇 공학 맥락에서 ROM Test는 세 가지 핵심 요소를 포함합니다:

- **측정 대상**: 단일 관절(예: MCP 굴곡 각도) 또는 전체 운동 사슬(예: 다섯 손가락 손의 작업 공간);
- **측정 수단**: 관절 엔코더 판독값, 모션 캡처 시스템(예: MANUS 글러브) 또는 시각 마커 추적;
- **출력 지표**: 관절 각도 범위(도), 말단 도달 부피(세제곱센티미터 또는 세제곱미터), 등방성 지수 등.

이 테스트는 평가 벤치마크 분야에 속하면서도 설계 엔지니어링과 깊이 결합되어 있습니다——이는 검수 수단이자 설계 반복의 입력 조건입니다.

### 왜 존재하는가: 문제점과 역사적 위치

로봇 손 설계는 오랫동안 "불가능한 삼각"에 갇혀 있었습니다: 직접 구동 방식(예: LEAP, Allegro)은 제어가 정밀하지만 부피가 크고 비용이 높습니다; 힘줄 구동 방식(예: Shadow Hand)은 형태가 인간을 닮았지만 비선형 탄성 모델링이 어렵고 비용이 $100,000 수준에 달합니다. 이러한 배경에서 ROM Test의 존재는 세 가지 구체적인 문제점에서 비롯됩니다:

**① 설계 검증의 통일된 척도 부재.** 서로 다른 팀이 설계한 로봇 손은 자유도 수가 6에서 20까지 다양하고 액추에이터 배치도 제각각입니다. ROM 데이터가 없으면 "이 손의 파지 능력이 자유도 수에 걸맞는가"라는 기본 질문에 답할 수 없습니다.

**② 학습 기반 제어는 데이터 경계를 필요로 함.** RUKA로 대표되는 힘줄 구동 손은 학습 기반 제어기를 사용하며, 모션 캡처 데이터에서 관절-액추에이터 매핑 모델을 피팅해야 합니다. ROM 테스트는 모델 훈련의 유효 입력 공간을 규정합니다——ROM 범위를 벗어난 관절 각도는 물리적으로 도달 불가능하므로, 모델은 외삽할 필요도 없고 해서도 안 됩니다.

**③ 형태 설계는 정량적 피드백을 필요로 함.** 엄지 대립 각도, 손가락 벌림 범위, 손바닥 아치 곡률 등의 형태 파라미터는 손의 파지 구성 다양성에 직접 영향을 미칩니다. ROM 테스트는 이러한 형태 특징을 비교 가능한 숫자로 변환하여 설계자가 "인간형 형태"가 실제로 "인간형 기능"을 가져왔는지 판단할 수 있게 합니다.

### 원리 분석

**① 관절 각도 측정의 기하학적 기초**

ROM 테스트의 본질은 인접 링크 간의 상대 회전을 측정하는 것입니다. 로봇 관절의 경우 이 측정은 일반적으로 엔코더로 직접 수행됩니다; 모션 캡처 시스템의 경우 마커 점의 공간 좌표에서 관절 각도를 역산합니다. 두 인접 링크의 마커 점을 각각 \(P_1\), \(P_2\), 관절 중심을 \(O\)라고 하면, 관절 각도 \(\theta\)는 벡터 각도로 주어집니다:

$$
\theta = \arccos\left(\frac{(P_1 - O) \cdot (P_2 - O)}{\|P_1 - O\| \, \|P_2 - O\|}\right)
$$

다중 관절 사슬의 경우 말단 위치는 정기구학 누적으로 얻어집니다. 다섯 손가락 손을 예로 들면, 각 손가락은 개방 사슬 구조로 볼 수 있으며, 손끝 위치 \(\mathbf{p}\)는 각 관절 각도 \(\boldsymbol{\theta} = [\theta_1, \theta_2, \ldots, \theta_n]^T\)의 함수입니다:

$$
\mathbf{p} = f(\boldsymbol{\theta}) = \sum_{i=1}^{n} \left( \prod_{j=1}^{i-1} \mathbf{R}_j \right) \mathbf{l}_i
$$

여기서 \(\mathbf{R}_j\)는 \(j\)번째 관절의 회전 행렬, \(\mathbf{l}_i\)는 \(i\)번째 링크의 길이 벡터입니다. ROM 테스트는 관절 각도의 한계 조합을 순회하여 말단 도달 가능 공간의 경계를 얻습니다.

**② 작업 공간 포락선 구축**

각 관절을 독립적으로 한계 위치까지 구동하고 조합 샘플링하여 일련의 말단 위치 점군을 얻습니다. 이 점들의 볼록 껍질 또는 곡면 포락선이 작업 공간이 됩니다. RUKA 논문에서도 이러한 방법을 사용하여 모션 캡처 데이터를 통해 손의 도달성(reachability)을 평가하고 다른 로봇 손과 비교합니다.

**③ 운동학적 투명성과의 관계**

ROM 테스트는 "어디까지 갈 수 있는지"만 측정하는 것이 아니라 "제어가 투명한지"도 암묵적으로 평가합니다. 힘줄 구동 시스템의 경우 힘줄의 탄성과 마찰로 인해 실제 관절 각도와 액추에이터 위치 사이에 편차가 존재합니다. ROM 테스트에서 기록된 "지시 각도"와 "실제 각도"의 차이는 시스템의 운동학적 투명성을 반영합니다——차이가 작을수록 제어가 더 정밀합니다.

### 핵심 파라미터와 사양

ROM 테스트의 핵심 파라미터는 다음과 같습니다:

| 파라미터 | 일반값/범위 | 설명 |
|------|------------|------|
| 관절 각도 분해능 | 0.1°–1° | 엔코더 정밀도에 의해 결정, 직접 확인 필요 |
| 샘플링 주파수 | 100–1000 Hz | 동적 ROM 측정 시 요구, 직접 확인 필요 |
| 자유도 수 | 6–20 | 로봇 손 일반 범위, RUKA는 15 DOF |
| 액추에이터 수 | 5–16 | 자유도보다 적으면 결합 구동 존재, RUKA는 11개 액추에이터 |
| 말단 위치 측정 정밀도 | ±0.5–2 mm | 모션 캡처 시스템 일반값, 직접 확인 필요 |
| 테스트 지속 시간 | 5–30분 | 전체 ROM 스캔 소요 시간, 직접 확인 필요 |

RUKA 손의 구체적 사양: 5손가락, 15개의 과소구동 자유도, 11개 액추에이터, 엄지 3개 모터(각 관절 독립), 나머지 4개 손가락 각각 2개 액추에이터, PIP/DIP 결합 구동. 이러한 파라미터는 ROM 테스트의 측정 방식을 직접 결정합니다——결합 관절은 단일 관절 각도를 얻기 위해 디커플링 알고리즘이 필요합니다.

### 수평 비교

ROM 테스트와 다른 일반적인 로봇 손 평가 벤치마크의 비교:

| 벤치마크 유형 | 측정 대상 | 핵심 지표 | 장점 | 한계 |
|---------|---------|---------|------|------|
| ROM Test | 관절 각도/작업 공간 | 각도 범위, 도달 부피 | 직관적, 재현 가능, 저비용 | 동적 성능과 파지력을 반영하지 않음 |
| 파지 성공률 테스트 | 전체 파지 작업 | 성공 횟수/총 횟수 | 작업 지향, 종단 간 | 컨트롤러 품질에 의존, 하드웨어 요인 분리 어려움 |
| 부하 테스트 | 관절 토크 | 최대 지속/피크 토크 | 강도 지표 정량화 | 전용 고정 장치 필요, 테스트 복잡 |
| 내구성 테스트 | 반복 운동 | 파손 전 사이클 수 | 수명 평가 | 시간 소요, 비용 높음 |

RUKA 논문의 평가는 ROM(도달성), 내구성 및 강도 테스트를 동시에 사용하여 상호 보완합니다. ROM 테스트는 그중 "기본 능력 선별" 역할을 합니다——도달성이 부족하면 이후의 파지 및 조작 테스트는 의미가 없습니다.

### 누가 사용하는가: 적용 사례

**① RUKA 로봇 손(학술 연구)**

RUKA 팀은 MANUS 글러브를 사용하여 모션 캡처 데이터를 수집하고 손의 형태학적 정밀도와 도달성을 평가합니다. 오픈소스 설계 덕분에 다른 연구자들이 ROM 테스트 절차를 재현하고 비교 가능한 벤치마크 데이터를 구축할 수 있습니다.

**② 정밀 조작 연구실(학술 연구)**

파지 계획을 연구하는 실험실은 일반적으로 ROM 테스트를 손 모델 검증의 첫 단계로 사용합니다. 시뮬레이션 환경에서는 ROM 데이터가 URDF 모델 보정에 사용됩니다; 실물 실험에서는 ROM 데이터가 조립 품질과 설계 일관성을 확인하는 데 사용됩니다.

**③ 의수 및 재활 로봇(임상 응용)**

생체 모방 의수 손의 ROM 지표는 절단자의 기능 회복 정도에 직접 대응합니다. 임상 팀은 ROM 테스트를 사용하여 의수 손이 일상생활에 필요한 핵심 파지 구성을 포함하는지 평가합니다.

**④ 산업용 로봇 팔(설계 검증)**

산업용 팔의 ROM은 일반적으로 사양서에 직접 제공되지만, 실제 조립 후 ROM 테스트는 링크 간섭, 케이블 구속 등의 설계 문제를 발견할 수 있습니다.

### 한계와 경계

ROM 테스트의 경계는 명확하지만 종종 간과됩니다:

**① 정적 지표, 동적 능력 미반영.** ROM은 한계 위치를 측정하는 것이지 운동 과정에서의 실제 도달성을 측정하는 것이 아닙니다. 고속 운동에서는 관성력으로 인해 실제 도달 범위가 줄어들 수 있습니다.

**② 단일 관절 측정은 결합 효과를 무시.** 다중 관절 협조 운동 시 힘줄 결합 또는 링크 간섭으로 인해 실제 작업 공간은 각 관절 독립 ROM의 데카르트 곱보다 작을 수 있습니다. RUKA의 PIP/DIP 결합 설계는 단일 관절 ROM을 단순히 더할 수 없음을 의미합니다.

**③ 힘 정보 미포함.** ROM 경계에서 관절의 출력 토크는 0에 가까워질 수 있습니다. ROM 데이터는 "어느 범위에서 얼마나 큰 힘을 가할 수 있는가"라는 핵심 질문에 답할 수 없습니다.

**④ 측정 프로토콜이 결과에 영향.** 운동 속도, 부하 조건, 측정 장비 정밀도 모두 ROM 수치에 영향을 미칩니다. 서로 다른 실험실 간 데이터 비교 가능성은 통일된 측정 프로토콜이 필요합니다.

### 일반적인 오해

1. **"ROM이 클수록 좋다"**——부정확. 과도한 ROM은 구조 강성을 희생하거나 무게를 증가시킬 수 있습니다. RUKA의 설계 철학은 "학습 친화적"이지 "극한 ROM"이 아니며, 그 가치는 학습 기반 제어로 하드웨어 단순화로 인한 비선형성을 보완하는 데 있습니다.

2. **"ROM 테스트가 파지 테스트를 대체할 수 있다"**——오류. ROM은 필요 조건이지 충분 조건이 아닙니다. ROM이 큰 손도 제어 정밀도가 부족하면 정밀 파지를 완료하지 못할 수 있습니다.

3. **"시뮬레이션의 ROM은 실물 ROM과 같다"**——일반적으로 성립하지 않음. 조립 공차, 힘줄 탄성, 마찰 등의 요인으로 실물 ROM은 시뮬레이션 값보다 작은 경우가 많습니다. 공학적 판단: 설계 시 10%–20%의 ROM 여유를预留해야 합니다.

4. **"ROM 테스트는 손에만 적용된다"**——부정확. 이 테스트는 로봇 팔, 보행 로봇의 다리 관절, 그리고 휴머노이드 로봇의 전신 관절에도 동일하게 적용됩니다. 발바닥 도달 영역의 시각화는 ROM 테스트의 한 응용입니다.

### 관련 지식

- `ent_paper_zorin_ruka_rethinking_the_design_of_2025` — RUKA 논문은 ROM 테스트를 핵심 평가 수단 중 하나로 사용하여 힘줄 구동 손의 도달성 우위를 검증합니다.
- `ent_paper_chauhan_exploitation_of_material_conso_2023` — 이 연구는 공급망 최적화에 초점을 맞추지만, "계층 간 균형" 사고는 ROM 테스트에서의 "자유도 수 vs 제어 복잡성" 균형과 방법론적으로 유사합니다.
