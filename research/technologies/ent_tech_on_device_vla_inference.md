---
$id: ent_tech_on_device_vla_inference
$schema: ../../../../../data/schema/v1/entry_schema.json
$version: 1
type: technology
names:
  en: On-Device VLA Inference
  zh: 端侧 VLA 推理
  ko: 온디바이스 VLA 추론
summary:
  en: The practice of running Vision-Language-Action models directly on robot-embedded edge compute rather than offloading
    to edge or cloud servers, driven by latency, connectivity, and privacy constraints.
  zh: 概述 将视觉-语言-动作模型直接部署在机器人内置边缘计算设备上，而非卸载到边缘或云端服务器，以满足延迟、连接性和隐私约束。
  ko: 지연 시간, 연결성 및 프라이버시 제약을 해결하기 위해 VLA 모델을 엣지 또는 클라우드 서버가 아닌 로봇 임베디드 엣지 컴퓨팅에서 직접 실행하는 기술 방식.
domains:
- 07_ai_models_algorithms
- 08_software_middleware
- 02_components
layers:
- intelligence
functional_roles:
- intelligence
- tool_equipment
tags:
- vla
- on_device_inference
- edge_compute
- latency
- real_time_control
- humanoid
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-06-26'
  confidence: high
  notes: 'AI-extracted from VLA-Perf paper and NVIDIA deployment blog; specific latency claims depend on model and quantization
    configuration. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12] body upgraded
    to textbook-grade (.staging/textbook_grade_run/b2_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters
    + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_vla_perf_paper
  type: paper
  title: How Fast Can I Run My VLA? Demystifying VLA Inference Performance with VLA-Perf
  url: https://arxiv.org/abs/2602.18397
  date: '2026-02-20'
  accessed_at: '2026-06-26'
- id: src_nvidia_gr00t_n16_blog
  type: website
  title: Building Generalist Humanoid Capabilities with NVIDIA Isaac GR00T N1.6 Using a Sim-to-Real Workflow
  url: https://developer.nvidia.com/blog/building-generalist-humanoid-capabilities-with-nvidia-isaac-gr00t-n1-6-using-a-sim-to-real-workflow/
  date: '2026-01-08'
  accessed_at: '2026-06-26'
theoretical_depth:
- system
related_entities:
- id: ent_method_action_token_prediction
  relationship: uses
  description:
    en: On-device VLA inference deploys models that rely on action token prediction.
    zh: 端侧 VLA 推理部署依赖动作 token 预测的模型。
    ko: 온디바이스 VLA 추론은 액션 토큰 예측에 의존하는 모델을 배포한다.
- id: ent_paper_openvla_2024
  relationship: is_based_on
  description:
    en: On-device VLA inference builds on architectures such as OpenVLA.
    zh: 端侧 VLA 推理建立在 OpenVLA 等架构之上。
    ko: 온디바이스 VLA 추론은 OpenVLA와 같은 아키텍처에 기반한다.
---
## 概述

端侧 VLA 推理（On-Device VLA Inference）指将视觉-语言-动作模型直接部署在机器人内置的边缘计算设备上，而非卸载到边缘服务器或云端，以满足延迟、连接性和隐私约束。它真正改变的不是模型本身的精度，而是机器人对物理世界做出反应的速度上限——当控制闭环要求端到端推理延迟低于 20 ms 时，网络往返的代价已不可接受。

## 核心内容

### 是什么：准确定义

端侧 VLA 推理是一种部署范式，其核心约束在于"推理发生在机器人本体上"。与云端推理将输入数据上传、在远端算力上执行、再回传动作指令不同，端侧推理要求视觉编码器、语言编码器与动作解码器全部运行在机器人携带的计算硬件上——典型如 NVIDIA Jetson 系列嵌入式平台。

从系统架构看，端侧 VLA 推理处于感知与决策的交界处：它接收来自 egocentric 相机流的视觉观测、机器人状态（关节角度、IMU 数据）与自然语言指令，在机载计算单元上完成多模态融合与动作生成，输出直接馈入控制律计算模块。这一位置决定了它必须与实时操作系统（RTOS）、中间件序列化层和执行器驱动紧密耦合。

### 为什么存在：痛点与历史定位

在 VLA 模型出现之前，机器人控制的主流范式是"感知-规划-控制"三级流水线，每一级由独立模块承担，推理延迟的预算相对宽松。VLA 模型将视觉、语言与动作映射压缩进单一网络，虽然简化了策略设计，却引入了新的部署难题：参数量动辄数十亿的 Transformer 模型，其单次前向推理在通用硬件上需要数百毫秒，远超控制闭环的时序要求。

云端推理的诱惑在于算力无限，但物理定律不可回避：数据从机器人到云端再返回，即便在理想网络条件下也要消耗数十毫秒的传输时间，且伴随抖动（jitter）。对于需要 0.5–2 ms 硬实时周期的关节电流环和平衡控制，这种不确定性是致命的。端侧推理因此不是性能妥协，而是实时性约束下的必然选择——它真正改变的不是"模型能做什么"，而是"模型能在什么时间尺度内做出反应"。

### 原理拆解

**① 延迟预算的分解：端侧推理的物理基础**

端侧 VLA 推理的可行性取决于一个关键等式：端到端延迟必须小于控制闭环的周期要求。以 50 Hz 视觉闭环为例，其延迟预算可分解为：

$$
L_{\text{total}} = L_{\text{sensor}} + L_{\text{preprocess}} + L_{\text{inference}} + L_{\text{control}} + L_{\text{actuator}}
$$

其中 $L_{\text{inference}}$ 是 VLA 模型的前向推理时间。语料指出，50 Hz 视觉闭环要求端到端推理延迟低于 20 ms。若将推理放在云端，$L_{\text{inference}}$ 本身可能降至 5 ms，但网络往返 $L_{\text{network}}$ 会额外增加 20–100 ms（取决于链路质量），且抖动不可控。端侧推理以牺牲峰值算力为代价，换取延迟的可预测性——这是硬实时控制的前提。

**② 部署链路：PyTorch → ONNX → TensorRT**

语料给出了当前 NVIDIA Jetson 平台最主流的部署路径：PyTorch → ONNX → TensorRT。训练阶段使用的 PyTorch 模型包含动态图结构和大量算子，不适合直接在嵌入式 GPU 上运行。转换为 ONNX 中间表示后，TensorRT 会针对目标 GPU 架构进行层融合、精度校准与内核自动调优，生成高度优化的推理引擎。

量化是这条链路的关键环节：从 FP32 到 FP16 再到 INT8，每一步都在精度与速度之间权衡。语料强调，FP16 通常是延迟-功耗-精度的最佳平衡点，而 INT8 前必须做全量验证——感知误差可能直接转化为碰撞或跌倒。

**③ 内存与调度：端侧推理的系统约束**

端侧硬件资源有限，GPU 全局内存以事务（transaction）为单位访问，当同一 warp 的 32 个线程访问连续地址时，硬件可把多次访问合并为少量事务，显著提高有效带宽。占用率（occupancy）指每个 SM 上活跃 warp 数与最大 warp 数之比；高占用率有助于隐藏内存延迟，但过高可能导致寄存器/共享内存资源竞争。

在操作系统层面，通用 Linux 并非为硬实时设计。PREEMPT_RT 补丁使内核中大部分代码可抢占，中断也可线程化，提供数十微秒的调度延迟，是机器人主控的常见选择。端侧 VLA 推理任务通常运行在非实时的 Linux 用户态，但其输出必须通过实时内核调度器传递给控制任务——两个世界之间的交接延迟，同样是延迟预算的一部分。

### 关键参数与规格

| 参数 | 典型值/要求 | 说明 |
|------|------------|------|
| 端到端推理延迟 | < 20 ms | 50 Hz 视觉闭环的硬性要求（语料数据） |
| 控制闭环周期 | 0.5–2 ms | 关节电流环、力控和平衡控制（语料数据） |
| 调度延迟 | 数十微秒 | PREEMPT_RT 补丁后的 Linux 内核水平（语料数据） |
| 量化精度 | FP16 为最佳平衡点 | 延迟-功耗-精度的折中（语料数据，工程判断） |
| 功耗 | 需自行确认 | 直接影响散热与续航，是约束而非副作用 |
| 吞吐量 | 需自行确认 | 单位时间完成的推理次数，常用 fps 表示 |

### 横向对比

端侧推理、边缘服务器推理与云端推理构成三种部署选项，其差异集中在延迟、带宽与隐私三个维度：

| 维度 | 端侧推理 | 边缘服务器推理 | 云端推理 |
|------|---------|--------------|---------|
| 推理延迟 | 最低，无网络开销 | 低，但受局域网质量影响 | 高，受公网链路影响 |
| 延迟抖动 | 最小，可预测 | 中等 | 最大，不可控 |
| 算力上限 | 受功耗与散热约束 | 较高 | 几乎无限 |
| 隐私性 | 最高，数据不出本体 | 中等，数据出本体但不出本地 | 最低，数据上传至远端 |
| 适用场景 | 硬实时控制、隐私敏感任务 | 大规模地图构建、多机协同 | 训练、离线评估、非实时任务 |

语料指出，VLA-Perf 分析框架从部署视角考察了"推理应在何处执行——端侧、边缘服务器还是云端"，并强调硬件能力与网络性能共同决定端到端延迟。工程判断：对于人形机器人的平衡控制与灵巧操作，端侧推理是唯一满足硬实时约束的选项；而云端推理更适合模型迭代验证与跨场景泛化评估。

### 谁在用：应用案例

**NVIDIA Isaac GR00T N1.6** 是端侧 VLA 推理的典型实践。该模型是 multimodal vision-language-action 模型，整合 egocentric 相机流、机器人状态与自然语言指令为统一策略表示，通过世界模型（如 NVIDIA Cosmos Reason）将高层指令分解为基于场景理解的逐步动作计划。其 sim-to-real 流水线在 NVIDIA Isaac Lab 中完成全身强化学习训练，部署时依托 Jetson 平台实现端侧推理。

**VLA-Perf 基准研究**（arXiv:2602.18397）系统考察了 VLA 推理性能全景，从模型设计视角分析模型缩放、架构选择、长上下文视频输入、异步推理与双系统模型流水线对推理性能的影响，并从部署视角分析端侧、边缘与云端的执行位置选择。该研究提炼了 15 项关键结论，为端侧 VLA 推理的工程实践提供了量化依据。

### 局限与边界

端侧 VLA 推理的边界首先来自算力天花板。嵌入式 GPU/NPU 的算力受功耗与散热约束，无法运行最大规模的 VLA 模型——语料明确将功耗列为约束而非副作用。这意味着模型缩放（model scaling）在端侧存在硬上限，超出后必须依赖模型蒸馏或量化压缩。

其次，端侧推理不解决模型本身的推理效率问题。VLA 模型的 Transformer 堆叠结构导致深层计算冗余，早期退出机制（如 DeeAD 框架）与自适应剪枝（如 AutoPrune）是互补的优化手段，但它们的有效性取决于任务复杂度分布——对于复杂度恒定的任务，自适应策略的收益有限。

最后，端侧推理与实时控制之间的接口仍是工程难点。VLA 推理运行在 Linux 用户态，而关节控制需要硬实时周期，两个世界之间的数据交接延迟与调度抖动，构成了端到端延迟中不可忽略的组成部分。

### 常见误区

1. **"端侧推理就是模型压缩"**——错。量化与蒸馏是端侧部署的使能技术，但端侧推理的本质是部署位置的选择。一个未压缩的小模型在端侧运行，同样是端侧推理；一个 INT8 量化的大模型若部署在云端，则属于云端推理。

2. **"端侧推理延迟一定低于云端"**——不必然。对于单次推理，云端高端 GPU 的算力可能使 $L_{\text{inference}}$ 远低于端侧；端侧的优势在于消除网络延迟 $L_{\text{network}}$ 与抖动，使总延迟可预测。若任务对延迟不敏感（如离线数据分析），云端推理反而更优。

3. **"FP16 量化没有精度损失"**——需谨慎。FP16 相比 FP32 的精度损失通常可接受，但 INT8 量化可能引入显著误差。语料强调 INT8 前必须做全量验证，因为感知误差可能直接转化为碰撞或跌倒。

4. **"端侧推理不需要考虑操作系统"**——错。通用 Linux 的调度延迟在数十到数百微秒之间抖动，无法满足最严苛回路的确定性需求。PREEMPT_RT 或双核实时架构是端侧推理进入控制闭环的前提。

### 相关知识

- `ent_paper_wang_autoprune_each_complexity_dese_2025` — 复杂度自适应剪枝方法，通过互信息量化任务复杂度并动态调整 token 保留策略，可降低端侧 VLA 推理的计算负担。
- `ent_paper_hu_deead_dynamic_early_exit_of_vi_2025` — 早期退出机制，在预测轨迹与轻量级规划先验对齐时提前终止推理，减少 Transformer 层计算。
- `ent_component_manufacturer_nvidia` — NVIDIA 提供 Jetson 边缘计算平台与 GR00T 人形基础模型，是端侧 VLA 推理的核心硬件与软件使能者。
- `ent_paper_wu_vla_an_an_efficient_and_onboar_2025` — 机载 VLA 部署案例，通过深度优化部署管线在资源受限的无人机上实现推理吞吐量 8.3 倍提升。
- `ent_method_gr00t_n1` — NVIDIA Isaac GR00T N1.6 是端侧 VLA 推理的典型模型，整合视觉、语言与动作的统一策略表示。
- `ent_software_ros_2_middleware_2024` — ROS 2 中间件提供消息序列化与网络拓扑设计，其序列化开销直接影响端侧推理的端到端延迟。
- `ent_paper_openvla_2024` — OpenVLA 是开源 VLA 模型的代表，其部署实践为端侧推理提供了基准参考。
- `ent_paper_ma_running_vlas_at_real_time_spee_2025` — 探讨 VLA 模型实时运行速度的研究，与端侧推理的延迟约束直接相关。

## 参考

- [How Fast Can I Run My VLA? Demystifying VLA Inference Performance with VLA-Perf](https://arxiv.org/abs/2602.18397)
- [Building Generalist Humanoid Capabilities with NVIDIA Isaac GR00T N1.6 Using a Sim-to-Real Workflow](https://developer.nvidia.com/blog/building-generalist-humanoid-capabilities-with-nvidia-isaac-gr00t-n1-6-using-a-sim-to-real-workflow)
- [Humanoid Robot Development Workflow](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-06.md)

## Overview

On-device VLA inference refers to deploying vision-language-action models directly on edge computing devices embedded in robots, rather than offloading to edge servers or the cloud, to satisfy latency, connectivity, and privacy constraints. What it truly changes is not the model's accuracy itself, but the upper bound on how quickly a robot can react to the physical world—when the control loop requires end-to-end inference latency below 20 ms, the cost of network round trips becomes unacceptable.

## Content

### What It Is: A Precise Definition

On-device VLA inference is a deployment paradigm whose core constraint is that "inference occurs on the robot itself." Unlike cloud inference, which uploads input data, executes on remote compute, and sends back action commands, on-device inference requires the vision encoder, language encoder, and action decoder to all run on the computing hardware carried by the robot—typically embedded platforms such as the NVIDIA Jetson series.

From a system architecture perspective, on-device VLA inference sits at the boundary between perception and decision-making: it receives visual observations from egocentric camera streams, robot state (joint angles, IMU data), and natural language instructions, performs multimodal fusion and action generation on the onboard compute unit, and outputs directly to the control law computation module. This position dictates that it must be tightly coupled with real-time operating systems (RTOS), middleware serialization layers, and actuator drivers.

### Why It Exists: Pain Points and Historical Positioning

Before VLA models emerged, the dominant paradigm for robot control was a three-stage "perception-planning-control" pipeline, where each stage was handled by independent modules and the inference latency budget was relatively relaxed. VLA models compress vision, language, and action mapping into a single network, simplifying policy design but introducing new deployment challenges: Transformer models with billions of parameters require hundreds of milliseconds for a single forward pass on general-purpose hardware, far exceeding the timing requirements of the control loop.

The allure of cloud inference lies in unlimited compute, but the laws of physics cannot be avoided: data traveling from the robot to the cloud and back consumes tens of milliseconds of transmission time even under ideal network conditions, accompanied by jitter. For joint current loops and balance control requiring hard real-time cycles of 0.5–2 ms, this uncertainty is fatal. On-device inference is therefore not a performance compromise but a necessity under real-time constraints—what it truly changes is not "what the model can do," but "the timescale within which the model can react."

### Principle Breakdown

**① Latency Budget Decomposition: The Physical Basis of On-Device Inference**

The feasibility of on-device VLA inference hinges on a key equation: end-to-end latency must be less than the control loop's cycle requirement. Taking a 50 Hz visual closed loop as an example, its latency budget can be decomposed as:

$$
L_{\text{total}} = L_{\text{sensor}} + L_{\text{preprocess}} + L_{\text{inference}} + L_{\text{control}} + L_{\text{actuator}}
$$

where $L_{\text{inference}}$ is the forward inference time of the VLA model. The source material notes that a 50 Hz visual closed loop requires end-to-end inference latency below 20 ms. If inference is placed in the cloud, $L_{\text{inference}}$ itself might drop to 5 ms, but network round-trip $L_{\text{network}}$ adds an extra 20–100 ms (depending on link quality), with uncontrollable jitter. On-device inference trades peak compute for latency predictability—a prerequisite for hard real-time control.

**② Deployment Pipeline: PyTorch → ONNX → TensorRT**

The source material outlines the most mainstream deployment path on current NVIDIA Jetson platforms: PyTorch → ONNX → TensorRT. PyTorch models used during training contain dynamic graph structures and numerous operators, making them unsuitable for direct execution on embedded GPUs. After conversion to the ONNX intermediate representation, TensorRT performs layer fusion, precision calibration, and automatic kernel tuning for the target GPU architecture, generating a highly optimized inference engine.

Quantization is a critical step in this pipeline: from FP32 to FP16 to INT8, each step trades off precision against speed. The source material emphasizes that FP16 is typically the best balance point for latency-power-precision, while INT8 requires full validation beforehand—perception errors can directly translate into collisions or falls.

**③ Memory and Scheduling: System Constraints of On-Device Inference**

On-device hardware resources are limited. GPU global memory is accessed in transactions; when 32 threads of the same warp access consecutive addresses, the hardware can merge multiple accesses into fewer transactions, significantly improving effective bandwidth. Occupancy refers to the ratio of active warps to the maximum number of warps per SM; high occupancy helps hide memory latency, but excessively high occupancy can lead to register/shared memory resource contention.

At the operating system level, general-purpose Linux is not designed for hard real-time. The PREEMPT_RT patch makes most kernel code preemptible and interrupts threadable, providing scheduling latency in the tens of microseconds, making it a common choice for robot main controllers. On-device VLA inference tasks typically run in non-real-time Linux user space, but their outputs must be passed to control tasks through the real-time kernel scheduler—the handoff latency between these two worlds is also part of the latency budget.

### Key Parameters and Specifications

| Parameter | Typical Value/Requirement | Description |
|-----------|--------------------------|-------------|
| End-to-end inference latency | < 20 ms | Hard requirement for 50 Hz visual closed loop (source data) |
| Control loop cycle | 0.5–2 ms | Joint current loop, force control, and balance control (source data) |
| Scheduling latency | Tens of microseconds | Linux kernel level after PREEMPT_RT patch (source data) |
| Quantization precision | FP16 as best balance point | Trade-off among latency, power, and precision (source data, engineering judgment) |
| Power consumption | To be confirmed | Directly affects thermal management and battery life; a constraint, not a side effect |
| Throughput | To be confirmed | Number of inferences completed per unit time, commonly expressed in fps |

### Horizontal Comparison

On-device inference, edge server inference, and cloud inference constitute three deployment options, differing primarily across latency, bandwidth, and privacy dimensions:

| Dimension | On-Device Inference | Edge Server Inference | Cloud Inference |
|-----------|--------------------|-----------------------|-----------------|
| Inference latency | Lowest, no network overhead | Low, but affected by LAN quality | High, affected by public network links |
| Latency jitter | Minimal, predictable | Moderate | Largest, uncontrollable |
| Compute ceiling | Constrained by power and thermal limits | Relatively high | Nearly unlimited |
| Privacy | Highest, data never leaves the device | Moderate, data leaves device but stays local | Lowest, data uploaded to remote servers |
| Suitable scenarios | Hard real-time control, privacy-sensitive tasks | Large-scale mapping, multi-robot coordination | Training, offline evaluation, non-real-time tasks |

The source material notes that the VLA-Perf analysis framework examines "where inference should be executed—on-device, edge server, or cloud" from a deployment perspective, emphasizing that hardware capability and network performance jointly determine end-to-end latency. Engineering judgment: for humanoid robot balance control and dexterous manipulation, on-device inference is the only option satisfying hard real-time constraints; cloud inference is better suited for model iteration validation and cross-scenario generalization evaluation.

### Who Uses It: Application Cases

**NVIDIA Isaac GR00T N1.6** is a typical practice of on-device VLA inference. This model is a multimodal vision-language-action model that integrates egocentric camera streams, robot state, and natural language instructions into a unified policy representation, using world models (such as NVIDIA Cosmos Reason) to decompose high-level instructions into step-by-step action plans based on scene understanding. Its sim-to-real pipeline completes full-body reinforcement learning training in NVIDIA Isaac Lab, and deployment relies on the Jetson platform for on-device inference.

**The VLA-Perf Benchmark Study** (arXiv:2602.18397) systematically examines the landscape of VLA inference performance, analyzing from a model design perspective the impact of model scaling, architecture choices, long-context video inputs, asynchronous inference, and dual-system model pipelines on inference performance, and from a deployment perspective analyzing execution location choices among on-device, edge, and cloud. The study distills 15 key findings, providing quantitative evidence for the engineering practice of on-device VLA inference.

### Limitations and Boundaries

The boundary of on-device VLA inference first comes from the compute ceiling. The compute power of embedded GPUs/NPUs is constrained by power and thermal limits, preventing the deployment of the largest VLA models—the source material explicitly lists power consumption as a constraint rather than a side effect. This means model scaling has a hard upper limit on-device, beyond which model distillation or quantization compression becomes necessary.

Second, on-device inference does not solve the model's own inference efficiency problem. The stacked Transformer architecture of VLA models leads to redundant computation in deep layers; early exit mechanisms (such as the DeeAD framework) and adaptive pruning (such as AutoPrune) are complementary optimization techniques, but their effectiveness depends on the distribution of task complexity—for tasks with constant complexity, the benefits of adaptive strategies are limited.

Finally, the interface between on-device inference and real-time control remains an engineering challenge. VLA inference runs in Linux user space, while joint control requires hard real-time cycles; the data handoff latency and scheduling jitter between these two worlds constitute a non-negligible component of end-to-end latency.

### Common Misconceptions

1. **"On-device inference is just model compression"**—Wrong. Quantization and distillation are enabling technologies for on-device deployment, but the essence of on-device inference is the choice of deployment location. An uncompressed small model running on-device is still on-device inference; an INT8-quantized large model deployed in the cloud belongs to cloud inference.

2. **"On-device inference latency is always lower than cloud"**—Not necessarily. For a single inference, high-end cloud GPUs may achieve $L_{\text{inference}}$ far lower than on-device; the advantage of on-device lies in eliminating network latency $L_{\text{network}}$ and jitter, making total latency predictable. If the task is latency-insensitive (e.g., offline data analysis), cloud inference may actually be superior.

3. **"FP16 quantization has no precision loss"**—Needs caution. The precision loss of FP16 compared to FP32 is usually acceptable, but INT8 quantization can introduce significant errors. The source material emphasizes that full validation is required before INT8, because perception errors can directly translate into collisions or falls.

4. **"On-device inference does not need to consider the operating system"**—Wrong. The scheduling latency of general-purpose Linux jitters between tens and hundreds of microseconds, failing to meet the determinism requirements of the most demanding loops. PREEMPT_RT or dual-core real-time architectures are prerequisites for on-device inference entering the control loop.

### Related Knowledge

- `ent_paper_wang_autoprune_each_complexity_dese_2025` — Complexity-adaptive pruning method that quantifies task complexity via mutual information and dynamically adjusts token retention strategies, reducing the computational burden of on-device VLA inference.
- `ent_paper_hu_deead_dynamic_early_exit_of_vi_2025` — Early exit mechanism that terminates inference early when predicted trajectories align with lightweight planning priors, reducing Transformer layer computation.
- `ent_component_manufacturer_nvidia` — NVIDIA provides the Jetson edge computing platform and GR00T humanoid foundation models, serving as the core hardware and software enabler for on-device VLA inference.
- `ent_paper_wu_vla_an_an_efficient_and_onboar_2025` — Onboard VLA deployment case achieving an 8.3× improvement in inference throughput on resource-constrained drones through deeply optimized deployment pipelines.
- `ent_method_gr00t_n1` — NVIDIA Isaac GR00T N1.6 is a typical model for on-device VLA inference, integrating vision, language, and action into a unified policy representation.
- `ent_software_ros_2_middleware_2024` — ROS 2 middleware provides message serialization and network topology design; its serialization overhead directly affects the end-to-end latency of on-device inference.
- `ent_paper_openvla_2024` — OpenVLA is a representative open-source VLA model whose deployment practices provide a baseline reference for on-device inference.
- `ent_paper_ma_running_vlas_at_real_time_spee_2025` — Research exploring real-time execution speeds of VLA models, directly related to the latency constraints of on-device inference.

## 개요

온디바이스 VLA 추론(On-Device VLA Inference)은 비전-언어-행동 모델을 로봇에 내장된 엣지 컴퓨팅 장치에 직접 배포하는 것을 의미하며, 지연 시간, 연결성, 프라이버시 제약을 충족하기 위해 엣지 서버나 클라우드로 오프로드하지 않는다. 이것이 실제로 바꾸는 것은 모델 자체의 정확도가 아니라 로봇이 물리적 세계에 반응하는 속도의 상한선이다——제어 폐루프가 엔드투엔드 추론 지연 시간 20ms 미만을 요구할 때, 네트워크 왕복의 비용은 이미 수용할 수 없는 수준이다.

## 핵심 내용

### 무엇인가: 정확한 정의

온디바이스 VLA 추론은 배포 패러다임으로, 핵심 제약은 "추론이 로봇 본체에서 발생한다"는 점이다. 입력 데이터를 업로드하고 원격 컴퓨팅 파워에서 실행한 후 동작 명령을 다시 전송하는 클라우드 추론과 달리, 온디바이스 추론은 비전 인코더, 언어 인코더, 행동 디코더가 모두 로봇이 휴대하는 컴퓨팅 하드웨어에서 실행되어야 한다——대표적으로 NVIDIA Jetson 시리즈 임베디드 플랫폼이 있다.

시스템 아키텍처 관점에서 온디바이스 VLA 추론은 인식과 결정의 경계에 위치한다: egocentric 카메라 스트림의 시각적 관측, 로봇 상태(관절 각도, IMU 데이터), 자연어 명령을 수신하여 온보드 컴퓨팅 유닛에서 다중 모달 융합과 행동 생성을 완료하고, 출력은 직접 제어 법칙 계산 모듈로 전달된다. 이러한 위치는 실시간 운영체제(RTOS), 미들웨어 직렬화 계층, 액추에이터 드라이버와 긴밀하게 결합되어야 함을 결정한다.

### 왜 존재하는가:痛点과 역사적 위치

VLA 모델이 등장하기 전, 로봇 제어의 주류 패러다임은 "인식-계획-제어" 3단계 파이프라인이었으며, 각 단계는 독립 모듈이 담당하여 추론 지연 시간 예산이 상대적으로 여유로웠다. VLA 모델은 비전, 언어, 행동 매핑을 단일 네트워크로 압축하여 정책 설계를 단순화했지만, 새로운 배포 과제를 도입했다: 수십억 개의 파라미터를 가진 Transformer 모델의 단일 순방향 추론은 범용 하드웨어에서 수백 밀리초가 소요되어 제어 폐루프의 타이밍 요구를 훨씬 초과한다.

클라우드 추론의 유혹은 무한한 컴퓨팅 파워에 있지만, 물리 법칙은 피할 수 없다: 데이터가 로봇에서 클라우드로 갔다가 다시 돌아오는 데는 이상적인 네트워크 조건에서도 수십 밀리초의 전송 시간이 소요되며, 지터(jitter)가 동반된다. 0.5–2ms 하드 실시간 주기를 요구하는 관절 전류 루프와 균형 제어에는 이러한 불확실성이 치명적이다. 따라서 온디바이스 추론은 성능 타협이 아니라 실시간성 제약 하의 필연적 선택이다——실제로 바꾸는 것은 "모델이 무엇을 할 수 있는가"가 아니라 "모델이 어떤 시간 척도 내에서 반응할 수 있는가"이다.

### 원리 분석

**① 지연 시간 예산의 분해: 온디바이스 추론의 물리적 기반**

온디바이스 VLA 추론의 실현 가능성은 핵심 등식에 달려 있다: 엔드투엔드 지연 시간은 제어 폐루프의 주기 요구보다 작아야 한다. 50Hz 시각 폐루프를 예로 들면, 지연 시간 예산은 다음과 같이 분해된다:

$$
L_{\text{total}} = L_{\text{sensor}} + L_{\text{preprocess}} + L_{\text{inference}} + L_{\text{control}} + L_{\text{actuator}}
$$

여기서 $L_{\text{inference}}$는 VLA 모델의 순방향 추론 시간이다. 자료에 따르면 50Hz 시각 폐루프는 엔드투엔드 추론 지연 시간이 20ms 미만이어야 한다. 추론을 클라우드에 두면 $L_{\text{inference}}$ 자체는 5ms로 줄일 수 있지만, 네트워크 왕복 $L_{\text{network}}$가 추가로 20–100ms(링크 품질에 따라 다름)를 더하고 지터도 제어할 수 없다. 온디바이스 추론은 최고 컴퓨팅 파워를 희생하는 대신 지연 시간의 예측 가능성을 얻는다——이것이 하드 실시간 제어의 전제 조건이다.

**② 배포 체인: PyTorch → ONNX → TensorRT**

자료는 현재 NVIDIA Jetson 플랫폼에서 가장 주류인 배포 경로를 제시한다: PyTorch → ONNX → TensorRT. 훈련 단계에서 사용되는 PyTorch 모델은 동적 그래프 구조와 많은 연산자를 포함하여 임베디드 GPU에서 직접 실행하기에 적합하지 않다. ONNX 중간 표현으로 변환한 후, TensorRT는 대상 GPU 아키텍처에 맞춰 레이어 융합, 정밀도 보정, 커널 자동 튜닝을 수행하여 고도로 최적화된 추론 엔진을 생성한다.

양자화는 이 체인의 핵심 단계이다: FP32에서 FP16, INT8로 갈수록 각 단계에서 정밀도와 속도 사이의 균형을 맞춘다. 자료는 FP16이 일반적으로 지연 시간-전력-정밀도의 최적 균형점이며, INT8은 반드시 전체 검증이 선행되어야 한다고 강조한다——인식 오류가 직접 충돌이나 낙상으로 이어질 수 있기 때문이다.

**③ 메모리와 스케줄링: 온디바이스 추론의 시스템 제약**

온디바이스 하드웨어 리소스는 제한적이며, GPU 전역 메모리는 트랜잭션(transaction) 단위로 접근된다. 동일한 warp의 32개 스레드가 연속 주소에 접근하면 하드웨어가 여러 접근을 소수의 트랜잭션으로 병합하여 유효 대역폭을 크게 향상시킬 수 있다. 점유율(occupancy)은 각 SM에서 활성 warp 수와 최대 warp 수의 비율을 의미한다; 높은 점유율은 메모리 지연 시간을 숨기는 데 도움이 되지만, 너무 높으면 레지스터/공유 메모리 리소스 경쟁이 발생할 수 있다.

운영체제 수준에서 범용 Linux는 하드 실시간용으로 설계되지 않았다. PREEMPT_RT 패치는 커널의 대부분 코드를 선점 가능하게 하고 인터럽트도 스레드화하여 수십 마이크로초의 스케줄링 지연 시간을 제공하며, 로봇 메인 컨트롤러의 일반적인 선택이다. 온디바이스 VLA 추론 작업은 일반적으로 비실시간 Linux 사용자 공간에서 실행되지만, 그 출력은 실시간 커널 스케줄러를 통해 제어 작업으로 전달되어야 한다——두 세계 사이의 핸드오버 지연 시간도 지연 시간 예산의 일부이다.

### 핵심 파라미터 및 사양

| 파라미터 | 일반적인 값/요구사항 | 설명 |
|------|------------|------|
| 엔드투엔드 추론 지연 시간 | < 20 ms | 50Hz 시각 폐루프의 하드 요구사항(자료 데이터) |
| 제어 폐루프 주기 | 0.5–2 ms | 관절 전류 루프, 힘 제어, 균형 제어(자료 데이터) |
| 스케줄링 지연 시간 | 수십 마이크로초 | PREEMPT_RT 패치 후 Linux 커널 수준(자료 데이터) |
| 양자화 정밀도 | FP16이 최적 균형점 | 지연 시간-전력-정밀도의 절충(자료 데이터, 엔지니어링 판단) |
| 전력 소비 | 직접 확인 필요 | 방열과 배터리 수명에 직접 영향, 제약이지 부작용이 아님 |
| 처리량 | 직접 확인 필요 | 단위 시간당 완료된 추론 횟수, 일반적으로 fps로 표시 |

### 수평 비교

온디바이스 추론, 엣지 서버 추론, 클라우드 추론은 세 가지 배포 옵션을 구성하며, 차이는 지연 시간, 대역폭, 프라이버시 세 가지 차원에 집중된다:

| 차원 | 온디바이스 추론 | 엣지 서버 추론 | 클라우드 추론 |
|------|---------|--------------|---------|
| 추론 지연 시간 | 가장 낮음, 네트워크 오버헤드 없음 | 낮음, 하지만 LAN 품질 영향 | 높음, 공용 네트워크 링크 영향 |
| 지연 시간 지터 | 가장 작음, 예측 가능 | 중간 | 가장 큼, 제어 불가 |
| 컴퓨팅 파워 상한 | 전력 및 방열 제약 | 비교적 높음 | 거의 무한 |
| 프라이버시 | 가장 높음, 데이터가 본체를 벗어나지 않음 | 중간, 데이터가 본체를 벗어나지만 로컬을 벗어나지 않음 | 가장 낮음, 데이터가 원격으로 업로드 |
| 적용 시나리오 | 하드 실시간 제어, 프라이버시 민감 작업 | 대규모 지도 구축, 다중 로봇 협업 | 훈련, 오프라인 평가, 비실시간 작업 |

자료에 따르면 VLA-Perf 분석 프레임워크는 배포 관점에서 "추론이 어디에서 실행되어야 하는가——온디바이스, 엣지 서버, 클라우드"를 검토하고 하드웨어 성능과 네트워크 성능이 함께 엔드투엔드 지연 시간을 결정한다고 강조한다. 엔지니어링 판단: 휴머노이드 로봇의 균형 제어와 정밀 조작의 경우 온디바이스 추론이 하드 실시간 제약을 충족하는 유일한 옵션이다; 클라우드 추론은 모델 반복 검증과 교차 시나리오 일반화 평가에 더 적합하다.

### 누가 사용하는가: 적용 사례

**NVIDIA Isaac GR00T N1.6**은 온디바이스 VLA 추론의 대표적 사례이다. 이 모델은 multimodal vision-language-action 모델로, egocentric 카메라 스트림, 로봇 상태, 자연어 명령을 통합 정책 표현으로 통합하고, 세계 모델(예: NVIDIA Cosmos Reason)을 통해 고수준 명령을 장면 이해 기반의 단계별 행동 계획으로 분해한다. sim-to-real 파이프라인은 NVIDIA Isaac Lab에서 전신 강화 학습 훈련을 완료하고, 배포 시 Jetson 플랫폼을 기반으로 온디바이스 추론을 구현한다.

**VLA-Perf 벤치마크 연구**(arXiv:2602.18397)는 VLA 추론 성능의 전체 그림을 체계적으로 조사하여, 모델 설계 관점에서 모델 스케일링, 아키텍처 선택, 장문맥 비디오 입력, 비동기 추론, 이중 시스템 모델 파이프라인이 추론 성능에 미치는 영향을 분석하고, 배포 관점에서 온디바이스, 엣지, 클라우드의 실행 위치 선택을 분석한다. 이 연구는 15가지 핵심 결론을 도출하여 온디바이스 VLA 추론의 엔지니어링 실무에 정량적 근거를 제공한다.

### 한계와 경계

온디바이스 VLA 추론의 경계는 먼저 컴퓨팅 파워 천장에서 온다. 임베디드 GPU/NPU의 컴퓨팅 파워는 전력 및 방열 제약을 받아 최대 규모의 VLA 모델을 실행할 수 없다——자료는 전력 소비를 부작용이 아닌 제약으로 명시한다. 이는 모델 스케일링(model scaling)이 온디바이스에서 하드 상한선을 가지며, 이를 초과하면 모델 증류나 양자화 압축에 의존해야 함을 의미한다.

둘째, 온디바이스 추론은 모델 자체의 추론 효율성 문제를 해결하지 않는다. VLA 모델의 Transformer 스택 구조는 깊은 계층의 계산 중복을 초래하며, 조기 종료 메커니즘(예: DeeAD 프레임워크)과 적응형 프루닝(예: AutoPrune)은 상호 보완적인 최적화 수단이지만, 그 효과는 작업 복잡도 분포에 달려 있다——복잡도가 일정한 작업에서는 적응형 전략의 이점이 제한적이다.

마지막으로, 온디바이스 추론과 실시간 제어 사이의 인터페이스는 여전히 엔지니어링 난제이다. VLA 추론은 Linux 사용자 공간에서 실행되는 반면, 관절 제어는 하드 실시간 주기를 요구하며, 두 세계 사이의 데이터 핸드오버 지연 시간과 스케줄링 지터는 엔드투엔드 지연 시간에서 무시할 수 없는 구성 요소를 형성한다.

### 일반적인 오해

1. **"온디바이스 추론은 모델 압축이다"**——틀림. 양자화와 증류는 온디바이스 배포의 가능 기술이지만, 온디바이스 추론의 본질은 배포 위치의 선택이다. 압축되지 않은 소형 모델이 온디바이스에서 실행되는 것도 온디바이스 추론이다; INT8 양자화된 대형 모델이 클라우드에 배포되면 클라우드 추론에 속한다.

2. **"온디바이스 추론 지연 시간이 반드시 클라우드보다 낮다"**——반드시 그렇지 않음. 단일 추론의 경우 클라우드의 고급 GPU 컴퓨팅 파워가 $L_{\text{inference}}$를 온디바이스보다 훨씬 낮출 수 있다; 온디바이스의 장점은 네트워크 지연 시간 $L_{\text{network}}$와 지터를 제거하여 총 지연 시간을 예측 가능하게 만드는 것이다. 지연 시간에 민감하지 않은 작업(예: 오프라인 데이터 분석)에서는 클라우드 추론이 오히려 더 우수하다.

3. **"FP16 양자화는 정밀도 손실이 없다"**——주의 필요. FP16은 FP32 대비 정밀도 손실이 일반적으로 수용 가능하지만, INT8 양자화는 상당한 오류를 도입할 수 있다. 자료는 INT8 전에 반드시 전체 검증이 필요하다고 강조하는데, 인식 오류가 직접 충돌이나 낙상으로 이어질 수 있기 때문이다.

4. **"온디바이스 추론은 운영체제를 고려할 필요가 없다"**——틀림. 범용 Linux의 스케줄링 지연 시간은 수십에서 수백 마이크로초 사이에서 지터가 발생하여 가장 엄격한 루프의 결정성 요구를 충족할 수 없다. PREEMPT_RT 또는 듀얼 코어 실시간 아키텍처는 온디바이스 추론이 제어 폐루프에 진입하기 위한 전제 조건이다.

### 관련 지식

- `ent_paper_wang_autoprune_each_complexity_dese_2025` — 복잡도 적응형 프루닝 방법으로, 상호 정보를 통해 작업 복잡도를 정량화하고 토큰 유지 전략을 동적으로 조정하여 온디바이스 VLA 추론의 계산 부담을 줄일 수 있다.
- `ent_paper_hu_deead_dynamic_early_exit_of_vi_2025` — 조기 종료 메커니즘으로, 예측 궤적이 경량 계획 사전 지식과 정렬될 때 추론을 조기 종료하여 Transformer 레이어 계산을 줄인다.
- `ent_component_manufacturer_nvidia` — NVIDIA는 Jetson 엣지 컴퓨팅 플랫폼과 GR00T 휴머노이드 기반 모델을 제공하며, 온디바이스 VLA 추론의 핵심 하드웨어 및 소프트웨어 가능 기술 제공자이다.
- `ent_paper_wu_vla_an_an_efficient_and_onboar_2025` — 온보드 VLA 배포 사례로, 배포 파이프라인을 심층 최적화하여 리소스가 제한된 드론에서 추론 처리량을 8.3배 향상시킨다.
- `ent_method_gr00t_n1` — NVIDIA Isaac GR00T N1.6은 온디바이스 VLA 추론의 대표 모델로, 비전, 언어, 행동을 통합한 통합 정책 표현이다.
- `ent_software_ros_2_middleware_2024` — ROS 2 미들웨어는 메시지 직렬화와 네트워크 토폴로지 설계를 제공하며, 직렬화 오버헤드는 온디바이스 추론의 엔드투엔드 지연 시간에 직접 영향을 미친다.
- `ent_paper_openvla_2024` — OpenVLA는 오픈소스 VLA 모델의 대표로, 배포 실무가 온디바이스 추론에 벤치마크 참조를 제공한다.
- `ent_paper_ma_running_vlas_at_real_time_spee_2025` — VLA 모델의 실시간 실행 속도를 탐구하는 연구로, 온디바이스 추론의 지연 시간 제약과 직접 관련된다.
