---
$id: ent_software_mujoco_physics_engine_2022
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: software_platform
names:
  en: MuJoCo Physics Engine
  zh: MuJoCo 物理引擎
  ko: MuJoCo 물리 엔진
summary:
  en: High-fidelity physics engine with rich contact dynamics, widely used for humanoid control research.
  zh: 具有丰富接触动力学的高保真物理引擎，广泛用于人形控制研究。
  ko: 풍부한 접촉 역학을 가진 고충실도 물리 엔진, 휨로봇 제어 연구에 널리 사용.
domains:
- 08_software_middleware
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
- intelligence
tags:
- contact
- mujoco
- physics
- simulator
- software_platform
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.
    [2026-08-06] body upgraded to textbook-grade pilot (.staging/textbook_grade): zh 概述/核心内容/参考 rewritten from card + graph
    neighbors + wiki chapters + first-hand web sources (UA-rate-limited fetches, number whitelist audit passed); en/ko sections
    to be regenerated by translate_entry_bodies.py.'
sources:
- id: src_001
  type: website
  title: MuJoCo Physics Engine
  url: https://mujoco.org/
  date: '2022'
  accessed_at: '2026-07-02'
---
## 概述

MuJoCo（Multi-Joint dynamics with Contact）是一个为"多关节系统 + 丰富接触"场景设计的高保真物理引擎：用广义坐标动力学与凸优化接触求解，在 CPU 上即可高精度、高吞吐地仿真腿足行走与灵巧操作，长期是机器人控制与强化学习研究的事实标准仿真器。它由 Emo Todorov 团队创立（Roboti LLC），2021 年 10 月被 Google DeepMind 收购并免费开放，2022 年 5 月开源，当前由 DeepMind 持续维护（来源：MuJoCo 官方文档 overview；GitHub README）。

## 核心内容

### 是什么：准确定义

MuJoCo 是一个 **C/C++ 物理引擎库**（C API + 官方 Python 绑定），输入用 MJCF（XML 格式）描述的机器人模型，输出高精度、可复现的动力学仿真。三个判别特征（来源：GitHub README；官方文档 overview）：

1. **为接触而生**：接触动力学被表述为凸优化问题，接触力平滑、物理一致性好；
2. **为控制而生**：广义坐标 + 预分配底层数据结构，运行时零内存分配，天然适合嵌入控制/训练回路；
3. **为研究而生**：模型、状态、导数全部可通过 API 访问，"正向动力学有唯一定义的逆"，便于数据分析与控制。

### 为什么存在：痛点与历史定位

2010 年前后的机器人仿真有一个尴尬的空档：游戏引擎（ODE、Bullet 等）为视觉效果优化，接触求解充斥近似与抖动，控制算法在其上"学会"的往往是仿真器的伪影而非物理规律；而动力学库（如各实验室自研代码）准确却没有统一的模型格式与可用工具链。Todorov、Erez、Tassa 2012 年在 IROS 发表 *MuJoCo: A physics engine for model-based control*，提出以**基于模型的控制**为目标的引擎：接触用凸优化严格求解、动力学在广义坐标下递推、模型用统一 XML 描述（来源：IROS 2012 论文；项目 Wiki 第 23 章）。

**它真正改变的不是仿真速度，而是"仿真结果可不可信"的默认预期**——腿足控制论文第一次在投稿时可以默认审稿人认可仿真器的接触力学。2021 年 DeepMind 收购并免费开放、2022 年开源后，它从"研究圈的昂贵工具"变成全社区的基础设施（来源：官方文档 overview）。

### 原理拆解

**① 广义坐标动力学与 ABA**

MuJoCo 直接在关节空间（广义坐标）递推多体动力学，避免拉格朗日乘子法的大规模稀疏系统；正向动力学采用铰接体算法（ABA），不显式构造质量矩阵即以 O(n) 复杂度求出关节加速度——这是它在小到中等规模系统上高吞吐的算法根基（来源：项目 Wiki 第 23 章 §23.2、§23.3.1）。

**② 凸优化接触求解**

接触的物理本质是互补条件（不穿透 + 库仑摩擦锥）。MuJoCo 的独特选择是把接触建模为**凸优化问题**：允许软接触（contact softness）与约束正则化，在保证物理合理性的同时获得数值稳定性与可微性潜力（来源：项目 Wiki 第 23 章 §23.2）。默认求解器为 Newton 法，另提供可处理椭圆摩擦锥的 Projected Gauss-Seidel（PGS）；约束求解器自动发现独立的"约束岛"（constraint islands）并让静止岛休眠，大幅降低大场景开销（来源：官方文档 overview）。

**③ MJCF 模型格式**

MJCF 是 MuJoCo 的原生 XML 格式：面向"接触 + 执行器 + 传感器"的铰接刚体系统，物理参数（摩擦、阻尼、执行器增益、软接触参数）的表达远比 URDF 完整，且每对几何可配置 condim 与软接触参数、与凸接触求解器语义对齐（来源：项目 Wiki 第 23 章 §23.4.2）。

**④ 两条 GPU 路线：MJX 与 MuJoCo Warp**

原版 MuJoCo 以 CPU 为主；大规模并行由两个后端承接——**MJX**（基于 JAX）与 **MuJoCo Warp**（基于 NVIDIA Warp），二者消费同一 MJCF 模型（来源：官方文档 overview）。这意味着"模型一次编写，CPU 调接触精度、GPU 跑万级并行训练"的工作流成为可能。

### 关键参数与规格

| 项目 | 内容 | 来源 |
|------|------|------|
| 当前版本 | 3.11.0（2026-07-27 发布） | 官方 changelog |
| 许可证 | Apache-2.0（开源；2021-10 免费、2022-05 开源） | GitHub README / 官方 overview |
| 接口 | C API；官方 Python 绑定；Unity 插件 | GitHub README |
| 模型格式 | MJCF（原生 XML）；可导入 URDF | Wiki §23.4.2 |
| 动力学 | 广义坐标；ABA O(n) 正向动力学 | Wiki §23.2/§23.3.1 |
| 接触 | 凸优化接触；软接触；Newton 默认求解器；PGS 支持椭圆摩擦锥 | Wiki §23.2 / 官方 overview |
| 并行 | CPU 多线程；MJX（JAX）与 MuJoCo Warp（NVIDIA Warp）GPU 后端 | 官方 overview |
| 渲染 | OpenGL 交互式查看器（simulate），渲染能力相对朴素 | GitHub README / Wiki §23.3.1 |

### 横向对比：主流仿真平台

| 平台 | 物理内核 | 并行能力 | 渲染保真 | 接触质量 | 典型定位 |
|------|---------|---------|---------|---------|---------|
| **MuJoCo** | 自研（凸接触） | CPU 为主，MJX/Warp 提供 GPU 路线 | 中 | **高** | 控制研究、RL 训练 |
| Isaac Sim / Lab | PhysX | GPU 大规模并行 | 高（RTX 光追） | 中高 | 合成数据、大规模 RL |
| Gazebo | ODE/Bullet/DART 等 | CPU 为主 | 中 | 中 | ROS 集成测试 |
| Drake | 自研（hydroelastic） | CPU | 中低 | 高（研究级） | 优化控制与分析 |
| Genesis | 自研统一求解器 | GPU 大规模并行 | 中高 | 中高（含软体） | 通用/生成式仿真 |
| ManiSkill3 | SAPIEN（PhysX 系） | GPU 物理+渲染并行 | 高 | 中 | 具身操作基准 |

（来源：项目 Wiki 第 23 章 §23.3.8。选型口诀：接触精度与控制验证优先选 MuJoCo/Drake；视觉合成数据与大规模 RL 优先 Isaac 系或 ManiSkill3；ROS 全栈集成选 Gazebo；软体/可变形交互关注 Genesis。多数团队采用多仿真器交叉验证。）

### 谁在用 / 应用案例

- **DeepMind Control Suite**（`ent_paper_deepmind_control_suite_2018`）：建立在 MuJoCo 上的经典连续控制基准，几乎定义了 RL 论文的实验范式；
- **HumanoidBench**（`ent_benchmark_humanoidbench`）：人形机器人全身任务基准，以 MuJoCo 为物理后端；
- **MuJoCo Playground**（`ent_paper_202501_mujoco_playground_2025`）：基于 MJX 的 GPU 并行人形/四足训练场，把 MuJoCo 模型直接接入大规模 RL；
- **人形控制研究**：全身控制与 MPC 的算法验证、中小规模（数十到数百并行环境）的策略训练、需要精确接触行为的操作仿真，是 Wiki 第 23 章总结的典型用途；
- **学术谱系**：Todorov 团队（华盛顿大学）创立，现由 Google DeepMind 维护（官方 overview）。

### 局限与边界

- **渲染朴素**：原生渲染以调试可视化为目标，照片级相机数据需 Isaac 系或外部渲染器（来源：Wiki §23.3.1）；
- **GPU 并行是"另一条产品线"**：MJX/Warp 与 CPU 版在接触细节上并非逐位等价，跨后端结论需复核（**工程判断**）；
- **软体/流体非主场**：可变形体、流体等统一建模是 Genesis 等新兴引擎的目标场景（Wiki §23.3.6）；
- **MJCF 学习成本**：语义与 URDF 不完全对应，模型迁移需重新标定接触与执行器参数；
- **接触模型的"软"需要标定**：软接触参数改善数值稳定性，但与真实接触刚度的对应需要系统辨识（来源：Wiki §23.2 软接触定义；工程判断）。

### 常见误区

1. **"MuJoCo 是游戏引擎式模拟器"**——它的设计目标是基于模型的控制研究，接触力学严格性高于视觉表现。
2. **"MJX 就是 MuJoCo 的 GPU 加速开关"**——MJX/Warp 是独立后端，模型一致但数值行为有差异，不能直接假设结果可互换。
3. **"软接触 = 不真实"**——软接触是正则化手段，物理一致性由凸优化框架保证；反而是刚性 LCP 的高频抖动更容易产生伪影。
4. **"MJCF 和 URDF 是一回事"**——MJCF 承载执行器、传感器、软接触等仿真语义，URDF 只是结构描述；把 URDF 当 MJCF 用是 sim-to-real 失败的常见起点。
5. **"开源 = 停止演进"**——恰恰相反，3.x 系列仍在高频更新（最新 3.11.0，2026-07-27），MJX/Warp 双后端是当前演进主线。

### 相关知识

- `ent_technology_mjcf_simulation_format_2024` — MuJoCo 的原生模型格式
- `ent_benchmark_humanoidbench` / `ent_benchmark_maniskill` — 建立在其上/相邻的基准
- `ent_paper_deepmind_control_suite_2018` — 定义 RL 实验范式的套件
- `ent_software_nvidia_isaac_sim_2024` / `ent_software_nvidia_isaac_lab_2024` — GPU 大规模路线对照
- `ent_software_genesis_generative_physics_eng_2024` — 可微统一物理的新兴对照
- `ent_method_whole_body_control` / `ent_method_model_predictive_control` — 其典型用户方法
- `ent_concept_world_model` — 学习式"神经仿真器"与物理引擎的分工

## 参考

- [MuJoCo 官方文档（readthedocs）](https://mujoco.readthedocs.io/en/stable/overview.html)
- [google-deepmind/mujoco GitHub 仓库](https://github.com/google-deepmind/mujoco)
- [MuJoCo 更新日志（版本 3.11.0，2026-07-27）](https://mujoco.readthedocs.io/en/stable/changelog.html)
- [Todorov, Erez & Tassa, MuJoCo: A Physics Engine for Model-Based Control, IEEE/RSJ IROS 2012](https://doi.org/10.1109/IROS.2012.6386109)
- [MuJoCo 官网](https://mujoco.org/)
- [项目 Wiki 第 23 章 仿真与物理引擎](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-23.md)

## Overview

MuJoCo (Multi-Joint dynamics with Contact) is a high-fidelity physics engine designed for scenarios involving "multi-joint systems + rich contact": it uses generalized coordinate dynamics with convex optimization contact solving, enabling high-precision, high-throughput simulation of legged locomotion and dexterous manipulation on CPU alone. It has long been the de facto standard simulator for robot control and reinforcement learning research. It was founded by Emo Todorov's team (Roboti LLC), acquired by Google DeepMind in October 2021 and made freely available, open-sourced in May 2022, and is currently maintained by DeepMind (source: MuJoCo official documentation overview; GitHub README).

## Content

### What it is: precise definition

MuJoCo is a **C/C++ physics engine library** (C API + official Python bindings) that takes robot models described in MJCF (XML format) as input and outputs high-precision, reproducible dynamic simulations. Three distinguishing features (source: GitHub README; official documentation overview):

1. **Built for contact**: Contact dynamics are formulated as a convex optimization problem, yielding smooth contact forces with good physical consistency;
2. **Built for control**: Generalized coordinates plus pre-allocated low-level data structures ensure zero memory allocation at runtime, making it naturally suited for embedding in control/training loops;
3. **Built for research**: Models, states, and derivatives are all accessible via the API, and "forward dynamics has a uniquely defined inverse," facilitating data analysis and control.

### Why it exists: pain points and historical positioning

Robot simulation around 2010 had an awkward gap: game engines (ODE, Bullet, etc.) were optimized for visual effects, with contact solving full of approximations and jitter, so control algorithms trained on them often learned artifacts of the simulator rather than physical laws; meanwhile, dynamics libraries (such as various labs' in-house code) were accurate but lacked a unified model format and usable toolchain. In 2012, Todorov, Erez, and Tassa published *MuJoCo: A physics engine for model-based control* at IROS, proposing an engine aimed at **model-based control**: contact solved rigorously via convex optimization, dynamics recursively computed in generalized coordinates, and models described in a unified XML format (source: IROS 2012 paper; project Wiki Chapter 23).

**What it truly changed was not simulation speed, but the default expectation of whether "simulation results are trustworthy"** — for the first time, legged control papers could assume reviewers would accept the simulator's contact mechanics upon submission. After DeepMind acquired and freely released it in 2021, and open-sourced it in 2022, it transformed from an "expensive tool for the research circle" into community-wide infrastructure (source: official documentation overview).

### Principle breakdown

**① Generalized coordinate dynamics and ABA**

MuJoCo directly computes multibody dynamics in joint space (generalized coordinates), avoiding the large sparse systems of the Lagrangian multiplier method; forward dynamics uses the Articulated Body Algorithm (ABA), which computes joint accelerations with O(n) complexity without explicitly constructing the mass matrix — this is the algorithmic foundation of its high throughput on small to medium-scale systems (source: project Wiki Chapter 23 §23.2, §23.3.1).

**② Convex optimization contact solving**

The physical essence of contact is a complementarity condition (no penetration + Coulomb friction cone). MuJoCo's distinctive choice is to model contact as a **convex optimization problem**: allowing soft contacts (contact softness) and constraint regularization, achieving numerical stability and differentiability potential while maintaining physical plausibility (source: project Wiki Chapter 23 §23.2). The default solver is Newton's method, with Projected Gauss-Seidel (PGS) also available for handling elliptical friction cones; the constraint solver automatically discovers independent "constraint islands" and puts static islands to sleep, significantly reducing overhead in large scenes (source: official documentation overview).

**③ MJCF model format**

MJCF is MuJoCo's native XML format: designed for articulated rigid-body systems with "contact + actuator + sensor," its expression of physical parameters (friction, damping, actuator gains, soft contact parameters) is far more complete than URDF, and each geometry pair can be configured with condim and soft contact parameters, aligned with the semantics of the convex contact solver (source: project Wiki Chapter 23 §23.4.2).

**④ Two GPU paths: MJX and MuJoCo Warp**

The original MuJoCo is CPU-centric; large-scale parallelism is handled by two backends — **MJX** (based on JAX) and **MuJoCo Warp** (based on NVIDIA Warp), both consuming the same MJCF model (source: official documentation overview). This enables a workflow where "the model is written once, CPU tunes contact precision, GPU runs ten-thousand-scale parallel training."

### Key parameters and specifications

| Item | Content | Source |
|------|---------|--------|
| Current version | 3.11.0 (released 2026-07-27) | Official changelog |
| License | Apache-2.0 (open source; free since 2021-10, open-sourced 2022-05) | GitHub README / official overview |
| Interfaces | C API; official Python bindings; Unity plugin | GitHub README |
| Model format | MJCF (native XML); URDF import supported | Wiki §23.4.2 |
| Dynamics | Generalized coordinates; ABA O(n) forward dynamics | Wiki §23.2/§23.3.1 |
| Contact | Convex optimization contact; soft contact; Newton default solver; PGS supports elliptical friction cones | Wiki §23.2 / official overview |
| Parallelism | CPU multithreading; MJX (JAX) and MuJoCo Warp (NVIDIA Warp) GPU backends | Official overview |
| Rendering | OpenGL interactive viewer (simulate), relatively basic rendering capabilities | GitHub README / Wiki §23.3.1 |

### Horizontal comparison: mainstream simulation platforms

| Platform | Physics core | Parallel capability | Rendering fidelity | Contact quality | Typical positioning |
|----------|-------------|--------------------|--------------------|-----------------|---------------------|
| **MuJoCo** | Proprietary (convex contact) | CPU-centric, MJX/Warp provide GPU paths | Medium | **High** | Control research, RL training |
| Isaac Sim / Lab | PhysX | GPU large-scale parallelism | High (RTX ray tracing) | Medium-high | Synthetic data, large-scale RL |
| Gazebo | ODE/Bullet/DART, etc. | CPU-centric | Medium | Medium | ROS integration testing |
| Drake | Proprietary (hydroelastic) | CPU | Medium-low | High (research-grade) | Optimal control and analysis |
| Genesis | Proprietary unified solver | GPU large-scale parallelism | Medium-high | Medium-high (including soft bodies) | General/generative simulation |
| ManiSkill3 | SAPIEN (PhysX-based) | GPU physics + rendering parallelism | High | Medium | Embodied manipulation benchmarks |

(Source: project Wiki Chapter 23 §23.3.8. Selection guide: prioritize MuJoCo/Drake for contact precision and control validation; Isaac family or ManiSkill3 for visual synthetic data and large-scale RL; Gazebo for full ROS stack integration; watch Genesis for soft/deformable interactions. Most teams adopt multi-simulator cross-validation.)

### Who uses it / application cases

- **DeepMind Control Suite** (`ent_paper_deepmind_control_suite_2018`): a classic continuous control benchmark built on MuJoCo that largely defined the experimental paradigm for RL papers;
- **HumanoidBench** (`ent_benchmark_humanoidbench`): a whole-body humanoid task benchmark using MuJoCo as its physics backend;
- **MuJoCo Playground** (`ent_paper_202501_mujoco_playground_2025`): a GPU-parallel humanoid/quadruped training ground based on MJX, connecting MuJoCo models directly to large-scale RL;
- **Humanoid control research**: algorithm validation for whole-body control and MPC, policy training at small-to-medium scale (tens to hundreds of parallel environments), and manipulation simulation requiring precise contact behavior — the typical use cases summarized in Wiki Chapter 23;
- **Academic lineage**: founded by Todorov's team (University of Washington), now maintained by Google DeepMind (official overview).

### Limitations and boundaries

- **Basic rendering**: native rendering targets debugging visualization; photorealistic camera data requires Isaac family or external renderers (source: Wiki §23.3.1);
- **GPU parallelism is "a separate product line"**: MJX/Warp and the CPU version are not bit-for-bit equivalent in contact details; conclusions across backends require re-verification (**engineering judgment**);
- **Soft bodies/fluids are not its home turf**: unified modeling of deformable bodies, fluids, etc., is the target scenario for emerging engines like Genesis (Wiki §23.3.6);
- **MJCF learning cost**: its semantics do not fully correspond to URDF; model migration requires re-calibrating contact and actuator parameters;
- **The "softness" of the contact model requires calibration**: soft contact parameters improve numerical stability, but their correspondence to real contact stiffness requires system identification (source: Wiki §23.2 soft contact definition; engineering judgment).

### Common misconceptions

1. **"MuJoCo is a game-engine-style simulator"** — its design goal is model-based control research, with contact mechanics rigor prioritized over visual presentation.
2. **"MJX is just MuJoCo's GPU acceleration switch"** — MJX/Warp are independent backends; models are consistent but numerical behavior differs, so results cannot be assumed interchangeable.
3. **"Soft contact = unrealistic"** — soft contact is a regularization technique; physical consistency is guaranteed by the convex optimization framework; rather, the high-frequency jitter of rigid LCP is more prone to artifacts.
4. **"MJCF and URDF are the same thing"** — MJCF carries simulation semantics such as actuators, sensors, and soft contacts, while URDF is merely a structural description; treating URDF as MJCF is a common starting point for sim-to-real failure.
5. **"Open source = no more evolution"** — quite the opposite: the 3.x series is still being updated at high frequency (latest 3.11.0, 2026-07-27), with the dual MJX/Warp backends as the current main line of evolution.

### Related knowledge

- `ent_technology_mjcf_simulation_format_2024` — MuJoCo's native model format
- `ent_benchmark_humanoidbench` / `ent_benchmark_maniskill` — benchmarks built on it / adjacent to it
- `ent_paper_deepmind_control_suite_2018` — the suite that defined the RL experimental paradigm
- `ent_software_nvidia_isaac_sim_2024` / `ent_software_nvidia_isaac_lab_2024` — GPU large-scale route comparison
- `ent_software_genesis_generative_physics_eng_2024` — emerging differentiable unified physics comparison
- `ent_method_whole_body_control` / `ent_method_model_predictive_control` — its typical user methods
- `ent_concept_world_model` — the division of labor between learned "neural simulators" and physics engines

## 개요

MuJoCo(Multi-Joint dynamics with Contact)는 "다관절 시스템 + 풍부한 접촉" 시나리오를 위해 설계된 고충실도 물리 엔진입니다: 일반화 좌표 동역학과 볼록 최적화 접촉 해법을 사용하여 CPU에서도 고정밀·고처리량으로 보행과 정밀 조작을 시뮬레이션할 수 있으며, 오랫동안 로봇 제어 및 강화 학습 연구의 사실상 표준 시뮬레이터로 자리 잡았습니다. 이 엔진은 Emo Todorov 팀(Roboti LLC)이 창립했으며, 2021년 10월 Google DeepMind에 인수되어 무료로 공개되었고, 2022년 5월 오픈소스로 전환되어 현재 DeepMind가 지속적으로 유지보수하고 있습니다(출처: MuJoCo 공식 문서 overview; GitHub README).

## 핵심 내용

### 무엇인가: 정확한 정의

MuJoCo는 **C/C++ 물리 엔진 라이브러리**(C API + 공식 Python 바인딩)로, MJCF(XML 형식)로 기술된 로봇 모델을 입력으로 받아 고정밀·재현 가능한 동역학 시뮬레이션을 출력합니다. 세 가지 식별 특징(출처: GitHub README; 공식 문서 overview):

1. **접촉을 위해 설계됨**: 접촉 동역학이 볼록 최적화 문제로 표현되어 접촉력이 매끄럽고 물리적 일관성이 우수합니다;
2. **제어를 위해 설계됨**: 일반화 좌표 + 사전 할당된 저수준 데이터 구조로 런타임 시 메모리 할당이 전혀 없어 제어/학습 루프에 자연스럽게 내장됩니다;
3. **연구를 위해 설계됨**: 모델, 상태, 도함수가 모두 API로 접근 가능하며, "정방향 동역학에 유일하게 정의된 역"이 있어 데이터 분석과 제어에 용이합니다.

### 왜 존재하는가: 문제점과 역사적 위치

2010년 전후의 로봇 시뮬레이션에는 어색한 공백이 있었습니다: 게임 엔진(ODE, Bullet 등)은 시각 효과에 최적화되어 접촉 해법에 근사와 떨림이 가득했고, 제어 알고리즘은 그 위에서 "학습"한 것이 물리 법칙이 아닌 시뮬레이터의 아티팩트인 경우가 많았습니다; 반면 동역학 라이브러리(각 연구실의 자체 코드 등)는 정확했지만 통일된 모델 형식과 사용 가능한 도구 체인이 없었습니다. Todorov, Erez, Tassa는 2012년 IROS에서 *MuJoCo: A physics engine for model-based control*을 발표하며 **모델 기반 제어**를 목표로 하는 엔진을 제안했습니다: 접촉은 볼록 최적화로 엄밀하게 해결하고, 동역학은 일반화 좌표에서 재귀적으로 계산하며, 모델은 통일된 XML로 기술합니다(출처: IROS 2012 논문; 프로젝트 Wiki 23장).

**이것이 실제로 바꾼 것은 시뮬레이션 속도가 아니라 "시뮬레이션 결과를 신뢰할 수 있는지"에 대한 기본 기대였습니다**——보행 제어 논문은 처음으로 투고 시 심사위원이 시뮬레이터의 접촉 역학을 인정할 것이라는 기본 전제를 가질 수 있게 되었습니다. 2021년 DeepMind가 인수하여 무료로 공개하고, 2022년 오픈소스화된 이후, 이 엔진은 "연구계의 고가 도구"에서 전 커뮤니티의 기반 시설로 변모했습니다(출처: 공식 문서 overview).

### 원리 분석

**① 일반화 좌표 동역학과 ABA**

MuJoCo는 관절 공간(일반화 좌표)에서 직접 다물체 동역학을 재귀적으로 계산하여 라그랑주 승수법의 대규모 희소 시스템을 피합니다; 정방향 동역학은 관절체 알고리즘(ABA)을 사용하여 질량 행렬을 명시적으로 구성하지 않고 O(n) 복잡도로 관절 가속도를 계산합니다——이것이 중소 규모 시스템에서 높은 처리량을 가능하게 하는 알고리즘적 기반입니다(출처: 프로젝트 Wiki 23장 §23.2, §23.3.1).

**② 볼록 최적화 접촉 해법**

접촉의 물리적 본질은 상보성 조건(비관통 + 쿨롱 마찰 원뿔)입니다. MuJoCo의 독특한 선택은 접촉을 **볼록 최적화 문제**로 모델링하는 것입니다: 소프트 접촉(contact softness)과 제약 조건 정규화를 허용하여 물리적 타당성을 보장하면서도 수치적 안정성과 미분 가능성의 잠재력을 얻습니다(출처: 프로젝트 Wiki 23장 §23.2). 기본 솔버는 Newton 방법이며, 타원 마찰 원뿔을 처리할 수 있는 Projected Gauss-Seidel(PGS)도 제공합니다; 제약 조건 솔버는 독립적인 "제약 섬(constraint islands)"을 자동으로 발견하고 정지된 섬을 휴면 상태로 전환하여 대규모 장면의 오버헤드를 크게 줄입니다(출처: 공식 문서 overview).

**③ MJCF 모델 형식**

MJCF는 MuJoCo의 네이티브 XML 형식입니다: "접촉 + 액추에이터 + 센서"를 위한 관절 강체 시스템을 대상으로 하며, 물리 파라미터(마찰, 감쇠, 액추에이터 이득, 소프트 접촉 파라미터)의 표현이 URDF보다 훨씬 완전하고, 각 형상 쌍에 대해 condim과 소프트 접촉 파라미터를 구성할 수 있어 볼록 접촉 솔버의 의미론과 정렬됩니다(출처: 프로젝트 Wiki 23장 §23.4.2).

**④ 두 가지 GPU 경로: MJX와 MuJoCo Warp**

원본 MuJoCo는 CPU 중심입니다; 대규모 병렬 처리는 두 백엔드가 담당합니다——**MJX**(JAX 기반)와 **MuJoCo Warp**(NVIDIA Warp 기반)로, 둘 다 동일한 MJCF 모델을 사용합니다(출처: 공식 문서 overview). 이는 "모델을 한 번 작성하면 CPU에서 접촉 정밀도를 조정하고 GPU에서 만 단위 병렬 학습을 실행하는" 워크플로우를 가능하게 합니다.

### 주요 파라미터 및 사양

| 항목 | 내용 | 출처 |
|------|------|------|
| 현재 버전 | 3.11.0(2026-07-27 출시) | 공식 changelog |
| 라이선스 | Apache-2.0(오픈소스; 2021-10 무료, 2022-05 오픈소스) | GitHub README / 공식 overview |
| 인터페이스 | C API; 공식 Python 바인딩; Unity 플러그인 | GitHub README |
| 모델 형식 | MJCF(네이티브 XML); URDF 가져오기 가능 | Wiki §23.4.2 |
| 동역학 | 일반화 좌표; ABA O(n) 정방향 동역학 | Wiki §23.2/§23.3.1 |
| 접촉 | 볼록 최적화 접촉; 소프트 접촉; Newton 기본 솔버; PGS는 타원 마찰 원뿔 지원 | Wiki §23.2 / 공식 overview |
| 병렬 처리 | CPU 멀티스레드; MJX(JAX) 및 MuJoCo Warp(NVIDIA Warp) GPU 백엔드 | 공식 overview |
| 렌더링 | OpenGL 대화형 뷰어(simulate), 렌더링 기능은 상대적으로 단순 | GitHub README / Wiki §23.3.1 |

### 수평 비교: 주요 시뮬레이션 플랫폼

| 플랫폼 | 물리 커널 | 병렬 처리 능력 | 렌더링 충실도 | 접촉 품질 | 전형적 용도 |
|------|---------|---------|---------|---------|---------|
| **MuJoCo** | 자체 개발(볼록 접촉) | CPU 중심, MJX/Warp로 GPU 경로 제공 | 중 | **높음** | 제어 연구, RL 학습 |
| Isaac Sim / Lab | PhysX | GPU 대규모 병렬 처리 | 높음(RTX 레이 트레이싱) | 중상 | 합성 데이터, 대규모 RL |
| Gazebo | ODE/Bullet/DART 등 | CPU 중심 | 중 | 중 | ROS 통합 테스트 |
| Drake | 자체 개발(hydroelastic) | CPU | 중하 | 높음(연구급) | 최적화 제어 및 분석 |
| Genesis | 자체 개발 통합 솔버 | GPU 대규모 병렬 처리 | 중상 | 중상(소프트 바디 포함) | 범용/생성형 시뮬레이션 |
| ManiSkill3 | SAPIEN(PhysX 계열) | GPU 물리+렌더링 병렬 처리 | 높음 | 중 | 임베디드 조작 벤치마크 |

(출처: 프로젝트 Wiki 23장 §23.3.8. 선택 요령: 접촉 정밀도와 제어 검증이 우선이면 MuJoCo/Drake; 시각 합성 데이터와 대규모 RL은 Isaac 계열 또는 ManiSkill3; ROS 전체 스택 통합은 Gazebo; 소프트 바디/변형 가능 상호작용은 Genesis에 주목. 대부분의 팀은 다중 시뮬레이터 교차 검증을 사용합니다.)

### 누가 사용하는가 / 적용 사례

- **DeepMind Control Suite**(`ent_paper_deepmind_control_suite_2018`): MuJoCo 위에 구축된 고전적 연속 제어 벤치마크로, RL 논문의 실험 패러다임을 거의 정의했습니다;
- **HumanoidBench**(`ent_benchmark_humanoidbench`): 휴머노이드 로봇 전신 작업 벤치마크로, MuJoCo를 물리 백엔드로 사용합니다;
- **MuJoCo Playground**(`ent_paper_202501_mujoco_playground_2025`): MJX 기반 GPU 병렬 휴머노이드/사족 보행 훈련장으로, MuJoCo 모델을 대규모 RL에 직접 연결합니다;
- **휴머노이드 제어 연구**: 전신 제어 및 MPC의 알고리즘 검증, 중소 규모(수십~수백 병렬 환경) 정책 학습, 정밀한 접촉 동작이 필요한 조작 시뮬레이션은 Wiki 23장에서 요약한 전형적 용도입니다;
- **학술 계보**: Todorov 팀(워싱턴 대학교)이 창립했으며, 현재 Google DeepMind가 유지보수합니다(공식 overview).

### 한계와 경계

- **렌더링 단순함**: 네이티브 렌더링은 디버깅 시각화를 목표로 하며, 포토리얼리스틱 카메라 데이터는 Isaac 계열 또는 외부 렌더러가 필요합니다(출처: Wiki §23.3.1);
- **GPU 병렬 처리는 "별도의 제품 라인"**: MJX/Warp는 CPU 버전과 접촉 세부 사항에서 비트 단위로 동일하지 않으며, 백엔드 간 결론은 재검증이 필요합니다(**엔지니어링 판단**);
- **소프트 바디/유체는 주력 분야가 아님**: 변형 가능체, 유체 등의 통합 모델링은 Genesis와 같은 신흥 엔진의 목표 시나리오입니다(Wiki §23.3.6);
- **MJCF 학습 비용**: 의미론이 URDF와 완전히 대응하지 않아 모델 마이그레이션 시 접촉 및 액추에이터 파라미터를 다시 보정해야 합니다;
- **접촉 모델의 "소프트함"은 보정이 필요**: 소프트 접촉 파라미터는 수치적 안정성을 개선하지만, 실제 접촉 강성과의 대응은 시스템 식별이 필요합니다(출처: Wiki §23.2 소프트 접촉 정의; 엔지니어링 판단).

### 흔한 오해

1. **"MuJoCo는 게임 엔진식 시뮬레이터다"**——설계 목표는 모델 기반 제어 연구이며, 접촉 역학의 엄밀성이 시각적 표현보다 우선합니다.
2. **"MJX는 MuJoCo의 GPU 가속 스위치다"**——MJX/Warp는 독립 백엔드로, 모델은 동일하지만 수치적 동작에 차이가 있어 결과가 상호 교환 가능하다고 직접 가정할 수 없습니다.
3. **"소프트 접촉 = 비현실적"**——소프트 접촉은 정규화 수단이며, 물리적 일관성은 볼록 최적화 프레임워크가 보장합니다; 오히려 강성 LCP의 고주파 떨림이 아티팩트를 더 쉽게 생성합니다.
4. **"MJCF와 URDF는 같은 것이다"**——MJCF는 액추에이터, 센서, 소프트 접촉 등 시뮬레이션 의미론을 담고 있으며, URDF는 구조 설명에 불과합니다; URDF를 MJCF처럼 사용하는 것은 sim-to-real 실패의 흔한 출발점입니다.
5. **"오픈소스 = 발전 중단"**——정반대로, 3.x 시리즈는 여전히 높은 빈도로 업데이트되고 있으며(최신 3.11.0, 2026-07-27), MJX/Warp 이중 백엔드가 현재 발전의 주축입니다.

### 관련 지식

- `ent_technology_mjcf_simulation_format_2024` — MuJoCo의 네이티브 모델 형식
- `ent_benchmark_humanoidbench` / `ent_benchmark_maniskill` — 그 위에 구축된/인접한 벤치마크
- `ent_paper_deepmind_control_suite_2018` — RL 실험 패러다임을 정의한 스위트
- `ent_software_nvidia_isaac_sim_2024` / `ent_software_nvidia_isaac_lab_2024` — GPU 대규모 경로 대조
- `ent_software_genesis_generative_physics_eng_2024` — 미분 가능 통합 물리의 신흥 대조
- `ent_method_whole_body_control` / `ent_method_model_predictive_control` — 전형적 사용자 방법
- `ent_concept_world_model` — 학습 기반 "신경 시뮬레이터"와 물리 엔진의 역할 분담
