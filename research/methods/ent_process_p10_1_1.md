---
$id: ent_process_p10_1_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: IMU and joint encoder fusion
  zh: IMU 与关节编码器融合
  ko: IMU와 관절 엔코더 융합
summary:
  en: State estimator, measured trajectory error &lt;target, standing drift controllable
  zh: '- P10.1.1.1 输入梳理与目标量化 - 整理「IMU 与关节编码器融合」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 状态估计器、实测轨迹误差 < 目标、站立漂移可控
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

IMU 与关节编码器融合是足式机器人状态估计的核心工序节点，属于运动控制算法开发与验证（Motion Control）工作包中的状态估计与感知融合环节。该方法通过融合惯性测量单元（IMU）的高频姿态数据与关节编码器的运动学信息，实时估计机器人的质心位置、速度与姿态，为平衡控制提供基础状态输入。其本质是解决单一传感器各自局限性的互补性问题——IMU 存在积分漂移，编码器运动学推算缺乏全局姿态参考，而两者的融合能够实现优势互补。

## 核心内容

### 是什么：准确定义

IMU 与关节编码器融合是一种多传感器状态估计方法，其核心思想是将两类异构传感信息在滤波框架内进行最优合并。IMU 提供三轴加速度与三轴角速度，采样率高但存在积分漂移；关节编码器提供各关节角度，通过正向运动学可推算足端位置与机体运动增量，但缺乏绝对姿态参考。融合的目标是：在每一个控制周期内，输出对机器人质心位置、速度、姿态的最优估计，且估计误差有界、漂移可控。

该工序节点在开发流程中对应 P10.1.1，包含五个三级子任务：输入梳理与目标量化（P10.1.1.1）、方案/方法设计（P10.1.1.2）、实施/原型/样件制作（P10.1.1.3）、验证与问题闭环（P10.1.1.4）、文档输出与下游交付（P10.1.1.5）。它真正改变的不是传感器本身，而是将"两个各自不完美的传感器"变成"一个足够可靠的状态源"——这是平衡控制能否从仿真走向实机的分水岭。

### 为什么存在：痛点与历史定位

在足式机器人发展的早期，状态估计主要依赖单一 IMU 积分。这种方法在短时间内（数秒）尚可接受，但随时间推移，加速度计噪声的双重积分会导致位置估计呈三次方增长的发散趋势，姿态估计也会因陀螺仪零偏累积而逐渐偏离真实值。纯运动学推算（仅用关节编码器）则面临另一个问题：足端打滑时，运动学假设的"足端与地面无相对滑动"不再成立，导致位置估计出现不可预测的偏差。

融合方法的出现正是为了同时解决这两个痛点。它将 IMU 的高频动态响应与编码器的低频但无漂移的运动学约束相结合，在滤波框架内实现互补。从历史定位看，这一技术是足式机器人从"能走"迈向"走得稳"的关键技术节点——没有可靠的状态估计，任何高性能平衡控制器都无从谈起。它真正改变的不是算法复杂度，而是对"机器人当前到底处于什么状态"这一基本问题的回答方式：从"猜"变成了"算"。

### 原理拆解

**① 传感器模型与互补特性**

IMU 的测量模型可表示为：

$$
\tilde{\boldsymbol{\omega}} = \boldsymbol{\omega} + \mathbf{b}_g + \mathbf{n}_g, \quad \tilde{\mathbf{a}} = \mathbf{R}^{\top}(\mathbf{g} + \mathbf{a}) + \mathbf{b}_a + \mathbf{n}_a
$$

其中 $\mathbf{b}_g$、$\mathbf{b}_a$ 为陀螺仪与加速度计零偏，$\mathbf{n}_g$、$\mathbf{n}_a$ 为高斯白噪声。IMU 的优势在于高频（通常 100–1000 Hz）且包含绝对重力参考（加速度计可感知重力方向），但位置信息需二次积分获得，漂移不可避免。

关节编码器则通过正向运动学计算机体速度：

$$
\mathbf{v}_{\text{base}} = \mathbf{J}(\mathbf{q}) \dot{\mathbf{q}}
$$

其中 $\mathbf{J}$ 为雅可比矩阵，$\mathbf{q}$ 为关节角度。该方法无积分漂移，但依赖运动学模型的准确性，且无法提供绝对姿态。

**② 滤波融合框架**

最常用的融合框架是扩展卡尔曼滤波器（EKF）。其核心思想是：以 IMU 为状态预测源（高频传播），以编码器运动学推算为观测更新源（低频修正）。状态向量通常包含位置、速度、姿态四元数、陀螺仪零偏与加速度计零偏。预测步由 IMU 驱动，更新步由运动学约束驱动。

关键设计点在于协方差矩阵的设定：IMU 噪声协方差决定预测的不确定性，编码器观测噪声协方差决定修正的权重。协方差调参是工程中最重要的环节——设置过小会导致滤波发散，设置过大会使融合失去意义。工程判断：初始协方差通常依据传感器数据手册标称噪声密度设定，再通过实验数据迭代调整。

**③ 零速修正与接触约束**

当机器人处于站立或支撑相时，足端速度理论上为零。这一约束可作为虚拟观测注入滤波器，显著抑制漂移。零速修正（ZUPT）的实现方式是：检测到支撑相时，将足端速度为零作为观测更新。更进一步的接触辅助方法（如 Contact-Aided InEKF）将接触事件建模为状态约束，利用李群理论实现误差状态的不变性，提升估计的鲁棒性。

### 关键参数与规格

| 参数 | 典型值/要求 | 说明 |
|------|------------|------|
| IMU 采样率 | 100–1000 Hz | 需自行确认，取决于具体 IMU 型号 |
| 编码器采样率 | 100–1000 Hz | 需自行确认，与控制周期匹配 |
| 陀螺仪零偏稳定性 | < 10°/h | 需自行确认，影响姿态估计精度 |
| 加速度计零偏 | < 1 mg | 需自行确认，影响位置估计精度 |
| 滤波器更新频率 | 与编码器采样率一致 | 需自行确认 |
| 状态估计延迟 | < 1 个控制周期 | 需自行确认，延迟过大会影响控制稳定性 |
| 站立漂移 | 可控（具体值需自行确认） | 完成标准之一 |
| 实测轨迹误差 | < 目标值（具体值需自行确认） | 完成标准之一 |

### 横向对比

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 纯 IMU 积分 | 实现简单、高频 | 漂移严重，数秒后不可用 | 短时姿态估计 |
| 纯编码器运动学 | 无漂移、计算量小 | 足端打滑时失效、无绝对姿态 | 地面条件理想时 |
| EKF 融合（本工序） | 互补性强、工程成熟 | 线性化误差、协方差调参复杂 | 大多数足式机器人 |
| Contact-Aided InEKF | 利用接触约束、鲁棒性高 | 实现复杂、需精确接触检测 | 复杂地形行走 |
| VINS-Fusion（视觉惯性） | 可提供绝对位置参考 | 依赖视觉特征、计算量大 | 室内外导航 |

### 谁在用·应用案例

该融合方法广泛应用于各类足式机器人平台。波士顿动力 Atlas 在其平衡控制中采用了 IMU 与关节编码器融合的状态估计方案；Unitree、宇树等国内机器人公司的双足/四足产品同样基于类似框架。在学术研究中，MIT Cheetah、ETH ANYmal 等平台均公开了基于 EKF 或 InEKF 的状态估计实现。

在开发流程中，该工序节点的输出（状态估计器）直接服务于下游的平衡控制、步态规划与导航模块。具体而言，P10.1.1.4 验证阶段要求"实测轨迹误差 < 目标、站立漂移可控"，这需要通过实际行走实验验证估计器的精度与稳定性。

### 局限与边界

该融合方法存在若干固有局限。第一，IMU 与编码器融合本质上仍是相对定位方法，无法提供全局绝对位置——长时间运行后位置漂移不可避免，需借助视觉、激光或 GPS 等外部传感器进行绝对修正。第二，足端打滑是运动学观测的主要失效模式，在湿滑或松散地面上，编码器推算的速度信息不可信，滤波器可能被误导。第三，协方差调参高度依赖经验，不同平台、不同工况下需要重新整定，缺乏自适应性。第四，EKF 的线性化误差在强非线性运动（如快速转身、跳跃）中可能增大，导致估计发散。

### 常见误区

1. **"融合就是简单加权平均"**——错。真正的融合是在滤波框架内考虑传感器噪声统计特性与运动模型的最优估计，简单加权无法处理时变噪声与状态耦合。

2. **"IMU 越贵越好"**——不全面。在融合框架中，IMU 与编码器的噪声匹配比绝对精度更重要。高性能 IMU 若与低精度编码器搭配，反而可能因协方差设置不当导致滤波性能下降。

3. **"协方差调参一次到位"**——不现实。实际运行中，地面条件、负载变化、温度漂移都会影响传感器噪声特性，协方差需要在线自适应或定期重新整定。

4. **"零速修正只在静止时有效"**——不准确。支撑相的足端速度为零这一约束在行走过程中同样成立，关键在于准确检测支撑相与摆动相的切换时机。

### 相关知识

- `ent_paper_contact_aided_invariant_extend_2026` — 将接触约束与不变性扩展卡尔曼滤波结合，是本文所述融合方法在复杂地形下的重要演进方向
- `ent_paper_vins_fusion_an_optimization_ba_2026` — 基于优化的多传感器融合框架，可作为 IMU 与编码器融合的替代或补充方案
- `ent_paper_learning_contact_representatio_2026` — 用深度学习方法学习接触表示，可提升足端打滑场景下的融合鲁棒性
- `ent_paper_ultra_fusion_resilient_tightly_coupled_m_2026` — 因子级可靠性调度思想可借鉴用于自适应调整融合权重
- `ent_component_joint_encoder_2024` — 关节编码器作为本融合方法的核心输入传感器，其精度与延迟直接影响估计性能
- `ent_process_p10` — 本工序所属的上级工作包，涵盖运动控制算法开发与验证全流程

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 资源汇总](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Fusing IMU and joint encoders is a core process node in legged robot state estimation, belonging to the state estimation and perception fusion segment within the Motion Control algorithm development and validation work package. This method fuses the high-frequency attitude data from an Inertial Measurement Unit (IMU) with kinematic information from joint encoders to estimate the robot's center of mass position, velocity, and attitude in real time, providing fundamental state input for balance control. Its essence lies in addressing the complementary limitations of individual sensors—IMU suffers from integration drift, while encoder-based kinematic estimation lacks a global attitude reference—and their fusion achieves complementary advantages.

## Content

### What It Is: Precise Definition

IMU and joint encoder fusion is a multi-sensor state estimation method whose core idea is to optimally combine two types of heterogeneous sensing information within a filtering framework. The IMU provides three-axis acceleration and three-axis angular velocity with high sampling rates but suffers from integration drift; joint encoders provide individual joint angles, from which foot positions and body motion increments can be derived via forward kinematics, but lack an absolute attitude reference. The fusion objective is: within each control cycle, output an optimal estimate of the robot's center of mass position, velocity, and attitude, with bounded estimation errors and controllable drift.

This process node corresponds to P10.1.1 in the development workflow and includes five third-level subtasks: input analysis and target quantification (P10.1.1.1), solution/method design (P10.1.1.2), implementation/prototype/sample fabrication (P10.1.1.3), verification and issue closure (P10.1.1.4), and documentation output and downstream delivery (P10.1.1.5). What it truly changes is not the sensors themselves, but rather transforming "two individually imperfect sensors" into "one sufficiently reliable state source"—this is the watershed determining whether balance control can transition from simulation to physical hardware.

### Why It Exists: Pain Points and Historical Context

In the early development of legged robots, state estimation primarily relied on single-IMU integration. This approach was acceptable over short durations (seconds), but over time, double integration of accelerometer noise causes position estimates to diverge with cubic growth, and attitude estimates gradually deviate from true values due to gyroscope bias accumulation. Pure kinematic estimation (using only joint encoders) faces another issue: when feet slip, the kinematic assumption of "no relative sliding between foot and ground" no longer holds, leading to unpredictable position estimation errors.

The emergence of fusion methods directly addresses both pain points. It combines the IMU's high-frequency dynamic response with the encoder's low-frequency but drift-free kinematic constraints, achieving complementarity within a filtering framework. From a historical perspective, this technology is a key technical milestone for legged robots transitioning from "being able to walk" to "walking stably"—without reliable state estimation, any high-performance balance controller is impossible. What it truly changes is not algorithmic complexity, but the way of answering the fundamental question of "what state is the robot currently in": from "guessing" to "computing."

### Principle Breakdown

**① Sensor Models and Complementary Characteristics**

The IMU measurement model can be expressed as:

$$
\tilde{\boldsymbol{\omega}} = \boldsymbol{\omega} + \mathbf{b}_g + \mathbf{n}_g, \quad \tilde{\mathbf{a}} = \mathbf{R}^{\top}(\mathbf{g} + \mathbf{a}) + \mathbf{b}_a + \mathbf{n}_a
$$

where $\mathbf{b}_g$, $\mathbf{b}_a$ are the gyroscope and accelerometer biases, and $\mathbf{n}_g$, $\mathbf{n}_a$ are Gaussian white noise. The IMU's advantage lies in its high frequency (typically 100–1000 Hz) and its inclusion of an absolute gravity reference (the accelerometer can sense the direction of gravity), but position information requires double integration, making drift inevitable.

Joint encoders, on the other hand, compute body velocity through forward kinematics:

$$
\mathbf{v}_{\text{base}} = \mathbf{J}(\mathbf{q}) \dot{\mathbf{q}}
$$

where $\mathbf{J}$ is the Jacobian matrix and $\mathbf{q}$ is the joint angles. This method has no integration drift but relies on the accuracy of the kinematic model and cannot provide absolute attitude.

**② Filtering Fusion Framework**

The most commonly used fusion framework is the Extended Kalman Filter (EKF). Its core idea is: use the IMU as the state prediction source (high-frequency propagation) and encoder-based kinematic estimation as the measurement update source (low-frequency correction). The state vector typically includes position, velocity, attitude quaternion, gyroscope bias, and accelerometer bias. The prediction step is driven by the IMU, and the update step is driven by kinematic constraints.

The key design point lies in the setting of covariance matrices: the IMU noise covariance determines prediction uncertainty, and the encoder measurement noise covariance determines the weight of corrections. Covariance tuning is the most critical aspect in engineering—setting it too small causes filter divergence, while setting it too large renders the fusion meaningless. Engineering judgment: initial covariances are typically set based on nominal noise densities from sensor datasheets, then iteratively adjusted using experimental data.

**③ Zero Velocity Update and Contact Constraints**

When the robot is standing or in the support phase, the foot velocity is theoretically zero. This constraint can be injected into the filter as a virtual measurement, significantly suppressing drift. The Zero Velocity Update (ZUPT) implementation detects the support phase and uses zero foot velocity as a measurement update. More advanced contact-aided methods (such as Contact-Aided InEKF) model contact events as state constraints, leveraging Lie group theory to achieve invariance of error states and enhance estimation robustness.

### Key Parameters and Specifications

| Parameter | Typical Value/Requirement | Description |
|-----------|--------------------------|-------------|
| IMU sampling rate | 100–1000 Hz | To be confirmed, depends on specific IMU model |
| Encoder sampling rate | 100–1000 Hz | To be confirmed, must match control cycle |
| Gyroscope bias stability | < 10°/h | To be confirmed, affects attitude estimation accuracy |
| Accelerometer bias | < 1 mg | To be confirmed, affects position estimation accuracy |
| Filter update frequency | Consistent with encoder sampling rate | To be confirmed |
| State estimation latency | < 1 control cycle | To be confirmed; excessive latency affects control stability |
| Standing drift | Controllable (specific value to be confirmed) | One completion criterion |
| Measured trajectory error | < target value (specific value to be confirmed) | One completion criterion |

### Horizontal Comparison

| Method | Advantages | Disadvantages | Applicable Scenarios |
|--------|------------|---------------|----------------------|
| Pure IMU integration | Simple implementation, high frequency | Severe drift, unusable after seconds | Short-term attitude estimation |
| Pure encoder kinematics | Drift-free, low computational cost | Fails with foot slip, no absolute attitude | Ideal ground conditions |
| EKF fusion (this process) | Strong complementarity, mature engineering | Linearization errors, complex covariance tuning | Most legged robots |
| Contact-Aided InEKF | Leverages contact constraints, high robustness | Complex implementation, requires precise contact detection | Walking on complex terrain |
| VINS-Fusion (visual-inertial) | Provides absolute position reference | Relies on visual features, high computational cost | Indoor and outdoor navigation |

### Who Uses It: Application Cases

This fusion method is widely applied across various legged robot platforms. Boston Dynamics' Atlas employs IMU and joint encoder fusion for state estimation in its balance control; domestic robotics companies such as Unitree and Yushu use similar frameworks in their bipedal/quadrupedal products. In academic research, platforms such as MIT Cheetah and ETH ANYmal have publicly released state estimation implementations based on EKF or InEKF.

In the development workflow, the output of this process node (the state estimator) directly serves downstream balance control, gait planning, and navigation modules. Specifically, the P10.1.1.4 verification phase requires "measured trajectory error < target, standing drift controllable," which necessitates validating the estimator's accuracy and stability through actual walking experiments.

### Limitations and Boundaries

This fusion method has several inherent limitations. First, IMU and encoder fusion is fundamentally a relative positioning method that cannot provide global absolute position—position drift over extended operation is inevitable, requiring external sensors such as vision, LiDAR, or GPS for absolute correction. Second, foot slip is the primary failure mode for kinematic measurements; on slippery or loose terrain, encoder-derived velocity information becomes unreliable and may mislead the filter. Third, covariance tuning is highly experience-dependent, requiring re-tuning across different platforms and operating conditions, lacking adaptability. Fourth, EKF linearization errors may increase during strongly nonlinear motions (such as rapid turns or jumps), potentially causing estimation divergence.

### Common Misconceptions

1. **"Fusion is simply weighted averaging"**—Wrong. True fusion is optimal estimation within a filtering framework that accounts for sensor noise statistics and motion models; simple weighting cannot handle time-varying noise and state coupling.

2. **"More expensive IMUs are always better"**—Not entirely. In fusion frameworks, noise matching between the IMU and encoders matters more than absolute accuracy. A high-performance IMU paired with low-precision encoders may actually degrade filtering performance due to improper covariance settings.

3. **"Covariance tuning is a one-time effort"**—Unrealistic. In actual operation, ground conditions, load variations, and temperature drift all affect sensor noise characteristics, requiring online adaptation or periodic re-tuning of covariances.

4. **"Zero velocity update only works when stationary"**—Inaccurate. The constraint of zero foot velocity during the support phase also holds during walking; the key lies in accurately detecting the transition between support and swing phases.

### Related Knowledge

- `ent_paper_contact_aided_invariant_extend_2026` — Combines contact constraints with invariant extended Kalman filtering, representing an important evolution of the fusion method described here for complex terrain
- `ent_paper_vins_fusion_an_optimization_ba_2026` — An optimization-based multi-sensor fusion framework that can serve as an alternative or supplement to IMU and encoder fusion
- `ent_paper_learning_contact_representatio_2026` — Uses deep learning to learn contact representations, improving fusion robustness in foot-slip scenarios
- `ent_paper_ultra_fusion_resilient_tightly_coupled_m_2026` — Factor-level reliability scheduling concepts can be borrowed for adaptive adjustment of fusion weights
- `ent_component_joint_encoder_2024` — Joint encoders serve as the core input sensor for this fusion method; their precision and latency directly affect estimation performance
- `ent_process_p10` — The parent work package to which this process belongs, covering the full workflow of motion control algorithm development and validation

## 개요

IMU와 관절 엔코더 융합은 족형 로봇 상태 추정의 핵심 공정 노드로, 운동 제어 알고리즘 개발 및 검증(Motion Control) 작업 패키지 내 상태 추정 및 센서 융합 단계에 해당한다. 이 방법은 관성 측정 장치(IMU)의 고주파 자세 데이터와 관절 엔코더의 운동학적 정보를 융합하여 로봇의 질량 중심 위치, 속도 및 자세를 실시간으로 추정하고, 균형 제어에 기본 상태 입력을 제공한다. 그 본질은 단일 센서의 각각의 한계를 보완하는 상호 보완성 문제를 해결하는 것이다——IMU는 적분 드리프트가 있고, 엔코더 운동학 추정은 전역 자세 기준이 부족하며, 두 센서의 융합을 통해 장점을 상호 보완할 수 있다.

## 핵심 내용

### 무엇인가: 정확한 정의

IMU와 관절 엔코더 융합은 다중 센서 상태 추정 방법으로, 그 핵심 아이디어는 두 종류의 이기종 센서 정보를 필터링 프레임워크 내에서 최적으로 결합하는 것이다. IMU는 3축 가속도와 3축 각속도를 제공하며, 샘플링 주파수가 높지만 적분 드리프트가 존재한다. 관절 엔코더는 각 관절 각도를 제공하며, 정운동학을 통해 발끝 위치와 본체 운동 증분을 추정할 수 있지만 절대 자세 기준이 부족하다. 융합의 목표는: 각 제어 주기마다 로봇 질량 중심 위치, 속도, 자세에 대한 최적 추정을 출력하고, 추정 오차가 유계이며 드리프트가 제어 가능하도록 하는 것이다.

이 공정 노드는 개발 프로세스에서 P10.1.1에 해당하며, 5개의 3차 하위 작업을 포함한다: 입력 정리 및 목표 정량화(P10.1.1.1), 방안/방법 설계(P10.1.1.2), 구현/프로토타입/시제품 제작(P10.1.1.3), 검증 및 문제 폐루프(P10.1.1.4), 문서 출력 및 하류 인도(P10.1.1.5). 이것이 실제로 바꾸는 것은 센서 자체가 아니라 "각각 불완전한 두 센서"를 "충분히 신뢰할 수 있는 하나의 상태 소스"로 만드는 것이다——이것은 균형 제어가 시뮬레이션에서 실물 로봇으로 넘어갈 수 있는지의 분기점이다.

### 왜 존재하는가:痛点과 역사적 위치

족형 로봇 개발 초기에는 상태 추정이 주로 단일 IMU 적분에 의존했다. 이 방법은 짧은 시간(수 초) 동안은 허용 가능했지만, 시간이 지남에 따라 가속도계 노이즈의 이중 적분으로 인해 위치 추정이 3차 함수적으로 발산하는 경향이 있었고, 자세 추정도 자이로스코프 바이어스 누적으로 인해 점차 실제 값에서 벗어났다. 순수 운동학 추정(관절 엔코더만 사용)은 또 다른 문제에 직면했다: 발끝 슬립이 발생하면 운동학적 가정인 "발끝과 지면 사이에 상대 슬립 없음"이 더 이상 성립하지 않아 위치 추정에 예측 불가능한 편차가 발생했다.

융합 방법의 등장은 바로 이 두 가지痛点을 동시에 해결하기 위한 것이다. 이것은 IMU의 고주파 동적 응답과 엔코더의 저주파이지만 드리프트가 없는 운동학적 제약을 결합하여 필터링 프레임워크 내에서 상호 보완을 실현한다. 역사적 위치에서 볼 때, 이 기술은 족형 로봇이 "걸을 수 있는" 단계에서 "안정적으로 걷는" 단계로 나아가는 핵심 기술 노드이다——신뢰할 수 있는 상태 추정이 없다면 어떤 고성능 균형 제어기도 불가능하다. 이것이 실제로 바꾸는 것은 알고리즘 복잡성이 아니라 "로봇이 현재 정확히 어떤 상태에 있는가"라는 기본 질문에 대한 답변 방식이다: "추측"에서 "계산"으로 바뀐 것이다.

### 원리 분해

**① 센서 모델 및 상호 보완 특성**

IMU의 측정 모델은 다음과 같이 표현할 수 있다:

$$
\tilde{\boldsymbol{\omega}} = \boldsymbol{\omega} + \mathbf{b}_g + \mathbf{n}_g, \quad \tilde{\mathbf{a}} = \mathbf{R}^{\top}(\mathbf{g} + \mathbf{a}) + \mathbf{b}_a + \mathbf{n}_a
$$

여기서 $\mathbf{b}_g$, $\mathbf{b}_a$는 자이로스코프와 가속도계 바이어스, $\mathbf{n}_g$, $\mathbf{n}_a$는 가우시안 백색 노이즈이다. IMU의 장점은 고주파(보통 100–1000 Hz)이고 절대 중력 기준을 포함한다는 점(가속도계가 중력 방향을 감지할 수 있음)이지만, 위치 정보는 이중 적분으로 얻어야 하므로 드리프트가 불가피하다.

관절 엔코더는 정운동학을 통해 본체 속도를 계산한다:

$$
\mathbf{v}_{\text{base}} = \mathbf{J}(\mathbf{q}) \dot{\mathbf{q}}
$$

여기서 $\mathbf{J}$는 야코비 행렬, $\mathbf{q}$는 관절 각도이다. 이 방법은 적분 드리프트가 없지만 운동학 모델의 정확성에 의존하며 절대 자세를 제공할 수 없다.

**② 필터링 융합 프레임워크**

가장 일반적인 융합 프레임워크는 확장 칼만 필터(EKF)이다. 핵심 아이디어는: IMU를 상태 예측 소스(고주파 전파)로, 엔코더 운동학 추정을 관측 업데이트 소스(저주파 수정)로 사용하는 것이다. 상태 벡터는 일반적으로 위치, 속도, 자세 쿼터니언, 자이로스코프 바이어스 및 가속도계 바이어스를 포함한다. 예측 단계는 IMU에 의해 구동되고, 업데이트 단계는 운동학적 제약에 의해 구동된다.

핵심 설계 포인트는 공분산 행렬 설정이다: IMU 노이즈 공분산은 예측 불확실성을 결정하고, 엔코더 관측 노이즈 공분산은 수정 가중치를 결정한다. 공분산 튜닝은 공학에서 가장 중요한 단계이다——너무 작게 설정하면 필터가 발산하고, 너무 크게 설정하면 융합의 의미가 사라진다. 공학적 판단: 초기 공분산은 일반적으로 센서 데이터 시트의 공칭 노이즈 밀도에 따라 설정한 후, 실험 데이터를 통해 반복적으로 조정한다.

**③ 영속도 수정 및 접촉 제약**

로봇이 서 있거나 지지 구간에 있을 때 발끝 속도는 이론적으로 0이다. 이 제약은 가상 관측으로 필터에 주입되어 드리프트를 크게 억제할 수 있다. 영속도 수정(ZUPT)의 구현 방식은: 지지 구간을 감지하면 발끝 속도 0을 관측 업데이트로 사용하는 것이다. 더 나아가 접촉 보조 방법(예: Contact-Aided InEKF)은 접촉 이벤트를 상태 제약으로 모델링하고, 리 그룹 이론을 활용하여 오차 상태의 불변성을 구현하여 추정의 견고성을 향상시킨다.

### 핵심 파라미터 및 사양

| 파라미터 | 일반값/요구사항 | 설명 |
|------|------------|------|
| IMU 샘플링 주파수 | 100–1000 Hz | 직접 확인 필요, 구체적인 IMU 모델에 따라 다름 |
| 엔코더 샘플링 주파수 | 100–1000 Hz | 직접 확인 필요, 제어 주기와 일치해야 함 |
| 자이로스코프 바이어스 안정성 | < 10°/h | 직접 확인 필요, 자세 추정 정확도에 영향 |
| 가속도계 바이어스 | < 1 mg | 직접 확인 필요, 위치 추정 정확도에 영향 |
| 필터 업데이트 주파수 | 엔코더 샘플링 주파수와 일치 | 직접 확인 필요 |
| 상태 추정 지연 | < 1 제어 주기 | 직접 확인 필요, 지연이 너무 크면 제어 안정성에 영향 |
| 정지 드리프트 | 제어 가능(구체적인 값은 직접 확인 필요) | 완료 기준 중 하나 |
| 실측 궤적 오차 | < 목표값(구체적인 값은 직접 확인 필요) | 완료 기준 중 하나 |

### 횡적 비교

| 방법 | 장점 | 단점 | 적용 시나리오 |
|------|------|------|----------|
| 순수 IMU 적분 | 구현 간단, 고주파 | 드리프트 심각, 수 초 후 사용 불가 | 단기 자세 추정 |
| 순수 엔코더 운동학 | 드리프트 없음, 계산량 적음 | 발끝 슬립 시 무효, 절대 자세 없음 | 지면 조건이 이상적일 때 |
| EKF 융합(본 공정) | 상호 보완성 강함, 공학적으로 성숙 | 선형화 오차, 공분산 튜닝 복잡 | 대부분의 족형 로봇 |
| Contact-Aided InEKF | 접촉 제약 활용, 견고성 높음 | 구현 복잡, 정밀한 접촉 감지 필요 | 복잡한 지형 보행 |
| VINS-Fusion(시각 관성) | 절대 위치 기준 제공 가능 | 시각 특징 의존, 계산량 큼 | 실내외 내비게이션 |

### 누가 사용하는가·응용 사례

이 융합 방법은 다양한 족형 로봇 플랫폼에 널리 적용된다. 보스턴 다이내믹스 Atlas는 균형 제어에 IMU와 관절 엔코더 융합 상태 추정 방식을 채택했다. Unitree, 우슈 등 국내 로봇 회사의 이족/사족 제품도 유사한 프레임워크를 기반으로 한다. 학술 연구에서는 MIT Cheetah, ETH ANYmal 등 플랫폼이 EKF 또는 InEKF 기반 상태 추정 구현을 공개했다.

개발 프로세스에서 이 공정 노드의 출력(상태 추정기)은 하류의 균형 제어, 보행 계획 및 내비게이션 모듈에 직접 서비스를 제공한다. 구체적으로 P10.1.1.4 검증 단계는 "실측 궤적 오차 < 목표, 정지 드리프트 제어 가능"을 요구하며, 이는 실제 보행 실험을 통해 추정기의 정확도와 안정성을 검증해야 한다.

### 한계와 경계

이 융합 방법에는 몇 가지 고유한 한계가 있다. 첫째, IMU와 엔코더 융합은 본질적으로 상대 위치 측정 방법으로, 전역 절대 위치를 제공할 수 없다——장시간 운용 후 위치 드리프트는 불가피하며, 시각, 라이다 또는 GPS 등 외부 센서를 통한 절대 수정이 필요하다. 둘째, 발끝 슬립은 운동학 관측의 주요 고장 모드로, 젖거나 느슨한 지면에서는 엔코더로 추정한 속도 정보를 신뢰할 수 없어 필터가 오도될 수 있다. 셋째, 공분산 튜닝은 경험에 크게 의존하며, 플랫폼과 작업 조건이 다르면 재튜닝이 필요하고 적응성이 부족하다. 넷째, EKF의 선형화 오차는 강비선형 운동(예: 빠른 회전, 점프)에서 증가하여 추정 발산을 초래할 수 있다.

### 일반적인 오해

1. **"융합은 단순 가중 평균이다"**——틀렸다. 진정한 융합은 필터링 프레임워크 내에서 센서 노이즈 통계 특성과 운동 모델을 고려한 최적 추정이며, 단순 가중 평균은 시변 노이즈와 상태 결합을 처리할 수 없다.

2. **"IMU가 비쌀수록 좋다"**——불완전하다. 융합 프레임워크에서는 IMU와 엔코더의 노이즈 매칭이 절대 정확도보다 더 중요하다. 고성능 IMU를 저정밀 엔코더와 조합하면 오히려 공분산 설정 부적절로 필터 성능이 저하될 수 있다.

3. **"공분산 튜닝은 한 번에 끝난다"**——비현실적이다. 실제 운용에서는 지면 조건, 하중 변화, 온도 드리프트가 센서 노이즈 특성에 영향을 미치므로 공분산은 온라인 적응 또는 주기적 재튜닝이 필요하다.

4. **"영속도 수정은 정지 상태에서만 유효하다"**——정확하지 않다. 지지 구간의 발끝 속도 0이라는 제약은 보행 중에도 성립하며, 핵심은 지지 구간과 스윙 구간의 전환 시점을 정확히 감지하는 것이다.

### 관련 지식

- `ent_paper_contact_aided_invariant_extend_2026` — 접촉 제약과 불변성 확장 칼만 필터를 결합한 것으로, 본문에서 설명한 융합 방법의 복잡한 지형에서의 중요한 발전 방향
- `ent_paper_vins_fusion_an_optimization_ba_2026` — 최적화 기반 다중 센서 융합 프레임워크로, IMU와 엔코더 융합의 대안 또는 보완 방안으로 사용 가능
- `ent_paper_learning_contact_representatio_2026` — 딥러닝 방법으로 접촉 표현을 학습하여 발끝 슬립 시나리오에서 융합 견고성을 향상시킬 수 있음
- `ent_paper_ultra_fusion_resilient_tightly_coupled_m_2026` — 팩터 수준 신뢰성 스케줄링 아이디어를 차용하여 융합 가중치를 적응적으로 조정할 수 있음
- `ent_component_joint_encoder_2024` — 관절 엔코더는 본 융합 방법의 핵심 입력 센서로, 그 정밀도와 지연이 추정 성능에 직접적인 영향을 미침
- `ent_process_p10` — 본 공정이 속한 상위 작업 패키지로, 운동 제어 알고리즘 개발 및 검증 전체 프로세스를 포함
