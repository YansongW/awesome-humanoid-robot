---
$id: ent_method_action_chunking_transformer
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Action Chunking with Transformers (ACT)
  zh: 动作分块变压器（ACT）
  ko: 트랜스포머를 이용한 행동 청킹(ACT)
summary:
  en: An imitation-learning method that predicts a sequence of future actions as a chunk using a transformer, reducing compounding
    errors in long-horizon tasks.
  zh: 动作分块变压器（ACT）是一种模仿学习方法：用变压器以条件变分自编码器（CVAE）形式一次性预测未来一段动作序列（chunk），再经时间集成平滑执行，显著降低长时程精细操作中的误差累积。
  ko: 트랜스포머로 미래 행동 시퀀스를 한 덩어리로 예측하여 장기 과제의 오차 누적을 줄이는 모방 학습 방법.
domains:
- 07_ai_models_algorithms
- 09_data_datasets
layers:
- intelligence
functional_roles:
- knowledge
tags:
- method
- chapter_18
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body rewritten about ACT (previously mis-backfilled with power-distribution content); source set to the ACT paper.
    [2026-08-06] body upgraded to textbook-grade B1 batch (.staging/textbook_grade, methods top-20 by degree): zh 概述/核心内容/参考
    rewritten from card + graph neighbors + wiki chapters + first-hand sources (arXiv/Crossref/fetched pages in sources/,
    number whitelist audit 20/20 passed); en/ko sections to be regenerated by translate_entry_bodies.py.'
sources:
- id: src_001
  type: paper
  title: Learning Fine-Grained Bimanual Manipulation with Low-Cost Hardware
  url: https://arxiv.org/abs/2304.13705
  date: '2023'
  accessed_at: '2026-07-16'
---
## 概述

动作分块变压器（Action Chunking with Transformers, ACT）是 Zhao 等人 2023 年在低成本双臂遥操作平台 ALOHA 上提出的模仿学习方法：策略不再逐帧回归单步动作，而是一次性预测未来 k 步的动作序列，配合条件变分自编码器（CVAE）吸收演示风格多样性、时间集成平滑输出。它用约 2 万美元级硬件与 50 条量级演示即可在精细双臂任务上达到 80%–90% 成功率，"动作分块"已成为机器人基础模型动作头的主流设计（来源：项目 Wiki 第 18 章 §18.3；arXiv:2304.13705）。

## 核心内容

### 是什么：准确定义

ACT = **动作分块 + CVAE 策略建模 + 时间集成**的三件套（来源：项目 Wiki 第 18 章 §18.3.1）：

1. **动作分块（Action Chunking）**：每次推理输出未来 k 步动作 $\hat{a}_{t:t+k}$（典型 k=100），把 T 步决策链缩短为 T/k 次；
2. **CVAE 建模**：训练时编码器把演示动作块压缩为风格变量 z，解码器以多视角图像、关节状态与 z 为条件重建动作块；推理时 z 置 0；
3. **时间集成（Temporal Ensemble）**：不同时刻推理批次的重叠预测按指数衰减权重 $w_i \propto \exp(-m \cdot i)$ 加权平均，输出平滑轨迹。

### 为什么存在：痛点与历史定位

2023 年前后的精细双臂操作（拉拉链、开杯盖、插槽）有三座大山：单步 BC 的**复合误差**让分钟级任务漂移失控；回归损失在多峰演示上**平均化**；逐帧推理对**控制频率**要求高。ACT 的三件套恰好一一对应：分块缩短决策链条抑制复合误差；CVAE 的潜变量吸收风格多样性避免平均化；时间集成用重叠预测换平滑，允许较低推理频率。

它真正改变的不是某个网络结构，而是**策略与控制器之间的接口**：从"每帧一个动作"变成"每段一个意图"。这一接口后来成为 π0、GR00T N1 等基础模型动作专家的默认形态。

### 原理拆解

**① 动作分块为何抑制复合误差**

单步策略的决策链条长 T 步，每步都有偏离演示分布的机会；分块后链条缩短到 T/k 步，且每步预测的是"短期意图"而非瞬时动作，对观测噪声的敏感性同步下降（来源：项目 Wiki 第 18 章 §18.3.1）。

**② CVAE 训练目标**

$$
\mathcal{L} = \sum_{i=1}^{k} \|\hat{a}_{t+i} - a_{t+i}\|_1 + \beta\, D_{KL}\!\left(q_\phi(z \mid o_t, a_{t:t+k}) \,\|\, \mathcal{N}(0, I)\right)
$$

重建项用 L1（比 L2 更耐离群演示），KL 项把后验压向标准正态先验；推理时 z 取先验均值 0，即"平均风格"（来源：项目 Wiki 第 18 章 §18.3.1–18.3.2）。

**③ 时间集成的权衡**

执行时每一时刻都有多个重叠预测，指数加权平均得平滑输出。衰减系数 m 控制"记忆长度"：m 越小平滑越强，但对扰动的响应越慢——这是 ACT 在动态环境下的结构性迟滞来源（来源：项目 Wiki 第 18 章 §18.3.1）。

### 关键参数与规格

| 参数 | 典型值 | 来源 |
|------|--------|------|
| 动作块长度 k | 50–100（抑制复合误差 vs 闭环响应性的权衡） | Wiki §18.3.2 |
| 演示规模 | 50 条量级/任务（ALOHA 平台） | Wiki §18.3.1 |
| 执行频率 | 50 Hz（ALOHA 关节位置指令） | Wiki §18.3.1 |
| 精细双臂任务成功率 | 80%–90%（拉拉链、开杯盖、插槽等） | Wiki §18.3.1 |
| 硬件成本 | 约 2 万美元级双臂平台 | Wiki §18.3.1 |
| 重建损失 | L1（耐离群演示） | Wiki §18.3.2 |
| 图像骨干 | 轻量 ResNet 即可（瓶颈在数据不在容量） | Wiki §18.3.2 |

### 横向对比

| 维度 | 单步 BC | **ACT** | 扩散策略 |
|------|---------|---------|----------|
| 复合误差 | 严重 | 分块缓解 | 分块 + 滚动时域缓解 |
| 多峰建模 | 不支持（平均化失效） | CVAE 弱多峰 | 天然多峰 |
| 推理代价 | 极低 | 低（单次前向 + 集成） | 较高（多步去噪） |
| 训练数据 | 中等 | 数十至数百条 | 中等，随复杂度上升 |
| 典型代表 | BC-Z、RT-1 | ACT（ALOHA） | Diffusion Policy、DP3 |

（来源：项目 Wiki 第 18 章 §18.4.4。）

### 谁在用 / 应用案例

- **ALOHA / Mobile ALOHA**：ACT 的原生平台，低成本双臂精细操作的事实基线（`ent_technology_mobile_aloha_2024`）；
- **VLA 动作头**：π0、GR00T N1 等模型的动作专家均以分块形式输出动作块（Wiki §18.3.3）；
- **人形全身扩展**：图谱收录的 H-RDT（2025）把分块扩散动作头扩展到人形全身动作空间；Coordinated Humanoid Manipulation with Choice Policies（2025）在分块框架内引入离散模式选择（Wiki §18.3.3）。

### 局限与边界

- **单任务、单场景**：对语言指令与开放物体集合的泛化有限（Wiki §18.3.2）；
- **时间集成的响应迟滞**：假设连续推理批次预测一致，动态环境或突发扰动下响应滞后；
- **闭环性弱于滚动时域**：分块执行期间近似开环，块长越大越明显；
- **风格变量推理时退化为均值**：CVAE 只吸收风格多样性，不解决多峰决策本身。

### 常见误区

1. **"动作分块就是开环回放"**——每块结束后重新观测再推理，是闭环的；但块内确实近似开环，这是 k 不能太大的原因。
2. **"CVAE 解决了多峰问题"**——它吸收的是风格多样性（同任务不同做法），对"左绕/右绕"级别的决策多峰，扩散/流匹配才是正解。
3. **"k 越大越省算力越划算"**——k 大抑制复合误差但牺牲闭环响应性，典型取 50–100 是权衡结果，不是越大越好。
4. **"ACT 已被淘汰"**——动作分块作为**接口设计**已被基础模型普遍继承；"分块解决接口问题、生成模型解决分布问题，二者正交可组合"（Wiki §18.3.3）。

### 相关知识

- `ent_method_behavior_cloning` — ACT 的基线与上游范式
- `ent_method_diffusion_policy` — 多峰分布的替代生成方案
- `ent_method_openvla` — 分块动作头被 VLA 继承的去处
- `ent_technology_mobile_aloha_2024` — ACT 的原生硬件平台
- `ent_method_forward_kinematics` — 动作空间定义依赖的运动学

## 参考

- [Zhao et al., Learning Fine-Grained Bimanual Manipulation with Low-Cost Hardware（ACT/ALOHA）, arXiv:2304.13705](https://arxiv.org/abs/2304.13705)
- [项目 Wiki 第 18 章 §18.3 ACT 与动作分块](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-18.md)

## Overview

Action Chunking with Transformers (ACT) is an imitation learning method proposed by Zhao et al. in 2023 on the low-cost dual-arm teleoperation platform ALOHA: instead of regressing single-step actions frame by frame, the policy predicts a sequence of future k actions at once, combined with a Conditional Variational Autoencoder (CVAE) to absorb demonstration style diversity and temporal ensembling to smooth outputs. It achieves 80%–90% success rates on fine-grained dual-arm tasks with approximately $20K-level hardware and around 50 demonstrations. "Action chunking" has become the mainstream design for action heads in robot foundation models (Source: Project Wiki Chapter 18 §18.3; arXiv:2304.13705).

## Content

### What it is: precise definition

ACT = a three-part combination of **action chunking + CVAE policy modeling + temporal ensembling** (Source: Project Wiki Chapter 18 §18.3.1):

1. **Action Chunking**: each inference outputs the next k steps of actions $\hat{a}_{t:t+k}$ (typically k=100), shortening the T-step decision chain to T/k times;
2. **CVAE modeling**: during training, the encoder compresses demonstration action chunks into a style variable z, and the decoder reconstructs action chunks conditioned on multi-view images, joint states, and z; during inference, z is set to 0;
3. **Temporal Ensemble**: overlapping predictions from different inference batches are weighted averaged with exponentially decaying weights $w_i \propto \exp(-m \cdot i)$, producing smooth trajectories.

### Why it exists: pain points and historical positioning

Before 2023, fine-grained dual-arm manipulation (zippering, opening cup lids, peg insertion) faced three major challenges: the **compounding error** of single-step BC caused minute-level task drift to spiral out of control; regression losses **averaged** over multi-modal demonstrations; and frame-by-frame inference imposed high **control frequency** requirements. ACT's three components map exactly to these: chunking shortens the decision chain to suppress compounding errors; CVAE's latent variable absorbs style diversity to avoid averaging; temporal ensembling trades overlapping predictions for smoothness, allowing lower inference frequencies.

What it truly changed is not a specific network architecture, but the **interface between policy and controller**: from "one action per frame" to "one intention per chunk." This interface later became the default form for action experts in foundation models such as π0 and GR00T N1.

### Principle breakdown

**① Why action chunking suppresses compounding errors**

The decision chain of a single-step policy is T steps long, with each step having an opportunity to deviate from the demonstration distribution; after chunking, the chain shortens to T/k steps, and each step predicts a "short-term intention" rather than an instantaneous action, reducing sensitivity to observation noise accordingly (Source: Project Wiki Chapter 18 §18.3.1).

**② CVAE training objective**

$$
\mathcal{L} = \sum_{i=1}^{k} \|\hat{a}_{t+i} - a_{t+i}\|_1 + \beta\, D_{KL}\!\left(q_\phi(z \mid o_t, a_{t:t+k}) \,\|\, \mathcal{N}(0, I)\right)
$$

The reconstruction term uses L1 (more robust to outlier demonstrations than L2), and the KL term pushes the posterior toward a standard normal prior; during inference, z takes the prior mean 0, i.e., the "average style" (Source: Project Wiki Chapter 18 §18.3.1–18.3.2).

**③ The trade-off in temporal ensembling**

During execution, multiple overlapping predictions exist at every timestep, and exponentially weighted averaging yields smooth outputs. The decay coefficient m controls "memory length": smaller m gives stronger smoothing but slower responses to disturbances—this is the source of ACT's structural latency in dynamic environments (Source: Project Wiki Chapter 18 §18.3.1).

### Key parameters and specifications

| Parameter | Typical value | Source |
|-----------|---------------|--------|
| Action chunk length k | 50–100 (trade-off between suppressing compounding errors vs. closed-loop responsiveness) | Wiki §18.3.2 |
| Demonstration scale | Around 50 per task (ALOHA platform) | Wiki §18.3.1 |
| Execution frequency | 50 Hz (ALOHA joint position commands) | Wiki §18.3.1 |
| Success rate on fine-grained dual-arm tasks | 80%–90% (zippering, cup lid opening, peg insertion, etc.) | Wiki §18.3.1 |
| Hardware cost | Approximately $20K-level dual-arm platform | Wiki §18.3.1 |
| Reconstruction loss | L1 (robust to outlier demonstrations) | Wiki §18.3.2 |
| Image backbone | Lightweight ResNet suffices (bottleneck is data, not capacity) | Wiki §18.3.2 |

### Horizontal comparison

| Dimension | Single-step BC | **ACT** | Diffusion Policy |
|-----------|----------------|---------|------------------|
| Compounding error | Severe | Mitigated by chunking | Mitigated by chunking + receding horizon |
| Multi-modality modeling | Not supported (fails via averaging) | CVAE weak multi-modality | Naturally multi-modal |
| Inference cost | Extremely low | Low (single forward pass + ensembling) | Higher (multi-step denoising) |
| Training data | Moderate | Tens to hundreds of demonstrations | Moderate, increases with complexity |
| Typical representatives | BC-Z, RT-1 | ACT (ALOHA) | Diffusion Policy, DP3 |

(Source: Project Wiki Chapter 18 §18.4.4.)

### Who uses it / application cases

- **ALOHA / Mobile ALOHA**: ACT's native platform, the de facto baseline for low-cost fine-grained dual-arm manipulation (`ent_technology_mobile_aloha_2024`);
- **VLA action heads**: action experts in models such as π0 and GR00T N1 all output action chunks in chunked form (Wiki §18.3.3);
- **Humanoid whole-body extension**: H-RDT (2025), included in the atlas, extends the chunked diffusion action head to humanoid whole-body action spaces; Coordinated Humanoid Manipulation with Choice Policies (2025) introduces discrete mode selection within the chunking framework (Wiki §18.3.3).

### Limitations and boundaries

- **Single-task, single-scenario**: limited generalization to language instructions and open object sets (Wiki §18.3.2);
- **Response latency from temporal ensembling**: assumes consecutive inference batches produce consistent predictions, leading to lagged responses under dynamic environments or sudden disturbances;
- **Weaker closed-loop behavior than receding horizon**: approximately open-loop during chunk execution, more pronounced with larger chunk lengths;
- **Style variable degenerates to the mean during inference**: CVAE only absorbs style diversity, it does not resolve multi-modal decision-making itself.

### Common misconceptions

1. **"Action chunking is open-loop replay"**—after each chunk ends, it re-observes and re-infers, making it closed-loop; however, within a chunk it is indeed approximately open-loop, which is why k cannot be too large.
2. **"CVAE solves the multi-modality problem"**—it absorbs style diversity (different ways of doing the same task); for decision-level multi-modality like "go left vs. go right," diffusion/flow matching is the correct solution.
3. **"Larger k means more computational savings and better value"**—larger k suppresses compounding errors but sacrifices closed-loop responsiveness; the typical 50–100 range is a trade-off result, not "the bigger the better."
4. **"ACT is obsolete"**—action chunking as an **interface design** has been widely inherited by foundation models; "chunking solves the interface problem, generative models solve the distribution problem, and the two are orthogonal and composable" (Wiki §18.3.3).

### Related knowledge

- `ent_method_behavior_cloning` — ACT's baseline and upstream paradigm
- `ent_method_diffusion_policy` — alternative generative approach for multi-modal distributions
- `ent_method_openvla` — where the chunked action head is inherited by VLAs
- `ent_technology_mobile_aloha_2024` — ACT's native hardware platform
- `ent_method_forward_kinematics` — the kinematics on which action space definition depends

## 개요

액션 청킹 트랜스포머(Action Chunking with Transformers, ACT)는 Zhao 등이 2023년 저비용 양팔 원격 조작 플랫폼 ALOHA에서 제안한 모방 학습 방법이다. 정책이 더 이상 프레임별로 단일 동작을 회귀하지 않고, 한 번에 미래 k步의 동작 시퀀스를 예측하며, 조건부 변분 오토인코더(CVAE)를 결합하여 시연 스타일 다양성을 흡수하고 시간 앙상블로 출력을 평활화한다. 약 2만 달러급 하드웨어와 50개 수준의 시연만으로 정밀 양팔 작업에서 80%–90% 성공률을 달성하며, "액션 청킹"은 로봇 기반 모델의 액션 헤드 주류 설계가 되었다(출처: 프로젝트 Wiki 18장 §18.3; arXiv:2304.13705).

## 핵심 내용

### 무엇인가: 정확한 정의

ACT = **액션 청킹 + CVAE 정책 모델링 + 시간 앙상블**의 세 가지 구성 요소(출처: 프로젝트 Wiki 18장 §18.3.1):

1. **액션 청킹(Action Chunking)**: 매 추론마다 미래 k步 동작 $\hat{a}_{t:t+k}$(일반적으로 k=100)을 출력하여 T步 결정 체인을 T/k회로 단축한다;
2. **CVAE 모델링**: 훈련 시 인코더가 시연 동작 블록을 스타일 변수 z로 압축하고, 디코더가 다중 시점 이미지, 관절 상태 및 z를 조건으로 동작 블록을 재구성한다; 추론 시 z는 0으로 설정;
3. **시간 앙상블(Temporal Ensemble)**: 서로 다른 시점의 추론 배치에서 중첩된 예측을 지수 감쇠 가중치 $w_i \propto \exp(-m \cdot i)$로 가중 평균하여 평활한 궤적을 출력한다.

### 왜 존재하는가: 문제점과 역사적 위치

2023년 전후의 정밀 양팔 조작(지퍼 잠그기, 컵 뚜껑 열기, 슬롯 삽입)에는 세 가지 큰 장애물이 있었다: 단일 단계 BC의 **복합 오차**로 인해 수 분 단위 작업이 표류하여 통제 불능이 되고; 회귀 손실이 다봉 시연에서 **평균화**되며; 프레임별 추론은 **제어 주파수** 요구가 높다. ACT의 세 가지 구성 요소는 정확히 하나씩 대응한다: 청킹은 결정 체인을 단축하여 복합 오차를 억제하고; CVAE의 잠재 변수는 스타일 다양성을 흡수하여 평균화를 방지하며; 시간 앙상블은 중첩 예측으로 평활함을 얻어 낮은 추론 주파수를 허용한다.

ACT가 실제로 바꾼 것은 특정 네트워크 구조가 아니라 **정책과 컨트롤러 사이의 인터페이스**이다: "매 프레임 하나의 동작"에서 "매 세그먼트 하나의 의도"로 바뀌었다. 이 인터페이스는 이후 π0, GR00T N1 등 기반 모델 동작 전문가의 기본 형태가 되었다.

### 원리 분석

**① 액션 청킹이 복합 오차를 억제하는 이유**

단일 단계 정책의 결정 체인은 T步로 길며, 각 단계마다 시연 분포에서 벗어날 기회가 있다; 청킹 후 체인은 T/k步로 단축되고, 각 단계는 순간 동작이 아닌 "단기 의도"를 예측하므로 관측 노이즈에 대한 민감도도 함께 감소한다(출처: 프로젝트 Wiki 18장 §18.3.1).

**② CVAE 훈련 목표**

$$
\mathcal{L} = \sum_{i=1}^{k} \|\hat{a}_{t+i} - a_{t+i}\|_1 + \beta\, D_{KL}\!\left(q_\phi(z \mid o_t, a_{t:t+k}) \,\|\, \mathcal{N}(0, I)\right)
$$

재구성 항은 L1을 사용하고(L2보다 이상치 시연에 강함), KL 항은 사후 분포를 표준 정규 사전 분포로 압축한다; 추론 시 z는 사전 평균 0을 취하여 "평균 스타일"을 얻는다(출처: 프로젝트 Wiki 18장 §18.3.1–18.3.2).

**③ 시간 앙상블의 절충**

실행 시 매 순간 여러 중첩 예측이 있으며, 지수 가중 평균으로 평활한 출력을 얻는다. 감쇠 계수 m은 "기억 길이"를 제어한다: m이 작을수록 평활화가 강하지만 교란에 대한 응답이 느려진다 — 이는 동적 환경에서 ACT의 구조적 지연의 원인이다(출처: 프로젝트 Wiki 18장 §18.3.1).

### 주요 파라미터 및 사양

| 파라미터 | 일반값 | 출처 |
|------|--------|------|
| 액션 블록 길이 k | 50–100(복합 오차 억제 vs 폐루프 응답성의 절충) | Wiki §18.3.2 |
| 시연 규모 | 작업당 50개 수준(ALOHA 플랫폼) | Wiki §18.3.1 |
| 실행 주파수 | 50 Hz(ALOHA 관절 위치 명령) | Wiki §18.3.1 |
| 정밀 양팔 작업 성공률 | 80%–90%(지퍼 잠그기, 컵 뚜껑 열기, 슬롯 삽입 등) | Wiki §18.3.1 |
| 하드웨어 비용 | 약 2만 달러급 양팔 플랫폼 | Wiki §18.3.1 |
| 재구성 손실 | L1(이상치 시연에 강함) | Wiki §18.3.2 |
| 이미지 백본 | 경량 ResNet으로 충분(병목은 데이터에 있지 용량에 있지 않음) | Wiki §18.3.2 |

### 횡적 비교

| 차원 | 단일 단계 BC | **ACT** | 확산 정책 |
|------|---------|---------|----------|
| 복합 오차 | 심각 | 청킹으로 완화 | 청킹 + 롤링 호라이즌으로 완화 |
| 다봉 모델링 | 미지원(평균화로 실패) | CVAE 약한 다봉 | 자연스러운 다봉 |
| 추론 비용 | 매우 낮음 | 낮음(단일 순방향 + 앙상블) | 비교적 높음(다단계 노이즈 제거) |
| 훈련 데이터 | 중간 | 수십~수백 개 | 중간, 복잡도에 따라 증가 |
| 대표 사례 | BC-Z, RT-1 | ACT(ALOHA) | Diffusion Policy, DP3 |

(출처: 프로젝트 Wiki 18장 §18.4.4.)

### 사용처 / 적용 사례

- **ALOHA / Mobile ALOHA**: ACT의 원래 플랫폼, 저비용 양팔 정밀 조작의 사실상 기준선(`ent_technology_mobile_aloha_2024`);
- **VLA 액션 헤드**: π0, GR00T N1 등 모델의 동작 전문가는 모두 청킹 형태로 동작 블록을 출력한다(Wiki §18.3.3);
- **휴머노이드 전신 확장**: 도감에 수록된 H-RDT(2025)는 청킹 확산 액션 헤드를 휴머노이드 전신 동작 공간으로 확장했다; Coordinated Humanoid Manipulation with Choice Policies(2025)는 청킹 프레임워크 내에 이산 모드 선택을 도입했다(Wiki §18.3.3).

### 한계와 경계

- **단일 작업, 단일 시나리오**: 언어 명령과 개방형 객체 집합에 대한 일반화가 제한적이다(Wiki §18.3.2);
- **시간 앙상블의 응답 지연**: 연속 추론 배치의 예측 일관성을 가정하므로, 동적 환경이나 갑작스러운 교란에서 응답이 지연된다;
- **롤링 호라이즌보다 약한 폐루프성**: 청킹 실행 중에는 근사적으로 개루프이며, 블록 길이가 클수록 더 두드러진다;
- **추론 시 스타일 변수가 평균으로 퇴화**: CVAE는 스타일 다양성만 흡수하며, 다봉 결정 자체를 해결하지 않는다.

### 흔한 오해

1. **"액션 청킹은 개루프 재생이다"** — 각 블록이 끝난 후 관측을 다시 하고 추론하므로 폐루프이다; 그러나 블록 내부는 실제로 근사 개루프이며, 이것이 k가 너무 클 수 없는 이유이다.
2. **"CVAE가 다봉 문제를 해결했다"** — CVAE가 흡수하는 것은 스타일 다양성(같은 작업의 다른 방식)이며, "왼쪽 우회/오른쪽 우회" 수준의 결정 다봉에는 확산/플로우 매칭이 올바른 해법이다.
3. **"k가 클수록 연산을 아끼고 효율적이다"** — k가 크면 복합 오차를 억제하지만 폐루프 응답성을 희생하며, 일반적으로 50–100을 취하는 것은 절충의 결과이지 클수록 좋은 것이 아니다.
4. **"ACT는 이미 도태되었다"** — 액션 청킹은 **인터페이스 설계**로서 기반 모델에 보편적으로 계승되었다; "청킹은 인터페이스 문제를 해결하고, 생성 모델은 분포 문제를 해결하며, 둘은 직교하여 결합 가능하다"(Wiki §18.3.3).

### 관련 지식

- `ent_method_behavior_cloning` — ACT의 기준선이자 상위 패러다임
- `ent_method_diffusion_policy` — 다봉 분포의 대체 생성 방식
- `ent_method_openvla` — 청킹 액션 헤드가 VLA에 계승된 곳
- `ent_technology_mobile_aloha_2024` — ACT의 원래 하드웨어 플랫폼
- `ent_method_forward_kinematics` — 동작 공간 정의가 의존하는 운동학
