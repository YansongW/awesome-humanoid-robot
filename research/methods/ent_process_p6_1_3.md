---
$id: ent_process_p6_1_3
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Model of collision and self-interference
  zh: 碰撞与自干涉模型
  ko: 충돌 및 자체 간섭 모델
summary:
  en: There is no self-collision false alarm and no environment penetration in key postures
  zh: '- P6.1.3.1 输入梳理与目标量化 - 整理「碰撞与自干涉模型」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 典型动作无自碰撞误报、关键姿态无环境穿透
domains:
- 06_design_engineering
- 08_software_middleware
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

碰撞与自干涉模型（Model of collision and self-interference）是人形机器人 URDF 建模与运动学校核阶段中的关键工序节点，其核心任务是构建机器人运动过程中肢体与环境、肢体与肢体之间不发生碰撞的几何与逻辑约束体系。它真正改变的不是"检查机器人会不会撞到东西"这件事本身，而是让"安全运动"从经验判断转变为可量化、可追溯、可自动验证的工程标准。

## 核心内容

### 是什么：为机器人运动划定"安全边界"的几何与逻辑体系

碰撞与自干涉模型，本质上是一套将机器人本体几何、环境几何与运动学约束三者耦合起来的判定系统。它由三部分构成：**碰撞体几何表示**（凸包/简化网格）、**碰撞检测引擎**（FCL/PyBullet）、**碰撞矩阵**（定义哪些连杆对之间允许或禁止碰撞）。

在人形机器人开发流程中，该模型承担着承上启下的作用：上游接收 URDF 建模阶段的运动学参数与连杆几何，下游为运动规划、轨迹生成与安全保护逻辑提供碰撞约束输入。它不是一个独立的仿真工具，而是嵌入在开发工具链中的"安全守门员"——任何运动指令在下发到实机之前，都必须先通过它的检验。

### 为什么存在：碰撞体"过紧误报、过松漏报"的工程悖论

人形机器人是典型的"高自由度、强耦合、多约束"系统。若直接将 CAD 模型导入碰撞检测环境，会面临三个致命问题：第一，CAD 模型包含大量装饰性几何（圆角、螺纹、倒角），直接用于碰撞检测会导致计算量爆炸；第二，CAD 装配体中的坐标系定义往往基于设计便利，而非运动学约定；第三，CAD 模型是静态的，而碰撞检测需要的是运动过程中的动态判定。

碰撞与自干涉模型的存在，正是为了解决上述问题。它通过**几何简化**（凸包/简化网格）降低计算开销，通过**碰撞矩阵**显式声明允许/禁止的碰撞对，通过**仿真验证**确保模型在典型动作下无自碰撞误报、关键姿态下无环境穿透。它真正改变的不是"检测碰撞"这个动作，而是让"安全"从模糊的定性要求变成了可量化的 KPI。

### 原理拆解

**① 几何简化：从 CAD 到碰撞体的降维**

CAD 模型包含大量装饰性几何（圆角、螺纹、倒角），直接用于碰撞检测会导致计算量爆炸。碰撞与自干涉模型采用两种简化策略：

- **凸包（Convex Hull）**：将复杂凹形几何外包为最小凸包，碰撞检测算法对凸体有高效的判定方法（如 GJK 算法）。凸包的优势是计算快、误报率低，但会引入"虚体积"——原本不接触的两个连杆可能因凸包膨胀而被判定为碰撞。
- **简化网格（Simplified Mesh）**：保留关键表面特征，去除装饰性细节。简化网格更贴近真实几何，但计算开销高于凸包。

工程上通常采用"凸包为主、简化网格为辅"的混合策略：大范围快速筛查用凸包，关键区域精确判定用简化网格。

**② 碰撞矩阵：显式声明"谁可以和谁碰"**

碰撞矩阵是一个 N×N 的布尔矩阵（N 为连杆数），定义任意两个连杆之间是否允许碰撞。矩阵元素为 `true` 表示允许碰撞（如相邻连杆通过关节连接），`false` 表示禁止碰撞（如左右手臂之间）。碰撞矩阵的价值在于：

- 将"自干涉"问题从"全对全检测"降维为"关键对检测"，计算量从 O(N²) 降至 O(M)，其中 M 为禁止碰撞的对数（工程判断：M 通常远小于 N²）。
- 显式声明设计意图——哪些部位在运动中可以接近，哪些必须保持距离。

**③ 碰撞检测引擎：FCL 与 PyBullet 的分工**

FCL（Flexible Collision Library）是高效的碰撞检测库，支持凸体间的 GJK/EPA 算法，适合大规模快速筛查。PyBullet 则提供完整的物理仿真环境，支持刚体动力学、接触力计算与可视化调试。工程上通常用 FCL 做离线批量验证，用 PyBullet 做在线可视化调试与异常复现。

### 关键参数与规格

碰撞与自干涉模型的关键参数包括：

| 参数 | 规格说明 | 来源 |
|------|---------|------|
| 碰撞体类型 | 凸包 / 简化网格 | 语料 |
| 检测引擎 | FCL / PyBullet | 语料 |
| 碰撞矩阵维度 | N×N（N 为连杆数） | 工程判断 |
| 计算开销约束 | 需满足实时性要求 | 语料（关键约束） |
| 精度要求 | 过紧误报、过松漏报均不可接受 | 语料（设计思考逻辑） |
| 维护性 | 模型需随设计变更同步更新 | 语料（关键约束） |

完成标准为：典型动作无自碰撞误报、关键姿态无环境穿透（语料原文）。

### 横向对比

| 维度 | 碰撞与自干涉模型（P6.1.3） | 传统碰撞检测（通用） |
|------|---------------------------|---------------------|
| 几何来源 | URDF 连杆模型 | CAD 原始模型 |
| 简化策略 | 凸包/简化网格 | 通常不做简化 |
| 碰撞对声明 | 碰撞矩阵显式声明 | 全对全检测 |
| 验证目标 | 典型动作无误报、关键姿态无穿透 | 仅检测是否碰撞 |
| 下游输出 | 碰撞约束供运动规划使用 | 无明确下游接口 |

它真正改变的不是"检测碰撞"这个动作，而是让"安全"从模糊的定性要求变成了可量化的 KPI。

### 谁在用·应用案例

碰撞与自干涉模型是人形机器人开发流程中的标准工序，主要使用者包括：

- **运动规划工程师**：在生成轨迹前，用碰撞模型验证轨迹是否安全；在轨迹生成后，用碰撞模型做最终确认。
- **仿真验证工程师**：在 PyBullet 环境中加载碰撞模型，执行典型动作（如行走、抓取、蹲起）的批量仿真，记录碰撞事件并分析原因。
- **安全保护逻辑开发者**：将碰撞模型的输出接入实时安全监控，当检测到即将发生碰撞时触发急停或避让。

典型应用场景：在机器人执行"双臂协同搬运"任务前，工程师用碰撞矩阵确认左右手臂在搬运轨迹中不会相互干涉；在机器人进入狭窄环境前，用简化网格模型验证机身与环境的最小距离是否满足安全阈值。

### 局限与边界

碰撞与自干涉模型并非万能，其局限包括：

1. **几何简化的精度损失**：凸包会引入"虚体积"，导致误报；简化网格可能丢失微小特征，导致漏报。工程上需要在计算开销与精度之间做权衡（语料：关键约束）。
2. **静态模型的动态局限**：碰撞模型基于 URDF 的静态几何，无法反映运动过程中的弹性形变、关节间隙等动态因素。
3. **环境模型的不确定性**：环境几何通常来自感知系统，存在测量误差与动态变化，碰撞模型无法完全消除这种不确定性。
4. **维护成本**：设计变更后，碰撞体与碰撞矩阵需同步更新，否则会引入新的误报或漏报（语料：维护性约束）。

### 常见误区

1. **"碰撞体越精细越好"**——错。精细网格计算开销大，且对微小误差敏感，反而容易误报。工程上应优先使用凸包做快速筛查，仅在关键区域使用精细网格。
2. **"碰撞矩阵可以省略"**——错。没有碰撞矩阵，系统会对所有连杆对做碰撞检测，计算量巨大且容易误报。碰撞矩阵是降低计算量、提升准确性的关键设计。
3. **"仿真通过就等于实机安全"**——错。仿真环境无法完全模拟真实世界的摩擦、形变、传感器噪声等因素，碰撞模型验证通过只是必要条件，而非充分条件。
4. **"碰撞模型是一次性工作"**——错。设计变更后必须同步更新碰撞体与碰撞矩阵，否则模型会逐渐失效（语料：维护性约束）。

### 相关知识

- `ent_process_p6` — 碰撞与自干涉模型是 URDF 建模与运动学校核阶段（P6）的三级子任务之一，承接上游运动学参数，为下游运动规划提供碰撞约束。
- `ent_process_p6_1_1` — 同族相邻工序：URDF/SDF 文件生成（同一工作包内的并行工序）。
- `ent_process_p6_1_2` — 同族相邻工序：DH / 修改 DH 参数定义（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

The Model of collision and self-interference is a critical process node in the URDF modeling and kinematics verification stage of humanoid robots. Its core task is to construct a geometric and logical constraint system that ensures no collisions occur between limbs and the environment, or between limbs themselves, during robot motion. What it truly changes is not the act of "checking whether the robot will hit something," but rather transforming "safe motion" from an empirical judgment into a quantifiable, traceable, and automatically verifiable engineering standard.

## Content

### What It Is: A Geometric and Logical System Defining "Safety Boundaries" for Robot Motion

The collision and self-interference model is essentially a determination system that couples robot body geometry, environmental geometry, and kinematic constraints. It consists of three parts: **collision body geometric representation** (convex hull/simplified mesh), **collision detection engine** (FCL/PyBullet), and **collision matrix** (defining which link pairs allow or prohibit collisions).

In the humanoid robot development workflow, this model serves a bridging role: upstream, it receives kinematic parameters and link geometry from the URDF modeling stage; downstream, it provides collision constraint inputs for motion planning, trajectory generation, and safety protection logic. It is not an independent simulation tool but a "safety gatekeeper" embedded in the development toolchain—any motion command must pass its verification before being sent to the physical robot.

### Why It Exists: The Engineering Paradox of Collision Bodies Being "Too Tight Causing False Positives, Too Loose Causing Missed Detections"

Humanoid robots are typical systems with "high degrees of freedom, strong coupling, and multiple constraints." Directly importing CAD models into a collision detection environment presents three fatal issues: first, CAD models contain extensive decorative geometry (fillets, threads, chamfers), and using them directly for collision detection leads to computational explosion; second, coordinate system definitions in CAD assemblies are often based on design convenience rather than kinematic conventions; third, CAD models are static, while collision detection requires dynamic determination during motion.

The existence of the collision and self-interference model is precisely to address these issues. It reduces computational overhead through **geometric simplification** (convex hull/simplified mesh), explicitly declares allowed/prohibited collision pairs through the **collision matrix**, and ensures the model has no false-positive self-collisions under typical actions and no environmental penetration in critical poses through **simulation verification**. What it truly changes is not the act of "detecting collisions," but turning "safety" from a vague qualitative requirement into a quantifiable KPI.

### Principle Breakdown

**① Geometric Simplification: Dimensionality Reduction from CAD to Collision Bodies**

CAD models contain extensive decorative geometry (fillets, threads, chamfers), and using them directly for collision detection leads to computational explosion. The collision and self-interference model employs two simplification strategies:

- **Convex Hull**: Wraps complex concave geometry into a minimal convex hull, and collision detection algorithms have efficient determination methods for convex bodies (e.g., the GJK algorithm). The advantage of convex hulls is fast computation and low false-positive rates, but they introduce "virtual volume"—two links that do not actually touch may be judged as colliding due to hull expansion.
- **Simplified Mesh**: Retains key surface features while removing decorative details. Simplified meshes are closer to real geometry but have higher computational overhead than convex hulls.

In engineering practice, a hybrid strategy of "convex hull as primary, simplified mesh as supplementary" is typically adopted: convex hulls for large-scale rapid screening, and simplified meshes for precise determination in critical regions.

**② Collision Matrix: Explicitly Declaring "Who Can Touch Whom"**

The collision matrix is an N×N boolean matrix (N being the number of links) that defines whether collisions are allowed between any two links. A matrix element of `true` indicates allowed collision (e.g., adjacent links connected by joints), while `false` indicates prohibited collision (e.g., between left and right arms). The value of the collision matrix lies in:

- Reducing the "self-interference" problem from "all-to-all detection" to "critical-pair detection," lowering computational complexity from O(N²) to O(M), where M is the number of prohibited collision pairs (engineering judgment: M is typically much smaller than N²).
- Explicitly declaring design intent—which parts may approach each other during motion and which must maintain distance.

**③ Collision Detection Engine: Division of Labor Between FCL and PyBullet**

FCL (Flexible Collision Library) is an efficient collision detection library supporting GJK/EPA algorithms for convex bodies, suitable for large-scale rapid screening. PyBullet provides a complete physics simulation environment supporting rigid-body dynamics, contact force calculation, and visual debugging. In engineering practice, FCL is typically used for offline batch verification, while PyBullet is used for online visual debugging and anomaly reproduction.

### Key Parameters and Specifications

Key parameters of the collision and self-interference model include:

| Parameter | Specification | Source |
|-----------|---------------|--------|
| Collision body type | Convex hull / Simplified mesh | Corpus |
| Detection engine | FCL / PyBullet | Corpus |
| Collision matrix dimension | N×N (N = number of links) | Engineering judgment |
| Computational overhead constraint | Must meet real-time requirements | Corpus (key constraint) |
| Accuracy requirement | Neither too-tight false positives nor too-loose missed detections are acceptable | Corpus (design reasoning logic) |
| Maintainability | Model must be updated synchronously with design changes | Corpus (key constraint) |

The completion standard is: no false-positive self-collisions under typical actions, and no environmental penetration in critical poses (original corpus text).

### Horizontal Comparison

| Dimension | Collision and Self-Interference Model (P6.1.3) | Traditional Collision Detection (General) |
|-----------|-----------------------------------------------|-------------------------------------------|
| Geometric source | URDF link model | Original CAD model |
| Simplification strategy | Convex hull / Simplified mesh | Typically no simplification |
| Collision pair declaration | Explicit declaration via collision matrix | All-to-all detection |
| Verification goal | No false positives under typical actions, no penetration in critical poses | Only detects whether collision occurs |
| Downstream output | Collision constraints for motion planning | No clear downstream interface |

What it truly changes is not the act of "detecting collisions," but turning "safety" from a vague qualitative requirement into a quantifiable KPI.

### Who Uses It · Application Cases

The collision and self-interference model is a standard procedure in the humanoid robot development workflow, with primary users including:

- **Motion Planning Engineers**: Use the collision model to verify trajectory safety before generating trajectories; perform final confirmation with the collision model after trajectory generation.
- **Simulation Verification Engineers**: Load the collision model in the PyBullet environment, execute batch simulations of typical actions (e.g., walking, grasping, squatting), record collision events, and analyze causes.
- **Safety Protection Logic Developers**: Integrate the collision model's output into real-time safety monitoring, triggering emergency stops or avoidance when imminent collisions are detected.

Typical application scenarios: Before a robot executes a "dual-arm coordinated carrying" task, engineers use the collision matrix to confirm that the left and right arms will not interfere with each other along the carrying trajectory; before the robot enters a narrow environment, the simplified mesh model is used to verify whether the minimum distance between the body and the environment meets safety thresholds.

### Limitations and Boundaries

The collision and self-interference model is not omnipotent; its limitations include:

1. **Precision loss from geometric simplification**: Convex hulls introduce "virtual volume," causing false positives; simplified meshes may lose tiny features, causing missed detections. Engineering practice requires a trade-off between computational overhead and precision (corpus: key constraint).
2. **Dynamic limitations of static models**: The collision model is based on URDF static geometry and cannot reflect dynamic factors such as elastic deformation and joint clearance during motion.
3. **Uncertainty in environmental models**: Environmental geometry typically comes from perception systems and contains measurement errors and dynamic changes; the collision model cannot fully eliminate this uncertainty.
4. **Maintenance cost**: After design changes, collision bodies and the collision matrix must be updated synchronously; otherwise, new false positives or missed detections will be introduced (corpus: maintainability constraint).

### Common Misconceptions

1. **"The finer the collision body, the better"**—Wrong. Fine meshes have high computational overhead and are sensitive to minor errors, making them prone to false positives. Engineering practice should prioritize convex hulls for rapid screening and use fine meshes only in critical regions.
2. **"The collision matrix can be omitted"**—Wrong. Without a collision matrix, the system performs collision detection on all link pairs, resulting in massive computational overhead and frequent false positives. The collision matrix is a key design for reducing computational load and improving accuracy.
3. **"Passing simulation means real-machine safety"**—Wrong. Simulation environments cannot fully replicate real-world factors such as friction, deformation, and sensor noise; passing collision model verification is a necessary condition, not a sufficient one.
4. **"The collision model is a one-time effort"**—Wrong. Collision bodies and the collision matrix must be updated synchronously after design changes; otherwise, the model will gradually become ineffective (corpus: maintainability constraint).

### Related Knowledge

- `ent_process_p6` — The collision and self-interference model is one of the third-level subtasks in the URDF modeling and kinematics verification stage (P6), receiving upstream kinematic parameters and providing collision constraints for downstream motion planning.
- `ent_process_p6_1_1` — Adjacent process in the same family: URDF/SDF file generation (parallel process within the same work package).
- `ent_process_p6_1_2` — Adjacent process in the same family: DH / Modified DH parameter definition (parallel process within the same work package).

## 개요

충돌 및 자체 간섭 모델(Model of collision and self-interference)은 휴머노이드 로봇 URDF 모델링 및 운동학교核 단계에서의 핵심 공정 노드로, 그 핵심 임무는 로봇 운동 과정에서 팔다리와 환경, 팔다리와 팔다리 사이에 충돌이 발생하지 않도록 하는 기하학적 및 논리적 제약 체계를 구축하는 것입니다. 이 모델이 진정으로 바꾸는 것은 "로봇이 사물에 부딪힐지 확인하는 것" 자체가 아니라, "안전 운동"을 경험적 판단에서 정량화 가능하고, 추적 가능하며, 자동 검증 가능한 엔지니어링 표준으로 전환하는 것입니다.

## 핵심 내용

### 무엇인가: 로봇 운동에 "안전 경계"를 설정하는 기하학적 및 논리적 체계

충돌 및 자체 간섭 모델은 본질적으로 로봇 본체 기하학, 환경 기하학, 운동학적 제약을 결합하는 판정 시스템입니다. 이는 세 부분으로 구성됩니다: **충돌체 기하학적 표현**(볼록 껍질/단순화 메시), **충돌 감지 엔진**(FCL/PyBullet), **충돌 행렬**(어떤 링크 쌍 간의 충돌을 허용하거나 금지할지 정의).

휴머노이드 로봇 개발 프로세스에서 이 모델은 상하를 연결하는 역할을 담당합니다: 상위에서는 URDF 모델링 단계의 운동학적 매개변수와 링크 기하학을 수신하고, 하위에서는 운동 계획, 궤적 생성 및 안전 보호 로직에 충돌 제약 입력을 제공합니다. 이는 독립적인 시뮬레이션 도구가 아니라 개발 도구 체인에 내장된 "안전 게이트키퍼"로, 모든 운동 명령은 실기로 전달되기 전에 반드시 그 검증을 통과해야 합니다.

### 왜 존재하는가: 충돌체 "과밀 오탐, 과소 누락"의 엔지니어링 역설

휴머노이드 로봇은 전형적인 "고자유도, 강결합, 다중 제약" 시스템입니다. CAD 모델을 직접 충돌 감지 환경에 가져오면 세 가지 치명적인 문제에 직면합니다: 첫째, CAD 모델은 많은 장식적 기하학(라운드, 나사산, 모따기)을 포함하여 직접 충돌 감지에 사용하면 계산량이 폭발합니다; 둘째, CAD 어셈블리의 좌표계 정의는 종종 설계 편의에 기반하며 운동학적 규약이 아닙니다; 셋째, CAD 모델은 정적이지만 충돌 감지에는 운동 과정의 동적 판정이 필요합니다.

충돌 및 자체 간섭 모델의 존재는 바로 위 문제를 해결하기 위한 것입니다. 이는 **기하학적 단순화**(볼록 껍질/단순화 메시)를 통해 계산 오버헤드를 줄이고, **충돌 행렬**을 통해 허용/금지 충돌 쌍을 명시적으로 선언하며, **시뮬레이션 검증**을 통해 모델이 전형적인 동작에서 자체 간섭 오탐이 없고, 핵심 자세에서 환경 관통이 없음을 보장합니다. 이 모델이 진정으로 바꾸는 것은 "충돌 감지"라는 동작이 아니라, "안전"을 모호한 정성적 요구에서 정량화 가능한 KPI로 만드는 것입니다.

### 원리 분석

**① 기하학적 단순화: CAD에서 충돌체로의 차원 축소**

CAD 모델은 많은 장식적 기하학(라운드, 나사산, 모따기)을 포함하여 직접 충돌 감지에 사용하면 계산량이 폭발합니다. 충돌 및 자체 간섭 모델은 두 가지 단순화 전략을 사용합니다:

- **볼록 껍질(Convex Hull)**: 복잡한 오목 기하를 최소 볼록 껍질로 외곽 처리하며, 충돌 감지 알고리즘은 볼록체에 대해 효율적인 판정 방법(GJK 알고리즘 등)을 제공합니다. 볼록 껍질의 장점은 계산이 빠르고 오탐률이 낮지만, "가상 부피"를 도입하여 원래 접촉하지 않던 두 링크가 볼록 껍질 팽창으로 충돌로 판정될 수 있습니다.
- **단순화 메시(Simplified Mesh)**: 핵심 표면 특징을 유지하고 장식적 세부 사항을 제거합니다. 단순화 메시는 실제 기하에 더 가깝지만 계산 오버헤드가 볼록 껍질보다 높습니다.

공학적으로는 일반적으로 "볼록 껍질 위주, 단순화 메시 보조"의 혼합 전략을 사용합니다: 넓은 범위의 빠른 스크리닝에는 볼록 껍질, 핵심 영역의 정밀 판정에는 단순화 메시를 사용합니다.

**② 충돌 행렬: "누가 누구와 충돌할 수 있는지" 명시적 선언**

충돌 행렬은 N×N 부울 행렬(N은 링크 수)로, 임의의 두 링크 간 충돌 허용 여부를 정의합니다. 행렬 요소가 `true`이면 충돌 허용(예: 인접 링크가 관절로 연결됨), `false`이면 충돌 금지(예: 좌우 팔 사이)를 의미합니다. 충돌 행렬의 가치는:

- "자체 간섭" 문제를 "전체 대 전체 감지"에서 "핵심 쌍 감지"로 차원 축소하여 계산량을 O(N²)에서 O(M)으로 줄입니다. 여기서 M은 충돌 금지 쌍의 수입니다(공학적 판단: M은 일반적으로 N²보다 훨씬 작음).
- 설계 의도를 명시적으로 선언합니다 — 운동 중 어떤 부위가 접근할 수 있고, 어떤 부위는 거리를 유지해야 하는지.

**③ 충돌 감지 엔진: FCL과 PyBullet의 역할 분담**

FCL(Flexible Collision Library)은 효율적인 충돌 감지 라이브러리로, 볼록체 간의 GJK/EPA 알고리즘을 지원하며 대규모 빠른 스크리닝에 적합합니다. PyBullet은 완전한 물리 시뮬레이션 환경을 제공하며 강체 동역학, 접촉력 계산 및 시각화 디버깅을 지원합니다. 공학적으로는 일반적으로 FCL로 오프라인 배치 검증을 수행하고, PyBullet으로 온라인 시각화 디버깅 및 이상 재현을 수행합니다.

### 핵심 매개변수 및 사양

충돌 및 자체 간섭 모델의 핵심 매개변수는 다음과 같습니다:

| 매개변수 | 사양 설명 | 출처 |
|------|---------|------|
| 충돌체 유형 | 볼록 껍질 / 단순화 메시 | 말뭉치 |
| 감지 엔진 | FCL / PyBullet | 말뭉치 |
| 충돌 행렬 차원 | N×N(N은 링크 수) | 공학적 판단 |
| 계산 오버헤드 제약 | 실시간 요구 충족 필요 | 말뭉치(핵심 제약) |
| 정밀도 요구 | 과밀 오탐, 과소 누락 모두 허용 불가 | 말뭉치(설계 사고 로직) |
| 유지보수성 | 모델은 설계 변경에 따라 동기화 업데이트 필요 | 말뭉치(핵심 제약) |

완료 기준은: 전형적인 동작에서 자체 간섭 오탐 없음, 핵심 자세에서 환경 관통 없음(말뭉치 원문).

### 횡적 비교

| 차원 | 충돌 및 자체 간섭 모델(P6.1.3) | 전통적 충돌 감지(일반) |
|------|---------------------------|---------------------|
| 기하학 출처 | URDF 링크 모델 | CAD 원본 모델 |
| 단순화 전략 | 볼록 껍질/단순화 메시 | 일반적으로 단순화 없음 |
| 충돌 쌍 선언 | 충돌 행렬 명시적 선언 | 전체 대 전체 감지 |
| 검증 목표 | 전형적인 동작 오탐 없음, 핵심 자세 관통 없음 | 충돌 여부만 감지 |
| 하위 출력 | 운동 계획용 충돌 제약 제공 | 명확한 하위 인터페이스 없음 |

이 모델이 진정으로 바꾸는 것은 "충돌 감지"라는 동작이 아니라, "안전"을 모호한 정성적 요구에서 정량화 가능한 KPI로 만드는 것입니다.

### 사용자·적용 사례

충돌 및 자체 간섭 모델은 휴머노이드 로봇 개발 프로세스의 표준 공정으로, 주요 사용자는 다음과 같습니다:

- **운동 계획 엔지니어**: 궤적 생성 전에 충돌 모델로 궤적 안전성을 검증하고, 궤적 생성 후 충돌 모델로 최종 확인을 수행합니다.
- **시뮬레이션 검증 엔지니어**: PyBullet 환경에서 충돌 모델을 로드하고 전형적인 동작(보행, 파지, 스쿼트 등)의 배치 시뮬레이션을 실행하여 충돌 이벤트를 기록하고 원인을 분석합니다.
- **안전 보호 로직 개발자**: 충돌 모델의 출력을 실시간 안전 모니터링에 연결하여 충돌 발생이 임박했을 때 긴급 정지 또는 회피를 트리거합니다.

전형적인 적용 시나리오: 로봇이 "양팔 협동 운반" 작업을 수행하기 전에 엔지니어가 충돌 행렬로 좌우 팔이 운반 궤적에서 서로 간섭하지 않는지 확인합니다; 로봇이 좁은 환경에 진입하기 전에 단순화 메시 모델로 본체와 환경의 최소 거리가 안전 임계값을 충족하는지 검증합니다.

### 한계와 경계

충돌 및 자체 간섭 모델은 만능이 아니며, 그 한계는 다음과 같습니다:

1. **기하학적 단순화의 정밀도 손실**: 볼록 껍질은 "가상 부피"를 도입하여 오탐을 유발하고, 단순화 메시는 미세 특징을 잃어 누락을 유발할 수 있습니다. 공학적으로는 계산 오버헤드와 정밀도 사이에서 균형을 맞춰야 합니다(말뭉치: 핵심 제약).
2. **정적 모델의 동적 한계**: 충돌 모델은 URDF의 정적 기하학에 기반하여 운동 과정의 탄성 변형, 관절 간극 등의 동적 요인을 반영할 수 없습니다.
3. **환경 모델의 불확실성**: 환경 기하학은 일반적으로 인식 시스템에서 오며 측정 오차와 동적 변화가 존재하여 충돌 모델이 이러한 불확실성을 완전히 제거할 수 없습니다.
4. **유지보수 비용**: 설계 변경 후 충돌체와 충돌 행렬을 동기화 업데이트해야 하며, 그렇지 않으면 새로운 오탐 또는 누락이 발생할 수 있습니다(말뭉치: 유지보수성 제약).

### 일반적인 오해

1. **"충돌체가 정밀할수록 좋다"** — 틀림. 정밀 메시는 계산 오버헤드가 크고 미세 오차에 민감하여 오히려 오탐이 발생하기 쉽습니다. 공학적으로는 볼록 껍질로 빠른 스크리닝을 우선하고 핵심 영역에서만 정밀 메시를 사용해야 합니다.
2. **"충돌 행렬은 생략할 수 있다"** — 틀림. 충돌 행렬이 없으면 시스템이 모든 링크 쌍에 대해 충돌 감지를 수행하여 계산량이 크고 오탐이 발생하기 쉽습니다. 충돌 행렬은 계산량을 줄이고 정확도를 높이는 핵심 설계입니다.
3. **"시뮬레이션 통과 = 실기 안전"** — 틀림. 시뮬레이션 환경은 실제 세계의 마찰, 변형, 센서 노이즈 등의 요인을 완전히 모사할 수 없으며, 충돌 모델 검증 통과는 필요 조건일 뿐 충분 조건이 아닙니다.
4. **"충돌 모델은 일회성 작업"** — 틀림. 설계 변경 후 충돌체와 충돌 행렬을 동기화 업데이트해야 하며, 그렇지 않으면 모델이 점차失效합니다(말뭉치: 유지보수성 제약).

### 관련 지식

- `ent_process_p6` — 충돌 및 자체 간섭 모델은 URDF 모델링 및 운동학교核 단계(P6)의 3급 하위 작업 중 하나로, 상위 운동학적 매개변수를承接하고 하위 운동 계획에 충돌 제약을 제공합니다.
- `ent_process_p6_1_1` — 동일 계열 인접 공정: URDF/SDF 파일 생성(동일 작업 패키지 내 병렬 공정).
- `ent_process_p6_1_2` — 동일 계열 인접 공정: DH / 수정 DH 매개변수 정의(동일 작업 패키지 내 병렬 공정).
