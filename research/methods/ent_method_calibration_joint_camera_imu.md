---
$id: ent_method_calibration_joint_camera_imu
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Joint-Camera-IMU Calibration
  zh: 联合标定
  ko: 관절·칩라·IMU 통합 보정
summary:
  en: The process of estimating the intrinsic and extrinsic parameters, as well as time offsets, between joints, cameras,
    and inertial sensors to ensure coherent sensing and control.
  zh: 在人形机器人中，相机提供密集纹理与语义信息，LiDAR 提供精确三维几何。要实现 RGB 点云着色、深度融合、目标 3D 定位等功能，必须精确标定相机与 LiDAR 之间的外参（旋转 $\mathbf{R}_{CL}$ 和平移 $\mathbf{t}_{CL}$）。
  ko: 관절·칩라·관성 센서 간 내외부 파라미터와 시간 오프셋을 추정하여 감지와 제어의 일관성을 보장하는 과정.
domains:
- 04_assembly_integration_testing
layers:
- midstream
functional_roles:
- knowledge
tags:
- method
- chapter_11
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body backfilled from chapter-05.md#相机-LiDAR 联合标定 by scripts/backfill_nonpaper_entries.py. | WP4 trilingual backfill
    2026-08-10: en body retranslated from zh deep-read (2518 chars, DeepSeek). | WP4 trilingual backfill 2026-08-10: closed
    unclosed code fence(s) and removed duplicate stale translation block(s) (pre-existing ingestion defect).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

联合标定（Joint-Camera-IMU Calibration）是人形机器人多传感器融合中的关键前置方法，用于精确估计相机、LiDAR 与 IMU 之间的空间变换关系（外参）。在人形机器人中，相机提供密集纹理与语义信息，LiDAR 提供精确三维几何，IMU 提供高频运动状态，三者协同才能实现 RGB 点云着色、深度融合、目标 3D 定位等功能。它真正改变的不是单个传感器的标定精度，而是让异构传感器从"各自为政"走向"统一时空基准"的融合范式。

## 核心内容

### 是什么：准确定义

联合标定（Joint-Camera-IMU Calibration）是指在同一参考框架下，估计相机、LiDAR 与 IMU 三者之间刚体变换关系的过程。具体而言，需要求解相机与 LiDAR 之间的外参（旋转矩阵 $\mathbf{R}_{CL}$ 和平移向量 $\mathbf{t}_{CL}$）、相机与 IMU 之间的外参（$\mathbf{R}_{CI}, \mathbf{t}_{CI}$），以及 LiDAR 与 IMU 之间的外参（$\mathbf{R}_{LI}, \mathbf{t}_{LI}$）。这些外参构成一个闭环约束：$\mathbf{R}_{CL} = \mathbf{R}_{CI} \mathbf{R}_{IL}$，其中 $\mathbf{R}_{IL}$ 表示 IMU 到 LiDAR 的旋转。联合标定的核心思想是利用多传感器同步采集的数据，通过特征对应与非线性优化，同时求解所有外参，而非逐个独立标定。

**关键术语**：
- **外参（extrinsic parameters）**：两个传感器坐标系之间的旋转和平移。
- **重投影误差（reprojection error）**：LiDAR 点投影到图像后与对应图像特征之间的距离。
- **点云配准（point cloud registration）**：把多组点云对齐到同一坐标系的算法，如 ICP。
- **标定靶（calibration target）**：带有已知几何特征（棋盘格、圆孔、镂空图案）的板，用于提取对应特征。
- **手眼标定（hand-eye calibration）**：估计相机与机械臂末端之间变换关系的经典方法，联合标定是其多传感器推广。

### 为什么存在：痛点与历史定位

在多传感器融合系统出现之前，单一传感器（如仅用相机或仅用 LiDAR）的标定问题相对独立。但随着人形机器人需要同时依赖视觉、激光与惯性信息完成导航、避障与操作任务，传感器之间的空间对齐成为瓶颈。若外参不准，RGB 点云着色会出现颜色错位，深度融合会产生重影，目标 3D 定位会偏移数十厘米——这在人形机器人的精密操作场景中是致命的。

历史上，相机-LiDAR 标定经历了从**基于标定靶**到**无目标自动标定**的演进。早期方法依赖棋盘格或圆孔板，通过提取 3D-2D 对应点求解外参；后来发展出利用场景中自然特征（如边缘、平面）的自动标定方法。IMU 的加入进一步复杂化了问题：IMU 提供的是高频角速度与加速度，需要与视觉、激光的低频数据在时间与空间上同时对齐。联合标定正是为解决这种"多传感器、多模态、多频率"的对齐问题而生。

### 原理拆解

**① 坐标变换链：从 LiDAR 到像素**

设空间点在世界/标定靶坐标系下为 $\mathbf{P}_W$，在相机坐标系下为 $\mathbf{P}_C$，在 LiDAR 坐标系下为 $\mathbf{P}_L$。相机内参矩阵为 $\mathbf{K}$，相机-LiDAR 外参为 $(\mathbf{R}_{CL}, \mathbf{t}_{CL})$，则有：

$$
\mathbf{P}_C = \mathbf{R}_{CL} \mathbf{P}_L + \mathbf{t}_{CL}
$$

$$
\mathbf{p} = \mathbf{K} \mathbf{P}_C / Z_C
$$

其中 $\mathbf{p} = [u, v, 1]^T$ 为图像像素坐标，$Z_C$ 为相机坐标系下的深度。第一条公式建立了 LiDAR 到相机的刚体变换，第二条公式完成相机坐标系到像素坐标系的投影。联合标定的本质，就是通过最小化重投影误差来求解 $(\mathbf{R}_{CL}, \mathbf{t}_{CL})$。

**② 标定靶方法：3D-2D 对应的建立**

最常用的标定靶是棋盘格板或带圆孔的铝板。步骤如下：

1. **相机角点检测**：在图像中检测棋盘格角点，得到像素坐标 $\mathbf{p}_{ij}$。
2. **LiDAR 特征提取**：从 LiDAR 点云中提取标定靶平面或圆孔边缘，拟合得到 3D 坐标 $\mathbf{P}_{L,ij}$。
3. **建立对应**：将同一标定靶上的 3D 点与 2D 像素点配对，形成 3D-2D 对应集合。
4. **求解外参**：使用 PnP（Perspective-n-Point）算法获得初值，再用 ICP（Iterative Closest Point）或非线性优化（如 Levenberg-Marquardt）精化。

**③ IMU 的引入：时间与空间的双重对齐**

IMU 提供高频（通常 100–1000 Hz）的角速度与加速度，而相机与 LiDAR 通常工作在 10–30 Hz。联合标定需要解决两个问题：**时间同步**（将 IMU 数据插值到相机/LiDAR 采集时刻）与**空间对齐**（估计 IMU 与其他传感器的外参）。空间对齐通常利用运动激励：在标定过程中，操作者手持传感器平台做旋转与平移运动，IMU 积分得到的姿态变化与视觉/LiDAR 估计的姿态变化应一致，通过最小化两者差异求解外参。

### 关键参数与规格

| 参数 | 典型值/范围 | 说明 |
|------|------------|------|
| 相机分辨率 | 1280×720 – 4096×3072 | 影响角点检测精度 |
| LiDAR 线数 | 16 – 128 线 | 线数越高，点云越密，平面拟合越准 |
| IMU 采样率 | 100 – 1000 Hz | 越高越利于时间插值 |
| 标定靶尺寸 | 棋盘格边长 30 – 100 mm | 需覆盖相机视场与 LiDAR 扫描范围 |
| 重投影误差阈值 | < 1 像素（工程判断） | 超过则需重新标定 |
| 旋转误差阈值 | < 0.1°（工程判断） | 超过则需重新标定 |
| 平移误差阈值 | < 5 mm（工程判断） | 超过则需重新标定 |

### 横向对比

| 方法类型 | 代表方法 | 优点 | 缺点 |
|---------|---------|------|------|
| 基于标定靶 | 棋盘格 + PnP + ICP | 精度高、鲁棒性强 | 需要人工布置靶标，操作繁琐 |
| 无目标自动标定 | 边缘对齐、平面拟合 | 无需靶标，可在线标定 | 依赖场景特征，精度略低 |
| 基于运动的手眼标定 | AX = XB 求解 | 无需靶标，利用自然运动 | 需要充分激励，否则退化 |
| 联合标定（本方法） | 多传感器同步优化 | 同时求解所有外参，一致性高 | 计算复杂，需要精确时间同步 |

### 谁在用·应用案例

联合标定是人形机器人感知系统的标准前置流程，广泛应用于以下场景：

1. **RGB 点云着色**：将 LiDAR 点云投影到相机图像上，为每个 3D 点赋予 RGB 颜色。外参不准会导致颜色错位，影响后续语义分割与目标识别。
2. **深度融合**：将相机深度图与 LiDAR 点云融合，生成高密度、高精度的深度图，用于避障与导航。
3. **目标 3D 定位**：利用相机检测目标（如人、物体），结合 LiDAR 测距，实现目标的精确 3D 定位与跟踪。
4. **SLAM 与建图**：视觉-激光-惯性融合 SLAM（如 VINS-Fusion、LIO-SAM）依赖精确的外参初始化，联合标定是系统启动的第一步。
5. **人形机器人操作**：在抓取与操作任务中，需要将视觉识别的目标位置转换到机械臂坐标系，外参精度直接影响操作成功率。

### 局限与边界

1. **标定靶覆盖范围有限**：标定靶只能覆盖传感器视场的一部分，若传感器安装位置差异大（如相机朝前、LiDAR 朝上），可能需要多次摆放靶标。
2. **时间同步误差**：联合标定假设传感器数据已时间同步，若同步误差较大（如 > 10 ms），标定结果会显著退化。工程上需使用硬件同步或高精度软件同步。
3. **运动激励不足**：基于运动的方法需要充分的旋转与平移激励，若运动幅度过小或退化（如纯平移），外参的某些分量不可观测。
4. **环境敏感性**：无目标方法依赖场景中的边缘、平面等特征，在空旷或重复纹理环境中可能失效。
5. **标定结果漂移**：长期使用后，机械振动、热胀冷缩可能导致外参漂移，需要定期重新标定。

### 常见误区

1. **"标定一次就一劳永逸"**——错。人形机器人在运行中会经历振动、温度变化，外参可能漂移。工程上建议每次部署或定期（如每月）重新标定。
2. **"重投影误差越小越好"**——不完全对。重投影误差过小可能意味着过拟合（如标定靶摆放次数过多），反而降低泛化能力。工程判断：误差在 0.5–1 像素之间通常已足够。
3. **"联合标定就是手眼标定"**——错。手眼标定只解决相机与机械臂末端的关系，联合标定同时处理相机、LiDAR、IMU 三者，且涉及时间同步问题，复杂度更高。
4. **"IMU 外参可以忽略"**——错。IMU 外参不准会直接影响 VIO（视觉惯性里程计）与 LIO（激光惯性里程计）的精度，导致定位漂移。

### 相关知识

- `ent_method_forward_kinematics` — 正运动学将关节角映射到末端位姿，联合标定中的手眼标定（AX = XB）依赖正运动学提供机械臂末端位姿作为参考。
- `ent_method_forward_kinematics` — 正运动学与联合标定同属"装配集成与测试"领域，前者解决"内部状态到外部行为"的映射，后者解决"多传感器到统一坐标"的对齐，两者共同构成机器人感知-执行闭环的几何基础。

## 参考

- [Humanoid Robot Development Workflow](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Chapter 02: Knowledge Graph Schema](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)
- [Chapter 08: Kinematics and Dynamics](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)

## Overview

Joint-Camera-IMU Calibration is a key preliminary method in multi-sensor fusion for humanoid robots, used to precisely estimate the spatial transformation relationships (extrinsic parameters) between cameras, LiDAR, and IMUs. In humanoid robots, cameras provide dense texture and semantic information, LiDAR provides precise 3D geometry, and IMUs provide high-frequency motion states. Only when the three work together can functions such as RGB point cloud coloring, deep fusion, and 3D target localization be achieved. What it truly changes is not the calibration accuracy of a single sensor, but rather the fusion paradigm that moves heterogeneous sensors from "working independently" to a "unified spatiotemporal reference frame."

## Content

### What It Is: A Precise Definition

Joint-Camera-IMU Calibration refers to the process of estimating the rigid body transformation relationships among cameras, LiDAR, and IMUs within the same reference frame. Specifically, it requires solving for the extrinsic parameters between the camera and LiDAR (rotation matrix \(\mathbf{R}_{CL}\) and translation vector \(\mathbf{t}_{CL}\)), between the camera and IMU (\(\mathbf{R}_{CI}, \mathbf{t}_{CI}\)), and between the LiDAR and IMU (\(\mathbf{R}_{LI}, \mathbf{t}_{LI}\)). These extrinsics form a closed-loop constraint: \(\mathbf{R}_{CL} = \mathbf{R}_{CI} \mathbf{R}_{IL}\), where \(\mathbf{R}_{IL}\) represents the rotation from the IMU to the LiDAR. The core idea of joint calibration is to use data synchronously collected from multiple sensors and, through feature correspondence and nonlinear optimization, solve for all extrinsics simultaneously rather than calibrating each one independently.

**Key Terms**:
- **Extrinsic parameters**: The rotation and translation between two sensor coordinate systems.
- **Reprojection error**: The distance between a LiDAR point projected onto an image and its corresponding image feature.
- **Point cloud registration**: Algorithms that align multiple point clouds into the same coordinate system, such as ICP.
- **Calibration target**: A board with known geometric features (checkerboard, circular holes, cutout patterns) used to extract corresponding features.
- **Hand-eye calibration**: A classic method for estimating the transformation between a camera and a robot arm's end effector; joint calibration is its multi-sensor generalization.

### Why It Exists: Pain Points and Historical Context

Before multi-sensor fusion systems emerged, calibration problems for single sensors (e.g., camera-only or LiDAR-only) were relatively independent. However, as humanoid robots need to simultaneously rely on visual, laser, and inertial information for navigation, obstacle avoidance, and manipulation tasks, spatial alignment between sensors became a bottleneck. If extrinsics are inaccurate, RGB point cloud coloring suffers from color misalignment, deep fusion produces ghosting artifacts, and 3D target localization can drift by tens of centimeters—which is fatal in precision manipulation scenarios for humanoid robots.

Historically, camera-LiDAR calibration has evolved from **target-based** methods to **targetless automatic calibration**. Early methods relied on checkerboards or circular hole boards, solving extrinsics by extracting 3D-2D correspondences; later, automatic calibration methods using natural scene features (such as edges and planes) were developed. The addition of IMUs further complicated the problem: IMUs provide high-frequency angular velocity and acceleration, which need to be aligned both temporally and spatially with the lower-frequency data from cameras and LiDAR. Joint calibration was born precisely to address this "multi-sensor, multi-modal, multi-frequency" alignment problem.

### Principle Breakdown

**① Coordinate Transformation Chain: From LiDAR to Pixels**

Let a spatial point be \(\mathbf{P}_W\) in the world/calibration target coordinate system, \(\mathbf{P}_C\) in the camera coordinate system, and \(\mathbf{P}_L\) in the LiDAR coordinate system. The camera intrinsic matrix is \(\mathbf{K}\), and the camera-LiDAR extrinsics are \((\mathbf{R}_{CL}, \mathbf{t}_{CL})\). Then:

$$
\mathbf{P}_C = \mathbf{R}_{CL} \mathbf{P}_L + \mathbf{t}_{CL}
$$

$$
\mathbf{p} = \mathbf{K} \mathbf{P}_C / Z_C
$$

where \(\mathbf{p} = [u, v, 1]^T\) is the image pixel coordinate and \(Z_C\) is the depth in the camera coordinate system. The first equation establishes the rigid body transformation from LiDAR to camera, and the second completes the projection from the camera coordinate system to pixel coordinates. The essence of joint calibration is to solve for \((\mathbf{R}_{CL}, \mathbf{t}_{CL})\) by minimizing the reprojection error.

**② Calibration Target Method: Establishing 3D-2D Correspondences**

The most commonly used calibration targets are checkerboard boards or aluminum plates with circular holes. The steps are as follows:

1. **Camera corner detection**: Detect checkerboard corners in the image to obtain pixel coordinates \(\mathbf{p}_{ij}\).
2. **LiDAR feature extraction**: Extract the calibration target plane or circular hole edges from the LiDAR point cloud and fit to obtain 3D coordinates \(\mathbf{P}_{L,ij}\).
3. **Establish correspondences**: Pair 3D points and 2D pixel points on the same calibration target to form a set of 3D-2D correspondences.
4. **Solve for extrinsics**: Use the PnP (Perspective-n-Point) algorithm to obtain initial values, then refine with ICP (Iterative Closest Point) or nonlinear optimization (e.g., Levenberg-Marquardt).

**③ Introduction of IMU: Dual Alignment in Time and Space**

IMUs provide high-frequency (typically 100–1000 Hz) angular velocity and acceleration, while cameras and LiDAR typically operate at 10–30 Hz. Joint calibration needs to address two issues: **temporal synchronization** (interpolating IMU data to the camera/LiDAR acquisition timestamps) and **spatial alignment** (estimating extrinsics between the IMU and other sensors). Spatial alignment typically leverages motion excitation: during calibration, the operator moves the sensor platform with rotations and translations, and the attitude changes obtained by integrating IMU data should be consistent with those estimated from vision/LiDAR. The extrinsics are solved by minimizing the difference between the two.

### Key Parameters and Specifications

| Parameter | Typical Value/Range | Description |
|-----------|--------------------|-------------|
| Camera resolution | 1280×720 – 4096×3072 | Affects corner detection accuracy |
| LiDAR channels | 16 – 128 lines | More lines yield denser point clouds and more accurate plane fitting |
| IMU sampling rate | 100 – 1000 Hz | Higher rates facilitate temporal interpolation |
| Calibration target size | Checkerboard edge 30 – 100 mm | Must cover the camera field of view and LiDAR scanning range |
| Reprojection error threshold | < 1 pixel (engineering judgment) | Recalibration required if exceeded |
| Rotation error threshold | < 0.1° (engineering judgment) | Recalibration required if exceeded |
| Translation error threshold | < 5 mm (engineering judgment) | Recalibration required if exceeded |

### Horizontal Comparison

| Method Type | Representative Methods | Advantages | Disadvantages |
|------------|------------------------|------------|---------------|
| Target-based | Checkerboard + PnP + ICP | High accuracy, strong robustness | Requires manual target placement, cumbersome operation |
| Targetless automatic calibration | Edge alignment, plane fitting | No target needed, online calibration possible | Relies on scene features, slightly lower accuracy |
| Motion-based hand-eye calibration | AX = XB solving | No target needed, leverages natural motion | Requires sufficient excitation, otherwise degenerates |
| Joint calibration (this method) | Multi-sensor synchronous optimization | Solves all extrinsics simultaneously, high consistency | Computationally complex, requires precise temporal synchronization |

### Who Uses It: Application Cases

Joint calibration is a standard prerequisite in the perception systems of humanoid robots and is widely applied in the following scenarios:

1. **RGB point cloud coloring**: Project LiDAR point clouds onto camera images to assign RGB colors to each 3D point. Inaccurate extrinsics cause color misalignment, affecting subsequent semantic segmentation and object recognition.
2. **Deep fusion**: Fuse camera depth maps with LiDAR point clouds to generate high-density, high-precision depth maps for obstacle avoidance and navigation.
3. **3D target localization**: Use cameras to detect targets (e.g., people, objects) and combine with LiDAR ranging to achieve precise 3D localization and tracking of targets.
4. **SLAM and mapping**: Visual-laser-inertial fusion SLAM (e.g., VINS-Fusion, LIO-SAM) relies on accurate extrinsic initialization; joint calibration is the first step in system startup.
5. **Humanoid robot manipulation**: In grasping and manipulation tasks, target positions identified by vision need to be transformed into the robot arm coordinate system; extrinsic accuracy directly affects manipulation success rates.

### Limitations and Boundaries

1. **Limited calibration target coverage**: The calibration target can only cover a portion of the sensor field of view. If sensors are installed with large positional differences (e.g., camera facing forward, LiDAR facing upward), the target may need to be placed multiple times.
2. **Temporal synchronization errors**: Joint calibration assumes sensor data is temporally synchronized. If synchronization errors are large (e.g., > 10 ms), calibration results degrade significantly. Hardware synchronization or high-precision software synchronization is required in practice.
3. **Insufficient motion excitation**: Motion-based methods require sufficient rotational and translational excitation. If motion amplitude is too small or degenerate (e.g., pure translation), certain extrinsic components become unobservable.
4. **Environmental sensitivity**: Targetless methods rely on scene features such as edges and planes, which may fail in open spaces or environments with repetitive textures.
5. **Calibration drift**: Over long-term use, mechanical vibration and thermal expansion/contraction can cause extrinsic drift, requiring periodic recalibration.

### Common Misconceptions

1. **"Calibrate once and it's done forever"**—Wrong. Humanoid robots experience vibration and temperature changes during operation, and extrinsics may drift. In practice, recalibration is recommended at each deployment or periodically (e.g., monthly).
2. **"The smaller the reprojection error, the better"**—Not entirely true. An excessively small reprojection error may indicate overfitting (e.g., too many target placements), which reduces generalization. Engineering judgment: errors between 0.5–1 pixel are typically sufficient.
3. **"Joint calibration is just hand-eye calibration"**—Wrong. Hand-eye calibration only addresses the relationship between the camera and the robot arm's end effector, while joint calibration simultaneously handles the camera, LiDAR, and IMU, and also involves temporal synchronization, making it more complex.
4. **"IMU extrinsics can be ignored"**—Wrong. Inaccurate IMU extrinsics directly affect the accuracy of VIO (Visual-Inertial Odometry) and LIO (LiDAR-Inertial Odometry), leading to localization drift.

### Related Knowledge

- `ent_method_forward_kinematics` — Forward kinematics maps joint angles to end-effector poses. Hand-eye calibration (AX = XB) in joint calibration relies on forward kinematics to provide the robot arm's end-effector pose as a reference.
- `ent_method_forward_kinematics` — Forward kinematics and joint calibration both belong to the "assembly, integration, and testing" domain. The former addresses the mapping from "internal states to external behavior," while the latter addresses the alignment from "multiple sensors to a unified coordinate system." Together, they form the geometric foundation of the robot's perception-action loop.

## 개요

결합 캘리브레이션(Joint-Camera-IMU Calibration)은 휴머노이드 로봇의 다중 센서 융합에서 핵심적인 선행 방법으로, 카메라, LiDAR, IMU 간의 공간 변환 관계(외부 파라미터)를 정밀하게 추정하는 데 사용됩니다. 휴머노이드 로봇에서 카메라는 밀집된 텍스처와 의미 정보를 제공하고, LiDAR는 정밀한 3차원 기하 정보를 제공하며, IMU는 고주파 운동 상태를 제공합니다. 이 세 가지가 협력해야 RGB 포인트 클라우드 착색, 심층 융합, 목표 3D 위치 파악 등의 기능을 구현할 수 있습니다. 이것이 진정으로 바꾸는 것은 단일 센서의 캘리브레이션 정밀도가 아니라, 이기종 센서가 "각자 따로"에서 "통일된 시공간 기준"으로 나아가는 융합 패러다임입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

결합 캘리브레이션(Joint-Camera-IMU Calibration)은 동일한 기준 프레임 하에서 카메라, LiDAR, IMU 세 가지 간의 강체 변환 관계를 추정하는 과정을 의미합니다. 구체적으로, 카메라와 LiDAR 간의 외부 파라미터(회전 행렬 $\mathbf{R}_{CL}$ 및 평행 이동 벡터 $\mathbf{t}_{CL}$), 카메라와 IMU 간의 외부 파라미터($\mathbf{R}_{CI}, \mathbf{t}_{CI}$), 그리고 LiDAR와 IMU 간의 외부 파라미터($\mathbf{R}_{LI}, \mathbf{t}_{LI}$)를 풀어야 합니다. 이러한 외부 파라미터는 폐쇄 루프 제약을 구성합니다: $\mathbf{R}_{CL} = \mathbf{R}_{CI} \mathbf{R}_{IL}$, 여기서 $\mathbf{R}_{IL}$은 IMU에서 LiDAR로의 회전을 나타냅니다. 결합 캘리브레이션의 핵심 아이디어는 다중 센서 동기 수집 데이터를 활용하여 특징 대응과 비선형 최적화를 통해 모든 외부 파라미터를 동시에 푸는 것이지, 개별적으로 독립 캘리브레이션하는 것이 아닙니다.

**핵심 용어**:
- **외부 파라미터(extrinsic parameters)**: 두 센서 좌표계 간의 회전 및 평행 이동.
- **재투영 오차(reprojection error)**: LiDAR 포인트가 이미지에 투영된 후 해당 이미지 특징과의 거리.
- **포인트 클라우드 정합(point cloud registration)**: 여러 포인트 클라우드를 동일 좌표계로 정렬하는 알고리즘, 예: ICP.
- **캘리브레이션 타깃(calibration target)**: 알려진 기하학적 특징(체커보드, 원형 구멍, 절개 패턴)을 가진 판으로, 대응 특징 추출에 사용.
- **핸드-아이 캘리브레이션(hand-eye calibration)**: 카메라와 로봇 팔 끝단 간의 변환 관계를 추정하는 고전적 방법으로, 결합 캘리브레이션은 이를 다중 센서로 확장한 것입니다.

### 왜 존재하는가:痛点과 역사적 위치

다중 센서 융합 시스템이 등장하기 전에는 단일 센서(예: 카메라만 또는 LiDAR만)의 캘리브레이션 문제가 상대적으로 독립적이었습니다. 그러나 휴머노이드 로봇이 내비게이션, 장애물 회피, 조작 작업을 위해 시각, 레이저, 관성 정보를 동시에 의존해야 하면서 센서 간 공간 정렬이 병목이 되었습니다. 외부 파라미터가 부정확하면 RGB 포인트 클라우드 착색에 색상 어긋남이 발생하고, 심층 융합에 고스트 현상이 생기며, 목표 3D 위치 파악이 수십 센티미터 어긋날 수 있습니다—이는 휴머노이드 로봇의 정밀 조작 시나리오에서 치명적입니다.

역사적으로 카메라-LiDAR 캘리브레이션은 **캘리브레이션 타깃 기반**에서 **무타깃 자동 캘리브레이션**으로 진화해 왔습니다. 초기 방법은 체커보드나 원형 구멍 판에 의존하여 3D-2D 대응점을 추출해 외부 파라미터를 풀었고, 이후 장면의 자연 특징(예: 모서리, 평면)을 활용하는 자동 캘리브레이션 방법이 개발되었습니다. IMU의 추가는 문제를 더욱 복잡하게 만들었습니다: IMU는 고주파 각속도와 가속도를 제공하며, 시각 및 레이저의 저주파 데이터와 시간 및 공간에서 동시에 정렬되어야 합니다. 결합 캘리브레이션은 바로 이러한 "다중 센서, 다중 모달, 다중 주파수" 정렬 문제를 해결하기 위해 탄생했습니다.

### 원리 분해

**① 좌표 변환 체인: LiDAR에서 픽셀로**

공간 점이 세계/캘리브레이션 타깃 좌표계에서 $\mathbf{P}_W$, 카메라 좌표계에서 $\mathbf{P}_C$, LiDAR 좌표계에서 $\mathbf{P}_L$이라고 가정합니다. 카메라 내부 파라미터 행렬이 $\mathbf{K}$, 카메라-LiDAR 외부 파라미터가 $(\mathbf{R}_{CL}, \mathbf{t}_{CL})$이면:

$$
\mathbf{P}_C = \mathbf{R}_{CL} \mathbf{P}_L + \mathbf{t}_{CL}
$$

$$
\mathbf{p} = \mathbf{K} \mathbf{P}_C / Z_C
$$

여기서 $\mathbf{p} = [u, v, 1]^T$는 이미지 픽셀 좌표, $Z_C$는 카메라 좌표계에서의 깊이입니다. 첫 번째 공식은 LiDAR에서 카메라로의 강체 변환을 수립하고, 두 번째 공식은 카메라 좌표계에서 픽셀 좌표계로의 투영을 완성합니다. 결합 캘리브레이션의 본질은 재투영 오차를 최소화하여 $(\mathbf{R}_{CL}, \mathbf{t}_{CL})$를 푸는 것입니다.

**② 캘리브레이션 타깃 방법: 3D-2D 대응 구축**

가장 일반적인 캘리브레이션 타깃은 체커보드 판 또는 원형 구멍이 있는 알루미늄 판입니다. 절차는 다음과 같습니다:

1. **카메라 코너 검출**: 이미지에서 체커보드 코너를 검출하여 픽셀 좌표 $\mathbf{p}_{ij}$를 얻습니다.
2. **LiDAR 특징 추출**: LiDAR 포인트 클라우드에서 캘리브레이션 타깃 평면 또는 원형 구멍 모서리를 추출하여 3D 좌표 $\mathbf{P}_{L,ij}$를 피팅합니다.
3. **대응 구축**: 동일한 캘리브레이션 타깃의 3D 점과 2D 픽셀 점을 짝지어 3D-2D 대응 집합을 형성합니다.
4. **외부 파라미터 풀이**: PnP(Perspective-n-Point) 알고리즘으로 초기값을 얻은 후, ICP(Iterative Closest Point) 또는 비선형 최적화(예: Levenberg-Marquardt)로 정밀화합니다.

**③ IMU 도입: 시간과 공간의 이중 정렬**

IMU는 고주파(보통 100–1000 Hz)의 각속도와 가속도를 제공하며, 카메라와 LiDAR는 일반적으로 10–30 Hz로 작동합니다. 결합 캘리브레이션은 두 가지 문제를 해결해야 합니다: **시간 동기화**(IMU 데이터를 카메라/LiDAR 수집 시점으로 보간)와 **공간 정렬**(IMU와 다른 센서 간의 외부 파라미터 추정). 공간 정렬은 일반적으로 운동 자극을 활용합니다: 캘리브레이션 과정에서 조작자가 센서 플랫폼을 손에 들고 회전 및 평행 이동 운동을 수행하며, IMU 적분으로 얻은 자세 변화와 시각/LiDAR로 추정한 자세 변화가 일치해야 하며, 두 차이를 최소화하여 외부 파라미터를 풉니다.

### 핵심 파라미터 및 사양

| 파라미터 | 일반 값/범위 | 설명 |
|------|------------|------|
| 카메라 해상도 | 1280×720 – 4096×3072 | 코너 검출 정밀도에 영향 |
| LiDAR 라인 수 | 16 – 128 라인 | 라인 수가 높을수록 포인트 클라우드가 밀집되고 평면 피팅이 정확 |
| IMU 샘플링 레이트 | 100 – 1000 Hz | 높을수록 시간 보간에 유리 |
| 캘리브레이션 타깃 크기 | 체커보드 변 길이 30 – 100 mm | 카메라 시야와 LiDAR 스캔 범위를 커버해야 함 |
| 재투영 오차 임계값 | < 1 픽셀(공학적 판단) | 초과 시 재캘리브레이션 필요 |
| 회전 오차 임계값 | < 0.1°(공학적 판단) | 초과 시 재캘리브레이션 필요 |
| 평행 이동 오차 임계값 | < 5 mm(공학적 판단) | 초과 시 재캘리브레이션 필요 |

### 횡적 비교

| 방법 유형 | 대표 방법 | 장점 | 단점 |
|---------|---------|------|------|
| 캘리브레이션 타깃 기반 | 체커보드 + PnP + ICP | 정밀도 높음, 강건성 우수 | 타깃 수동 배치 필요, 절차 번거로움 |
| 무타깃 자동 캘리브레이션 | 모서리 정렬, 평면 피팅 | 타깃 불필요, 온라인 캘리브레이션 가능 | 장면 특징 의존, 정밀도 다소 낮음 |
| 운동 기반 핸드-아이 캘리브레이션 | AX = XB 풀이 | 타깃 불필요, 자연 운동 활용 | 충분한 자극 필요, 부족 시 퇴화 |
| 결합 캘리브레이션(본 방법) | 다중 센서 동기 최적화 | 모든 외부 파라미터 동시 해결, 일관성 높음 | 계산 복잡, 정밀한 시간 동기화 필요 |

### 누가 사용하는가·응용 사례

결합 캘리브레이션은 휴머노이드 로봇 인식 시스템의 표준 선행 절차로, 다음 시나리오에 널리 적용됩니다:

1. **RGB 포인트 클라우드 착색**: LiDAR 포인트 클라우드를 카메라 이미지에 투영하여 각 3D 점에 RGB 색상을 부여합니다. 외부 파라미터가 부정확하면 색상 어긋남이 발생하여 이후 의미 분할과 목표 인식에 영향을 줍니다.
2. **심층 융합**: 카메라 깊이 맵과 LiDAR 포인트 클라우드를 융합하여 고밀도, 고정밀 깊이 맵을 생성하고 장애물 회피와 내비게이션에 사용합니다.
3. **목표 3D 위치 파악**: 카메라로 목표(예: 사람, 물체)를 검출하고 LiDAR 거리 측정을 결합하여 목표의 정밀한 3D 위치 파악 및 추적을 구현합니다.
4. **SLAM 및 매핑**: 시각-레이저-관성 융합 SLAM(예: VINS-Fusion, LIO-SAM)은 정밀한 외부 파라미터 초기화에 의존하며, 결합 캘리브레이션은 시스템 시작의 첫 단계입니다.
5. **휴머노이드 로봇 조작**: 파지 및 조작 작업에서 시각으로 인식한 목표 위치를 로봇 팔 좌표계로 변환해야 하며, 외부 파라미터 정밀도가 조작 성공률에 직접 영향을 줍니다.

### 한계와 경계

1. **캘리브레이션 타깃 커버 범위 제한**: 캘리브레이션 타깃은 센서 시야의 일부만 커버할 수 있으며, 센서 설치 위치 차이가 크면(예: 카메라 전방, LiDAR 상방) 타깃을 여러 번 배치해야 할 수 있습니다.
2. **시간 동기화 오차**: 결합 캘리브레이션은 센서 데이터가 시간 동기화되었다고 가정하며, 동기화 오차가 크면(예: > 10 ms) 캘리브레이션 결과가 크게 저하됩니다. 공학적으로 하드웨어 동기화 또는 고정밀 소프트웨어 동기화가 필요합니다.
3. **운동 자극 부족**: 운동 기반 방법은 충분한 회전 및 평행 이동 자극이 필요하며, 운동 폭이 너무 작거나 퇴화되면(예: 순수 평행 이동) 외부 파라미터의 일부 성분이 관측 불가능합니다.
4. **환경 민감성**: 무타깃 방법은 장면의 모서리, 평면 등의 특징에 의존하며, 개방된 공간이나 반복 텍스처 환경에서는 실패할 수 있습니다.
5. **캘리브레이션 결과 드리프트**: 장기 사용 후 기계적 진동, 열팽창 수축으로 외부 파라미터가 드리프트할 수 있으며, 정기적인 재캘리브레이션이 필요합니다.

### 일반적인 오해

1. **"캘리브레이션 한 번이면 영구적이다"**——틀림. 휴머노이드 로봇은 운행 중 진동, 온도 변화를 겪으며 외부 파라미터가 드리프트할 수 있습니다. 공학적으로 배포 시마다 또는 정기적으로(예: 매월) 재캘리브레이션을 권장합니다.
2. **"재투영 오차가 작을수록 좋다"**——완전히 맞지 않음. 재투영 오차가 너무 작으면 과적합(예: 캘리브레이션 타깃 배치 횟수 과다)을 의미할 수 있으며, 오히려 일반화 능력이 저하됩니다. 공학적 판단: 오차가 0.5–1 픽셀 사이면 일반적으로 충분합니다.
3. **"결합 캘리브레이션은 핸드-아이 캘리브레이션이다"**——틀림. 핸드-아이 캘리브레이션은 카메라와 로봇 팔 끝단의 관계만 해결하며, 결합 캘리브레이션은 카메라, LiDAR, IMU 세 가지를 동시에 처리하고 시간 동기화 문제도 포함하므로 복잡도가 더 높습니다.
4. **"IMU 외부 파라미터는 무시해도 된다"**——틀림. IMU 외부 파라미터가 부정확하면 VIO(시각 관성 주행 거리 측정)와 LIO(레이저 관성 주행 거리 측정)의 정밀도에 직접 영향을 주어 위치 파악 드리프트를 유발합니다.

### 관련 지식

- `ent_method_forward_kinematics` — 정운동학은 관절 각도를 끝단 자세로 매핑하며, 결합 캘리브레이션의 핸드-아이 캘리브레이션(AX = XB)은 정운동학이 제공하는 로봇 팔 끝단 자세를 참조로 사용합니다.
- `ent_method_forward_kinematics` — 정운동학과 결합 캘리브레이션은 모두 "조립 통합 및 테스트" 영역에 속하며, 전자는 "내부 상태에서 외부 행동으로의 매핑"을 해결하고 후자는 "다중 센서에서 통일 좌표로의 정렬"을 해결하며, 둘은 함께 로봇 인식-실행 폐쇄 루프의 기하학적 기반을 구성합니다.
