---
$id: ent_process_p13_3_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Hardware emergency stop and safety chain
  zh: 硬件急停与安全链
  ko: 하드웨어 비상정지 및 안전 체인
summary:
  en: Safety system schematic, FMEA, emergency stop response time < target
  zh: '- P13.3.1.1 输入梳理与目标量化 - 整理「硬件急停与安全链」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 安全系统原理图、FMEA、急停响应时间 < 目标
domains:
- 02_components
- 08_software_middleware
layers:
- intelligence
- upstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

硬件急停与安全链（Hardware emergency stop and safety chain）是人形机器人电子电气与能源系统（P13）中的关键工序节点，其核心设计逻辑是：任何软件失效都能通过硬件切断动力，且整体设计符合功能安全要求。它真正改变的不是"装一个红色按钮"这件事，而是让"机器人在任何异常情况下都能被物理强制停止"成为一条不依赖软件正确性的独立保障路径。

## 核心内容

### 是什么：准确定义

硬件急停与安全链是一套独立于主控制系统的电气安全机制，由硬件急停开关、看门狗、熔断器、安全 PLC/继电器、双通道冗余等元件构成。它的职责是：当系统检测到异常（或操作员主动触发）时，通过纯硬件路径切断动力源，使机器人进入安全停止状态，且该路径不依赖主控软件是否正常运行。

在 WBS V3 的 P13.3.1 工序中，这一节点被展开为五个四级子任务：输入梳理与目标量化（P13.3.1.1）、方案/方法设计（P13.3.1.2）、实施/原型/样件制作（P13.3.1.3）、验证与问题闭环（P13.3.1.4）、文档输出与下游交付（P13.3.1.5）。每个子任务遵循统一的工程动作模板，确保安全链从需求到交付全程可追溯。

### 为什么存在：痛点与历史定位

人形机器人的安全设计面临一个根本矛盾：**软件越复杂，失效模式越多，但安全要求反而越高**。早期机器人开发中，急停功能往往被简单实现为一个"软件里读个按键、然后发指令停机"的逻辑——这带来三类系统性痛点：

**第一，软件失效即安全失效。** 如果主控芯片死机、操作系统卡死或通信总线中断，软件急停逻辑根本无法执行。此时机器人可能带着动力继续运动，造成人身伤害或设备损坏。

**第二，单点故障缺乏冗余。** 一条急停回路中若只有一个继电器、一根线缆，任何一个元件失效都会导致急停功能整体丧失。这在功能安全标准中是不可接受的。

**第三，响应时间不可控。** 软件急停的延迟取决于 CPU 负载、中断优先级和任务调度，最坏情况下的响应时间难以保证。而硬件安全链的响应时间由继电器动作时间、接触器断开时间等物理参数决定，是确定且可测量的。

硬件急停与安全链的历史定位，正是为了解决上述痛点而出现的**独立于主控的"最后一道防线"**。它不替代软件层面的安全功能（如碰撞检测、力矩限制），而是在软件全部失效时仍然能够物理切断动力。

### 原理拆解

**① 安全链的基本拓扑：串联即安全**

硬件安全链的核心拓扑是**串联回路**：急停按钮、安全继电器、接触器线圈、看门狗输出等元件串联在一条回路中。正常工作时，所有触点闭合，接触器吸合，动力电源接通；任意一个元件触发（按钮按下、看门狗超时、继电器断开），回路断开，接触器释放，动力电源被切断。

$$
V_{\text{coil}} = V_{\text{supply}} \cdot \prod_{i=1}^{n} S_i
$$

其中 $$S_i \in \{0, 1\}$$ 表示第 i 个安全元件的状态（1 为闭合/正常，0 为断开/触发）。只要任意一个 $$S_i = 0$$，线圈电压 $$V_{\text{coil}} = 0$$，接触器释放。这就是"串联即安全"的物理本质——**安全链的可靠性不取决于软件逻辑，而取决于每个串联元件的物理可靠性**。

**② 双通道冗余：单点失效不导致功能丧失**

功能安全要求安全链具备冗余能力。双通道设计将安全回路分为两个独立通道（如两套急停按钮触点、两套安全继电器），两个通道的信号在安全 PLC 中进行"与"逻辑判断。只有当两个通道都触发时，才执行切断；若仅一个通道触发，系统进入降级状态并报警，但不会误停机。

**③ 看门狗：软件失效的硬件哨兵**

看门狗（Watchdog）是连接软件与硬件安全链的桥梁。主控软件周期性"喂狗"（输出脉冲信号）；若软件死机或任务卡死，喂狗信号停止，看门狗定时器超时，其输出触点断开，直接触发安全链切断动力。这实现了"软件失效→硬件动作"的自动转换。

### 关键参数与规格

| 参数 | 含义 | 设计要求 |
|------|------|----------|
| 响应时间 | 从触发到动力完全切断的时间 | 急停响应时间 < 目标值（需自行确认具体数值） |
| 可靠性 | 安全链在需要时正确动作的概率 | 需通过 FMEA 评估并满足功能安全等级 |
| 误触发率 | 无异常情况下安全链误动作的概率 | 越低越好，需在可靠性与可用性间平衡 |
| 通道数 | 冗余通道数量 | 至少双通道（工程判断） |
| 触点容量 | 急停按钮/继电器触点允许的电流电压 | 需匹配动力回路参数（需自行确认） |

### 横向对比

| 方案 | 响应速度 | 可靠性 | 成本 | 适用场景 |
|------|----------|--------|------|----------|
| 纯软件急停 | 慢（依赖 CPU 调度） | 低（软件失效即失效） | 低 | 非安全关键场景 |
| 硬件急停 + 单继电器 | 快（毫秒级） | 中（单点故障风险） | 中 | 低风险设备 |
| 硬件急停 + 安全 PLC/双通道 | 快（毫秒级） | 高（冗余设计） | 高 | 人形机器人等安全关键系统 |
| 硬件急停 + 熔断器 | 慢（取决于过流程度） | 中（仅保护过流） | 低 | 作为补充保护 |

### 谁在用·应用案例

在 P13 电子电气与能源系统阶段，硬件急停与安全链是每个工作包都必须覆盖的横切关注点。具体应用场景包括：

- **关节模组**：每个关节驱动器的动力输入端接入安全链接触器，急停时所有关节同时断电，机器人失去动力自然下坠（或由制动器保持位置）。
- **电池管理系统（BMS）**：BMS 的故障输出（如过温、过流）接入安全链，电池主回路继电器可在异常时切断。
- **整机调试**：调试人员在机器人旁手持急停按钮，一旦出现异常运动趋势立即按下，硬件回路在毫秒级切断动力。

### 局限与边界

硬件急停与安全链并非万能，其局限包括：

**第一，只能切断动力，不能保证姿态。** 急停后机器人可能因重力倒下，需要额外的机械制动或坠落保护措施。

**第二，响应时间有物理下限。** 接触器机械动作时间、继电器响应时间决定了急停不可能"瞬时"完成，对于高速运动部件可能不够快。

**第三，误触发是设计权衡。** 过于灵敏的安全链会导致频繁误停机，影响可用性；过于迟钝则失去保护意义。这一权衡需要在 P13.3.1.2 方案设计阶段通过评估矩阵量化打分解决。

**第四，不能替代软件安全功能。** 安全链是"最后防线"，不是"唯一防线"。碰撞检测、力矩限制、速度监控等软件安全功能仍然必要，二者是互补关系。

### 常见误区

1. **"急停按钮装了就安全了"**——错。急停按钮只是安全链的一个元件，如果后续的继电器、接触器、线缆没有冗余设计，整条链仍然可能单点失效。安全链的可靠性取决于**最薄弱的串联环节**。

2. **"看门狗是软件功能，不算硬件安全"**——错。看门狗虽然由软件"喂狗"，但其超时检测和触点输出是纯硬件实现，是连接软件失效与硬件切断的关键桥梁。

3. **"响应时间越快越好"**——不完全对。响应时间需要与误触发率权衡。过于激进的看门狗超时设置可能导致正常工况下的误停机，反而降低系统可用性。

4. **"安全链设计一次就完事"**——错。P13.3.1.4 验证与问题闭环要求制定测试/评审计划与通过准则，执行测试并记录原始数据，输出问题清单与改进措施。安全链需要持续验证和迭代。

### 相关知识

- `ent_process_p13` — 硬件急停与安全链是 P13 电子电气与能源系统阶段的三级子任务之一，承接上游架构定义并向下游软件集成交付安全接口。
- `ent_method_fmea` — FMEA 是安全链设计的关键分析工具，用于识别每个安全元件的失效模式、评估风险优先级并驱动改进措施。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Hardware emergency stop and safety chain is a critical process node in the humanoid robot electronics and energy systems (P13). Its core design logic is: any software failure can be cut off through hardware, and the overall design meets functional safety requirements. What it truly changes is not "installing a red button," but making "the robot can be physically forced to stop under any abnormal condition" an independent safeguard path that does not rely on software correctness.

## Content

### What It Is: Precise Definition

The hardware emergency stop and safety chain is an electrical safety mechanism independent of the main control system, composed of hardware emergency stop switches, watchdogs, fuses, safety PLCs/relays, dual-channel redundancy, and other components. Its responsibility is: when the system detects an anomaly (or the operator actively triggers it), it cuts off the power source through a purely hardware path, causing the robot to enter a safe stop state, and this path does not depend on whether the main control software is running normally.

In the P13.3.1 process of WBS V3, this node is expanded into five level-4 subtasks: input review and target quantification (P13.3.1.1), solution/method design (P13.3.1.2), implementation/prototype/sample fabrication (P13.3.1.3), verification and issue closure (P13.3.1.4), and documentation output and downstream delivery (P13.3.1.5). Each subtask follows a unified engineering action template, ensuring full traceability of the safety chain from requirements to delivery.

### Why It Exists: Pain Points and Historical Positioning

The safety design of humanoid robots faces a fundamental contradiction: **the more complex the software, the more failure modes there are, but the higher the safety requirements become**. In early robot development, the emergency stop function was often simply implemented as "read a button in software, then send a command to stop"—this led to three systemic pain points:

**First, software failure means safety failure.** If the main control chip crashes, the operating system freezes, or the communication bus is interrupted, the software emergency stop logic cannot execute at all. At this point, the robot may continue moving with power, causing personal injury or equipment damage.

**Second, single-point failures lack redundancy.** If an emergency stop circuit has only one relay and one cable, the failure of any single component will cause the entire emergency stop function to be lost. This is unacceptable under functional safety standards.

**Third, response time is uncontrollable.** The latency of a software emergency stop depends on CPU load, interrupt priority, and task scheduling, making the worst-case response time difficult to guarantee. In contrast, the response time of a hardware safety chain is determined by physical parameters such as relay actuation time and contactor opening time, which are deterministic and measurable.

The historical positioning of the hardware emergency stop and safety chain is precisely to address the above pain points as an **independent "last line of defense" separate from the main controller**. It does not replace software-level safety functions (such as collision detection, torque limiting), but rather ensures that power can still be physically cut off when all software fails.

### Principle Breakdown

**① Basic Topology of the Safety Chain: Series Equals Safety**

The core topology of the hardware safety chain is a **series circuit**: emergency stop buttons, safety relays, contactor coils, watchdog outputs, and other components are connected in series in a single loop. During normal operation, all contacts are closed, the contactor is energized, and the power supply is connected; when any component triggers (button pressed, watchdog timeout, relay opened), the loop is broken, the contactor releases, and the power supply is cut off.

$$
V_{\text{coil}} = V_{\text{supply}} \cdot \prod_{i=1}^{n} S_i
$$

where $$S_i \in \{0, 1\}$$ represents the state of the i-th safety component (1 = closed/normal, 0 = open/triggered). If any $$S_i = 0$$, the coil voltage $$V_{\text{coil}} = 0$$, and the contactor releases. This is the physical essence of "series equals safety"—**the reliability of the safety chain does not depend on software logic, but on the physical reliability of each series component**.

**② Dual-Channel Redundancy: Single-Point Failure Does Not Cause Loss of Function**

Functional safety requires the safety chain to have redundancy capability. The dual-channel design divides the safety circuit into two independent channels (e.g., two sets of emergency stop button contacts, two sets of safety relays), and the signals from both channels are evaluated with an "AND" logic in the safety PLC. Power is cut off only when both channels are triggered; if only one channel is triggered, the system enters a degraded state and alarms, but does not cause a false shutdown.

**③ Watchdog: The Hardware Sentinel for Software Failure**

The watchdog is the bridge connecting software and the hardware safety chain. The main control software periodically "feeds the dog" (outputs a pulse signal); if the software crashes or a task hangs, the feed signal stops, the watchdog timer times out, its output contact opens, and it directly triggers the safety chain to cut off power. This achieves the automatic conversion of "software failure → hardware action."

### Key Parameters and Specifications

| Parameter | Meaning | Design Requirement |
|-----------|---------|--------------------|
| Response time | Time from trigger to complete power cutoff | Emergency stop response time < target value (specific value to be confirmed by yourself) |
| Reliability | Probability that the safety chain operates correctly when needed | Must be assessed via FMEA and meet the functional safety level |
| False trigger rate | Probability of safety chain false activation under no abnormal conditions | The lower the better; balance between reliability and availability is required |
| Number of channels | Number of redundant channels | At least dual-channel (engineering judgment) |
| Contact rating | Allowable current and voltage of emergency stop button/relay contacts | Must match the power circuit parameters (to be confirmed by yourself) |

### Horizontal Comparison

| Solution | Response Speed | Reliability | Cost | Applicable Scenario |
|----------|----------------|-------------|------|---------------------|
| Pure software emergency stop | Slow (depends on CPU scheduling) | Low (fails when software fails) | Low | Non-safety-critical scenarios |
| Hardware emergency stop + single relay | Fast (millisecond-level) | Medium (single-point failure risk) | Medium | Low-risk equipment |
| Hardware emergency stop + safety PLC/dual-channel | Fast (millisecond-level) | High (redundant design) | High | Safety-critical systems such as humanoid robots |
| Hardware emergency stop + fuse | Slow (depends on overcurrent level) | Medium (protects only against overcurrent) | Low | As supplementary protection |

### Who Uses It: Application Cases

In the P13 electronics and energy systems phase, the hardware emergency stop and safety chain is a cross-cutting concern that every work package must cover. Specific application scenarios include:

- **Joint modules**: The power input of each joint driver is connected to the safety chain contactor; during an emergency stop, all joints are de-energized simultaneously, and the robot loses power and falls naturally (or is held in position by brakes).
- **Battery management system (BMS)**: The BMS fault outputs (such as over-temperature, overcurrent) are connected to the safety chain, allowing the battery main circuit relay to be cut off under abnormal conditions.
- **Whole-machine debugging**: Debugging personnel hold an emergency stop button beside the robot; once an abnormal motion trend appears, they press it immediately, and the hardware circuit cuts off power within milliseconds.

### Limitations and Boundaries

The hardware emergency stop and safety chain is not a panacea; its limitations include:

**First, it can only cut off power, not guarantee posture.** After an emergency stop, the robot may fall due to gravity, requiring additional mechanical braking or fall protection measures.

**Second, response time has a physical lower limit.** The mechanical actuation time of the contactor and the response time of the relay determine that an emergency stop cannot be "instantaneous," which may not be fast enough for high-speed moving parts.

**Third, false triggering is a design trade-off.** An overly sensitive safety chain will cause frequent false shutdowns, reducing availability; an overly sluggish one loses its protective purpose. This trade-off must be resolved through quantitative scoring with an evaluation matrix during the P13.3.1.2 solution design phase.

**Fourth, it cannot replace software safety functions.** The safety chain is the "last line of defense," not the "only line of defense." Software safety functions such as collision detection, torque limiting, and speed monitoring remain necessary; the two are complementary.

### Common Misconceptions

1. **"Installing an emergency stop button means it's safe"**—Wrong. The emergency stop button is only one component of the safety chain. If the subsequent relays, contactors, and cables lack redundant design, the entire chain can still fail at a single point. The reliability of the safety chain depends on the **weakest series link**.

2. **"The watchdog is a software function, not hardware safety"**—Wrong. Although the watchdog is "fed" by software, its timeout detection and contact output are purely hardware implementations, serving as the key bridge connecting software failure to hardware cutoff.

3. **"The faster the response time, the better"**—Not entirely correct. Response time must be weighed against the false trigger rate. Overly aggressive watchdog timeout settings can cause false shutdowns under normal operating conditions, reducing system availability.

4. **"Safety chain design is done once and for all"**—Wrong. P13.3.1.4 verification and issue closure requires formulating test/review plans and acceptance criteria, executing tests and recording raw data, and outputting an issue list and improvement measures. The safety chain requires continuous verification and iteration.

### Related Knowledge

- `ent_process_p13` — The hardware emergency stop and safety chain is one of the level-3 subtasks in the P13 electronics and energy systems phase, receiving upstream architecture definitions and delivering safety interfaces downstream to software integration.
- `ent_method_fmea` — FMEA is a key analysis tool for safety chain design, used to identify failure modes of each safety component, assess risk priorities, and drive improvement measures.

## 개요

하드웨어 비상정지 및 안전 체인(Hardware emergency stop and safety chain)은 휴머노이드 로봇의 전자전기 및 에너지 시스템(P13)에서 핵심 공정 노드이며, 핵심 설계 논리는 다음과 같습니다: 어떤 소프트웨어 오류가 발생하더라도 하드웨어를 통해 동력을 차단할 수 있고, 전체 설계가 기능 안전 요구사항을 충족한다는 것입니다. 이것이 실제로 바꾸는 것은 "빨간 버튼을 설치하는 것"이 아니라, "로봇이 어떤 비정상 상황에서도 물리적으로 강제 정지될 수 있도록 하는 것"을 소프트웨어 정확성에 의존하지 않는 독립적인 보장 경로로 만드는 것입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

하드웨어 비상정지 및 안전 체인은 주 제어 시스템과 독립된 전기 안전 메커니즘으로, 하드웨어 비상정지 스위치, 워치독, 퓨즈, 안전 PLC/릴레이, 이중 채널 리던던시 등의 요소로 구성됩니다. 그 역할은 시스템이 이상을 감지하거나(또는 운영자가 능동적으로 트리거) 순수 하드웨어 경로를 통해 동력원을 차단하여 로봇을 안전 정지 상태로 진입시키는 것이며, 이 경로는 주 제어 소프트웨어의 정상 작동 여부에 의존하지 않습니다.

WBS V3의 P13.3.1 공정에서 이 노드는 5개의 4단계 하위 작업으로 전개됩니다: 입력 정리 및 목표 정량화(P13.3.1.1), 방안/방법 설계(P13.3.1.2), 구현/프로토타입/시제품 제작(P13.3.1.3), 검증 및 문제 종결(P13.3.1.4), 문서 출력 및 하위 전달(P13.3.1.5). 각 하위 작업은 통일된 엔지니어링 작업 템플릿을 따르며, 안전 체인이 요구사항부터 납품까지 전체 과정이 추적 가능하도록 보장합니다.

### 왜 존재하는가:痛点과 역사적 위치

휴머노이드 로봇의 안전 설계는 근본적인 모순에 직면합니다: **소프트웨어가 복잡할수록 고장 모드가 많아지지만, 안전 요구사항은 오히려 더 높아집니다.** 초기 로봇 개발에서 비상정지 기능은 종종 "소프트웨어에서 버튼을 읽고 명령을 내려 정지시키는" 단순한 로직으로 구현되었으며, 이는 세 가지 유형의 체계적인痛点을 초래했습니다:

**첫째, 소프트웨어 고장이 곧 안전 고장입니다.** 주 제어 칩이 다운되거나, 운영체제가 멈추거나, 통신 버스가 중단되면 소프트웨어 비상정지 로직은 실행될 수 없습니다. 이때 로봇은 동력을 유지한 채 계속 움직여 인명 피해나 장비 손상을 초래할 수 있습니다.

**둘째, 단일 지점 고장에 대한 리던던시가 부족합니다.** 비상정지 회로에 릴레이 하나, 케이블 하나만 있다면 어떤 요소 하나가 고장 나도 비상정지 기능 전체가 상실됩니다. 이는 기능 안전 표준에서 허용되지 않습니다.

**셋째, 응답 시간을 제어할 수 없습니다.** 소프트웨어 비상정지의 지연은 CPU 부하, 인터럽트 우선순위, 작업 스케줄링에 따라 달라지며, 최악의 경우 응답 시간을 보장하기 어렵습니다. 반면 하드웨어 안전 체인의 응답 시간은 릴레이 동작 시간, 접촉기 차단 시간 등의 물리적 매개변수에 의해 결정되며, 결정적이고 측정 가능합니다.

하드웨어 비상정지 및 안전 체인의 역사적 위치는 바로 위의痛点을 해결하기 위해 등장한 **주 제어와 독립된 "최후의 방어선"** 입니다. 이는 소프트웨어 수준의 안전 기능(충돌 감지, 토크 제한 등)을 대체하는 것이 아니라, 소프트웨어가 모두 고장 났을 때에도 물리적으로 동력을 차단할 수 있게 합니다.

### 원리 분석

**① 안전 체인의 기본 토폴로지: 직렬이 곧 안전**

하드웨어 안전 체인의 핵심 토폴로지는 **직렬 회로**입니다: 비상정지 버튼, 안전 릴레이, 접촉기 코일, 워치독 출력 등의 요소가 하나의 회로에 직렬로 연결됩니다. 정상 작동 시 모든 접점이 닫히고 접촉기가 흡인되어 동력 전원이 연결됩니다; 어떤 요소 하나가 트리거되면(버튼 누름, 워치독 타임아웃, 릴레이 차단) 회로가 끊기고 접촉기가 해제되어 동력 전원이 차단됩니다.

$$
V_{\text{coil}} = V_{\text{supply}} \cdot \prod_{i=1}^{n} S_i
$$

여기서 $$S_i \in \{0, 1\}$$는 i번째 안전 요소의 상태를 나타냅니다(1은 닫힘/정상, 0은 열림/트리거). 어떤 $$S_i = 0$$이 하나라도 있으면 코일 전압 $$V_{\text{coil}} = 0$$이 되어 접촉기가 해제됩니다. 이것이 "직렬이 곧 안전"의 물리적 본질입니다 — **안전 체인의 신뢰성은 소프트웨어 로직이 아니라 각 직렬 요소의 물리적 신뢰성에 달려 있습니다.**

**② 이중 채널 리던던시: 단일 지점 고장이 기능 상실로 이어지지 않음**

기능 안전은 안전 체인에 리던던시 능력을 요구합니다. 이중 채널 설계는 안전 회로를 두 개의 독립된 채널(예: 두 세트의 비상정지 버튼 접점, 두 세트의 안전 릴레이)로 나누고, 두 채널의 신호는 안전 PLC에서 "AND" 로직으로 판단합니다. 두 채널이 모두 트리거될 때만 차단을 실행합니다; 하나의 채널만 트리거되면 시스템은 성능 저하 상태로 전환되고 경보를 발생시키지만, 오작동 정지는 발생하지 않습니다.

**③ 워치독: 소프트웨어 고장을 감시하는 하드웨어 센티널**

워치독(Watchdog)은 소프트웨어와 하드웨어 안전 체인을 연결하는 브리지입니다. 주 제어 소프트웨어는 주기적으로 "개밥 주기"(펄스 신호 출력)를 수행합니다; 소프트웨어가 다운되거나 작업이 멈추면 피드 신호가 중단되고, 워치독 타이머가 타임아웃되어 출력 접점이 열리며, 안전 체인을 직접 트리거하여 동력을 차단합니다. 이는 "소프트웨어 고장 → 하드웨어 동작"의 자동 변환을 실현합니다.

### 핵심 매개변수 및 사양

| 매개변수 | 의미 | 설계 요구사항 |
|------|------|----------|
| 응답 시간 | 트리거부터 동력 완전 차단까지의 시간 | 비상정지 응답 시간 < 목표값(구체적 수치는 직접 확인 필요) |
| 신뢰성 | 안전 체인이 필요 시 올바르게 동작할 확률 | FMEA 평가를 통해 기능 안전 등급 충족 필요 |
| 오작동 트리거율 | 이상 없음에도 안전 체인이 오작동할 확률 | 낮을수록 좋으며, 신뢰성과 가용성 간 균형 필요 |
| 채널 수 | 리던던시 채널 수 | 최소 이중 채널(엔지니어링 판단) |
| 접점 용량 | 비상정지 버튼/릴레이 접점이 허용하는 전류·전압 | 동력 회로 매개변수와 일치 필요(직접 확인 필요) |

### 횡적 비교

| 방안 | 응답 속도 | 신뢰성 | 비용 | 적용 시나리오 |
|------|----------|--------|------|----------|
| 순수 소프트웨어 비상정지 | 느림(CPU 스케줄링 의존) | 낮음(소프트웨어 고장 시 즉시 고장) | 낮음 | 안전 비핵심 시나리오 |
| 하드웨어 비상정지 + 단일 릴레이 | 빠름(밀리초 수준) | 중간(단일 지점 고장 위험) | 중간 | 저위험 장비 |
| 하드웨어 비상정지 + 안전 PLC/이중 채널 | 빠름(밀리초 수준) | 높음(리던던시 설계) | 높음 | 휴머노이드 로봇 등 안전 핵심 시스템 |
| 하드웨어 비상정지 + 퓨즈 | 느림(과전류 정도에 따라 다름) | 중간(과전류만 보호) | 낮음 | 보조 보호 수단 |

### 누가 사용하는가·적용 사례

P13 전자전기 및 에너지 시스템 단계에서 하드웨어 비상정지 및 안전 체인은 모든 작업 패키지가 반드시 포함해야 하는 횡단 관심사입니다. 구체적인 적용 시나리오는 다음과 같습니다:

- **관절 모듈**: 각 관절 드라이버의 동력 입력단에 안전 체인 접촉기를 연결하여, 비상정지 시 모든 관절이 동시에 전원이 차단되고 로봇은 동력을 잃고 자연스럽게 낙하합니다(또는 브레이크가 위치를 유지).
- **배터리 관리 시스템(BMS)**: BMS의 고장 출력(과열, 과전류 등)을 안전 체인에 연결하여, 배터리 주 회로 릴레이가 이상 시 차단될 수 있습니다.
- **전체 기계 디버깅**: 디버깅 인력이 로봇 옆에서 휴대용 비상정지 버튼을 들고, 비정상적인 움직임 경향이 발생하면 즉시 눌러 하드웨어 회로가 밀리초 수준에서 동력을 차단합니다.

### 한계와 경계

하드웨어 비상정지 및 안전 체인은 만능이 아니며, 그 한계는 다음과 같습니다:

**첫째, 동력만 차단할 수 있고 자세는 보장할 수 없습니다.** 비상정지 후 로봇은 중력으로 인해 넘어질 수 있으므로, 추가적인 기계적 브레이크나 낙하 보호 조치가 필요합니다.

**둘째, 응답 시간에는 물리적 하한이 있습니다.** 접촉기의 기계적 동작 시간, 릴레이 응답 시간으로 인해 비상정지는 "순간적"으로 완료될 수 없으며, 고속 운동 부품에는 충분히 빠르지 않을 수 있습니다.

**셋째, 오작동 트리거는 설계 트레이드오프입니다.** 너무 민감한 안전 체인은 잦은 오작동 정지를 유발하여 가용성을 떨어뜨리고, 너무 둔감하면 보호 의미를 잃습니다. 이 트레이드오프는 P13.3.1.2 방안 설계 단계에서 평가 매트릭스를 통해 정량적으로 점수를 매겨 해결해야 합니다.

**넷째, 소프트웨어 안전 기능을 대체할 수 없습니다.** 안전 체인은 "최후의 방어선"이지 "유일한 방어선"이 아닙니다. 충돌 감지, 토크 제한, 속도 모니터링 등의 소프트웨어 안전 기능은 여전히 필요하며, 둘은 상호 보완 관계입니다.

### 흔한 오해

1. **"비상정지 버튼을 설치하면 안전하다"** — 틀렸습니다. 비상정지 버튼은 안전 체인의 한 요소일 뿐이며, 이후의 릴레이, 접촉기, 케이블에 리던던시 설계가 없다면 전체 체인은 여전히 단일 지점 고장이 발생할 수 있습니다. 안전 체인의 신뢰성은 **가장 취약한 직렬 연결 고리**에 달려 있습니다.

2. **"워치독은 소프트웨어 기능이므로 하드웨어 안전이 아니다"** — 틀렸습니다. 워치독은 소프트웨어가 "개밥을 주지만" 그 타임아웃 감지와 접점 출력은 순수 하드웨어 구현이며, 소프트웨어 고장과 하드웨어 차단을 연결하는 핵심 브리지입니다.

3. **"응답 시간이 빠를수록 좋다"** — 완전히 맞지는 않습니다. 응답 시간은 오작동 트리거율과 균형을 이루어야 합니다. 너무 공격적인 워치독 타임아웃 설정은 정상 작동 조건에서 오작동 정지를 유발하여 오히려 시스템 가용성을 떨어뜨릴 수 있습니다.

4. **"안전 체인 설계는 한 번으로 끝난다"** — 틀렸습니다. P13.3.1.4 검증 및 문제 종결은 테스트/검토 계획과 통과 기준을 수립하고, 테스트를 실행하며 원시 데이터를 기록하고, 문제 목록과 개선 조치를 출력하도록 요구합니다. 안전 체인은 지속적인 검증과 반복이 필요합니다.

### 관련 지식

- `ent_process_p13` — 하드웨어 비상정지 및 안전 체인은 P13 전자전기 및 에너지 시스템 단계의 3단계 하위 작업 중 하나로, 상위 아키텍처 정의를 이어받아 하위 소프트웨어 통합에 안전 인터페이스를 전달합니다.
- `ent_method_fmea` — FMEA는 안전 체인 설계의 핵심 분석 도구로, 각 안전 요소의 고장 모드를 식별하고, 위험 우선순위를 평가하며, 개선 조치를 도출하는 데 사용됩니다.
