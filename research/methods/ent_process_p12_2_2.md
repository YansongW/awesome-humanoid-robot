---
$id: ent_process_p12_2_2
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Data Collection and Data Pipelines
  zh: 数据收集与数据流水线
  ko: 데이터 수집 및 데이터 파이프라인
summary:
  en: Dataset size, data quality assessment, and version control
  zh: '- P12.2.2.1 输入梳理与目标量化 - 整理「数据收集与数据流水线」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 数据集规模、数据质量检查、版本管理
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

数据收集与数据流水线（Data Collection and Data Pipelines）是 VLA / WAM / AI 算法集成阶段（P12）中编号为 P12.2.2 的路线图工序节点，属于方法（method）类型，归属领域为 `07_ai_models_algorithms`，价值链层级为 `intelligence`。它定义了从原始数据采集到可训练数据集交付的完整工程链路，涵盖遥操作、仿真数据、互联网视频、数据清洗与增强四大方法工具，以及输入梳理、方案设计、样机实现、结果校核、文档交付五个标准化工序动作。它真正改变的不是数据量的堆积方式，而是让数据从"采集到的素材"变成"可验收、可追溯、可复现的工程资产"。

## 核心内容

### 是什么：准确定义

数据收集与数据流水线（P12.2.2）是 VLA/WAM 模型开发中负责数据侧工程化的工序节点。它不是一个具体的数据集，而是一套将原始数据转化为高质量训练语料的流程规范。该节点在 WBS V3 中展开为五个三级子任务：P12.2.2.1 输入梳理与目标量化、P12.2.2.2 方案/方法设计、P12.2.2.3 建模/仿真与样机实现、P12.2.2.4 仿真结果校核与优化、P12.2.2.5 文档输出与下游交付。

每个子任务遵循统一的四级关键动作模板，形成"梳理→设计→实现→验证→交付"的闭环。其方法工具箱明确包含四类：**遥操作**（Teleoperation）、**仿真数据**（Simulation Data）、**互联网视频**（Internet Videos）、**数据清洗与增强**（Data Cleaning and Augmentation）。这四类方法分别对应真实物理交互数据、虚拟环境生成数据、人类行为先验数据、以及数据质量治理手段。

### 为什么存在：痛点与历史定位

VLA/WAM 模型的训练依赖高质量、多样化的机器人数据，这是该工序节点存在的根本动因。在人形机器人产品化进程中，数据侧面临三重结构性矛盾：

**第一，数据稀缺性矛盾。** 与互联网文本或图像数据不同，机器人操作数据无法通过爬虫大规模获取。每一次真实遥操作采集都需要物理机器人、操作员、场景布置的完整配合，单条数据成本远高于文本标注。仿真数据虽然可以大规模生成，但存在 sim-to-real 迁移的领域差距问题。互联网视频虽然海量，但缺乏机器人可执行的动作标签和力觉信息。

**第二，质量与规模的权衡矛盾。** 数据量不足导致模型泛化能力差，数据量过大则引入噪声和冗余。数据清洗与增强环节正是为了在有限预算下最大化数据的信息密度，而非简单追求数量。

**第三，工程可追溯性矛盾。** 在实验室环境中，数据采集往往是"采完即用"，缺乏版本管理、质量检查和需求追溯。但在产品开发流程中，数据集必须像软件代码一样接受版本控制、变更管理和下游依赖通知。P12.2.2 的五个子任务正是将这种工程纪律制度化。

从历史定位看，该节点处于 P12 阶段的中游位置：它接收上游感知栈（P12.1）的传感器配置与标定信息作为输入，同时为下游 VLA 模型训练（P12.2.3 及后续）提供数据供给。它真正改变的不是数据采集的自动化程度，而是让数据从"研究者的个人资产"变成"组织的可复用工程资产"。

### 原理拆解

**① 输入梳理与目标量化（P12.2.2.1）**

该子任务的核心动作有三项：列出所有上游输入清单并确认版本、将验收标准转化为可量化 KPI、建立任务 Owner、时间节点与风险登记。

上游输入清单包括：感知栈的传感器选型文档、标定参数、场景定义文件、以及 P12 阶段整体的需求规格书。确认版本的意义在于：数据采集必须与感知配置的特定版本绑定，否则采集到的数据无法与后续模型输入对齐。将验收标准转化为可量化 KPI 的典型做法是：将"数据集质量良好"转化为"标注准确率 ≥ 需自行确认%、无效样本率 ≤ 需自行确认%、覆盖场景数 ≥ 需自行确认 个"等可测量指标。建立 Owner 与里程碑则是将责任落实到人，避免数据工作"人人有责、无人负责"。

**② 方案/方法设计（P12.2.2.2）**

该子任务要求形成不少于 2 个候选方案，建立评估矩阵并量化打分，组织评审并冻结方案。候选方案通常围绕四类数据来源的不同组合展开：

| 方案维度 | 遥操作为主 | 仿真为主 | 混合方案 |
|---------|-----------|---------|---------|
| 数据真实性 | 最高 | 较低 | 中等 |
| 采集成本 | 最高 | 最低 | 中等 |
| 场景覆盖 | 有限 | 广泛 | 较广 |
| 标注难度 | 较低（天然有动作标签） | 中等（需自动标注） | 中等 |
| 规模上限 | 低（受物理机时限制） | 高（可并行生成） | 中高 |

评估矩阵的量化打分通常从数据质量、成本、时间、风险四个维度进行加权评分。评审并冻结方案的意义在于：数据采集一旦大规模启动，中途更换方案将导致已采集数据作废，因此方案冻结是控制风险的关键节点。

**③ 建模/仿真与样机实现（P12.2.2.3）**

该子任务要求建立仿真/数学模型或原型样机，执行计算或实验，记录关键参数与边界条件。对于数据流水线而言，"样机"可以是：一套遥操作采集系统（含主手、从手、力反馈设备）、一个仿真数据生成器（基于物理引擎的域随机化配置）、或一条数据清洗流水线原型。关键参数包括：采集频率、分辨率、力觉采样率、场景光照条件、物体材质属性等。边界条件则包括：机器人关节限位、操作空间范围、传感器量程等。

**④ 仿真结果校核与优化（P12.2.2.4）**

该子任务要求校核仿真结果与理论/试验数据的一致性，识别薄弱点并迭代优化。典型校核方式包括：将仿真生成的数据与少量真实遥操作数据对比分布差异；检查数据增强后的样本是否保持物理合理性（如物体不穿透、力觉不突变）；验证清洗规则是否误删有效样本。薄弱点识别后进入迭代循环：调整仿真参数、优化增强策略、修正清洗规则。

**⑤ 文档输出与下游交付（P12.2.2.5）**

该子任务要求输出最终报告/图纸/规范，更新 ICD/BOM/SOP/需求追溯链，完成向下游环节的正式交付。具体动作包括：按模板编写文档并引用原始数据、完成内部评审与版本控制、发布并通知下游依赖方。这里的"文档"不仅是数据集说明，还包括数据采集 SOP（标准作业程序）、数据格式规范、质量检查报告、版本变更记录。需求追溯链确保每条数据需求都能追溯到具体的采集批次和清洗规则。

### 关键参数与规格

数据流水线的关键参数分布在四个层面：

**采集层参数**：遥操作采集频率（通常需自行确认 Hz）、力觉采样率（需自行确认 Hz）、图像分辨率（需自行确认 像素）、同步精度（需自行确认 ms）。仿真数据生成参数包括：物理引擎步长（需自行确认 s）、域随机化范围（需自行确认）、场景数量（需自行确认 个）。

**质量层参数**：标注准确率（需自行确认 %）、无效样本率（需自行确认 %）、重复率（需自行确认 %）、场景覆盖率（需自行确认 %）。这些参数构成 P12.2.2.1 中 KPI 的具体指标。

**规模层参数**：数据集总样本数（需自行确认 条）、有效样本数（需自行确认 条）、类别均衡度（需自行确认）、时间跨度（需自行确认 天）。

**版本层参数**：数据集版本号（语义化版本）、变更记录条数（需自行确认 条）、下游依赖方数量（需自行确认 个）。

### 横向对比

将 P12.2.2 与相邻工序节点对比，可以更清晰地定位其边界：

| 对比维度 | P12.2.2 数据收集与数据流水线 | P12.1 感知栈 | P12.2.3 模型训练（推断） |
|---------|---------------------------|-------------|------------------------|
| 核心产出 | 可训练数据集 | 传感器配置与标定参数 | 训练好的模型权重 |
| 主要方法 | 遥操作、仿真、清洗增强 | 标定、SLAM、检测跟踪 | 监督学习、强化学习 |
| 交付物形式 | 数据集 + 文档 + SOP | 配置文件 + 标定报告 | 模型文件 + 评估报告 |
| 质量衡量 | 数据质量指标 | 标定精度指标 | 任务成功率 |
| 迭代周期 | 周级 | 天级 | 月级 |

与 P12.2.2 最接近的邻居是 P12.2.1（假设为数据需求分析，需自行确认），前者定义"需要什么数据"，后者解决"如何拿到并处理好这些数据"。

### 谁在用·应用案例

**案例一：双臂操作任务的遥操作数据采集。** 某团队需要训练 VLA 模型完成"双手协作搬运"任务。按照 P12.2.2.1，他们首先梳理上游输入：感知栈提供的双臂关节角度范围、末端执行器力觉量程、工作空间尺寸。随后在 P12.2.2.2 阶段形成两个候选方案：方案 A 为纯遥操作采集（预计需自行确认 小时），方案 B 为遥操作 + 仿真数据混合（预计需自行确认 小时）。评估矩阵显示方案 B 在成本维度得分更高，最终冻结方案 B。P12.2.2.3 阶段搭建遥操作采集台，记录关键参数：采集频率需自行确认 Hz、力觉采样率需自行确认 Hz。P12.2.2.4 阶段将仿真数据与真实数据混合后检查分布差异，发现仿真数据中物体摩擦系数与真实差异较大，迭代调整后通过校核。P12.2.2.5 阶段输出数据集 v1.0，附带数据说明文档和清洗规则 SOP，并通知下游模型训练团队。

**案例二：互联网视频数据的清洗流水线。** 某团队希望利用互联网视频补充人类操作先验。P12.2.2.1 阶段将"视频可用率"转化为 KPI：可用视频占比 ≥ 需自行确认 %。P12.2.2.2 阶段设计清洗流水线方案：方案 A 为纯人工筛选（成本高），方案 B 为自动预筛 + 人工复核（成本适中）。评估后选择方案 B。P12.2.2.3 阶段实现自动预筛原型：基于场景分类器过滤无关视频，基于动作检测器提取操作片段。P12.2.2.4 阶段校核发现预筛误删率需自行确认 %，通过调整分类器阈值优化至可接受范围。P12.2.2.5 阶段交付清洗后的视频数据集，附带标注文件和清洗日志。

### 局限与边界

**数据采集成本边界。** 遥操作采集受限于物理机器人数量和操作员工时，单日有效数据量存在硬上限。当项目需要百万级样本时，纯遥操作方案在成本上不可行，必须依赖仿真数据或互联网视频补充。这是 P12.2.2.2 方案评估中必须量化的约束。

**隐私与合规边界。** 互联网视频涉及人物肖像权、场景隐私、平台使用条款等法律问题。数据清洗环节必须包含人脸模糊、场景脱敏、来源合规审查等动作。该边界在 P12.2.2.1 的 KPI 设定中应体现为"合规率 100%"的硬性指标。

**标注质量边界。** 无论是人工标注还是自动标注，都存在错误率。标注质量直接影响下游模型训练效果，但过度追求标注准确率会显著推高成本。工程判断：标注准确率目标通常设定在需自行确认 % 至需自行确认 % 之间，具体值取决于任务难度和模型容错能力。

**仿真与真实的领域差距。** 仿真数据虽然成本低、规模大，但物理引擎的近似特性导致数据分布与真实世界存在偏差。P12.2.2.4 的校核环节正是为了量化并缩小这一差距，但无法完全消除。对于力觉敏感任务（如精密装配），仿真数据的可信度需谨慎评估。

### 常见误区

1. **"数据越多越好"**——错。数据规模只是 KPI 之一，质量、多样性、标注准确性同等重要。盲目扩充数据量可能引入大量冗余样本，反而稀释有效信息密度。P12.2.2.1 的 KPI 设定应同时包含规模指标和质量指标。

2. **"仿真数据可以完全替代真实数据"**——错。仿真数据在覆盖度和成本上有优势，但 sim-to-real 差距始终存在。工程判断：对于接触丰富的操作任务，真实数据占比不应低于需自行确认 %，否则模型在真实环境中的表现可能显著退化。

3. **"数据清洗只是过滤掉坏样本"**——错。数据清洗还包括去重、对齐、标注修正、增强生成等正向操作。P12.2.2.3 中的"记录异常与偏差"动作表明，清洗过程本身也是发现数据问题、改进采集方案的重要反馈渠道。

4. **"数据集交付后工作就结束了"**——错。P12.2.2.5 要求"发布并通知下游依赖方"，但下游使用过程中可能发现新的数据问题，需要版本迭代。数据流水线是持续运营的系统，而非一次性交付的静态产物。

### 相关知识

- `ent_process_p12` — 本节点所属的 VLA / WAM / AI 算法集成阶段，定义了五步法工序模板和整体验收标准。
- `ent_process_p12_2_1` — 同族相邻工序：VLA 模型选型与微调（同一工作包内的并行工序）。
- `ent_process_p12_2_3` — 同族相邻工序：技能库与任务规划（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Data Collection and Data Pipelines is a roadmap process node numbered P12.2.2 within the VLA / WAM / AI algorithm integration phase (P12). It is of the method type, belongs to the domain `07_ai_models_algorithms`, and operates at the `intelligence` value chain level. It defines the complete engineering pipeline from raw data acquisition to deliverable training datasets, encompassing four major methodological tools—teleoperation, simulation data, internet videos, and data cleaning and augmentation—as well as five standardized process actions: input review, solution design, prototype implementation, result verification, and documentation delivery. What it truly changes is not how data volume is accumulated, but rather how data transforms from "collected material" into "acceptable, traceable, and reproducible engineering assets."

## Content

### What It Is: Precise Definition

Data Collection and Data Pipelines (P12.2.2) is the process node responsible for data-side engineering in VLA/WAM model development. It is not a specific dataset, but rather a set of procedural specifications that convert raw data into high-quality training corpora. In WBS V3, this node expands into five third-level subtasks: P12.2.2.1 Input Review and Target Quantification, P12.2.2.2 Solution/Method Design, P12.2.2.3 Modeling/Simulation and Prototype Implementation, P12.2.2.4 Simulation Result Verification and Optimization, and P12.2.2.5 Documentation Output and Downstream Delivery.

Each subtask follows a unified four-level key action template, forming a closed loop of "Review → Design → Implement → Verify → Deliver." The method toolbox explicitly includes four categories: **Teleoperation**, **Simulation Data**, **Internet Videos**, and **Data Cleaning and Augmentation**. These four methods correspond respectively to real physical interaction data, virtual environment-generated data, human behavior prior data, and data quality governance measures.

### Why It Exists: Pain Points and Historical Positioning

VLA/WAM model training relies on high-quality, diverse robot data, which is the fundamental driver for this process node. In the productization of humanoid robots, the data side faces three structural contradictions:

**First, data scarcity.** Unlike internet text or image data, robot manipulation data cannot be obtained at scale through web crawling. Each real teleoperation collection session requires the full coordination of a physical robot, an operator, and scene setup, making the per-sample cost far higher than text annotation. While simulation data can be generated at scale, it suffers from the domain gap in sim-to-real transfer. Internet videos are abundant but lack robot-executable action labels and force feedback information.

**Second, the quality-versus-scale trade-off.** Insufficient data leads to poor model generalization, while excessive data introduces noise and redundancy. The data cleaning and augmentation stage exists precisely to maximize the information density of data under a limited budget, rather than simply pursuing quantity.

**Third, engineering traceability.** In laboratory settings, data collection is often "collect-and-use," lacking version management, quality checks, and requirement traceability. However, in product development processes, datasets must undergo version control, change management, and downstream dependency notifications just like software code. The five subtasks of P12.2.2 institutionalize this engineering discipline.

From a historical positioning perspective, this node sits in the middle of Phase P12: it receives sensor configuration and calibration information from the upstream perception stack (P12.1) as input, while providing data supply to downstream VLA model training (P12.2.3 and beyond). What it truly changes is not the degree of automation in data collection, but rather the transformation of data from "a researcher's personal asset" into "an organization's reusable engineering asset."

### Principle Breakdown

**① Input Review and Target Quantification (P12.2.2.1)**

This subtask has three core actions: list all upstream input items and confirm versions, convert acceptance criteria into quantifiable KPIs, and establish task owners, milestones, and risk registers.

The upstream input list includes: sensor selection documentation from the perception stack, calibration parameters, scene definition files, and the overall requirements specification for Phase P12. Confirming versions matters because data collection must be bound to a specific version of the perception configuration; otherwise, the collected data cannot align with downstream model inputs. A typical approach to converting acceptance criteria into quantifiable KPIs is transforming "dataset quality is good" into measurable metrics such as "annotation accuracy ≥ [to be confirmed]%, invalid sample rate ≤ [to be confirmed]%, covered scene count ≥ [to be confirmed]." Establishing owners and milestones assigns responsibility to specific individuals, avoiding the situation where data work is "everyone's responsibility but no one's."

**② Solution/Method Design (P12.2.2.2)**

This subtask requires forming no fewer than 2 candidate solutions, establishing an evaluation matrix with quantitative scoring, organizing reviews, and freezing the chosen solution. Candidate solutions typically revolve around different combinations of the four data source categories:

| Solution Dimension | Teleoperation-Dominant | Simulation-Dominant | Hybrid Solution |
|---------|-----------|---------|---------|
| Data Authenticity | Highest | Lower | Medium |
| Collection Cost | Highest | Lowest | Medium |
| Scene Coverage | Limited | Extensive | Relatively Broad |
| Annotation Difficulty | Lower (naturally has action labels) | Medium (requires auto-annotation) | Medium |
| Scale Ceiling | Low (limited by physical machine hours) | High (can be generated in parallel) | Medium-High |

The quantitative scoring in the evaluation matrix typically uses weighted scores across four dimensions: data quality, cost, time, and risk. The significance of reviewing and freezing the solution lies in the fact that once large-scale data collection begins, switching approaches midway would invalidate already-collected data; therefore, solution freezing is a critical risk control point.

**③ Modeling/Simulation and Prototype Implementation (P12.2.2.3)**

This subtask requires establishing simulation/mathematical models or prototype systems, executing computations or experiments, and recording key parameters and boundary conditions. For data pipelines, the "prototype" can be: a teleoperation collection system (including master hand, slave hand, and force feedback devices), a simulation data generator (domain randomization configuration based on a physics engine), or a data cleaning pipeline prototype. Key parameters include: collection frequency, resolution, force feedback sampling rate, scene lighting conditions, object material properties, etc. Boundary conditions include: robot joint limits, operational space range, sensor measurement ranges, etc.

**④ Simulation Result Verification and Optimization (P12.2.2.4)**

This subtask requires verifying the consistency between simulation results and theoretical/experimental data, identifying weak points, and iterating for optimization. Typical verification methods include: comparing distribution differences between simulation-generated data and a small amount of real teleoperation data; checking whether augmented samples maintain physical plausibility (e.g., objects do not penetrate, force feedback does not jump abruptly); and verifying whether cleaning rules mistakenly remove valid samples. After identifying weak points, the process enters an iterative loop: adjusting simulation parameters, optimizing augmentation strategies, and correcting cleaning rules.

**⑤ Documentation Output and Downstream Delivery (P12.2.2.5)**

This subtask requires outputting final reports/drawings/specifications, updating the ICD/BOM/SOP/requirement traceability chain, and completing formal delivery to downstream stages. Specific actions include: writing documentation according to templates with references to raw data, completing internal reviews and version control, and publishing and notifying downstream dependent parties. The "documentation" here is not just dataset descriptions, but also includes data collection SOPs (Standard Operating Procedures), data format specifications, quality inspection reports, and version change records. The requirement traceability chain ensures that every data requirement can be traced back to specific collection batches and cleaning rules.

### Key Parameters and Specifications

Key parameters for the data pipeline are distributed across four levels:

**Collection-Level Parameters:** Teleoperation collection frequency (typically [to be confirmed] Hz), force feedback sampling rate ([to be confirmed] Hz), image resolution ([to be confirmed] pixels), synchronization precision ([to be confirmed] ms). Simulation data generation parameters include: physics engine step size ([to be confirmed] s), domain randomization range ([to be confirmed]), and scene count ([to be confirmed]).

**Quality-Level Parameters:** Annotation accuracy ([to be confirmed] %), invalid sample rate ([to be confirmed] %), duplication rate ([to be confirmed] %), scene coverage rate ([to be confirmed] %). These parameters constitute the specific metrics for the KPIs defined in P12.2.2.1.

**Scale-Level Parameters:** Total dataset sample count ([to be confirmed]), valid sample count ([to be confirmed]), class balance ([to be confirmed]), and time span ([to be confirmed] days).

**Version-Level Parameters:** Dataset version number (semantic versioning), number of change records ([to be confirmed]), and number of downstream dependent parties ([to be confirmed]).

### Horizontal Comparison

Comparing P12.2.2 with adjacent process nodes clarifies its boundaries:

| Comparison Dimension | P12.2.2 Data Collection and Data Pipelines | P12.1 Perception Stack | P12.2.3 Model Training (Inference) |
|---------|---------------------------|-------------|------------------------|
| Core Output | Trainable dataset | Sensor configuration and calibration parameters | Trained model weights |
| Primary Methods | Teleoperation, simulation, cleaning and augmentation | Calibration, SLAM, detection and tracking | Supervised learning, reinforcement learning |
| Deliverable Form | Dataset + documentation + SOP | Configuration files + calibration reports | Model files + evaluation reports |
| Quality Measurement | Data quality metrics | Calibration accuracy metrics | Task success rate |
| Iteration Cycle | Weekly | Daily | Monthly |

The closest neighbor to P12.2.2 is P12.2.1 (assumed to be data requirements analysis, to be confirmed), where the former defines "what data is needed" and the latter addresses "how to obtain and process this data properly."

### Who Uses It · Application Cases

**Case 1: Teleoperation data collection for dual-arm manipulation tasks.** A team needs to train a VLA model for a "dual-arm collaborative transport" task. Following P12.2.2.1, they first review upstream inputs: dual-arm joint angle ranges, end-effector force sensing ranges, and workspace dimensions provided by the perception stack. Then, in the P12.2.2.2 stage, they form two candidate solutions: Solution A is pure teleoperation collection (estimated [to be confirmed] hours), and Solution B is a hybrid of teleoperation and simulation data (estimated [to be confirmed] hours). The evaluation matrix shows Solution B scores higher on the cost dimension, so Solution B is ultimately frozen. In the P12.2.2.3 stage, they set up the teleoperation collection station and record key parameters: collection frequency [to be confirmed] Hz, force feedback sampling rate [to be confirmed] Hz. In the P12.2.2.4 stage, they mix simulation data with real data and check distribution differences, finding that the object friction coefficient in simulation data differs significantly from reality; after iterative adjustments, verification passes. In the P12.2.2.5 stage, they deliver dataset v1.0, along with a data description document and cleaning rule SOP, and notify the downstream model training team.

**Case 2: Cleaning pipeline for internet video data.** A team wants to use internet videos to supplement human manipulation priors. In the P12.2.2.1 stage, they convert "video usability rate" into a KPI: usable video proportion ≥ [to be confirmed]%. In the P12.2.2.2 stage, they design cleaning pipeline solutions: Solution A is purely manual screening (high cost), and Solution B is automatic pre-screening plus manual review (moderate cost). After evaluation, Solution B is selected. In the P12.2.2.3 stage, they implement an automatic pre-screening prototype: a scene classifier filters out irrelevant videos, and an action detector extracts manipulation segments. In the P12.2.2.4 stage, verification reveals a pre-screening false-removal rate of [to be confirmed]%, which is optimized to an acceptable range by adjusting classifier thresholds. In the P12.2.2.5 stage, they deliver the cleaned video dataset, along with annotation files and cleaning logs.

### Limitations and Boundaries

**Data collection cost boundary.** Teleoperation collection is constrained by the number of physical robots and operator hours, placing a hard upper limit on daily valid data volume. When a project requires millions of samples, a pure teleoperation approach is cost-infeasible and must rely on simulation data or internet videos as supplements. This is a constraint that must be quantified in the solution evaluation of P12.2.2.2.

**Privacy and compliance boundary.** Internet videos involve legal issues such as portrait rights, scene privacy, and platform terms of use. The data cleaning stage must include actions such as face blurring, scene desensitization, and source compliance review. This boundary should be reflected in the KPI settings of P12.2.2.1 as a hard requirement of "100% compliance rate."

**Annotation quality boundary.** Whether manual or automatic, annotation always carries an error rate. Annotation quality directly affects downstream model training performance, but excessively pursuing annotation accuracy significantly drives up costs. Engineering judgment: annotation accuracy targets are typically set between [to be confirmed]% and [to be confirmed]%, with specific values depending on task difficulty and model fault tolerance.

**Simulation-to-real domain gap.** Although simulation data is low-cost and large-scale, the approximate nature of physics engines causes distribution deviations from the real world. The verification stage of P12.2.2.4 exists precisely to quantify and narrow this gap, but it cannot be completely eliminated. For force-sensitive tasks (such as precision assembly), the credibility of simulation data requires careful assessment.

### Common Misconceptions

1. **"More data is always better"** — Incorrect. Data scale is only one KPI; quality, diversity, and annotation accuracy are equally important. Blindly expanding data volume may introduce large amounts of redundant samples, diluting effective information density. The KPI settings in P12.2.2.1 should include both scale and quality metrics.

2. **"Simulation data can fully replace real data"** — Incorrect. Simulation data has advantages in coverage and cost, but the sim-to-real gap always exists. Engineering judgment: for contact-rich manipulation tasks, the proportion of real data should not fall below [to be confirmed]%, otherwise model performance in real environments may degrade significantly.

3. **"Data cleaning is just filtering out bad samples"** — Incorrect. Data cleaning also includes positive operations such as deduplication, alignment, annotation correction, and augmentation generation. The "record anomalies and deviations" action in P12.2.2.3 indicates that the cleaning process itself is an important feedback channel for discovering data issues and improving collection solutions.

4. **"The work ends once the dataset is delivered"** — Incorrect. P12.2.2.5 requires "publishing and notifying downstream dependent parties," but downstream users may discover new data issues during use, necessitating version iterations. The data pipeline is a continuously operated system, not a one-time static deliverable.

### Related Knowledge

- `ent_process_p12` — The VLA / WAM / AI algorithm integration phase to which this node belongs, defining the five-step process template and overall acceptance criteria.
- `ent_process_p12_2_1` — Adjacent process in the same family: VLA model selection and fine-tuning (a parallel process within the same work package).
- `ent_process_p12_2_3` — Adjacent process in the same family: skill library and task planning (a parallel process within the same work package).

## 개요

데이터 수집 및 데이터 파이프라인(Data Collection and Data Pipelines)은 VLA / WAM / AI 알고리즘 통합 단계(P12)에서 번호가 P12.2.2인 로드맵 공정 노드로, 메서드(method) 유형에 속하며, 소속 도메인은 `07_ai_models_algorithms`, 가치 사슬 계층은 `intelligence`입니다. 이는 원시 데이터 수집부터 훈련 가능한 데이터셋 전달까지의 완전한 엔지니어링 체인을 정의하며, 원격 조작, 시뮬레이션 데이터, 인터넷 비디오, 데이터 정제 및 증강의 네 가지 방법 도구와 입력 정리, 방안 설계, 시제품 구현, 결과 검증, 문서 전달의 다섯 가지 표준화 공정 동작을 포함합니다. 이것이 실제로 바꾸는 것은 데이터 양의 축적 방식이 아니라, 데이터를 '수집된 소재'에서 '검수 가능하고, 추적 가능하며, 재현 가능한 엔지니어링 자산'으로 만드는 것입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

데이터 수집 및 데이터 파이프라인(P12.2.2)은 VLA/WAM 모델 개발에서 데이터 측 엔지니어링을 담당하는 공정 노드입니다. 이는 특정 데이터셋이 아니라, 원시 데이터를 고품질 훈련 코퍼스로 변환하는 일련의 프로세스 규범입니다. 이 노드는 WBS V3에서 다섯 개의 3차 하위 작업으로 전개됩니다: P12.2.2.1 입력 정리 및 목표 정량화, P12.2.2.2 방안/방법 설계, P12.2.2.3 모델링/시뮬레이션 및 시제품 구현, P12.2.2.4 시뮬레이션 결과 검증 및 최적화, P12.2.2.5 문서 출력 및 하위 전달.

각 하위 작업은 통일된 4단계 핵심 동작 템플릿을 따르며, '정리→설계→구현→검증→전달'의 폐루프를 형성합니다. 그 방법 도구 상자에는 명확히 네 가지 유형이 포함됩니다: **원격 조작**(Teleoperation), **시뮬레이션 데이터**(Simulation Data), **인터넷 비디오**(Internet Videos), **데이터 정제 및 증강**(Data Cleaning and Augmentation). 이 네 가지 방법은 각각 실제 물리적 상호작용 데이터, 가상 환경 생성 데이터, 인간 행동 사전 데이터, 그리고 데이터 품질 관리 수단에 해당합니다.

### 왜 존재하는가:痛点 및 역사적 위치

VLA/WAM 모델의 훈련은 고품질의 다양한 로봇 데이터에 의존하며, 이것이 이 공정 노드가 존재하는 근본 동인입니다. 휴머노이드 로봇 제품화 과정에서 데이터 측은 세 가지 구조적 모순에 직면합니다:

**첫째, 데이터 희소성 모순.** 인터넷 텍스트나 이미지 데이터와 달리, 로봇 조작 데이터는 크롤러를 통해 대규모로 획득할 수 없습니다. 실제 원격 조작 수집은 매번 물리적 로봇, 운영자, 장면 배치의 완전한 협력이 필요하며, 단일 데이터 비용은 텍스트 라벨링보다 훨씬 높습니다. 시뮬레이션 데이터는 대규모로 생성할 수 있지만, sim-to-real 전이의 도메인 격차 문제가 존재합니다. 인터넷 비디오는 방대하지만, 로봇이 실행 가능한 동작 라벨과 힘 감각 정보가 부족합니다.

**둘째, 품질과 규모의 균형 모순.** 데이터 양이 부족하면 모델 일반화 능력이 떨어지고, 데이터 양이 과도하면 노이즈와 중복이 유입됩니다. 데이터 정제 및 증강 단계는 바로 제한된 예산 내에서 데이터의 정보 밀도를 최대화하기 위한 것이지, 단순히 수량을 추구하는 것이 아닙니다.

**셋째, 엔지니어링 추적 가능성 모순.** 실험실 환경에서 데이터 수집은 종종 '수집 후 바로 사용'이며, 버전 관리, 품질 검사, 요구사항 추적이 부족합니다. 그러나 제품 개발 프로세스에서 데이터셋은 소프트웨어 코드처럼 버전 제어, 변경 관리, 하위 의존성 통지를 받아야 합니다. P12.2.2의 다섯 하위 작업은 바로 이러한 엔지니어링 규율을 제도화합니다.

역사적 위치에서 보면, 이 노드는 P12 단계의 중류에 위치합니다: 상류 인식 스택(P12.1)의 센서 구성 및 캘리브레이션 정보를 입력으로 받아들이고, 하류 VLA 모델 훈련(P12.2.3 및 이후)에 데이터를 공급합니다. 이것이 실제로 바꾸는 것은 데이터 수집의 자동화 수준이 아니라, 데이터를 '연구자의 개인 자산'에서 '조직의 재사용 가능한 엔지니어링 자산'으로 만드는 것입니다.

### 원리 분해

**① 입력 정리 및 목표 정량화(P12.2.2.1)**

이 하위 작업의 핵심 동작은 세 가지입니다: 모든 상류 입력 목록을 나열하고 버전을 확인하며, 검수 기준을 정량화 가능한 KPI로 전환하고, 작업 Owner, 시간 노드 및 위험 등록을 수립합니다.

상류 입력 목록에는 인식 스택의 센서 선정 문서, 캘리브레이션 파라미터, 장면 정의 파일, 그리고 P12 단계 전체의 요구사항 명세서가 포함됩니다. 버전 확인의 의미는 데이터 수집이 인식 구성의 특정 버전과 바인딩되어야 하며, 그렇지 않으면 수집된 데이터가 이후 모델 입력과 정렬되지 않기 때문입니다. 검수 기준을 정량화 가능한 KPI로 전환하는 전형적인 방법은 '데이터셋 품질 양호'를 '라벨링 정확도 ≥ 자체 확인 필요 %, 무효 샘플 비율 ≤ 자체 확인 필요 %, 커버 시나리오 수 ≥ 자체 확인 필요 개' 등의 측정 가능한 지표로 전환하는 것입니다. Owner와 마일스톤 수립은 책임을 개인에게 할당하여 데이터 작업이 '모두가 책임지지만 아무도 책임지지 않는' 상황을 방지합니다.

**② 방안/방법 설계(P12.2.2.2)**

이 하위 작업은 2개 이상의 후보 방안을 구성하고, 평가 매트릭스를 수립하여 정량적으로 점수를 매기며, 심의를 조직하고 방안을 동결할 것을 요구합니다. 후보 방안은 일반적으로 네 가지 데이터 소스의 다양한 조합을 중심으로 전개됩니다:

| 방안 차원 | 원격 조작 중심 | 시뮬레이션 중심 | 혼합 방안 |
|---------|-----------|---------|---------|
| 데이터 실제성 | 최고 | 낮음 | 중간 |
| 수집 비용 | 최고 | 최저 | 중간 |
| 시나리오 커버리지 | 제한적 | 광범위 | 비교적 넓음 |
| 라벨링 난이도 | 낮음(자연스러운 동작 라벨 보유) | 중간(자동 라벨링 필요) | 중간 |
| 규모 상한 | 낮음(물리적 머신 시간 제한) | 높음(병렬 생성 가능) | 중상 |

평가 매트릭스의 정량적 점수는 일반적으로 데이터 품질, 비용, 시간, 위험의 네 가지 차원에서 가중치를 두고 평가합니다. 심의 및 방안 동결의 의미는 데이터 수집이 대규모로 시작되면 중간에 방안을 변경하면 이미 수집된 데이터가 무효화되므로, 방안 동결이 위험을 통제하는 핵심 노드이기 때문입니다.

**③ 모델링/시뮬레이션 및 시제품 구현(P12.2.2.3)**

이 하위 작업은 시뮬레이션/수학적 모델 또는 프로토타입 시제품을 구축하고, 계산 또는 실험을 실행하며, 핵심 파라미터와 경계 조건을 기록할 것을 요구합니다. 데이터 파이프라인의 경우 '시제품'은 다음이 될 수 있습니다: 원격 조작 수집 시스템(마스터 핸드, 슬레이브 핸드, 힘 피드백 장치 포함), 시뮬레이션 데이터 생성기(물리 엔진 기반 도메인 무작위화 구성), 또는 데이터 정제 파이프라인 프로토타입. 핵심 파라미터에는 수집 주파수, 해상도, 힘 감각 샘플링 속도, 장면 조명 조건, 객체 재질 속성 등이 포함됩니다. 경계 조건에는 로봇 관절 한계, 작업 공간 범위, 센서 측정 범위 등이 포함됩니다.

**④ 시뮬레이션 결과 검증 및 최적화(P12.2.2.4)**

이 하위 작업은 시뮬레이션 결과와 이론/실험 데이터의 일관성을 검증하고, 취약점을 식별하여 반복 최적화할 것을 요구합니다. 전형적인 검증 방식은 다음과 같습니다: 시뮬레이션 생성 데이터와 소량의 실제 원격 조작 데이터의 분포 차이 비교; 데이터 증강 후 샘플이 물리적 합리성을 유지하는지 확인(예: 객체 관통 없음, 힘 감각 급변 없음); 정제 규칙이 유효 샘플을 잘못 삭제하지 않는지 검증. 취약점 식별 후 반복 루프에 진입합니다: 시뮬레이션 파라미터 조정, 증강 전략 최적화, 정제 규칙 수정.

**⑤ 문서 출력 및 하위 전달(P12.2.2.5)**

이 하위 작업은 최종 보고서/도면/규격을 출력하고, ICD/BOM/SOP/요구사항 추적 체인을 업데이트하며, 하위 단계로의 공식 전달을 완료할 것을 요구합니다. 구체적 동작은 다음과 같습니다: 템플릿에 따라 문서를 작성하고 원시 데이터를 참조하며, 내부 심의 및 버전 제어를 완료하고, 게시 및 하위 의존자에게 통지. 여기서 '문서'는 데이터셋 설명뿐만 아니라 데이터 수집 SOP(표준 작업 절차), 데이터 형식 규격, 품질 검사 보고서, 버전 변경 기록도 포함합니다. 요구사항 추적 체인은 각 데이터 요구사항이 특정 수집 배치 및 정제 규칙까지 추적될 수 있도록 보장합니다.

### 핵심 파라미터 및 규격

데이터 파이프라인의 핵심 파라미터는 네 가지 계층에 분포합니다:

**수집 계층 파라미터**: 원격 조작 수집 주파수(일반적으로 자체 확인 필요 Hz), 힘 감각 샘플링 속도(자체 확인 필요 Hz), 이미지 해상도(자체 확인 필요 픽셀), 동기화 정밀도(자체 확인 필요 ms). 시뮬레이션 데이터 생성 파라미터에는 물리 엔진 스텝(자체 확인 필요 s), 도메인 무작위화 범위(자체 확인 필요), 시나리오 수(자체 확인 필요 개)가 포함됩니다.

**품질 계층 파라미터**: 라벨링 정확도(자체 확인 필요 %), 무효 샘플 비율(자체 확인 필요 %), 중복률(자체 확인 필요 %), 시나리오 커버리지(자체 확인 필요 %). 이러한 파라미터는 P12.2.2.1의 KPI 구체적 지표를 구성합니다.

**규모 계층 파라미터**: 데이터셋 총 샘플 수(자체 확인 필요 개), 유효 샘플 수(자체 확인 필요 개), 클래스 균형도(자체 확인 필요), 시간 범위(자체 확인 필요 일).

**버전 계층 파라미터**: 데이터셋 버전 번호(시맨틱 버전), 변경 기록 수(자체 확인 필요 개), 하위 의존자 수(자체 확인 필요 개).

### 수평 비교

P12.2.2를 인접 공정 노드와 비교하면 그 경계를 더 명확히 파악할 수 있습니다:

| 비교 차원 | P12.2.2 데이터 수집 및 데이터 파이프라인 | P12.1 인식 스택 | P12.2.3 모델 훈련(추론) |
|---------|---------------------------|-------------|------------------------|
| 핵심 산출물 | 훈련 가능한 데이터셋 | 센서 구성 및 캘리브레이션 파라미터 | 훈련된 모델 가중치 |
| 주요 방법 | 원격 조작, 시뮬레이션, 정제 증강 | 캘리브레이션, SLAM, 탐지 추적 | 지도 학습, 강화 학습 |
| 전달물 형태 | 데이터셋 + 문서 + SOP | 구성 파일 + 캘리브레이션 보고서 | 모델 파일 + 평가 보고서 |
| 품질 측정 | 데이터 품질 지표 | 캘리브레이션 정밀도 지표 | 작업 성공률 |
| 반복 주기 | 주 단위 | 일 단위 | 월 단위 |

P12.2.2와 가장 가까운 이웃은 P12.2.1(데이터 요구사항 분석으로 가정, 자체 확인 필요)이며, 전자는 '어떤 데이터가 필요한지'를 정의하고, 후자는 '이 데이터를 어떻게 확보하고 처리할지'를 해결합니다.

### 누가 사용하는가·적용 사례

**사례 1: 이중 팔 조작 작업의 원격 조작 데이터 수집.** 한 팀이 '양손 협력 운반' 작업을 수행하는 VLA 모델을 훈련해야 합니다. P12.2.2.1에 따라 먼저 상류 입력을 정리합니다: 인식 스택이 제공하는 이중 팔 관절 각도 범위, 엔드 이펙터 힘 감각 측정 범위, 작업 공간 크기. 이후 P12.2.2.2 단계에서 두 개의 후보 방안을 구성합니다: 방안 A는 순수 원격 조작 수집(예상 자체 확인 필요 시간), 방안 B는 원격 조작 + 시뮬레이션 데이터 혼합(예상 자체 확인 필요 시간). 평가 매트릭스는 방안 B가 비용 차원에서 더 높은 점수를 보여 최종적으로 방안 B를 동결합니다. P12.2.2.3 단계에서 원격 조작 수집대를 구축하고 핵심 파라미터를 기록합니다: 수집 주파수 자체 확인 필요 Hz, 힘 감각 샘플링 속도 자체 확인 필요 Hz. P12.2.2.4 단계에서 시뮬레이션 데이터와 실제 데이터를 혼합한 후 분포 차이를 확인하고, 시뮬레이션 데이터의 객체 마찰 계수가 실제와 큰 차이가 있음을 발견하여 반복 조정 후 검증을 통과합니다. P12.2.2.5 단계에서 데이터셋 v1.0을 출력하고, 데이터 설명 문서와 정제 규칙 SOP를 첨부하며, 하류 모델 훈련 팀에 통지합니다.

**사례 2: 인터넷 비디오 데이터의 정제 파이프라인.** 한 팀이 인터넷 비디오를 활용하여 인간 조작 사전 지식을 보충하려고 합니다. P12.2.2.1 단계에서 '비디오 사용 가능률'을 KPI로 전환합니다: 사용 가능한 비디오 비율 ≥ 자체 확인 필요 %. P12.2.2.2 단계에서 정제 파이프라인 방안을 설계합니다: 방안 A는 순수 수동 선별(비용 높음), 방안 B는 자동 사전 선별 + 수동 검토(비용 적정). 평가 후 방안 B를 선택합니다. P12.2.2.3 단계에서 자동 사전 선별 프로토타입을 구현합니다: 장면 분류기를 기반으로 관련 없는 비디오를 필터링하고, 동작 감지기를 기반으로 조작 클립을 추출합니다. P12.2.2.4 단계에서 검증 결과 사전 선별 오삭제율이 자체 확인 필요 %임을 발견하고, 분류기 임계값을 조정하여 허용 가능한 범위로 최적화합니다. P12.2.2.5 단계에서 정제된 비디오 데이터셋을 라벨 파일 및 정제 로그와 함께 전달합니다.

### 한계 및 경계

**데이터 수집 비용 경계.** 원격 조작 수집은 물리적 로봇 수와 운영자 작업 시간에 제한되며, 일일 유효 데이터 양에는 하드 상한이 존재합니다. 프로젝트가 수백만 개 샘플을 필요로 할 때, 순수 원격 조작 방안은 비용 측면에서 불가능하며, 반드시 시뮬레이션 데이터나 인터넷 비디오로 보충해야 합니다. 이것은 P12.2.2.2 방안 평가에서 반드시 정량화해야 하는 제약입니다.

**프라이버시 및 규정 준수 경계.** 인터넷 비디오는 인물 초상권, 장면 프라이버시, 플랫폼 이용 약관 등의 법적 문제를 수반합니다. 데이터 정제 단계에는 얼굴 모자이크, 장면 비식별화, 출처 규정 준수 심사 등의 동작이 포함되어야 합니다. 이 경계는 P12.2.2.1의 KPI 설정에서 '규정 준수율 100%'의 하드 지표로 반영되어야 합니다.

**라벨링 품질 경계.** 수동 라벨링이든 자동 라벨링이든 오류율이 존재합니다. 라벨링 품질은 하류 모델 훈련 효과에 직접 영향을 미치지만, 라벨링 정확도를 과도하게 추구하면 비용이 크게 상승합니다. 엔지니어링 판단: 라벨링 정확도 목표는 일반적으로 자체 확인 필요 %에서 자체 확인 필요 % 사이로 설정되며, 구체적 값은 작업 난이도와 모델 오류 허용 능력에 따라 달라집니다.

**시뮬레이션과 실제의 도메인 격차.** 시뮬레이션 데이터는 비용이 낮고 규모가 크지만, 물리 엔진의 근사 특성으로 인해 데이터 분포가 실제 세계와 편차가 있습니다. P12.2.2.4의 검증 단계는 바로 이 격차를 정량화하고 줄이기 위한 것이지만, 완전히 제거할 수는 없습니다. 힘 감각에 민감한 작업(예: 정밀 조립)의 경우 시뮬레이션 데이터의 신뢰성을 신중히 평가해야 합니다.

### 일반적인 오해

1. **'데이터가 많을수록 좋다'** — 틀림. 데이터 규모는 KPI 중 하나일 뿐이며, 품질, 다양성, 라벨링 정확도가 동등하게 중요합니다. 맹목적으로 데이터 양을 늘리면 많은 중복 샘플이 유입되어 오히려 유효 정보 밀도가 희석될 수 있습니다. P12.2.2.1의 KPI 설정에는 규모 지표와 품질 지표가 동시에 포함되어야 합니다.

2. **'시뮬레이션 데이터가 실제 데이터를 완전히 대체할 수 있다'** — 틀림. 시뮬레이션 데이터는 커버리지와 비용에서 장점이 있지만, sim-to-real 격차는 항상 존재합니다. 엔지니어링 판단: 접촉이 많은 조작 작업의 경우 실제 데이터 비율은 자체 확인 필요 % 이상이어야 하며, 그렇지 않으면 실제 환경에서 모델 성능이 크게 저하될 수 있습니다.

3. **'데이터 정제는 단지 나쁜 샘플을 걸러내는 것'** — 틀림. 데이터 정제에는 중복 제거, 정렬, 라벨 수정, 증강 생성 등의 긍정적 작업도 포함됩니다. P12.2.2.3의 '이상 및 편차 기록' 동작은 정제 과정 자체가 데이터 문제를 발견하고 수집 방안을 개선하는 중요한 피드백 채널임을 보여줍니다.

4. **'데이터셋 전달 후 작업이 끝난다'** — 틀림. P12.2.2.5는 '게시 및 하위 의존자 통지'를 요구하지만, 하위 사용 과정에서 새로운 데이터 문제가 발견될 수 있으며 버전 반복이 필요합니다. 데이터 파이프라인은 지속적으로 운영되는 시스템이지, 일회성 전달의 정적 산출물이 아닙니다.

### 관련 지식

- `ent_process_p12` — 본 노드가 속한 VLA / WAM / AI 알고리즘 통합 단계로, 5단계 공정 템플릿과 전체 검수 기준을 정의합니다.
- `ent_process_p12_2_1` — 동일 계열 인접 공정: VLA 모델 선정 및 미세 조정(동일 작업 패키지 내 병렬 공정).
- `ent_process_p12_2_3` — 동일 계열 인접 공정: 스킬 라이브러리 및 작업 계획(동일 작업 패키지 내 병렬 공정).
