---
$id: ent_process_p10_2_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Standing and Anti-Interference Balance Control
  zh: 站立与抗扰平衡控制
  ko: 서 있기 및 외란 저항 균형 제어
summary:
  en: Video of the standing anti-interference physical prototype; its recovery capability meets the specified performance
    criteria.
  zh: '- P10.2.1.1 输入梳理与目标量化 - 整理「站立与抗扰平衡控制」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 站立抗扰实物视频、恢复能力满足指标
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b2): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

站立与抗扰平衡控制（Standing and Anti-Interference Balance Control）是人形机器人运动控制算法开发流程中的关键工序节点，属于 P10.2 平衡、步态与 WBC 工作包的第一个子任务。它解决的是双足机器人在静止站立状态下受到外部推扰时维持平衡的核心安全功能问题，其方法工具涵盖 LQR、MPC、ZMP/Capture Point 与角动量控制，关键约束为实时求解频率不低于 100 Hz 且需满足关节极限。该工序通过五个三级子任务（输入梳理、方案设计、仿真验证、调参验证、文档交付）将抽象的控制目标转化为可量化、可验收的工程成果。

## 核心内容

### 是什么：准确定义

站立与抗扰平衡控制是双足人形机器人运动控制算法开发流程（P10.2）中的第一个工序节点，编号为 P10.2.1。它指在机器人双足站立状态下，通过控制算法维持质心在支撑多边形内的稳定，并在受到外部推力、冲击或自身扰动后能够主动恢复平衡的能力。该工序不是单一算法的实现，而是一个完整的开发闭环：从输入梳理与目标量化开始，经过方案设计、仿真验证、调参验证，最终以文档形式向下游交付。

它真正改变的不是"让机器人站住"这个结果，而是"如何系统性地保证机器人站得住"这一工程过程——将平衡控制从经验调参转化为有量化指标、有评审节点、有追溯链的规范化开发流程。

### 为什么存在：痛点与历史定位

双足站立平衡是人形机器人最基础也最苛刻的安全功能。与轮式机器人不同，双足机器人的支撑面是离散的、有限的，质心一旦越出支撑多边形边界，倾倒便不可避免。早期平衡控制依赖手工整定 PID 参数，面对不同负载、不同地面条件时鲁棒性差，且无法显式处理关节限位、力矩饱和等硬约束。

该工序的历史定位在于：它是连接上游感知、动力学建模与下游步态规划、全身控制（WBC）的枢纽节点。没有可靠的站立抗扰能力，步态规划与 WBC 都失去了安全底座。因此，P10.2.1 被设计为 P10.2 工作包的第一个子任务，其完成质量直接决定后续步态与 WBC 工序的可行性。

### 原理拆解

**① 平衡控制的物理基础：ZMP 判据**

站立平衡的核心判据是零力矩点（ZMP）必须位于支撑多边形内。在简化模型中，假设质心高度 $z_c$ 恒定，ZMP 坐标为：

$$
x_{\text{ZMP}} = x_{\text{CoM}} - \frac{z_c}{g} \ddot{x}_{\text{CoM}}
$$

$$
y_{\text{ZMP}} = y_{\text{CoM}} - \frac{z_c}{g} \ddot{y}_{\text{CoM}}
$$

其中 $g$ 为重力加速度，$\ddot{x}_{\text{CoM}}$、$\ddot{y}_{\text{CoM}}$ 为质心水平加速度。该式表明：ZMP 偏离质心投影点的距离与质心加速度成正比——控制平衡的本质就是控制质心加速度，使 ZMP 始终留在支撑多边形内。

**② 捕获点：将动态问题几何化**

捕获点（Capture Point, CP）是地面上这样一个点——若机器人能在该点立即踩下去并把质心置于支撑脚正上方，则无需再迈下一步即可停止。在**线性倒立摆模型（Linear Inverted Pendulum Model, LIPM）**中，假设质心高度 $z_c$ 恒定，且质点通过无质量杆连接到地面上的 ZMP，LIPM 的水平动力学为：

$$
\ddot{x} = \frac{g}{z_c}(x - x_{\text{ZMP}})
$$

捕获点的存在把复杂的动态平衡问题转化为"下一步应踩在哪里"的几何问题。在站立抗扰场景中，若扰动较小，机器人可通过踝关节力矩将捕获点拉回支撑多边形内；若扰动较大，则需要迈步——这正是站立抗扰与步态规划的接口。

**③ 控制框架：LQR 与 MPC 的分工**

LQR 在无限时域上最优，但无法显式处理约束——而人形机器人的核心约束恰恰是硬性的：关节限位不可超、力矩有限幅、ZMP 必须留在支撑多边形内。MPC 则通过滚动时域机制，在每个控制周期求解一个有限时域开环最优控制问题，但只执行第一步控制，下一周期用新的状态估计重新求解。MPC 的预测模型在每次求解时被"重新锚定"到当前状态——这是 MPC 与离线轨迹优化的本质区别。

在站立抗扰场景中，MPC 可统一处理约束：将 ZMP 支撑多边形约束、关节力矩限幅、关节位置极限全部纳入优化问题，同时通过预测时域内的滚动优化获得前瞻性。LQR 则常用于小扰动下的局部线性化控制，或在 MPC 的终端代价函数中作为无限时域近似。

### 关键参数与规格

| 参数/规格 | 数值/要求 | 来源 |
| --- | --- | --- |
| 实时求解频率 | ≥ 100 Hz | 语料 |
| 候选方案数量 | 不少于 2 个 | 语料 |
| 关键约束 | 关节极限 | 语料 |
| 完成标准 | 站立抗扰实物视频、恢复能力满足指标 | 语料 |

实时求解频率 ≥ 100 Hz 意味着控制周期不超过 10 ms。对于 MPC 而言，这要求在 10 ms 内完成一次有限时域最优控制问题的求解——这对优化求解器的计算效率提出了极高要求，也是方案选型时必须评估实时性的根本原因（工程判断）。

### 横向对比

| 方法 | 约束处理能力 | 实时性 | 适用场景 |
| --- | --- | --- | --- |
| LQR | 弱（需事后限幅） | 极快（解析解） | 小扰动、线性化模型 |
| MPC | 强（显式硬约束） | 较慢（需在线求解） | 大扰动、约束主导场景 |
| ZMP/Capture Point | 中（几何判据） | 快（解析计算） | 平衡判据、落脚点规划 |
| 角动量控制 | 中（补充质心模型） | 快 | 全身姿态协调 |

LQR 与 MPC 并非互斥关系——在工程实践中，常见做法是使用 MPC 处理约束与前瞻，而在 MPC 内部或终端使用 LQR 近似（工程判断）。Capture Point 则提供了直观的几何判据，可作为 MPC 的参考轨迹生成器或安全监控层。

### 谁在用·应用案例

该工序是双足人形机器人产品开发全流程（V3 版）中 P10.2 平衡、步态与 WBC 工作包的组成部分，适用于任何需要双足站立与抗扰能力的全尺寸人形机器人产品开发项目。其应用场景包括：

- **工业巡检**：机器人在不平整地面站立，抵抗风力或线缆拉扯扰动；
- **物流搬运**：机器人双手抱持重物站立，重心偏移时维持平衡；
- **人机交互**：机器人接受人类推搡或拍打时保持不倒，是安全交互的前提。

该工序的完成标准——站立抗扰实物视频与恢复能力满足指标——直接作为产品验收依据，其输出文档将更新 ICD/BOM/SOP/需求追溯链，为下游步态规划与 WBC 工序提供输入。

### 局限与边界

该工序聚焦于**站立**状态下的抗扰平衡，其边界条件包括：

- **不覆盖动态行走**：行走过程中的平衡由后续步态规划工序处理，本工序仅处理站立场景；
- **不覆盖大扰动迈步恢复**：当捕获点超出支撑多边形且无法通过踝关节力矩拉回时，需要迈步策略——这属于站立抗扰与步态规划的接口，本工序的边界是"恢复能力满足指标"，具体指标值需在 P10.2.1.1 中量化；
- **模型简化假设**：ZMP 与 Capture Point 的推导基于 LIPM 假设（质心高度恒定），实际机器人质心高度会有波动，需通过角动量控制补偿；
- **实时性瓶颈**：MPC 在 ≥ 100 Hz 频率下的在线求解对算力有硬性要求，若硬件无法满足，需降阶模型或采用显式 MPC（工程判断）。

### 常见误区

1. **"站立平衡就是 PID 的事"**——错。PID 无法显式处理关节限位与 ZMP 支撑多边形约束，面对大扰动时缺乏前瞻性。该工序明确要求 MPC 等优化控制框架，正是为了统一处理约束。

2. **"Capture Point 只用于行走"**——错。Capture Point 在站立抗扰中同样关键：它给出了"是否需要迈步"的判据。若捕获点在支撑多边形内，踝关节策略即可恢复；若在边界外，则必须迈步。

3. **"仿真验证通过就可以上实物"**——错。P10.2.1.4 明确要求"执行测试并记录原始数据"、"输出问题清单与改进措施"，且完成标准是"站立抗扰实物视频"——仿真与实物之间存在模型误差、延迟、噪声等差异，必须经过实物调参验证。

4. **"实时频率越高越好"**——错。≥ 100 Hz 是下限而非上限。频率过高可能导致 MPC 求解时间不足、控制周期抖动；频率过低则无法捕捉扰动动态。合理频率需根据机器人动力学特性与硬件算力综合确定（工程判断）。

### 相关知识

- `ent_principle_capture_point` — 捕获点是站立抗扰中判断"是否需要迈步"的核心判据，将动态平衡问题转化为落脚点几何问题。
- `ent_principle_zero_moment_point` — ZMP 是站立平衡的核心稳定性判据，控制目标即保持 ZMP 在支撑多边形内。
- `ent_method_model_predictive_control` — MPC 是该工序的关键控制框架，通过滚动时域优化统一处理 ZMP 约束、关节限位与力矩限幅。
- `ent_paper_linear_quadratic_regulator_lqr` — LQR 作为线性最优控制方法，在小扰动场景或 MPC 终端代价中发挥作用。
- `ent_process_p10` — 本工序（P10.2.1）是 P10.2 平衡、步态与 WBC 工作包的第一个子任务，其输出为后续步态规划与 WBC 提供基础。
- `ent_paper_roncone_gaze_stabilization_for_humanoi_2014` — 涉及人形机器人稳定控制的相关研究，可作为抗扰平衡控制的参考。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [项目 Wiki 第 8 章（ZMP 与动态平衡）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [项目 Wiki 第 14 章（MPC 与优化控制）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-14.md)

## Overview

Standing and Anti-Interference Balance Control is a critical process node in the development workflow of humanoid robot motion control algorithms, serving as the first subtask of the P10.2 Balance, Gait, and WBC work package. It addresses the core safety functionality of maintaining balance when a bipedal robot is subjected to external push disturbances while standing still. Its methodological toolkit encompasses LQR, MPC, ZMP/Capture Point, and angular momentum control, with key constraints requiring real-time solving frequencies of no less than 100 Hz and compliance with joint limits. This process translates abstract control objectives into quantifiable, verifiable engineering deliverables through five third-level subtasks (input analysis, solution design, simulation validation, parameter tuning validation, and documentation delivery).

## Content

### What It Is: Precise Definition

Standing and Anti-Interference Balance Control is the first process node in the humanoid robot motion control algorithm development workflow (P10.2), numbered P10.2.1. It refers to the ability, while the robot is in a bipedal standing state, to maintain the center of mass (CoM) stable within the support polygon through control algorithms, and to actively recover balance after being subjected to external pushes, impacts, or self-induced disturbances. This process is not the implementation of a single algorithm but a complete development loop: starting from input analysis and objective quantification, proceeding through solution design, simulation validation, and parameter tuning validation, and finally delivering outputs downstream in documentation form.

What it truly changes is not the outcome of "making the robot stand," but the engineering process of "systematically guaranteeing that the robot can stand"—transforming balance control from empirical parameter tuning into a standardized development workflow with quantitative metrics, review checkpoints, and traceability chains.

### Why It Exists: Pain Points and Historical Positioning

Bipedal standing balance is the most fundamental and demanding safety function of humanoid robots. Unlike wheeled robots, the support surface of a bipedal robot is discrete and finite; once the CoM exceeds the boundary of the support polygon, falling becomes inevitable. Early balance control relied on manually tuned PID parameters, which exhibited poor robustness across different payloads and ground conditions and could not explicitly handle hard constraints such as joint limits and torque saturation.

The historical positioning of this process lies in its role as the hub connecting upstream perception and dynamics modeling with downstream gait planning and whole-body control (WBC). Without reliable standing anti-interference capability, gait planning and WBC would lack a safe foundation. Therefore, P10.2.1 is designed as the first subtask of the P10.2 work package, and its completion quality directly determines the feasibility of subsequent gait and WBC processes.

### Principle Breakdown

**① Physical Foundation of Balance Control: The ZMP Criterion**

The core criterion for standing balance is that the Zero Moment Point (ZMP) must lie within the support polygon. In a simplified model, assuming a constant CoM height \(z_c\), the ZMP coordinates are:

$$
x_{\text{ZMP}} = x_{\text{CoM}} - \frac{z_c}{g} \ddot{x}_{\text{CoM}}
$$

$$
y_{\text{ZMP}} = y_{\text{CoM}} - \frac{z_c}{g} \ddot{y}_{\text{CoM}}
$$

where \(g\) is the gravitational acceleration, and \(\ddot{x}_{\text{CoM}}\), \(\ddot{y}_{\text{CoM}}\) are the horizontal accelerations of the CoM. This equation indicates that the distance of the ZMP from the CoM projection point is proportional to the CoM acceleration—the essence of balance control is controlling CoM acceleration so that the ZMP remains within the support polygon.

**② Capture Point: Geometrizing the Dynamic Problem**

The Capture Point (CP) is a point on the ground such that if the robot could immediately step onto it and position the CoM directly above the support foot, it could stop without taking another step. In the **Linear Inverted Pendulum Model (LIPM)**, assuming a constant CoM height \(z_c\) and a point mass connected to the ground ZMP via a massless rod, the horizontal dynamics of the LIPM are:

$$
\ddot{x} = \frac{g}{z_c}(x - x_{\text{ZMP}})
$$

The existence of the Capture Point transforms the complex dynamic balance problem into a geometric question of "where to step next." In the standing anti-interference scenario, if the disturbance is small, the robot can pull the Capture Point back within the support polygon using ankle joint torques; if the disturbance is large, stepping becomes necessary—this is precisely the interface between standing anti-interference and gait planning.

**③ Control Framework: Division of Labor Between LQR and MPC**

LQR is optimal over an infinite horizon but cannot explicitly handle constraints—yet the core constraints of humanoid robots are precisely hard constraints: joint limits cannot be exceeded, torques are bounded, and the ZMP must remain within the support polygon. MPC, by contrast, employs a receding horizon mechanism, solving a finite-horizon open-loop optimal control problem at each control cycle but executing only the first control action, then re-solving with updated state estimates in the next cycle. The prediction model of MPC is "re-anchored" to the current state at each solve—this is the fundamental difference between MPC and offline trajectory optimization.

In the standing anti-interference scenario, MPC can uniformly handle constraints: incorporating ZMP support polygon constraints, joint torque limits, and joint position limits into the optimization problem while achieving foresight through receding-horizon optimization within the prediction horizon. LQR is commonly used for local linearized control under small disturbances or as an infinite-horizon approximation in the terminal cost function of MPC.

### Key Parameters and Specifications

| Parameter/Specification | Value/Requirement | Source |
| --- | --- | --- |
| Real-time solving frequency | ≥ 100 Hz | Corpus |
| Number of candidate solutions | No fewer than 2 | Corpus |
| Key constraints | Joint limits | Corpus |
| Completion criteria | Physical standing anti-interference video, recovery capability meeting metrics | Corpus |

A real-time solving frequency of ≥ 100 Hz implies a control cycle of no more than 10 ms. For MPC, this requires solving a finite-horizon optimal control problem within 10 ms—imposing extremely high demands on the computational efficiency of the optimization solver, which is the fundamental reason why real-time performance must be evaluated during solution selection (engineering judgment).

### Horizontal Comparison

| Method | Constraint Handling Capability | Real-Time Performance | Applicable Scenarios |
| --- | --- | --- | --- |
| LQR | Weak (requires post-hoc saturation) | Extremely fast (analytical solution) | Small disturbances, linearized models |
| MPC | Strong (explicit hard constraints) | Slower (requires online solving) | Large disturbances, constraint-dominated scenarios |
| ZMP/Capture Point | Medium (geometric criterion) | Fast (analytical computation) | Balance criteria, foothold planning |
| Angular momentum control | Medium (supplements CoM model) | Fast | Whole-body posture coordination |

LQR and MPC are not mutually exclusive—in engineering practice, a common approach is to use MPC for constraint handling and foresight while employing LQR approximations within MPC or at its terminal (engineering judgment). Capture Point provides an intuitive geometric criterion that can serve as a reference trajectory generator or safety monitoring layer for MPC.

### Who Uses It: Application Cases

This process is a component of the P10.2 Balance, Gait, and WBC work package in the full humanoid robot product development workflow (V3 version), applicable to any full-size humanoid robot product development project requiring bipedal standing and anti-interference capability. Its application scenarios include:

- **Industrial inspection**: Robots standing on uneven ground, resisting disturbances from wind or cable pulling;
- **Logistics and material handling**: Robots standing while holding heavy objects with both hands, maintaining balance when the CoM shifts;
- **Human-robot interaction**: Robots remaining upright when pushed or patted by humans, a prerequisite for safe interaction.

The completion criteria of this process—physical standing anti-interference video and recovery capability meeting metrics—serve directly as product acceptance evidence, and its output documents update the ICD/BOM/SOP/requirements traceability chain, providing inputs for downstream gait planning and WBC processes.

### Limitations and Boundaries

This process focuses on anti-interference balance in the **standing** state, with boundary conditions including:

- **Does not cover dynamic walking**: Balance during walking is handled by subsequent gait planning processes; this process addresses only standing scenarios;
- **Does not cover step recovery from large disturbances**: When the Capture Point exceeds the support polygon and cannot be pulled back via ankle torques, a stepping strategy is required—this falls at the interface between standing anti-interference and gait planning; the boundary of this process is "recovery capability meeting metrics," with specific metric values to be quantified in P10.2.1.1;
- **Model simplification assumptions**: The derivations of ZMP and Capture Point are based on the LIPM assumption (constant CoM height); actual robot CoM height fluctuates and requires compensation via angular momentum control;
- **Real-time performance bottleneck**: Online solving of MPC at frequencies ≥ 100 Hz imposes hard computational requirements; if hardware cannot meet them, model reduction or explicit MPC is needed (engineering judgment).

### Common Misconceptions

1. **"Standing balance is just a PID matter"**—Wrong. PID cannot explicitly handle joint limits and ZMP support polygon constraints and lacks foresight under large disturbances. This process explicitly requires optimization-based control frameworks like MPC precisely to handle constraints uniformly.

2. **"Capture Point is only for walking"**—Wrong. Capture Point is equally critical in standing anti-interference: it provides the criterion for "whether stepping is needed." If the Capture Point lies within the support polygon, ankle strategy suffices for recovery; if outside the boundary, stepping is mandatory.

3. **"Passing simulation validation means it's ready for the physical robot"**—Wrong. P10.2.1.4 explicitly requires "executing tests and recording raw data" and "outputting issue lists and improvement measures," and the completion criterion is "physical standing anti-interference video"—discrepancies in model errors, latency, and noise exist between simulation and reality, necessitating physical parameter tuning validation.

4. **"Higher real-time frequency is always better"**—Wrong. ≥ 100 Hz is a lower bound, not an upper limit. Excessively high frequencies may lead to insufficient MPC solving time and control cycle jitter; excessively low frequencies fail to capture disturbance dynamics. The appropriate frequency must be determined based on robot dynamic characteristics and hardware computational power (engineering judgment).

### Related Knowledge

- `ent_principle_capture_point` — The Capture Point is the core criterion in standing anti-interference for determining "whether stepping is needed," transforming the dynamic balance problem into a foothold geometry problem.
- `ent_principle_zero_moment_point` — ZMP is the core stability criterion for standing balance; the control objective is to keep the ZMP within the support polygon.
- `ent_method_model_predictive_control` — MPC is the key control framework of this process, uniformly handling ZMP constraints, joint limits, and torque limits through receding-horizon optimization.
- `ent_paper_linear_quadratic_regulator_lqr` — LQR, as a linear optimal control method, plays a role in small-disturbance scenarios or in the terminal cost of MPC.
- `ent_process_p10` — This process (P10.2.1) is the first subtask of the P10.2 Balance, Gait, and WBC work package, with its outputs providing the foundation for subsequent gait planning and WBC.
- `ent_paper_roncone_gaze_stabilization_for_humanoi_2014` — Relevant research on humanoid robot stabilization control, serving as a reference for anti-interference balance control.

## 개요

서서 자세 유지 및 외란 저항 균형 제어(Standing and Anti-Interference Balance Control)는 휴머노이드 로봇 운동 제어 알고리즘 개발 프로세스에서 핵심 공정 단계로, P10.2 균형, 보행 및 WBC 작업 패키지의 첫 번째 하위 작업입니다. 이는 이족 로봇이 정지 상태에서 외부 밀림 외란을 받을 때 균형을 유지하는 핵심 안전 기능 문제를 해결하며, 방법론 도구로는 LQR, MPC, ZMP/Capture Point 및 각운동량 제어를 포함하고, 핵심 제약 조건은 실시간 해석 주파수가 100 Hz 이상이어야 하며 관절 한계를 충족해야 한다는 것입니다. 이 공정은 다섯 개의 3단계 하위 작업(입력 정리, 설계, 시뮬레이션 검증, 파라미터 튜닝 검증, 문서 인도)을 통해 추상적인 제어 목표를 정량화 가능하고 검수 가능한 엔지니어링 성과로 전환합니다.

## 핵심 내용

### 무엇인가: 정확한 정의

서서 자세 유지 및 외란 저항 균형 제어는 이족 휴머노이드 로봇 운동 제어 알고리즘 개발 프로세스(P10.2)의 첫 번째 공정 노드로, 번호는 P10.2.1입니다. 이는 로봇이 이족으로 서 있는 상태에서 제어 알고리즘을 통해 질량 중심을 지지 다각형 내에 안정적으로 유지하고, 외부 밀림, 충격 또는 자체 외란 후 능동적으로 균형을 회복하는 능력을 의미합니다. 이 공정은 단일 알고리즘 구현이 아니라 완전한 개발 폐루프입니다: 입력 정리와 목표 정량화에서 시작하여 설계, 시뮬레이션 검증, 파라미터 튜닝 검증을 거쳐 최종적으로 문서 형태로 하류에 인도됩니다.

이것이 실제로 바꾸는 것은 "로봇을 서 있게 하는 결과"가 아니라 "로봇이 서 있을 수 있음을 체계적으로 보장하는 방법"이라는 엔지니어링 프로세스입니다 — 균형 제어를 경험적 파라미터 튜닝에서 정량적 지표, 검토 노드, 추적 체인이 있는 표준화된 개발 프로세스로 전환합니다.

### 왜 존재하는가: 문제점과 역사적 위치

이족 보행 균형은 휴머노이드 로봇의 가장 기본적이면서도 가장 까다로운 안전 기능입니다. 바퀴형 로봇과 달리 이족 로봇의 지지면은 이산적이고 제한적이며, 질량 중심이 지지 다각형 경계를 벗어나면 전도는 불가피합니다. 초기 균형 제어는 수동 PID 파라미터 튜닝에 의존했으며, 다양한 하중과 지면 조건에서 강건성이 떨어지고 관절 한계, 토크 포화와 같은 하드 제약을 명시적으로 처리할 수 없었습니다.

이 공정의 역사적 위치는: 상류의 인식, 동역학 모델링과 하류의 보행 계획, 전신 제어(WBC)를 연결하는 허브 노드입니다. 신뢰할 수 있는 서서 자세 유지 능력이 없으면 보행 계획과 WBC 모두 안전 기반을 잃습니다. 따라서 P10.2.1은 P10.2 작업 패키지의 첫 번째 하위 작업으로 설계되었으며, 그 완성 품질은 후속 보행 및 WBC 공정의 실행 가능성을 직접 결정합니다.

### 원리 분석

**① 균형 제어의 물리적 기초: ZMP 판정 기준**

서서 균형의 핵심 판정 기준은 영점 모멘트(ZMP)가 지지 다각형 내에 있어야 한다는 것입니다. 단순화된 모델에서 질량 중심 높이 $z_c$가 일정하다고 가정하면 ZMP 좌표는:

$$
x_{\text{ZMP}} = x_{\text{CoM}} - \frac{z_c}{g} \ddot{x}_{\text{CoM}}
$$

$$
y_{\text{ZMP}} = y_{\text{CoM}} - \frac{z_c}{g} \ddot{y}_{\text{CoM}}
$$

여기서 $g$는 중력 가속도, $\ddot{x}_{\text{CoM}}$, $\ddot{y}_{\text{CoM}}$는 질량 중심 수평 가속도입니다. 이 식은 ZMP가 질량 중심 투영점에서 벗어난 거리가 질량 중심 가속도에 비례함을 보여줍니다 — 균형 제어의 본질은 질량 중심 가속도를 제어하여 ZMP가 항상 지지 다각형 내에 머물게 하는 것입니다.

**② 캡처 포인트: 동적 문제의 기하학적 변환**

캡처 포인트(Capture Point, CP)는 지면 위의 한 점으로 — 로봇이 그 점에 즉시 발을 디디고 질량 중심을 지지 발 바로 위에 놓을 수 있다면, 다음 걸음을 내딛지 않고도 멈출 수 있는 점입니다. **선형 도립진자 모델(Linear Inverted Pendulum Model, LIPM)**에서 질량 중심 높이 $z_c$가 일정하고, 질점이 무질량 막대를 통해 지면의 ZMP에 연결된다고 가정하면 LIPM의 수평 동역학은:

$$
\ddot{x} = \frac{g}{z_c}(x - x_{\text{ZMP}})
$$

캡처 포인트의 존재는 복잡한 동적 균형 문제를 "다음 발을 어디에 디뎌야 하는가"라는 기하학적 문제로 변환합니다. 서서 자세 유지 외란 저항 시나리오에서 외란이 작으면 로봇은 발목 관절 토크를 통해 캡처 포인트를 지지 다각형 내로 끌어올 수 있고, 외란이 크면 보행이 필요합니다 — 이것이 서서 자세 유지 외란 저항과 보행 계획의 인터페이스입니다.

**③ 제어 프레임워크: LQR과 MPC의 역할 분담**

LQR은 무한 시간 영역에서 최적이지만 제약을 명시적으로 처리할 수 없습니다 — 반면 휴머노이드 로봇의 핵심 제약은 정확히 하드 제약입니다: 관절 한계는 초과할 수 없고, 토크는 제한되며, ZMP는 지지 다각형 내에 있어야 합니다. MPC는 롤링 시간 영역 메커니즘을 통해 각 제어 주기마다 유한 시간 영역 개루프 최적 제어 문제를 풀지만 첫 번째 제어 단계만 실행하고, 다음 주기에는 새로운 상태 추정으로 다시 풉니다. MPC의 예측 모델은 각 해석 시 "현재 상태에 재정박"됩니다 — 이것이 MPC와 오프라인 궤적 최적화의 본질적 차이입니다.

서서 자세 유지 외란 저항 시나리오에서 MPC는 제약을 통합 처리할 수 있습니다: ZMP 지지 다각형 제약, 관절 토크 제한, 관절 위치 한계를 모두 최적화 문제에 포함시키고, 예측 시간 영역 내 롤링 최적화를 통해 선견성을 얻습니다. LQR은 작은 외란에서의 국소 선형화 제어나 MPC의 터미널 비용 함수에서 무한 시간 영역 근사로 자주 사용됩니다.

### 핵심 파라미터 및 사양

| 파라미터/사양 | 값/요구사항 | 출처 |
| --- | --- | --- |
| 실시간 해석 주파수 | ≥ 100 Hz | 말뭉치 |
| 후보 방안 수 | 2개 이상 | 말뭉치 |
| 핵심 제약 | 관절 한계 | 말뭉치 |
| 완성 기준 | 서서 자세 유지 외란 저항 실물 영상, 회복 능력 지표 충족 | 말뭉치 |

실시간 해석 주파수 ≥ 100 Hz는 제어 주기가 10 ms를 초과하지 않음을 의미합니다. MPC의 경우 이는 10 ms 내에 유한 시간 영역 최적 제어 문제를 한 번 해결해야 함을 요구합니다 — 이는 최적화 솔버의 계산 효율성에 매우 높은 요구를 하며, 방안 선정 시 실시간성을 평가해야 하는 근본 이유입니다(공학적 판단).

### 수평 비교

| 방법 | 제약 처리 능력 | 실시간성 | 적용 시나리오 |
| --- | --- | --- | --- |
| LQR | 약함(사후 제한 필요) | 매우 빠름(해석해) | 작은 외란, 선형화 모델 |
| MPC | 강함(명시적 하드 제약) | 느림(온라인 해석 필요) | 큰 외란, 제약 지배 시나리오 |
| ZMP/Capture Point | 중간(기하학적 판정) | 빠름(해석 계산) | 균형 판정, 착지점 계획 |
| 각운동량 제어 | 중간(질량 중심 모델 보완) | 빠름 | 전신 자세 조정 |

LQR과 MPC는 상호 배타적이지 않습니다 — 공학 실무에서 일반적인 방법은 MPC로 제약과 선견성을 처리하고, MPC 내부 또는 터미널에서 LQR 근사를 사용하는 것입니다(공학적 판단). Capture Point는 직관적인 기하학적 판정을 제공하며 MPC의 참조 궤적 생성기 또는 안전 모니터링 계층으로 사용될 수 있습니다.

### 누가 사용하는가·응용 사례

이 공정은 이족 휴머노이드 로봇 제품 개발 전체 프로세스(V3 버전)에서 P10.2 균형, 보행 및 WBC 작업 패키지의 구성 요소로, 이족 서서 자세 유지 및 외란 저항 능력이 필요한 모든 전신 휴머노이드 로봇 제품 개발 프로젝트에 적용됩니다. 응용 시나리오는 다음과 같습니다:

- **산업 순찰**: 로봇이 고르지 않은 지면에 서서 바람이나 케이블 당김 외란에 저항;
- **물류 운반**: 로봇이 양손으로 무거운 물건을 들고 서서 무게 중심이 이동할 때 균형 유지;
- **인간-로봇 상호작용**: 로봇이 인간의 밀침이나 두드림을 받을 때 넘어지지 않는 것, 이는 안전한 상호작용의 전제.

이 공정의 완성 기준 — 서서 자세 유지 외란 저항 실물 영상과 회복 능력 지표 충족 — 은 직접 제품 검수 근거로 사용되며, 출력 문서는 ICD/BOM/SOP/요구사항 추적 체인을 업데이트하여 하류 보행 계획 및 WBC 공정에 입력을 제공합니다.

### 한계와 경계

이 공정은 **서서 자세**에서의 외란 저항 균형에 초점을 맞추며, 경계 조건은 다음과 같습니다:

- **동적 보행 미포함**: 보행 중 균형은 후속 보행 계획 공정에서 처리되며, 본 공정은 서서 자세 시나리오만 처리;
- **큰 외란 보행 회복 미포함**: 캡처 포인트가 지지 다각형을 벗어나고 발목 관절 토크로 끌어올 수 없을 때 보행 전략이 필요합니다 — 이는 서서 자세 유지 외란 저항과 보행 계획의 인터페이스에 속하며, 본 공정의 경계는 "회복 능력이 지표를 충족"하는 것이고, 구체적 지표 값은 P10.2.1.1에서 정량화해야 합니다;
- **모델 단순화 가정**: ZMP와 Capture Point의 유도는 LIPM 가정(질량 중심 높이 일정)에 기반하며, 실제 로봇의 질량 중심 높이는 변동이 있으므로 각운동량 제어로 보상해야 합니다;
- **실시간성 병목**: MPC의 ≥ 100 Hz 주파수에서의 온라인 해석은 연산 능력에 하드 요구를 하며, 하드웨어가 충족하지 못하면 모델 차수 축소 또는 명시적 MPC를 사용해야 합니다(공학적 판단).

### 흔한 오해

1. **"서서 균형은 PID만으로 충분하다"** — 틀림. PID는 관절 한계와 ZMP 지지 다각형 제약을 명시적으로 처리할 수 없으며, 큰 외란에서 선견성이 부족합니다. 이 공정은 MPC와 같은 최적화 제어 프레임워크를 명시적으로 요구하는데, 이는 제약을 통합 처리하기 위함입니다.

2. **"Capture Point는 보행에만 사용된다"** — 틀림. Capture Point는 서서 자세 유지 외란 저항에서도 핵심적입니다: "보행이 필요한지"의 판정을 제공합니다. 캡처 포인트가 지지 다각형 내에 있으면 발목 전략으로 회복할 수 있고, 경계 밖에 있으면 반드시 보행해야 합니다.

3. **"시뮬레이션 검증 통과하면 실물에 올려도 된다"** — 틀림. P10.2.1.4는 "테스트 실행 및 원시 데이터 기록", "문제 목록 및 개선 조치 출력"을 명시적으로 요구하며, 완성 기준은 "서서 자세 유지 외란 저항 실물 영상"입니다 — 시뮬레이션과 실물 사이에는 모델 오차, 지연, 노이즈 등의 차이가 있으므로 반드시 실물 파라미터 튜닝 검증을 거쳐야 합니다.

4. **"실시간 주파수는 높을수록 좋다"** — 틀림. ≥ 100 Hz는 하한이지 상한이 아닙니다. 주파수가 너무 높으면 MPC 해석 시간 부족, 제어 주기 지터가 발생할 수 있고, 너무 낮으면 외란 동역학을 포착할 수 없습니다. 합리적 주파수는 로봇 동역학 특성과 하드웨어 연산 능력에 따라 종합적으로 결정해야 합니다(공학적 판단).

### 관련 지식

- `ent_principle_capture_point` — 캡처 포인트는 서서 자세 유지 외란 저항에서 "보행 필요 여부"를 판단하는 핵심 기준으로, 동적 균형 문제를 착지점 기하학 문제로 변환합니다.
- `ent_principle_zero_moment_point` — ZMP는 서서 균형의 핵심 안정성 판정 기준이며, 제어 목표는 ZMP를 지지 다각형 내에 유지하는 것입니다.
- `ent_method_model_predictive_control` — MPC는 이 공정의 핵심 제어 프레임워크로, 롤링 시간 영역 최적화를 통해 ZMP 제약, 관절 한계 및 토크 제한을 통합 처리합니다.
- `ent_paper_linear_quadratic_regulator_lqr` — LQR은 선형 최적 제어 방법으로, 작은 외란 시나리오 또는 MPC 터미널 비용에서 역할을 합니다.
- `ent_process_p10` — 본 공정(P10.2.1)은 P10.2 균형, 보행 및 WBC 작업 패키지의 첫 번째 하위 작업이며, 그 출력은 후속 보행 계획 및 WBC의 기초를 제공합니다.
- `ent_paper_roncone_gaze_stabilization_for_humanoi_2014` — 휴머노이드 로봇 안정 제어 관련 연구로, 외란 저항 균형 제어의 참고 자료로 사용될 수 있습니다.
