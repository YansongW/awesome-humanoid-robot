---
$id: ent_method_force_position_hybrid_control
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Force/Position Hybrid Control
  zh: 力位混合控制
  ko: 힘/위치 하이브리드 제어
summary:
  en: A control strategy that simultaneously regulates force along constrained directions and position along free directions
    in contact tasks.
  zh: 在许多任务中，某些方向需要控制位置，而另一些方向需要控制力。例如，沿桌面推动物体时，水平方向控制位置，垂直方向控制接触力。混合力/位置控制（hybrid force/position control）通过选择矩阵 \(\boldsymbol{\Sigma}\)
    把任务空间分解为位置控制子空间与力控制子空间：
  ko: 접촉 작업에서 제약 방향으로는 힘을, 자유 방향으로는 위치를 동시에 제어하는 전략.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- method
- chapter_14
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body backfilled from chapter-08.md#混合力/位置控制 by scripts/backfill_nonpaper_entries.py. | WP4 trilingual backfill 2026-08-10:
    closed unclosed code fence(s) and removed duplicate stale translation block(s) (pre-existing ingestion defect).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

力位混合控制（Force/Position Hybrid Control）是一种将任务空间按方向正交分解、分别进行位置控制与力控制的操作策略。它通过选择矩阵 \(\boldsymbol{\Sigma}\) 将任务空间划分为位置控制子空间与力控制子空间，在约束方向（如垂直于桌面的法向）控制接触力，在自由方向（如沿桌面的切向）控制位置。它真正改变的不是"同时控制力和位置"这一朴素愿望，而是把"哪个方向该控什么"这一任务语义显式编码进控制律，使机器人能在接触丰富的操作中兼顾几何精度与交互安全。

## 核心内容

### 是什么：一个方向，一种控制目标

力位混合控制解决的是操作任务中一个根本性的不对称：**不同方向对控制目标的要求本质不同**。沿桌面推动物体时，水平方向需要精确跟踪期望位置（否则物体会偏离目标轨迹），而垂直方向需要维持期望接触力（否则要么压坏桌面，要么失去接触）。若对所有方向都做位置控制，接触力会因环境刚度的不确定性而失控；若对所有方向都做力控制，自由方向的位置精度又无法保证。

该方法的数学核心是选择矩阵 \(\boldsymbol{\Sigma}\)，一个对角 0/1 矩阵：

$$
\boldsymbol{\Sigma} = \text{diag}(\sigma_1, \ldots, \sigma_m), \quad \sigma_i \in \{0,1\}
$$

- 若 \(\sigma_i = 1\)，第 \(i\) 个方向控制位置。
- 若 \(\sigma_i = 0\)，第 \(i\) 个方向控制力。

由此，任务空间被正交分解为两个互补的子空间：位置控制子空间（\(\boldsymbol{\Sigma}\) 的像空间）与力控制子空间（\(\mathbf{I} - \boldsymbol{\Sigma}\) 的像空间）。控制律为：

$$
\mathbf{F}_{\text{cmd}} = \boldsymbol{\Sigma} \, \mathbf{K}_p (\mathbf{x}_d - \mathbf{x}) + (\mathbf{I} - \boldsymbol{\Sigma}) \, \mathbf{K}_f (\mathbf{F}_d - \mathbf{F})
$$

其中 \(\mathbf{x}_d - \mathbf{x}\) 为位置误差，\(\mathbf{F}_d - \mathbf{F}\) 为力误差，\(\mathbf{K}_p\) 与 \(\mathbf{K}_f\) 分别为位置与力的比例增益。最终通过 \(\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}\) 将任务空间力映射到关节力矩。

### 为什么存在：单一控制模式的天然缺陷

在力位混合控制出现之前，机器人操作面临两难。纯位置控制（如工业机器人最常见的 PTP/CP 模式）在接触任务中极其脆弱：环境位置误差哪怕只有几毫米，接触力就可能因环境刚度过大而飙升到破坏性水平。纯力控制则相反，在自由空间几乎无法用——力误差信号在无接触时没有意义，位置漂移不可避免。

力位混合控制的历史定位，正是为"既有几何约束、又有力约束"的任务提供第一套系统化的控制框架。它不试图用一个统一目标近似两种需求，而是承认任务空间的各向异性，用选择矩阵显式声明每个方向的控制模式。这一思想后来深刻影响了阻抗控制与导纳控制的设计——后两者虽不显式分解方向，但本质上也是在力与位置之间做动态折中。

### 原理拆解

**① 选择矩阵：任务语义的数学编码**

选择矩阵 \(\boldsymbol{\Sigma}\) 的物理含义是"约束方向检测"。在接触任务中，环境几何对运动施加了自然约束：垂直于桌面的方向被桌面"锁死"，机器人只能施加力而不能自由运动；平行于桌面的方向则不受约束，机器人可以自由运动但无法施加稳定力。力位混合控制的核心洞察是：**约束方向应控力，自由方向应控位**。\(\boldsymbol{\Sigma}\) 正是这一语义的数学表达。

**② 正交分解：避免通道间耦合**

位置控制与力控制被分配到正交子空间，意味着位置误差不会在力通道产生控制作用，力误差也不会在位置通道产生控制作用。这种解耦是力位混合控制区别于"加权求和"式柔顺控制的关键——后者本质上仍是单一控制目标，只是把位置误差与力误差按权重混合，无法真正实现"一个方向严格控位、另一个方向严格控力"。

**③ 关节力矩映射：从任务空间到关节空间**

控制律输出的 \(\mathbf{F}_{\text{cmd}}\) 是任务空间广义力，需通过雅可比转置映射为关节力矩：

$$
\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}
$$

这一映射假设机器人处于非冗余、非奇异构型。在人形机器人上，由于存在浮动基座与全身协调需求，通常需要将力位混合控制嵌入全身控制（WBC）框架，与运动学约束、动力学优化联合求解。

### 关键参数与规格

| 参数 | 含义 | 典型取值/说明 |
|------|------|--------------|
| \(\boldsymbol{\Sigma}\) | 选择矩阵，对角 0/1 | 1 表示位置通道，0 表示力通道；需按任务方向预先设定 |
| \(\mathbf{K}_p\) | 位置比例增益 | 决定位置通道的刚度，需低于环境刚度以避免冲击（工程判断） |
| \(\mathbf{K}_f\) | 力比例增益 | 决定力通道的收敛速度，过大会引起力振荡（工程判断） |
| \(\mathbf{J}^T\) | 雅可比转置 | 将任务空间力映射到关节力矩，需保证机器人非奇异 |
| 控制频率 | 力控环更新率 | 需显著高于接触动力学频率，通常不低于 1 kHz（工程判断） |

### 横向对比：三大力控框架的定位

| 框架 | 核心思想 | 适用场景 | 关键局限 |
|------|---------|---------|---------|
| 阻抗控制 | 将机器人表现为质量-弹簧-阻尼系统，通过调节阻抗参数间接控制力 | 腿部落地冲击吸收、不确定环境接触 | 力精度依赖环境位置估计，环境刚度未知时力误差大 |
| 导纳控制 | 外环力误差生成位置修正量，内环位置控制跟踪 | 有腕力传感器时的稳定接触力跟踪 | 需要高精度位置内环，力环带宽受限 |
| 力位混合控制 | 按方向正交分解，位置通道与力通道独立控制 | 插轴入孔、曲面打磨、桌面推动 | 接触切换时选择矩阵突变，易引起力冲击 |

工程上的选择规律是：**关节力矩可控性好时用阻抗，位置可控性好时用导纳**。人形机器人的腿部多用阻抗（吸收落地冲击），臂部在有腕力传感器时常用导纳（稳定跟踪接触力）。力位混合控制则适用于任务方向语义明确、且需要同时保证几何精度与力安全的场景。

### 谁在用·应用案例

**工业装配**是力位混合控制最经典的应用场景。插轴入孔（peg-in-hole）任务中，轴向需要控制插入力（防止卡死或损坏工件），径向需要控制位置（保证对准孔心）。传统位置控制方案在轴孔间隙较小时极易因微小位置误差导致卡死，力位混合控制通过轴向力控、径向位控的分解，显著提高了装配成功率。

**人形机器人操作**中，力位混合控制常与全身控制结合。例如，人形机器人推动桌面上的物体时，手臂水平方向跟踪期望轨迹，垂直方向维持恒定接触力；又如，人形机器人用抹布擦桌子时，法向力控保证清洁压力一致，切向位控保证覆盖路径完整。

**接触切换场景**是力位混合控制最具挑战性的应用。从自由空间运动到接触建立，各方向的角色突变——原本自由的方向突然变成约束方向，原本控位的方向突然需要控力。若选择矩阵切换不平滑，会引起力冲击与失稳。实践中常将力位混合与阻抗框架结合，即"变阻抗 + 方向选择"，在接触瞬间先降低接近方向刚度，再逐步建立目标接触力。

### 局限与边界

力位混合控制最经典的难点是**接触切换时的选择矩阵切换**。从自由空间运动到接触建立，各方向的角色突变，若切换不平滑会引起力冲击。这一问题的根源在于：选择矩阵是离散的 0/1 变量，而接触状态是连续演化的物理过程。离散切换与连续物理之间的鸿沟，是力位混合控制固有的理论局限。

此外，力位混合控制对**环境几何模型**有较强依赖。选择矩阵的设定需要预先知道哪些方向受约束、哪些方向自由，这要求环境几何的准确建模。在未知或动态变化的环境中，选择矩阵的在线估计本身就是一个难题。

力位混合控制还面临**通道间动态耦合**的挑战。虽然位置与力通道在任务空间正交，但通过机器人动力学（尤其是惯性耦合与科氏力）仍会相互影响。在高速运动或大惯量负载下，这种耦合可能破坏控制性能。

### 常见误区

1. **"力位混合控制就是同时控力和位置"**——不准确。它不是在每个方向都同时控力和位置，而是按方向正交分解：每个方向要么控力、要么控位，二者互斥。真正的"同时"只发生在不同方向之间。

2. **"选择矩阵可以随意设定"**——错误。选择矩阵必须与任务的环境约束匹配：约束方向必须设为力通道，自由方向必须设为位置通道。设反了会导致控制失稳或任务失败。

3. **"力位混合控制比阻抗控制更高级"**——不是。三种力控框架各有适用场景。力位混合控制在方向语义明确时优势明显，但在接触切换频繁、环境不确定时，阻抗控制的鲁棒性更好。

4. **"力位混合控制不需要力传感器"**——错误。力通道需要实时的力反馈信号，通常来自腕部力/力矩传感器或关节力矩传感器。没有力反馈，力通道的控制律无法闭合。

### 相关知识

- `ent_method_admittance_control` — 导纳控制是力位混合控制的姊妹框架，通过外环力误差生成位置修正量，适用于有腕力传感器时的稳定接触力跟踪。
- `ent_paper_yu_forcevla_enhancing_vla_models_2025` — ForceVLA 将外部力传感提升为第一类模态，在动作解码阶段动态整合力反馈，代表了力控思想向学习型策略的延伸。
- `ent_paper_one_shot_multimodal_learning_f_2026` — 该工作从单次示范中同时复现运动与接触力特征，其力感知运动基元与力位混合控制的方向分解思想一脉相承。
- `ent_paper_wang_tacrefinenet_tactile_only_gras_2025` — TacRefineNet 通过纯触觉反馈迭代调整抓取位姿，其触觉-动作闭环与力位混合控制的力反馈闭环在原理上相通。
- `ent_paper_hafo_a_force_adaptive_control_2025` — HAFO 的力自适应控制方法可视为力位混合控制在动态接触场景下的扩展，强调接触力的在线适应而非固定方向分解。

## 参考

- [Humanoid Robot Knowledge Base - Chapter 16](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-16.md)
- [Humanoid Robot Knowledge Base - Chapter 02](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)

## Overview

Force/Position Hybrid Control is an operational strategy that orthogonally decomposes the task space by direction, applying position control and force control separately. It uses a selection matrix \(\boldsymbol{\Sigma}\) to partition the task space into a position-controlled subspace and a force-controlled subspace, controlling contact force in constrained directions (e.g., the normal direction perpendicular to a table surface) and position in free directions (e.g., the tangential direction along the table surface). What it truly changes is not the naive desire to "control force and position simultaneously," but rather explicitly encoding the task semantics of "which direction should control what" into the control law, enabling robots to balance geometric precision and interaction safety in contact-rich manipulation.

## Content

### What It Is: One Direction, One Control Objective

Force/Position Hybrid Control addresses a fundamental asymmetry in manipulation tasks: **different directions have inherently different control objectives**. When pushing an object along a table, the horizontal direction requires precise tracking of the desired position (otherwise the object deviates from the target trajectory), while the vertical direction requires maintaining the desired contact force (otherwise either the table gets crushed or contact is lost). If position control is applied to all directions, contact forces become uncontrollable due to uncertainty in environmental stiffness; if force control is applied to all directions, positional accuracy in free directions cannot be guaranteed.

The mathematical core of this method is the selection matrix \(\boldsymbol{\Sigma}\), a diagonal 0/1 matrix:

$$
\boldsymbol{\Sigma} = \text{diag}(\sigma_1, \ldots, \sigma_m), \quad \sigma_i \in \{0,1\}
$$

- If \(\sigma_i = 1\), the \(i\)-th direction is position-controlled.
- If \(\sigma_i = 0\), the \(i\)-th direction is force-controlled.

Thus, the task space is orthogonally decomposed into two complementary subspaces: the position-controlled subspace (the image space of \(\boldsymbol{\Sigma}\)) and the force-controlled subspace (the image space of \(\mathbf{I} - \boldsymbol{\Sigma}\)). The control law is:

$$
\mathbf{F}_{\text{cmd}} = \boldsymbol{\Sigma} \, \mathbf{K}_p (\mathbf{x}_d - \mathbf{x}) + (\mathbf{I} - \boldsymbol{\Sigma}) \, \mathbf{K}_f (\mathbf{F}_d - \mathbf{F})
$$

where \(\mathbf{x}_d - \mathbf{x}\) is the position error, \(\mathbf{F}_d - \mathbf{F}\) is the force error, and \(\mathbf{K}_p\) and \(\mathbf{K}_f\) are the proportional gains for position and force, respectively. Finally, the task-space force is mapped to joint torques via \(\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}\).

### Why It Exists: Inherent Deficiencies of Single Control Modes

Before Force/Position Hybrid Control, robot manipulation faced a dilemma. Pure position control (e.g., the most common PTP/CP modes in industrial robots) is extremely fragile in contact tasks: even a few millimeters of environmental position error can cause contact forces to surge to destructive levels due to high environmental stiffness. Pure force control, conversely, is nearly unusable in free space—force error signals are meaningless without contact, and position drift becomes inevitable.

The historical role of Force/Position Hybrid Control is precisely to provide the first systematic control framework for tasks with both geometric and force constraints. It does not attempt to approximate both needs with a single unified objective; instead, it acknowledges the anisotropy of the task space and explicitly declares the control mode for each direction via the selection matrix. This idea later profoundly influenced the design of impedance control and admittance control—although the latter two do not explicitly decompose directions, they are essentially dynamic compromises between force and position.

### Principle Breakdown

**① Selection Matrix: Mathematical Encoding of Task Semantics**

The physical meaning of the selection matrix \(\boldsymbol{\Sigma}\) is "constraint direction detection." In contact tasks, environmental geometry imposes natural constraints on motion: the direction perpendicular to a table is "locked" by the table, allowing the robot to exert force but not move freely; the direction parallel to the table is unconstrained, allowing free motion but no stable force exertion. The core insight of Force/Position Hybrid Control is: **constrained directions should control force, free directions should control position**. \(\boldsymbol{\Sigma}\) is the mathematical expression of this semantics.

**② Orthogonal Decomposition: Avoiding Cross-Channel Coupling**

Position control and force control are assigned to orthogonal subspaces, meaning position errors do not produce control actions in the force channel, and force errors do not produce control actions in the position channel. This decoupling is what distinguishes Force/Position Hybrid Control from "weighted summation" style compliant control—the latter is essentially still a single control objective that blends position and force errors with weights, and cannot truly achieve "strict position control in one direction and strict force control in another."

**③ Joint Torque Mapping: From Task Space to Joint Space**

The \(\mathbf{F}_{\text{cmd}}\) output by the control law is a task-space generalized force, mapped to joint torques via the Jacobian transpose:

$$
\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}
$$

This mapping assumes a non-redundant, non-singular robot configuration. On humanoid robots, due to the floating base and whole-body coordination requirements, Force/Position Hybrid Control is typically embedded within a Whole-Body Control (WBC) framework, solved jointly with kinematic constraints and dynamic optimization.

### Key Parameters and Specifications

| Parameter | Meaning | Typical Value/Description |
|-----------|---------|---------------------------|
| \(\boldsymbol{\Sigma}\) | Selection matrix, diagonal 0/1 | 1 indicates position channel, 0 indicates force channel; must be preset according to task directions |
| \(\mathbf{K}_p\) | Position proportional gain | Determines stiffness of the position channel; must be lower than environmental stiffness to avoid impacts (engineering judgment) |
| \(\mathbf{K}_f\) | Force proportional gain | Determines convergence speed of the force channel; excessive values cause force oscillations (engineering judgment) |
| \(\mathbf{J}^T\) | Jacobian transpose | Maps task-space forces to joint torques; requires a non-singular robot configuration |
| Control frequency | Force control loop update rate | Must be significantly higher than contact dynamics frequency, typically no less than 1 kHz (engineering judgment) |

### Horizontal Comparison: Positioning of Three Major Force Control Frameworks

| Framework | Core Idea | Applicable Scenarios | Key Limitations |
|-----------|-----------|----------------------|-----------------|
| Impedance Control | Represents the robot as a mass-spring-damper system, indirectly controlling force by adjusting impedance parameters | Leg landing impact absorption, uncertain environment contact | Force accuracy depends on environmental position estimation; large force errors when environmental stiffness is unknown |
| Admittance Control | Outer loop generates position corrections from force errors, inner loop tracks position | Stable contact force tracking with wrist force sensors | Requires high-precision position inner loop; limited force loop bandwidth |
| Force/Position Hybrid Control | Orthogonal decomposition by direction, independent position and force channels | Peg-in-hole insertion, curved surface polishing, table pushing | Abrupt selection matrix changes during contact transitions, causing force impacts |

The engineering selection rule is: **use impedance when joint torque controllability is good, use admittance when position controllability is good**. Humanoid robot legs typically use impedance (to absorb landing impacts), while arms often use admittance when wrist force sensors are available (for stable contact force tracking). Force/Position Hybrid Control is suitable for scenarios with clear task direction semantics and the need to simultaneously guarantee geometric precision and force safety.

### Who Uses It · Application Cases

**Industrial assembly** is the most classic application scenario for Force/Position Hybrid Control. In peg-in-hole tasks, the axial direction requires controlling insertion force (to prevent jamming or workpiece damage), while the radial direction requires position control (to ensure alignment with the hole center). Traditional position control schemes are highly prone to jamming due to tiny position errors when the peg-hole clearance is small; Force/Position Hybrid Control significantly improves assembly success rates through axial force control and radial position control decomposition.

**Humanoid robot manipulation** often combines Force/Position Hybrid Control with whole-body control. For example, when a humanoid robot pushes an object on a table, the arm tracks the desired trajectory in the horizontal direction while maintaining constant contact force in the vertical direction; similarly, when wiping a table with a cloth, normal force control ensures consistent cleaning pressure, while tangential position control ensures complete path coverage.

**Contact transition scenarios** are the most challenging applications of Force/Position Hybrid Control. During the transition from free-space motion to contact establishment, the roles of each direction change abruptly—previously free directions suddenly become constrained, and previously position-controlled directions suddenly require force control. If the selection matrix transition is not smooth, force impacts and instability can occur. In practice, Force/Position Hybrid Control is often combined with impedance frameworks, i.e., "variable impedance + direction selection," reducing stiffness in the approach direction at the moment of contact before gradually establishing the target contact force.

### Limitations and Boundaries

The most classic difficulty of Force/Position Hybrid Control is **selection matrix switching during contact transitions**. During the transition from free-space motion to contact establishment, the roles of each direction change abruptly, and unsmooth switching can cause force impacts. The root cause of this problem is that the selection matrix is a discrete 0/1 variable, while the contact state is a continuously evolving physical process. The gap between discrete switching and continuous physics is an inherent theoretical limitation of Force/Position Hybrid Control.

Additionally, Force/Position Hybrid Control has a **strong dependence on environmental geometric models**. Setting the selection matrix requires prior knowledge of which directions are constrained and which are free, demanding accurate modeling of the environment geometry. In unknown or dynamically changing environments, online estimation of the selection matrix is itself a challenging problem.

Force/Position Hybrid Control also faces the challenge of **cross-channel dynamic coupling**. Although the position and force channels are orthogonal in task space, they still influence each other through robot dynamics (especially inertial coupling and Coriolis forces). Under high-speed motion or large inertial loads, this coupling can degrade control performance.

### Common Misconceptions

1. **"Force/Position Hybrid Control means controlling force and position simultaneously"**—inaccurate. It does not control both force and position in every direction; rather, it orthogonally decomposes by direction: each direction either controls force or position, and the two are mutually exclusive. True "simultaneity" only occurs across different directions.

2. **"The selection matrix can be set arbitrarily"**—wrong. The selection matrix must match the task's environmental constraints: constrained directions must be set to force channels, and free directions must be set to position channels. Setting them incorrectly leads to control instability or task failure.

3. **"Force/Position Hybrid Control is more advanced than impedance control"**—not true. The three force control frameworks each have their applicable scenarios. Force/Position Hybrid Control has clear advantages when direction semantics are well-defined, but impedance control offers better robustness in scenarios with frequent contact transitions and environmental uncertainty.

4. **"Force/Position Hybrid Control does not require force sensors"**—wrong. The force channel requires real-time force feedback signals, typically from wrist force/torque sensors or joint torque sensors. Without force feedback, the control law in the force channel cannot be closed.

### Related Knowledge

- `ent_method_admittance_control` — Admittance control is a sister framework to Force/Position Hybrid Control, generating position corrections from outer-loop force errors, suitable for stable contact force tracking with wrist force sensors.
- `ent_paper_yu_forcevla_enhancing_vla_models_2025` — ForceVLA elevates external force sensing to a first-class modality, dynamically integrating force feedback during action decoding, representing an extension of force control ideas toward learning-based policies.
- `ent_paper_one_shot_multimodal_learning_f_2026` — This work reproduces both motion and contact force features from a single demonstration, and its force-aware movement primitives share the same lineage as the directional decomposition idea of Force/Position Hybrid Control.
- `ent_paper_wang_tacrefinenet_tactile_only_gras_2025` — TacRefineNet iteratively adjusts grasp poses through pure tactile feedback, and its tactile-action closed loop is conceptually similar to the force feedback loop of Force/Position Hybrid Control.
- `ent_paper_hafo_a_force_adaptive_control_2025` — HAFO's force-adaptive control method can be viewed as an extension of Force/Position Hybrid Control to dynamic contact scenarios, emphasizing online adaptation of contact forces rather than fixed directional decomposition.

## 개요

힘/위치 혼합 제어(Force/Position Hybrid Control)는 작업 공간을 방향별로 직교 분해하여 각각 위치 제어와 힘 제어를 수행하는 조작 전략입니다. 선택 행렬 \(\boldsymbol{\Sigma}\)을 통해 작업 공간을 위치 제어 부분 공간과 힘 제어 부분 공간으로 나누며, 구속 방향(예: 테이블 표면에 수직인 법선 방향)에서는 접촉력을 제어하고 자유 방향(예: 테이블 표면에 평행한 접선 방향)에서는 위치를 제어합니다. 이 방법이 실제로 바꾸는 것은 "힘과 위치를 동시에 제어한다"는 단순한 바람이 아니라, "어느 방향에서 무엇을 제어할지"라는 작업 의미론을 제어 법칙에 명시적으로 부호화하여, 접촉이 풍부한 조작에서 기하학적 정밀도와 상호작용 안전성을 동시에 확보하는 것입니다.

## 핵심 내용

### 무엇인가: 하나의 방향, 하나의 제어 목표

힘/위치 혼합 제어는 조작 작업에서 근본적인 비대칭성을 해결합니다: **방향에 따라 제어 목표에 대한 요구가 본질적으로 다릅니다.** 테이블 위에서 물체를 밀 때, 수평 방향은 목표 위치를 정밀하게 추적해야 하며(그렇지 않으면 물체가 목표 궤적에서 벗어남), 수직 방향은 목표 접촉력을 유지해야 합니다(그렇지 않으면 테이블을 부수거나 접촉을 잃게 됨). 모든 방향에 위치 제어를 적용하면 환경 강성의 불확실성으로 인해 접촉력이 통제 불능이 되고, 모든 방향에 힘 제어를 적용하면 자유 방향의 위치 정밀도를 보장할 수 없습니다.

이 방법의 수학적 핵심은 선택 행렬 \(\boldsymbol{\Sigma}\)로, 대각 0/1 행렬입니다:

$$
\boldsymbol{\Sigma} = \text{diag}(\sigma_1, \ldots, \sigma_m), \quad \sigma_i \in \{0,1\}
$$

- \(\sigma_i = 1\)이면, \(i\)번째 방향은 위치를 제어합니다.
- \(\sigma_i = 0\)이면, \(i\)번째 방향은 힘을 제어합니다.

이로써 작업 공간은 두 개의 상보적 부분 공간으로 직교 분해됩니다: 위치 제어 부분 공간(\(\boldsymbol{\Sigma}\)의 상공간)과 힘 제어 부분 공간(\(\mathbf{I} - \boldsymbol{\Sigma}\)의 상공간). 제어 법칙은 다음과 같습니다:

$$
\mathbf{F}_{\text{cmd}} = \boldsymbol{\Sigma} \, \mathbf{K}_p (\mathbf{x}_d - \mathbf{x}) + (\mathbf{I} - \boldsymbol{\Sigma}) \, \mathbf{K}_f (\mathbf{F}_d - \mathbf{F})
$$

여기서 \(\mathbf{x}_d - \mathbf{x}\)는 위치 오차, \(\mathbf{F}_d - \mathbf{F}\)는 힘 오차, \(\mathbf{K}_p\)와 \(\mathbf{K}_f\)는 각각 위치와 힘의 비례 이득입니다. 최종적으로 \(\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}\)를 통해 작업 공간 힘을 관절 토크로 매핑합니다.

### 왜 존재하는가: 단일 제어 모드의 자연적 결함

힘/위치 혼합 제어가 등장하기 전, 로봇 조작은 딜레마에 직면했습니다. 순수 위치 제어(예: 산업용 로봇에서 가장 흔한 PTP/CP 모드)는 접촉 작업에서 극도로 취약합니다: 환경 위치 오차가 수 밀리미터에 불과해도 접촉력이 환경 강성 때문에 파괴적 수준으로 급증할 수 있습니다. 순수 힘 제어는 반대로 자유 공간에서 거의 사용할 수 없습니다—접촉이 없을 때 힘 오차 신호는 의미가 없으며 위치 드리프트가 불가피합니다.

힘/위치 혼합 제어의 역사적 위상은 "기하학적 구속과 힘 구속이 모두 있는" 작업을 위한 최초의 체계적 제어 프레임워크를 제공한 것입니다. 이는 하나의 통합 목표로 두 요구를 근사하려 하지 않고, 작업 공간의 이방성을 인정하며 선택 행렬로 각 방향의 제어 모드를 명시적으로 선언합니다. 이 사상은 이후 임피던스 제어와 어드미턴스 제어 설계에 깊은 영향을 미쳤습니다—후자들은 방향을 명시적으로 분해하지 않지만, 본질적으로도 힘과 위치 사이에서 동적 절충을 수행합니다.

### 원리 분해

**① 선택 행렬: 작업 의미론의 수학적 부호화**

선택 행렬 \(\boldsymbol{\Sigma}\)의 물리적 의미는 "구속 방향 검출"입니다. 접촉 작업에서 환경 기하학은 운동에 자연적 구속을 부과합니다: 테이블에 수직인 방향은 테이블에 의해 "잠겨" 로봇은 힘만 가할 수 있고 자유롭게 움직일 수 없습니다; 테이블에 평행한 방향은 구속이 없어 로봇은 자유롭게 움직일 수 있지만 안정적인 힘을 가할 수 없습니다. 힘/위치 혼합 제어의 핵심 통찰은 다음과 같습니다: **구속 방향은 힘을 제어하고, 자유 방향은 위치를 제어해야 합니다.** \(\boldsymbol{\Sigma}\)는 바로 이 의미론의 수학적 표현입니다.

**② 직교 분해: 채널 간 커플링 방지**

위치 제어와 힘 제어가 직교 부분 공간에 할당된다는 것은 위치 오차가 힘 채널에서 제어 작용을 생성하지 않고, 힘 오차도 위치 채널에서 제어 작용을 생성하지 않음을 의미합니다. 이러한 분리는 힘/위치 혼합 제어가 "가중 합산" 방식의 컴플라이언트 제어와 구별되는 핵심입니다—후자는 본질적으로 여전히 단일 제어 목표이며 위치 오차와 힘 오차를 가중치로 혼합할 뿐, "한 방향은 엄격히 위치 제어, 다른 방향은 엄격히 힘 제어"를 실제로 구현할 수 없습니다.

**③ 관절 토크 매핑: 작업 공간에서 관절 공간으로**

제어 법칙이 출력하는 \(\mathbf{F}_{\text{cmd}}\)는 작업 공간 일반화 힘이며, 야코비안 전치를 통해 관절 토크로 매핑해야 합니다:

$$
\boldsymbol{\tau} = \mathbf{J}^T \mathbf{F}_{\text{cmd}}
$$

이 매핑은 로봇이 비-중복, 비-특이 형상에 있다고 가정합니다. 휴머노이드 로봇에서는 플로팅 베이스와 전신 협조 요구가 있으므로, 일반적으로 힘/위치 혼합 제어를 전신 제어(WBC) 프레임워크에 내장하여 운동학적 구속 및 동역학적 최적화와 함께 연립으로 해결합니다.

### 주요 파라미터 및 사양

| 파라미터 | 의미 | 일반적인 값/설명 |
|------|------|--------------|
| \(\boldsymbol{\Sigma}\) | 선택 행렬, 대각 0/1 | 1은 위치 채널, 0은 힘 채널; 작업 방향에 따라 사전 설정 필요 |
| \(\mathbf{K}_p\) | 위치 비례 이득 | 위치 채널의 강성을 결정하며, 충격을 피하기 위해 환경 강성보다 낮아야 함(공학적 판단) |
| \(\mathbf{K}_f\) | 힘 비례 이득 | 힘 채널의 수렴 속도를 결정하며, 너무 크면 힘 진동 유발(공학적 판단) |
| \(\mathbf{J}^T\) | 야코비안 전치 | 작업 공간 힘을 관절 토크로 매핑하며, 로봇이 특이 형상이 아님을 보장해야 함 |
| 제어 주파수 | 힘 제어 루프 업데이트율 | 접촉 동역학 주파수보다 현저히 높아야 하며, 일반적으로 1 kHz 이상(공학적 판단) |

### 수평 비교: 세 가지 힘 제어 프레임워크의 위치

| 프레임워크 | 핵심 사상 | 적용 시나리오 | 주요 한계 |
|------|---------|---------|---------|
| 임피던스 제어 | 로봇을 질량-스프링-댐퍼 시스템으로 표현하고, 임피던스 파라미터 조절로 힘을 간접 제어 | 다리 착지 충격 흡수, 불확실한 환경 접촉 | 힘 정밀도가 환경 위치 추정에 의존하며, 환경 강성 미지 시 힘 오차 큼 |
| 어드미턴스 제어 | 외부 루프 힘 오차가 위치 보정량을 생성하고, 내부 루프 위치 제어가 추적 | 손목 힘 센서가 있을 때 안정적인 접촉력 추적 | 고정밀 위치 내부 루프 필요, 힘 루프 대역폭 제한적 |
| 힘/위치 혼합 제어 | 방향별 직교 분해, 위치 채널과 힘 채널 독립 제어 | 축 삽입, 곡면 연마, 테이블 밀기 | 접촉 전환 시 선택 행렬 급변, 힘 충격 유발 가능 |

공학적 선택 규칙은 다음과 같습니다: **관절 토크 제어성이 좋으면 임피던스, 위치 제어성이 좋으면 어드미턴스**를 사용합니다. 휴머노이드 로봇의 다리는 주로 임피던스(착지 충격 흡수)를 사용하고, 팔은 손목 힘 센서가 있을 때 어드미턴스(접촉력 안정 추적)를 자주 사용합니다. 힘/위치 혼합 제어는 작업 방향 의미론이 명확하고 기하학적 정밀도와 힘 안전성을 동시에 보장해야 하는 시나리오에 적합합니다.

### 사용 사례·응용 사례

**산업용 조립**은 힘/위치 혼합 제어의 가장 고전적인 응용 시나리오입니다. 축 삽입(peg-in-hole) 작업에서 축 방향은 삽입력을 제어해야 하며(걸림 또는 부품 손상 방지), 반경 방향은 위치를 제어해야 합니다(구멍 중심 정렬 보장). 전통적인 위치 제어 방식은 축-구멍 간극이 작을 때 미세한 위치 오차로 인해 쉽게 걸릴 수 있으며, 힘/위치 혼합 제어는 축 방향 힘 제어와 반경 방향 위치 제어의 분해를 통해 조립 성공률을 크게 향상시킵니다.

**휴머노이드 로봇 조작**에서 힘/위치 혼합 제어는 종종 전신 제어와 결합됩니다. 예를 들어, 휴머노이드 로봇이 테이블 위 물체를 밀 때 팔의 수평 방향은 목표 궤적을 추적하고 수직 방향은 일정한 접촉력을 유지합니다; 또 다른 예로, 휴머노이드 로봇이 걸레로 테이블을 닦을 때 법선 방향 힘 제어로 청소 압력을 일정하게 유지하고 접선 방향 위치 제어로 커버 경로의 완전성을 보장합니다.

**접촉 전환 시나리오**는 힘/위치 혼합 제어의 가장 도전적인 응용입니다. 자유 공간 운동에서 접촉 확립으로 전환될 때 각 방향의 역할이 급변합니다—원래 자유였던 방향이 갑자기 구속 방향이 되고, 원래 위치 제어였던 방향이 갑자기 힘 제어가 필요해집니다. 선택 행렬 전환이 매끄럽지 않으면 힘 충격과 불안정이 발생합니다. 실제로는 힘/위치 혼합을 임피던스 프레임워크와 결합하는 경우가 많습니다, 즉 "가변 임피던스 + 방향 선택" 방식으로, 접촉 순간에 접근 방향 강성을 먼저 낮춘 후 목표 접촉력을 점진적으로 구축합니다.

### 한계와 경계

힘/위치 혼합 제어의 가장 고전적인 어려움은 **접촉 전환 시 선택 행렬의 전환**입니다. 자유 공간 운동에서 접촉 확립으로 전환될 때 각 방향의 역할이 급변하며, 전환이 매끄럽지 않으면 힘 충격이 발생할 수 있습니다. 이 문제의 근원은 선택 행렬이 이산적인 0/1 변수인 반면 접촉 상태는 연속적으로 진화하는 물리적 과정이라는 점입니다. 이산 전환과 연속 물리 사이의 간극은 힘/위치 혼합 제어의 고유한 이론적 한계입니다.

또한, 힘/위치 혼합 제어는 **환경 기하학 모델**에 대한 의존도가 높습니다. 선택 행렬의 설정은 어떤 방향이 구속되고 어떤 방향이 자유로운지 사전에 알아야 하며, 이는 환경 기하학의 정확한 모델링을 요구합니다. 미지의 또는 동적으로 변화하는 환경에서는 선택 행렬의 온라인 추정 자체가 어려운 문제입니다.

힘/위치 혼합 제어는 또한 **채널 간 동적 커플링**의 도전에 직면합니다. 위치와 힘 채널이 작업 공간에서 직교하지만, 로봇 동역학(특히 관성 커플링과 코리올리 힘)을 통해 여전히 서로 영향을 미칩니다. 고속 운동이나 대관성 부하에서는 이러한 커플링이 제어 성능을 저하시킬 수 있습니다.

### 일반적인 오해

1. **"힘/위치 혼합 제어는 힘과 위치를 동시에 제어하는 것이다"**—부정확합니다. 이는 모든 방향에서 힘과 위치를 동시에 제어하는 것이 아니라, 방향별로 직교 분해하여 각 방향은 힘 또는 위치 중 하나만 제어하며 둘은 상호 배타적입니다. 진정한 "동시"는 서로 다른 방향 사이에서만 발생합니다.

2. **"선택 행렬은 임의로 설정할 수 있다"**—오류입니다. 선택 행렬은 작업의 환경 구속과 일치해야 합니다: 구속 방향은 반드시 힘 채널로, 자유 방향은 반드시 위치 채널로 설정해야 합니다. 반대로 설정하면 제어 불안정 또는 작업 실패가 발생합니다.

3. **"힘/위치 혼합 제어가 임피던스 제어보다 더 고급이다"**—아닙니다. 세 가지 힘 제어 프레임워크는 각각 적용 시나리오가 있습니다. 힘/위치 혼합 제어는 방향 의미론이 명확할 때 장점이 뚜렷하지만, 접촉 전환이 빈번하고 환경이 불확실할 때는 임피던스 제어의 강건성이 더 좋습니다.

4. **"힘/위치 혼합 제어는 힘 센서가 필요 없다"**—오류입니다. 힘 채널은 실시간 힘 피드백 신호가 필요하며, 일반적으로 손목 힘/토크 센서 또는 관절 토크 센서에서 얻습니다. 힘 피드백이 없으면 힘 채널의 제어 법칙은 폐루프를 형성할 수 없습니다.

### 관련 지식

- `ent_method_admittance_control` — 어드미턴스 제어는 힘/위치 혼합 제어의 자매 프레임워크로, 외부 루프 힘 오차로 위치 보정량을 생성하며 손목 힘 센서가 있을 때 안정적인 접촉력 추적에 적합합니다.
- `ent_paper_yu_forcevla_enhancing_vla_models_2025` — ForceVLA는 외부 힘 감지를 제1종 모달리티로 승격시키고, 동작 디코딩 단계에서 힘 피드백을 동적으로 통합하여 힘 제어 사상의 학습 기반 정책으로의 확장을 대표합니다.
- `ent_paper_one_shot_multimodal_learning_f_2026` — 이 연구는 단일 시연에서 운동 및 접촉력 특징을 동시에 재현하며, 힘 인식 운동 프리미티브는 힘/위치 혼합 제어의 방향 분해 사상과 맥을 같이합니다.
- `ent_paper_wang_tacrefinenet_tactile_only_gras_2025` — TacRefineNet은 순수 촉각 피드백으로 파지 자세를 반복 조정하며, 촉각-동작 폐루프는 힘/위치 혼합 제어의 힘 피드백 폐루프와 원리적으로 상통합니다.
- `ent_paper_hafo_a_force_adaptive_control_2025` — HAFO의 힘 적응 제어 방법은 동적 접촉 시나리오에서 힘/위치 혼합 제어의 확장으로 볼 수 있으며, 고정된 방향 분해보다 접촉력의 온라인 적응을 강조합니다.
