---
$id: ent_process_p14_2_3
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: CI/CD and simulation test chain
  zh: CI/CD 与仿真测试链
  ko: CI/CD 및 시뮬레이션 테스트 체인
summary:
  en: CI pipeline, automatic test pass rate, coverage indicators
  zh: '- P14.2.3.1 输入梳理与目标量化 - 整理「CI/CD 与仿真测试链」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: CI 流水线、自动测试通过率、覆盖率指标
domains:
- 08_software_middleware
layers:
- intelligence
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

CI/CD 与仿真测试链（CI/CD and simulation test chain）是人形机器人软件中间件与系统集成阶段（P14）中的关键工序节点，通过 GitLab CI、Docker、SIL/HIL 自动化测试与代码覆盖率工具，构建从代码提交到自动化验证的持续集成流水线。它真正改变的不是"测试工具的数量"，而是"软件质量问题的发现时机"——让回归缺陷在合入主干之前就被拦截，而非等到整机联调时才暴露。该工序以五个三级子任务（P14.2.3.1 至 P14.2.3.5）展开，覆盖从输入量化到下游交付的完整工程闭环。

## 核心内容

### 是什么：准确定义

CI/CD 与仿真测试链是一套将**持续集成（Continuous Integration）**、**持续交付（Continuous Delivery）** 与**仿真测试（Simulation Testing）** 深度耦合的工程方法体系。在人形机器人开发流程中，它位于软件中间件与系统集成阶段（P14）内部，作为 P14.2 工作包的三级子任务存在，编号为 P14.2.3。

其核心构成包括四个技术支柱：

- **GitLab CI**：作为流水线编排引擎，定义代码从提交到构建、测试、打包的自动化流程；
- **Docker**：提供环境一致性保障，确保"在我机器上能跑"在流水线中不再成立；
- **SIL/HIL 自动化测试**：软件在环（Software-in-the-Loop）与硬件在环（Hardware-in-the-Loop）两级测试，分别验证算法逻辑正确性与控制器硬件时序行为；
- **代码覆盖率**：量化测试对代码路径的覆盖程度，作为质量门禁的量化依据。

该工序的完成标准明确为三件事：**CI 流水线**稳定运行、**自动测试通过率**达到预设阈值、**覆盖率指标**满足验收要求。

### 为什么存在：痛点与历史定位

人形机器人软件栈的复杂性远超普通嵌入式系统。上层是视觉感知、路径规划、大模型推理等 AI 任务，以 10–100 Hz 节奏运行；下层是关节控制环，需要 1 kHz 以上的更新率。两层之间通过中间件通信，任何一层的回归缺陷都可能引发整机异常。

在缺乏 CI/CD 与仿真测试链的时代，软件验证依赖两种低效手段：**人工集成**——开发者在本地手动合并代码、手动触发测试，效率低且极易遗漏回归；**整机实测**——将软件部署到物理样机上验证，成本高、周期长、风险大。一个典型的痛点场景是：某位开发者修改了通信协议的一个字段，本地编译通过、单测通过，但合入主干后导致其他模块的解析逻辑崩溃——这类问题在纯人工流程中往往要等到整机联调时才被发现。

CI/CD 与仿真测试链的历史定位，正是将验证环节**左移**（Shift-Left）：在代码合入主干之前，通过自动化流水线完成构建、静态检查、单元测试、SIL 仿真测试，必要时触发 HIL 硬件在环测试。它真正改变的不是"测试地点"，而是"缺陷发现的时间分布"——把原本集中在集成阶段的缺陷爆发，分散到每一次代码提交中。

### 原理拆解

**① 流水线即门禁：CI 的核心机制**

GitLab CI 的核心是**流水线（Pipeline）** 与**作业（Job）** 的层级结构。一次代码推送触发一条流水线，流水线由多个阶段（Stage）串联，每个阶段包含若干并行作业。典型阶段划分如下：

$$
\text{Pipeline:} \quad \underbrace{\text{build}}_{\text{编译}} \rightarrow \underbrace{\text{static\_check}}_{\text{静态检查}} \rightarrow \underbrace{\text{unit\_test}}_{\text{单元测试}} \rightarrow \underbrace{\text{sil\_test}}_{\text{软件在环}} \rightarrow \underbrace{\text{coverage}}_{\text{覆盖率门禁}}
$$

每个作业在 Docker 容器中运行，容器镜像锁定了编译工具链、依赖库版本与系统环境。这意味着**环境一致性由镜像不可变性保证**——同一份代码在任何时间、任何 runner 上构建，结果应当一致。作业的退出码（exit code）决定流水线是否继续：任一作业失败，后续阶段自动跳过，合入请求被阻止。

**② 两级仿真：SIL 与 HIL 的分工**

SIL（Software-in-the-Loop）将被测软件直接运行在宿主机或仿真环境中，被控对象为纯数学模型。它验证的是**算法逻辑正确性**——控制器的状态机转换、规划器的路径输出、通信协议的编解码是否与设计一致。SIL 的优点是速度快、成本低、易于并行，缺点是**不涉及真实硬件时序**。

HIL（Hardware-in-the-Loop）则将真实控制器硬件接入闭环，被控对象由实时仿真机解算。它验证的是**时序与接口正确性**——控制周期是否稳定、总线通信是否丢帧、中断响应是否及时。HIL 的闭环结构可表示为：

$$
\text{HIL 闭环:}\quad \underbrace{\text{真实控制器}}_{\text{真实硬件}} \;\xrightarrow{\text{控制指令}}\; \underbrace{\text{实时仿真机}}_{\text{虚拟被控对象}} \;\xrightarrow{\text{传感器反馈}}\; \text{真实控制器}
$$

两级仿真在流水线中的位置不同：SIL 作为常规门禁在每次提交时运行；HIL 因需要实时仿真机资源，通常在 nightly 构建或关键里程碑节点触发。

**③ 覆盖率作为质量门禁**

代码覆盖率（Code Coverage）衡量测试执行覆盖的代码行、分支或路径比例。它作为质量门禁的量化依据，防止"测试通过但大量代码未被执行"的假阳性。覆盖率指标通常与测试通过率共同构成完成标准中的量化 KPI。

### 关键参数与规格

| 参数/规格 | 说明 | 来源 |
|---|---|---|
| 候选方案数量 | 方案设计阶段需形成不少于 2 个候选方案 | 语料 P14.2.3.2 |
| 评估矩阵 | 对候选方案建立评估矩阵并量化打分 | 语料 P14.2.3.2 |
| 测试/评审计划 | 需制定测试/评审计划与通过准则 | 语料 P14.2.3.4 |
| 原始数据记录 | 测试执行需记录原始数据 | 语料 P14.2.3.4 |
| 问题清单 | 测试后需输出问题清单与改进措施 | 语料 P14.2.3.4 |
| 文档模板 | 按模板编写文档并引用原始数据 | 语料 P14.2.3.5 |
| 版本控制 | 完成内部评审与版本控制 | 语料 P14.2.3.5 |
| 下游通知 | 发布并通知下游依赖方 | 语料 P14.2.3.5 |

### 横向对比

在人形机器人软件验证体系中，CI/CD 与仿真测试链与相邻验证手段形成互补关系：

| 验证手段 | 验证对象 | 环境 | 时机 | 成本 |
|---|---|---|---|---|
| 单元测试 | 单个函数/模块 | 宿主机 | 每次提交 | 低 |
| SIL 仿真测试 | 算法逻辑/多模块集成 | 宿主机/仿真环境 | 每次提交 | 中 |
| HIL 硬件在环测试 | 控制器硬件+时序 | 实时仿真机 | Nightly/里程碑 | 高 |
| 整机实测 | 完整系统 | 物理样机 | 集成阶段 | 极高 |

CI/CD 与仿真测试链的独特价值在于**将前三级验证自动化、流水线化**，使得每次代码变更都能触发从单元到 SIL 的完整验证链。与纯人工测试相比，它真正改变的不是"测试是否执行"，而是"测试执行的频率与确定性"——人工测试可能因时间紧张而跳过，流水线则无条件执行。

### 谁在用·应用案例

该工序的典型应用场景包括：

- **人形机器人软件团队**：在开发运动控制、感知、导航等算法模块时，通过 CI 流水线确保每次合入不破坏既有功能。典型流程是：开发者推送代码 → GitLab CI 触发流水线 → Docker 容器中完成编译与单元测试 → SIL 仿真验证控制逻辑 → 覆盖率检查 → 全部通过后允许合入主干。
- **中间件与驱动开发**：修改通信协议或驱动接口时，通过 SIL 测试验证协议编解码正确性，通过 HIL 测试验证真实总线时序。这避免了"协议改了但驱动没跟上"的典型集成事故。
- **多团队协作场景**：当多个团队（运动控制、感知、导航）共享同一代码仓库时，CI 流水线作为**合入门禁**，确保任何团队的变更不破坏其他团队的功能。评估矩阵与量化打分用于在候选方案之间做出理性选择。

### 局限与边界

CI/CD 与仿真测试链并非万能，其局限包括：

- **算力约束**：SIL 仿真尤其是多关节动力学仿真需要较大算力，流水线并发执行时可能造成资源瓶颈。这是语料中明确列出的关键约束之一。
- **测试环境维护成本**：Docker 镜像需要持续更新以跟进依赖库版本；HIL 实时仿真机需要专业维护。环境维护本身成为持续性投入。
- **仿真与现实的差距**：SIL 中被控对象是数学模型，模型精度直接影响测试有效性；HIL 虽接入真实硬件，但被控对象仍是仿真。仿真无法完全替代整机实测，**仿真测试通过 ≠ 真机行为正确**。
- **覆盖率指标的局限性**：高覆盖率不保证高测试质量——测试可能覆盖了代码行但未覆盖关键边界条件。覆盖率是必要非充分条件。

### 常见误区

1. **"CI/CD 就是自动化编译"**——错。编译只是流水线的一个阶段。CI/CD 的核心是**持续验证**：每次提交都触发完整的测试链，包括单元测试、SIL 仿真、覆盖率检查。自动化编译只是第一步，自动化验证才是目的。
2. **"SIL 通过就可以上真机"**——错。SIL 验证的是算法逻辑，不涉及真实硬件时序。控制器在真机上可能因总线延迟、中断抖动、资源竞争而表现异常。SIL 与 HIL 是互补关系，不是替代关系。
3. **"覆盖率 100% 就是测试完备"**——错。覆盖率只说明代码行被执行过，不说明断言的有效性。一个覆盖了所有代码行但断言全部为空的测试套件，覆盖率 100% 也毫无价值。覆盖率指标必须与断言质量、边界条件覆盖共同考量。
4. **"Docker 解决了所有环境问题"**——错。Docker 保证了**构建环境**的一致性，但不保证**运行环境**的一致性。HIL 测试涉及真实硬件，其行为受物理因素影响，无法被容器化。Docker 解决的是"构建依赖"问题，不是"硬件行为"问题。

### 相关知识

- `ent_process_p14` — 本工序所属的上级工作包，软件中间件与系统集成阶段的完整任务框架。
- `ent_method_hardware_in_the_loop` — 本工序中 HIL 自动化测试环节的方法论基础，详细阐述硬件在环测试的闭环原理与实施要点。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

The CI/CD and simulation test chain is a critical process node in the software middleware and system integration phase (P14) of humanoid robot development. It builds a continuous integration pipeline from code commit to automated verification through GitLab CI, Docker, SIL/HIL automated testing, and code coverage tools. What it truly changes is not "the number of testing tools," but "the timing of software quality issue discovery"—allowing regression defects to be intercepted before merging into the main branch, rather than surfacing only during full-system integration. This process unfolds across five third-level subtasks (P14.2.3.1 to P14.2.3.5), covering a complete engineering loop from input quantification to downstream delivery.

## Content

### What It Is: Precise Definition

The CI/CD and simulation test chain is an engineering methodology system that deeply couples **Continuous Integration**, **Continuous Delivery**, and **Simulation Testing**. In the humanoid robot development workflow, it resides within the software middleware and system integration phase (P14), existing as a third-level subtask of work package P14.2, numbered P14.2.3.

Its core composition includes four technical pillars:

- **GitLab CI**: Serves as the pipeline orchestration engine, defining the automated flow from code commit to build, test, and packaging;
- **Docker**: Provides environment consistency guarantees, ensuring that "it works on my machine" no longer holds in the pipeline;
- **SIL/HIL Automated Testing**: Two levels of testing—Software-in-the-Loop and Hardware-in-the-Loop—which respectively validate algorithmic logic correctness and controller hardware timing behavior;
- **Code Coverage**: Quantifies the extent to which tests cover code paths, serving as the quantitative basis for quality gates.

The completion criteria for this process are explicitly defined as three things: the **CI pipeline** runs stably, the **automated test pass rate** reaches the preset threshold, and **coverage metrics** meet acceptance requirements.

### Why It Exists: Pain Points and Historical Positioning

The complexity of humanoid robot software stacks far exceeds that of ordinary embedded systems. The upper layer consists of AI tasks such as visual perception, path planning, and large model inference, running at 10–100 Hz; the lower layer is the joint control loop, requiring update rates above 1 kHz. The two layers communicate through middleware, and a regression defect in either layer can trigger anomalies across the entire machine.

In the era before CI/CD and simulation test chains, software verification relied on two inefficient methods: **manual integration**—developers manually merging code and triggering tests locally, which is inefficient and highly prone to missing regressions; and **full-machine physical testing**—deploying software to physical prototypes for verification, which is costly, slow, and risky. A typical pain point scenario: a developer modifies one field in a communication protocol, local compilation passes, unit tests pass, but after merging into the main branch, it causes parsing logic in other modules to crash—such issues in purely manual processes often go undetected until full-system integration.

The historical positioning of the CI/CD and simulation test chain is precisely to **shift-left** the verification stage: before code merges into the main branch, an automated pipeline completes builds, static checks, unit tests, and SIL simulation tests, triggering HIL hardware-in-the-loop tests when necessary. What it truly changes is not "where testing happens," but "the temporal distribution of defect discovery"—dispersing the defect outbreak originally concentrated in the integration phase across every code commit.

### Principle Breakdown

**① Pipeline as Gate: The Core Mechanism of CI**

The core of GitLab CI is the hierarchical structure of **Pipelines** and **Jobs**. A code push triggers a pipeline, which consists of multiple sequential stages, each containing several parallel jobs. A typical stage division is as follows:

$$
\text{Pipeline:} \quad \underbrace{\text{build}}_{\text{Compilation}} \rightarrow \underbrace{\text{static\_check}}_{\text{Static Check}} \rightarrow \underbrace{\text{unit\_test}}_{\text{Unit Test}} \rightarrow \underbrace{\text{sil\_test}}_{\text{Software-in-the-Loop}} \rightarrow \underbrace{\text{coverage}}_{\text{Coverage Gate}}
$$

Each job runs in a Docker container, where the container image locks in the compilation toolchain, dependency library versions, and system environment. This means **environment consistency is guaranteed by image immutability**—the same code built at any time on any runner should produce identical results. The job's exit code determines whether the pipeline continues: if any job fails, subsequent stages are automatically skipped, and the merge request is blocked.

**② Two-Level Simulation: The Division of Labor Between SIL and HIL**

SIL (Software-in-the-Loop) runs the software under test directly on the host machine or in a simulation environment, with the controlled object being a purely mathematical model. It validates **algorithmic logic correctness**—whether the controller's state machine transitions, the planner's path outputs, and the communication protocol's encoding/decoding match the design. SIL's advantages are speed, low cost, and ease of parallelization; its disadvantage is that it **does not involve real hardware timing**.

HIL (Hardware-in-the-Loop), by contrast, connects real controller hardware into the closed loop, with the controlled object solved by a real-time simulation machine. It validates **timing and interface correctness**—whether the control cycle is stable, whether bus communication drops frames, and whether interrupt responses are timely. The HIL closed-loop structure can be expressed as:

$$
\text{HIL Closed Loop:}\quad \underbrace{\text{Real Controller}}_{\text{Real Hardware}} \;\xrightarrow{\text{Control Commands}}\; \underbrace{\text{Real-Time Simulator}}_{\text{Virtual Controlled Object}} \;\xrightarrow{\text{Sensor Feedback}}\; \text{Real Controller}
$$

The two simulation levels occupy different positions in the pipeline: SIL runs as a regular gate on every commit; HIL, requiring real-time simulator resources, is typically triggered on nightly builds or key milestone nodes.

**③ Coverage as a Quality Gate**

Code coverage measures the proportion of code lines, branches, or paths executed by tests. It serves as the quantitative basis for quality gates, preventing false positives where "tests pass but large amounts of code are never executed." Coverage metrics, together with test pass rates, typically constitute the quantitative KPIs in completion criteria.

### Key Parameters and Specifications

| Parameter/Specification | Description | Source |
|---|---|---|
| Number of candidate solutions | At least 2 candidate solutions must be formed during the solution design phase | Corpus P14.2.3.2 |
| Evaluation matrix | Establish an evaluation matrix for candidate solutions and score quantitatively | Corpus P14.2.3.2 |
| Test/review plan | A test/review plan with pass criteria must be developed | Corpus P14.2.3.4 |
| Raw data recording | Raw data must be recorded during test execution | Corpus P14.2.3.4 |
| Issue list | An issue list with improvement measures must be output after testing | Corpus P14.2.3.4 |
| Document template | Documents must be written per the template and reference raw data | Corpus P14.2.3.5 |
| Version control | Internal review and version control must be completed | Corpus P14.2.3.5 |
| Downstream notification | Publish and notify downstream dependent parties | Corpus P14.2.3.5 |

### Horizontal Comparison

Within the humanoid robot software verification system, the CI/CD and simulation test chain complements adjacent verification methods:

| Verification Method | Verification Target | Environment | Timing | Cost |
|---|---|---|---|---|
| Unit testing | Individual functions/modules | Host machine | Every commit | Low |
| SIL simulation testing | Algorithmic logic/multi-module integration | Host machine/simulation environment | Every commit | Medium |
| HIL hardware-in-the-loop testing | Controller hardware + timing | Real-time simulator | Nightly/milestones | High |
| Full-machine physical testing | Complete system | Physical prototype | Integration phase | Extremely high |

The unique value of the CI/CD and simulation test chain lies in **automating and pipelining the first three levels of verification**, enabling every code change to trigger a complete verification chain from unit to SIL. Compared to purely manual testing, what it truly changes is not "whether tests are executed," but "the frequency and determinism of test execution"—manual testing may be skipped due to time pressure, while the pipeline executes unconditionally.

### Who Uses It: Application Cases

Typical application scenarios for this process include:

- **Humanoid robot software teams**: When developing algorithm modules such as motion control, perception, and navigation, the CI pipeline ensures that each merge does not break existing functionality. The typical flow is: developer pushes code → GitLab CI triggers the pipeline → compilation and unit tests complete in Docker containers → SIL simulation validates control logic → coverage check → merge into the main branch is allowed only after all pass.
- **Middleware and driver development**: When modifying communication protocols or driver interfaces, SIL tests validate protocol encoding/decoding correctness, and HIL tests validate real bus timing. This avoids the typical integration incident of "the protocol changed but the driver didn't keep up."
- **Multi-team collaboration scenarios**: When multiple teams (motion control, perception, navigation) share the same code repository, the CI pipeline serves as a **merge gate**, ensuring that any team's changes do not break other teams' functionality. The evaluation matrix and quantitative scoring are used to make rational choices among candidate solutions.

### Limitations and Boundaries

The CI/CD and simulation test chain is not a panacea; its limitations include:

- **Computational constraints**: SIL simulation, especially multi-joint dynamics simulation, requires significant computational resources, and concurrent pipeline execution may create resource bottlenecks. This is one of the key constraints explicitly listed in the corpus.
- **Test environment maintenance costs**: Docker images require continuous updates to track dependency library versions; HIL real-time simulators require specialized maintenance. Environment maintenance itself becomes a sustained investment.
- **The gap between simulation and reality**: In SIL, the controlled object is a mathematical model, and model accuracy directly affects test validity; although HIL connects real hardware, the controlled object remains simulated. Simulation cannot fully replace full-machine physical testing—**passing simulation tests ≠ correct real-machine behavior**.
- **Limitations of coverage metrics**: High coverage does not guarantee high test quality—tests may cover code lines but miss critical boundary conditions. Coverage is a necessary but not sufficient condition.

### Common Misconceptions

1. **"CI/CD is just automated compilation"**—Wrong. Compilation is only one stage of the pipeline. The core of CI/CD is **continuous verification**: every commit triggers a complete test chain, including unit tests, SIL simulation, and coverage checks. Automated compilation is only the first step; automated verification is the goal.
2. **"Passing SIL means ready for the real machine"**—Wrong. SIL validates algorithmic logic and does not involve real hardware timing. Controllers may behave abnormally on real machines due to bus latency, interrupt jitter, and resource contention. SIL and HIL are complementary, not substitutive.
3. **"100% coverage means complete testing"**—Wrong. Coverage only indicates that code lines were executed, not that assertions are effective. A test suite that covers all code lines but has entirely empty assertions is worthless even at 100% coverage. Coverage metrics must be considered together with assertion quality and boundary condition coverage.
4. **"Docker solves all environment problems"**—Wrong. Docker guarantees consistency of the **build environment**, but not the **runtime environment**. HIL testing involves real hardware whose behavior is influenced by physical factors and cannot be containerized. Docker solves the "build dependency" problem, not the "hardware behavior" problem.

### Related Knowledge

- `ent_process_p14` — The parent work package to which this process belongs, the complete task framework for the software middleware and system integration phase.
- `ent_method_hardware_in_the_loop` — The methodological foundation for the HIL automated testing component of this process, elaborating on the closed-loop principles and implementation points of hardware-in-the-loop testing.

## 개요

CI/CD 및 시뮬레이션 테스트 체인(CI/CD and simulation test chain)은 휴머노이드 로봇 소프트웨어 미들웨어 및 시스템 통합 단계(P14)의 핵심 공정 노드로, GitLab CI, Docker, SIL/HIL 자동화 테스트 및 코드 커버리지 도구를 통해 코드 커밋부터 자동화 검증까지의 지속적 통합 파이프라인을 구축합니다. 이 공정이 실제로 바꾸는 것은 "테스트 도구의 수"가 아니라 "소프트웨어 품질 문제의 발견 시점"입니다. 즉, 회귀 결함을 메인 브랜치 통합 전에 차단하고, 전체 기계 연동 디버깅 때까지 노출되는 것을 방지합니다. 이 공정은 5개의 세부 하위 작업(P14.2.3.1~P14.2.3.5)으로 전개되며, 입력 정량화부터 하류 전달까지의 완전한 엔지니어링 폐루프를 포괄합니다.

## 핵심 내용

### 정의: 정확한 정의

CI/CD 및 시뮬레이션 테스트 체인은 **지속적 통합(Continuous Integration)**, **지속적 전달(Continuous Delivery)** 및 **시뮬레이션 테스트(Simulation Testing)** 를 긴밀하게 결합한 엔지니어링 방법론 체계입니다. 휴머노이드 로봇 개발 프로세스에서 이는 소프트웨어 미들웨어 및 시스템 통합 단계(P14) 내부에 위치하며, P14.2 워크팩의 3차 하위 작업으로 번호 P14.2.3으로 존재합니다.

핵심 구성은 4가지 기술 기둥으로 이루어집니다:

- **GitLab CI**: 파이프라인 오케스트레이션 엔진으로, 코드 커밋부터 빌드, 테스트, 패키징까지의 자동화 프로세스를 정의합니다.
- **Docker**: 환경 일관성을 보장하여 "내 컴퓨터에서는 실행된다"는 말이 파이프라인에서 더 이상 성립하지 않게 합니다.
- **SIL/HIL 자동화 테스트**: 소프트웨어 인 더 루프(Software-in-the-Loop)와 하드웨어 인 더 루프(Hardware-in-the-Loop)의 2단계 테스트로, 각각 알고리즘 로직의 정확성과 컨트롤러 하드웨어 타이밍 동작을 검증합니다.
- **코드 커버리지**: 테스트가 코드 경로를 얼마나 덮는지 정량화하여 품질 게이트의 정량적 근거로 사용합니다.

이 공정의 완료 기준은 명확히 세 가지로 정의됩니다: **CI 파이프라인**이 안정적으로 운영되고, **자동 테스트 통과율**이 사전 설정된 임계값에 도달하며, **커버리지 지표**가 인수 요구사항을 충족해야 합니다.

### 존재 이유: 문제점과 역사적 위치

휴머노이드 로봇 소프트웨어 스택의 복잡성은 일반 임베디드 시스템을 훨씬 초과합니다. 상위 계층은 시각 인식, 경로 계획, 대규모 모델 추론 등의 AI 작업으로 10–100 Hz 주기로 실행됩니다. 하위 계층은 관절 제어 루프로 1 kHz 이상의 업데이트율이 필요합니다. 두 계층 사이는 미들웨어 통신으로 연결되며, 어느 한 계층의 회귀 결함도 전체 기계 이상을 유발할 수 있습니다.

CI/CD 및 시뮬레이션 테스트 체인이 없던 시대에는 소프트웨어 검증이 두 가지 비효율적인 수단에 의존했습니다: **수동 통합** — 개발자가 로컬에서 수동으로 코드를 병합하고 수동으로 테스트를 트리거하여 효율이 낮고 회귀 누락이 매우 쉬웠습니다. **전체 기계 실측** — 소프트웨어를 물리적 프로토타입에 배포하여 검증하는 방식으로 비용이 높고 주기가 길며 위험이 큽니다. 전형적인 문제 시나리오는 다음과 같습니다: 한 개발자가 통신 프로토콜의 한 필드를 수정했고, 로컬 컴파일과 단위 테스트는 통과했지만 메인 브랜치에 통합한 후 다른 모듈의 파싱 로직이 붕괴되는 경우 — 이러한 문제는 순수 수동 프로세스에서는 종종 전체 기계 연동 디버깅 때까지 발견되지 않습니다.

CI/CD 및 시뮬레이션 테스트 체인의 역사적 위치는 바로 검증 단계를 **좌측 이동(Shift-Left)** 시키는 것입니다: 코드가 메인 브랜치에 통합되기 전에 자동화 파이프라인을 통해 빌드, 정적 검사, 단위 테스트, SIL 시뮬레이션 테스트를 완료하고, 필요 시 HIL 하드웨어 인 더 루프 테스트를 트리거합니다. 이것이 실제로 바꾸는 것은 "테스트 위치"가 아니라 "결함 발견의 시간 분포"입니다 — 원래 통합 단계에 집중되었던 결함 폭발을 각 코드 커밋으로 분산시키는 것입니다.

### 원리 분석

**① 파이프라인 곧 게이트: CI의 핵심 메커니즘**

GitLab CI의 핵심은 **파이프라인(Pipeline)** 과 **작업(Job)** 의 계층 구조입니다. 한 번의 코드 푸시가 하나의 파이프라인을 트리거하며, 파이프라인은 여러 단계(Stage)가 직렬로 연결되고 각 단계는 여러 병렬 작업을 포함합니다. 전형적인 단계 구분은 다음과 같습니다:

$$
\text{Pipeline:} \quad \underbrace{\text{build}}_{\text{컴파일}} \rightarrow \underbrace{\text{static\_check}}_{\text{정적 검사}} \rightarrow \underbrace{\text{unit\_test}}_{\text{단위 테스트}} \rightarrow \underbrace{\text{sil\_test}}_{\text{소프트웨어 인 더 루프}} \rightarrow \underbrace{\text{coverage}}_{\text{커버리지 게이트}}
$$

각 작업은 Docker 컨테이너에서 실행되며, 컨테이너 이미지는 컴파일 툴체인, 의존성 라이브러리 버전 및 시스템 환경을 고정합니다. 이는 **환경 일관성이 이미지 불변성으로 보장됨**을 의미합니다 — 동일한 코드는 언제, 어떤 러너에서 빌드하든 결과가 일치해야 합니다. 작업의 종료 코드(exit code)가 파이프라인 지속 여부를 결정합니다: 어떤 작업이 실패하면 후속 단계는 자동으로 건너뛰고 통합 요청이 차단됩니다.

**② 2단계 시뮬레이션: SIL과 HIL의 역할 분담**

SIL(Software-in-the-Loop)은 테스트 대상 소프트웨어를 호스트 또는 시뮬레이션 환경에서 직접 실행하며, 제어 대상은 순수 수학적 모델입니다. 이것이 검증하는 것은 **알고리즘 로직의 정확성**입니다 — 컨트롤러의 상태 머신 전환, 플래너의 경로 출력, 통신 프로토콜의 인코딩/디코딩이 설계와 일치하는지 여부입니다. SIL의 장점은 속도가 빠르고 비용이 낮으며 병렬화가 쉽다는 점이고, 단점은 **실제 하드웨어 타이밍을 포함하지 않는다**는 점입니다.

HIL(Hardware-in-the-Loop)은 실제 컨트롤러 하드웨어를 폐루프에 연결하며, 제어 대상은 실시간 시뮬레이터가 계산합니다. 이것이 검증하는 것은 **타이밍 및 인터페이스 정확성**입니다 — 제어 주기가 안정적인지, 버스 통신이 프레임을 손실하는지, 인터럽트 응답이 적시에 이루어지는지 여부입니다. HIL의 폐루프 구조는 다음과 같이 표현할 수 있습니다:

$$
\text{HIL 폐루프:}\quad \underbrace{\text{실제 컨트롤러}}_{\text{실제 하드웨어}} \;\xrightarrow{\text{제어 명령}}\; \underbrace{\text{실시간 시뮬레이터}}_{\text{가상 제어 대상}} \;\xrightarrow{\text{센서 피드백}}\; \text{실제 컨트롤러}
$$

두 단계 시뮬레이션은 파이프라인에서 위치가 다릅니다: SIL은 일반 게이트로 매 커밋마다 실행됩니다. HIL은 실시간 시뮬레이터 리소스가 필요하므로 일반적으로 nightly 빌드 또는 핵심 마일스톤 시점에 트리거됩니다.

**③ 커버리지를 품질 게이트로 사용**

코드 커버리지(Code Coverage)는 테스트 실행이 덮는 코드 줄, 분기 또는 경로의 비율을 측정합니다. 이는 품질 게이트의 정량적 근거로 사용되어 "테스트는 통과했지만 많은 코드가 실행되지 않은" 거짓 양성을 방지합니다. 커버리지 지표는 일반적으로 테스트 통과율과 함께 완료 기준의 정량적 KPI를 구성합니다.

### 핵심 매개변수 및 사양

| 매개변수/사양 | 설명 | 출처 |
|---|---|---|
| 후보 방안 수 | 방안 설계 단계에서 2개 이상의 후보 방안을 구성해야 함 | 말뭉치 P14.2.3.2 |
| 평가 매트릭스 | 후보 방안에 대한 평가 매트릭스를 구축하고 정량적으로 점수화 | 말뭉치 P14.2.3.2 |
| 테스트/검토 계획 | 테스트/검토 계획 및 통과 기준을 수립해야 함 | 말뭉치 P14.2.3.4 |
| 원시 데이터 기록 | 테스트 실행 시 원시 데이터를 기록해야 함 | 말뭉치 P14.2.3.4 |
| 문제 목록 | 테스트 후 문제 목록과 개선 조치를 출력해야 함 | 말뭉치 P14.2.3.4 |
| 문서 템플릿 | 템플릿에 따라 문서를 작성하고 원시 데이터를 인용 | 말뭉치 P14.2.3.5 |
| 버전 관리 | 내부 검토 및 버전 관리를 완료 | 말뭉치 P14.2.3.5 |
| 하류 통지 | 게시 및 하류 의존자에게 통지 | 말뭉치 P14.2.3.5 |

### 수평 비교

휴머노이드 로봇 소프트웨어 검증 체계에서 CI/CD 및 시뮬레이션 테스트 체인은 인접 검증 수단과 상호 보완 관계를 형성합니다:

| 검증 수단 | 검증 대상 | 환경 | 시점 | 비용 |
|---|---|---|---|---|
| 단위 테스트 | 개별 함수/모듈 | 호스트 | 매 커밋 | 낮음 |
| SIL 시뮬레이션 테스트 | 알고리즘 로직/다중 모듈 통합 | 호스트/시뮬레이션 환경 | 매 커밋 | 중간 |
| HIL 하드웨어 인 더 루프 테스트 | 컨트롤러 하드웨어+타이밍 | 실시간 시뮬레이터 | Nightly/마일스톤 | 높음 |
| 전체 기계 실측 | 완전한 시스템 | 물리적 프로토타입 | 통합 단계 | 매우 높음 |

CI/CD 및 시뮬레이션 테스트 체인의 고유 가치는 **상위 3단계 검증을 자동화하고 파이프라인화**하여, 매 코드 변경이 단위 테스트부터 SIL까지의 완전한 검증 체인을 트리거할 수 있게 한다는 점입니다. 순수 수동 테스트와 비교하여 실제로 바꾸는 것은 "테스트가 실행되는지 여부"가 아니라 "테스트 실행의 빈도와 결정성"입니다 — 수동 테스트는 시간 부족으로 건너뛸 수 있지만, 파이프라인은 무조건 실행합니다.

### 사용자·적용 사례

이 공정의 전형적인 적용 시나리오는 다음과 같습니다:

- **휴머노이드 로봇 소프트웨어 팀**: 운동 제어, 인식, 내비게이션 등의 알고리즘 모듈을 개발할 때 CI 파이프라인을 통해 매 통합이 기존 기능을 깨뜨리지 않도록 보장합니다. 전형적인 프로세스는: 개발자가 코드 푸시 → GitLab CI가 파이프라인 트리거 → Docker 컨테이너에서 컴파일 및 단위 테스트 완료 → SIL 시뮬레이션으로 제어 로직 검증 → 커버리지 검사 → 모두 통과한 후 메인 브랜치 통합 허용.
- **미들웨어 및 드라이버 개발**: 통신 프로토콜이나 드라이버 인터페이스를 수정할 때 SIL 테스트로 프로토콜 인코딩/디코딩 정확성을 검증하고, HIL 테스트로 실제 버스 타이밍을 검증합니다. 이는 "프로토콜은 바뀌었지만 드라이버가 따라가지 못하는" 전형적인 통합 사고를 방지합니다.
- **다중 팀 협업 시나리오**: 여러 팀(운동 제어, 인식, 내비게이션)이 동일한 코드 저장소를 공유할 때 CI 파이프라인은 **통합 게이트** 역할을 하여 어떤 팀의 변경도 다른 팀의 기능을 깨뜨리지 않도록 보장합니다. 평가 매트릭스와 정량적 점수는 후보 방안 사이에서 합리적인 선택을 하는 데 사용됩니다.

### 한계와 경계

CI/CD 및 시뮬레이션 테스트 체인은 만능이 아니며, 그 한계는 다음과 같습니다:

- **연산 자원 제약**: SIL 시뮬레이션, 특히 다중 관절 동역학 시뮬레이션은 상당한 연산 자원이 필요하며, 파이프라인 동시 실행 시 리소스 병목이 발생할 수 있습니다. 이는 말뭉치에서 명시적으로 나열된 핵심 제약 중 하나입니다.
- **테스트 환경 유지 비용**: Docker 이미지는 의존성 라이브러리 버전을 따라잡기 위해 지속적으로 업데이트해야 하며, HIL 실시간 시뮬레이터는 전문 유지보수가 필요합니다. 환경 유지 자체가 지속적 투자가 됩니다.
- **시뮬레이션과 현실의 격차**: SIL에서 제어 대상은 수학적 모델이며, 모델 정확도가 테스트 유효성에 직접 영향을 줍니다. HIL은 실제 하드웨어를 연결하지만 제어 대상은 여전히 시뮬레이션입니다. 시뮬레이션은 전체 기계 실측을 완전히 대체할 수 없으며, **시뮬레이션 테스트 통과 ≠ 실제 기계 동작 정확**입니다.
- **커버리지 지표의 한계**: 높은 커버리지가 높은 테스트 품질을 보장하지 않습니다 — 테스트가 코드 줄을 덮었지만 핵심 경계 조건을 덮지 않았을 수 있습니다. 커버리지는 필요 조건이지 충분 조건이 아닙니다.

### 흔한 오해

1. **"CI/CD는 자동화 컴파일이다"** — 틀림. 컴파일은 파이프라인의 한 단계일 뿐입니다. CI/CD의 핵심은 **지속적 검증**입니다: 매 커밋이 단위 테스트, SIL 시뮬레이션, 커버리지 검사를 포함한 완전한 테스트 체인을 트리거합니다. 자동화 컴파일은 첫 단계일 뿐이며, 자동화 검증이 목적입니다.
2. **"SIL 통과하면 실제 기계에 올려도 된다"** — 틀림. SIL은 알고리즘 로직을 검증하며 실제 하드웨어 타이밍을 포함하지 않습니다. 컨트롤러는 실제 기계에서 버스 지연, 인터럽트 지터, 리소스 경쟁으로 인해 비정상 동작할 수 있습니다. SIL과 HIL은 상호 보완 관계이지 대체 관계가 아닙니다.
3. **"커버리지 100%면 테스트가 완비된 것이다"** — 틀림. 커버리지는 코드 줄이 실행되었음을 나타낼 뿐, 단언(assertion)의 유효성을 나타내지 않습니다. 모든 코드 줄을 덮었지만 단언이 모두 비어 있는 테스트 스위트는 커버리지 100%여도 가치가 없습니다. 커버리지 지표는 단언 품질, 경계 조건 커버리지와 함께 고려해야 합니다.
4. **"Docker가 모든 환경 문제를 해결한다"** — 틀림. Docker는 **빌드 환경**의 일관성을 보장하지만 **실행 환경**의 일관성은 보장하지 않습니다. HIL 테스트는 실제 하드웨어를 포함하며, 그 동작은 물리적 요인의 영향을 받아 컨테이너화할 수 없습니다. Docker가 해결하는 것은 "빌드 의존성" 문제이지 "하드웨어 동작" 문제가 아닙니다.

### 관련 지식

- `ent_process_p14` — 본 공정이 속한 상위 워크팩으로, 소프트웨어 미들웨어 및 시스템 통합 단계의 완전한 작업 프레임워크.
- `ent_method_hardware_in_the_loop` — 본 공정에서 HIL 자동화 테스트 단계의 방법론적 기초로, 하드웨어 인 더 루프 테스트의 폐루프 원리와 구현 요점을 상세히 설명.
