---
$id: ent_method_pid_control
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: PID Control
  zh: PID控制
  ko: PID 제어
summary:
  en: A foundational feedback control method that computes a control signal as a linear combination of proportional, integral,
    and derivative terms of the tracking error.
  zh: 一种基础反馈控制方法，将控制量表示为跟踪误差的比例、积分、微分三项线性组合。
  ko: 추적 오차의 비례·적분·미분 항의 선형 조합으로 제어 신호를 계산하는 기초 피드백 제어 방법.
domains:
- 07_ai_models_algorithms
- 02_components
layers:
- intelligence
functional_roles:
- knowledge
theoretical_depth:
- method
tags:
- control
- feedback
- joint_control
- actuator
- classical_control
- wiki_gap
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-13'
  confidence: high
  notes: 'Body populated from Wiki chapter section by scripts/fill_gap_bodies_from_wiki.py; pending human review and translation
    to en/ko. Body backfilled from entity metadata by scripts/backfill_critical_entities.py. [2026-08-06] body upgraded to
    textbook-grade B1 batch (.staging/textbook_grade, methods top-20 by degree): zh 概述/核心内容/参考 rewritten from card + graph
    neighbors + wiki chapters + first-hand sources (arXiv/Crossref/fetched pages in sources/, number whitelist audit 20/20
    passed); en/ko sections to be regenerated by translate_entry_bodies.py.'
sources:
- id: src_astrom_hagglund_2006
  type: other
  title: K. J. Åström and T. Hägglund, Advanced PID Control, ISA, 2006
  url: https://www.ieeectl.org/awards/astrom-hagglund-advanced-pid-control/
  date: '2006-01-01'
  accessed_at: '2026-07-09'
- id: src_ogata_2010
  type: other
  title: K. Ogata, Modern Control Engineering, 5th ed., Prentice Hall, 2010
  url: https://doi.org/10.1002/9780470544162
  date: '2010-01-01'
  accessed_at: '2026-07-09'
related_entities:
- id: ent_method_impedance_control
  relationship: mentions
  description:
    en: PID control regulates position or velocity; impedance control extends this to regulate dynamic interaction forces
      with the environment.
    zh: PID 控制调节位置或速度；阻抗控制进一步调节与环境的动态交互力。
---
## 概述

PID 控制（比例-积分-微分控制）是关节伺服的主力算法：比例项提供即时纠正、积分项消除稳态误差、微分项提供阻尼抑制超调。在人形机器人中，它以**级联结构**运行——最内电流环（即力矩环，带宽数千赫兹）、中间速度环、最外位置环，内环带宽逐层高于外环 3–10 倍。教科书 PID 与真机可用 PID 之间隔着抗积分饱和、微分滤波与前馈补偿三件套工程细节（来源：项目 Wiki 第 14 章 §14.2）。

## 核心内容

### 是什么：准确定义

连续形式的 PID 控制律（来源：项目 Wiki 第 14 章 §14.2.2）：

$$
u(t) = K_p e(t) + K_i \int_0^t e(s)\, ds + K_d \frac{d e(t)}{dt}, \qquad e(t) = r(t) - y(t)
$$

数字控制器以周期 $T_s$ 采样，工程上更常用**增量式**（velocity form）：

$$
\Delta u[k] = K_p \left(e[k] - e[k-1]\right) + K_i T_s e[k] + \frac{K_d}{T_s}\left(e[k] - 2e[k-1] + e[k-2]\right)
$$

增量式输出控制量增量，天然避免积分累加器显式累加，手动/自动切换无扰（Wiki §14.2.2）。

### 为什么存在：痛点与历史定位

PID 的生命力来自一个工程事实：**大多数单关节跟踪问题不需要模型**。Ziegler 与 Nichols 1942 年给出的经验整定法（DOI 10.1115/1.4019264）让 PID 免模型落地。在人形机器人中，它不是"低级算法"而是**控制栈的地基**：无论上层是 WBC、MPC 还是 RL 策略，最终都要经关节伺服环执行；RL 论文里的"PD 目标关节角"就是 PID 去积分项的变体。

它真正的价值在于**分层解耦**：内环把电机动态、摩擦、齿隙等脏动态"压平"成近似理想的力矩/位置源，上层控制器才得以在干净的抽象上工作。理解 PID 的局限，就是理解上层一切方法的前提假设从哪来。

### 原理拆解

**① 级联控制结构**（Wiki §14.2.1）

关节伺服普遍采用电流环/速度环/位置环三级嵌套，内环带宽必须显著高于外环——典型每向外一层降低 3–10 倍，否则内外环动态相互干涉。电流环对象是电机电气动态：经 FOC 变换后 q 轴电流近似一阶模型 $L_q \frac{di_q}{dt} = v_q - R_s i_q - k_e \omega_m$，电磁时间常数 $L_q/R_s$ 典型亚毫秒级，**电流环带宽可达数千赫兹**。关节输出力矩 $\tau_j = N\eta k_t i_q$，故电流环实质就是力矩环。

**② 反射惯量与减速比的分水岭**

速度/位置环的对象是机电动态 $J_{eff}\,\dot\omega = \tau_j - \tau_g - \tau_f - \tau_{ext}$，其中 $J_{eff} = J_m N^2 + J_l$。注意 $N^2$：高减速比关节（谐波 N 典型 50–160）中负载侧惯量与扰动折算到电机侧缩小 $N^2$ 倍——"天然抗扰但天然不透明"；QDD 刻意取低减速比（典型 N=6–12）换力矩透明度，代价是电流环直接承受负载扰动（Wiki §14.2.1）。

**③ 工程三件套**（Wiki §14.2.3）

- **抗积分饱和**：执行器限幅时误差持续使积分无限累积，解除后巨大超调。反算抗饱和把饱和差值经增益反馈到积分器：$I[k+1] = I[k] + K_i T_s e[k] + K_t T_s (u_{sat}[k] - u_{unsat}[k])$；
- **微分滤波**：微分放大噪声，必须串低通 $D(s) = \frac{K_d s}{1 + s/N_f}$，滤波系数 $N_f$ 典型取 5–20；并用微分先行（只作用于测量值）避免给定阶跃的"微分冲击"；
- **前馈补偿**：$\tau_{cmd} = \underbrace{J_{eff}\ddot q_d + \tau_g(\hat q) + \hat\tau_f(\hat\omega)}_{\text{模型前馈}} + \underbrace{K_p e + K_d \dot e + K_i \int e}_{\text{反馈}}$——模型前馈承担可预测部分，反馈只纠残差。

### 关键参数与规格

| 参数 | 典型值/指导 | 来源 |
|------|------------|------|
| 级联带宽比 | 每向外一层降 3–10 倍 | Wiki §14.2.1 |
| 电流环带宽 | 数千赫兹（电磁时间常数亚毫秒级） | Wiki §14.2.1 |
| 微分滤波系数 $N_f$ | 5–20 | Wiki §14.2.3 |
| 高减速比 N | 50–160（谐波） | Wiki §14.2.1 |
| QDD 减速比 N | 6–12 | Wiki §14.2.1 |
| 整定经典法 | Ziegler-Nichols 1942 | DOI 10.1115/1.4019264 |

### 横向对比：关节控制律选型

| 维度 | **PID（级联）** | LQR | MPC | 阻抗/导纳 |
|------|----------------|-----|-----|-----------|
| 模型需求 | 免模型 | 线性模型 | 预测模型 | 力矩/位置环保真度 |
| 约束处理 | 仅饱和截断 | — | 显式 | 间接（经柔顺设计） |
| 计算成本 | 极低 | 一次矩阵乘 | 每周期一个 QP | 低 |
| 适用层 | 关节伺服（kHz） | 线性化子系统 | 轨迹/质心层 | 接触交互层 |

（依据：项目 Wiki 第 14 章。）

### 谁在用 / 应用案例

- **一切关节的底层**：从 Dynamixel 舵机（`ent_component_dynamixel_xm430_w210_t`）到 Unitree 自研关节（`ent_oem_unitree_robotics`），级联 PID 是默认实现；
- **RL 策略的执行接口**：人形 RL 论文的"PD 目标关节角"即去积分项的 PID（JAEGER 等工作均以 PD 目标输出）；
- **FOC 电流环**：`ent_method_field_oriented_control` 与 PID 共同构成电机伺服内核。

### 局限与边界

- **单入单出**：PID 不理解关节间耦合，人形全身协调必须由上层（WBC/MPC/策略）承担；
- **无约束概念**：限位、力矩限幅只能靠饱和截断与抗饱和补救；
- **固定增益**：非线性工况（变负载、接触切换）下需增益调度或前馈补偿；
- **积分项在力控场景危险**：力矩环中慎用积分，避免与接触刚度构成慢振荡（**工程判断**）。

### 常见误区

1. **"PID 过时了"**——它是控制栈地基；上层方法越先进，越依赖内环 PID 把关节压成理想源。
2. **"三增益调大响应就快"**——超带宽比整定会引发级联干涉振荡；先内后外、带宽比 3–10 倍是纪律。
3. **"抗饱和可有可无"**——限幅场景缺抗积分饱和是炸机级隐患。
4. **"微分直接用误差差分"**——无滤波微分放大噪声、给定阶跃产生微分冲击；微分滤波 + 微分先行是默认配置。

### 相关知识

- `ent_method_field_oriented_control` — 电流环的矢量控制基础
- `ent_method_impedance_control` — 力矩环之上的柔顺层
- `ent_method_model_predictive_control` / `ent_method_whole_body_control` — 上层优化控制
- `ent_component_bldc_motor` / `ent_comp_servo_motor` — 被控对象硬件
- `ent_component_dynamixel_xm430_w210_t` — 集成伺服实例

## 参考

- [Ziegler & Nichols, Optimum Settings for Automatic Controllers, ASME 1942](https://doi.org/10.1115/1.4019264)
- [项目 Wiki 第 14 章 §14.2 关节级伺服控制](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-14.md)

## Overview

PID control (Proportional-Integral-Derivative control) is the workhorse algorithm for joint servos: the proportional term provides immediate correction, the integral term eliminates steady-state error, and the derivative term provides damping to suppress overshoot. In humanoid robots, it operates in a **cascade structure**—the innermost current loop (i.e., torque loop, with bandwidth in the kilohertz range), the middle velocity loop, and the outermost position loop, with inner-loop bandwidth 3–10 times higher than the outer loop layer by layer. Between textbook PID and production-ready PID lie three engineering details: anti-windup, derivative filtering, and feedforward compensation (source: Project Wiki Chapter 14 §14.2).

## Content

### What It Is: Precise Definition

The continuous-form PID control law (source: Project Wiki Chapter 14 §14.2.2):

$$
u(t) = K_p e(t) + K_i \int_0^t e(s)\, ds + K_d \frac{d e(t)}{dt}, \qquad e(t) = r(t) - y(t)
$$

Digital controllers sample at period $T_s$, and in engineering the **incremental form** (velocity form) is more commonly used:

$$
\Delta u[k] = K_p \left(e[k] - e[k-1]\right) + K_i T_s e[k] + \frac{K_d}{T_s}\left(e[k] - 2e[k-1] + e[k-2]\right)
$$

The incremental form outputs the control increment, naturally avoiding explicit accumulation in the integrator, and enables bumpless transfer between manual and automatic modes (Wiki §14.2.2).

### Why It Exists: Pain Points and Historical Positioning

The vitality of PID stems from an engineering fact: **most single-joint tracking problems do not require a model**. The empirical tuning method given by Ziegler and Nichols in 1942 (DOI 10.1115/1.4019264) allowed PID to be deployed without a model. In humanoid robots, it is not a "low-level algorithm" but the **foundation of the control stack**: whether the upper layer is WBC, MPC, or RL policies, everything ultimately executes through the joint servo loop; the "PD target joint angle" in RL papers is a variant of PID with the integral term removed.

Its true value lies in **hierarchical decoupling**: the inner loop "flattens" dirty dynamics such as motor dynamics, friction, and backlash into an approximately ideal torque/position source, allowing upper-layer controllers to work on a clean abstraction. Understanding the limitations of PID is understanding where the assumptions of all upper-layer methods come from.

### Principle Breakdown

**① Cascade Control Structure** (Wiki §14.2.1)

Joint servos universally adopt a three-level nested structure of current loop/velocity loop/position loop. The inner-loop bandwidth must be significantly higher than the outer loop—typically decreasing 3–10 times per outward layer—otherwise the inner and outer loop dynamics interfere. The current loop's plant is the motor's electrical dynamics: after FOC transformation, the q-axis current approximates a first-order model $L_q \frac{di_q}{dt} = v_q - R_s i_q - k_e \omega_m$, with the electrical time constant $L_q/R_s$ typically in the sub-millisecond range, **allowing current-loop bandwidth up to several kilohertz**. The joint output torque is $\tau_j = N\eta k_t i_q$, so the current loop is essentially the torque loop.

**② Reflected Inertia and the Dividing Line of Gear Ratios**

The plant for the velocity/position loops is the electromechanical dynamics $J_{eff}\,\dot\omega = \tau_j - \tau_g - \tau_f - \tau_{ext}$, where $J_{eff} = J_m N^2 + J_l$. Note the $N^2$: in high-ratio joints (harmonic drives with N typically 50–160), load-side inertia and disturbances are reduced by $N^2$ when reflected to the motor side—"naturally disturbance-rejecting but naturally opaque"; QDD deliberately uses low gear ratios (typically N=6–12) for torque transparency, at the cost of the current loop directly bearing load disturbances (Wiki §14.2.1).

**③ The Engineering Trio** (Wiki §14.2.3)

- **Anti-windup**: When the actuator saturates, the error continuously accumulates in the integrator, causing large overshoot upon desaturation. Back-calculation anti-windup feeds the saturation difference back to the integrator via a gain: $I[k+1] = I[k] + K_i T_s e[k] + K_t T_s (u_{sat}[k] - u_{unsat}[k])$;
- **Derivative filtering**: Differentiation amplifies noise and must be cascaded with a low-pass filter $D(s) = \frac{K_d s}{1 + s/N_f}$, with filter coefficient $N_f$ typically 5–20; derivative-on-measurement (acting only on the measured value) is also used to avoid "derivative kick" from setpoint steps;
- **Feedforward compensation**: $\tau_{cmd} = \underbrace{J_{eff}\ddot q_d + \tau_g(\hat q) + \hat\tau_f(\hat\omega)}_{\text{model feedforward}} + \underbrace{K_p e + K_d \dot e + K_i \int e}_{\text{feedback}}$—the model feedforward handles the predictable part, and feedback only corrects residuals.

### Key Parameters and Specifications

| Parameter | Typical Value/Guideline | Source |
|-----------|------------------------|--------|
| Cascade bandwidth ratio | Decrease 3–10 times per outward layer | Wiki §14.2.1 |
| Current-loop bandwidth | Several kilohertz (electrical time constant sub-millisecond) | Wiki §14.2.1 |
| Derivative filter coefficient $N_f$ | 5–20 | Wiki §14.2.3 |
| High gear ratio N | 50–160 (harmonic drive) | Wiki §14.2.1 |
| QDD gear ratio N | 6–12 | Wiki §14.2.1 |
| Classical tuning method | Ziegler-Nichols 1942 | DOI 10.1115/1.4019264 |

### Horizontal Comparison: Joint Control Law Selection

| Dimension | **PID (cascade)** | LQR | MPC | Impedance/Admittance |
|-----------|-------------------|-----|-----|----------------------|
| Model requirement | Model-free | Linear model | Predictive model | Torque/position loop fidelity |
| Constraint handling | Saturation clipping only | — | Explicit | Indirect (via compliance design) |
| Computational cost | Extremely low | One matrix multiply | One QP per cycle | Low |
| Applicable layer | Joint servo (kHz) | Linearized subsystems | Trajectory/CoM layer | Contact interaction layer |

(Basis: Project Wiki Chapter 14.)

### Who Uses It / Application Cases

- **The bottom layer of all joints**: From Dynamixel servos (`ent_component_dynamixel_xm430_w210_t`) to Unitree's in-house joints (`ent_oem_unitree_robotics`), cascade PID is the default implementation;
- **Execution interface for RL policies**: The "PD target joint angle" in humanoid RL papers is PID with the integral term removed (works such as JAEGER all output PD targets);
- **FOC current loop**: `ent_method_field_oriented_control` together with PID forms the core of the motor servo.

### Limitations and Boundaries

- **Single-input single-output**: PID does not understand joint coupling; whole-body coordination in humanoids must be handled by the upper layer (WBC/MPC/policy);
- **No concept of constraints**: Limits and torque saturation can only be remedied by saturation clipping and anti-windup;
- **Fixed gains**: Under nonlinear conditions (varying loads, contact switching), gain scheduling or feedforward compensation is needed;
- **The integral term is dangerous in force control**: Use the integral term cautiously in the torque loop to avoid slow oscillations with contact stiffness (**engineering judgment**).

### Common Misconceptions

1. **"PID is outdated"**—It is the foundation of the control stack; the more advanced the upper-layer method, the more it relies on inner-loop PID to compress joints into ideal sources.
2. **"Cranking up the three gains makes response faster"**—Tuning beyond the bandwidth ratio causes cascade interference oscillations; inner-to-outer tuning with a 3–10 times bandwidth ratio is the discipline.
3. **"Anti-windup is optional"**—Missing anti-windup in saturation scenarios is a catastrophic-level hazard.
4. **"Use raw error differences for the derivative"**—Unfiltered differentiation amplifies noise and causes derivative kick on setpoint steps; derivative filtering plus derivative-on-measurement is the default configuration.

### Related Knowledge

- `ent_method_field_oriented_control` — Vector control foundation for the current loop
- `ent_method_impedance_control` — Compliance layer above the torque loop
- `ent_method_model_predictive_control` / `ent_method_whole_body_control` — Upper-layer optimization control
- `ent_component_bldc_motor` / `ent_comp_servo_motor` — Controlled plant hardware
- `ent_component_dynamixel_xm430_w210_t` — Integrated servo example

## 개요

PID 제어(비례-적분-미분 제어)는 관절 서보의 주력 알고리즘이다: 비례항은 즉각적인 교정을 제공하고, 적분항은 정상 상태 오차를 제거하며, 미분항은 댐핑을 제공하여 오버슈트를 억제한다. 휴머노이드 로봇에서는 **캐스케이드 구조**로 작동한다——최내부 전류 루프(즉 토크 루프, 대역폭 수천 헤르츠), 중간 속도 루프, 최외부 위치 루프로 구성되며, 내부 루프의 대역폭은 외부 루프보다 3–10배 높다. 교과서 PID와 실제 기기에서 사용 가능한 PID 사이에는 적분 와인드업 방지, 미분 필터링, 피드포워드 보상이라는 세 가지 엔지니어링 디테일이 있다(출처: 프로젝트 Wiki 14장 §14.2).

## 핵심 내용

### 무엇인가: 정확한 정의

연속 형식의 PID 제어 법칙(출처: 프로젝트 Wiki 14장 §14.2.2):

$$
u(t) = K_p e(t) + K_i \int_0^t e(s)\, ds + K_d \frac{d e(t)}{dt}, \qquad e(t) = r(t) - y(t)
$$

디지털 컨트롤러는 주기 $T_s$로 샘플링하며, 엔지니어링에서는 **증분형**(velocity form)을 더 자주 사용한다:

$$
\Delta u[k] = K_p \left(e[k] - e[k-1]\right) + K_i T_s e[k] + \frac{K_d}{T_s}\left(e[k] - 2e[k-1] + e[k-2]\right)
$$

증분형은 제어량의 증분을 출력하므로 적분 누산기의 명시적 누적을 자연스럽게 피하고, 수동/자동 전환 시 무범프(bumpless) 전환이 가능하다(Wiki §14.2.2).

### 왜 존재하는가: 문제점과 역사적 위치

PID의 생명력은 하나의 엔지니어링 사실에서 나온다: **대부분의 단일 관절 추적 문제는 모델이 필요 없다**. Ziegler와 Nichols가 1942년에 제시한 경험적 튜닝법(DOI 10.1115/1.4019264)은 PID가 모델 없이도 적용 가능하게 만들었다. 휴머노이드 로봇에서 PID는 "저급 알고리즘"이 아니라 **제어 스택의 기초**다: 상위 계층이 WBC, MPC, RL 정책 중 무엇이든 최종적으로 관절 서보 루프를 통해 실행된다; RL 논문의 "PD 목표 관절각"은 PID에서 적분항을 제거한 변형이다.

그 진정한 가치는 **계층적 분리**에 있다: 내부 루프가 모터 동역학, 마찰, 백래시 등의 복잡한 동역학을 "평탄화"하여 이상적인 토크/위치 소스로 만들고, 상위 컨트롤러가 깨끗한 추상화 위에서 작동할 수 있게 한다. PID의 한계를 이해하는 것은 상위 모든 방법의 전제 조건이 어디서 오는지 이해하는 것이다.

### 원리 분석

**① 캐스케이드 제어 구조** (Wiki §14.2.1)

관절 서보는 일반적으로 전류 루프/속도 루프/위치 루프의 3단 중첩 구조를 사용하며, 내부 루프의 대역폭은 외부 루프보다 현저히 높아야 한다——일반적으로 바깥으로 한 층 갈 때마다 3–10배 감소하며, 그렇지 않으면 내외부 루프 동역학이 서로 간섭한다. 전류 루프의 대상은 모터 전기 동역학이다: FOC 변환 후 q축 전류는 1차 모델 $L_q \frac{di_q}{dt} = v_q - R_s i_q - k_e \omega_m$로 근사되며, 전자기 시간 상수 $L_q/R_s$는 일반적으로 서브 밀리초 수준이고, **전류 루프 대역폭은 수천 헤르츠에 도달할 수 있다**. 관절 출력 토크는 $\tau_j = N\eta k_t i_q$이므로 전류 루프는 실질적으로 토크 루프다.

**② 반사 관성과 감속비의 분기점**

속도/위치 루프의 대상은 전기기계 동역학 $J_{eff}\,\dot\omega = \tau_j - \tau_g - \tau_f - \tau_{ext}$이며, 여기서 $J_{eff} = J_m N^2 + J_l$이다. $N^2$에 주목하라: 고감속비 관절(하모닉 N 일반적으로 50–160)에서는 부하 측 관성과 외란이 모터 측으로 환산될 때 $N^2$배 축소된다——"자연적 외란 억제지만 자연적 불투명성"; QDD는 의도적으로 낮은 감속비(일반적으로 N=6–12)를 선택하여 토크 투명성을 얻는 대신, 전류 루프가 부하 외란을 직접 받는다(Wiki §14.2.1).

**③ 엔지니어링 3종 세트** (Wiki §14.2.3)

- **적분 와인드업 방지**: 액추에이터가 포화될 때 오차가 지속적으로 적분을 무한히 누적시키고, 해제 후 큰 오버슈트가 발생한다. 역산(back-calculation) 안티와인드업은 포화 차이를 게인을 통해 적분기에 피드백한다: $I[k+1] = I[k] + K_i T_s e[k] + K_t T_s (u_{sat}[k] - u_{unsat}[k])$;
- **미분 필터링**: 미분은 노이즈를 증폭하므로 저역 통과 필터 $D(s) = \frac{K_d s}{1 + s/N_f}$를 직렬로 연결해야 하며, 필터 계수 $N_f$는 일반적으로 5–20; 또한 미분 선행(측정값에만 적용)을 사용하여 기준 입력 계단에 의한 "미분 충격"을 피한다;
- **피드포워드 보상**: $\tau_{cmd} = \underbrace{J_{eff}\ddot q_d + \tau_g(\hat q) + \hat\tau_f(\hat\omega)}_{\text{모델 피드포워드}} + \underbrace{K_p e + K_d \dot e + K_i \int e}_{\text{피드백}}$——모델 피드포워드가 예측 가능한 부분을 담당하고, 피드백은 잔차만 교정한다.

### 핵심 파라미터 및 사양

| 파라미터 | 일반적 값/지침 | 출처 |
|------|------------|------|
| 캐스케이드 대역폭 비 | 바깥으로 한 층 갈 때마다 3–10배 감소 | Wiki §14.2.1 |
| 전류 루프 대역폭 | 수천 헤르츠(전자기 시간 상수 서브 밀리초) | Wiki §14.2.1 |
| 미분 필터 계수 $N_f$ | 5–20 | Wiki §14.2.3 |
| 고감속비 N | 50–160(하모닉) | Wiki §14.2.1 |
| QDD 감속비 N | 6–12 | Wiki §14.2.1 |
| 고전적 튜닝법 | Ziegler-Nichols 1942 | DOI 10.1115/1.4019264 |

### 수평 비교: 관절 제어 법칙 선택

| 차원 | **PID(캐스케이드)** | LQR | MPC | 임피던스/어드미턴스 |
|------|----------------|-----|-----|-----------|
| 모델 요구 | 모델 불필요 | 선형 모델 | 예측 모델 | 토크/위치 루프 충실도 |
| 제약 처리 | 포화 절단만 | — | 명시적 | 간접(컴플라이언스 설계를 통해) |
| 계산 비용 | 매우 낮음 | 행렬 곱 1회 | 주기당 QP 1회 | 낮음 |
| 적용 계층 | 관절 서보(kHz) | 선형화된 서브시스템 | 궤적/질량 중심 계층 | 접촉 상호작용 계층 |

(근거: 프로젝트 Wiki 14장.)

### 누가 사용하는가 / 적용 사례

- **모든 관절의 기반**: Dynamixel 서보(`ent_component_dynamixel_xm430_w210_t`)부터 Unitree 자체 개발 관절(`ent_oem_unitree_robotics`)까지, 캐스케이드 PID가 기본 구현이다;
- **RL 정책의 실행 인터페이스**: 휴머노이드 RL 논문의 "PD 목표 관절각"은 적분항이 제거된 PID다(JAEGER 등의 연구는 모두 PD 목표 출력을 사용);
- **FOC 전류 루프**: `ent_method_field_oriented_control`과 PID가 함께 모터 서보 커널을 구성한다.

### 한계와 경계

- **단일 입력 단일 출력**: PID는 관절 간 커플링을 이해하지 못하므로, 휴머노이드 전신 조화는 상위 계층(WBC/MPC/정책)이 담당해야 한다;
- **제약 개념 없음**: 리미트, 토크 포화는 포화 절단과 안티와인드업으로만 보완 가능;
- **고정 게인**: 비선형 조건(가변 부하, 접촉 전환)에서는 게인 스케줄링 또는 피드포워드 보상이 필요;
- **힘 제어 시나리오에서 적분항은 위험**: 토크 루프에서는 적분 사용을 신중히 해야 하며, 접촉 강성과의 느린 진동을 피해야 한다(**엔지니어링 판단**).

### 흔한 오해

1. **"PID는 구식이다"**——제어 스택의 기초다; 상위 방법이 고도화될수록 내부 루프 PID가 관절을 이상적인 소스로 압축하는 데 더 의존한다.
2. **"세 게인을 키우면 응답이 빨라진다"**——대역폭 비를 초과하는 튜닝은 캐스케이드 간섭 진동을 유발한다; 내부 먼저, 외부 나중, 대역폭 비 3–10배가 원칙이다.
3. **"안티와인드업은 있어도 그만 없어도 그만"**——포화 시나리오에서 적분 와인드업 방지가 없으면 기기 파손급 위험이 된다.
4. **"미분은 오차 차분을 직접 사용"**——필터 없는 미분은 노이즈를 증폭하고, 기준 입력 계단 시 미분 충격이 발생한다; 미분 필터링 + 미분 선행이 기본 설정이다.

### 관련 지식

- `ent_method_field_oriented_control` — 전류 루프의 벡터 제어 기초
- `ent_method_impedance_control` — 토크 루프 위의 컴플라이언스 계층
- `ent_method_model_predictive_control` / `ent_method_whole_body_control` — 상위 최적화 제어
- `ent_component_bldc_motor` / `ent_comp_servo_motor` — 제어 대상 하드웨어
- `ent_component_dynamixel_xm430_w210_t` — 통합 서보 인스턴스
