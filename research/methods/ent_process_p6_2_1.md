---
$id: ent_process_p6_2_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Verification of forward and inverse kinematics solution
  zh: 正逆运动学解算验证
  ko: 정역운동학 해석 검증
summary:
  en: The success rate of pose IK in 1000 groups was > 99% , and the position error was < 1 mm
  zh: '- P6.2.1.1 输入梳理与目标量化 - 整理「正逆运动学解算验证」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 随机 1000 组位姿 IK 成功率 > 99%、位置误差 < 1 mm
domains:
- 06_design_engineering
- 08_software_middleware
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b2): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

正逆运动学解算验证（Verification of forward and inverse kinematics solution）是 URDF 建模与运动学校核阶段（P6.2）中的首个三级子任务，其核心目标是通过系统化的测试与量化指标，确认机器人运动学模型（URDF）与 CAD 设计的一致性，并验证逆运动学（IK）求解器的精度与可靠性。该任务以「随机 1000 组位姿 IK 成功率 > 99%、位置误差 < 1 mm」为完成标准，它真正改变的不是"跑通一次仿真"这件事本身，而是让"模型正确性"从定性判断转变为可量化、可追溯的工程验收流程。

## 核心内容

### 是什么：准确定义

正逆运动学解算验证是一套结构化的工程验证流程，包含五个四级子任务：输入梳理与目标量化（P6.2.1.1）、方案/方法设计（P6.2.1.2）、实施/原型/样件制作（P6.2.1.3）、测试执行与结果分析（P6.2.1.4）、文档输出与下游交付（P6.2.1.5）。其技术手段包括解析/数值逆解、随机姿态采样与奇异性分析，验证对象是 URDF 模型中关节极限、连杆长度与 CAD 的一致性，以及 IK 解算的成功率与精度。

从数学本质看，正运动学定义映射 $x = f(q)$（关节空间到任务空间），而逆运动学求解其逆映射 $q = f^{-1}(x)$。验证任务的核心在于：确认 $f(q)$ 与物理样机一致（正解验证），并确认 $f^{-1}(x)$ 在目标工作空间内可靠收敛（逆解验证）。

### 为什么存在：痛点与历史定位

在机器人开发流程中，URDF 模型是仿真、控制与规划工具链的共同输入。若模型与 CAD 不一致——例如连杆长度偏差 0.5 mm（示例值，工程判断）、关节极限设置错误——将导致仿真结果与实机行为系统性偏离，后续所有依赖该模型的算法（运动规划、动力学控制、状态估计）都会继承这一误差。历史上，这类问题常在样机调试阶段才暴露，修复成本极高。

正逆运动学解算验证正是为了在"设计阶段"而非"调试阶段"发现并消除这些偏差。它将验收标准量化为「随机 1000 组位姿 IK 成功率 > 99%、位置误差 < 1 mm」，使得模型正确性有了明确的、可测试的判据。它真正改变的不是"验证方法"本身，而是让"模型可信度"成为开发流程中一个可审计、可追溯的工程节点。

### 原理拆解

**① 正运动学验证：URDF 与 CAD 的一致性检查**

正运动学验证的核心是确认 URDF 中的运动学参数（连杆长度、关节原点、旋转轴方向、关节极限）与 CAD 模型一致。具体做法是：从 CAD 导出关键尺寸，与 URDF 中的 `<origin>`、`<axis>`、`<limit>` 字段逐项比对。对于人形机器人这类高自由度系统（通常 30+ 自由度），手工比对不现实，需借助自动化脚本提取 URDF 参数并与 CAD 导出的参数表进行数值比对，公差通常设定在亚毫米级（工程判断）。

**② 逆运动学验证：随机采样与成功率统计**

逆运动学验证采用随机姿态采样策略：在工作空间内随机生成 1000 组末端位姿，调用 IK 求解器逐一求解，统计成功率与位置误差。这一方法的数学基础是 IK 问题的多解性与奇异性：

$$
q^* = \arg\min_{q} \| f(q) - x_d \|^2 \quad \text{s.t.} \quad q_{\min} \le q \le q_{\max}
$$

其中 $x_d$ 为期望末端位姿，$q_{\min}$、$q_{\max}$ 为关节极限。数值求解器（如阻尼最小二乘、Levenberg-Marquardt）通过迭代最小化位姿误差来逼近 $q^*$。成功率低于阈值通常意味着：URDF 参数错误、关节极限设置不合理、或工作空间内存在未处理的奇异区域。

**③ 奇异性分析：识别不可达区域**

奇异性是指 Jacobian 矩阵 $J(q)$ 秩亏的位形，此时 IK 求解器可能发散或产生关节速度突变。验证流程需识别工作空间内的奇异区域，并确认这些区域是否落在实际任务需求之外。对于人形机器人，奇异位形常见于手臂完全伸展或腿部接近直立等姿态。

### 关键参数与规格

| 参数 | 规格 | 来源 |
|------|------|------|
| 随机采样位姿数 | 1000 组 | 语料 |
| IK 成功率阈值 | > 99% | 语料 |
| 位置误差阈值 | < 1 mm | 语料 |
| 候选方案数 | ≥ 2 个 | 语料 |
| 关节极限检查 | 与 CAD 一致 | 语料 |
| 连杆长度检查 | 与 CAD 一致 | 语料 |

### 横向对比

与通用机械臂 IK 验证相比，人形机器人的正逆运动学解算验证有显著差异。通用 6-DOF 机械臂（满足 Pieper 条件）存在解析闭式解，验证重点在于多解选择策略；而人形机器人通常包含 7-DOF 冗余手臂与 6-DOF 腿部，数值求解是主流，验证重点转向求解器收敛性与奇异鲁棒性。此外，人形机器人的工作空间包含大量自碰撞约束与关节极限约束，随机采样需考虑这些约束的联合影响（工程判断）。

与学术研究中的 IK 算法评测（如 TRAC-IK 基准测试）相比，本任务的定位是**工程验收**而非**算法研究**：不追求算法性能上限，而是确保当前选定的求解器在目标工作空间内满足既定阈值。

### 谁在用·应用案例

该验证流程是机器人产品开发中运动学模型交付的标准工序，主要使用者包括：

1. **运动学工程师**：执行 URDF 参数提取、CAD 比对、IK 求解器配置与测试；
2. **仿真工程师**：接收验证通过的 URDF 模型，用于动力学仿真与运动规划；
3. **控制算法工程师**：依赖验证过的运动学模型开发 WBC（全身控制）与任务空间控制策略；
4. **系统集成工程师**：在样机调试阶段，将仿真模型与实机行为进行对照。

典型应用场景：在双足人形机器人开发中，该验证确保仿真中的步态规划结果可以直接迁移到实机，而无需大量现场调参。若验证不通过，下游所有依赖运动学模型的工作都将被阻塞。

### 局限与边界

1. **静态验证的局限**：该验证仅覆盖运动学层面，不涉及动力学参数（质量、惯量、摩擦）的辨识与验证，后者属于 P6.2 后续子任务或独立阶段；
2. **采样覆盖的盲区**：随机 1000 组位姿虽具统计意义，但无法保证覆盖所有边界条件（如关节极限附近的极端位姿），需辅以针对性边界测试（工程判断）；
3. **求解器依赖性**：验证结果与所选 IK 求解器强相关，更换求解器后需重新验证；
4. **CAD 基准的准确性**：验证以 CAD 模型为基准，若 CAD 本身与物理样机存在偏差，验证无法发现该偏差；
5. **不涉及动态性能**：位置误差 < 1 mm 的指标仅针对静态位姿，不包含运动过程中的跟踪误差。

### 常见误区

1. **"验证通过 = 模型完全正确"**——错。验证只覆盖采样到的位姿空间，且以 CAD 为基准，无法保证绝对正确；
2. **"IK 成功率越高越好"**——不必然。过高的成功率可能意味着求解器过度松弛（如允许较大姿态误差），需同时检查位置误差指标；
3. **"随机采样可以替代边界测试"**——错。随机采样对边界条件的覆盖概率低，需补充关节极限、奇异位形附近的针对性测试；
4. **"验证是一次性工作"**——错。URDF 模型在开发过程中会迭代更新（如结构优化、关节限位调整），每次变更后都需重新执行验证；
5. **"数值 IK 一定比解析 IK 差"**——不必然。现代数值求解器（如 TRAC-IK）在鲁棒性上往往优于解析法，尤其对于冗余自由度系统。

### 相关知识

- `ent_method_inverse_kinematics` — 逆运动学求解的数学基础与算法分类，本任务的验证对象；
- `ent_process_p6` — URDF 建模与运动学校核阶段的整体定位，本任务是该阶段的首个子任务；
- `ent_paper_desai_automatic_design_of_task_speci_2018` — 任务驱动的机械臂自动设计方法，其轨迹跟踪验证思路与本任务的随机采样验证有方法论上的相似性。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [项目 Wiki 第 8 章（运动学与动力学）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)

## Overview

Verification of forward and inverse kinematics solution is the first third-level subtask in the URDF modeling and kinematics calibration phase (P6.2). Its core objective is to confirm the consistency between the robot kinematics model (URDF) and the CAD design through systematic testing and quantitative metrics, while also validating the accuracy and reliability of the inverse kinematics (IK) solver. This task uses "random 1000 pose IK success rate > 99%, position error < 1 mm" as its completion criterion. What it truly changes is not merely "running a simulation once," but rather transforming "model correctness" from a qualitative judgment into a quantifiable, traceable engineering acceptance process.

## Content

### What It Is: Precise Definition

Verification of forward and inverse kinematics solution is a structured engineering validation process comprising five fourth-level subtasks: input organization and target quantification (P6.2.1.1), scheme/method design (P6.2.1.2), implementation/prototype/sample fabrication (P6.2.1.3), test execution and result analysis (P6.2.1.4), and documentation output and downstream delivery (P6.2.1.5). Its technical means include analytical/numerical inverse solutions, random pose sampling, and singularity analysis. The validation targets are the consistency of joint limits and link lengths in the URDF model with CAD, as well as the success rate and accuracy of the IK solver.

From a mathematical perspective, forward kinematics defines the mapping $x = f(q)$ (from joint space to task space), while inverse kinematics solves its inverse mapping $q = f^{-1}(x)$. The core of the validation task lies in confirming that $f(q)$ is consistent with the physical prototype (forward solution verification) and that $f^{-1}(x)$ converges reliably within the target workspace (inverse solution verification).

### Why It Exists: Pain Points and Historical Positioning

In the robot development pipeline, the URDF model serves as the common input to the simulation, control, and planning toolchain. If the model is inconsistent with CAD—for example, a link length deviation of 0.5 mm (example value, engineering judgment) or incorrect joint limit settings—simulation results will systematically diverge from actual robot behavior, and all downstream algorithms relying on this model (motion planning, dynamics control, state estimation) will inherit this error. Historically, such issues often surface only during prototype debugging, where the cost of correction is extremely high.

Verification of forward and inverse kinematics solution exists precisely to detect and eliminate these deviations during the "design phase" rather than the "debugging phase." It quantifies the acceptance criteria as "random 1000 pose IK success rate > 99%, position error < 1 mm," giving model correctness a clear, testable criterion. What it truly changes is not the "validation method" itself, but rather making "model credibility" an auditable, traceable engineering milestone in the development process.

### Principle Breakdown

**① Forward Kinematics Verification: URDF-CAD Consistency Check**

The core of forward kinematics verification is confirming that the kinematic parameters in the URDF (link lengths, joint origins, rotation axis directions, joint limits) match the CAD model. The specific approach is to export key dimensions from CAD and compare them item by item against the `<origin>`, `<axis>`, and `<limit>` fields in the URDF. For high-degree-of-freedom systems like humanoid robots (typically 30+ DOF), manual comparison is impractical; automated scripts are needed to extract URDF parameters and perform numerical comparison against the CAD-exported parameter table, with tolerances typically set at the sub-millimeter level (engineering judgment).

**② Inverse Kinematics Verification: Random Sampling and Success Rate Statistics**

Inverse kinematics verification employs a random pose sampling strategy: 1000 end-effector poses are randomly generated within the workspace, and the IK solver is invoked to solve each one, with success rate and position error being recorded. The mathematical foundation of this method lies in the multi-solution nature and singularity of the IK problem:

$$
q^* = \arg\min_{q} \| f(q) - x_d \|^2 \quad \text{s.t.} \quad q_{\min} \le q \le q_{\max}
$$

where $x_d$ is the desired end-effector pose, and $q_{\min}$, $q_{\max}$ are the joint limits. Numerical solvers (such as damped least squares, Levenberg-Marquardt) approximate $q^*$ through iterative minimization of pose error. A success rate below the threshold typically indicates: incorrect URDF parameters, unreasonable joint limit settings, or unhandled singular regions within the workspace.

**③ Singularity Analysis: Identifying Unreachable Regions**

Singularity refers to configurations where the Jacobian matrix $J(q)$ is rank-deficient, potentially causing the IK solver to diverge or produce abrupt joint velocity changes. The validation process must identify singular regions within the workspace and confirm whether these regions fall outside actual task requirements. For humanoid robots, singular configurations commonly occur in poses such as fully extended arms or nearly upright legs.

### Key Parameters and Specifications

| Parameter | Specification | Source |
|-----------|---------------|--------|
| Number of random sampled poses | 1000 sets | Corpus |
| IK success rate threshold | > 99% | Corpus |
| Position error threshold | < 1 mm | Corpus |
| Number of candidate schemes | ≥ 2 | Corpus |
| Joint limit check | Consistent with CAD | Corpus |
| Link length check | Consistent with CAD | Corpus |

### Horizontal Comparison

Compared with IK verification for general robotic arms, verification of forward and inverse kinematics solution for humanoid robots exhibits significant differences. General 6-DOF arms (satisfying the Pieper condition) admit analytical closed-form solutions, with the verification focus being multi-solution selection strategies; humanoid robots, by contrast, typically include 7-DOF redundant arms and 6-DOF legs, where numerical solving is the mainstream approach, shifting the verification focus to solver convergence and singularity robustness. Furthermore, the humanoid robot workspace contains numerous self-collision constraints and joint limit constraints, and random sampling must account for the combined effects of these constraints (engineering judgment).

Compared with academic IK algorithm evaluations (such as the TRAC-IK benchmark), this task is positioned as **engineering acceptance** rather than **algorithm research**: it does not pursue upper-bound algorithm performance but rather ensures that the currently selected solver meets established thresholds within the target workspace.

### Who Uses It: Application Cases

This validation process is a standard procedure for kinematics model delivery in robot product development, with primary users including:

1. **Kinematics engineers**: Execute URDF parameter extraction, CAD comparison, IK solver configuration, and testing;
2. **Simulation engineers**: Receive validated URDF models for dynamics simulation and motion planning;
3. **Control algorithm engineers**: Rely on validated kinematics models to develop WBC (whole-body control) and task-space control strategies;
4. **System integration engineers**: Compare simulation models against actual robot behavior during prototype debugging.

Typical application scenario: In bipedal humanoid robot development, this validation ensures that gait planning results from simulation can be directly transferred to the physical robot without extensive on-site tuning. If validation fails, all downstream work dependent on the kinematics model is blocked.

### Limitations and Boundaries

1. **Limitations of static validation**: This validation covers only the kinematics level and does not involve identification or validation of dynamics parameters (mass, inertia, friction), which belong to subsequent subtasks of P6.2 or a separate phase;
2. **Blind spots in sampling coverage**: Although random 1000 poses carry statistical significance, they cannot guarantee coverage of all boundary conditions (such as extreme poses near joint limits), necessitating supplementary targeted boundary testing (engineering judgment);
3. **Solver dependency**: Validation results are strongly correlated with the chosen IK solver; switching solvers requires re-validation;
4. **Accuracy of the CAD baseline**: Validation uses the CAD model as the reference; if the CAD itself deviates from the physical prototype, the validation cannot detect this deviation;
5. **No dynamic performance coverage**: The position error < 1 mm metric applies only to static poses and does not include tracking errors during motion.

### Common Misconceptions

1. **"Validation passed = model is completely correct"**—Wrong. Validation covers only the sampled pose space and uses CAD as the reference, so absolute correctness cannot be guaranteed;
2. **"The higher the IK success rate, the better"**—Not necessarily. An excessively high success rate may indicate an overly relaxed solver (e.g., allowing larger pose errors); the position error metric must also be checked;
3. **"Random sampling can replace boundary testing"**—Wrong. Random sampling has low coverage probability for boundary conditions; targeted testing near joint limits and singular configurations must be supplemented;
4. **"Validation is a one-time effort"**—Wrong. The URDF model undergoes iterative updates during development (e.g., structural optimization, joint limit adjustments), and validation must be re-executed after each change;
5. **"Numerical IK is always worse than analytical IK"**—Not necessarily. Modern numerical solvers (such as TRAC-IK) often outperform analytical methods in robustness, especially for redundant-degree-of-freedom systems.

### Related Knowledge

- `ent_method_inverse_kinematics` — Mathematical foundations and algorithm taxonomy of inverse kinematics solving; the validation target of this task;
- `ent_process_p6` — Overall positioning of the URDF modeling and kinematics calibration phase; this task is the first subtask of that phase;
- `ent_paper_desai_automatic_design_of_task_speci_2018` — Task-driven automatic design of robotic arms; its trajectory tracking validation approach shares methodological similarities with the random sampling validation used in this task.

## 개요

정역운동학 해석 검증(Verification of forward and inverse kinematics solution)은 URDF 모델링 및 운동학교核 단계(P6.2)의 첫 번째 3급 하위 작업으로, 체계적인 테스트와 정량적 지표를 통해 로봇 운동학 모델(URDF)과 CAD 설계의 일치성을 확인하고, 역운동학(IK) 솔버의 정밀도와 신뢰성을 검증하는 것을 핵심 목표로 합니다. 이 작업은 "무작위 1000개 자세 IK 성공률 > 99%, 위치 오차 < 1 mm"를 완료 기준으로 삼으며, 진정으로 바꾸는 것은 "시뮬레이션을 한 번 실행하는 것" 자체가 아니라 "모델 정확성"을 정성적 판단에서 정량적이고 추적 가능한 엔지니어링 검수 프로세스로 전환하는 것입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

정역운동학 해석 검증은 구조화된 엔지니어링 검증 프로세스로, 다섯 개의 4급 하위 작업을 포함합니다: 입력 정리 및 목표 정량화(P6.2.1.1), 방안/방법 설계(P6.2.1.2), 구현/프로토타입/시제품 제작(P6.2.1.3), 테스트 실행 및 결과 분석(P6.2.1.4), 문서 출력 및 하류 전달(P6.2.1.5). 기술적 수단으로는 해석적/수치적 역해, 무작위 자세 샘플링 및 특이성 분석을 포함하며, 검증 대상은 URDF 모델의 관절 한계, 링크 길이와 CAD의 일치성, 그리고 IK 해석의 성공률과 정밀도입니다.

수학적 본질에서 볼 때, 정운동학은 매핑 $x = f(q)$(관절 공간에서 작업 공간으로)을 정의하고, 역운동학은 그 역매핑 $q = f^{-1}(x)$을 해석합니다. 검증 작업의 핵심은 $f(q)$가 물리적 프로토타입과 일치하는지(정해 검증) 확인하고, $f^{-1}(x)$가 목표 작업 공간 내에서 안정적으로 수렴하는지(역해 검증) 확인하는 것입니다.

### 왜 존재하는가:痛点과 역사적 위치

로봇 개발 프로세스에서 URDF 모델은 시뮬레이션, 제어 및 계획 도구 체인의 공통 입력입니다. 모델이 CAD와 일치하지 않을 경우—예를 들어 링크 길이 편차 0.5 mm(예시 값, 엔지니어링 판단), 관절 한계 설정 오류—시뮬레이션 결과와 실제 기계 동작이 체계적으로 벗어나게 되며, 이후 이 모델에 의존하는 모든 알고리즘(운동 계획, 동역학 제어, 상태 추정)이 이 오차를 상속받게 됩니다. 역사적으로 이러한 문제는 종종 프로토타입 디버깅 단계에서야 드러나며, 수정 비용이 매우 높습니다.

정역운동학 해석 검증은 바로 "디버깅 단계"가 아닌 "설계 단계"에서 이러한 편차를 발견하고 제거하기 위한 것입니다. 이는 검수 기준을 "무작위 1000개 자세 IK 성공률 > 99%, 위치 오차 < 1 mm"로 정량화하여, 모델 정확성에 명확하고 테스트 가능한 판단 기준을 제공합니다. 진정으로 바꾸는 것은 "검증 방법" 자체가 아니라 "모델 신뢰성"을 개발 프로세스에서 감사 가능하고 추적 가능한 엔지니어링 노드로 만드는 것입니다.

### 원리 분석

**① 정운동학 검증: URDF와 CAD의 일치성 확인**

정운동학 검증의 핵심은 URDF의 운동학 파라미터(링크 길이, 관절 원점, 회전축 방향, 관절 한계)가 CAD 모델과 일치하는지 확인하는 것입니다. 구체적인 방법은 CAD에서 주요 치수를 내보내고 URDF의 `<origin>`, `<axis>`, `<limit>` 필드와 항목별로 비교하는 것입니다. 휴머노이드 로봇과 같은 고자유도 시스템(일반적으로 30+ 자유도)의 경우 수동 비교는 현실적이지 않으므로, 자동화 스크립트를 통해 URDF 파라미터를 추출하고 CAD에서 내보낸 파라미터 테이블과 수치 비교를 수행해야 하며, 공차는 일반적으로 서브밀리미터 수준(엔지니어링 판단)으로 설정됩니다.

**② 역운동학 검증: 무작위 샘플링 및 성공률 통계**

역운동학 검증은 무작위 자세 샘플링 전략을 채택합니다: 작업 공간 내에서 무작위로 1000개의 말단 자세를 생성하고, IK 솔버를 호출하여 하나씩 해석하며, 성공률과 위치 오차를 통계적으로 분석합니다. 이 방법의 수학적 기초는 IK 문제의 다중 해와 특이성입니다:

$$
q^* = \arg\min_{q} \| f(q) - x_d \|^2 \quad \text{s.t.} \quad q_{\min} \le q \le q_{\max}
$$

여기서 $x_d$는 목표 말단 자세, $q_{\min}$, $q_{\max}$는 관절 한계입니다. 수치 솔버(예: 감쇠 최소 제곱, Levenberg-Marquardt)는 반복적으로 자세 오차를 최소화하여 $q^*$에 근접합니다. 성공률이 임계값보다 낮은 경우 일반적으로 URDF 파라미터 오류, 관절 한계 설정 불합리, 또는 작업 공간 내 처리되지 않은 특이 영역이 존재함을 의미합니다.

**③ 특이성 분석: 도달 불가능 영역 식별**

특이성은 Jacobian 행렬 $J(q)$의 랭크 결손 자세를 의미하며, 이때 IK 솔버가 발산하거나 관절 속도 급변을 유발할 수 있습니다. 검증 프로세스는 작업 공간 내 특이 영역을 식별하고, 이러한 영역이 실제 작업 요구 범위 밖에 있는지 확인해야 합니다. 휴머노이드 로봇의 경우 특이 자세는 팔이 완전히 펴지거나 다리가 직립에 가까운 자세에서 흔히 발생합니다.

### 핵심 파라미터 및 사양

| 파라미터 | 사양 | 출처 |
|------|------|------|
| 무작위 샘플링 자세 수 | 1000개 | 말뭉치 |
| IK 성공률 임계값 | > 99% | 말뭉치 |
| 위치 오차 임계값 | < 1 mm | 말뭉치 |
| 후보 방안 수 | ≥ 2개 | 말뭉치 |
| 관절 한계 확인 | CAD와 일치 | 말뭉치 |
| 링크 길이 확인 | CAD와 일치 | 말뭉치 |

### 횡적 비교

일반 로봇 팔 IK 검증과 비교할 때, 휴머노이드 로봇의 정역운동학 해석 검증은 현저한 차이가 있습니다. 일반 6-DOF 로봇 팔(Pieper 조건 충족)은 해석적 폐쇄형 해가 존재하며, 검증의 초점은 다중 해 선택 전략에 있습니다. 반면 휴머노이드 로봇은 일반적으로 7-DOF 여유 팔과 6-DOF 다리를 포함하므로 수치 해석이 주류이며, 검증의 초점은 솔버 수렴성과 특이 강건성으로 전환됩니다. 또한 휴머노이드 로봇의 작업 공간은 많은 자체 충돌 제약과 관절 한계 제약을 포함하므로, 무작위 샘플링은 이러한 제약의 결합 영향을 고려해야 합니다(엔지니어링 판단).

학술 연구의 IK 알고리즘 평가(예: TRAC-IK 벤치마크 테스트)와 비교할 때, 이 작업의 위치는 **엔지니어링 검수**이지 **알고리즘 연구**가 아닙니다: 알고리즘 성능 상한을 추구하는 것이 아니라, 현재 선택된 솔버가 목표 작업 공간 내에서 설정된 임계값을 충족하는지 확인하는 것입니다.

### 사용자·적용 사례

이 검증 프로세스는 로봇 제품 개발에서 운동학 모델 인도의 표준 공정으로, 주요 사용자는 다음과 같습니다:

1. **운동학 엔지니어**: URDF 파라미터 추출, CAD 비교, IK 솔버 구성 및 테스트 수행;
2. **시뮬레이션 엔지니어**: 검증을 통과한 URDF 모델을 받아 동역학 시뮬레이션 및 운동 계획에 사용;
3. **제어 알고리즘 엔지니어**: 검증된 운동학 모델에 의존하여 WBC(전신 제어) 및 작업 공간 제어 전략 개발;
4. **시스템 통합 엔지니어**: 프로토타입 디버깅 단계에서 시뮬레이션 모델과 실제 기계 동작을 대조.

전형적인 적용 시나리오: 이족 보행 휴머노이드 로봇 개발에서 이 검증은 시뮬레이션의 보행 계획 결과가 현장 파라미터 튜닝 없이 실제 기계로 직접 이전될 수 있도록 보장합니다. 검증이 통과되지 않으면 하류의 모든 운동학 모델 의존 작업이 차단됩니다.

### 한계와 경계

1. **정적 검증의 한계**: 이 검증은 운동학 수준만 다루며, 동역학 파라미터(질량, 관성, 마찰)의 식별 및 검증은 포함하지 않습니다. 후자는 P6.2의 후속 하위 작업 또는 독립 단계에 속합니다;
2. **샘플링 커버리지의 사각지대**: 무작위 1000개 자세는 통계적 의미가 있지만, 모든 경계 조건(예: 관절 한계 근처의 극단 자세)을 보장할 수는 없으므로, 목표 지향적 경계 테스트(엔지니어링 판단)를 보완해야 합니다;
3. **솔버 의존성**: 검증 결과는 선택된 IK 솔버와 강하게 연관되므로, 솔버를 교체한 후에는 재검증이 필요합니다;
4. **CAD 기준의 정확성**: 검증은 CAD 모델을 기준으로 하므로, CAD 자체가 물리적 프로토타입과 편차가 있다면 검증은 이 편차를 발견할 수 없습니다;
5. **동적 성능 미포함**: 위치 오차 < 1 mm 지표는 정적 자세에만 해당하며, 운동 과정의 추적 오차는 포함하지 않습니다.

### 일반적인 오해

1. **"검증 통과 = 모델 완전 정확"**—아닙니다. 검증은 샘플링된 자세 공간만 다루며 CAD를 기준으로 하므로 절대적 정확성을 보장할 수 없습니다;
2. **"IK 성공률이 높을수록 좋다"**—반드시 그렇지 않습니다. 과도하게 높은 성공률은 솔버가 과도하게 이완되었음을 의미할 수 있으며(예: 큰 자세 오차 허용), 위치 오차 지표를 함께 확인해야 합니다;
3. **"무작위 샘플링이 경계 테스트를 대체할 수 있다"**—아닙니다. 무작위 샘플링은 경계 조건 커버리지 확률이 낮으므로, 관절 한계, 특이 자세 주변의 목표 지향적 테스트를 보완해야 합니다;
4. **"검증은 일회성 작업이다"**—아닙니다. URDF 모델은 개발 과정에서 반복적으로 업데이트되며(예: 구조 최적화, 관절 한계 조정), 변경 후마다 검증을 다시 실행해야 합니다;
5. **"수치 IK가 반드시 해석 IK보다 나쁘다"**—반드시 그렇지 않습니다. 현대 수치 솔버(예: TRAC-IK)는 강건성에서 종종 해석법보다 우수하며, 특히 여유 자유도 시스템에서 그렇습니다.

### 관련 지식

- `ent_method_inverse_kinematics` — 역운동학 해석의 수학적 기초와 알고리즘 분류, 이 작업의 검증 대상;
- `ent_process_p6` — URDF 모델링 및 운동학교核 단계의 전체적 위치, 이 작업은 해당 단계의 첫 번째 하위 작업;
- `ent_paper_desai_automatic_design_of_task_speci_2018` — 작업 중심 로봇 팔 자동 설계 방법, 그 궤적 추적 검증 접근 방식은 이 작업의 무작위 샘플링 검증과 방법론적 유사성이 있습니다.
