---
$id: ent_process_p12_2_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: VLA Model Selection and Fine-Tuning
  zh: VLA 模型选型与微调
  ko: VLA 모델 선정 및 미세 조정
summary:
  en: End-to-end demo that executes natural language instructions, success rate metric
  zh: '- P12.2.1.1 输入梳理与目标量化 - 整理「VLA 模型选型与微调」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 可执行自然语言指令的端到端 demo、成功率指标
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b2): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

VLA 模型选型与微调（P12.2.1）是人形机器人产品开发全流程中「VLA 与操作策略」工作包下的首个三级子任务，属于方法（method）类型，归属领域为 `07_ai_models_algorithms`，价值链层级为 `intelligence`。该工序以 OpenVLA、RT-X、π0、Diffusion Policy 及自研模型为候选方法池，核心目标是将视觉-语言指令映射到机器人动作，并针对本机本体进行微调。它真正改变的不是单个模型的精度上限，而是让「能跑通 demo 的模型」变成「可量化验收、可追溯交付的工程模块」。

## 核心内容

### 是什么：准确定义

VLA（Vision-Language-Action）模型选型与微调，是指在人形机器人操作策略开发中，从候选模型池中筛选适配本机硬件与任务需求的视觉-语言-动作模型，并通过数据采集、仿真验证与参数调优，使其具备可执行自然语言指令的端到端操作能力。该工序的完成标准是交付「可执行自然语言指令的端到端 demo」并达到预设的成功率指标。

在 WBS V3 的工序分解中，P12.2.1 被展开为五个四级关键动作组：输入梳理与目标量化（P12.2.1.1）、算法/控制方案设计（P12.2.1.2）、算法实现与仿真验证（P12.2.1.3）、算法调参与性能验证（P12.2.1.4）、文档输出与下游交付（P12.2.1.5）。每个动作组遵循「输入→方案→实施→验证→交付」的标准化工程闭环。

### 为什么存在：痛点与历史定位

在 VLA 模型进入机器人领域之前，操作策略开发长期被两条路线割裂：传统运动规划方法依赖精确的环境建模与手工设计的奖励函数，泛化能力弱；而端到端模仿学习方法虽然能拟合演示数据，却难以理解自然语言指令，且对硬件差异极度敏感。VLA 模型试图用「视觉-语言联合表征」统一这两条路线，但随之而来的是新的工程痛点：

- **模型规模与算力矛盾**：主流 VLA 模型参数量动辄数十亿，而人形机器人本体往往只配备消费级 GPU。如何在有限算力下完成微调与推理，是选型的第一道门槛。
- **sim-to-real 鸿沟**：仿真环境中训练的策略迁移到真实本体时，因视觉特征漂移、动力学差异等因素导致性能骤降。
- **数据效率瓶颈**：真实机器人演示数据采集成本极高，单条轨迹的标注与清洗耗时远超传统 CV 数据集。
- **安全性约束**：人形机器人的操作对象可能包含人类或易碎物品，模型输出的动作必须满足关节限位、力矩约束等硬性安全边界。

P12.2.1 在流程中的历史定位，正是为了在「模型能力」与「工程可交付性」之间建立一座可量化的桥梁——它不负责发明新模型，而是负责把已有模型「驯化」成符合本机本体约束、可验收、可追溯的产品组件。

### 原理拆解

**① 输入梳理与目标量化（P12.2.1.1）**

该动作组的本质是「把模糊的完成标准翻译成可测量的 KPI」。上游输入包括：感知栈输出的视觉特征格式、机械臂/灵巧手的运动学参数、任务场景的自然语言指令集、以及算力资源清单。关键动作有三项：

1. 列出所有上游输入清单并确认版本——防止因传感器固件或接口版本不一致导致后续微调失效；
2. 将验收标准转化为可量化 KPI——例如「成功率 ≥ 90%（按 50 次测试统计）」「单次推理延迟 ≤ 100ms」（均为示例值，工程判断）；
3. 建立任务 Owner、时间节点与风险登记——明确责任人与里程碑，将风险（如数据不足、算力超限）提前登记在册。

**② 算法/控制方案设计（P12.2.1.2）**

该动作组要求形成不少于 2 个候选方案，并建立评估矩阵量化打分。候选方案通常来自 OpenVLA、RT-X、π0、Diffusion Policy 及自研模型的组合。评估维度至少包括：

- **稳定性**：在相同输入下输出动作的方差；
- **实时性**：从视觉输入到动作指令的端到端延迟；
- **可扩展性**：能否在不重训的情况下适配新任务指令。

评估矩阵的量化打分需要结合本机本体的实测数据（如关节响应带宽、视觉帧率），而非仅依赖论文报告值。最终通过组织评审冻结实现路径。

**③ 算法实现与仿真验证（P12.2.1.3）**

在仿真环境或离线数据中实现算法，验证功能正确性、实时性与鲁棒性。关键动作包括：建立模型/样机并记录关键参数（如学习率、batch size、LoRA rank）、执行仿真或原型验证、记录异常与偏差。此阶段的核心价值在于「低成本试错」——仿真中暴露的问题（如动作序列偏差、末端执行器定位误差）可以在不消耗真实机器人寿命的前提下修复。

**④ 算法调参与性能验证（P12.2.1.4）**

对算法进行参数调优与边界测试，验证在典型/极端工况下的性能是否满足指标。关键动作包括：制定测试/评审计划与通过准则、执行测试并记录原始数据、输出问题清单与改进措施。边界测试应覆盖：光照突变、遮挡、目标物体颜色与训练分布偏移、关节接近限位等场景。

**⑤ 文档输出与下游交付（P12.2.1.5）**

输出最终报告/图纸/规范，更新 ICD/BOM/SOP/需求追溯链，完成向下游环节的正式交付。关键动作包括：按模板编写文档并引用原始数据、完成内部评审与版本控制、发布并通知下游依赖方。这一步保证了「模型微调」这一智力活动被固化为组织资产，而非停留在个人经验层面。

### 关键参数与规格

| 参数/规格 | 典型值/范围 | 来源说明 |
|---|---|---|
| 候选方案数量下限 | 不少于 2 个 | 语料 P12.2.1.2 |
| 微调方法 | LoRA（低秩适配） | 邻居卡 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 模型参数量示例 | 31 亿参数（3.1B） | 邻居卡 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 最低显存要求 | 8GB VRAM | 邻居卡 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 演示数据量示例 | 200 条演示轨迹 | 邻居卡 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 在线微调时长示例 | 101 分钟 | 邻居卡 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 多任务成功率示例 | 100%（3 项基础任务） | 邻居卡 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 长时域任务成功率示例 | 50%（连续 12 次操作） | 邻居卡 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 双机器人协同效率提升 | 2 倍 | 邻居卡 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |

### 横向对比

在 VLA 模型选型中，不同候选方案的核心差异体现在「架构设计」与「数据需求」两个维度：

- **OpenVLA**：基于预训练视觉-语言模型（如 Prismatic）扩展动作头，采用离散化动作 token 预测。优势是语言理解能力强，劣势是动作精度受离散化粒度限制。
- **RT-X（Open X-Embodiment）**：强调跨本体、跨任务的大规模数据预训练，通过统一动作空间实现多机器人迁移。优势是泛化性强，劣势是微调时需要适配本体的动作空间定义。
- **π0（π-zero）**：采用流匹配（flow matching）生成连续动作序列，适合高精度操作。优势是动作平滑性好，劣势是训练稳定性对超参数敏感。
- **Diffusion Policy**：将动作生成建模为去噪扩散过程，擅长多模态动作分布建模。优势是能表达多种可行策略，劣势是推理延迟较高。
- **自研模型**：针对本机本体定制，可深度耦合运动学约束，但开发周期与数据需求最大。

选型决策的关键不是「哪个模型最强」，而是「哪个模型在给定算力、数据量与安全约束下，能以最低成本达到 KPI」。例如，在 8GB 显存限制下，3.1B 参数的 VLA 模型需配合 LoRA 与量化技术才能运行（按语料数据推算），此时 Diffusion Policy 类模型的参数量可能成为瓶颈。

### 谁在用·应用案例

**案例一：资源受限平台的低成本微调（源自 ent_paper_omaisan_towards_accessible_physical_ai_2025）**

研究团队在 SO101 机械臂上执行按钮按压任务，使用 200 条演示数据对 3.1B 参数的 VLA 模型进行 LoRA 微调，并配合量化技术将显存需求压缩至 8GB。关键发现是：冻结视觉编码器策略在低数据量下表现更优，可避免过拟合；主要失败模式包括视觉特征漂移、动作序列偏差与末端执行器定位误差。该案例证明了「消费级 GPU + 少量演示数据」也能完成有效的 VLA 微调。

**案例二：人机协同双智能体微调（源自 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025）**

该研究提出主智能体（负责多任务鲁棒执行）与精调智能体（负责潜在空间适应性调整）的协同框架，并引入「对话-调整」机制将人类物理干预转化为语义化指令。在真实多任务实验中，经过 101 分钟在线微调后，三项基础任务（抓取、放置、堆叠）均达到 100% 成功率，而标准监督微调（SFT）基线平均成功率仅 67%；长时域任务（连续 12 次操作）保持 50% 成功率，显著优于纯强化学习（RL）方法的 12% 成功率。双机器人协同训练时效率提升达 2 倍。

**案例三：跨平台数据采集与策略部署（源自 ent_paper_nian_control_your_robot_a_unified_s_2025）**

Control Your Robot 框架通过标准化工作流（模块化设计、统一 API、闭环架构）支持遥操作与轨迹回放双模式控制，实现从多模态数据采集到推理的无缝集成。在单臂与双臂系统上的实验表明，基于该框架采集数据训练的策略与专家演示高度匹配，为 VLA 微调提供了高质量的数据基础设施。

### 局限与边界

1. **数据量-性能的边际递减**：训练数据量与真实世界性能呈正相关，但存在边际递减效应（源自 ent_paper_omaisan_towards_accessible_physical_ai_2025）。盲目增加数据量并不总能线性提升成功率。
2. **视觉编码器冻结的权衡**：冻结视觉编码器在低数据量下表现更优，但可能限制模型对新型视觉特征的适应能力；微调视觉编码器则需更多数据与算力。
3. **长时域任务的稳定性瓶颈**：即使经过微调，长时域任务（如连续 12 次操作）的成功率仍显著低于单步任务，误差累积问题尚未根本解决。
4. **sim-to-real 的残余鸿沟**：仿真验证通过不等于真实部署成功，视觉特征漂移与动力学差异仍需在真实本体上迭代调参。
5. **算力约束的硬边界**：8GB 显存是当前低成本部署的典型下限，但更大模型（如 7B 以上）的微调与推理仍需要更高规格的硬件。

### 常见误区

1. **「选型就是选最强的模型」**——错。选型的本质是「在约束下求最优解」，算力、数据量、安全边界都是硬约束。3.1B 模型在 8GB 显存上能跑，但 7B 模型可能连加载都失败。
2. **「微调就是全参数重训」**——错。LoRA 等参数高效微调方法仅更新低秩矩阵，显存占用与训练时间大幅降低，且在小数据量下表现更优。
3. **「仿真验证通过就等于真实可用」**——错。仿真环境无法完全模拟视觉光照变化、关节摩擦、线缆拖拽等真实因素，必须预留真实本体调参周期。
4. **「成功率是唯一 KPI」**——错。延迟、稳定性、可扩展性同样重要。一个成功率 95% 但推理延迟 500ms 的模型（示例值，工程判断），在实时操作场景中可能完全不可用。
5. **「文档输出是形式主义」**——错。P12.2.1.5 的文档交付（报告、ICD/BOM/SOP 更新、需求追溯链）是保证模型可维护、可复现、可审计的关键，缺失文档的模型在后续阶段将无法迭代。

### 相关知识

- `ent_process_p12` — 本工序所属的上级工作包，定义了 VLA / WAM / AI 算法集成的整体工序框架与五步法。
- `ent_paper_omaisan_towards_accessible_physical_ai_2025` — 提供了 LoRA + 量化在 8GB 显存上微调 3.1B VLA 模型的完整方法，是资源受限场景的选型参考。
- `ent_paper_jin_dual_actor_fine_tuning_of_vla_2025` — 展示了人机协同双智能体微调框架，为「人类修正指令 → 语义化训练数据」提供了可操作路径。
- `ent_paper_nian_control_your_robot_a_unified_s_2025` — 提供了跨平台数据采集与策略部署的标准化工作流，是 P12.2.1.1 输入梳理环节的工具参考。
- `ent_method_openvla` — 候选模型池中的代表方法之一，其离散化动作 token 预测机制与 Diffusion Policy 的连续动作生成形成对比。
- `ent_paper_collaboration_open_x_embodiment_robotic_lear_2023` — 对应 RT-X 方法，强调跨本体预训练与动作空间统一，是选型时「泛化性」维度的关键参考。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

VLA model selection and fine-tuning (P12.2.1) is the first level-3 sub-task under the "VLA and Manipulation Policy" work package in the humanoid robot product development lifecycle. It is classified as a method type, belonging to the domain `07_ai_models_algorithms`, with a value chain level of `intelligence`. This process uses OpenVLA, RT-X, π0, Diffusion Policy, and self-developed models as the candidate method pool. Its core objective is to map vision-language instructions to robot actions and fine-tune them for the specific robot platform. What it truly changes is not the accuracy ceiling of a single model, but rather transforming "models that can run demos" into "engineering modules that can be quantitatively validated and traceably delivered."

## Content

### What It Is: Precise Definition

VLA (Vision-Language-Action) model selection and fine-tuning refers to, within humanoid robot manipulation policy development, selecting vision-language-action models from a candidate model pool that match the hardware and task requirements of the specific robot, and through data collection, simulation validation, and parameter tuning, enabling them to possess end-to-end manipulation capabilities that can execute natural language instructions. The completion criterion for this process is delivering an "end-to-end demo that can execute natural language instructions" and achieving the preset success rate metrics.

In the WBS V3 process decomposition, P12.2.1 is expanded into five level-4 key action groups: input organization and target quantification (P12.2.1.1), algorithm/control scheme design (P12.2.1.2), algorithm implementation and simulation validation (P12.2.1.3), algorithm tuning and performance validation (P12.2.1.4), and documentation output and downstream delivery (P12.2.1.5). Each action group follows the standardized engineering closed loop of "input → scheme → implementation → validation → delivery."

### Why It Exists: Pain Points and Historical Positioning

Before VLA models entered the robotics domain, manipulation policy development had long been fragmented across two approaches: traditional motion planning methods relied on precise environment modeling and hand-designed reward functions, exhibiting weak generalization; while end-to-end imitation learning methods could fit demonstration data but struggled to understand natural language instructions and were extremely sensitive to hardware differences. VLA models attempt to unify these two approaches using "vision-language joint representations," but this brings new engineering pain points:

- **Model scale vs. compute contradiction**: Mainstream VLA models have parameter counts in the billions, while humanoid robot platforms often only have consumer-grade GPUs. How to complete fine-tuning and inference under limited compute is the first hurdle in model selection.
- **Sim-to-real gap**: Policies trained in simulation environments often suffer severe performance degradation when transferred to real robots due to visual feature drift, dynamics differences, and other factors.
- **Data efficiency bottleneck**: Real robot demonstration data collection costs are extremely high, with annotation and cleaning of a single trajectory taking far longer than traditional CV datasets.
- **Safety constraints**: Humanoid robots may manipulate objects involving humans or fragile items, so model output actions must satisfy hard safety boundaries such as joint limits and torque constraints.

The historical positioning of P12.2.1 in the process is precisely to build a quantifiable bridge between "model capability" and "engineering deliverability"—it is not responsible for inventing new models, but rather for "domesticating" existing models into product components that conform to the specific robot's constraints, are acceptable for validation, and are traceable.

### Principle Breakdown

**① Input Organization and Target Quantification (P12.2.1.1)**

The essence of this action group is "translating vague completion criteria into measurable KPIs." Upstream inputs include: visual feature formats output by the perception stack, kinematic parameters of the robotic arm/dexterous hand, natural language instruction sets for task scenarios, and compute resource inventories. There are three key actions:

1. List all upstream input inventories and confirm versions—to prevent fine-tuning failures caused by inconsistent sensor firmware or interface versions;
2. Convert acceptance criteria into quantifiable KPIs—for example, "success rate ≥ 90% (based on 50 test trials)" or "single inference latency ≤ 100ms" (both are example values, subject to engineering judgment);
3. Establish task owners, timelines, and risk registers—clarifying responsible parties and milestones, and registering risks (such as insufficient data or compute overruns) in advance.

**② Algorithm/Control Scheme Design (P12.2.1.2)**

This action group requires forming no fewer than 2 candidate schemes and establishing an evaluation matrix for quantitative scoring. Candidate schemes typically come from combinations of OpenVLA, RT-X, π0, Diffusion Policy, and self-developed models. Evaluation dimensions must include at least:

- **Stability**: variance of output actions under identical inputs;
- **Real-time performance**: end-to-end latency from visual input to action command;
- **Scalability**: whether new task instructions can be adapted without retraining.

The quantitative scoring in the evaluation matrix must incorporate measured data from the specific robot platform (such as joint response bandwidth and visual frame rate), rather than relying solely on values reported in papers. The implementation path is ultimately frozen through organizational review.

**③ Algorithm Implementation and Simulation Validation (P12.2.1.3)**

Implement the algorithm in simulation environments or on offline data to validate functional correctness, real-time performance, and robustness. Key actions include: establishing models/prototypes and recording key parameters (such as learning rate, batch size, LoRA rank), executing simulation or prototype validation, and recording anomalies and deviations. The core value of this stage lies in "low-cost trial and error"—issues exposed in simulation (such as action sequence deviations or end-effector positioning errors) can be fixed without consuming real robot lifespan.

**④ Algorithm Tuning and Performance Validation (P12.2.1.4)**

Perform parameter tuning and boundary testing on the algorithm to validate whether performance meets metrics under typical/extreme operating conditions. Key actions include: formulating test/review plans and pass criteria, executing tests and recording raw data, and outputting issue lists and improvement measures. Boundary testing should cover scenarios such as: sudden illumination changes, occlusion, target object color distribution shifts from training data, and joints approaching limits.

**⑤ Documentation Output and Downstream Delivery (P12.2.1.5)**

Output final reports/drawings/specifications, update ICD/BOM/SOP/requirements traceability chains, and complete formal delivery to downstream stages. Key actions include: writing documentation per templates with references to raw data, completing internal review and version control, and publishing and notifying downstream dependent parties. This step ensures that the intellectual activity of "model fine-tuning" is solidified as organizational assets rather than remaining at the level of personal experience.

### Key Parameters and Specifications

| Parameter/Specification | Typical Value/Range | Source Description |
|---|---|---|
| Minimum number of candidate schemes | No fewer than 2 | Corpus P12.2.1.2 |
| Fine-tuning method | LoRA (Low-Rank Adaptation) | Neighbor card ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| Example model parameter count | 3.1 billion parameters (3.1B) | Neighbor card ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| Minimum VRAM requirement | 8GB VRAM | Neighbor card ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| Example demonstration data volume | 200 demonstration trajectories | Neighbor card ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| Example online fine-tuning duration | 101 minutes | Neighbor card ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| Example multi-task success rate | 100% (3 basic tasks) | Neighbor card ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| Example long-horizon task success rate | 50% (12 consecutive manipulations) | Neighbor card ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| Dual-robot collaborative efficiency improvement | 2x | Neighbor card ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |

### Horizontal Comparison

In VLA model selection, the core differences among candidate schemes manifest in two dimensions: "architecture design" and "data requirements":

- **OpenVLA**: Extends action heads based on pre-trained vision-language models (such as Prismatic), using discretized action token prediction. Its strength is strong language understanding; its weakness is that action precision is limited by discretization granularity.
- **RT-X (Open X-Embodiment)**: Emphasizes large-scale cross-embodiment, cross-task data pre-training, achieving multi-robot transfer through unified action spaces. Its strength is strong generalization; its weakness is that fine-tuning requires adapting to the robot's action space definition.
- **π0 (π-zero)**: Uses flow matching to generate continuous action sequences, suitable for high-precision manipulation. Its strength is smooth action generation; its weakness is that training stability is sensitive to hyperparameters.
- **Diffusion Policy**: Models action generation as a denoising diffusion process, excelling at multimodal action distribution modeling. Its strength is expressing multiple feasible policies; its weakness is higher inference latency.
- **Self-developed models**: Customized for the specific robot platform, capable of deeply coupling kinematic constraints, but with the longest development cycles and greatest data requirements.

The key to model selection is not "which model is strongest," but rather "which model can achieve the KPIs at the lowest cost given the compute, data volume, and safety constraints." For example, under an 8GB VRAM limit, a 3.1B-parameter VLA model requires LoRA and quantization techniques to run (inferred from corpus data), in which case the parameter count of Diffusion Policy-type models may become a bottleneck.

### Who Uses It: Application Cases

**Case 1: Low-Cost Fine-Tuning on Resource-Constrained Platforms (from ent_paper_omaisan_towards_accessible_physical_ai_2025)**

The research team executed a button-pressing task on a SO101 robotic arm, using 200 demonstration trajectories to fine-tune a 3.1B-parameter VLA model with LoRA, combined with quantization to compress VRAM requirements to 8GB. Key findings were: freezing the vision encoder performed better with low data volumes, avoiding overfitting; primary failure modes included visual feature drift, action sequence deviations, and end-effector positioning errors. This case demonstrates that "consumer-grade GPU + small demonstration datasets" can accomplish effective VLA fine-tuning.

**Case 2: Human-Robot Collaborative Dual-Agent Fine-Tuning (from ent_paper_jin_dual_actor_fine_tuning_of_vla_2025)**

This study proposed a collaborative framework between a primary agent (responsible for robust multi-task execution) and a fine-tuning agent (responsible for latent space adaptive adjustments), introducing a "dialogue-adjustment" mechanism that converts human physical interventions into semantic instructions. In real multi-task experiments, after 101 minutes of online fine-tuning, three basic tasks (grasping, placing, stacking) all achieved 100% success rates, while the standard supervised fine-tuning (SFT) baseline averaged only 67%; long-horizon tasks (12 consecutive manipulations) maintained a 50% success rate, significantly outperforming the pure reinforcement learning (RL) method's 12% success rate. Dual-robot collaborative training achieved a 2x efficiency improvement.

**Case 3: Cross-Platform Data Collection and Policy Deployment (from ent_paper_nian_control_your_robot_a_unified_s_2025)**

The Control Your Robot framework supports both teleoperation and trajectory playback control modes through standardized workflows (modular design, unified API, closed-loop architecture), enabling seamless integration from multimodal data collection to inference. Experiments on single-arm and dual-arm systems showed that policies trained on data collected via this framework closely matched expert demonstrations, providing high-quality data infrastructure for VLA fine-tuning.

### Limitations and Boundaries

1. **Diminishing returns of data volume vs. performance**: Training data volume is positively correlated with real-world performance, but with diminishing marginal returns (from ent_paper_omaisan_towards_accessible_physical_ai_2025). Blindly increasing data volume does not always linearly improve success rates.
2. **Trade-off of freezing the vision encoder**: Freezing the vision encoder performs better with low data volumes but may limit the model's ability to adapt to novel visual features; fine-tuning the vision encoder requires more data and compute.
3. **Stability bottleneck in long-horizon tasks**: Even after fine-tuning, success rates for long-horizon tasks (e.g., 12 consecutive manipulations) remain significantly lower than single-step tasks, with error accumulation issues not yet fundamentally resolved.
4. **Residual sim-to-real gap**: Passing simulation validation does not equal successful real-world deployment; visual feature drift and dynamics differences still require iterative tuning on the real robot.
5. **Hard boundary of compute constraints**: 8GB VRAM is a typical lower bound for current low-cost deployment, but fine-tuning and inference for larger models (e.g., 7B and above) still require higher-spec hardware.

### Common Misconceptions

1. **"Model selection means picking the strongest model"**—Wrong. The essence of model selection is "finding the optimal solution under constraints"; compute, data volume, and safety boundaries are all hard constraints. A 3.1B model can run on 8GB VRAM, but a 7B model may fail even to load.
2. **"Fine-tuning means full parameter retraining"**—Wrong. Parameter-efficient fine-tuning methods like LoRA only update low-rank matrices, drastically reducing VRAM usage and training time, and perform better with small data volumes.
3. **"Passing simulation validation means it works in reality"**—Wrong. Simulation environments cannot fully replicate real-world factors such as visual illumination changes, joint friction, and cable drag; a tuning cycle on the real robot must be reserved.
4. **"Success rate is the only KPI"**—Wrong. Latency, stability, and scalability are equally important. A model with 95% success rate but 500ms inference latency (example value, engineering judgment) may be completely unusable in real-time manipulation scenarios.
5. **"Documentation output is formalism"**—Wrong. The documentation deliverables in P12.2.1.5 (reports, ICD/BOM/SOP updates, requirements traceability chains) are key to ensuring the model is maintainable, reproducible, and auditable; models lacking documentation cannot be iterated upon in subsequent stages.

### Related Knowledge

- `ent_process_p12` — The parent work package to which this process belongs, defining the overall process framework and five-step method for VLA / WAM / AI algorithm integration.
- `ent_paper_omaisan_towards_accessible_physical_ai_2025` — Provides a complete methodology for fine-tuning a 3.1B VLA model on 8GB VRAM using LoRA + quantization, serving as a selection reference for resource-constrained scenarios.
- `ent_paper_jin_dual_actor_fine_tuning_of_vla_2025` — Demonstrates a human-robot collaborative dual-agent fine-tuning framework, providing an actionable path for "human correction instructions → semantic training data."
- `ent_paper_nian_control_your_robot_a_unified_s_2025` — Provides a standardized workflow for cross-platform data collection and policy deployment, serving as a tool reference for the input organization stage of P12.2.1.1.
- `ent_method_openvla` — One of the representative methods in the candidate model pool, whose discretized action token prediction mechanism contrasts with Diffusion Policy's continuous action generation.
- `ent_paper_collaboration_open_x_embodiment_robotic_lear_2023` — Corresponds to the RT-X method, emphasizing cross-embodiment pre-training and action space unification, serving as a key reference for the "generalization" dimension in model selection.

## 개요

VLA 모델 선정 및 미세 조정(P12.2.1)은 휴머노이드 로봇 제품 개발 전체 프로세스 중 'VLA 및 조작 전략' 작업 패키지의 첫 번째 3차 하위 작업으로, 방법(method) 유형에 속하며, 소속 도메인은 `07_ai_models_algorithms`, 가치 사슬 계층은 `intelligence`입니다. 이 공정은 OpenVLA, RT-X, π0, Diffusion Policy 및 자체 개발 모델을 후보 방법 풀로 사용하며, 핵심 목표는 시각-언어 명령을 로봇 동작에 매핑하고 본체에 맞춰 미세 조정하는 것입니다. 이 공정이 실제로 바꾸는 것은 단일 모델의 정확도 상한이 아니라, '데모를 실행할 수 있는 모델'을 '정량적 검수가 가능하고 추적 가능한 납품이 가능한 엔지니어링 모듈'로 만드는 것입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

VLA(Vision-Language-Action) 모델 선정 및 미세 조정은 휴머노이드 로봇 조작 전략 개발에서 후보 모델 풀에서 본체 하드웨어 및 작업 요구사항에 적합한 시각-언어-동작 모델을 선별하고, 데이터 수집, 시뮬레이션 검증 및 파라미터 튜닝을 통해 자연어 명령을 실행할 수 있는 엔드투엔드 조작 능력을 갖추게 하는 것을 의미합니다. 이 공정의 완료 기준은 '자연어 명령을 실행할 수 있는 엔드투엔드 데모'를 납품하고 사전 설정된 성공률 지표에 도달하는 것입니다.

WBS V3의 공정 분해에서 P12.2.1은 5개의 4차 핵심 동작 그룹으로 전개됩니다: 입력 정리 및 목표 정량화(P12.2.1.1), 알고리즘/제어 방안 설계(P12.2.1.2), 알고리즘 구현 및 시뮬레이션 검증(P12.2.1.3), 알고리즘 튜닝 및 성능 검증(P12.2.1.4), 문서 출력 및 하류 납품(P12.2.1.5). 각 동작 그룹은 '입력→방안→구현→검증→납품'의 표준화된 엔지니어링 폐루프를 따릅니다.

### 왜 존재하는가:痛点 및 역사적 위치

VLA 모델이 로봇 분야에 진입하기 전까지, 조작 전략 개발은 오랫동안 두 가지 경로로 분열되어 있었습니다: 전통적인 운동 계획 방법은 정밀한 환경 모델링과 수작업으로 설계된 보상 함수에 의존하여 일반화 능력이 약했고, 엔드투엔드 모방 학습 방법은 시연 데이터를 피팅할 수 있었지만 자연어 명령을 이해하지 못했으며 하드웨어 차이에 극도로 민감했습니다. VLA 모델은 '시각-언어 공동 표현'으로 이 두 경로를 통합하려 시도했지만, 새로운 엔지니어링痛点이 뒤따랐습니다:

- **모델 규모와 연산 능력의 모순**: 주류 VLA 모델의 파라미터 수는 수십억 개에 달하는 반면, 휴머노이드 로봇 본체는 종종 소비자용 GPU만 장착되어 있습니다. 제한된 연산 능력에서 미세 조정과 추론을 완료하는 방법이 선정의 첫 번째 관문입니다.
- **sim-to-real 격차**: 시뮬레이션 환경에서 훈련된 전략을 실제 본체에 전이할 때, 시각적 특징 드리프트, 역학 차이 등의 요인으로 성능이 급격히 저하됩니다.
- **데이터 효율성 병목**: 실제 로봇 시연 데이터 수집 비용은 극히 높으며, 단일 궤적의 라벨링과 정제는 기존 CV 데이터셋보다 훨씬 많은 시간이 소요됩니다.
- **안전성 제약**: 휴머노이드 로봇의 조작 대상에는 인간이나 깨지기 쉬운 물체가 포함될 수 있으며, 모델이 출력하는 동작은 관절 한계, 토크 제약 등의 하드 안전 경계를 충족해야 합니다.

P12.2.1의 프로세스 내 역사적 위치는 바로 '모델 능력'과 '엔지니어링 납품 가능성' 사이에 정량화 가능한 다리를 구축하는 것입니다—새로운 모델을 발명하는 것이 아니라, 기존 모델을 본체 제약에 부합하고 검수 가능하며 추적 가능한 제품 구성 요소로 '길들이는' 역할을 담당합니다.

### 원리 분해

**① 입력 정리 및 목표 정량화(P12.2.1.1)**

이 동작 그룹의 본질은 '모호한 완료 기준을 측정 가능한 KPI로 번역하는 것'입니다. 상류 입력에는: 인식 스택이 출력하는 시각적 특징 형식, 로봇 팔/다섯 손가락 로봇 손의 운동학 파라미터, 작업 시나리오의 자연어 명령 집합, 연산 능력 자원 목록이 포함됩니다. 핵심 동작은 세 가지입니다:

1. 모든 상류 입력 목록을 나열하고 버전을 확인—센서 펌웨어 또는 인터페이스 버전 불일치로 인한 후속 미세 조정 실패를 방지;
2. 검수 기준을 정량화 가능한 KPI로 변환—예: '성공률 ≥ 90%(50회 테스트 기준)', '단일 추론 지연 ≤ 100ms'(모두 예시 값, 엔지니어링 판단);
3. 작업 Owner, 시간 노드 및 위험 등록을 수립—책임자와 마일스톤을 명확히 하고, 위험(데이터 부족, 연산 능력 초과 등)을 사전에 등록.

**② 알고리즘/제어 방안 설계(P12.2.1.2)**

이 동작 그룹은 2개 이상의 후보 방안을 구성하고 평가 매트릭스를 구축하여 정량적 점수를 매기도록 요구합니다. 후보 방안은 일반적으로 OpenVLA, RT-X, π0, Diffusion Policy 및 자체 개발 모델의 조합에서 비롯됩니다. 평가 차원은 최소한 다음을 포함합니다:

- **안정성**: 동일한 입력에서 출력 동작의 분산;
- **실시간성**: 시각 입력에서 동작 명령까지의 엔드투엔드 지연;
- **확장성**: 재훈련 없이 새로운 작업 명령에 적응할 수 있는지 여부.

평가 매트릭스의 정량적 점수는 논문 보고 값에만 의존하지 않고 본체의 실측 데이터(관절 응답 대역폭, 시각 프레임 속도 등)를 결합해야 합니다. 최종적으로 조직 검토를 통해 구현 경로를 확정합니다.

**③ 알고리즘 구현 및 시뮬레이션 검증(P12.2.1.3)**

시뮬레이션 환경 또는 오프라인 데이터에서 알고리즘을 구현하여 기능 정확성, 실시간성 및 견고성을 검증합니다. 핵심 동작에는: 모델/프로토타입 구축 및 핵심 파라미터 기록(학습률, batch size, LoRA rank 등), 시뮬레이션 또는 프로토타입 검증 실행, 이상 및 편차 기록이 포함됩니다. 이 단계의 핵심 가치는 '저비용 시행착오'에 있습니다—시뮬레이션에서 드러난 문제(동작 시퀀스 편차, 엔드 이펙터 위치 오차 등)는 실제 로봇 수명을 소모하지 않고 수정할 수 있습니다.

**④ 알고리즘 튜닝 및 성능 검증(P12.2.1.4)**

알고리즘의 파라미터 튜닝과 경계 테스트를 수행하여 일반/극한 조건에서 성능이 지표를 충족하는지 검증합니다. 핵심 동작에는: 테스트/검토 계획 및 통과 기준 수립, 테스트 실행 및 원시 데이터 기록, 문제 목록 및 개선 조치 출력이 포함됩니다. 경계 테스트는 다음을 포함해야 합니다: 조명 급변, 폐색, 목표 물체 색상과 훈련 분포 편차, 관절 한계 근접 등의 시나리오.

**⑤ 문서 출력 및 하류 납품(P12.2.1.5)**

최종 보고서/도면/규격을 출력하고, ICD/BOM/SOP/요구사항 추적 체인을 업데이트하며, 하류 단계로의 공식 납품을 완료합니다. 핵심 동작에는: 템플릿에 따라 문서 작성 및 원시 데이터 인용, 내부 검토 및 버전 관리 완료, 게시 및 하류 의존자 통지가 포함됩니다. 이 단계는 '모델 미세 조정'이라는 지적 활동이 개인 경험 수준에 머무르지 않고 조직 자산으로 고정되도록 보장합니다.

### 핵심 파라미터 및 사양

| 파라미터/사양 | 일반 값/범위 | 출처 설명 |
|---|---|---|
| 후보 방안 수 하한 | 2개 이상 | 말뭉치 P12.2.1.2 |
| 미세 조정 방법 | LoRA(저랭크 적응) | 이웃 카드 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 모델 파라미터 수 예시 | 31억 파라미터(3.1B) | 이웃 카드 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 최소 VRAM 요구사항 | 8GB VRAM | 이웃 카드 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 시연 데이터량 예시 | 200개 시연 궤적 | 이웃 카드 ent_paper_omaisan_towards_accessible_physical_ai_2025 |
| 온라인 미세 조정 시간 예시 | 101분 | 이웃 카드 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 다중 작업 성공률 예시 | 100%(3가지 기본 작업) | 이웃 카드 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 장시간 작업 성공률 예시 | 50%(연속 12회 조작) | 이웃 카드 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |
| 이중 로봇 협업 효율 향상 | 2배 | 이웃 카드 ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 |

### 횡적 비교

VLA 모델 선정에서 서로 다른 후보 방안의 핵심 차이는 '아키텍처 설계'와 '데이터 요구사항' 두 차원에서 나타납니다:

- **OpenVLA**: 사전 훈련된 시각-언어 모델(예: Prismatic)을 기반으로 동작 헤드를 확장하고, 이산화된 동작 토큰 예측을 채택합니다. 장점은 언어 이해 능력이 강하고, 단점은 동작 정밀도가 이산화 세분성에 제한됩니다.
- **RT-X(Open X-Embodiment)**: 크로스 본체, 크로스 태스크 대규모 데이터 사전 훈련을 강조하며, 통합 동작 공간을 통해 다중 로봇 전이를 실현합니다. 장점은 일반화 능력이 강하고, 단점은 미세 조정 시 본체의 동작 공간 정의를 적응시켜야 합니다.
- **π0(π-zero)**: 흐름 매칭(flow matching)을 사용하여 연속 동작 시퀀스를 생성하며, 고정밀 조작에 적합합니다. 장점은 동작 평활성이 좋고, 단점은 훈련 안정성이 하이퍼파라미터에 민감합니다.
- **Diffusion Policy**: 동작 생성을 노이즈 제거 확산 과정으로 모델링하며, 다중 모드 동작 분포 모델링에 능합니다. 장점은 여러 실행 가능한 전략을 표현할 수 있고, 단점은 추론 지연이 높습니다.
- **자체 개발 모델**: 본체에 맞춰 맞춤 제작되어 운동학 제약을 깊이 결합할 수 있지만, 개발 주기와 데이터 요구사항이 가장 큽니다.

선정 결정의 핵심은 '어떤 모델이 가장 강한가'가 아니라 '주어진 연산 능력, 데이터량 및 안전 제약에서 어떤 모델이 가장 낮은 비용으로 KPI에 도달할 수 있는가'입니다. 예를 들어, 8GB VRAM 제한에서 3.1B 파라미터 VLA 모델은 LoRA 및 양자화 기술과 함께 실행되어야 하며(말뭉치 데이터 기준 추산), 이때 Diffusion Policy 계열 모델의 파라미터 수가 병목이 될 수 있습니다.

### 누가 사용하는가·응용 사례

**사례 1: 리소스 제한 플랫폼의 저비용 미세 조정(ent_paper_omaisan_towards_accessible_physical_ai_2025 출처)**

연구팀은 SO101 로봇 팔에서 버튼 누름 작업을 수행하고, 200개의 시연 데이터로 3.1B 파라미터 VLA 모델을 LoRA 미세 조정하고 양자화 기술을 결합하여 VRAM 요구사항을 8GB로 압축했습니다. 핵심 발견은: 시각 인코더 동결 전략이 낮은 데이터량에서 더 우수한 성능을 보여 과적합을 피할 수 있고, 주요 실패 모드는 시각적 특징 드리프트, 동작 시퀀스 편차 및 엔드 이펙터 위치 오차를 포함합니다. 이 사례는 '소비자용 GPU + 소량의 시연 데이터'로도 효과적인 VLA 미세 조정이 가능함을 증명했습니다.

**사례 2: 인간-로봇 협업 이중 에이전트 미세 조정(ent_paper_jin_dual_actor_fine_tuning_of_vla_2025 출처)**

이 연구는 주 에이전트(다중 작업 견고한 실행 담당)와 정밀 조정 에이전트(잠재 공간 적응 조정 담당)의 협업 프레임워크를 제안하고, '대화-조정' 메커니즘을 도입하여 인간의 물리적 개입을 의미론적 명령으로 변환합니다. 실제 다중 작업 실험에서 101분 온라인 미세 조정 후 세 가지 기본 작업(잡기, 놓기, 쌓기) 모두 100% 성공률에 도달했으며, 표준 지도 미세 조정(SFT) 기준선의 평균 성공률은 67%에 불과했습니다; 장시간 작업(연속 12회 조작)은 50% 성공률을 유지하여 순수 강화 학습(RL) 방법의 12% 성공률보다 크게 우수했습니다. 이중 로봇 협업 훈련 시 효율 향상은 2배에 달했습니다.

**사례 3: 크로스 플랫폼 데이터 수집 및 전략 배포(ent_paper_nian_control_your_robot_a_unified_s_2025 출처)**

Control Your Robot 프레임워크는 표준화된 워크플로우(모듈식 설계, 통합 API, 폐루프 아키텍처)를 통해 원격 조작과 궤적 재생 이중 모드 제어를 지원하여 다중 모드 데이터 수집부터 추론까지의 원활한 통합을 실현합니다. 단일 팔 및 이중 팔 시스템 실험에서 이 프레임워크 기반으로 수집된 데이터로 훈련된 전략이 전문가 시연과 높은 일치도를 보여, VLA 미세 조정을 위한 고품질 데이터 인프라를 제공합니다.

### 한계 및 경계

1. **데이터량-성능의 한계 체감**: 훈련 데이터량과 실제 세계 성능은 양의 상관관계를 가지지만 한계 체감 효과가 존재합니다(ent_paper_omaisan_towards_accessible_physical_ai_2025 출처). 무분별한 데이터량 증가가 항상 성공률을 선형적으로 향상시키지는 않습니다.
2. **시각 인코더 동결의 트레이드오프**: 시각 인코더 동결은 낮은 데이터량에서 더 우수한 성능을 보이지만, 새로운 시각적 특징에 대한 적응 능력을 제한할 수 있습니다; 시각 인코더 미세 조정은 더 많은 데이터와 연산 능력이 필요합니다.
3. **장시간 작업의 안정성 병목**: 미세 조정 후에도 장시간 작업(연속 12회 조작 등)의 성공률은 단일 단계 작업보다 현저히 낮으며, 오차 누적 문제는 아직 근본적으로 해결되지 않았습니다.
4. **sim-to-real의 잔여 격차**: 시뮬레이션 검증 통과가 실제 배포 성공을 의미하지 않으며, 시각적 특징 드리프트와 역학 차이는 실제 본체에서 반복 튜닝이 필요합니다.
5. **연산 능력 제약의 하드 경계**: 8GB VRAM은 현재 저비용 배포의 일반적인 하한이지만, 더 큰 모델(7B 이상)의 미세 조정과 추론은 여전히 더 높은 사양의 하드웨어가 필요합니다.

### 일반적인 오해

1. **'선정은 가장 강한 모델을 고르는 것'**—틀림. 선정의 본질은 '제약 조건에서 최적해를 찾는 것'이며, 연산 능력, 데이터량, 안전 경계 모두 하드 제약입니다. 3.1B 모델은 8GB VRAM에서 실행할 수 있지만, 7B 모델은 로드조차 실패할 수 있습니다.
2. **'미세 조정은 전체 파라미터 재훈련'**—틀림. LoRA와 같은 파라미터 효율적 미세 조정 방법은 저랭크 행렬만 업데이트하여 VRAM 점유와 훈련 시간을 크게 줄이고, 소량 데이터에서 더 우수한 성능을 보입니다.
3. **'시뮬레이션 검증 통과 = 실제 사용 가능'**—틀림. 시뮬레이션 환경은 시각적 조명 변화, 관절 마찰, 케이블 드래깅 등의 실제 요인을 완전히 모사할 수 없으므로, 실제 본체 튜닝 기간을 반드시 예약해야 합니다.
4. **'성공률이 유일한 KPI'**—틀림. 지연, 안정성, 확장성도 동일하게 중요합니다. 성공률 95%지만 추론 지연 500ms인 모델(예시 값, 엔지니어링 판단)은 실시간 조작 시나리오에서 완전히 사용 불가능할 수 있습니다.
5. **'문서 출력은 형식주의'**—틀림. P12.2.1.5의 문서 납품(보고서, ICD/BOM/SOP 업데이트, 요구사항 추적 체인)은 모델의 유지보수성, 재현성, 감사 가능성을 보장하는 핵심이며, 문서가 없는 모델은 후속 단계에서 반복할 수 없습니다.

### 관련 지식

- `ent_process_p12` — 본 공정이 속한 상위 작업 패키지로, VLA / WAM / AI 알고리즘 통합의 전체 공정 프레임워크와 5단계 방법을 정의합니다.
- `ent_paper_omaisan_towards_accessible_physical_ai_2025` — 8GB VRAM에서 3.1B VLA 모델을 미세 조정하는 LoRA + 양자화의 완전한 방법을 제공하며, 리소스 제한 시나리오의 선정 참고 자료입니다.
- `ent_paper_jin_dual_actor_fine_tuning_of_vla_2025` — 인간-로봇 협업 이중 에이전트 미세 조정 프레임워크를 보여주며, '인간 수정 명령 → 의미론적 훈련 데이터'의 실행 가능한 경로를 제공합니다.
- `ent_paper_nian_control_your_robot_a_unified_s_2025` — 크로스 플랫폼 데이터 수집 및 전략 배포의 표준화된 워크플로우를 제공하며, P12.2.1.1 입력 정리 단계의 도구 참고 자료입니다.
- `ent_method_openvla` — 후보 모델 풀의 대표 방법 중 하나로, 이산화 동작 토큰 예측 메커니즘이 Diffusion Policy의 연속 동작 생성과 대비를 이룹니다.
- `ent_paper_collaboration_open_x_embodiment_robotic_lear_2023` — RT-X 방법에 해당하며, 크로스 본체 사전 훈련과 동작 공간 통일을 강조하여, 선정 시 '일반화' 차원의 핵심 참고 자료입니다.
