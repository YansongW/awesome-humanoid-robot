---
$id: ent_process_p6_1_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: URDF/SDF file generation
  zh: URDF/SDF 文件生成
  ko: URDF/SDF 파일 생성
summary:
  en: It loads correctly in RViz/Gazebo/Isaac Sim with no parsing errors
  zh: 方法 / 工具：ROS URDF、Xacro、SolidWorks/NX → URDF 插件、惯性参数校核
  ko: 可在 RViz/Gazebo/Isaac Sim 中正确加载、无解析错误
domains:
- 06_design_engineering
- 08_software_middleware
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

URDF/SDF 文件生成是人形机器人开发流程 P6 阶段（URDF 建模与运动学校核）的首个三级子任务，其核心工作是将 CAD 设计数据转化为可被仿真、控制与规划工具链正确解析的标准化机器人描述文件。它真正改变的不是"导出文件"这个动作本身，而是让"设计意图"与"算法可执行性"之间第一次建立起可量化、可追溯的工程桥梁。

## 核心内容

### 是什么：从 CAD 到可计算模型的工程翻译

URDF（Unified Robot Description Format）是 ROS 生态中描述机器人运动学与动力学属性的 XML 格式，通过 `<link>`（连杆）与 `<joint>`（关节）的树状结构定义机器人的拓扑连接关系、关节类型（revolute/prismatic/fixed）、运动学参数（原点、轴、极限）以及惯性属性。SDF（Simulation Description Format）则是 Gazebo 仿真环境原生支持的格式，在 URDF 基础上扩展了物理属性（摩擦系数、阻尼、材质）与传感器模型描述。

本工序节点 P6.1.1 的具体任务，是使用 ROS URDF、Xacro、SolidWorks/NX → URDF 插件、惯性参数校核等方法工具，完成从 CAD 模型到 URDF/SDF 文件的转换与验证。其设计思考逻辑要求：连杆、关节、惯性、碰撞体、视觉模型需与 CAD 完全一致；质量属性来自 P3.1.2/P3.1.3。关键约束包括坐标系一致性、单位统一、碰撞体不能太密。完成标准是可在 RViz/Gazebo/Isaac Sim 中正确加载、无解析错误。

### 为什么存在：仿真与实机之间的"翻译误差"痛点

人形机器人是典型的"高自由度、强耦合、多约束"系统。若直接将 CAD 模型导入仿真环境，会面临三个致命问题：第一，CAD 模型包含大量装饰性几何（圆角、螺纹、倒角），直接用于碰撞检测会导致计算量爆炸；第二，CAD 装配体中的坐标系定义往往基于设计便利，而非运动学约定（如 D-H 参数或 ROS REP 103 规定的右手定则），导致关节轴方向混乱；第三，CAD 软件导出的惯性参数（质量、质心、惯性张量）默认基于零件坐标系，而 URDF 要求基于连杆坐标系，且单位制可能不一致（kg·m² vs g·mm²）。

URDF/SDF 文件生成工序的存在，正是为了消除这三层"翻译误差"。它真正改变的不是"画一个机器人模型"这件事本身，而是让"设计意图"与"算法可执行性"之间第一次建立起可量化、可追溯的工程桥梁。没有这一步，后续的运动学验证、动力学仿真、控制算法开发都将建立在错误的模型之上，导致"仿真能跑、实机就炸"的经典工程灾难。

### 原理拆解

**① 树状拓扑：URDF 的数学本质**

URDF 采用树状结构描述机器人，每个 `<joint>` 连接一个父 `<link>` 与一个子 `<link>`。这种结构对应运动学中的传递链：

$$
T_{\text{child}}^{\text{parent}} = T_{\text{joint\_origin}} \cdot T_{\text{joint\_axis}}(\theta)
$$

其中 $T_{\text{joint\_origin}}$ 是关节坐标系在父连杆坐标系中的固定变换（由 `<origin>` 标签定义），$T_{\text{joint\_axis}}(\theta)$ 是绕关节轴的旋转或沿关节轴的平移（由 `<axis>` 与 `<limit>` 标签定义）。整个机器人的正运动学就是沿树根到叶子的变换连乘。

**② Xacro：宏编程消除重复**

人形机器人左右对称，若手写 URDF，双腿、双臂的连杆定义会重复数百行。Xacro（XML Macros）通过 `<xacro:macro>` 定义可复用的连杆模板，配合参数化（如 `leg_prefix`、`hip_yaw_offset`）实现"一次定义、两处实例化"。这不仅是代码复用，更是工程一致性的保障——修改左腿的某个参数，右腿自动同步，避免"左右不对称"的隐性错误。

**③ 惯性参数校核：动力学仿真的命门**

URDF 中的 `<inertial>` 标签包含质量（`mass`）、质心位置（`origin`）与惯性张量（`inertia` 的 6 个独立分量）。SolidWorks/NX 的 URDF 插件会自动导出这些参数，但存在两个常见陷阱：一是单位制——CAD 软件默认使用 g·mm²，而 URDF 要求 kg·m²，换算系数为 $10^{-9}$；二是坐标系——插件导出的惯性张量基于连杆原点，但若 CAD 装配体的原点定义与运动学约定不一致，需要手动旋转惯性张量：

$$
I' = R \, I \, R^T
$$

其中 $R$ 是从 CAD 坐标系到 URDF 连杆坐标系的旋转矩阵。这一步若出错，动力学仿真的力矩输出将与实机严重偏离。

### 关键参数与规格

| 参数/规格 | 要求 | 来源/说明 |
|---|---|---|
| 坐标系 | 遵循 ROS REP 103（右手定则，x 向前，z 向上） | 关键约束 |
| 单位 | 长度：米（m）；质量：千克（kg）；惯性：kg·m² | 关键约束 |
| 碰撞体 | 使用简化几何（box/cylinder/sphere），不能太密 | 关键约束 |
| 质量属性 | 来自 P3.1.2/P3.1.3 的惯性参数校核结果 | 设计思考逻辑 |
| 视觉模型 | 与 CAD 完全一致（可包含装饰性几何） | 设计思考逻辑 |
| 解析验证 | RViz/Gazebo/Isaac Sim 中无解析错误 | 完成标准 |

### 横向对比

| 方法 | 优势 | 劣势 | 适用场景 |
|---|---|---|---|
| SolidWorks → URDF 插件 | 自动化程度高，惯性参数直接导出 | 坐标系需手动调整，碰撞体需手动简化 | 机械设计已完成、需快速生成模型 |
| NX → URDF 插件 | 与 NX 装配体关联性强，支持批量导出 | 插件成熟度不如 SolidWorks 版本 | 使用 NX 作为主力 CAD 工具 |
| 手写 URDF + Xacro | 完全可控，可精确调整每个参数 | 工作量大，易出错 | 模型简单或需精细调参 |
| 第三方工具（如 Fusion 360 导出） | 云端协作方便 | 惯性参数精度需校核 | 分布式团队协作 |

### 谁在用·应用案例

**案例一：全尺寸双足人形机器人开发（本流程）**

在 P6.1.1 工序中，团队从 P3.1.2/P3.1.3 获取质量属性数据，使用 SolidWorks URDF 插件导出初始模型，再通过 Xacro 重构左右对称的腿部连杆，最后在 RViz 中加载验证。关键动作包括：列出所有上游输入清单并确认版本、将验收标准转化为可量化 KPI、建立任务 Owner 与时间节点与风险登记。

**案例二：开源人形机器人项目**

许多开源项目（如 awesome-humanoid-robot 仓库中收录的项目）将 URDF 文件作为标准交付物，供全球开发者下载后在 Gazebo 中仿真或在 RViz 中可视化。这些项目通常同时提供 URDF 与 SDF 两种格式，以满足 ROS 与 Gazebo 不同版本的需求。

### 局限与边界

**① URDF 的树状结构限制**：URDF 不允许闭环结构（如并联机构或闭链运动学），若人形机器人包含平行连杆机构，需改用 SDF 或其它格式。工程判断：大多数双足人形机器人采用串联结构，此限制影响有限。

**② 碰撞体简化与真实性的矛盾**：碰撞体不能太密（否则计算量大），但过于简化会导致仿真中的碰撞检测失真（如手指抓取）。需在精度与性能间权衡，通常对末端执行器使用较精细的碰撞体，对躯干使用简化几何。

**③ 惯性参数的静态假设**：URDF 中的惯性参数是固定值，无法描述负载变化（如机器人手持重物）。对于动态负载场景，需在控制层进行在线补偿，而非修改 URDF。

### 常见误区

1. **"URDF 导出后就能直接用"**——错。CAD 插件导出的 URDF 几乎必然需要手动调整：坐标系旋转、单位换算、碰撞体简化、关节极限校准。直接使用会导致仿真与实机严重偏离。

2. **"碰撞体越精细越好"**——错。碰撞体不能太密，否则仿真计算量爆炸，实时性无法保证。正确做法是：视觉模型保留细节，碰撞体使用简化几何，仅在关键交互部位（如手爪）提高精度。

3. **"惯性参数不重要，反正仿真看不出来"**——错。惯性参数直接决定动力学仿真的力矩输出，若质量属性错误，控制算法在仿真中调好的参数移植到实机必然失败。质量属性必须来自 P3.1.2/P3.1.3 的校核结果。

4. **"URDF 和 SDF 可以互相替代"**——部分错。URDF 是 ROS 的标准格式，SDF 是 Gazebo 的原生格式。URDF 可通过 `gz sdf` 工具转换为 SDF，但 SDF 支持 URDF 不具备的特性（如闭链、传感器模型）。需根据目标仿真环境选择格式。

### 相关知识

- `ent_process_p6` — 本工序节点所属的父阶段，URDF 建模与运动学校核的整体流程与验收标准在此定义。
- `ent_process_p6_1_2` — 同族相邻工序：DH / 修改 DH 参数定义（同一工作包内的并行工序）。
- `ent_process_p6_1_3` — 同族相邻工序：碰撞与自干涉模型（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [awesome-humanoid-robot 仓库主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

The generation of URDF/SDF files is the first third-level subtask in Stage P6 (URDF modeling and kinematic verification) of the humanoid robot development process. Its core work is to convert CAD design data into standardized robot description files that can be correctly parsed by simulation, control, and planning toolchains. What it truly changes is not the "export file" action itself, but rather the establishment of a quantifiable and traceable engineering bridge between "design intent" and "algorithm executability" for the first time.

## Content

### What It Is: Engineering Translation from CAD to Computable Models

URDF (Unified Robot Description Format) is an XML format in the ROS ecosystem that describes a robot's kinematic and dynamic properties. It defines the robot's topological connections, joint types (revolute/prismatic/fixed), kinematic parameters (origin, axis, limits), and inertial properties through a tree structure of `<link>` and `<joint>` elements. SDF (Simulation Description Format) is the native format supported by the Gazebo simulation environment, extending URDF with physical properties (friction coefficients, damping, materials) and sensor model descriptions.

The specific task of this process node P6.1.1 is to use methods and tools such as ROS URDF, Xacro, SolidWorks/NX → URDF plugins, and inertial parameter verification to complete the conversion and validation from CAD models to URDF/SDF files. Its design reasoning logic requires: links, joints, inertia, collision bodies, and visual models must be fully consistent with CAD; mass properties come from P3.1.2/P3.1.3. Key constraints include coordinate system consistency, unit uniformity, and collision bodies must not be too dense. The completion criterion is correct loading in RViz/Gazebo/Isaac Sim without parsing errors.

### Why It Exists: The "Translation Error" Pain Point Between Simulation and Real Hardware

Humanoid robots are typical "high-degree-of-freedom, strongly coupled, multi-constrained" systems. Directly importing CAD models into simulation environments presents three critical problems: First, CAD models contain a large amount of decorative geometry (fillets, threads, chamfers), and using them directly for collision detection causes computational explosion; second, coordinate systems in CAD assemblies are often defined for design convenience rather than kinematic conventions (such as D-H parameters or the right-hand rule specified by ROS REP 103), leading to confusion in joint axis directions; third, inertial parameters (mass, center of mass, inertia tensor) exported by CAD software default to the part coordinate system, while URDF requires the link coordinate system, and unit systems may be inconsistent (kg·m² vs g·mm²).

The existence of the URDF/SDF file generation process is precisely to eliminate these three layers of "translation error." What it truly changes is not the act of "drawing a robot model" itself, but rather establishing a quantifiable and traceable engineering bridge between "design intent" and "algorithm executability" for the first time. Without this step, subsequent kinematic verification, dynamic simulation, and control algorithm development would all be built on incorrect models, leading to the classic engineering disaster of "works in simulation, breaks on the real robot."

### Principle Breakdown

**① Tree Topology: The Mathematical Essence of URDF**

URDF uses a tree structure to describe the robot, with each `<joint>` connecting a parent `<link>` and a child `<link>`. This structure corresponds to the kinematic transmission chain:

$$
T_{\text{child}}^{\text{parent}} = T_{\text{joint\_origin}} \cdot T_{\text{joint\_axis}}(\theta)
$$

where $T_{\text{joint\_origin}}$ is the fixed transformation of the joint coordinate system in the parent link coordinate system (defined by the `<origin>` tag), and $T_{\text{joint\_axis}}(\theta)$ is the rotation about the joint axis or translation along the joint axis (defined by the `<axis>` and `<limit>` tags). The forward kinematics of the entire robot is the sequential multiplication of transformations from the tree root to the leaves.

**② Xacro: Macro Programming Eliminates Repetition**

Humanoid robots are left-right symmetric. If URDF were written by hand, the link definitions for the legs and arms would repeat for hundreds of lines. Xacro (XML Macros) defines reusable link templates through `<xacro:macro>`, combined with parameterization (such as `leg_prefix`, `hip_yaw_offset`) to achieve "define once, instantiate twice." This is not just code reuse but a guarantee of engineering consistency—modifying a parameter on the left leg automatically synchronizes the right leg, avoiding hidden "left-right asymmetry" errors.

**③ Inertial Parameter Verification: The Lifeline of Dynamic Simulation**

The `<inertial>` tag in URDF contains mass (`mass`), center of mass position (`origin`), and the inertia tensor (6 independent components of `inertia`). The SolidWorks/NX URDF plugins automatically export these parameters, but there are two common pitfalls: first, unit systems—CAD software defaults to g·mm², while URDF requires kg·m², with a conversion factor of $10^{-9}$; second, coordinate systems—the inertia tensor exported by the plugin is based on the link origin, but if the origin definition in the CAD assembly is inconsistent with kinematic conventions, the inertia tensor must be manually rotated:

$$
I' = R \, I \, R^T
$$

where $R$ is the rotation matrix from the CAD coordinate system to the URDF link coordinate system. If this step is incorrect, the torque output of dynamic simulation will deviate significantly from the real robot.

### Key Parameters and Specifications

| Parameter/Specification | Requirement | Source/Description |
|---|---|---|
| Coordinate system | Follow ROS REP 103 (right-hand rule, x forward, z upward) | Key constraint |
| Units | Length: meters (m); Mass: kilograms (kg); Inertia: kg·m² | Key constraint |
| Collision bodies | Use simplified geometry (box/cylinder/sphere), must not be too dense | Key constraint |
| Mass properties | From inertial parameter verification results of P3.1.2/P3.1.3 | Design reasoning logic |
| Visual model | Fully consistent with CAD (may include decorative geometry) | Design reasoning logic |
| Parsing validation | No parsing errors in RViz/Gazebo/Isaac Sim | Completion criterion |

### Horizontal Comparison

| Method | Advantages | Disadvantages | Applicable Scenarios |
|---|---|---|---|
| SolidWorks → URDF plugin | High automation, inertial parameters directly exported | Coordinate system requires manual adjustment, collision bodies require manual simplification | Mechanical design completed, rapid model generation needed |
| NX → URDF plugin | Strong association with NX assemblies, supports batch export | Plugin maturity lower than SolidWorks version | NX as the primary CAD tool |
| Handwritten URDF + Xacro | Fully controllable, precise adjustment of every parameter | High workload, error-prone | Simple models or fine-tuning required |
| Third-party tools (e.g., Fusion 360 export) | Convenient cloud collaboration | Inertial parameter accuracy requires verification | Distributed team collaboration |

### Who Uses It · Application Cases

**Case One: Full-Size Bipedal Humanoid Robot Development (This Process)**

In the P6.1.1 process, the team obtains mass property data from P3.1.2/P3.1.3, uses the SolidWorks URDF plugin to export the initial model, then refactors the left-right symmetric leg links using Xacro, and finally loads and validates in RViz. Key actions include: listing all upstream input inventories and confirming versions, converting acceptance criteria into quantifiable KPIs, and establishing task owners, timelines, and risk registers.

**Case Two: Open-Source Humanoid Robot Projects**

Many open-source projects (such as those collected in the awesome-humanoid-robot repository) deliver URDF files as standard artifacts for global developers to download and simulate in Gazebo or visualize in RViz. These projects typically provide both URDF and SDF formats to accommodate different versions of ROS and Gazebo.

### Limitations and Boundaries

**① URDF's Tree Structure Limitation**: URDF does not allow closed-loop structures (such as parallel mechanisms or closed-chain kinematics). If a humanoid robot includes parallel link mechanisms, SDF or other formats must be used instead. Engineering judgment: most bipedal humanoid robots use serial structures, so this limitation has limited impact.

**② The Contradiction Between Collision Body Simplification and Realism**: Collision bodies must not be too dense (otherwise computational load explodes), but excessive simplification leads to distorted collision detection in simulation (such as finger grasping). A trade-off between accuracy and performance is required; typically, finer collision bodies are used for end effectors and simplified geometry for the torso.

**③ Static Assumption of Inertial Parameters**: Inertial parameters in URDF are fixed values and cannot describe load variations (such as a robot holding a heavy object). For dynamic load scenarios, online compensation must be performed at the control layer rather than modifying the URDF.

### Common Misconceptions

1. **"URDF can be used directly after export"**—Wrong. URDF exported by CAD plugins almost inevitably requires manual adjustment: coordinate system rotation, unit conversion, collision body simplification, and joint limit calibration. Direct use will cause severe deviation between simulation and the real robot.

2. **"The finer the collision bodies, the better"**—Wrong. Collision bodies must not be too dense, otherwise simulation computational load explodes and real-time performance cannot be guaranteed. The correct approach is: preserve details in the visual model, use simplified geometry for collision bodies, and only increase precision at critical interaction points (such as grippers).

3. **"Inertial parameters don't matter since simulation can't tell the difference"**—Wrong. Inertial parameters directly determine the torque output of dynamic simulation. If mass properties are incorrect, control parameters tuned in simulation will inevitably fail when transferred to the real robot. Mass properties must come from the verification results of P3.1.2/P3.1.3.

4. **"URDF and SDF are interchangeable"**—Partially wrong. URDF is the standard format for ROS, while SDF is the native format for Gazebo. URDF can be converted to SDF using the `gz sdf` tool, but SDF supports features that URDF does not (such as closed chains and sensor models). The format should be selected based on the target simulation environment.

### Related Knowledge

- `ent_process_p6` — The parent stage to which this process node belongs; the overall workflow and acceptance criteria for URDF modeling and kinematic verification are defined here.
- `ent_process_p6_1_2` — Adjacent process in the same family: DH / modified DH parameter definition (parallel process within the same work package).
- `ent_process_p6_1_3` — Adjacent process in the same family: collision and self-interference model (parallel process within the same work package).

## 개요

URDF/SDF 파일 생성은 휴머노이드 로봇 개발 프로세스 P6 단계(URDF 모델링 및 운동학 검증)의 첫 번째 3차 하위 작업으로, 핵심 작업은 CAD 설계 데이터를 시뮬레이션, 제어 및 계획 도구 체인이 올바르게 해석할 수 있는 표준화된 로봇 설명 파일로 변환하는 것입니다. 이것이 진정으로 바꾸는 것은 "파일 내보내기"라는 행위 자체가 아니라, "설계 의도"와 "알고리즘 실행 가능성" 사이에 처음으로 정량화 가능하고 추적 가능한 엔지니어링 다리를 구축한다는 점입니다.

## 핵심 내용

### 무엇인가: CAD에서 계산 가능한 모델로의 엔지니어링 번역

URDF(Unified Robot Description Format)는 ROS 생태계에서 로봇의 운동학 및 동역학 속성을 설명하는 XML 형식으로, `<link>`(링크)와 `<joint>`(관절)의 트리 구조를 통해 로봇의 토폴로지 연결 관계, 관절 유형(revolute/prismatic/fixed), 운동학 파라미터(원점, 축, 한계) 및 관성 속성을 정의합니다. SDF(Simulation Description Format)는 Gazebo 시뮬레이션 환경이 기본 지원하는 형식으로, URDF 기반 위에 물리 속성(마찰 계수, 감쇠, 재질)과 센서 모델 설명을 확장합니다.

본 공정 노드 P6.1.1의 구체적 작업은 ROS URDF, Xacro, SolidWorks/NX → URDF 플러그인, 관성 파라미터 검증 등의 방법과 도구를 사용하여 CAD 모델에서 URDF/SDF 파일로의 변환 및 검증을 완료하는 것입니다. 설계 사고 논리 요구사항: 링크, 관절, 관성, 충돌체, 시각 모델이 CAD와 완전히 일치해야 함; 질량 속성은 P3.1.2/P3.1.3에서 가져옴. 핵심 제약 조건에는 좌표계 일관성, 단위 통일, 충돌체가 너무 조밀하지 않아야 함이 포함됩니다. 완료 기준은 RViz/Gazebo/Isaac Sim에서 올바르게 로드되고 파싱 오류가 없는 것입니다.

### 왜 존재하는가: 시뮬레이션과 실기 간의 "번역 오차" 문제점

휴머노이드 로봇은 전형적인 "고자유도, 강결합, 다중 제약" 시스템입니다. CAD 모델을 시뮬레이션 환경에 직접 가져오면 세 가지 치명적인 문제에 직면합니다: 첫째, CAD 모델에는 장식적 형상(라운드, 나사산, 모따기)이 많이 포함되어 있어 충돌 감지에 직접 사용하면 계산량이 폭발합니다; 둘째, CAD 어셈블리의 좌표계 정의는 종종 설계 편의에 기반하며 운동학적 규약(예: D-H 파라미터 또는 ROS REP 103이 규정한 오른손 법칙)이 아니므로 관절 축 방향이 혼란스러워집니다; 셋째, CAD 소프트웨어가 내보내는 관성 파라미터(질량, 질량 중심, 관성 텐서)는 기본적으로 부품 좌표계를 기준으로 하지만, URDF는 링크 좌표계를 기준으로 요구하며 단위계가 일치하지 않을 수 있습니다(kg·m² vs g·mm²).

URDF/SDF 파일 생성 공정이 존재하는 이유는 바로 이 세 가지 "번역 오차"를 제거하기 위해서입니다. 이것이 진정으로 바꾸는 것은 "로봇 모델을 그리는 것" 자체가 아니라, "설계 의도"와 "알고리즘 실행 가능성" 사이에 처음으로 정량화 가능하고 추적 가능한 엔지니어링 다리를 구축한다는 점입니다. 이 단계가 없으면 이후의 운동학 검증, 동역학 시뮬레이션, 제어 알고리즘 개발이 모두 잘못된 모델 위에 구축되어 "시뮬레이션은 돌아가는데 실기에서는 폭발하는" 전형적인 엔지니어링 재앙으로 이어집니다.

### 원리 분석

**① 트리 토폴로지: URDF의 수학적 본질**

URDF는 트리 구조로 로봇을 설명하며, 각 `<joint>`는 부모 `<link>`와 자식 `<link>`를 연결합니다. 이 구조는 운동학의 전달 체인에 해당합니다:

$$
T_{\text{child}}^{\text{parent}} = T_{\text{joint\_origin}} \cdot T_{\text{joint\_axis}}(\theta)
$$

여기서 $T_{\text{joint\_origin}}$은 부모 링크 좌표계에서의 관절 좌표계 고정 변환(`<origin>` 태그로 정의), $T_{\text{joint\_axis}}(\theta)$는 관절 축을 따른 회전 또는 병진 이동(`<axis>` 및 `<limit>` 태그로 정의)입니다. 로봇 전체의 정운동학은 트리 루트에서 리프까지의 변환 연쇄 곱입니다.

**② Xacro: 매크로 프로그래밍으로 중복 제거**

휴머노이드 로봇은 좌우 대칭이므로 URDF를 수동으로 작성하면 다리, 팔의 링크 정의가 수백 줄 반복됩니다. Xacro(XML Macros)는 `<xacro:macro>`로 재사용 가능한 링크 템플릿을 정의하고 파라미터화(예: `leg_prefix`, `hip_yaw_offset`)를 통해 "한 번 정의, 두 곳에서 인스턴스화"를 구현합니다. 이는 단순한 코드 재사용이 아니라 엔지니어링 일관성의 보장입니다—왼쪽 다리의 특정 파라미터를 수정하면 오른쪽 다리도 자동으로 동기화되어 "좌우 비대칭"의 잠재적 오류를 방지합니다.

**③ 관성 파라미터 검증: 동역학 시뮬레이션의 핵심**

URDF의 `<inertial>` 태그에는 질량(`mass`), 질량 중심 위치(`origin`) 및 관성 텐서(`inertia`의 6개 독립 성분)가 포함됩니다. SolidWorks/NX의 URDF 플러그인은 이러한 파라미터를 자동으로 내보내지만 두 가지 일반적인 함정이 있습니다: 첫째, 단위계—CAD 소프트웨어는 기본적으로 g·mm²를 사용하지만 URDF는 kg·m²를 요구하며 환산 계수는 $10^{-9}$입니다; 둘째, 좌표계—플러그인이 내보내는 관성 텐서는 링크 원점을 기준으로 하지만 CAD 어셈블리의 원점 정의가 운동학적 규약과 일치하지 않으면 관성 텐서를 수동으로 회전해야 합니다:

$$
I' = R \, I \, R^T
$$

여기서 $R$은 CAD 좌표계에서 URDF 링크 좌표계로의 회전 행렬입니다. 이 단계에서 오류가 발생하면 동역학 시뮬레이션의 토크 출력이 실기와 심각하게偏离됩니다.

### 핵심 파라미터 및 사양

| 파라미터/사양 | 요구사항 | 출처/설명 |
|---|---|---|
| 좌표계 | ROS REP 103 준수(오른손 법칙, x 전방, z 상방) | 핵심 제약 |
| 단위 | 길이: 미터(m); 질량: 킬로그램(kg); 관성: kg·m² | 핵심 제약 |
| 충돌체 | 단순 형상(box/cylinder/sphere) 사용, 너무 조밀하지 않아야 함 | 핵심 제약 |
| 질량 속성 | P3.1.2/P3.1.3의 관성 파라미터 검증 결과에서 가져옴 | 설계 사고 논리 |
| 시각 모델 | CAD와 완전히 일치(장식적 형상 포함 가능) | 설계 사고 논리 |
| 파싱 검증 | RViz/Gazebo/Isaac Sim에서 파싱 오류 없음 | 완료 기준 |

### 횡적 비교

| 방법 | 장점 | 단점 | 적용 시나리오 |
|---|---|---|---|
| SolidWorks → URDF 플러그인 | 자동화 수준 높음, 관성 파라미터 직접 내보내기 | 좌표계 수동 조정 필요, 충돌체 수동 단순화 필요 | 기계 설계 완료, 빠른 모델 생성 필요 |
| NX → URDF 플러그인 | NX 어셈블리와 연관성 강함, 배치 내보내기 지원 | 플러그인 성숙도가 SolidWorks 버전보다 낮음 | NX를 주력 CAD 도구로 사용 |
| 수동 URDF + Xacro | 완전 제어 가능, 각 파라미터 정밀 조정 가능 | 작업량 큼, 오류 발생 쉬움 | 모델이 단순하거나 정밀 튜닝 필요 |
| 타사 도구(예: Fusion 360 내보내기) | 클라우드 협업 편리 | 관성 파라미터 정밀도 검증 필요 | 분산 팀 협업 |

### 사용 사례·적용 사례

**사례 1: 전신 이족 휴머노이드 로봇 개발(본 프로세스)**

P6.1.1 공정에서 팀은 P3.1.2/P3.1.3에서 질량 속성 데이터를 획득하고 SolidWorks URDF 플러그인으로 초기 모델을 내보낸 후 Xacro로 좌우 대칭 다리 링크를 재구성하고 마지막으로 RViz에서 로드하여 검증합니다. 핵심 작업에는 모든 상위 입력 목록 작성 및 버전 확인, 수용 기준을 정량화 가능한 KPI로 변환, 작업 Owner 및 시간 노드와 위험 등록부 설정이 포함됩니다.

**사례 2: 오픈소스 휴머노이드 로봇 프로젝트**

많은 오픈소스 프로젝트(예: awesome-humanoid-robot 저장소에 수록된 프로젝트)는 URDF 파일을 표준 산출물로 제공하여 전 세계 개발자가 다운로드하여 Gazebo에서 시뮬레이션하거나 RViz에서 시각화할 수 있게 합니다. 이러한 프로젝트는 일반적으로 ROS와 Gazebo의 다양한 버전 요구를 충족하기 위해 URDF와 SDF 두 형식을 동시에 제공합니다.

### 한계와 경계

**① URDF의 트리 구조 제한**: URDF는 폐쇄 루프 구조(예: 병렬 메커니즘 또는 폐쇄 체인 운동학)를 허용하지 않습니다. 휴머노이드 로봇에 평행 링크 메커니즘이 포함된 경우 SDF 또는 다른 형식을 사용해야 합니다. 엔지니어링 판단: 대부분의 이족 휴머노이드 로봇은 직렬 구조를 채택하므로 이 제한의 영향은 제한적입니다.

**② 충돌체 단순화와 현실성의 모순**: 충돌체가 너무 조밀하면 안 되지만(계산량 증가), 너무 단순화하면 시뮬레이션의 충돌 감지가 왜곡됩니다(예: 손가락 파지). 정밀도와 성능 사이의 균형이 필요하며, 일반적으로 말단 실행기에는 더 정밀한 충돌체를, 몸통에는 단순 형상을 사용합니다.

**③ 관성 파라미터의 정적 가정**: URDF의 관성 파라미터는 고정값으로 부하 변화(예: 로봇이 물건을 들고 있는 경우)를 설명할 수 없습니다. 동적 부하 시나리오의 경우 URDF를 수정하는 대신 제어 계층에서 온라인 보상을 수행해야 합니다.

### 일반적인 오해

1. **"URDF를 내보내면 바로 사용할 수 있다"** — 틀림. CAD 플러그인이 내보낸 URDF는 거의 항상 수동 조정이 필요합니다: 좌표계 회전, 단위 환산, 충돌체 단순화, 관절 한계 보정. 직접 사용하면 시뮬레이션과 실기가 심각하게偏离됩니다.

2. **"충돌체가 정밀할수록 좋다"** — 틀림. 충돌체가 너무 조밀하면 시뮬레이션 계산량이 폭발하여 실시간성을 보장할 수 없습니다. 올바른 방법은: 시각 모델은 세부 사항을 유지하고, 충돌체는 단순 형상을 사용하며, 핵심 상호작용 부위(예: 손爪)에서만 정밀도를 높이는 것입니다.

3. **"관성 파라미터는 중요하지 않다, 어차피 시뮬레이션에서 안 보인다"** — 틀림. 관성 파라미터는 동역학 시뮬레이션의 토크 출력을 직접 결정하므로 질량 속성이 잘못되면 제어 알고리즘을 시뮬레이션에서 튜닝해도 실기에 이식하면 반드시 실패합니다. 질량 속성은 반드시 P3.1.2/P3.1.3의 검증 결과에서 가져와야 합니다.

4. **"URDF와 SDF는 서로 대체할 수 있다"** — 부분적으로 틀림. URDF는 ROS의 표준 형식이고 SDF는 Gazebo의 기본 형식입니다. URDF는 `gz sdf` 도구로 SDF로 변환할 수 있지만, SDF는 URDF가 지원하지 않는 기능(예: 폐쇄 체인, 센서 모델)을 지원합니다. 목표 시뮬레이션 환경에 따라 형식을 선택해야 합니다.

### 관련 지식

- `ent_process_p6` — 본 공정 노드가 속한 상위 단계로, URDF 모델링 및 운동학 검증의 전체 프로세스와 수용 기준이 여기에 정의됩니다.
- `ent_process_p6_1_2` — 동일 계열 인접 공정: DH / 수정 DH 파라미터 정의(동일 작업 패키지 내 병렬 공정).
- `ent_process_p6_1_3` — 동일 계열 인접 공정: 충돌 및 자체 간섭 모델(동일 작업 패키지 내 병렬 공정).
