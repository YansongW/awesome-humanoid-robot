---
$id: ent_method_shooting_method
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Shooting Method
  zh: 打靶法
  ko: 슈팅 방법
summary:
  en: A numerical method for solving boundary-value problems by integrating from an initial guess and adjusting decision variables
    to satisfy terminal constraints.
  zh: 通过从初始猜测积分并调整决策变量以满足终端约束，从而求解边值问题的数值方法。
  ko: 초기 추정값으로부터 적분하고 최종 제약을 만족하도록 결정 변수를 조정하여 경계값 문제를 푸는 수치 방법.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- method
- chapter_15
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-13'
  confidence: high
  notes: 'Body populated from Wiki chapter section by scripts/fill_gap_bodies_from_wiki.py; pending human review and translation
    to en/ko. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-11] body upgraded to
    textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters
    + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

打靶法（Shooting Method）是一种求解两点边值问题（Boundary Value Problem, BVP）的数值方法，其核心思想是将边值问题转化为初值问题（Initial Value Problem, IVP）进行迭代求解。它真正改变的不是求解器的选择，而是将"满足边界条件"这一全局约束转化为"调整初始猜测"这一局部迭代过程，从而让无法直接求解的边值问题变得可解。

## 核心内容

### 是什么：打靶法的准确定义

打靶法属于 **方法** 类型，所属领域为 AI 模型与算法（`07_ai_models_algorithms`），价值链层级为智能层。其英文名称为 *Shooting Method*，韩文名称为 *슈팅 방법*。

从数学上讲，打靶法针对如下形式的两点边值问题：给定系统动力学 $\dot{x}(t) = f(t, x(t), u(t))$，边界条件为 $x(t_0) = x_0$ 和 $x(t_f) = x_f$，其中 $t_0$ 与 $t_f$ 分别为初始时刻与终端时刻。打靶法的基本策略是：先猜测初始状态中的未知分量（即"射击角度"），然后从 $t_0$ 向前积分系统动力学，检查终端时刻的状态是否满足 $x(t_f) = x_f$。若不满足，则根据终端误差调整初始猜测，重复积分直至收敛。

这一过程与炮兵射击的试射-校正逻辑高度相似：先打一发，观察落点偏差，再调整炮口角度，故得名"打靶法"。

### 为什么存在：痛点与历史定位

边值问题在机器人学中无处不在：最优控制中的两点边值问题、轨迹规划中的起止状态约束、以及步态规划中的周期边界条件。然而，边值问题无法像初值问题那样直接通过单次积分求解——终端约束的存在使得解必须同时满足两端条件，这本质上是一个隐式方程求解问题。

在打靶法出现之前，工程师面对边值问题的主要手段是解析方法或试错法。解析方法仅对极少数线性或特殊非线性系统有效；试错法则完全依赖经验，缺乏系统性和收敛保证。打靶法的历史定位在于：它首次将边值问题转化为一个低维的根求解问题——只需求解终端误差函数 $F(s) = 0$，其中 $s$ 为初始猜测向量，$F$ 为终端误差向量。这一转化使得 Newton-Raphson 等成熟的求根算法可以直接应用，从而将边值问题的求解纳入了数值分析的统一框架。

### 原理拆解

**① 从边值问题到初值问题的转化**

考虑一个一阶常微分方程组的两点边值问题：

$$
\dot{x}(t) = f(t, x(t)), \quad t \in [t_0, t_f], \quad g(x(t_0), x(t_f)) = 0
$$

其中 $g(\cdot, \cdot)$ 为边界条件函数。设系统状态维度为 $n$，边界条件数量为 $n$（即约束数等于状态数，问题适定）。将边界条件拆分为初始端已知部分与未知部分：令 $x(t_0) = x_0(s)$，其中 $s \in \mathbb{R}^k$ 为待定的初始猜测参数（$k$ 为未知初始状态的数量），$x_0(s)$ 为已知函数关系。从该初始状态积分系统至 $t_f$，得到终端状态 $x(t_f; s)$，定义终端误差函数：

$$
F(s) = h(x(t_f; s))
$$

其中 $h(\cdot)$ 为终端边界条件的残差函数。于是原边值问题转化为求 $F(s) = 0$ 的根问题。

**② 迭代求解与灵敏度传播**

求解 $F(s) = 0$ 的标准方法是 Newton-Raphson 迭代。设当前猜测为 $s^{(k)}$，则迭代格式为：

$$
s^{(k+1)} = s^{(k)} - \left[ J_F(s^{(k)}) \right]^{-1} F(s^{(k)})
$$

其中 $J_F = \partial F / \partial s$ 为终端误差对初始猜测的雅可比矩阵（即灵敏度矩阵）。该矩阵可通过变分方程（variational equation）传播获得：令 $\Phi(t) = \partial x(t) / \partial s$，则 $\Phi(t)$ 满足：

$$
\dot{\Phi}(t) = \frac{\partial f}{\partial x}(t, x(t)) \, \Phi(t), \quad \Phi(t_0) = \frac{\partial x_0}{\partial s}
$$

将变分方程与系统动力学方程同时从 $t_0$ 积分至 $t_f$，即可得到 $J_F = \partial h / \partial x \cdot \Phi(t_f)$。这一"同时积分状态与灵敏度"的做法，使得每次迭代只需一次正向积分即可获得完整的梯度信息，避免了有限差分带来的额外计算开销。

**③ 步长控制与收敛性**

打靶法的收敛性依赖于初始猜测的质量。若初始猜测距离真解过远，Newton 迭代可能发散。工程上常用的缓解手段包括：阻尼 Newton 法（在迭代步长上乘以衰减因子）、同伦法（从松弛边界条件逐步过渡到严格边界条件）、以及多重打靶法（将时间域分段，每段独立打靶并施加连续性约束）。其中多重打靶法通过将长积分区间切分为多个短区间，显著降低了单次积分对初始误差的敏感性，是工程应用中最稳健的变体。

### 关键参数与规格

| 参数 | 典型取值/范围 | 说明 |
|------|--------------|------|
| 积分器阶数 | 4–8 阶（如 RK45、DOP853） | 需自行确认；高阶积分器在光滑动力学下精度更高 |
| 终端误差容差 | $10^{-6}$–$10^{-10}$ | 需自行确认；取决于应用对边界精度的要求 |
| Newton 迭代最大次数 | 10–50 次 | 需自行确认；超过则判定为不收敛 |
| 灵敏度矩阵计算方式 | 变分方程 / 有限差分 | 变分方程精度高但需额外推导；有限差分实现简单但引入截断误差 |
| 多重打靶分段数 | 2–20 段 | 需自行确认；分段越多对初始猜测越鲁棒，但变量维度线性增长 |

### 横向对比

| 方法 | 离散化方式 | 变量维度 | 对初始猜测的敏感性 | 约束处理方式 |
|------|-----------|---------|-------------------|-------------|
| 打靶法 | 仅离散初始猜测 | 低（$k$ 维） | 高（单段积分放大误差） | 通过迭代隐式满足 |
| 多重打靶法 | 离散分段点状态 | 中（$k \times N$ 维） | 中（分段抑制误差传播） | 通过连续性约束显式满足 |
| 直接配点法 | 离散全部状态与控制 | 高（$n \times N$ 维） | 低（全局代数约束） | 通过 NLP 约束显式满足 |

打靶法的核心优势在于变量维度低——仅需优化初始猜测参数，因此单次迭代的计算成本远低于直接配点法。但其代价是：单段积分对初始误差的放大效应使得收敛半径较小，对强非线性系统或长时域问题尤为敏感。直接配点法通过将动力学约束离散为代数约束，牺牲变量维度换取了对初始猜测的鲁棒性，但 NLP 问题的规模也随之增大。工程判断：对于时域较短（如单步规划，约 0.5–1 秒）、动力学相对平滑的问题，打靶法因其低维度和高精度而更具优势；对于时域较长或存在强非线性（如足式机器人全身运动规划）的问题，直接配点法更为稳妥。

### 谁在用·应用案例

在人形机器人领域，打靶法主要应用于以下场景：

1. **单步轨迹规划**：给定起始姿态与目标姿态，求解满足动力学约束的最优轨迹。由于单步时域短（通常 0.5–1 秒），打靶法的低维度优势得以充分发挥，且终端精度高（可达 $10^{-8}$ 量级，需自行确认），适合对落点精度要求严格的任务。

2. **周期步态生成**：行走步态要求相邻步周期的状态完全一致（周期边界条件），这天然是一个两点边值问题。打靶法通过调整初始状态猜测，使终端状态回到初始状态，从而生成稳定的周期步态。

3. **最优控制问题的间接法求解**：基于 Pontryagin 极小值原理，最优控制问题可转化为 Hamiltonian 系统的两点边值问题（协态变量在两端满足横截条件）。打靶法直接求解该边值问题，得到严格满足一阶最优性条件的解，精度高于直接法的 NLP 近似解。

### 局限与边界

打靶法的核心局限在于对初始猜测的敏感性。对于强非线性系统，终端误差函数 $F(s)$ 可能出现多根、不连续甚至无界的情况，导致 Newton 迭代发散。此外，当系统动力学在积分区间内存在刚性（stiffness）时，显式积分器需要极小的步长，计算成本急剧上升。

另一个重要边界是：打靶法求解的是"给定边界条件下的确定性轨迹"，它不直接处理不等式约束（如关节力矩极限、足底摩擦锥约束）。若问题包含不等式约束，需在打靶迭代外层嵌套约束处理机制（如惩罚函数或投影），这会显著增加实现复杂度。对于包含大量不等式约束的全身运动规划问题，直接配点法或直接转录法（direct transcription）是更自然的选择。

### 常见误区

1. **"打靶法只能用于线性系统"**——错。打靶法对非线性系统同样适用，只是收敛性依赖于初始猜测质量；线性系统的终端误差函数是线性的，Newton 迭代一次收敛，而非线性系统需要多次迭代。

2. **"打靶法一定比直接配点法快"**——不必然。打靶法单次迭代成本低，但若初始猜测差导致迭代次数多甚至发散，总成本可能超过直接配点法。工程判断：在时域短、初始猜测好的场景下打靶法更快；否则直接配点法更可靠。

3. **"多重打靶只是打靶法的简单分段"**——不准确。多重打靶不仅分段积分，还引入了分段点处的连续性约束，将原问题转化为一个带约束的优化问题（而非纯求根问题），其数学结构与单段打靶有本质区别。

### 相关知识

- `ent_method_direct_collocation` — 直接配点法，与打靶法同属轨迹优化方法家族，但采用全局离散化策略，对初始猜测更鲁棒，适合高维强非线性问题。
- `ent_method_model_predictive_control` — 打靶法求得的轨迹可经 MPC 滚动重解获得在线鲁棒性，二者常串联使用。

## 参考

- [Wiki extraction](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-02.md)
- [Chapter 08](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)

## Overview

The Shooting Method is a numerical technique for solving two-point Boundary Value Problems (BVPs). Its core idea is to transform the boundary value problem into an Initial Value Problem (IVP) through iterative solving. What it truly changes is not the choice of solver, but rather the conversion of the global constraint of "satisfying boundary conditions" into a local iterative process of "adjusting the initial guess," thereby making boundary value problems, which cannot be solved directly, tractable.

## Content

### What It Is: A Precise Definition of the Shooting Method

The Shooting Method belongs to the **method** category, within the domain of AI models and algorithms (`07_ai_models_algorithms`), at the intelligence layer of the value chain. Its English name is *Shooting Method*, and its Korean name is *슈팅 방법*.

Mathematically, the Shooting Method addresses two-point boundary value problems of the following form: given system dynamics $\dot{x}(t) = f(t, x(t), u(t))$, with boundary conditions $x(t_0) = x_0$ and $x(t_f) = x_f$, where $t_0$ and $t_f$ are the initial and terminal times, respectively. The basic strategy of the Shooting Method is: first, guess the unknown components of the initial state (the "shooting angle"), then integrate the system dynamics forward from $t_0$, and check whether the terminal state satisfies $x(t_f) = x_f$. If not, adjust the initial guess based on the terminal error and repeat the integration until convergence.

This process closely resembles the trial-and-correction logic of artillery firing: fire a shot, observe the impact point deviation, then adjust the barrel angle—hence the name "Shooting Method."

### Why It Exists: Pain Points and Historical Positioning

Boundary value problems are ubiquitous in robotics: two-point boundary value problems in optimal control, start and end state constraints in trajectory planning, and periodic boundary conditions in gait planning. However, boundary value problems cannot be solved directly through a single integration like initial value problems—the terminal constraint means the solution must satisfy conditions at both ends, which is essentially an implicit equation solving problem.

Before the Shooting Method emerged, engineers primarily relied on analytical methods or trial-and-error for boundary value problems. Analytical methods are only effective for a very few linear or special nonlinear systems; trial-and-error relies entirely on experience, lacking systematic rigor and convergence guarantees. The historical significance of the Shooting Method lies in its first transformation of boundary value problems into a low-dimensional root-finding problem—requiring only the solution of the terminal error function $F(s) = 0$, where $s$ is the initial guess vector and $F$ is the terminal error vector. This transformation allows mature root-finding algorithms such as Newton-Raphson to be applied directly, thereby incorporating the solution of boundary value problems into the unified framework of numerical analysis.

### Principle Breakdown

**① Transformation from Boundary Value Problem to Initial Value Problem**

Consider a two-point boundary value problem for a system of first-order ordinary differential equations:

$$
\dot{x}(t) = f(t, x(t)), \quad t \in [t_0, t_f], \quad g(x(t_0), x(t_f)) = 0
$$

where $g(\cdot, \cdot)$ is the boundary condition function. Assume the system state dimension is $n$ and the number of boundary conditions is $n$ (i.e., the number of constraints equals the number of states, making the problem well-posed). Split the boundary conditions into known parts at the initial end and unknown parts: let $x(t_0) = x_0(s)$, where $s \in \mathbb{R}^k$ is the undetermined initial guess parameter ($k$ is the number of unknown initial states), and $x_0(s)$ is a known functional relationship. Integrate the system from this initial state to $t_f$ to obtain the terminal state $x(t_f; s)$, and define the terminal error function:

$$
F(s) = h(x(t_f; s))
$$

where $h(\cdot)$ is the residual function of the terminal boundary condition. The original boundary value problem is thus transformed into a root-finding problem for $F(s) = 0$.

**② Iterative Solving and Sensitivity Propagation**

The standard method for solving $F(s) = 0$ is Newton-Raphson iteration. Given the current guess $s^{(k)}$, the iteration scheme is:

$$
s^{(k+1)} = s^{(k)} - \left[ J_F(s^{(k)}) \right]^{-1} F(s^{(k)})
$$

where $J_F = \partial F / \partial s$ is the Jacobian matrix of the terminal error with respect to the initial guess (i.e., the sensitivity matrix). This matrix can be obtained through propagation of the variational equation: let $\Phi(t) = \partial x(t) / \partial s$, then $\Phi(t)$ satisfies:

$$
\dot{\Phi}(t) = \frac{\partial f}{\partial x}(t, x(t)) \, \Phi(t), \quad \Phi(t_0) = \frac{\partial x_0}{\partial s}
$$

By integrating the variational equation simultaneously with the system dynamics from $t_0$ to $t_f$, we obtain $J_F = \partial h / \partial x \cdot \Phi(t_f)$. This approach of "integrating state and sensitivity simultaneously" allows each iteration to obtain complete gradient information with only one forward integration, avoiding the additional computational cost of finite differences.

**③ Step Size Control and Convergence**

The convergence of the Shooting Method depends on the quality of the initial guess. If the initial guess is too far from the true solution, Newton iteration may diverge. Common mitigation strategies in engineering include: damped Newton methods (multiplying the iteration step by a decay factor), homotopy methods (gradually transitioning from relaxed to strict boundary conditions), and multiple shooting methods (dividing the time domain into segments, shooting each segment independently with continuity constraints imposed). Among these, the multiple shooting method significantly reduces the sensitivity of a single integration to initial errors by splitting the long integration interval into multiple shorter intervals, making it the most robust variant in engineering applications.

### Key Parameters and Specifications

| Parameter | Typical Value/Range | Description |
|-----------|---------------------|-------------|
| Integrator order | 4–8 (e.g., RK45, DOP853) | To be confirmed by user; higher-order integrators offer greater accuracy for smooth dynamics |
| Terminal error tolerance | $10^{-6}$–$10^{-10}$ | To be confirmed by user; depends on the application's boundary accuracy requirements |
| Maximum Newton iterations | 10–50 | To be confirmed by user; exceeding this indicates non-convergence |
| Sensitivity matrix computation | Variational equation / finite difference | Variational equation is more accurate but requires additional derivation; finite difference is simpler to implement but introduces truncation error |
| Multiple shooting segments | 2–20 | To be confirmed by user; more segments improve robustness to initial guesses but increase variable dimension linearly |

### Horizontal Comparison

| Method | Discretization Approach | Variable Dimension | Sensitivity to Initial Guess | Constraint Handling |
|--------|------------------------|--------------------|------------------------------|---------------------|
| Shooting Method | Discretizes only the initial guess | Low ($k$-dimensional) | High (single-segment integration amplifies errors) | Satisfied implicitly through iteration |
| Multiple Shooting Method | Discretizes segment point states | Medium ($k \times N$-dimensional) | Medium (segmentation suppresses error propagation) | Satisfied explicitly through continuity constraints |
| Direct Collocation | Discretizes all states and controls | High ($n \times N$-dimensional) | Low (global algebraic constraints) | Satisfied explicitly through NLP constraints |

The core advantage of the Shooting Method lies in its low variable dimension—only the initial guess parameters need to be optimized, making the per-iteration computational cost significantly lower than direct collocation. However, its trade-off is that the error amplification effect of single-segment integration results in a smaller convergence radius, making it particularly sensitive to strongly nonlinear systems or long-horizon problems. Direct collocation sacrifices variable dimension for robustness to initial guesses by discretizing dynamics constraints into algebraic constraints, but the scale of the NLP problem correspondingly increases. Engineering judgment: for problems with short horizons (e.g., single-step planning, approximately 0.5–1 second) and relatively smooth dynamics, the Shooting Method is advantageous due to its low dimensionality and high accuracy; for problems with long horizons or strong nonlinearity (e.g., whole-body motion planning for legged robots), direct collocation is more reliable.

### Who Uses It: Application Cases

In the field of humanoid robotics, the Shooting Method is primarily applied in the following scenarios:

1. **Single-step trajectory planning**: Given a starting posture and a target posture, solve for the optimal trajectory satisfying dynamics constraints. Since the single-step horizon is short (typically 0.5–1 second), the low-dimensional advantage of the Shooting Method is fully leveraged, and terminal accuracy is high (reaching $10^{-8}$ order of magnitude, to be confirmed by user), making it suitable for tasks with strict landing point accuracy requirements.

2. **Periodic gait generation**: Walking gaits require the states of adjacent step cycles to be identical (periodic boundary conditions), which is naturally a two-point boundary value problem. The Shooting Method generates stable periodic gaits by adjusting the initial state guess so that the terminal state returns to the initial state.

3. **Indirect method for optimal control problems**: Based on Pontryagin's minimum principle, optimal control problems can be transformed into two-point boundary value problems of the Hamiltonian system (with costate variables satisfying transversality conditions at both ends). The Shooting Method directly solves this boundary value problem, yielding solutions that strictly satisfy first-order optimality conditions, with higher accuracy than the NLP approximations of direct methods.

### Limitations and Boundaries

The core limitation of the Shooting Method is its sensitivity to the initial guess. For strongly nonlinear systems, the terminal error function $F(s)$ may exhibit multiple roots, discontinuities, or even unbounded behavior, causing Newton iteration to diverge. Additionally, when the system dynamics exhibit stiffness within the integration interval, explicit integrators require extremely small step sizes, leading to sharply increased computational costs.

Another important boundary is that the Shooting Method solves for "deterministic trajectories under given boundary conditions"; it does not directly handle inequality constraints (such as joint torque limits or foot friction cone constraints). If the problem includes inequality constraints, a constraint handling mechanism (such as penalty functions or projection) must be nested outside the shooting iteration loop, which significantly increases implementation complexity. For whole-body motion planning problems with numerous inequality constraints, direct collocation or direct transcription methods are more natural choices.

### Common Misconceptions

1. **"The Shooting Method can only be used for linear systems"**—Incorrect. The Shooting Method is equally applicable to nonlinear systems; convergence merely depends on the quality of the initial guess. For linear systems, the terminal error function is linear, and Newton iteration converges in one step, whereas nonlinear systems require multiple iterations.

2. **"The Shooting Method is always faster than direct collocation"**—Not necessarily. The Shooting Method has low per-iteration cost, but if a poor initial guess leads to many iterations or divergence, the total cost may exceed that of direct collocation. Engineering judgment: the Shooting Method is faster in scenarios with short horizons and good initial guesses; otherwise, direct collocation is more reliable.

3. **"Multiple shooting is just a simple segmentation of the Shooting Method"**—Inaccurate. Multiple shooting not only integrates in segments but also introduces continuity constraints at segment points, transforming the original problem into a constrained optimization problem (rather than a pure root-finding problem), which is structurally distinct from single-segment shooting.

### Related Knowledge

- `ent_method_direct_collocation` — Direct collocation, which belongs to the same family of trajectory optimization methods as the Shooting Method, but employs a global discretization strategy, is more robust to initial guesses, and is suitable for high-dimensional strongly nonlinear problems.
- `ent_method_model_predictive_control` — Trajectories obtained via the Shooting Method can be re-solved in a receding horizon manner through MPC to achieve online robustness; the two are often used in tandem.

## 개요

슈팅 방법(Shooting Method)은 2점 경계값 문제(Boundary Value Problem, BVP)를 푸는 수치적 방법으로, 그 핵심 아이디어는 경계값 문제를 초기값 문제(Initial Value Problem, IVP)로 변환하여 반복적으로 푸는 것입니다. 이 방법이 실제로 바꾸는 것은 솔버의 선택이 아니라, "경계 조건을 만족한다"는 전역적 제약을 "초기 추측을 조정한다"는 국소적 반복 과정으로 변환하여, 직접 풀 수 없던 경계값 문제를 풀 수 있게 만드는 것입니다.

## 핵심 내용

### 무엇인가: 슈팅 방법의 정확한 정의

슈팅 방법은 **방법** 유형에 속하며, 소속 분야는 AI 모델 및 알고리즘(`07_ai_models_algorithms`), 가치 사슬 계층은 지능 계층입니다. 영문 명칭은 *Shooting Method*, 한글 명칭은 *슈팅 방법*입니다.

수학적으로 슈팅 방법은 다음과 같은 형태의 2점 경계값 문제를 대상으로 합니다: 시스템 동역학 $\dot{x}(t) = f(t, x(t), u(t))$이 주어지고, 경계 조건은 $x(t_0) = x_0$ 및 $x(t_f) = x_f$이며, 여기서 $t_0$와 $t_f$는 각각 초기 시각과 종단 시각입니다. 슈팅 방법의 기본 전략은: 먼저 초기 상태의 미지 성분(즉, "발사 각도")을 추측한 다음, $t_0$에서 시스템 동역학을 전방으로 적분하여 종단 시각의 상태가 $x(t_f) = x_f$를 만족하는지 확인합니다. 만족하지 않으면 종단 오차에 따라 초기 추측을 조정하고 수렴할 때까지 적분을 반복합니다.

이 과정은 포병 사격의 시험 발사-보정 논리와 매우 유사합니다: 먼저 한 발을 쏘고, 낙점 편차를 관찰한 다음, 포구 각도를 조정합니다. 그래서 "슈팅 방법"이라는 이름이 붙었습니다.

### 왜 존재하는가:痛点과 역사적 위치

경계값 문제는 로봇 공학에서 어디에나 존재합니다: 최적 제어의 2점 경계값 문제, 궤적 계획의 시작-종료 상태 제약, 보행 계획의 주기적 경계 조건 등이 있습니다. 그러나 경계값 문제는 초기값 문제처럼 단일 적분으로 직접 풀 수 없습니다——종단 제약의 존재로 인해 해가 양쪽 끝 조건을 동시에 만족해야 하며, 이는 본질적으로 암시적 방정식 풀이 문제입니다.

슈팅 방법이 등장하기 전에, 엔지니어들이 경계값 문제를 다루는 주요 수단은 해석적 방법이나 시행착오법이었습니다. 해석적 방법은 극소수의 선형 또는 특수 비선형 시스템에만 유효했습니다; 시행착오법은 완전히 경험에 의존하여 체계성과 수렴 보장이 부족했습니다. 슈팅 방법의 역사적 위치는: 처음으로 경계값 문제를 저차원 근 찾기 문제로 변환했다는 점입니다——종단 오차 함수 $F(s) = 0$만 풀면 되며, 여기서 $s$는 초기 추측 벡터, $F$는 종단 오차 벡터입니다. 이러한 변환 덕분에 Newton-Raphson과 같은 성숙한 근 찾기 알고리즘을 직접 적용할 수 있어, 경계값 문제의 해법을 수치 해석의 통일된 프레임워크 안에 포함시켰습니다.

### 원리 분해

**① 경계값 문제에서 초기값 문제로의 변환**

1계 상미분 방정식계의 2점 경계값 문제를 고려합니다:

$$
\dot{x}(t) = f(t, x(t)), \quad t \in [t_0, t_f], \quad g(x(t_0), x(t_f)) = 0
$$

여기서 $g(\cdot, \cdot)$는 경계 조건 함수입니다. 시스템 상태 차원을 $n$, 경계 조건 개수를 $n$이라고 가정합니다(즉, 제약 수가 상태 수와 같아 문제가 적절히 설정됨). 경계 조건을 초기 끝의 알려진 부분과 미지 부분으로 분할합니다: $x(t_0) = x_0(s)$라고 두면, 여기서 $s \in \mathbb{R}^k$는 결정해야 할 초기 추측 매개변수($k$는 미지 초기 상태의 개수), $x_0(s)$는 알려진 함수 관계입니다. 이 초기 상태에서 시스템을 $t_f$까지 적분하여 종단 상태 $x(t_f; s)$를 얻고, 종단 오차 함수를 정의합니다:

$$
F(s) = h(x(t_f; s))
$$

여기서 $h(\cdot)$는 종단 경계 조건의 잔차 함수입니다. 따라서 원래 경계값 문제는 $F(s) = 0$의 근을 찾는 문제로 변환됩니다.

**② 반복 해법과 민감도 전파**

$F(s) = 0$을 푸는 표준 방법은 Newton-Raphson 반복입니다. 현재 추측을 $s^{(k)}$라고 하면, 반복 형식은:

$$
s^{(k+1)} = s^{(k)} - \left[ J_F(s^{(k)}) \right]^{-1} F(s^{(k)})
$$

여기서 $J_F = \partial F / \partial s$는 초기 추측에 대한 종단 오차의 야코비 행렬(즉, 민감도 행렬)입니다. 이 행렬은 변분 방정식(variational equation)을 통해 전파하여 얻을 수 있습니다: $\Phi(t) = \partial x(t) / \partial s$라고 두면, $\Phi(t)$는 다음을 만족합니다:

$$
\dot{\Phi}(t) = \frac{\partial f}{\partial x}(t, x(t)) \, \Phi(t), \quad \Phi(t_0) = \frac{\partial x_0}{\partial s}
$$

변분 방정식과 시스템 동역학 방정식을 $t_0$에서 $t_f$까지 동시에 적분하면 $J_F = \partial h / \partial x \cdot \Phi(t_f)$를 얻을 수 있습니다. 이렇게 "상태와 민감도를 동시에 적분"하는 방식은 각 반복에서 단 한 번의 전방 적분만으로 완전한 기울기 정보를 얻을 수 있게 하여, 유한 차분으로 인한 추가 계산 비용을 피할 수 있습니다.

**③ 보폭 제어와 수렴성**

슈팅 방법의 수렴성은 초기 추측의 품질에 의존합니다. 초기 추측이 실제 해에서 너무 멀면 Newton 반복이 발산할 수 있습니다. 공학에서 일반적으로 사용하는 완화 수단은: 감쇠 Newton 방법(반복 보폭에 감쇠 인자를 곱함), 동형 방법(완화된 경계 조건에서 엄격한 경계 조건으로 점진적으로 전환), 그리고 다중 슈팅 방법(시간 영역을 분할하고, 각 구간을 독립적으로 슈팅하며 연속성 제약을 부과) 등이 있습니다. 그중 다중 슈팅 방법은 긴 적분 구간을 여러 개의 짧은 구간으로 나누어 단일 적분이 초기 오차에 대한 민감도를 크게 줄이며, 공학 응용에서 가장 견고한 변형입니다.

### 주요 매개변수와 사양

| 매개변수 | 일반적인 값/범위 | 설명 |
|------|--------------|------|
| 적분기 차수 | 4–8차 (예: RK45, DOP853) | 직접 확인 필요; 고차 적분기는 매끄러운 동역학에서 정밀도가 더 높음 |
| 종단 오차 허용 오차 | $10^{-6}$–$10^{-10}$ | 직접 확인 필요; 응용 분야의 경계 정밀도 요구에 따라 다름 |
| Newton 반복 최대 횟수 | 10–50회 | 직접 확인 필요; 초과 시 수렴 실패로 판정 |
| 민감도 행렬 계산 방식 | 변분 방정식 / 유한 차분 | 변분 방정식은 정밀도가 높지만 추가 유도 필요; 유한 차분은 구현이 간단하지만 절단 오차 발생 |
| 다중 슈팅 분할 수 | 2–20구간 | 직접 확인 필요; 분할이 많을수록 초기 추측에 대한 견고성이 높아지지만 변수 차원이 선형적으로 증가 |

### 횡적 비교

| 방법 | 이산화 방식 | 변수 차원 | 초기 추측에 대한 민감도 | 제약 처리 방식 |
|------|-----------|---------|-------------------|-------------|
| 슈팅 방법 | 초기 추측만 이산화 | 낮음 ($k$ 차원) | 높음 (단일 구간 적분이 오차 증폭) | 반복을 통해 암시적으로 충족 |
| 다중 슈팅 방법 | 분할점 상태 이산화 | 중간 ($k \times N$ 차원) | 중간 (분할이 오차 전파 억제) | 연속성 제약을 통해 명시적으로 충족 |
| 직접 배치법 | 전체 상태 및 제어 이산화 | 높음 ($n \times N$ 차원) | 낮음 (전역 대수 제약) | NLP 제약을 통해 명시적으로 충족 |

슈팅 방법의 핵심 장점은 변수 차원이 낮다는 것입니다——초기 추측 매개변수만 최적화하면 되므로, 단일 반복의 계산 비용이 직접 배치법보다 훨씬 낮습니다. 그러나 그 대가는: 단일 구간 적분이 초기 오차를 증폭시켜 수렴 반경이 작아지고, 강비선형 시스템이나 긴 시간 영역 문제에 특히 민감하다는 것입니다. 직접 배치법은 동역학 제약을 대수 제약으로 이산화하여 변수 차원을 희생하는 대신 초기 추측에 대한 견고성을 얻지만, NLP 문제의 규모도 함께 증가합니다. 공학적 판단: 시간 영역이 짧고(예: 단일 보행 계획, 약 0.5–1초), 동역학이 비교적 매끄러운 문제에서는 슈팅 방법이 저차원성과 고정밀도로 인해 더 유리합니다; 시간 영역이 길거나 강비선형이 존재하는 문제(예: 족식 로봇 전신 운동 계획)에서는 직접 배치법이 더 안정적입니다.

### 누가 사용하는가·응용 사례

인간형 로봇 분야에서 슈팅 방법은 주로 다음 시나리오에 적용됩니다:

1. **단일 보행 궤적 계획**: 주어진 시작 자세와 목표 자세에 대해 동역학 제약을 만족하는 최적 궤적을求解합니다. 단일 보행 시간 영역이 짧기(보통 0.5–1초) 때문에 슈팅 방법의 저차원성 장점이 충분히 발휘되며, 종단 정밀도가 높아(최대 $10^{-8}$ 수준, 직접 확인 필요) 착지점 정밀도 요구가 엄격한 작업에 적합합니다.

2. **주기적 보행 생성**: 보행 보행은 인접 보행 주기의 상태가 완전히 일치해야 합니다(주기적 경계 조건). 이는 자연스럽게 2점 경계값 문제입니다. 슈팅 방법은 초기 상태 추측을 조정하여 종단 상태가 초기 상태로 돌아오게 함으로써 안정적인 주기적 보행을 생성합니다.

3. **최적 제어 문제의 간접법 해법**: Pontryagin 최소 원리에 기반하여, 최적 제어 문제는 Hamiltonian 시스템의 2점 경계값 문제(공동 상태 변수가 양쪽 끝에서 횡단 조건을 만족)로 변환될 수 있습니다. 슈팅 방법은 이 경계값 문제를 직접 풀어 1차 최적성 조건을 엄격히 만족하는 해를 얻으며, 직접법의 NLP 근사 해보다 정밀도가 높습니다.

### 한계와 경계

슈팅 방법의 핵심 한계는 초기 추측에 대한 민감성입니다. 강비선형 시스템의 경우 종단 오차 함수 $F(s)$가 다중 근, 불연속, 심지어 무계가 될 수 있어 Newton 반복이 발산할 수 있습니다. 또한 시스템 동역학이 적분 구간 내에서 강성(stiffness)을 가질 때, 명시적 적분기는 매우 작은 보폭이 필요하여 계산 비용이 급격히 증가합니다.

또 다른 중요한 경계는: 슈팅 방법은 "주어진 경계 조건 하의 결정적 궤적"을 푸는 것이며, 부등식 제약(관절 토크 한계, 발바닥 마찰 원뿔 제약 등)을 직접 처리하지 않습니다. 문제에 부등식 제약이 포함된 경우, 슈팅 반복 외부에 제약 처리 메커니즘(페널티 함수 또는 투영 등)을 중첩해야 하며, 이는 구현 복잡도를 크게 증가시킵니다. 많은 부등식 제약을 포함하는 전신 운동 계획 문제의 경우 직접 배치법 또는 직접 전사법(direct transcription)이 더 자연스러운 선택입니다.

### 일반적인 오해

1. **"슈팅 방법은 선형 시스템에만 사용할 수 있다"**——틀림. 슈팅 방법은 비선형 시스템에도 동일하게 적용 가능하며, 단지 수렴성이 초기 추측 품질에 의존할 뿐입니다; 선형 시스템의 종단 오차 함수는 선형이므로 Newton 반복이 한 번에 수렴하지만, 비선형 시스템은 여러 번 반복해야 합니다.

2. **"슈팅 방법이 직접 배치법보다 항상 빠르다"**——반드시 그렇지 않음. 슈팅 방법은 단일 반복 비용이 낮지만, 초기 추측이 나빠 반복 횟수가 많아지거나 발산하면 총 비용이 직접 배치법을 초과할 수 있습니다. 공학적 판단: 시간 영역이 짧고 초기 추측이 좋은 시나리오에서는 슈팅 방법이 더 빠릅니다; 그렇지 않으면 직접 배치법이 더 신뢰할 수 있습니다.

3. **"다중 슈팅은 슈팅 방법의 단순한 분할일 뿐이다"**——정확하지 않음. 다중 슈팅은 구간 적분뿐만 아니라 분할점에서의 연속성 제약을 도입하여 원래 문제를 제약이 있는 최적화 문제(순수 근 찾기 문제가 아닌)로 변환하며, 그 수학적 구조는 단일 구간 슈팅과 본질적으로 다릅니다.

### 관련 지식

- `ent_method_direct_collocation` — 직접 배치법, 슈팅 방법과 같은 궤적 최적화 방법 계열에 속하지만 전역 이산화 전략을 사용하여 초기 추측에 더 견고하며, 고차원 강비선형 문제에 적합합니다.
- `ent_method_model_predictive_control` — 슈팅 방법으로 얻은 궤적은 MPC의 구름 재해석을 통해 온라인 견고성을 얻을 수 있으며, 두 방법은 종종 직렬로 사용됩니다.
