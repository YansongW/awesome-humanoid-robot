---
$id: ent_method_admittance_control
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Admittance Control
  zh: 导纳控制
  ko: 어드미턴스 제어
summary:
  en: A control approach that converts measured external forces into desired position or velocity trajectories, complementary
    to impedance control.
  zh: '概述 阻抗控制的物理基础：从质点-弹簧-阻尼到端口特性相关内容如下。 ## 核心内容 #### 阻抗控制的物理基础：从质点-弹簧-阻尼到端口特性 把机器人末端抽象为一个质量-弹簧-阻尼单元，其动力学方程可由牛顿第二定律直接写出：'
  ko: 측정된 외력을 원하는 위치·속도 궤적으로 변환하는 제어 방식으로, 임피던스 제어와 상호 보완적.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- method
- chapter_14
- wiki_gap
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. | WP4 trilingual backfill 2026-08-10:
    closed unclosed code fence(s) and removed duplicate stale translation block(s) (pre-existing ingestion defect).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b1): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_wiki_extraction
  type: other
  title: Wiki extraction
  date: '2026-07-09'
  accessed_at: '2026-07-09'
---
## 概述

导纳控制（Admittance Control）是阻抗控制的"对偶"方法：它根据测得的接触力计算期望运动轨迹，再由位置/速度控制器跟踪，其基本方程为 \(\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}\)。它真正改变的不是力控制的方式，而是把"力-运动"的耦合关系从力矩层转移到了轨迹层——让现有高精度位置控制硬件直接参与柔顺交互。

## 核心内容

### 是什么：准确定义

导纳控制是一种**根据外力生成期望运动**的控制策略。它通过力传感器测量外部接触力 \(\mathbf{F}_{\text{ext}}\)，将其输入导纳控制器，输出一个修正后的指令轨迹 \(\mathbf{x}_c\)，再由内环位置控制器跟踪该轨迹。其核心方程（来源：项目 Wiki 第 8 章 §8.4.11）：

$$
\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}
$$

其中 \(\mathbf{x}_d\) 为期望轨迹，\(\mathbf{x}_c\) 为导纳修正后的指令轨迹，\(\mathbf{F}_{\text{ext}}\) 为外部接触力，\(\mathbf{M}_d\)、\(\mathbf{D}_d\)、\(\mathbf{K}_d\) 分别为期望惯量、阻尼与刚度矩阵。**输入是力，输出是运动修正量**——这与阻抗控制（输入运动偏差、输出力）正好相反，二者构成力-运动对偶关系。

### 为什么存在：痛点与历史定位

纯位置控制在接触场景有本质缺陷：几何误差（模型不准、物体位姿未知）会转化为**不受控的接触力**——轻则任务失败，重则损坏机器人或伤人。阻抗控制虽然解决了这一问题，但它要求机器人具备**力矩级控制能力**，即能直接向电流环发送力矩指令。然而，绝大多数工业机器人和传统位置控制平台并不具备力矩接口，其高精度位置环反而成为实现柔顺控制的障碍。

导纳控制的历史定位正在于此：它**不改变底层位置控制架构**，而是在位置环之外叠加一个"导纳层"，把力信号翻译成位置修正量。这使得：

- 可直接利用现有高精度位置控制硬件；
- 对外部力传感器噪声有一定滤波作用；
- 适合刚度较高的工业机器人与环境交互。

它真正改变的不是控制架构，而是**柔顺能力的部署方式**——让没有力矩接口的机器人也能安全地与环境交互。

### 原理拆解

**① 导纳方程：力到轨迹的映射**

导纳控制的核心是二阶微分方程，它规定了外力与轨迹修正量之间的动态关系。当外力 \(\mathbf{F}_{\text{ext}}\) 作用于末端时，导纳控制器计算出修正轨迹 \(\mathbf{x}_c\)，使得虚拟质量-阻尼-弹簧系统的响应符合期望动态特性。稳态下，弹簧项主导：

$$
\mathbf{F}_{\text{ext}} \approx \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c)
$$

这意味着外力越大，轨迹偏离期望位置越远——机器人表现得越"柔顺"。

**② 内/外环架构：两层回路的协作**

导纳控制采用典型的内/外环架构：

- **外环（导纳环）**：读取力传感器信号，通过导纳方程生成修正轨迹 \(\mathbf{x}_c\)；
- **内环（位置环）**：跟踪 \(\mathbf{x}_c\)，驱动执行器运动。

这种分层结构的优势在于：位置内环的高带宽保证了跟踪精度，而导纳外环的较低带宽恰好滤除了力传感器的高频噪声。**它真正改变的不是控制精度，而是力-位耦合的层级位置**——从力矩层上移到轨迹层。

**③ 与阻抗控制的本质区别**

| 维度 | 阻抗控制 | 导纳控制 |
|---|---|---|
| 输入 | 运动偏差（位置、速度） | 外部接触力 |
| 输出 | 力矩指令 | 修正轨迹 |
| 内环 | 电流环/力矩环 | 位置环/速度环 |
| 硬件要求 | 力矩控制能力 | 力传感器 + 位置控制 |
| 适用场景 | 低刚度环境、直接力控 | 高刚度环境、工业机器人 |

### 关键参数与规格

导纳控制的关键参数包括（来源：项目 Wiki 第 8 章 §8.4.11）：

- **期望惯量 \(\mathbf{M}_d\)**（kg）：决定加速度响应，影响接触瞬间的动态行为；
- **期望阻尼 \(\mathbf{D}_d\)**（N·s/m）：决定能量耗散速率，影响稳定性与振荡衰减；
- **期望刚度 \(\mathbf{K}_d\)**（N/m）：决定稳态位置偏差与恢复力关系；
- **力传感器量程与噪声**：直接影响导纳输出的信噪比；
- **位置内环带宽**：决定导纳控制可实现的力响应速度上限。

工程判断：导纳控制的位置内环带宽通常需高于导纳外环带宽 5–10 倍，否则内外环耦合可能导致不稳定。

### 横向对比

**与阻抗控制对比**：阻抗控制直接生成力矩指令，响应快但要求力矩接口；导纳控制生成位置指令，兼容性好但受位置环带宽限制。**它真正改变的不是柔顺效果，而是对硬件前提的假设**——阻抗假设"有力矩接口"，导纳假设"有力传感器+位置环"。

**与混合力/位置控制对比**：混合控制通过选择矩阵决定哪些方向走位置环、哪些方向走力环，是"方向上的分工"；导纳控制则在所有方向上都建立力-运动的动态关系，是"动态上的耦合"。

**与变刚度控制对比**：变刚度控制根据任务阶段调整关节刚度，是"参数调度"；导纳控制通过调节 \(\mathbf{K}_d\) 也能实现类似效果，但本质上是"动态关系设计"。

### 谁在用·应用案例

**TALOS 人形机器人**（PAL Robotics）：身高约 175 cm，质量约 95 kg，32 个 DOF，全身关节配备力/力矩传感器，支持阻抗/导纳控制，采用 ROS 软件栈（来源：项目 Wiki 第 8 章 §8.4.11）。TALOS 是面向研究的全尺寸扭矩可控人形机器人，其导纳控制能力使其能够安全地进行人机交互与柔顺操作。

**碰撞安全反应**：在检测到碰撞后，机器人可切换到**柔顺顺应**模式——即切换到零力/导纳控制模式，让机器人顺应外力自由运动（来源：项目 Wiki 第 8 章 §8.4.11）。这是导纳控制在人形机器人安全策略中的典型应用。

**落地缓冲与不平地面顺应**：人形机器人在落地冲击吸收、不平地面顺应和跌倒保护中，常将导纳控制与 SEA/VSA 等柔顺执行器技术配合使用（来源：项目 Wiki 第 8 章 §8.4.11）。

### 局限与边界

1. **受位置控制带宽限制**：导纳控制难以实现快速力响应。位置内环的带宽决定了力响应的上限，对于需要高频力跟踪的任务（如打磨、抛光），导纳控制可能力不从心。

2. **高环境刚度下的稳定性问题**：若环境刚度极高，导纳控制器可能不稳定。这是因为高刚度环境下，微小的位置误差就会产生巨大的接触力变化，容易激发振荡。

3. **依赖力传感器质量**：导纳控制直接使用力传感器信号，传感器噪声会直接影响修正轨迹的质量。虽然导纳方程本身有一定滤波作用，但噪声过大时仍需额外滤波。

4. **不适用于纯力矩控制场景**：对于需要精确力矩输出的任务（如精密装配中的力跟踪），导纳控制的轨迹修正方式可能不够直接。

### 常见误区

1. **"导纳控制就是阻抗控制"**——错。二者是力-运动对偶关系：阻抗控制输入运动偏差、输出力；导纳控制输入力、输出运动修正。虽然数学形式相似，但物理意义与控制架构完全不同。

2. **"导纳控制比阻抗控制更先进"**——错。二者各有适用场景：导纳控制适合有位置环但无力矩接口的工业机器人；阻抗控制适合有力矩接口的先进平台。选择取决于硬件条件与任务需求。

3. **"导纳控制不需要力传感器"**——错。导纳控制的核心是"根据测得的接触力计算期望运动轨迹"，力传感器是其必要硬件前提。

4. **"导纳控制只用于人机交互"**——错。在人形机器人中，导纳控制不仅用于人机交互，也用于落地冲击吸收、不平地面顺应和跌倒保护（来源：项目 Wiki 第 8 章 §8.4.11）。

### 相关知识

- `ent_method_impedance_control` — 阻抗控制是导纳控制的"对偶"方法，输入运动偏差、输出力矩指令，二者共同构成柔顺控制的完整框架。
- `ent_subsystem_ankle_compliance` — 脚踝柔顺机构常与导纳控制配合，实现落地缓冲与不平地面顺应。
- `ent_technology_series_elastic_actuator_2024` — SEA 等柔顺执行器可与导纳控制配合，实现更优的冲击吸收与力交互。
- `ent_method_force_position_hybrid_control` — 混合力/位置控制通过选择矩阵决定方向分工，与导纳控制的动态耦合形成互补。
- `ent_paper_li_human_aware_physical_human_rob_2024` — 人机物理交互研究常以导纳控制为实现基础，验证安全交互策略。

## 参考

- [项目 Wiki 第 8 章：人形机器人设计](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [项目 Wiki 第 14 章：控制方法](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-14.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Admittance Control is the "dual" method of impedance control: it computes the desired motion trajectory based on measured contact forces, which is then tracked by a position/velocity controller. Its fundamental equation is \(\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}\). What it truly changes is not the way force is controlled, but rather the transfer of the "force-motion" coupling relationship from the torque level to the trajectory level—allowing existing high-precision position control hardware to directly participate in compliant interaction.

## Content

### What It Is: Precise Definition

Admittance control is a control strategy that **generates desired motion based on external forces**. It measures the external contact force \(\mathbf{F}_{\text{ext}}\) via a force sensor, feeds it into an admittance controller, outputs a corrected command trajectory \(\mathbf{x}_c\), and then an inner-loop position controller tracks this trajectory. Its core equation (source: Project Wiki Chapter 8 §8.4.11):

$$
\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}
$$

where \(\mathbf{x}_d\) is the desired trajectory, \(\mathbf{x}_c\) is the admittance-corrected command trajectory, \(\mathbf{F}_{\text{ext}}\) is the external contact force, and \(\mathbf{M}_d\), \(\mathbf{D}_d\), \(\mathbf{K}_d\) are the desired inertia, damping, and stiffness matrices, respectively. **The input is force, and the output is motion correction**—this is exactly the opposite of impedance control (which takes motion deviation as input and outputs force), and the two form a force-motion dual relationship.

### Why It Exists: Pain Points and Historical Positioning

Pure position control has fundamental flaws in contact scenarios: geometric errors (inaccurate models, unknown object poses) translate into **uncontrolled contact forces**—at best causing task failure, and at worst damaging the robot or injuring humans. Although impedance control solves this problem, it requires the robot to possess **torque-level control capability**, meaning it can directly send torque commands to the current loop. However, the vast majority of industrial robots and traditional position-controlled platforms do not have torque interfaces, and their high-precision position loops actually become an obstacle to achieving compliant control.

The historical positioning of admittance control lies precisely here: it **does not alter the underlying position control architecture**, but rather superimposes an "admittance layer" outside the position loop, translating force signals into position corrections. This enables:

- Direct use of existing high-precision position control hardware;
- A certain filtering effect on external force sensor noise;
- Suitability for interaction between high-stiffness industrial robots and the environment.

What it truly changes is not the control architecture, but **the deployment method of compliance capability**—allowing robots without torque interfaces to interact with the environment safely.

### Principle Breakdown

**① Admittance Equation: Mapping from Force to Trajectory**

The core of admittance control is a second-order differential equation that specifies the dynamic relationship between external forces and trajectory corrections. When an external force \(\mathbf{F}_{\text{ext}}\) acts on the end-effector, the admittance controller computes a corrected trajectory \(\mathbf{x}_c\), causing the virtual mass-damper-spring system to respond according to the desired dynamic characteristics. At steady state, the spring term dominates:

$$
\mathbf{F}_{\text{ext}} \approx \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c)
$$

This means that the larger the external force, the farther the trajectory deviates from the desired position—the robot appears more "compliant."

**② Inner/Outer Loop Architecture: Collaboration of Two Control Layers**

Admittance control employs a typical inner/outer loop architecture:

- **Outer loop (admittance loop)**: Reads force sensor signals and generates a corrected trajectory \(\mathbf{x}_c\) via the admittance equation;
- **Inner loop (position loop)**: Tracks \(\mathbf{x}_c\) and drives the actuators.

The advantage of this hierarchical structure is that the high bandwidth of the inner position loop ensures tracking accuracy, while the lower bandwidth of the outer admittance loop naturally filters out high-frequency noise from the force sensor. **What it truly changes is not control precision, but the hierarchical level of force-position coupling**—moving it from the torque level up to the trajectory level.

**③ Essential Differences from Impedance Control**

| Dimension | Impedance Control | Admittance Control |
|---|---|---|
| Input | Motion deviation (position, velocity) | External contact force |
| Output | Torque command | Corrected trajectory |
| Inner loop | Current loop/torque loop | Position loop/velocity loop |
| Hardware requirement | Torque control capability | Force sensor + position control |
| Applicable scenarios | Low-stiffness environments, direct force control | High-stiffness environments, industrial robots |

### Key Parameters and Specifications

Key parameters of admittance control include (source: Project Wiki Chapter 8 §8.4.11):

- **Desired inertia \(\mathbf{M}_d\)** (kg): Determines acceleration response, affecting dynamic behavior at the moment of contact;
- **Desired damping \(\mathbf{D}_d\)** (N·s/m): Determines the rate of energy dissipation, affecting stability and oscillation decay;
- **Desired stiffness \(\mathbf{K}_d\)** (N/m): Determines the relationship between steady-state position deviation and restoring force;
- **Force sensor range and noise**: Directly affects the signal-to-noise ratio of the admittance output;
- **Position inner-loop bandwidth**: Determines the upper limit of achievable force response speed for admittance control.

Engineering judgment: The bandwidth of the position inner loop in admittance control typically needs to be 5–10 times higher than that of the admittance outer loop; otherwise, coupling between the two loops may lead to instability.

### Horizontal Comparison

**Comparison with impedance control**: Impedance control directly generates torque commands, offering fast response but requiring a torque interface; admittance control generates position commands, offering better compatibility but limited by position-loop bandwidth. **What it truly changes is not the compliance effect, but the assumption about hardware prerequisites**—impedance assumes "a torque interface exists," while admittance assumes "a force sensor plus position loop exists."

**Comparison with hybrid force/position control**: Hybrid control uses a selection matrix to determine which directions operate under position control and which under force control—this is "division of labor by direction"; admittance control establishes a force-motion dynamic relationship in all directions—this is "coupling in dynamics."

**Comparison with variable stiffness control**: Variable stiffness control adjusts joint stiffness according to task phases—this is "parameter scheduling"; admittance control can achieve similar effects by adjusting \(\mathbf{K}_d\), but its essence is "dynamic relationship design."

### Who Uses It: Application Cases

**TALOS humanoid robot** (PAL Robotics): Approximately 175 cm tall, approximately 95 kg in mass, 32 DOF, equipped with force/torque sensors at all joints, supporting impedance/admittance control, and using the ROS software stack (source: Project Wiki Chapter 8 §8.4.11). TALOS is a full-size torque-controllable humanoid robot designed for research, and its admittance control capability enables safe human-robot interaction and compliant manipulation.

**Collision safety response**: After detecting a collision, the robot can switch to **compliant yielding** mode—that is, switching to zero-force/admittance control mode, allowing the robot to move freely in response to external forces (source: Project Wiki Chapter 8 §8.4.11). This is a typical application of admittance control in humanoid robot safety strategies.

**Landing impact absorption and uneven terrain compliance**: In landing impact absorption, uneven terrain compliance, and fall protection, humanoid robots often combine admittance control with compliant actuator technologies such as SEA/VSA (source: Project Wiki Chapter 8 §8.4.11).

### Limitations and Boundaries

1. **Limited by position control bandwidth**: Admittance control struggles to achieve fast force responses. The bandwidth of the inner position loop determines the upper limit of force response, and for tasks requiring high-frequency force tracking (e.g., grinding, polishing), admittance control may be inadequate.

2. **Stability issues under high environmental stiffness**: If the environmental stiffness is extremely high, the admittance controller may become unstable. This is because in high-stiffness environments, tiny position errors produce large contact force variations, easily exciting oscillations.

3. **Dependence on force sensor quality**: Admittance control directly uses force sensor signals, and sensor noise directly affects the quality of the corrected trajectory. Although the admittance equation itself provides some filtering, additional filtering is still needed when noise is excessive.

4. **Not suitable for pure torque control scenarios**: For tasks requiring precise torque output (such as force tracking in precision assembly), the trajectory-correction approach of admittance control may be insufficiently direct.

### Common Misconceptions

1. **"Admittance control is impedance control"**—Wrong. The two are force-motion duals: impedance control takes motion deviation as input and outputs force; admittance control takes force as input and outputs motion corrections. Although their mathematical forms are similar, their physical meanings and control architectures are completely different.

2. **"Admittance control is more advanced than impedance control"**—Wrong. Each has its own applicable scenarios: admittance control suits industrial robots with position loops but no torque interfaces; impedance control suits advanced platforms with torque interfaces. The choice depends on hardware conditions and task requirements.

3. **"Admittance control does not require a force sensor"**—Wrong. The core of admittance control is "computing the desired motion trajectory based on measured contact forces," and the force sensor is a necessary hardware prerequisite.

4. **"Admittance control is only used for human-robot interaction"**—Wrong. In humanoid robots, admittance control is used not only for human-robot interaction but also for landing impact absorption, uneven terrain compliance, and fall protection (source: Project Wiki Chapter 8 §8.4.11).

### Related Knowledge

- `ent_method_impedance_control` — Impedance control is the "dual" method of admittance control, taking motion deviation as input and outputting torque commands; together they form the complete framework of compliant control.
- `ent_subsystem_ankle_compliance` — Ankle compliance mechanisms are often combined with admittance control to achieve landing impact absorption and uneven terrain compliance.
- `ent_technology_series_elastic_actuator_2024` — Compliant actuators such as SEA can be combined with admittance control to achieve better impact absorption and force interaction.
- `ent_method_force_position_hybrid_control` — Hybrid force/position control uses a selection matrix to determine directional division of labor, complementing the dynamic coupling of admittance control.
- `ent_paper_li_human_aware_physical_human_rob_2024` — Research on physical human-robot interaction often uses admittance control as the implementation foundation to validate safe interaction strategies.

## 개요

어드미턴스 제어(Admittance Control)는 임피던스 제어의 "쌍대" 방법이다. 측정된 접촉력으로부터 기대 운동 궤적을 계산하고, 이를 위치/속도 제어기가 추종하는 방식이며, 기본 방정식은 \(\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}\)이다. 이것이 실제로 바꾸는 것은 힘 제어 방식이 아니라, "힘-운동" 결합 관계를 토크 계층에서 궤적 계층으로 옮기는 것이다. 즉, 기존의 고정밀 위치 제어 하드웨어가 직접 순응 상호작용에 참여할 수 있게 한다.

## 핵심 내용

### 무엇인가: 정확한 정의

어드미턴스 제어는 **외력에 따라 기대 운동을 생성하는** 제어 전략이다. 힘 센서로 외부 접촉력 \(\mathbf{F}_{\text{ext}}\)을 측정하고, 이를 어드미턴스 제어기에 입력하여 수정된 명령 궤적 \(\mathbf{x}_c\)을 출력한 뒤, 내부 루프 위치 제어기가 이 궤적을 추종한다. 핵심 방정식(출처: 프로젝트 Wiki 제8장 §8.4.11):

$$
\mathbf{M}_d (\ddot{\mathbf{x}}_d - \ddot{\mathbf{x}}_c) + \mathbf{D}_d (\dot{\mathbf{x}}_d - \dot{\mathbf{x}}_c) + \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c) = \mathbf{F}_{\text{ext}}
$$

여기서 \(\mathbf{x}_d\)는 기대 궤적, \(\mathbf{x}_c\)는 어드미턴스 수정 후 명령 궤적, \(\mathbf{F}_{\text{ext}}\)는 외부 접촉력, \(\mathbf{M}_d\), \(\mathbf{D}_d\), \(\mathbf{K}_d\)는 각각 기대 관성, 감쇠, 강성 행렬이다. **입력은 힘, 출력은 운동 수정량**이다. 이는 임피던스 제어(입력은 운동 편차, 출력은 힘)와 정반대이며, 둘은 힘-운동 쌍대 관계를 이룬다.

### 왜 존재하는가: 문제점과 역사적 위치

순수 위치 제어는 접촉 상황에서 본질적 결함이 있다. 기하 오차(모델 부정확, 물체 자세 미지)가 **제어되지 않는 접촉력**으로 변환된다. 가벼우면 작업 실패, 심하면 로봇 손상이나 인명 피해로 이어진다. 임피던스 제어는 이 문제를 해결하지만, 로봇이 **토크 수준 제어 능력**, 즉 전류 루프에 직접 토크 명령을 보낼 수 있는 능력을 요구한다. 그러나 대부분의 산업용 로봇과 전통적 위치 제어 플랫폼은 토크 인터페이스가 없으며, 오히려 고정밀 위치 루프가 순응 제어 구현의 장애물이 된다.

어드미턴스 제어의 역사적 위치는 바로 여기에 있다. **기저 위치 제어 아키텍처를 바꾸지 않고**, 위치 루프 외부에 "어드미턴스 계층"을 추가하여 힘 신호를 위치 수정량으로 변환한다. 이를 통해:

- 기존 고정밀 위치 제어 하드웨어를 직접 활용 가능;
- 외부 힘 센서 잡음에 대한 일정한 필터링 효과 제공;
- 강성이 높은 산업용 로봇과 환경 상호작용에 적합.

이것이 실제로 바꾸는 것은 제어 아키텍처가 아니라, **순응 능력의 배치 방식**이다. 즉, 토크 인터페이스가 없는 로봇도 안전하게 환경과 상호작용할 수 있게 한다.

### 원리 분해

**① 어드미턴스 방정식: 힘에서 궤적으로의 매핑**

어드미턴스 제어의 핵심은 2차 미분 방정식으로, 외력과 궤적 수정량 사이의 동적 관계를 규정한다. 외력 \(\mathbf{F}_{\text{ext}}\)이 말단에 작용할 때, 어드미턴스 제어기는 수정 궤적 \(\mathbf{x}_c\)을 계산하여 가상 질량-감쇠-스프링 시스템의 응답이 기대 동적 특성을 따르게 한다. 정상 상태에서는 스프링 항이 지배적이다:

$$
\mathbf{F}_{\text{ext}} \approx \mathbf{K}_d (\mathbf{x}_d - \mathbf{x}_c)
$$

이는 외력이 클수록 궤적이 기대 위치에서 더 멀리 벗어난다는 뜻이며, 로봇이 더 "순응적"으로 동작함을 의미한다.

**② 내/외부 루프 아키텍처: 두 계층 루프의 협력**

어드미턴스 제어는 전형적인 내/외부 루프 아키텍처를 사용한다:

- **외부 루프(어드미턴스 루프)**: 힘 센서 신호를 읽고, 어드미턴스 방정식을 통해 수정 궤적 \(\mathbf{x}_c\)을 생성;
- **내부 루프(위치 루프)**: \(\mathbf{x}_c\)을 추종하며 액추에이터를 구동.

이 계층 구조의 장점은 위치 내부 루프의 높은 대역폭이 추종 정밀도를 보장하고, 어드미턴스 외부 루프의 낮은 대역폭이 힘 센서의 고주파 잡음을 자연스럽게 필터링한다는 점이다. **이것이 실제로 바꾸는 것은 제어 정밀도가 아니라, 힘-위치 결합의 계층 위치**이다. 즉, 토크 계층에서 궤적 계층으로 이동한다.

**③ 임피던스 제어와의 본질적 차이**

| 차원 | 임피던스 제어 | 어드미턴스 제어 |
|---|---|---|
| 입력 | 운동 편차(위치, 속도) | 외부 접촉력 |
| 출력 | 토크 명령 | 수정 궤적 |
| 내부 루프 | 전류 루프/토크 루프 | 위치 루프/속도 루프 |
| 하드웨어 요구 | 토크 제어 능력 | 힘 센서 + 위치 제어 |
| 적용 시나리오 | 저강성 환경, 직접 힘 제어 | 고강성 환경, 산업용 로봇 |

### 핵심 파라미터와 사양

어드미턴스 제어의 핵심 파라미터는 다음과 같다(출처: 프로젝트 Wiki 제8장 §8.4.11):

- **기대 관성 \(\mathbf{M}_d\)** (kg): 가속도 응답을 결정하며, 접촉 순간의 동적 거동에 영향;
- **기대 감쇠 \(\mathbf{D}_d\)** (N·s/m): 에너지 소산 속도를 결정하며, 안정성과 진동 감쇠에 영향;
- **기대 강성 \(\mathbf{K}_d\)** (N/m): 정상 상태 위치 편차와 복원력 관계를 결정;
- **힘 센서 측정 범위와 잡음**: 어드미턴스 출력의 신호 대 잡음비에 직접 영향;
- **위치 내부 루프 대역폭**: 어드미턴스 제어가 구현 가능한 힘 응답 속도의 상한을 결정.

공학적 판단: 어드미턴스 제어의 위치 내부 루프 대역폭은 일반적으로 어드미턴스 외부 루프 대역폭보다 5–10배 높아야 하며, 그렇지 않으면 내/외부 루프 결합으로 불안정해질 수 있다.

### 수평 비교

**임피던스 제어와 비교**: 임피던스 제어는 토크 명령을 직접 생성하여 응답이 빠르지만 토크 인터페이스가 필요하다. 어드미턴스 제어는 위치 명령을 생성하여 호환성이 좋지만 위치 루프 대역폭에 제한을 받는다. **이것이 실제로 바꾸는 것은 순응 효과가 아니라, 하드웨어 전제 조건에 대한 가정**이다. 임피던스는 "토크 인터페이스 존재"를 가정하고, 어드미턴스는 "힘 센서 + 위치 루프 존재"를 가정한다.

**혼합 힘/위치 제어와 비교**: 혼합 제어는 선택 행렬을 통해 어떤 방향은 위치 루프, 어떤 방향은 힘 루프로 동작할지 결정하며, "방향의 분업"이다. 어드미턴스 제어는 모든 방향에서 힘-운동의 동적 관계를 구축하며, "동적 결합"이다.

**가변 강성 제어와 비교**: 가변 강성 제어는 작업 단계에 따라 관절 강성을 조정하며, "파라미터 스케줄링"이다. 어드미턴스 제어는 \(\mathbf{K}_d\)를 조절하여 유사한 효과를 낼 수 있지만, 본질적으로는 "동적 관계 설계"이다.

### 누가 사용하는가·응용 사례

**TALOS 휴머노이드 로봇** (PAL Robotics): 키 약 175 cm, 질량 약 95 kg, 32 DOF, 전신 관절에 힘/토크 센서 장착, 임피던스/어드미턴스 제어 지원, ROS 소프트웨어 스택 사용(출처: 프로젝트 Wiki 제8장 §8.4.11). TALOS는 연구용 전신 토크 제어 가능 휴머노이드 로봇으로, 어드미턴스 제어 능력을 통해 안전한 인간-로봇 상호작용과 순응 조작이 가능하다.

**충돌 안전 반응**: 충돌 감지 후 로봇은 **순응 대응** 모드로 전환할 수 있다. 즉, 제로 힘/어드미턴스 제어 모드로 전환하여 로봇이 외력에 따라 자유롭게 움직이게 한다(출처: 프로젝트 Wiki 제8장 §8.4.11). 이는 휴머노이드 로봇 안전 전략에서 어드미턴스 제어의 전형적 응용이다.

**착지 완충과 불평탄 지면 순응**: 휴머노이드 로봇은 착지 충격 흡수, 불평탄 지면 순응, 낙상 보호에서 어드미턴스 제어를 SEA/VSA 등의 순응 액추에이터 기술과 함께 사용하는 경우가 많다(출처: 프로젝트 Wiki 제8장 §8.4.11).

### 한계와 경계

1. **위치 제어 대역폭 제한**: 어드미턴스 제어는 빠른 힘 응답을 구현하기 어렵다. 위치 내부 루프의 대역폭이 힘 응답의 상한을 결정하므로, 고주파 힘 추종이 필요한 작업(연삭, 연마 등)에서는 어드미턴스 제어가 역부족일 수 있다.

2. **고강성 환경에서의 안정성 문제**: 환경 강성이 매우 높으면 어드미턴스 제어기가 불안정해질 수 있다. 고강성 환경에서는 미세한 위치 오차가 큰 접촉력 변화를 유발하여 진동을 쉽게 유발하기 때문이다.

3. **힘 센서 품질 의존성**: 어드미턴스 제어는 힘 센서 신호를 직접 사용하므로, 센서 잡음이 수정 궤적의 품질에 직접 영향을 미친다. 어드미턴스 방정식 자체에 일정한 필터링 효과가 있지만, 잡음이 과도하면 추가 필터링이 필요하다.

4. **순수 토크 제어 시나리오에는 부적합**: 정밀한 토크 출력이 필요한 작업(정밀 조립의 힘 추종 등)에서는 어드미턴스 제어의 궤적 수정 방식이 충분히 직접적이지 않을 수 있다.

### 흔한 오해

1. **"어드미턴스 제어는 임피던스 제어다"** — 틀렸다. 둘은 힘-운동 쌍대 관계이다. 임피던스 제어는 운동 편차를 입력으로, 힘을 출력으로 한다. 어드미턴스 제어는 힘을 입력으로, 운동 수정을 출력으로 한다. 수학적 형태는 유사하지만, 물리적 의미와 제어 아키텍처는 완전히 다르다.

2. **"어드미턴스 제어가 임피던스 제어보다 더 진보적이다"** — 틀렸다. 둘은 각각 적용 시나리오가 있다. 어드미턴스 제어는 위치 루프가 있지만 토크 인터페이스가 없는 산업용 로봇에 적합하고, 임피던스 제어는 토크 인터페이스가 있는 고급 플랫폼에 적합하다. 선택은 하드웨어 조건과 작업 요구에 달려 있다.

3. **"어드미턴스 제어는 힘 센서가 필요 없다"** — 틀렸다. 어드미턴스 제어의 핵심은 "측정된 접촉력으로 기대 운동 궤적을 계산하는 것"이며, 힘 센서는 필수 하드웨어 전제 조건이다.

4. **"어드미턴스 제어는 인간-로봇 상호작용에만 사용된다"** — 틀렸다. 휴머노이드 로봇에서 어드미턴스 제어는 인간-로봇 상호작용뿐만 아니라 착지 충격 흡수, 불평탄 지면 순응, 낙상 보호에도 사용된다(출처: 프로젝트 Wiki 제8장 §8.4.11).

### 관련 지식

- `ent_method_impedance_control` — 임피던스 제어는 어드미턴스 제어의 "쌍대" 방법으로, 운동 편차를 입력으로, 토크 명령을 출력으로 하며, 둘은 함께 순응 제어의 완전한 프레임워크를 구성한다.
- `ent_subsystem_ankle_compliance` — 발목 순응 메커니즘은 종종 어드미턴스 제어와 함께 사용되어 착지 완충과 불평탄 지면 순응을 구현한다.
- `ent_technology_series_elastic_actuator_2024` — SEA 등의 순응 액추에이터는 어드미턴스 제어와 함께 사용되어 더 우수한 충격 흡수와 힘 상호작용을 구현할 수 있다.
- `ent_method_force_position_hybrid_control` — 혼합 힘/위치 제어는 선택 행렬을 통해 방향 분업을 결정하며, 어드미턴스 제어의 동적 결합과 상호 보완적이다.
- `ent_paper_li_human_aware_physical_human_rob_2024` — 인간-로봇 물리적 상호작용 연구는 종종 어드미턴스 제어를 구현 기반으로 사용하여 안전한 상호작용 전략을 검증한다.
