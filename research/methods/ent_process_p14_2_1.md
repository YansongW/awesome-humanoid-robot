---
$id: ent_process_p14_2_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Data recording and playback
  zh: 数据记录与回放
  ko: 데이터 기록 및 재생
summary:
  en: Data recording scheme, playback consistency verification
  zh: '- P14.2.1.1 输入梳理与目标量化 - 整理「数据记录与回放」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 数据记录方案、回放一致性验证
domains:
- 08_software_middleware
layers:
- intelligence
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

数据记录与回放（Data recording and playback）是软件中间件与系统集成阶段（P14）中工具链与 DevOps 工作包（P14.2）下的关键工序节点，其核心任务是利用 rosbag2、结构化日志、数据湖与数据版本管理等工具，将机器人运行过程中的传感器流、控制指令与系统状态完整捕获并结构化存储，以便后续进行问题复现、模型训练与性能分析。它真正改变的不是数据存储本身，而是整个开发流程从"出了问题靠现场复现"到"任何时刻都能回到任意历史现场"的工程可回溯能力。

## 核心内容

### 是什么：准确定义

数据记录与回放是机器人软件开发中一项基础性的工程能力，指在系统运行期间持续采集多源异构数据（包括关节状态、IMU 读数、相机图像、控制指令、日志事件等），以时间戳为索引进行同步存储，并在事后按需精确重放这些数据流，使开发人员能够脱离真实硬件环境，在仿真或离线分析中还原当时的系统行为。

在 WBS V3 的工序分解中，该节点（P14.2.1）向下展开为五个四级子任务：P14.2.1.1 输入梳理与目标量化、P14.2.1.2 方案/方法设计、P14.2.1.3 实施/原型/样件制作、P14.2.1.4 验证与问题闭环、P14.2.1.5 文档输出与下游交付。每个子任务遵循统一的工程动作模板，确保从需求到交付的每一步都有明确的验收标准与责任归属。

### 为什么存在：痛点与历史定位

在人形机器人开发的早期阶段，调试往往依赖"现场复现"——让机器人在同样的场景下再跑一遍，观察是否重现故障。这种方式存在三个根本性缺陷：**不可复现性**——机器人系统的状态受噪声、磨损、环境扰动影响，同一段代码在不同时刻运行的结果可能完全不同；**低效性**——每次复现都需要完整的硬件平台和现场环境，迭代周期以天计；**不可追溯性**——即使问题复现了，也无法回答"上一次运行时控制器的中间变量到底是什么"这类问题。

数据记录与回放的出现，将调试范式从"现场复现"转变为"离线回放"。开发人员可以在故障发生后，从数据湖中调取当时的完整数据流，在仿真环境中逐帧回放，观察每一个中间变量的变化轨迹。这一转变使得问题定位的时间从数天缩短到数小时，也使得基于真实数据的模型训练与性能回归分析成为可能。

### 原理拆解

**① 时间同步：多源数据对齐的基石**

人形机器人通常搭载数十个传感器，每个传感器有自己的采样频率与时钟源。数据记录系统需要为每条数据打上统一的时间戳，并在回放时按照时间轴对齐所有数据流。rosbag2 采用 ROS 的 `/clock` 机制与 TF 树配合，实现多传感器数据的硬件时间同步。对于跨主机的数据采集，则依赖 PTP（精确时间协议）或 NTP 进行时钟同步，同步精度直接影响回放时数据关联的准确性。

**② 存储架构：从环形缓冲区到数据湖**

实时记录要求写入延迟极低，而长期存储要求容量可扩展。工程上通常采用两级架构：前端使用环形缓冲区（ring buffer）在内存中暂存最近一段时间的数据，满足"崩溃前最后 N 秒"的现场保留需求；后端通过异步写入将数据落盘到分布式存储或数据湖中，满足长期归档与批量分析需求。两级之间的数据搬运由后台线程完成，避免阻塞实时采集路径。

**③ 回放机制：时间轴驱动的确定性重演**

回放的核心挑战在于**确定性**——每次回放必须产生完全一致的数据序列，才能保证问题可复现。为此，回放器以记录时的时间戳为基准，按比例缩放或原速推进虚拟时钟，所有订阅者根据虚拟时钟消费数据。对于需要与仿真环境联动的场景，回放器还支持"步进模式"——每收到一个外部触发信号才推进一帧数据，便于逐帧检查状态变化。

### 关键参数与规格

| 参数 | 典型规格 | 说明 |
|------|----------|------|
| 记录带宽 | 需自行确认 | 取决于传感器数量与分辨率，多相机系统可达数百 MB/s |
| 存储容量 | 需自行确认 | 单次实验数据量从数 GB 到数 TB 不等 |
| 时间戳精度 | 需自行确认 | 硬件同步可达微秒级，软件同步为毫秒级 |
| 回放速度比 | 0.1× – 2.0× | 支持慢放与快进，便于观察细节或加速回归 |
| 数据压缩率 | 需自行确认 | 无损压缩通常为 2–5 倍，有损压缩更高但损失精度 |

### 横向对比

| 方案 | 优势 | 劣势 | 适用场景 |
|------|------|------|----------|
| rosbag2 | 与 ROS 生态原生集成，支持话题级录制与回放 | 数据格式绑定 ROS，跨框架复用需转换 | ROS/ROS2 开发环境 |
| 结构化日志 | 文本可读性强，便于 grep 与告警关联 | 不适合高频二进制数据（如图像、点云） | 系统事件与状态监控 |
| 数据湖 | 容量弹性扩展，支持 SQL 分析与机器学习 | 写入延迟高，不适合实时采集 | 长期归档与批量分析 |
| 数据版本管理 | 支持数据血缘追踪与版本回滚 | 存储开销大，需配套元数据管理 | 数据集管理与模型训练 |

### 谁在用·应用案例

在 P14.2.1 的工序定义中，该节点的应用贯穿整个软件中间件与系统集成阶段。典型使用场景包括：

- **问题复现**：当机器人现场出现跌倒、抖动或通信超时等异常时，开发人员从数据湖中调取故障时间窗口的数据，在仿真环境中回放，定位是感知延迟、控制参数还是通信丢包导致的问题。
- **模型训练**：将真实运行中记录的传感器数据与人类示教数据整理为训练集，用于强化学习策略或模仿学习模型的离线训练与评估。
- **性能分析**：通过回放历史数据，对比不同版本算法在同一输入下的输出差异，量化性能退化或改进幅度，支撑版本迭代决策。

### 局限与边界

数据记录与回放并非万能，其核心局限在于**记录的是"发生了什么"，而非"为什么发生"**。回放只能还原传感器输入与系统输出，无法还原硬件层面的物理过程（如电机温度变化、机械磨损）。此外，该节点受三个关键约束制约：**存储容量**——长时间运行会产生海量数据，必须设计分级存储与过期策略；**带宽**——高频传感器数据的实时写入可能成为系统瓶颈，需在采集端做降采样或压缩；**隐私**——若机器人部署在公共或家庭环境，记录的图像与音频可能涉及隐私合规问题，需在记录前进行脱敏处理。

### 常见误区

1. **"记录得越多越好"**——错。无差别记录所有话题会迅速耗尽存储与带宽，且让后续检索变得困难。正确的做法是根据问题域选择关键话题，并设计合理的采样率与压缩策略。
2. **"回放数据等同于真实运行"**——错。回放只能还原数据层面的输入输出，无法还原硬件时序、中断延迟等物理特性。对于依赖实时性的问题，回放结果只能作为参考，不能替代真机验证。
3. **"数据记录是事后补救"**——错。数据记录方案应在系统设计阶段就纳入架构，包括话题命名规范、时间戳策略、存储容量规划等。事后补录往往因缺少关键数据而无法定位问题。

### 相关知识

- `ent_process_p14` — 本节点所属的软件中间件与系统集成阶段，定义了工具链与 DevOps 的整体框架与上下游衔接关系。
- `ent_process_p14_2_2` — 同族相邻工序：OTA、诊断与健康管理（同一工作包内的并行工序）。
- `ent_process_p14_2_3` — 同族相邻工序：CI/CD 与仿真测试链（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Data recording and playback is a critical process node under the Toolchain and DevOps work package (P14.2) in the software middleware and system integration phase (P14) of the development lifecycle. Its core task is to leverage tools such as rosbag2, structured logging, data lakes, and data version management to fully capture and structurally store sensor streams, control commands, and system states during robot operation, enabling subsequent problem reproduction, model training, and performance analysis. What it truly changes is not data storage itself, but the engineering traceability capability of the entire development process—shifting from "reproducing issues on-site" to "being able to return to any historical state at any time."

## Content

### What It Is: Precise Definition

Data recording and playback is a foundational engineering capability in robot software development. It refers to the continuous acquisition of multi-source heterogeneous data during system operation (including joint states, IMU readings, camera images, control commands, log events, etc.), synchronized storage indexed by timestamps, and precise on-demand replay of these data streams afterward, allowing developers to decouple from real hardware environments and reconstruct system behavior in simulation or offline analysis.

In the WBS V3 work breakdown, this node (P14.2.1) expands downward into five level-4 subtasks: P14.2.1.1 Input Analysis and Target Quantification, P14.2.1.2 Solution/Method Design, P14.2.1.3 Implementation/Prototype/Sample Fabrication, P14.2.1.4 Verification and Issue Closure, and P14.2.1.5 Documentation Output and Downstream Delivery. Each subtask follows a unified engineering action template, ensuring clear acceptance criteria and accountability at every step from requirements to delivery.

### Why It Exists: Pain Points and Historical Positioning

In the early stages of humanoid robot development, debugging often relied on "on-site reproduction"—running the robot again in the same scenario to observe whether the fault recurred. This approach has three fundamental flaws: **Non-reproducibility**—robot system states are affected by noise, wear, and environmental disturbances, so the same code may produce different results at different times; **Inefficiency**—each reproduction requires the full hardware platform and on-site environment, with iteration cycles measured in days; **Non-traceability**—even if a problem is reproduced, it is impossible to answer questions like "what exactly was the controller's intermediate variable during the last run."

The advent of data recording and playback shifted the debugging paradigm from "on-site reproduction" to "offline replay." After a fault occurs, developers can retrieve the complete data stream from the data lake, replay it frame by frame in a simulation environment, and observe the trajectory of every intermediate variable. This shift reduces problem localization time from days to hours and makes model training and performance regression analysis based on real data feasible.

### Principle Breakdown

**① Time Synchronization: The Foundation for Multi-Source Data Alignment**

Humanoid robots typically carry dozens of sensors, each with its own sampling frequency and clock source. The data recording system must stamp each data item with a unified timestamp and align all data streams along the time axis during replay. rosbag2 leverages ROS's `/clock` mechanism in conjunction with the TF tree to achieve hardware time synchronization across multi-sensor data. For cross-host data acquisition, clock synchronization relies on PTP (Precision Time Protocol) or NTP, and synchronization precision directly affects the accuracy of data association during replay.

**② Storage Architecture: From Ring Buffers to Data Lakes**

Real-time recording demands extremely low write latency, while long-term storage requires scalable capacity. Engineering practice typically employs a two-tier architecture: the front end uses a ring buffer in memory to temporarily hold recent data, satisfying the "last N seconds before crash" on-site retention requirement; the back end asynchronously writes data to distributed storage or a data lake, satisfying long-term archiving and batch analysis needs. Data transfer between the two tiers is handled by background threads to avoid blocking the real-time acquisition path.

**③ Replay Mechanism: Time-Axis-Driven Deterministic Reenactment**

The core challenge of replay lies in **determinism**—each replay must produce an identical data sequence to ensure problem reproducibility. To achieve this, the replay engine uses the recorded timestamps as the reference, advancing the virtual clock at a scaled ratio or original speed, with all subscribers consuming data according to the virtual clock. For scenarios requiring integration with simulation environments, the replay engine also supports "step mode"—advancing one frame of data only upon receiving an external trigger signal, facilitating frame-by-frame inspection of state changes.

### Key Parameters and Specifications

| Parameter | Typical Specification | Description |
|-----------|-----------------------|-------------|
| Recording bandwidth | To be confirmed | Depends on sensor count and resolution; multi-camera systems can reach hundreds of MB/s |
| Storage capacity | To be confirmed | Single experiment data volume ranges from several GB to several TB |
| Timestamp precision | To be confirmed | Hardware synchronization can reach microsecond level; software synchronization is millisecond level |
| Replay speed ratio | 0.1× – 2.0× | Supports slow motion and fast forward for detail inspection or accelerated regression |
| Data compression ratio | To be confirmed | Lossless compression typically achieves 2–5×; lossy compression achieves higher but sacrifices precision |

### Horizontal Comparison

| Solution | Advantages | Disadvantages | Applicable Scenarios |
|----------|------------|---------------|----------------------|
| rosbag2 | Native integration with the ROS ecosystem; supports topic-level recording and replay | Data format is tied to ROS; cross-framework reuse requires conversion | ROS/ROS2 development environments |
| Structured logging | High text readability; convenient for grep and alert correlation | Unsuitable for high-frequency binary data (e.g., images, point clouds) | System events and status monitoring |
| Data lake | Elastic capacity scaling; supports SQL analysis and machine learning | High write latency; unsuitable for real-time acquisition | Long-term archiving and batch analysis |
| Data version management | Supports data lineage tracking and version rollback | High storage overhead; requires accompanying metadata management | Dataset management and model training |

### Who Uses It: Application Cases

In the process definition of P14.2.1, the application of this node spans the entire software middleware and system integration phase. Typical usage scenarios include:

- **Problem Reproduction**: When anomalies such as falls, jitter, or communication timeouts occur on-site, developers retrieve data from the fault time window in the data lake, replay it in a simulation environment, and determine whether the issue stems from perception latency, control parameters, or communication packet loss.
- **Model Training**: Real-world recorded sensor data and human demonstration data are organized into training sets for offline training and evaluation of reinforcement learning policies or imitation learning models.
- **Performance Analysis**: By replaying historical data, the output differences of different algorithm versions under identical inputs are compared, quantifying performance degradation or improvement to support version iteration decisions.

### Limitations and Boundaries

Data recording and playback is not a panacea; its core limitation is that it records "what happened," not "why it happened." Replay can only reconstruct sensor inputs and system outputs, not physical processes at the hardware level (such as motor temperature changes or mechanical wear). Additionally, this node is constrained by three key factors: **Storage capacity**—long-duration operation generates massive data, requiring tiered storage and expiration policies; **Bandwidth**—real-time writing of high-frequency sensor data can become a system bottleneck, necessitating downsampling or compression at the acquisition end; **Privacy**—if robots are deployed in public or home environments, recorded images and audio may raise privacy compliance concerns, requiring desensitization before recording.

### Common Misconceptions

1. **"The more data recorded, the better"**—Wrong. Indiscriminately recording all topics rapidly exhausts storage and bandwidth and makes subsequent retrieval difficult. The correct approach is to select key topics based on the problem domain and design reasonable sampling rates and compression strategies.
2. **"Replayed data is equivalent to real operation"**—Wrong. Replay can only reconstruct data-level inputs and outputs, not hardware timing, interrupt latency, or other physical characteristics. For real-time-dependent issues, replay results serve only as a reference and cannot replace verification on actual hardware.
3. **"Data recording is a post-hoc remedy"**—Wrong. Data recording solutions should be incorporated into the architecture during the system design phase, including topic naming conventions, timestamp strategies, and storage capacity planning. Retroactive recording often fails to localize problems due to missing critical data.

### Related Knowledge

- `ent_process_p14` — The software middleware and system integration phase to which this node belongs; defines the overall framework of the toolchain and DevOps and its upstream/downstream relationships.
- `ent_process_p14_2_2` — Adjacent process in the same family: OTA, diagnostics, and health management (parallel process within the same work package).
- `ent_process_p14_2_3` — Adjacent process in the same family: CI/CD and simulation testing chain (parallel process within the same work package).

## 개요

데이터 기록 및 재생(Data recording and playback)은 소프트웨어 미들웨어 및 시스템 통합 단계(P14)에서 도구 체인 및 DevOps 작업 패키지(P14.2)에 속하는 핵심 공정 노드로, 그 핵심 임무는 rosbag2, 구조화 로그, 데이터 레이크 및 데이터 버전 관리 등의 도구를 활용하여 로봇 실행 과정에서의 센서 스트림, 제어 명령 및 시스템 상태를 완전히 캡처하고 구조화된 방식으로 저장하여, 이후 문제 재현, 모델 훈련 및 성능 분석을 가능하게 하는 것입니다. 이는 데이터 저장 자체를 바꾸는 것이 아니라, 전체 개발 프로세스를 "문제 발생 시 현장 재현"에서 "언제든지 임의의 과거 현장으로 복귀할 수 있는" 엔지니어링 추적 가능성으로 전환하는 데 진정한 의미가 있습니다.

## 핵심 내용

### 무엇인가: 정확한 정의

데이터 기록 및 재생은 로봇 소프트웨어 개발에서 기본적인 엔지니어링 역량으로, 시스템 실행 중 다중 소스 이기종 데이터(관절 상태, IMU 판독값, 카메라 이미지, 제어 명령, 로그 이벤트 등)를 지속적으로 수집하고, 타임스탬프를 인덱스로 사용하여 동기화 저장하며, 사후에 필요에 따라 이러한 데이터 스트림을 정밀하게 재생하여 개발자가 실제 하드웨어 환경에서 벗어나 시뮬레이션이나 오프라인 분석에서 당시의 시스템 동작을 복원할 수 있게 하는 것을 의미합니다.

WBS V3의 공정 분해에서 이 노드(P14.2.1)는 아래로 다섯 개의 4단계 하위 작업으로 확장됩니다: P14.2.1.1 입력 정리 및 목표 정량화, P14.2.1.2 방안/방법 설계, P14.2.1.3 구현/프로토타입/시제품 제작, P14.2.1.4 검증 및 문제 폐루프, P14.2.1.5 문서 출력 및 하류 인계. 각 하위 작업은 통일된 엔지니어링 작업 템플릿을 따르며, 요구사항부터 인도까지 각 단계마다 명확한 검수 기준과 책임 소재가 보장됩니다.

### 왜 존재하는가:痛点 및 역사적 위치

휴머노이드 로봇 개발 초기 단계에서 디버깅은 종종 "현장 재현"에 의존했습니다 — 로봇을 동일한 시나리오에서 다시 실행하여 결함이 재현되는지 관찰하는 방식입니다. 이 방식에는 세 가지 근본적인 결함이 있습니다: **비재현성** — 로봇 시스템의 상태는 노이즈, 마모, 환경 교란의 영향을 받아 동일한 코드라도 실행 시점에 따라 결과가 완전히 다를 수 있습니다; **비효율성** — 재현할 때마다 완전한 하드웨어 플랫폼과 현장 환경이 필요하여 반복 주기가 며칠 단위입니다; **추적 불가능성** — 문제가 재현되더라도 "지난 실행에서 컨트롤러의 중간 변수가 정확히 무엇이었는가"와 같은 질문에 답할 수 없습니다.

데이터 기록 및 재생의 등장은 디버깅 패러다임을 "현장 재현"에서 "오프라인 재생"으로 전환했습니다. 개발자는 결함 발생 후 데이터 레이크에서 당시의 완전한 데이터 스트림을 불러와 시뮬레이션 환경에서 프레임별로 재생하며 각 중간 변수의 변화 궤적을 관찰할 수 있습니다. 이러한 전환은 문제 위치 파악 시간을 며칠에서 몇 시간으로 단축시켰고, 실제 데이터 기반의 모델 훈련과 성능 회귀 분석도 가능하게 했습니다.

### 원리 분해

**① 시간 동기화: 다중 소스 데이터 정렬의 기초**

휴머노이드 로봇은 일반적으로 수십 개의 센서를 탑재하며, 각 센서는 고유한 샘플링 주파수와 클록 소스를 가집니다. 데이터 기록 시스템은 각 데이터에 통일된 타임스탬프를 부여하고, 재생 시 시간 축을 기준으로 모든 데이터 스트림을 정렬해야 합니다. rosbag2는 ROS의 `/clock` 메커니즘과 TF 트리를 결합하여 다중 센서 데이터의 하드웨어 시간 동기화를 구현합니다. 호스트 간 데이터 수집의 경우 PTP(정밀 시간 프로토콜) 또는 NTP를 사용한 클록 동기화에 의존하며, 동기화 정밀도는 재생 시 데이터 연관의 정확성에 직접적인 영향을 미칩니다.

**② 저장 아키텍처: 링 버퍼에서 데이터 레이크까지**

실시간 기록은 매우 낮은 쓰기 지연을 요구하는 반면, 장기 저장은 용량 확장성을 요구합니다. 엔지니어링에서는 일반적으로 2단계 아키텍처를 채택합니다: 프런트엔드는 링 버퍼를 사용하여 메모리에 최근 데이터를 임시 저장하여 "충돌 직전 마지막 N초"의 현장 보존 요구를 충족합니다; 백엔드는 비동기 쓰기를 통해 데이터를 분산 저장소 또는 데이터 레이크에 디스크로 기록하여 장기 보관 및 일괄 분석 요구를 충족합니다. 두 단계 간의 데이터 이동은 백그라운드 스레드가 수행하여 실시간 수집 경로를 차단하지 않도록 합니다.

**③ 재생 메커니즘: 시간 축 기반의 결정적 재연**

재생의 핵심 과제는 **결정성**입니다 — 매번 재생 시 완전히 동일한 데이터 시퀀스를 생성해야 문제 재현이 보장됩니다. 이를 위해 재생기는 기록 시의 타임스탬프를 기준으로 비율을 조정하거나 원래 속도로 가상 클록을 진행시키며, 모든 구독자는 가상 클록에 따라 데이터를 소비합니다. 시뮬레이션 환경과 연동해야 하는 시나리오의 경우, 재생기는 "스텝 모드"도 지원합니다 — 외부 트리거 신호를 받을 때마다 한 프레임의 데이터를 진행시켜 프레임별 상태 변화를 검사하기 쉽게 합니다.

### 핵심 매개변수 및 사양

| 매개변수 | 일반 사양 | 설명 |
|------|----------|------|
| 기록 대역폭 | 직접 확인 필요 | 센서 수와 해상도에 따라 달라지며, 다중 카메라 시스템은 수백 MB/s에 달할 수 있음 |
| 저장 용량 | 직접 확인 필요 | 단일 실험 데이터량은 수 GB에서 수 TB까지 다양 |
| 타임스탬프 정밀도 | 직접 확인 필요 | 하드웨어 동기화는 마이크로초 수준, 소프트웨어 동기화는 밀리초 수준 |
| 재생 속도 비율 | 0.1× – 2.0× | 슬로우 재생과 빨리 감기를 지원하여 세부 관찰 또는 회귀 가속화 |
| 데이터 압축률 | 직접 확인 필요 | 무손실 압축은 일반적으로 2–5배, 손실 압축은 더 높지만 정밀도 손실 |

### 수평 비교

| 방안 | 장점 | 단점 | 적용 시나리오 |
|------|------|------|----------|
| rosbag2 | ROS 생태계와 네이티브 통합, 토픽 수준 기록 및 재생 지원 | 데이터 형식이 ROS에 바인딩되어 프레임워크 간 재사용 시 변환 필요 | ROS/ROS2 개발 환경 |
| 구조화 로그 | 텍스트 가독성이 높아 grep 및 알람 연관에 용이 | 고주파 바이너리 데이터(이미지, 포인트 클라우드 등)에 부적합 | 시스템 이벤트 및 상태 모니터링 |
| 데이터 레이크 | 용량 탄력적 확장, SQL 분석 및 머신러닝 지원 | 쓰기 지연이 높아 실시간 수집에 부적합 | 장기 보관 및 일괄 분석 |
| 데이터 버전 관리 | 데이터 혈통 추적 및 버전 롤백 지원 | 저장 오버헤드가 크고 메타데이터 관리 필요 | 데이터셋 관리 및 모델 훈련 |

### 사용자·응용 사례

P14.2.1의 공정 정의에서 이 노드의 응용은 소프트웨어 미들웨어 및 시스템 통합 단계 전체에 걸쳐 있습니다. 대표적인 사용 시나리오는 다음과 같습니다:

- **문제 재현**: 로봇 현장에서 낙상, 떨림 또는 통신 타임아웃 등의 이상이 발생하면 개발자는 데이터 레이크에서 결함 시간 창의 데이터를 불러와 시뮬레이션 환경에서 재생하여 인식 지연, 제어 매개변수 또는 통신 패킷 손실 중 무엇이 원인인지 파악합니다.
- **모델 훈련**: 실제 실행에서 기록된 센서 데이터와 인간 시연 데이터를 훈련 세트로 정리하여 강화 학습 정책 또는 모방 학습 모델의 오프라인 훈련 및 평가에 사용합니다.
- **성능 분석**: 과거 데이터를 재생하여 동일한 입력에서 서로 다른 버전의 알고리즘 출력 차이를 비교하고, 성능 저하 또는 개선 폭을 정량화하여 버전 반복 결정을 지원합니다.

### 한계 및 경계

데이터 기록 및 재생은 만능이 아니며, 핵심 한계는 **"무엇이 발생했는가"를 기록할 뿐 "왜 발생했는가"는 기록하지 않는다는 점**입니다. 재생은 센서 입력과 시스템 출력만 복원할 수 있으며, 하드웨어 수준의 물리적 과정(모터 온도 변화, 기계적 마모 등)은 복원할 수 없습니다. 또한 이 노드는 세 가지 핵심 제약에 의해 제한됩니다: **저장 용량** — 장시간 실행 시 방대한 데이터가 생성되므로 계층적 저장 및 만료 정책을 설계해야 합니다; **대역폭** — 고주파 센서 데이터의 실시간 쓰기는 시스템 병목이 될 수 있어 수집 측에서 다운샘플링 또는 압축이 필요합니다; **프라이버시** — 로봇이 공공 또는 가정 환경에 배포된 경우 기록된 이미지와 오디오가 프라이버시 규정 문제를 야기할 수 있으므로 기록 전에 비식별화 처리가 필요합니다.

### 일반적인 오해

1. **"많이 기록할수록 좋다"** — 틀림. 무차별적으로 모든 토픽을 기록하면 저장 공간과 대역폭이 빠르게 소진되고 후속 검색도 어려워집니다. 올바른 방법은 문제 영역에 따라 핵심 토픽을 선택하고 합리적인 샘플링률과 압축 전략을 설계하는 것입니다.
2. **"재생 데이터는 실제 실행과 동일하다"** — 틀림. 재생은 데이터 수준의 입력과 출력만 복원할 수 있으며, 하드웨어 타이밍, 인터럽트 지연 등의 물리적 특성은 복원할 수 없습니다. 실시간성에 의존하는 문제의 경우 재생 결과는 참고용일 뿐 실제 기기 검증을 대체할 수 없습니다.
3. **"데이터 기록은 사후 대응이다"** — 틀림. 데이터 기록 방안은 시스템 설계 단계에서 아키텍처에 포함되어야 하며, 토픽 명명 규칙, 타임스탬프 전략, 저장 용량 계획 등을 포함합니다. 사후에 추가 기록하면 핵심 데이터가 없어 문제를 파악하지 못하는 경우가 많습니다.

### 관련 지식

- `ent_process_p14` — 본 노드가 속한 소프트웨어 미들웨어 및 시스템 통합 단계로, 도구 체인 및 DevOps의 전체 프레임워크와 상하류 연결 관계를 정의합니다.
- `ent_process_p14_2_2` — 동일 계열 인접 공정: OTA, 진단 및 상태 관리(동일 작업 패키지 내 병렬 공정).
- `ent_process_p14_2_3` — 동일 계열 인접 공정: CI/CD 및 시뮬레이션 테스트 체인(동일 작업 패키지 내 병렬 공정).
