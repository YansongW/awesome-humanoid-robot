---
$id: ent_process_p10_1_2
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Contact State Estimation
  zh: 接触状态估计
  ko: 접촉 상태 추정
summary:
  en: Contact detection accuracy&gt; 98%, false trigger rate &lt;2%
  zh: '- P10.1.2.1 输入梳理与目标量化 - 整理「接触状态估计」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 接触检测准确率 > 98%、误触发率 < 2%
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

接触状态估计（Contact State Estimation）是运动控制算法开发与验证阶段（P10）中的关键工序节点，用于准确判断机器人足端或操作末端是否与地面或物体发生接触。它真正改变的不是单次触地检测的精度，而是整个步态切换与平衡控制从"开环时序假设"到"闭环感知驱动"的可靠性跨越。该工序以足底力/力矩、加速度突变检测、概率接触检测为核心方法，以接触检测准确率 > 98%、误触发率 < 2% 为量化验收标准。

## 核心内容

### 是什么：准确定义

接触状态估计（Contact State Estimation）是指利用机器人本体传感器（足底力/力矩传感器、IMU 加速度计等）的测量数据，通过确定性阈值判断或概率推理方法，实时推断机器人足端（或操作末端）与外部环境之间接触状态（触地/离地、滑动/静止）的算法过程。它是步态切换与平衡控制的关键前置条件——只有准确知道哪只脚在何时真正着地，控制器才能决定何时转移重心、何时摆动下一步。

在 WBS V3 的工序分解中，该节点（P10.1.2）被展开为五个三级子任务：P10.1.2.1 输入梳理与目标量化、P10.1.2.2 算法/控制方案设计、P10.1.2.3 算法实现与仿真验证、P10.1.2.4 算法调参与性能验证、P10.1.2.5 文档输出与下游交付。每个子任务进一步细化为若干四级关键动作，形成从需求量化到交付闭环的完整工程链路。

### 为什么存在：痛点与历史定位

在早期的人形机器人控制中，步态切换往往依赖预设的时序逻辑——控制器按固定时间间隔假设"脚已着地"，然后切换支撑相。这种做法在平坦硬质地面、低速行走时勉强可用，但一旦遇到软地面、斜坡或未知地形，时序假设与真实接触状态之间会出现显著偏差，导致重心转移过早或过晚，机器人极易摔倒。

接触状态估计正是为了消除这一"假设与现实的鸿沟"而存在。它真正改变的不是单次触地检测的精度，而是整个步态切换与平衡控制从"开环时序假设"到"闭环感知驱动"的可靠性跨越。从历史定位看，它是足式机器人从实验室的预设轨迹演示走向真实世界自适应行走的关键技术门槛之一。

该工序面临的关键约束包括三类：**噪声**（力传感器与加速度计的本底噪声会污染接触特征）、**传感器位置**（足底传感器安装位置影响力分布测量的准确性）、**地面硬度**（软地面会缓冲足底力冲击，使阈值检测失效）。这些约束决定了接触状态估计不能依赖单一信号源，而需要多源融合与概率推理。

### 原理拆解

**① 足底力/力矩阈值检测：最直接的物理判据**

当足底与地面接触时，足底力/力矩传感器会测量到法向力 F_z 与绕 x/y 轴的力矩 τ_x、τ_y。在理想刚性地面条件下，触地瞬间法向力会从接近零跳变到支撑体重所需的量级。最简单的接触判据是：

$$
\text{Contact} = \begin{cases} 1, & \text{if } F_z > F_{\text{th}} \text{ and } \sqrt{\tau_x^2 + \tau_y^2} > \tau_{\text{th}} \\ 0, & \text{otherwise} \end{cases}
$$

其中 F_th 与 τ_th 是预设阈值。该方法的优点是计算量极小、实时性极高；缺点是阈值对地面硬度敏感——软地面上法向力上升缓慢，容易漏检。

**② 加速度突变检测：惯性信号的补充通道**

IMU 加速度计在足端触地瞬间会捕捉到高频冲击信号（加速度突变）。这一信号与力信号在时间上几乎同步，但物理来源不同——力信号反映接触反力，加速度信号反映机体动量变化。两者融合可以提高检测鲁棒性：当力信号因软地面而模糊时，加速度突变仍能提供触地时刻的强证据。

**③ 概率接触检测：从确定性判据到贝叶斯推理**

概率接触检测将接触状态建模为隐变量，利用传感器观测序列进行贝叶斯更新。设接触状态为 C ∈ {0, 1}，观测为 z（力、力矩、加速度特征向量），则后验概率为：

$$
P(C_t = 1 \mid z_{1:t}) \propto P(z_t \mid C_t = 1) \sum_{C_{t-1}} P(C_t \mid C_{t-1}) \, P(C_{t-1} \mid z_{1:t-1})
$$

这一框架天然支持多传感器融合（将各传感器的似然函数相乘）与时间平滑（通过状态转移矩阵抑制单帧误检）。工程上通常以 P(C_t = 1) > 0.9 作为触地判定阈值，以 P(C_t = 0) > 0.9 作为离地判定阈值，中间区间视为"不确定态"并保持上一状态（滞回特性）。

### 关键参数与规格

| 参数 | 规格/指标 | 来源 |
|------|-----------|------|
| 接触检测准确率 | > 98% | 语料：完成标准 |
| 误触发率 | < 2% | 语料：完成标准 |
| 候选方案数量 | 不少于 2 个 | 语料：P10.1.2.2 四级动作 |
| 核心方法 | 足底力/力矩、加速度突变检测、概率接触检测 | 语料：方法/工具 |
| 关键约束 | 噪声、传感器位置、地面硬度 | 语料：关键约束 |
| 设计逻辑 | 准确判断足端是否触地是步态切换与平衡控制的关键 | 语料：设计思考逻辑 |

上述准确率与误触发率指标是工序的量化验收标准，需在典型工况（硬质平地、斜坡）与极端工况（软地面、冲击载荷）下分别验证（工程判断：极端工况下指标可能放宽，需在测试计划中明确工况矩阵）。

### 横向对比

| 方法 | 实时性 | 鲁棒性（噪声） | 鲁棒性（软地面） | 实现复杂度 |
|------|--------|----------------|------------------|------------|
| 足底力阈值检测 | 极高 | 中 | 低 | 低 |
| 加速度突变检测 | 高 | 低 | 中 | 低 |
| 力+加速度融合 | 高 | 中 | 中 | 中 |
| 概率接触检测 | 中 | 高 | 高 | 高 |

从对比可见，单一方法无法同时满足所有约束条件。工程实践中通常采用分层策略：以足底力阈值检测为主通道（实时性优先），以加速度突变检测为辅助通道（补充冲击证据），以概率接触检测为最终仲裁器（融合多源信息并抑制误检）。这种分层设计在保证实时性的同时，将准确率与误触发率推向验收指标。

### 谁在用·应用案例

接触状态估计在足式机器人领域是通用基础模块。在双足人形机器人中，它直接服务于步态生成器——支撑相与摆动相的切换时机完全由接触状态估计的结果驱动。在四足机器人中，接触状态估计同样是步态规划与落足点选择的前提。

在灵巧操作领域，接触状态估计有更精细的对应物——接触模式切换（抓握-滑动-滚动）的调度。传统方法需要精确的接触状态估计来实现手中操作（in-hand manipulation），例如把笔从"握持"调整到"书写"姿态。近年强化学习与模仿学习路线逐渐占据主导，先在仿真中大规模并行训练手中重定向策略，再通过 sim-to-real 迁移到实物——但仿真到现实的迁移本身仍依赖对接触状态的准确建模与估计。

### 局限与边界

接触状态估计的边界条件包括：

1. **传感器物理极限**：足底力传感器的量程与分辨率决定了可检测的最小接触力；IMU 的采样率与噪声密度决定了加速度突变检测的时间分辨率。超出传感器物理极限的接触事件（如极轻的脚尖点地）可能无法被可靠检测。

2. **地面硬度极端情况**：在极软地面（如厚海绵垫）上，法向力上升时间可能长达数百毫秒，阈值检测与加速度突变检测都会失效。此时需要引入额外的感知通道（如关节电流观测或视觉触觉融合），但已超出本工序的默认方法范围。

3. **动态行走中的冲击干扰**：高速奔跑或跳跃时，足底力信号包含大幅冲击分量，可能与触地特征混淆。概率接触检测的滞回特性可以缓解但无法完全消除这一问题。

4. **工序边界**：本工序只负责接触状态的估计，不负责接触力的精确控制（后者属于柔顺/阻抗控制工作包）。估计结果的精度直接制约下游控制性能，但本工序不承诺对控制闭环的稳定性负责。

### 常见误区

1. **"接触状态估计就是阈值比较"**——错。阈值比较只是最基础的方法，在噪声、传感器位置、地面硬度三类约束同时存在时，单一阈值方法无法同时满足准确率 > 98% 与误触发率 < 2% 的指标。概率接触检测通过贝叶斯融合与时间平滑，才是达到验收标准的工程正解。

2. **"准确率越高越好"**——不准确。准确率与误触发率是一对矛盾指标：提高检测灵敏度会降低漏检但增加误触发，反之亦然。工程上需要在两者之间取平衡，而非单方面追求准确率。

3. **"仿真验证通过即可交付"**——错。仿真环境中的接触模型（刚性地面假设、理想传感器噪声）与真实物理世界存在显著差异。P10.1.2.4 明确要求在典型/极端工况下执行测试并记录原始数据，仿真验证只是中间环节而非终点。

4. **"接触状态估计只属于足式机器人"**——不准确。如语料中手中操作与重定向部分所述，灵巧操作的接触模式切换同样依赖接触状态估计，只是传感器从足底力/力矩换成了指尖触觉。该方法的本质是通用的"接触事件推断"问题。

### 相关知识

- `ent_process_p10` — 本工序所属的运动控制算法开发与验证阶段，定义了从输入梳理到文档交付的标准工程动作框架。
- `ent_process_p10_1_1` — 同族相邻工序：IMU 与关节编码器融合（同一工作包内的并行工序）。
- `ent_process_p10_1_3` — 同族相邻工序：外力/扰动估计（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Chapter 16：灵巧操作与手中操作（接触模式切换与触觉传感）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-16.md)

## Overview

Contact State Estimation is a critical process node in the motion control algorithm development and validation phase (P10), used to accurately determine whether the robot's foot end or manipulation end is in contact with the ground or an object. What it truly changes is not the precision of single-contact detection, but the reliability leap of the entire gait switching and balance control from "open-loop timing assumptions" to "closed-loop perception-driven" operation. This process uses foot force/torque, acceleration transient detection, and probabilistic contact detection as core methods, with quantitative acceptance criteria of contact detection accuracy > 98% and false trigger rate < 2%.

## Content

### What It Is: Precise Definition

Contact State Estimation refers to the algorithmic process of using measurement data from the robot's proprioceptive sensors (foot force/torque sensors, IMU accelerometers, etc.) to infer, in real time, the contact state (contact/no contact, sliding/stationary) between the robot's foot end (or manipulation end) and the external environment, through deterministic threshold judgment or probabilistic inference methods. It is a key prerequisite for gait switching and balance control—only by accurately knowing which foot is truly on the ground and when can the controller decide when to shift the center of mass and when to swing the next step.

In the WBS V3 task decomposition, this node (P10.1.2) is expanded into five level-3 subtasks: P10.1.2.1 Input Organization and Target Quantification, P10.1.2.2 Algorithm/Control Scheme Design, P10.1.2.3 Algorithm Implementation and Simulation Validation, P10.1.2.4 Algorithm Tuning and Performance Validation, and P10.1.2.5 Documentation Output and Downstream Delivery. Each subtask is further refined into several level-4 key actions, forming a complete engineering chain from requirement quantification to delivery closure.

### Why It Exists: Pain Points and Historical Positioning

In early humanoid robot control, gait switching often relied on preset timing logic—the controller assumed "the foot has landed" at fixed time intervals and then switched the support phase. This approach was barely usable on flat, rigid ground at low walking speeds, but once encountering soft ground, slopes, or unknown terrain, significant deviations would emerge between timing assumptions and actual contact states, causing the center of mass to shift too early or too late, making the robot highly prone to falling.

Contact State Estimation exists precisely to eliminate this "gap between assumption and reality." What it truly changes is not the precision of single-contact detection, but the reliability leap of the entire gait switching and balance control from "open-loop timing assumptions" to "closed-loop perception-driven" operation. From a historical perspective, it is one of the key technical thresholds for legged robots to move from preset trajectory demonstrations in the laboratory to adaptive walking in the real world.

The key constraints faced by this process fall into three categories: **noise** (the background noise of force sensors and accelerometers contaminates contact features), **sensor placement** (the installation position of foot sensors affects the accuracy of force distribution measurement), and **ground hardness** (soft ground buffers the foot force impact, rendering threshold detection ineffective). These constraints determine that contact state estimation cannot rely on a single signal source but requires multi-source fusion and probabilistic inference.

### Principle Breakdown

**① Foot Force/Torque Threshold Detection: The Most Direct Physical Criterion**

When the foot contacts the ground, the foot force/torque sensor measures the normal force F_z and the moments τ_x, τ_y around the x/y axes. Under ideal rigid ground conditions, the normal force jumps from near zero to the magnitude required to support body weight at the moment of contact. The simplest contact criterion is:

$$
\text{Contact} = \begin{cases} 1, & \text{if } F_z > F_{\text{th}} \text{ and } \sqrt{\tau_x^2 + \tau_y^2} > \tau_{\text{th}} \\ 0, & \text{otherwise} \end{cases}
$$

where F_th and τ_th are preset thresholds. The advantage of this method is extremely low computational cost and very high real-time performance; the disadvantage is that the threshold is sensitive to ground hardness—on soft ground, the normal force rises slowly, making missed detection likely.

**② Acceleration Transient Detection: A Supplementary Channel from Inertial Signals**

The IMU accelerometer captures high-frequency impact signals (acceleration transients) at the moment of foot contact. This signal is nearly synchronized in time with the force signal but has a different physical origin—the force signal reflects contact reaction force, while the acceleration signal reflects changes in body momentum. Fusing the two improves detection robustness: when the force signal is blurred due to soft ground, the acceleration transient can still provide strong evidence of the contact moment.

**③ Probabilistic Contact Detection: From Deterministic Criteria to Bayesian Inference**

Probabilistic contact detection models the contact state as a latent variable and performs Bayesian updates using sensor observation sequences. Let the contact state be C ∈ {0, 1} and the observation be z (force, torque, acceleration feature vector); the posterior probability is:

$$
P(C_t = 1 \mid z_{1:t}) \propto P(z_t \mid C_t = 1) \sum_{C_{t-1}} P(C_t \mid C_{t-1}) \, P(C_{t-1} \mid z_{1:t-1})
$$

This framework naturally supports multi-sensor fusion (by multiplying the likelihood functions of each sensor) and temporal smoothing (by suppressing single-frame false detections through the state transition matrix). In engineering practice, P(C_t = 1) > 0.9 is typically used as the contact determination threshold, P(C_t = 0) > 0.9 as the no-contact determination threshold, and the intermediate range is treated as an "uncertain state" that maintains the previous state (hysteresis characteristic).

### Key Parameters and Specifications

| Parameter | Specification/Indicator | Source |
|-----------|-------------------------|--------|
| Contact detection accuracy | > 98% | Corpus: Completion criteria |
| False trigger rate | < 2% | Corpus: Completion criteria |
| Number of candidate schemes | No fewer than 2 | Corpus: P10.1.2.2 level-4 actions |
| Core methods | Foot force/torque, acceleration transient detection, probabilistic contact detection | Corpus: Methods/tools |
| Key constraints | Noise, sensor placement, ground hardness | Corpus: Key constraints |
| Design logic | Accurately determining whether the foot is in contact is key to gait switching and balance control | Corpus: Design reasoning logic |

The above accuracy and false trigger rate indicators are the quantitative acceptance criteria for the process and need to be validated separately under typical operating conditions (rigid flat ground, slopes) and extreme conditions (soft ground, impact loads) (engineering judgment: indicators may be relaxed under extreme conditions, and the condition matrix needs to be clearly defined in the test plan).

### Horizontal Comparison

| Method | Real-time Performance | Robustness (Noise) | Robustness (Soft Ground) | Implementation Complexity |
|--------|----------------------|--------------------|--------------------------|---------------------------|
| Foot force threshold detection | Extremely high | Medium | Low | Low |
| Acceleration transient detection | High | Low | Medium | Low |
| Force + acceleration fusion | High | Medium | Medium | Medium |
| Probabilistic contact detection | Medium | High | High | High |

From the comparison, it is evident that no single method can simultaneously satisfy all constraint conditions. Engineering practice typically adopts a hierarchical strategy: foot force threshold detection as the main channel (prioritizing real-time performance), acceleration transient detection as the auxiliary channel (supplementing impact evidence), and probabilistic contact detection as the final arbiter (fusing multi-source information and suppressing false detections). This hierarchical design ensures real-time performance while pushing accuracy and false trigger rates toward the acceptance indicators.

### Who Uses It: Application Cases

Contact state estimation is a universal foundational module in the legged robot domain. In bipedal humanoid robots, it directly serves the gait generator—the switching timing between the support phase and the swing phase is entirely driven by the results of contact state estimation. In quadruped robots, contact state estimation is likewise a prerequisite for gait planning and foothold selection.

In the dexterous manipulation domain, contact state estimation has a more refined counterpart—the scheduling of contact mode switching (grasp-slide-roll). Traditional methods require accurate contact state estimation to achieve in-hand manipulation, such as adjusting a pen from a "grasp" to a "writing" posture. In recent years, reinforcement learning and imitation learning approaches have gradually become dominant, first training in-hand reorientation policies at scale in parallel within simulation, then transferring to physical hardware via sim-to-real—but the simulation-to-reality transfer itself still relies on accurate modeling and estimation of contact states.

### Limitations and Boundaries

The boundary conditions of contact state estimation include:

1. **Sensor physical limits**: The range and resolution of foot force sensors determine the minimum detectable contact force; the sampling rate and noise density of the IMU determine the temporal resolution of acceleration transient detection. Contact events beyond the physical limits of the sensors (such as extremely light toe-touch) may not be reliably detected.

2. **Extreme ground hardness cases**: On extremely soft ground (such as thick foam pads), the normal force rise time can be as long as hundreds of milliseconds, rendering both threshold detection and acceleration transient detection ineffective. In such cases, additional perception channels (such as joint current observation or visual-tactile fusion) are needed, but these fall outside the default method scope of this process.

3. **Impact interference during dynamic locomotion**: During high-speed running or jumping, foot force signals contain large impact components that may be confused with contact features. The hysteresis characteristic of probabilistic contact detection can mitigate but not completely eliminate this issue.

4. **Process boundary**: This process is only responsible for estimating contact states, not for precise control of contact forces (the latter belongs to the compliance/impedance control work package). The accuracy of the estimation results directly constrains downstream control performance, but this process does not commit to being responsible for the stability of the control loop.

### Common Misconceptions

1. **"Contact state estimation is just threshold comparison"**—Wrong. Threshold comparison is only the most basic method. When the three types of constraints (noise, sensor placement, ground hardness) exist simultaneously, a single threshold method cannot simultaneously satisfy the indicators of accuracy > 98% and false trigger rate < 2%. Probabilistic contact detection, through Bayesian fusion and temporal smoothing, is the engineering solution that meets the acceptance criteria.

2. **"Higher accuracy is always better"**—Inaccurate. Accuracy and false trigger rate are a pair of conflicting indicators: increasing detection sensitivity reduces missed detections but increases false triggers, and vice versa. Engineering requires a balance between the two, not a one-sided pursuit of accuracy.

3. **"Passing simulation validation is sufficient for delivery"**—Wrong. The contact models in simulation environments (rigid ground assumptions, ideal sensor noise) differ significantly from the real physical world. P10.1.2.4 explicitly requires executing tests under typical/extreme conditions and recording raw data; simulation validation is only an intermediate step, not the endpoint.

4. **"Contact state estimation belongs only to legged robots"**—Inaccurate. As described in the corpus sections on in-hand manipulation and reorientation, contact mode switching in dexterous manipulation also relies on contact state estimation, except that the sensors change from foot force/torque to fingertip tactile sensing. The essence of this method is a general "contact event inference" problem.

### Related Knowledge

- `ent_process_p10` — The motion control algorithm development and validation phase to which this process belongs, defining the standard engineering action framework from input organization to documentation delivery.
- `ent_process_p10_1_1` — Adjacent sibling process: IMU and joint encoder fusion (a parallel process within the same work package).
- `ent_process_p10_1_3` — Adjacent sibling process: External force/disturbance estimation (a parallel process within the same work package).

## 개요

접촉 상태 추정(Contact State Estimation)은 운동 제어 알고리즘 개발 및 검증 단계(P10)에서의 핵심 공정 노드로, 로봇의 발끝 또는 조작 말단이 지면이나 물체와 접촉했는지 여부를 정확히 판단하는 데 사용됩니다. 이 공정이 실제로 바꾸는 것은 단일 접촉 감지의 정밀도가 아니라, 전체 보행 전환 및 균형 제어가 "개루프 시퀀스 가정"에서 "폐루프 인식 구동"으로 도약하는 신뢰성입니다. 해당 공정은 족저력/토크, 가속도 급변 감지, 확률적 접촉 감지를 핵심 방법으로 하며, 접촉 감지 정확도 > 98%, 오발생률 < 2%를 정량적 수락 기준으로 삼습니다.

## 핵심 내용

### 무엇인가: 정확한 정의

접촉 상태 추정(Contact State Estimation)은 로봇 자체 센서(족저력/토크 센서, IMU 가속도계 등)의 측정 데이터를 활용하여, 결정론적 임계값 판단 또는 확률적 추론 방법을 통해 로봇 발끝(또는 조작 말단)과 외부 환경 간의 접촉 상태(접지/이지, 슬립/정지)를 실시간으로 추론하는 알고리즘 과정입니다. 이는 보행 전환 및 균형 제어의 핵심 선행 조건입니다. 즉, 어느 발이 언제 실제로 착지했는지를 정확히 알아야 컨트롤러가 언제 무게 중심을 이동하고 언제 다음 발을 스윙할지 결정할 수 있습니다.

WBS V3의 공정 분해에서 이 노드(P10.1.2)는 5개의 3차 하위 작업으로 확장됩니다: P10.1.2.1 입력 정리 및 목표 정량화, P10.1.2.2 알고리즘/제어 방안 설계, P10.1.2.3 알고리즘 구현 및 시뮬레이션 검증, P10.1.2.4 알고리즘 튜닝 및 성능 검증, P10.1.2.5 문서 출력 및 하류 인계. 각 하위 작업은 다시 여러 4차 핵심 동작으로 세분화되어, 요구사항 정량화에서 인계 완료까지의 완전한 엔지니어링 체인을 형성합니다.

### 왜 존재하는가: 문제점과 역사적 위치

초기 휴머노이드 로봇 제어에서 보행 전환은 종종 사전 설정된 시퀀스 로직에 의존했습니다. 컨트롤러는 고정된 시간 간격으로 "발이 착지했다"고 가정한 후 지지 상을 전환했습니다. 이러한 방식은 평평한 단단한 지면, 저속 보행에서 간신히 사용할 수 있었지만, 부드러운 지면, 경사로 또는 미지의 지형을 만나면 시퀀스 가정과 실제 접촉 상태 사이에 상당한 편차가 발생하여 무게 중심 이동이 너무 이르거나 늦어져 로봇이 쉽게 넘어졌습니다.

접촉 상태 추정은 바로 이러한 "가정과 현실의 간극"을 제거하기 위해 존재합니다. 이 공정이 실제로 바꾸는 것은 단일 접촉 감지의 정밀도가 아니라, 전체 보행 전환 및 균형 제어가 "개루프 시퀀스 가정"에서 "폐루프 인식 구동"으로 도약하는 신뢰성입니다. 역사적 위치에서 보면, 이는 족식 로봇이 실험실의 사전 설정 궤적 시연에서 실제 세계의 적응형 보행으로 나아가는 핵심 기술 관문 중 하나입니다.

이 공정이 직면한 핵심 제약은 세 가지 유형입니다: **노이즈**(힘 센서와 가속도계의 바닥 노이즈가 접촉 특징을 오염시킴), **센서 위치**(족저 센서 설치 위치가 힘 분포 측정의 정확도에 영향을 미침), **지면 경도**(부드러운 지면은 족저 힘 충격을 완충하여 임계값 감지를 무력화함). 이러한 제약은 접촉 상태 추정이 단일 신호원에 의존할 수 없고 다중 소스 융합과 확률적 추론이 필요함을 결정합니다.

### 원리 분해

**① 족저 힘/토크 임계값 감지: 가장 직접적인 물리적 판단 기준**

발끝이 지면과 접촉할 때, 족저 힘/토크 센서는 법선 힘 F_z와 x/y축 주변의 토크 τ_x, τ_y를 측정합니다. 이상적인 강체 지면 조건에서 접촉 순간 법선 힘은 0에 가까운 값에서 체중 지지에 필요한 크기로 점프합니다. 가장 간단한 접촉 판단 기준은 다음과 같습니다:

$$
\text{Contact} = \begin{cases} 1, & \text{if } F_z > F_{\text{th}} \text{ and } \sqrt{\tau_x^2 + \tau_y^2} > \tau_{\text{th}} \\ 0, & \text{otherwise} \end{cases}
$$

여기서 F_th와 τ_th는 사전 설정된 임계값입니다. 이 방법의 장점은 계산량이 매우 적고 실시간성이 매우 높다는 점입니다. 단점은 임계값이 지면 경도에 민감하다는 점입니다. 부드러운 지면에서는 법선 힘이 천천히 상승하여 누락 감지가 발생하기 쉽습니다.

**② 가속도 급변 감지: 관성 신호의 보조 채널**

IMU 가속도계는 발끝 접촉 순간 고주파 충격 신호(가속도 급변)를 포착합니다. 이 신호는 힘 신호와 시간적으로 거의 동기화되지만 물리적 출처는 다릅니다. 힘 신호는 접촉 반력을 반영하고, 가속도 신호는 기체 운동량 변화를 반영합니다. 두 신호를 융합하면 감지 견고성을 높일 수 있습니다. 힘 신호가 부드러운 지면으로 인해 모호해질 때, 가속도 급변은 여전히 접촉 시점에 대한 강력한 증거를 제공합니다.

**③ 확률적 접촉 감지: 결정론적 판단 기준에서 베이지안 추론으로**

확률적 접촉 감지는 접촉 상태를 잠재 변수로 모델링하고 센서 관측 시퀀스를 사용하여 베이지안 업데이트를 수행합니다. 접촉 상태를 C ∈ {0, 1}, 관측을 z(힘, 토크, 가속도 특징 벡터)라고 하면 사후 확률은 다음과 같습니다:

$$
P(C_t = 1 \mid z_{1:t}) \propto P(z_t \mid C_t = 1) \sum_{C_{t-1}} P(C_t \mid C_{t-1}) \, P(C_{t-1} \mid z_{1:t-1})
$$

이 프레임워크는 자연스럽게 다중 센서 융합(각 센서의 우도 함수를 곱함)과 시간 평활화(상태 전이 행렬을 통해 단일 프레임 오검출 억제)를 지원합니다. 공학적으로는 일반적으로 P(C_t = 1) > 0.9를 접지 판단 임계값으로, P(C_t = 0) > 0.9를 이지 판단 임계값으로 사용하며, 중간 구간은 "불확실 상태"로 간주하고 이전 상태를 유지합니다(히스테리시스 특성).

### 핵심 매개변수 및 사양

| 매개변수 | 사양/지표 | 출처 |
|------|-----------|------|
| 접촉 감지 정확도 | > 98% | 말뭉치: 완료 기준 |
| 오발생률 | < 2% | 말뭉치: 완료 기준 |
| 후보 방안 수 | 2개 이상 | 말뭉치: P10.1.2.2 4차 동작 |
| 핵심 방법 | 족저 힘/토크, 가속도 급변 감지, 확률적 접촉 감지 | 말뭉치: 방법/도구 |
| 핵심 제약 | 노이즈, 센서 위치, 지면 경도 | 말뭉치: 핵심 제약 |
| 설계 논리 | 발끝 접촉 여부를 정확히 판단하는 것이 보행 전환 및 균형 제어의 핵심 | 말뭉치: 설계 사고 논리 |

위의 정확도 및 오발생률 지표는 공정의 정량적 수락 기준이며, 일반 작업 조건(단단한 평지, 경사로)과 극한 작업 조건(부드러운 지면, 충격 하중)에서 각각 검증해야 합니다(공학적 판단: 극한 작업 조건에서는 지표가 완화될 수 있으며, 테스트 계획에 작업 조건 매트릭스를 명확히 명시해야 함).

### 횡적 비교

| 방법 | 실시간성 | 견고성(노이즈) | 견고성(부드러운 지면) | 구현 복잡도 |
|------|--------|----------------|------------------|------------|
| 족저 힘 임계값 감지 | 매우 높음 | 중 | 낮음 | 낮음 |
| 가속도 급변 감지 | 높음 | 낮음 | 중 | 낮음 |
| 힘+가속도 융합 | 높음 | 중 | 중 | 중 |
| 확률적 접촉 감지 | 중 | 높음 | 높음 | 높음 |

비교에서 알 수 있듯이 단일 방법으로는 모든 제약 조건을 동시에 충족할 수 없습니다. 공학 실무에서는 일반적으로 계층적 전략을 채택합니다: 족저 힘 임계값 감지를 주 채널(실시간성 우선), 가속도 급변 감지를 보조 채널(충격 증거 보완), 확률적 접촉 감지를 최종 중재자(다중 소스 정보 융합 및 오검출 억제)로 사용합니다. 이러한 계층적 설계는 실시간성을 보장하면서 정확도와 오발생률을 수락 지표로 끌어올립니다.

### 누가 사용하는가·응용 사례

접촉 상태 추정은 족식 로봇 분야에서 범용 기본 모듈입니다. 이족 휴머노이드 로봇에서는 보행 생성기에 직접 서비스를 제공합니다. 지지 상과 스윙 상의 전환 시점은 전적으로 접촉 상태 추정 결과에 의해 구동됩니다. 사족 로봇에서도 접촉 상태 추정은 보행 계획 및 착지점 선택의 전제 조건입니다.

정밀 조작 분야에서 접촉 상태 추정은 더 세밀한 대응물을 가집니다. 접촉 모드 전환(파지-슬립-롤링)의 스케줄링입니다. 전통적인 방법은 정확한 접촉 상태 추정이 필요한 손 안 조작(in-hand manipulation)을 구현하기 위해 사용되었습니다. 예를 들어 펜을 "파지" 자세에서 "필기" 자세로 조정하는 경우입니다. 최근에는 강화 학습과 모방 학습 경로가 점차 주도적이 되었으며, 먼저 시뮬레이션에서 손 안 재지향 정책을 대규모 병렬 훈련한 다음 sim-to-real 전이를 통해 실물로 옮깁니다. 그러나 시뮬레이션에서 현실로의 전이 자체는 여전히 접촉 상태의 정확한 모델링과 추정에 의존합니다.

### 한계와 경계

접촉 상태 추정의 경계 조건은 다음과 같습니다:

1. **센서 물리적 한계**: 족저 힘 센서의 측정 범위와 분해능은 감지 가능한 최소 접촉 힘을 결정합니다. IMU의 샘플링 속도와 노이즈 밀도는 가속도 급변 감지의 시간 분해능을 결정합니다. 센서 물리적 한계를 초과하는 접촉 이벤트(예: 매우 가벼운 발끝 착지)는 안정적으로 감지되지 못할 수 있습니다.

2. **지면 경도 극단 상황**: 매우 부드러운 지면(예: 두꺼운 스펀지 패드)에서는 법선 힘 상승 시간이 수백 밀리초에 달할 수 있으며, 임계값 감지와 가속도 급변 감지 모두 무력화됩니다. 이 경우 추가 감지 채널(예: 관절 전류 관측 또는 시각-촉각 융합)을 도입해야 하지만, 이는 본 공정의 기본 방법 범위를 벗어납니다.

3. **동적 보행 중 충격 간섭**: 고속 주행이나 점프 시 족저 힘 신호에는 큰 충격 성분이 포함되어 접촉 특징과 혼동될 수 있습니다. 확률적 접촉 감지의 히스테리시스 특성은 완화할 수 있지만 완전히 제거할 수는 없습니다.

4. **공정 경계**: 본 공정은 접촉 상태의 추정만 담당하며, 접촉 힘의 정밀 제어는 담당하지 않습니다(후자는 컴플라이언스/임피던스 제어 작업 패키지에 속함). 추정 결과의 정밀도는 하류 제어 성능을 직접 제약하지만, 본 공정은 제어 폐루프의 안정성에 대해 책임을 지지 않습니다.

### 일반적인 오해

1. **"접촉 상태 추정은 임계값 비교다"** — 틀림. 임계값 비교는 가장 기본적인 방법일 뿐이며, 노이즈, 센서 위치, 지면 경도의 세 가지 제약이 동시에 존재할 때 단일 임계값 방법으로는 정확도 > 98%와 오발생률 < 2%의 지표를 동시에 충족할 수 없습니다. 확률적 접촉 감지는 베이지안 융합과 시간 평활화를 통해 수락 기준을 달성하는 공학적 정답입니다.

2. **"정확도가 높을수록 좋다"** — 부정확함. 정확도와 오발생률은 상충되는 지표 쌍입니다. 감지 민감도를 높이면 누락 감지는 줄어들지만 오발생이 증가하며, 그 반대도 마찬가지입니다. 공학적으로는 단순히 정확도를 추구하는 것이 아니라 두 지표 사이의 균형을 잡아야 합니다.

3. **"시뮬레이션 검증 통과 시 인계 가능하다"** — 틀림. 시뮬레이션 환경의 접촉 모델(강체 지면 가정, 이상적 센서 노이즈)은 실제 물리 세계와 상당한 차이가 있습니다. P10.1.2.4는 일반/극한 작업 조건에서 테스트를 수행하고 원시 데이터를 기록할 것을 명시적으로 요구하며, 시뮬레이션 검증은 중간 단계일 뿐 종착점이 아닙니다.

4. **"접촉 상태 추정은 족식 로봇에만 속한다"** — 부정확함. 말뭉치의 손 안 조작 및 재지향 부분에서 설명한 바와 같이, 정밀 조작의 접촉 모드 전환도 접촉 상태 추정에 의존하며, 단지 센서가 족저 힘/토크에서 손끝 촉각으로 바뀔 뿐입니다. 이 방법의 본질은 범용적인 "접촉 이벤트 추론" 문제입니다.

### 관련 지식

- `ent_process_p10` — 본 공정이 속한 운동 제어 알고리즘 개발 및 검증 단계로, 입력 정리에서 문서 인계까지의 표준 엔지니어링 동작 프레임워크를 정의합니다.
- `ent_process_p10_1_1` — 동족 인접 공정: IMU와 관절 엔코더 융합(동일 작업 패키지 내 병렬 공정).
- `ent_process_p10_1_3` — 동족 인접 공정: 외력/교란 추정(동일 작업 패키지 내 병렬 공정).
