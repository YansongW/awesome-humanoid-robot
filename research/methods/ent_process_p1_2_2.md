---
$id: ent_process_p1_2_2
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Whole machine architecture and function allocation
  zh: 整机架构与功能分配
  ko: 전체 구조와 기능 배분
summary:
  en: System architecture diagram, function allocation table, ICD draft
  zh: '- P1.2.2.1 输入梳理与目标量化 - 整理「整机架构与功能分配」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 系统架构图、功能分配表、ICD 草案
domains:
- 06_design_engineering
- 12_policy_regulation_ethics
layers:
- midstream
- validation_markets
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

整机架构与功能分配（P1.2.2）是人形机器人系统方案设计阶段的核心工序节点，属于需求定义与系统方案（Concept / Pre-A）阶段中 P1.2 系统方案设计工作包下的三级子任务。它真正改变的不是画一张架构图的动作，而是"从零件堆叠到系统契约"的决策方式——在详细设计之前，用功能分解、N² 图与接口控制文档（ICD）草案，把运动、操作、感知、计算、能源、热管理、结构七大子系统的边界与接口关系冻结为可追溯的工程基线。该节点统一展开为五个四级关键动作：输入梳理与目标量化、方案/方法设计、实施/原型/样件制作、验证与问题闭环、文档输出与下游交付。

## 核心内容

### 是什么：准确定义

整机架构与功能分配是 P1.2 系统方案设计工作包下的一个工序节点，其输入是 P1.1 需求工程输出的产品规格与需求基线，输出物为**系统架构图、功能分配表、ICD 草案**。它回答三个问题：整机由哪些子系统组成、每个子系统承担什么功能、子系统之间如何交互。

该节点的方法/工具明确为**功能分解、N² 图、接口控制文档（ICD）草案**。功能分解将整机功能逐层拆解为可分配的原子功能；N² 图以矩阵形式枚举子系统两两之间的接口关系；ICD 草案则对每个接口的电气、机械、通信、热、软件属性进行初步约定。三者共同构成"架构描述—接口枚举—契约固化"的完整链条。

设计思考逻辑要求明确**运动、操作、感知、计算、能源、热管理、结构**七个子系统的边界。关键约束为**实时性、功耗、线缆数量、可维护性**——这四个约束决定了架构的拓扑形态：实时性决定计算与执行之间的通信方式，功耗决定能源子系统的容量与分布，线缆数量决定走线拓扑与连接器选型，可维护性决定模块划分粒度与快拆结构设计。

### 为什么存在：痛点与历史定位

在人形机器人行业早期，团队往往跳过整机架构设计直接进入各子系统详细设计。机械工程师按直觉选型电机，软件工程师按想象定义接口，等到集成阶段才发现关节峰值扭矩不足、通信带宽不够、安全等级不达标——返工成本呈指数级上升。整机架构与功能分配节点的存在，正是为了在详细设计之前，用一张架构图和一份 ICD 草案把"谁做什么、谁和谁怎么说话"固定下来。

从流程定位看，P1.2.2 处于 P1.2 系统方案设计的中间位置：上游是 P1.1 需求工程输出的量化需求基线，下游是各子系统的详细设计任务。它承担的是"翻译"职能——把需求语言翻译成架构语言，把"机器人要能搬 10 kg 物体"翻译成"操作子系统需要 6 自由度臂、峰值扭矩 ≥ X N·m、与计算子系统的通信周期 ≤ Y ms"。这种翻译一旦缺失，后续所有子系统设计都会各自为政。

### 原理拆解

**① 功能分解：从"整机能力"到"原子功能"**

功能分解采用"目标—功能—手段"三层递进。第一层列出整机必须实现的目标能力（如行走、抓取、导航）；第二层将每个目标能力分解为功能（如行走分解为步态规划、关节伺服、姿态平衡）；第三层将每个功能指派给具体子系统（如步态规划归计算子系统，关节伺服归运动子系统）。分解的粒度以"一个功能只属于一个子系统"为准则，避免功能归属模糊导致的责任真空。

**② N² 图：接口关系的穷举与冲突发现**

N² 图是一个 N×N 矩阵，N 为子系统数量。对角线格子放置子系统名称，非对角线格子放置两个子系统之间的接口描述。对于七个子系统，N² 图的非对角线格子（N×(N−1) 个）每个都必须显式填写"有接口"或"无接口"。这一穷举过程的价值在于：它强迫设计团队回答每一个子系统对之间"到底有没有交互"，从而在图纸阶段就发现遗漏的接口。

**③ ICD 草案：接口属性的契约化**

ICD 草案对每个已识别的接口定义五类属性：电气（电压、电流、信号类型）、机械（连接器型号、安装尺寸、防护等级）、通信（协议、波特率、周期）、热（热流方向、允许温升）、软件（数据格式、调用方式）。ICD 草案的"草案"二字意味着它允许在后续详细设计中修订，但修订必须走变更流程——这正是"契约"的含义。

### 关键参数与规格

| 参数/规格 | 数值/要求 | 来源 |
|---|---|---|
| 候选方案数量 | 不少于 2 个 | 语料 P1.2.2.2 |
| 评估方式 | 评估矩阵并量化打分 | 语料 P1.2.2.2 |
| 子系统数量 | 7 个（运动、操作、感知、计算、能源、热管理、结构） | 语料 |
| 关键约束 | 实时性、功耗、线缆数量、可维护性 | 语料 |
| 输出物 | 系统架构图、功能分配表、ICD 草案 | 语料 |
| 四级关键动作数 | 每个三级子任务统一 5 个 | 语料 |

### 横向对比

整机架构与功能分配节点与相邻节点的关系可通过下表理解：

| 对比维度 | P1.1 需求工程 | P1.2.2 整机架构与功能分配 | P1.2.3 子系统详细设计（下游） |
|---|---|---|---|
| 核心问题 | 机器人要做什么 | 机器人由哪些部分组成、如何协作 | 每个部分具体怎么做 |
| 输出物 | 需求规格书、量化 KPI | 系统架构图、功能分配表、ICD 草案 | 详细设计图纸、计算书 |
| 抽象层级 | 需求层（用户语言） | 架构层（系统语言） | 实现层（工程语言） |
| 变更成本 | 低（改文档） | 中（改架构图与 ICD） | 高（改图纸与代码） |

这一对比揭示了一个关键规律：**架构层的变更成本远低于实现层**。P1.2.2 存在的意义，就是把高成本的变更尽量提前到低成本阶段解决。

### 谁在用·应用案例

该节点适用于所有采用 WBS V3 流程的人形机器人产品开发项目，尤其是从概念阶段走向 Pre-A 样机阶段的团队。典型应用场景包括：

- **整机方案评审**：在 Pre-A 阶段，团队使用系统架构图向投资方和管理层展示技术路线，使用功能分配表说明各子系统的责任边界，使用 ICD 草案证明接口风险已被识别。
- **跨团队协作启动**：当机械、电气、软件、算法团队并行启动详细设计时，ICD 草案作为"接口宪法"约束各团队的接口定义，避免集成阶段的"接口扯皮"。
- **供应链选型输入**：功能分配表明确了各子系统的功能需求，采购团队据此向供应商提出量化技术要求，而非模糊的"性能要好"。

### 局限与边界

该节点的局限在于：**架构设计无法消除所有集成风险**。N² 图只能枚举已知的接口关系，无法预见动态交互中的涌现问题（如电磁干扰、热耦合、软件死锁）。ICD 草案的"草案"属性意味着接口可能在详细设计中被修订，而修订的连锁影响难以在架构层完全预估。

边界条件包括：该节点不涉及具体零部件的选型计算（属于下游详细设计）；不涉及控制算法的具体实现（属于软件详细设计）；不涉及制造工艺规划（属于工艺阶段）。它只负责"定义边界和接口"，不负责"实现边界内的内容"。

### 常见误区

1. **"架构图就是画一张框图"**——错。系统架构图必须与功能分配表一一对应，每个框图必须有明确的功能描述和接口清单，否则只是一张装饰画。
2. **"N² 图只填有接口的格子"**——错。N² 图的价值恰恰在于穷举所有格子，包括"无接口"的格子。只有显式确认"无接口"，才能避免集成阶段发现意外交互。
3. **"ICD 草案可以等详细设计再写"**——错。ICD 草案的核心价值是提前暴露接口冲突。等到详细设计阶段再写，接口冲突已经转化为返工成本。
4. **"功能分配表越细越好"**——错。功能分配粒度以"一个功能只属于一个子系统"为上限。过度细分会导致接口数量爆炸，反而增加集成复杂度。

### 相关知识

- `ent_process_p1` — 本节点所属的上级阶段实体，P1 需求定义与系统方案（Concept / Pre-A）的核心交付物是冻结产品规格与平台化策略，P1.2.2 是其系统方案设计工作包下的关键工序节点。
- `ent_process_p1_2_1` — 同族相邻工序：概念方案发散与收敛（同一工作包内的并行工序）。
- `ent_process_p1_2_3` — 同族相邻工序：关键技术指标权衡分析（同一工作包内的并行工序）。
- `ent_process_p1_2_4` — 同族相邻工序：概念验证（Proof of Concept）（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

The overall architecture and function allocation (P1.2.2) is a core process node in the system solution design phase of a humanoid robot project, belonging to the third-level subtask under the P1.2 system solution design work package within the requirements definition and system solution (Concept / Pre-A) phase. What it truly changes is not the act of drawing an architecture diagram, but the decision-making approach "from part stacking to system contracts"—before detailed design, it freezes the boundaries and interface relationships of the seven subsystems—motion, manipulation, perception, computing, energy, thermal management, and structure—into a traceable engineering baseline using functional decomposition, N² diagrams, and interface control document (ICD) drafts. This node is uniformly expanded into five fourth-level key actions: input review and target quantification, solution/method design, implementation/prototype/sample fabrication, verification and issue closure, and documentation output and downstream delivery.

## Content

### What It Is: Precise Definition

Overall architecture and function allocation is a process node under the P1.2 system solution design work package. Its input is the product specification and requirements baseline output by P1.1 requirements engineering, and its outputs are the **system architecture diagram, function allocation table, and ICD draft**. It answers three questions: what subsystems constitute the entire machine, what functions each subsystem undertakes, and how subsystems interact with each other.

The methods/tools for this node are explicitly defined as **functional decomposition, N² diagrams, and interface control document (ICD) drafts**. Functional decomposition breaks down the overall machine functions layer by layer into allocatable atomic functions; N² diagrams enumerate the interface relationships between subsystem pairs in matrix form; ICD drafts provide preliminary agreements on the electrical, mechanical, communication, thermal, and software attributes of each interface. Together, they form a complete chain of "architecture description—interface enumeration—contract solidification."

The design reasoning logic requires clarifying the boundaries of the seven subsystems: **motion, manipulation, perception, computing, energy, thermal management, and structure**. The key constraints are **real-time performance, power consumption, cable count, and maintainability**—these four constraints determine the topology of the architecture: real-time performance dictates the communication method between computing and actuation, power consumption determines the capacity and distribution of the energy subsystem, cable count determines the routing topology and connector selection, and maintainability determines the module partitioning granularity and quick-release structure design.

### Why It Exists: Pain Points and Historical Positioning

In the early days of the humanoid robot industry, teams often skipped overall architecture design and went directly into subsystem detailed design. Mechanical engineers selected motors based on intuition, software engineers defined interfaces based on imagination, and only during integration did they discover insufficient joint peak torque, inadequate communication bandwidth, or substandard safety levels—rework costs escalated exponentially. The existence of the overall architecture and function allocation node is precisely to fix "who does what, and who talks to whom and how" with an architecture diagram and an ICD draft before detailed design.

From a process positioning perspective, P1.2.2 sits in the middle of P1.2 system solution design: upstream is the quantified requirements baseline output by P1.1 requirements engineering, and downstream are the detailed design tasks of each subsystem. It serves a "translation" function—translating requirements language into architecture language, turning "the robot must be able to lift a 10 kg object" into "the manipulation subsystem requires a 6-DOF arm, peak torque ≥ X N·m, and a communication cycle with the computing subsystem ≤ Y ms." If this translation is missing, all subsequent subsystem designs will operate in silos.

### Principle Breakdown

**① Functional Decomposition: From "Whole-Machine Capability" to "Atomic Functions"**

Functional decomposition follows a three-layer progression of "goal—function—means." The first layer lists the goal capabilities the entire machine must achieve (e.g., walking, grasping, navigation); the second layer decomposes each goal capability into functions (e.g., walking decomposes into gait planning, joint servo control, posture balancing); the third layer assigns each function to a specific subsystem (e.g., gait planning belongs to the computing subsystem, joint servo control belongs to the motion subsystem). The granularity of decomposition follows the criterion that "one function belongs to only one subsystem," avoiding responsibility vacuums caused by ambiguous function ownership.

**② N² Diagram: Exhaustive Enumeration and Conflict Discovery of Interface Relationships**

An N² diagram is an N×N matrix, where N is the number of subsystems. Diagonal cells contain subsystem names, and off-diagonal cells contain interface descriptions between two subsystems. For seven subsystems, every off-diagonal cell (N×(N−1) cells) must be explicitly filled with "has interface" or "no interface." The value of this exhaustive process lies in the fact that it forces the design team to answer "whether there is actually interaction" for every subsystem pair, thereby discovering omitted interfaces at the drawing stage.

**③ ICD Draft: Contractualization of Interface Attributes**

The ICD draft defines five categories of attributes for each identified interface: electrical (voltage, current, signal type), mechanical (connector model, mounting dimensions, protection rating), communication (protocol, baud rate, cycle), thermal (heat flow direction, allowable temperature rise), and software (data format, invocation method). The word "draft" in ICD draft means that revisions are permitted during subsequent detailed design, but revisions must go through a change management process—this is precisely what "contract" means.

### Key Parameters and Specifications

| Parameter/Specification | Value/Requirement | Source |
|---|---|---|
| Number of candidate solutions | No fewer than 2 | Corpus P1.2.2.2 |
| Evaluation method | Evaluation matrix with quantified scoring | Corpus P1.2.2.2 |
| Number of subsystems | 7 (motion, manipulation, perception, computing, energy, thermal management, structure) | Corpus |
| Key constraints | Real-time performance, power consumption, cable count, maintainability | Corpus |
| Outputs | System architecture diagram, function allocation table, ICD draft | Corpus |
| Number of fourth-level key actions | Uniformly 5 per third-level subtask | Corpus |

### Horizontal Comparison

The relationship between the overall architecture and function allocation node and its adjacent nodes can be understood through the following table:

| Comparison Dimension | P1.1 Requirements Engineering | P1.2.2 Overall Architecture and Function Allocation | P1.2.3 Subsystem Detailed Design (Downstream) |
|---|---|---|---|
| Core question | What the robot needs to do | What parts constitute the robot and how they collaborate | How each part is specifically implemented |
| Outputs | Requirements specification, quantified KPIs | System architecture diagram, function allocation table, ICD draft | Detailed design drawings, calculation reports |
| Abstraction level | Requirements layer (user language) | Architecture layer (system language) | Implementation layer (engineering language) |
| Change cost | Low (modify documents) | Medium (modify architecture diagram and ICD) | High (modify drawings and code) |

This comparison reveals a key rule: **the change cost at the architecture layer is far lower than at the implementation layer**. The purpose of P1.2.2 is to move high-cost changes as early as possible to the low-cost stage for resolution.

### Who Uses It · Application Cases

This node applies to all humanoid robot product development projects adopting the WBS V3 process, especially teams transitioning from the concept stage to the Pre-A prototype stage. Typical application scenarios include:

- **Whole-machine solution review**: During the Pre-A stage, teams use the system architecture diagram to present the technical roadmap to investors and management, use the function allocation table to explain the responsibility boundaries of each subsystem, and use the ICD draft to demonstrate that interface risks have been identified.
- **Cross-team collaboration kickoff**: When mechanical, electrical, software, and algorithm teams start detailed design in parallel, the ICD draft serves as the "interface constitution" constraining each team's interface definitions, avoiding "interface disputes" during integration.
- **Supply chain selection input**: The function allocation table clarifies the functional requirements of each subsystem, enabling the procurement team to issue quantified technical requirements to suppliers rather than vague "good performance."

### Limitations and Boundaries

The limitation of this node is that **architecture design cannot eliminate all integration risks**. N² diagrams can only enumerate known interface relationships and cannot foresee emergent issues in dynamic interactions (such as electromagnetic interference, thermal coupling, or software deadlocks). The "draft" nature of the ICD draft means interfaces may be revised during detailed design, and the cascading impact of such revisions is difficult to fully predict at the architecture layer.

Boundary conditions include: this node does not involve specific component selection calculations (which belong to downstream detailed design); it does not involve the specific implementation of control algorithms (which belongs to software detailed design); it does not involve manufacturing process planning (which belongs to the process phase). It is only responsible for "defining boundaries and interfaces," not for "implementing the content within the boundaries."

### Common Misconceptions

1. **"An architecture diagram is just drawing a block diagram"**—Wrong. The system architecture diagram must correspond one-to-one with the function allocation table; every block must have a clear functional description and interface list; otherwise, it is merely a decorative picture.
2. **"The N² diagram only fills in cells with interfaces"**—Wrong. The value of the N² diagram lies precisely in exhaustively filling all cells, including "no interface" cells. Only by explicitly confirming "no interface" can unexpected interactions during integration be avoided.
3. **"The ICD draft can wait until detailed design"**—Wrong. The core value of the ICD draft is to expose interface conflicts early. If written only during detailed design, interface conflicts have already turned into rework costs.
4. **"The finer the function allocation table, the better"**—Wrong. The granularity of function allocation is capped by "one function belongs to only one subsystem." Overly fine granularity leads to an explosion in the number of interfaces, increasing integration complexity instead.

### Related Knowledge

- `ent_process_p1` — The parent phase entity to which this node belongs. The core deliverable of P1 requirements definition and system solution (Concept / Pre-A) is freezing the product specification and platform strategy; P1.2.2 is a key process node under its system solution design work package.
- `ent_process_p1_2_1` — Adjacent process in the same family: concept solution divergence and convergence (a parallel process within the same work package).
- `ent_process_p1_2_3` — Adjacent process in the same family: key technical indicator trade-off analysis (a parallel process within the same work package).
- `ent_process_p1_2_4` — Adjacent process in the same family: proof of concept (a parallel process within the same work package).

## 개요

전체 아키텍처 및 기능 할당(P1.2.2)은 휴머노이드 로봇 시스템 설계 단계의 핵심 공정 노드로, 요구사항 정의 및 시스템 개념(Concept / Pre-A) 단계에서 P1.2 시스템 설계 작업 패키지 아래의 3차 하위 작업입니다. 이 노드가 실제로 바꾸는 것은 아키텍처 다이어그램을 그리는 행위가 아니라, "부품 적층에서 시스템 계약으로"의 의사결정 방식입니다. 상세 설계 이전에 기능 분해, N² 다이어그램, 인터페이스 제어 문서(ICD) 초안을 통해 운동, 조작, 인지, 계산, 에너지, 열관리, 구조의 7대 하위 시스템의 경계와 인터페이스 관계를 추적 가능한 엔지니어링 기준선으로 고정합니다. 이 노드는 통일적으로 5가지 4차 핵심 작업으로 전개됩니다: 입력 정리 및 목표 정량화, 설계/방법 설계, 구현/프로토타입/시제품 제작, 검증 및 문제 폐쇄, 문서 출력 및 하류 전달.

## 핵심 내용

### 무엇인가: 정확한 정의

전체 아키텍처 및 기능 할당은 P1.2 시스템 설계 작업 패키지 아래의 공정 노드로, 입력은 P1.1 요구사항 엔지니어링이 출력한 제품 사양 및 요구사항 기준선이며, 출력물은 **시스템 아키텍처 다이어그램, 기능 할당표, ICD 초안**입니다. 이는 세 가지 질문에 답합니다: 전체 기계가 어떤 하위 시스템으로 구성되는가, 각 하위 시스템이 어떤 기능을 담당하는가, 하위 시스템 간에 어떻게 상호작용하는가.

이 노드의 방법/도구는 명확히 **기능 분해, N² 다이어그램, 인터페이스 제어 문서(ICD) 초안**으로 정의됩니다. 기능 분해는 전체 기계 기능을 계층적으로 할당 가능한 원자 기능으로 분해합니다. N² 다이어그램은 행렬 형태로 하위 시스템 간의 모든 인터페이스 관계를 열거합니다. ICD 초안은 각 인터페이스의 전기, 기계, 통신, 열, 소프트웨어 속성을 초기 규약으로 정의합니다. 이 세 가지가 함께 "아키텍처 설명—인터페이스 열거—계약 고정"의 완전한 체인을 구성합니다.

설계 사고 논리는 **운동, 조작, 인지, 계산, 에너지, 열관리, 구조**의 7대 하위 시스템 경계를 명확히 할 것을 요구합니다. 핵심 제약 조건은 **실시간성, 전력 소비, 케이블 수, 유지보수성**입니다. 이 네 가지 제약이 아키텍처의 토폴로지 형태를 결정합니다: 실시간성은 계산과 실행 간의 통신 방식을 결정하고, 전력 소비는 에너지 하위 시스템의 용량과 분포를 결정하며, 케이블 수는 배선 토폴로지와 커넥터 선정을 결정하고, 유지보수성은 모듈 분할 입도와 퀵 릴리스 구조 설계를 결정합니다.

### 왜 존재하는가: 문제점과 역사적 위치

휴머노이드 로봇 산업 초기에는 팀이 전체 아키텍처 설계를 건너뛰고 각 하위 시스템의 상세 설계로 직접 들어가는 경우가 많았습니다. 기계 엔지니어는 직관에 따라 모터를 선정하고, 소프트웨어 엔지니어는 상상에 따라 인터페이스를 정의했으며, 통합 단계에 이르러서야 관절 피크 토크 부족, 통신 대역폭 부족, 안전 등급 미달을 발견했습니다. 재작업 비용은 기하급수적으로 증가했습니다. 전체 아키텍처 및 기능 할당 노드가 존재하는 이유는 바로 상세 설계 이전에 아키텍처 다이어그램 한 장과 ICD 초안 한 부로 "누가 무엇을 하는지, 누가 누구와 어떻게 소통하는지"를 고정하기 위해서입니다.

프로세스 위치에서 보면 P1.2.2는 P1.2 시스템 설계의 중간에 위치합니다. 상류는 P1.1 요구사항 엔지니어링이 출력한 정량화된 요구사항 기준선이고, 하류는 각 하위 시스템의 상세 설계 작업입니다. 이 노드는 "번역" 기능을 담당합니다. 요구사항 언어를 아키텍처 언어로 번역하고, "로봇이 10kg 물체를 들어 올릴 수 있어야 한다"를 "조작 하위 시스템은 6자유도 암, 피크 토크 ≥ X N·m, 계산 하위 시스템과의 통신 주기 ≤ Y ms가 필요하다"로 번역합니다. 이러한 번역이 없으면 이후 모든 하위 시스템 설계가 제각각 진행됩니다.

### 원리 분해

**① 기능 분해: "전체 기계 능력"에서 "원자 기능"으로**

기능 분해는 "목표—기능—수단"의 3계층 점진적 접근을 채택합니다. 첫 번째 계층은 전체 기계가 반드시 구현해야 하는 목표 능력을 나열합니다(예: 보행, 파지, 내비게이션). 두 번째 계층은 각 목표 능력을 기능으로 분해합니다(예: 보행을 보행 계획, 관절 서보, 자세 균형으로 분해). 세 번째 계층은 각 기능을 특정 하위 시스템에 할당합니다(예: 보행 계획은 계산 하위 시스템, 관절 서보는 운동 하위 시스템). 분해의 입도는 "하나의 기능은 하나의 하위 시스템에만 속한다"는 기준을 따르며, 기능 소속의 모호함으로 인한 책임 공백을 피합니다.

**② N² 다이어그램: 인터페이스 관계의 완전 열거와 충돌 발견**

N² 다이어그램은 N×N 행렬로, N은 하위 시스템 수입니다. 대각선 셀에는 하위 시스템 이름을 배치하고, 비대각선 셀에는 두 하위 시스템 간의 인터페이스 설명을 배치합니다. 7개 하위 시스템의 경우 N² 다이어그램의 비대각선 셀(N×(N−1)개) 각각에 "인터페이스 있음" 또는 "인터페이스 없음"을 명시적으로 기입해야 합니다. 이 완전 열거 과정의 가치는 설계 팀이 모든 하위 시스템 쌍에 대해 "실제로 상호작용이 있는지"를 답하도록 강제하여, 도면 단계에서 누락된 인터페이스를 발견하게 하는 데 있습니다.

**③ ICD 초안: 인터페이스 속성의 계약화**

ICD 초안은 식별된 각 인터페이스에 대해 5가지 속성 클래스를 정의합니다: 전기(전압, 전류, 신호 유형), 기계(커넥터 모델, 설치 치수, 보호 등급), 통신(프로토콜, 전송 속도, 주기), 열(열 흐름 방향, 허용 온도 상승), 소프트웨어(데이터 형식, 호출 방식). ICD 초안의 "초안"이라는 단어는 이후 상세 설계에서 수정을 허용하지만, 수정은 반드시 변경 절차를 거쳐야 함을 의미합니다. 이것이 바로 "계약"의 의미입니다.

### 핵심 파라미터 및 사양

| 파라미터/사양 | 값/요구사항 | 출처 |
|---|---|---|
| 후보 설계 수 | 2개 이상 | 말뭉치 P1.2.2.2 |
| 평가 방식 | 평가 매트릭스 및 정량적 점수화 | 말뭉치 P1.2.2.2 |
| 하위 시스템 수 | 7개(운동, 조작, 인지, 계산, 에너지, 열관리, 구조) | 말뭉치 |
| 핵심 제약 조건 | 실시간성, 전력 소비, 케이블 수, 유지보수성 | 말뭉치 |
| 출력물 | 시스템 아키텍처 다이어그램, 기능 할당표, ICD 초안 | 말뭉치 |
| 4차 핵심 작업 수 | 각 3차 하위 작업마다 통일 5개 | 말뭉치 |

### 수평 비교

전체 아키텍처 및 기능 할당 노드와 인접 노드의 관계는 아래 표로 이해할 수 있습니다:

| 비교 차원 | P1.1 요구사항 엔지니어링 | P1.2.2 전체 아키텍처 및 기능 할당 | P1.2.3 하위 시스템 상세 설계(하류) |
|---|---|---|---|
| 핵심 문제 | 로봇이 무엇을 해야 하는가 | 로봇이 어떤 부분으로 구성되고 어떻게 협력하는가 | 각 부분이 구체적으로 어떻게 구현되는가 |
| 출력물 | 요구사항 사양서, 정량화 KPI | 시스템 아키텍처 다이어그램, 기능 할당표, ICD 초안 | 상세 설계 도면, 계산서 |
| 추상 계층 | 요구사항 계층(사용자 언어) | 아키텍처 계층(시스템 언어) | 구현 계층(엔지니어링 언어) |
| 변경 비용 | 낮음(문서 수정) | 중간(아키텍처 다이어그램 및 ICD 수정) | 높음(도면 및 코드 수정) |

이 비교는 핵심 법칙을 드러냅니다: **아키텍처 계층의 변경 비용은 구현 계층보다 훨씬 낮습니다.** P1.2.2가 존재하는 의미는 고비용 변경을 가능한 한 저비용 단계로 앞당겨 해결하는 것입니다.

### 사용자·적용 사례

이 노드는 WBS V3 프로세스를 채택한 모든 휴머노이드 로봇 제품 개발 프로젝트, 특히 개념 단계에서 Pre-A 프로토타입 단계로 나아가는 팀에 적용됩니다. 대표적인 적용 시나리오는 다음과 같습니다:

- **전체 기계 설계 검토**: Pre-A 단계에서 팀은 시스템 아키텍처 다이어그램을 투자자와 경영진에게 기술 로드맵을 제시하고, 기능 할당표로 각 하위 시스템의 책임 경계를 설명하며, ICD 초안으로 인터페이스 위험이 식별되었음을 입증합니다.
- **교차 팀 협업 시작**: 기계, 전기, 소프트웨어, 알고리즘 팀이 병렬로 상세 설계를 시작할 때, ICD 초안은 "인터페이스 헌법"으로 각 팀의 인터페이스 정의를 구속하여 통합 단계의 "인터페이스 분쟁"을 방지합니다.
- **공급망 선정 입력**: 기능 할당표는 각 하위 시스템의 기능 요구사항을 명확히 하여, 조달 팀이 공급업체에 모호한 "성능이 좋아야 함"이 아닌 정량적 기술 요구사항을 제시할 수 있게 합니다.

### 한계와 경계

이 노드의 한계는 **아키텍처 설계가 모든 통합 위험을 제거할 수 없다**는 점입니다. N² 다이어그램은 알려진 인터페이스 관계만 열거할 수 있으며, 동적 상호작용에서 발생하는 창발적 문제(예: 전자기 간섭, 열 커플링, 소프트웨어 교착 상태)를 예측할 수 없습니다. ICD 초안의 "초안" 속성은 인터페이스가 상세 설계에서 수정될 수 있음을 의미하며, 수정의 연쇄 영향은 아키텍처 계층에서 완전히 예측하기 어렵습니다.

경계 조건은 다음과 같습니다: 이 노드는 구체적인 부품 선정 계산(하류 상세 설계에 속함)을 다루지 않습니다. 제어 알고리즘의 구체적 구현(소프트웨어 상세 설계에 속함)을 다루지 않습니다. 제조 공정 계획(공정 단계에 속함)을 다루지 않습니다. 이 노드는 "경계와 인터페이스 정의"만 담당하며 "경계 내부 내용의 구현"은 담당하지 않습니다.

### 일반적인 오해

1. **"아키텍처 다이어그램은 블록 다이어그램 한 장을 그리는 것"** — 틀렸습니다. 시스템 아키텍처 다이어그램은 기능 할당표와 일대일로 대응해야 하며, 각 블록 다이어그램에는 명확한 기능 설명과 인터페이스 목록이 있어야 합니다. 그렇지 않으면 장식 그림에 불과합니다.
2. **"N² 다이어그램은 인터페이스가 있는 셀만 채운다"** — 틀렸습니다. N² 다이어그램의 가치는 바로 모든 셀을 완전 열거하는 데 있으며, "인터페이스 없음" 셀도 포함합니다. "인터페이스 없음"을 명시적으로 확인해야만 통합 단계에서 예상치 못한 상호작용을 발견하는 것을 피할 수 있습니다.
3. **"ICD 초안은 상세 설계 때 작성해도 된다"** — 틀렸습니다. ICD 초안의 핵심 가치는 인터페이스 충돌을 조기에 노출하는 것입니다. 상세 설계 단계에서 작성하면 인터페이스 충돌이 이미 재작업 비용으로 전환된 상태입니다.
4. **"기능 할당표는 세밀할수록 좋다"** — 틀렸습니다. 기능 할당의 입도는 "하나의 기능은 하나의 하위 시스템에만 속한다"를 상한으로 합니다. 과도한 세분화는 인터페이스 수의 폭발을 초래하여 오히려 통합 복잡성을 증가시킵니다.

### 관련 지식

- `ent_process_p1` — 본 노드가 속한 상위 단계 엔터티로, P1 요구사항 정의 및 시스템 개념(Concept / Pre-A)의 핵심 산출물은 제품 사양과 플랫폼화 전략의 동결이며, P1.2.2는 시스템 설계 작업 패키지 아래의 핵심 공정 노드입니다.
- `ent_process_p1_2_1` — 동일 계열 인접 공정: 개념 설계 발산과 수렴(동일 작업 패키지 내 병렬 공정).
- `ent_process_p1_2_3` — 동일 계열 인접 공정: 핵심 기술 지표 트레이드오프 분석(동일 작업 패키지 내 병렬 공정).
- `ent_process_p1_2_4` — 동일 계열 인접 공정: 개념 검증(Proof of Concept)(동일 작업 패키지 내 병렬 공정).
