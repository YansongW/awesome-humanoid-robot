---
$id: ent_process_p6_2_2
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Workspace and accessibility analysis
  zh: 工作空间与可达性分析
  ko: 작업 공간과 도달 가능성 분석
summary:
  en: 'Workspace Analysis Report: > 95% coverage of reachable region and key attitude'
  zh: '- P6.2.2.1 输入梳理与目标量化 - 整理「工作空间与可达性分析」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 《工作空间分析报告》：可达域包络、关键姿态覆盖度 > 95%
domains:
- 06_design_engineering
- 08_software_middleware
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

工作空间与可达性分析（Workspace and accessibility analysis）是人形机器人开发流程中 URDF 建模与运动学校核阶段（P6.2.2）的核心工序，通过蒙特卡洛采样等方法生成足端/手末端可达空间云图，验证机器人工作空间对 P2.2.1 人机工程需求的覆盖程度。它真正改变的不是"画一张可达域云图"这件事本身，而是让"人机工程需求"与"机构设计参数"之间第一次建立起可量化、可追溯的验收闭环。

## 核心内容

### 是什么：从关节空间到笛卡尔空间的映射可视化

工作空间分析的本质是求解机器人运动学正解在笛卡尔空间中的像集。对于人形机器人，通常关注两类工作空间：**足端工作空间**（决定步态规划的可行域）与**手末端工作空间**（决定操作任务的覆盖范围）。给定关节角度向量 $\boldsymbol{q} \in \mathbb{R}^n$，末端位置由正运动学映射 $f(\boldsymbol{q})$ 给出，工作空间定义为：

$$
\mathcal{W} = \left\{ \boldsymbol{p} \in \mathbb{R}^3 \mid \boldsymbol{p} = f(\boldsymbol{q}),\; \boldsymbol{q}_{\min} \leq \boldsymbol{q} \leq \boldsymbol{q}_{\max},\; \text{无自碰撞} \right\}
$$

其中 $\boldsymbol{q}_{\min}$ 与 $\boldsymbol{q}_{\max}$ 为关节极限，自碰撞约束由 URDF 中的碰撞几何体对检测实现。蒙特卡洛采样法通过随机生成大量关节角组合（通常数万至数十万组），计算末端位置并绘制点云，从而近似工作空间边界。

### 为什么存在：人形机器人"高自由度"带来的可达性盲区

人形机器人拥有 20–40 个自由度，远超工业机械臂的 6 轴。自由度越高，末端可达性对关节构型的依赖越敏感——某个位置可能只在特定肘部姿态下可达，而另一姿态下则完全不可达。若不在设计阶段验证可达性，会出现三类典型问题：

1. **机构干涉**：髋关节或肩关节的关节极限设置不当，导致末端无法到达人机工程需求指定的操作区域（如厨房台面高度、货架顶层）。
2. **姿态退化**：末端位置可达，但腕部或踝部姿态超出允许范围，导致抓取或踩踏姿态不可行。
3. **自碰撞隐患**：在可达域边缘，连杆之间可能发生碰撞，而纯运动学规划器不会主动规避。

P6.2.2 正是为消除这些盲区而设：它把 P2.2.1 的人机工程需求（如"手末端需覆盖 800–1600 mm 高度范围（示例值，工程判断）"）转化为可量化的覆盖度指标，并给出验收判据。

### 原理拆解

**① 蒙特卡洛采样：用随机性逼近确定性边界**

蒙特卡洛法的核心思想是：在关节空间内按均匀分布随机采样 $N$ 组关节角，代入正运动学求得末端位置，统计落入目标区域内的点数占比。采样数 $N$ 越大，近似精度越高，但计算成本线性增长。工程上通常取 $N = 10^4 \sim 10^5$，即可获得误差在 1% 以内的覆盖度估计（工程判断）。

**② 自碰撞检测：碰撞几何体的层次化加速**

URDF 中每个 link 可定义 `<collision>` 几何体（通常为简化后的球体、圆柱或网格）。自碰撞检测通过两两 link 的碰撞几何体对进行求交测试。为加速计算，常用 BVH（包围体层次结构）或 FCL 库进行粗检测与精检测两级过滤。在蒙特卡洛采样中，每组关节角都需执行一次自碰撞检测，因此碰撞几何体的简化程度直接影响分析速度。

**③ 覆盖度计算：需求区域与可达域的交集度量**

设需求区域为 $\mathcal{R}$（由 P2.2.1 人机工程需求定义，如矩形体或球体），可达域点云为 $\mathcal{W}_s$，则覆盖度定义为：

$$
\text{Coverage} = \frac{\left| \mathcal{W}_s \cap \mathcal{R} \right|}{\left| \mathcal{R} \right|} \times 100\%
$$

其中 $|\cdot|$ 表示体积或点数。完成标准要求关键姿态覆盖度 > 95%，即需求区域内至少 95% 的体积被可达域覆盖。

### 关键参数与规格

| 参数 | 规格/范围 | 来源 |
|------|-----------|------|
| 采样方法 | 蒙特卡洛采样 | 语料 |
| 可视化工具 | RViz | 语料 |
| 计算平台 | Matlab/Python | 语料 |
| 覆盖度验收阈值 | > 95% | 语料 |
| 候选方案数量 | ≥ 2 个 | 语料 |
| 输出物 | 《工作空间分析报告》 | 语料 |
| 关键约束 | 自碰撞约束、关节极限 | 语料 |

### 横向对比：蒙特卡洛采样 vs. 解析边界法 vs. 网格扫描法

| 方法 | 精度 | 计算成本 | 适用场景 |
|------|------|----------|----------|
| 解析边界法 | 精确 | 低 | 低自由度（≤6）机械臂 |
| 网格扫描法 | 高 | 高 | 高自由度但需规则网格 |
| 蒙特卡洛采样 | 统计近似 | 中 | 高自由度人形机器人 |

蒙特卡洛法的优势在于实现简单、对关节数不敏感，且天然支持自碰撞约束的嵌入；劣势是边界精度受采样数限制，且无法给出工作空间的解析表达式。对于人形机器人这种高自由度系统，蒙特卡洛法是工程上最务实的选择（工程判断）。

### 谁在用·应用案例

**P6.2.2 在开发流程中的位置**：该工序隶属于 URDF 建模与运动学校核阶段（P6），上游接收 P3.1.2/P3.1.3 的质量属性数据与 P5 阶段的结构设计成果，下游为 P10 运动控制算法开发提供可仿真的模型基础。

**典型应用场景**：

1. **足端工作空间验证**：在步态规划前，验证踝关节在支撑相与摆动相的可达范围是否覆盖预期步长与抬脚高度。
2. **手末端操作域验证**：对厨房、货架等典型家居场景，验证手末端可达域是否覆盖 P2.2.1 定义的操作区域。
3. **关节极限校核**：若覆盖度不足，反向调整关节极限或连杆长度，迭代至满足 > 95% 的验收阈值。

### 局限与边界

1. **静态分析的局限**：工作空间分析仅考虑运动学可达性，不包含动力学约束（如力矩极限、惯性效应）。一个运动学可达的点，在动态运动中可能因力矩不足而不可达。
2. **采样密度的两难**：采样数过少导致覆盖度估计方差大，过多则计算时间不可接受。工程上需在精度与速度间权衡（工程判断）。
3. **碰撞几何体简化误差**：URDF 中碰撞几何体为简化形状，与真实 CAD 模型存在偏差，可能导致自碰撞检测结果与实际不符。
4. **不包含环境障碍**：工作空间分析仅考虑机器人自身，不包含外部环境障碍物。环境可达性需在路径规划阶段另行验证。

### 常见误区

1. **"覆盖度 > 95% 意味着所有关键姿态都可达"**——错。覆盖度是体积占比，不保证每个离散姿态点都可达。关键姿态需单独逐点验证。
2. **"蒙特卡洛采样数越多越好"**——错。采样数超过一定阈值后，精度提升趋缓，而计算成本线性增长。工程上应通过收敛性测试确定合理采样数（工程判断）。
3. **"工作空间分析可以替代运动学仿真"**——错。工作空间分析是静态的、统计性的，无法替代动态运动学仿真中关于速度、加速度与奇异位形的验证。
4. **"自碰撞检测只需在采样时做一次"**——错。不同关节构型下的自碰撞状态不同，每组采样都必须独立执行检测。

### 相关知识

- `ent_process_p6` — 本工序所属的上级阶段，URDF 建模与运动学校核的整体流程与交付物定义。
- `ent_process_p6_2_1` — 同族相邻工序：正逆运动学解算验证（同一工作包内的并行工序）。
- `ent_process_p6_2_3` — 同族相邻工序：雅可比与速度/力传递分析（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Workspace and accessibility analysis is a core procedure in the URDF modeling and kinematic verification stage (P6.2.2) of the humanoid robot development process. It generates reachable space cloud maps for the foot end/hand end using methods such as Monte Carlo sampling, verifying the coverage of the robot's workspace against the ergonomic requirements of P2.2.1. What it truly changes is not the act of "drawing a reachable domain cloud map" itself, but rather establishing, for the first time, a quantifiable and traceable acceptance loop between "ergonomic requirements" and "mechanism design parameters."

## Content

### What it is: Mapping visualization from joint space to Cartesian space

The essence of workspace analysis is solving the image set of the robot's forward kinematics in Cartesian space. For humanoid robots, two types of workspaces are typically considered: the **foot-end workspace** (determining the feasible region for gait planning) and the **hand-end workspace** (determining the coverage range for manipulation tasks). Given a joint angle vector $\boldsymbol{q} \in \mathbb{R}^n$, the end-effector position is given by the forward kinematics mapping $f(\boldsymbol{q})$, and the workspace is defined as:

$$
\mathcal{W} = \left\{ \boldsymbol{p} \in \mathbb{R}^3 \mid \boldsymbol{p} = f(\boldsymbol{q}),\; \boldsymbol{q}_{\min} \leq \boldsymbol{q} \leq \boldsymbol{q}_{\max},\; \text{no self-collision} \right\}
$$

where $\boldsymbol{q}_{\min}$ and $\boldsymbol{q}_{\max}$ are joint limits, and the self-collision constraint is implemented through collision geometry pair detection in the URDF. The Monte Carlo sampling method generates a large number of random joint angle combinations (typically tens of thousands to hundreds of thousands), computes end-effector positions, and plots point clouds to approximate the workspace boundary.

### Why it exists: Reachability blind spots caused by the "high degrees of freedom" of humanoid robots

Humanoid robots possess 20–40 degrees of freedom, far exceeding the 6 axes of industrial manipulators. The higher the degrees of freedom, the more sensitive end-effector reachability becomes to joint configurations—a certain position may only be reachable with a specific elbow posture, while completely unreachable with another. If reachability is not verified during the design phase, three typical problems arise:

1. **Mechanism interference**: Improper joint limit settings for the hip or shoulder joints prevent the end-effector from reaching operation areas specified by ergonomic requirements (e.g., kitchen countertop height, top shelf levels).
2. **Posture degradation**: The end-effector position is reachable, but the wrist or ankle posture exceeds the allowable range, making grasping or stepping postures infeasible.
3. **Self-collision hazards**: At the edges of the reachable domain, links may collide with each other, and pure kinematic planners will not actively avoid this.

P6.2.2 exists precisely to eliminate these blind spots: it transforms the ergonomic requirements of P2.2.1 (e.g., "the hand end must cover a height range of 800–1600 mm (example value, engineering judgment)") into quantifiable coverage metrics and provides acceptance criteria.

### Principle breakdown

**① Monte Carlo sampling: Approximating deterministic boundaries with randomness**

The core idea of the Monte Carlo method is to randomly sample $N$ sets of joint angles uniformly in joint space, substitute them into forward kinematics to obtain end-effector positions, and count the proportion of points falling within the target region. The larger the sample size $N$, the higher the approximation accuracy, but the computational cost grows linearly. In engineering practice, $N = 10^4 \sim 10^5$ is typically used, yielding coverage estimates with errors within 1% (engineering judgment).

**② Self-collision detection: Hierarchical acceleration of collision geometries**

Each link in the URDF can define `<collision>` geometries (typically simplified spheres, cylinders, or meshes). Self-collision detection is performed through intersection tests between collision geometry pairs of two links. To accelerate computation, BVH (Bounding Volume Hierarchy) or the FCL library is commonly used for two-level filtering: broad-phase and narrow-phase detection. In Monte Carlo sampling, each set of joint angles requires one self-collision check, so the degree of simplification of collision geometries directly affects analysis speed.

**③ Coverage calculation: Intersection metric between the requirement region and the reachable domain**

Let the requirement region be $\mathcal{R}$ (defined by the ergonomic requirements of P2.2.1, such as a rectangular box or sphere) and the reachable domain point cloud be $\mathcal{W}_s$. The coverage is then defined as:

$$
\text{Coverage} = \frac{\left| \mathcal{W}_s \cap \mathcal{R} \right|}{\left| \mathcal{R} \right|} \times 100\%
$$

where $|\cdot|$ denotes volume or point count. The completion standard requires key posture coverage > 95%, meaning at least 95% of the volume within the requirement region is covered by the reachable domain.

### Key parameters and specifications

| Parameter | Specification/Range | Source |
|-----------|---------------------|--------|
| Sampling method | Monte Carlo sampling | Corpus |
| Visualization tool | RViz | Corpus |
| Computing platform | Matlab/Python | Corpus |
| Coverage acceptance threshold | > 95% | Corpus |
| Number of candidate solutions | ≥ 2 | Corpus |
| Deliverable | "Workspace Analysis Report" | Corpus |
| Key constraints | Self-collision constraints, joint limits | Corpus |

### Horizontal comparison: Monte Carlo sampling vs. analytical boundary method vs. grid scanning method

| Method | Accuracy | Computational cost | Applicable scenarios |
|--------|----------|--------------------|----------------------|
| Analytical boundary method | Exact | Low | Low-DOF (≤6) manipulators |
| Grid scanning method | High | High | High-DOF but requires regular grids |
| Monte Carlo sampling | Statistical approximation | Medium | High-DOF humanoid robots |

The advantages of the Monte Carlo method lie in its simple implementation, insensitivity to the number of joints, and natural support for embedding self-collision constraints; its disadvantages are that boundary accuracy is limited by sample size and it cannot provide an analytical expression of the workspace. For high-DOF systems like humanoid robots, the Monte Carlo method is the most pragmatic engineering choice (engineering judgment).

### Who uses it: Application cases

**Position of P6.2.2 in the development process**: This procedure belongs to the URDF modeling and kinematic verification stage (P6). Upstream, it receives quality attribute data from P3.1.2/P3.1.3 and structural design outputs from the P5 stage; downstream, it provides a simulatable model foundation for P10 motion control algorithm development.

**Typical application scenarios**:

1. **Foot-end workspace verification**: Before gait planning, verify whether the ankle joint's reachable range during the stance and swing phases covers the expected step length and foot lift height.
2. **Hand-end manipulation domain verification**: For typical home scenarios such as kitchens and shelves, verify whether the hand-end reachable domain covers the operation areas defined by P2.2.1.
3. **Joint limit verification**: If coverage is insufficient, iteratively adjust joint limits or link lengths until the > 95% acceptance threshold is met.

### Limitations and boundaries

1. **Limitations of static analysis**: Workspace analysis only considers kinematic reachability and does not include dynamic constraints (such as torque limits or inertial effects). A kinematically reachable point may be unreachable during dynamic motion due to insufficient torque.
2. **The sampling density dilemma**: Too few samples lead to high variance in coverage estimates, while too many make computation time unacceptable. Engineering requires a trade-off between accuracy and speed (engineering judgment).
3. **Collision geometry simplification errors**: Collision geometries in the URDF are simplified shapes that deviate from the actual CAD model, potentially causing self-collision detection results to differ from reality.
4. **Exclusion of environmental obstacles**: Workspace analysis only considers the robot itself and does not include external environmental obstacles. Environmental reachability must be verified separately during the path planning stage.

### Common misconceptions

1. **"Coverage > 95% means all key postures are reachable"**—Wrong. Coverage is a volume ratio and does not guarantee that every discrete posture point is reachable. Key postures must be verified point-by-point individually.
2. **"The more Monte Carlo samples, the better"**—Wrong. Beyond a certain threshold, accuracy improvement plateaus while computational cost grows linearly. In engineering, the appropriate sample size should be determined through convergence testing (engineering judgment).
3. **"Workspace analysis can replace kinematic simulation"**—Wrong. Workspace analysis is static and statistical; it cannot replace dynamic kinematic simulation for verifying velocity, acceleration, and singular configurations.
4. **"Self-collision detection only needs to be performed once during sampling"**—Wrong. Self-collision states differ across joint configurations, and each sample must independently undergo detection.

### Related knowledge

- `ent_process_p6` — The parent stage to which this procedure belongs, covering the overall process and deliverable definitions for URDF modeling and kinematic verification.
- `ent_process_p6_2_1` — Adjacent procedure in the same family: forward and inverse kinematics solution verification (parallel procedure within the same work package).
- `ent_process_p6_2_3` — Adjacent procedure in the same family: Jacobian and velocity/force transmission analysis (parallel procedure within the same work package).

## 개요

작업공간 및 접근성 분석(Workspace and accessibility analysis)은 휴머노이드 로봇 개발 프로세스에서 URDF 모델링 및 운동학교核 단계(P6.2.2)의 핵심 공정으로, 몬테카를로 샘플링 등의 방법을 통해 발끝/손끝 도달 가능 공간 클라우드 맵을 생성하여 로봇 작업공간이 P2.2.1 인간공학 요구사항을 충족하는 범위를 검증합니다. 이 공정이 진정으로 바꾸는 것은 "도달 영역 클라우드 맵을 그리는 것" 자체가 아니라, "인간공학 요구사항"과 "메커니즘 설계 파라미터" 사이에 처음으로 정량적이고 추적 가능한 검수 폐루프를 구축한다는 점입니다.

## 핵심 내용

### 무엇인가: 관절 공간에서 데카르트 공간으로의 매핑 시각화

작업공간 분석의 본질은 로봇 운동학 정해(正解)를 데카르트 공간에서의 상(image) 집합으로 푸는 것입니다. 휴머노이드 로봇의 경우 일반적으로 두 가지 유형의 작업공간을 고려합니다: **발끝 작업공간**(보행 계획의 실현 가능 영역 결정)과 **손끝 작업공간**(조작 작업의 커버 범위 결정). 관절 각도 벡터 $\boldsymbol{q} \in \mathbb{R}^n$가 주어졌을 때, 말단 위치는 정운동학 매핑 $f(\boldsymbol{q})$로 주어지며, 작업공간은 다음과 같이 정의됩니다:

$$
\mathcal{W} = \left\{ \boldsymbol{p} \in \mathbb{R}^3 \mid \boldsymbol{p} = f(\boldsymbol{q}),\; \boldsymbol{q}_{\min} \leq \boldsymbol{q} \leq \boldsymbol{q}_{\max},\; \text{자기충돌 없음} \right\}
$$

여기서 $\boldsymbol{q}_{\min}$과 $\boldsymbol{q}_{\max}$는 관절 한계이며, 자기충돌 제약은 URDF의 충돌 지오메트리 쌍 검출로 구현됩니다. 몬테카를로 샘플링 방법은 무작위로 많은 관절 각도 조합(일반적으로 수만~수십만 개)을 생성하고, 말단 위치를 계산하여 포인트 클라우드를 그려 작업공간 경계를 근사합니다.

### 왜 존재하는가: 휴머노이드 로봇의 "고자유도"로 인한 도달 가능성 사각지대

휴머노이드 로봇은 20–40개의 자유도를 가지며, 산업용 로봇 팔의 6축을 훨씬 초과합니다. 자유도가 높을수록 말단 도달 가능성은 관절 구성에 더 민감하게 의존합니다—특정 위치는 특정 팔꿈치 자세에서만 도달 가능하고, 다른 자세에서는 완전히 도달 불가능할 수 있습니다. 설계 단계에서 도달 가능성을 검증하지 않으면 세 가지 전형적인 문제가 발생합니다:

1. **메커니즘 간섭**: 고관절 또는 어깨 관절의 관절 한계 설정이 잘못되어 말단이 인간공학 요구사항이 지정한 조작 영역(예: 주방 조리대 높이, 선반 최상단)에 도달할 수 없습니다.
2. **자세 저하**: 말단 위치는 도달 가능하지만, 손목 또는 발목 자세가 허용 범위를 벗어나 그립 또는 디딤 자세가 불가능합니다.
3. **자기충돌 위험**: 도달 가능 영역 가장자리에서 링크 간 충돌이 발생할 수 있으며, 순수 운동학적 플래너는 이를 능동적으로 회피하지 않습니다.

P6.2.2는 바로 이러한 사각지대를 제거하기 위해 설계되었습니다: P2.2.1의 인간공학 요구사항(예: "손끝이 800–1600 mm 높이 범위를 커버해야 함(예시 값, 공학적 판단)")을 정량화 가능한 커버리지 지표로 변환하고 검수 판정 기준을 제공합니다.

### 원리 분석

**① 몬테카를로 샘플링: 무작위성으로 결정적 경계 근사**

몬테카를로 방법의 핵심 아이디어는 관절 공간에서 균등 분포로 $N$개의 관절 각도 세트를 무작위 샘플링하고, 정운동학에 대입하여 말단 위치를 구한 후, 목표 영역 내에落入하는 포인트 비율을 계산하는 것입니다. 샘플 수 $N$이 클수록 근사 정밀도는 높아지지만 계산 비용은 선형적으로 증가합니다. 공학적으로 일반적으로 $N = 10^4 \sim 10^5$를 사용하면 1% 이내의 오차로 커버리지 추정이 가능합니다(공학적 판단).

**② 자기충돌 검출: 충돌 지오메트리의 계층적 가속**

URDF의 각 링크는 `<collision>` 지오메트리(일반적으로 단순화된 구, 원통 또는 메시)를 정의할 수 있습니다. 자기충돌 검출은 링크 쌍의 충돌 지오메트리 쌍에 대한 교차 테스트를 통해 수행됩니다. 계산을 가속화하기 위해 일반적으로 BVH(경계 볼륨 계층 구조) 또는 FCL 라이브러리를 사용하여 조검출과 정검출의 2단계 필터링을 수행합니다. 몬테카를로 샘플링에서 각 관절 각도 세트마다 자기충돌 검출을 한 번씩 수행해야 하므로, 충돌 지오메트리의 단순화 정도가 분석 속도에 직접적인 영향을 미칩니다.

**③ 커버리지 계산: 요구 영역과 도달 가능 영역의 교집합 측정**

요구 영역을 $\mathcal{R}$(P2.2.1 인간공학 요구사항으로 정의, 예: 직육면체 또는 구), 도달 가능 영역 포인트 클라우드를 $\mathcal{W}_s$라고 하면, 커버리지는 다음과 같이 정의됩니다:

$$
\text{Coverage} = \frac{\left| \mathcal{W}_s \cap \mathcal{R} \right|}{\left| \mathcal{R} \right|} \times 100\%
$$

여기서 $|\cdot|$는 부피 또는 포인트 수를 나타냅니다. 완료 기준은 핵심 자세 커버리지 > 95%, 즉 요구 영역 내 최소 95%의 부피가 도달 가능 영역으로 커버되어야 합니다.

### 핵심 파라미터 및 사양

| 파라미터 | 사양/범위 | 출처 |
|------|-----------|------|
| 샘플링 방법 | 몬테카를로 샘플링 | 말뭉치 |
| 시각화 도구 | RViz | 말뭉치 |
| 계산 플랫폼 | Matlab/Python | 말뭉치 |
| 커버리지 검수 임계값 | > 95% | 말뭉치 |
| 후보 방안 수 | ≥ 2개 | 말뭉치 |
| 산출물 | 《작업공간 분석 보고서》 | 말뭉치 |
| 핵심 제약 | 자기충돌 제약, 관절 한계 | 말뭉치 |

### 횡적 비교: 몬테카를로 샘플링 vs. 해석적 경계법 vs. 그리드 스캔법

| 방법 | 정밀도 | 계산 비용 | 적용 시나리오 |
|------|------|----------|----------|
| 해석적 경계법 | 정확 | 낮음 | 저자유도(≤6) 로봇 팔 |
| 그리드 스캔법 | 높음 | 높음 | 고자유도지만 규칙적 그리드 필요 |
| 몬테카를로 샘플링 | 통계적 근사 | 중간 | 고자유도 휴머노이드 로봇 |

몬테카를로 방법의 장점은 구현이 간단하고 관절 수에 둔감하며, 자기충돌 제약의 내장을 자연스럽게 지원한다는 점입니다. 단점은 경계 정밀도가 샘플 수에 제한되고 작업공간의 해석적 표현을 제공할 수 없다는 점입니다. 휴머노이드 로봇과 같은 고자유도 시스템의 경우 몬테카를로 방법이 공학적으로 가장 실용적인 선택입니다(공학적 판단).

### 누가 사용하는가·응용 사례

**P6.2.2의 개발 프로세스 내 위치**: 이 공정은 URDF 모델링 및 운동학교核 단계(P6)에 속하며, 상류는 P3.1.2/P3.1.3의 품질 속성 데이터와 P5 단계의 구조 설계 성과를 수신하고, 하류는 P10 운동 제어 알고리즘 개발에 시뮬레이션 가능한 모델 기반을 제공합니다.

**전형적인 응용 시나리오**:

1. **발끝 작업공간 검증**: 보행 계획 전에 발목 관절이 지지상과 스윙상에서 예상 보폭과 발 들기 높이를 커버하는지 검증합니다.
2. **손끝 조작 영역 검증**: 주방, 선반 등 전형적인 가정 시나리오에서 손끝 도달 가능 영역이 P2.2.1에서 정의한 조작 영역을 커버하는지 검증합니다.
3. **관절 한계 검교정**: 커버리지가 부족하면 관절 한계 또는 링크 길이를 역방향으로 조정하고, > 95% 검수 임계값을 충족할 때까지 반복합니다.

### 한계와 경계

1. **정적 분석의 한계**: 작업공간 분석은 운동학적 도달 가능성만 고려하며 동역학적 제약(토크 한계, 관성 효과 등)은 포함하지 않습니다. 운동학적으로 도달 가능한 지점도 동적 운동에서는 토크 부족으로 도달 불가능할 수 있습니다.
2. **샘플 밀도의 딜레마**: 샘플 수가 너무 적으면 커버리지 추정 분산이 커지고, 너무 많으면 계산 시간이 허용 불가능해집니다. 공학적으로 정밀도와 속도 사이의 균형이 필요합니다(공학적 판단).
3. **충돌 지오메트리 단순화 오차**: URDF의 충돌 지오메트리는 단순화된 형상으로 실제 CAD 모델과 차이가 있어 자기충돌 검출 결과가 실제와 다를 수 있습니다.
4. **환경 장애물 미포함**: 작업공간 분석은 로봇 자체만 고려하며 외부 환경 장애물은 포함하지 않습니다. 환경 접근성은 경로 계획 단계에서 별도로 검증해야 합니다.

### 흔한 오해

1. **"커버리지 > 95%는 모든 핵심 자세가 도달 가능함을 의미한다"**—틀림. 커버리지는 부피 비율이며, 모든 개별 자세 포인트의 도달 가능성을 보장하지 않습니다. 핵심 자세는 별도로 포인트별 검증이 필요합니다.
2. **"몬테카를로 샘플 수가 많을수록 좋다"**—틀림. 샘플 수가 특정 임계값을 초과하면 정밀도 향상은 둔화되는 반면 계산 비용은 선형적으로 증가합니다. 공학적으로 수렴성 테스트를 통해 합리적인 샘플 수를 결정해야 합니다(공학적 판단).
3. **"작업공간 분석이 운동학 시뮬레이션을 대체할 수 있다"**—틀림. 작업공간 분석은 정적이고 통계적인 것이며, 동적 운동학 시뮬레이션에서의 속도, 가속도 및 특이 자세 검증을 대체할 수 없습니다.
4. **"자기충돌 검출은 샘플링 시 한 번만 수행하면 된다"**—틀림. 관절 구성에 따라 자기충돌 상태가 다르므로 각 샘플 세트마다 독립적으로 검출을 수행해야 합니다.

### 관련 지식

- `ent_process_p6` — 본 공정이 속한 상위 단계, URDF 모델링 및 운동학교核의 전체 프로세스와 산출물 정의.
- `ent_process_p6_2_1` — 동일 계열 인접 공정: 정/역운동학 해석 검증(동일 작업 패키지 내 병행 공정).
- `ent_process_p6_2_3` — 동일 계열 인접 공정: 야코비안 및 속도/힘 전달 분석(동일 작업 패키지 내 병행 공정).
