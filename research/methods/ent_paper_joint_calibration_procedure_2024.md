---
$id: ent_paper_joint_calibration_procedure_2024
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Joint Calibration Procedure
  zh: 关节标定流程
  ko: Joint Calibration Procedure
summary:
  en: Post-assembly procedure to determine absolute zero positions, encoder offsets, and direction signs for each actuator.
  zh: 装配后确定每个执行器绝对零位、编码器偏置和方向标志的流程。
  ko: 조립 후 각 액추에이터의 절대 원점, 인코더 오프셋 및 방향 부호를 결정하는 절차.
domains:
- 04_assembly_integration_testing
- 06_design_engineering
layers:
- midstream
functional_roles:
- knowledge
- intelligence
tags:
- assembly
- calibration
- encoder
- joint
- method
theoretical_depth:
- system
verification:
  status: partially_verified
  reviewed_by: ai
  reviewed_at: '2026-07-02'
  confidence: medium
  notes: 'Imported via ingestion framework from source_type=website. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: website
  title: Joint Calibration Procedure
  url: https://en.wikipedia.org/wiki/Robot_calibration
  date: '2024'
  accessed_at: '2026-07-02'
---
## 概述

关节标定流程（Joint Calibration Procedure）是装配完成后，确定每个执行器绝对零位、编码器偏置和方向标志的系统性方法。它解决的是机器人从"零件装好"到"能精确运动"之间不可跳过的关键一步——没有它，电机转了多少圈、关节处于什么角度都无从谈起。它真正改变的不是装配车间的操作方式，而是让运动学模型从纸面走向实物的可信度。

## 核心内容

### 是什么：准确定义

关节标定流程属于**方法（method）**类型，英文名称为 *Joint Calibration Procedure*。其核心任务可拆解为三个部分：

- **绝对零位（absolute zero position）**：关节机械结构上的基准角度位置，通常由机械限位、定位销或零位标记确定。
- **编码器偏置（encoder offset）**：电机或关节编码器读数与机械零位之间的固定差值。由于编码器上电时读数随机，必须通过标定建立"电气读数→机械角度"的映射关系。
- **方向标志（direction flag）**：编码器计数方向与关节实际旋转方向的一致性标志。方向接反会导致正反馈，使控制系统瞬间发散。

该流程属于组装集成测试（assembly integration testing）与设计工程（design engineering）两个领域，处于价值链的中游层级——它连接了上游的零部件制造与下游的整机调试，是机器人从"能转"到"转得准"的分水岭。

### 为什么存在：痛点与历史定位

在关节标定流程出现之前，机器人装配后的调试依赖人工目测和经验判断。操作者手动将关节转到目测的"零位"，再在控制器中记录当前编码器读数作为偏置。这种方式存在三个根本性痛点：

**第一，精度不可控。** 人工目测的重复定位精度通常在数度量级，而现代人形机器人的运动规划要求关节角度误差在 0.1° 以内（工程判断）。以 300 mm 长的连杆为例，0.1° 的角度误差在末端会产生约 0.5 mm 的位置偏差（按语料中"±0.1 mm 偏差放大到 ±(n×0.1) mm"的推算逻辑）。

**第二，效率低下。** 一台全尺寸人形机器人有 20–40 个关节（工程判断），逐个手动标定需要数小时，且容易遗漏或重复。

**第三，可追溯性差。** 人工标定结果依赖操作者记录，难以标准化、难以复核、难以与运动学模型自动对接。

从历史定位看，关节标定流程是机器人产业从"实验室样机"走向"量产产品"的必然产物。它借鉴了工业机械臂领域成熟的运动学校准方法，但针对人形机器人的高自由度、浮动基座和多闭合运动链特点做了专门适配。

### 原理拆解

**① 编码器读数与机械角度的映射**

关节标定的数学本质是建立编码器原始读数 \( \theta_{\text{raw}} \) 与机械关节角 \( \theta_{\text{joint}} \) 之间的仿射变换：

$$
\theta_{\text{joint}} = s \cdot \theta_{\text{raw}} + \theta_{\text{offset}}
$$

其中 \( s \) 为方向标志（+1 或 -1），\( \theta_{\text{offset}} \) 为编码器偏置。标定流程的目标就是确定这两个参数。

**② 与运动学校准的关系**

关节标定是运动学校准（kinematic calibration）的前置步骤。运动学校准通过外部测量数据辨识真实几何参数，其核心方程为：

$$
\Delta \mathbf{p} = \sum_{i=1}^{n} \left( \frac{\partial \mathbf{p}}{\partial a_i} \Delta a_i + \frac{\partial \mathbf{p}}{\partial \alpha_i} \Delta \alpha_i + \frac{\partial \mathbf{p}}{\partial d_i} \Delta d_i + \frac{\partial \mathbf{p}}{\partial \theta_i} \Delta \theta_i \right) = \mathbf{J}_{\text{cal}} \, \Delta \boldsymbol{\phi}
$$

其中 \( \mathbf{J}_{\text{cal}} \) 为标定雅可比，\( \Delta \boldsymbol{\phi} \) 为待辨识的参数误差向量。如果关节零位本身不准确，\( \Delta \theta_i \) 项就会污染整个辨识过程——这就是为什么关节标定必须在运动学校准之前完成。

**③ 可辨识性约束**

关节标定还面临**可辨识性（identifiability）**问题：某些参数误差对末端位姿影响很小，或与关节零点误差耦合，导致无法独立辨识。对于人形机器人，由于存在浮动基和多个闭合运动链（双腿同时触地），标定问题比固定基机械臂更复杂，通常需要结合 IMU、视觉与运动捕捉系统共同完成。

### 关键参数与规格

| 参数 | 典型值/范围 | 说明 |
|---|---|---|
| 编码器分辨率 | 17–23 bit（工程判断） | 决定角度测量精度的下限 |
| 零位重复定位精度 | ±0.01°–±0.1°（工程判断） | 多次标定结果的一致性 |
| 标定时间 | 单关节 1–5 分钟（工程判断） | 含机械对准与软件记录 |
| 温度范围 | 常温 20±5°C（工程判断） | 温度变化会引起机械零位漂移 |
| 方向标志 | +1 或 -1 | 与电机接线和编码器安装方向相关 |

### 横向对比

关节标定与运动学校准是两个不同层级的过程，常被混淆：

| 维度 | 关节标定 | 运动学校准 |
|---|---|---|
| 标定对象 | 单个关节的零位、偏置、方向 | 整机运动学参数（连杆长度、扭角等） |
| 测量工具 | 机械限位、定位销、编码器读数 | 激光跟踪仪、三坐标测量机、视觉测量 |
| 输出结果 | 每个关节的偏置与方向标志 | 修正后的 DH 或 PoE 参数 |
| 执行时机 | 装配后、上电调试前 | 整机装配完成后、出厂前 |
| 精度影响 | 关节角度绝对精度 | 末端绝对定位精度 |

两者是递进关系：关节标定是运动学校准的前提，运动学校准是关节标定的延伸。高端人形机器人整机出厂前通常进行完整的运动学校准流程，可将末端绝对误差从数毫米降至 ±0.3 mm 甚至更低。

### 谁在用·应用案例

**宇树 H1 人形机器人**（`ent_robot_unitree_h1_humanoid_robot_2024`）是关节标定流程的典型应用案例。H1 采用宇树自研 M107 永磁同步电机，膝关节峰值扭矩达 360 N·m，峰值扭矩密度约 189 N·m/kg。其 4 DOF 基础版手臂（可选配 H1-2 升级至 7 DOF）在装配后必须逐一完成关节标定，才能实现 3.3 m/s 奔跑速度下的精确运动控制。

在 H1 的标定流程中，每个关节模组（集成电机、减速器、传感器的标准化单元）需要：

1. 通过机械限位或定位销确定绝对零位；
2. 记录当前编码器读数作为偏置；
3. 验证方向标志，确保正反馈不会发生；
4. 将标定结果写入控制器参数文件，供运动学模型调用。

### 局限与边界

**第一，温度敏感性。** 关节标定在常温下完成，但机器人运行时温度升高会引起机械结构热膨胀，导致零位漂移。对于高精度应用，需要引入温度补偿或定期重新标定。

**第二，机械磨损。** 减速器偏心、轴承间隙会随使用时间增大，使初始标定结果逐渐失效。这解释了为什么工业机器人通常要求定期重新标定。

**第三，不可辨识参数。** 某些参数误差对末端位姿影响很小，或与关节零点误差耦合，导致无法独立辨识。提高可辨识性的方法包括：选择测量位形使标定雅可比条件数较小、固定部分已知参数、采用闭环运动链约束等。

**第四，浮动基挑战。** 人形机器人存在浮动基和多个闭合运动链（双腿同时触地），标定问题比固定基机械臂更复杂，通常需要结合 IMU、视觉与运动捕捉系统共同完成。

### 常见误区

1. **"关节标定就是运动学校准"**——错。关节标定只解决每个关节的零位、偏置和方向，不涉及连杆长度、扭角等几何参数的辨识。后者属于运动学校准的范畴，需要外部测量设备。

2. **"编码器分辨率高就不需要标定"**——错。编码器分辨率决定测量精度下限，但偏置和方向是系统性的固定误差，不通过标定无法消除。高分辨率编码器只是让标定结果更精确，不能替代标定本身。

3. **"标定一次就永远有效"**——错。温度变化、机械磨损、减速器偏心都会使标定结果逐渐失效。对于高精度应用，需要定期重新标定。

4. **"方向标志无所谓，反正软件里可以改"**——错。方向接反会导致正反馈，使控制系统瞬间发散。虽然理论上可以在软件中反转，但如果在标定阶段未发现，调试阶段会浪费大量时间排查。

### 相关知识

- `ent_robot_unitree_h1_humanoid_robot_2024` — 宇树 H1 是关节标定流程的典型应用对象，其 4 DOF 基础版手臂和 7 DOF 升级方案均需在装配后完成关节标定才能实现高动态运动控制。
- `ent_method_system_identification` — 关节标定与参数辨识共享回归估计的数学框架，标定可视为辨识的工程化子集。

## 参考

- [Robot calibration - Wikipedia](https://en.wikipedia.org/wiki/Robot_calibration)
- [Chapter 08 - 运动学与动力学基础](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [Humanoid Full Development Workflow](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)

## Overview

The Joint Calibration Procedure is a systematic method for determining the absolute zero position, encoder offset, and direction flag of each actuator after assembly. It addresses the critical, non-skippable step in transitioning a robot from "parts assembled" to "capable of precise motion"—without it, there is no basis for understanding how many turns the motor has made or what angle a joint is at. What it truly changes is not the operational methods on the assembly floor, but the credibility of the kinematic model as it moves from paper to physical reality.

## Content

### What It Is: Precise Definition

The Joint Calibration Procedure falls under the **method** category, with the English name *Joint Calibration Procedure*. Its core tasks can be broken down into three parts:

- **Absolute zero position**: The reference angular position on the joint's mechanical structure, typically determined by mechanical limits, alignment pins, or zero marks.
- **Encoder offset**: The fixed difference between the motor or joint encoder reading and the mechanical zero position. Since encoder readings are random at power-up, a mapping from "electrical reading → mechanical angle" must be established through calibration.
- **Direction flag**: An indicator of consistency between the encoder counting direction and the actual joint rotation direction. A reversed direction can cause positive feedback, leading to instantaneous divergence of the control system.

This procedure belongs to both assembly integration testing and design engineering, occupying a mid-stream position in the value chain—it connects upstream component manufacturing with downstream system commissioning, serving as the dividing line between a robot that "can rotate" and one that "rotates accurately."

### Why It Exists: Pain Points and Historical Context

Before the Joint Calibration Procedure existed, post-assembly robot tuning relied on manual visual inspection and empirical judgment. Operators manually rotated joints to a visually estimated "zero position" and then recorded the current encoder reading as the offset in the controller. This approach had three fundamental pain points:

**First, precision was uncontrollable.** The repeatability of manual visual inspection is typically on the order of several degrees, whereas modern humanoid robot motion planning requires joint angle errors within 0.1° (engineering judgment). For a 300 mm link, a 0.1° angular error produces approximately 0.5 mm of positional deviation at the end effector (following the extrapolation logic of "±0.1 mm deviation amplified to ±(n×0.1) mm" from the source material).

**Second, efficiency was low.** A full-size humanoid robot has 20–40 joints (engineering judgment), and manually calibrating each one takes hours, with a high risk of omissions or repetitions.

**Third, traceability was poor.** Manual calibration results depended on operator records, making standardization, verification, and automatic integration with kinematic models difficult.

From a historical perspective, the Joint Calibration Procedure is an inevitable product of the robotics industry's transition from "laboratory prototypes" to "mass-produced products." It borrows mature kinematic calibration methods from the industrial manipulator field but has been specifically adapted for the high degrees of freedom, floating base, and multiple closed kinematic chains characteristic of humanoid robots.

### Principle Breakdown

**① Mapping Between Encoder Readings and Mechanical Angles**

The mathematical essence of joint calibration is establishing an affine transformation between the raw encoder reading \( \theta_{\text{raw}} \) and the mechanical joint angle \( \theta_{\text{joint}} \):

$$
\theta_{\text{joint}} = s \cdot \theta_{\text{raw}} + \theta_{\text{offset}}
$$

where \( s \) is the direction flag (+1 or -1) and \( \theta_{\text{offset}} \) is the encoder offset. The goal of the calibration procedure is to determine these two parameters.

**② Relationship with Kinematic Calibration**

Joint calibration is a prerequisite step for kinematic calibration. Kinematic calibration identifies true geometric parameters through external measurement data, with the core equation:

$$
\Delta \mathbf{p} = \sum_{i=1}^{n} \left( \frac{\partial \mathbf{p}}{\partial a_i} \Delta a_i + \frac{\partial \mathbf{p}}{\partial \alpha_i} \Delta \alpha_i + \frac{\partial \mathbf{p}}{\partial d_i} \Delta d_i + \frac{\partial \mathbf{p}}{\partial \theta_i} \Delta \theta_i \right) = \mathbf{J}_{\text{cal}} \, \Delta \boldsymbol{\phi}
$$

where \( \mathbf{J}_{\text{cal}} \) is the calibration Jacobian and \( \Delta \boldsymbol{\phi} \) is the vector of parameter errors to be identified. If the joint zero position itself is inaccurate, the \( \Delta \theta_i \) term will contaminate the entire identification process—this is why joint calibration must be completed before kinematic calibration.

**③ Identifiability Constraints**

Joint calibration also faces the **identifiability** problem: certain parameter errors have minimal impact on the end-effector pose, or are coupled with joint zero errors, making independent identification impossible. For humanoid robots, due to the floating base and multiple closed kinematic chains (both legs in contact with the ground simultaneously), the calibration problem is more complex than for fixed-base manipulators and typically requires a combination of IMU, vision, and motion capture systems.

### Key Parameters and Specifications

| Parameter | Typical Value/Range | Description |
|---|---|---|
| Encoder resolution | 17–23 bit (engineering judgment) | Determines the lower bound of angular measurement precision |
| Zero position repeatability | ±0.01°–±0.1° (engineering judgment) | Consistency across multiple calibration runs |
| Calibration time | 1–5 minutes per joint (engineering judgment) | Includes mechanical alignment and software recording |
| Temperature range | Room temperature 20±5°C (engineering judgment) | Temperature changes can cause mechanical zero drift |
| Direction flag | +1 or -1 | Related to motor wiring and encoder mounting direction |

### Horizontal Comparison

Joint calibration and kinematic calibration are two processes at different levels that are often confused:

| Dimension | Joint Calibration | Kinematic Calibration |
|---|---|---|
| Calibration target | Zero position, offset, direction of individual joints | Whole-robot kinematic parameters (link lengths, twist angles, etc.) |
| Measurement tools | Mechanical limits, alignment pins, encoder readings | Laser trackers, coordinate measuring machines, vision measurement |
| Output | Offset and direction flag for each joint | Corrected DH or PoE parameters |
| Timing | After assembly, before power-on debugging | After full robot assembly, before shipment |
| Precision impact | Absolute accuracy of joint angles | Absolute positioning accuracy of the end effector |

The two are progressive: joint calibration is the prerequisite for kinematic calibration, and kinematic calibration is an extension of joint calibration. High-end humanoid robots typically undergo a complete kinematic calibration process before shipment, which can reduce absolute end-effector errors from several millimeters to ±0.3 mm or even lower.

### Who Uses It: Application Cases

**Unitree H1 Humanoid Robot** (`ent_robot_unitree_h1_humanoid_robot_2024`) is a typical application case of the Joint Calibration Procedure. The H1 uses Unitree's self-developed M107 permanent magnet synchronous motor, with a peak knee joint torque of 360 N·m and a peak torque density of approximately 189 N·m/kg. Its 4 DOF basic version arm (upgradeable to 7 DOF with the H1-2 option) must undergo joint calibration for each joint after assembly to achieve precise motion control at a running speed of 3.3 m/s.

In the H1's calibration procedure, each joint module (a standardized unit integrating motor, reducer, and sensors) requires:

1. Determining the absolute zero position via mechanical limits or alignment pins;
2. Recording the current encoder reading as the offset;
3. Verifying the direction flag to ensure positive feedback cannot occur;
4. Writing the calibration results to the controller parameter file for use by the kinematic model.

### Limitations and Boundaries

**First, temperature sensitivity.** Joint calibration is performed at room temperature, but temperature increases during robot operation cause thermal expansion of mechanical structures, leading to zero drift. For high-precision applications, temperature compensation or periodic recalibration is required.

**Second, mechanical wear.** Reducer eccentricity and bearing clearance increase with usage time, gradually invalidating initial calibration results. This explains why industrial robots typically require periodic recalibration.

**Third, non-identifiable parameters.** Certain parameter errors have minimal impact on end-effector pose, or are coupled with joint zero errors, making independent identification impossible. Methods to improve identifiability include: selecting measurement configurations that yield a well-conditioned calibration Jacobian, fixing certain known parameters, and employing closed-loop kinematic chain constraints.

**Fourth, floating base challenges.** Humanoid robots have a floating base and multiple closed kinematic chains (both legs in contact with the ground simultaneously), making the calibration problem more complex than for fixed-base manipulators. This typically requires combining IMU, vision, and motion capture systems.

### Common Misconceptions

1. **"Joint calibration is the same as kinematic calibration"**—Wrong. Joint calibration only addresses the zero position, offset, and direction of each joint; it does not involve identifying geometric parameters such as link lengths or twist angles. The latter falls under kinematic calibration and requires external measurement equipment.

2. **"High encoder resolution means calibration is unnecessary"**—Wrong. Encoder resolution determines the lower bound of measurement precision, but offset and direction are systematic fixed errors that cannot be eliminated without calibration. High-resolution encoders only make calibration results more precise; they cannot replace calibration itself.

3. **"Calibrate once, valid forever"**—Wrong. Temperature changes, mechanical wear, and reducer eccentricity all cause calibration results to gradually degrade. For high-precision applications, periodic recalibration is required.

4. **"The direction flag doesn't matter; it can be changed in software anyway"**—Wrong. A reversed direction causes positive feedback, leading to instantaneous divergence of the control system. Although it can theoretically be reversed in software, if not detected during the calibration phase, debugging will waste significant time troubleshooting.

### Related Knowledge

- `ent_robot_unitree_h1_humanoid_robot_2024` — The Unitree H1 is a typical application target of the Joint Calibration Procedure; both its 4 DOF basic version arm and 7 DOF upgrade option require joint calibration after assembly to achieve high-dynamic motion control.
- `ent_method_system_identification` — Joint calibration and parameter identification share the mathematical framework of regression estimation; calibration can be viewed as an engineering-oriented subset of identification.

## 개요

관절 캘리브레이션 절차(Joint Calibration Procedure)는 조립 완료 후 각 액추에이터의 절대 영점, 엔코더 오프셋 및 방향 플래그를 결정하는 체계적인 방법입니다. 이는 로봇이 "부품 조립 완료"에서 "정밀 운동 가능" 상태로 넘어가는 데 있어 생략할 수 없는 핵심 단계를 해결합니다——이것 없이는 모터가 몇 바퀴 돌았는지, 관절이 어떤 각도에 있는지조차 알 수 없습니다. 이 절차가 실제로 바꾸는 것은 조립 공장의 작업 방식이 아니라, 운동학 모델이 종이 위에서 실물로 옮겨질 때의 신뢰성입니다.

## 핵심 내용

### 무엇인가: 정확한 정의

관절 캘리브레이션 절차는 **방법(method)** 유형에 속하며, 영문 명칭은 *Joint Calibration Procedure*입니다. 핵심 작업은 세 부분으로 나눌 수 있습니다:

- **절대 영점(absolute zero position)**: 관절 기계 구조상의 기준 각도 위치로, 일반적으로 기계적 리미트, 위치 결정 핀 또는 영점 마크로 결정됩니다.
- **엔코더 오프셋(encoder offset)**: 모터 또는 관절 엔코더 판독값과 기계적 영점 사이의 고정 차이입니다. 엔코더는 전원 인가 시 판독값이 무작위이므로, 캘리브레이션을 통해 "전기적 판독값→기계적 각도"의 매핑 관계를 구축해야 합니다.
- **방향 플래그(direction flag)**: 엔코더 카운트 방향과 관절 실제 회전 방향의 일치 여부를 나타내는 플래그입니다. 방향이 반대로 연결되면 정궤환(positive feedback)이 발생하여 제어 시스템이 순간적으로 발산할 수 있습니다.

이 절차는 조립 통합 테스트(assembly integration testing)와 설계 엔지니어링(design engineering) 두 영역에 속하며, 가치 사슬의 중간 단계에 위치합니다——상류의 부품 제조와 하류의 완제품 디버깅을 연결하며, 로봇이 "돌아간다"에서 "정확히 돈다"로 넘어가는 분기점입니다.

### 왜 존재하는가:痛点과 역사적 위치

관절 캘리브레이션 절차가 등장하기 전에는 로봇 조립 후 디버깅이 수동 육안 검사와 경험적 판단에 의존했습니다. 작업자가 수동으로 관절을 육안상 "영점"으로 돌린 후, 컨트롤러에 현재 엔코더 판독값을 오프셋으로 기록했습니다. 이 방식에는 세 가지 근본적인痛点이 있었습니다:

**첫째, 정밀도 제어 불가.** 수동 육안 검사의 반복 위치 결정 정밀도는 일반적으로 수 도(degree) 수준인 반면, 현대 휴머노이드 로봇의 운동 계획은 관절 각도 오차가 0.1° 이내(공학적 판단)일 것을 요구합니다. 300 mm 길이의 링크를 예로 들면, 0.1°의 각도 오차는 말단에서 약 0.5 mm의 위치 편차를 발생시킵니다(원문의 "±0.1 mm 편차가 ±(n×0.1) mm로 증폭"되는 추론 논리에 따름).

**둘째, 효율성 저하.** 전신 휴머노이드 로봇에는 20–40개의 관절(공학적 판단)이 있으며, 하나씩 수동 캘리브레이션하는 데 수 시간이 걸리고 누락이나 중복이 발생하기 쉽습니다.

**셋째, 추적성 부족.** 수동 캘리브레이션 결과는 작업자 기록에 의존하므로 표준화, 재검증, 운동학 모델과의 자동 연동이 어렵습니다.

역사적 위치에서 볼 때, 관절 캘리브레이션 절차는 로봇 산업이 "실험실 프로토타입"에서 "양산 제품"으로 전환되는 과정의 필연적 산물입니다. 이는 산업용 로봇 팔 분야의 성숙한 운동학 캘리브레이션 방법을 차용했지만, 휴머노이드 로봇의 높은 자유도, 플로팅 베이스 및 다중 폐쇄 운동 사슬 특성에 맞게 특별히 적응시켰습니다.

### 원리 분석

**① 엔코더 판독값과 기계적 각도의 매핑**

관절 캘리브레이션의 수학적 본질은 엔코더 원시 판독값 \( \theta_{\text{raw}} \)와 기계적 관절 각도 \( \theta_{\text{joint}} \) 사이의 아핀 변환을 구축하는 것입니다:

$$
\theta_{\text{joint}} = s \cdot \theta_{\text{raw}} + \theta_{\text{offset}}
$$

여기서 \( s \)는 방향 플래그(+1 또는 -1), \( \theta_{\text{offset}} \)는 엔코더 오프셋입니다. 캘리브레이션 절차의 목표는 이 두 매개변수를 결정하는 것입니다.

**② 운동학 캘리브레이션과의 관계**

관절 캘리브레이션은 운동학 캘리브레이션(kinematic calibration)의 선행 단계입니다. 운동학 캘리브레이션은 외부 측정 데이터를 통해 실제 기하학적 매개변수를 식별하며, 핵심 방정식은 다음과 같습니다:

$$
\Delta \mathbf{p} = \sum_{i=1}^{n} \left( \frac{\partial \mathbf{p}}{\partial a_i} \Delta a_i + \frac{\partial \mathbf{p}}{\partial \alpha_i} \Delta \alpha_i + \frac{\partial \mathbf{p}}{\partial d_i} \Delta d_i + \frac{\partial \mathbf{p}}{\partial \theta_i} \Delta \theta_i \right) = \mathbf{J}_{\text{cal}} \, \Delta \boldsymbol{\phi}
$$

여기서 \( \mathbf{J}_{\text{cal}} \)는 캘리브레이션 야코비안, \( \Delta \boldsymbol{\phi} \)는 식별 대상 매개변수 오차 벡터입니다. 관절 영점 자체가 정확하지 않으면 \( \Delta \theta_i \) 항이 전체 식별 과정을 오염시킵니다——이것이 관절 캘리브레이션이 운동학 캘리브레이션보다 먼저 완료되어야 하는 이유입니다.

**③ 식별 가능성 제약**

관절 캘리브레이션은 또한 **식별 가능성(identifiability)** 문제에 직면합니다: 일부 매개변수 오차는 말단 자세에 미치는 영향이 매우 작거나 관절 영점 오차와 결합되어 독립적으로 식별할 수 없습니다. 휴머노이드 로봇의 경우 플로팅 베이스와 여러 폐쇄 운동 사슬(양발 동시 착지)이 존재하므로, 캘리브레이션 문제는 고정 베이스 로봇 팔보다 더 복잡하며, 일반적으로 IMU, 비전 및 모션 캡처 시스템을 결합하여 완료해야 합니다.

### 핵심 매개변수 및 사양

| 매개변수 | 일반값/범위 | 설명 |
|---|---|---|
| 엔코더 해상도 | 17–23 bit(공학적 판단) | 각도 측정 정밀도의 하한 결정 |
| 영점 반복 위치 결정 정밀도 | ±0.01°–±0.1°(공학적 판단) | 여러 번 캘리브레이션 결과의 일관성 |
| 캘리브레이션 시간 | 단일 관절 1–5분(공학적 판단) | 기계적 정렬 및 소프트웨어 기록 포함 |
| 온도 범위 | 상온 20±5°C(공학적 판단) | 온도 변화는 기계적 영점 드리프트 유발 |
| 방향 플래그 | +1 또는 -1 | 모터 배선 및 엔코더 설치 방향과 관련 |

### 수평 비교

관절 캘리브레이션과 운동학 캘리브레이션은 서로 다른 수준의 프로세스로, 종종 혼동됩니다:

| 차원 | 관절 캘리브레이션 | 운동학 캘리브레이션 |
|---|---|---|
| 캘리브레이션 대상 | 단일 관절의 영점, 오프셋, 방향 | 전체 기계 운동학 매개변수(링크 길이, 비틀림 각도 등) |
| 측정 도구 | 기계적 리미트, 위치 결정 핀, 엔코더 판독값 | 레이저 트래커, 3차원 측정기, 비전 측정 |
| 출력 결과 | 각 관절의 오프셋 및 방향 플래그 | 수정된 DH 또는 PoE 매개변수 |
| 실행 시점 | 조립 후, 전원 인가 디버깅 전 | 전체 기계 조립 완료 후, 출하 전 |
| 정밀도 영향 | 관절 각도 절대 정밀도 | 말단 절대 위치 결정 정밀도 |

두 프로세스는 점진적 관계입니다: 관절 캘리브레이션은 운동학 캘리브레이션의 전제 조건이고, 운동학 캘리브레이션은 관절 캘리브레이션의 확장입니다. 고급 휴머노이드 로봇은 출하 전에 일반적으로 완전한 운동학 캘리브레이션 절차를 수행하여 말단 절대 오차를 수 mm에서 ±0.3 mm 이하로 줄일 수 있습니다.

### 누가 사용하는가·적용 사례

**Unitree H1 휴머노이드 로봇**(`ent_robot_unitree_h1_humanoid_robot_2024`)은 관절 캘리브레이션 절차의 대표적 적용 사례입니다. H1은 Unitree 자체 개발 M107 영구 자석 동기 모터를 사용하며, 무릎 관절 피크 토크는 360 N·m, 피크 토크 밀도는 약 189 N·m/kg입니다. 4 DOF 기본형 팔(선택적으로 H1-2로 7 DOF 업그레이드 가능)은 조립 후 반드시 관절 캘리브레이션을 하나씩 완료해야 3.3 m/s 주행 속도에서 정밀한 운동 제어를 구현할 수 있습니다.

H1의 캘리브레이션 절차에서 각 관절 모듈(모터, 감속기, 센서가 통합된 표준화 유닛)은 다음을 수행해야 합니다:

1. 기계적 리미트 또는 위치 결정 핀을 통해 절대 영점 결정;
2. 현재 엔코더 판독값을 오프셋으로 기록;
3. 방향 플래그 검증, 정궤환 발생 방지;
4. 캘리브레이션 결과를 컨트롤러 매개변수 파일에 기록, 운동학 모델이 호출하도록 함.

### 한계와 경계

**첫째, 온도 민감성.** 관절 캘리브레이션은 상온에서 완료되지만, 로봇 작동 중 온도 상승은 기계 구조의 열팽창을 유발하여 영점 드리프트를 발생시킵니다. 고정밀 응용의 경우 온도 보상 또는 주기적 재캘리브레이션이 필요합니다.

**둘째, 기계적 마모.** 감속기 편심, 베어링 간극은 사용 시간에 따라 증가하여 초기 캘리브레이션 결과가 점차 무효화됩니다. 이것이 산업용 로봇이 일반적으로 주기적 재캘리브레이션을 요구하는 이유를 설명합니다.

**셋째, 식별 불가 매개변수.** 일부 매개변수 오차는 말단 자세에 미치는 영향이 매우 작거나 관절 영점 오차와 결합되어 독립적으로 식별할 수 없습니다. 식별 가능성을 높이는 방법에는 다음이 포함됩니다: 캘리브레이션 야코비안 조건수를 작게 만드는 측정 자세 선택, 일부 알려진 매개변수 고정, 폐쇄 운동 사슬 제약 채택 등.

**넷째, 플로팅 베이스 도전.** 휴머노이드 로봇에는 플로팅 베이스와 여러 폐쇄 운동 사슬(양발 동시 착지)이 존재하므로, 캘리브레이션 문제는 고정 베이스 로봇 팔보다 더 복잡하며, 일반적으로 IMU, 비전 및 모션 캡처 시스템을 결합하여 완료해야 합니다.

### 일반적인 오해

1. **"관절 캘리브레이션은 운동학 캘리브레이션이다"**——틀림. 관절 캘리브레이션은 각 관절의 영점, 오프셋 및 방향만 해결하며, 링크 길이, 비틀림 각도 등 기하학적 매개변수 식별은 포함하지 않습니다. 후자는 운동학 캘리브레이션의 범주에 속하며 외부 측정 장비가 필요합니다.

2. **"엔코더 해상도가 높으면 캘리브레이션이 필요 없다"**——틀림. 엔코더 해상도는 측정 정밀도의 하한을 결정하지만, 오프셋과 방향은 체계적인 고정 오차로 캘리브레이션 없이는 제거할 수 없습니다. 고해상도 엔코더는 캘리브레이션 결과를 더 정밀하게 만들 뿐, 캘리브레이션 자체를 대체할 수 없습니다.

3. **"한 번 캘리브레이션하면 영원히 유효하다"**——틀림. 온도 변화, 기계적 마모, 감속기 편심은 캘리브레이션 결과를 점차 무효화합니다. 고정밀 응용의 경우 주기적 재캘리브레이션이 필요합니다.

4. **"방향 플래그는 중요하지 않다, 어차피 소프트웨어에서 바꿀 수 있다"**——틀림. 방향이 반대로 연결되면 정궤환이 발생하여 제어 시스템이 순간적으로 발산합니다. 이론적으로 소프트웨어에서 반전할 수 있지만, 캘리브레이션 단계에서 발견하지 못하면 디버깅 단계에서 많은 시간을 낭비하게 됩니다.

### 관련 지식

- `ent_robot_unitree_h1_humanoid_robot_2024` — Unitree H1은 관절 캘리브레이션 절차의 대표적 적용 대상으로, 4 DOF 기본형 팔과 7 DOF 업그레이드 방안 모두 조립 후 관절 캘리브레이션을 완료해야 고동적 운동 제어를 구현할 수 있습니다.
- `ent_method_system_identification` — 관절 캘리브레이션과 매개변수 식별은 회귀 추정의 수학적 프레임워크를 공유하며, 캘리브레이션은 식별의 공학적 하위 집합으로 간주할 수 있습니다.
