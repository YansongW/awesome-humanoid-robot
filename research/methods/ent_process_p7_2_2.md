---
$id: ent_process_p7_2_2
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Simulation of disturbance and fall
  zh: 扰动与跌倒仿真
  ko: 교란 및 낙상 시뮬레이션
summary:
  en: Disturbance Resilience, fall injury assessment, safety margin definition
  zh: '- P7.2.2.1 输入梳理与目标量化 - 整理「扰动与跌倒仿真」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 扰动恢复能力、跌倒损伤评估、安全边界定义
domains:
- 08_software_middleware
- 10_evaluation_benchmarks
layers:
- intelligence
- validation_markets
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

扰动与跌倒仿真（Simulation of disturbance and fall）是人形机器人开发全流程 P7 仿真验证阶段中的关键工序节点，属于方法类型，覆盖软件中间件与评测基准两个领域。该节点通过外部推力、地面不平、负载变化、跌落测试四类典型扰动场景的虚拟建模，在实物样机调试之前验证控制算法的鲁棒性，并建立安全边界定义。它真正改变的不是"提前发现跌倒问题"这一表面目的，而是将人形机器人安全验证从"实机摔坏后修复"的被动模式，转变为"仿真预演、保守留余量"的主动设计范式。

## 核心内容

### 是什么：扰动与跌倒仿真的准确定义

扰动与跌倒仿真（Simulation of disturbance and fall）是 P7 仿真平台搭建与验证阶段（P7.2 仿真验证实施工作包）下的一个工序节点，实体 id 为 `ent_process_p7_2_2`。该节点以外部推力、地面不平、负载变化、跌落测试为四大核心扰动输入，在虚拟物理环境中对双足人形机器人的动态稳定性进行系统性测试，输出扰动恢复能力评估、跌倒损伤评估与安全边界定义三项关键结果。

从任务结构看，该节点遵循 P7 阶段统一的五级子任务展开逻辑：P7.2.2.1 输入梳理与目标量化、P7.2.2.2 方案/方法设计、P7.2.2.3 实施/原型/样件制作、P7.2.2.4 验证与问题闭环、P7.2.2.5 文档输出与下游交付。每个子任务内部再细分为若干四级关键动作，形成从输入确认到文档交付的完整闭环。

### 为什么存在：痛点与历史定位

在人形机器人开发早期，扰动与跌倒测试几乎完全依赖实物样机。每一次跌倒测试都意味着硬件损坏风险、维修周期与高昂成本，且测试场景难以精确复现——同一推力角度、同一地面摩擦系数在实机上的重复性很差。这导致控制算法的鲁棒性验证严重滞后于开发进度，许多问题直到实机联调阶段才暴露，修复代价极高。

扰动与跌倒仿真的历史定位正是为了解决这一痛点。它将"摔机测试"从实机前置到虚拟环境，使开发者可以在数小时内完成数百次不同扰动参数的迭代测试，而无需担心硬件损耗。更重要的是，仿真允许对极端工况进行系统性扫描——例如同时施加最大推力与最差地面摩擦系数——这在实机测试中几乎不可能安全执行。它真正改变的不是"用软件替代硬件测试"这一表面形式，而是将安全验证从"事后补救"转变为"事前预演"，为后续 P10 实机测试建立经过保守估计的安全边界。

### 原理拆解

**① 扰动建模：四类输入的物理本质**

扰动与跌倒仿真的核心在于对四类扰动的物理建模：

- **外部推力**：在机器人质心或躯干特定位置施加瞬态或持续力，模拟碰撞、推搡等场景。推力的大小、方向、作用点与持续时间构成参数空间。
- **地面不平**：通过修改接触模型中的地面高度场或摩擦系数，模拟台阶、斜坡、碎石等非理想地面条件。
- **负载变化**：改变机器人末端或躯干的附加质量，模拟搬运物体、背包等工况变化。
- **跌落测试**：从不同高度、不同姿态释放机器人，评估结构冲击响应与损伤程度。

**② 保守性原则：仿真结果的安全余量**

该节点的关键约束是"仿真结果需保守"。这意味着仿真中使用的扰动强度应高于实际预期值，摩擦系数应取不利值，控制增益应留有裕度。保守性的数学表达可写为：

$$
S_{\text{design}} = \frac{F_{\text{sim, max}}}{F_{\text{real, max}}} \geq \gamma
$$

其中 $F_{\text{sim, max}}$ 为仿真中施加的最大扰动强度，$F_{\text{real, max}}$ 为实机预期最大扰动强度，$\gamma$ 为安全系数（工程判断，通常取 1.2–1.5）。保守性原则确保仿真通过的结果在实机上具有足够的安全余量。

**③ 闭环验证逻辑：从扰动到恢复**

扰动与跌倒仿真的验证逻辑是一个闭环：施加扰动 → 观测响应 → 评估恢复能力 → 调整控制参数 → 重新仿真。这一闭环在 P7.2.2.4 验证与问题闭环子任务中体现为"制定测试/评审计划与通过准则 → 执行测试并记录原始数据 → 输出问题清单与改进措施"的三步循环。

### 关键参数与规格

扰动与跌倒仿真的关键参数与规格如下表所示：

| 参数类别 | 具体参数 | 规格/要求 | 来源 |
|---------|---------|----------|------|
| 输入参数 | 外部推力幅值范围 | 需自行确认（依据具体机型负载能力标定） | 语料未给出 |
| 输入参数 | 地面摩擦系数范围 | 需自行确认（依据目标应用场景标定） | 语料未给出 |
| 输入参数 | 负载变化范围 | 需自行确认（依据最大有效载荷标定） | 语料未给出 |
| 输入参数 | 跌落高度范围 | 需自行确认（依据结构强度仿真标定） | 语料未给出 |
| 方案要求 | 候选方案数量 | 不少于 2 个 | 语料 P7.2.2.2 |
| 验证要求 | 测试/评审计划 | 需明确通过准则 | 语料 P7.2.2.4 |
| 输出物 | 扰动恢复能力 | 量化指标（如恢复时间、最大偏移角） | 语料完成标准 |
| 输出物 | 跌倒损伤评估 | 损伤等级与部位分布 | 语料完成标准 |
| 输出物 | 安全边界定义 | 保守性参数区间 | 语料完成标准 |

### 横向对比

扰动与跌倒仿真与 P7 阶段其他仿真工序节点（如运动控制仿真、操作抓取仿真）相比，具有以下差异：

| 对比维度 | 扰动与跌倒仿真 | 运动控制仿真 | 操作抓取仿真 |
|---------|--------------|-------------|-------------|
| 核心关注点 | 动态稳定性与安全边界 | 轨迹跟踪精度 | 抓取成功率 |
| 典型扰动类型 | 外部推力、地面不平、负载变化、跌落 | 目标轨迹变化 | 物体位姿变化 |
| 输出物性质 | 安全边界定义、损伤评估 | 控制性能指标 | 操作成功率 |
| 验证侧重点 | 鲁棒性与保守性 | 精度与响应速度 | 适应性与可靠性 |

扰动与跌倒仿真的独特之处在于其"保守性"要求——其他仿真追求精度与性能，而该节点刻意追求"偏保守"的结果，以为实机测试留出安全余量。这一差异源于其安全验证的本质定位。

### 谁在用·应用案例

扰动与跌倒仿真在 P7 阶段的实际执行中，由仿真工程师、控制算法工程师与安全验证工程师协同完成。其典型应用场景包括：

- **控制鲁棒性预验证**：在实机调试前，通过外部推力与地面不平仿真，验证平衡控制算法在最不利工况下的恢复能力，为 P10 实机测试提供控制参数基线。
- **安全边界定义**：通过跌落测试仿真，确定机器人结构在不同高度、不同姿态下的损伤阈值，为实机测试划定安全操作范围。
- **设计迭代反馈**：当仿真发现扰动恢复能力不足时，将问题反馈至 P5 结构设计或 P6 算法设计环节，形成跨阶段闭环。

该节点的下游依赖方包括 P10 实机测试团队（接收安全边界定义）与 P12 量产部署团队（接收经过验证的鲁棒性基线）。

### 局限与边界

扰动与跌倒仿真的核心局限在于**仿真与现实的差距**。尽管高保真物理引擎可以精确模拟刚体动力学，但以下因素难以完全复现：

- **接触摩擦的非线性**：真实地面的摩擦系数随速度、温度、湿度变化，仿真中通常简化为常数或简单函数。
- **结构柔性**：真实机器人的连杆存在弹性变形，而仿真中通常假设为刚体。
- **执行器非线性**：电机饱和、减速器背隙、温度漂移等效应在仿真中难以精确建模。

因此，仿真结果需保守的约束正是对这一局限的补偿——通过放大扰动强度、取不利参数，确保仿真通过的结果在实机上仍有足够余量。该节点的边界在于：它提供的是**相对安全边界**而非绝对安全保证，最终仍需 P10 实机测试的确认。

### 常见误区

1. **"仿真通过就等于实机安全"**——错。仿真结果需保守的约束意味着仿真通过是实机测试的必要条件而非充分条件；实机仍可能遇到仿真未覆盖的工况。
2. **"扰动类型越多越好"**——错。扰动参数空间巨大，盲目增加扰动类型会导致仿真成本失控；应聚焦于目标应用场景中最关键的扰动模式。
3. **"保守性就是加大扰动强度"**——错。保守性还包括取不利摩擦系数、降低控制增益裕度等多维度操作，而非单一参数放大。
4. **"跌倒仿真只是摔一下看看"**——错。跌倒仿真需要系统评估损伤分布、冲击力峰值、恢复可能性等多维度指标，并形成量化输出物。

### 相关知识

- `ent_process_p7` — 扰动与跌倒仿真所属的 P7 仿真平台搭建与验证阶段，该阶段定义了仿真验证的整体框架与统一任务结构。
- `ent_process_p7_2_1` — 同族相邻工序：基础站立与行走仿真（同一工作包内的并行工序）。
- `ent_process_p7_2_3` — 同族相邻工序：操作与抓取仿真（同一工作包内的并行工序）。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [Awesome Humanoid Robot 项目主页](https://github.com/YansongW/awesome-humanoid-robot/tree/main)

## Overview

Simulation of disturbance and fall is a critical process node in the P7 simulation verification stage of the full humanoid robot development pipeline. It is a method-type node covering both software middleware and evaluation benchmarks. This node validates the robustness of control algorithms before physical prototype debugging and defines safety boundaries through virtual modeling of four typical disturbance scenarios: external thrust, uneven ground, load variation, and drop testing. What it truly changes is not the superficial goal of "detecting fall issues early," but rather a shift in humanoid robot safety verification from the passive mode of "fixing after physical breakage" to the proactive design paradigm of "simulation rehearsal with conservative margins."

## Content

### What It Is: Precise Definition of Disturbance and Fall Simulation

Simulation of disturbance and fall is a process node under the P7 simulation platform construction and verification stage (P7.2 simulation verification implementation work package), with entity id `ent_process_p7_2_2`. This node uses external thrust, uneven ground, load variation, and drop testing as its four core disturbance inputs, systematically testing the dynamic stability of bipedal humanoid robots in a virtual physical environment. It outputs three key results: disturbance recovery capability assessment, fall damage assessment, and safety boundary definition.

In terms of task structure, this node follows the unified five-level subtask expansion logic of the P7 stage: P7.2.2.1 input organization and target quantification, P7.2.2.2 solution/method design, P7.2.2.3 implementation/prototype/sample fabrication, P7.2.2.4 verification and issue closure, and P7.2.2.5 documentation output and downstream delivery. Each subtask is further subdivided into several level-four key actions, forming a complete closed loop from input confirmation to documentation delivery.

### Why It Exists: Pain Points and Historical Positioning

In the early stages of humanoid robot development, disturbance and fall testing relied almost entirely on physical prototypes. Every fall test entailed hardware damage risk, maintenance cycles, and high costs, and test scenarios were difficult to reproduce precisely—the same thrust angle and ground friction coefficient showed poor repeatability on physical machines. This caused robustness verification of control algorithms to lag significantly behind development progress, with many issues only surfacing during physical integration debugging, at extremely high remediation cost.

The historical positioning of disturbance and fall simulation is precisely to address this pain point. It moves "crash testing" from physical machines to the virtual environment, enabling developers to complete hundreds of iterative tests with different disturbance parameters within hours without worrying about hardware wear. More importantly, simulation allows systematic scanning of extreme operating conditions—such as simultaneously applying maximum thrust and the worst ground friction coefficient—which is nearly impossible to execute safely in physical testing. What it truly changes is not the superficial form of "replacing hardware testing with software," but rather transforming safety verification from "post-hoc remediation" to "pre-hoc rehearsal," establishing conservatively estimated safety boundaries for subsequent P10 physical testing.

### Principle Breakdown

**① Disturbance Modeling: Physical Essence of Four Input Types**

The core of disturbance and fall simulation lies in the physical modeling of four disturbance types:

- **External thrust**: Applying transient or sustained forces at the robot's center of mass or specific torso positions to simulate collisions, shoves, and similar scenarios. The magnitude, direction, point of application, and duration of the thrust constitute the parameter space.
- **Uneven ground**: Simulating non-ideal ground conditions such as steps, slopes, and gravel by modifying the ground height field or friction coefficient in the contact model.
- **Load variation**: Changing the additional mass at the robot's end-effectors or torso to simulate scenarios such as carrying objects or backpacks.
- **Drop testing**: Releasing the robot from different heights and postures to evaluate structural impact response and damage severity.

**② Conservatism Principle: Safety Margins in Simulation Results**

The key constraint of this node is that "simulation results must be conservative." This means disturbance intensities used in simulation should be higher than actual expected values, friction coefficients should take unfavorable values, and control gains should retain margins. The mathematical expression of conservatism can be written as:

$$
S_{\text{design}} = \frac{F_{\text{sim, max}}}{F_{\text{real, max}}} \geq \gamma
$$

where $F_{\text{sim, max}}$ is the maximum disturbance intensity applied in simulation, $F_{\text{real, max}}$ is the expected maximum disturbance intensity on the physical machine, and $\gamma$ is the safety factor (engineering judgment, typically 1.2–1.5). The conservatism principle ensures that simulation-passed results carry sufficient safety margins on the physical machine.

**③ Closed-Loop Verification Logic: From Disturbance to Recovery**

The verification logic of disturbance and fall simulation is a closed loop: apply disturbance → observe response → evaluate recovery capability → adjust control parameters → re-simulate. This closed loop is embodied in the P7.2.2.4 verification and issue closure subtask as a three-step cycle: "develop test/review plan and pass criteria → execute tests and record raw data → output issue list and improvement measures."

### Key Parameters and Specifications

The key parameters and specifications of disturbance and fall simulation are shown in the following table:

| Parameter Category | Specific Parameter | Specification/Requirement | Source |
|---------|---------|----------|------|
| Input parameter | External thrust amplitude range | To be self-determined (calibrated based on specific model load capacity) | Not provided in corpus |
| Input parameter | Ground friction coefficient range | To be self-determined (calibrated based on target application scenario) | Not provided in corpus |
| Input parameter | Load variation range | To be self-determined (calibrated based on maximum payload) | Not provided in corpus |
| Input parameter | Drop height range | To be self-determined (calibrated based on structural strength simulation) | Not provided in corpus |
| Solution requirement | Number of candidate solutions | No fewer than 2 | Corpus P7.2.2.2 |
| Verification requirement | Test/review plan | Pass criteria must be clearly defined | Corpus P7.2.2.4 |
| Output | Disturbance recovery capability | Quantitative metrics (e.g., recovery time, maximum offset angle) | Corpus completion criteria |
| Output | Fall damage assessment | Damage level and location distribution | Corpus completion criteria |
| Output | Safety boundary definition | Conservative parameter intervals | Corpus completion criteria |

### Horizontal Comparison

Compared with other simulation process nodes in the P7 stage (such as motion control simulation and manipulation/grasping simulation), disturbance and fall simulation has the following differences:

| Comparison Dimension | Disturbance and Fall Simulation | Motion Control Simulation | Manipulation/Grasping Simulation |
|---------|--------------|-------------|-------------|
| Core focus | Dynamic stability and safety boundaries | Trajectory tracking accuracy | Grasping success rate |
| Typical disturbance types | External thrust, uneven ground, load variation, drop | Target trajectory changes | Object pose changes |
| Output nature | Safety boundary definition, damage assessment | Control performance metrics | Manipulation success rate |
| Verification emphasis | Robustness and conservatism | Accuracy and response speed | Adaptability and reliability |

The unique aspect of disturbance and fall simulation lies in its "conservatism" requirement—other simulations pursue accuracy and performance, while this node deliberately pursues "overly conservative" results to leave safety margins for physical testing. This difference stems from its essential positioning as safety verification.

### Who Uses It: Application Cases

In the actual execution of the P7 stage, disturbance and fall simulation is completed collaboratively by simulation engineers, control algorithm engineers, and safety verification engineers. Typical application scenarios include:

- **Control robustness pre-verification**: Before physical debugging, verifying the recovery capability of balance control algorithms under the most unfavorable conditions through external thrust and uneven ground simulation, providing a control parameter baseline for P10 physical testing.
- **Safety boundary definition**: Through drop test simulation, determining the robot structure's damage thresholds at different heights and postures, delineating safe operating ranges for physical testing.
- **Design iteration feedback**: When simulation reveals insufficient disturbance recovery capability, feeding the issue back to P5 structural design or P6 algorithm design, forming a cross-stage closed loop.

Downstream dependents of this node include the P10 physical testing team (receiving safety boundary definitions) and the P12 mass production deployment team (receiving the verified robustness baseline).

### Limitations and Boundaries

The core limitation of disturbance and fall simulation lies in the **gap between simulation and reality**. Although high-fidelity physics engines can accurately simulate rigid body dynamics, the following factors are difficult to fully reproduce:

- **Nonlinearity of contact friction**: The friction coefficient of real ground varies with velocity, temperature, and humidity, whereas simulation typically simplifies it to a constant or simple function.
- **Structural flexibility**: Real robot links exhibit elastic deformation, whereas simulation typically assumes rigid bodies.
- **Actuator nonlinearity**: Effects such as motor saturation, gearbox backlash, and temperature drift are difficult to model precisely in simulation.

Therefore, the conservative constraint on simulation results is precisely a compensation for this limitation—by amplifying disturbance intensities and adopting unfavorable parameters, simulation-passed results are ensured to retain sufficient margins on the physical machine. The boundary of this node is that it provides a **relative safety boundary** rather than an absolute safety guarantee; final confirmation still requires P10 physical testing.

### Common Misconceptions

1. **"Passing simulation means physical safety"**—Wrong. The conservative constraint on simulation results means that passing simulation is a necessary but not sufficient condition for physical testing; the physical machine may still encounter conditions not covered by simulation.
2. **"The more disturbance types, the better"**—Wrong. The disturbance parameter space is enormous; blindly increasing disturbance types leads to uncontrolled simulation costs. Focus should be on the most critical disturbance patterns in the target application scenario.
3. **"Conservatism means increasing disturbance intensity"**—Wrong. Conservatism also includes multi-dimensional operations such as adopting unfavorable friction coefficients and reducing control gain margins, rather than simply amplifying a single parameter.
4. **"Drop simulation is just dropping and watching"**—Wrong. Drop simulation requires systematic evaluation of multiple dimensions such as damage distribution, peak impact force, and recovery possibility, producing quantitative outputs.

### Related Knowledge

- `ent_process_p7` — The P7 simulation platform construction and verification stage to which disturbance and fall simulation belongs; this stage defines the overall framework and unified task structure for simulation verification.
- `ent_process_p7_2_1` — Adjacent process in the same family: basic standing and walking simulation (parallel process within the same work package).
- `ent_process_p7_2_3` — Adjacent process in the same family: manipulation and grasping simulation (parallel process within the same work package).

## 개요

교란 및 낙상 시뮬레이션(Simulation of disturbance and fall)은 휴머노이드 로봇 개발 전체 프로세스 중 P7 시뮬레이션 검증 단계의 핵심 공정 노드로, 방법 유형에 속하며 소프트웨어 미들웨어와 평가 벤치마크 두 영역을 포괄합니다. 이 노드는 외부 추력, 지면 불균일, 부하 변화, 낙하 테스트의 네 가지 대표적 교란 시나리오의 가상 모델링을 통해 실물 프로토타입 디버깅 이전에 제어 알고리즘의 강건성을 검증하고 안전 경계를 정의합니다. 이 노드가 진정으로 바꾸는 것은 "낙상 문제를 조기에 발견한다"는 표면적 목적이 아니라, 휴머노이드 로봇 안전 검증을 "실물 파손 후 수리"라는 수동적 모드에서 "시뮬레이션 사전 예행, 보수적 여유 확보"라는 능동적 설계 패러다임으로 전환하는 것입니다.

## 핵심 내용

### 무엇인가: 교란 및 낙상 시뮬레이션의 정확한 정의

교란 및 낙상 시뮬레이션(Simulation of disturbance and fall)은 P7 시뮬레이션 플랫폼 구축 및 검증 단계(P7.2 시뮬레이션 검증 구현 작업 패키지)의 공정 노드로, 엔티티 id는 `ent_process_p7_2_2`입니다. 이 노드는 외부 추력, 지면 불균일, 부하 변화, 낙하 테스트를 네 가지 핵심 교란 입력으로 삼아 가상 물리 환경에서 이족 보행 휴머노이드 로봇의 동적 안정성을 체계적으로 테스트하고, 교란 회복 능력 평가, 낙상 손상 평가, 안전 경계 정의의 세 가지 핵심 결과를 출력합니다.

작업 구조 측면에서 이 노드는 P7 단계의 통일된 5단계 하위 작업 전개 논리를 따릅니다: P7.2.2.1 입력 정리 및 목표 정량화, P7.2.2.2 방안/방법 설계, P7.2.2.3 구현/프로토타입/시제품 제작, P7.2.2.4 검증 및 문제 폐루프, P7.2.2.5 문서 출력 및 하위 전달. 각 하위 작업 내부는 다시 여러 4단계 핵심 액션으로 세분화되어 입력 확인부터 문서 전달까지의 완전한 폐루프를 형성합니다.

### 왜 존재하는가:痛点과 역사적 위치

휴머노이드 로봇 개발 초기에는 교란 및 낙상 테스트가 거의 전적으로 실물 프로토타입에 의존했습니다. 낙하 테스트를 할 때마다 하드웨어 손상 위험, 수리 주기, 높은 비용이 수반되었고, 테스트 시나리오를 정밀하게 재현하기 어려웠습니다—동일한 추력 각도, 동일한 지면 마찰 계수라도 실물에서의 반복성은 매우 낮았습니다. 이로 인해 제어 알고리즘의 강건성 검증이 개발 진행 속도에 크게 뒤처졌고, 많은 문제가 실물 연동 디버깅 단계에서야 드러나 수리 비용이 매우 높았습니다.

교란 및 낙상 시뮬레이션의 역사적 위치는 바로 이痛点을 해결하는 데 있습니다. "추락 테스트"를 실물에서 가상 환경으로 앞당겨, 개발자가 하드웨어 손상을 걱정하지 않고 수백 번의 서로 다른 교란 매개변수 반복 테스트를 몇 시간 안에 완료할 수 있게 합니다. 더 중요한 것은, 시뮬레이션이 극한 조건을 체계적으로 스캔할 수 있게 한다는 점입니다—예를 들어 최대 추력과 최악의 지면 마찰 계수를 동시에 적용하는 경우—이는 실물 테스트에서는 거의 안전하게 실행할 수 없습니다. 이 노드가 진정으로 바꾸는 것은 "소프트웨어로 하드웨어 테스트를 대체한다"는 표면적 형태가 아니라, 안전 검증을 "사후 대응"에서 "사전 예행"으로 전환하여 이후 P10 실물 테스트를 위한 보수적 추정의 안전 경계를 구축하는 것입니다.

### 원리 분석

**① 교란 모델링: 네 가지 입력의 물리적 본질**

교란 및 낙상 시뮬레이션의 핵심은 네 가지 교란의 물리적 모델링에 있습니다:

- **외부 추력**: 로봇의 질량 중심 또는 특정 몸통 위치에 과도적 또는 지속적 힘을 가하여 충돌, 밀침 등의 시나리오를 모사합니다. 추력의 크기, 방향, 작용점, 지속 시간이 매개변수 공간을 구성합니다.
- **지면 불균일**: 접촉 모델의 지면 높이 필드 또는 마찰 계수를 수정하여 계단, 경사로, 자갈 등의 비이상적 지면 조건을 모사합니다.
- **부하 변화**: 로봇 말단 또는 몸통의 추가 질량을 변경하여 물체 운반, 배낭 착용 등의 작업 조건 변화를 모사합니다.
- **낙하 테스트**: 서로 다른 높이, 서로 다른 자세에서 로봇을 방출하여 구조적 충격 응답과 손상 정도를 평가합니다.

**② 보수성 원칙: 시뮬레이션 결과의 안전 여유**

이 노드의 핵심 제약은 "시뮬레이션 결과는 보수적이어야 한다"는 것입니다. 이는 시뮬레이션에 사용된 교란 강도가 실제 예상 값보다 높아야 하고, 마찰 계수는 불리한 값을 취해야 하며, 제어 이득은 여유를 두어야 함을 의미합니다. 보수성의 수학적 표현은 다음과 같습니다:

$$
S_{\text{design}} = \frac{F_{\text{sim, max}}}{F_{\text{real, max}}} \geq \gamma
$$

여기서 $F_{\text{sim, max}}$는 시뮬레이션에서 가해진 최대 교란 강도, $F_{\text{real, max}}$는 실물에서 예상되는 최대 교란 강도, $\gamma$는 안전 계수(공학적 판단, 일반적으로 1.2–1.5)입니다. 보수성 원칙은 시뮬레이션을 통과한 결과가 실물에서 충분한 안전 여유를 갖도록 보장합니다.

**③ 폐루프 검증 논리: 교란에서 회복까지**

교란 및 낙상 시뮬레이션의 검증 논리는 폐루프입니다: 교란 가하기 → 응답 관찰 → 회복 능력 평가 → 제어 매개변수 조정 → 재시뮬레이션. 이 폐루프는 P7.2.2.4 검증 및 문제 폐루프 하위 작업에서 "테스트/검토 계획 및 통과 기준 수립 → 테스트 실행 및 원시 데이터 기록 → 문제 목록 및 개선 조치 출력"의 3단계 순환으로 구현됩니다.

### 핵심 매개변수 및 사양

교란 및 낙상 시뮬레이션의 핵심 매개변수 및 사양은 아래 표와 같습니다:

| 매개변수 유형 | 구체적 매개변수 | 사양/요구사항 | 출처 |
|---------|---------|----------|------|
| 입력 매개변수 | 외부 추력 진폭 범위 | 자체 확인 필요(특정 기종 부하 능력에 따라 보정) | 말뭉치 미제공 |
| 입력 매개변수 | 지면 마찰 계수 범위 | 자체 확인 필요(목표 적용 시나리오에 따라 보정) | 말뭉치 미제공 |
| 입력 매개변수 | 부하 변화 범위 | 자체 확인 필요(최대 유효 탑재량에 따라 보정) | 말뭉치 미제공 |
| 입력 매개변수 | 낙하 높이 범위 | 자체 확인 필요(구조 강도 시뮬레이션에 따라 보정) | 말뭉치 미제공 |
| 방안 요구사항 | 후보 방안 수 | 2개 이상 | 말뭉치 P7.2.2.2 |
| 검증 요구사항 | 테스트/검토 계획 | 통과 기준 명시 필요 | 말뭉치 P7.2.2.4 |
| 출력물 | 교란 회복 능력 | 정량적 지표(회복 시간, 최대 이탈 각도 등) | 말뭉치 완료 기준 |
| 출력물 | 낙상 손상 평가 | 손상 등급 및 부위 분포 | 말뭉치 완료 기준 |
| 출력물 | 안전 경계 정의 | 보수적 매개변수 구간 | 말뭉치 완료 기준 |

### 횡적 비교

교란 및 낙상 시뮬레이션은 P7 단계의 다른 시뮬레이션 공정 노드(예: 운동 제어 시뮬레이션, 조작 파지 시뮬레이션)와 비교하여 다음과 같은 차이가 있습니다:

| 비교 차원 | 교란 및 낙상 시뮬레이션 | 운동 제어 시뮬레이션 | 조작 파지 시뮬레이션 |
|---------|--------------|-------------|-------------|
| 핵심 관심사 | 동적 안정성 및 안전 경계 | 궤적 추적 정밀도 | 파지 성공률 |
| 대표적 교란 유형 | 외부 추력, 지면 불균일, 부하 변화, 낙하 | 목표 궤적 변화 | 물체 자세 변화 |
| 출력물 성격 | 안전 경계 정의, 손상 평가 | 제어 성능 지표 | 조작 성공률 |
| 검증 중점 | 강건성 및 보수성 | 정밀도 및 응답 속도 | 적응성 및 신뢰성 |

교란 및 낙상 시뮬레이션의 독특한 점은 "보수성" 요구사항입니다—다른 시뮬레이션은 정밀도와 성능을 추구하는 반면, 이 노드는 의도적으로 "다소 보수적인" 결과를 추구하여 실물 테스트를 위한 안전 여유를 확보합니다. 이러한 차이는 안전 검증이라는 본질적 위치에서 비롯됩니다.

### 사용자·적용 사례

교란 및 낙상 시뮬레이션은 P7 단계의 실제 실행에서 시뮬레이션 엔지니어, 제어 알고리즘 엔지니어, 안전 검증 엔지니어가 협력하여 완료합니다. 대표적 적용 시나리오는 다음과 같습니다:

- **제어 강건성 사전 검증**: 실물 디버깅 전에 외부 추력 및 지면 불균일 시뮬레이션을 통해 균형 제어 알고리즘의 최악 조건에서의 회복 능력을 검증하고, P10 실물 테스트를 위한 제어 매개변수 기준선을 제공합니다.
- **안전 경계 정의**: 낙하 테스트 시뮬레이션을 통해 로봇 구조의 서로 다른 높이, 서로 다른 자세에서의 손상 임계값을 결정하고, 실물 테스트의 안전 작동 범위를 설정합니다.
- **설계 반복 피드백**: 시뮬레이션에서 교란 회복 능력 부족이 발견되면 문제를 P5 구조 설계 또는 P6 알고리즘 설계 단계로 피드백하여 단계 간 폐루프를 형성합니다.

이 노드의 하위 의존자는 P10 실물 테스트 팀(안전 경계 정의 수신)과 P12 양산 배포 팀(검증된 강건성 기준선 수신)입니다.

### 한계 및 경계

교란 및 낙상 시뮬레이션의 핵심 한계는 **시뮬레이션과 현실의 차이**입니다. 고충실도 물리 엔진이 강체 동역학을 정밀하게 모사할 수 있지만, 다음 요소는 완전히 재현하기 어렵습니다:

- **접촉 마찰의 비선형성**: 실제 지면의 마찰 계수는 속도, 온도, 습도에 따라 변하며, 시뮬레이션에서는 일반적으로 상수 또는 단순 함수로 단순화됩니다.
- **구조 유연성**: 실제 로봇의 링크에는 탄성 변형이 존재하지만, 시뮬레이션에서는 일반적으로 강체로 가정합니다.
- **액추에이터 비선형성**: 모터 포화, 감속기 백래시, 온도 드리프트 등의 효과는 시뮬레이션에서 정밀하게 모델링하기 어렵습니다.

따라서 시뮬레이션 결과가 보수적이어야 한다는 제약은 바로 이러한 한계를 보상하기 위한 것입니다—교란 강도를 확대하고 불리한 매개변수를 취함으로써 시뮬레이션을 통과한 결과가 실물에서도 충분한 여유를 갖도록 보장합니다. 이 노드의 경계는 **상대적 안전 경계**를 제공할 뿐 절대적 안전 보장은 아니며, 최종적으로는 P10 실물 테스트의 확인이 필요하다는 점입니다.

### 흔한 오해

1. **"시뮬레이션 통과 = 실물 안전"** — 틀림. 시뮬레이션 결과의 보수성 제약은 시뮬레이션 통과가 실물 테스트의 필요 조건이지 충분 조건이 아님을 의미합니다; 실물에서는 시뮬레이션이 다루지 못한 조건이 여전히 발생할 수 있습니다.
2. **"교란 유형이 많을수록 좋다"** — 틀림. 교란 매개변수 공간은 방대하므로, 무분별하게 교란 유형을 늘리면 시뮬레이션 비용이 통제 불능이 됩니다; 목표 적용 시나리오에서 가장 핵심적인 교란 패턴에 집중해야 합니다.
3. **"보수성 = 교란 강도 확대"** — 틀림. 보수성에는 불리한 마찰 계수 적용, 제어 이득 여유 축소 등 다차원적 조작이 포함되며, 단일 매개변수 확대가 아닙니다.
4. **"낙상 시뮬레이션은 그냥 떨어뜨려 보는 것"** — 틀림. 낙상 시뮬레이션은 손상 분포, 충격력 피크, 회복 가능성 등 다차원 지표를 체계적으로 평가하고 정량적 출력물을 형성해야 합니다.

### 관련 지식

- `ent_process_p7` — 교란 및 낙상 시뮬레이션이 속한 P7 시뮬레이션 플랫폼 구축 및 검증 단계로, 이 단계는 시뮬레이션 검증의 전체 프레임워크와 통일된 작업 구조를 정의합니다.
- `ent_process_p7_2_1` — 동일 계열 인접 공정: 기초 보행 및 보행 시뮬레이션(동일 작업 패키지 내 병렬 공정).
- `ent_process_p7_2_3` — 동일 계열 인접 공정: 조작 및 파지 시뮬레이션(동일 작업 패키지 내 병렬 공정).
