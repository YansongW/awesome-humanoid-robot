---
$id: ent_method_hierarchical_qp_wbc
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: Hierarchical QP Whole-Body Control
  zh: 分层二次规划全身控制
  ko: 계층적 QP 전신 제어
summary:
  en: A whole-body control method that stacks multiple tasks by priority and solves them as a cascade of quadratic programs,
    ensuring higher-priority tasks are fulfilled before lower-priority ones.
  zh: 一种按优先级堆叠多个任务并通过级联二次规划求解的全身控制方法，确保高优先级任务优先满足。
  ko: 여러 작업을 우선순위에 따라 쌓아 올리고 연속된 이차 계획법으로 풀어 높은 우선순위 작업을 먼저 만족시키는 전신 제어 방법이다.
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- intelligence
- knowledge
tags:
- whole_body_control
- hierarchical_qp
- stack_of_tasks
- quadratic_programming
- humanoid_robot
theoretical_depth:
- method
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-06-26'
  confidence: high
  notes: 'Based on de Lasa et al. 2010, Herzog et al. 2016, and Kim et al. 2019. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.
    [2026-08-06] body upgraded to textbook-grade B1 batch (.staging/textbook_grade, methods top-20 by degree): zh 概述/核心内容/参考
    rewritten from card + graph neighbors + wiki chapters + first-hand sources (arXiv/Crossref/fetched pages in sources/,
    number whitelist audit 20/20 passed); en/ko sections to be regenerated by translate_entry_bodies.py.'
sources:
- id: src_001
  type: paper
  title: 'Hierarchical QP whole-body control: from theory to practice'
  url: https://arxiv.org/abs/1910.13329
  date: '2019'
  accessed_at: '2026-06-26'
related_entities:
- id: ent_formalism_inverse_dynamics_qp
  relationship: formalizes
  description:
    en: Hierarchical QP WBC instantiates the inverse-dynamics QP formulation.
    zh: 分层 QP WBC 实例化了逆动力学 QP 形式化。
    ko: 계층적 QP WBC는 역동역학 QP 공식화를 구현한다.
- id: ent_operator_task_jacobian
  relationship: uses
  description:
    en: Each task in the hierarchy is expressed through its task Jacobian.
    zh: 层级中的每个任务都通过其任务 Jacobian 表达。
    ko: 계층의 각 작업은 작업 Jacobian을 통해 표현된다.
---
## 概述

分层 QP 全身控制（Hierarchical QP WBC）是全身控制的严格优先级实现：把"保持平衡 > 跟踪末端轨迹 > 关节限位避让"等任务按优先级排序，高优先级任务完全满足后，低优先级任务只能在其零空间内求解，每层求解一个 QP 或经零空间投影合成。它解决了加权和方法"任务互相妥协"在安全关键场景不可接受的问题（来源：项目 Wiki 第 8 章 §8.4.10.2；Escande et al. 2014，DOI 10.1177/0278364914521306）。

## 核心内容

### 是什么：准确定义

给定优先级排序的任务栈 $\{(\mathbf{J}_i, \ddot{x}_i^*)\}_{i=1..p}$，严格层次要求：第 1 级任务最优；第 2 级在第 1 级的零空间内最优；依此类推。关节速度的经典两层分解为（来源：项目 Wiki 第 8 章 §8.4.10.2）：

$$
\dot{\mathbf{q}} = \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* + \mathbf{N}_1 \mathbf{J}_2^\dagger \left( \dot{\mathbf{x}}_2^* - \mathbf{J}_2 \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* \right), \qquad \mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1
$$

第一项完成主任务；第二项在**不影响主任务**的前提下完成次级任务。推广到力矩/加速度层级并叠加动力学、摩擦锥等约束，即分层 QP（HQP）：逐层求解带约束 QP，每层在前层的零空间中优化（Escande et al. 2014 给出了在线人形运动生成的快速 HQP 解法）。

### 为什么存在：痛点与历史定位

加权和 WBC 用一个 QP 的权重 $w_i$ 表达任务重要性，实现简单但有结构性风险：**权重是软约束**——扰动足够大时，"不摔倒"任务也会被"够到物体"任务分走资源。对安全关键任务，妥协不可接受。

分层思想的历史脉络：Khatib 的操作空间零空间投影（1987）→ Sentis & Khatib 的全身任务优先级（2000 年代）→ Escande 等把严格层次做成可实时求解的 HQP（2014，IJRR）。它真正改变的不是求解器，而是**任务冲突的处置原则**：从"折中"变成"等级制度"——宁可放弃次级任务，也不让安全任务打折扣。

### 原理拆解

**① 零空间投影的直觉**

$\mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1$ 把任意关节运动投影到"不改变主任务输出"的子空间。人形机器人有 30+ 自由度而单一任务只需其中几个方向，剩余自由度就是次级任务的生存空间——**冗余度是层次控制的燃料**（Wiki §8.4.10.1–8.4.10.2）。

**② 严格层次 vs 加权和**（Wiki §8.4.10.2）

- **严格层次**：高优先级完全满足，低优先级在零空间内执行；安全任务不可被妥协；
- **加权和**：所有任务加权求和解一个 QP，允许任务间权衡，权重调参直观但有妥协风险；
- 二者关系：严格层次可用极大权差**近似**，但精确实现需要逐层零空间投影。

**③ HQP 的实时化**

逐层 QP 的计算量随层数增长，Escande 等 2014 年的工作给出快速在线解法，把 HQP 推到人形在线运动生成（DOI 10.1177/0278364914521306）。工程上常见的折中是"混合层次"：顶层 1–2 级严格（平衡、限位），底层合并为加权和（姿态优化等舒适性任务）。

### 关键参数与规格

| 项目 | 内容 | 来源 |
|------|------|------|
| 层次结构 | 典型：平衡/CoM > 足端轨迹 > 手臂操作 > 姿态优化 | Wiki §8.4.10.2 任务栈图 |
| 每层求解 | 带约束 QP + 前层零空间投影 | Wiki §8.4.10.2 |
| 实时解法 | Escande et al. 2014（在线人形 HQP） | DOI 10.1177/0278364914521306 |
| 控制频率 | 随 WBC 外环 50–200 Hz | Wiki 第 8 章环路分层 |
| 近似实现 | 极大权差加权和可近似严格层次 | Wiki §8.4.10.2 |

### 横向对比

| 维度 | 加权和 WBC | **严格层次 HQP** | 混合层次 |
|------|-----------|------------------|----------|
| 任务冲突 | 互相妥协 | 高优先级不可侵犯 | 顶层严格、底层加权 |
| 调参 | 权重直觉但脆弱 | 优先级明确 | 折中 |
| 计算量 | 单个 QP | 逐层 QP（较高） | 中 |
| 安全性 | 软约束 | 硬保证 | 顶层硬保证 |
| 适用 | 舒适性任务 | 安全关键任务栈 | 工程主流 |

（依据：项目 Wiki 第 8 章 §8.4.10；Escande 2014。）

### 谁在用 / 应用案例

- **操作任务的全身支撑**：Wiki 第 16 章 §16.5.2 记载人形操作中以 HQP 按优先级求解"保持平衡 > 跟踪末端轨迹 > 关节限位避让"任务栈；
- **在线运动生成**：Escande et al. 2014 把人形 HQP 推到实时（DOI 见参考）；
- **图谱邻居**：`ent_formalism_inverse_dynamics_qp`（逆动力学 QP 形式化）、`ent_equation_weighted_task_error_objective`（加权目标式）、`ent_operator_task_jacobian`（任务雅可比算子）。

### 局限与边界

- **计算量随层数增长**：逐层 QP + 零空间投影比单 QP 贵，嵌入式实时需剪裁；
- **层间可行性**：高优先级约束过强时低优先级零空间可能为空，需松弛策略；
- **优先级设计即安全设计**：层次排错（如把姿态优化排在平衡之前）是隐性事故源；
- **模型依赖**：与一切 WBC 相同，动力学模型误差直接污染零空间。

### 常见误区

1. **"加权和权重调大就等于优先级"**——极大权差只是近似；扰动下软优先级仍会被突破，严格层次才是不等式保证。
2. **"层次越多越好"**——每层都付计算与可行性代价；工程上 2–4 级加混合层是常态（**工程判断**）。
3. **"HQP 输出位置"**——与 WBC 相同，输出是加速度/力矩级量，需经伺服层执行。
4. **"有了 RL 就不需要层次"**——学习策略的动作仍常经 WBC/HQP 层做安全约束兜底。

### 相关知识

- `ent_method_whole_body_control` — 上位框架（WBC 卡）
- `ent_method_model_predictive_control` — 上游前瞻规划
- `ent_formalism_inverse_dynamics_qp` — 逆动力学 QP 形式化
- `ent_operator_task_jacobian` — 任务雅可比算子
- `ent_equation_weighted_task_error_objective` — 加权和目标式
- `ent_principle_capture_point` — 平衡任务的落脚点概念

## 参考

- [Escande, Mansard & Wieber, Hierarchical Quadratic Programming: Fast Online Humanoid-Robot Motion Generation, IJRR 2014](https://doi.org/10.1177/0278364914521306)
- [项目 Wiki 第 8 章 §8.4.10 全身控制（任务优先级）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)

## Overview

Hierarchical QP Whole-Body Control (HQP WBC) is a strict-priority implementation of whole-body control: tasks such as "maintain balance > track end-effector trajectory > avoid joint limits" are ordered by priority, with higher-priority tasks fully satisfied before lower-priority tasks are solved only within their null space, each layer solving a QP or synthesized via null-space projection. It addresses the unacceptable issue in safety-critical scenarios where weighted-sum methods allow tasks to "compromise" with each other (source: project Wiki Chapter 8 §8.4.10.2; Escande et al. 2014, DOI 10.1177/0278364914521306).

## Content

### What it is: precise definition

Given a priority-ordered task stack $\{(\mathbf{J}_i, \ddot{x}_i^*)\}_{i=1..p}$, the strict hierarchy requires: level 1 task is optimal; level 2 is optimal within the null space of level 1; and so on. The classical two-level decomposition for joint velocities is (source: project Wiki Chapter 8 §8.4.10.2):

$$
\dot{\mathbf{q}} = \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* + \mathbf{N}_1 \mathbf{J}_2^\dagger \left( \dot{\mathbf{x}}_2^* - \mathbf{J}_2 \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* \right), \qquad \mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1
$$

The first term completes the primary task; the second term accomplishes the secondary task **without affecting the primary task**. Extending this to the torque/acceleration level and incorporating dynamics, friction cones, and other constraints yields Hierarchical QP (HQP): solving constrained QPs layer by layer, with each layer optimizing in the null space of the previous layers (Escande et al. 2014 provides a fast HQP solver for online humanoid motion generation).

### Why it exists: pain points and historical positioning

Weighted-sum WBC expresses task importance via weights $w_i$ in a single QP, which is simple to implement but has a structural risk: **weights are soft constraints**—when disturbances are large enough, the "don't fall" task can have resources diverted to the "reach the object" task. For safety-critical tasks, compromise is unacceptable.

Historical lineage of the hierarchical idea: Khatib's operational-space null-space projection (1987) → Sentis & Khatib's whole-body task prioritization (2000s) → Escande et al. turning strict hierarchy into a real-time solvable HQP (2014, IJRR). What it truly changes is not the solver but the **principle for handling task conflicts**: from "compromise" to "hierarchy"—rather than degrading safety tasks, secondary tasks are sacrificed.

### Principle breakdown

**① Intuition behind null-space projection**

$\mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1$ projects any joint motion onto the subspace that "does not change the primary task output." A humanoid robot has 30+ degrees of freedom while a single task requires only a few directions; the remaining DOFs are the living space for secondary tasks—**redundancy is the fuel for hierarchical control** (Wiki §8.4.10.1–8.4.10.2).

**② Strict hierarchy vs. weighted sum** (Wiki §8.4.10.2)

- **Strict hierarchy**: higher-priority tasks are fully satisfied, lower-priority tasks execute in the null space; safety tasks cannot be compromised;
- **Weighted sum**: all tasks are weighted and solved in one QP, allowing trade-offs between tasks; weight tuning is intuitive but carries compromise risk;
- Relationship: strict hierarchy can be **approximated** by extremely large weight differences, but exact implementation requires layer-by-layer null-space projection.

**③ Real-time HQP**

The computational cost of layer-by-layer QPs grows with the number of layers. The 2014 work by Escande et al. provides a fast online solver, pushing HQP to online humanoid motion generation (DOI 10.1177/0278364914521306). A common engineering compromise is "hybrid hierarchy": the top 1–2 levels are strict (balance, limits), while lower levels are merged into a weighted sum (comfort tasks such as posture optimization).

### Key parameters and specifications

| Item | Content | Source |
|------|---------|--------|
| Hierarchy structure | Typical: balance/CoM > foot trajectory > arm manipulation > posture optimization | Wiki §8.4.10.2 task stack diagram |
| Per-layer solving | Constrained QP + null-space projection of previous layers | Wiki §8.4.10.2 |
| Real-time solver | Escande et al. 2014 (online humanoid HQP) | DOI 10.1177/0278364914521306 |
| Control frequency | 50–200 Hz with the WBC outer loop | Wiki Chapter 8 loop hierarchy |
| Approximate implementation | Weighted sum with extremely large weight differences can approximate strict hierarchy | Wiki §8.4.10.2 |

### Horizontal comparison

| Dimension | Weighted-sum WBC | **Strict-hierarchy HQP** | Hybrid hierarchy |
|-----------|-----------------|--------------------------|------------------|
| Task conflicts | Mutual compromise | Higher priority is inviolable | Strict at top, weighted at bottom |
| Tuning | Intuitive but fragile weights | Clear priorities | Compromise |
| Computational cost | Single QP | Layer-by-layer QPs (higher) | Medium |
| Safety | Soft constraints | Hard guarantees | Hard guarantees at top |
| Applicability | Comfort tasks | Safety-critical task stacks | Mainstream engineering |

(Based on: project Wiki Chapter 8 §8.4.10; Escande 2014.)

### Who uses it / application cases

- **Whole-body support for manipulation tasks**: Wiki Chapter 16 §16.5.2 documents humanoid manipulation solving the task stack "maintain balance > track end-effector trajectory > avoid joint limits" with HQP by priority;
- **Online motion generation**: Escande et al. 2014 pushes humanoid HQP to real time (DOI in references);
- **Graph neighbors**: `ent_formalism_inverse_dynamics_qp` (inverse dynamics QP formalism), `ent_equation_weighted_task_error_objective` (weighted objective), `ent_operator_task_jacobian` (task Jacobian operator).

### Limitations and boundaries

- **Computational cost grows with the number of layers**: layer-by-layer QPs plus null-space projection are more expensive than a single QP; embedded real-time use requires trimming;
- **Inter-layer feasibility**: if higher-priority constraints are too strong, the lower-priority null space may be empty, requiring relaxation strategies;
- **Priority design is safety design**: misordering the hierarchy (e.g., placing posture optimization before balance) is a hidden source of accidents;
- **Model dependence**: as with all WBC, dynamics model errors directly contaminate the null space.

### Common misconceptions

1. **"Increasing the weighted-sum weight equals priority"**—extremely large weight differences are only an approximation; soft priorities can still be violated under disturbances, while strict hierarchy provides inequality-based guarantees.
2. **"More layers is always better"**—each layer incurs computational and feasibility costs; in engineering, 2–4 levels plus a hybrid layer is the norm (**engineering judgment**).
3. **"HQP outputs positions"**—like WBC, the output is at the acceleration/torque level and must be executed by the servo layer.
4. **"With RL, hierarchy is unnecessary"**—learned policy actions are still often passed through a WBC/HQP layer for safety constraint enforcement.

### Related knowledge

- `ent_method_whole_body_control` — upper-level framework (WBC card)
- `ent_method_model_predictive_control` — upstream look-ahead planning
- `ent_formalism_inverse_dynamics_qp` — inverse dynamics QP formalism
- `ent_operator_task_jacobian` — task Jacobian operator
- `ent_equation_weighted_task_error_objective` — weighted objective
- `ent_principle_capture_point` — footstep placement concept for balance tasks

## 개요

계층적 QP 전신 제어(Hierarchical QP WBC)는 전신 제어의 엄격한 우선순위 구현입니다: "균형 유지 > 말단 궤적 추종 > 관절 한계 회피" 등의 작업을 우선순위에 따라 정렬하고, 높은 우선순위 작업이 완전히 충족된 후에만 낮은 우선순위 작업이 그 영공간(null space) 내에서 해결되며, 각 계층은 QP를 풀거나 영공간 투영을 통해 합성됩니다. 이는 가중 합 방식의 "작업 간 상호 타협"이 안전 필수 시나리오에서 허용될 수 없는 문제를 해결합니다(출처: 프로젝트 Wiki 8장 §8.4.10.2; Escande et al. 2014, DOI 10.1177/0278364914521306).

## 핵심 내용

### 무엇인가: 정확한 정의

우선순위가 정렬된 작업 스택 $\{(\mathbf{J}_i, \ddot{x}_i^*)\}_{i=1..p}$이 주어졌을 때, 엄격한 계층 구조는 다음을 요구합니다: 1단계 작업이 최적; 2단계는 1단계의 영공간 내에서 최적; 이런 식으로 계속됩니다. 관절 속도의 고전적인 2계층 분해는 다음과 같습니다(출처: 프로젝트 Wiki 8장 §8.4.10.2):

$$
\dot{\mathbf{q}} = \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* + \mathbf{N}_1 \mathbf{J}_2^\dagger \left( \dot{\mathbf{x}}_2^* - \mathbf{J}_2 \mathbf{J}_1^\dagger \dot{\mathbf{x}}_1^* \right), \qquad \mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1
$$

첫 번째 항은 주 작업을 완료합니다; 두 번째 항은 **주 작업에 영향을 주지 않으면서** 하위 작업을 완료합니다. 이를 토크/가속도 계층으로 확장하고 동역학, 마찰 원뿔 등의 제약 조건을 중첩하면 계층적 QP(HQP)가 됩니다: 각 계층에서 제약 조건이 있는 QP를 풀고, 각 계층은 이전 계층의 영공간에서 최적화합니다(Escande et al. 2014는 온라인 휴머노이드 동작 생성을 위한 빠른 HQP 해법을 제시했습니다).

### 왜 존재하는가: 문제점과 역사적 위치

가중 합 WBC는 하나의 QP에서 가중치 $w_i$로 작업 중요도를 표현하며 구현이 간단하지만 구조적 위험이 있습니다: **가중치는 소프트 제약 조건**입니다——교란이 충분히 크면 "넘어지지 않기" 작업도 "물체 잡기" 작업에 자원을 빼앗길 수 있습니다. 안전 필수 작업의 경우 타협은 허용될 수 없습니다.

계층적 사고의 역사적 흐름: Khatib의 운영 공간 영공간 투영(1987) → Sentis & Khatib의 전신 작업 우선순위(2000년대) → Escande 등이 엄격한 계층 구조를 실시간으로 풀 수 있는 HQP로 만듦(2014, IJRR). 실제로 바뀐 것은 솔버가 아니라 **작업 충돌 처리 원칙**입니다: "절충"에서 "계급 제도"로——하위 작업을 포기하더라도 안전 작업이 타협되지 않도록 합니다.

### 원리 분석

**① 영공간 투영의 직관**

$\mathbf{N}_1 = \mathbf{I} - \mathbf{J}_1^\dagger \mathbf{J}_1$은 임의의 관절 운동을 "주 작업 출력을 변경하지 않는" 부분 공간으로 투영합니다. 휴머노이드 로봇은 30+ 자유도를 가지지만 단일 작업에는 그중 몇 개의 방향만 필요하므로, 나머지 자유도가 하위 작업의 생존 공간이 됩니다——**여유도는 계층 제어의 연료입니다**(Wiki §8.4.10.1–8.4.10.2).

**② 엄격한 계층 구조 vs 가중 합**(Wiki §8.4.10.2)

- **엄격한 계층 구조**: 높은 우선순위가 완전히 충족되고, 낮은 우선순위는 영공간 내에서 실행됩니다; 안전 작업은 타협될 수 없습니다;
- **가중 합**: 모든 작업에 가중치를 두고 하나의 QP를 풀어 해를 구하며, 작업 간 절충이 허용되고 가중치 튜닝이 직관적이지만 타협 위험이 있습니다;
- 두 방식의 관계: 엄격한 계층 구조는 매우 큰 가중치 차이로 **근사**할 수 있지만, 정확한 구현은 계층별 영공간 투영이 필요합니다.

**③ HQP의 실시간화**

계층별 QP의 계산량은 계층 수에 따라 증가합니다. Escande 등 2014년 연구는 빠른 온라인 해법을 제시하여 HQP를 휴머노이드 온라인 동작 생성에 적용했습니다(DOI 10.1177/0278364914521306). 공학적으로 흔한 절충은 "혼합 계층"입니다: 상위 1–2개 계층은 엄격하게(균형, 한계), 하위 계층은 가중 합으로 병합(자세 최적화 등 편안함 작업).

### 주요 매개변수 및 사양

| 항목 | 내용 | 출처 |
|------|------|------|
| 계층 구조 | 일반적: 균형/CoM > 발끝 궤적 > 팔 조작 > 자세 최적화 | Wiki §8.4.10.2 작업 스택 그림 |
| 각 계층 해법 | 제약 조건이 있는 QP + 이전 계층 영공간 투영 | Wiki §8.4.10.2 |
| 실시간 해법 | Escande et al. 2014(온라인 휴머노이드 HQP) | DOI 10.1177/0278364914521306 |
| 제어 주파수 | WBC 외부 루프에 따라 50–200 Hz | Wiki 8장 루프 계층 구조 |
| 근사 구현 | 매우 큰 가중치 차이의 가중 합으로 엄격한 계층 구조 근사 가능 | Wiki §8.4.10.2 |

### 비교 분석

| 차원 | 가중 합 WBC | **엄격한 계층 HQP** | 혼합 계층 |
|------|-----------|------------------|----------|
| 작업 충돌 | 상호 타협 | 높은 우선순위 불가침 | 상위 엄격, 하위 가중 |
| 튜닝 | 가중치 직관적이지만 취약 | 우선순위 명확 | 절충 |
| 계산량 | 단일 QP | 계층별 QP(높음) | 중간 |
| 안전성 | 소프트 제약 | 하드 보장 | 상위 하드 보장 |
| 적용 | 편안함 작업 | 안전 필수 작업 스택 | 공학적 주류 |

(근거: 프로젝트 Wiki 8장 §8.4.10; Escande 2014.)

### 사용 사례 / 적용 사례

- **조작 작업의 전신 지지**: Wiki 16장 §16.5.2는 휴머노이드 조작에서 HQP로 "균형 유지 > 말단 궤적 추종 > 관절 한계 회피" 작업 스택을 우선순위에 따라 푸는 것을 기록;
- **온라인 동작 생성**: Escande et al. 2014는 휴머노이드 HQP를 실시간으로 확장(DOI는 참고 문헌 참조);
- **지식 그래프 이웃**: `ent_formalism_inverse_dynamics_qp`(역동역학 QP 형식화), `ent_equation_weighted_task_error_objective`(가중 작업 오차 목적식), `ent_operator_task_jacobian`(작업 야코비 연산자).

### 한계 및 경계

- **계층 수에 따른 계산량 증가**: 계층별 QP + 영공간 투영은 단일 QP보다 비싸며, 임베디드 실시간에서는 축소가 필요합니다;
- **계층 간 실현 가능성**: 높은 우선순위 제약이 너무 강하면 낮은 우선순위의 영공간이 비어 있을 수 있으므로 완화 전략이 필요합니다;
- **우선순위 설계가 곧 안전 설계**: 계층 순서 오류(예: 자세 최적화를 균형보다 앞에 배치)는 잠재적 사고 원인입니다;
- **모델 의존성**: 모든 WBC와 마찬가지로 동역학 모델 오차가 영공간을 직접 오염시킵니다.

### 일반적인 오해

1. **"가중 합 가중치를 키우면 우선순위와 같다"**——매우 큰 가중치 차이는 근사일 뿐입니다; 교란 하에서 소프트 우선순위는 여전히 돌파될 수 있으며, 엄격한 계층 구조만이 부등식으로 보장됩니다.
2. **"계층이 많을수록 좋다"**——각 계층은 계산 및 실현 가능성 비용을 지불합니다; 공학적으로 2–4개 계층에 혼합 계층을 더하는 것이 일반적입니다(**공학적 판단**).
3. **"HQP 출력 위치"**——WBC와 동일하게 출력은 가속도/토크 수준의 양이며, 서보 계층을 통해 실행되어야 합니다.
4. **"RL이 있으면 계층이 필요 없다"**——학습 정책의 동작도 여전히 WBC/HQP 계층을 통해 안전 제약을 보장받는 경우가 많습니다.

### 관련 지식

- `ent_method_whole_body_control` — 상위 프레임워크(WBC 카드)
- `ent_method_model_predictive_control` — 상류 선행 계획
- `ent_formalism_inverse_dynamics_qp` — 역동역학 QP 형식화
- `ent_operator_task_jacobian` — 작업 야코비 연산자
- `ent_equation_weighted_task_error_objective` — 가중 작업 오차 목적식
- `ent_principle_capture_point` — 균형 작업의 발 디딤 지점 개념
