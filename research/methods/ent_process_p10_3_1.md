---
$id: ent_process_p10_3_1
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: method
names:
  en: System Identification and Parameter Calibration
  zh: 系统辨识与参数校准
  ko: 시스템 식별 및 파라미터 보정
summary:
  en: Identification report, simulation–hardware frequency response comparison
  zh: '- P10.3.1.1 输入梳理与目标量化 - 整理「系统辨识与参数校准」所需的上游输入、参考标准与资源，将完成标准转化为可量化的验收指标，并明确 Owner 与里程碑。 - 列出所有上游输入清单并确认版本 - 将验收标准转化为可量化
    KPI - 建立任务 Owner、时间节点与风险登记'
  ko: 辨识报告、仿真-实物频率响应对比
domains:
- 07_ai_models_algorithms
- 06_design_engineering
layers:
- intelligence
- midstream
functional_roles:
- process
- knowledge
tags: []
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py. English name/summary machine-translated
    from Chinese by scripts/backfill_en_translations.py. | WP3 marker fix 2026-08-10: names.ko translated zh->ko (DeepSeek).  [2026-08-11]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b1r_b2): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: wbs_v3_report
  type: report
  title: 全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）
  date: '2026-06-27'
theoretical_depth:
- method
---
## 概述

系统辨识与参数校准（System Identification and Parameter Calibration）是运动控制算法开发与验证阶段（P10.3）中的关键工序节点，其核心使命是通过实验数据估计机器人真实动力学参数，缩小仿真与实物之间的差距。它真正改变的不是单次仿真的精度，而是整个 sim-to-real 迁移链条的可信度——没有经过校准的模型，再优秀的控制算法也只是空中楼阁。

## 核心内容

### 是什么：准确定义

系统辨识与参数校准是从实验数据中估计机器人真实动力学参数（质量、质心、惯性张量、摩擦、执行器增益与延迟）的方法体系。其核心数学事实是：刚体动力学方程对这些参数是**线性的**——$\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q},\dot{\mathbf{q}},\ddot{\mathbf{q}})\,\boldsymbol{\pi}$，因此可用最小二乘从力矩-运动数据中解出最小参数集。它是 sim-to-real"缩小差距"路线与高精度模型控制（MPC/WBC）的共同前提。

在工程流程层面，该节点属于 P10.3 Sim-to-Real 与测试工作包，具体展开为五个三级子任务：输入梳理与目标量化（P10.3.1.1）、方案/方法设计（P10.3.1.2）、实施/原型/样件制作（P10.3.1.3）、验证与问题闭环（P10.3.1.4）、文档输出与下游交付（P10.3.1.5）。每个子任务又细分为若干四级关键动作，形成完整的工程闭环。

### 为什么存在：痛点与历史定位

在人形机器人开发早期，仿真参数往往直接沿用 CAD 模型的理论值，而实际硬件存在加工公差、装配误差、摩擦非线性、线缆拖拽等不确定因素。这些差异导致仿真中表现优异的控制算法在实物上失效——典型的"仿真能走，实物就摔"困境。

系统辨识与参数校准正是为解决这一痛点而存在。它的历史定位可以概括为：**在硬件平台定型之后、整机集成测试之前，承担着将物理系统的真实特性"翻译"成控制算法可用的模型参数**。没有这一步，后续的柔顺控制、全身运动控制（WBC）都建立在错误的模型假设之上，调试成本呈指数级上升。

### 原理拆解

**① 线性回归：动力学参数的数学本质**

机器人标准动力学方程为：

$$
\boldsymbol{\tau} = \mathbf{M}(\mathbf{q})\ddot{\mathbf{q}} + \mathbf{C}(\mathbf{q},\dot{\mathbf{q}})\dot{\mathbf{q}} + \mathbf{g}(\mathbf{q})
$$

其中 $\mathbf{M}$ 为惯性矩阵，$\mathbf{C}$ 为科氏力/离心力项，$\mathbf{g}$ 为重力项。关键性质是：该方程对动力学参数（质量、质心位置、惯性张量元素）是线性的，可重写为：

$$
\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q}, \dot{\mathbf{q}}, \ddot{\mathbf{q}}) \, \boldsymbol{\pi}
$$

$\mathbf{Y}$ 为回归矩阵，$\boldsymbol{\pi}$ 为待辨识参数向量。这意味着可以通过采集多组（运动状态，力矩）数据对，用最小二乘法直接求解参数——无需迭代优化，计算效率高且全局收敛。

**② 激励轨迹设计：可辨识性的前提**

参数可辨识性取决于回归矩阵 $\mathbf{Y}$ 的列秩。若激励轨迹不充分（如关节运动范围过小、速度过低），某些参数将不可辨识或辨识精度极差。工程上常用**傅里叶级数轨迹**作为激励信号，其频率成分丰富且幅值可控，能有效激发各动力学项。关键约束是激励信号安全性——轨迹幅值、速度、加速度必须限制在硬件允许范围内，避免损坏执行器或触发安全保护。

**③ 摩擦与惯量辨识：非线性项的工程处理**

摩擦模型通常采用库仑+粘滞摩擦形式：$\tau_f = f_c \cdot \text{sign}(\dot{q}) + f_v \cdot \dot{q}$。惯量辨识则需在恒定速度段进行，以分离惯性项与摩擦项。工程上常采用**分段辨识**策略：先低速段辨识摩擦参数，再高速段辨识惯量参数，最后用频域辨识交叉验证。

### 关键参数与规格

| 参数/规格 | 典型值/要求 | 说明 |
|-----------|-------------|------|
| 激励信号类型 | 傅里叶级数轨迹 | 频率成分丰富，幅值可控 |
| 激励频率范围 | 0.1–10 Hz | 覆盖控制带宽（工程判断） |
| 采样率 | ≥ 1 kHz | 需满足奈奎斯特采样定理（工程判断） |
| 最小二乘求解 | 批量或递推 | 需确认数据量充足（工程判断） |
| 辨识报告输出 | 参数表+频率响应对比 | 完成标准之一 |
| 候选方案数量 | 不少于 2 个 | P10.3.1.2 的硬性要求 |

### 横向对比

系统辨识与参数校准在机器人领域有多种实现路径，各有适用场景：

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| 最小二乘（LS） | 计算简单、全局收敛 | 对噪声敏感、需激励充分 | 线性参数模型 |
| 最大似然（ML） | 统计最优、抗噪强 | 计算量大、需噪声模型 | 高精度辨识 |
| 频域辨识 | 直观、可分离频段 | 需稳态激励、非线性难处理 | 执行器/结构辨识 |
| 摩擦/惯量分段辨识 | 工程实用、易实现 | 忽略耦合项 | 快速标定 |

### 谁在用·应用案例

该工序节点在人形机器人开发中具有广泛的应用场景。在运动控制算法开发与验证阶段（P10.3），系统辨识与参数校准是连接仿真与实物的关键桥梁。具体应用包括：

- **动力学模型前馈控制**：辨识后的参数用于计算前馈力矩，显著降低反馈控制的负担，提升跟踪精度。
- **MPC/WBC 模型更新**：全身运动控制（WBC）依赖准确的动力学模型，辨识结果直接更新模型参数。
- **仿真环境校准**：将辨识参数回填至仿真环境，使仿真行为更接近实物，提升 sim-to-real 迁移成功率。
- **故障诊断**：当辨识参数与标称值偏差过大时，可提示机械磨损、装配松动等异常（参考 CARE 方法的因果诊断思路）。

### 局限与边界

系统辨识与参数校准并非万能，其局限性需要清醒认识：

1. **参数可辨识性受限**：某些参数（如连杆惯性张量的交叉项）在常规激励下难以独立辨识，需特殊轨迹设计或降维处理。
2. **模型误差不可消除**：辨识只能缩小模型与实物的差距，无法完全消除——未建模动态（如柔性、迟滞）始终存在。
3. **激励信号安全性约束**：过强的激励可能损坏硬件或触发安全保护，限制了可辨识参数的精度上限。
4. **时变参数问题**：摩擦、温度等参数随时间变化，单次辨识结果可能很快失效，需定期重标定。

### 常见误区

1. **"辨识一次就一劳永逸"**——错。摩擦随温度、磨损变化，惯量随负载变化，需建立定期重标定机制。
2. **"激励越强辨识越准"**——错。过强激励可能激发非线性效应（如柔性模态），反而降低模型精度，且存在安全风险。
3. **"最小二乘是唯一方法"**——错。最小二乘对噪声敏感，在低信噪比场景下最大似然或频域辨识更优。
4. **"辨识报告只是文档"**——错。辨识报告中的频率响应对比是验证模型质量的核心证据，也是下游依赖方（如 WBC 开发）的重要输入。

### 相关知识

- `ent_method_system_identification` — 本工序节点的方法论基础，提供系统辨识的数学原理与实现细节。
- `ent_process_p10` — 本工序节点所属的上级工作包，定义了运动控制算法开发与验证的整体流程。
- `ent_paper_hossen_care_finding_root_causes_of_co_2023` — CARE 方法展示了从系统数据中定位故障根因的因果视角，与参数校准中的异常检测思路相通。

## 参考

- [全尺寸双足人形机器人产品开发全流程报告（V3 / 三四级任务展开版）](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [项目 Wiki 第 8 章](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [项目 Wiki 第 18 章](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-18.md)

## Overview

System Identification and Parameter Calibration is a critical process node in the motion control algorithm development and validation phase (P10.3). Its core mission is to estimate the robot's true dynamic parameters from experimental data, narrowing the gap between simulation and reality. What it truly changes is not the accuracy of a single simulation, but the credibility of the entire sim-to-real transfer chain—without a calibrated model, even the best control algorithm is nothing more than a castle in the air.

## Content

### What It Is: Precise Definition

System Identification and Parameter Calibration is a methodological framework for estimating a robot's true dynamic parameters (mass, center of mass, inertia tensor, friction, actuator gains, and delays) from experimental data. Its core mathematical fact is that rigid-body dynamics equations are **linear** in these parameters—$\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q},\dot{\mathbf{q}},\ddot{\mathbf{q}})\,\boldsymbol{\pi}$—allowing the minimum parameter set to be solved from torque-motion data via least squares. It is the common prerequisite for the sim-to-real "gap reduction" approach and high-fidelity model-based control (MPC/WBC).

At the engineering process level, this node belongs to the P10.3 Sim-to-Real and Testing work package, unfolding into five third-level subtasks: input organization and target quantification (P10.3.1.1), approach/method design (P10.3.1.2), implementation/prototype/sample fabrication (P10.3.1.3), validation and issue closure (P10.3.1.4), and documentation output and downstream delivery (P10.3.1.5). Each subtask is further broken down into several fourth-level key actions, forming a complete engineering loop.

### Why It Exists: Pain Points and Historical Positioning

In the early stages of humanoid robot development, simulation parameters often directly adopted theoretical values from CAD models, while actual hardware involves uncertainties such as machining tolerances, assembly errors, friction nonlinearities, and cable drag. These discrepancies cause control algorithms that perform excellently in simulation to fail on real hardware—the classic "works in simulation, falls in reality" dilemma.

System Identification and Parameter Calibration exists precisely to address this pain point. Its historical positioning can be summarized as: **after the hardware platform is finalized and before full-system integration testing, it translates the physical system's true characteristics into model parameters usable by control algorithms**. Without this step, subsequent compliant control and whole-body control (WBC) are built on incorrect model assumptions, and debugging costs escalate exponentially.

### Principle Breakdown

**① Linear Regression: The Mathematical Essence of Dynamic Parameters**

The standard robot dynamics equation is:

$$
\boldsymbol{\tau} = \mathbf{M}(\mathbf{q})\ddot{\mathbf{q}} + \mathbf{C}(\mathbf{q},\dot{\mathbf{q}})\dot{\mathbf{q}} + \mathbf{g}(\mathbf{q})
$$

where $\mathbf{M}$ is the inertia matrix, $\mathbf{C}$ is the Coriolis/centrifugal term, and $\mathbf{g}$ is the gravity term. The key property is that this equation is linear in the dynamic parameters (mass, center-of-mass position, inertia tensor elements) and can be rewritten as:

$$
\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q}, \dot{\mathbf{q}}, \ddot{\mathbf{q}}) \, \boldsymbol{\pi}
$$

$\mathbf{Y}$ is the regression matrix, and $\boldsymbol{\pi}$ is the parameter vector to be identified. This means that by collecting multiple (motion state, torque) data pairs, the parameters can be solved directly using least squares—no iterative optimization required, with high computational efficiency and global convergence.

**② Excitation Trajectory Design: The Prerequisite for Identifiability**

Parameter identifiability depends on the column rank of the regression matrix $\mathbf{Y}$. If the excitation trajectory is insufficient (e.g., joint motion range too small, speeds too low), some parameters become unidentifiable or have extremely poor identification accuracy. In engineering, **Fourier series trajectories** are commonly used as excitation signals, offering rich frequency content and controllable amplitudes that effectively excite various dynamic terms. A key constraint is excitation signal safety—trajectory amplitude, velocity, and acceleration must be limited to the hardware's allowable range to avoid damaging actuators or triggering safety protections.

**③ Friction and Inertia Identification: Engineering Handling of Nonlinear Terms**

Friction models typically use Coulomb + viscous friction form: $\tau_f = f_c \cdot \text{sign}(\dot{q}) + f_v \cdot \dot{q}$. Inertia identification requires constant-velocity segments to separate inertial and friction terms. Engineering practice often employs a **segmented identification** strategy: first identify friction parameters at low speeds, then inertia parameters at high speeds, and finally cross-validate using frequency-domain identification.

### Key Parameters and Specifications

| Parameter/Specification | Typical Value/Requirement | Description |
|-------------------------|---------------------------|-------------|
| Excitation signal type | Fourier series trajectory | Rich frequency content, controllable amplitude |
| Excitation frequency range | 0.1–10 Hz | Covers control bandwidth (engineering judgment) |
| Sampling rate | ≥ 1 kHz | Must satisfy Nyquist sampling theorem (engineering judgment) |
| Least squares solution | Batch or recursive | Must confirm sufficient data volume (engineering judgment) |
| Identification report output | Parameter table + frequency response comparison | One of the completion criteria |
| Number of candidate approaches | No fewer than 2 | Hard requirement of P10.3.1.2 |

### Horizontal Comparison

System Identification and Parameter Calibration has multiple implementation paths in robotics, each with its applicable scenarios:

| Method | Advantages | Disadvantages | Applicable Scenarios |
|--------|------------|---------------|----------------------|
| Least Squares (LS) | Simple computation, global convergence | Sensitive to noise, requires sufficient excitation | Linear parameter models |
| Maximum Likelihood (ML) | Statistically optimal, strong noise resistance | High computational cost, requires noise model | High-precision identification |
| Frequency-domain identification | Intuitive, allows frequency-band separation | Requires steady-state excitation, difficult for nonlinearities | Actuator/structure identification |
| Segmented friction/inertia identification | Engineering-practical, easy to implement | Ignores coupling terms | Rapid calibration |

### Who Uses It · Application Cases

This process node has broad application scenarios in humanoid robot development. In the motion control algorithm development and validation phase (P10.3), System Identification and Parameter Calibration serves as the key bridge connecting simulation and reality. Specific applications include:

- **Dynamic model feedforward control**: Identified parameters are used to compute feedforward torques, significantly reducing the burden on feedback control and improving tracking accuracy.
- **MPC/WBC model updates**: Whole-body control (WBC) relies on accurate dynamic models; identification results directly update model parameters.
- **Simulation environment calibration**: Identified parameters are fed back into the simulation environment, making simulated behavior closer to reality and improving sim-to-real transfer success rates.
- **Fault diagnosis**: When identified parameters deviate excessively from nominal values, it can indicate mechanical wear, loose assembly, or other anomalies (referencing the causal diagnosis approach of the CARE method).

### Limitations and Boundaries

System Identification and Parameter Calibration is not a panacea, and its limitations must be clearly recognized:

1. **Limited parameter identifiability**: Certain parameters (e.g., cross-terms of link inertia tensors) are difficult to identify independently under conventional excitation, requiring special trajectory design or dimensionality reduction.
2. **Model errors cannot be eliminated**: Identification can only narrow the gap between model and reality, not eliminate it entirely—unmodeled dynamics (e.g., flexibility, hysteresis) always remain.
3. **Excitation signal safety constraints**: Overly strong excitation may damage hardware or trigger safety protections, limiting the upper bound of identifiable parameter accuracy.
4. **Time-varying parameter issues**: Parameters such as friction and temperature change over time; single identification results may quickly become invalid, requiring periodic recalibration.

### Common Misconceptions

1. **"Identify once and done"**—Wrong. Friction changes with temperature and wear, inertia changes with load; a periodic recalibration mechanism must be established.
2. **"Stronger excitation means better identification"**—Wrong. Overly strong excitation may excite nonlinear effects (e.g., flexible modes), actually reducing model accuracy and posing safety risks.
3. **"Least squares is the only method"**—Wrong. Least squares is sensitive to noise; in low signal-to-noise ratio scenarios, maximum likelihood or frequency-domain identification is superior.
4. **"The identification report is just documentation"**—Wrong. The frequency response comparison in the identification report is core evidence for validating model quality and a critical input for downstream dependents (e.g., WBC development).

### Related Knowledge

- `ent_method_system_identification` — The methodological foundation of this process node, providing the mathematical principles and implementation details of system identification.
- `ent_process_p10` — The parent work package to which this process node belongs, defining the overall workflow of motion control algorithm development and validation.
- `ent_paper_hossen_care_finding_root_causes_of_co_2023` — The CARE method demonstrates a causal perspective for locating fault root causes from system data, sharing conceptual alignment with anomaly detection in parameter calibration.

## 개요

시스템 식별 및 파라미터 보정(System Identification and Parameter Calibration)은 운동 제어 알고리즘 개발 및 검증 단계(P10.3)에서의 핵심 공정 노드로, 그 핵심 사명은 실험 데이터를 통해 로봇의 실제 동역학 파라미터를 추정하여 시뮬레이션과 실물 간의 격차를 줄이는 것입니다. 이것이 실제로 바꾸는 것은 단일 시뮬레이션의 정밀도가 아니라, 전체 sim-to-real 전이 체인의 신뢰성입니다——보정되지 않은 모델로는 아무리 훌륭한 제어 알고리즘도 허상에 불과합니다.

## 핵심 내용

### 무엇인가: 정확한 정의

시스템 식별 및 파라미터 보정은 실험 데이터에서 로봇의 실제 동역학 파라미터(질량, 질량 중심, 관성 텐서, 마찰, 액추에이터 이득 및 지연)를 추정하는 방법론 체계입니다. 핵심 수학적 사실은 강체 동역학 방정식이 이러한 파라미터에 대해 **선형적**이라는 것입니다——$\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q},\dot{\mathbf{q}},\ddot{\mathbf{q}})\,\boldsymbol{\pi}$——따라서 최소자승법을 사용하여 토크-운동 데이터에서 최소 파라미터 집합을 풀 수 있습니다. 이것은 sim-to-real "격차 줄이기" 경로와 고정밀 모델 제어(MPC/WBC)의 공통 전제 조건입니다.

엔지니어링 프로세스 측면에서 이 노드는 P10.3 Sim-to-Real 및 테스트 작업 패키지에 속하며, 구체적으로 다섯 개의 3단계 하위 작업으로 전개됩니다: 입력 정리 및 목표 정량화(P10.3.1.1), 방안/방법 설계(P10.3.1.2), 구현/프로토타입/시제품 제작(P10.3.1.3), 검증 및 문제 폐루프(P10.3.1.4), 문서 출력 및 하류 전달(P10.3.1.5). 각 하위 작업은 다시 여러 4단계 핵심 동작으로 세분화되어 완전한 엔지니어링 폐루프를 형성합니다.

### 왜 존재하는가:痛点 및 역사적 위치

휴머노이드 로봇 개발 초기에는 시뮬레이션 파라미터가 종종 CAD 모델의 이론값을 직접 사용했지만, 실제 하드웨어에는 가공 공차, 조립 오차, 마찰 비선형성, 케이블 드래깅 등 불확실한 요소가 존재합니다. 이러한 차이로 인해 시뮬레이션에서 우수한 성능을 보인 제어 알고리즘이 실물에서 실패하게 됩니다——전형적인 "시뮬레이션에서는 걷는데, 실물에서는 넘어진다"는 딜레마입니다.

시스템 식별 및 파라미터 보정은 바로 이러한痛点를 해결하기 위해 존재합니다. 그 역사적 위치는 다음과 같이 요약할 수 있습니다: **하드웨어 플랫폼이 확정된 이후, 전체 시스템 통합 테스트 이전에, 물리 시스템의 실제 특성을 제어 알고리즘이 사용할 수 있는 모델 파라미터로 "번역"하는 역할을 담당합니다**. 이 단계가 없으면 이후의 컴플라이언트 제어, 전신 운동 제어(WBC)는 모두 잘못된 모델 가정 위에 구축되어 디버깅 비용이 기하급수적으로 증가합니다.

### 원리 분해

**① 선형 회귀: 동역학 파라미터의 수학적 본질**

로봇의 표준 동역학 방정식은 다음과 같습니다:

$$
\boldsymbol{\tau} = \mathbf{M}(\mathbf{q})\ddot{\mathbf{q}} + \mathbf{C}(\mathbf{q},\dot{\mathbf{q}})\dot{\mathbf{q}} + \mathbf{g}(\mathbf{q})
$$

여기서 $\mathbf{M}$은 관성 행렬, $\mathbf{C}$는 코리올리/원심력 항, $\mathbf{g}$는 중력 항입니다. 핵심 특성은 이 방정식이 동역학 파라미터(질량, 질량 중심 위치, 관성 텐서 요소)에 대해 선형적이며, 다음과 같이 다시 쓸 수 있다는 것입니다:

$$
\boldsymbol{\tau} = \mathbf{Y}(\mathbf{q}, \dot{\mathbf{q}}, \ddot{\mathbf{q}}) \, \boldsymbol{\pi}
$$

$\mathbf{Y}$는 회귀 행렬, $\boldsymbol{\pi}$는 식별 대상 파라미터 벡터입니다. 이는 다중 (운동 상태, 토크) 데이터 쌍을 수집하여 최소자승법으로 직접 파라미터를 풀 수 있음을 의미합니다——반복 최적화가 필요 없으며, 계산 효율이 높고 전역 수렴합니다.

**② 여기 궤적 설계: 식별 가능성의 전제 조건**

파라미터 식별 가능성은 회귀 행렬 $\mathbf{Y}$의 열 랭크에 달려 있습니다. 여기 궤적이 충분하지 않으면(예: 관절 운동 범위가 너무 작거나 속도가 너무 낮은 경우) 일부 파라미터는 식별 불가능하거나 식별 정밀도가 극히 낮아집니다. 공학에서는 일반적으로 **푸리에 급수 궤적**을 여기 신호로 사용하는데, 주파수 성분이 풍부하고 진폭이 제어 가능하여 각 동역학 항을 효과적으로 여기할 수 있습니다. 핵심 제약은 여기 신호 안전성입니다——궤적 진폭, 속도, 가속도는 하드웨어 허용 범위 내로 제한되어 액추에이터 손상이나 안전 보호 작동을 방지해야 합니다.

**③ 마찰 및 관성 식별: 비선형 항의 공학적 처리**

마찰 모델은 일반적으로 쿨롱 + 점성 마찰 형태를 사용합니다: $\tau_f = f_c \cdot \text{sign}(\dot{q}) + f_v \cdot \dot{q}$. 관성 식별은 관성 항과 마찰 항을 분리하기 위해 일정 속도 구간에서 수행해야 합니다. 공학에서는 일반적으로 **구간별 식별** 전략을 사용합니다: 먼저 저속 구간에서 마찰 파라미터를 식별하고, 그다음 고속 구간에서 관성 파라미터를 식별한 후, 마지막으로 주파수 영역 식별로 교차 검증합니다.

### 핵심 파라미터 및 사양

| 파라미터/사양 | 일반값/요구사항 | 설명 |
|-----------|-------------|------|
| 여기 신호 유형 | 푸리에 급수 궤적 | 주파수 성분 풍부, 진폭 제어 가능 |
| 여기 주파수 범위 | 0.1–10 Hz | 제어 대역폭 커버(공학적 판단) |
| 샘플링 주파수 | ≥ 1 kHz | 나이퀴스트 샘플링 정리 충족 필요(공학적 판단) |
| 최소자승법 해법 | 배치 또는 순환 | 데이터 양 충분성 확인 필요(공학적 판단) |
| 식별 보고서 출력 | 파라미터 표 + 주파수 응답 비교 | 완료 기준 중 하나 |
| 후보 방안 수 | 2개 이상 | P10.3.1.2의 필수 요구사항 |

### 횡적 비교

시스템 식별 및 파라미터 보정은 로봇 분야에서 여러 구현 경로가 있으며, 각각 적용 시나리오가 다릅니다:

| 방법 | 장점 | 단점 | 적용 시나리오 |
|------|------|------|----------|
| 최소자승법(LS) | 계산 간단, 전역 수렴 | 노이즈에 민감, 충분한 여기 필요 | 선형 파라미터 모델 |
| 최대우도(ML) | 통계적 최적, 노이즈 저항 강함 | 계산량 큼, 노이즈 모델 필요 | 고정밀 식별 |
| 주파수 영역 식별 | 직관적, 주파수 대역 분리 가능 | 정상 상태 여기 필요, 비선형 처리 어려움 | 액추에이터/구조 식별 |
| 마찰/관성 구간별 식별 | 공학적 실용성, 구현 용이 | 결합 항 무시 | 빠른 보정 |

### 누가 사용하는가·적용 사례

이 공정 노드는 휴머노이드 로봇 개발에서 광범위한 적용 시나리오를 가집니다. 운동 제어 알고리즘 개발 및 검증 단계(P10.3)에서 시스템 식별 및 파라미터 보정은 시뮬레이션과 실물을 연결하는 핵심 다리입니다. 구체적인 적용은 다음과 같습니다:

- **동역학 모델 피드포워드 제어**: 식별된 파라미터는 피드포워드 토크 계산에 사용되어 피드백 제어의 부담을 크게 줄이고 추적 정밀도를 향상시킵니다.
- **MPC/WBC 모델 업데이트**: 전신 운동 제어(WBC)는 정확한 동역학 모델에 의존하며, 식별 결과는 모델 파라미터를 직접 업데이트합니다.
- **시뮬레이션 환경 보정**: 식별된 파라미터를 시뮬레이션 환경에 다시 채워 넣어 시뮬레이션 동작이 실물에 더 가깝게 만들고, sim-to-real 전이 성공률을 높입니다.
- **고장 진단**: 식별 파라미터가 공칭값과 편차가 너무 크면 기계 마모, 조립 느슨함 등의 이상을 알릴 수 있습니다(CARE 방법의 인과 진단 사고방식 참조).

### 한계 및 경계

시스템 식별 및 파라미터 보정은 만능이 아니며, 그 한계를 명확히 인식해야 합니다:

1. **파라미터 식별 가능성 제한**: 일부 파라미터(예: 링크 관성 텐서의 교차 항)는 일반적인 여기에서 독립적으로 식별하기 어려우며, 특수 궤적 설계 또는 차원 축소 처리가 필요합니다.
2. **모델 오차는 제거 불가능**: 식별은 모델과 실물 간의 격차를 줄일 수 있을 뿐 완전히 제거할 수는 없습니다——미모델링 동역학(예: 유연성, 히스테리시스)은 항상 존재합니다.
3. **여기 신호 안전성 제약**: 과도한 여기는 하드웨어를 손상시키거나 안전 보호를 작동시킬 수 있어, 식별 가능한 파라미터의 정밀도 상한을 제한합니다.
4. **시변 파라미터 문제**: 마찰, 온도 등의 파라미터는 시간에 따라 변하므로, 단일 식별 결과는 빠르게 무효화될 수 있으며 주기적인 재보정이 필요합니다.

### 일반적인 오해

1. **"한 번 식별하면 영구적이다"**——틀림. 마찰은 온도와 마모에 따라 변하고, 관성은 부하에 따라 변하므로 주기적 재보정 메커니즘을 구축해야 합니다.
2. **"여기가 강할수록 식별이 정확하다"**——틀림. 과도한 여기는 비선형 효과(예: 유연 모드)를 유발하여 오히려 모델 정밀도를 낮추고 안전 위험이 있습니다.
3. **"최소자승법이 유일한 방법이다"**——틀림. 최소자승법은 노이즈에 민감하며, 낮은 신호 대 잡음비 시나리오에서는 최대우도 또는 주파수 영역 식별이 더 우수합니다.
4. **"식별 보고서는 단지 문서일 뿐이다"**——틀림. 식별 보고서의 주파수 응답 비교는 모델 품질을 검증하는 핵심 증거이며, 하류 의존자(예: WBC 개발)에게 중요한 입력입니다.

### 관련 지식

- `ent_method_system_identification` — 본 공정 노드의 방법론적 기초로, 시스템 식별의 수학적 원리와 구현 세부 사항을 제공합니다.
- `ent_process_p10` — 본 공정 노드가 속한 상위 작업 패키지로, 운동 제어 알고리즘 개발 및 검증의 전체 프로세스를 정의합니다.
- `ent_paper_hossen_care_finding_root_causes_of_co_2023` — CARE 방법은 시스템 데이터에서 고장 근본 원인을 찾는 인과적 관점을 보여주며, 파라미터 보정의 이상 탐지 사고방식과 통합니다.
