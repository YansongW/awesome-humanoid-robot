---
$id: ent_equation_weighted_task_error_objective
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: equation
names:
  en: Weighted Task-Error Objective
  zh: 加权任务误差目标函数
  ko: 가중 작업 오차 목적 함수
summary:
  en: The QP objective that penalizes the weighted squared difference between desired and predicted task accelerations plus
    a regularization term on generalized accelerations.
  zh: 惩罚期望任务加速度与预测任务加速度之间加权平方差，并对广义加速度添加正则项的 QP 目标。
  ko: 원하는 작업 가속도와 예측 작업 가속도 간의 가중 제곱 차이를 벌점으로 부여하고 일반화 가속도에 정규화 항을 추가한 QP 목적 함수이다.
domains:
- 07_ai_models_algorithms
layers:
- intelligence
functional_roles:
- knowledge
tags:
- qp_objective
- task_error
- weighted_norm
- regularization
- humanoid_control
theoretical_depth:
- formalism
verification:
  status: partially_verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-06-26'
  confidence: high
  notes: 'Standard weighted least-squares objective in WBC. Body backfilled from entity metadata by scripts/backfill_critical_entities.py.  [2026-08-12]
    body upgraded to textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors
    + wiki chapters + first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_001
  type: paper
  title: 'Hierarchical QP whole-body control: from theory to practice'
  url: https://arxiv.org/abs/1910.13329
  date: '2019'
  accessed_at: '2026-06-26'
related_entities:
- id: ent_operator_task_jacobian
  relationship: uses
  description:
    en: The objective uses the task Jacobian to map joint accelerations to task accelerations.
    zh: 目标函数使用任务 Jacobian 将关节加速度映射到任务加速度。
    ko: 목적 함수는 작업 Jacobian을 사용하여 관절 가속도를 작업 가속도로 매핑한다.
---
## 概述

加权任务误差目标函数（Weighted Task-Error Objective）是全身控制中一种 QP 目标函数形式：惩罚期望任务加速度与预测任务加速度之间的加权平方差，并对广义加速度添加正则项。它通过权重 \(w_i\) 反映任务相对重要性，是连接任务优先级理论与实际数值求解的桥梁。

## 核心内容

### 是什么：准确定义

加权任务误差目标函数属于 **方程** 类型，英文名称为 *Weighted Task-Error Objective*，韩文名称为 *가중 작업 오차 목적 함수*。它属于 AI 模型与算法领域，位于价值链的智能层。

其数学形式可概括为：给定一组任务（如质心平衡、足端轨迹、手臂操作），每个任务 i 对应一个期望任务加速度 \(\ddot{x}_i^*\) 和一个权重 \(w_i\)，目标函数惩罚预测任务加速度与期望值之间的加权平方差，同时添加广义加速度的正则项以改善数值稳定性。权重 \(w_i\) 反映任务相对重要性——权重越大，该任务在优化中的优先级越高。

它真正改变的不是"如何描述任务"，而是"如何在同一个优化框架内让多个任务共存"——从硬性的优先级排序转向软性的加权折中。

### 为什么存在：痛点与历史定位

在全身控制（WBC）发展早期，多任务协调主要依赖严格层次方法：任务按优先级排序，高优先级任务完全满足后，低优先级任务只能在其零空间内求解。这种方法的数学基础是零空间投影——矩阵 \(N = I - J^{\dagger}J\) 把任意关节速度投影到不改变任务量的方向集合。严格优先级下，低优先级任务不得劣化高优先级任务的解。

但严格层次存在两个痛点：其一，任务优先级需要预先硬编码，无法根据实时状态动态调整；其二，每层求解一个 QP 或伪逆计算，计算开销随任务数量线性增长，实时性受限。

加权任务误差目标函数提供了一种替代路径：通过大权重差距近似严格层次，但允许所有任务在同一个 QP 中同时优化。它真正改变的不是"任务优先级是否存在"，而是"优先级如何表达"——从离散的层次结构变为连续的权重标量。

### 原理拆解

**① 加权平方差：任务误差的软性度量**

设任务 i 的 Jacobian 为 \(J_i\)，广义加速度为 \(\ddot{q}\)，则预测任务加速度为 \(J_i \ddot{q}\)。期望任务加速度为 \(\ddot{x}_i^*\)。加权任务误差目标函数的核心项为：

$$
\min_{\ddot{q}} \sum_{i=1}^{p} w_i \left\| J_i \ddot{q} - \ddot{x}_i^* \right\|^2 + \lambda \|\ddot{q}\|^2
$$

其中 \(w_i\) 为任务 i 的权重，\(\lambda\) 为正则化系数。第一项惩罚任务跟踪误差，第二项为正则项，用于改善数值稳定性或避免平凡解。

**② 权重与近似层次**

权重 \(w_i\) 反映任务相对重要性。严格层次可通过极大权重差距近似，但精确实现需要逐层零空间投影。这意味着：当权重差距足够大时，加权方法的行为趋近于严格层次；但若权重差距不够大，低优先级任务会"污染"高优先级任务的解。工程判断：权重比通常需要达到 \(10^3\)–\(10^6\) 量级才能近似严格层次，具体数值需根据任务特性标定。

**③ 正则项的作用**

正则化（regularization）是在伪逆/QP 中加入小的 \(\| \cdot \|\) 惩罚，避免奇异附近增益爆炸。它真正改变的不是"优化目标本身"，而是"解的数值稳定性"——没有正则项，当 Jacobian 接近奇异时，解会趋向无穷大。

### 关键参数与规格

| 参数 | 含义 | 典型取值（工程判断） |
|------|------|----------------------|
| \(w_i\) | 任务 i 的权重 | 主任务 \(10^3\)–\(10^6\)，次任务 \(1\)–\(10^2\) |
| \(\lambda\) | 正则化系数 | \(10^{-6}\)–\(10^{-3}\)，需自行确认 |
| \(p\) | 任务数量 | 3–10 个（典型全身控制场景） |
| 求解频率 | QP 求解频率 | 1 kHz（典型实时控制） |

### 横向对比

| 方法 | 优先级表达 | 计算开销 | 实时性 | 适用场景 |
|------|-----------|----------|--------|----------|
| 严格层次（零空间投影） | 离散层次 | 随任务数线性增长 | 较低 | 安全关键场景 |
| 加权任务误差 QP | 连续权重 | 单次 QP 求解 | 较高 | 多任务折中 |
| 分层 QP WBC | 逐层 QP | 每层一次 QP | 中等 | 严格优先级+实时性 |

加权方法的核心优势在于单次 QP 求解即可完成多任务优化，计算效率高；代价是任务间存在妥协，无法保证高优先级任务绝对满足。

### 谁在用·应用案例

加权任务误差目标函数广泛应用于人形机器人的全身控制。典型任务栈包括：主任务为平衡/质心（CoM）控制，次任务为足端轨迹跟踪，再次任务为手臂操作，最低优先级为姿态优化。在逆动力学 QP 形式化（Inverse-Dynamics QP Formulation）中，该目标函数与浮动基动力学约束、接触力约束联合求解，输出广义加速度、接触力和关节力矩。

它真正改变的不是"控制器的结构"，而是"多任务协调的数学表达"——让研究者可以在同一个 QP 框架内灵活调整任务权重，而无需重写整个控制架构。

### 局限与边界

1. **权重标定困难**：权重 \(w_i\) 的选取缺乏系统方法，通常依赖人工调参或经验公式。工程判断：权重标定是实际部署中最耗时的环节之一。
2. **任务妥协不可避免**：加权方法本质上是软性约束，高优先级任务无法被绝对满足。在安全关键场景（如防止摔倒）中，这可能不可接受。
3. **对模型精度敏感**：目标函数依赖任务 Jacobian 和动力学模型，模型误差会直接导致任务跟踪性能下降。
4. **正则项引入偏差**：正则化虽然改善数值稳定性，但会引入额外的优化偏差，需要在稳定性和精度之间权衡。

### 常见误区

1. **"权重越大越好"**——错。权重过大可能导致数值病态，且会放大模型误差的影响。权重的意义在于相对关系，而非绝对大小。
2. **"加权方法可以完全替代严格层次"**——不准确。加权方法只能近似严格层次，精确实现仍需要逐层零空间投影。语料明确指出："严格层次可通过极大权重差距近似，但精确实现需要逐层零空间投影。"
3. **"正则项越小越好"**——错。正则项的作用是避免奇异附近增益爆炸，过小的正则化系数会导致数值不稳定。

### 相关知识

- `ent_formalism_inverse_dynamics_qp` — 逆动力学 QP 形式化是加权任务误差目标函数的典型载体，两者联合求解广义加速度与接触力。
- `ent_operator_task_jacobian` — 任务 Jacobian 将关节空间运动映射到任务空间，是构造加权任务误差目标函数的核心算子。
- `ent_method_hierarchical_qp_wbc` — 分层 QP WBC 是加权方法的严格优先级替代方案，通过零空间投影实现任务间绝对隔离。

## 参考

- [Hierarchical QP whole-body control: from theory to practice](https://arxiv.org/abs/1910.13329)
- [Chapter 8: 冗余与零空间运动学](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)
- [Chapter 14: 全身控制与逆动力学 QP](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-14.md)

## Overview

The Weighted Task-Error Objective is a form of QP objective function in whole-body control: it penalizes the weighted squared difference between the desired task acceleration and the predicted task acceleration, while adding a regularization term on the generalized acceleration. It reflects the relative importance of tasks through weights \(w_i\), serving as a bridge between task-priority theory and practical numerical solving.

## Content

### What it is: precise definition

The weighted task-error objective function belongs to the **equation** type, with the English name *Weighted Task-Error Objective* and the Korean name *가중 작업 오차 목적 함수*. It falls within the AI models and algorithms domain, positioned in the intelligence layer of the value chain.

Its mathematical form can be summarized as follows: given a set of tasks (e.g., center-of-mass balancing, foot trajectory tracking, arm manipulation), each task \(i\) corresponds to a desired task acceleration \(\ddot{x}_i^*\) and a weight \(w_i\). The objective function penalizes the weighted squared difference between the predicted task acceleration and the desired value, while adding a regularization term on the generalized acceleration to improve numerical stability. The weight \(w_i\) reflects the relative importance of the task—the larger the weight, the higher the priority of that task in the optimization.

What it truly changes is not "how tasks are described," but "how multiple tasks can coexist within the same optimization framework"—shifting from rigid priority ordering to soft weighted compromise.

### Why it exists: pain points and historical context

In the early development of whole-body control (WBC), multi-task coordination primarily relied on strict hierarchical methods: tasks were sorted by priority, and after higher-priority tasks were fully satisfied, lower-priority tasks could only be solved within their null space. The mathematical foundation of this approach is null-space projection—the matrix \(N = I - J^{\dagger}J\) projects any joint velocity onto the set of directions that do not alter the task quantity. Under strict priority, lower-priority tasks must not degrade the solution of higher-priority tasks.

However, strict hierarchy has two pain points: first, task priorities must be hard-coded in advance and cannot be dynamically adjusted based on real-time states; second, solving a QP or pseudo-inverse computation at each layer incurs computational cost that grows linearly with the number of tasks, limiting real-time performance.

The weighted task-error objective provides an alternative path: approximating strict hierarchy through large weight gaps while allowing all tasks to be optimized simultaneously in a single QP. What it truly changes is not "whether task priorities exist," but "how priorities are expressed"—from discrete hierarchical structures to continuous weight scalars.

### Principle breakdown

**① Weighted squared difference: a soft measure of task error**

Let the Jacobian of task \(i\) be \(J_i\) and the generalized acceleration be \(\ddot{q}\). Then the predicted task acceleration is \(J_i \ddot{q}\), and the desired task acceleration is \(\ddot{x}_i^*\). The core term of the weighted task-error objective is:

$$
\min_{\ddot{q}} \sum_{i=1}^{p} w_i \left\| J_i \ddot{q} - \ddot{x}_i^* \right\|^2 + \lambda \|\ddot{q}\|^2
$$

where \(w_i\) is the weight of task \(i\) and \(\lambda\) is the regularization coefficient. The first term penalizes task tracking error, and the second term is a regularization term used to improve numerical stability or avoid trivial solutions.

**② Weights and approximate hierarchy**

The weight \(w_i\) reflects the relative importance of tasks. Strict hierarchy can be approximated through extremely large weight gaps, but exact implementation requires layer-by-layer null-space projection. This means: when the weight gap is sufficiently large, the behavior of the weighted method approaches that of strict hierarchy; however, if the weight gap is not large enough, lower-priority tasks will "contaminate" the solution of higher-priority tasks. Engineering judgment: weight ratios typically need to reach the order of \(10^3\)–\(10^6\) to approximate strict hierarchy, and the specific values must be calibrated based on task characteristics.

**③ Role of the regularization term**

Regularization involves adding a small \(\| \cdot \|\) penalty in the pseudo-inverse/QP to avoid gain explosion near singularities. What it truly changes is not "the optimization objective itself," but "the numerical stability of the solution"—without the regularization term, the solution tends toward infinity when the Jacobian approaches singularity.

### Key parameters and specifications

| Parameter | Meaning | Typical values (engineering judgment) |
|-----------|---------|----------------------------------------|
| \(w_i\) | Weight of task \(i\) | Primary tasks \(10^3\)–\(10^6\), secondary tasks \(1\)–\(10^2\) |
| \(\lambda\) | Regularization coefficient | \(10^{-6}\)–\(10^{-3}\), requires self-validation |
| \(p\) | Number of tasks | 3–10 (typical whole-body control scenarios) |
| Solve frequency | QP solve frequency | 1 kHz (typical real-time control) |

### Horizontal comparison

| Method | Priority expression | Computational cost | Real-time performance | Applicable scenarios |
|--------|---------------------|--------------------|------------------------|----------------------|
| Strict hierarchy (null-space projection) | Discrete hierarchy | Grows linearly with task count | Lower | Safety-critical scenarios |
| Weighted task-error QP | Continuous weights | Single QP solve | Higher | Multi-task compromise |
| Hierarchical QP WBC | Layer-by-layer QP | One QP per layer | Medium | Strict priority + real-time performance |

The core advantage of the weighted method lies in completing multi-task optimization with a single QP solve, offering high computational efficiency; the cost is that compromises exist between tasks, and high-priority tasks cannot be guaranteed to be absolutely satisfied.

### Who uses it: application cases

The weighted task-error objective is widely applied in whole-body control of humanoid robots. A typical task stack includes: primary task of balance/center-of-mass (CoM) control, secondary task of foot trajectory tracking, tertiary task of arm manipulation, and lowest priority of posture optimization. In the Inverse-Dynamics QP Formulation, this objective is solved jointly with floating-base dynamics constraints and contact force constraints, outputting generalized acceleration, contact forces, and joint torques.

What it truly changes is not "the structure of the controller," but "the mathematical expression of multi-task coordination"—allowing researchers to flexibly adjust task weights within the same QP framework without rewriting the entire control architecture.

### Limitations and boundaries

1. **Difficulty in weight calibration**: The selection of weights \(w_i\) lacks a systematic method and typically relies on manual tuning or empirical formulas. Engineering judgment: weight calibration is one of the most time-consuming steps in practical deployment.
2. **Task compromise is unavoidable**: The weighted method is inherently a soft constraint, and high-priority tasks cannot be absolutely satisfied. In safety-critical scenarios (e.g., fall prevention), this may be unacceptable.
3. **Sensitivity to model accuracy**: The objective function depends on task Jacobians and the dynamics model; model errors directly degrade task tracking performance.
4. **Regularization introduces bias**: Although regularization improves numerical stability, it introduces additional optimization bias, requiring a trade-off between stability and accuracy.

### Common misconceptions

1. **"The larger the weight, the better"**—incorrect. Excessively large weights can lead to numerical ill-conditioning and amplify the impact of model errors. The significance of weights lies in their relative relationships, not absolute magnitudes.
2. **"The weighted method can fully replace strict hierarchy"**—inaccurate. The weighted method can only approximate strict hierarchy; exact implementation still requires layer-by-layer null-space projection. The source text explicitly states: "Strict hierarchy can be approximated through extremely large weight gaps, but exact implementation requires layer-by-layer null-space projection."
3. **"The smaller the regularization term, the better"**—incorrect. The purpose of the regularization term is to avoid gain explosion near singularities; an excessively small regularization coefficient leads to numerical instability.

### Related knowledge

- `ent_formalism_inverse_dynamics_qp` — The inverse-dynamics QP formulation is a typical carrier of the weighted task-error objective; the two are solved jointly to obtain generalized acceleration and contact forces.
- `ent_operator_task_jacobian` — The task Jacobian maps joint-space motion to task space and is the core operator for constructing the weighted task-error objective.
- `ent_method_hierarchical_qp_wbc` — Hierarchical QP WBC is a strict-priority alternative to the weighted method, achieving absolute isolation between tasks through null-space projection.

## 개요

가중 작업 오차 목적 함수(Weighted Task-Error Objective)는 전신 제어에서 사용되는 QP 목적 함수의 한 형태로, 기대 작업 가속도와 예측 작업 가속도 간의 가중 제곱 오차를 페널티로 부과하고 일반화 가속도에 정규화 항을 추가합니다. 가중치 \(w_i\)를 통해 작업의 상대적 중요성을 반영하며, 작업 우선순위 이론과 실제 수치 해석을 연결하는 다리 역할을 합니다.

## 핵심 내용

### 무엇인가: 정확한 정의

가중 작업 오차 목적 함수는 **방정식** 유형에 속하며, 영문 명칭은 *Weighted Task-Error Objective*, 한글 명칭은 *가중 작업 오차 목적 함수*입니다. 이는 AI 모델 및 알고리즘 분야에 속하며, 가치 사슬의 지능 계층에 위치합니다.

수학적 형태는 다음과 같이 요약할 수 있습니다: 일련의 작업(예: 질량 중심 균형, 발끝 궤적, 팔 조작)이 주어졌을 때, 각 작업 i는 기대 작업 가속도 \(\ddot{x}_i^*\)와 가중치 \(w_i\)에 대응합니다. 목적 함수는 예측 작업 가속도와 기대값 간의 가중 제곱 오차를 페널티로 부과하며, 동시에 일반화 가속도에 정규화 항을 추가하여 수치적 안정성을 개선합니다. 가중치 \(w_i\)는 작업의 상대적 중요성을 반영합니다—가중치가 클수록 해당 작업의 최적화 우선순위가 높아집니다.

이것이 실제로 바꾸는 것은 "작업을 어떻게 기술하는가"가 아니라 "동일한 최적화 프레임워크 내에서 여러 작업을 어떻게 공존시키는가"입니다—경직된 우선순위 순서에서 유연한 가중 절충으로의 전환입니다.

### 왜 존재하는가: 문제점과 역사적 위치

전신 제어(WBC) 발전 초기에는 다중 작업 조정이 주로 엄격한 계층적 방법에 의존했습니다: 작업이 우선순위에 따라 정렬되고, 높은 우선순위의 작업이 완전히 충족된 후에야 낮은 우선순위의 작업이 그 영공간 내에서 해를 구할 수 있었습니다. 이 방법의 수학적 기초는 영공간 투영입니다—행렬 \(N = I - J^{\dagger}J\)는 임의의 관절 속도를 작업량을 변경하지 않는 방향 집합으로 투영합니다. 엄격한 우선순위 하에서 낮은 우선순위 작업은 높은 우선순위 작업의 해를 저하시킬 수 없습니다.

그러나 엄격한 계층 구조에는 두 가지 문제점이 있습니다: 첫째, 작업 우선순위는 사전에 하드코딩되어야 하며 실시간 상태에 따라 동적으로 조정할 수 없습니다. 둘째, 각 계층마다 QP 또는 의사역행렬 계산을 수행해야 하므로 계산 비용이 작업 수에 따라 선형적으로 증가하여 실시간성이 제한됩니다.

가중 작업 오차 목적 함수는 대안적 경로를 제공합니다: 큰 가중치 차이를 통해 엄격한 계층 구조를 근사하지만, 모든 작업이 동일한 QP 내에서 동시에 최적화되도록 허용합니다. 이것이 실제로 바꾸는 것은 "작업 우선순위가 존재하는지 여부"가 아니라 "우선순위가 어떻게 표현되는가"입니다—이산적 계층 구조에서 연속적 가중치 스칼라로의 전환입니다.

### 원리 분석

**① 가중 제곱 오차: 작업 오차의 유연한 측정**

작업 i의 Jacobian이 \(J_i\), 일반화 가속도가 \(\ddot{q}\)라고 할 때, 예측 작업 가속도는 \(J_i \ddot{q}\)입니다. 기대 작업 가속도는 \(\ddot{x}_i^*\)입니다. 가중 작업 오차 목적 함수의 핵심 항은 다음과 같습니다:

$$
\min_{\ddot{q}} \sum_{i=1}^{p} w_i \left\| J_i \ddot{q} - \ddot{x}_i^* \right\|^2 + \lambda \|\ddot{q}\|^2
$$

여기서 \(w_i\)는 작업 i의 가중치, \(\lambda\)는 정규화 계수입니다. 첫 번째 항은 작업 추적 오차를 페널티로 부과하고, 두 번째 항은 정규화 항으로 수치적 안정성 개선 또는 자명한 해 회피에 사용됩니다.

**② 가중치와 근사 계층 구조**

가중치 \(w_i\)는 작업의 상대적 중요성을 반영합니다. 엄격한 계층 구조는 극단적인 가중치 차이를 통해 근사할 수 있지만, 정확한 구현은 계층별 영공간 투영이 필요합니다. 이는 가중치 차이가 충분히 클 때 가중 방법의 동작이 엄격한 계층 구조에 수렴함을 의미합니다. 그러나 가중치 차이가 충분하지 않으면 낮은 우선순위 작업이 높은 우선순위 작업의 해를 "오염"시킬 수 있습니다. 공학적 판단: 가중치 비율은 일반적으로 \(10^3\)–\(10^6\) 수준에 도달해야 엄격한 계층 구조를 근사할 수 있으며, 구체적인 수치는 작업 특성에 따라 보정해야 합니다.

**③ 정규화 항의 역할**

정규화(regularization)는 의사역행렬/QP에 작은 \(\| \cdot \|\) 페널티를 추가하여 특이점 부근에서 이득 폭주를 방지합니다. 이것이 실제로 바꾸는 것은 "최적화 목표 자체"가 아니라 "해의 수치적 안정성"입니다—정규화 항이 없으면 Jacobian이 특이점에 가까워질 때 해가 무한대로 발산합니다.

### 주요 매개변수 및 사양

| 매개변수 | 의미 | 일반적 값 (공학적 판단) |
|------|------|----------------------|
| \(w_i\) | 작업 i의 가중치 | 주 작업 \(10^3\)–\(10^6\), 보조 작업 \(1\)–\(10^2\) |
| \(\lambda\) | 정규화 계수 | \(10^{-6}\)–\(10^{-3}\), 직접 확인 필요 |
| \(p\) | 작업 수 | 3–10개 (일반적 전신 제어 시나리오) |
| 해석 주파수 | QP 해석 주파수 | 1 kHz (일반적 실시간 제어) |

### 횡적 비교

| 방법 | 우선순위 표현 | 계산 비용 | 실시간성 | 적용 시나리오 |
|------|-----------|----------|--------|----------|
| 엄격한 계층 구조 (영공간 투영) | 이산적 계층 | 작업 수에 따라 선형 증가 | 낮음 | 안전 핵심 시나리오 |
| 가중 작업 오차 QP | 연속적 가중치 | 단일 QP 해석 | 높음 | 다중 작업 절충 |
| 계층적 QP WBC | 계층별 QP | 계층당 QP 1회 | 중간 | 엄격한 우선순위 + 실시간성 |

가중 방법의 핵심 장점은 단일 QP 해석만으로 다중 작업 최적화를 완료할 수 있어 계산 효율이 높다는 점입니다. 대가는 작업 간 절충이 발생하여 높은 우선순위 작업의 절대적 충족을 보장할 수 없다는 것입니다.

### 사용 사례·적용 사례

가중 작업 오차 목적 함수는 인간형 로봇의 전신 제어에 널리 적용됩니다. 일반적인 작업 스택은 다음과 같습니다: 주 작업은 균형/질량 중심(CoM) 제어, 보조 작업은 발끝 궤적 추적, 그 다음은 팔 조작, 최저 우선순위는 자세 최적화입니다. 역동역학 QP 형식화(Inverse-Dynamics QP Formulation)에서 이 목적 함수는 부유 기반 동역학 제약, 접촉력 제약과 함께 연립 해석되어 일반화 가속도, 접촉력, 관절 토크를 출력합니다.

이것이 실제로 바꾸는 것은 "제어기의 구조"가 아니라 "다중 작업 조정의 수학적 표현"입니다—연구자가 전체 제어 아키텍처를 다시 작성하지 않고도 동일한 QP 프레임워크 내에서 작업 가중치를 유연하게 조정할 수 있게 합니다.

### 한계와 경계

1. **가중치 보정의 어려움**: 가중치 \(w_i\)의 선택에는 체계적인 방법이 없으며, 일반적으로 수동 튜닝이나 경험적 공식에 의존합니다. 공학적 판단: 가중치 보정은 실제 배포에서 가장 시간이 많이 소요되는 단계 중 하나입니다.
2. **작업 절충 불가피**: 가중 방법은 본질적으로 유연한 제약이므로 높은 우선순위 작업이 절대적으로 충족될 수 없습니다. 안전 핵심 시나리오(예: 넘어짐 방지)에서는 이는 허용되지 않을 수 있습니다.
3. **모델 정밀도에 민감**: 목적 함수는 작업 Jacobian과 동역학 모델에 의존하므로 모델 오차는 작업 추적 성능 저하로 직접 이어집니다.
4. **정규화 항이 편향 도입**: 정규화는 수치적 안정성을 개선하지만 추가적인 최적화 편향을 도입하므로 안정성과 정밀도 사이의 균형이 필요합니다.

### 일반적인 오해

1. **"가중치가 클수록 좋다"**—틀림. 가중치가 과도하게 크면 수치적 병적 상태가 발생할 수 있고 모델 오차의 영향을 증폭시킬 수 있습니다. 가중치의 의미는 절대적 크기가 아니라 상대적 관계에 있습니다.
2. **"가중 방법이 엄격한 계층 구조를 완전히 대체할 수 있다"**—부정확. 가중 방법은 엄격한 계층 구조를 근사할 수 있을 뿐이며, 정확한 구현은 여전히 계층별 영공간 투영이 필요합니다. 원문 자료는 명확히 지적합니다: "엄격한 계층 구조는 극단적인 가중치 차이를 통해 근사할 수 있지만, 정확한 구현은 계층별 영공간 투영이 필요합니다."
3. **"정규화 항이 작을수록 좋다"**—틀림. 정규화 항의 역할은 특이점 부근에서 이득 폭주를 방지하는 것이며, 너무 작은 정규화 계수는 수치적 불안정을 초래합니다.

### 관련 지식

- `ent_formalism_inverse_dynamics_qp` — 역동역학 QP 형식화는 가중 작업 오차 목적 함수의 대표적 운반체이며, 둘은 일반화 가속도와 접촉력을 연립 해석합니다.
- `ent_operator_task_jacobian` — 작업 Jacobian은 관절 공간 운동을 작업 공간으로 매핑하며, 가중 작업 오차 목적 함수를 구성하는 핵심 연산자입니다.
- `ent_method_hierarchical_qp_wbc` — 계층적 QP WBC는 가중 방법의 엄격한 우선순위 대안으로, 영공간 투영을 통해 작업 간 절대적 분리를 구현합니다.
