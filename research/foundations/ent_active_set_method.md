---
$id: ent_active_set_method
$schema: ../../data/schema/v1/entry_schema.json
$version: 1
type: algorithm
names:
  en: Active-set method
  zh: 起作用集法
  ko: 활성 집합법
summary:
  en: An iterative optimization algorithm that tracks the set of constraints that are active at the solution and solves a
    sequence of equality-constrained subproblems.
  zh: '> 生活实例：想象拼拼图时先猜哪些块接触边框（起作用约束），只拼这些边框块，再检查内部是否有块凸出。不断交换边框块，直到整幅图完整。 > > 自然语言逻辑：起作用集法在每次迭代中假设哪些不等式约束取等号，然后在该工作集上求解等式约束子问题。若结果违反某个先前不起作用的约束，则将其加入工作集；若某起作用约束对应的乘子符号错误，则将其移除。重复直到满足
    KKT 条件。'
  ko: 최적해에서 활성(등호를 만족하는) 제약 집합을 추적하고 일련의 등식 제약 부분 문제를 풀어 최적해를 접근하는 반복 최적화 알고리즘.
domains:
- 00_foundations
layers:
- foundations
functional_roles:
- knowledge
theoretical_depth:
- method
tags:
- optimization
- constrained_optimization
- active_set
- qp
- inequality_constraints
verification:
  status: verified
  reviewed_by: human_and_ai
  reviewed_at: '2026-07-14'
  confidence: high
  notes: 'Body restructured into standard sections by scripts/restructure_entry_bodies.py.  [2026-08-12] body upgraded to
    textbook-grade (.staging/textbook_grade_run/b3): zh 概述/核心内容/参考 rewritten from card + graph neighbors + wiki chapters +
    first-hand sources (number whitelist audit passed); en/ko sections to be regenerated by translate pipeline.'
sources:
- id: src_nocedal_wright_2006
  type: other
  title: J. Nocedal and S. J. Wright, Numerical Optimization, 2nd ed., Springer, 2006
  url: https://doi.org/10.1007/978-0-387-40065-5
  date: '2006-01-01'
  accessed_at: '2026-06-25'
related_entities:
- id: ent_constraint_qualification
  relationship: requires
  description:
    en: Reliable convergence of active-set QP iterations usually assumes a constraint qualification such as LICQ on the working
      set.
    zh: 起作用集 QP 迭代的可靠收敛通常假设工作集上满足 LICQ 等约束规范。
    ko: 활성 집합 QP 반복의 신뢰할 수 있는 수렴은 일반적으로 작업 집합에서 LICQ와 같은 제약 자격을 가정합니다.
- id: ent_interior_point_method
  relationship: is_alternative_to
  description:
    en: Interior-point methods solve the same constrained problems by staying strictly inside the feasible region rather than
      tracking active sets.
    zh: 内点法通过始终严格位于可行域内部来求解同一类约束问题，而非追踪起作用集。
    ko: 내점법은 활성 집합을 추적하는 대신 실행 가능 영역 내에서 엄격하게 머무륾며 동일한 제약 문제를 풉니다.
---
## 概述

起作用集法（Active-set method）是一种求解不等式约束优化问题的迭代算法，其核心思想是在每次迭代中维护一个"工作集"（working set），即当前被当作等式约束处理的不等式约束集合，通过反复增删约束来逼近真正起作用的最优约束集合。它真正改变的不是求解单个子问题的效率，而是将"哪些约束该被激活"这一组合决策转化为一系列可高效求解的等式约束子问题，从而在中小规模凸二次规划（QP）中成为最可靠的选择之一。

## 核心内容

### 是什么：准确定义

起作用集法是一种求解带不等式约束优化问题的**可行点法**。它从某个可行点出发，在每一步迭代中，将当前"猜"为取等号的不等式约束与所有等式约束合并为**工作集** $\mathcal{W}_k$，然后在该工作集上求解一个等式约束的子问题来确定搜索方向。若子问题的最优解违反了工作集之外的某个约束，则将该约束加入工作集；若工作集中某个约束对应的 Lagrange 乘子为负（对最小化问题而言），则说明该约束不应被激活，将其移出工作集。如此反复，直到满足 Karush-Kuhn-Tucker（KKT）条件。

该方法特别适用于**凸二次规划**问题，因为此时每个子问题都有闭式解或可通过直接法高效求解，且算法在有限步内终止。

### 为什么存在：痛点与历史定位

在起作用集法出现之前，处理不等式约束的主流思路是**罚函数法**——将约束惩罚项并入目标函数，把约束问题转化为无约束问题。罚函数法实现简单，但存在两个根本缺陷：一是惩罚参数需要人工调节，过大导致 Hessian 病态，过小则解严重偏离可行域；二是无法精确满足约束，只能逼近。

起作用集法的历史定位在于：它**将组合问题与连续问题分离**。不等式约束的难点在于"哪些约束起作用"是一个组合决策——若有 $m$ 个不等式约束，理论上需要检查 $2^m$ 种激活组合。起作用集法通过逐步交换工作集，避免了穷举，使得每次迭代只需求解一个规模远小于原问题的等式约束子问题。这一思想在 20 世纪 80 年代被系统化，并成为 QP 求解器（如 qpOASES）的核心引擎。

### 原理拆解

**① 工作集：把不等式"假装"成等式**

考虑凸二次规划：

$$
\min_x \; \frac{1}{2} x^{\top} H x + g^{\top} x \quad \text{s.t.} \quad A x = b, \; C x \le d.
$$

设当前迭代点为 $x_k$，工作集 $\mathcal{W}_k$ 包含所有等式约束以及当前被激活（取等号）的不等式约束。在 $x_k$ 处，我们求解以下**等式约束子问题**来确定搜索方向 $p_k$：

$$
\min_p \; \frac{1}{2} p^{\top} H p + (H x_k + g)^{\top} p \quad \text{s.t.} \quad A p = 0, \; C_{\mathcal{W}_k} p = 0.
$$

注意子问题中约束的右侧被设为 0，因为 $x_k$ 已经满足工作集约束，我们只关心沿 $p$ 方向移动时这些约束是否保持成立。

**② 步长与约束交换：两个关键判定**

求解子问题得到 $p_k$ 后，需要确定步长 $\alpha_k$。若 $p_k = 0$，则 $x_k$ 是工作集子问题的最优解，此时检查工作集中不等式约束对应的 Lagrange 乘子 $\mu_j$：若所有 $\mu_j \ge 0$，则 $x_k$ 是原问题的最优解，算法终止；若存在 $\mu_j < 0$，则移除该约束（它不应对解产生"推力"），重新求解。

若 $p_k \neq 0$，则沿 $p_k$ 方向移动，步长上限由工作集之外的约束决定：

$$
\alpha_k = \min \left\{ 1, \; \min_{i \notin \mathcal{W}_k, \, c_i^{\top} p_k > 0} \frac{d_i - c_i^{\top} x_k}{c_i^{\top} p_k} \right\}.
$$

若 $\alpha_k < 1$，说明某个未加入工作集的约束被"撞上"，将其加入工作集，然后重新求解子问题。

**③ 有限终止性：凸 QP 的保证**

对于凸二次规划，起作用集法具有**有限终止性**：每次迭代要么使目标函数值严格下降，要么在保持目标值不变的情况下改变工作集（移除乘子为负的约束）。由于工作集的组合数有限，算法必然在有限步内到达 KKT 点。这一性质是内点法所不具备的——内点法只能渐近收敛。

### 关键参数与规格

| 参数/概念 | 含义 | 典型取值或性质 |
|-----------|------|----------------|
| 工作集 $\mathcal{W}_k$ | 当前被当作等式处理的约束集合 | 随迭代动态变化，大小通常远小于总约束数 |
| 搜索方向 $p_k$ | 在工作集约束下使目标下降的方向 | 由等式约束子问题求解得到 |
| 步长 $\alpha_k$ | 沿搜索方向移动的距离 | 取 1 与"撞到最近约束的距离"中的较小值 |
| Lagrange 乘子 $\mu_j$ | 工作集中不等式约束的对偶变量 | 用于判定约束是否应被移出工作集 |
| 终止条件 | 梯度在零空间投影为零 + 乘子非负 | 等价于 KKT 条件 |

### 横向对比

| 方法 | 迭代方式 | 约束处理 | 收敛速度 | 适用规模 | 特点 |
|------|----------|----------|----------|----------|------|
| 起作用集法 | 可行点序列 | 显式维护工作集 | 有限步终止（凸 QP） | 中小规模（数百变量） |  warm-start 友好，适合在线重复求解 |
| 内点法 | 不可行点序列 | 障碍函数 + 中心路径 | 超线性/二次收敛 | 大规模（数千至百万变量） | 对稀疏结构敏感，warm-start 困难 |
| 罚函数法 | 无约束序列 | 惩罚项逼近 | 线性收敛 | 任意规模 | 参数敏感，约束只能近似满足 |
| 梯度投影法 | 可行点序列 | 投影到可行域 | 次线性/线性 | 大规模 | 实现简单，但收敛慢 |

**工程判断**：在人形机器人全身控制（WBC）中，QP 问题通常规模不大（决策变量数十到数百），但需要在 1 kHz 甚至更高频率下实时求解。起作用集法因其**warm-start 特性**——上一控制周期的解可作为当前周期的初始工作集——成为 qpOASES 等实时求解器的首选。内点法（如 OSQP、ProxQP）则更适合离线规划或对精度要求极高的场景。

### 谁在用·应用案例

**qpOASES** 是起作用集法最著名的工程实现之一，其设计目标就是模型预测控制（MPC）中的参数化 QP 求解。在人形机器人领域，它被广泛用于：

- **全身运动控制（WBC）**：在满足动力学等式、摩擦锥不等式、关节力矩限制的前提下，实时求解关节加速度与接触力。语料中提到的"低层用 QP 或 MPC 实时跟踪"正是这一场景。
- **模型预测控制（MPC）**：每一步滚动优化中，QP 的结构（Hessian 与约束矩阵）变化很小，起作用集法可以利用上一时刻的工作集作为初始猜测，大幅减少迭代次数。
- **轨迹优化**：在分层规划的中层，用 QP 求解局部轨迹修正问题。

### 局限与边界

1. **组合爆炸风险**：虽然避免了穷举，但在最坏情况下，工作集的交换次数仍可能随约束数量指数增长。对于约束数量极大的问题（如 $10^4$ 以上），内点法通常更优。
2. **仅适用于凸问题**：对于非凸 QP，起作用集法只能保证找到局部最优解，且有限终止性不再成立。
3. **需要初始可行点**：算法要求从可行点出发。若初始点不可行，需要先运行 Phase I 过程（求解一个辅助 LP 或 QP 来找到可行点），这增加了实现复杂度。
4. **数值稳定性**：当工作集约束接近线性相关时，子问题的 KKT 矩阵可能病态，需要正则化或采用更稳健的分解方法。

### 常见误区

1. **"起作用集法就是单纯形法"**——两者共享"沿约束边界移动"的几何直觉，但单纯形法针对线性规划，起作用集法针对（通常凸的）非线性规划，且每次迭代的搜索方向由目标函数的二阶信息（Hessian）决定，而非单纯沿边。
2. **"工作集越大越好"**——工作集过大意味着子问题规模增大，计算量上升；关键在于工作集是否**准确**，而非大小。一个包含多余约束的工作集可能导致错误的搜索方向。
3. **"乘子为负就一定要移除"**——这仅在当前点是最优解（$p_k = 0$）时成立。若 $p_k \neq 0$，即使存在负乘子，也应先沿搜索方向移动，而非立即调整工作集。
4. **"起作用集法比内点法快"**——这是**问题规模依赖**的判断。在中小规模、需要 warm-start 的场景下成立；但在大规模、稀疏问题上，内点法通常更快。

### 相关知识

- `ent_kkt_conditions` — 起作用集法的终止条件正是 KKT 条件的逐条验证：平稳性、原始可行性、对偶可行性、互补松弛。
- `ent_constraint_qualification` — 保证 KKT 条件在最优解处成立的前提条件，起作用集法的收敛性分析同样依赖这些正则性条件。
- `ent_interior_point_method` — 与起作用集法并列的另一大类约束优化算法，两者在收敛速度、适用规模与 warm-start 能力上形成互补。
- `ent_paper_baek_toward_control_of_wheeled_huma_2024` — 该论文中的平衡点预测框架涉及实时参数估计与优化，起作用集法可作为其底层 QP 求解的候选算法。
- `ent_paper_sartore_optimization_of_humanoid_robot_2022` — 人形机器人设计优化中的多目标问题常需求解带约束的子问题，起作用集法可用于其中凸 QP 子问题的快速求解。

## 参考

- [Nocedal & Wright, *Numerical Optimization*, Springer, 2006](https://doi.org/10.1007/978-0-387-40065-5)
- [人形机器人全流程开发工作流文档](https://github.com/YansongW/awesome-humanoid-robot/tree/main/docs/humanoid_full_development_workflow_v3.md)
- [人形机器人知识库第 8 章：设计与优化](https://github.com/YansongW/awesome-humanoid-robot/tree/main/wiki/docs/chapters/chapter-08.md)

## Overview

The active-set method is an iterative algorithm for solving optimization problems with inequality constraints. Its core idea is to maintain a "working set" during each iteration—the set of inequality constraints currently treated as equality constraints—and to approximate the truly active optimal constraint set by repeatedly adding and removing constraints. What it genuinely changes is not the efficiency of solving a single subproblem, but rather the transformation of the combinatorial decision of "which constraints should be active" into a sequence of efficiently solvable equality-constrained subproblems, making it one of the most reliable choices for small- to medium-scale convex quadratic programming (QP).

## Content

### What It Is: Precise Definition

The active-set method is a **feasible-point method** for solving optimization problems with inequality constraints. Starting from a feasible point, at each iteration it merges the inequality constraints currently "guessed" to be active (at equality) with all equality constraints into a **working set** $\mathcal{W}_k$, then solves an equality-constrained subproblem on this working set to determine the search direction. If the subproblem's optimal solution violates a constraint outside the working set, that constraint is added to the working set; if a Lagrange multiplier corresponding to a constraint in the working set is negative (for a minimization problem), it indicates that the constraint should not be active, and it is removed from the working set. This process repeats until the Karush-Kuhn-Tucker (KKT) conditions are satisfied.

The method is particularly well-suited for **convex quadratic programming** problems, because each subproblem then has a closed-form solution or can be solved efficiently via direct methods, and the algorithm terminates in a finite number of steps.

### Why It Exists: Pain Points and Historical Positioning

Before the active-set method emerged, the dominant approach for handling inequality constraints was the **penalty method**—incorporating constraint penalty terms into the objective function to convert the constrained problem into an unconstrained one. The penalty method is simple to implement but has two fundamental flaws: first, the penalty parameter requires manual tuning—too large leads to an ill-conditioned Hessian, while too small causes the solution to deviate significantly from the feasible region; second, constraints cannot be satisfied exactly, only approximately.

The historical significance of the active-set method lies in its **separation of the combinatorial problem from the continuous problem**. The difficulty of inequality constraints is that "which constraints are active" is a combinatorial decision—with $m$ inequality constraints, one would theoretically need to check $2^m$ active-set combinations. The active-set method avoids exhaustive enumeration by gradually exchanging the working set, so that each iteration only requires solving an equality-constrained subproblem whose size is far smaller than the original problem. This idea was systematized in the 1980s and became the core engine of QP solvers such as qpOASES.

### Principle Breakdown

**① Working Set: "Pretending" Inequalities Are Equalities**

Consider the convex quadratic program:

$$
\min_x \; \frac{1}{2} x^{\top} H x + g^{\top} x \quad \text{s.t.} \quad A x = b, \; C x \le d.
$$

Let the current iterate be $x_k$, and let the working set $\mathcal{W}_k$ contain all equality constraints plus the inequality constraints currently active (at equality) at $x_k$. At $x_k$, we solve the following **equality-constrained subproblem** to determine the search direction $p_k$:

$$
\min_p \; \frac{1}{2} p^{\top} H p + (H x_k + g)^{\top} p \quad \text{s.t.} \quad A p = 0, \; C_{\mathcal{W}_k} p = 0.
$$

Note that the right-hand sides of the constraints in the subproblem are set to 0, because $x_k$ already satisfies the working-set constraints, and we only care about whether these constraints remain satisfied when moving along the direction $p$.

**② Step Size and Constraint Exchange: Two Key Decisions**

After solving the subproblem to obtain $p_k$, we need to determine the step size $\alpha_k$. If $p_k = 0$, then $x_k$ is optimal for the working-set subproblem; at this point, we check the Lagrange multipliers $\mu_j$ corresponding to the inequality constraints in the working set: if all $\mu_j \ge 0$, then $x_k$ is optimal for the original problem and the algorithm terminates; if some $\mu_j < 0$, we remove that constraint (it should not exert a "push" on the solution) and re-solve.

If $p_k \neq 0$, we move along the direction $p_k$, with the step-size upper bound determined by constraints outside the working set:

$$
\alpha_k = \min \left\{ 1, \; \min_{i \notin \mathcal{W}_k, \, c_i^{\top} p_k > 0} \frac{d_i - c_i^{\top} x_k}{c_i^{\top} p_k} \right\}.
$$

If $\alpha_k < 1$, a constraint not yet in the working set has been "hit"; we add it to the working set and re-solve the subproblem.

**③ Finite Termination: Guarantee for Convex QP**

For convex quadratic programming, the active-set method enjoys **finite termination**: each iteration either strictly decreases the objective function value or changes the working set while keeping the objective value unchanged (by removing a constraint with a negative multiplier). Since the number of possible working sets is finite, the algorithm necessarily reaches a KKT point in a finite number of steps. This property is not shared by interior-point methods, which only converge asymptotically.

### Key Parameters and Specifications

| Parameter/Concept | Meaning | Typical Value or Property |
|-------------------|---------|---------------------------|
| Working set $\mathcal{W}_k$ | The set of constraints currently treated as equalities | Changes dynamically across iterations; its size is usually far smaller than the total number of constraints |
| Search direction $p_k$ | The direction that decreases the objective under working-set constraints | Obtained by solving the equality-constrained subproblem |
| Step size $\alpha_k$ | The distance moved along the search direction | The smaller of 1 and the "distance to the nearest constraint hit" |
| Lagrange multiplier $\mu_j$ | Dual variable for an inequality constraint in the working set | Used to decide whether a constraint should be removed from the working set |
| Termination condition | Gradient projection onto the null space is zero + nonnegative multipliers | Equivalent to the KKT conditions |

### Horizontal Comparison

| Method | Iteration Mode | Constraint Handling | Convergence Rate | Applicable Scale | Characteristics |
|--------|----------------|---------------------|------------------|------------------|-----------------|
| Active-set method | Sequence of feasible points | Explicitly maintains a working set | Finite termination (convex QP) | Small to medium scale (hundreds of variables) | Warm-start friendly; suitable for online repeated solving |
| Interior-point method | Sequence of infeasible points | Barrier function + central path | Superlinear/quadratic convergence | Large scale (thousands to millions of variables) | Sensitive to sparsity structure; warm-start is difficult |
| Penalty method | Sequence of unconstrained problems | Penalty terms approximate constraints | Linear convergence | Any scale | Parameter-sensitive; constraints only approximately satisfied |
| Gradient projection method | Sequence of feasible points | Projects onto the feasible region | Sublinear/linear convergence | Large scale | Simple to implement, but slow convergence |

**Engineering Judgment**: In humanoid robot whole-body control (WBC), QP problems are typically small in scale (tens to hundreds of decision variables) but must be solved in real time at 1 kHz or even higher frequencies. The active-set method, thanks to its **warm-start capability**—the solution from the previous control cycle can serve as the initial working set for the current cycle—has become the preferred choice for real-time solvers such as qpOASES. Interior-point methods (e.g., OSQP, ProxQP) are better suited for offline planning or scenarios requiring extremely high accuracy.

### Who Uses It: Application Cases

**qpOASES** is one of the most famous engineering implementations of the active-set method, designed specifically for parametric QP solving in model predictive control (MPC). In the humanoid robotics domain, it is widely used for:

- **Whole-body motion control (WBC)**: Solving joint accelerations and contact forces in real time while satisfying dynamic equalities, friction-cone inequalities, and joint torque limits. The "low-level QP or MPC real-time tracking" mentioned in the corpus is precisely this scenario.
- **Model predictive control (MPC)**: In each receding-horizon optimization step, the QP structure (Hessian and constraint matrices) changes only slightly; the active-set method can leverage the working set from the previous time step as an initial guess, significantly reducing the number of iterations.
- **Trajectory optimization**: In the middle layer of hierarchical planning, QPs are used to solve local trajectory correction problems.

### Limitations and Boundaries

1. **Risk of combinatorial explosion**: Although exhaustive enumeration is avoided, in the worst case the number of working-set exchanges can still grow exponentially with the number of constraints. For problems with extremely many constraints (e.g., more than $10^4$), interior-point methods are usually preferable.
2. **Applicable only to convex problems**: For nonconvex QPs, the active-set method can only guarantee a locally optimal solution, and finite termination no longer holds.
3. **Requires an initial feasible point**: The algorithm requires starting from a feasible point. If the initial point is infeasible, a Phase I procedure (solving an auxiliary LP or QP to find a feasible point) must be run first, which adds implementation complexity.
4. **Numerical stability**: When the working-set constraints are nearly linearly dependent, the KKT matrix of the subproblem may become ill-conditioned, requiring regularization or more robust factorization methods.

### Common Misconceptions

1. **"The active-set method is just the simplex method"**—Both share the geometric intuition of "moving along constraint boundaries," but the simplex method targets linear programming, whereas the active-set method targets (typically convex) nonlinear programming, and the search direction at each iteration is determined by second-order information (the Hessian) of the objective, rather than simply moving along edges.
2. **"A larger working set is better"**—An overly large working set increases the subproblem size and computational cost; what matters is whether the working set is **accurate**, not its size. A working set containing redundant constraints can lead to an incorrect search direction.
3. **"A negative multiplier must always be removed"**—This holds only when the current point is optimal ($p_k = 0$). If $p_k \neq 0$, one should first move along the search direction even if negative multipliers exist, rather than immediately adjusting the working set.
4. **"The active-set method is faster than interior-point methods"**—This is a **problem-size-dependent** judgment. It holds in small- to medium-scale scenarios requiring warm-start; however, on large-scale sparse problems, interior-point methods are typically faster.

### Related Knowledge

- `ent_kkt_conditions` — The termination condition of the active-set method is precisely a point-by-point verification of the KKT conditions: stationarity, primal feasibility, dual feasibility, and complementary slackness.
- `ent_constraint_qualification` — Prerequisites ensuring that the KKT conditions hold at the optimal solution; the convergence analysis of the active-set method likewise relies on these regularity conditions.
- `ent_interior_point_method` — Another major class of constrained optimization algorithms alongside the active-set method; the two complement each other in convergence speed, applicable scale, and warm-start capability.
- `ent_paper_baek_toward_control_of_wheeled_huma_2024` — The balance-point prediction framework in this paper involves real-time parameter estimation and optimization; the active-set method can serve as a candidate algorithm for its underlying QP solving.
- `ent_paper_sartore_optimization_of_humanoid_robot_2022` — Multi-objective problems in humanoid robot design optimization often require solving constrained subproblems; the active-set method can be used for fast solving of the convex QP subproblems involved.

## 개요

활성 집합법(Active-set method)은 부등식 제약이 있는 최적화 문제를 푸는 반복 알고리즘으로, 핵심 아이디어는 각 반복에서 "작업 집합"(working set), 즉 현재 등식 제약으로 취급되는 부등식 제약 집합을 유지하면서 제약을 반복적으로 추가·제거하여 실제로 활성화되는 최적 제약 집합에 접근하는 것입니다. 이 방법이 실제로 바꾸는 것은 단일 하위 문제를 푸는 효율성이 아니라, "어떤 제약이 활성화되어야 하는가"라는 조합적 결정을 일련의 효율적으로 풀 수 있는 등식 제약 하위 문제로 변환한다는 점이며, 이로 인해 중소규모 볼록 2차 계획법(QP)에서 가장 신뢰할 수 있는 선택지 중 하나가 되었습니다.

## 핵심 내용

### 무엇인가: 정확한 정의

활성 집합법은 부등식 제약이 있는 최적화 문제를 푸는 **실현 가능점 방법**입니다. 어떤 실현 가능점에서 출발하여 각 반복에서 현재 "등식이 성립한다"고 추정되는 부등식 제약을 모든 등식 제약과 합쳐 **작업 집합** $\mathcal{W}_k$을 구성한 뒤, 해당 작업 집합에서 등식 제약 하위 문제를 풀어 탐색 방향을 결정합니다. 하위 문제의 최적해가 작업 집합 외부의 어떤 제약을 위반하면 해당 제약을 작업 집합에 추가하고, 작업 집합 내 어떤 제약에 대응하는 라그랑주 승수(Lagrange multiplier)가 음수(최소화 문제의 경우)이면 해당 제약이 활성화되지 않아야 함을 의미하므로 작업 집합에서 제거합니다. 이러한 과정을 Karush-Kuhn-Tucker(KKT) 조건이 만족될 때까지 반복합니다.

이 방법은 특히 **볼록 2차 계획법** 문제에 적합한데, 이 경우 각 하위 문제가 닫힌 형태의 해를 가지거나 직접법으로 효율적으로 풀 수 있고, 알고리즘이 유한 단계 내에 종료되기 때문입니다.

### 왜 존재하는가:痛点과 역사적 위치

활성 집합법이 등장하기 전에는 부등식 제약을 다루는 주류 접근법이 **벌점 함수법**이었습니다—제약 벌점 항을 목적 함수에 통합하여 제약 문제를 무제약 문제로 변환하는 방식입니다. 벌점 함수법은 구현이 간단하지만 두 가지 근본적인 결함이 있었습니다: 첫째, 벌점 매개변수를 수동으로 조정해야 하는데, 너무 크면 Hessian이 병적(ill-conditioned)이 되고 너무 작으면 해가 실현 가능 영역에서 크게 벗어납니다; 둘째, 제약을 정확히 만족시킬 수 없고 근사만 가능합니다.

활성 집합법의 역사적 위치는 **조합 문제와 연속 문제를 분리**한 데 있습니다. 부등식 제약의 어려움은 "어떤 제약이 활성화되는가"가 조합적 결정이라는 점입니다—$m$개의 부등식 제약이 있으면 이론적으로 $2^m$개의 활성화 조합을 검사해야 합니다. 활성 집합법은 작업 집합을 점진적으로 교체함으로써 완전 탐색을 피하고, 각 반복에서 원래 문제보다 훨씬 작은 등식 제약 하위 문제만 풀면 되게 합니다. 이 아이디어는 20세기 80년대에 체계화되었고, QP 솔버(예: qpOASES)의 핵심 엔진이 되었습니다.

### 원리 분해

**① 작업 집합: 부등식을 "등식"으로 가장하기**

볼록 2차 계획법을 고려합니다:

$$
\min_x \; \frac{1}{2} x^{\top} H x + g^{\top} x \quad \text{s.t.} \quad A x = b, \; C x \le d.
$$

현재 반복점을 $x_k$라고 하고, 작업 집합 $\mathcal{W}_k$은 모든 등식 제약과 현재 활성화된(등식이 성립하는) 부등식 제약을 포함합니다. $x_k$에서 다음 **등식 제약 하위 문제**를 풀어 탐색 방향 $p_k$를 결정합니다:

$$
\min_p \; \frac{1}{2} p^{\top} H p + (H x_k + g)^{\top} p \quad \text{s.t.} \quad A p = 0, \; C_{\mathcal{W}_k} p = 0.
$$

하위 문제에서 제약의 우변이 0으로 설정된 이유는 $x_k$가 이미 작업 집합 제약을 만족하므로, $p$ 방향으로 이동할 때 이러한 제약이 계속 성립하는지에만 관심이 있기 때문입니다.

**② 보폭과 제약 교환: 두 가지 핵심 판정**

하위 문제를 풀어 $p_k$를 얻은 후, 보폭 $\alpha_k$를 결정해야 합니다. $p_k = 0$이면 $x_k$는 작업 집합 하위 문제의 최적해이며, 이때 작업 집합 내 부등식 제약에 대응하는 라그랑주 승수 $\mu_j$를 검사합니다: 모든 $\mu_j \ge 0$이면 $x_k$는 원래 문제의 최적해이므로 알고리즘이 종료됩니다; $\mu_j < 0$인 것이 있으면 해당 제약을 제거하고(해에 "추진력"을 주지 않아야 함) 다시 풉니다.

$p_k \neq 0$이면 $p_k$ 방향으로 이동하며, 보폭의 상한은 작업 집합 외부의 제약에 의해 결정됩니다:

$$
\alpha_k = \min \left\{ 1, \; \min_{i \notin \mathcal{W}_k, \, c_i^{\top} p_k > 0} \frac{d_i - c_i^{\top} x_k}{c_i^{\top} p_k} \right\}.
$$

$\alpha_k < 1$이면 작업 집합에 포함되지 않은 어떤 제약에 "충돌"했음을 의미하므로, 해당 제약을 작업 집합에 추가한 뒤 하위 문제를 다시 풉니다.

**③ 유한 종료성: 볼록 QP의 보장**

볼록 2차 계획법의 경우, 활성 집합법은 **유한 종료성**을 가집니다: 각 반복에서 목적 함수 값이 엄격히 감소하거나, 목적 값이 유지된 채 작업 집합이 변경됩니다(음수 승수를 가진 제약 제거). 작업 집합의 조합 수가 유한하므로 알고리즘은 반드시 유한 단계 내에 KKT 점에 도달합니다. 이 성질은 내점법이 가지지 못한 것입니다—내점법은 점근적으로만 수렴합니다.

### 핵심 매개변수와 사양

| 매개변수/개념 | 의미 | 일반적인 값 또는 성질 |
|-----------|------|----------------|
| 작업 집합 $\mathcal{W}_k$ | 현재 등식으로 처리되는 제약 집합 | 반복에 따라 동적으로 변화하며, 크기는 일반적으로 전체 제약 수보다 훨씬 작음 |
| 탐색 방향 $p_k$ | 작업 집합 제약 하에서 목적을 감소시키는 방향 | 등식 제약 하위 문제를 풀어 얻음 |
| 보폭 $\alpha_k$ | 탐색 방향으로 이동하는 거리 | 1과 "가장 가까운 제약까지의 거리" 중 작은 값 |
| 라그랑주 승수 $\mu_j$ | 작업 집합 내 부등식 제약의 쌍대 변수 | 제약이 작업 집합에서 제거되어야 하는지 판정하는 데 사용 |
| 종료 조건 | 영공간에서의 기울기 투영이 0 + 승수 비음수 | KKT 조건과 동일 |

### 횡적 비교

| 방법 | 반복 방식 | 제약 처리 | 수렴 속도 | 적용 규모 | 특징 |
|------|----------|----------|----------|----------|------|
| 활성 집합법 | 실현 가능점 수열 | 작업 집합을 명시적으로 유지 | 유한 단계 종료(볼록 QP) | 중소규모(수백 변수) | warm-start에 유리, 온라인 반복 해석에 적합 |
| 내점법 | 비실현 가능점 수열 | 장벽 함수 + 중심 경로 | 초선형/2차 수렴 | 대규모(수천~수백만 변수) | 희소 구조에 민감, warm-start 어려움 |
| 벌점 함수법 | 무제약 수열 | 벌점 항으로 근사 | 선형 수렴 | 임의 규모 | 매개변수 민감, 제약은 근사적으로만 만족 |
| 기울기 투영법 | 실현 가능점 수열 | 실현 가능 영역으로 투영 | 준선형/선형 | 대규모 | 구현 간단하지만 수렴이 느림 |

**공학적 판단**: 휴머노이드 로봇 전신 제어(WBC)에서 QP 문제는 일반적으로 규모가 크지 않지만(결정 변수 수십~수백 개), 1 kHz 이상의 주파수에서 실시간으로 풀어야 합니다. 활성 집합법은 **warm-start 특성**—이전 제어 주기의 해를 현재 주기의 초기 작업 집합으로 사용할 수 있음—덕분에 qpOASES와 같은 실시간 솔버의首选이 되었습니다. 내점법(OSQP, ProxQP 등)은 오프라인 계획이나 정밀도 요구가 매우 높은 시나리오에 더 적합합니다.

### 누가 사용하는가·응용 사례

**qpOASES**는 활성 집합법의 가장 유명한 공학적 구현 중 하나로, 설계 목표 자체가 모델 예측 제어(MPC)에서의 매개변수화된 QP 해석입니다. 휴머노이드 로봇 분야에서 널리 사용되는 곳은 다음과 같습니다:

- **전신 운동 제어(WBC)**: 동역학 등식, 마찰 원뿔 부등식, 관절 토크 제한을 만족하면서 관절 가속도와 접촉력을 실시간으로 해석합니다. 말뭉치에서 언급된 "저수준에서 QP 또는 MPC로 실시간 추적"이 바로 이 시나리오입니다.
- **모델 예측 제어(MPC)**: 각 단계의 롤링 최적화에서 QP의 구조(Hessian과 제약 행렬) 변화가 작으므로, 활성 집합법은 이전 시점의 작업 집합을 초기 추측으로 사용하여 반복 횟수를 크게 줄일 수 있습니다.
- **궤적 최적화**: 계층적 계획의 중간 계층에서 QP로 국소 궤적 수정 문제를 해석합니다.

### 한계와 경계

1. **조합 폭발 위험**: 완전 탐색은 피하지만, 최악의 경우 작업 집합 교환 횟수가 제약 수에 따라 지수적으로 증가할 수 있습니다. 제약 수가 매우 많은 문제(예: $10^4$ 이상)에서는 일반적으로 내점법이 더 우수합니다.
2. **볼록 문제에만 적용 가능**: 비볼록 QP의 경우 활성 집합법은 국소 최적해만 보장할 수 있고, 유한 종료성도 더 이상 성립하지 않습니다.
3. **초기 실현 가능점 필요**: 알고리즘은 실현 가능점에서 출발해야 합니다. 초기 점이 실현 불가능하면 먼저 Phase I 과정(보조 LP 또는 QP를 풀어 실현 가능점을 찾는 과정)을 실행해야 하므로 구현 복잡도가 증가합니다.
4. **수치 안정성**: 작업 집합 제약이 선형 종속에 가까우면 하위 문제의 KKT 행렬이 병적이 될 수 있으며, 정규화나 더 견고한 분해 방법이 필요합니다.

### 흔한 오해

1. **"활성 집합법은 단체법(simplex method)이다"**—둘 다 "제약 경계를 따라 이동"하는 기하학적 직관을 공유하지만, 단체법은 선형 계획법을 대상으로 하고 활성 집합법은 (일반적으로 볼록한) 비선형 계획법을 대상으로 하며, 각 반복의 탐색 방향은 단순히 경계를 따르는 것이 아니라 목적 함수의 2차 정보(Hessian)에 의해 결정됩니다.
2. **"작업 집합이 클수록 좋다"**—작업 집합이 너무 크면 하위 문제 규모가 커져 계산량이 증가합니다. 핵심은 작업 집합의 **정확성**이지 크기가 아닙니다. 불필요한 제약이 포함된 작업 집합은 잘못된 탐색 방향을 초래할 수 있습니다.
3. **"승수가 음수이면 반드시 제거해야 한다"**—이것은 현재 점이 최적해($p_k = 0$)일 때만 성립합니다. $p_k \neq 0$이면 음수 승수가 존재하더라도 먼저 탐색 방향으로 이동해야 하며, 즉시 작업 집합을 조정해서는 안 됩니다.
4. **"활성 집합법이 내점법보다 빠르다"**—이것은 **문제 규모에 의존적인** 판단입니다. 중소규모이고 warm-start가 필요한 시나리오에서는 성립하지만, 대규모 희소 문제에서는 일반적으로 내점법이 더 빠릅니다.

### 관련 지식

- `ent_kkt_conditions` — 활성 집합법의 종료 조건은 KKT 조건의 항목별 검증입니다: 정상성, 원시 실현 가능성, 쌍대 실현 가능성, 상보 여유.
- `ent_constraint_qualification` — 최적해에서 KKT 조건이 성립하기 위한 전제 조건으로, 활성 집합법의 수렴성 분석도 이러한 정규성 조건에 의존합니다.
- `ent_interior_point_method` — 활성 집합법과 나란히 놓이는 또 다른 대규모 제약 최적화 알고리즘으로, 수렴 속도, 적용 규모, warm-start 능력에서 상호 보완적입니다.
- `ent_paper_baek_toward_control_of_wheeled_huma_2024` — 이 논문의 균형점 예측 프레임워크는 실시간 매개변수 추정과 최적화를 포함하며, 활성 집합법은 그 하위 QP 해석의 후보 알고리즘이 될 수 있습니다.
- `ent_paper_sartore_optimization_of_humanoid_robot_2022` — 휴머노이드 로봇 설계 최적화의 다중 목적 문제는 종종 제약이 있는 하위 문제를 풀어야 하며, 활성 집합법은 그중 볼록 QP 하위 문제의 빠른 해석에 사용될 수 있습니다.
